# Bloco do Caddy para a instancia ARGENTINA do piloto (Bloco E / BLK-INTL-08).
#
# TEMPLATE versionado: o Caddyfile REAL e' gitignored (backup cifrado em
# secrets/Caddyfile.enc) e vive na VPS em /opt/motor-expansao/app/Caddyfile.
# Aplicacao (cada comando na VPS com aprovacao do Felipe, comando a comando —
# CLAUDE.md §6):
#   1. Anexar este bloco ao Caddyfile da VPS (ao lado do bloco de
#      piloto.ultra-expansao.tech — docs/deploy_piloto_web.md §3).
#   2. A regra do Authelia para piloto-ar.ultra-expansao.tech JA EXISTE (Bloco D,
#      aplicado na VPS em 2026-09-03; backups no PR #308): subject group:expansao_ar,
#      e as regras BR ja tem group:expansao_br. Resta so conceder o grupo
#      expansao_ar aos usuarios AR no users_database.yml.
#   3. Recarregar sem downtime:
#      docker compose -f docker-compose.prod.yml exec caddy \
#          caddy reload --config /etc/caddy/Caddyfile
#   4. Atualizar o backup cifrado (sops secrets/Caddyfile.enc).
#
# Espelha o bloco BR (docs/deploy_piloto_web.md §3) com UMA diferenca deliberada:
# o BR usa `reverse_proxy web:8899` (alias de SERVICO, valido porque Caddy e `web`
# estao no MESMO compose). O alias de servico NAO cruza projects diferentes — o
# `web_ar` mora no docker-compose.ar.yml (project motor-expansao-ar) — entao aqui
# o alvo e' o container_name, que e' resolvido na rede compartilhada app_app_net.

# ─────────────────────────────────────────────────────────────────────────────────
# A AR FICA NO AUTHELIA, e isto e' DECISAO (Vinicius, 25/09/2026), nao esquecimento.
#
# O corte do P19 (DEC-067, D4) troca o alvo do `forward_auth` do Authelia para o nosso
# `/api/verify` — mas SO' NO BR. O motivo e' medido e e' de dependencia: a sessao propria
# vive em TABELA (D1/migration 018), e o `web_ar` NAO TEM BANCO — `docker-compose.ar.yml`
# lista seis variaveis de ambiente e nenhuma e' `MOTOR_DATABASE_URL`. Sem banco nao ha'
# tabela de sessao, logo o motor nao teria como autenticar ninguem deste lado.
#
# Apontar este bloco para o `web_ar` derrubaria a AR INTEIRA: a rota responderia 404 (a
# chave desligada) ou 503 (sem banco), e o Caddy nega tudo que nao for 2xx — inclusive
# para quem ja' estava dentro.
#
# CONSEQUENCIA QUE ISTO CRIA, e que precisa continuar verdadeira enquanto durar: o
# servico `authelia` do `docker-compose.prod.yml` NAO pode ser removido no corte, e o
# `users_database.yml` precisa seguir com as pessoas da AR. Passam a existir DOIS lugares
# guardando quem entra — o banco (BR) e o arquivo do Authelia (AR) — e isso e' divida
# declarada, nao desenho: quem sai da empresa tem de sair dos dois.
#
# Para a AR acompanhar o corte um dia, o pre-requisito e' o banco. Enquanto ele nao
# existir, este bloco nao muda.
# ─────────────────────────────────────────────────────────────────────────────────

piloto-ar.ultra-expansao.tech {
    forward_auth authelia:9091 {
        uri /api/verify?rd=https://auth.ultra-expansao.tech
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
    reverse_proxy motor_expansao_web_ar:8899
    # Access log de edge do host AR (DEC-027 — metade da trilha: IP real, user-agent,
    # latencia no Caddy; a outra metade e' o JSONL do backend). O bloco BR real tem
    # diretiva `log` equivalente (o Caddyfile real e' gitignored): CONFERIR o formato
    # contra o bloco BR na hora de aplicar. /var/log/caddy e' o mount de
    # /opt/motor-expansao/logs/caddy (docker-compose.prod.yml:297).
    log {
        output file /var/log/caddy/piloto-ar-access.log
    }
}
