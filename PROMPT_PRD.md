Leia primeiro `CLAUDE.md`, `README.md`  e depois `PRD.md`.

Sua tarefa e executar apenas o proximo bloco pendente do `PRD.md`, respeitando integralmente as instrucoes dos dois arquivos.

Regras obrigatorias:
- execute no maximo 1 bloco por vez;
- nao avance para o bloco seguinte, mesmo se sobrar tempo;
- ao concluir o bloco, atualize os checkboxes e observacoes no `PRD.md`;
- atualize tambem o `CLAUDE.md` se alguma regra, decisao, semantica de coluna ou fluxo tiver mudado;
- tente manter `CLAUDE.md` e `PRD.md` com no maximo 200 linhas;
- valide minimamente o que alterar;
- ao final, encerre a tarefa com um resumo curto do que foi feito, pendencias e proximos passos.

Se houver ambiguidade entre codigo e documentacao, corrija primeiro a documentacao e depois o codigo.

Ao tratar classificacao de risco, considere quartis apenas como recurso auxiliar de ranking relativo. Para reporte executivo e sizing de carteira, priorize regua absoluta, churn esperado, receita esperada perdida e corte por capacidade operacional.

