# Fingerprints de falsos positivos por linha (formato <file>:<rule>:<line>).
#
# VAZIO por design (BLK-SEC-02): fingerprints linha-a-linha quebram a cada
# refatoracao/insercao de linha (o repo migrou jobs/ -> src/ e as linhas andaram,
# ressurgindo os FPs no gate). Falsos positivos de CONTEUDO estaveis (nomes de
# coluna/arquivo) agora ficam em [allowlist].regexes do .gitleaks.toml, robustos a
# drift. Adicionar aqui SO um FP genuinamente unico a uma linha que nao da para
# casar por conteudo; renovar o numero da linha se ela mover.
