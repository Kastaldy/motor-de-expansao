# Contrato: Relatorio Pontual Censitario 1 km

> **DEC-021 (2026-07-29, autorizada por Felipe): o raio passou de 1,5 km para 1,0 km.**
> Este documento foi atualizado para o raio vigente. O metodo de intersecao continua o
> mesmo (ponderacao por area de intersecao setor x circulo, calculada em runtime); mudou
> o raio, o rotulo do metodo (`setor_censitario_intersecao_area_1km`) e as metas
> ABSOLUTAS dos Big Numbers, reescaladas pela area. O enquadramento de 5 km dos paineis
> de hexagono (DEC-011) NAO mudou — onde este doc diz 5 km, segue 5 km.

> Contrato tecnico do ciclo `Relatorio Pontual Censitario 1 km`.
> Versao: 2026-08-12 (atualizada no BLK-CONC-ESTUDO; PDF de 7 paginas base, Conclusao CONDICIONAL — +1 pagina no caminho API/bot —, gerador UNIFICADO na estetica classica).

## 1. Objetivo

Criar uma analise pontual complementar ao H3 atual, usando geometria real de setores censitarios IBGE 2022 e intersecao geometrica com um circulo de raio fixo `1.0 km` ao redor de uma coordenada.

Esta feature vive no motor compartilhado (`src/motor_expansao/dashboard/`), com carregamento lazy/cacheado de artefatos locais otimizados, e hoje e servida pelo piloto web, pela API GeoEspacial e pelo bot Telegram (o dashboard Streamlit, superficie original da feature, foi aposentado pela DEC-022, 2026-08-03). O motor nao depende de internet em producao e nao substitui a Analise Pontual H3 nem qualquer decisao do M1 oficial.

## 2. Entradas inventariadas

### Malha de setores

| item | valor |
| --- | --- |
| Arquivo | `data/raw/CENSO 2022/BR_setores_CD2022/BR_setores_CD2022.shp` |
| Componentes presentes | `.shp`, `.dbf`, `.shx`, `.prj`, `.cpg` |
| Tamanho principal | `.shp` ~1.25 GB; `.dbf` ~988 MB |
| Features | 468.099 setores |
| CRS | `EPSG:4674` |
| Geometria amostrada | `Polygon` |

Colunas confirmadas na malha: `CD_SETOR`, `SITUACAO`, `CD_SIT`, `CD_TIPO`, `AREA_KM2`, `CD_REGIAO`, `NM_REGIAO`, `CD_UF`, `NM_UF`, `CD_MUN`, `NM_MUN`, `CD_DIST`, `NM_DIST`, `CD_SUBDIST`, `NM_SUBDIST`, `CD_BAIRRO`, `NM_BAIRRO`, `CD_NU`, `NM_NU`, `CD_FCU`, `NM_FCU`, `CD_AGLOM`, `NM_AGLOM`, `CD_RGINT`, `NM_RGINT`, `CD_RGI`, `NM_RGI`, `CD_CONCURB`, `NM_CONCURB`, `geometry`.

### CSV Basico

| item | valor |
| --- | --- |
| Arquivo | `data/raw/CENSO 2022/Agregados_por_setores_basico_BR_20250417/Agregados_por_setores_basico_BR_20250417.csv` |
| Encoding confirmado | `latin-1` |
| Separador | `;` |
| Linhas | 468.099 |
| Colunas | 36 |

Colunas essenciais confirmadas: `CD_SETOR`, `CD_UF`, `CD_MUN`, `AREA_KM2`, `v0001`, `v0002`, `v0005`, `v0007`.

Semantica canonica:
- `v0001`: populacao total do setor, usar como `pop_total_setor_2022`.
- `v0002`: total de domicilios, nao usar como populacao.
- `v0005`: media de moradores, usar como apoio para renda per capita quando necessario.
- `v0007`: domicilios particulares ocupados, apoio para consistencia.

### CSV renda responsavel

| item | valor |
| --- | --- |
| Arquivo | `data/raw/CENSO 2022/Agregados_por_setores_renda_responsavel_BR_csv/Agregados_por_setores_renda_responsavel_BR.csv` |
| Encoding confirmado | `utf-8-sig` na amostra; codigo legado tambem tolera `latin-1` |
| Separador | `;` |
| Linhas | 458.772 |
| Colunas | 6 |

Colunas confirmadas: `CD_SETOR`, `V06001`, `V06002`, `V06003`, `V06004`, `V06005`.

Uso recomendado:
- `V06004`: rendimento medio mensal do responsavel, proxy de renda a calibrar.
- `V06001`: quantidade de domicilios/responsaveis com renda, apoio para totalizacao.
- `V06005`: manter apenas como campo auditavel; o pipeline atual evita usa-lo como renda principal por inconsistencias de escala.

### Parquets censitarios atuais

Arquivos confirmados:
- `data/staging/censo2022_setores_h3_res7.parquet`: 114.986 linhas, 22 colunas.
- `data/staging/censo2022_setores_validado_v2.parquet`: 114.986 linhas, 41 colunas.
- `data/staging/censo2022_setores_calibrado.parquet`: 114.986 linhas, 54 colunas.
- `data/staging/censo2022_setores_calibrado_nacional_completo.parquet`: 1.251.771 linhas, 50 colunas.
- `data/outputs/oportunidades_expansao_hibrido.parquet`: 1.542.531 linhas, 82 colunas. (Era 1.532.645 ate a regeneracao oficial do litoral — DEC-002/DEC-003, criterio de centroide -> OVERLAP com `M1_HEX_LAND_FRACTION_MIN=0.05`; numero corrigido aqui em 2026-07-27.)

Colunas censitarias uteis ja existentes: `hex_id`, `lat`, `lng`, `uf`, `cod_municipio`, `nome_municipio`, `pop_total_setor_2022`, `renda_per_capita_setor_2022`, `renda_per_capita_setor_2022_calibrada`, `coverage_pct_setor_2022`, `qualidade_join_uf`, `flag_baixa_pop_setor`, `densidade_pop_setor_hab_km2`, `score_setor_2022_calibrado`.

Confirmacao importante: esses Parquets sao agregados por H3/hex e nao possuem `geometry` nem geometria serializada. Eles servem para rastreabilidade e fallback, mas nao bastam para intersecao real setor x circulo.

## 3. Saidas esperadas

O subsistema deve retornar, sob demanda:
- KPIs do ponto: populacao estimada no raio, renda ponderada, densidade, score censitario medio/maximo, numero de setores intersectados, area analisada e flags de qualidade.
- Tabela de setores intersectados com pesos de area e metricas ponderadas.
- Conjunto de mapas estaticos PNG (ate 7 camadas numa unica geracao): as camadas do raio trazem setores, circulo do raio, ponto central e pins opcionais de concorrentes/Ultra; `socioeconomia` e `residual` tem frame proprio de 5 km, NAO desenham pins e (BLK-RELPON-14) NAO desenham o circulo nem o rotulo de raio -- o ponto e identificado pela borda fina do hexagono que o contem -- ver §6.
- CSV da tabela de setores.
- PDF executivo com metodologia, qualidade e limitacoes.

Nenhuma saida deve recalcular ou alterar `score_priorizacao`, `hex_score_estrutural`, carteira, plano curto prazo, plano dominio ou artefatos oficiais do M1.

**BLK-RELPON-05:** alem dos agregados ponderados do raio, `result` expoe 5 campos com o valor BRUTO do setor censitario que CONTEM o ponto pesquisado (nao o agregado do raio nem valor por hex): `cod_setor_ponto`, `renda_per_capita_setor_ponto`, `densidade_pop_setor_ponto`, `score_setor_2022_calibrado_ponto` e `flag_setor_ponto_encontrado`. Quando o ponto cai fora de qualquer setor da malha (agua/orla, setor com geometria invalida), os 5 campos ficam `None`/`False`. Esses 5 campos permanecem no `result` para CSV/auditoria; **a partir do BLK-RELPON-06 eles NAO alimentam mais a faixa superior do mapa** (ver `densidade_pop_raio_valida_hab_km2` abaixo e §6).

**BLK-RELPON-06 (D1, reverte a fonte da faixa do BLK-RELPON-05):** `result` expoe tambem `densidade_pop_raio_valida_hab_km2` = `pop_total_raio / (area_intersecao_total_m2 / 1e6)` -- populacao do RAIO dividida pela area de espaco VALIDO (soma das areas de intersecao dos setores IBGE com o circulo; o IBGE nao cobre agua/vazio). Diferente de `densidade_pop_raio_hab_km2` (divide por `pi*raio_km**2` FIXO, incluindo agua/vazio dentro do circulo -- subestima a densidade em pontos com rio/mar no raio, ex.: Rio Branco/AC). `None` ("n/d" no display) quando nao ha setores intersectados ou a area valida e 0. Os 3 mapas choropleth (Densidade/Renda/Score) passam a exibir os agregados do RAIO numa faixa superior ("Renda no raio: R$ 3.321", "Densidade no raio: 678 hab/km2", "Score no raio: 70"; "n/d" quando nao ha setores/area valida); a camada Concorrentes e Ultra fica byte-a-byte igual (sem faixa). A faixa "no raio" foi depois estendida a `renda_domiciliar` (PR #126); `socioeconomia` (a partir do BLK-RELPON-13) e `residual` usam rotulo proprio -- sao camadas de HEXAGONO enquadradas a 5 km e mostram "Score/Residual no hexagono: ..." (`_legenda_valor_hex`), nao "no raio" -- ver §6.

**BLK-RELPON-08 (novo campo `domicilios_total_raio`):** `result` expoe tambem `domicilios_total_raio` -- soma de `domicilios_particulares_ocupados_setor_2022 * peso_area_setor` sobre os setores intersectados ao circulo do raio (1,0 km), EXATAMENTE o mesmo padrao de peso de area de `pop_total_raio` (nao a coluna `domicilios_total` do BLK-RELPON-07, que agrega por bairro/distrito, escopo diferente). `None` ("n/d") quando nenhum setor intersectado tem a coluna de domicilios. Nao persistido como coluna em `setores_intersectados`/CSV auditavel -- so o agregado do raio.

## 4. Colunas canonicas do artefato geo

O Bloco 2 deve materializar um artefato local otimizado, preferencialmente particionado por `uf` e `cod_municipio`, com estas colunas minimas:

| coluna | origem/regra |
| --- | --- |
| `cod_setor` | `CD_SETOR` normalizado como string de 15 digitos |
| `uf` | sigla derivada de `CD_UF` |
| `cod_uf` | `CD_UF` |
| `cod_municipio` | `CD_MUN` |
| `nome_municipio` | `NM_MUN` |
| `situacao_setor` | `SITUACAO` |
| `area_setor_km2_ibge` | `AREA_KM2` da malha/CSV |
| `geometry` ou `geometry_wkb` | geometria real do setor |
| `crs_origem` | `EPSG:4674` |
| `pop_total_setor_2022` | `v0001` do Basico |
| `renda_per_capita_setor_2022` | proxy derivada de `V06004`, calibravel |
| `renda_per_capita_setor_2022_calibrada` | valor calibrado quando disponivel |
| `score_setor_2022_calibrado` | score operacional paralelo, quando disponivel |
| `densidade_pop_setor_hab_km2` | populacao / area metrificada |
| `bbox_minx`, `bbox_miny`, `bbox_maxx`, `bbox_maxy` | bbox para recorte rapido |
| `flag_renda_disponivel` | renda valida no setor |
| `flag_geometria_valida` | geometria presente e valida |
| `qualidade_join_uf` | qualidade herdada quando houver join com Parquets atuais |

**BLK-RELPON-07:** `cod_bairro`/`nome_bairro`/`nome_subdistrito`/`nome_distrito`, `domicilios_particulares_ocupados_setor_2022`, `renda_responsavel_media_setor_2022` e `area_setor_m2` ja existem no artefato geo (confirmados em `COLUNAS_ARTEFATO`, `materializar_setores_censitarios_geo.py`) mesmo nao estando na tabela "minima" acima; o BLK-RELPON-07 e o primeiro consumidor de `cod_bairro`/`nome_bairro`/`nome_distrito`/`domicilios_particulares_ocupados_setor_2022`/`renda_responsavel_media_setor_2022` no motor pontual (`nome_subdistrito` segue nao utilizado, descartado por D1 -- ver abaixo).

### Implementacao do Bloco 2

Pipeline criado: `jobs/pipelines/materializar_setores_censitarios_geo.py`.

Saida padrao: `data/outputs/setores_censitarios_2022_geo/uf=XX/cod_municipio=NNNNNNN/part-000.parquet`, com `geometry_wkb` e colunas de bbox para recorte rapido. A geometria fica serializada em WKB no CRS original `EPSG:4674`; `area_setor_m2` e `densidade_pop_setor_hab_km2` sao calculadas em `EPSG:5880`.

Regras implementadas:
- Join malha x Basico por posicao dentro da UF, preservando o alinhamento confirmado no Bloco 1.
- Join da renda por posicao dentro da UF, porque `CD_SETOR` do CSV de renda vem truncado em notacao cientifica e o arquivo tem menos linhas que a malha/Basico.
- `renda_per_capita_setor_2022 = V06004 / v0005`; quando `data/staging/brasil_estrutural.parquet` esta disponivel, `renda_per_capita_setor_2022_calibrada` usa multiplicador global contra a mediana M1.
- `score_setor_2022_calibrado` e operacional paralelo e, desde a DEC-040 (2026-08-26), esta em REGUA ABSOLUTA: `clip(0.60*nota_renda + 0.40*nota_pop + ajuste_executivo, 0, 100)`, com `nota_renda` LINEAR sobre `renda_per_capita_setor_2022_calibrada` (R$ 300 -> 0, R$ 4.000 -> 100) e `nota_pop` em LOG sobre `pop_total_setor_2022` (1.000 hab -> 0, 100.000 hab -> 100). Os pesos 0.60/0.40 sao os mesmos de sempre; o que mudou foi a ESCALA. Ate 2026-08-25 os dois termos eram PERCENTIS (`0.60*renda_pct_nacional_calibrado + 0.40*pop_pct_municipal`), e o de populacao era percentil MUNICIPAL, entao toda cidade produzia seus proprios "melhores hexagonos" no topo da escala por construcao. As duas colunas de percentil continuam materializadas, mas so' como AUDITORIA e caminho de reversao (`recalcular_score_absoluto --reverter`); nao alimentam mais o score. Nao altera o M1.

Validacao real executada no DF: 5.418 setores, 1 arquivo, 3,46 MB, ~35s, 97,16% de cobertura de renda/score. Relatorio em `data/reports/relatorio_pontual_censitario_base_geo.md`.

## 5. Metodo de intersecao

1. Receber `lat`, `lng` e usar `raio_km=1.0` fixo no fluxo do relatorio.
2. Identificar UF/municipio provavel para carregar apenas particoes relevantes do artefato geo.
3. Reprojetar ponto e setores candidatos para CRS metrico apropriado ao local antes de criar buffer e medir area.
4. Criar circulo/buffer de `1.000 m`.
5. Filtrar setores por bbox e depois por `intersects`.
6. Calcular `area_setor_m2`, `area_intersecao_m2` e `peso_area_setor = area_intersecao_m2 / area_setor_m2`.
7. Ponderar metricas por area intersectada. Populacao estimada = `pop_total_setor_2022 * peso_area_setor`; renda e scores devem ser medias ponderadas pela populacao estimada quando houver populacao positiva, com fallback para peso de area.
8. Reaproveitar pins de concorrentes/Ultra por distancia haversine ao ponto, sem alterar suas bases.

Metodo canonico de resultado: `setor_censitario_intersecao_area_1km`.

### Implementacao do Bloco 3

Motor criado em `src/motor_expansao/dashboard/censo_point.py`: `analisar_ponto_censitario_setores(lat, lng, setores_df, raio_km=1.0, competitors_df=None, ultra_df=None)`.

Regras implementadas:
- entrada setorial por `DataFrame` ja recortado/carregado, com `geometry_wkb` em `EPSG:4674`;
- CRS metrico local azimutal equidistante centrado no ponto para criar o buffer de `1.000 m` e medir areas;
- pre-filtro por bbox quando `bbox_minx/bbox_miny/bbox_maxx/bbox_maxy` existem;
- `area_intersecao_m2`, `peso_area_setor`, `pop_estimada_intersecao` e tabela auditavel de setores intersectados;
- renda e score medios ponderados por populacao estimada, com fallback por area de intersecao quando nao ha populacao positiva;
- concorrentes/Ultra filtrados por distancia haversine ao ponto, sem mutar as bases de entrada.

O retorno explicita `metodo = setor_censitario_intersecao_area_1km` e permanece paralelo ao M1.

**BLK-RELPON-05:** o mesmo laco de intersecao ja decodifica/projeta a geometria de cada setor candidato para o CRS metrico local; reaproveitando essa geometria, cada `record` ganha `contains_ponto = geom_metric.covers(Point(0, 0))` (o proprio ponto pesquisado, origem do CRS local). Depois de montar `intersectados`, o setor que cobre o ponto (tie-break por maior `peso_area_setor` quando mais de um "cobre" por ruido de ponto flutuante) alimenta os 5 campos `*_ponto` do `result`. Nao recalcula geometria nova, nao altera `setores_intersectados`/agregados do raio/metodo/raio.

## 6. Mapa do relatorio (camadas combinadas + fundo de ruas)

Implementacao: `src/motor_expansao/dashboard/censo_map.py`. Criado no ciclo BLK-CENSO-01 (DEC-004) e ampliado depois por BLK-CENSO-03/-FU5, BLK-RELPON-06, renda domiciliar (PR #126) e BLK-RELPON-10/-FU1 (`socioeconomia`/`residual`). A camada `entorno` (mapa de quadra do BLK-RELPON-11) foi REMOVIDA por completo no BLK-RELPON-14.

Funcao principal: `render_mapas_censitarios_combinados(lat, lng, setores_df, *, raio_km=1.0, competitors_df=None, ultra_df=None, width=1000, height=760, basemap=True, logos_dir=None, ultra_logo_dir=None, street_ceil=None, street_gain=None, street_cap=None, choropleth_alpha=None, hexes_df=None) -> dict[str, bytes]`. Retorna ate 7 camadas numa unica geracao — as 5 INCONDICIONAIS `{"densidade", "renda", "score", "renda_domiciliar", "concorrentes"}` mais as CONDICIONAIS `"socioeconomia"` e `"residual"` (BLK-RELPON-13: ambas so entram quando `hexes_df` foi passado, tem a coluna do dado — `score_setor_2022_calibrado` / `oferta_efetiva_disponivel` — E ha pelo menos 1 hex desenhavel; sem elas a celula do slide cai no fallback textual). Chaves canonicas e ordem em `CAMADAS_CENSITARIAS` (`score` restaurado no BLK-CENSO-03-FU5; `renda_domiciliar` em 2026-07-17, PR #126; `socioeconomia`/`residual` no BLK-RELPON-10, `socioeconomia` migrada para hexagono a 5 km no BLK-RELPON-13; `entorno` entrou no BLK-RELPON-11 e SAIU no BLK-RELPON-14). O legado `render_mapa_censitario_estatico_png(...)` continua como wrapper fino que devolve UMA camada pelo antigo `metric_column` (`score_setor_2022_calibrado` -> `score`) e NAO conhece `socioeconomia`/`residual` — nao serve para montar o PDF.

**Camada `renda_domiciliar` (5o choropleth, PR #126; READ-ONLY M1; formula corrigida em 2026-08-14):** renda media domiciliar por SETOR = `(renda_responsavel_media_setor_2022 [V06004 BRUTA]; quando NaN -> renda_per_capita_setor_2022 x avg_moradores_domicilio_setor_2022) x uplift_composicao_por_setor(cod_setor,uf,cod_municipio)` [uplift SETORIAL, cada setor e' um poligono] `x FATOR_TEMPORAL_RENDA` — NUNCA a coluna calibrada: ela carrega o `k` do score e o uplift ja converte a escala; a formula antiga (`renda_pc_calibrada x moradores x uplift x temporal`) tinha DUPLA CONTAGEM do `k` (+23,35% medido em 5.551 municipios). Espelha a formula de `censo_point.py`; a faixa "no raio" usa `result["renda_domiciliar_total_raio"]` (COM uplift+temporal). Bandas `RENDA_MEDIA_DOMICILIAR_BANDS`: MESMA paleta da `RENDA_PER_CAPITA_BANDS` + faixas 2.000/4.600/8.000/14.000 + rotulo "ate" SEM acento (o font do PNG da legenda nao renderiza acento — excecao de RENDER a regra §2 do CLAUDE.md). No PDF (`censo_report.py`) o slide "Mapas de calor" virou **grid 2x2** `[densidade, renda, score, renda_domiciliar]` (`_draw_maps_grid(..., pack=True)` -> `_map_grid_cells_packed`, era tira 1x3); a variante CLASSICA (a que o dashboard baixa) tem header fixo -> celula 2x2 mais baixa -> legenda ~8pt (recente >=9pt). A renda media domiciliar TAMBEM aparece no tooltip do hex do Mapa Territorial, porem com uplift MUNICIPAL (hex res-7 cobre varios setores) — ver CLAUDE.md §4.

Regras implementadas:
- **7 camadas canonicas, geradas numa unica chamada** (`CAMADAS_CENSITARIAS` = `densidade`, `renda`, `score`, `renda_domiciliar`, `socioeconomia`, `residual`, `concorrentes`) e distribuidas por 3 paginas do PDF — **NAO** mais uma pagina por camada (o BLK-RELPON-01 consolidou os choropleths): "Socioeconomia e Residual Fitness" (grid 2x1, `socioeconomia`+`residual`), "Mapas de calor" (grid 2x2, `densidade`/`renda`/`score`/`renda_domiciliar`) e "Concorrentes" (camada `concorrentes`). A UI nao tem dropdown que esconda camadas. Densidade/Renda/**Score**/Renda domiciliar compartilham basemap, bbox, projecao, pins e choropleth (varia so o fill dos setores + legenda/titulo); a camada **Score censitario** usa o choropleth de `score_setor_2022_calibrado` (`_color_for_score`) COM legenda de faixas 0-100 (`_score_legend_entries()`); a camada **Concorrentes e Ultra e SO de pins** (sem choropleth — BLK-CENSO-03): basemap + pins de concorrentes/Ultra + ponto central + legenda de pins; as camadas **`socioeconomia`** (BLK-RELPON-13: `score_setor_2022_calibrado` por hexagono H3, mesma paleta 0-100 do `score`) e **`residual`** (`oferta_efetiva_disponivel` em alunos por hexagono H3) tem frame/bbox proprios, enquadrados a 5 km, e NAO desenham pins.
- **Fundo de ruas por tiles online** (CartoDB **Voyager COM labels** — base CLARA estilo GeoFusion; realce de contraste `_BASEMAP_CONTRAST=1.15` + zoom+1; `_BASEMAP_PROVIDER_ATTR="Voyager"`/`_BASEMAP_CONTRAST`/`_BASEMAP_ZOOM_BUMP` em `censo_map.py`) SO na geracao, via `contextily` (DEC-004), com **import lazy** sob `try/except ImportError` dentro da funcao de fundo. Cache local em `data/cache/basemap_tiles/` (gitignored). **Fallback offline gracioso**: `basemap=False` OU import/fetch falha (sem internet, sem extra `[basemap]`) -> mapa sobre **canvas CLARO** `(245,245,245)` SEM ruas, sem excecao. O dashboard interativo NAO depende de internet.
- **Composicao em `EPSG:3857`** (CRS nativo dos tiles): os setores e o circulo do raio (1,0 km) sao reprojetados do CRS metrico azimutal local -> 3857 SO para render, reusando o transformer do motor. O motor (`analisar_ponto_censitario_setores`, intersecao setor x circulo, raio 1,0 km) fica INTOCADO. Distorcao Mercator do circulo < 0,5% sobre ~3 km em latitudes brasileiras.
- **Faixas absolutas fixas por camada** (estilo GeoFusion), substituindo o quartil: `DENSIDADE_POP_BANDS` (rampa de vermelhos; cortes 1k/5k/10k/25k/inf hab/km2) e `RENDA_PER_CAPITA_BANDS` (rampa **amarelo -> laranja escuro -> verde** adaptada a PER CAPITA; cortes 1k/2k/3,5k/5k/inf R$/pessoa — NAO confundir com renda domiciliar do M1). O FILL usa `_CHOROPLETH_ALPHA=140` (translucido sobre o claro); a legenda usa a cor cheia da faixa. As **ruas/nomes ESCUROS do Voyager** sao recolocados POR CIMA do choropleth com os PROPRIOS pixels do tile (NAO edge-detection): luminancia `< _STREET_CEIL` vira opacidade (ganho `_STREET_GAIN`, teto `_STREET_CAP`) -> arruamento nitido sobre a cor, estilo GeoFusion. A camada Concorrentes e Ultra nao tem choropleth (so pins).
- **Cobertura de frame inteiro + transicao suave** (estilo GeoFusion): o choropleth cobre TODO o frame do mapa (setores recortados ao **RETANGULO** do frame, **nao a um quadrado** e nao ao circulo), com os corners preenchidos; o frame e' `_frame_box_metric` (`censo_map.py`), um retangulo com a **proporcao da area de mapa** (via `_map_inner_dims`) cujo **lado MENOR** = `raio*(1+_MAP_FRAME_MARGIN)` — assim o choropleth preenche a figura inteira sem letterbox e o circulo do raio continua redondo (x/y compartilham o mesmo `scale` no render); valores vindos da propria linha do setor (`setores_df`) p/ colorir tambem setores fora do circulo. SEM borda nos poligonos (transicao suave). O circulo do raio (1,0 km) segue desenhado so como referencia visual do raio. Tudo display-only: a analise/KPIs (`analisar_ponto_censitario_setores`) segue circular e INTOCADA.
- **Marcador de concorrente/Ultra = LOGO QUADRADA** (BLK-RELPON-09) via `competitors._render_square_logo_tile` (`_paste_logo_pin`, lado `_PIN_LOGO_PX = 30` px do PNG-fonte, ancorado pelo CENTRO do quadrado no ponto): Ultra=`"__ultra__"`, concorrente pela coluna `rede`; **linha SEM `rede` e' academia INDEPENDENTE (DEC-046) e recebe o marcador comum do agregador — chave `competitors.CHAVE_AGREGADOR`, lado `PIN_INDEPENDENTE_PX = 20` px, portanto MENOR que a bandeira de cadeia, e desenhada ANTES dela (`_project_points` ordena independente primeiro), de modo que na sobreposicao a rede instalada fica por cima — mesma precedencia do Mapa Territorial do piloto. O PNG (`logo_wellhub.png` no diretorio de logos) e' OPCIONAL: sem ele o marcador cai numa placa SOLIDA na cor da marca, sem sigla**; placa branca quando ha logo, placa na cor da marca + sigla no fallback; borda branca de 2 px + sombra leve para contraste sobre o choropleth. Os logos sao lidos do `_ICON_CACHE` populado por `preload_logos` no boot dos consumidores (backend do piloto `web/server/app.py`, API `api/service.py`) e, lazy, pelo proprio `censo_map` quando recebe `logos_dir`. `competitors._render_pin_tile` (balao teardrop 128x128) segue INTOCADO servindo `build_icon_atlas` (`anchorY=122`; o mapa interativo pydeck que o consumia saiu com o Streamlit — DEC-022, 2026-08-03).
- Exibe circulo do raio (1,0 km) (referencia do raio), ponto central como **pin vermelho**, legenda por camada, escala visual, coordenada central, metodologia e atribuicao `(c) OpenStreetMap, (c) CARTO` no rodape. **Excecoes das camadas com frame proprio (`socioeconomia` e `residual`, enquadradas a 5 km):** nao desenham pins nem a legenda de pins (`pins=[]`, `ultra_pins=[]`, `mostrar_legenda_pins=False` — BLK-RELPON-10-FU1: a 5 km os logos tapavam o choropleth) e, desde o **BLK-RELPON-14**, tambem NAO REPRESENTAM o raio: `circle_3857=None` (sem o circulo azul) e `raio_km=None` (sem o prefixo `"Raio X km"` no rodape, que nada substitui; EPSG:3857 + atribuicao continuam). Quem identifica o ponto nesses dois paineis e o **pin vermelho central** mais a **borda fina do hexagono H3 res-7 que CONTEM o ponto** (`destaque_3857` de `_render_camada`, cor `_HEX_CENTRAL_EDGE_COLOR` = o mesmo azul do antigo circulo, `_HEX_CENTRAL_LINEWIDTH=3`): so a BORDA e desenhada por cima do choropleth — o preenchimento do hex continua vindo do `color_fn`, como o dos demais. Quando o hex central nao esta entre os desenhados, simplesmente nao ha borda. As camadas do raio seguem passando circulo + `raio_km` e saem byte-a-byte identicas. O `zoom_bump` local (BLK-RELPON-11) saiu junto com a camada de quadra: a constante global `_BASEMAP_ZOOM_BUMP` continua `1` e serve o frame do raio e o de 5 km.
- Estado vazio ou ausencia de concorrentes/Ultra nao quebra a geracao.
- Dependencias: `pillow>=10.0.0` (base), `pyproj`/`shapely` (base) para a reprojecao aeqd->3857; `contextily>=1.5.0` no **extra dedicado `[basemap]`** (fora do deploy base), import lazy.

- **Refinamento visual BLK-EST-02 (tema "Ultra Clean / GeoFusion", gate Felipe 2026-06-11):**
  - **Titulos curtos (D6=A):** os 4 mapas de entao (hoje sao 7 camadas, todas com titulo curto; desde o BLK-RELPON-14 nem mesmo os paineis enquadrados a 5 km trazem o raio no titulo — sao so `"Socioeconomia"` e `"Residual Fitness"`) perderam o prefixo repetitivo `"Relatorio Pontual Censitario - "` (a camada ja aparece na faixa de titulo do PDF) -> `"Densidade populacional"`, `"Renda per capita"`, `"Score censitario"`, `"Concorrentes e Ultra"`. O **subtitulo tecnico** (coordenada/raio/setores) e mantido para auditoria inline (o PNG tambem serve a UI). Tamanho do titulo aumentado no BLK-RELPON-06 (ver abaixo).
  - **Legenda (D7=C subset seguro):** amostras de cor arredondadas (`draw.rounded_rectangle`, radius 5) + linha separadora fina antes do bloco de pins. `_map_box`/`legend_x`/largura da coluna foram REABERTOS e ampliados pelo BLK-RELPON-06 (ver abaixo) para acomodar as fontes maiores.
  - **Escala/rodape (D8=B):** barra de escala mais grossa (`width=5`) com box branco semi-translucido atras do label (legibilidade sobre qualquer cor do choropleth); rodape enxuto `"Raio {raio} km - EPSG:3857 - {atribuicao}"` (o raio vem do motor, DEC-021). A atribuicao CARTO/OSM (`_ATRIBUICAO_TILES`, constante nova) permanece quando ha basemap (licenca DEC-004) e some no fallback offline.

- **Faixa superior "\<variavel\> no raio" (BLK-RELPON-05, campo REVERTIDO para o raio pelo BLK-RELPON-06/D1):** `_render_camada` tem o parametro opcional `valor_ponto: str | None = None` (default `None` = render IDENTICO); quando informado, desenha uma 2a linha de texto em `(28, _VALOR_Y)` (`_font(_FS_VALOR_RAIO)`, tinta `_DARK_MAP_INK`), entre o titulo e o `map_box`. `render_mapas_censitarios_combinados` monta as strings de raio com `_legenda_valor_ponto` + `_format_valor_ponto_renda/_densidade/_score` e as passa para `densidade_png`/`renda_png`/`score_png` e, desde o PR #126, tambem para `renda_domiciliar_png` (`renda_domiciliar_total_raio`). As camadas de frame proprio usam rotulo diferente: `socioeconomia` (BLK-RELPON-13) e `residual` usam `_legenda_valor_hex` ("Score/Residual no hexagono: ...", valor do hex CENTRAL — o mesmo que leva a borda de destaque do BLK-RELPON-14 —, nao a soma do disco). A chamada de `concorrentes_png` nao recebe o parametro (fica `None`, mapa so-pins byte-a-byte igual). Formato: moeda com separador de milhar `.` sem centavos (renda), inteiro com unidade ASCII `hab/km2` (densidade), inteiro 0-100 (score); `"n/d"` quando nao ha setores intersectados no raio (sem area valida). **A partir do BLK-RELPON-06 os 3 valores vem dos AGREGADOS DO RAIO** (`densidade_pop_raio_valida_hab_km2`, `renda_per_capita_media_raio`, `score_setor_medio`), NAO mais do valor bruto do setor que contem o ponto (os 5 campos `*_setor_ponto` do BLK-RELPON-05 continuam no `result` para CSV/auditoria, mas deixaram de alimentar esta faixa).

- **Legibilidade dos mapas — BLK-RELPON-06 (D3+D4, 2026-07-14):** o texto dos mapas estava quebrado em producao (dashboard **e** PDF): `_font()` chamava `ImageFont.truetype("arial.ttf", size)` e caia em `ImageFont.load_default()` **sem** `size` no `OSError` -- na imagem `python:3.11-slim` (sem NENHUMA fonte TrueType instalada) isso significava um bitmap fixo ~10px que IGNORAVA o `size` pedido (`_font(20)` e `_font(60)` renderizavam IGUAL). Fix: `_font()` passa a usar **exclusivamente** a fonte TrueType **embutida do Pillow** (`ImageFont.load_default(size=size)`, Pillow >= 10.1; o projeto pina `pillow==12.3.0`), **abandonando `arial.ttf`/TTF do sistema** -- o render fica IDENTICO em Windows e na VPS (determinismo auditavel), ao custo de mudar o visual local (Arial -> fonte embutida) e quebrar de proposito a byte-identidade dos PNGs vs. o estado anterior. Junto com isso, as fontes do mapa cresceram **na BASE** (dashboard, PDF e API com **UM UNICO render**, sem parametro de escala por-caminho -- `pages.py` INTOCADO):

  | Elemento | px antigo | px novo (constante) |
  |---|---|---|
  | Titulo do mapa | 20 | 44 (`_FS_TITULO`) |
  | Linha "\<variavel\> no raio" | 17 | 38 (`_FS_VALOR_RAIO`) |
  | Legenda — titulo "Legenda" | 17 | 34 (`_FS_LEGENDA_TITULO`) |
  | Legenda — subtitulo (ex. "Renda per capita (R$/pessoa)") | 13 | 22 (`_FS_LEGENDA_SUBTITULO`) |
  | Legenda — corpo (rotulos de faixa) | 13 | 32 (`_FS_LEGENDA_CORPO`) |
  | Legenda — captions ("Ponto central"/"Pins: Ultra e concorrentes") | 13 | 20 (`_FS_LEGENDA_CAPTION`) |
  | Mensagem "Sem setores intersectados no raio" | 12 | 26 (`_FS_BODY`) |
  | Rodape (atribuicao CARTO) | 11 | 22 (`_FS_FOOTER`) |
  | Barra de escala (label) | 11 | 24 (`_FS_ESCALA`) |

  O subtitulo e as captions da legenda ganharam constantes de fonte **proprias, menores** que o corpo (`_FS_LEGENDA_SUBTITULO`/`_FS_LEGENDA_CAPTION`) porque, no ajuste visual, o subtitulo mais longo ("Renda per capita (R$/pessoa)") e a frase "Pins: Ultra e concorrentes" transbordavam do canvas quando desenhados a `_FS_LEGENDA_TITULO`/`_FS_LEGENDA_CORPO` -- sao texto secundario/anotacao, como o rodape. Layout acompanhou o crescimento das fontes: `_map_box` usa `_MAP_TOP=132` (era 92) e a coluna da legenda alargou de `_LEGEND_COL_W=330` (era 252, `legend_x = width - _LEGEND_COL_W`) porque o rotulo de faixa mais longo (`"R$ 2.001-3.500"`, 14 chars) nao cabia mais na coluna antiga a 32px; a linha de dado usa `_VALOR_Y=78` (era 51). A area util do mapa encolheu de ~663x590 para ~585x550 px (inner) para abrir espaco -- o choropleth continua cobrindo o frame inteiro, sem mudanca de cor/faixa. Dimensionamento ancorado no **pior caso do PDF**: na epoca do BLK-RELPON-06 o PNG de 1000px entrava numa celula de ~298,67pt da tira 1x3 do slide "Mapas de calor" (`ratio ~0,2987`), com a legenda-corpo (32px) a ~9,6pt. **Geometria vigente:** o slide virou **grid 2x2** de 4 camadas — a legenda-corpo fica >=9pt na variante recente e cai para ~8pt na CLASSICA (header fixo `_CLASSICO_MAPS_TOP` ~122 pt reduz a altura da celula), piso legivel aceito.

Validacao minima: `tests/unit/test_relatorio_pontual_censitario_mapa.py` trava as 7 chaves canonicas da tupla `CAMADAS_CENSITARIAS` (teste que so confere `set()`/`len()`) e gera as 5 camadas INCONDICIONAIS com `basemap=False` (`test_mapas_combinados_gera_png_com_setores_pins_legenda_e_escala`; as 2 CONDICIONAIS, `socioeconomia` e `residual`, sao cobertas a parte por `test_camada_residual_presente_com_hexes_df_e_png_valido` e pelos casos da socioeconomia), verifica faixas fixas (nao quartil), que a camada `score` tem choropleth (modo de cor) e a `concorrentes` nao, pin com logo de concorrente/Ultra e o fallback offline sem tiles, alem do estado vazio; o teste `test_atribuicao_tiles_constante_e_legenda_arredondada_disponiveis` cobre o refinamento BLK-EST-02 (titulos curtos, legenda arredondada+separador, atribuicao); os testes `test_valor_ponto_repassado_aos_4_choropleths_nao_a_concorrentes`, `test_valor_raio_nao_e_nd_quando_setor_nao_cobre_o_ponto_mas_intersecta_raio`, `test_valor_raio_e_nd_quando_setor_fora_do_raio` e `test_valor_ponto_muda_pixels_do_png` cobrem a faixa "no raio" do BLK-RELPON-05/BLK-RELPON-06; `test_font_escala_com_o_size`, `test_font_nao_chama_truetype_do_sistema_no_proprio_codigo`, `test_font_nao_depende_de_ttf_do_sistema`, `test_legenda_corpo_atinge_o_alvo_de_legibilidade_no_pdf`, `test_rotulo_mais_longo_da_legenda_cabe_na_coluna`, `test_legenda_subtitulo_mais_longo_nao_transborda_do_canvas` e `test_legenda_caption_pins_nao_transborda_do_canvas` cobrem a fonte embutida (D3) e o contrato de legibilidade/layout do BLK-RELPON-06 (D4) em codigo, sem depender so de revisao visual. **BLK-RELPON-10/-FU1** adiciona `test_camada_residual_presente_com_hexes_df_e_png_valido`, `test_camada_residual_ausente_sem_hexes_df_ou_sem_hex_no_disco`, `test_camada_residual_nao_desenha_pins_blk_relpon_10_fu1`, `test_faixa_de_cor_do_residual_por_valor_absoluto`, `test_labels_da_regua_de_residual_sem_acento_para_legenda_png` e `test_raio_de_exibicao_nao_toca_o_raio_do_motor`. **BLK-RELPON-11** adicionou a bateria da camada `entorno`, **REMOVIDA** com ela no BLK-RELPON-14 (junto com o caso do `zoom_bump` local). **BLK-RELPON-14** passa a cobrir, no lugar: que `RAIO_RESIDUAL_DISPLAY_KM` continua `5.0` e segue alimentando o enquadramento, que `socioeconomia`/`residual` nao desenham mais o circulo do raio nem o prefixo `"Raio X km"` no rodape, que a borda fina do hex central (`destaque_3857`) muda pixels sem alterar o preenchimento do choropleth, e que a ausencia do hex central no frame nao quebra o render (sem borda, sem excecao). As camadas do raio seguem cobertas pela regressao de byte-identidade.

## 7. Export CSV e PDF

Implementacao: `src/motor_expansao/dashboard/censo_report.py`. **Template Ultra reescrito sobre `fpdf2`** no ciclo BLK-CENSO-02 (gate humano de Felipe em 2026-06-05 aprovou trocar o writer manual PDF-1.4 por `fpdf2`, dependencia BASE em `pyproject.toml`). O writer roda com **compressao de stream desativada** (`set_compression(False)`) para auditabilidade anti-PII e asserts de texto cru — os PDFs sao pequenos. **Follow-up BLK-CENSO-02-FU1 (Felipe, 2026-06-06):** paginas em **proporcao 16:9 widescreen (960x540 pt)** em vez de A4 retrato (a capa 16:9 do `.pptx` deixou de distorcer e o titulo foi para a zona limpa inferior-direita, sem colidir com o logo "GRUPO ULTRA" embutido); Big Numbers passou a **grid 4x2 de 8 metricas** e o card de residual passou a exibir **Residual Fitness em alunos** (`oferta_efetiva_disponivel`) + **SAM Fitness em alunos** (`sam_fitness_potencial`) no lugar do `score_oportunidade_residual`.

Funcoes principais:
- `gerar_csv_setores_censitarios(result)`: retorna bytes CSV da tabela de setores intersectados, com `sep=";"` e `encoding="utf-8-sig"`, sem incluir geometria bruta. **INALTERADO** neste ciclo.
- `gerar_pdf_relatorio_pontual_classico(result, mapas=None, *, residual=None, perfil_bairro=None, ultra_dir=None, solicitante=None, rotulo=None, now=None, fotos=None, info_imovel=None, viabilidade=None, foto_satelite=None, foto_satelite_grande=False)`: retorna bytes PDF em memoria com a **estrutura de 7 paginas** (template Ultra; **BLK-RELPON-01** consolidou os 3 choropleths em 1 slide; **BLK-RELPON-07** inseriu "Perfil do Bairro/Distrito"; **BLK-RELPON-10** inseriu o slide-hero "Socioeconomia e Residual Fitness"; **BLK-RELPON-11** inseriu "Imagem do Entorno" e **BLK-RELPON-14** a REMOVEU), nesta ordem. **Unificacao do gerador (BLK-RELPON-14):** a estetica CLASSICA — ja o default de producao no dashboard, na API e no bot — venceu e virou a implementacao UNICA; `gerar_pdf_relatorio_pontual_censitario` continua exportada como **wrapper fino DEPRECIADO** que repassa todos os kwargs a classica e emite `DeprecationWarning` (assinatura SUPERSET da antiga: aceita tambem `now`, `foto_satelite` e `foto_satelite_grande`), para nao quebrar quem ainda a importa pelo nome (retrocompat; o backend do piloto web ja chama a classica direto). As funcoes de pagina exclusivas do template "recente" (`_cover_page`, `_socioeconomia_residual_page`, `_mapas_calor_page`, `_competitors_page`, `_perfil_bairro_page`, `_credit_page` e `_entorno_page`) foram DELETADAS; as compartilhadas (`_big_numbers_page`, `_viabilidade_page`, `_fotos_imovel_page`, `_info_imovel_page`) permanecem:
  1. **Capa** (16:9) — fundo de marca da capa (`relatorio_capa_bg.png`, ja com logo "GRUPO ULTRA" + faixa de marcas), titulo + coordenada `lat,lng` + `municipio/UF` + raio 1,0 km na **zona turquesa limpa do quadrante inferior-direito** (nao colide com o branding embutido). Sem o asset -> turquesa solido com o bloco centralizado.
  2. **Socioeconomia e Residual Fitness** (BLK-RELPON-10, gate Vinicius 2026-07-21; **socioeconomia revista no BLK-RELPON-13, gate Vinicius 2026-07-23; representacao do raio revista no BLK-RELPON-14**) — slide-hero com 2 mapas lado a lado (grid 2x1): `socioeconomia` = choropleth de `score_setor_2022_calibrado` por **hexagono H3 res-7** num ENQUADRAMENTO de `RAIO_RESIDUAL_DISPLAY_KM = 5.0` km (disco `h3.grid_disk` k=5, clip ao frame), com a **MESMA metrica e a MESMA paleta** (`score_band_to_color`, 0-100) do `score` do grid 2x2 — o que muda e' a **geometria/escala** (hex a 5 km, nao setor no raio do relatorio), reusando o caminho de render do `residual`; o `score` PERMANECE no grid 2x2 (S1=A do gate). `residual` = choropleth de `oferta_efetiva_disponivel` (em ALUNOS) por hexagono H3 res-7 no MESMO enquadramento de 5 km, com regua absoluta de 6 faixas `OFERTA_DISPONIVEL_ALUNOS_BANDS` ancorada em 2.500 alunos = 1 unidade. **Ambas** as camadas sao CONDICIONAIS: sem `hexes_df`, sem a coluna do dado (`score_setor_2022_calibrado` / `oferta_efetiva_disponivel`) ou sem hex desenhavel, a celula cai no fallback textual (offline-safe). As 2 imagens do slide sao reduzidas por `_HERO_MAP_SCALE` (fator de escala calibrado no gate visual). SEM pins nas duas camadas. **BLK-RELPON-14:** os 5 km continuam sendo o ENQUADRAMENTO (`RAIO_RESIDUAL_DISPLAY_KM` INALTERADO — encolher o frame traria de volta o mosaico chapado de 3 a 5 hexes que motivou a DEC-011), mas deixam de ser REPRESENTADOS: sem circulo azul, sem `"Raio 5,0 km"` no rodape e sem raio no titulo. O ponto passa a ser identificado pelo pin vermelho central + a **borda fina no hexagono que o contem** (ver §6).
  3. **Mapas de calor** — os 4 choropleths (`densidade`, `renda`, `score`, `renda_domiciliar`) num **grid 2x2** (BLK-RELPON-01 consolidou a tira 1x3; a 4a camada entrou com a renda media domiciliar, PR #126), sobre fundo claro `#F8F8F8` + faixa de titulo. Cada PNG (do BLK-CENSO-01) e embutido **separadamente** (nao pre-composto), com sua **legenda embutida** (as escalas sao distintas: densidade hab/km², renda R$/pessoa, score 0-100 — restaurado COM legenda no BLK-CENSO-03-FU5 —, renda domiciliar R$/domicilio). Camada ausente -> fallback textual "Mapa indisponivel para esta camada." naquela celula (offline-safe).
  4. **Concorrentes** — mapa de concorrentes (mapa `concorrentes` SO de pins: basemap + pins de concorrentes/Ultra + ponto central, SEM choropleth) a esquerda + lista textual das redes no raio a direita (coluna `rede`/`nome_unidade`, NUNCA dados de pessoas).
  5. **Perfil do Bairro/Distrito** (BLK-RELPON-07) — titulo+unidade (bairro, com fallback distrito) + 4 cards (populacao, densidade demografica, domicilios, renda media) agregados sobre TODOS os setores da unidade administrativa que CONTEM o ponto, independente do raio do relatorio (1,0 km). SEM mapa (so texto/numero). "n/d" gracioso quando o ponto cai fora da malha ou a unidade nao tem dado suficiente.
  6. **Big Numbers** — grid 4x2 das 8 metricas (ver abaixo).
  7. **Realizacao/Credito** — fundo turquesa solido, texto fixo "Relatorio gerado pelo Motor de Expansao - Ultra Academia", atribuicao de tiles `(c) OpenStreetMap, (c) CARTO` e nota READ-ONLY. SEM PII.
  - `mapas` aceita o `dict[str,bytes]` das camadas combinadas OU, por retrocompat, `bytes` de um unico mapa legado (vai como camada `densidade` no grid 2x2 de Mapas de calor; as demais celulas caem no fallback textual). PNGs passados ao fpdf2 via `io.BytesIO`. Camada cuja chave nao esteja em `MAP_LAYER_TITLES` e descartada em SILENCIO por `_normalize_mapas`; a ordem dessa tupla NAO e a ordem das paginas (a composicao usa `layers.get(<chave>)`).
  - **Paginas OPCIONAIS (nao alteram a ordem base das 7):** `fotos` -> `_fotos_imovel_page` e `info_imovel` -> `_info_imovel_page` entram logo APOS a Capa (antes do slide-hero); `viabilidade` -> `_viabilidade_page` entra entre Big Numbers e Conclusao e vale 1 pagina ("Projeção de Viabilidade - Números", titulo renomeado em 2026-08-19) ou 2 (mais "Viabilidade - Projeção financeira") quando `viabilidade["graficos"]` traz PNGs, **mais a pagina "Conclusão"** (`_conclusao_page`), que entra logo depois delas e antes da Realizacao. A Conclusao sai sob `if viabilidade or conclusao_so_estudo:` — ou seja, tambem sem o payload quando o chamador pede o modo so-estudo, hoje somente `api/service.py` (ver secao dedicada abaixo). Teto com todas essas opcionais = **12 paginas** (`/Count 12`). A pagina de vista aerea (`foto_satelite`, DEC-018) tambem entra logo apos a Capa quando o PNG de satelite existe, e e contada a parte deste teto.

**BLK-RELPON-07 (slide "Perfil do Bairro/Distrito", entre Concorrentes e Big Numbers):**
- **D1** (unidade geografica): BAIRRO com fallback para DISTRITO; `nome_subdistrito` descartado (nao usado como unidade).
- **D2** (escopo): agrega TODOS os setores da unidade administrativa que CONTEM o ponto -- nao o raio de 1,0 km usado pelo resto do relatorio.
- **D3** (4 blocos com dado fiel): populacao, densidade demografica, domicilios, renda media. Faixa etaria, faixa de renda ABEP e PEA NAO entram (sem dado).
- **D3.5** (formula da renda media): `renda_media_domiciliar` = media de `renda_responsavel_media_setor_2022` PONDERADA por `domicilios_particulares_ocupados_setor_2022` (Metodo A, leitura GeoFusion "Renda Media" -- distinta da renda per capita usada nas outras 2 paginas do PDF). Exclusao SIMETRICA: um setor so entra no numerador E denominador se renda E domicilios forem nao-nulos E domicilios > 0; sem nenhum setor valido -> `None` ("n/d").
- `analisar_ponto_censitario_setores` (`censo_point.py`) ganhou 5 campos novos no `result`: `cod_bairro_ponto`, `nome_bairro_ponto`, `nome_distrito_ponto`, `unidade_ponto_tipo` (`"bairro"`/`"distrito"`/`None`, identificador cru) e `unidade_ponto_rotulo` (nome de exibicao ja com o fallback resolvido) -- leitura do MESMO setor que contem o ponto (reaproveita o lookup do BLK-RELPON-05).
- Novo helper `agregar_perfil_bairro_distrito(setores_df, *, cod_bairro=None, nome_bairro=None, nome_distrito=None, nome_municipio=None, uf=None)` (`censo_point.py`) agrega populacao/domicilios/area/renda sobre TODOS os setores da unidade resolvida; funcao pura, "n/d" gracioso (sem excecao) quando o identificador esta ausente, as colunas nao existem ou a unidade nao tem setores.
- `perfil_bairro=None` (default, em `gerar_pdf_relatorio_pontual_classico`/`gerar_payloads_download_relatorio_censitario`/`render_downloads_relatorio_censitario`) produz a pagina nova com texto "n/d" gracioso, sem excecao.
- **Nota de escopo (historico do BLK-RELPON-07):** na epoca, `src/motor_expansao/api/service.py` (endpoint `POST /analisar` com `formato=pdf`, DEC-005) chamava `gerar_pdf_relatorio_pontual_classico` DIRETAMENTE, sem passar `perfil_bairro` -- FORA do escopo daquele bloco: o PDF da API ganhou a pagina nova automaticamente (contagem 5->6 tambem la), mas com os 4 cards em "n/d". **Isso ja nao vale:** hoje `gerar_pdf_ponto` monta `result` + `perfil_bairro` + `hexes_df` (disco de 91 hexes, insumo do choropleth Residual do slide-hero) e gera o MESMO PDF de 7 paginas que o dashboard, com os 4 cards preenchidos. Diferencas que permanecem: (a) a API sobrescreve `street_ceil=215`, `street_gain=1.3`, `street_cap=200` e `choropleth_alpha=110` (o dashboard usa os defaults do modulo: 160/2.2/210/140); (b) a API renderiza os PNGs em 1000x760 (default do modulo), enquanto o expander do Mapa Territorial pede 1280x760 -- o outro caminho de download do dashboard usa os defaults 1000x760, como a API.
- **Big Numbers (8 metricas, READ-ONLY):** **populacao total no raio**, **renda per capita media**, **numero de domicilios no raio** (`domicilios_total_raio`, BLK-RELPON-08) e **renda media domiciliar** (`renda_domiciliar_total_raio`) (nessa ordem, linha 1) vem do `result` censitario; **SAM Fitness em alunos** (`sam_fitness_potencial`), **Residual Fitness em alunos** (`oferta_efetiva_disponivel`), **concorrentes no raio** (`n_concorrentes`) e **consumo de concorrentes** (`oferta_consumida_mercado_estimada`) vem de `lookup_hex_by_coord(lat, lng, df, h3_res=7)` lendo o `df` ja em escopo em `pages.py` — leitura pura, SEM load novo de parquet e SEM recalcular M1/residual. NB: SAM e Residual Fitness saem em **numero de alunos** (sizing absoluto da camada de mercado), nao em score. `score_setor_max` (antigo card "Score censitario maximo") SAIU do PDF a partir do BLK-RELPON-08 e `score_setor_medio` (antigo card "Score censitario medio") SAIU em 2026-07-17, cedendo o 4o card da linha 1 a renda media domiciliar -- os dois continuam calculados em `result` para auditoria em CSV, so nao aparecem mais como card. Campo ausente/NaN/hex-ausente -> **"n/d"** auditavel, com nota de fonte citando o metodo `setor_censitario_intersecao_area_1km`.
- `gerar_payloads_download_relatorio_censitario(...)` e `render_downloads_relatorio_censitario(...)`: preparam nomes/bytes e botoes `st.download_button`; propagam ao PDF os kwargs `residual`, `perfil_bairro`, `ultra_dir`, `solicitante`, `rotulo`, `fotos`, `info_imovel` e `viabilidade`, e escolhem a variante por `template`. **Desde o BLK-RELPON-14 os dois ramos produzem o MESMO PDF** (estetica classica, gerador unico): `"classico"` chama `gerar_pdf_relatorio_pontual_classico` direto e o ramo `else` continua passando pelo simbolo legado `gerar_pdf_relatorio_pontual_censitario` (hoje wrapper depreciado) so para nao quebrar quem faz spy/patch nesse nome — producao ja chama sempre com `template="classico"`.

Assets de branding (Fase 0):
- Dois fundos LIMPOS extraidos do `data/referencias/Teste Modelo.pptx` (zip Office) para `data/ultra/` (gitignored; ficam no host/volume `:ro`): `relatorio_capa_bg.png` (capa turquesa, de `ppt/media/image6.png`) e `relatorio_conteudo_bg.png` (fundo claro, de `ppt/media/image1.jpg` -> PNG). Otimizados com Pillow `optimize=True`.
- **Anti-PII (guardrail):** `ppt/media/image24.png` (cartao de contato com nome/telefone/e-mail reais) NUNCA e extraido, embutido ou versionado; o `.pptx` e qualquer PDF de saida NUNCA sao versionados. Teste `test_pdf_sem_pii_de_pessoas` e defesa em profundidade.
- **Fallback gracioso/offline-safe:** `_load_branding_assets` retorna `None` por asset em qualquer falha; o writer desenha entao fundo de cor solida (turquesa na capa, branco-gelo no conteudo). PDF valido SEM excecao mesmo em CI/checkout/deploy limpo (sem os assets). Geracao 100% offline (fpdf2 nao faz fetch; os tiles ficam so na geracao dos PNGs do BLK-CENSO-01/DEC-004).

Decisao operacional:
- `fpdf2` e pure-Python e offline; a geracao e sob demanda e retorna bytes, sem escrever artefatos permanentes.
- **Deploy:** adotar `fpdf2` exige rebuild da imagem Docker + redeploy por digest na VPS; os PNGs de `data/ultra/` (gitignored) precisam ser copiados ao volume `/opt/motor-expansao/data/ultra/` para o branding aparecer em producao (sem eles, o fallback solido garante PDF valido). Passo de OPS gated (guardrail §6).

**Consolidacao BLK-RELPON-01 (gate humano Vinicius 2026-07-01):** os 3 choropleths (Populacao/Renda/Score) — antes 1 pagina cada — foram consolidados em **UM slide "Mapas de calor"** (tira 1x3 lado a lado, sem sobreposicao, cada PNG embutido separadamente com sua legenda), levando o PDF de **7->5 paginas** (Capa->Mapas de calor->Concorrentes->Big Numbers->Realizacao), `/Count 7`->`/Count 5`, `PDF_SECTION_HEADERS` de 7->5 strings. Feito nas duas variantes (`censitario` e `classico`). READ-ONLY sobre o M1; metodo de intersecao/raio de entao (1,5 km)/score/artefatos oficiais INTOCADOS. Helpers `_map_page`/`_classico_map_page`/`_draw_map`/`_classico_draw_map` removidos (substituidos por `_mapas_calor_page`/`_classico_mapas_calor_page` + `_draw_maps_grid`/`_classico_draw_maps_grid` + `_map_grid_cells`).

**Insercao BLK-RELPON-07 (2026-07-15):** a pagina "Perfil do Bairro/Distrito" foi inserida entre Concorrentes e Big Numbers, levando o PDF de **5->6 paginas** (Capa->Mapas de calor->Concorrentes->Perfil do Bairro/Distrito->Big Numbers->Realizacao), `/Count 5`->`/Count 6`, `PDF_SECTION_HEADERS` de 5->6 strings. Feito nas duas variantes. Consequencia aceita da alternancia de tema por ordinal (`_tema_bicolor`): a cor de destaque da pagina Big Numbers muda de turquesa para magenta (era o 3o slide de conteudo, passa a ser o 4o); a nova pagina Perfil do Bairro/Distrito herda o turquesa que antes era de Big Numbers. Ver secao dedicada acima (§7 topo) para o contrato completo da pagina nova.

**Insercao BLK-RELPON-10 (gate Vinicius 2026-07-21):** o slide-hero "Socioeconomia e Residual Fitness" foi inserido ANTES de "Mapas de calor", levando o PDF de **6->7 paginas**, `/Count 6`->`/Count 7`, `PDF_SECTION_HEADERS` de 6->7 strings. Feito nas duas variantes. 2 mapas lado a lado via `_draw_maps_grid` generalizado com `cols`/`rows` KEYWORD-ONLY (default 2x2 preservado -> o caminho de "Mapas de calor" ficou byte-identico): `socioeconomia` = MESMO choropleth de `score_setor_2022_calibrado` no raio do relatorio (1,5 km a epoca) (o `score` PERMANECE no grid 2x2, S1=A do gate) e `residual` = `oferta_efetiva_disponivel` em ALUNOS por hexagono H3 res-7 no raio de EXIBICAO de 5 km (S2=B do gate; emenda a DEC-011, que limitava o residual a NUMERO nos Big Numbers). O slide usa `_tema_bicolor(0)` (magenta) e NAO inverte a cor das paginas ja existentes (ordinais decrescentes a partir de 0 para paginas inseridas antes das 4 paginas de conteudo historicas). **BLK-RELPON-10-FU1 (2026-07-22):** a camada Residual deixou de desenhar pins de concorrentes/Ultra e a legenda de pins (a 5 km a area e ~11x a do raio do relatorio e os logos tapavam o choropleth). Motor censitario (`setor_censitario_intersecao_area_1p5km`, raio 1,5 km a epoca) INTOCADO. **O BLK-RELPON-14 preservou o VALOR de 5 km** (`RAIO_RESIDUAL_DISPLAY_KM`, enquadramento) e removeu apenas a sua REPRESENTACAO — circulo e rotulo — ver abaixo.

**Insercao BLK-RELPON-11 (caminho A1, gate Vinicius 2026-07-22) — REVERTIDA pelo BLK-RELPON-14:** a pagina "Imagem do Entorno" foi inserida entre a Capa e o slide-hero, levando o PDF de **7->8 paginas**, e saiu por completo em 2026-07-28 (ver abaixo). Enquanto existiu: grid 1x1 empacotado com a camada `entorno`, tema `_tema_bicolor(-1)` = turquesa (`-1 % 2 == 1` em Python), nas duas variantes (`_entorno_page`/`_classico_entorno_page`).

**Remocao + unificacao BLK-RELPON-14 (2026-07-28):**
- **Slide "Imagem do Entorno" REMOVIDO por completo** — pagina do PDF, camada PNG `entorno`, constantes orfas (`RAIO_ENTORNO_DISPLAY_KM`, `_ENTORNO_VALOR_LINHA`, o parametro `rotulo_escala` de `_render_camada` e o override local de `zoom_bump`) e a chave `entorno` de `CAMADAS_CENSITARIAS`/`MAP_LAYER_TITLES`. O PDF volta de **8->7 paginas** (Capa->Socioeconomia e Residual Fitness->Mapas de calor->Concorrentes->Perfil do Bairro/Distrito->Big Numbers->Realizacao), `/Count 8`->`/Count 7`, `PDF_SECTION_HEADERS` de 8->7 strings, e o teto com as opcionais cai de **12->11** (`/Count 11`). Os ordinais de `_tema_bicolor` sao ABSOLUTOS (nao um contador incremental), entao liberar o `-1` que so servia a essa pagina **nao desloca cor nenhuma**: hero=magenta, mapas=turquesa, concorrentes=magenta, perfil=turquesa, big numbers=magenta, exatamente como antes.
- **Gerador UNIFICADO na estetica classica** — `gerar_pdf_relatorio_pontual_classico` e a implementacao unica; `gerar_pdf_relatorio_pontual_censitario` vira wrapper fino com `DeprecationWarning`. As paginas duplicadas do template "recente" (`_cover_page`, `_socioeconomia_residual_page`, `_mapas_calor_page`, `_competitors_page`, `_perfil_bairro_page`, `_credit_page`, `_entorno_page`) foram DELETADAS; `_big_numbers_page`, `_viabilidade_page`, `_fotos_imovel_page` e `_info_imovel_page` (compartilhadas desde sempre) FICAM.
- **Representacao do raio nos paineis de 5 km** — `RAIO_RESIDUAL_DISPLAY_KM = 5.0` **NAO MUDA** (segue definindo o ENQUADRAMENTO dos dois paineis; encolher reintroduziria o mosaico chapado que a DEC-011 documenta). O que saiu foi a REPRESENTACAO: circulo azul e rotulo "Raio X km" removidos, e o hexagono H3 res-7 que CONTEM o ponto ganhou uma borda fina de destaque. Ver §6 e a emenda de 2026-07-28 na DEC-011.
- Motor censitario (`setor_censitario_intersecao_area_1p5km`, raio 1,5 km a epoca), score e artefatos oficiais do M1 INTOCADOS — e mudanca de RENDER/estrutura de relatorio (§5 guardrail permanente).

**Correcao BLK-RELPON-13 (gate visual Vinicius 2026-07-23):** o painel `socioeconomia` do slide-hero deixou de ser o choropleth de setores a 1,5 km e passou a ser `score_setor_2022_calibrado` por **hexagono H3 res-7 num enquadramento de 5 km** (disco `h3.grid_disk` k=5, clip ao frame) — a MESMA metrica e a MESMA paleta (`score_band_to_color`, 0-100) do `score` do grid 2x2, mudando so a geometria/escala para casar com o `residual` ao lado. Implementado GENERALIZANDO o caminho do `residual` (`_render_camada_residual_hex`/`_hex_polygons_3857`/`_residual_hex_central` ganharam parametros keyword-only DEFAULT-PRESERVING para `value_col`/titulo/legenda/cor/valor central -> o `residual` fica byte-a-byte igual). Consequencia: `socioeconomia` virou **CONDICIONAL** como o `residual` (sem `hexes_df`/coluna/hex desenhavel -> chave ausente + fallback textual; SEM pins), e a faixa superior passou a usar `_legenda_valor_hex` ("Score no hexagono: NN"). As 2 imagens do slide sao reduzidas por `_HERO_MAP_SCALE` (constante de RENDER de `censo_report.py`, **0.85**, calibrada por Vinicius no gate visual de 2026-07-24 -- partiu de 0.92; so as paginas socioeconomia+residual a aplicam via `packed_scale` do `_draw_maps_grid`/`_map_grid_cells_packed`). `/Count 8`, as 8 chaves de `CAMADAS_CENSITARIAS` e as demais 7 camadas seguiam byte-identicas (numeros da epoca; hoje sao 7 paginas e 7 chaves — BLK-RELPON-14). Feito nas duas variantes. READ-ONLY sobre o M1: `score_setor_2022_calibrado` e apenas LIDO do `hexes_df`; motor censitario (`setor_censitario_intersecao_area_1km`; raio 1,5 km a epoca, hoje 1,0 km — DEC-021) INTOCADO.

**Refinamento visual BLK-EST-02 (tema "Ultra Clean / GeoFusion", gate Felipe 2026-06-11):** so estilo/geometria/strings — o grid **4x2 de 8 metricas** READ-ONLY, `set_compression(False)` e `pdf_version="1.4"` permanecem INTOCADOS (a contagem de paginas/ordem foi atualizada depois por BLK-RELPON-01, -07, -10, -11 e -14, acima; hoje sao 7 paginas).
- **Tipografia (D1=B):** capa titulo 30 pt; faixa de titulo de conteudo 22 pt (banda `band_h` 48->56, `set_xy(36,16)`); Realizacao 34/18/12 pt. Mantida a fonte core Helvetica (sem TTF/asset novo).
- **Cards Big Numbers (D2=B + D3=B):** rotulo em cinza-escuro `(45,45,45)` e valor grande em cinza-escuro `(40,40,40)` (acento so na barra do topo); `card_h=132` (`_BIG_NUMBERS_CARD_H`), `gap=12` (`_BIG_NUMBERS_GAP`), barra acento 6 pt, valor 26 pt, **borda fina** `(225,225,228)` via `rect(..., style="D")` apos o fill; rotulo em `y+20`, valor em `y+74` (offset proporcional ao card de 132 pt; era `y+88` no card de 156). As 8 metricas/ordem e a nota de fonte abaixo do grid seguem intactas.
- **Semaforo de cor de fundo dos cards (BLK-RELPON-08):** cada um dos 8 cards ganha um fundo pastel por meta -- verde `_CARD_VERDE_RGB=(205,236,217)` quando o valor bate a meta, vermelho `_CARD_VERMELHO_RGB=(248,209,209)` quando nao bate, cinza `_CARD_NEUTRO_RGB=(232,233,237)` quando o valor e "n/d" (indecidivel, sempre neutro, nunca reprovacao/aprovacao falsa). Metas nomeadas em `censo_report.py`: `_META_POP_TOTAL_RAIO=10000`, `_META_RENDA_PER_CAPITA_MEDIA_RAIO=1500`, `_META_DOMICILIOS_TOTAL_RAIO=3000`, `_META_RENDA_DOMICILIAR_TOTAL_RAIO=4000` (verde a partir de 4.000; baixado de 6.200 por Vinicius no gate visual de 2026-07-24), `_META_SAM_FITNESS_POTENCIAL=2000`, `_META_RESIDUAL_FITNESS_DISPONIVEL=2000` (`_META_SCORE_SETOR_MEDIO=60` continua definida no modulo, mas ficou SEM card desde a remocao do "Score censitario medio" em 2026-07-17). Helper puro `_cor_por_meta(valor, meta)` cobre os 6 cards de meta simples (`>=meta` -> verde). O card "Consumo concorrentes (est.)" usa regra assimetrica via `_cor_consumo_concorrentes(sam, residual_disponivel)`: vermelho SO quando o mercado ja esta consumido (SAM Fitness >= meta E Residual Fitness < meta), verde caso contrario; o card "Concorrentes no raio" ESPELHA essa mesma cor (sem meta propria). Linha 1 vigente: **Populacao total no raio -> Renda per capita media -> Numero de domicilios -> Renda media domiciliar**; os cards "Score censitario maximo" (BLK-RELPON-08) e "Score censitario medio" (2026-07-17) SAIRAM do PDF (campos `score_setor_max`/`score_setor_medio` continuam em `result`/CSV). Sao thresholds de DISPLAY locais a `censo_report.py`; nao alteram `flag_sam`/DEC-006/DEC-007 nem `sam_fitness_potencial`/`oferta_efetiva_disponivel` no pipeline de mercado.
- **Concorrentes (D4=B):** **bullet colorido por tipo** antes de cada linha (Ultra=turquesa, concorrente=magenta, `pdf.ellipse`); cabecalho com **contagem total** `"... (N no total)"` quando `total > 10` (`total = len(concorrentes_raio) + len(ultra_raio)`, guarda None/empty via `_safe_len`); linha `"... e mais {total-10}"` quando a lista trunca em 10. `_point_rows` passou a retornar `(texto, is_ultra)`; `name_col` segue restrito a colunas de UNIDADE (anti-PII).
- **Realizacao (D5=C):** metodo encurtado para 1 frase ("Intersecao de setores censitarios IBGE 2022 com circulo do raio (1,0 km); distribuicao intrassetor por area."); blocos de texto subidos. Nota READ-ONLY e atribuicao de tiles mantidas. Texto novo `_ascii()`-safe (latin-1). O logo Ultra no topo, previsto no D5=C, NAO esta no codigo atual: nem `_credit_page` nem `_classico_credit_page` desenham `assets["logo"]` (fundo turquesa solido + texto; a docstring da classica diz explicitamente "SEM logo").

**Pagina "Conclusão" (parecer do ponto, 2026-08-06; DOIS eixos desde a DEC-030, 2026-08-14):** ultima pagina de conteudo do relatorio, entre as de Viabilidade e a Realizacao. Da ao ponto **dois pareceres tri-estado independentes** — um DEMOGRAFICO (a praca, o que os Big Numbers medem) e um FINANCEIRO (o imovel e o retorno, o que as paginas de Informacoes do Imovel e Viabilidade medem) —, cada um `aprovado` / `com_ressalvas` / `reprovado` (valores BRUTOS, sem acento; a acentuacao vive so na camada de label do selo, `_CONCLUSAO_SELO_TEXTOS`), mais a lista de observacoes do que pesou contra, agrupada pelos mesmos dois eixos.

- **Nao existe status agregado (DEC-030, pedido de Vinicius em 2026-08-14).** A pagina nasceu em 2026-08-06 compondo num status UNICO os tres juizos dispersos que o repo tinha (semaforo por METRICA dos Big Numbers, veredito BINARIO `dre.flag_viavel`, parecer tri-estado de `rede_diagnostico` apontado para unidade MADURA). Esse status unico **acabou**: os dois eixos respondem perguntas diferentes e falham por motivos que nao se compensam — uma praca que nao sustenta a operacao nao vira aceitavel porque o imovel e' barato, e um imovel fora do envelope nao vira aceitavel porque a praca e' boa. O agregado ESCONDIA o conflito (colapsava no pior lado, e o leitor tinha de reconstruir pela lista de observacoes de onde vinha a reprovacao); com dois selos ele fica VISIVEL — verde de um lado, vermelho do outro, na mesma pagina. `_ConclusaoPonto` deixou de ter `status` **de proposito**, e `test_parecer_nao_expoe_status_agregado` impede o campo de voltar por conveniencia: um agregado sem consumidor de render seria lido como "o veredito", que e' a leitura abolida. Quem precisa de um juizo unico diz, no ponto de uso, qual eixo esta perguntando.
- **Regua (cortes aprovados por Vinicius em 2026-08-06; particao em eixos na DEC-030).** A gramatica por eixo continua a de `rede_diagnostico` ("1 grave OU N medios"): um gate ELIMINATORIO reprova **aquele eixo** sozinho, os demais so o rebaixam para "Aprovado com ressalvas"; eixo sem nenhum dos dois -> Aprovado.
  - **Eixo DEMOGRAFICO** (sempre avaliado): **E4** `flag_zona_morta`; **E5** N metas censitarias vermelhas ao mesmo tempo (so-estudo, ver abaixo); **R4** meta de Big Number nao atingida; **R7** mercado ja consumido; e a ressalva de censo indisponivel.
  - **Eixo FINANCEIRO** (fora no modo so-estudo): **E1** margem EBITDA abaixo da regua **E** payback acima dela (falha nos DOIS lados); **E2** aluguel pedido acima da 3a faixa de aluguel-teto (excecao, 30%); **E3** metragem `< AREA_MIN_M2`; **R1** falha em UM dos dois criterios de retorno; **R2** aluguel acima da 2a faixa (teto, 20%) e dentro da excecao; **R3** metragem fora de `AREA_IDEAL_MIN_M2..AREA_IDEAL_MAX_M2`; **R5** `flag_fora_envelope` (extrapolacao da base de calibracao); **R6** campo essencial do imovel nao informado (metragem e aluguel).
  - **E4 e' DEMOGRAFICO embora chegue pelo payload de viabilidade.** O flag e' levantado por `pop<5000` / `renda<500` na captacao (ver `_CONCLUSAO_MOTIVO_ZONA_MORTA`) — juizo sobre a PRACA, nao sobre o imovel nem sobre o retorno. **Ate' a DEC-042 (2026-08-28) este gate NUNCA disparava** no piloto: `_payload_viabilidade` chamava o motor sem `setores_df`, o catchment nao rodava e `flag_zona_morta` saia `None` em 100% das requisicoes. A mesma DEC derrubou o piso de renda de 1.600 para 500, porque no valor antigo o gate reprovava 46,3% da propria rede Ultra — 12 das vetadas operando ACIMA da mediana. Por isso ele sobrevive no modo so-estudo, onde o eixo financeiro inteiro nao existe. **Mudanca de comportamento declarada e aceita**, sem efeito no bot de producao: o caminho API/bot chama a pagina com `viabilidade=None`, entao `dados_viab` sai vazio e o gate nunca tem insumo (travado nas duas metades por `test_so_estudo_ainda_reprova_por_zona_morta_se_o_flag_chegar`). **R6** e' o unico item que mudou de casa por arrumacao, nao por semantica: saiu de `_avaliar_conclusao` para o fim de `_conclusao_eixo_financeiro`, que e' a mesma posicao relativa na ordem de leitura.
- **Pe-direito FORA da regua (Vinicius, 2026-08-07).** `PE_DIREITO_MIN` chegou a ser gate eliminatorio na primeira versao (era a estreia da constante, ate entao nunca comparada com nada) e foi RETIRADO: o campo volta a ser apenas digitado e impresso na pagina de informacoes do imovel. Saiu tambem de `_CONCLUSAO_CAMPOS_ESSENCIAIS` — cobrar preenchimento de um dado que nao decide nada so geraria ressalva sem consequencia.
- **Coerencia com o SELO da tela de Viabilidade.** O selo (`ViabilityCharts.Veredito`) e' BINARIO e sai de `dre.flag_viavel`; a Conclusao e' TRI-estado e olha o IMOVEL alem do cenario. Os dois **nao sao o mesmo juizo**, e a coerencia exigida tem duas metades, ambas travadas em teste por varredura da matriz de cenarios: **(A)** `flag_viavel=False` NUNCA vira "Aprovado" — se o motor diz que o cenario nao fecha, o parecer nao aprova (invariante forte, 0 violacoes na matriz); **(B)** `flag_viavel=True` PODE virar "Reprovado" por gate do IMOVEL (aluguel acima do maximo, metragem abaixo do minimo), mas **nunca em silencio** — sempre com eliminatorio escrito explicando (0 divergencias sem motivo). Ou seja: a Conclusao pode ser mais dura que o selo, jamais mais permissiva. **Desde a DEC-030 as duas metades apontam para o eixo FINANCEIRO**, nao para um status unico: o selo da tela julga o CENARIO, e exigir que um problema de PRACA aparecesse nele seria pedir de volta o colapso que a DEC-030 desfez. Nao e' afrouxamento — (A) sobrevive por CONSTRUCAO naquele eixo, porque o ramo final de `_conclusao_retorno` ("Cenário fora da régua de viabilidade da Ultra") rebaixa o financeiro mesmo quando margem e payback passam e o `flag_viavel` vem `False`; nenhum caminho o deixa verde. A metade nova e' a simetrica, `test_eixo_demografico_nao_herda_o_veredito_do_cenario`: um cenario inviavel com praca impecavel tem de sair com o selo demografico VERDE — antes o status unico o carimbava REPROVADO e a leitura da praca sumia junto.
- **"Mercado consumido" e RESSALVA, nao eliminatorio** (decisao de Vinicius, 2026-08-06, que reverteu a proposta original): saturacao e leitura de disputa, nao sentenca — um ponto com residual curto e economia sadia segue negociavel. Usa a MESMA regra de `_cor_consumo_concorrentes` (SAM >= meta E Residual < meta), e quando dispara **suprime** a linha R4 do card Residual, que diria a mesma coisa com menos contexto.
- **Primeira aplicacao real de `AREA_MIN_M2` / `AREA_IDEAL_MIN_M2` / `AREA_IDEAL_MAX_M2` / `PE_DIREITO_MIN`.** Eram canonicos declarados em `config.py` e travados em `tests/contracts/test_parametros_canonicos.py`, mas **nao comparados com nada em lugar nenhum** — o pe-direito era so um campo digitado e impresso. Sao os unicos parametros do `config.py` que a pagina le; nada do M1 e tocado.
- **INDECIDIVEL NUNCA REPROVA.** Dado ausente segue a regra ja estabelecida em `_cor_por_meta` (BLK-RELPON-08 D3/Q2): condicao indecidivel vira neutra, jamais falsa reprovacao. Todo eliminatorio so dispara com o dado em maos, e campo essencial faltando vira R6 ("o criterio correspondente nao pode ser avaliado"), nunca reprovacao. Payload legado **sem** as reguas (`margem_viavel_min` / `payback_viavel_max`) cai no `flag_viavel` pronto, que basta para ressalva e nunca para reprovar — sem os limites nao ha como saber se o cenario falhou em um criterio ou nos dois.
- **SEM FATURAMENTO, COM ALUGUEL-TETO** (pedido de Vinicius em 2026-08-06, ajustado no mesmo dia). A pagina **nao** imprime faturamento, EBITDA, investimento, VPL nem TIR, e as observacoes de retorno sao QUALITATIVAS — nem margem nem payback saem em numero. Invariante travado em teste com valores-sentinela. O **aluguel-teto ESTA impresso**, em card proprio, por decisao explicita tomada depois de a implicacao ser levantada e aceita: o teto e' 20% do faturamento bruto, entao quem conhecer a regra reconstroi o faturamento dividindo por `SIM_ALUGUEL_TETO_TETO` (0,20). O card mostra a faixa `teto` (2a, canonica), **nunca** a `excecao` — esta segue apenas como regua de E2. Os demais numeros sao FISICOS (metragem, pe-direito), de CUSTO DO IMOVEL (aluguel pedido, informado pelo operador) ou CENSITARIOS (populacao, domicilios, renda, SAM/residual em alunos).
- **Faixa do aluguel (2 cards):** "Aluguel-teto (mês)" com fundo neutro — e' referencia, nao meta que se atinge — e "Aluguel pedido (mês)" com semaforo proprio, `_cor_aluguel_pedido`: **verde** ate o teto, **ambar** dentro da excecao, **vermelho** acima dela, **neutro** sem dado. Espelha o `tetoCls` da `ViabilityScreen` com uma simplificacao deliberada: la sao 4 estados (a faixa `ideal` tem verde proprio), aqui `ideal` e `teto` compartilham o verde, porque a pagina nao imprime a faixa `ideal` e um quarto tom sem rotulo que o explicasse so confundiria. Faltando teto ou aluguel, o card cai em "n/d" gracioso **em vez de sumir** — a ausencia do dado tem de ficar visivel no parecer.
- **Estrutura em 2 COLUNAS (reestruturacao pedida por Vinicius, 2026-08-07; dois selos na DEC-030):** a DIREITA fica **so com os selos**, empilhados — demografico em cima, financeiro embaixo (`_CONCLUSAO_COL_DIR_W` = **210 pt**, era 240); a ESQUERDA (`_CONCLUSAO_COL_ESQ_W`, **652 pt**, era 622) ficam os **dois cards de aluguel lado a lado** e, abaixo deles, as **observacoes**. A largura da coluna esquerda e' DERIVADA por subtracao (`_CONCLUSAO_COL_DIR_X - margem - gap`), entao mexer na coluna do selo nunca deixa a soma estourar a pagina — foi essa derivacao que absorveu, sozinha, o estreitamento da coluna direita. O que a direita perde, a esquerda ganha, e e' ela que precisa: passou a comportar DOIS titulos de bloco alem dos cards. O card de status em largura total que existia antes SAIU, e com ele o dicionario `_CONCLUSAO_LABELS` ("Aprovado com ressalvas" etc.): a UNICA camada de label do status passa a ser `_CONCLUSAO_SELO_TEXTOS`.
- **Observacoes agrupadas por eixo (DEC-030).** O titulo unico "Observações" deu lugar a **dois titulos de bloco** — `"Praça (demográfico)"` e `"Imóvel e retorno (financeiro)"` (`_CONCLUSAO_BLOCO_TITULOS`) —, na MESMA ordem dos selos, para a coluna esquerda ser lida de par com a direita. Sem eles o leitor veria dois carimbos e uma lista unica, sem saber qual apontamento pesou em qual. Cada eixo sem apontamento nenhum recebe o SEU card de confirmacao: o texto unico anterior citava envelope do imovel E metas censitarias E criterios de retorno na mesma frase e, sob dois selos, seria falso dos dois lados (`_CONCLUSAO_APROVADO_TEXTO_DEMOGRAFICO` / `_CONCLUSAO_APROVADO_TEXTO_FINANCEIRO`). O respiro antes do 2o titulo (`_CONCLUSAO_BLOCO_GAP` = 14 pt) fica entre as duas reguas que a pagina ja usava: 6 pt entre cards irmaos e 22 entre blocos de natureza diferente — com so os 6, o titulo colava no ultimo card do bloco anterior e os dois blocos liam como uma lista so.
- **Centralizacao VERTICAL (mesmo pedido):** as duas colunas sao centradas na area entre a banda de titulo e a nota metodologica (`_CONCLUSAO_AREA_TOPO` .. `_CONCLUSAO_AREA_BASE`), **cada uma no seu proprio eixo** — antes tudo nascia colado no topo e sobrava um vazio grande embaixo em quase todo relatorio. Conteudo mais alto que a area comeca no TOPO (`max(0, ...)`) em vez de subir acima da banda de titulo, e a guarda de altura corta o excedente com aviso.
- **Cada observacao e' um CARD** (mesmo pedido: em bullet de texto solto elas ficavam discretas demais e davam a impressao de que so o valor tinha decidido o parecer). Fundo + borda fina + barra de acento no topo, a mesma receita dos cards de valor, mas aqui a cor da barra e' **semantica**. `_CONCLUSAO_OBS_CORES` mapeia severidade -> (fundo, acento), com hierarquia deliberada: **eliminatorio** leva fundo tingido (`_CARD_VERMELHO_RGB`) e salta; **ressalva** fica no cinza neutro dos demais cards com a cor so na barra — com 8+ apontamentos, tingir todos deixaria a pagina inteira vermelha e nada saltaria; **confirmacao** (parecer limpo) usa o verde. A altura de cada card e' medida ANTES de desenhar, por `multi_cell(..., dry_run=True, output="LINES")`: sem isso nao daria para centralizar o bloco, porque a altura so seria conhecida depois de desenhar.
- **Truncagem reparte o espaco ENTRE os blocos** (`_conclusao_plano_elementos`, funcao pura; DEC-030). Defeito encontrado na revisao visual do proprio bloco: preenchendo a coluna na ordem, um ponto ruim em tudo (10 apontamentos para ~6 vagas) a enchia inteira com o bloco demografico e o bloco financeiro **nao entrava — nem o titulo**. O selo financeiro ficava carimbado REPROVADO ao lado de uma coluna que nao trazia UMA linha sobre o financeiro: e' o "reprova, mas nunca em silencio" prometido desde 2026-08-06, quebrado pela unica via que a lista unica nao tinha, a truncagem. A selecao passou a ser **por rodadas**: cada bloco entra com o seu primeiro elemento antes de qualquer bloco pegar o segundo e, como o primeiro elemento de um bloco e' o que carrega o titulo e a ordem interna poe os eliminatorios na frente, **o que cada selo tem de mais grave aparece antes de qualquer ressalva do outro**. Bloco que nao caiba mais e' pulado sem travar os demais, entao um bloco de cards altos nao bloqueia um bloco de card curto que ainda caberia. O titulo e' modelado como PARTE do primeiro card do bloco (`_ConclusaoElemento.altura_titulo`), o que tem dois efeitos: `_conclusao_quantos_cabem` / `_conclusao_plano_observacoes` seguem INTACTAS (continuam vendo uma sequencia de alturas com gap uniforme) e nunca sobra titulo orfao anunciando bloco vazio — titulo e primeiro card cabem juntos ou nenhum dos dois cabe.
- **Corte com aviso reservado** (`_conclusao_plano_observacoes`, funcao pura): quando nem todos os cards cabem, a contagem e' REFEITA com a altura da linha de aviso **e o gap** ja descontados. Sem essa segunda passada o loop enchia a coluna ate o limite e o `"(+N nao exibido(s))"` era escrito **por cima** do ultimo card. Descontar so o aviso, sem o gap, ainda estourava a area em ate `_CONCLUSAO_OBS_GAP` pt — `_conclusao_quantos_cabem` garante que o ultimo CARD termina no limite, mas o aviso comeca um gap depois dele. Travado por `test_plano_reserva_espaco_para_o_aviso_de_truncamento`, **parametrizado em varias alturas**: com uma altura so o teste passava mesmo com a correcao removida (para muitas alturas a reserva e' no-op), e `test_reserva_do_aviso_nao_e_no_op` garante que existe altura discriminante.
- **Correcoes da revisao adversarial de 2026-08-07** (todas com teste proprio):
  - **Censo indisponivel nao aprova.** Ponto com ZERO setor no raio (ponto no mar, particao geo ausente, borda de malha) tem as 4 metricas censitarias em `None` e saia **"Aprovado"** com o texto de confirmacao AFIRMANDO que atendia "as metas censitarias do raio" — que nunca foram avaliadas. E' o reverso do invariante de indecidivel: falta de dado nao reprova, mas tambem nao pode APROVAR. Agora vira a ressalva "Metas censitárias não avaliadas: nenhum setor censitário no raio do ponto." (rebaixa, jamais reprova). So dispara com as QUATRO ausentes — uma metrica faltando e' outro caso.
  - **Gate e card de aluguel leem o MESMO par de faixas** (`_conclusao_faixas_aluguel`). Com payload de `aluguel_teto` escalar e sem `aluguel_teto_faixas` — forma legada que `_viab_normalizado` aceita de proposito — o gate ficava MUDO enquanto o card ao lado pintava vermelho na mesma pagina. Mesma classe de divergencia que o FIN-VIAB-01 combateu entre PDF e tela.
  - **`motivo_zona_morta` traduzido TOKEN A TOKEN** (`_conclusao_motivo_zona_morta`). `viabilidade_ponto` junta os motivos com `"; "` quando populacao E renda estao abaixo do piso, entao o caso MAIS grave era o unico que nunca traduzia e saia `"pop<5000; renda<500"` cru no PDF. Desde a DEC-042 o dicionario DERIVA dos limiares (`_motivos_zona_morta_traduzidos`): repetir o numero literal fazia toda recalibracao de piso quebrar a traducao em silencio.
  - **Flags por `_conclusao_flag`, nao truthiness.** `NaN` e' truthy em Python: um `flag_zona_morta` corrompido REPROVAVA o ponto, contra o invariante declarado no proprio cabecalho da secao.
  - **Densidade dos cards de observacao** (`_CONCLUSAO_OBS_PAD_Y` 8→5, `_CONCLUSAO_OBS_GAP` 8→6): a coluna comportava 6 cards e o parecer de um ponto ruim tipico tem ~10 apontamentos — 4 caiam no "(+N)" justamente onde a leitura mais importa. Agora cabem 8.
- **Selos (dois, empilhados):** mesma anatomia do carimbo de veredito da tela de Viabilidade (`ViabilityCharts.Veredito`, escolha de Felipe em 2026-07-31 — "bater o olho na tela e ja ter o veredito"), portada para o PDF e estendida de 2 para **3** estados. Quadrado de **210x176** (era 240x196 quando havia um so) com **cantos arredondados** (`_CONCLUSAO_SELO_RAIO` = 13, via `round_corners=True` do `rect` — um pouco menor que `_CLASSICO_CORNER_RADIUS`=16 porque a borda aqui tem 2 pt e o raio da banda classica achataria o traco nas quinas). Os **cards seguem retos**, como todos os outros do relatorio: o arredondamento e' o que marca o selo como elemento a parte. Fundo no pastel `_CARD_*_RGB` que o resto do relatorio ja usa, borda de 2 pt e simbolo/texto na cor SOLIDA de `_CONCLUSAO_SELO_RGB` (os pasteis nao teriam contraste como traco nem como tipografia).
  - **LEGENDA no topo** (`_CONCLUSAO_SELO_LEGENDAS`: `DEMOGRÁFICO` / `FINANCEIRO`) — unico acrescimo a anatomia de 2026-08-07, e obrigatorio: com dois carimbos identicos em forma na mesma coluna, sem ela nao ha como saber qual eixo cada um julga. E' a UNICA parte do selo que nao escala (9 pt ja e' a menor tipografia da pagina).
  - **Rotulo em CAIXA ALTA mais uma linha de apoio, esta POR EIXO.** O rotulo principal e' o mesmo nos dois — e' o status, e status nao muda de nome conforme o eixo. Quem diferencia e' o apoio, que diz a consequencia daquele eixo: o FINANCEIRO conserva os textos de 2026-08-07, que espelham a tela (`APROVADO / PARA COMITÊ`, `COM RESSALVAS / REQUER REVISÃO`, `REPROVADO / FORA DA RÉGUA`); o DEMOGRAFICO estreia os seus, que falam da praca e nao prometem rito nenhum (`PRAÇA SUSTENTA`, `PRAÇA COM RISCO`, `PRAÇA NÃO SUSTENTA`). Sao esses pares — unicos por (eixo, estado), ao contrario do rotulo principal — que permitem aos testes afirmar QUAL selo carimbou o que.
  - **A excecao do so-estudo virou ESTRUTURAL e deixou de existir como codigo.** O pedido de Juan (2026-08-12) era esconder `PARA COMITÊ` no PDF do bot, para nao prometer um rito sem base. Com a DEC-030 o rito de comite pertence ao eixo FINANCEIRO, e o so-estudo nao desenha esse selo: o parecer do bot nao tem mais como prometer o que nao pode disparar, **por construcao**. Sairam `_CONCLUSAO_SELO_APOIO_OCULTO_NO_ESTUDO` e o ramo `0.55`/`0.62` do rotulo (todo estado tem apoio agora). `test_selo_aprovado_so_estudo_sem_mencao_a_comite` fica guardando o invariante.
  - **Geometria interna em FRACAO da altura** (`_CONCLUSAO_SELO_*_Y` / `_*_TAM` / `_*_H`, escala de corpo por `altura / _CONCLUSAO_SELO_H_REF`). Antes eram absolutos — simbolo 48 pt, celulas 22/14, corpos 17/9,5 — calibrados para a altura unica de 196. **Duas mudancas de naturezas diferentes, e so a primeira e' inocua** (a redacao anterior desta linha afirmava que as duas eram, e a revisao adversarial do bloco mostrou que nao): **(a)** os TAMANHOS viraram fracao e reproduzem o desenho de 2026-08-07 em `altura = 196` (48/196 = 0,245; 22/196 = 0,112; 14/196 = 0,0714) — parametrizacao pura, sem a qual o simbolo fixo de 48 pt ficaria desproporcional num selo menor cujos rotulos encolheram; **(b)** as POSICOES foram REANCORADAS e mudam o desenho mesmo em 196 (simbolo 0,30 -> 0,335; rotulo 0,55 -> 0,60; apoio 0,74 -> 0,78 — em 196, 6,9 / 9,8 / 7,8 pt mais baixos). O motivo e' a LEGENDA, elemento que o desenho de 2026-08-07 nao tinha: com as posicoes antigas em 176 ela ficaria a 4,5 pt do simbolo; descendo o conjunto, o respiro vai a 13,1 pt e a area morta no pe cai de 33,2 para 26,2 pt. Um selo de quatro niveis nao pode "reproduzir exatamente" um de tres. (Registro, porque a justificativa anterior citava um numero errado: com a geometria antiga em 176 o simbolo **nao** termina a ~5 pt do rotulo — o gap real seria 18,85 pt no pior caso, o "!" do estado intermediario, e 28,16 pt no check.) As oito fracoes estao travadas em `test_geometria_interna_do_selo`. Raio e borda seguem ABSOLUTOS de proposito: escalados, o selo menor pareceria um card arredondado em vez do mesmo carimbo em outro tamanho. O rotulo principal passa por `_ajustar_fonte_para_largura` porque "COM RESSALVAS" e' bem mais largo que "APROVADO" e nao pode vazar a borda (medido: 133 pt dos 194 uteis, folgado).
  - **Por que 176 e nao 196:** a area util da coluna e' de `_CONCLUSAO_AREA_BASE - _CONCLUSAO_AREA_TOPO` = **400 pt**, e `2 x 196 + gap` estoura isso em 8 a 16 pt. Com `auto_page_break` OFF o excedente **nao** vaza para a pagina seguinte: sai por baixo do rodape, em silencio — a mesma armadilha que `_CONCLUSAO_OBS_LIMITE_Y` documenta para as observacoes, e que a coluna direita nunca precisou enfrentar com um selo so (folga de 102 pt). `2 x 176 + 20` (`_CONCLUSAO_SELO_GAP`) = 372 deixa **28 pt** de folga, 14 de cada lado. Travado por `test_os_dois_selos_cabem_na_area_util`, que e' a guarda que a coluna direita passou a precisar.
- **Simbolos desenhados em VETOR, nao como glifo** (`_conclusao_simbolo`): check, exclamacao e X saem de `line`/`rect`. O selo da tela usa `✓` e `✕`, ambos **fora do latin-1** — no core font do fpdf2 sairiam como `"?"` em silencio, exatamente a armadilha que a regra de acentuacao do projeto descreve. Em vetor os tres ainda ganham o mesmo peso otico; a haste do `!` e' ~1,8x a espessura das linhas dos outros dois DE PROPOSITO, porque ocupando so uma faixa vertical estreita ele pesava menos e o selo intermediario parecia mais fraco.
- **Fonte das reguas de retorno:** lidas do PROPRIO payload (`premissas.margem_viavel_min` / `premissas.payback_viavel_max`, que o backend serve a partir de `dimensionamento/config.py`) e expostas por `_viab_normalizado`. A pagina **nao importa `dimensionamento`** nem crava 0,30/36 por conta propria — e a mesma fronteira de RENDER PURO do FIN-VIAB-01 que impediu o PDF de divergir da tela. `payback` nulo ou infinito significa "nao paga no horizonte" (o que `_viab_payback` ja imprime como "> 60 meses"), portanto FALHA conhecida, nao dado ausente.
- **Condicao de entrada:** `if viabilidade or conclusao_so_estudo:`. Ate 2026-08-10 valia so `if viabilidade:`, o que deixava a **API e o bot Telegram** (que nao mandam `viabilidade`; ver `api/service.py`) sem parecer nenhum. O BLK-CONC-ESTUDO acrescentou o kwarg **`conclusao_so_estudo`** (default `False`), ligado em **um unico ponto do sistema**: `api/service.py:gerar_pdf_ponto`. Consequencia do escopo fechado por Juan em 2026-08-12: o **piloto web** chama a mesma funcao SEM o flag, entao quando o operador nao preenche a Viabilidade o relatorio dele sai com as **7 paginas de antes**, sem pagina de Conclusao — a mudanca fica confinada ao caminho da API/bot. Presente o payload, o flag e' INERTE (travado por `test_flag_e_inerte_quando_ha_viabilidade`, que compara os bytes). O payload decide o **MODO**:
  - **completo** (`viabilidade` presente — piloto web): regua inteira, com os dois cards de aluguel. Byte-a-byte o de antes.
  - **so-estudo** (`viabilidade` ausente — API e bot): `_avaliar_conclusao(..., somente_estudo=True)` avalia **so o eixo DEMOGRAFICO** (metas censitarias do raio, mercado do hexagono, zona morta e a ressalva de censo indisponivel). O eixo financeiro (`_conclusao_eixo_financeiro`: retorno E1/R1, envelope fisico E3/R3, aluguel E2/R2, extrapolacao R5 e campos essenciais R6) fica **FORA da regua** — nao "sem dado" —, e os cards de aluguel nao sao desenhados. Texto de aprovacao e nota metodologica trocam por `_CONCLUSAO_APROVADO_TEXTO_ESTUDO` / `_CONCLUSAO_NOTA_ESTUDO`, que descrevem a regua que de fato rodou. **Desde a DEC-030 isso tem consequencia VISUAL direta:** `parecer.financeiro is None` e a pagina desenha **um selo so**, centrado, com o bloco de observacoes da praca ao lado. `None` e' o unico valor que diz "este eixo nao existe neste parecer" — um eixo vazio sairia "Aprovado" e carimbaria verde o que ninguem avaliou (`test_so_estudo_nao_tem_eixo_financeiro`, `test_so_estudo_nao_desenha_o_selo_financeiro`).
- **Gate E5 — metas censitarias em BLOCO (corte de Juan, 2026-08-11).** Ate aqui nenhum criterio da praca era eliminatorio, e o parecer da API/bot parava sempre em `com_ressalvas`. O E5 fecha isso: **`_CONCLUSAO_METAS_ELIMINATORIO_MIN` = 4** das **6** metas dos Big Numbers vermelhas AO MESMO TEMPO valem um eliminatorio ("a praca nao sustenta a operacao"), na mesma gramatica "1 grave OU N medios" herdada de `rede_diagnostico`. Detalhes que importam:
  - **Vale nos DOIS modos** desde a emenda da DEC-030 (Vinicius, 2026-08-14). Nasceu preso ao so-estudo (escopo fechado por Juan em 2026-08-12: "a mudanca e' do PDF do bot, e so dele"), e a divergencia que isso criava era declarada e ACEITA — um ponto com 4+ metas vermelhas e financeiro bom saia `reprovado` no PDF do bot e `com_ressalvas` no do piloto. **O que mudou nao foi o corte, foi o que a divergencia significa:** enquanto o parecer era um veredito unico, ela ficava diluida num status que misturava praca e financeiro; com o selo demografico proprio, o MESMO ponto passa a exibir um carimbo de cor diferente em dois documentos, e a leitura da praca nao pode depender de por qual porta o relatorio saiu. **Custo medido antes de estender:** os 5 pontos do golden do Recife falham no maximo **1** meta contra corte **4** — a calibracao de Vinicius nao se move (era exatamente o que `test_e5_nao_desloca_o_golden_do_recife` existia para garantir). O corte **nao foi remedido nem alterado**; so o escopo. `test_regua_demografica_e_identica_nos_dois_modos` varre os cortes 0..4 exigindo parecer identico da praca nos dois modos, entao a assimetria nao volta por uma borda. A nota metodologica do modo completo passou a declarar o gate (`_CONCLUSAO_NOTA`), senao o piloto reprovaria a praca sem dizer por qual regua.
  - **Conta a lista CRUA** de `_conclusao_metas_vermelhas`, nao a exibida. A linha do Residual e' suprimida da exibicao quando "mercado ja consumido" ja a disse com mais contexto; descontar isso do gate faria o mesmo ponto reprovar ou nao conforme uma decisao de TEXTO.
  - **Calibracao do corte:** medido em 40 pontos reais de SP sorteados entre hexes povoados — corte 3 reprovaria 65% deles ("Reprovado" viraria a resposta padrao do bot), corte 4 reprova 60% da MESMA amostra, que puxa para cidade pequena porque as metas sao ABSOLUTAS e calibradas para o raio de 1,0 km urbano. Mexer no corte pede remedir.
  - **Guardrail:** o golden dos 5 pontos do Recife (calibracao de Vinicius) falha no **maximo 1 meta por ponto**, muito abaixo do corte. O gate ja esta preso ao so-estudo, mas `test_e5_nao_desloca_o_golden_do_recife` fica como 2a linha de defesa: se alguem estender o E5 ao modo completo, a calibracao continua de pe.
- **Render:** ordinal 5 de `_tema_bicolor` (o primeiro LIVRE; como os ordinais sao ABSOLUTOS, tomar o 5 nao desloca a cor de nenhuma pagina existente). Fundos por `_CONCLUSAO_CORES` — verde/vermelho reusam `_CARD_VERDE_RGB`/`_CARD_VERMELHO_RGB` e o intermediario estreia `_CARD_AMBAR_RGB=(250,233,195)`, da mesma familia pastel e distinguivel tambem em P&B. A area de observacoes tem **guarda de altura** (`_CONCLUSAO_OBS_LIMITE_Y`): a pagina e FIXA com `auto_page_break` OFF, entao texto que nao cabe NAO vaza para a proxima — sumiria por baixo do rodape. O excedente sai contado numa linha "(+N apontamento(s) nao exibido(s))", nunca truncado em silencio. A nota metodologica final volta a ocupar a largura TOTAL: ela fala da pagina inteira, e presa a coluna esquerda ganharia varias linhas. Ela descreve **os dois eixos** desde a DEC-030 e declara explicitamente que **nao ha veredito unico** — sem isso o leitor tenderia a compor mentalmente os dois selos num terceiro que a pagina nao emite.
- **Calibracao:** travada por golden dos **5 pontos reais do Recife** que a originaram, hoje com o **par** `(demografico, financeiro)` por ponto. A coluna FINANCEIRA reproduz, ponto a ponto, os 5 status que Vinicius aprovou em 2026-08-06 (**1 aprovado / 2 com ressalvas / 2 reprovados**) — o golden original era, na pratica, um golden desse eixo: os 5 reprovam e ressalvam por metragem, aluguel e retorno. A coluna DEMOGRAFICA (**4 / 1 / 0**) e' o que a separacao revelou: so Rosa e Silva, com residual zero contra SAM de 18.148, sai com ressalva na praca. Ou seja, a DEC-030 **nao reabriu a calibracao** — mostrou de onde vinham os vereditos. Mudar um corte sem reavaliar esses 5 quebra o teste de proposito, e a 2a contagem denuncia deslocamento do lado da praca (cujas metas absolutas sao decisao de Felipe, DEC-021).
- READ-ONLY sobre o M1: nao recalcula `score_priorizacao`, carteira, plano nem artefatos oficiais. E' RENDER + leitura.

Validacao minima: `tests/unit/test_relatorio_pontual_censitario_export.py` cobre bytes CSV/PDF, os **7** headers de secao, `/Count 7`, as imagens de mapa embutidas no PDF (**socioeconomia/residual + os 4 choropleths do grid 2x2 "Mapas de calor" + a de Concorrentes so-pins**, separadamente e nao pre-compostas, via `/Subtype /Image >= 5`), Big Numbers com/sem residual ("n/d"), fallback offline sem assets, anti-PII, atribuicao de tiles, retrocompat de `bytes` unico e helper de download; os testes `test_pdf_concorrentes_contagem_total_e_mais_n_quando_excede_10` e `test_pdf_concorrentes_sem_contagem_quando_ate_10` cobrem o cabecalho/contagem D4=B do BLK-EST-02 (sem PII). **BLK-RELPON-01** adiciona `test_slide_unico_quatro_mapas_sem_sobreposicao` (bounding boxes das celulas do grid, recente + classico), o assert de `/Count` + titulo do slide "Mapas de calor" (o nome desse teste carrega a contagem vigente de paginas), `test_slide_unico_offline_safe_por_camada` e `test_slide_unico_quatro_imagens_embutidas`. **BLK-RELPON-05** nao muda logica em `censo_report.py` (a faixa ja vem "assada" nos PNGs recebidos como `mapas: dict[str, bytes]`); `test_pdf_estrutura_inalterada_com_faixa_valor_ponto_blk_relpon_05` reforca que a contagem de paginas, a de imagens e os headers permanecem identicos. **BLK-RELPON-07** adiciona `test_perfil_bairro_page_presente_com_4_metricas_recente`, `test_perfil_bairro_page_nd_quando_perfil_bairro_none` e `test_classico_perfil_bairro_page_presente_e_nd` (nova pagina "Perfil do Bairro/Distrito", com/sem `perfil_bairro`), alem de atualizar os 13 asserts `/Count 5`->`/Count 6` e as 2 contagens de marca d'agua `>= 5`->`>= 6` nos testes existentes (5->6 paginas); em `tests/unit/test_relatorio_pontual_censitario_motor.py`, `test_lookup_bairro_ponto_quando_setor_tem_bairro`, `test_lookup_distrito_ponto_fallback_quando_bairro_ausente` e os testes de `agregar_perfil_bairro_distrito` (agregacao, fallback, exclusao simetrica de renda, "n/d" sem identificador/DataFrame vazio) cobrem o motor novo. **BLK-RELPON-10** adiciona o slide-hero (presenca, `offline-safe` sem a camada residual, `_tema_bicolor` ordinal 0 = magenta sem mexer nos existentes, `MAP_LAYER_TITLES` com as chaves novas e o grid aceitando 2x1/1x1 com default 2x2). **BLK-RELPON-11** adicionou a bateria da pagina "Imagem do Entorno", **REMOVIDA** no BLK-RELPON-14 junto com a pagina. **BLK-RELPON-14** atualiza os asserts de estrutura para `/Count 7` + 7 headers e a marca d'agua para `>= 7`, e cobre: a ausencia do header "Imagem do Entorno" e da chave `entorno` (descartada em silencio por `_normalize_mapas` quando alguem ainda a passa), a equivalencia entre o wrapper depreciado e a classica (mesmo numero de paginas + `DeprecationWarning` emitido) e os dois ramos de `template` produzindo o mesmo PDF. O teto com as opcionais, hoje **12 paginas** (era 11 antes da pagina de Conclusao), e travado em `tests/unit/test_relatorio_pontual_viabilidade.py` e `tests/unit/test_relatorio_pontual_orquestracao.py` (`/Count 12`). A **pagina de Conclusao** tem bateria propria em `tests/unit/test_relatorio_pontual_conclusao.py` (**125 casos** apos a DEC-030): um por gate com as bordas inclusivas (`>=` meta, teto exato, faixa ideal), os invariantes "indecidivel nunca reprova" e "zero numero de faturamento" (por valores-sentinela), a insercao no PDF com a Realizacao permanecendo a ultima pagina e o golden dos 5 pontos do Recife. **DEC-030** nao mexeu em nenhum assert de estrutura (a pagina segue UMA, entao os `/Count` de todos os arquivos vizinhos ficaram intactos), mas cada teste de gate passou a assertar o status do EIXO que aquele gate alimenta — e' isso que prova a particao: um gate financeiro que sujasse o selo demografico (ou o contrario) quebra ali, mesmo que o apontamento saia escrito na pagina do mesmo jeito. Acrescentou: ausencia de status agregado, `financeiro is None` no so-estudo, zona morta reprovando a praca e nao o financeiro, os dois selos carimbando eixos independentes nos bytes (pelas linhas de apoio, unicas por eixo+estado), legendas e titulos de bloco no PDF, a truncagem repartida entre blocos (com o caso de 10 apontamentos que expos o defeito), a guarda de altura dos dois selos e a equivalencia da geometria interna na altura de referencia. Fechou tambem um **buraco de cobertura anterior**: `test_relatorio_acentuacao_regressao` gera os PDFs sem `viabilidade` e sem `conclusao_so_estudo`, entao a pagina de Conclusao nunca e' renderizada la, e o teste de latin-1 daqui so percorria eliminatorios/ressalvas — os **rotulos do selo nao eram auditados por ninguem**; agora sao (`test_textos_de_exibicao_do_selo_nao_usam_caractere_fora_de_latin1`).

**Cinco testes da propria DEC-030 foram reescritos apos revisao adversarial**, por nao travarem o que a docstring prometia — e a correcao de cada um foi provada por MUTACAO (patch em runtime, repo intocado): o teste tem de derrubar a quebra que ele existe para pegar. Os casos, porque a classe de defeito se repete: (a) `test_legendas_identificam_os_dois_selos_no_pdf` era tautologico — `DEMOGRÁFICO`/`FINANCEIRO` tambem saem da NOTA metodologica na mesma pagina, entao apagar as duas legendas dos selos deixava a suite verde; passou a assertar CONTAGEM (2 de cada = selo + nota); (b) `_ConclusaoElemento.altura` nao era confrontada com o avanco que `_conclusao_page` faz por conta propria — devolver so `altura_card` orcava 62 pt a menos e a suite nao via; (c) `_conclusao_elementos` nao tinha teste, e `titulo_offset=0.0` apagava na pratica o `_CONCLUSAO_BLOCO_GAP`; (d) o rotulo principal do selo FINANCEIRO nao tinha cobertura, porque no PDF inteiro `APROVADO` ja vinha do selo demografico — virou golden LITERAL com o selo renderizado isolado (`_bytes_do_selo`), depois de a primeira correcao falhar pelo mesmo defeito que corrigia: ela lia o rotulo esperado do dicionario que a mutacao alterava; (e) `test_eixo_demografico_nao_herda_o_veredito_do_cenario` era um `> 0` que sobrevivia a reunificacao PARCIAL dos eixos (concatenar so os eliminatorios financeiros deixava 144 dos 384 cenarios ainda aprovados) — passou a exigir DISJUNCAO dos apontamentos nos 4.096 cenarios. `test_truncagem_reparte_o_espaco_entre_os_blocos` tambem so verificava o prefixo do 1o bloco, e agora verifica os dois. **BLK-CONC-ESTUDO** NAO mexeu em nenhum assert de estrutura dos testes existentes — o default do gerador ficou intocado, e por isso `/Count 7` e os 7 headers seguem valendo. Acrescentou: `test_sem_viabilidade_e_sem_flag_nao_ha_pagina_de_conclusao` (o caminho do piloto web, provando que 7 paginas continuam 7), `test_sem_viabilidade_com_flag_a_pagina_entra_em_modo_so_estudo` (o caminho API/bot), `test_flag_e_inerte_quando_ha_viabilidade` (bytes IDENTICOS com e sem o flag quando o payload existe) e a bateria do modo so-estudo: gates de imovel/retorno desligados em bloco, metas censitarias e mercado preservados, ausencia dos cards de aluguel, nota metodologica propria, selo sem "PARA COMITÊ" (contra `test_com_viabilidade_...`, que prova a manutencao no modo completo) e o gate **E5** (bordas 0/1/2/3 nao reprovam, 4 reprova, contagem sobre a lista crua, constante de total batendo com a funcao real, NAO valendo no modo completo e o golden do Recife intocado).

### Implementacao do Bloco 6

O fluxo visual foi integrado ao dashboard em `render_relatorio_pontual_censitario`, dentro do expander `Relatorio Pontual Censitario` na aba `Mapa Territorial`. Ele reutiliza a coordenada ativa do clique no mapa ou da busca da sidebar, mantem raio fixo `1.0 km`, exibe KPIs, as 4 imagens censitarias em grade 2x2 (densidade/renda/score/concorrentes) SEM seletor de metrica, tabela de setores e botoes de download CSV/PDF (o download usa `template="classico"`, o default de producao).

A carga operacional usa `read_censo_geo_partition` em `dashboard/data.py` (na epoca havia tambem o wrapper cacheado `load_censo_geo_setores` no `streamlit_app.py`, que saiu com o Streamlit — DEC-022). O caminho preferencial le apenas `data/outputs/setores_censitarios_2022_geo/uf=XX/cod_municipio=NNNNNNN`; se a base municipal nao existir, a UI mostra mensagem clara e nao tenta carregar shapefile nacional.

Baseline simples DF/Brasilia: `5.418` setores carregados, `34` setores intersectados, leitura `~0,04s`, analise `~0,16s`, pico Python via `tracemalloc` `~6,3 MB`.

## 8. Qualidade e limites

- A distribuicao intrassetor e aproximada por area; o relatorio nao promete precisao de rua, lote ou quadra.
- Setores extensos ou rurais podem diluir populacao/renda quando a ocupacao real nao for uniforme.
- Renda censitaria e proxy/calibrada; exibir metodologia e flags quando ausente, suprimida ou herdada por fallback.
- Setores com geometria invalida devem ser corrigidos com rotina explicita ou marcados por flag.
- Os Parquets H3 atuais nao devem ser tratados como base geometrica de setor.
- O choropleth do mapa usa **faixas absolutas fixas por camada** (`DENSIDADE_POP_BANDS`/`RENDA_PER_CAPITA_BANDS`/`RESIDUAL_SCORE_BANDS`/`RENDA_MEDIA_DOMICILIAR_BANDS`/`OFERTA_DISPONIVEL_ALUNOS_BANDS`, esta ultima em ALUNOS, com 6 faixas ancoradas em 2.500 alunos = 1 unidade), nao quartis relativos: cores comparaveis entre pontos diferentes. Quartis continuam apoio relativo em outras telas; sizing executivo deve priorizar regua absoluta, populacao estimada, renda, residual, receita esperada perdida e capacidade operacional.
- O fundo de ruas depende de tiles online so na geracao (CartoDB **Voyager COM labels**, base clara, via `contextily`, extra `[basemap]`); sem internet/extra o mapa cai em canvas CLARO sem ruas (fallback offline), sem afetar KPIs, CSV nem o motor de intersecao. A camada Concorrentes e Ultra e SO de pins (sem choropleth).

## 9. Decisao operacional

Na epoca do ciclo, o relatorio nasceu dentro do dashboard Streamlit porque:
- o produto precisava conviver com a entrada por coordenada ja existente;
- o deploy inicial continuava offline com dados locais;
- o processamento pesado podia ser amortizado por artefato GeoParquet/WKB particionado e cache;
- API/worker separado so entraria no backlog se houvesse concorrencia de usuarios, fila de PDFs, historico por usuario ou deploy independente.

A API separada acabou acontecendo (API GeoEspacial + bot), e com a aposentadoria do Streamlit (DEC-022, 2026-08-03) as superficies do relatorio passaram a ser o piloto web, a API e o bot — o motor de intersecao e o gerador de PDF continuam os mesmos, no pacote compartilhado `dashboard/`. A fila/lote de relatorios (que so existia no Streamlit) e o export CSV na tela sao perdas aceitas da DEC-022; a fila e o porte prioritario pos-corte (BLK-WEB-22).

## 10. Validacao do Bloco 1

Comandos executados:
- `python -c "import geopandas, shapely, pandas; print('ok')"`: passou.
- Leitura da malha com `geopandas.read_file(..., rows=1)`: passou; CRS `EPSG:4674`.
- Inventario com `pyogrio.read_info`: 468.099 features.
- Leitura de cabecalho/amostra dos CSVs Basico e Renda: passou.
- Leitura de schemas dos Parquets com `pyarrow.parquet`: passou; nenhuma coluna `geometry` detectada nos Parquets censitarios atuais.

### Confidencialidade e exibicao do solicitante (2026-08-19, pedido do Felipe)

- **Marca d'agua "ARQUIVO CONFIDENCIAL"** diagonal e translucida em TODAS as paginas do PDF (constantes `_CONFIDENCIAL_*` em `censo_report.py`; branca na capa, cinza nas demais), somada a marca de rastreabilidade "Ultra Academia | {solicitante}" que ja existia (BLK-EST-01).
- **Solicitante humanizado na exibicao**: `_nome_exibicao` converte o login (`felipe_castaldi`) em nome (`Felipe Castaldi`) em toda aparicao no PDF. So' camada de exibicao — o identificador bruto segue intacto na trilha/log (regra de identificadores do CLAUDE.md §2).
- **Pagina Realizacao**: a fala tecnica de READ-ONLY/score/plano saiu; entrou o aviso de dados dinamicos (`_AVISO_DADOS_DINAMICOS`: o estudo retrata as bases na data de geracao e pode mudar com o tempo). O mesmo jargao saiu do rodape do slide de viabilidade, das notas da Conclusao e da linha de fontes dos Big Numbers — o guardrail READ-ONLY continua valendo por codigo, so' deixou de ser texto voltado a quem recebe o estudo.
- **Pop-up de confidencialidade no piloto web** (`web/src/components/AvisoConfidencialidade.tsx`): bloqueante, centrado, exibido a CADA entrada no app (estado local, sem persistencia); o uso so' prossegue apos o OK.
