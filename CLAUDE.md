# Motor de Expansao Ultra Academia - CLAUDE.md
> Fonte canonica curta do projeto. Ler antes de qualquer tarefa.
> Responsavel: Felipe Silva | Estrategia e Growth | Ultra Academia
> Versao: Maio 2026
> Regra de manutencao: manter curto; historico detalhado fica em `docs/`, `data/reports/` e `PRD.md`.

## 1. Norte
- O repo tem duas trilhas complementares:
  - `M1 oficial territorial`: decide onde expandir no nivel executivo.
  - `mercado por hexagono`: camada paralela para ler demanda, oferta mapeada, residual fitness e restricao da rede Ultra.
- Perguntas centrais: onde expandir, quem ja atua ali, qual mercado residual existe e como ocupar regioes com sequencia controlada.
- Posicionamento da Ultra: marca **low-cost / massa** (NAO e premium). Publico-alvo prioritario: 18-45 anos. Concorrente low-cost direto e comparavel de mesmo segmento: Smart Fit.
- `score_priorizacao` continua sendo o score oficial do M1 (camada executiva), nao o score operacional do dia a dia.
- Nenhuma trilha paralela pode alterar o M1 sem aprovacao explicita.
- Papeis das camadas:
  - M1 e a camada EXECUTIVA: decide municipios e ranking oficial de carteira no nivel executivo (uma camada entre varias, nao a primaria operacional).
  - Censitario (`score_setor_2022_calibrado`) e a camada PRIMARIA no uso operacional do dia a dia; hibrido refina leitura intraurbana.
  - Mercado/residual e Expansao de Dominio apoiam estrategia operacional, nao substituem o M1.

## 2. Regras operacionais
- Ler o repositorio real antes de editar; este arquivo resume contexto, nao substitui o codigo.
- Tratar `config.py`, este arquivo e `PRD.md` como fontes canonicas de parametros e guardrails.
- Staging sempre em Parquet; CSVs locais gerados pelo projeto usam `sep=";"` e `encoding="utf-8-sig"`.
- Excecao de legado: `data/ultra/Ultra.csv` usa `sep=";"`, `encoding="latin-1"` e 1 linha inicial de metadado.
- Bases de validacao de scores (concorrentes) ficam em `data/validacao/` (gitignored, dados reais): `Sky Fit dados.xlsx` e `academias_engenharia_do_corpo.xlsx` (alunos/m² + metragem). Insumo do BLK-SCORE-01; contrato em `data/validacao/README.md`. Read-only sobre o M1.
- Ao tocar em camadas paralelas, preservar 100% das linhas e colunas oficiais do M1.
- Nao criar dependencia de API ao vivo no dashboard de producao.
- Toda mudanca relevante entra com teste; nenhum PR deve subir com CI quebrado.
- Quartis sao apoio de ranking relativo; para sizing e decisao executiva, priorizar regua absoluta, residual, receita esperada e capacidade operacional.
- Pins/logos de concorrentes e Ultra no dashboard sao camada visual de apoio; nao alteram score, ranking, carteira nem artefatos oficiais.
- Variaveis IBGE Censo 2022 Basico: `v0001` = Total de pessoas; `v0002` = Total de Domicilios; `v0007` = Domicilios Particulares Ocupados; `v0005` = media de moradores.
- O guia operacional do ciclo ativo fica em `PRD.md`; contratos tecnicos detalhados ficam em `docs/`.
- **Acentuacao (regra permanente, vale para todo trabalho apos a epic BLK-ACENTO):** TODO texto voltado ao usuario — dashboard (labels, botoes, `help=`, `st.caption/markdown/info/warning/success`, `st.metric`, legendas, `column_config`) e relatorios gerados (PDF/CSV) — deve ser escrito com **acentuacao correta do portugues**; nao regredir para texto sem acento ao criar/editar strings novas. **NUNCA acentuar IDENTIFICADORES:** `key=`/`st.session_state`, seletores CSS `.st-key-*`, valores brutos de enum/categoria (ex.: `FAIXA_ORDEM` `"media"`/`"alta"`/`"prioridade_maxima"`, produzidos pelo pipeline core e comparados em `.isin`/dict de cores), nomes de coluna de DataFrame e slugs/nomes de arquivo — para exibir acentuado, usar uma **camada de LABEL de exibicao** (`{valor_bruto: "Texto Acentuado"}`), sem tocar o valor bruto. No PDF (`fpdf2` core font Helvetica, encoding `latin-1` via `_ascii()`), os acentos portugueses renderizam normalmente, mas caracteres **fora de latin-1** (travessao `—`/`–`, bullet `•`, seta `→`, reticencias `…`, aspas curvas, `©`) viram `"?"` silenciosamente — usar pontuacao ASCII (`-`, `"`, `(c)`, `...`). Execucao/detalhe: epic BLK-ACENTO no backlog.
Interpretação operacional da regra de criticidade para score (decidida em 2026-05-30):
- LEITURA/ANÁLISE de score sem escrita em artefato M1 → Alta (revisão humana antes do Builder)
- ALTERAÇÃO de fórmula, pesos, ou qualquer artefato M1 → Crítica (aprovação obrigatória + DEC)

## 3. Nucleo oficial M1

### Parametros canonicos
```python
H3_RESOLUTION = 7
DIST_MIN_ULTRA_KM = 1.0
RENDA_MIN = 4500.0
AREA_MIN_M2 = 1200.0
AREA_IDEAL_MIN_M2 = 1500.0
AREA_IDEAL_MAX_M2 = 2000.0
PE_DIREITO_MIN = 3.5
M1_SCORE_OFICIAL = "score_priorizacao"
M1_PRIORIZACAO_TOP_PCT_POR_UF = 0.20
M1_OSM_ENABLED = False
M1_SETOR_CENSITARIO_OBRIGATORIO = False
M1_POP_MINIMA_PROXY = 1
```

### Fluxo oficial
Implementacao em `src/motor_expansao/pipelines/m1/`; scripts da raiz continuam como wrappers legados:
1. `base_h3_brasil.py` -> `data/staging/brasil/uf=XX/hexagonos.parquet`
2. `hex_enrichment.py` -> `data/staging/brasil_estrutural.parquet`, `data/staging/brasil_priorizados.parquet`, `data/staging/hexagonos_brasil_oportunidades.parquet`
3. `fase1_bi_exports.py` -> artefatos executivos e BI estaveis

### Score oficial
```python
renda_pct_nacional = percentil_nacional(renda_per_capita)
pop_pct_nacional = percentil_nacional(populacao_proxy)
hex_score_estrutural = 100 * (0.40 * renda_pct_nacional + 0.60 * pop_pct_nacional)
score_priorizacao = clip(hex_score_estrutural + ajuste_executivo, 0, 100)
score_oficial = score_priorizacao
```
- Inputs oficiais: `renda_per_capita` e `populacao_proxy` (= `pop_total`; trava 18-45 removida em 2026-05-15).
- Pesos aprovados: `renda=0.40`, `pop=0.60`.
- Campos minimos: `renda_pct_nacional`, `pop_pct_nacional`, `hex_score_estrutural`, `ajuste_executivo`, `score_priorizacao`, `score_oficial`, `score_oficial_nome`, `score_percentil_nacional`.
- Artefatos oficiais: `brasil_estrutural.parquet`, `brasil_priorizados.parquet`, `hexagonos_brasil_oportunidades.parquet`, `hexagonos_brasil_dashboard.parquet`, `hexagonos_mapa_sample.parquet`, `top_oportunidades_resumo.csv`, `resumo_por_uf.csv`.

## 4. Camadas paralelas e estado atual
- `M1.1`, censitario, hibrido, mercado residual e Expansao de Dominio sao paralelos ao M1.
- Modelo hibrido: M1 aprova municipios; censitario ranqueia hexes dentro de municipios aprovados; `score_expansao_hibrido` e operacional.
- Dashboard separa papeis: mapa executivo/hibrido para leitura local, carteira por M1, residual como apoio de mercado.
- Camada de mercado por hexagono:
  - objetivo: combinar demanda, oferta mapeada e restricao da rede propria Ultra;
  - base principal: `data/staging/hexagonos_mercado_mapeado.parquet`;
  - outputs acionaveis: `carteira_expansao_acionavel.parquet` e `plano_expansao_curto_prazo.parquet`;
  - residual fitness: `oferta_efetiva_disponivel` e `score_oportunidade_residual`;
  - oferta consumida: concorrentes mapeados + Ultra propria;
  - capacidade default de concorrente/unidade proxy: 2500 alunos;
  - `pop_hex_base` usa `pop_total_setor_2022` quando disponivel e fallback `populacao_proxy / total_hex_municipio`.
- Dashboard funciona offline com Parquets locais em `data/outputs/`.
- Artefato derivado (NAO oficial M1): `fase1_bi_exports.py` tambem materializa `data/outputs/hexagonos_dashboard_enriquecido/uf=XX/parte-*.parquet` (resultado de `enrich_dashboard_data`, particionado por UF) para acelerar a carga do dashboard; nao recalcula score nem altera artefatos oficiais. Detalhe em `docs/m1_outputs_oficiais.md`.
- Carga lazy por UF (Bloco 4): o dashboard le so a particao `uf=XX` desse artefato (`load_uf_slice`/`read_enriched_uf_partition`), com catalogo leve via diretorios de particao (`load_uf_catalog`/`list_partitioned_ufs`) e fallback para `build_dashboard_dataset()` filtrado quando a particao nao existe. Consequencia: a busca por coordenada resolve dentro da UF carregada.
- Render lazy das abas (Bloco 5): `main()` usa `render_tab_selector` (`st.segmented_control` + `session_state`) no lugar de `st.tabs`; so o `render_*` da aba ativa roda por rerun (UX de 4 abas preservada). `build_city_summary`/`build_uf_summary` so sao computados nas abas Visao Executiva/Mapa Territorial. Nao recalcula score nem altera artefatos.
- Fonte de mapa enxuta (Bloco 6): os builders de mapa fazem downsample antes do cap via `_downsample_map_index` (ordena/dedup/`head(MAP_POINT_LIMIT)` sobre projecao leve de chaves) e so materializam as colunas completas (`MAP_SOURCE_COLUMNS_M1`/`MAP_SOURCE_COLUMNS_HYBRID` em `constants.py`) para os ≤35k sobreviventes. Cap inalterado (mesmos top-N por prioridade); busca por hex fora do recorte lida do `df`. Nao usa `hexagonos_mapa_sample.parquet` (nacional, so 39 cols M1). Nao recalcula score nem altera artefatos.
- Relatorio Pontual Censitario 1.5 km: ciclo concluido em 2026-05-22; base geo completa em 2026-05-25. Contrato em `docs/relatorio_pontual_censitario.md`; usa malha real IBGE 2022 em `data/outputs/setores_censitarios_2022_geo/uf=XX/cod_municipio=NNNNNNN/part-000.parquet`, com `geometry_wkb` em `EPSG:4674`, area/densidade em `EPSG:5880`, bbox e score setorial paralelo. Cobertura: 27 UFs, 5.571 municipios, 468.099 setores (~1,17 GB). Parquets `censo2022_setores_*.parquet` sao agregados H3 sem geometria e nao bastam para o motor setorial.
- Motor/UI censitario: `analisar_ponto_censitario_setores` cruza setor real x circulo 1.5 km em CRS metrico local; metodo `setor_censitario_intersecao_area_1p5km` (INTOCADO). `render_mapas_censitarios_combinados` (BLK-CENSO-01 / DEC-004) gera as 4 camadas (Densidade/Renda/**Score censitario = choropleth COM legenda, restaurado no BLK-CENSO-03-FU5**/**Concorrentes e Ultra = SO-pins, sem choropleth, a partir do BLK-CENSO-03**) numa unica chamada, com **fundo de ruas por tiles online no relatorio** (CartoDB **Voyager COM labels** — base CLARA estilo GeoFusion, trocada da Dark Matter pelo BLK-CENSO-03 — com realce de contraste `_BASEMAP_CONTRAST=1.15` + zoom+1 via `contextily`, extra `[basemap]`, import lazy + cache `data/cache/basemap_tiles/` + fallback offline gracioso/**canvas CLARO** `(245,245,245)` — o dashboard interativo NAO depende de internet), composicao em `EPSG:3857`, choropleth cobrindo a figura inteira via **frame RETANGULAR** (proporcao da area de mapa via `_map_inner_dims`, lado menor = raio*(1+`_MAP_FRAME_MARGIN`); setores recortados a este retangulo, nao ao circulo; circulo segue redondo, em LARANJA `_CIRCLE_RGBA` como o do dashboard) e SEM bordas, **faixas absolutas fixas estilo GeoFusion** (`DENSIDADE_POP_BANDS`/`RENDA_PER_CAPITA_BANDS` amarelo->laranja->verde/`RESIDUAL_SCORE_BANDS`; legenda usa RGB solido) em vez de quartil, **ruas ESCURAS do basemap claro POR CIMA da cor** (BLK-CENSO-03 / pedido de Felipe 2026-06-08 de base CLARA mantendo a legibilidade: choropleth translucido `_CHOROPLETH_ALPHA=140` sobre o Voyager e, por cima, recoloca-se os PROPRIOS pixels ESCUROS das ruas/nomes do tile — luminancia < `_STREET_CEIL` vira opacidade, ganho `_STREET_GAIN`, teto `_STREET_CAP`; NAO e edge-detection, sao os pixels nativos do tile -> arruamento e nomes nitidos sobre a cor, estilo GeoFusion; a abordagem de FIND_EDGES (FU1/FU2) foi DESCARTADA por deixar o mapa ilegivel), e **pins com logo** via `competitors._render_pin_tile` (Ultra=`"__ultra__"`, concorrente por `rede`, fallback de sigla); `render_mapa_censitario_estatico_png` segue como wrapper fino legado. `censo_report.py` gera CSV (inalterado) + PDF com **template Ultra reescrito sobre `fpdf2`** (dep BASE; gate humano de Felipe em 2026-06-05, BLK-CENSO-02; **FU1 2026-06-06: paginas 16:9 widescreen 960x540** — corrige distorcao da capa, titulo na zona limpa inferior-direita), em **5 paginas** (Capa -> **Mapas de calor** -> **Concorrentes** -> Big Numbers -> Realizacao/Credito; **BLK-RELPON-01, gate Vinicius 2026-07-01: os 3 choropleths Populacao/Renda/Score foram consolidados em UM slide "Mapas de calor" — tira 1x3 lado a lado, cada PNG embutido separadamente com sua legenda, fallback textual por camada ausente — levando o PDF de 7->5 paginas nas duas variantes `censitario` e `classico`; helpers `_map_page`/`_classico_map_page`/`_draw_map`/`_classico_draw_map` substituidos por `_mapas_calor_page`/`_classico_mapas_calor_page` + `_draw_maps_grid`/`_map_grid_cells`**), com fundos do `Teste Modelo.pptx` extraidos p/ `data/ultra/` (gitignored; fallback gracioso de cor solida sem assets), compressao de stream OFF para auditabilidade anti-PII, e Big Numbers READ-ONLY em **grid 4x2 de 8 metricas** (pop/renda/score medio/score max do `result` + **SAM Fitness e Residual Fitness em ALUNOS** (`sam_fitness_potencial`/`oferta_efetiva_disponivel`, nao score) + concorrentes/consumo via `lookup_hex_by_coord` do hex H3 do ponto, "n/d" quando ausente). Anti-PII: `.pptx`/PDF nunca versionados, cartao de contato `image24.png` NUNCA embutido. `render_relatorio_pontual_censitario` roda no expander do Mapa Territorial (sem dropdown de metrica), com lazy load/cache por `uf` + `cod_municipio` e mensagem clara quando a base geo nao existe. **Refinamento visual BLK-EST-02 (tema "Ultra Clean / GeoFusion", gate Felipe 2026-06-11; SO estilo/geometria/strings — grid 4x2 READ-ONLY, `set_compression(False)`, `pdf_version="1.4"`, raio 1.5 km e metodo de intersecao INTOCADOS; a contagem de paginas/ordem/`/Count`/`PDF_SECTION_HEADERS` foi atualizada depois pelo BLK-RELPON-01 acima, de 7->5):** capa 30 pt / banda de conteudo 22 pt (band_h 56) / Realizacao 34·18·12 pt em Helvetica (sem TTF) [D1=B]; cards com rotulo/valor em cinza-escuro (45,45,45 / 40,40,40) e acento so na barra do topo [D2=B], `card_h=156`/`gap=16`/barra 6 pt/valor 26 pt/borda fina (225,225,228) [D3=B]; Concorrentes com bullet colorido por tipo (Ultra=turquesa, conc.=magenta), contagem total "(N no total)" quando >10 e "... e mais N" ao truncar (`_point_rows` retorna `(texto, is_ultra)`; `name_col` so de UNIDADE) [D4=B]; Realizacao com logo Ultra no topo (fallback gracioso) + metodo em 1 frase [D5=C]; titulos dos 4 mapas curtos sem o prefixo "Relatorio Pontual Censitario -" (subtitulo tecnico mantido) [D6=A]; legenda com amostras arredondadas + separador faixas×pins (sem mexer em `_map_box`/`legend_x`) [D7=C subset seguro]; barra de escala width 5 com box branco atras do label e rodape enxuto "Raio 1,5 km - EPSG:3857 - {atribuicao CARTO quando drew_basemap}" [D8=B]. READ-ONLY sobre o M1: nao recalcula score nem altera artefatos.
- Resolucao de `cod_municipio`: o dataset M1 base nao tem essa coluna; ela flui do censo trace via `enrich_dashboard_data` (`censo_extra_cols` inclui `cod_municipio`). Fallback para parquets enriquecidos antigos: `resolve_cod_municipio_from_geo_dir` le uma linha por particao `uf=XX/cod_municipio=N/` para casar pelo `nome_municipio`. `render_mapa_territorial` e `render_relatorio_pontual_censitario` aceitam `censo_geo_dir: Path` para ativar esse fallback.
- Camada paralela PLANEJADA `Motor de Dimensionamento e Viabilidade` (epic `BLK-DIM-00..04`, spec `modelo_dimensionamento_expansao.md` do CEO; substitui o BLK-SCORE-05 — DEC-008): motor inverso de 4 camadas (Potencial via aderencia calibrada -> Captura/Huff -> Dimensionamento m2 pela curva de densidade -> Viabilidade financeira deterministica). READ-ONLY sobre o M1 (nao toca `score_priorizacao`/pesos/artefatos; DEC-001 intacta). Metodologia obrigatoria: LOO-CV vs baseline, sem R2 in-sample, com intervalos + flag de extrapolacao. Camadas 3-4 prototipaveis ja (BLK-DIM-03); 1-2 dependem de insumos a disponibilizar (serie diaria das maduras, datas de abertura, simulador `.xlsx`). Detalhe no backlog (epic BLK-DIM) e DEC-008.
- API/FastAPI, PostGIS, Prefect, pipelines pesados, M2/M3, pesquisas e Power BI continuam fora do deploy inicial.

## 5. Ciclos concluidos
- **Baseline pytest atual (2026-05-28): `532 passed, 1 skipped, 9 warnings`** via `pytest -q`. Os numeros menores citados nos ciclos abaixo (497, 509 etc.) sao contagens historicas de cada ciclo no momento em que foi concluido — nao comparar com o estado atual.
- Ciclo `BLK-FIX-06-C — Orla nao renderiza no dashboard (display/render)` (Alta; gate humano) concluido em 2026-06-03 pela esteira /run-cycle (BO->Planner->[aprovacao humana das decisoes de produto]->Builder->QA): follow-up VISUAL do BLK-FIX-06-B — os dados da orla JA estavam corretos e servidos em producao (universo 1.542.531; Mongagua `87a810998ffffff` e PG-Mongagua `87a810d4cffffff` presentes em todos os parquets), mas o caminho de RENDER os ocultava por (A) cinza alpha-70 quase invisivel do corte "<5k hab" e (B) descarte por `score_setor_2022_calibrado` NaN no `scope` dos builders Hibrido/Residual ANTES do cap (M1 nao sofria de B). Fix SO de display em `src/motor_expansao/dashboard/components.py` (zero escrita em score/artefatos; visualizacao nao recalcula M1, §5 guardrail): `_DISCARDED_FILL` 70->**alpha 150** (`[150,150,170,150]`) + `_DISCARDED_LINE` reforcado + legenda espelhada; scope Hibrido/Residual relaxado de `score_setor_2022_calibrado.notna()` para `hex_id/lat/lng notna()` (guard de COLUNA ausente preservado) + nova cor de fallback visivel/distinta `_NAN_SCORE_FILL=[110,116,140,150]` aplicada a score NaN ANTES de `_apply_pop_cut_colors` (precedencia: hex NaN+pop<5k termina "descartado <5k"). Decisoes de produto aprovadas por Felipe no gate: (i) alpha 150, (ii) fallback visivel, (iii) NAO mexer nas tabelas (`pages.py` intocado), (iv) duas cores de excecao distintas. QA APROVADO: suite full `660 passed, 1 skipped, 0 failed`; ruff+mypy limpos; smoke import ok; escopo so `components.py` + `tests/integration/test_streamlit_app.py` (1 teste de cor atualizado + 4 novos). Pesos `renda=0.40`/`pop=0.60`, formula e artefatos oficiais INALTERADOS. Cap `MAP_POINT_LIMIT*` e `POP_MIN_ACIONAVEL` inalterados. Deploy de IMAGEM ao VPS feito em 2026-06-03 (digest `sha256:868349e0...a044d`, tag `sha-ab521ec`). **FOLLOW-UP BLK-FIX-06-C-FU1 (mesmo dia, aprovado por Felipe):** o 06-C corrigiu so os builders Hibrido/Residual; o mapa EXECUTIVO M1 (`build_map_figure`) AINDA sumia a orla por um filtro INTENCIONAL e testado (`key.loc[granular_rows | municipal_rows]`) que descartava hex SEM censo em UF granular (SP/RJ) — a orla, sobre agua sem setor IBGE, caia exatamente ai (renderizava so nos modos Hibrido/Censitario). FU1 transforma granular/municipal em ROTULO de confianca (nao filtro de exibicao): hexes validos do M1 sem censo agora renderizam coloridos pelo `score_priorizacao` real, com borda municipal ambar; impacto medido = ~1.000 hexes/SP (orla litoranea + margens de rio/represa; score 22-100; Mongagua 78, PG-Mongagua 100). Display-only: score/pesos/artefatos INALTERADOS. 2 testes do descarte antigo atualizados (`sp_municipal_descartado`->`sp_sem_censo`, agora RENDERIZA) + 1 novo (orla Mongagua colorida). Suite full `661 passed, 1 skipped`; ruff+mypy limpos. Redeploy de imagem apos FU1.
- Ciclo `BLK-FIX-06-B — Geracao de candidatos por OVERLAP + limiar 0.05` (Critica + DEC-003) concluido em 2026-06-03: correcao do defeito do BLK-FIX-06 — a geracao de candidatos usava `h3.geo_to_cells` (containment por CENTRO), que NUNCA gerava os hexes da orla de centroide-no-mar, entao o filtro de fracao-de-terra nunca os avaliava (os "+474" eram casos de borda das malhas, nao a orla; a costa aparecia vazia no dashboard, "como se o hex nao existisse"). Fix em `base_h3_brasil.py`: candidatos por containment OVERLAP (`h3shape_to_cells_experimental(..., "overlap")`, fallback centro+anel) + atribuicao por UF (fast-path por centro-na-UF, descarte de centro em UF vizinha, recuperacao costeira por fracao-de-terra vs Brasil) + DEDUP nacional (hex unico). Limiar reduzido 0.20->**0.05** (DEC-003, escolha humana p/ cobertura maxima da orla). Universo 1.538.424->**1.542.531 (+4.107 hexes costeiros, 23 UFs)**; impacto no score dos existentes ~nulo (max 0.02; 0 alem de +-0.5); pesos/formula INALTERADOS. QA: suite full `656 passed, 1 skipped`; ruff+mypy limpos; oficiais regenerados + repro (Mongagua `87a810998ffffff`, PG-Mongagua `87a810d4cffffff`). Nota UX: hexes costeiros de baixa pop caem no corte de display "<5k hab" e renderizam CINZA (passam a EXISTIR vs ausentes; colori-los = follow-up do corte de 5k). Detalhe em DEC-003 (§8), `data/reports/base_h3_litoral_impacto_06b.md` e `_sweep_06b.md`.
- Ciclo `BLK-FIX-06 — Hexagonos do litoral recortados pelo pipeline` (Critica + DEC) concluido em 2026-06-03 pela esteira /run-cycle (BO->Planner->[aprovacao humana + DEC-002]->Builder Fase A medicao->[DEC-002]->Builder Fase B aplicacao->QA): a geracao da base nacional H3 (`base_h3_brasil.py`) filtrava hexes SO por centroide dentro do Brasil, descartando costeiros povoados com centroide na agua (Praia Grande/SP, litoral RJ). Correcao: criterio HIBRIDO "centroide-dentro OU fracao_terra >= `M1_HEX_LAND_FRACTION_MIN`" (novo parametro, default 0.20 aprovado em DEC-002), reusando `_hex_polygon` e aplicando a interseccao poligono×poligono SO aos hexes hoje-descartados (fast-path do interior preservado, sem regressao). Fase A mediu o impacto em scratch (leque {0.15,0.20,0.25,0.30}; delta caro em 0.20 e 0.30): impacto nos hexes existentes DESPREZIVEL (mediana 0.0; maior deslocamento de `score_priorizacao` = 0.01; 0 hexes mudam alem de +-0.5). Universo regenerado: 1.537.950 -> **1.538.424 hexes (+474)**; artefatos oficiais regenerados de forma reprodutivel; repro Praia Grande `87a810c02ffffff` e RJ `87a8a078cffffff` confirmada nos oficiais. Pesos `renda=0.40`/`pop=0.60` e formula INALTERADOS (DEC-001 vigente). QA APROVADO (suite full `656 passed, 1 skipped`, regeneracao verificada por evidencia propria). Detalhe em DEC-002 (§8) e `data/reports/base_h3_litoral_impacto.md`.
- Ciclo `BLK-ORQ-01 — Otimizacao de tempo do /run-cycle (Tier 1 + tiering de modelo)` concluido em 2026-06-02 pela propria esteira /run-cycle (BO->Planner->[aprovacao humana]->Builder->QA + dry-run autonomo pos-merge): (a) **fluxo** — o orquestrador passa CAMINHOS dos arquivos ao sub-agente (nao embute conteudo); cada sub-agente le via Read. (b) **validacao** — `pytest-xdist` em `[dev]`; o Builder valida subconjunto impactado + smoke `import streamlit_app` com `pytest -n auto`; a SUITE FULL roda 1x no QA (gate unico) com `pytest -n auto`; NO-BYPASS preservado integralmente. (c) **tiering de modelo** (Passo 4 do run-cycle): o orquestrador escolhe o `model` de cada Agent por criticidade x papel; **QA SEMPRE em Opus 4.8**; override +-1 nivel com pisos (QA nunca sai de Opus, nunca abaixo de haiku); o dry-run usa a coluna Baixa (BO=haiku, Builder=sonnet). QA APROVADO: suite serial `651 passed, 1 skipped` == `-n auto` (identicas; Python 3.14 local — divergencia vs baseline 532/2026-05-28 e de ambiente, nao defeito). Dry-run autonomo pos-merge passou (commit por path, handoffs versionados, sem arrasto). M1/score/artefatos/CI inalterados. Sucessor: BLK-ORQ-02 (Fase 2) e promocao dos itens Tier 2.
- Ciclo `BLK-OPS-01 — Backup encriptado de segredos e plano de regeneracao` (tooling) concluido em 2026-05-28: tooling SOPS+age, runbook `docs/backup_restore.md` com 16 secoes + 7-bis (Plano B), scripts `setup_secrets_vps.sh` + `secrets_roundtrip_test.{sh,ps1}`. FU1 (stage do `.ps1`) e FU2 (execucao do roundtrip + gitleaks com correcoes do tooling + criacao de `.gitleaks.toml`/`.gitleaksignore`) concluidos no mesmo dia.
- Ciclo `BLK-OPS-01 — Fechamento real (backup real + restore validado)` concluido em 2026-05-29: chave age real gerada via Plano B (recipient `age1lau0w4xg...` commitado, privada em KeePassXC + papel), 5 segredos reais encriptados in-place no VPS (chave privada nunca tocou o VPS), 5 `.enc.*` commitados em `secrets/`, restore real validado em pasta limpa (binarios byte-identicos, textos semanticamente identicos via PyYAML). Defeitos 3-5 do tooling original descobertos e corrigidos durante o fechamento: path_regex sem `/` em SOPS 3.8.1, sufixo `env.enc.env` para regra dotenv casar, `.gitattributes` com `binary` para `.enc.*` (evita CRLF conversion em checkouts Windows). Risco de DR eliminado. Detalhes em `tasks/completed.md`. Nenhum segredo real exposto a Claude; M1, dashboard e artefatos oficiais inalterados.
- Ciclo `Relatorio Pontual Censitario 1.5 km` concluido em 2026-05-22 (7 blocos): feature paralela no Streamlit usando geometria real de setores IBGE 2022, raio fixo 1.5 km, intersecao geometrica, mapa censitario offline e export PDF/CSV em memoria. Validacao de fechamento: suite censitaria + `test_streamlit_app.py` verdes e import do app ok. Nao substitui Analise Pontual H3 nem altera M1.
- Ciclo `Performance e Refatoracao do Dashboard` concluido em 2026-05-22 (7 blocos): carga lazy por UF (le so a particao do enriquecido), render lazy de abas, fonte de mapa enxuta e dataset enriquecido particionado; 509 testes passam, 1 skipped. Detalhe em `data/reports/perf_baseline_dashboard.md`. Sem recalculo de score/carteira/plano/artefatos M1.
- Ciclo `Cenarios Multi-Hex e Dominio Hibrido Censitario-Residual` concluido em 2026-05-21 (Blocos 14-19 do PRD).
- Ciclo `Hardening da Analise Pontual e Padronizacao Visual de Scores` concluido em 2026-05-20 (Blocos 8-13 do PRD).
- Ciclo `Visao Executiva Ultra e Analise Pontual` concluido em 2026-05-20 (Blocos 1-7 do PRD).
- Dashboard tem 4 tabs: `Visao Executiva`, `Mapa Territorial`, `Expansao de Dominio`, `Carteira e Plano`.
- Multi-Hex (Blocos 14-16.2): `agregar_cenario_multihex` retorna 25 campos; hex_id copiavel via `st.code()`; botao add/remove na Analise Pontual; `parse_hex_ids_from_text` aceita qualquer separador; `column_config TextColumn(width="large")` para hex_id integral.
- Dominio Hibrido (Bloco 17): `score_dominio_hibrido = clip(0.60*score_setor_2022_calibrado + 0.40*score_oportunidade_residual, 0, 100)` com fallback para componente unico; rastreabilidade via `motivo_dominio`.
- Consumo fitness (Bloco 18): `Consumo Conc. (est.)`, `Consumo Ultra (real)`, `Consumo Total Instalado` em tooltips, Analise Pontual e tabelas. `analisar_entorno_ponto` retorna `consumo_concorrentes_raio`/`consumo_ultra_raio`.
- Bloco 19 concluido em 2026-05-21: 497 testes passam, 1 skipped; docs atualizados; ciclo Blocos 14-19 completo.
- `Visao Executiva`: mapa Ultra-only (sem hexagonos), KPIs de rede, graficos de residual por UF/cidade.
- `Analise Pontual de Entorno`: raio 1.6 km, helper `analisar_entorno_ponto` em `data.py`; usa centroide de hex/ponto como aproximacao, nao muta inputs, retorna populacao/renda do raio e exibe pins de concorrentes/Ultra filtrados pelo raio.
- Hardening concluido (Bloco 13): 189 testes passam; ciclo completo em 2026-05-20.
- Padronizacao visual concluida (Bloco 9): todos os 4 modos quantitativos usam `score_band_to_color` (10 faixas via `RESIDUAL_SCORE_BANDS`); helper em `dashboard/utils.py`; legenda generica `render_score_bands_legend(mode_label)` em `components.py`.
- Clique exato — decisao tecnica concluida (Bloco 12): manter `st.pydeck_chart` com centroide do hex. `streamlit-folium` (+2 deps) e componente customizado (fora de escopo) descartados. Nota de centroide exibida no dashboard quando clique ativo.
- Captura por clique: `st.pydeck_chart(on_select="rerun")`; retorna centroide do hex selecionado; espaco vazio e botao direito nao suportados; fallback: campo lat,lng na sidebar.
- Regra visual canonica (Bloco 9 em diante): faixas de 10 pontos (0-10 a 90-100) via `RESIDUAL_SCORE_BANDS`; M1 colore por `score_priorizacao`, Censitario por `score_setor_2022_calibrado`, Hibrido por `score_expansao_hibrido`, Residual por `score_oportunidade_residual`.
- Area do raio 1.6 km = pi*1.6^2 = 8.04 km2 (corrige `~5 km2` que aparecia em docs anteriores); para ~5 km2 usar raio ~1.26 km com aprovacao explicita.
- Guardrail permanente: visualizacoes, analise radial e interacoes de mapa nao podem recalcular ou alterar `score_priorizacao`, `hex_score_estrutural`, carteira, plano curto prazo, plano dominio ou artefatos oficiais do M1 sem aprovacao explicita.

## 6. Guardrails de infraestrutura e VPS
- MCP configurado: `ssh-vps-ultra` conecta em `root@2.25.137.241` (Hostinger KVM4, producao).
- GUARDRAIL ABSOLUTO: nunca executar qualquer comando no servidor via MCP (ou qualquer tool SSH) sem confirmacao explicita do usuario para cada comando individual. Isso inclui git pull, docker compose, chmod, rm, e qualquer outro.
- Nao encadear multiplos comandos no servidor sem aprovacao intermediaria.
- Coleta de concorrentes (GymScraping) roda **semanal e autonoma na VPS** (cron `0 6 * * 0` = dom 03:00 BRT): scrape dos 90 coletores -> relatorio de crescimento por rede -> integracao ao motor (regen camada paralela mercado/residual, **READ-ONLY M1**) -> restart. Runbook em `docs/infra_producao.md`; decisao em DEC-013 (§8).
- Detalhes de manutencao e deploy em `docs/infra_producao.md`.

## 6.1 Loop autonomo (ralph) — BLK-LOOP-01
- Padrao "ralph" portado do Growth RPG: um laco roda `/run-cycle` 100% autonomo num **container Docker isolado** (`Dockerfile.loop`, non-root, repo como volume), auth pela assinatura Max (`CLAUDE_CODE_OAUTH_TOKEN`, NAO API key). Lancador de 1 clique: **`iniciar-loop.cmd`** -> `scripts/iniciar_loop.ps1` (le so o token do `.env`, MASCARA o `.env` no container, builda a imagem, cria branch `ciclo/loop-*`, roda). Runbook: `docs/loop_autonomo.md`.
- **REGRA CRITICA — opt-in por marcador:** o loop SO executa blocos que tenham a linha **`| **Autonomia** | loop-safe ... |`** na tabela do bloco em `tasks/backlog.md`. **Bloco SEM esse marcador e TOTALMENTE ignorado pelo loop.** Logo: para um backlog novo entrar no loop, ALGUEM (humano) precisa adicionar essa linha — e isso e a pre-aprovacao que substitui o gate humano interativo. Trabalho futuro/manual: nao marcar (ou `Autonomia: futuro`). O loop tambem respeita `Depende de` (so inicia bloco com dependencias ja em `completed.md`). Hoje marcados: BLK-DIM-01/02/03/04.
- **Criterio para um bloco PODER ser `loop-safe`:** READ-ONLY sobre o M1, NAO toca VPS/deploy/segredos, NAO persiste PII, consome `data/staging` (sem ingestao ao vivo). Blocos que tocam M1/score/pesos/artefatos/VPS NUNCA sao loop-safe.
- **Redes de seguranca que substituem o humano no loop:** (1) `run-ralph-loop.sh` aborta se houver credencial sensivel no ambiente (`VPS_/SSH_PRIVATE_KEY/CLICKUP_WRITE/DEPLOY_KEY/GROWTH_API_`); (2) `scripts/loop_guard.py` roda apos cada bloco e aborta + escreve `RELATORIO-BLOQUEIO.md` se o diff tocar `config.py`/`pipelines/m1`/`*scoring*`/artefatos M1/`deploy/`/`Dockerfile.{streamlit,api}`/compose/Caddy/authelia/`.env`/`secrets/`/CI; (3) testes (ruff+pytest) como gate sem bypass; (4) container sem chave de VPS -> nao deploya. O loop **commita por path** no branch e **NUNCA** faz merge/push/deploy — revisao + merge sao passos humanos.
  - **Ressalva (DEC-016, 2026-07-13):** a parte "**NUNCA** faz merge/push" acima esta **SUPERSEDED PARCIALMENTE** — **somente** para blocos de criticidade **Baixa/Media** e **somente** com os **4 checks verdes** (`test` + `guard` + `claude-review` + `review-gate`), o merge passa a ocorrer por **auto-merge nativo do GitHub**, sem aprovacao humana. **Alta** exige a label `aprovado-humano` e **Critica** a label `critica-aprovada` do proprio Felipe -> nesses casos o merge **segue humano**. **DEPLOY CONTINUA NUNCA AUTOMATICO, EM NENHUM CASO:** auto-merge NAO deploya (push na main so publica a imagem no GHCR; subir a imagem na VPS e sempre passo manual do Felipe, por digest — §6). Detalhe e gatilho de suspensao em DEC-016 (§8).

## 7. Onde aprofundar
- `docs/estado_dos_modelos.md`: sintese de desempenho, arquitetura e uso dos modelos + roadmap de produto (2026-07-08). Ler para entender o que cada camada preve e como usar.
- `PRD.md`: guia operacional em blocos do ciclo ativo.
- `docs/modelo_mercado_hexagonos.md`: contrato tecnico de colunas e calculos de mercado/residual.
- `docs/m1_outputs_oficiais.md`: contrato curto dos outputs do M1.
- `docs/m1_1_arquitetura_enriquecimento.md`: design da camada M1.1.
- `docs/streamlit_dashboard_m1.md`: governanca e uso do dashboard.
- `data/reports/validacao_penetracao_ultra_hex.md`: aderencia e cautelas com dados reais das unidades Ultra.
- `data/reports/validacao_geofusion_vs_hex.md`: comparacao GeoFusion 1km vs H3.

Se um detalhe historico nao estiver aqui, procurar primeiro nesses docs antes de expandir novamente este arquivo.

## 8. Decisoes registradas (DEC)

### DEC-001 — Manter pesos/formula do score_priorizacao (M1) apos backtest BLK-SCORE-02
- ID: DEC-001 | Data: 2026-05-31 | Criticidade: critica (decisao sobre pesos M1)
- Status: APROVADA por Felipe Silva em 2026-05-31.
- Decisao: NAO recalibrar. Manter `renda=0.40` / `pop=0.60` (`PESOS_HEX_SCORE_ESTRUTURAL`) e a formula de `score_priorizacao` INALTERADAS; nenhum artefato M1 regerado. O M1 e a camada EXECUTIVA (municipios/ranking de carteira), nao o score operacional primario do dia a dia (esse papel e da camada censitaria).
- Evidencia-chave (de `data/analysis/relatorio_backtest.md`, BLK-SCORE-02, read-only, gitignored):
  - `score_priorizacao` (AGG): Spearman rho ≈ -0.004, IC95% [-0.104, +0.094] (atravessa zero) -> poder preditivo nulo, sem direcao a corrigir.
  - Componentes nao-significativos: `renda_pct_nacional` +0.067 (n.s.) e `pop_pct_nacional` +0.095 (n.s.); diferenca dentro do ruido e o peso atual ja favorece pop (0.60>0.40). Recalibrar seria sobreajuste a ruido.
  - Sem feature nova usavel DENTRO do M1: `n_domicilios` e `densidade_dom` em `brasil_estrutural.parquet` EXISTEM mas estao 100% ZERADAS (placeholders, nunique=1, min=max=0.0); correlacao indefinida. As unicas features reais do M1 sao `renda_per_capita` e `pop_total`.
  - Censitario (`score_setor_2022_calibrado`, camada PRIMARIA operacional) e o unico preditor positivo significativo: rho +0.148, IC [+0.052, +0.251].
- Pesos antigos -> novos: `renda_per_capita` 0.40 -> 0.40 (INALTERADO); `populacao_proxy` 0.60 -> 0.60 (INALTERADO); formula INALTERADA.
- Plano de reabertura (novo bloco CRITICO; Planner -> gate humano -> Builder): so se TODOS satisfeitos —
  - PRE-REQUISITO de engenharia de dados: popular `n_domicilios`/`densidade_dom` (hoje 100% zeradas); sem isso nao existe M1 multivariado a calibrar.
  - G1: `maturacao_status` deixar de ser constante unica (data de abertura por unidade) para controlar maturacao.
  - G2: desfecho homogeneo e auditavel entre redes (sem estimativa para EngCorpo).
  - G3: N adequado por celula e hex com precisao de unidade na maioria das linhas.
  - G4: sob esses controles e/ou com sinal significativo vindo do BLK-SCORE-04, um componente/feature com correlacao significativa (p<0.05, IC sem cruzar zero) e materialmente diferente do peso atual.
- Referencias: `data/analysis/relatorio_backtest.md` (BLK-SCORE-02); `context/handoff.md` (BLK-SCORE-03); BLK-SCORE-04 (proposta no backlog).

### DEC-002 — Criterio geometrico HIBRIDO no litoral (M1) e regeneracao dos artefatos oficiais (BLK-FIX-06)
- ID: DEC-002 | Data: 2026-06-03 | Criticidade: critica (altera o universo de hexes do M1 e regenera artefatos oficiais).
- Status: APROVADA por Felipe Silva em 2026-06-03.
- Decisao: adotar em `base_h3_brasil.py` o criterio geometrico HIBRIDO "centroide-dentro OU fracao_terra >= `M1_HEX_LAND_FRACTION_MIN`" com limiar aprovado = **0.20**; novo parametro `M1_HEX_LAND_FRACTION_MIN` (default 0.20) adicionado em `config.py`; artefatos oficiais do M1 REGENERADOS com o novo universo. Recupera hexes costeiros povoados hoje descartados pelo filtro de centroide (ex.: orla de Praia Grande/SP), sem distorcer o M1.
- Evidencia-chave / DELTA (de `data/reports/base_h3_litoral_impacto.md`, BLK-FIX-06):
  - Hexes recuperados em 0.20 = **474** (total 1.537.950 -> **1.538.424**).
  - Impacto nos hexes EXISTENTES desprezivel: mediana de delta = 0.0 em `renda_pct_nacional`, `pop_pct_nacional` e `score_priorizacao`; maior deslocamento de `score_priorizacao` = 0.01; ZERO hexes mudam alem de +-0.5 (474 novos sobre 1,54 M nao movem percentis nacionais).
  - Recorte top-20%/UF (`brasil_priorizados`): entram 136 / saem 41 (entrantes incluem os proprios costeiros; saidas sao hexes de borda deslocados pela leve mudanca de floor(20%)).
  - Repro do litoral: Praia Grande/SP `87a810c02ffffff` (fracao_terra 0.231, reaparece a partir de 0.20; SAIRIA em 0.30) e litoral RJ `87a8a078cffffff` (fracao_terra 0.401).
  - Comparacao 0.20 vs 0.30: 0.30 recuperaria so 383 e EXCLUIRIA Praia Grande -> 0.20 escolhido para cobrir o litoral povoado reportado; quartis de fracao_terra dos que entram em 0.20 = [min 0.20, q25 0.33, mediana 0.40, q75 0.46, max 0.86] (maioria bem acima do piso, nao sao "esquinas" oceanicas).
- Pesos/formula do score: INALTERADOS (`renda=0.40` / `pop=0.60`, `PESOS_HEX_SCORE_ESTRUTURAL`, `score_priorizacao`/`hex_score_estrutural`). Parametros canonicos existentes do §3: INALTERADOS (apenas ADICAO de `M1_HEX_LAND_FRACTION_MIN`).
- Referencias: `context/handoff.md` (BLK-FIX-06, v2); `data/reports/base_h3_litoral_impacto.md`; `data/reports/base_h3_brasil.md`; CLAUDE.md §3.

### DEC-003 — Geracao de candidatos por OVERLAP + limiar 0.05 (correcao do BLK-FIX-06; BLK-FIX-06-B)
- ID: DEC-003 | Data: 2026-06-03 | Criticidade: critica (altera o universo de hexes do M1 e regenera artefatos oficiais).
- Status: APROVADA por Felipe Silva em 2026-06-03.
- Contexto / defeito corrigido: a DEC-002 adicionou o filtro de fracao-de-terra, mas a geracao de candidatos em `base_h3_brasil.py` usava `h3.geo_to_cells` (containment por CENTRO), que NUNCA produzia os hexes da orla cujo centroide cai no mar — entao o filtro nunca os avaliava. Os "+474" da DEC-002 eram casos de borda entre as malhas UF/Brasil, NAO a orla povoada (verificado: a orla de Mongagua/SP `87a810998ffffff`, com 0.218 de terra, seguia ausente). No dashboard, a costa aparecia vazia ("como se o hex nao existisse").
- Decisao: (a) gerar candidatos por containment OVERLAP (`h3.h3shape_to_cells_experimental(..., "overlap")`, com fallback centro+anel) para a orla de centroide-no-mar entrar na avaliacao; (b) atribuir cada hex a UF do seu centroide (fast-path por UF + descarte de centro em UF vizinha) e recuperar costeiros por fracao-de-terra (vs Brasil), com DEDUP nacional (hex unico por base); (c) REDUZIR `M1_HEX_LAND_FRACTION_MIN` de 0.20 -> **0.05** para cobertura maxima da orla povoada. Artefatos oficiais do M1 REGENERADOS.
- Evidencia-chave / DELTA (de `data/reports/base_h3_litoral_impacto_06b.md` + `_sweep_06b.md`, scratch):
  - Hexes recuperados em 0.05 = **+4.107** (1.538.424 -> **1.542.531**) em 23 UFs (RS:665, AM:503, RR:312, PA:303, MA:276, ...). Integridade: linhas == unicos (0 duplicatas).
  - Impacto nos hexes EXISTENTES desprezivel: mediana de delta ~0.0; maior deslocamento de `score_priorizacao` = **0.02**; ZERO hexes mudam alem de +-0.5.
  - Recorte top-20%/UF: 307.674 -> 308.494 (entram 1.148 / saem 328).
  - Leque {0.05..0.25} medido: 0.05=+3.890, 0.10=+3.234, 0.15=+2.692, 0.20=+2.229 hexes de mar (+ ~217 de interior em qualquer limiar). Repro: Mongagua 0.218 e PG-Mongagua 0.104 recuperados em 0.05; praia de Itanhaem 0.035 (96% agua) fica fora (correto).
- Nota de UX (fora do escopo desta DEC): hexes costeiros de baixa pop residencial caem no corte de display "<5k hab" (`POP_MIN_ACIONAVEL`) e renderizam CINZA no mapa; passam a EXISTIR (vs ausentes), mas colori-los seria um follow-up separado (corte de 5k).
- Pesos/formula do score: INALTERADOS (`renda=0.40` / `pop=0.60`). Parametros canonicos existentes do §3: INALTERADOS (so o valor de `M1_HEX_LAND_FRACTION_MIN`: 0.20 -> 0.05).
- Referencias: `data/reports/base_h3_litoral_impacto_06b.md`; `data/reports/base_h3_litoral_sweep_06b.md`; `scripts/medir_impacto_litoral_blk_fix_06b.py`; `scripts/medir_sweep_litoral_06b.py`; CLAUDE.md §3 / DEC-002.

### DEC-004 — Fundo de ruas por tiles online no Relatorio Pontual Censitario (BLK-CENSO-01)
- ID: DEC-004 | Data: 2026-06-05 | Criticidade: **Alta** (desvia do guardrail §2 "nao criar dependencia de API ao vivo no dashboard de producao"; READ-ONLY sobre M1).
- Status: **APROVADA por Felipe Silva em 2026-06-05.**
- Decisao: permitir, **restrito ao caminho de GERACAO do Relatorio Pontual Censitario** (nao a carga/interatividade do dashboard), buscar tiles de basemap claro online para compor o fundo de ruas dos mapas do relatorio. Provedor: **CartoDB Voyager COM labels** com realce de contraste `_BASEMAP_CONTRAST=1.15` + zoom+1 (basemap claro com ruas/nomes bem visiveis; "Voyager No-Labels" era a DEC-004 original; passou para Dark Matter no FU3; voltou para **Voyager COM labels** no BLK-CENSO-03, base clara com ruas/nomes ESCUROS recolocados por cima do choropleth), via **`contextily`** em **extra dedicado `[basemap]`** do `pyproject.toml` (fora das dependencias base do deploy). Composicao do mapa em **EPSG:3857** (CRS nativo dos tiles), reprojetando os setores e o circulo do CRS metrico azimutal local para 3857 SO para render — o metodo de intersecao (`setor_censitario_intersecao_area_1p5km`) e o raio de 1.5 km ficam INTOCADOS.
- Justificativa: o fundo branco/claro com ruas (estilo GeoFusion) e requisito operacional aprovado por Felipe (2026-06-05) para o mapa de calor nao ficar escuro e as ruas seguirem legiveis. O desvio e cosmetico e local ao relatorio.
- Provedor / licenca / atribuicao: **CartoDB Voyager** (No-Labels) — tiles (c) OpenStreetMap contributors, (c) CARTO; uso sob atribuicao. Atribuicao registrada no rodape do mapa/PDF ("(c) OpenStreetMap, (c) CARTO"); cache local minimiza requisicoes.
- Mitigacoes obrigatorias (parte da decisao):
  - (a) **Cache local** de tiles em `data/cache/basemap_tiles/` (gitignored); cada ponto cobre ~3 km de lado (poucos tiles).
  - (b) **Fallback offline gracioso**: sem internet/tiles/contextily -> mapa sobre canvas branco SEM ruas, sem excecao. Default seguro em CI/teste (`basemap=False`), coberto por teste explicito.
  - (c) **Import lazy + carga do dashboard nao depende de internet**: `contextily` é importado `try/except` DENTRO da funcao de fundo; a carga/interatividade do dashboard NAO faz fetch de tiles (so a geracao do relatorio faz, sob demanda). **Atualizacao 2026-06-05 (Felipe, BLK-CENSO-01 follow-up de deploy):** o extra `[basemap]` passou a ser EMBUTIDO na imagem de producao (`Dockerfile.streamlit` usa `pip install ".[basemap]"`) para o fundo de ruas funcionar na VPS — escolha explicita que reverte o "fora da imagem" original desta mitigacao; o import segue lazy e o fallback offline (b) segue valendo se faltar internet em runtime.
  - (d) Distorcao do circulo de 1.5 km em EPSG:3857 < 0,5% sobre 3 km em latitudes brasileiras -> nao distorce visualmente (criterio de aceite).
- Pesos/formula/artefatos do M1: **INALTERADOS** (`renda=0.40`/`pop=0.60`, `score_priorizacao`, `hex_score_estrutural`, carteira, plano, artefatos oficiais). E camada de visualizacao (§5 guardrail permanente).
- Parametros canonicos do §3: INALTERADOS (nenhum parametro de score; so adicao de extra de dependencia e constantes de cor/faixa de visualizacao `DENSIDADE_POP_BANDS`/`RENDA_PER_CAPITA_BANDS`).
- Referencias: `tasks/backlog.md` (BLK-CENSO-01); `context/handoff.md` (Builder); `docs/relatorio_pontual_censitario.md` §6/§7/§8; CLAUDE.md §2/§4/§5.

### DEC-005 — Arquitetura e contrato da API GeoEspacial on-demand (BLK-API-01 / G1)
- ID: DEC-005 | Data: 2026-06-10 | Criticidade: **Estrategica** (nova fase: stand-up de uma API; redesenho da superficie de consumo do motor). READ-ONLY sobre o M1.
- Status: **APROVADA por Felipe Silva em 2026-06-10.**
- Decisao: aprovar o contrato da **API GeoEspacial** (complementar ao Motor, on-demand, para bots de Telegram/WhatsApp; ClickUp `86e1rtfe3`, pai `86e1rtfcy`) com as **6 decisoes-chave** resolvidas no gate:
  1. **Formato de saida = (c)** JSON por padrao + `?formato=pdf` (ou `Accept: application/pdf`) devolve o PDF. Reusa `analisar_ponto_censitario_setores` (JSON) + `gerar_pdf_relatorio_pontual_censitario` (PDF).
  2. **Autenticacao = (b)** token por consumidor/bot (rastreia QUEM pediu; casa com BLK-EST-01 marca d'agua/LGPD). MVP = lista estatica `token->consumidor`, evoluivel para Authelia depois.
  3. **Superficie do MVP = (a)** minimo `GET /health` + `POST /analisar`. Lookup M1/mercado NAO entra no MVP -> bloco sucessor condicional **BLK-API-05** (roadmap pos-MVP).
  4. **Entrada de coordenada = (b)** `{lat,lng}` E link do Google Maps (parser puro string->coordenada, sem tocar o motor).
  5. **Raio no MVP = (a)** fixo 1.5 km (metodo de intersecao `setor_censitario_intersecao_area_1p5km` e `RAIO_CENSITARIO_DEFAULT_KM` INTOCADOS).
  6. **Carimbo de versao = Incluir** versao do contrato/score no JSON e no rodape do PDF (reprodutibilidade).
- Premissas canonicas fixadas (Felipe, 2026-06-09; reafirmadas aqui, NAO reabrir): (i) **on-demand a partir do motor, PostGIS FORA do MVP**; (ii) fronteira **"importa, nao edita `censo_*`"** (`censo_point.py`/`censo_map.py`/`censo_report.py` sao interface estavel da trilha do Vini); (iii) codigo novo so em **`src/motor_expansao/api/`** (pasta disjunta); (iv) dependencias so no **extra `[api]`** do `pyproject.toml`, fora do deploy base do Streamlit (subset MVP = `fastapi`/`uvicorn[standard]`/`pydantic`/`pydantic-settings`; legado PostGIS `sqlalchemy`/`asyncpg`/`psycopg2`/`alembic`/`geoalchemy2`/`sentry-sdk`/`prefect` NAO arrastado).
- Escopo deste bloco: **ZERO codigo de producao** — so contrato/OpenAPI/ADR/decomposicao. A pasta `src/motor_expansao/api/` nasce no BLK-API-02. Decomposicao G2+: BLK-API-02 (esqueleto+/health+settings+auth), BLK-API-03 (`POST /analisar` JSON), BLK-API-04 (saida PDF), BLK-API-05 (M1/mercado condicional), BLK-API-06 (integracao G3), BLK-API-07 (G4 Telegram/WhatsApp).
- Pesos/formula/artefatos do M1: **INALTERADOS** (`renda=0.40`/`pop=0.60`, `score_priorizacao`, `hex_score_estrutural`, carteira, plano, artefatos oficiais). Parametros canonicos do §3: INALTERADOS (a API e camada paralela de consumo; nada de score/pesos/raio/metodo de intersecao alterado).
- Referencias: `docs/api_geoespacial_contrato.md`; `docs/api_geoespacial_openapi.yaml`; `tasks/backlog.md` (BLK-API-01..07); `fora_primeira_fase/api_postgis/main.py` (scaffold legado); `context/handoff.md` (Builder); CLAUDE.md §2/§4/§5.
- **Emenda 2026-06-12 (APROVADA por Felipe): a API pode ESTENDER `censo_map.py`/`censo_report.py` com parametros opcionais de RENDER.** A premissa original "importa, NAO edita `censo_*`" e relaxada para permitir parametros novos *opcionais* (default `None` = comportamento IDENTICO ao dashboard) que so a API passa: em `render_mapas_censitarios_combinados`/`_render_camada` os knobs `street_ceil`/`street_gain`/`street_cap`/`choropleth_alpha` (ruas mais visiveis sobre o choropleth); em `gerar_pdf_relatorio_pontual_censitario`/`_cover_page` o `rotulo` (nome do endereco/estabelecimento na capa). E EXTENSAO da interface, NAO alteracao do nucleo: `setor_censitario_intersecao_area_1p5km`, raio 1.5 km, logica de intersecao, score, M1 e artefatos oficiais seguem INTOCADOS; o caminho do dashboard (chamada sem os params) e preservado byte-a-byte (suite censo/dashboard 213 verdes). Contexto: o merge do PR #4 (`feat/api-geoespacial-telegram`, BLK-API-02..07) na `main` foi NAO-INTENCIONAL (commit `4304170`, merge `6a6ddb2`) e subiu com CI vermelho; a `main` passou a exigir PR+1 aprovacao+`test` verde (protecao habilitada em 2026-06-12). Correcao do CI (ruff `B008` idiomatico do FastAPI via per-file-ignore + UP032/UP037/UP017 auto-fix + 3 erros de mypy da API, 1 deles bug latente de `None` em `maps_geocoder._resolve_url`) feita no PR `fix/api-lint`.

### DEC-006 — Redefinição do gate do SAM: Faixa M1 elegível AND população ≥ 5000 (BLK-SAM-01)
- ID: DEC-006 | Data: 2026-06-10 | Criticidade: **Alta** (redefine a semântica de um campo ativo do dashboard na camada PARALELA de mercado/residual; **não** altera o M1 oficial). READ-ONLY sobre o M1.
- Status: **APROVADA por Felipe Silva** — decisões de PRODUTO em 2026-06-09; **PLANO TÉCNICO aprovado pelo humano (Felipe/usuário) em 2026-06-10** antes do Builder (esteira Alta).
- Decisão (3 sub-decisões de produto, todas de Felipe 2026-06-09):
  1. **Gate efetivo:** `flag_sam = flag_viavel & faixa_oportunidade ∈ {baixa,media,alta,prioridade_maxima} & (populacao_corte_hex ≥ 5000) & ~flag_canibalizacao_ultra_1km`. REMOVE `top_municipio` (SAM passa a existir fora do recorte top-20%/UF) e o piso trivial `tam_populacao_hex > 0`. MANTÉM `flag_viavel` (preserva de brinde o filtro de renda `renda_target_proxy ≥ RENDA_MIN`) e `~flag_canibalizacao_ultra_1km`. `flag_sam_fitness = flag_sam` (piso `tam_populacao_hex>0` removido por redundância com o corte ≥5000); `sam_fitness_potencial` coercido sem NaN e não-negativo.
  2. **Campo de população do corte = `populacao_corte_hex`/`flag_pop_min_5k`** (régua operacional `POP_MIN_ACIONAVEL=5000` do dashboard, em `derive_pop_cut_columns`: **setor 2022 quando o hex é `granular`, fallback `pop_total` municipal**). NÃO usar `pop_hex_base`/`tam_populacao_hex` (pop rateada por hex, mediana nacional ≈5 hab — cortaria ~97% do SAM). **`granular` é definido como `qualidade_join_uf ∈ {A,B}` AND (`flag_censo_disponivel` OR `score_setor_2022_calibrado` notna)** — NÃO é `flag_censo_elegivel` nem `mask_hex_censo`. Diferença documentada e validada no repro.
  3. **Limiar inclusivo `≥ 5000`.**
- Implementação: lógica de corte EXTRAÍDA para `src/motor_expansao/pipelines/pop_corte.py` (fonte única de verdade), consumida tanto pelo dashboard (`data.py`, comportamento preservado byte-a-byte por delegação fina) quanto pelo pipeline de mercado (`calcular_colunas_mercado.py`). Limiar parametrizado em `POP_MIN_SAM_GATE=5_000` (constante local do pipeline, espelha `POP_MIN_ACIONAVEL`; NÃO entra no §3).
- Evidência-chave (dados reais, 2026-06-10):
  - Repro `87a91b18dffffff` (Santo Amaro da Imperatriz/SC): `granular`, `pop_total_setor_2022=35.4`, `pop_total=27272`; gate antigo dava SAM≈7,28 (passava por `top_municipio`+`tam_populacao_hex>0`); gate novo → `populacao_corte_hex=35.4 < 5000` → `flag_pop_min_5k=False` → **SAM=0**.
  - Volume `flag_sam` True: **196.715 → 27.996** (delta −168.719). A remoção de `top_municipio` expande; o corte ≥5000 reduz mais (filtro forte, não aniquilador). `fonte_populacao_corte`: 740.524 `setor_2022` + 802.007 `total_municipal`.
- Pesos/fórmula/artefatos do M1: **INALTERADOS** (`renda=0.40`/`pop=0.60`, `score_priorizacao`, `hex_score_estrutural`, carteira, plano, artefatos oficiais). Parâmetros canônicos do §3: INALTERADOS (`POP_MIN_SAM_GATE` é constante da camada de mercado, não do M1). `faixa_oportunidade` é LIDA (DEC-001).
- Regeneração dos parquets paralelos de staging = passo OPERACIONAL pós-merge separado (não requisito de fechamento de testes); ordem canônica `híbrido → mercado → calcular_colunas_mercado → carteira → plano → domínio → residual → fase1_bi_exports`. ZERO escrita em artefatos M1 oficiais.
- Referencias: `tasks/backlog.md` (BLK-SAM-01); `context/handoff.md` (Planner/Builder); `docs/modelo_mercado_hexagonos.md` §5.4; `src/motor_expansao/pipelines/calcular_colunas_mercado.py`; `src/motor_expansao/pipelines/pop_corte.py`; CLAUDE.md §4/§5; DEC-001.

### DEC-007 — Afrouxar o gate do SAM: apenas Faixa M1 elegível AND população ≥ 5000 (reverte 2 sub-decisões da DEC-006; BLK-SAM-02)
- ID: DEC-007 | Data: 2026-06-10 | Criticidade: **Alta** (altera o VALOR de um campo ativo do dashboard na camada PARALELA de mercado/residual; reverte 2 sub-decisões de uma DEC aprovada; **não** altera o M1 oficial). READ-ONLY sobre o M1.
- Status: **APROVADA pelo usuário Vinicius em 2026-06-10**, com ciência do impacto (×17 no volume de hexes SAM). PLANO TÉCNICO aprovado pelo humano antes do Builder (esteira Alta).
- Decisão: o gate do SAM em `src/motor_expansao/pipelines/calcular_colunas_mercado.py` passa de
  `flag_sam = flag_viavel & faixa_oportunidade ∈ {baixa,media,alta,prioridade_maxima} & flag_pop_min_5k(≥5000) & ~flag_canibalizacao_ultra_1km`
  para
  `flag_sam = faixa_oportunidade ∈ {baixa,media,alta,prioridade_maxima} & flag_pop_min_5k(≥5000)`.
  **Reverte 2 sub-decisões da DEC-006** (sub-decisão 1): (a) REMOVE `flag_viavel` do gate — junto com ele sai o filtro de renda `renda_target_proxy ≥ RENDA_MIN` e o guard `pop ≥ 1` que vinham "de brinde"; (b) REMOVE `~flag_canibalizacao_ultra_1km` — o SAM passa a INCLUIR áreas já cobertas por unidade Ultra < 1 km. As sub-decisões 2 (campo de população = `populacao_corte_hex`/`flag_pop_min_5k`, régua `pop_corte.py`) e 3 (limiar inclusivo ≥ 5000) da DEC-006 PERMANECEM VIGENTES e intactas.
- `flag_sam_fitness = flag_sam` e `sam_fitness_potencial` (coercido, sem NaN, não-negativo): INALTERADOS — propagam o novo gate automaticamente.
- Labels: `sam_granularidade` e `tese_entrada` NÃO mudam de lógica. Consequência semântica aceita: um hex `flag_sam=True & flag_canibalizacao_ultra_1km=True` (não híbrido) recebe `sam_granularidade = "municipio_priorizado"` (a cláusula `flag_sam` vence `flag_canibal` na precedência do `np.select`), não `"bloqueado_rede_ultra"`; `tese_entrada` continua `"proteger_rede_atual"` para qualquer hex canibal (1º critério). `"bloqueado_rede_ultra"` segue existindo só para hexes canibais que NÃO entram no SAM.
- Guardrail interno obsoleto: o assert `(flag_sam & flag_canibalizacao_ultra_1km).sum() == 0` em `validar()` foi REMOVIDO (a regra deixou de existir).
- Evidência-chave (dados reais, 2026-06-10): `flag_sam` True **27.996 → 479.568** (+451.572, ≈ ×17). Breakdown: ~451.496 entram por dropar `flag_viavel` (filtro de renda); 76 por dropar `~canibalização`.
- Pesos/fórmula/artefatos do M1: **INALTERADOS** (`renda=0.40`/`pop=0.60`, `score_priorizacao`, `hex_score_estrutural`, carteira, plano, artefatos oficiais). Parâmetros canônicos do §3: INALTERADOS. `faixa_oportunidade` é LIDA (DEC-001). Régua `populacao_corte_hex`/`flag_pop_min_5k` e helper `pop_corte.py`: INTOCADOS (só consumidos).
- Relação com a DEC-006: a DEC-006 fica **SUPERSEDED nas 2 sub-decisões revertidas** (manutenção de `flag_viavel` e de `~flag_canibalizacao_ultra_1km` no gate); permanece vigente no restante (campo de corte, régua e limiar ≥5000).
- Regeneração dos parquets paralelos de staging = passo OPERACIONAL pós-merge separado (não requisito de fechamento de testes); ordem canônica `híbrido → mercado → calcular_colunas_mercado → carteira → plano → domínio → residual → fase1_bi_exports` (+ enriquecido derivado). ZERO escrita em artefatos M1 oficiais.
- Referências: `tasks/backlog.md` (BLK-SAM-02); `context/handoff.md` (Planner/Builder); `docs/modelo_mercado_hexagonos.md` §5.4; `src/motor_expansao/pipelines/calcular_colunas_mercado.py`; CLAUDE.md §4/§5; DEC-001; DEC-006.

### DEC-008 — Supersessão do BLK-SCORE-05 pela epic BLK-DIM (Motor de Dimensionamento e Viabilidade — camada paralela)
- ID: DEC-008 | Data: 2026-06-12 | Criticidade: **Estratégica** (nova camada paralela / nova fase de modelagem; substitui um bloco do backlog). READ-ONLY sobre o M1.
- Status: **APROVADA por Felipe Silva em 2026-06-12** (escolha de produto: substituir SCORE-05 pela epic BLK-DIM; insumos externos serão disponibilizados).
- Decisão: **SUPERSEDER o BLK-SCORE-05** (diagnóstico read-only de viabilidade de proxy exógeno de demanda) pela epic **BLK-DIM-00..04**, derivada do spec `modelo_dimensionamento_expansao.md` (CEO, 2026-06-10; origem nos testes do projeto externo `Análise Preditiva`, base de 54 academias). A epic implementa um **motor inverso de 4 camadas**: (1) Potencial = `pop_captação × aderência_calibrada × ajuste_renda` (NÃO "20% fixo"); (2) Captura = share via **Huff/gravitacional** (concorrência OSM, saturação, canibalização); (3) Dimensionamento = `m²_ideal = alunos_alvo / curva_densidade(m²)` (densidade cai com tamanho, corr −0,26; existe faixa ótima; vagas só como restrição); (4) Unit economics determinística (faturamento/margem/aluguel-teto/payback/ROIC sobre **pagantes** líquidos de churn/inadimplência, com curva de maturação). A pergunta de GO/NO-GO honesto do SCORE-05 (a aderência/penetração é calibrável?) é a **Camada 1** e ficou **dobrada no BLK-DIM-01** com a disciplina dos 3 bloqueios estruturais (viés de seleção, alvo pós-maturação, sinal exógeno ≈ nulo).
- Relação com a DEC-001: **DEC-001 permanece intacta** — a epic é **camada PARALELA, READ-ONLY sobre o M1**; NÃO recalibra `score_priorizacao`/`hex_score_estrutural`/pesos nem regenera artefatos oficiais. O BLK-DIM-00 deriva maturação real das datas de abertura (endereça o gate G1 da DEC-001 sem alterar o M1).
- Metodologia não-negociável (todos os blocos de modelagem, §7 do spec): LOO-CV / k-fold repetido **sempre contra baseline da média**; BANIR R² in-sample e `fit(X,y)→predict(X)`; começar simples (linear regularizado/GLM); toda saída com intervalo de predição + flag de extrapolação.
- Sequenciamento aprovado: **BLK-DIM-03 primeiro** (camadas 3-4, determinístico, prototipável com os dados que já existem) → DIM-00 → DIM-01 (gate) → DIM-02 → DIM-04. Camadas 1-2 só avançam se as lacunas de dados fecharem no DIM-00.
- Insumos (auditoria 2026-06-12): EXISTEM no repo as 54 unidades Ultra com metragem/faturamento/pagantes/ticket/alunos-por-m² (`unidades_ultra_performance_hex.parquet`), a concorrência OSM (`concorrentes_mapeados.parquet`, 3.296 un.), a camada de mercado/residual e o helper de catchment `analisar_entorno_ponto`. FALTAM (Felipe disponibilizará): série diária das ~60 maduras (vendas/cancelamentos/churn), datas de abertura por unidade (`maturacao_status` hoje 100% `maturacao_indisponivel`), `ULTRA padrão - Simulador Financeiro.xlsx`, e `modelo_demanda.py`/`teste_densidade.py` (referência).
- Pesos/fórmula/artefatos do M1: **INALTERADOS** (`renda=0.40`/`pop=0.60`, `score_priorizacao`, `hex_score_estrutural`, carteira, plano, artefatos oficiais). Parâmetros canônicos do §3: INALTERADOS.
- Referências: `modelo_dimensionamento_expansao.md` (raiz); `tasks/backlog.md` (epic BLK-DIM-00..04, substitui BLK-SCORE-05); CLAUDE.md §3/§4; DEC-001.

### DEC-009 — Pivô da epic BLK-DIM para "property-first" (viabilidade/break-even); previsão de demanda pela geografia encerrada
- ID: DEC-009 | Data: 2026-06-15 | Criticidade: **Estratégica** (redefine o rumo e o entregável da epic BLK-DIM). READ-ONLY sobre o M1.
- Status: **APROVADA por Felipe Silva em 2026-06-15.**
- Decisão: **pivotar a esteira** de *"prever o melhor lugar"* para *"stress-testar a viabilidade de um imóvel real"*. O operador traz um imóvel (`lat,lng` + `m²` + aluguel) e a ferramenta devolve faixa de alunos pela curva tamanho→densidade, break-even, aluguel-teto, ROI e sensibilidade demanda×aluguel — **software faz a conta, humano decide o imóvel**. Materializado: **BLK-DIM-11** (engine `viabilidade_ponto.py`, concluído) + **BLK-DIM-12** (UI no dashboard). O **Caminho B (BLK-DIM-DATA) é REDEFINIDO**: só faz sentido atrás de **atributos de imóvel** (visibilidade, fluxo, esquina), NÃO de mais dado demográfico.
- Princípio operacional não-negociável: a **demanda entra como PREMISSA EXPLÍCITA** (input do operador ou faixa de comparáveis), **NUNCA prevista pela geografia**. Proibido reintroduzir "20% fixo" ou regressão geográfica de demanda como preditor. A triagem de **ONDE** olhar segue com o M1/censitário (camada executiva, intacta) — o que se encerra é só a contagem fina de alunos por ponto.
- Evidência-chave (4 NO-GOs honestos, dado real, LOO vs baseline; relatórios gitignored em `data/analysis/`): DIM-01R `R²_LOO=−0,01` (pop+renda); DIM-05 (features exógenas) NO-GO; DIM-08 AUC residual `0,48 ≈ acaso` (não discrimina ≥2k de <2k); spike de densidade `R²_LOO=−0,01` (geografia) vs `+0,10` (metragem). DIM-02R Huff "GO técnico, mas não agrega" (LOO −0,25). **Único sinal usável = curva tamanho→densidade** (metragem, corr −0,37; DIM-03R) + DRE determinístico (DIM-06, camadas 3+4 `R²=+0,23`).
- Relação com DEC-001/DEC-008: **DEC-001 permanece intacta** (M1 não recalibrado). **DEC-008 permanece** (a epic existe e é paralela), mas seu sequenciamento original ("Camadas 1-2 são o coração") é **superado pela evidência**: as Camadas 1-2 (demanda/captura) ficam **encerradas como NO-GO com o dado interno**, e o valor da epic concentra-se nas Camadas 3-4 (dimensionamento/viabilidade).
- Pesos/fórmula/artefatos do M1: **INALTERADOS** (`renda=0.40`/`pop=0.60`, `score_priorizacao`, `hex_score_estrutural`, carteira, plano, artefatos oficiais). Parâmetros canônicos do §3: INALTERADOS.
- Referências: `tasks/completed.md` (BLK-DIM-01R/05/06/08/02R/11); `tasks/backlog.md` (BLK-DIM-10 resolução, BLK-DIM-11, BLK-DIM-12, BLK-DIM-DATA redefinido); `data/analysis/{aderencia_real,features_aderencia,backtest_dim,residual_discriminacao,densidade_contexto}.md` (gitignored); `modelo_dimensionamento_expansao.md` §5; DEC-001; DEC-008.

### DEC-010 — Resolução de endereço por fetch HTTP na barra de busca do dashboard (BLK-UI-08)
- ID: DEC-010 | Data: 2026-06-17 | Criticidade: **Alta** (desvia do guardrail §2 "não criar dependência de API ao vivo no dashboard de produção"; READ-ONLY sobre o M1).
- Status: **APROVADA pelo usuário Vinicius em 2026-06-17.**
- Decisão: permitir, **restrito ao caminho de resolução de ENDEREÇO da barra de busca por coordenada** do dashboard (`render_coord_search_sidebar`), uma requisição HTTP leve para converter texto livre (endereço) em coordenada. O campo aceita tanto `lat,lng` quanto endereço; o caminho numérico (`parse_coordinate_input`) é SEMPRE tentado primeiro e fica INTOCADO; só quando ele falha o texto é tratado como endereço e resolvido por `resolve_endereco_http` (helper novo em `src/motor_expansao/api/maps_geocoder.py`, **`urllib.request` puro, sem Selenium**), que segue o redirect da URL de busca do Google Maps e extrai a coordenada via `extract_any_coord`, validando o bounding box do Brasil. Escolha do humano = **Alternativa B** (fetch automático) com fallback gracioso para o link clicável da Alternativa A.
- Justificativa: o operador quer digitar um endereço e cair direto no hexágono, sem o passo manual de copiar a coordenada do Maps. O desvio é local à busca e não afeta o restante do dashboard.
- Provedor / atribuição / anti-PII: Google Maps (busca pública via redirect). **O endereço digitado é ENVIADO ao Google** para resolver a coordenada — registrado aqui e exibido como aviso curto no campo de busca (anti-PII: nenhum dado é persistido além do par lat,lng no cache local). Sem chaves de API; uma requisição leve por consulta nova.
- Mitigações obrigatórias (parte da decisão):
  - (a) **Escopo restrito:** fetch HTTP SÓ no caminho de resolução de endereço da barra de busca. NADA de rede na carga/interatividade do dashboard nem no mapa (Blocos 4–6 e basemap seguem como estão; DEC-004 inalterada).
  - (b) **Cache local** das resoluções em `data/cache/geocode/` (gitignored, coberto pelo padrão `data/cache/` do `.gitignore`); minimiza requisições repetidas.
  - (c) **Fallback offline gracioso:** sem rede/falha/timeout → exibe link clicável do Maps (`build_search_url`, função PURA) + instrução para copiar/colar a coordenada, sem exceção. Default seguro sem rede em CI/teste, coberto por teste com mock (a suíte NUNCA bate na rede real).
  - (d) **Timeout curto** (6 s) + `try/except` amplo na função e no chamador; a interatividade offline do dashboard NUNCA quebra se a rede faltar.
  - (e) **I/O isolado** no helper de `maps_geocoder.py` (camada `api`, `urllib` puro, sem Selenium); o import no dashboard é lazy e a rede só ocorre na chamada de busca por endereço.
  - (f) **Nota anti-PII + atribuição** registradas nesta DEC e no aviso do campo (mitigação (c)/(f)).
- Pesos/fórmula/artefatos do M1: **INALTERADOS** (`renda=0.40`/`pop=0.60`, `score_priorizacao`, `hex_score_estrutural`, carteira, plano, artefatos oficiais). Camada de visualização/consumo (§5 guardrail permanente).
- Parâmetros canônicos do §3: INALTERADOS (nenhum parâmetro de score; só adição do helper de rede e do diretório de cache de geocode).
- Referências: `tasks/backlog.md` (BLK-UI-08); `context/handoff.md` (Planner/Builder); `src/motor_expansao/api/maps_geocoder.py` (`resolve_endereco_http`); `src/motor_expansao/dashboard/pages.py` (`render_coord_search_sidebar`); CLAUDE.md §2/§4/§5; DEC-004 (precedente de desvio do §2 restrito a um caminho específico).
- **Emenda 2026-06-17 (BLK-UI-08-FU1, APROVADA por Vinicius): provedor de geocoding = Nominatim/OpenStreetMap, não Google.** Na validação visual o fetch `urllib` puro contra o Google Maps NÃO resolvia coordenada (a página do Maps é renderizada por JS; sem navegador a URL final não traz o pino `!3d/!4d` nem `@lat,lng`), então a Alternativa B só caía no fallback. Correção: `resolve_endereco_http` passa a fazer um GET ao **Nominatim** (`https://nominatim.openstreetmap.org/search?format=jsonv2&limit=1&countrycodes=br&q=...`) e lê `lat/lon` do JSON — HTTP puro `urllib`, sem navegador, funciona de fato. Consequências: (i) o endereço digitado passa a ser enviado ao **OpenStreetMap/Nominatim** (não ao Google) — nota anti-PII e atribuição **© OpenStreetMap contributors** atualizadas; (ii) enviamos **User-Agent identificável** (`motor-expansao-ultra/...`) conforme a [Nominatim Usage Policy](https://operations.osmfoundation.org/policies/nominatim/) (máx. 1 req/s, sem uso em massa) — uso sob demanda do dashboard fica folgado e o **cache local `data/cache/geocode/`** (mitigação (b), inalterada) cobre a exigência de caching. As demais mitigações (a)/(c)/(d)/(e)/(f) seguem VIGENTES; o fallback offline (c) ainda usa o link clicável do Google Maps (`build_search_url`, função pura) só como atalho manual. READ-ONLY sobre o M1 inalterado.
- **Emenda 2026-06-19 (BLK-UI-09, APROVADA por Vinicius — Opção B): link do Google Maps como 3º formato da busca, incluindo seguir o redirect de LINKS CURTOS via rede.** A barra de busca (`render_coord_search_sidebar`) passa a aceitar TRÊS formatos: coordenada `lat,lng` (numérico, INTOCADO) → **link do Maps** (NOVO) → endereço (Nominatim, INTOCADO). URLs longas (`!3d/!4d` ou `@lat,lng`) são resolvidas **offline por regex pura** (`extract_any_coord`, sem rede). Para **links curtos** (`maps.app.goo.gl`, `goo.gl/maps`) — que não trazem a coordenada na string — o humano aprovou seguir o **redirect HTTP** para obter a URL longa final e então extrair a coordenada: novo helper `resolve_short_link(url, *, timeout=6.0) -> str | None` em `src/motor_expansao/api/maps_geocoder.py` (camada `api`, `urllib.request` puro, lê `resp.geturl()` após os redirects), import lazy no dashboard. Este é um **3º sub-caminho de rede** que ESTENDE a DEC-010 (antes: rede só no caminho de endereço). Mitigações herdadas da DEC-010, todas VIGENTES e obrigatórias: (a) escopo restrito — rede SÓ neste sub-caminho da busca, nada na carga/interatividade do dashboard nem nos Blocos 4–6/basemap; (c) fallback gracioso — sem rede/falha/timeout → mensagem clara pedindo a URL longa ou a coordenada, sem exceção (a suíte NUNCA bate na rede real, testada com mock de `urllib`); (d) timeout curto (6 s) + `try/except` amplo; (e) I/O isolado em `maps_geocoder.py` + import lazy; (f) nota anti-PII — a URL curta é enviada ao Google **só** para seguir o redirect; nada é persistido além do par lat,lng. O resultado de qualquer caminho de link é validado contra o bounding box do Brasil (`_validate_brazil_bbox`); fora do Brasil → `None` + mensagem. Pesos/fórmula/artefatos do M1: **INALTERADOS**; parâmetros canônicos do §3: INALTERADOS (só helper de rede + roteamento de UI). READ-ONLY sobre o M1. Referências: `tasks/backlog.md` (BLK-UI-09); `context/handoff.md` (Planner/Builder); `src/motor_expansao/api/maps_geocoder.py` (`resolve_short_link`, `extract_any_coord`); `src/motor_expansao/dashboard/pages.py` (`render_coord_search_sidebar`).

### DEC-011 — Fundo de ruas por tiles online no Relatório Municipal (BLK-RELMUN-01); e critério/rótulo dos hexágonos destacados
- ID: DEC-011 | Data: 2026-06-22 | Criticidade: **Alta** (estende a DEC-004 — desvio do guardrail §2 "não criar dependência de API ao vivo no dashboard de produção" — a um NOVO relatório; e fixa a semântica de "hexágono destacado/amarelo" da camada de VISUALIZAÇÃO do novo relatório). READ-ONLY sobre o M1.
- Status: **APROVADA por Vinicius em 2026-06-22** (gate humano do BLK-RELMUN-01; D3 = tiles online, fiel ao template).
- Contexto: o **Relatório Municipal** (BLK-RELMUN-01) é um novo PDF de 8 páginas, gerável e baixável após selecionar UM município na aba Mapa Territorial, que COEXISTE com o Relatório Pontual Censitário (raio 1,5 km) — este último fica INTOCADO. O template (`docs/relatorio_municipal_template.md`) exige mapas com fundo de ruas estilo GeoFusion.
- Decisão (2 partes):
  1. **Tiles online no Relatório Municipal.** A DEC-004 permitiu tiles de basemap claro (CartoDB Voyager via `contextily`, extra `[basemap]`, composição em EPSG:3857) **restrito ao caminho de geração do Relatório Pontual**. A DEC-011 **ESTENDE essa permissão ao caminho de geração do Relatório Municipal**, com TODAS as mitigações da DEC-004 vigentes e obrigatórias: (a) **cache local** em `data/cache/basemap_tiles/` (gitignored); (b) **fallback offline gracioso** — sem internet/tiles/`contextily` → mapa sobre canvas claro SEM ruas, sem exceção; default seguro em CI/teste (`basemap=False`), coberto por teste; (c) **import lazy** de `contextily` dentro da função de fundo — a CARGA/interatividade do dashboard NÃO faz fetch de tiles (só a geração do relatório, sob demanda); o extra `[basemap]` segue embutido na imagem de produção (`Dockerfile.streamlit`); (d) distorção do mapa em EPSG:3857 desprezível na escala municipal; atribuição "© OpenStreetMap, © CARTO" no rodapé do mapa/PDF.
  2. **Critério e rótulo dos "hexágonos destacados" (amarelos) do Relatório Municipal** (camada de visualização do novo relatório; NÃO altera nenhum gate/score/artefato do M1 nem do pipeline de mercado): um hexágono é **destacado** quando `sam_fitness_potencial >= 3000` **E** `oferta_efetiva_disponivel >= 2000` (ambas colunas existentes, em alunos). O **rótulo exibido sobre cada hexágono destacado** = `oferta_efetiva_disponivel` (Residual Fitness daquele hexágono). O **"Espaço para academias"** (Páginas 2 e 8) = `round( Σ oferta_efetiva_disponivel dos hexágonos destacados ÷ 2.500 )` (capacidade default 2.500 alunos/unidade, §4). São limiares de DISPLAY do relatório, locais a `relatorio_municipal.py`; NÃO mexem em `flag_sam`/DEC-006/DEC-007 (que vivem no pipeline de mercado) nem no M1.
- Demais decisões do gate (D2, D4–D9) travadas em `tasks/current_task.md` e no handoff do ciclo; READ-ONLY sobre o M1 em todas.
- Pesos/fórmula/artefatos do M1: **INALTERADOS** (`renda=0.40`/`pop=0.60`, `score_priorizacao`, `hex_score_estrutural`, carteira, plano, artefatos oficiais). Parâmetros canônicos do §3: INALTERADOS (nenhum parâmetro de score; só extensão do extra `[basemap]` ao novo relatório + constantes de display do módulo novo). Camada de visualização/relatório (§5 guardrail permanente).
- Referências: `tasks/backlog.md` (BLK-RELMUN-01); `docs/relatorio_municipal_template.md`; `context/handoff.md` (Planner/Builder); `src/motor_expansao/dashboard/relatorio_municipal.py` (novo); `src/motor_expansao/dashboard/censo_map.py` (padrão de tiles da DEC-004, reusado em leitura); CLAUDE.md §2/§4/§5; DEC-004 (precedente direto).
- **Emenda 2026-07-02 (BLK-RELMUN-03, APROVADA por Vinicius): critério do "hexágono destacado/válido" do Relatório Municipal passa a ser SÓ Residual Fitness.** O item de critério de destaque da parte 2 muda de `sam_fitness_potencial >= 3000` **E** `oferta_efetiva_disponivel >= 2000` para **somente `oferta_efetiva_disponivel >= OFERTA_DESTAQUE_MIN (2000)`** — o termo de SAM Fitness (≥ 3.000) é REMOVIDO de `_hex_destacado_mask` em `relatorio_municipal.py`. Consequência de produto (aprovada, esperada, NÃO é bug): passam a contar também os hexes com `oferta_efetiva_disponivel ≥ 2000` e `sam_fitness_potencial < 3000` → mais hexágonos destacados/"amarelos", "Espaço para academias" igual ou MAIOR, e contagem "Aprovados" maior em todos os relatórios já gerados/futuros. O rótulo sobre cada hex destacado (= `oferta_efetiva_disponivel`) e a fórmula "Espaço para academias" = round(Σ oferta_efetiva_disponivel dos destacados ÷ 2.500, capacidade 2.500 §4) permanecem. Constante `SAM_DESTAQUE_MIN`: **REMOVIDA** (ficou sem uso após a mudança; rastro no git/DEC-011/completed.md). É mudança de DISPLAY LOCAL ao Relatório Municipal (limiares em `relatorio_municipal.py`): **NÃO toca `flag_sam`/DEC-006/DEC-007** (gate do SAM no pipeline de mercado, `calcular_colunas_mercado.py`), nem `score_priorizacao`/pesos/`hex_score_estrutural`/carteira/plano/artefatos oficiais do M1 (READ-ONLY, §5). A parte 1 da DEC-011 (tiles online no Relatório Municipal) e as demais decisões do gate (D2, D4–D9) permanecem INTACTAS. Referências: `tasks/backlog.md` (BLK-RELMUN-03); `context/handoff/20260702-121318-planner.md`; `src/motor_expansao/dashboard/relatorio_municipal.py` (`_hex_destacado_mask`); `tests/unit/test_relatorio_municipal.py`.
- **Emenda 2026-07-08 (BLK-RELMUN-05, APROVADA por Vinicius): a COR de exibição dos hexágonos aprovados na "Visão Geral do Município" (camada `cobertura`) e no Resumo (camada `resumo`) do Relatório Municipal passa de amarelo/laranja para VERDE (2 tons — otimismo).** Mudam SOMENTE `_COR_APROVADO_PROPRIO` (255,210,28)→**(20,170,80)** (verde forte, dado próprio) e `_COR_APROVADO_MUNICIPAL` (245,140,30)→**(90,190,120)** (verde médio, fallback municipal) em `relatorio_municipal.py`; `_COR_REPROVADO` (cinza, `(150,156,170)`) e as cores de ZONA da página Domínio (`_ZONA_CORES_*`) permanecem INTOCADOS. O TEXTO visível "hexágonos amarelos" das páginas 2/8 ("Espaço para academias") vira **"hexágonos destacados"** (wording neutro). É mudança de **DISPLAY LOCAL** a `relatorio_municipal.py` (§5 guardrail permanente): o CRITÉRIO de "hexágono destacado" (`oferta_efetiva_disponivel >= OFERTA_DESTAQUE_MIN=2000`, emenda BLK-RELMUN-03 acima), os IDENTIFICADORES (`n_hex_amarelos`, `soma_oferta_amarelos`, `parcelas_amarelos`, chaves de `result` — NÃO renomeados nem acentuados), `flag_sam`/DEC-006/DEC-007, `score_priorizacao`/pesos/`hex_score_estrutural`/carteira/plano/artefatos oficiais do M1 permanecem TODOS INTOCADOS (READ-ONLY, §5). As partes 1/2 da DEC-011 e a emenda BLK-RELMUN-03 permanecem INTACTAS. Referências: `tasks/backlog.md` (BLK-RELMUN-05); `context/handoff/20260708-154926-builder.md`; `src/motor_expansao/dashboard/relatorio_municipal.py` (`_COR_APROVADO_PROPRIO`/`_COR_APROVADO_MUNICIPAL`); `tests/unit/test_relatorio_municipal.py`.
- **Emenda 2026-07-10 (BLK-RELMUN-05-FU1, APROVADA por Vinicius): o fallback municipal fica mais amarelado.** Só `_COR_APROVADO_MUNICIPAL` muda de **(90,190,120)** (verde médio) para **(215,200,60)** (amarelo-âmbar), para distinguir melhor do dado próprio; `_COR_APROVADO_PROPRIO` (20,170,80) e `_COR_REPROVADO` (150,156,170) INALTERADOS. Ajuste de DISPLAY LOCAL (§5): critério de destaque, identificadores, `flag_sam`, score e artefatos M1 seguem INTOCADOS. Ref.: `tests/unit/test_relatorio_municipal.py` (`test_cores_aprovados_verdes_blk_relmun_05`).

### DEC-012 — Adoção da camada paralela de Demanda Revelada (H3 res-7, sem PII; BLK-TP-01)
- ID: DEC-012 | Data: 2026-06-24 | Criticidade: **Estratégica/Alta** (nova camada paralela de dados que ingere insumo externo com **PII na origem**; **READ-ONLY sobre o M1**). Exigiu gate humano (LGPD/anonimização).
- Status: **APROVADA por Felipe Silva em 2026-06-24** (gate humano do BLK-TP-01; adotadas TODAS as recomendações do Planner para as 6 perguntas).
- Contexto: a DEC-009 encerrou a previsão de *magnitude de demanda* pela geografia interna (renda/pop têm sinal nulo no M1 — DEC-001); faltava um sinal de **demanda observada**. Um insumo externo de benefício corporativo de academias (centrado na Smart Fit, concorrente low-cost da Ultra — §1) fornece **demanda paga georreferenciada já agregada por célula**. Análise exploratória interna (2026-06-24, `NAO_ABRA/`, gitignored) indicou Spearman **+0,52** vs. `score_oportunidade_residual` e **+0,75** vs. alunos capturados — primeira validação externa positiva forte de uma camada do Motor.
- Decisão: **adotar a camada de Demanda Revelada** como camada PARALELA, materializada em `data/staging/demanda_revelada_h3.parquet` (H3 res-7, gitignored, NÃO oficial do M1), casável por `hex_id` com `hexagonos_mercado_mapeado.parquet`, isolada em `src/motor_expansao/demanda_revelada/` (pacote disjunto — NUNCA importa de `pipelines/m1/`, `censo_*`, `dashboard/`). Contrato canônico de **9 colunas genéricas** (nomes NÃO acoplados à marca): `hex_id`, `membros`, `membros_gt5km_concorrente_lc`, `dist_concorrente_lc_min_m`, `n_celulas_agregadas`, `n_acad_parceiras`, `alunos_parceiras`, `n_concorrente_lc`, `versao_contrato` (`demanda_revelada_v1`). `dist_concorrente_lc_min_m`/`n_concorrente_lc` vêm do próprio dump; `concorrentes_mapeados.parquet` entra **só como cross-check** do relatório de qualidade. Versão res-8: **ADIADA** (fora deste bloco). **Sem extra `[demanda]`** no `pyproject.toml` (usa só pandas/numpy/pyarrow/h3 da base + `re`/`json` stdlib).
- **Princípios anti-PII por construção (não-negociáveis):** a ingestão consome **apenas as 4 estruturas já agregadas** (`CELLS`/`GYMS_PTS`/`SF`/`BANDS`); qualquer outra `var` do dump é IGNORADA; `employee_id`/`company_id`/coordenadas residenciais individuais **nunca** são lidos para o staging nem persistidos em arquivo/log/cache; a agregação para H3 ocorre na **fronteira de entrada**; o artefato e os testes contêm **zero** PII (`COLUNAS_PII_PROIBIDAS` + teste `test_zero_pii` falham se qualquer coluna proibida existir); a **fonte real nunca é versionada** (`NAO_ABRA/`, gitignored); os testes usam **fixture sintética** (`tests/fixtures/demanda_revelada_fake.html`).
- **Procedência/licença:** **licença plenamente liberada** (Felipe Silva, 2026-06-24) — procedência confirmada inclusive para produtização (não trava os sucessores BLK-TP-02..05).
- **Relação com a DEC-001:** **INTACTA** — a camada NÃO recalibra `score_priorizacao`/`hex_score_estrutural`/pesos (`renda=0.40`/`pop=0.60`) nem regenera artefatos oficiais; é READ-ONLY sobre o M1 (§5 guardrail permanente).
- **Relação com a DEC-009:** **INTACTA** — a demanda entra como **insumo OBSERVADO**, NUNCA como preditor geográfico de magnitude. Proibido usar `membros` ou qualquer coluna desta camada como input em regressão geográfica de demanda ou como ajuste do `score_priorizacao` (o re-teste honesto demanda→captura é bloco próprio, BLK-TP-05, sob a metodologia LOO/k-fold vs baseline da DEC-008).
- **Caveats de cobertura (relatório gitignored `data/reports/scratch/demanda_revelada_qualidade.md`):** casa com ~1% do universo de hexes do Motor (16.575 hexes, ~99% casam no Motor; concentração em SP) → camada de **refino sobre metrópoles**, NÃO cobertura nacional; coords arredondadas (~1 km) → ruído no join res-7; **não substitui M1/censitário** (§1).
- Parâmetros canônicos do §3: **INALTERADOS** (a camada não introduz parâmetro de score; `H3_RESOLUTION=7` reutilizado como chave de join).
- Referências: `tasks/backlog.md` (Epic BLK-TP / BLK-TP-01); `context/handoff.md` (Planner/Builder); `src/motor_expansao/demanda_revelada/{contrato,ingestao}.py`; `tests/unit/test_demanda_revelada_ingestao.py`; `docs/modelo_mercado_hexagonos.md` (join por `hex_id`); CLAUDE.md §1/§2/§4/§5; DEC-001; DEC-009.

### DEC-013 — Coleta recorrente de concorrentes (GymScraping) automatizada na VPS + integração ao residual (READ-ONLY M1)
- ID: DEC-013 | Data: 2026-06-26 | Criticidade: **Alta** (novo processo autônomo recorrente em produção que escreve dados consumidos pelo dashboard/API; **READ-ONLY sobre o M1**). Aprovada por Felipe Silva em 2026-06-26.
- Contexto: a base de concorrentes do Motor estava desatualizada (prod tinha ~14 redes pequenas; o scraper do Vini — `VinhoAbencoado/GymScraping`, privado — tem **90 coletores** + agregadores WellHub/TotalPass). A coleta era 100% manual (rodar local + `scp`), criando dependência de pessoas. A antiga "Opção B" de `docs/infra_producao.md` (pipeline na VPS) nunca tinha sido implementada.
- Decisão (3 partes):
  1. **Automatizar a coleta semanal na própria VPS**, em container Docker dedicado, **autônoma e auto-atualizável**: deploy key read-only clona `/opt/gymscraping`; runner `/opt/gymscraping-infra/run_weekly_90.sh` faz `git pull` (coletores novos do Vini) → `docker build` → lote dos 90 (`--workers 3 --scheduler-policy weighted`, container `--user 0:0`) → **relatório de crescimento por rede** → integração ao motor → restart. Cron `0 6 * * 0` (dom 03:00 BRT). Logs em `/var/log/gymscraping/`.
  2. **Integrar ao residual via regen da camada PARALELA mercado/residual, estritamente READ-ONLY sobre o M1:** a cadeia `normalizar_concorrentes → calcular_colunas_mercado → gerar_carteira_acionavel → gerar_plano_expansao_curto_prazo → gerar_plano_expansao_dominio → enriquecer_outputs_residual_mercado → fase1_bi_exports.materialize_enriched_dashboard()` roda pelo checkout do motor em `/opt/motor-expansao/app` (imagem do `streamlit`, `PYTHONPATH=/app/src`, `ROOT=/app`). Usa-se **`materialize_enriched_dashboard()`** (só o artefato enriquecido derivado), **NÃO** o `main()` do `fase1_bi_exports` — logo **`hexagonos_brasil_dashboard.parquet` e os artefatos oficiais do M1 NÃO são recompostos** (verificado por mtime). Insumos de staging M1/censitário enviados **uma vez** por `scp` (~68 MB).
  3. **WellHub/TotalPass (agregadores, >25 mil academias de bairro/independentes): coletar e ARMAZENAR como ativo bruto, sem integrar ao residual ainda.** Cuidado com DEDUP (muitas já mapeadas direto nos sites dos concorrentes) e capacidade variável por tipo. A integração deles ao residual é **epic futura** (Huff por tipo de rede + dedup, usando as bases `NAO_ABRA/`), sob gate humano e metodologia da DEC-008. Cadência prevista: **mensal** (~20h), separada da semanal.
- **Relação com DEC-001 / DEC-009:** **INTACTAS** — não recalibra `score_priorizacao`/`hex_score_estrutural`/pesos nem regenera artefatos oficiais; concorrente entra só na camada de mercado/residual. Censitário (`score_setor_2022_calibrado`) também intocado.
- **Guardrail §6 (comando manual na VPS exige confirmação):** o cron é a **pré-aprovação explícita** desta automação recorrente; alterações no runner/cadeia/escopo exigem nova decisão. A coleta NÃO é dependência de runtime do dashboard (é batch que escreve dados; o dashboard/API seguem offline sobre Parquets locais — não viola o §2 "sem API ao vivo no dashboard").
- Pesos/fórmula/artefatos do M1: **INALTERADOS**. Parâmetros canônicos do §3: **INALTERADOS**.
- Referências: `docs/infra_producao.md` (§ Integração com gymscraping — runbook completo); `VinhoAbencoado/GymScraping` (Dockerfile/requirements, PR #1); `/opt/gymscraping-infra/` (runner + relatório de crescimento, na VPS); CLAUDE.md §4/§5/§6; DEC-001; DEC-008; DEC-009; DEC-012.

### DEC-014 — Score de retenção territorial (camada paralela M2 READ-ONLY sobre o M1; BLK-LTV-04)
- ID: DEC-014 | Data: 2026-07-01 | Criticidade: **Estratégica** (cria um EIXO DE SCORE novo — camada paralela; **NÃO** altera o M1 oficial). READ-ONLY sobre o M1.
- Status: **APROVADA por Felipe Silva em 2026-07-01** (gate humano do BLK-LTV-04; 4 decisões de produto fechadas — ver abaixo).
- Contexto: sucessor CONDICIONAL do **BLK-LTV-03 = GO** (correlação território × retenção/LTV maturidade-robusta). O epic BLK-LTV integrou a base Lifetime (retenção/LTV por unidade) ao Motor pela ponte unidade→hex; o LTV-03 confirmou que o território (via `score_priorizacao`) prevê retenção. Este bloco compõe o score paralelo que operacionaliza esse sinal — sem jamais tocar o M1.
- Decisão — compor um **score de retenção territorial por hexágono** `score_retencao = clip(w_cap·score_priorizacao + w_ret·retencao_norm, 0, 100)`, com as **4 decisões de produto do gate**:
  1. **Pesos = variante A: `w_cap=0.50` / `w_ret=0.50`** (equilíbrio captação↔retenção; escolhido sobre B 0.60/0.40 e C 0.40/0.60).
  2. **Fallback em NO-GO = (i) ENCERRAR sem score** (desfecho honesto DEC-008: se a retenção territorial não bate o baseline, o bloco encerra sem gerar score; NÃO degradar para `score_priorizacao` como proxy).
  3. **Modelo do eixo retenção = numpy puro** (mínimos quadrados regularizados / ridge em numpy/scipy; **sem introduzir `scikit-learn`** como dependência — mantém o pacote `lifetime/` sem dep nova).
  4. **Nome = `score_retencao`** (coluna final `score_retencao`; parquet `data/staging/score_retencao_territorial.parquet`; relatório `data/analysis/relatorio_score_retencao.md`; `versao_contrato=score_retencao_v1`). Todos gitignored / NÃO oficiais.
- Fórmula e réguas (aprovadas): eixo **captação** = `score_priorizacao` (0–100) **LIDO** do M1 (composto renda 0.40 + pop 0.60 — o carregador do sinal do LTV-03, NÃO renda pura); eixo **retenção** = `retencao_norm = 100·percentil_nacional(retencao_prevista_territorial)`, onde `retencao_prevista_territorial` é a predição de um modelo territorial calibrado **fora-de-fold** contra `LTV_PROSPECTIVO_12M_MEDIANO` (agregado por unidade, N=56). Régua 0–100 com clip; `flag_extrapolacao` para hexes fora do envelope de calibração.
- Validação (DEC-008, honrada): **k-fold 5×5 + LOO fallback vs baseline da média**, R²_oof + Spearman rho_oof com **IC95 bootstrap (seed=42)**; **R² in-sample e `fit(X,y)→predict(X)` BANIDOS**; maturidade (de `data/staging/growth_api_historico.parquet`, campo `inauguracao`) entra **SÓ como covariável de controle na validação**, NUNCA como feature do score por hex. **Critério de NO-GO honesto:** o eixo retenção só se justifica se, no melhor modelo out-of-fold, `R²_oof>0` (IC não cruza zero) **E** `rho_oof≥0.30` (IC não cruza zero) **E** superar `score_priorizacao` sozinho; caso contrário → NO-GO → encerrar sem score (decisão 2).
- Evidência-chave (do BLK-LTV-03 = GO, `data/analysis/relatorio_correlacao_ltv.md`, gitignored): `score_priorizacao × LTV_PROSPECTIVO_12M_MEDIANO` rho=+0.391, IC95[+0.183,+0.573], p=0.0029, N=56; SOBREVIVE ao controle de maturidade (parcial +0.370) e maturidade+renda (+0.306, IC[+0.034,+0.550] não cruza zero). Renda SOZINHA colapsa sob controle de maturidade → o carregador é o COMPOSTO. Churn-90d é maturidade (−0.394, atributo de unidade) → fora do eixo. `TICKET_MEDIO_UNIDADE × LTV` +0.626 é atributo de UNIDADE (não territorial) → PROIBIDO como feature (confound declarado).
- Relação com DEC-001: **INTACTA** — pesos `renda=0.40`/`pop=0.60` e a fórmula do `score_priorizacao` INALTERADOS; `score_priorizacao` só LIDO como feature; nenhum artefato M1 regerado (mtime dos 4 oficiais inalterado). Relação com DEC-008: **honrada** (out-of-fold vs baseline, IC, R² in-sample banido, NO-GO válido). Relação com DEC-009: **INTACTA** — o M2 é score de RETENÇÃO territorial, NÃO preditor de magnitude de demanda; proibido usar qualquer coluna do M2 como preditor geográfico de demanda ou ajuste do `score_priorizacao`.
- Parâmetros canônicos do §3: **INALTERADOS** (o M2 é camada paralela; `w_cap`/`w_ret` são constantes da camada de retenção, não do M1). Pacote disjunto `lifetime/` NÃO importa de `pipelines/m1/`, `dashboard/`, `censo_*`, `api`.
- Referências: `tasks/backlog.md` (BLK-LTV-04, epic BLK-LTV); `src/motor_expansao/lifetime/score_retencao_territorial.py` (novo); `src/motor_expansao/lifetime/correlacao_territorio_retencao.py` (base LTV-03); `data/analysis/relatorio_score_retencao.md`; `data/staging/score_retencao_territorial.parquet`; `context/handoff/20260701-153000-planner.md`; CLAUDE.md §1/§3/§5; DEC-001; DEC-008; DEC-009.
- **Emenda 2026-07-01 (preview exploratório read-only pós-gate — fecha o flanco de "alvo contaminado por ticket"):** crítica levantada por 2º agente e verificada no dicionário (`unidade_para_motor_DICIONARIO.md:52`): `LTV_PROSPECTIVO_12M` = **ticket × meses ativos** por construção; como ticket é decisão comercial (largamente exógena ao hexágono), o NO-GO do LTV-04 — que usou `LTV_PROSPECTIVO_12M_MEDIANO` como alvo — poderia ser atribuído a alvo contaminado, não a território mudo. Re-testados **OUT-OF-FOLD pelo MESMO harness** (k-fold 5×5, Ridge numpy, IC95 bootstrap seed=42, maturidade como covariável) os **3 alvos ticket-free** — `E_MESES_ATIVOS_12M_MEDIANO` (durabilidade), `P_CANCEL_12M_MEDIA`, `PROB_CANCEL_90D_MEDIA` (N=56 cada). **Resultado: TODOS os modelos puramente territoriais colapsam** (melhor R²_oof territorial entre −0.011 e −0.038, todos ≤ 0 → pior que a média; rho territorial instável, inclusive wrong-signed em durabilidade → ruído). O ÚNICO sinal robusto (rho_oof +0.33 a +0.47, IC não cruza zero) aparece SÓ ao adicionar **maturidade** — atributo de UNIDADE, não de hex candidato — e ainda assim com R²_oof de IC cruzando zero. **Conclusão: a parte previsível de retenção/durabilidade é a MATURIDADE (tempo de operação), NÃO a geografia** → o NO-GO fica blindado contra a crítica de alvo-contaminado, de forma redundante nos 3 alvos. **NÃO abre BLK-LTV-05:** sinal territorial de durabilidade em N=56 censurado por sobrevivência é hipótese a **re-testar quando N crescer** (mais unidades + casos fechados), não bloco imediato. Corolário operacional: o lugar honesto da retenção no projeto de expansão é como **premissa/curva de maturação no motor de viabilidade** (DEC-009, `viabilidade_ponto.py`), NÃO como preditor territorial. Preview READ-ONLY (nada persistido; mtime M1 inalterado); reprodutível via monkeypatch de `ALVO` no harness.

### DEC-016 — Portão da `main` por CHECKS DE CI (0 aprovações) e auto-merge de blocos Baixa/Média
- ID: DEC-016 | Data: 2026-07-13 | Criticidade: **Estratégica** (redesenha a governança de merge do repositório: quem/o que autoriza um merge na `main`). **READ-ONLY sobre o M1.**
- Status: **APROVADA por Felipe Silva em 2026-07-13.**
- Contexto: o gate humano único (Felipe aprovando TODO merge) é o **gargalo dominante da esteira** — a produção de blocos já é maior do que a capacidade de revisão de uma pessoa, e PRs prontos ficam parados por dias. A troca **não é "confiar no agente"**: é **mover a garantia** de "alguém leu o diff" (promessa social, não verificável, que falha em silêncio quando o revisor está sem tempo) para **"checks de CI que são código versionado, auditável e que ninguém consegue furar"** (promessa mecânica, verificável a cada PR). O que era julgamento implícito de uma pessoa vira **regra explícita no repositório**, revisável por PR como qualquer outro código.
- Decisão (4 partes):
  1. **`required_approving_review_count` da `main`: 1 → 0.** A aprovação humana deixa de ser o portão por padrão.
  2. **`enforce_admins`: false → true.** Sem isso, `gh pr merge --admin` **fura CI e review** — foi exatamente o caminho do **merge NÃO-INTENCIONAL do PR #4** (ver emenda 2026-06-12 da DEC-005, que subiu com CI vermelho) e é o caminho que um **agente autônomo encontra sozinho** quando o merge normal é barrado. **Custo explicitamente aceito: nem o Felipe mergeia sem CI verde** (o bypass some para todo mundo, inclusive o dono).
  3. **Required status checks passam a ser `test` + `guard` + `claude-review` + `review-gate`** (hoje só `test`).
  4. **Régua de autonomia por criticidade:** blocos de criticidade **Baixa e Média** mergeiam **SOZINHOS**, via **auto-merge NATIVO do GitHub**, quando os **4 checks** ficam verdes. **Alta** exige a label `aprovado-humano`. **Crítica** (M1/score/pesos/`config.py`/deploy/segredos/CI/governança) exige a label `critica-aprovada` **aplicada pelo PRÓPRIO Felipe** (login `Kastaldy`) — o job `guard` **valida o autor da label via API**, não basta a label existir.
- O que cada check faz:
  - **`guard`** = `scripts/loop_guard.py` rodando **a partir da BASE** (evento `pull_request_target`, **sem checkout nem execução de código do PR** — impede que um PR agêntico edite o próprio job e **desarme o guarda que deveria barrá-lo**), classificando os paths tocados em **CRÍTICO** (M1, scores paralelos servidos em produção, `deploy/`, segredos, CI) e **GOVERNANÇA** (o próprio guard, `.claude/`, `REVIEW.md`, `CLAUDE.md`, `tasks/backlog.md`).
  - **`claude-review`** = agente revisor (`anthropics/claude-code-action`, autenticado pela **assinatura Max** via `CLAUDE_CODE_OAUTH_TOKEN` — **sem custo de API**) que **REPROVA o check** em achado de severidade alta. É **diferente do "Code Review app" da Anthropic**, que sai **sempre neutro por design** (nunca bloqueia merge) e é exclusivo de Team/Enterprise (~US$ 15–25 por review) — por isso não serve como portão.
  - **`review-gate`** = exige a **label humana** conforme a criticidade do bloco (nada em Baixa/Média; `aprovado-humano` em Alta; `critica-aprovada` do Felipe em Crítica).
- Restrição operacional de UM comentário (sticky): a `main` tem `required_conversation_resolution: true` — se o revisor abrir **review threads inline**, o auto-merge **trava** até alguém resolver cada thread. Logo, o `claude-review` **DEVE postar UM comentário único (sticky), atualizado a cada push, NUNCA review threads**.
- **Blindagens do portão (fechadas no red team de 2026-07-13; a DEC descreve o que os checks fazem DE FATO):**
  1. **Aprovação NÃO é cheque em branco.** Um push novo (`synchronize`) ou troca de base/título (`edited`) em um PR já aprovado **REMOVE automaticamente** as labels `critica-aprovada` / `aprovado-humano` — no **primeiro passo (fail-closed) de cada gate `guard`/`review-gate`**, com **releitura autoritativa** (se a label persistir após o DELETE, o check fica VERMELHO). Ficou **dentro** de cada gate, não num job separado: um job auxiliar do qual os gates dependessem (`needs:`) seria **fail-open** — se ele falhasse, os gates ficariam `skipped`, que conta como SUCESSO. A aprovação vale para o **diff que o humano viu**, não para o próximo commit. O GitHub só faz essa invalidação nativamente com **reviews** (`dismiss_stale_reviews`), **não com labels** — como o portão passou a ser por label, invalidar virou responsabilidade nossa.
  2. **A criticidade NÃO é auto-declarada pelo agente.** A label `criticidade:*` do PR é **conferida contra o campo Criticidade do bloco em `tasks/backlog.md` da BASE** (que o PR **não consegue editar** para efeito do check, porque o `guard`/`review-gate` rodam da base via `pull_request_target`). **Divergência entre a label e o backlog = REPROVADO.** PR **sem bloco identificável** só passa com `aprovado-humano` aplicada por humano — caso contrário bastaria "esquecer" a label para um bloco Crítico se apresentar como Baixa e auto-mergear.
  3. **Quem aplica a label importa (validado via API).** `aprovado-humano` só vale se aplicada por um **humano (não-bot)**, que **NÃO seja o autor do PR**, e que tenha permissão **`write`/`admin`** no repositório. `critica-aprovada` só vale se aplicada pelo **próprio Felipe** (`Kastaldy`). Sem essas três checagens, um bot (ou o próprio autor) "aprovaria" o PR que ele mesmo abriu.
  4. **PR de fork NÃO passa pelo revisor automático.** Em repositório público, um PR vindo de fork **não enxerga secrets** — logo o `claude-review` não tem como autenticar (`CLAUDE_CODE_OAUTH_TOKEN`). Nesse caso o check **REPROVA explicitamente**; merge de fork só por **condução humana**. Detalhe que decidiu o desenho: no GitHub, um job **PULADO por `if:` conta como SUCESSO** em required check — se o revisor simplesmente se pulasse no fork, o portão ficaria **verde por omissão**. Por isso ele **reprova em vez de se pular**.
  5. **O `guard` também protege o que ARMA os gates.** Entram na classe **CRÍTICO** também `pyproject.toml`, `constraints.txt`, `conftest.py`, `.gitleaks*`, `.trivyignore` e `Dockerfile.*`. Sem isso, um PR **desarmaria o próprio check `test`** — que roda do **HEAD do PR**, não da base — sem tocar em nenhum path protegido: bastaria neutralizar a suíte pelo `conftest.py`, afrouxar as dependências pinadas ou silenciar o scanner de segredos, e o portão continuaria verde.
  6. **Fail-closed em toda parte.** Revisor que **não consegue ler o diff** (`arquivos_revisados < 1`), saída **inválida ou ausente**, classe fora do contrato conhecido, ou **action fora do ar** → check **VERMELHO**. **Nunca verde por omissão**: ausência de sinal é tratada como **reprovação**, jamais como aprovação.
  7. **O portão não é forjável por check run falso (N0 — a blindagem que sustenta as outras).** Um PR de branch **do próprio repo** roda seu workflow **a partir do HEAD do PR**, e — verificado na doc oficial — o `default_workflow_permissions: read` do repo **não é um teto**, apenas o ponto de partida: um workflow do PR pode declarar `permissions: {checks: write, statuses: write}` e **publicar check runs verdes com os nomes obrigatórios** (`test`/`guard`/`claude-review`/`review-gate`), tentando forjar o portão (só PR de **fork** tem o write rebaixado para read). A doc de required status checks sugere que, havendo dois check runs homônimos, **ambos são exigidos** (o `guard` vermelho real ainda bloquearia) — mas isso é **ambíguo e não se pode depender dele**. A defesa **fail-safe, que independe da ambiguidade**: **`.github/CODEOWNERS`** declara `@Kastaldy` dono de `/.github/`, `scripts/loop_guard.py`, `REVIEW.md`, `CLAUDE.md`, `tasks/backlog.md`, `pyproject.toml`, `constraints.txt`, `conftest.py`, e o BLK-ORQ-21 liga **`require_code_owner_reviews: true`** — que **funciona mesmo com `required_approving_review_count: 0`** (são regras independentes, confirmado na doc). Para publicar um check falso o PR **precisa adicionar/editar um workflow sob `.github/`**, e isso passa a **exigir revisão do próprio Felipe**, avaliada **nativamente pelo GitHub** (não é um check run → não é forjável). **Pré-condição dura do BLK-ORQ-21:** `require_code_owner_reviews: true` + o teste empírico do spoof antes de confiar no portão.
- O que fica **INTOCADO**: **M1 READ-ONLY** (DEC-001: pesos `renda=0.40`/`pop=0.60` e a fórmula do `score_priorizacao` **INALTERADOS**); **suíte FULL como gate sem bypass** (`test` continua obrigatório); **deploy SEMPRE manual, por digest, pelo Felipe** — **auto-merge NÃO deploya** (push na `main` só publica a imagem no GHCR; subir a imagem na VPS segue passo humano, §6); anti-PII; **NO-GO como desfecho válido** (DEC-008).
- O que esta DEC **REVOGA explicitamente** (revogação NOMEADA — não é "guardrail inalterado"): a regra **"o loop NUNCA faz merge/push"** (CLAUDE.md §6.1) e **"(6.b) Merge — ator: humano"** (`.claude/commands/run-cycle.md`) ficam **SUPERSEDED SOMENTE para blocos de criticidade Baixa/Média com os 4 checks verdes**. Para **Alta/Crítica o merge segue humano** (por label). **Deploy permanece humano em TODOS os casos.**
- **Gatilho de suspensão (com detector).** **Incidente** = PR auto-mergeado que (i) **reprove o `guard`** em auditoria posterior, (ii) **introduza segredo ou PII**, (iii) **exija `revert`** na `main`, ou (iv) **quebre o CI da `main`**. **Detector:** o **relatório diário do Auditor de PRs (BLK-ORQ-23)** conta os incidentes — o gatilho tem dono e medição, não é promessa. **2 incidentes em 90 dias → auto-merge de Baixa/Média SUSPENSO** e esta DEC revisada. **Kill-switch:** um único `gh api --method PUT .../branches/main/protection` com `required_approving_review_count: 1` restaura o gate humano imediatamente.
- Pesos/fórmula/artefatos do M1: **INALTERADOS** (`renda=0.40`/`pop=0.60`, `score_priorizacao`, `hex_score_estrutural`, carteira, plano, artefatos oficiais). Parâmetros canônicos do §3: **INALTERADOS** (esta DEC é de governança de merge; não toca score, dado nem pipeline).
- Referências: `tasks/backlog.md` (BLK-ORQ-20..24); `.github/workflows/guard.yml`; `.github/workflows/claude-review.yml`; `.github/CODEOWNERS`; `REVIEW.md`; `scripts/loop_guard.py`; CLAUDE.md §2/§5/§6.1; DEC-001; DEC-005 (emenda 2026-06-12 — merge não-intencional do PR #4 com CI vermelho); DEC-008; DEC-013.

### DEC-017 — Normalização de EOL (Markdown de `tasks/`+`docs/` em LF) + enxugamento do CODEOWNERS (uma aprovação para governança de bookkeeping)
- ID: DEC-017 | Data: 2026-07-15 | Criticidade: **Estratégica** (política de EOL do repo + governança de review do merge). **READ-ONLY sobre o M1.**
- Status: **APROVADA por Felipe Silva em 2026-07-15.**
- Contexto (dois atritos recorrentes, mesma família de causa): (1) **Conflito de "arquivo inteiro"** em `tasks/backlog.md`/`tasks/completed.md` (e `docs/*.md`) a cada PR de ciclo/relatório — a causa era **EOL misto**: `backlog.md` era **CRLF** e `completed.md` **misto (CRLF+LF)** na `main`, contra **LF** nos branches; o git via **toda linha** como divergente. O `merge=union` do `completed.md` **NÃO resolvia**: o GitHub **não aplica** o driver de `.gitattributes` na checagem de mergeabilidade da PR, e union sobre EOL misto duplicaria linhas. (2) PRs de ciclo/relatório exigiam **DUAS aprovações** — a label `aprovado-humano` (guard) **e** uma review nativa de code owner — porque `tasks/backlog.md` estava no CODEOWNERS. Como todo PR de ciclo toca `backlog.md` no housekeeping (Passo 6.0), o atrito era universal.
- Decisão (2 partes):
  1. **EOL: Markdown de `tasks/` e `docs/` normalizado para LF nos blobs.** `.gitattributes` ganha `tasks/*.md text eol=lf` e `docs/*.md text eol=lf`; um commit de `git add --renormalize` converte os blobs de `tasks/backlog.md`, `tasks/completed.md` e `docs/*.md` (os que eram CRLF/misto) para **LF**. Com EOL consistente entre `main` e branches, os merges viram 3-way real (blocos/regiões diferentes = sem conflito) e o `merge=union` do `completed.md` volta a funcionar de fato. O `tasks/completed.md merge=union` é **MANTIDO** (ganha `text eol=lf` de brinde pelo glob).
  2. **CODEOWNERS enxugado — remove SÓ `/tasks/backlog.md`.** Distinção de princípio: o `backlog.md` é **lido da BASE** pelo guard/review-gate (`pull_request_target`), então um PR que o edita **não afeta a própria** classificação de criticidade, e ele segue **protegido pela label `aprovado-humano`** (guard, validada por API: não-bot, ≠ autor, com write) — a review nativa era **redundante**. **MANTIDOS** no CODEOWNERS: `/.github/` + `/scripts/loop_guard.py` (defesa **N0 anti-spoof** da DEC-016), os arquivos que **ARMAM um check a partir do HEAD do PR** (`pyproject.toml`/`constraints.txt`/`conftest.py` armam o `test`; `REVIEW.md`/`CLAUDE.md` podem influenciar o `claude-review`, que roda do HEAD) e o **trilho crítico** (`config.py`, `pipelines/m1/`, `deploy/`, `secrets/`, `Dockerfile.*`, `docker-compose*`, `.gitleaks*`, `.trivyignore`). Regra: caminho **lido da BASE** → a label basta; caminho **que arma check do HEAD** ou **forjável** → review nativa fica. Efeito: PR de ciclo/relatório (que só toca `backlog.md`+`censo_*`/docs no CODEOWNERS) passa a exigir **UMA aprovação** — a label `aprovado-humano`.
- Guardrails preservados: **N0 anti-spoof INTACTO** (`.github/` + `loop_guard.py` seguem no CODEOWNERS, com `require_code_owner_reviews: true`); governança de `backlog.md` segue **gated pela label** `aprovado-humano` do guard; **M1 READ-ONLY** (`renda=0.40`/`pop=0.60`, `score_priorizacao`, `hex_score_estrutural`, artefatos oficiais **INALTERADOS**). Nada de score/pesos/pipeline/config tocado.
- Relação com a DEC-016: **COMPLEMENTA, não revoga** — os 4 checks, a régua de merge por criticidade e o gate humano por label seguem; esta DEC só (a) conserta o EOL e (b) tira a review NATIVA redundante do `backlog.md` (que já é gated pela label).
- Referências: `.gitattributes`; `.github/CODEOWNERS`; `tasks/backlog.md`/`tasks/completed.md` (renormalizados); DEC-016 (§8, blindagem N0/#7); CLAUDE.md §8.
