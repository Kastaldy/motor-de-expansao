# Handoff — Builder → QA

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA

## Bloco
BLK-DIM-21 — UI: gráficos financeiros e curva de maturidade na aba de viabilidade

## Criticidade
**Média** — enriquecimento visual; READ-ONLY sobre M1. Esteira: BO → Planner → Builder → QA (sem gate humano; bloco loop-safe).

---

## 1. O que foi feito

### `src/motor_expansao/dimensionamento/simulador.py`
- **Nova função `gerar_serie_mensal()`** adicionada antes de `aluguel_teto()` (linha ~348). Assinatura idêntica à de `viabilidade()`, retorna `list[dict]` com campos: `mes`, `alunos_balcao`, `faturamento_mensal`, `ebitda_mensal`, `fcf_acumulado`. 60 registros (meses 1–60).
- **Refatoração de `viabilidade()`**: o loop `for t in range(1, 61)` foi substituído por uma chamada a `gerar_serie_mensal()`. O cálculo de `capex_efetivo` permanece localmente em `viabilidade()` (necessário para ROIC). API/assinatura/retorno de `viabilidade()` preservados byte-a-byte.
- Lógica do loop: fonte única de verdade em `gerar_serie_mensal()`.

### `src/motor_expansao/dashboard/pages.py`
- **Import adicionado**: `import plotly.graph_objects as go` (linha ~8, junto com `import pandas as pd`).
- **Import adicionado**: `from motor_expansao.dimensionamento.simulador import gerar_serie_mensal` e `SIM_MATURACAO_MESES` do config.
- **Nova Seção 8** inserida em `render_viabilidade_ponto()` entre a grade de sensibilidade (Seção 7) e o pino do imóvel (renomeado para Seção 9). Contém 4 gráficos `st.plotly_chart(use_container_width=True)`:
  1. **Gráfico 1**: Rampa de alunos (balcão) — Scatter com linha de steady-state e vline de maturação.
  2. **Gráfico 2**: Faturamento bruto (Bar) + EBITDA (Scatter) mensal.
  3. **Gráfico 3**: FCF acumulado com área bicolor (vermelho para negativo, turquesa para positivo) e vline de payback.
  4. **Gráfico 4**: DRE breakdown waterfall (steady-state): Faturamento → Deduções → Impostos → Custos operacionais → EBITDA.
- Paleta Ultra: `_TURQUESA="#00BFB3"`, `_VERDE="#2ECC71"`, `_VERMELHO="#E74C3C"`, `_CINZA="#2E3040"`, fundo branco.
- Cards existentes (Seções 4–7) **não foram alterados**.

### `tests/unit/dimensionamento/test_simulador.py`
- Import de `gerar_serie_mensal` adicionado ao bloco de imports do topo.
- **8 novos testes CA-11a..h** adicionados ao final do arquivo:
  - CA-11a: comprimento 60 registros
  - CA-11b: campos obrigatórios presentes
  - CA-11c: sem NaN/None
  - CA-11d: meses sequenciais 1..60
  - CA-11e: FCF acumulado negativo no mês 1
  - CA-11f: payback consistente com `viabilidade()`
  - CA-11g: alunos crescem monotonicamente
  - CA-11h: com financiamento, payback >= sem financiamento

---

## 2. Resultado das validações

```
pytest tests/unit/dimensionamento/test_simulador.py -q
  43 passed in 1.56s   (35 originais CA-01..CA-10 + 8 novos CA-11a..h)

ruff check simulador.py pages.py test_simulador.py
  All checks passed!

mypy simulador.py pages.py --ignore-missing-imports
  Success: no issues found in 2 source files

python -c "import streamlit_app; print('import ok')"
  import ok
```

---

## 3. Guardrails confirmados
- **READ-ONLY sobre M1**: nenhum arquivo toca `config.py` do M1, `pipelines/m1/`, `score_priorizacao`, `hex_score_estrutural`, pesos, artefatos oficiais ou parquets M1.
- **`viabilidade()` API preservada**: mesma assinatura, mesmo retorno `ViabilidadeResult`. Testes CA-01..CA-10 passam sem alteração.
- **Sem dependências novas**: `plotly>=5.20.0` já é dep base.
- **Renderização condicional**: gráficos só aparecem após submit do formulário.
- **Seções 4–7 intocadas**: cards e grade de sensibilidade preservados.
- **`viabilidade_ponto.py` intocado**: zero mudanças.

---

## 4. Arquivos alterados (commit por path)
| Arquivo | Mudança |
|---|---|
| `src/motor_expansao/dimensionamento/simulador.py` | `gerar_serie_mensal()` + refatoração do loop de `viabilidade()` |
| `src/motor_expansao/dashboard/pages.py` | imports + Seção 8 com 4 gráficos |
| `tests/unit/dimensionamento/test_simulador.py` | 8 testes CA-11a..h |

---

## 5. Sugestões de validação para o QA

1. **Suite completa**: `pytest -q` (baseline esperado: todos passam; em 2026-06-22 era 532 passed, 1 skipped).
2. **Suite do simulador**: `pytest tests/unit/dimensionamento/test_simulador.py -v` — confirmar 43 testes verdes.
3. **Ruff e mypy**: `ruff check .` + `mypy src/motor_expansao/dimensionamento/simulador.py src/motor_expansao/dashboard/pages.py --ignore-missing-imports`.
4. **Import**: `python -c "import streamlit_app; print('import ok')"`.
5. **Loop guard**: `python scripts/loop_guard.py` — confirmar que os 3 arquivos não estão na lista de bloqueio.
