# Handoff — Builder (BLK-OPS-02b — Saneamento ruff/mypy + CI bloqueante)

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA / Quality Analyzer

## Bloco executado
BLK-OPS-02b — Saneamento ruff/mypy (preferindo correção a supressão) + tornar ruff/mypy bloqueantes
no CI. Gate de aprovação humana registrado: "APROVADO POR Felipe Silva EM 2026-05-29" (plano de 13
passos como está). Branch: `ciclo/BLK-OPS-02b` (não troquei/criei branch; não commitei).

## O que foi feito (passo a passo)

**PASSO 0 — Baseline (pré).** Branch confirmado `ciclo/BLK-OPS-02b`. `pytest -q` = `532 passed, 1 skipped`;
`ruff check . --statistics` = 286; `mypy src/` = 23; 4 hashes M1 capturados e idênticos aos do Orchestrator.

**PASSO 1 — Config ruff.** Em `pyproject.toml` movi `select`/`ignore` de `[tool.ruff]` para `[tool.ruff.lint]`
(mantive `line-length`/`target-version` em `[tool.ruff]` e `[tool.ruff.format]` intacto). Warning de
deprecation sumiu; `ruff` continuou reportando 286 (mesmo set de regras — canário OK).

**PASSO 2 — Auto-fixes (`ruff check . --fix`, sem `--unsafe-fixes`).** 227 fixes (UP045/I001/F401/UP037/
F541/UP035/E401) em ~52 arquivos; ruff 286 → 68. **Smoke import quebrou** (collection error): o `--fix`
removeu re-exports usados por testes. Tratei conforme o próprio plano ("reintroduzir com `# noqa: F401`
documentado"):
- `src/.../m1/hex_enrichment.py`: reintroduzi `normalizar_0_100` e `normalizar_serie` (re-exportados;
  testes/wrappers fazem `from hex_enrichment import ...`) com `# noqa: F401` documentado. Confirmei que
  os outros 5 símbolos removidos (`PESOS_HEX_SCORE*`, `_combinar_scores_proporcionalmente`,
  `_normalizar_serie_disponivel`) são genuinamente não usados (nem no corpo, nem como re-export) → mantidos removidos.
- `src/.../dashboard/pages.py`: restaurei o arquivo, marquei `build_map_figure` com `# noqa: F401`
  (teste patcha `pages.build_map_figure`) e re-rodei o `--fix` só nele. Os demais flagged
  (`_carteira_prioridade_color`, `render_censo_score_legend/faixa/residual/residual_score`, `format_score`)
  não são referenciados como `pages.X`/`streamlit_app.X` em teste → removidos com segurança.
Após o ajuste: smoke `import ok` e `pytest` voltou a `532 passed, 1 skipped`; 4 hashes M1 idênticos.

**PASSO 3 — Triviais ruff (classe b).** 49 violações corrigidas (correção, não supressão, salvo 1 caso):
- B905 (13): `strict=False` em todos os `zip()` (preserva o default atual — truncar ao menor).
- E712 (20): pandas `df[col] == True`/`!= True` → `.eq(True)`/`.ne(True)` (invariante, preserva NaN);
  asserts escalares de teste `== True/False` → truthy/falsy (`assert x` / `assert not x`).
- F841 (7): variáveis mortas removidas (`uf_upper`, `key_cols`, `n`, `color_hex2_*`) ou `as mock_gen` dropado.
- B007 (2): loop vars não usadas → `_uf`/`_idx`.
- E731 (2): `lambda` atribuída → `def` (`_fmt_int_none` em pages.py; `icon` em smoke_test_fontes.py).
- E741 (2): `l` → `label` em test_streamlit_app.py.
- B017 (1): `pytest.raises(Exception)` → `pytest.raises(FileNotFoundError)` (confirmado o tipo real).
- B023 (1): `validar_fase_a_censo2022.py` — `# noqa: B023` documentado (closure usa `nonlocal` para
  escrever de volta; thread criada/join na mesma iteração; default-arg quebraria o write-back).
- F401 (1): probe de disponibilidade em `score_consolidado.py` (legado) → `# noqa: F401` documentado.
`pytest` = 532/1; hashes M1 idênticos.

**PASSO 4 — mypy baixo risco (fora de M1).**
- `censo_map.py`: `_font` retorna `FreeTypeFont` (não subclasse de `ImageFont` nos stubs Pillow) →
  `cast(ImageFont.ImageFont, ...)` nos dois returns (mantém assinatura aceita por `_draw_text`/`_text_width`;
  runtime inalterado). Adicionado `from typing import cast`.
- `components.py:_format_coordinate_pair`: params `object` → `Any` (anotação; `float(...)` já guardado por
  `pd.isna`). Adicionado `from typing import Any`.
- `pages.py`: `format_score`/`format_int` com `cast(float)`/`cast("int|float")`; `filter_points_to_radius`
  com `cast(float, lat_ref/lng_ref)` (dentro de `search_pin is not None`, garantidamente não-None);
  `int(value)` em `_format_brl` com `cast(float, value)` (guard `pd.notna`).

**PASSO 5 — F821 `pdk` em pages.py.** Adicionado bloco `if TYPE_CHECKING: import pydeck as pdk` no topo
(sem custo de import em runtime; resolve a anotação `deck: "pdk.Deck"`). ruff perde F821, mypy perde name-defined.

**PASSO 6 — mypy M1 `base_h3_brasil.py:117`.** `_chunks` é generator que faz `yield values[start:...]`
(uma `list[str]`), então anotei `-> Iterator[list[str]]` (import `from collections.abc import Iterator`).
NOTA: desviei do literal `Iterator[str]` do plano porque o valor cedido é `list[str]`, não `str`; a
anotação correta zera o erro sem mascarar nada. Runtime idêntico.

**PASSO 7 — mypy M1 `hex_enrichment.py`.**
- 7a: `unidades_ultra: list = None` → `list | None = None` (linhas `rodar_pipeline_hex` e multicidade).
- 7b: `linhas_capitais: list[dict[str, object]] = []` (resolve dict-item de `int(melhor[...])`; valores intocados).
- 7c: fallback `from fase1_bi_exports import generate_fase1_bi_artifacts` com
  `# type: ignore[no-redef,attr-defined]` (fallback de import fora do pacote). Ruff I001 e mypy entraram
  em conflito no formato do bloco (ruff queria multi-linha, mypy precisa do ignore na linha do `from`);
  resolvido com `from fase1_bi_exports import (  # type: ignore[...]` multi-linha → ambos satisfeitos.

**PASSO 8 — B019 `ibge_censo.py` (decisão humana = suprimir).** `# noqa: B019` documentado (justificativa
única por método) nas 3 `@lru_cache` (linhas 291/362/380): cache de resposta de rede / despacho a métodos
irmãos / estado de instância; instância de pipeline efêmera; refator a função de módulo mudaria o despacho
de produção M1. NÃO refatorado (decisão humana foi suprimir).

**PASSO 9 — F601 nos testes M1 (isolado, por último).** Provei invariância (`{'pop_total':0.0,'pop_total':1000.0}`
→ `{'pop_total': 1000.0}`; última chave vence). Reli os 15 dicts: em todos a 1ª chave `pop_total` (0/0.0)
é a morta e a 2ª (real) é a flagada pelo ruff; nenhum outro key duplicado. Removi SOMENTE a 1ª ocorrência
de cada um dos 15 dicts (linhas 117/127/137/147 + 166–175 + 189). Dicts resultantes byte-idênticos.
ruff → 0. `pytest` seguiu 532/1 (asserts dependentes dos valores reais permaneceram).

**PASSO 10 — Verificação dupla.** ruff 0, mypy 0, pytest 532/1, smoke ok, 4 hashes M1 idênticos.

**PASSO 11 — CI bloqueante (só após 0/0 locais).** Em `.github/workflows/ci.yml` removi os 2
`continue-on-error: true`, renomeei os steps para "Lint (ruff)" / "Types (mypy src/)" e reescrevi o bloco
de comentário (agora documenta que a dívida foi zerada em BLK-OPS-02b e os steps são bloqueantes).

**PASSO 12 — Controle.** Atualizei `tasks/current_task.md` (Builder concluído, aguardando QA) e este
handoff + snapshot. `tasks/completed.md`/`tasks/backlog.md` deixados para o fechamento do orquestrador
(conforme briefing: orquestrador commita/encerra por path).

## Arquivos alterados
Produção/infra com correção manual direcionada:
- `pyproject.toml` (config ruff → `[tool.ruff.lint]`).
- `src/motor_expansao/pipelines/m1/hex_enrichment.py` (noqa F401 re-export; mypy 7a/7b/7c; B007 `_uf`).
- `src/motor_expansao/pipelines/m1/base_h3_brasil.py` (Iterator return-type).
- `src/motor_expansao/dashboard/pages.py` (TYPE_CHECKING pdk; casts mypy; E731; noqa F401 build_map_figure).
- `src/motor_expansao/dashboard/components.py` (B905 ×4; E712 ×4 `.eq(True)`; Any import + arg-type).
- `src/motor_expansao/dashboard/data.py` (E712 ×2 `.eq(True)`).
- `src/motor_expansao/dashboard/censo_map.py` (cast FreeTypeFont; import cast).
- `src/motor_expansao/dashboard/censo_point.py` (B007 `_idx`).
- `src/motor_expansao/dashboard/censo_report.py` (F841 `_page_h`).
- `config.py` (type:ignore fallback BaseSettings/SettingsConfigDict/2ª class Settings).
- `ibge_censo.py` (noqa B019 ×3 documentado).
- `.github/workflows/ci.yml` (ruff/mypy bloqueantes).
- `jobs/pipelines/`: `enriquecimento_espacial_hexagonos.py` (B905 ×2), `fase_a_censo2022_setores.py`
  (B905 + F841), `fase_a_piloto_expandido.py` (B905), `gerar_carteira_acionavel.py` (E712 ×3),
  `gerar_plano_expansao_dominio.py` (B905), `validar_fase_a_censo2022.py` (B905 + noqa B023).
- `scripts/smoke_test_fontes.py` (E731).
- `fora_primeira_fase/m2_m3_imobiliario/score_consolidado.py` (noqa F401 probe + E712).
- `fora_primeira_fase/powerbi/powerbi_m1_dashboard.py` (B905 ×2).
- Testes com correção manual: `tests/integration/test_hex_enrichment_brasil.py` (F601 ×15 + E712),
  `tests/integration/test_fase_a_censo2022.py` (E712 + F841), `tests/integration/test_streamlit_app.py`
  (F841 + B905 + E741), `tests/integration/test_fase1_bi_exports.py` (F841),
  `tests/integration/test_fase_a_nacional_completo.py` (B017), `tests/contracts/test_fontes_gratuitas.py` (F841).

Arquivos tocados APENAS pelo auto-fix do PASSO 2 (I001/UP045/UP037/F401/F541/E401 — reordenação de
imports e modernização de anotações; sem mudança de runtime):
- `conftest.py`, `poi_enrichment.py`, `scripts/check_artifacts.py`, vários `jobs/pipelines/*.py`
  (calibrar_renda_setor_2022, comparar_geofusion_vs_hex, fase_a_nacional_completo, gerar_relatorio_expansao_dominio,
  normalizar_concorrentes, teste_setor_censitario_2010, validar_modelo_ultra), `streamlit_app.py`,
  vários `fora_primeira_fase/*` (api_postgis/*, m2_m3_imobiliario/*, powerbi/test_*, tests/*),
  e vários `tests/*` (unit/integration) — ver `git status` para a lista completa (65 arquivos no total).

Controle: `tasks/current_task.md`, `context/handoff.md`, `context/handoff/20260529-205109-builder.md`.

## Validações executadas (checkpoints)
- Baseline (PASSO 0): `532 passed, 1 skipped, 9 warnings`; ruff 286; mypy 23.
- Checkpoint (a) pós PASSO 2+3: `532 passed, 1 skipped, 9 warnings`; smoke `import ok`; 4 hashes idênticos.
- Checkpoint (b) pós PASSOS 5–8 (mypy/M1 produção): `532 passed, 1 skipped, 9 warnings`; smoke `import ok`;
  4 hashes idênticos; mypy `Success: no issues found in 18 source files`.
- Checkpoint (c) pós PASSO 9 (F601 — o mais sensível): ruff `All checks passed!`;
  `532 passed, 1 skipped, 9 warnings`; 4 hashes idênticos.
- Final (PASSO 10): `ruff check .` → `All checks passed!` (0); `mypy src/` → `Success: no issues found in
  18 source files` (0); `python -c "import streamlit_app"` → `import ok`; `pytest -q` →
  `532 passed, 1 skipped, 9 warnings in 84.43s`.

### Hashes M1 — baseline (PASSO 0) vs final (PASSO 10), lado a lado
| Artefato | Baseline | Final |
|---|---|---|
| `data/staging/brasil_priorizados.parquet` | `c226954945ad0757a0429c84c43f410492c0ea7d15ca1d9b6a15f68727806567` | `C226954945AD0757A0429C84C43F410492C0EA7D15CA1D9B6A15F68727806567` |
| `data/staging/brasil_estrutural.parquet` | `7baa07a2cbc0b7d8f2a8878932cae0ebf9400fce00e9c48c366c9903215f131b` | `7BAA07A2CBC0B7D8F2A8878932CAE0EBF9400FCE00E9C48C366C9903215F131B` |
| `data/staging/hexagonos_brasil_oportunidades.parquet` | `805c65e28adfd800c7d5524e73fa9b0a044b992f17d617d0f2c6e40f9bbf61ca` | `805C65E28ADFD800C7D5524E73FA9B0A044B992F17D617D0F2C6E40F9BBF61CA` |
| `data/outputs/hexagonos_brasil_dashboard.parquet` | `0cfb1015fc9df8b63776eb07ae3e666766905f768a4a78a406ae4d6b7cb6f618` | `0CFB1015FC9DF8B63776EB07AE3E666766905F768A4A78A406AE4D6B7CB6F618` |

Os 4 hashes são idênticos (apenas diferença de caixa hexadecimal entre `sha256sum` minúsculo do Orchestrator
e `Get-FileHash` maiúsculo do PowerShell; valores byte-a-byte iguais).

## Problemas encontrados
1. **PASSO 2 quebrou smoke/collection**: o `--fix` removeu re-exports (`normalizar_0_100`, `normalizar_serie`
   em hex_enrichment; `build_map_figure` em pages) consumidos por testes via `from hex_enrichment import ...`
   e `patch("...pages.build_map_figure")`. Resolvido reintroduzindo só os necessários com `# noqa: F401`
   documentado (caminho previsto pelo plano). Os demais removidos foram confirmados sem consumidores.
2. **Conflito ruff I001 × mypy** no bloco de fallback de import (PASSO 7c): ruff queria multi-linha movendo
   o `# type: ignore` para a linha do símbolo, onde o mypy não o aplica. Resolvido com o ignore na linha do
   `from (` em formato multi-linha — ambos passam.

## Pendências
- Nenhuma técnica. `tasks/completed.md`/`tasks/backlog.md` (marcar BLK-OPS-02b concluído) e os commits
  por path ficam para o fechamento do orquestrador/QA.

## Riscos remanescentes
- Baixos. Mudanças são lint/tipos/anotações e remoção de chave morta provada invariante. As 3 supressões
  documentadas (B019 ×3, B023 ×1, F401 ×3) seguem a decisão do plano/humano (B019) ou são fallbacks/probes
  legítimos. Nenhuma altera comportamento de runtime.
- CI agora bloqueia ruff/mypy: futuras regressões de lint/tipos reprovam o gate (intencional).

## Guardrails verificados
- `score_priorizacao` / pesos / parâmetros canônicos (H3=7, DIST_MIN_ULTRA=1.0, RENDA_MIN=4500.0,
  0.40/0.60): **não tocados** (nenhuma mudança de lógica de scoring).
- Artefatos M1 preservados: **sim** — 4 hashes idênticos baseline vs final.
- Dashboard offline mantido: **sim** — `import streamlit_app` ok; nenhuma dependência de API ao vivo
  introduzida (pdk só sob `TYPE_CHECKING`).
- VPS (CLAUDE.md §6): **não tocado**.

---
*Handoff produzido pelo Builder em 2026-05-29 (timestamp 20260529-205109).*
