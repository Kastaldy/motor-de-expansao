# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Builder (criticidade Média; gate visual resolvido upfront; modelo opus conforme tiering aprovado em current_task.md)

## Entendimento da tarefa
Trocar o basemap escuro Dark Matter pelo CartoDB Voyager COM labels, inverter o overlay de pixels (claros→escuros, nunca FIND_EDGES), transformar a camada "Concorrentes" em mapa só-pins sem choropleth, e ajustar todo o tema claro (fallback, tinta, atribuição), preservando o frame retangular e a integridade READ-ONLY do M1.

---

## Plano técnico

### Bloco A — `censo_map.py`: constantes de tema claro

**Arquivo:** `src/motor_expansao/dashboard/censo_map.py`

**Passo 1 — Trocar o provedor de tiles**
- Constante `_BASEMAP_PROVIDER_ATTR`: `"DarkMatter"` → `"Voyager"`
- Comentário de cabeçalho (linha ~35): substituir toda a explicação do Dark Matter pela do Voyager (provedor Voyager COM labels; fundo claro; ruas/nomes são pixels escuros sobre fundo claro).

**Passo 2 — Retunar contraste e alpha do choropleth**
- `_BASEMAP_CONTRAST`: `1.35` → `1.15` (Voyager já é legível; menos contraste em fundo claro evita saturar os tiles).
- `_CHOROPLETH_ALPHA`: `95` → `140` (fundo claro precisa de choropleth mais opaco para a cor de faixa ser legível; ruas já não somem pois vêm por cima via overlay). Valor sugerido 140 ± 10 — Builder calibra visualmente.

**Passo 3 — Inverter a lógica do overlay de ruas**
  - Hoje (Dark Matter): recoloca pixels com **luminância > `_STREET_FLOOR`** (pixels CLAROS = ruas claras sobre fundo escuro).
  - Voyager: ruas/nomes são pixels **escuros** sobre fundo claro. Logo: recolocar pixels com **luminância < `_STREET_CEIL`** (pixels escuros).
  - Renomear/substituir as constantes:
    - Remover `_STREET_FLOOR = 52` e `_STREET_GAIN = 2.6` e `_STREET_CAP = 230`.
    - Adicionar:
      ```python
      _STREET_CEIL = 160    # pixels com luminancia <= CEIL (escuros) sao ruas/nomes do Voyager
      _STREET_GAIN = 2.2    # ganho de opacidade proporcional a escuridao do pixel
      _STREET_CAP  = 210    # teto de opacidade do overlay (evita ruas "negras demais")
      ```
    - Os valores acima são ponto de partida calibrado para Voyager; o Builder DEVE rodar a verificação visual (Passo 12) e ajustar antes de fixar.
  - No método `_render_camada`, substituir o bloco do overlay (linhas ~543-553) pela nova lógica:
    ```python
    if basemap_patch is not None:
        ceil  = _STREET_CEIL
        gain  = _STREET_GAIN
        cap   = _STREET_CAP
        lum   = basemap_patch.convert("L")
        # pixels ESCUROS (lum <= ceil) = ruas/nomes do Voyager; opacidade proporcional à escuridão
        street_mask = lum.point(
            lambda v: 0 if v >= ceil else min(cap, int((ceil - v) * gain))
        )
        region = image.crop((left, top, right, bottom))
        region.paste(basemap_patch, (0, 0), street_mask)
        image.paste(region, (left, top))
    ```
  - NUNCA usar `ImageFilter.FIND_EDGES`. O overlay usa os pixels nativos do tile.

**Passo 4 — Ajustar as cores do tema claro**
  - `_DARK_MAP_INK`: `(226, 232, 240)` → `(31, 41, 55)` (tinta escura para barra de escala e labels legíveis sobre fundo claro).
  - `_CIRCLE_RGBA`: manter `(255, 176, 59, 235)` (laranja, consistência com o dashboard — decisão do gate).
  - Comentário de `_CIRCLE_RGBA`: atualizar para "laranja, visível sobre o fundo claro (decisão gate BLK-CENSO-03)".

**Passo 5 — Fallback offline: canvas claro**
  - No `_render_camada`, no bloco `if not drew_basemap:` (linha ~519-521), substituir a cor de fundo do fallback:
    - Remover: `fill=(34, 38, 49)` (escuro)
    - Substituir por: `fill=(245, 245, 245)` (cinza claro quase branco)
  - Também corrigir o fill do `crop` quando o basemap é colado (linha ~508): `(34, 38, 49)` → `(245, 245, 245)` (para os cantos fora dos tiles também ficarem claros).
  - No bloco de interiors de polígono (linha ~537), o `fill=(34, 38, 49, 120)` (interior escuro) deve virar `(200, 200, 200, 80)` (cinza claro, neutro sobre fundo claro).

**Passo 6 — Atualizar o footer (atribuição Voyager)**
  - No `_render_camada`, no bloco que define `fundo` (linha ~590):
    - Substituir `"Fundo de ruas: CartoDB Dark Matter."` por `"Fundo de ruas: CartoDB Voyager. (c) OpenStreetMap, (c) CARTO."`
  - A string já existente `"(c) OpenStreetMap, (c) CARTO."` deve permanecer/ser mantida.

### Bloco B — `censo_map.py`: camada Concorrentes SEM choropleth

**Arquivo:** `src/motor_expansao/dashboard/censo_map.py`

**Passo 7 — Camada Concorrentes = apenas basemap + pins + ponto central (sem choropleth)**

Há duas abordagens; adotar a **Abordagem A** (branch dedicado dentro de `_render_camada`):

- Adicionar parâmetro booleano opcional `pins_only: bool = False` à assinatura de `_render_camada`.
- Quando `pins_only=True`:
  - **Pular** o loop `for geom_3857, idx in sector_records_3857:` (sem choropleth de faixas).
  - **Pular** o bloco do overlay de ruas de pixel (`if basemap_patch is not None`).
  - **Manter** tudo o mais: basemap, círculo laranja, pin central, pins de concorrentes/Ultra, escala, footer.
  - **Legenda** diferente: passar `legenda_entries=[]` e `legenda_titulo` atualizado (ver Passo 9).

- Em `render_mapas_censitarios_combinados`, ao chamar `_render_camada` para a camada concorrentes, adicionar `pins_only=True`:
  ```python
  concorrentes_png = _render_camada(
      titulo="Relatorio Pontual Censitario - Concorrentes e Ultra",
      legenda_titulo="Pins: Ultra e concorrentes",
      legenda_entries=[],  # sem faixas de score
      color_fn=_color_for_score,   # irrelevante quando pins_only=True
      source_values=score_series,  # irrelevante quando pins_only=True
      pins_only=True,
      **common,
  )
  ```

**Passo 8 — Atualizar `_draw_legend_camada` para o caso sem faixas**
  - Hoje o método escreve o título das faixas e depois itera as entradas. Quando `entries=[]`, o loop não executa; apenas o "Ponto central" e "Pins: Ultra e concorrentes" aparecem.
  - Garantir que `_draw_legend_camada` seja robusto para `entries=[]` (sem erro). Verificar o cálculo de `base_y` — nenhuma mudança deve ser necessária além de checar que `len(entries) * 24` funciona para zero entradas.

**Passo 9 — Título e legenda corretos para a camada pins-pura**
  - `titulo`: `"Relatorio Pontual Censitario - Concorrentes e Ultra"` (sem "score" nem "contexto")
  - `legenda_titulo`: `"Pins: Ultra e concorrentes"` (sem faixas de choropleth)
  - `legenda_entries`: `[]`
  - O texto da legenda de baixo ("Ponto central" / "Pins: Ultra e concorrentes") fica visível e suficiente.

### Bloco C — `censo_report.py`: atualizar título e atribuição

**Arquivo:** `src/motor_expansao/dashboard/censo_report.py`

**Passo 10 — Atualizar `MAP_LAYER_TITLES` e `_map_page` da camada concorrentes**
  - `MAP_LAYER_TITLES` (linha ~28): terceira entrada `("concorrentes", "Score censitario de contexto")` → `("concorrentes", "Concorrentes e Ultra")`.
  - No `_map_page` chamado para concorrentes em `gerar_pdf_relatorio_pontual_censitario` (linha ~530): o argumento `title=` muda automaticamente via `MAP_LAYER_TITLES`; verificar e confirmar.
  - No `_competitors_page` (função `_competitors_page`) não há mudança de título da página (o título é "Concorrentes" e permanece); apenas o PNG embutido nessa página vem da camada concorrentes (que agora é pins-pura, o que é exatamente o que se quer numa página de concorrentes).

**Passo 11 — Atualizar `_ATRIBUICAO_TILES` e `_credit_page`**
  - `_ATRIBUICAO_TILES` (linha ~60): não alterar o valor `"(c) OpenStreetMap, (c) CARTO"` — ele é genérico e cobre tanto Dark Matter quanto Voyager.
  - No `_credit_page`, linha ~485: `"Fundo de ruas: {_ATRIBUICAO_TILES}."` permanece válido. Nenhuma mudança necessária.
  - Se houver qualquer referência textual literal a "Dark Matter" em `censo_report.py`, substituir por "Voyager". Verificar com busca no arquivo.

### Bloco D — Validação visual e documentação

**Passo 12 — Validação visual OBRIGATÓRIA antes de commitar (Builder executa)**
  - Criar script one-shot em `data/reports/scratch/` (gitignored):
    ```bash
    python - <<'EOF'
    import sys, os
    sys.path.insert(0, "src")
    import pandas as pd
    from shapely.geometry import box
    from pyproj import Transformer
    from shapely.ops import transform
    from motor_expansao.dashboard.censo_point import CRS_ORIGEM_CENSO, _local_metric_crs
    from motor_expansao.dashboard.censo_map import render_mapas_censitarios_combinados

    LAT, LNG = -22.87650, -43.34582   # ponto de referência RJ
    metric_crs = _local_metric_crs(LAT, LNG)
    tr = Transformer.from_crs(metric_crs, CRS_ORIGEM_CENSO, always_xy=True)
    def to_wgs(g): return transform(tr.transform, g)

    rows = []
    for key, b in (("A", box(-700,-700,0,700)), ("B", box(0,-700,700,700))):
        g = to_wgs(b)
        minx,miny,maxx,maxy = g.bounds
        rows.append({
            "cod_setor": key, "uf": "RJ", "cod_municipio": "3304557",
            "nome_municipio": "RIO DE JANEIRO",
            "area_setor_m2": float(b.area),
            "geometry_wkb": g.wkb,
            "bbox_minx": minx, "bbox_miny": miny, "bbox_maxx": maxx, "bbox_maxy": maxy,
            "pop_total_setor_2022": 1000.0,
            "renda_per_capita_setor_2022_calibrada": 3000.0,
            "densidade_pop_setor_hab_km2": 8000.0,
            "score_setor_2022_calibrado": 70.0,
            "flag_renda_disponivel": True, "flag_geometria_valida": True,
            "qualidade_join_uf": "A",
        })
    df = pd.DataFrame(rows)
    mapas = render_mapas_censitarios_combinados(LAT, LNG, df, width=1000, height=760, basemap=True)
    os.makedirs("data/reports/scratch", exist_ok=True)
    for k, v in mapas.items():
        path = f"data/reports/scratch/blk_censo_03_rj_{k}.png"
        open(path, "wb").write(v)
        print(f"Saved {path} ({len(v)} bytes)")
    print("DONE. Abrir PNGs para inspeção visual:")
    print("  - Fundo claro (Voyager) visível? Sim/Não")
    print("  - Ruas/nomes escuros visíveis sobre o choropleth? Sim/Não")
    print("  - Camada concorrentes = SÓ PINS (sem choropleth de cor)? Sim/Não")
    print("  - Círculo laranja presente? Sim/Não")
    print("  - Barra de escala em tinta escura? Sim/Não")
    EOF
    ```
  - Salvar PNGs em `data/reports/scratch/blk_censo_03_rj_*.png` (gitignored).
  - Builder anota resultado da checagem visual no handoff ao QA.
  - Critério de reprovação: ruas invisíveis, choropleth ilegível, canvas escuro no fallback, camada concorrentes com choropleth de cor.

**Passo 13 — `docs/relatorio_pontual_censitario.md`: atualizar §6 e §8**
  - §6, parágrafo "Fundo de ruas": substituir menção a "CartoDB Voyager No-Labels" por "CartoDB Voyager COM labels"; atualizar `_BASEMAP_PROVIDER_ATTR` e contraste.
  - §6, camada concorrentes: substituir "mapa de score com pins" por "mapa SÓ de pins (sem choropleth de score); exibe basemap + pins de concorrentes/Ultra + ponto central + legenda de pins".
  - §8, último bullet: atualizar "CartoDB Voyager via contextily" + nota de que a camada Concorrentes é pins-pura.

**Passo 14 — `CLAUDE.md §4`: atualizar estado FU3 → FU4**
  - No bloco de texto do BLK-CENSO-03 dentro do §4, substituir referências a "DarkMatter", "fundo escuro", "pixels CLAROS das ruas", "luminância > `_STREET_FLOOR`" pelas equivalentes: "Voyager COM labels", "fundo claro", "pixels ESCUROS das ruas", "luminância < `_STREET_CEIL`", "fallback offline = canvas claro".
  - Acrescentar: "camada Concorrentes é SÓ-pins (sem choropleth) a partir de BLK-CENSO-03".

**Passo 15 — `CLAUDE.md DEC-004`: atualizar provedor**
  - Na seção `DEC-004` (§8 do CLAUDE.md), campo "Decisão": substituir "CartoDB Dark Matter COM labels" por "CartoDB **Voyager COM labels**"; manter o restante do guardrail inalterado.

### Bloco E — Testes

**Passo 16 — Atualizar testes existentes em `tests/unit/test_relatorio_pontual_censitario_mapa.py`**

Testes que DEVEM ser verificados (provavelmente não requerem mudança, mas confirmar):
1. `test_mapa_censitario_ponto_central_pin_vermelho` — pin vermelho continua presente na camada concorrentes (pins_only); confirmar que o assert não quebra.
2. `test_mapa_censitario_fallback_offline_sem_tiles` — sem mudança necessária.
3. `test_fetch_basemap_sem_contextily_devolve_none` — sem mudança necessária.

**Passo 17 — Adicionar novos testes em `tests/unit/test_relatorio_pontual_censitario_mapa.py`**

Adicionar ao final (5 testes novos):
1. `test_basemap_provider_attr_e_voyager`: assert `m._BASEMAP_PROVIDER_ATTR == "Voyager"`.
2. `test_overlay_usa_pixels_escuros_nao_claros`: assert `hasattr(m, "_STREET_CEIL")` + `not hasattr(m, "_STREET_FLOOR")` + `100 < m._STREET_CEIL < 220`.
3. `test_camada_concorrentes_pins_pura_sem_choropleth`: variância de cor na área central da camada "concorrentes" < variância da camada "densidade" (sem choropleth = mais uniforme).
4. `test_fallback_offline_canvas_claro`: luminância do pixel central no canvas fallback > 180.
5. `test_dark_map_ink_e_escuro`: luminância de `_DARK_MAP_INK[:3]` < 100.

**Passo 18 — Atualizar testes existentes em `tests/unit/test_relatorio_pontual_censitario_export.py`**

1. `_count_layer_titles` (linha ~116): tuple `titulos` → substituir `b"Score censitario de contexto"` por `b"Concorrentes e Ultra"`.
2. `test_pdf_retrocompat_aceita_bytes_unico_legado`: confirmar que não há assert direto de `b"Score censitario de contexto"` — se houver, atualizar.
3. Adicionar `test_pdf_footer_cita_voyager`: assert `b"OpenStreetMap"` e `b"CARTO"` no PDF + `m._BASEMAP_PROVIDER_ATTR == "Voyager"` via import.

---

## Arquivos afetados (lista completa para commit por path)

- `src/motor_expansao/dashboard/censo_map.py`
- `src/motor_expansao/dashboard/censo_report.py`
- `tests/unit/test_relatorio_pontual_censitario_mapa.py`
- `tests/unit/test_relatorio_pontual_censitario_export.py`
- `docs/relatorio_pontual_censitario.md`
- `CLAUDE.md` (§4 BLK-CENSO-03 + DEC-004)

---

## Dependências

- `contextily` já no extra `[basemap]` e na imagem. Nenhuma nova dependência.
- `numpy`, `PIL/Pillow` já são dependências base.

---

## Riscos técnicos

1. Calibração visual do overlay Voyager: `_STREET_CEIL=160` é ponto de partida; Builder DEVE validar visualmente (Passo 12).
2. Conflito verde Voyager × rampas de renda/score: gate decidiu MANTER rampas. Se QA reprovar visualmente, vira follow-up (não trocar paleta neste ciclo).
3. Alpha choropleth 140: em fundo claro pode ficar opaco demais ou fino demais — Builder calibra.
4. Rebuild de imagem Docker obrigatório para deploy: QA não executa deploy; deploy exige aprovação de Felipe.

---

## Critérios de aceite finais

1. `_BASEMAP_PROVIDER_ATTR == "Voyager"`.
2. `_STREET_CEIL` existe; `_STREET_FLOOR` não existe. Overlay usa `lum < _STREET_CEIL`. Nunca FIND_EDGES.
3. `_DARK_MAP_INK` tem luminância < 100 (tinta escura).
4. Fallback offline: fill claro `(245, 245, 245)`; luminância do pixel central > 180.
5. Camada "concorrentes": `pins_only=True`, sem choropleth.
6. Título PDF camada concorrentes: `"Concorrentes e Ultra"`.
7. `MAP_LAYER_TITLES[2][1] == "Concorrentes e Ultra"`.
8. Footer cita "CartoDB Voyager".
9. Frame retangular, raio 1.5 km, `setor_censitario_intersecao_area_1p5km`: INTOCADOS.
10. Suite `pytest -n auto` sem falhas; ruff/mypy limpos; smoke import ok.
11. Validação visual aprovada no ponto RJ `-22.87650,-43.34582`.
12. Aprovação de Felipe antes de deploy.

---

## Validações obrigatórias

### Builder (subconjunto):
```
python -m pytest -n auto tests/unit/test_relatorio_pontual_censitario_mapa.py tests/unit/test_relatorio_pontual_censitario_export.py -v
ruff check src/motor_expansao/dashboard/censo_map.py src/motor_expansao/dashboard/censo_report.py
mypy src/motor_expansao/dashboard/censo_map.py src/motor_expansao/dashboard/censo_report.py
python -c "from motor_expansao.dashboard.censo_map import render_mapas_censitarios_combinados; print('import ok')"
```

### QA (suite full):
```
python -m pytest -n auto -q
ruff check src/ tests/
mypy src/
python -c "import streamlit_app; print('smoke ok')"
```

### Validação visual REAL (Builder, obrigatória):
- Executar script do Passo 12 (requer internet + contextily).
- Salvar PNGs em `data/reports/scratch/blk_censo_03_rj_*.png`.
- Anotar resultado no handoff ao QA.

---

## Fora de escopo

- Recálculo/escrita em M1 (score, carteira, plano, artefatos).
- `setor_censitario_intersecao_area_1p5km` ou raio 1.5 km.
- Nova dependência de biblioteca.
- Dashboard interativo dependente de internet.
- `ImageFilter.FIND_EDGES`.
- Troca de paleta de renda/score.
- Adicionar 4ª camada ao relatório.
- `constants.py`, `pages.py` (verificar referências literais a Dark Matter; se nenhuma, INALTERADOS).
- Deploy VPS (aprovação de Felipe por comando individual).

---

## Alerta de aprovação
Não aplicável: criticidade Média; gate resolvido upfront. READ-ONLY sobre M1.

---

## Guardrails ativos

- ABSOLUTO: zero escrita em `score_priorizacao`, `hex_score_estrutural`, carteira, plano ou artefatos M1.
- DEC-004: tiles só na geração do relatório; cache; fallback gracioso; dashboard interativo sem internet. Provedor Dark Matter → Voyager COM labels; guardrail idêntico.
- NUNCA FIND_EDGES.
- `setor_censitario_intersecao_area_1p5km` e raio 1.5 km: INTOCADOS.
- Sem nova dependência.
- Guardrail VPS (§6 CLAUDE.md): confirmação explícita de Felipe por comando individual.
