# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-MA-01 — Contrato e decisões do enriquecimento de vulnerabilidade (design, Plano B). Bloco
**SÓ-DOC — ZERO código de produção**.

## O que foi feito
Escrito o contrato canônico `docs/vulnerabilidade_ma_contrato.md` (NOVO) seguindo o esqueleto de 15
seções do Planner, com as 8 decisões de produto do gate humano (2026-07-23, Vinicius) já resolvidas e
gravadas. Conteúdo:
- Cabeçalho `[canônico]` (responsável Felipe Silva, versão 2026-07-23) + guardrails invariantes (§5
  READ-ONLY M1, DEC-012 anti-PII, DEC-013 extensão do lote, §2 sem API ao vivo, SEM DEC).
- Objetivo do epic + a **INVERSÃO** da tese de M&A em destaque (comprar quer demanda ALTA + residual
  BAIXO, oposto de `abrir_agora`).
- D1 FASEADO: MVP hex-level (anti-PII) já; nomeação por-academia deferida, com a nuance factual de que o
  `nome` existe nos CSVs brutos e é dropado em `_ler_csv_tp_wh` (dado de negócio, não PII DEC-012).
- Tabela dos 6 sinais (direção ↑vuln, fonte, coluna/artefato, maturidade, tratamento n/d, condicional) +
  mapa "4 sinais originais → Plano B".
- D3=Não: sinal 2 (rating) `n/d` PERMANENTE no Plano B, movido a BLK-MA-07.
- D4: metodologia do score — componentes `vi`, normalização percentil-por-universo, pesos-alvo
  `S1=0,15/S2=0,25/S3=0,35/S4=0,25` e EFETIVOS no Plano B (renormalizados `0,15/0,75`, `0,35/0,75`,
  `0,25/0,75` → `S1≈0,20 / S3≈0,467 / S4≈0,333`), renormalização para ausente/imaturo, flags
  (`n_sinais_disponiveis`, `flag_serie_imatura`, `flag_score_provisorio`), NÃO-preditivo,
  `score_vulnerabilidade ∈ [0,100]`.
- D5: hex quente = `sam_fitness_potencial` top quartil AND `score_oportunidade_residual < 25`; distância
  `h3.grid_disk(k=1)`; join READ-ONLY no molde `enriquecer_outputs_residual_mercado.py:68-82` com asserts
  (`validate="many_to_one"`, `score_priorizacao`/ranks `.equals`); a camada M&A LÊ, nunca escreve.
- D6 (entregável Parquet gitignored + CSV `sep=";"`/`utf-8-sig`), D7 (anti-PII), D8 (passo no
  `run_weekly_90.sh` pós-regen).
- Decomposição BLK-MA-02..07 ajustada pelo D3=Não (rating movido a BLK-MA-07).
- Registro D1–D8 (tabela questão | opção escolhida | default).
Índice `docs/README.md`: 1 linha adicionada na seção "1. Modelo, scores e relatórios".

## Arquivos alterados
- `docs/vulnerabilidade_ma_contrato.md` — NOVO. Contrato canônico de 15 seções (entregável principal).
- `docs/README.md` — +1 linha no índice (seção 1) apontando o novo contrato.

## Validações executadas
- `python -m pytest -q tests/unit/test_claude_md_size.py` → **2 passed, 101 warnings in 0.48s**
  (0 falhas; o doc não incha nem toca o CLAUDE.md).
- `git status --porcelain` + `git diff --stat`:
  - ` M docs/README.md` (diff `--stat`: 1 file changed, 1 insertion(+))
  - `?? docs/vulnerabilidade_ma_contrato.md` (novo)
  - (os `?? context/handoff/2026072*-block-orchestrator.md` e `-planner.md` são snapshots pré-existentes,
    não tocados por este bloco)
  - **NENHUMA** linha de `src/`, `config.py`, `pipelines/m1`, artefato oficial, `PRD.md`,
    `tasks/current_task.md`.
- `import streamlit_app` → **import ok** (smoke mínimo; nada quebrou).
- Auto-revisão de acentuação §2: prosa em português acentuado; **todos** os identificadores/valores de
  enum/nomes de coluna dentro de backticks em ASCII (varredura confirmou que os acentos das linhas
  marcadas estão apenas na prosa ao redor, nunca dentro dos identificadores).

## Problemas encontrados
Nenhum.

## Pendências
Nenhuma (bloco SÓ-DOC concluído). Sucessores: BLK-MA-02..06 (implementação) e BLK-MA-07 (reputação
externa, opcional, com gate + DEC próprios).

## Riscos remanescentes
Nenhum de código (ZERO código de produção). Riscos de produto já documentados no próprio contrato
(sinal 2 `n/d` permanente no Plano B; ramp-up da série de snapshots; reconciliação 28 scrapers vs 90
coletores no BLK-MA-02).

## Guardrails verificados
- score_priorizacao não alterado: sim (bloco SÓ-DOC; nenhum artefato/código do M1 tocado)
- Artefatos M1 preservados: sim (diff confinado a `docs/`)
- Dashboard offline mantido: não aplicável (só-doc; nenhuma dependência de API introduzida)
