# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-EST-01-FU1 — Marca d'água sutil no rodapé inferior-direito (horizontal) do Relatório Pontual

## O que foi feito

Ajustadas constantes e corpo de `_draw_watermark` em `censo_report.py` para mover a marca d'água de faixa diagonal centralizada grande para texto horizontal pequeno e discreto no canto inferior-direito, conforme plano do Planner.

**Constantes alteradas (linhas 84–87):**
- `_WATERMARK_ALPHA`: 0.16 → **0.40** (discreto-porém-auditável em fonte 9 pt)
- `_WATERMARK_ANGLE`: 45.0 → **0.0** (horizontal; rotação removida)
- `_WATERMARK_FONT_PT`: 60 → **9** (corpo de rodapé tipográfico)
- `_WATERMARK_MARGIN`: nova constante **20.0** pt (margem das bordas direita e inferior)

**`_draw_watermark` reescrita:**
- Fonte: `"Helvetica", ""` (peso normal, não "B")
- Posição: `x = _PAGE_W - _WATERMARK_MARGIN - get_string_width(text)`, `y = _PAGE_H - _WATERMARK_MARGIN` → canto inferior-direito (baseline ~520 pt em página 540 pt)
- Removido: bloco `with pdf.rotation(_WATERMARK_ANGLE, x=cx, y=cy)` (era o único uso no arquivo)
- Mantido: `with pdf.local_context(fill_opacity=_WATERMARK_ALPHA)` envolvendo `pdf.text(x, y, text)`
- Docstring atualizada para descrever o novo comportamento (BLK-EST-01-FU1)

**Inalterado:** texto `_WATERMARK_BASE`, `_watermark_text()`, param `solicitante`, loop por-página (linhas 635–637), template 7 páginas, raio/interseção, `_WATERMARK_RGB`, `_PAGE_W`, `_PAGE_H`.

## Arquivos alterados
- `src/motor_expansao/dashboard/censo_report.py` — constantes `_WATERMARK_ALPHA`/`_WATERMARK_ANGLE`/`_WATERMARK_FONT_PT` (linhas 84–86) + nova `_WATERMARK_MARGIN` (linha 87) + corpo de `_draw_watermark` (linhas 261–275)

## Validações executadas
- **pytest (subconjunto impactado):** `tests/unit/test_relatorio_pontual_censitario_export.py` + `tests/unit/test_relatorio_pontual_censitario_mapa.py` → **29 passed, 0 failed, 0 skipped**
  - `test_pdf_marca_dagua_com_solicitante`: VERDE (texto `b"Ultra Academia"` e `b"Analista Teste"` presentes; `/Count 7` presente)
  - `test_pdf_marca_dagua_sem_solicitante`: VERDE
  - `test_pdf_marca_dagua_em_todas_as_paginas`: VERDE (`count >= 7`)
- **import:** `import streamlit_app` → **ok** (warnings de runtime Streamlit normais; sem erros)
- **ruff:** `python -m ruff check src/motor_expansao/dashboard/censo_report.py` → **All checks passed!**
- **mypy:** `python -m mypy src/motor_expansao/dashboard/censo_report.py --ignore-missing-imports` → **Success: no issues found in 1 source file**

## Problemas encontrados
Nenhum.

## Pendências
- Verificação visual pelo orquestrador (geração de PDF amostra com `solicitante="Teste"` + leitura da página) — conforme previsto em `tasks/current_task.md`.
- Suite completa (`pytest -q`) — gate do QA.

## Riscos remanescentes
- Legibilidade visual da marca em 9 pt / alpha 0.40 — julgamento subjetivo; valores são constantes fáceis de ajustar sem risco se QA/orquestrador julgar muito visível ou pouco visível.
- 3 falhas pré-existentes na suite completa (alheias a este ciclo; reportadas pelo BO); o Builder não introduziu novas falhas (confirmado no subconjunto).

## Guardrails verificados
- score_priorizacao não alterado: não aplicável (READ-ONLY sobre M1; zero escrita em score/artefatos)
- Artefatos M1 preservados: sim (nenhum parquet ou artefato oficial tocado)
- Dashboard offline mantido: sim (sem nova dependência de API; só `censo_report.py` alterado)
- Anti-PII: `set_compression(False)` e `pdf_version="1.4"` inalterados; texto da marca permanece `_watermark_text(solicitante)` sem alteração
- Parâmetros canônicos §3 inalterados: H3_RESOLUTION, DIST_MIN_ULTRA_KM, RENDA_MIN, pesos renda=0.40/pop=0.60
