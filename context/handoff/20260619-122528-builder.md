# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-UI-09 — Busca por link do Google Maps na barra de pesquisa (3 formatos: coordenada,
endereço, link). **Opção B** aprovada por Vinicius em 2026-06-19 (segue redirect HTTP de
links curtos `maps.app.goo.gl`/`goo.gl/maps`); emenda à DEC-010 (2026-06-19) já registrada
em CLAUDE.md §8.

## O que foi feito
1. **`maps_geocoder.py` (camada `api`)** — novo helper `resolve_short_link(url, *, timeout=6.0)
   -> str | None`: `urllib.request` puro (`Request` com User-Agent `_GEOCODE_USER_AGENT`,
   `urlopen`, lê `resp.geturl()`/`resp.url` após os redirects), `try/except` amplo → `None` em
   qualquer falha/sem rede/timeout/URL vazia. Sem Selenium. Módulo segue importável sem rede (a
   rede só ocorre na chamada). Reusa o User-Agent identificável e o estilo de `resolve_endereco_http`.
2. **`pages.py` — 2 helpers de módulo puros** (sem I/O, testáveis): `_parece_link(raw)`
   (prefixo http/https) e `_e_link_curto_maps(raw)` (`maps.app.goo.gl`/`goo.gl/maps`). Servem só
   para rotear a cascata e escolher a mensagem.
3. **`pages.py — render_coord_search_sidebar`** — cascata
   `numérico (INTOCADO) → link Maps (NOVO) → endereço (INTOCADO)`:
   - `_parece_link(raw)` True → ramo de link (nunca cai no Nominatim como endereço literal).
     - URL longa → `extract_any_coord` (regex OFFLINE) + `_validate_brazil_bbox`.
     - Link curto (`_e_link_curto_maps`) → `resolve_short_link` (rede, import lazy) p/ obter a URL
       longa, depois `extract_any_coord` + `_validate_brazil_bbox`. Falha/sem rede/timeout →
       `st.warning` claro pedindo URL longa ou coordenada (fallback gracioso, sem exceção).
     - Coordenada extraída mas fora do Brasil ou link sem coordenada → `st.warning` específico +
       `return None`.
   - Caminho **numérico** (`parse_coordinate_input`) e caminho de **endereço**
     (`resolve_endereco_http` + `_render_endereco_fallback_link`) preservados byte-a-byte.
   - Título/caption/label/placeholder atualizados p/ mencionar os 3 formatos (incl. "link do
     Google Maps"); `key="coord_search_input"` PRESERVADA.
4. **`tests/unit/test_coord_search.py`** — nova seção (nenhum teste bate na rede real):
   - `extract_any_coord` (regex pura, SEM rede): URL longa `!3d/!4d` no Brasil → coord; URL
     `@lat,lng` → centro; URL fora do Brasil (Paris) → par bruto rejeitado por `_validate_brazil_bbox`
     (None); URL longa sem coord (`/search?...`) → `(None, None)`; link curto `maps.app.goo.gl` →
     `(None, None)`.
   - `_parece_link` / `_e_link_curto_maps` — parametrizados True/False.
   - `resolve_short_link` SEMPRE com `urllib` MOCKADO (stub `_FakeRedirectResp` com `geturl()`):
     sucesso (mock devolve URL longa com `!3d/!4d` → string, e o `extract_any_coord` do resultado
     dá a coord do Brasil), falha de rede (`TimeoutError` → None), URL vazia → None.

## Arquivos alterados
- `src/motor_expansao/api/maps_geocoder.py` — adicionado `resolve_short_link` (urllib puro,
  segue redirect de link curto → URL longa final; None em qualquer falha). Nada mais tocado.
- `src/motor_expansao/dashboard/pages.py` — adicionados `_parece_link` e `_e_link_curto_maps`
  (helpers de módulo) acima de `render_coord_search_sidebar`; inserido o ramo (2) de link Maps na
  cascata; docstring + título + caption + label + placeholder atualizados p/ os 3 formatos.
- `tests/unit/test_coord_search.py` — nova seção de testes (extract_any_coord + helpers de
  roteamento + resolve_short_link mockado).

## Validações executadas
- pytest (subconjunto impactado, SERIAL): `python -m pytest -q tests/unit/test_coord_search.py
  tests/integration/test_streamlit_app.py` → **252 passed, 0 failed, 0 skipped** (12144 warnings,
  só DeprecationWarning de `pytest_asyncio`/Python 3.14, pré-existentes).
  - NOTA: `-n auto`/xdist abortou com `INTERNALERROR ... EOFError` de execnet (Python 3.14 local —
    precedente documentado nos ciclos BLK-ORQ-01/anteriores). Refiz SERIAL como autorizado pelo
    orquestrador; **não é bypass** (a suíte full roda 1× no QA).
- import: **ok** (`python -c "import streamlit_app; print('import ok')"`).
- ruff: **All checks passed!** (`pages.py`, `maps_geocoder.py`, `test_coord_search.py`).
- mypy: **Success: no issues found in 2 source files** (`pages.py`, `maps_geocoder.py`).
  - 1 erro inicial de `arg-type` (`target_url: str | None` passado a `extract_any_coord`) foi
    corrigido estreitando o tipo: `target_url: str = raw` + variável `resolved_url: str | None`
    com early-return; sem mudança de comportamento.

## Problemas encontrados
- xdist INTERNALERROR no Python 3.14 local (ambiente, não defeito) → fallback serial autorizado.
- mypy `arg-type` em `extract_any_coord(target_url)` → corrigido por narrowing de tipo (acima).

## Pendências
- QA deve rodar a suíte FULL (gate único) — preferir serial se o xdist abortar localmente.

## Riscos remanescentes
- `resolve_short_link` faz 1 requisição HTTP ao seguir redirect de link curto (rede). Coberto pela
  emenda à DEC-010 (Opção B): escopo restrito ao sub-caminho de link curto da busca, import lazy,
  timeout 6 s, try/except amplo → None, fallback gracioso, nada persistido além de lat,lng. A
  suíte NUNCA bate na rede real (urllib mockado). O dashboard offline e os Blocos 4–6/basemap
  seguem sem rede.
- `@lat,lng` é o centro da câmera (menos preciso que o pino `!3d/!4d`) — comportamento idêntico
  ao já usado na aba de viabilidade; precedência `!3d/!4d > @` garantida dentro de `extract_any_coord`.

## Guardrails verificados
- score_priorizacao não alterado: sim (não aplicável — só UX da barra de busca; entrada → coordenada).
- Artefatos M1 preservados: sim (nenhuma escrita em score/pesos/carteira/plano/artefatos oficiais).
- Dashboard offline mantido: sim (rede só no sub-caminho de link curto, import lazy, fallback
  gracioso; Blocos 4–6/basemap intocados).
- Caminhos numérico e de endereço: byte-a-byte preservados; testes existentes intactos e verdes.
