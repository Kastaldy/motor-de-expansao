# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-VIAB-02 — Faixa de demanda-premissa por tier de metragem (comparáveis reais)

## O que foi feito

Criados exatamente 2 arquivos novos conforme escopo:

1. `src/motor_expansao/dimensionamento/demanda_premissa.py` — módulo puro, determinístico,
   sem I/O oculto, sem dependências novas. Funções públicas:
   - `carregar_ultra(path)` → lê `unidades_ultra_performance_hex.parquet`, retorna ["metragem","alunos"]
   - `carregar_eng_corpo(path)` → lê `academias_engenharia_do_corpo.xlsx`, retorna ["metragem","alunos"]
   - `combinar_bases(ultra_df, eng_df)` → concatena, dropa NaN/<=0
   - `calcular_tiers(df, *, n_extrapolacao_min=5)` → 5 linhas com p10/p50/p90 por tier
   - `materializar(df_tiers, staging_dir, analysis_dir)` → (parquet_path, md_path)
   - `run(ultra_path, eng_path, staging_dir, analysis_dir)` → orquestra tudo

2. `tests/unit/dimensionamento/test_demanda_premissa.py` — 28 testes unitários com fixture
   sintética em memória/tmpdir (ZERO leitura de arquivos reais).

Executado contra dados reais: N por tier = 17, 46, 36, 11, 2 (total 112) — bate exatamente
com o preview do Block Orchestrator.

Artefatos materializados (gitignored):
- `data/staging/demanda_premissa_por_tier.parquet` (5 linhas, 9 colunas)
- `data/analysis/demanda_premissa_qualidade.md` (tabela de tiers + alertas)

Ajuste no backlog: "~1.100 academias" -> "~112 unidades (Ultra 54 + Eng Corpo 58)".

## Arquivos alterados

- `src/motor_expansao/dimensionamento/demanda_premissa.py` — CRIADO (novo módulo)
- `tests/unit/dimensionamento/test_demanda_premissa.py` — CRIADO (28 testes unitários)
- `tasks/backlog.md` — corrigido "~1.100 academias" -> "~112 unidades"; Depende de atualizado
- `tasks/completed.md` — BLK-VIAB-02 registrado como concluído
- `tasks/current_task.md` — status atualizado para aguardando QA
- `context/handoff.md` — este handoff
- `context/handoff/20260707-205600-builder.md` — cópia append-only

## Validações executadas

- ruff check: limpo (0 erros) — nos 2 arquivos novos
- mypy: limpo (0 erros) — demanda_premissa.py
- pytest subset (dimensionamento/ + integration): 500 passed, 2 failed (pré-existentes Plus Code,
  confirmados via git stash), 0 erros novos introduzidos por este bloco
- pytest novos (28 testes): 28/28 passed
- smoke import: ok
- loop_guard --base HEAD: GUARD OK, 0 caminhos proibidos
- viabilidade_ponto.py: INTOCADO (git diff HEAD vazio)
- mtimes M1: brasil_estrutural.parquet=1780501621, brasil_priorizados.parquet=1780501631 (INALTERADOS)
- Execução real: 17+46+36+11+2=112 unidades, flag_extrapolacao=True só no tier >=3000 (N=2<5)

## Problemas encontrados

1. Fixture sintética inicial tinha N=4 no tier <1000 (< N_EXTRAPOLACAO_MIN=5), causando falha
   em test_flag_extrapolacao_false. Corrigido aumentando o tier para N=5.
2. Ruff detectou: numpy e pytest imports não usados no teste, e zip() sem strict=. Todos corrigidos.
3. git stash pop conflitou com config.py (CRLF), resolvido via git checkout --. Nenhuma alteração de conteúdo.

## Pendências

Nenhuma (escopo completo).

## Riscos remanescentes

- Tier >=3000 tem N=2 (flag_extrapolacao=True): instabilidade de percentis — documentada no
  relatório de qualidade e alertada no VIAB-03 conforme contrato.
- 2 falhas pré-existentes nos testes Plus Code (test_streamlit_app.py) — não introduzidas por este bloco.

## Guardrails verificados

- score_priorizacao não alterado: sim (nenhum toque em pipelines/m1/, config.py, artefatos oficiais)
- Artefatos M1 preservados: sim (mtimes inalterados; loop_guard limpo)
- Dashboard offline mantido: sim (módulo novo não introduz dependência de rede)
- DEC-009 honrada: alvo = alunos REAIS; PROIBIDO usar lat/lng/membros como preditor (testado)
- viabilidade_ponto.py INTOCADO: confirmado (git diff vazio)
