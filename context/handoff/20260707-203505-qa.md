# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (housekeeping BLK-VIAB-01 → completed.md pelo orquestrador)

## VEREDITO
APROVADO

## Justificativa
As 4 funções puras estão corretas e aderentes ao escopo (header=2, placeholder por
tolerância absoluta 0.01, flag_sem_coord booleano, sem import do config.py do M1);
ruff/mypy/smoke limpos, os 13 testes novos passam e o diff COMMITADO contém apenas os
5 arquivos esperados. As 4 falhas + 1 erro da suíte full são pré-existentes/ambientais
(`openlocationcode` e `matplotlib` ausentes), não regressões deste bloco; artefatos M1 intactos.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- `test_flag_sem_coord_adicionada` compara dtype com `bool or "bool"` e usa `is False/== False`
  redundantes (`noqa: E712`) — funciona, mas poderia ser um `assert bool == numpy.bool_` mais direto.
- Ambiente de teste do loop não tem `openlocationcode`/`matplotlib` — não é deste bloco, mas
  vale um ciclo futuro para zerar o ruído de coleta (4 failed + 1 error recorrentes).

## Testes faltantes
- nenhum crítico. A cobertura de casos-limite da limpeza (área/placeholder/range/coord) e o
  roundtrip de parquet estão presentes.

## Riscos remanescentes
- `ler_imoveis_xlsx` só é exercitado em materialização real (não há teste unitário do parser de
  xlsx com fixture .xlsx). Aceitável: `header=2` e validação de colunas são triviais e a
  materialização real foi executada sem exceção (28→23 linhas). Baixo risco.

## Saída literal das validações (re-executadas pelo QA)

### 1. Verificação de código (leitura)
```
- header=2 na leitura do xlsx: OK (imoveis_candidatos.py:67 `pd.read_excel(..., sheet_name="Imóveis", header=2)`)
- placeholder por tolerância: OK (linha 108 `(aluguel1 - ALUGUEL_PLACEHOLDER).abs() < PLACEHOLDER_TOLERANCIA`, PLACEHOLDER_TOLERANCIA=0.01)
- flag_sem_coord booleano: OK (linha 119 `pd.isna(LATITUDE) | pd.isna(LONGITUDE)` → dtype bool; testado em test_flag_sem_coord_adicionada)
- fixture sintética nos testes: OK (_df_bruto() em memória; NÃO lê xlsx real)
- import de config.py do M1: AUSENTE (grep por from motor_expansao.config / .config → exit 1, nenhuma ocorrência)
```

### 2. ruff
```
$ python -m ruff check src/motor_expansao/dimensionamento/imoveis_candidatos.py tests/unit/dimensionamento/test_imoveis_candidatos.py
All checks passed!
```

### 3. mypy
```
$ python -m mypy src/motor_expansao/dimensionamento/imoveis_candidatos.py
Success: no issues found in 1 source file
```

### 4. Pytest SUBSET
```
$ python -m pytest tests/unit/dimensionamento/test_imoveis_candidatos.py -v -q
collected 13 items
tests/unit/dimensionamento/test_imoveis_candidatos.py .............      [100%]
============================== 13 passed in 1.36s ==============================
```

### 5. Pytest FULL (gate único, -n auto)
```
$ python -m pytest -n auto -q
...
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_curto_usa_localidade_mock
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_curto_usa_localidade_mockada
ERROR tests/unit/demanda_revelada/test_viz_atratividade.py
4 failed, 1436 passed, 4 skipped, 18 warnings, 1 error in 472.63s (0:07:52)
```
Diagnóstico das falhas (todas AMBIENTAIS, não regressões deste bloco):
```
$ python -c "import openlocationcode"  → ModuleNotFoundError: No module named 'openlocationcode'  (4 testes Plus Code)
$ python -c "import matplotlib"        → ModuleNotFoundError: No module named 'matplotlib'         (1 erro coleta viz_atratividade)
$ grep -l imoveis_candidatos <os 3 arquivos que falham>  → exit 1 (nenhum referencia o módulo do ciclo)
```
Os 13 testes novos passam; nenhuma nova falha ligada a `imoveis_candidatos`.

### 6. Smoke import
```
$ python -c "from motor_expansao.dimensionamento.imoveis_candidatos import run, validar_e_limpar, ler_imoveis_xlsx, materializar; print('ok')"
ok
```

### 7. Guardrails M1 (arquivos NÃO modificados no commit)
```
$ git diff HEAD~1 HEAD --name-only | grep -E "config\.py|pipelines/m1|brasil_estrutural|brasil_priorizados|hexagonos_brasil"
(vazio) grep-exit 1 → nenhum arquivo M1 no diff commitado

$ stat -c '%n %Y' data/staging/brasil_estrutural.parquet data/staging/brasil_priorizados.parquet
data/staging/brasil_estrutural.parquet 1780501621
data/staging/brasil_priorizados.parquet 1780501631
```
Nota: mtimes 1780501621/1780501631 (o prompt citou 1780501622/1780501632, ~1s de diferença =
imprecisão do valor citado). Os artefatos M1 NÃO aparecem no diff nem têm caminho de escrita
neste bloco → INALTERADOS.

### 8. Escopo do ciclo (diff COMMITADO)
```
$ git diff HEAD~1 HEAD --name-only
context/handoff.md
context/handoff/20260707-202341-builder.md
src/motor_expansao/dimensionamento/imoveis_candidatos.py
tasks/current_task.md
tests/unit/dimensionamento/test_imoveis_candidatos.py
```
Apenas os 5 arquivos esperados. NÃO inclui config.py do M1, pipelines/m1/ nem artefatos M1.

Ressalva de escopo (NÃO bloqueante): `git diff HEAD~1 --name-only` (que inclui working-tree)
mostra também `src/motor_expansao/dimensionamento/config.py` — mas ele é apenas mudança
UNSTAGED de fim-de-linha (CRLF→LF), NÃO commitada e sem delta de conteúdo:
```
$ git diff HEAD~1 HEAD --stat -- src/.../dimensionamento/config.py   → (vazio: não está no commit)
$ git diff --ignore-all-space src/.../dimensionamento/config.py       → (vazio: zero diff de conteúdo)
$ git diff --stat src/.../dimensionamento/config.py                   → 106 insertions(+), 106 deletions(-)  (puro EOL)
```
Artefato de checkout Windows, exatamente como o Builder reportou. Não entrou no commit do ciclo.

### 9. Housekeeping pré-check
```
$ python scripts/housekeeping_move_block.py BLK-VIAB-01 --check
FALHA --check: stub ausente no backlog para BLK-VIAB-01   (exit 1)
```
FALHA ESPERADA: o move ainda não foi feito (o orquestrador executa o Passo 6.0 após o veredito).

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock do
caminho crítico. Este bloco NÃO é tooling de produção (SOPS etc.); é módulo de dados com testes
unitários legítimos usando fixture sintética em memória (prática normal, fora do escopo da regra
de bypass). A suíte full rodou 1× aqui (`-n auto`) como gate único; não houve `-p no:xdist`.

## Conferência cruzada com log do Builder
Bate. O Builder reportou 13/13 testes novos, ruff/mypy/smoke limpos, materialização real 28→23
linhas e artefatos M1 intactos — tudo reproduzido. Divergência menor: o Builder citou "2 falhas
pré-existentes"; a suíte full do QA surfou 4 falhas + 1 erro pré-existentes (2 Plus Code adicionais
em test_coord_search.py + coleta de viz_atratividade por matplotlib) — todos ambientais e
independentes deste bloco (nenhum referencia `imoveis_candidatos`), consistente com o relato.

## Guardrails verificados
- score_priorizacao não alterado: sim (não aplicável — nenhum caminho de escrita; READ-ONLY M1)
- Artefatos M1 preservados: sim (brasil_estrutural/priorizados fora do diff e sem write path)
- Validações re-executadas pelo QA (sem bypass):
  - ruff → All checks passed!
  - mypy → Success: no issues found
  - pytest subset → 13 passed
  - pytest full -n auto → 4 failed (ambientais) / 1436 passed / 4 skipped / 1 error (ambiental)
  - smoke import → ok
  - housekeeping --check → FALHA esperada (stub ausente, move pós-veredito)
- Escopo respeitado: sim (commit = 5 arquivos esperados; config.py só CRLF unstaged, não commitado)

## Decisão recomendada
Fechar ciclo. O orquestrador deve executar o housekeeping (`scripts/housekeeping_move_block.py
BLK-VIAB-01`) para mover o bloco de backlog.md → completed.md antes do commit de fechamento.
