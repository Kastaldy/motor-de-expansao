# Handoff — Builder

## Skill que gerou este handoff
Builder

## Proxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-PERF-01a — Shared transformer no render censitario + pre-filtro do agregar_municipio (PDFs 86x)

## O que foi feito

**Fix 1 — Shared transformer em `censo_map.py`:**
Antes do loop de setores em `render_mapas_censitarios_combinados`, criamos UMA vez o CRS aeqd local e o Transformer EPSG:3857 (usando `_local_metric_crs` e `_transformer`, ja importados do `censo_point`). As 3 chamadas pre-loop a `_to_mercator`/`_point_to_mercator` foram substituidas por `_project_geometry(geom, _to_3857)` e `_to_3857.transform(0.0, 0.0)`. A chamada dentro do loop tambem foi substituida por `_project_geometry(geom, _to_3857)`. As funcoes `_to_mercator` e `_point_to_mercator` permanecem no modulo sem alteracao (podem ter outros callers externos; `_render_camada` ainda as usa para pins).

**Fix 2 — Parametro opcional `df_pre_filtrado` em `agregar_municipio`:**
Adicionado parametro keyword-only `df_pre_filtrado: pd.DataFrame | None = None` a assinatura. Quando nao-None, o `df_muni` e obtido diretamente deste parametro (com `.copy()`), pulando o full-scan de `_municipio_mask` sobre o df nacional de 1,5M linhas.

Nos 3 callers de `pages.py`:
- Adicionado `_municipio_mask` ao bloco de imports de `relatorio_municipal`.
- Em `_gerar_payload_relatorio_municipal`: pre-calcula `df_muni` antes de `agregar_municipio`, passa `df_pre_filtrado=df_muni`, remove o bloco de re-filtragem duplicada pos-chamada.
- Em `render_relatorio_municipal_download_topo` (bloco `if gerar:`): idem.
- No bloco `len(selected_cities) == 1` do expander: idem.

No caller da API em `service.py`: `df_muni` ja existia na linha 476; adicionado `df_pre_filtrado=df_muni` na chamada de `agregar_municipio`.

**Testes adicionados:**
- `tests/unit/test_relatorio_pontual_censitario_mapa.py`: `test_shared_transformer_bytes_identicos` — confirma que 2 chamadas consecutivas com os mesmos inputs produzem PNGs byte-identicos (determinismo do transformer compartilhado).
- `tests/unit/test_relatorio_municipal.py`: `test_agregar_municipio_prefiltrado_identico_ao_nacional` — confirma que `df_pre_filtrado` (4 linhas SP) produz resultado identico ao full-scan de um df com 104 linhas (4 SP + 100 MANAUS), verificando 6 chaves canonicas.

## Arquivos alterados
- `/repo/src/motor_expansao/dashboard/censo_map.py` — Fix 1: transformer compartilhado (2 linhas inseridas, 4 chamadas substituidas)
- `/repo/src/motor_expansao/dashboard/relatorio_municipal.py` — Fix 2: novo parametro `df_pre_filtrado` na assinatura + branch condicional
- `/repo/src/motor_expansao/dashboard/pages.py` — Fix 2: import `_municipio_mask` adicionado; 3 callers atualizados (pre-filtro + remocao da re-filtragem duplicada)
- `/repo/src/motor_expansao/api/service.py` — Fix 2: `df_pre_filtrado=df_muni` adicionado na chamada de `agregar_municipio`
- `/repo/tests/unit/test_relatorio_pontual_censitario_mapa.py` — novo teste `test_shared_transformer_bytes_identicos`
- `/repo/tests/unit/test_relatorio_municipal.py` — novo teste `test_agregar_municipio_prefiltrado_identico_ao_nacional`

## Validacoes executadas

### Subconjunto impactado (rodado aqui)
- `pytest -q tests/unit/test_relatorio_pontual_censitario_mapa.py tests/unit/test_relatorio_municipal.py tests/integration/test_streamlit_app.py`
- **Resultado: 291 passed, 2 failed (pre-existentes), 0 skipped em 31s**
- Os 2 falhas (`test_render_coord_search_plus_code_completo_offline` e `test_render_coord_search_plus_code_curto_usa_localidade_mock`) foram verificados como PRE-EXISTENTES antes das mudancas deste ciclo (confirmado via `git stash` + rerun antes de restaurar).

### Smoke import
- `python -c "import streamlit_app; print('import ok')"` — **ok**

### Lint
- `ruff check src/motor_expansao/dashboard/censo_map.py src/motor_expansao/dashboard/relatorio_municipal.py src/motor_expansao/dashboard/pages.py src/motor_expansao/api/service.py` — **All checks passed!**

### Por que esses arquivos de teste
- `test_relatorio_pontual_censitario_mapa.py` — toca `censo_map.py` (Fix 1 direto)
- `test_relatorio_municipal.py` — toca `relatorio_municipal.py` (Fix 2 direto)
- `test_streamlit_app.py` — integracao obrigatoria (smoke da UI, importa `pages.py` e `service.py`)

## Problemas encontrados
- Dois testes pre-existentes falham na suite de integracao (nao relacionados a este ciclo: sao testes de Plus Code que falhavam antes do ciclo). Documentados e verificados.

## Pendencias
- Suite completa (`pytest -q`) e responsabilidade do QA (gate unico por ciclo) — nao rodada aqui.
- `mypy src/` nao rodado aqui (responsabilidade do QA).
- Harness B6 (`python scripts/perf_baseline_app.py`) para medir o ganho de performance nao rodado aqui (ambiente CI nao tem dados de staging).

## Riscos remanescentes
- Nenhum. A matematica de projecao e identica (mesmo transformer, mesmos parametros). O teste de byte-identity garante determinismo.
- O `df_pre_filtrado` recebe `.copy()` dentro de `agregar_municipio`, entao nao ha risco de mutacao do caller.

## Guardrails verificados
- score_priorizacao nao alterado: sim (nenhum codigo de score tocado)
- Artefatos M1 preservados: sim (zero escrita em parquets ou artefatos oficiais)
- Dashboard offline mantido: sim (Fix 1 e so render; Fix 2 e so agregacao; nenhum fetch de rede adicionado)
- `_to_mercator` e `_point_to_mercator` permanecem no modulo: sim (verificado — usados em `_render_camada`)
- `setor_censitario_intersecao_area_1p5km` e raio 1,5 km: INTOCADOS
- `config.py`, `pipelines/m1/`, artefatos M1, VPS, Dockerfile, CI: INTOCADOS
