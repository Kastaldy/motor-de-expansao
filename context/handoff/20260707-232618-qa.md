# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Block Orchestrator (fechamento)

## VEREDITO
APROVADO

## Justificativa
O script `movimentacao_concorrencial.py` e seus 11 testes atendem ao escopo (relatório
READ-ONLY a partir de staging, sem rede); todas as validações obrigatórias foram
re-executadas por conta própria com evidência literal; zero falhas novas na suíte FULL,
src/ intocado (só CRLF→LF pré-existente), M1 mtimes inalterados e anti-dupla-contagem
TotalPass/Wellhub confirmado no código e no relatório.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- `resumo_por_rede` usa `groupby().apply(..., include_groups=False)`, idiomático e sem
  warnings na versão atual do pandas; nenhuma ação necessária.

## Testes faltantes
- nenhum — cobre join LEFT, contagem por rede/UF, dominância, impacto residual, densos,
  geração/idempotência do relatório e o `run()` end-to-end com parquets sintéticos.

## Riscos remanescentes
- nenhum novo. Relatório é snapshot único (coleta 2026-04-22..05-04); re-execução futura
  com dados da coleta semanal VPS (DEC-013) produz retratos atualizados.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suite FULL (`python -m pytest -n auto -q`)
```
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_curto_usa_localidade_mock
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_curto_usa_localidade_mockada
ERROR tests/unit/demanda_revelada/test_viz_atratividade.py
4 failed, 1504 passed, 4 skipped, 19 warnings, 1 error in 491.85s (0:08:11)
```
As 4 falhas (Plus Code / openlocationcode) e o 1 erro (test_viz_atratividade / matplotlib)
são PRÉ-EXISTENTES e explicitamente aceitos. `1504 passed` bate com o log do Builder.
Zero falhas novas.

### 2. ruff (`ruff check scripts/movimentacao_concorrencial.py tests/unit/test_movimentacao_concorrencial.py`)
```
All checks passed!
```

### 3. mypy (`mypy scripts/movimentacao_concorrencial.py --ignore-missing-imports`)
```
Success: no issues found in 1 source file
```

### 4. src/ intocado (`git diff HEAD -- src/`)
```
git diff --numstat HEAD -- src/  → 106 106 src/motor_expansao/dimensionamento/config.py
git diff --word-diff=porcelain ... config.py  → (vazio, apenas warning CRLF→LF)
```
Única mudança em src/ é a conversão CRLF→LF pré-existente em `dimensionamento/config.py`,
com word-diff VAZIO (zero mudança de conteúdo). Aceito conforme instruções.

### 5. Script executa (`python scripts/movimentacao_concorrencial.py`)
```
  join_uf_cidade: 16 unidades sem UF após join (hex não encontrado no mercado)
        3,179 unidades válidas
        28 redes identificadas
  Relatório gerado: /repo/data/analysis/movimentacao_concorrencial.md
Concluído: /repo/data/analysis/movimentacao_concorrencial.md
```

### 6. Anti-dupla-contagem TotalPass/Wellhub
Confirmado por leitura de código: `resumo_densos(df_denso)` opera SÓ sobre `df_denso` e
retorna um dict próprio; seu resultado NUNCA é somado a
`oferta_consumida_mercado_estimada` (calculado só em `impacto_residual` sobre `df_mkt`).
No relatório, os densos aparecem em Seção 7 SEPARADA com aviso explícito:
```
159:> **ATENÇÃO — risco de dupla contagem:** as linhas de fonte `totalpass` e `wellhub`
161:> são reportados SEPARADAMENTE e **NÃO são somados** ao `oferta_consumida_mercado_estimada`
```

### 7. M1 mtimes (`stat -c "%Y %n" ...`)
```
1780501621 /repo/data/staging/brasil_estrutural.parquet
1780501631 /repo/data/staging/brasil_priorizados.parquet
```
Inalterados (batem com os valores esperados).

### 8. loop_guard (`python scripts/loop_guard.py --base HEAD~1`)
```
GUARD OK: 5 caminho(s) alterado(s), nenhum proibido.
```

### 9. housekeeping
`--check` inicial retornou 1 (stub ausente); executei o move:
```
python scripts/housekeeping_move_block.py BLK-PROD-06 --date 2026-07-07
  → OK: movido BLK-PROD-06 (1701 bytes) -> tasks/completed.md; stub em tasks/backlog.md.
python scripts/housekeeping_move_block.py BLK-PROD-06 --check
  → OK: BLK-PROD-06 movido (stub no backlog + bloco em completed.md).  EXIT=0
python -m pytest tests/unit/test_housekeeping_helper.py -q
  → 10 passed in 1.17s
```
Byte-identidade: o conteúdo do bloco é preservado; a única diferença vs.
`git show HEAD:tasks/backlog.md` é o separador estrutural `---` entre blocos (elemento do
backlog, não do bloco), removido pelo helper versionado. `--check` (gate autoritativo) passa.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou
mock do caminho crítico. O script rodou contra os parquets REAIS de `data/staging/`
(3.296 concorrentes, 10.165 densos, 1.542.531 linhas de mercado); os testes unitários usam
fixtures sintéticas legítimas (isolamento de lógica, prática normal — não é bypass de tooling).

## Conferência cruzada com log do Builder
Bate. Builder reportou `1504 passed, 4 failed, 4 skipped, 1 error` → idêntico. ruff/mypy/
loop_guard/script-run coincidem. O Builder havia deixado o housekeeping pendente (esperado);
o QA executou o move e validou.

## Guardrails verificados
- score_priorizacao não alterado: sim (zero mudanças de conteúdo em src/; script não importa nada de src/motor_expansao/)
- Artefatos M1 preservados: sim (mtimes de brasil_estrutural/priorizados inalterados; nenhum parquet de staging escrito)
- Validações re-executadas pelo QA (sem bypass): pytest -n auto → 1504 passed (0 novas falhas); ruff → passed; mypy → passed; script → OK; loop_guard → GUARD OK; housekeeping → EXIT 0
- Escopo respeitado: sim (só os 2 arquivos novos + relatório gitignored + arquivos de handoff/task)

## Decisão recomendada
Fechar ciclo.
