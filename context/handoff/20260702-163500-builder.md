# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-TP-08-FU — Re-ingestão das academias menores com rótulo de rede (fecha o dado para o BLK-TP-06-FU1)

## Gate humano (registro)
APROVADO por Felipe Silva em 2026-07-02:
- (A) matching por TOKEN com word-boundary `\b…\b` + lista curada das 28 redes de `concorrentes_mapeados` (precisão > recall; tokens curtos ambíguos `live`/`race`/`jab`/`phd`/`kore`/`evoque`/`gavioes` curados → só marca composta).
- (B) rede com N < 3 filiais → colapsa em `independente` (anti-reidentificação); `N_MINIMO_ANTI_REIDENTIFICACAO=3`.
- (C) artefato de oferta em formato LONGO (`hex_id × rede_menor`).
- (D) capacidade reporta média+mediana, valor recomendado = MEDIANA; `flag_confiavel` com N≥10 (`N_MINIMO_CONFIABILIDADE=10`).
READ-ONLY sobre o M1.

## O que foi feito
- Novo módulo IRMÃO `src/motor_expansao/demanda_revelada/classificacao_rede_menor.py` (funções PURAS, isolado — importa só do próprio pacote `demanda_revelada`, `contrato`, stdlib `re`/`unicodedata`, e pandas/h3/pyarrow/openpyxl da base; NÃO importa `pipelines/m1`, `dashboard`, `censo_*`, `api`). Implementa:
  - `VOCABULARIO_REDES` (dict ordenado das 28 redes → tokens curados; tokens ambíguos só via marca composta) + regexes pré-compilados por rede (word-boundary `\b…\b`, first-match).
  - `_normalizar_nome` (lower + NFKD strip de acentos + remoção de pontuação + colapso de espaços) e `classificar_rede(nome) -> str` (nome cru só em memória; nunca logado/persistido).
  - `_colapsar_baixa_cardinalidade` (rede N<3 → `independente`, gate B).
  - `_ler_e_derivar_hex_rede`: leitura do xlsx → deriva `hex_id` + `rede_menor` na FRONTEIRA → DROP-PII imediato de `Latitude`/`Longitude`/`Nome_Academia`/`Cluster_ID` (reusa `_COLUNAS_DROP_FRONTEIRA` do TP-08) → colapso anti-reid. `Total_Alunos_Cluster` nunca somado.
  - Artefato (a): `ingerir_oferta_menores_por_rede` → `data/staging/oferta_academias_menores_rede_h3.parquet` (LONGO, contrato `oferta_menores_rede_v1`).
  - Artefato (b): `gerar_capacidade_media_por_rede` → `data/staging/capacidade_media_por_rede.parquet` (contrato `capacidade_media_rede_v1`; `independente` EXCLUÍDA da tabela).
  - `gerar_relatorio_classificacao` → `data/reports/scratch/rede_menor_classificacao_qualidade.md` (só agregados; dedup FINO por `(hex_id, rede_menor)` vs dedup GROSSEIRO por hex do TP-08).
  - `_assert_sem_pii_rede`/`_assert_res7` como redes de segurança.
- `__init__.py`: exports aditivos (sem quebrar `oferta_menores_v1` do TP-08).
- Testes novos `tests/unit/demanda_revelada/test_classificacao_rede_menor.py` (14 testes; fixtures sintéticas).
- Fixture sintética `tests/fixtures/rede_menor_fake.xlsx` (nomes FABRICADOS carregando tokens + casos de falso-positivo; versionada, NÃO gitignored).
- Doc aditivo em `docs/modelo_mercado_hexagonos.md` (§7, nota BLK-TP-08-FU).
- Bookkeeping: `tasks/backlog.md` (bloco BLK-TP-08-FU), `tasks/completed.md`, `tasks/current_task.md`.

## Arquivos alterados
- `src/motor_expansao/demanda_revelada/classificacao_rede_menor.py` — NOVO (módulo de classificação anti-PII + 2 artefatos + relatório).
- `src/motor_expansao/demanda_revelada/__init__.py` — aditivo (novos exports do FU).
- `tests/unit/demanda_revelada/test_classificacao_rede_menor.py` — NOVO (14 testes, fixtures sintéticas).
- `tests/fixtures/rede_menor_fake.xlsx` — NOVO (fixture sintética SEM PII, versionada).
- `docs/modelo_mercado_hexagonos.md` — aditivo (nota BLK-TP-08-FU no §7).
- `tasks/backlog.md`, `tasks/completed.md`, `tasks/current_task.md` — bookkeeping.
- `data/staging/oferta_academias_menores_rede_h3.parquet` — NOVO, gitignored (gerado sob demanda; NÃO commitado).
- `data/staging/capacidade_media_por_rede.parquet` — NOVO, gitignored (gerado sob demanda; NÃO commitado).
- `data/reports/scratch/rede_menor_classificacao_qualidade.md` — NOVO, gitignored.

## Validações executadas
- **pytest (subconjunto impactado):** `python -m pytest -n auto -q tests/unit/demanda_revelada/ tests/integration/test_streamlit_app.py` → **254 passed, 0 failed, 0 skipped**. Escolhido porque toca só o pacote `demanda_revelada/` (+ smoke do dashboard obrigatório). Suíte full = responsabilidade do QA.
- **Testes do módulo novo (14):** `test_classificacao_rede_menor.py` → todos passam (classificação por token boundary; falso-positivo evitado `Studio Live Well`/`Terrace Fit Club`/`PHD Consultoria` → `independente`; normalização de acento; colapso N<3; contrato+dtypes; ZERO PII nos 2 parquets; relatório sem PII; agregação hex×rede; capacidade média/mediana + `flag_confiavel`; isolamento import ast; determinismo).
- **import:** `python -c "import streamlit_app; print('import ok')"` → **ok**.
- **ruff:** `ruff check` nos 3 arquivos → 0 erro (1 import-sort auto-corrigido).
- **mypy:** `mypy src/motor_expansao/demanda_revelada/` → **Success: no issues found in 10 source files**.
- **Isolamento (ast):** `test_isolamento_import` passa (sem `pipelines.m1`/`dashboard`/`censo`/`motor_expansao.api`).

## Geração dos artefatos REAIS (sob demanda, sobre `NAO_ABRA/03_Competidores.xlsx`)
- **Cobertura de classificação (24.045 academias):** rede conhecida **2,2%** / `independente` **97,8%** (confirma ~3%/~97% esperado).
- **Redes na tabela de capacidade:** 13 (após colapso N<3); **10 confiáveis (N≥10)**.
- **Oferta (a):** 7.250 linhas `(hex_id, rede_menor)`, 14 categorias distintas (13 redes + `independente`).
- **Dedup fino vs 62,7% grosseiro:** dedup GROSSEIRO por hex (TP-08) = **62,7%** dos alunos em hex com concorrente; dedup FINO por `(hex, rede)` = **273 pares** hex×rede duplicados = **8,3%** dos alunos realmente duplicados. O fino corrige a super-dedução: a maior parte da oferta menor num hex de concorrente é rede DISTINTA (adição legítima à saturação).

## Prova anti-PII dos 2 parquets REAIS (só nomes de coluna)
- `oferta_academias_menores_rede_h3.parquet` → `['alunos_academias_menores', 'hex_id', 'n_academias_menores', 'rede_menor', 'versao_contrato']` — sem PII; `rede_menor` dtype `string` (categoria).
- `capacidade_media_por_rede.parquet` → `['flag_confiavel', 'media_alunos', 'mediana_alunos', 'n_filiais', 'rede_menor', 'versao_contrato']` — sem PII.
- Nenhuma das colunas `nome_academia`/`latitude`/`longitude`/`cluster_id`/`total_alunos_cluster` presente em qualquer dos 2 artefatos.

## Problemas encontrados
- Uma asserção inicial contraditória em `test_colapso_baixa_cardinalidade` (esperava `smart_fit` presente, mas smart_fit tem 2 filiais na fixture → colapsa com limiar 3). Corrigida para refletir o gate B (N<3 colapsa; panobianco N=3 sobrevive). Sem outros problemas.

## Pendências
- Nenhuma para este bloco. Follow-up (fora de escopo): BLK-TP-06-FU1 fica DESBLOQUEADO/retomável (dado fechado) — a subtração de oferta / integração ao residual é BLK-TP-06-FU1 / BLK-TP-09 sob gate próprio.

## Riscos remanescentes
- Cobertura ~2,2% (esperado): a capacidade por rede só é útil para as ~10 redes N≥10; oferta menor é majoritariamente `independente` (adição legítima à saturação). Documentado no relatório, não é falha.

## Guardrails verificados
- **score_priorizacao não alterado:** sim (READ-ONLY; nenhum toque em score/pesos/fórmula).
- **Artefatos M1 preservados:** sim — mtime dos 4 oficiais INALTERADO antes/depois de gerar os artefatos:
  - `data/outputs/hexagonos_brasil_dashboard.parquet` UNCHANGED
  - `data/outputs/top_oportunidades_resumo.csv` UNCHANGED
  - `data/outputs/resumo_por_uf.csv` UNCHANGED
  - `data/staging/brasil_priorizados.parquet` UNCHANGED
  - `git status --porcelain` sem nenhum artefato M1/mercado modificado; os 2 parquets novos + o relatório são gitignored (não aparecem).
- **Dashboard offline mantido:** sim (nenhuma dependência de rede; import `streamlit_app` ok).
- **DEC-012 anti-PII por construção:** sim — nome/coords/cluster dropados na fronteira; `rede_menor` é categoria; rede N<3 → `independente`; fonte real (`NAO_ABRA/`) não versionada; fixtures sintéticas; prova anti-PII acima nos 2 parquets reais.
- **Isolamento:** sim — pacote `demanda_revelada/` não importa de `pipelines/m1`/`dashboard`/`censo_*`/`api` (test ast verde).
- **Sem dependência nova:** sim (`re`/`unicodedata` stdlib; pandas/h3/openpyxl/pyarrow da base).
