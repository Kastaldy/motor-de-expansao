# Handoff — BLK-DIM-14 QA → Orquestrador

## Bloco
- **ID:** BLK-DIM-14 — Engine de risco (break-even + P(viável) + classe GO/ATENÇÃO/NÃO) + ranking DORMENTE
- **Criticidade:** Alta (reorienta produto para classe de risco; READ-ONLY M1)
- **Branch:** `ciclo/loop-20260617-114955`
- **Autonomia:** loop-safe (engine determinístico + testes; READ-ONLY M1; sem VPS/deploy/segredos/PII; ranking INATIVO).

## VEREDITO QA: APROVADO

### Evidências
- **Suite COMPLETA:** `982 passed, 4 skipped, 18 warnings in 550.10s` (`pytest -q`). Suite full, não subconjunto.
- **ruff check . :** `All checks passed!`
- **mypy src/motor_expansao/dimensionamento/risco.py :** `Success: no issues found in 1 source file`
- **RANKING_ATIVO=False:** confirmado (`risco.py` L34; T15 `test_t15_ranking_ativo_false` verde).
- **Guardrail anti-geográfico:** confirmado (T9 verifica via `inspect.signature` que `p_viavel` não tem `lat`/`lng`/`latitude`/`longitude`).
- **Ranking dormente não chamado por render:** confirmado (T16 varre `pages.py`/`components.py`/`streamlit_app.py` — ausência de `ranking_oportunidades`).
- **Paridade de tolerâncias com viabilidade_ponto:** confirmado (T17).

### Arquivos do ciclo (apenas 2 novos + handoffs)
- `src/motor_expansao/dimensionamento/risco.py` (NOVO) — `p_viavel`, `classe_risco`, `ranking_oportunidades`, constantes/flags.
- `tests/unit/dimensionamento/test_risco.py` (NOVO) — 26 testes T1-T17 (com sub-letras b/c/d), todos verdes.

### Verificação de não-toque em M1/config/score/artefatos
- Os dois únicos arquivos com conteúdo novo do ciclo são os listados acima (`git status`: `?? risco.py`, `?? test_risco.py`, `?? handoff planner`).
- O `git status` da branch lista centenas de arquivos como modificados (incl. `config.py`, `pipelines/m1/*`, `simulador.py`), MAS isso é **ruído de encoding/EOL pré-existente na branch do loop**, NÃO alteração deste ciclo:
  - `git diff --ignore-all-space src/.../config.py pipelines/m1/base_h3_brasil.py dimensionamento/simulador.py` → **0 linhas** (diff de conteúdo vazio).
  - Inspeção do diff bruto mostra que a versão COMMITADA na branch está com mojibake (UTF-8 duplo-codificado, ex.: `ç` → `M-CM-'`) e o working tree apresenta o UTF-8 correto. É estado pré-existente da branch, não escrita do Builder.
- Pesos `renda=0.40`/`pop=0.60`, fórmula `score_priorizacao`/`hex_score_estrutural`, carteira, plano e artefatos oficiais: **INALTERADOS**. DEC-001/DEC-008/DEC-009 intactas.

### Housekeeping
- `python scripts/housekeeping_move_block.py BLK-DIM-14 --check` → `FALHA --check: stub ausente no backlog` (exit 1). PENDENTE — esperado; o orquestrador executa o housekeeping após este veredito.

## Próximo bloco
- **BLK-DIM-16** (próximo na epic BLK-DIM, conforme orientação do ciclo).

## Resumo
Engine de risco property-first entregue como 3 funções puras determinísticas (DORMENTE: ranking sem render, `RANKING_ATIVO=False`). Suite full verde (982 passed/4 skipped), ruff e mypy limpos, guardrail anti-geográfico e não-chamada-pelo-render comprovados por teste. Zero escrita real em M1/config/score/artefatos (a churn de `git status` é encoding pré-existente da branch, diff de conteúdo vazio). APROVADO.
