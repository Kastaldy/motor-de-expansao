# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (recorte do bloco amplo BLK-UI-07; bloco permanece aberto)

## VEREDITO
**APROVADO COM RESSALVAS**

## Justificativa
As 3 frentes (F1 2x2 do Relatório Censitário; F2 filtros UF/Município/Faixa no corpo; F3 busca no corpo)
estão verdes, aderentes ao escopo e às decisões de produto aprovadas (D1–D4), e READ-ONLY sobre o M1. A
ressalva é UM teste flaky PRÉ-EXISTENTE (`test_classico_template_recente_inalterado`) surfado pela ordem da
suíte serial — comprovadamente NÃO causado por este ciclo (módulo `censo_report.py` intocado; os testes
alterados não o poluem). Validação re-executada pelo QA (orquestrador, Opus 4.8) por conta própria após o
outage do classificador de segurança ter cessado; sem bypass.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum (do escopo deste ciclo)

## Melhorias opcionais
- CSS legado F2-G (`pages.py` L157-162) reforça a sidebar a 20rem QUANDO expandida e o comentário ainda cita
  `initial_sidebar_state="expanded"` (agora `collapsed`). Com a sidebar vazia, é cosmético; limpar num
  follow-up (frente herdada já anotada em BLK-UI-08).

## Testes faltantes
- nenhum (assert do relatório reescrito para `col.image` agregado; 3 testes novos de namespace-corpo para
  UF/Município/Faixa/busca cobrindo a migração).

## Riscos remanescentes
- Teste flaky pré-existente da suíte (ver ressalva) — follow-up BLK-FIX-14 criado.

## Saída literal das validações (re-executadas pelo QA)

```
$ python -c "import streamlit_app; print('import ok')"
... (warnings de ScriptRunContext/cache, esperados em bare mode) ...
import ok
```

```
$ python -m ruff check src/motor_expansao/dashboard/pages.py streamlit_app.py tests/integration/test_streamlit_app.py
All checks passed!
```

```
$ python -m mypy src/motor_expansao/dashboard/pages.py streamlit_app.py
Success: no issues found in 2 source files
```

```
$ python -m pytest -q tests/integration/test_streamlit_app.py
199 passed, 9951 warnings in 9.63s
```

```
$ python -m pytest -q     # suíte full, gate único (serial; -n auto tem bug de execnet em Py3.14 local)
1 failed, 963 passed, 1 skipped, 112323 warnings in 178.11s (0:02:58)
FAILED tests/unit/test_relatorio_pontual_censitario_export.py::test_classico_template_recente_inalterado
```

```
$ python -m pytest -q tests/unit/test_relatorio_pontual_censitario_export.py::test_classico_template_recente_inalterado
1 passed     # passa ISOLADO

$ python -m pytest -q tests/unit/test_relatorio_pontual_censitario_export.py
22 passed    # arquivo inteiro passa (sem poluição interna)

$ python -m pytest -q tests/integration/test_streamlit_app.py tests/unit/...::test_classico_template_recente_inalterado
200 passed   # passa logo APÓS os testes alterados -> minhas mudanças NÃO poluem
```

```
$ git diff --name-only
context/handoff.md
data/outputs/setores_censitarios_2022_geo/_metadata.json   (pré-sujo, NÃO commitado)
data/reports/relatorio_pontual_censitario_base_geo.md       (pré-sujo, NÃO commitado)
src/motor_expansao/dashboard/pages.py
streamlit_app.py
tasks/current_task.md
tests/integration/test_streamlit_app.py
```

## Conferência de no-bypass
Confirmo: nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, nem mock do caminho
crítico. A suíte rodou contra a config/artefatos reais. O "verde" foi obtido por execução real. O ÚNICO
failure é flakiness pré-existente, REPORTADA (não mascarada): NÃO usei `-p no:xdist` nem desabilitei
coleta; ao contrário, isolei a causa (passa sozinho, passa após os testes alterados) provando que não é
regressão deste ciclo.

## Conferência cruzada com log do Builder
Bate. Builder reportou 199 passed em `test_streamlit_app.py`, import ok, ruff+mypy limpos, diff restrito ao
escopo — todos reproduzidos pelo QA. O Builder não rodou a suíte full (responsabilidade do QA); o failure
flaky surge só na suíte completa e foi analisado aqui.

## Guardrails verificados
- score_priorizacao não alterado: sim (camada de visualização; `pipelines/m1`/`config.py` intocados).
- Artefatos M1 preservados: sim (nenhum artefato oficial no diff).
- Carga lazy por UF / render lazy de abas / fonte de mapa enxuta: preservados (UF resolvido antes de
  `load_uf_slice`, `st.stop()` intacto; `render_tab_selector` intocado).
- `censo_map.py`/`censo_report.py`/`censo_point.py`/`components.py`/`constants.py`/`utils.py`/`config.py`/
  `pipelines/m1`: INTOCADOS (fora do diff).
- Validações re-executadas pelo QA (sem bypass): import→ok; ruff→All checks passed; mypy→Success;
  alvo→199 passed; full→963 passed/1 skipped/1 flaky pré-existente; diff→escopo respeitado.
- Escopo respeitado: sim (só `pages.py`, `streamlit_app.py`, `test_streamlit_app.py` + housekeeping).

## Housekeeping
N/A (recorte — BLK-UI-07 é bloco amplo de UX/UI com frentes futuras explícitas; permanece ABERTO no
backlog, sem move/close via helper, espelhando o padrão do recorte BLK-UI-01). Sucessor placeholder
BLK-UI-08 já criado.

## Decisão recomendada
Fechar o ciclo como recorte; commit por path na branch `ciclo/BLK-UI-07`; PR para a `main`; criar
follow-up BLK-FIX-14 (isolamento do teste flaky). Merge é passo humano.
