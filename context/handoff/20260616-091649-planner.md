# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Approver (gate humano) — tarefa Alta; decisões de produto F1-A / F2-A / F2-C precisam de aprovação antes do Builder.

## Entendimento da tarefa
2º recorte do BLK-UI-01: reduzir densidade visual (colunas/KPIs/captions) e melhorar navegação/fluxo das 5 abas do dashboard, READ-ONLY sobre o M1, offline, sem tocar score/artefatos/builders de mapa/lazy-load.

---

## Plano técnico (por candidato, com valores concretos para o gate)

### F1-A — Reduzir colunas-padrão nas tabelas (Carteira, Plano, Domínio) — ALTA, decisão de produto
Princípio: NENHUMA coluna sai do DataFrame/parquet; só reduz o set EXIBIDO por padrão. As colunas secundárias passam para um `st.expander("Mostrar colunas detalhadas", expanded=False)` que renderiza um SEGUNDO `st.dataframe` com o set completo (mesma `view`, mesmos formatadores). Implementação: extrair, em cada função, a renderização da tabela em "set primário" + "set secundário", onde secundário = (primário ∪ extras). Reusar os blocos de formatação já existentes aplicando-os ao frame completo e depois fatiando as colunas exibidas.

1. **`render_carteira_expansao`** (`pages.py` ~1388–1445, dict `display_cols_order` em 1399–1426).
   - PROPOSTA de set PRIMÁRIO (12 cols, ordem preservada):
     `rank_brasil` (Rank Brasil), `rank_uf` (Rank UF), `score_priorizacao` (Score M1), `prioridade_abertura` (Prioridade), `uf` (UF), `nome_municipio` (Municipio), `hex_id` (Hex ID), `modo_selecao_carteira` (Modo Hex), `score_setor_2022_calibrado` (Score Censo), `sam_fitness_potencial` (SAM Fitness), `oferta_efetiva_disponivel` (Oferta Residual), `motivo_priorizacao` (Motivo).
   - SECUNDÁRIAS (movidas p/ expander, 13 cols): `rank_hex_intraurbano` (Rank Intraurbano), `score_expansao_hibrido` (Score Hibrido (apoio)), `score_oportunidade_residual` (Score Residual), `quartil_oportunidade_residual` (Quartil Residual), `share_ultra_estimado_hex` (Share Ultra), `oferta_consumida_mercado_estimada` (Consumo Conc. (est.)), `oferta_consumida_ultra_real` (Consumo Ultra (real)), `rank_municipio_uf` (Rank Mun. UF), `rank_municipio_brasil` (Rank Mun. Brasil), `qualidade_join_uf` (Join), `coverage_pct_setor_2022` (Coverage %), `flag_outlier_espacial` (Outlier), `flag_baixa_pop_setor` (Dens. < 5k), `flag_join_uf_restrito` (Join Restrito).
   - Mudança: construir `tbl` completo (formatação inalterada), renderizar `st.dataframe(tbl[primárias].head(TABLE_ROW_LIMIT), ...)`; abaixo `with st.expander("Mostrar colunas detalhadas (residual, ranks, censo)", expanded=False): st.dataframe(tbl.head(TABLE_ROW_LIMIT), ...)`.
2. **`render_plano_expansao`** (`pages.py` ~1762–1807, dict `tbl_cols` 1765–1789).
   - PRIMÁRIO (11 cols): `rank_carteira_brasil` (Rank Brasil), `rank_carteira_uf` (Rank UF), `nivel_prioridade_final` (Nivel), `uf` (UF), `nome_municipio` (Municipio), `hex_id` (Hex ID), `score_expansao_hibrido` (Score Hibrido), `score_priorizacao` (Score M1), `score_setor_2022_calibrado` (Score Censo), `sam_fitness_potencial` (SAM Fitness), `oferta_efetiva_disponivel` (Oferta Residual).
   - SECUNDÁRIAS (expander): `modo_selecao_carteira` (Modo Hex), `score_oportunidade_residual` (Score Residual), `quartil_oportunidade_residual` (Quartil Residual), `share_ultra_estimado_hex` (Share Ultra), `oferta_consumida_mercado_estimada` (Consumo Conc. (est.)), `oferta_consumida_ultra_real` (Consumo Ultra (real)), `qualidade_join_uf` (Join), `coverage_pct_setor_2022` (Coverage %), `motivo_priorizacao` (Motivo), `flag_outlier_espacial` (Outlier), `flag_baixa_pop_setor` (Dens. < 5k), `status_pipeline` (Status).
   - Mesmo padrão de expander.
3. **`render_expansao_dominio`** (`pages.py` ~1566–1612, dict `display_cols` 1572–1589).
   - PRIMÁRIO (11 cols): `rank_dominio_brasil` (Rank Brasil), `rank_dominio_uf` (Rank UF), `uf` (UF), `nome_municipio` (Cidade), `hex_id` (Hex Ancora), `ordem_expansao_cidade` (Ordem na Cidade), `score_oportunidade_residual` (Score Residual), `residual_incremental_capturado` (Residual Capturado), `oferta_efetiva_disponivel` (Oferta Disponivel), `tese_dominio` (Tese), `dist_ultra_mais_proxima_m` (Dist. Ultra (m)).
   - SECUNDÁRIAS (expander): `cluster_id` (Cluster), `oferta_consumida_mercado_estimada` (Consumo Conc. (est.)), `oferta_consumida_ultra_real` (Consumo Ultra (real)), `n_concorrentes_mapeados_2km` (Concorrentes 2km), `rank_dominio_cidade` (Rank Cidade).
   - **ATENÇÃO TESTE**: `test_render_expansao_dominio_exibe_consumo_quando_colunas_presentes` (linha 4128–4133) lê `rendered_dfs[0]` (o PRIMEIRO `st.dataframe`) e exige "Consumo Conc. (est.)"/"Consumo Ultra (real)" nesse frame. Como o set primário NÃO contém Consumo, o 1º dataframe muda. → ATUALIZAR o teste para ler o frame do expander (segundo `st.dataframe`, i.e. `rendered_dfs[1]`) OU mover esse teste para checar o frame completo. Recomendação: como `rendered_dfs` agora terá 2+ frames (mapa não usa dataframe), o frame COMPLETO é o último; trocar `rendered_dfs[0]` por `rendered_dfs[-1]`.

### F1-B — Colapsar KPIs da Análise Pontual — MÉDIA
4. **`_render_analise_pontual_multihex`** (`pages.py` 1949–1965): hoje 4+5+3 = 12 KPIs.
   - PROPOSTA: 2 linhas, ≤9 KPIs. Linha 1 (4): Hexes selecionados, Habitantes, Renda per capita med., Residual fitness. Linha 2 (5): Score M1 med., Score Residual med., Score Dominio Hibrido med., Presenca Ultra, Concorrentes (hexes). Os 3 de consumo (Consumo concorrentes / Consumo Ultra / Consumo total instalado) viram 1 `st.caption` formatado abaixo (ex.: `Consumo instalado no cenario — concorrentes: X | Ultra: Y | total: Z alunos.`). Reusa `_fi`/`_fs`.
5. **`render_analise_pontual` (simples)** (`pages.py` 2114–2148): hoje 4+4+3 = 11 KPIs.
   - PROPOSTA: manter as 2 primeiras linhas de 4 (8 KPIs primários: Hexes no raio, Populacao no raio, Renda per capita med., Residual total, Score residual med., Score M1 med., Concorrentes no raio, Ultra no raio). Converter o bloco de consumo (2143–2149) de 3 `st.metric` para 1 `st.caption` consolidado, removendo as colunas `ka/kb/kc`. A caption de proveniência (2149) é absorvida nessa caption única.
   - **TESTE**: `test_render_analise_pontual_*_metric` (3203–3239) só exige que algum label contenha "hex" e algum "residual/score" — preservado pelo set proposto. Safe.

### F1-C — Disclaimer de centroide em 1 lugar só — BAIXA
6. Extrair constante de módulo em `pages.py` (perto de `DASHBOARD_TAB_LABELS`, ~444):
   `_CENTROID_DISCLAIMER = ("Leitura por centroide H3 res-7: precisao aproximada ~0.5-1 km. Nao altera score_priorizacao, carteira, plano ou artefatos do M1.")`
   - Substituir as 2 ocorrências literais: `pages.py` 1938–1941 (multihex) e 2088–2091 (simples) por `st.caption(_CENTROID_DISCLAIMER)`.
   - Nota: a string atual difere ligeiramente entre os 2 locais ("centroide H3 res-7" vs "centroide de hexagono H3 res-7"). Padronizar para a versão sem "de hexagono". Critério de aceite §3 (`grep "centroide H3 res-7" pages.py | wc -l == 1` após contar só a definição da constante).

### F1-D — Formatação de números — SEM AÇÃO (evidência)
7. **CONCLUSÃO: sem inconsistência real em `format_int`.** Verificado por grep:
   - `format_int` (`utils.py:8-9`) é o ÚNICO helper de inteiro e SEMPRE usa ponto como separador de milhar (`f"{int(round(...)):,}".replace(",", ".")`). Todos os ~40 chamadores em `pages.py` usam-no de forma uniforme para renda/residual/população.
   - As 2 ocorrências de `f"{int(v):,}".replace(",", ".")` inline (`pages.py:1606`, `1606` Dist. Ultra) produzem EXATAMENTE o mesmo resultado de `format_int` — duplicação cosmética, não inconsistência de saída.
   - Scores: tabelas/KPIs usam `f"{v:.1f}"` (1 casa, padrão consistente em todos os contextos de score). O helper `format_score` usa `:.2f`, mas é aplicado só a campos de tooltip/result do ponto, não às tabelas — não há mistura num mesmo contexto.
   - **Recomendação: NÃO alterar `utils.py`.** Custo/benefício negativo; mexer arrisca regressão sem ganho. Marcar F1-D como concluído sem ação.

### F2-A — Encurtar labels das 5 abas — ALTA, decisão de produto + acoplamento crítico
8. **`DASHBOARD_TAB_LABELS`** (`pages.py:444–450`). PROPOSTA (≤18 chars cada):
   `["Visão Executiva", "Mapa", "Domínio", "Carteira", "Viabilidade"]`
   (alternativa conservadora se o gate quiser manter contexto: `["Executivo", "Mapa Territorial", "Domínio", "Carteira", "Viabilidade"]` — todas ≤18).
   - **ACOPLAMENTO CRÍTICO**: `streamlit_app.py:531,535,549,565,573,581` faz dispatch por comparação literal de `active_tab` contra os labels antigos. Mudar `DASHBOARD_TAB_LABELS` SEM atualizar esses `if/elif` faz a aba não renderizar (silencioso). O Builder DEVE atualizar `main()` em lockstep (idealmente referenciando `DASHBOARD_TAB_LABELS[i]` por índice, ou as novas strings).
   - **TESTES QUE QUEBRAM** (todos em `tests/integration/test_streamlit_app.py`):
     - `test_render_tab_selector_e_exportado` (4226–4232): assert da lista literal completa → atualizar para os 5 novos labels.
     - `test_render_tab_selector_retorna_aba_ativa` (4240, 4245): `return_value="Mapa Territorial"` e `assert result == "Mapa Territorial"` → trocar pelo novo label "Mapa".
     - `test_render_tab_selector_fallback_quando_desmarcado` (4258, 4263, 4266): `assert result == "Visao Executiva"` e `session_state {"...": "Carteira e Plano"}` + `assert == "Carteira e Plano"` → novos labels.
     - `test_main_renderiza_apenas_a_aba_ativa` (4291, 4308): `render_tab_selector` mockado `return_value="Expansao de Dominio"` e o dispatch precisa rotear p/ dominio → trocar para o novo label "Domínio" (e o `main()` reescrito reconhecê-lo).
     - `test_blk_dim_12_*` (4869–4870): `assert "Viabilidade do Imovel" in DASHBOARD_TAB_LABELS` e `len(...) == 5` → o `len == 5` permanece; o `in "Viabilidade do Imovel"` quebra se renomear a 5ª aba → atualizar p/ "Viabilidade".
   - Acentos: usar `"Visão"`/`"Domínio"` (UTF-8) é seguro (arquivo já tem acentos em strings). Manter sem acento se o gate preferir consistência com o resto do código.

### F2-B — Estado vazio sem comando shell — MÉDIA
9. Trocar as 3 mensagens de `st.warning` que expõem `python -m jobs...`:
   - `render_carteira_expansao` (1222–1225): → `"Carteira acionavel indisponivel neste recorte. Ela e gerada no ciclo de regeneracao dos parquets — contate o time de dados se esta tela estiver vazia em producao."`
   - `render_plano_expansao` (1647–1650): → `"Plano de curto prazo indisponivel neste recorte. Ele e gerado no ciclo de regeneracao dos parquets — contate o time de dados se esta tela estiver vazia em producao."`
   - `render_expansao_dominio` (1489–1492): → `"Plano de Expansao de Dominio indisponivel neste recorte. Ele e gerado no ciclo de regeneracao dos parquets — contate o time de dados se esta tela estiver vazia em producao."`
   - **TESTE**: `test_render_expansao_dominio_exibe_warning_sem_dados` (1903) exige `"Expansao de Dominio" in msg or "plano" in msg.lower()` → o texto proposto contém "Expansao de Dominio" e "plano", PASSA sem alterar o teste.

### F2-C — Filtros avançados em expander colapsado — MÉDIA, decisão de produto
10. **`render_sidebar_filters`** (`pages.py:480–543`). Mover os 5 controles avançados (`Elegibilidade hibrida` 507, `Cobertura censitaria` 512, `Qualidade da camada` 517, `Apenas top_municipio` 522, `Apenas top_hex_intraurbano` 526) para `with st.sidebar.expander("Filtros avancados", expanded=False):`. Os filtros UF/Municipio/Faixa permanecem na primeira dobra.
    - **INVARIANTE**: `render_uf_selectbox` continua sendo o PRIMEIRO elemento renderizado na sidebar — ele é chamado em `main()` ANTES de `render_sidebar_filters` (streamlit_app.py:457 vs 490), então não é afetado; só garantir que o expander não vá antes do bloco UF/Municipio/Faixa dentro de `render_sidebar_filters`.
    - Mover o `st.sidebar.markdown("---")` + `### Camada Hibrida` (504–506) para dentro do expander (ou substituir por título do expander). O `return` (534–543) e a assinatura permanecem IDÊNTICOS (mesma ordem dos 8 valores) — `main()` não muda.
    - **TESTE**: nenhum teste mocka o conteúdo interno de `render_sidebar_filters` (só mockam a função inteira em `test_main_*`). Safe.

### F2-D — Separadores no Mapa Territorial — BAIXA
11. **`render_mapa_territorial`** (`pages.py:3214–3219`). Já existe `st.markdown("---")` antes do bloco de expanders (3215). Adicionar headings curtos consistentes com `render_visao_executiva` (que usa `st.markdown("#### ...")` antes de cada subseção): inserir `st.markdown("#### Detalhamento territorial")` logo após o `---` da linha 3215, antes do `with st.expander("Analise Territorial"...)`. Baixo custo, sem teste afetado. RECOMENDADO incluir (1 linha).

### F2-E — Hero header contextual com UF — BAIXA, avaliar custo
12. **AVALIAÇÃO: incluir, custo baixo.** Tornar `render_header(selected_uf: str | None = None)` e injetar no subtítulo quando presente (ex.: append `· UF: {selected_uf}` na `<p>`). Chamador `streamlit_app.py:447` está ANTES de `selected_uf = render_uf_selectbox(...)` (457) → para passar a UF, mover a chamada `render_header(selected_uf)` para DEPOIS da linha 461 (após o `st.stop()` que garante UF selecionada), OU manter `render_header()` no topo e re-renderizar. **Recomendação: OPCIONAL/futuro** — reordenar `render_header` quebra a sequência atual (header no topo absoluto) e o `test_main_*` mocka `render_header` sem args (linha 4286, `mock.patch.object(... "render_header")` aceita qualquer assinatura, então não quebra). Se o gate quiser, é seguro com default `None`. Marcado como opcional; sugiro DEIXAR PARA RECORTE FUTURO para manter o ciclo enxuto.

---

## Fatiamento recomendado
- **NÚCLEO deste ciclo (entregar):** F1-A (3 tabelas), F1-C (constante), F2-A (labels + dispatch + 5 testes), F2-B (3 mensagens), F2-C (expander sidebar), F1-B (colapso KPIs), F2-D (1 heading).
- **SEM AÇÃO (fechar com evidência):** F1-D (formatação já consistente).
- **FUTURO (recorte posterior):** F2-E (hero contextual — reordena `render_header`; custo/benefício baixo agora).

## Arquivos afetados
- `src/motor_expansao/dashboard/pages.py` — F1-A, F1-B, F1-C, F2-A (constante), F2-B, F2-C, F2-D.
- `streamlit_app.py` — F2-A (dispatch `main()` em lockstep com os novos labels).
- `tests/integration/test_streamlit_app.py` — F2-A (5 testes), F1-A Domínio (1 teste).
- `src/motor_expansao/dashboard/constants.py` — NÃO precisa mudar (`DASHBOARD_TAB_LABELS` vive em `pages.py`, reexportado por `streamlit_app.py`). Confirmar; não tocar se desnecessário.
- `src/motor_expansao/dashboard/utils.py` — NÃO tocar (F1-D sem ação).

## Dependências
- F2-A exige edição ATÔMICA de `DASHBOARD_TAB_LABELS` + dispatch em `main()` + 5 testes; não fazer parcial.
- F1-A Domínio exige atualizar `rendered_dfs[0]`→`rendered_dfs[-1]` no teste de consumo.

## Riscos técnicos
- **F2-A silencioso**: labels sem atualizar `main()` = aba em branco e nenhum teste de render de aba pega isso exceto `test_main_renderiza_apenas_a_aba_ativa`. Mitigar: Builder atualiza dispatch e roda esse teste.
- **F1-A expander**: o segundo `st.dataframe` aumenta payload de render; é o frame completo já calculado, sem novo cálculo de score (READ-ONLY preservado). Sem impacto em Bloco 6 (não toca builders de mapa).
- **Acentos UTF-8** nos labels: garantir encoding do arquivo preservado.

## Critérios de aceite finais
1. Carteira e Plano: tabela primária ≤12 colunas por padrão; secundárias acessíveis via expander; ZERO coluna removida do DataFrame.
2. `_render_analise_pontual_multihex` ≤9 KPIs em ≤2 linhas; `render_analise_pontual` simples ≤8 KPIs + caption de consumo.
3. `grep "centroide H3 res-7" pages.py` retorna apenas a definição da constante `_CENTROID_DISCLAIMER` (1 linha lógica).
4. `DASHBOARD_TAB_LABELS` com 5 entradas ≤18 chars; `main()` roteia cada aba corretamente; 5 testes de label atualizados e verdes.
5. Estados vazios das 3 abas sem `python -m jobs`/`jobs/pipelines` visível; mensagem de produto em 1 frase.
6. 5 controles avançados em `st.sidebar.expander(..., expanded=False)`; `render_uf_selectbox` continua 1º elemento da sidebar.
7. `pytest -q` com ≤3 falhas pré-existentes documentadas (csv_path1-223, csv_path2-472, test_parquet_final_respeita_guardrails_do_piloto).
8. `ruff check` limpo; `mypy` sem novos erros; diff não toca `config.py`, `pipelines/m1/`, scores, artefatos, deploy, `censo_*`, builders de mapa.

## Validações obrigatórias
- Comando: `python -m pytest -q tests/integration/test_streamlit_app.py` → Critério: 0 falhas novas (≤3 pré-existentes só se rodar suíte full).
- Comando: `python -m pytest -q` → Critério: ≤3 falhas, todas na dívida operacional herdada.
- Comando: `ruff check src/motor_expansao/dashboard/pages.py streamlit_app.py tests/integration/test_streamlit_app.py` → 0 erros.
- Comando: `mypy src/motor_expansao/dashboard/pages.py streamlit_app.py` → 0 erros novos.
- Comando: `python -c "import streamlit_app"` → import sem exceção.

## Fora de escopo
- `score_priorizacao`, `hex_score_estrutural`, pesos M1, artefatos oficiais — ZERO toque.
- Builders de mapa / `components.py` (Bloco 6), lazy-load (Bloco 4), render lazy de abas (Bloco 5) — INTOCADOS.
- `censo_map.py`, `censo_report.py`, `censo_point.py`, `config.py`, `pipelines/m1`.
- F2-E (recorte futuro). Redesenho de grid/proporções, ag-grid, animações, novas colunas derivadas.
- Paths pré-sujos NÃO commitar.

## Alerta de aprovação
ATENÇÃO: esta tarefa é de criticidade ALTA (navegação e apresentação do dashboard de PRODUÇÃO). Embora READ-ONLY sobre o M1, há 3 DECISÕES DE PRODUTO que o humano (Felipe/Vini) DEVE aprovar ANTES do Builder:
- **F1-A**: o set de colunas "primárias" propostas para Carteira (12), Plano (11) e Domínio (11) — Felipe é o usuário principal da carteira e precisa validar quais colunas ficam na primeira dobra.
- **F2-A**: os 5 novos labels das abas (`Visão Executiva / Mapa / Domínio / Carteira / Viabilidade`) — impactam dispatch, 6 testes e qualquer doc/asset que cite os nomes antigos.
- **F2-C**: mover os 5 filtros avançados para expander colapsado muda o comportamento padrão da sidebar para quem hoje os usa na primeira dobra.
O usuário deve ler este handoff e confirmar (ou ajustar os sets/labels) antes de liberar o Builder.

## Guardrails ativos
- §2: offline preservado; sem dependência de API ao vivo; CSV sep=";" encoding="utf-8-sig".
- §4 (Blocos 4/5/6): carga lazy por UF, render lazy de abas, fonte de mapa enxuta INTOCADOS. INVARIANTE: `render_uf_selectbox` é o PRIMEIRO elemento da sidebar.
- §5 guardrail permanente: visualizações não podem recalcular `score_priorizacao`, `hex_score_estrutural`, carteira, plano ou artefatos oficiais do M1.
- Loop guard: diff não pode tocar `config.py`, `pipelines/m1/`, `*scoring*`, artefatos M1, `deploy/`, `Dockerfile.*`, compose, Caddy, authelia, `.env`, `secrets/`.
- Paths pré-sujos NÃO commitar: `data/outputs/setores_censitarios_2022_geo/_metadata.json` e `data/reports/relatorio_pontual_censitario_base_geo.md`.
