# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (ciclo concluído; sucessor BLK-ATR-05 é manual/gate humano, fora do loop)

## VEREDITO
APROVADO

## Justificativa
Módulo `viz_atratividade.py` entrega os 6 gráficos + markdown-resumo pedidos, com backend headless
`Agg`, `plt.close(fig)` em todos os caminhos, guard anti-PII e isolamento de imports respeitado; suíte
FULL sem regressões novas (as 4 falhas são `ModuleNotFoundError: openlocationcode`, pré-existentes na
camada `api`, sem relação com este ciclo); M1 oficial intocado; housekeeping executado pelo helper versionado
com bloco byte-idêntico.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- O gráfico (a) cobertura_huff exibe subtítulo "base densa ATR-01 nao materializada" — reflete honestamente
  o estado atual (ATR-01 não materializou a base densa em produção), mas quando/se BLK-ATR-05 materializar,
  o subtítulo deverá ser revisto. Não bloqueante (documenta estado real).
- O critério de aceite do backlog cita "(b) re-validação do GO / RMSE por β" e "(e) comparação matriz vs
  composto": ambos são cobertos pelo gráfico (e) r2_modelos (R²_oof por modelo com IC95, incluindo composto/
  pesos_iguais/baseline) e pela tabela de modelos no markdown; RMSE por β não é plotado explicitamente, mas o
  eixo de decisão (R²_oof + IC95) está presente. Suficiente para a finalidade de apoio à decisão.

## Testes faltantes
- nenhum (10 testes cobrem geração de cada PNG, referência no markdown, ausência de PII, isolamento de
  imports e fechamento de figuras; fixtures 100% sintéticas, sem PII, sem leitura de arquivo real).

## Riscos remanescentes
- O orquestrador `executar()` (I/O de disco com dados reais) está sob `pragma: no cover` — não exercitado em
  teste, por design (operação cara). Risco baixo: é fina cola de leitura de parquet + chamada das funções puras
  já testadas.

## Saída literal das validações (re-executadas pelo QA)

### Suíte FULL (`python -m pytest -q -n auto`)
```
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_curto_usa_localidade_mock
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_curto_usa_localidade_mockada
4 failed, 1354 passed, 4 skipped, 18 warnings in 415.14s (0:06:55)
```
As 4 falhas são pré-existentes e não relacionadas: `ModuleNotFoundError: No module named 'openlocationcode'`
(dep ausente no ambiente, camada `motor_expansao.api.maps_geocoder`). HEAD só tocou `viz_atratividade.py` +
seu teste. Regressões novas = 0. Bate com o Builder (1354 passed, 4 failed).

### ruff
```
$ ruff check src/motor_expansao/demanda_revelada/viz_atratividade.py tests/unit/demanda_revelada/test_viz_atratividade.py
All checks passed!
```

### Imports proibidos
```
$ grep -n "from.*pipelines\|from.*dashboard\|from.*censo_\|from.*api\|^from.*config" src/motor_expansao/demanda_revelada/viz_atratividade.py
---exit:1---   (VAZIO)
```

### matplotlib no pyproject
```
$ grep -n "matplotlib" pyproject.toml
34:    "matplotlib>=3.7.0",
```

### loop_guard
```
$ python scripts/loop_guard.py --base ciclo/loop-20260706-152137
GUARD OK: 28 caminho(s) alterado(s), nenhum proibido.
```

### import streamlit_app
```
$ python -c "import streamlit_app; print('import ok')"
... (warnings de cache do streamlit, benignos)
import ok
```

### mtime dos 4 oficiais M1 (inalterado)
```
2026-06-03 15:47:01  data/staging/brasil_estrutural.parquet
2026-06-03 15:47:11  data/staging/brasil_priorizados.parquet
2026-06-03 15:47:35  data/staging/hexagonos_brasil_oportunidades.parquet
2026-06-12 13:46:03  data/outputs/hexagonos_brasil_dashboard.parquet
```

### Housekeeping (helper versionado)
```
$ python scripts/housekeeping_move_block.py BLK-ATR-04 --check      # ANTES: FALHA (stub ausente)
$ python scripts/housekeeping_move_block.py BLK-ATR-04 --date 2026-07-06
OK: movido BLK-ATR-04 (2360 bytes) -> tasks/completed.md; stub em tasks/backlog.md.
$ python scripts/housekeeping_move_block.py BLK-ATR-04 --check      # DEPOIS
OK: BLK-ATR-04 movido (stub no backlog + bloco em completed.md).
```
Byte-identidade do bloco vs `git show HEAD:tasks/backlog.md`: IDÊNTICO (2436 bytes = 2436 bytes, diff vazio).

### Teste do helper de housekeeping + teste do módulo
```
$ python -m pytest tests/unit/test_housekeeping_helper.py tests/unit/demanda_revelada/test_viz_atratividade.py -q
20 passed in 12.35s
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho
crítico. Os testes do módulo usam fixtures sintéticas por design (DEC-012, anti-PII — prática legítima, não
bypass de tooling). A suíte FULL rodou 1× com `-n auto` (gate único). Housekeeping executado contra os
arquivos reais `tasks/backlog.md`/`tasks/completed.md`, com `--check` real passando.

## Conferência cruzada com log do Builder
Bate. Builder reportou 1354 passed / 4 failed (openlocationcode pré-existente), ruff limpo, import ok,
GUARD OK, M1 intocado — todos reproduzidos por conta própria. Divergência única: o Builder NÃO executou o
housekeeping (Passo 6.0); o QA o executou no fechamento (helper versionado, byte-idêntico).

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; módulo só LÊ `score_priorizacao`/`score_oportunidade_residual`)
- Artefatos M1 preservados: sim (mtime dos 4 oficiais inalterado; loop_guard OK)
- Validações re-executadas pelo QA (sem bypass): pytest FULL → 1354 passed/4 failed(pré-existente); ruff → limpo; imports proibidos → vazio; matplotlib dep → presente; loop_guard → GUARD OK; import streamlit_app → ok; housekeeping --check → OK
- Escopo respeitado: sim (apenas `pyproject.toml` + módulo novo + teste novo + fechamento; sem Plotly/kaleido, só Matplotlib)

## Decisão recomendada
Fechar ciclo. Sucessor BLK-ATR-05 permanece manual (NÃO loop-safe: materializa em produção + gate humano/DEC).
