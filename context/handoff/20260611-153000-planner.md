# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
[REVISÃO HUMANA leve — D1/D2/D3] → Builder

## Entendimento da tarefa
Adicionar um `st.multiselect` de redes de concorrentes em `render_mapa_territorial` (`pages.py`) que filtre `competitors_df` em um ponto único antes de `_render_unified_legend` e `build_unified_map_figure`, tornando pins, clusters, legenda e tooltips coerentes com a seleção — puramente visual, READ-ONLY sobre o M1.

---

## Decisões pendentes do gate humano (leve) — RESPONDER ANTES DO BUILDER

> O Builder deve seguir as **recomendações abaixo (A para cada decisão)** salvo indicação explícita do humano em contrário.

### D1 — Estilo do controle de UI
- **A (recomendado):** `st.multiselect` separado de redes, exibido condicionalmente abaixo do multiselect de overlays quando "concorrentes" está ativo.
- B: checkboxes por rede com logo (mais código, fora de escopo atual).
- C: entradas de rede integradas ao multiselect de overlays existente (polui o controle).
- **Impacto se B/C:** o Builder precisará de um novo passo não coberto por este plano; a key do widget muda; o plano deve ser reaberto.

### D2 — Semântica de seleção vazia
- **A (recomendado):** seleção vazia → esconde todos os concorrentes (`competitors_df_filtered = None`); legenda de concorrentes desaparece; cap caption não conta concorrentes.
- B: seleção vazia → cai de volta para "todas as redes" (mais tolerante a clique acidental).
- **Impacto se B:** no passo 4 do plano, trocar `None` por `competitors_df` original quando `selected_redes` estiver vazio; o teste do cenário "vazia" verifica len igual ao original em vez de 0.

### D3 — Escopo da lista de redes exibida no controle
- **A (recomendado):** todas as redes presentes em `competitors_df` já carregado (estável para o recorte de UF/cidade corrente).
- B: só as redes do bbox atual do mapa (requer filtragem geográfica extra antes do controle; fora de escopo).
- **Impacto se B:** adicionar passo de filtragem geográfica de `competitors_df` antes de derivar `available_redes`; maior complexidade, não planejado aqui.

---

## Plano técnico

### Premissa: D1=A, D2=A, D3=A (recomendações do backlog; ajustar se humano escolher diferente)

### Passo 1 — Derivar lista de redes disponíveis (`pages.py`, logo após linha 2749)

Após o `st.multiselect` de overlays (linha 2743–2749), inserir bloco **fora** do `with ctrl_col2` (nova linha após o bloco `with ctrl_col2:`):

```python
# BLK-MAP-01: filtro individual de redes de concorrentes (puramente visual; READ-ONLY M1)
_show_rede_filter = (
    "concorrentes" in enabled_overlays
    and competitors_df is not None
    and not competitors_df.empty
    and "rede" in competitors_df.columns
)
selected_redes: list[str] = []
if _show_rede_filter:
    # Ordena pela posição em COMPETITOR_BRANDS; redes sem entrada ficam no final
    _all_redes_raw = competitors_df["rede"].dropna().unique().tolist()
    _brand_order = list(COMPETITOR_BRANDS.keys())
    _all_redes = sorted(
        _all_redes_raw,
        key=lambda r: (_brand_order.index(r) if r in _brand_order else len(_brand_order), r),
    )
    selected_redes = st.multiselect(
        "Redes de concorrentes",
        options=_all_redes,
        default=_all_redes,
        format_func=lambda r: COMPETITOR_BRANDS.get(r, {}).get("label", r),
        key="mapa_territorial_redes_concorrentes",
    )
```

- Importar `COMPETITOR_BRANDS` de `competitors.py` no topo de `pages.py` (verificar se já está importado; se não, adicionar ao bloco de imports de `competitors`).
- `_show_rede_filter = False` quando `competitors_df` é None/vazio ou overlay "concorrentes" está desligado → `selected_redes` permanece `[]` e o multiselect não aparece.

### Passo 2 — Aplicar filtro (ponto único, antes de `_render_unified_legend`)

Inserir **imediatamente antes** da linha 2765 (`_render_unified_legend(...)`):

```python
# BLK-MAP-01: ponto único de filtragem; D2=A => vazio => None (esconde tudo)
if _show_rede_filter and not selected_redes:
    competitors_df_filtered: pd.DataFrame | None = None
elif _show_rede_filter:
    competitors_df_filtered = competitors_df[competitors_df["rede"].isin(selected_redes)]
else:
    competitors_df_filtered = competitors_df
```

- `competitors_df_filtered` é a única variável passada às etapas seguintes (nunca mais `competitors_df` direto).
- Se `_show_rede_filter` é False (overlay desligado ou dados ausentes), `competitors_df_filtered = competitors_df` preserva o comportamento atual byte-a-byte (retrocompatibilidade).

### Passo 3 — Passar `competitors_df_filtered` para `_render_unified_legend` (linha 2765)

Alterar:
```python
# ANTES
_render_unified_legend(selected_mode, enabled_overlays, competitors_df=competitors_df, ultra_df=ultra_df)
# DEPOIS
_render_unified_legend(selected_mode, enabled_overlays, competitors_df=competitors_df_filtered, ultra_df=ultra_df)
```

`_render_unified_legend` já passa `competitors_df` para `render_competitor_legend(competitors_df)` em `components.py:1825`, que já trata DF vazio/None com `return` na linha 197. **Nenhuma mudança em `components.py` é necessária** para a legenda (o DF filtrado propaga automaticamente).

### Passo 4 — Passar `competitors_df_filtered` para `build_unified_map_figure` (linha 2774)

Alterar:
```python
# ANTES
    competitors_df=competitors_df,
# DEPOIS
    competitors_df=competitors_df_filtered,
```

O dispatcher em `components.py:3031` já trata `None` (`_comp = competitors_df if "concorrentes" in enabled_overlays else None`). Com `competitors_df_filtered=None` (D2=A, seleção vazia), `_comp` fica `None` e nenhuma camada de pins/clusters é gerada.

### Passo 5 — Propagar `competitors_df_filtered` para o bloco de caption (linhas 2804–2807)

O bloco `_comp_for_caption` (linha 2805) ainda usa `competitors_df`. Alterar:
```python
# ANTES (linha 2805)
_comp_for_caption = competitors_df if "concorrentes" in _enabled else None
# DEPOIS
_comp_for_caption = competitors_df_filtered if "concorrentes" in _enabled else None
```

Isso garante que o caption "amostrado" e o caption de cluster reflitam a seleção filtrada.

### Passo 6 — Verificação defensiva em `render_competitor_legend` (`components.py`) — APENAS se necessário

**Não é necessário alterar `components.py`**: `render_competitor_legend` (linha 196) já tem guard `if competitors_df is None or competitors_df.empty or "rede" not in competitors_df.columns: return`. DF vazio ou None → retorna silenciosamente. Nenhuma alteração nos builders `_build_competitor_icon_layer` / `_build_competitor_cluster_layer` (recebem o DF filtrado e já tratam `.empty`).

### Passo 7 — Testes (`tests/integration/test_streamlit_app.py`)

Adicionar três testes novos. Usar o padrão existente (ver `test_build_unified_map_figure_overlay_concorrentes_desligado`, linha 2137, e `test_build_map_figure_adiciona_pins_de_concorrentes_no_recorte`, linha 517) — sem render real de Streamlit, inspecionando o DF filtrado passado ao builder ou a camada resultante.

**Cenário A — Uma rede selecionada → só ela renderiza**
```python
def test_filtro_rede_uma_rede_so_ela_renderiza():
    # Montar competitors com smart_fit + bluefit
    # Filtrar manualmente: competitors_df[competitors_df["rede"].isin(["smart_fit"])]
    # Chamar build_unified_map_figure com competitors_df filtrado, enabled_overlays=["concorrentes"],
    #   selected_ufs=["SP"], selected_cities=["Sao Paulo"], cluster_competitors=False
    # Verificar: deck.layers[1] (IconLayer) só tem smart_fit no icon_mapping
    #   e rendered_competitors["rede"].unique() == ["smart_fit"]
```

**Cenário B — Seleção vazia → comportamento D2=A (esconde tudo)**
```python
def test_filtro_rede_vazio_esconde_concorrentes():
    # Chamar build_unified_map_figure com competitors_df=None (equivalente ao D2=A),
    #   enabled_overlays=["concorrentes"], selected_ufs=["SP"], selected_cities=["Sao Paulo"]
    # Verificar: len(deck.layers) == 1 (só hex layer; sem camada de pins)
```

**Cenário C — Todas as redes → retrocompatibilidade**
```python
def test_filtro_rede_todas_comportamento_atual():
    # competitors com smart_fit + bluefit, selected_cities=["Sao Paulo"] (cluster=False)
    # Chamar build_unified_map_figure com competitors_df completo (sem filtro)
    #   enabled_overlays=["concorrentes"]
    # Verificar: len(deck.layers) == 2; icon_mapping contém ambas as redes presentes no bbox
```

**Onde adicionar:** após o bloco `# ── BLK-FIX-11` existente (pós linha 2216), num bloco `# ── BLK-MAP-01: filtro individual de redes ──`.

**Como verificar sem render real:** passar o `competitors_df` já filtrado diretamente a `build_unified_map_figure` (simulando o que `pages.py` fará), inspecionar `deck.layers` e `pd.DataFrame(layer.data)["rede"].unique()`. Não requer mock de Streamlit.

---

## Arquivos afetados

- `src/motor_expansao/dashboard/pages.py` — passos 1–5 (único arquivo de produção alterado)
- `tests/integration/test_streamlit_app.py` — passo 7 (3 novos testes)
- `src/motor_expansao/dashboard/components.py` — **NÃO alterado** (guard existente é suficiente)

## Dependências

- `COMPETITOR_BRANDS` já existe em `competitors.py` (~linha 83); verificar import em `pages.py` antes de usar.
- `st.multiselect` com `key="mapa_territorial_redes_concorrentes"` — nova key; não conflita com `"mapa_territorial_overlays"` existente.
- Nenhuma dependência nova de biblioteca.

## Riscos técnicos

- **`COMPETITOR_BRANDS` não importado em `pages.py`:** verificar o bloco de imports no topo de `pages.py`; se ausente, adicionar `from motor_expansao.dashboard.competitors import COMPETITOR_BRANDS` ao import existente de `competitors`.
- **Multiselect visível quando "concorrentes" está desligado:** controlado pela flag `_show_rede_filter` (passo 1); sem "concorrentes" em `enabled_overlays`, o bloco não renderiza.
- **`competitors_df` None antes do bloco (dados não carregados):** coberto por `_show_rede_filter = False` → multiselect não aparece; `competitors_df_filtered = competitors_df = None` → comportamento atual.
- **Redes em `competitors_df` ausentes de `COMPETITOR_BRANDS`:** tratado com `key=lambda r: (..., r)` no sort (fallback ao final); `format_func` usa `.get(r, {}).get("label", r)` → exibe o valor bruto da coluna.
- **Rerun do Streamlit:** padrão esperado com qualquer `st.multiselect`; sem impacto de performance além do já existente (filtro `.isin()` sobre DF em memória; carga lazy por UF não é tocada).
- **Cap `COMPETITOR_PIN_LIMIT`:** incide sobre o subconjunto filtrado (comportamento correto); nenhum risco.

## Critérios de aceite finais

- Selecionar uma rede exibe APENAS pins/clusters/legenda/tooltips dessa rede (verificável por `deck.layers[1].data["rede"].unique()`).
- Seleção vazia → zero camadas de concorrentes (conforme D2=A; `len(deck.layers) == 1` no teste B).
- Todas as redes selecionadas → comportamento idêntico ao atual (retrocompatibilidade; teste C passa).
- Controle de redes aparece SOMENTE quando overlay "concorrentes" está ativo.
- `competitors_df` None/vazio → multiselect não aparece; fluxo segue sem erro.
- `ruff check` sem erros nos arquivos tocados.
- `mypy` sem erros nos arquivos tocados.
- `pytest -q tests/integration/test_streamlit_app.py` verde (0 falhas; novos testes A/B/C incluídos).
- Suite completa `pytest -q` verde (sem regressões).
- Nenhum score/pesos/artefato M1 alterado (READ-ONLY absoluto).

## Validações obrigatórias

- Comando: `python -c "import streamlit_app"` — Critério: sem erro de import.
- Comando: `python -m pytest -q tests/integration/test_streamlit_app.py` — Critério: 0 falhas, novos 3 testes passam.
- Comando: `python -m pytest -q` (suite completa) — Critério: baseline atual mantido (sem regressões; xdist opcional; fallback serial legítimo).
- Comando: `ruff check src/motor_expansao/dashboard/pages.py tests/integration/test_streamlit_app.py` — Critério: 0 erros.
- Comando: `mypy src/motor_expansao/dashboard/pages.py` — Critério: 0 erros novos.

## Fora de escopo

- Qualquer recálculo ou alteração de `score_priorizacao`, `hex_score_estrutural`, carteira, plano, residual, SAM, canibalização ou artefatos oficiais do M1.
- Overlay de Ultra (`ultra_df`), âncoras de domínio (`ancoras_dominio`), overlay `descartados_5k` — intocados.
- Quebrar otimizações de performance: carga lazy por UF, caps de pontos (`COMPETITOR_PIN_LIMIT`, `MAP_POINT_LIMIT*`), fonte de mapa enxuta.
- Alterar `build_unified_map_figure`, `_build_competitor_icon_layer`, `_build_competitor_cluster_layer`, `load_competitor_points` ou `COMPETITOR_BRANDS` (recebem o DF já filtrado).
- Alterar `render_competitor_legend` além de ajuste defensivo já desnecessário (guard existe).
- Filtro por bbox/geográfico das redes (D3=B).
- Checkboxes com logo (D1=B).
- Dependência de API ao vivo.

## Alerta de aprovação
Este bloco é classificado como **Média** (visualização pura; READ-ONLY sobre o M1). Não requer aprovação de Crítica/DEC. O gate humano leve é para confirmar D1/D2/D3 de UX antes do Builder.

## Guardrails ativos
- §5 CLAUDE.md (guardrail permanente): visualizações, análise radial e interações de mapa NÃO podem recalcular ou alterar `score_priorizacao`, `hex_score_estrutural`, carteira, plano curto prazo, plano domínio ou artefatos oficiais do M1 sem aprovação explícita.
- §2 CLAUDE.md: não criar dependência de API ao vivo no dashboard de produção.
- Performance: preservar carga lazy por UF, fonte de mapa enxuta e caps de pontos.
- BLK-FIX-11: overlays `ultra`, `ancoras_dominio`, `descartados_5k` intocados.
- Interpretação de criticidade (2026-05-30): este bloco é visualização pura → Média (sem DEC obrigatória).
