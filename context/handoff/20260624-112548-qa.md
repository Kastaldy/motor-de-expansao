# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (housekeeping via `scripts/housekeeping_move_block.py BLK-RELMUN-02 --date 2026-06-24` + `--check`) e merge humano. Documentation Skill opcional.

## VEREDITO
APROVADO COM RESSALVAS

## Justificativa
A Direção A2 foi implementada com escopo exato (parte comum + A2, sem A1/B/C), READ-ONLY sobre o M1, com a Página 6 listando bairros reais por zona e fallback gracioso sem `raise`/sem PII; suíte completa serial **1095 passed, 1 skipped**, ruff/mypy limpos, 9 páginas/`/Count 9` mantidos. A única ressalva é LEVE e NÃO bloqueante: `ruff format --check` reporta "would reformat" nos 4 arquivos tocados — verifiquei por conta própria que isso é **pré-existente** (idêntico nas versões `HEAD` antes do Builder) e o gate do projeto é `ruff check` (que passa).

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- Job operacional pós-merge (declarado pelo Builder, não bloqueia testes): re-materializar `data/outputs/setores_censitarios_2022_geo/` por UF para popular `nome_bairro` em produção; até lá a Página 6 cai no fallback gracioso. Validar RJ (~73%) e DF (0%), contagem de setores inalterada (468.099), acentos preservados.
- `ruff format` dos 4 arquivos poderia ser feito num bloco de housekeeping de estilo separado (fora de escopo deste bloco; pré-existente).

## Testes faltantes
- nenhum bloqueador. Cobertura adequada: bairros reais nos bytes do PDF agrupados por zona (`/Count 9`, sem PII), `bairros_por_zona` populado pela fonte + default None preservando comportamento, fallback do helper de leitura sem dir, fallback sem bairros sem PII, fluxo de coluna no pipeline geo. `test_coexistencia_relatorio_pontual_intocado` verde.

## Riscos remanescentes
- Cobertura de `NM_BAIRRO` HETEROGÊNEA por município (RJ 73%, AC 10%, DF 0%) — o fallback gracioso é o comportamento esperado, não defeito.
- Resolução intra-hex: um hex res-7 cobre vários setores/bairros; resolve-se bairro DOMINANTE por população (escolha A2; preserva melhor a resolução do setor que A1).
- Em produção a Página 6 só lista bairros reais APÓS o job de re-materialização pós-merge (acima); sem ele, fallback gracioso.

## Saída literal das validações (re-executadas pelo QA)

### `python -m pytest -n auto -q` (xdist) — FALHA DE AMBIENTE (não defeito)
```
INTERNALERROR> ... xdist/workermanage.py ... node.setup() ... self.gateway._rinfo() ...
INTERNALERROR>   File ".../execnet/gateway_base.py", line 571, in from_io
INTERNALERROR>     raise EOFError("couldn't load message header, " + e.args[0]) from None
INTERNALERROR> EOFError: couldn't load message header, expected 9 bytes, got 0
```
Confirmado: o `-n auto` (xdist/execnet) quebra na criação dos workers neste ambiente (Windows + Python 3.14), EXATAMENTE como o Builder relatou. É falha de ambiente, NÃO de código. Caí para serial (gate único abaixo).

### `python -m pytest -q -p no:xdist` (SUÍTE COMPLETA serial — gate único)
```
1095 passed, 1 skipped, 129970 warnings in 273.86s (0:04:33)
```

### `python -m pytest -q -p no:xdist tests/unit/test_relatorio_municipal.py`
```
27 passed, 1297 warnings in 9.84s
```

### `python -m pytest -q -p no:xdist tests/integration/test_materializar_setores_censitarios_geo.py`
```
3 passed, 145 warnings in 1.27s
```

### `python -m pytest -q -p no:xdist tests/ -k "censo or censitario or streamlit_app or relatorio or materializar or setores_geo"`
```
348 passed, 748 deselected, 129961 warnings in 109.87s (0:01:49)
```

### `python -c "import streamlit_app; print('import ok')"`
```
import ok
```

### `python -m ruff check src tests`
```
All checks passed!
```

### `python -m mypy src/.../relatorio_municipal.py .../materializar_setores_censitarios_geo.py .../pages.py`
```
Success: no issues found in 3 source files
```

### `ruff format --check` — RESSALVA LEVE pré-existente (verificada pelo QA)
Working tree (com edições do Builder):
```
Would reformat: src\motor_expansao\dashboard\pages.py
Would reformat: src\motor_expansao\dashboard\relatorio_municipal.py
Would reformat: src\motor_expansao\pipelines\materializar_setores_censitarios_geo.py
Would reformat: streamlit_app.py
4 files would be reformatted
```
Versões `HEAD` (ANTES do Builder, extraídas via `git show HEAD:<arquivo>`):
```
Would reformat: .../head_check/materializar_setores_censitarios_geo.py
Would reformat: .../head_check/pages.py
Would reformat: .../head_check/relatorio_municipal.py
Would reformat: .../head_check/streamlit_app.py
4 files would be reformatted
```
→ Os 4 arquivos já eram não-conformes ao `ruff format` ANTES das edições. Alegação do Builder CONFIRMADA: pré-existente, não introduzido. Gate do projeto = `ruff check` (passa). Ressalva LEVE, não bloqueante.

### Housekeeping (Passo 6.0 — pré-condição do fechamento)
```
tasks/backlog.md:101:### BLK-RELMUN-02 — "Bairros por Zona" com nomes reais de bairro (resolve o D9 do BLK-RELMUN-01)
scripts/housekeeping_move_block.py  (presente)
```
`python -m pytest -q -p no:xdist tests/unit/test_housekeeping_helper.py`:
```
10 passed, 481 warnings in 0.33s
```
Heading `### BLK-RELMUN-02` íntegro + helper presente + teste do helper verde → o move via helper é VIÁVEL. O orquestrador executará `housekeeping_move_block.py BLK-RELMUN-02 --date 2026-06-24` + `--check` no fechamento (NÃO executado pelo QA, conforme instrução).

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock do caminho crítico. Os testes novos usam DataFrames sintéticos (`_sample_df`, `_bairros_por_hex_sample`) para isolar a LÓGICA de agrupamento/agregação — prática normal de teste unitário, NÃO bypass (não há tooling dependente de config de produção neste bloco; a regra (b) do prompt mira tooling tipo SOPS, não unit tests). A suíte completa rodou de fato (serial, único caminho viável no ambiente); o `-n auto` foi tentado e falhou por ambiente, documentado acima.

## Conferência cruzada com log do Builder
Bate. `test_relatorio_municipal.py` 27 passed (Builder: 27, era 23 +4). `test_materializar_setores_censitarios_geo.py` 3 passed (Builder: 3). Subset censo/relatório 348 passed pelo QA vs 346 do Builder — divergência explicada pelo `-k` do QA incluir também `materializar`/`setores_geo` (2 testes a mais), sem nenhuma falha. ruff check / mypy / import ok idênticos. As 2 NOTAS de ambiente do Builder (xdist quebrado; ruff format pré-existente) foram re-verificadas e CONFIRMADAS por evidência própria.

## Guardrails verificados
- score_priorizacao não alterado: sim — `git diff HEAD` sobre `src`/`streamlit_app.py` não tem nenhuma linha +/- com `score_priorizacao`/`hex_score_estrutural`/`0.40`/`0.60`/`PESOS_HEX`/`RENDA_MIN`/`DIST_MIN_ULTRA`.
- Artefatos M1 preservados: sim — diff não toca `config.py`, `pipelines/m1/`, `*scoring*`, `brasil_estrutural/priorizados`, `hexagonos_brasil*` nem `data.py`/`censo_extra_cols` (isso seria A1). Só artefato derivado censitário geo ganha coluna nova `nome_bairro`/`cod_bairro` (job operacional pós-merge).
- Coexistência Relatório Pontual: sim — `censo_report.py`/`censo_map.py`/`censo_point.py`/`setor_censitario_intersecao_area_1p5km` AUSENTES do diff (byte-a-byte intocados); `test_coexistencia_relatorio_pontual_intocado` verde.
- Blocos 4–6 intocados: sim — sem toque em `load_uf_slice`/`read_enriched_uf_partition`/`build_dashboard_dataset`/`render_tab_selector`; A2 não mexe na cadeia hot.
- PDF 9 páginas / `/Count 9` / mesma ordem: sim (asserts `b"/Count 9"` nos testes verdes).
- Fallback gracioso real: sim — `_bairros_page` sem `raise`, sem "indisponível" como texto principal; cai nas zonas geométricas + nova nota "Bairros nao mapeados na base IBGE 2022".
- Escopo respeitado: sim — apenas os 7 arquivos esperados + tracking (handoff/current_task); A2 (não A1/B/C); nenhuma dependência nova de rede.
- Validações re-executadas pelo QA (sem bypass): pytest serial full → 1095 passed/1 skipped; import streamlit_app → ok; ruff check → All checks passed; mypy → Success; housekeeping helper → 10 passed.
- H3_RESOLUTION=7 / DIST_MIN_ULTRA_KM=1.0 / RENDA_MIN=4500.0 / pesos 0.40/0.60: INALTERADOS (config.py fora do diff).

## Decisão recomendada
Fechar ciclo. Executar o housekeeping via `scripts/housekeeping_move_block.py BLK-RELMUN-02 --date 2026-06-24` + `--check`, depois merge humano. Registrar a ressalva leve do `ruff format` (pré-existente) e a pendência operacional de re-materialização da malha geo.
