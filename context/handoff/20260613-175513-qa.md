# Handoff — QA

## Skill que gerou este handoff
QA

## Bloco validado
BLK-DIM-05

## Veredito
APROVADO

## Resultados dos gates

### Suite de testes
`python -m pytest -q` → **861 passed, 4 skipped, 18 warnings** em 121,75s.
Atende o gate (≥ 861 passed, ≤ 4 skipped). Warnings são pré-existentes (CRS geográfico
no censo `fase_a_censo2022_setores.py` + FutureWarning de downcast no pipeline de mercado),
não relacionados a este bloco.

### ruff + mypy
PASS.
- `ruff check src/.../features_exogenas.py tests/.../test_features_exogenas.py` → All checks passed (exit 0).
- `mypy src/.../features_exogenas.py --ignore-missing-imports` → Success: no issues found (exit 0).

### Guardrails
- M1 intocado: SIM. `git diff HEAD src/.../pipelines/m1/` vazio. `config.py` mostra diff,
  porém é **pura mudança de EOL (CRLF↔LF)** — `git diff --ignore-all-space --ignore-blank-lines`
  é VAZIO; conteúdo idêntico, último commit de conteúdo foi BLK-FIX-06-B. Artefato de working-tree
  pré-existente (gitStatus inicial já listava dezenas de arquivos assim), NÃO tocado pelo Builder.
- aderencia.py intocado: SIM. `git diff HEAD src/.../dimensionamento/aderencia.py` vazio (congelado, BLK-DIM-01R).
- Sem gitignored commitado: SIM. `git diff HEAD data/analysis/` vazio; `git check-ignore data/analysis/features_aderencia.md` confirma ignorado.
- Arquivos esperados presentes: SIM. `features_exogenas.py` (25.868 B) e `test_features_exogenas.py` (10.807 B) criados.

### Controle negativo
PRESENTE. `test_controle_negativo_features_aleatorias` (teste 6) gera n_conc/densidade/renda_resp
ALEATÓRIOS (fixture `df_features_ruido`, alvo = pop+ruído) e asserta `assert not material` para
A e B — features de ruído NÃO produzem GO material espúrio. Materialidade des-circular:
`r2_loo_log > LIMIAR_R2_GO (0.05) E > baseline + DELTA_MATERIAL (0.05)`. Pareado com controle
POSITIVO (teste 5): n_conc correlacionado com pagantes → A supera o baseline, provando que o
pipeline capta sinal real quando existe (o controle negativo não passa por pipeline quebrado).

### Housekeeping check
`scripts/housekeeping_move_block.py BLK-DIM-05 --check` → exit **1** ("stub ausente no backlog").
≠ 0 e ≠ 3 = esperado neste momento (helper roda APÓS aprovação do QA). NÃO bloqueia o veredito.

### Handoff versionado do Builder
PRESENTE: `context/handoff/20260613-174718-builder.md`.

## Observações finais
- Resultado real nos dados (reportado pelo Builder, coerente): N=53, NO-GO honesto —
  baseline pop+renda r2_loo_log=-0.0134 (reproduz EXATAMENTE o BLK-DIM-01R → LOO-CV
  reimplementado fiel), A=-0.0700 e B=-0.0675 (ambos PIORAM). Nenhuma feature exógena
  disponível melhora materialmente. Consistente com DEC-001/BLK-DIM-01R.
- LOO Ridge reimplementado do zero em `features_exogenas.py` (NÃO importa de `aderencia.py`,
  respeitando o congelamento). Censo helper `analisar_ponto_censitario_setores` reusado
  via import lazy, INTOCADO. Persistência só no relatório gitignored — sem PII versionada.
- Ressalva (não-bloqueante, já documentada pelo Builder): caminho de I/O geo
  (`derivar_features_censo`/`montar_df_features`/`main()`) não tem cobertura unitária por design
  (testes offline); foi exercido manualmente. N=53 é estruturalmente pequeno (alpha=100 em todos
  os sets → baixa estabilidade). Lacuna 18-45/CLT/% ocupados ausente no Censo 2022 Básico →
  trabalho futuro BLK-DIM-06.
- Próximo passo: orquestrador roda housekeeping (move BLK-DIM-05 → completed.md) e commit por path.
