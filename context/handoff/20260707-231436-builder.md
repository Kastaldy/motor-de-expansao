# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-PROD-06 — Relatório de movimentação concorrencial

## O que foi feito

Criados dois arquivos novos conforme plano do Planner:

1. **`scripts/movimentacao_concorrencial.py`** — script analítico READ-ONLY sobre o M1:
   - `carregar_dados()`: lê 3 parquets (3.296 concorrentes, 10.165 densos, 1.542.531 linhas mercado com 6 colunas)
   - `join_uf_cidade()`: filtra 3.179 válidos + LEFT merge via `hex_id_res7` (16 sem UF)
   - `resumo_por_rede()`: 28 redes, top 3 cidades por rede
   - `resumo_por_uf()`: distribuição por 26 UFs
   - `top_municipios()` (helper): top 10 por unidades
   - `rede_dominante_por_uf()`: baseado em `rede_dominante_2km` do mercado (1.717 hexes com dominância)
   - `impacto_residual()`: oferta consumida 6.159.181 | disponível 26.367.446 alunos
   - `resumo_densos()`: TotalPass/Wellhub separados (6.699 registros — anti-dupla contagem)
   - `gerar_relatorio()`: Markdown 8 seções com acentuação correta (BLK-ACENTO)
   - `run()`: orquestrador completo → `data/analysis/movimentacao_concorrencial.md`

2. **`tests/unit/test_movimentacao_concorrencial.py`** — 11 testes unitários com fixtures sintéticas em memória (zero leitura de parquets reais).

### Resultados reais (snapshot único, coleta 2026-04-22 a 2026-05-04)
- **Top redes:** smart_fit (999), panobianco (455), bluefit (225), selfit (218), pratique (164)
- **Top UFs:** SP (1.261 un, 23 redes), MG (281), RJ (236), PR (217), DF (136)
- **Top municípios:** São Paulo (497), Rio de Janeiro (157), Brasília (136), Belo Horizonte (126), Curitiba (105)
- **Impacto residual nacional:** oferta consumida = 6.159.181 alunos | disponível = 26.367.446 alunos
- **TotalPass + Wellhub (base densa, NÃO somados ao residual):** 6.699 registros

### Correção aplicada durante implementação
A fixture do Planner definia c3 com o mesmo hex de c1 (já no mercado), mas asserts esperavam 1 UF nula. Corrigi o hex de c3 para `87000000dffffff` (hex sem match no mercado) — comportamento fiel à intenção do Planner. Os asserts de `resumo_por_uf` foram ajustados de SP=2 para SP=1 (coerente com a fixture corrigida).

## Arquivos alterados

- `/repo/scripts/movimentacao_concorrencial.py` — NOVO (350 linhas)
- `/repo/tests/unit/test_movimentacao_concorrencial.py` — NOVO (11 testes, 284 linhas)
- `/repo/data/analysis/movimentacao_concorrencial.md` — gerado em runtime (gitignored, não versionado)
- `/repo/context/handoff.md` — este arquivo
- `/repo/context/handoff/20260707-231436-builder.md` — snapshot append-only
- `/repo/tasks/current_task.md` — atualizado (próxima Skill: QA)

## Validações executadas

- `python scripts/movimentacao_concorrencial.py`: OK — relatório gerado com dados reais
- `ruff check scripts/ tests/unit/test_movimentacao_concorrencial.py`: All checks passed
- `mypy scripts/movimentacao_concorrencial.py --ignore-missing-imports`: Success, no issues
- `pytest tests/unit/test_movimentacao_concorrencial.py -v`: **11 passed, 0 failed**
- `pytest -n auto -q` (suite FULL): **1504 passed, 4 failed, 4 skipped, 1 error** — falhas são todas pré-existentes (Plus Code tests + test_viz_atratividade matplotlib), zero regressão
- `import streamlit_app`: OK
- `git diff HEAD -- src/`: zero linhas novas (o diff CRLF→LF em `dimensionamento/config.py` é pré-existente no working tree desde antes deste bloco)
- `python scripts/loop_guard.py --base HEAD~1`: GUARD OK (7 caminhos alterados, nenhum proibido)

## Problemas encontrados

1. **Fixture inconsistente no handoff do Planner**: a descrição do fixture `df_conc_fake` descrevia c3 com hex `87a8c2090ffffff` (mesmo de c1, que está no mercado → uf=SP) mas os asserts esperavam 1 UF nula. Corrigi atribuindo hex `87000000dffffff` a c3, coerente com os asserts do Planner.

## Pendências

Nenhuma — bloco concluído.

## Riscos remanescentes

Nenhum novo. O relatório é snapshot único (coleta 2026-04-22..05-04); novas execuções com dados da coleta semanal VPS (DEC-013) produzirão retratos atualizados.

## Guardrails verificados

- score_priorizacao não alterado: sim — zero mudanças em src/
- Artefatos M1 preservados: sim — nenhum parquet de staging modificado
- Dashboard offline mantido: sim — script não importa nenhum módulo de src/motor_expansao/
- TotalPass/Wellhub separados: sim — reportados na Seção 7 com nota explícita, NÃO somados ao residual
- loop_guard.py: GUARD OK
