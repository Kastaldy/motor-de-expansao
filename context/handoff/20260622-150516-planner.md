# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Builder

## Entendimento da tarefa
Adicionar três parâmetros de financiamento de capex a `viabilidade()` em `simulador.py`, calcular a PMT uma vez antes do loop de payback e subtraí-la do FCF apenas durante o prazo de parcelamento (sem tocar EBITDA), e expor `capex_total` + controles condicionais de financiamento no expander "Parametros avancados" de `pages.py`.

## Plano técnico

### Passo 1 — `simulador.py`: adicionar parâmetros e calcular PMT antes do loop

Arquivo: `/repo/src/motor_expansao/dimensionamento/simulador.py`

**1a. Assinatura de `viabilidade()` (linhas 101–103, após `coef_capex_m2`):**

Inserir três novos parâmetros keyword-only imediatamente após `coef_capex_m2: float | None = None,`:

```python
# Parametros de financiamento de capex (capex parcelado — equipamentos/tecnologia)
capex_financiado_pct: float = 0.0,   # fracao do capex financiado (0.0 = pagamento a vista; sem PMT)
prazo_financiamento_meses: int = 36,  # prazo do parcelamento em meses (default planilha)
juros_financiamento_am: float = 0.018,  # taxa de juros mensal (default 1,8% a.m.)
```

**1b. Docstring de `viabilidade()` — adicionar ao bloco Parameters (após a entrada de `coef_capex_m2`):**

```
capex_financiado_pct:
    Fracao do capex efetivo financiado (0.0 a 1.0; default 0.0 = pagamento a vista).
    Com 0.0, o comportamento e identico ao atual (nenhuma PMT gerada).
prazo_financiamento_meses:
    Prazo do parcelamento em meses (default 36). So usado se capex_financiado_pct > 0.
juros_financiamento_am:
    Taxa de juros mensal do financiamento em fracao (default 0.018 = 1,8% a.m.).
    Se 0.0, a PMT e calculada como parcela simples (C / n), sem juros.
```

**1c. Calcular PMT ANTES do loop de payback (inserir após linha 271, antes de `for t in range(1, 61):`):**

```python
# -----------------------------------------------------------------------
# 11a. PMT de financiamento (calculada UMA VEZ, antes do loop)
# Afeta SOMENTE o fcf_t no loop (custo financeiro pos-EBITDA).
# margem_ebitda_pct e ebitda_mensal NAO sao alterados pela PMT.
# -----------------------------------------------------------------------
_C = capex_efetivo * capex_financiado_pct
if _C > 0 and juros_financiamento_am > 0:
    _r = juros_financiamento_am
    _n = prazo_financiamento_meses
    _pmt = _C * _r * (1.0 + _r) ** _n / ((1.0 + _r) ** _n - 1.0)
elif _C > 0:
    # juros zero: parcela simples (sem encargos)
    _pmt = _C / max(prazo_financiamento_meses, 1)
else:
    _pmt = 0.0
```

**1d. Dentro do loop `for t in range(1, 61):`, após linha 288 (`fcf_t = eb_t - ir_t`) e ANTES de `fcf_acum += fcf_t`:**

```python
        if _pmt > 0 and t <= prazo_financiamento_meses:
            fcf_t -= _pmt
```

**1e. Docstring de `ViabilidadeResult` — adicionar nota em `payback_meses`:**

```
payback_meses:
    Meses ate FCF acumulado zerar (com rampa de maturacao e PMT de financiamento
    se capex_financiado_pct > 0); float('inf') se nunca dentro de 60 meses.
```

**Posições exatas no arquivo:**
- Parâmetros novos: entre linha 103 (`coef_capex_m2`) e linha 104 (comentário `# Ratios %`)
- Bloco PMT: entre linha 271 (`_maturacao_meses = max(...)`) e linha 273 (`for t in range(1, 61):`)
- Subtração no loop: entre linha 288 (`fcf_t = eb_t - ir_t`) e linha 289 (`fcf_acum += fcf_t`)

### Passo 2 — `viabilidade_ponto.py`: nenhuma alteração

`grade_sensibilidade()` (linha 255–258) e `analisar_viabilidade_ponto()` (linhas 335–338, 341–344, 346–349, 351–354, 357–367) já propagam `**kwargs` para todas as chamadas de `viabilidade()`, `aluguel_teto()` e `alunos_minimos_viaveis()`. Os três novos parâmetros fluirão automaticamente via kwargs — zero alteração neste arquivo.

### Passo 3 — `pages.py`: UI dentro do expander e na chamada

Arquivo: `/repo/src/motor_expansao/dashboard/pages.py`

**3a. Importar `SIM_CAPEX_DEFAULT` no topo do arquivo (verificar se já está importado).**

Procurar na seção de imports se `SIM_CAPEX_DEFAULT` já é importado de `motor_expansao.dimensionamento.config`. Se não estiver, adicionar à linha de import correspondente (junto com `SIM_MENSALIDADE_BALCAO` que já está importado no arquivo).

**3b. Dentro do `with st.expander("Parametros avancados", expanded=False):` (linhas 3306–3321), APÓS `margem_alvo_pct` (linha 3321) e ANTES do `submitted = st.form_submit_button(...)` (linha 3322):**

```python
            st.markdown("**Capex e financiamento**")
            capex_total = st.number_input(
                "Capex total (R$)",
                min_value=0.0,
                value=float(SIM_CAPEX_DEFAULT),
                step=10_000.0,
                key="viab_ponto_capex",
            )
            pct_financiado = st.slider(
                "% do capex financiado",
                min_value=0,
                max_value=100,
                value=0,
                step=5,
                key="viab_ponto_pct_financiado",
            )
            if pct_financiado > 0:
                prazo_financiamento_meses = st.number_input(
                    "Prazo (meses)",
                    min_value=6,
                    max_value=60,
                    value=36,
                    step=6,
                    key="viab_ponto_prazo",
                )
                juros_am_pct = st.number_input(
                    "Juros a.m. (%)",
                    min_value=0.0,
                    max_value=10.0,
                    value=1.8,
                    step=0.1,
                    key="viab_ponto_juros",
                )
                st.caption(
                    "Equipamentos e tecnologia parcelados conforme planilha padrao "
                    "(36 meses, 1,8% a.m.). A PMT entra como custo financeiro no FCF "
                    "(nao altera EBITDA)."
                )
            else:
                prazo_financiamento_meses = 36
                juros_am_pct = 1.8
```

ATENÇÃO: os widgets `st.number_input`/`st.slider` DEVEM ficar dentro do `with st.form(...)` / `with st.expander(...)`, antes do `st.form_submit_button`. O `if pct_financiado > 0:` controla APENAS a exibição condicional dos campos de prazo/juros; `prazo_financiamento_meses` e `juros_am_pct` são sempre definidos (branch `else` garante default).

**3c. Na chamada de `analisar_viabilidade_ponto()` (linha ~3344), adicionar os novos kwargs:**

```python
        result = analisar_viabilidade_ponto(
            lat,
            lng,
            float(m2),
            float(aluguel_pedido),
            float(demanda_premissa),
            ticket_medio=float(ticket_medio),
            margem_alvo=float(margem_alvo_pct) / 100.0,
            base_calibracao_df=base_calibracao_df,
            setores_df=setores_df,
            capex=float(capex_total),
            capex_financiado_pct=float(pct_financiado) / 100.0,
            prazo_financiamento_meses=int(prazo_financiamento_meses),
            juros_financiamento_am=float(juros_am_pct) / 100.0,
        )
```

### Passo 4 — Testes: adicionar CA-10a..d em `tests/unit/dimensionamento/test_simulador.py`

Arquivo: `/repo/tests/unit/dimensionamento/test_simulador.py`

Adicionar ao final do arquivo (após o último teste `test_payback_finito_exibe_numero_meses`):

```python
# ---------------------------------------------------------------------------
# CA-10: financiamento de capex (BLK-DIM-20)
# ---------------------------------------------------------------------------


def test_financiamento_zero_resultado_identico() -> None:
    """CA-10a: capex_financiado_pct=0.0 => resultado identico ao sem o parametro (regressao zero)."""
    r_sem = viabilidade(**VIAVEL)
    r_com = viabilidade(**VIAVEL, capex_financiado_pct=0.0)
    assert r_sem.payback_meses == r_com.payback_meses
    assert r_sem.margem_ebitda_pct == r_com.margem_ebitda_pct
    assert r_sem.flag_viavel == r_com.flag_viavel


def test_financiamento_aumenta_payback() -> None:
    """CA-10b: capex_financiado_pct=1.0 => payback estritamente maior que sem financiamento."""
    r_sem = viabilidade(**VIAVEL)
    r_com = viabilidade(
        **VIAVEL,
        capex_financiado_pct=1.0,
        prazo_financiamento_meses=36,
        juros_financiamento_am=0.018,
    )
    assert r_com.payback_meses > r_sem.payback_meses, (
        f"payback com financiamento ({r_com.payback_meses}) deveria ser > sem ({r_sem.payback_meses})"
    )


def test_financiamento_nao_altera_ebitda() -> None:
    """CA-10c: margem_ebitda_pct e ebitda_mensal identicos com e sem financiamento (EBITDA pre-financiamento)."""
    r_sem = viabilidade(**VIAVEL)
    r_com = viabilidade(
        **VIAVEL,
        capex_financiado_pct=1.0,
        prazo_financiamento_meses=36,
        juros_financiamento_am=0.018,
    )
    assert r_sem.margem_ebitda_pct == r_com.margem_ebitda_pct, (
        "margem_ebitda_pct nao deve ser afetada pelo financiamento"
    )
    assert r_sem.ebitda_mensal == r_com.ebitda_mensal, (
        "ebitda_mensal nao deve ser afetado pelo financiamento"
    )


def test_pmt_formula_correta() -> None:
    """CA-10d: PMT correta para C=600k, r=0.018, n=36 (referencia calculada externamente).

    PMT = C * r * (1+r)^n / ((1+r)^n - 1)
        = 600000 * 0.018 * 1.018^36 / (1.018^36 - 1)
        = 600000 * 0.018 * 1.8983... / (1.8983... - 1)
        ~ 600000 * 0.03825... / 0.8983...
        ~ 600000 * 0.03827...
        ~ 22965... (aprox R$22.960-23.000)

    Valida via FCF: com financiamento 100%, os primeiros 36 meses do FCF acumulado
    devem ser menores que sem financiamento (PMT subtraida).
    """
    C = 600_000.0
    r = 0.018
    n = 36
    pmt_esperada = C * r * (1.0 + r) ** n / ((1.0 + r) ** n - 1.0)
    assert 22_000 < pmt_esperada < 24_000, (
        f"PMT de referencia fora do intervalo esperado: {pmt_esperada:.0f}"
    )

    # Confirmar que o simulador subtrai a PMT: no mes 1, FCF com financiamento < sem
    # (inferido via payback — financiamento sempre piora ou iguala o payback)
    r_sem = viabilidade(
        **VIAVEL,
        capex_financiado_pct=0.0,
        prazo_financiamento_meses=36,
        juros_financiamento_am=0.018,
    )
    r_com = viabilidade(
        **VIAVEL,
        capex_financiado_pct=1.0,
        prazo_financiamento_meses=36,
        juros_financiamento_am=0.018,
    )
    assert r_com.payback_meses >= r_sem.payback_meses
```

## Arquivos afetados
- `/repo/src/motor_expansao/dimensionamento/simulador.py` (assinatura, bloco PMT, loop, docstrings)
- `/repo/src/motor_expansao/dashboard/pages.py` (expander + chamada de `analisar_viabilidade_ponto`)
- `/repo/tests/unit/dimensionamento/test_simulador.py` (CA-10a..d, append no final)

## Dependências
- `SIM_CAPEX_DEFAULT` está em `src/motor_expansao/dimensionamento/config.py` linha 100 — confirmar que o import já existe em `pages.py` (buscar `SIM_CAPEX_DEFAULT` no arquivo; se ausente, adicionar ao import existente de `motor_expansao.dimensionamento.config`)
- `viabilidade_ponto.py` propaga `**kwargs` nas linhas 255–258, 335–338, 341–344, 346–349, 351–354 e 357–367 — nenhuma alteração necessária
- O expander "Parametros avancados" está na linha 3306 de `pages.py`; o `st.form_submit_button` está na linha 3322 — os novos widgets ficam dentro do expander, antes do submit button

## Riscos técnicos
- **Regressão com `capex_financiado_pct=0.0`:** `_C = 0 => _pmt = 0.0` => loop inalterado. CA-10a cobre este caso.
- **PMT afetando EBITDA:** o bloco PMT é inserido APÓS o cálculo de `ebitda` e `margem_ebitda_pct` (seções 7 e 8, linhas 238–244) e APÓS `capex_efetivo` (seção 10, linhas 255–262). A PMT só atua dentro do loop na linha `fcf_t -= _pmt`. CA-10c valida isso.
- **Widgets fora do `st.form`:** os novos campos devem estar dentro do bloco `with st.form(key="viab_ponto_form"):` (linha 3264) e antes do `st.form_submit_button` (linha 3322). O `if pct_financiado > 0:` é condicional de render no Streamlit — funciona dentro de formulário.
- **`prazo_financiamento_meses` e `juros_am_pct` indefinidos quando `pct_financiado == 0`:** coberto pelo branch `else` que atribui os defaults (36 e 1.8) antes da chamada. Isso garante que os kwargs passados a `analisar_viabilidade_ponto` são sempre definidos.
- **Import de `SIM_CAPEX_DEFAULT` em `pages.py`:** verificar se já existe; se não, adicionar. O import atual inclui `SIM_MENSALIDADE_BALCAO` da mesma linha do config do dimensionamento.
- **PMT com `juros_financiamento_am=0`:** tratado pelo branch `elif _C > 0: _pmt = _C / max(prazo, 1)` (parcela simples). Valor de borda não provável na UI mas coberto no código.

## Critérios de aceite finais
- CA-10a: `viabilidade(**VIAVEL, capex_financiado_pct=0.0)` retorna `payback_meses`, `margem_ebitda_pct` e `flag_viavel` idênticos a `viabilidade(**VIAVEL)` sem o parâmetro
- CA-10b: `viabilidade(**VIAVEL, capex_financiado_pct=1.0, prazo_financiamento_meses=36, juros_financiamento_am=0.018)` tem `payback_meses` estritamente maior que `viabilidade(**VIAVEL)`
- CA-10c: `margem_ebitda_pct` e `ebitda_mensal` idênticos entre os dois cenários de CA-10b
- CA-10d: PMT de referência para (C=600k, r=0.018, n=36) cai no intervalo [22k, 24k]; payback com financiamento >= sem financiamento
- Dashboard: com `pct_financiado > 0`, campos de prazo e juros aparecem no expander; com `pct_financiado == 0`, ficam ocultos
- `ruff check` e `mypy` limpos nos três arquivos alterados
- Suite completa (`pytest -q`) verde — zero falhas, zero novos warnings

## Validações obrigatórias
- Comando: `ruff check src/motor_expansao/dimensionamento/simulador.py src/motor_expansao/dashboard/pages.py tests/unit/dimensionamento/test_simulador.py`
- Comando: `mypy src/motor_expansao/dimensionamento/simulador.py src/motor_expansao/dashboard/pages.py --ignore-missing-imports`
- Comando: `pytest tests/unit/dimensionamento/test_simulador.py -q`
- Critério mínimo: 0 falhas, sem erro de import, CA-10a..d todos verdes
- Suite completa (gate QA): `pytest -q` (todos os testes do repositório)

## Fora de escopo
- `config.py` do M1 (pesos, fórmula, `score_priorizacao`, parâmetros canônicos do §3)
- `src/motor_expansao/dimensionamento/config.py` — novos defaults NÃO entram como constantes neste arquivo; ficam inline nos parâmetros de `viabilidade()`
- `viabilidade_ponto.py` — nenhuma alteração necessária
- VPS, deploy, Dockerfiles, CI fora do `pytest`/`ruff`/`mypy`
- Artefatos M1 (`brasil_estrutural.parquet`, `brasil_priorizados.parquet` etc.)
- Qualquer outro bloco (BLK-DIM-21, BLK-DIM-22 dependem deste)

## Alerta de aprovação
Criticidade MÉDIA, bloco loop-safe. Sem gate humano — substituído pelo guard automático (`scripts/loop_guard.py`). READ-ONLY sobre o M1: nenhum parâmetro canônico do §3 é alterado, nenhum artefato oficial é tocado.

## Guardrails ativos
- `margem_ebitda_pct` e `ebitda_mensal` são EBITDA pré-financiamento (spec §8.2) — a PMT NÃO entra no EBITDA.
- `config.py` do M1 e `dimensionamento/config.py`: INTOCADOS.
- Loop guard (`scripts/loop_guard.py`) aborta se o diff tocar `config.py`/`pipelines/m1`/`*scoring*`/artefatos M1/`deploy/`/Dockerfiles/compose/Caddy/authelia/`.env`/`secrets/`/CI.
- Sem bypass de testes (`--no-verify`, `-k`, `pytest -x` sem suite completa no QA).
- Visualizações, análise radial e interações de mapa não podem recalcular ou alterar `score_priorizacao`, `hex_score_estrutural`, carteira, plano curto prazo, plano domínio ou artefatos oficiais do M1 sem aprovação explícita (§5 permanente).
