# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador: housekeeping move + commit por path). Bloco Média, sem gate humano.

## VEREDITO
APROVADO COM RESSALVAS

## Justificativa
Todo o escopo planejado (acentuação latin-1 das strings-fonte dos 2 relatórios, comentários
corrigidos, teste anti-`"?"` em 2 camadas, asserts de bytes atualizados) foi entregue correto: 0
tipografia fora de latin-1 nas linhas adicionadas, identificadores/números/`_ascii()`/núcleo censo_*
intactos, e a suíte completa verde exceto a falha pré-existente/ambiental do M2 (parquet gitignored
ausente), sem relação com este bloco. Ressalva única, não-bloqueante: as legendas do mapa PNG
municipal (`_COBERTURA_LEGENDA`/`_RESUMO_LEGENDA`, "Aprovado (dado proprio)") ficaram sem acento —
texto VISÍVEL no PDF, renderizado via PIL (fora do `_ascii`/teste), e inconsistente com strings
irmãs do MESMO caminho PIL (dict `titulo`, rodapé) que o Builder acentuou.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- Legendas PIL do mapa municipal sem acento: `relatorio_municipal.py:106,112` — `"Aprovado (dado
  proprio)"` deveria ser `"próprio"`. É texto VISÍVEL no PDF (legenda das páginas "Visão Geral do
  Município" [cobertura] e "Resumo da Região" [resumo]), renderizado por `_draw_text`+`_font`
  (PIL), NÃO por `_ascii`. Consequência: (a) escapa das DUAS camadas do teste novo (5.1 só embrulha
  `_ascii`; 5.2 gera os PDFs SEM imagens); (b) contradiz a regra permanente §2 ("TODO texto voltado
  ao usuário acentuado"); (c) é INCONSISTENTE com o próprio commit — o dict `titulo`
  (`relatorio_municipal.py:895-901`) e o rodapé "Agregação H3" (l.1119/1121) usam o MESMÍSSIMO
  caminho PIL (`_font`→arial.ttf, fallback `load_default`) e FORAM acentuados. Logo a justificativa
  "renderiza via PIL, risco de glifo" não distingue as legendas das strings irmãs já acentuadas.
  Correção é trivial (2 literais idênticos). Ver "Decisão recomendada".

## Melhorias opcionais
- nenhuma

## Testes faltantes
- Não há cobertura de teste para o texto renderizado por PIL nas legendas/títulos do mapa municipal
  (o teste anti-`"?"` cobre só o caminho `_ascii`/bytes crus). Não é regressão deste bloco, mas é o
  motivo pelo qual a ressalva das legendas passou despercebida pela suíte; um assert visual/OCR ou
  um scan das strings-fonte das legendas fecharia o flanco (follow-up opcional).

## Riscos remanescentes
- Revisão visual humana dos 3 PDFs (pontual recente + classico + municipal) é critério de aceite
  explícito do backlog (não gate formal). Recomendado antes do fechamento — e é onde a ressalva das
  legendas "dado proprio" seria naturalmente vista.
- Ambiental: `pytest-xdist` (`-n auto`) instável no Python 3.14 desta máquina (execnet EOFError no
  setup do worker). A suíte SERIAL é a fonte de verdade aqui (1435 itens coletados; ver abaixo).

## Saída literal das validações (re-executadas pelo QA)

`python -m ruff check .`
```
All checks passed!
=== exit 0 ===
```

`python -m mypy src` (6 erros, TODOS pré-existentes de types-requests em arquivos NÃO tocados; ZERO
em censo_report.py/relatorio_municipal.py — bate com os 6 do Builder):
```
src\motor_expansao\dimensionamento\growth_api_client.py:28: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\ibge_censo.py:24: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\poi_enrichment.py:15: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\base_h3_brasil.py:31: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\geo.py:21: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\telegram_bot.py:23: error: Library stubs not installed for "requests"  [import-untyped]
Found 6 errors in 6 files (checked 102 source files)
=== exit 1 ===
```

`python -c "import streamlit_app; print('import ok')"`:
```
2026-07-07 14:53:38 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
(...warnings de cache do Streamlit sem runtime, esperados...)
import ok
```

`python -m pytest -q` (suíte COMPLETA SERIAL — gate único do ciclo; fonte de verdade):
```
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 1434 passed, 2 skipped, 218868 warnings in 606.32s (0:10:06)
```
- Única falha isolada e confirmada como pré-existente/ambiental: `FileNotFoundError` ao ler
  `data/staging/unidade_territorio_retencao.parquet` (parquet do M2/`lifetime/`, gitignored, ausente
  neste ambiente). NÃO toca nenhum arquivo deste bloco (report/dashboard). Exatamente a falha que a
  instrução do QA mandou distinguir.
- `-n auto` NÃO usado como fonte de verdade (execnet EOFError no Python 3.14, instável — reportado
  honestamente, sem bypass; a corrida em background anterior com `-n auto` foi descartada em favor
  do serial determinístico).

`python -m pytest -q tests/unit/test_relatorio_pontual_censitario_export.py tests/unit/test_relatorio_municipal.py tests/unit/test_relatorio_acentuacao_regressao.py` (os 3 arquivos do bloco, incl. o novo anti-`"?"`):
```
66 passed, 3183 warnings in 25.14s
```
- Bate com o Builder (26 censo export + 37 municipal + 3 regressão = 66).

`python scripts/housekeeping_move_block.py BLK-ACENTO-02 --check` (estado PRÉ-move esperado):
```
FALHA --check: stub ausente no backlog para BLK-ACENTO-02
=== exit 1 ===
```
- Correto: reconhece o bloco (NÃO é `BlockNotFound`), aponta que o stub ainda não foi criado. O move
  é passo do orquestrador no fechamento. Registrado como pré-move correto.

Scan de tipografia fora de latin-1 nas linhas ADICIONADAS do diff dos 2 módulos:
```
banned added: 0 | non-latin1(>0xFF) added: 0
```
- Zero travessão/en-dash/bullet/seta/reticências/aspas curvas/© e zero char >0xFF nas linhas novas.

## Conferência de no-bypass
Confirmo: nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock do
caminho crítico. Não há tooling de produção em escopo neste bloco (é texto de relatório). O teste
novo usa fixtures sintéticas legítimas (`_sample_result`/`_sample_df`/`_sample_dominio`) reaproveitadas
dos 2 arquivos existentes — prática normal de teste unitário, explicitamente FORA da regra anti-bypass
(que mira tooling que depende de config/artefatos reais). Todos os comandos foram re-executados por
mim, com saída literal colada. A suíte completa (gate) rodou 1× aqui.

## Conferência cruzada com log do Builder
Bate. mypy 6 (Builder relatou 6, com nota de que BLK-ACENTO-01 vira 7→6 por stubs pré-existentes em
arquivos intocados — sem regressão); ruff limpo; import ok; os 3 arquivos de teste = 66 passed
idêntico; git diff restrito aos arquivos declarados. O achado empírico da allowlist do `"?"` (PNG
embutido gera centenas de `0x3F` legítimos; sem imagens o classico sobra exatamente 1 `"?"` da URL
`/maps/search/?api=1&query=`) foi verificado no código do teste (5.2 gera SEM imagens; allowlist
mínima de 1 padrão, justificada). A pendência das legendas PIL foi honestamente sinalizada pelo
Builder — confirmo que é real e a avalio abaixo.

## Guardrails verificados
- score_priorizacao não alterado: sim (só LIDO/mencionado em prosa READ-ONLY dos créditos; nunca
  acentuado — `_credit_page`/`_classico_credit_page` mantêm `score_priorizacao` literal). Nenhum
  pipeline/artefato M1 tocado.
- Artefatos M1 preservados: sim. `git status` só lista censo_report.py, relatorio_municipal.py, os 3
  arquivos de teste, handoff e current_task; ZERO em pipelines/m1, config.py, censo_point.py,
  censo_map.py, data.py, pages.py, components.py, constants.py; nenhum parquet/data alterado.
- Identificadores preservados: `score_priorizacao`, `dominio_df` (nota de bairros), colunas de
  DataFrame, `template="classico"`, `METODO_RELATORIO_*`, `VERSAO_CONTRATO_MUNICIPAL`/
  `METODO_RELATORIO_MUNICIPAL` (não aparecem no diff), `_REDE_NOME_OVERRIDES` (marcas) — todos
  intactos. Verifiquei que o rótulo de zona "Âncora central" (valor de DISPLAY, não identificador)
  NÃO é usado como chave/`.isin()`/enum em lugar nenhum: `_ZONA_TEXTOS` é constante MORTA (só
  definida, nunca lida); `_ZONA_GEO_ROTULOS` e o `rotulos` local foram acentuados EM CONJUNTO
  (consistência exigida pelo plano, satisfeita). Separadores decimais `1.5 km`/`2.000`/`2.500`/`3.000`
  inalterados.
- Núcleo censo_* INTOCADO: `_ascii()` (assinatura + corpo `errors="replace"`) idêntica nos 2
  módulos; `setor_censitario_intersecao_area_1p5km`, raio 1,5 km, `RAIO_CENSITARIO_DEFAULT_KM`,
  `/Count 5` e `/Count 9`, grid de Big Numbers, marca d'água (BLK-EST-03), `set_compression(False)`,
  `pdf_version` — nenhum aparece no diff; só STRINGS/comentários mudaram.
- Teste anti-`"?"` significativo (não tautológico): 5.1 embrulha `_ascii` dos 2 módulos e valida
  `encode("latin-1", errors="strict")` de toda string que chega ao encoder, gerando os 3 PDFs com
  mapas reais (falha se um char fora de latin-1 for introduzido depois). 5.2 varre bytes crus com
  allowlist mínima (1 padrão de URL) e justificada. Ambos passam.
- Validações re-executadas pelo QA (sem bypass):
  - ruff check . → All checks passed!
  - mypy src → 6 erros pré-existentes (0 novos, 0 nos arquivos tocados)
  - import streamlit_app → import ok
  - pytest -q (serial, completa) → 1 failed (M2 pré-existente/ambiental) / 1434 passed / 2 skipped
  - pytest -q (3 arquivos do bloco) → 66 passed
  - housekeeping --check → FALHA "stub ausente" (pré-move correto)
  - scan de tipografia nas linhas add → 0 banidos / 0 non-latin1
- Escopo respeitado: sim. Nada além dos arquivos declarados no plano/handoff foi tocado. O Builder
  atualizou 3 (municipal) + 4 (pontual) asserts a MAIS que o plano previu — verifiquei que são
  legítimos (asserts de DADOS que dependem do rótulo/Big-Numbers acentuados, não edição às cegas).
- Parâmetros canônicos §3 (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, pesos
  renda=0.40/pop=0.60): inalterados, não tocados por este bloco.

## Recomendação sobre a pendência das legendas PIL (item 7) — EXPLÍCITA
Recomendo: ACEITAR COMO RESSALVA NÃO-BLOQUEANTE, com CORREÇÃO recomendada dobrada NESTE MESMO PR
antes do merge (não como bloco futuro separado). Não REPROVO por isso, mas também não dou APROVADO
limpo — daí "APROVADO COM RESSALVAS".
- Por que NÃO é bloqueador: (a) está FORA do escopo enumerado do plano (seções 2/3 listaram só
  strings via `_ascii`; as legendas não constavam); (b) é cosmético, numa legenda de mapa; (c) todo
  o resto do escopo está correto e verde; (d) a revisão visual humana (critério de aceite do próprio
  bloco) tende a capturá-la.
- Por que NÃO merece APROVADO limpo (e por que recomendo corrigir AGORA e não empurrar p/ FU): (a)
  é texto VISÍVEL no PDF e a regra §2 é "TODO texto voltado ao usuário acentuado" — a epic é
  literalmente sobre isso; (b) é INCONSISTENTE dentro do próprio commit: o dict `titulo` e o rodapé
  "Agregação H3" usam o MESMO caminho PIL (`_font`→arial.ttf, fallback `load_default`) e foram
  acentuados — logo a justificativa "risco de glifo em PIL" não separa as legendas das irmãs já
  acentuadas; (c) a correção é trivial: trocar `"Aprovado (dado proprio)"` → `"Aprovado (dado
  próprio)"` nos 2 literais idênticos `_COBERTURA_LEGENDA` (l.106) e `_RESUMO_LEGENDA` (l.112),
  e opcionalmente o comentário l.100 "Aprovado dado proprio"; (d) a branch acumula BLK-ACENTO-01+02
  num PR ÚNICO — é o momento certo para fechar o flanco sem abrir bloco novo. Se o orquestrador/
  humano preferir, é aceitável registrar como FU-01 do BLK-ACENTO-02, mas a via mais limpa é dobrar
  no mesmo PR.

## Decisão recomendada
Fechar o ciclo APÓS aplicar a correção trivial das 2 legendas PIL ("proprio"→"próprio") neste mesmo
PR (ou, alternativamente, registrar BLK-ACENTO-02-FU1 para a mesma correção). Housekeeping move
(`scripts/housekeeping_move_block.py BLK-ACENTO-02`) + commit por path são passos do orquestrador —
QA não move nem commita. Recomendo a revisão visual humana dos 3 PDFs antes do merge (critério de
aceite do backlog).
