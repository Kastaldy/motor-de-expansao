# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-PROD-03 — Avaliar hex_id como category com benchmark

## O que foi feito

Criado `scripts/benchmark_hexid_category.py` (novo arquivo) que:
1. Verifica cardinalidade de `hex_id` em 4 parquets de staging (todos 100% únicos)
2. Mede memória da coluna `hex_id` como string vs category via `memory_usage(deep=True)`
3. Mede mediana de 5 runs de merge (`brasil_priorizados` × `hexagonos_mercado_mapeado`)
4. Mede mediana de 5 runs de isin (1.000 ids em 1,54 M linhas)
5. Gera relatório `data/analysis/benchmark_hexid_category.md` (gitignored)

Script rodado com sucesso. Resultados reais:
- Cardinalidade: 100% única em todos os 4 parquets
- Merge: string 0.665 s vs category 0.751 s → **category +12.9% MAIS LENTO**
- isin: string 77 ms vs category 57 ms → category -25.6% (mas < 0.1 s absoluto, não é gargalo)
- Memória: string 94.1 MB vs category 132.3 MB → **category +38.1 MB MAIOR**

**Veredito: NO-GO confirmado.** Critério de ganho ≥ 15% não atingido em nenhuma operação
relevante de carga/join. Category é pior em merge e memória; o isin mais rápido não é gargalo.
Código de produção INALTERADO (zero diff em `src/`).

## Arquivos alterados

- `scripts/benchmark_hexid_category.py` — CRIADO (novo, 264 linhas)
- `tasks/completed.md` — Entrada BLK-PROD-03 adicionada ao final
- `tasks/current_task.md` — Status atualizado para "aguardando QA"
- `context/handoff.md` — Este arquivo (handoff Builder)
- `context/handoff/20260707-HHMMSS-builder.md` — Cópia de auditoria

## Validações executadas

- `python scripts/benchmark_hexid_category.py`: OK — relatório gerado, stdout formatado
- `python -m ruff check scripts/benchmark_hexid_category.py`: All checks passed
- `pytest -n auto -q tests/integration/test_streamlit_app.py`: 228 passed, 2 failed (falhas pré-existentes Plus Code — openlocationcode ausente, aceitas pelo escopo)
- `python scripts/loop_guard.py --base HEAD~1`: GUARD OK (4 caminhos, nenhum proibido)
- `git diff --ignore-cr-at-eol HEAD -- src/`: limpo (única modificação pré-existente em `dimensionamento/config.py` é CRLF→LF, não conteúdo)
- `data/analysis/benchmark_hexid_category.md` criado e verificado

## Problemas encontrados

- ruff: 2 issues auto-corrigidos (`tracemalloc` importado mas não usado; `timezone.utc` → `UTC` de `datetime`). Ambos corrigidos antes do commit.
- `datetime.UTC` (UP017) requer `from datetime import UTC` (não `datetime.UTC` diretamente) na versão Python 3.12 em uso.

## Pendências

Nenhuma.

## Riscos remanescentes

Nenhum. O script é read-only sobre staging; o relatório é gitignored.

## Guardrails verificados

- score_priorizacao não alterado: sim (zero mudança em src/)
- Artefatos M1 preservados: sim (só leitura de parquets de staging)
- Dashboard offline mantido: sim (nenhuma dependência de rede adicionada)
- loop_guard limpo: sim
- config.py M1 intocado: sim

## Resultados do benchmark (tabela)

| Operação | String | Category | Delta | Conclusão |
|---|---|---|---|---|
| Merge (priorizados × mercado, mediana 5 runs) | 0.665 s | 0.751 s | +12.9% | Category MAIS LENTO — NÃO aplica |
| isin (1.000 ids, mediana 5 runs) | 77.088 ms | 57.366 ms | -25.6% | lookup < 0,1 s (não é gargalo) |
| Memória hex_id mercado (coluna isolada) | 94.1 MB | 132.3 MB | +38.1 MB | Category USA MAIS — NÃO aplica |

**Decisão: NÃO converter hex_id para category. Manter string em todos os pipelines.**
