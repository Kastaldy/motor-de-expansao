# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
REVISÃO HUMANA (gate de decisões visuais — Felipe) → Builder

## Entendimento da tarefa
Refinar o visual/template do PDF (`censo_report.py`, fpdf2, 7 páginas 16:9) e dos 4 mapas PNG
(`censo_map.py`) do Relatório Pontual Censitário para um padrão mais profissional/clean estilo
GeoFusion, com escolhas A/B/C aprovadas por Felipe em gate, sem tocar conteúdo, scores, raio,
método de interseção, ordem/contagem das 7 páginas nem o grid 4×2 de 8 métricas READ-ONLY.

---

# PARTE A — PROPOSTA VISUAL PARA O GATE DE FELIPE

> Para cada decisão D1–D8 há 2–3 opções concretas com valores de knob exatos, o efeito visual
> esperado e a RECOMENDAÇÃO do Planner. Felipe escolhe **uma letra por decisão** (ou "manter
> atual"). Há também um **TEMA RECOMENDADO** (conjunto coeso) para aprovar em bloco.
> Nenhuma escolha é presumida pelo Planner — Felipe decide.

## Tema visual recomendado (aprovação em bloco) — "Ultra Clean / GeoFusion"
Conjunto coeso que combina entre si (clean, respiro, hierarquia clara, sem ruído):
**D1=B · D2=B · D3=B · D4=B · D5=C · D6=A · D7=C · D8=B.**
Resumo do efeito: capa com título maior e mais legível; cards de Big Numbers mais altos com
borda fina e contraste de rótulo; concorrentes com bullets coloridos Ultra×concorrente e
contagem total; Realização com logo Ultra e texto enxuto; mapas com títulos curtos (sem o
prefixo repetitivo), legenda com valor de corte por faixa e rodapé/atribuição enxutos.
Tudo dentro de Helvetica (zero dep de fonte nova) — risco de deploy nulo.

---

### D1 — Tipografia e hierarquia de títulos (`censo_report.py`)
Estado atual: faixa de título de conteúdo **Helvetica B 20 pt** (`_draw_title_band`, L225);
capa título **B 26 pt** (L306) + subtítulo **13 pt** (L310); Realização **B 30/16/12 pt** (L494–502).

- **A — Manter atual** (26/20/30·16·12). Efeito: sem mudança; hierarquia atual já funcional mas "tímida".
- **B — Reforço de hierarquia (RECOMENDADA).** Capa título **30 pt**; faixa de conteúdo **22 pt**;
  Realização **34/18/12 pt**. Banda de título sobe de `band_h=48` para **56 pt** e `set_xy(36,13)`→`(36,16)`.
  Efeito: títulos mais presentes e "executivos", respiro maior na banda, sem quebrar latin-1.
- **C — Discreto/elegante.** Capa **28 pt**; conteúdo **21 pt**; Realização **32/17/12 pt**; banda 52 pt.
  Efeito: incremento sutil, mais conservador que B.

Mockup banda de conteúdo (B):
```
████████████████████████████████████  (turquesa, 56 pt de altura)
  Populacao - Densidade            ← Helvetica B 22 pt, baseline y=16
████████████████████████████████████
```
**Nota técnica:** trocar de Helvetica para TTF (ex.: Inter/Montserrat via `add_font`) é POSSÍVEL mas
ADICIONA asset de fonte no repo/volume + risco de deploy (sem o `.ttf` no volume, fpdf2 falha no
`add_font`). **Planner desaconselha trocar de fonte neste ciclo** — ganho marginal, custo de OPS real.
Se Felipe quiser, é um sub-bloco próprio com asset versionado.

### D2 — Paleta de acento e cores de texto (`censo_report.py`, L58–62)
Estado atual: `ULTRA_TURQUESA=(0,167,157)`, `ULTRA_MAGENTA=(194,60,142)`, `_CINZA_TEXTO=(60,60,60)`,
card branco `_BRANCO`. Cards de Big Numbers alternam turquesa×magenta 1×1; valor grande na cor de acento.

- **A — Manter atual** (alternância 1×1, valor colorido no acento, rótulo cinza 60).
- **B — Contraste + legibilidade (RECOMENDADA).** Rótulo do card em **cinza mais escuro (45,45,45)**;
  **valor grande em cinza-escuro `(40,40,40)`** (não no acento) — o acento fica só na barra do topo;
  card sobre **branco puro** e fundo da página branco-gelo (diferenciação já existente). Alternância 1×1 mantida.
  Efeito: número fica mais legível (acento saturado em fonte 24 pt cansa); visual mais "dashboard limpo".
- **C — Acento sóbrio.** Manter valor colorido, mas só turquesa em TODOS os cards (sem magenta nos valores),
  magenta reservado só para títulos de seção (lista de redes). Efeito: paleta mais calma, menos "circo".

Mockup card (B):
```
┌──────────────────────┐
│▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔ (barra acento 6–8 pt)
│ Populacao total      │ ← rótulo cinza (45,45,45) 11 pt
│ no raio              │
│                      │
│ 12.480               │ ← valor cinza-escuro (40,40,40) B 24–26 pt
└──────────────────────┘
```

### D3 — Layout/dimensionamento dos cards de Big Numbers (`censo_report.py`, `_big_numbers_page`, L369–397)
Estado atual: grid 4×2, `card_h=150`, `gap=14`, `margin_x=36`, `top=70`, barra acento **8 pt**,
rótulo 11 pt, valor **B 24 pt**. Cards sem borda.

- **A — Manter atual.**
- **B — Cards com respiro + borda fina (RECOMENDADA).** `card_h=156`, `gap=16`, barra acento **6 pt**,
  valor **26 pt**, **borda 1 pt cinza-claro `(225,225,228)`** no retângulo do card (`draw rect style="D"`
  após o fill), rótulo sobe para `y+20`, valor desce para `y+88`. Efeito: cards "cartão" definidos,
  número maior, layout arejado — visual GeoFusion/Material.
- **C — Denso e chapado.** `card_h=146`, barra **10 pt**, valor **22 pt**, sem borda. Efeito: mais compacto,
  deixa folga vertical para a nota de fonte; menos "premium".

**Guardrail:** grid permanece **4×2 = 8 cards**, mesmas 8 métricas, mesma ordem — só geometria/estilo mudam.

### D4 — Layout da página de Concorrentes (`censo_report.py`, `_competitors_page`, L443–480)
Estado atual: mapa à esquerda (max 560×430), lista à direita de `x=620`. Título "Redes no raio de 1.5 km"
Magenta B 14 pt. Linhas Cinza 10 pt. Limite `points.head(10)` em `_point_rows`.

- **A — Manter atual** (60/40, 10 entradas, sem bullets, sem contagem total).
- **B — Bullets por tipo + contagem total (RECOMENDADA).** Mantém 60/40 e o teto de 10 LINHAS,
  mas: (i) **bullet colorido** antes de cada linha — turquesa `●` para Ultra, magenta `●` para
  concorrente (desenhado com `pdf.set_fill_color`+`pdf.ellipse`/`pdf.rect`); (ii) **cabeçalho mostra
  contagem total** quando houver mais de 10: `"Redes no raio de 1.5 km (N no total)"`, onde
  `N = n_concorrentes + len(ultra_raio)`; (iii) rodapé da lista "... e mais (N-10)" quando truncado.
  Efeito: leitura imediata de Ultra×concorrente e noção de volume real sem aumentar a lista.
- **C — Mais espaço para a lista (50/50).** Mapa max **480×430**, lista a partir de `x=540`, teto **14 entradas**.
  Efeito: cabe mais redes; mapa menor.

**Guardrail anti-PII:** `_point_rows` continua usando SÓ coluna de UNIDADE (`rede`/`nome_unidade`),
nunca dado de pessoa. Bullet/contagem não introduzem PII.

Mockup (B):
```
Redes no raio de 1.5 km (14 no total)    ← magenta B 14 pt
 ● Concorrente: SmartFit (0,82 km)        ← bullet magenta
 ● Concorrente: Bluefit (1,10 km)
 ● Ultra: Ultra Centro (0,40 km)          ← bullet turquesa
 ... e mais 4
```

### D5 — Página de Realização/Crédito (`censo_report.py`, `_credit_page`, L483–527)
Estado atual: fundo turquesa sólido (não usa asset), "Realizacao" B 30 pt (y=150), crédito B 16 (y=210),
método 12 pt (y=260, largura 640, C), nota READ-ONLY 12 pt (y=320), atribuição 9 pt (y=H-40).

- **A — Manter atual.**
- **B — Usar asset de capa como fundo.** `relatorio_capa_bg.png` em vez de turquesa sólido (consistência
  com a capa). Efeito: simetria abertura/fechamento; risco: o texto branco precisa cair na zona limpa
  do asset (mesma lógica de `_cover_page has_bg`). Mais frágil offline (fallback volta ao turquesa).
- **C — Logo Ultra + texto enxuto (RECOMENDADA).** Mantém turquesa sólido; **adiciona o logo Ultra**
  (`assets["logo"]`, fallback gracioso se ausente) centralizado no topo (y≈90, altura ~70 pt); encurta
  o método para 1 frase ("Interseção de setores censitários IBGE 2022 com círculo de 1,5 km; distribuição
  intrassetor por área."); bloco de texto sobe para começar em y≈210 (centro vertical melhor). Efeito:
  encerramento com marca, menos "muro de texto técnico", offline-safe (logo tem fallback).

### D6 — Títulos internos dos mapas PNG (`censo_map.py`, `_render_camada`, L479–481 e títulos L751–783)
Estado atual: título `_font(20)` em (28,22) + subtítulo `_font(12)` em (28,52) com
`Centro: lat,lng | raio | setores`. Títulos longos com prefixo "Relatorio Pontual Censitario - ...".

- **A — Títulos curtos, manter subtítulo técnico (RECOMENDADA).** Remover o prefixo repetitivo:
  `"Densidade populacional"`, `"Renda per capita"`, `"Score censitario"`, `"Concorrentes e Ultra"`
  (editar as 4 strings `titulo=` em `render_mapas_censitarios_combinados`). Manter subtítulo de
  coordenada/raio/setores (auditabilidade). Efeito: menos redundância com a faixa de título do PDF
  (que já diz a camada), mapa mais limpo. Tamanho 20 pt mantido (coerente com a banda do PDF).
- **B — Curto + sem subtítulo técnico no PDF.** Igual A, mas SUPRIME o subtítulo de coordenada/raio/setores
  no mapa (a coordenada já está na capa do PDF). Efeito: cabeçalho do mapa mais limpo; perde a auditoria
  inline no PNG. **Atenção:** o PNG também é exibido na UI do dashboard (mesma função) — suprimir tira a
  referência também da UI. Planner prefere A por isso.
- **C — Manter atual** (prefixo longo + subtítulo).

### D7 — Legenda dos mapas PNG (`censo_map.py`, `_draw_legend_camada`, L265–288; `legend_x=width-252` L484)
Estado atual: coluna fixa 252 px; "Legenda" `_font(14)` + nome `_font(11)`; amostras retângulo 22×16 px,
borda `(148,163,184)`; pins de referência ao final. Rótulos textuais (ex.: "1.001-5.000") sem o range numérico explícito ao lado.

- **A — Manter atual.**
- **B — Amostras arredondadas + separador de pins.** Trocar `draw.rectangle` por `draw.rounded_rectangle`
  (radius 4) nas amostras; desenhar uma **linha separadora** fina antes do bloco de pins. Efeito: legenda
  mais "card", separação clara faixas×pins.
- **C — Faixas com corte explícito + separador (RECOMENDADA).** Os rótulos das bands JÁ trazem o range
  (DENSIDADE/RENDA bands têm label tipo "1.001-5.000"); para SCORE, `_score_legend_entries()` já gera
  "0-20","20-40"... — então a melhoria é: (i) **separador fino** faixas×pins (como B); (ii) **amostra
  arredondada** (radius 4); (iii) reduzir a coluna de `width-252` → **`width-236`** (devolve ~16 px ao
  mapa, ajustando `_map_box` right de `width-285` → `width-269` em conjunto). Efeito: legenda elegante e
  um pouco mais de mapa. **Cuidado:** mexer em `_map_box` afeta projeção/escala — manter A na largura se Felipe
  preferir risco zero; o separador+arredondado é o ganho seguro.

### D8 — Barra de escala e rodapé dos mapas PNG (`censo_map.py`, `_draw_scale_bar` L227–243; rodapé L601–609)
Estado atual: escala tinta `_DARK_MAP_INK=(31,41,55)`, linha width 4; rodapé `_font(11)` cinza-slate em
y=H-34 com método **+ atribuição** ("Metodo: ... Render em EPSG:3857. Fundo de ruas: CartoDB Voyager...").

- **A — Manter atual.**
- **B — Rodapé enxuto, atribuição mantida só onde exigida (RECOMENDADA).** Encurtar o rodapé do PNG para
  `"Raio 1,5 km · EPSG:3857 · (c) OpenStreetMap, (c) CARTO"` (atribuição CARTO é EXIGIDA pela licença →
  permanece no PNG quando há basemap; some no fallback offline). Barra de escala: manter tinta escura,
  **engrossar para width 5** e box de fundo branco semi-translúcido atrás do label para legibilidade sobre
  o choropleth. Efeito: rodapé limpo, escala legível sobre qualquer cor. Mantém conformidade DEC-004.
- **C — Atribuição só no PDF.** Remover a atribuição do rodapé do PNG (deixar só método curto) já que o PDF
  tem `_ATRIBUICAO_TILES` no footer. **Planner desaconselha:** o PNG é exibido isolado na UI; a licença CARTO
  pede atribuição onde o tile aparece. Manter atribuição no PNG quando `drew_basemap=True`.

---

# PARTE B — PLANO TÉCNICO PARA O BUILDER (condicional ao gate)

> O Builder **NÃO executa** até o gate de Felipe ser aprovado. As letras escolhidas (D1..D8) serão
> fixadas no handoff pós-gate. Cada passo abaixo referencia a decisão e é neutro à opção, com o
> mapeamento "se X → muda linha Y". Todas as mudanças são **knobs/geometria/strings** dentro das
> funções já existentes — zero mudança estrutural, zero novo arquivo, zero dep nova (salvo D1=fonte TTF,
> que está DESACONSELHADO e exigiria sub-bloco de asset).

## Passos — `src/motor_expansao/dashboard/censo_report.py`

1. **(D1) Tipografia.** Em `_draw_title_band` (L219–227): `band_h` (L221) e `pdf.set_font("Helvetica","B",N)`
   (L225) — `N`: A=20 / B=22 / C=21; `band_h`: A=48 / B=56 / C=52; ajustar `set_xy(36, ⌊(band_h-24)/2⌋)`.
   Em `_cover_page` (L306): título `B 26` → A=26 / B=30 / C=28. Em `_credit_page` (L494/498/502): 30/16/12 →
   A=30·16·12 / B=34·18·12 / C=32·17·12. **NÃO** trocar para TTF salvo decisão explícita de fonte (ver Risco T).

2. **(D2) Paleta/cores de texto.** Em `_big_numbers_page` (L389–397): se **B** → rótulo
   `set_text_color(45,45,45)` (L389) e valor `set_text_color(40,40,40)` no lugar de `*accent` (L394);
   se **C** → `accents=[ULTRA_TURQUESA]` apenas (L375) e valor mantém `*accent`. Se **A** → sem mudança.
   `_CINZA_TEXTO`/`ULTRA_*` (L58–62) permanecem definidos (usados em rodapé/título); só a aplicação muda.

3. **(D3) Cards Big Numbers.** Em `_big_numbers_page` (L369–397): `card_h` (L374), `gap` (L371),
   barra acento `pdf.rect(x,y,card_w,8.0,...)` (L387), valor `B 24` (L395), offsets `y+22`/`y+84`.
   Se **B** → `card_h=156`, `gap=16`, barra `6.0`, valor `26`, rótulo `y+20`, valor `y+88`, e **após o
   fill branco (L384)** adicionar `pdf.set_draw_color(225,225,228); pdf.rect(x,y,card_w,card_h,style="D")`.
   Se **C** → `card_h=146`, barra `10.0`, valor `22`, sem borda. **Manter as 8 entradas de `cards` (L358–367)
   e a ordem INTACTAS** (contrato READ-ONLY). Reconferir que a nota de fonte (L400–412) ainda cabe abaixo do
   grid com o novo `card_h` (o `top + rows*(card_h+gap)` em L402 reposiciona automaticamente).

4. **(D4) Concorrentes.** Em `_competitors_page` (L443–480) e `_point_rows` (L416–429):
   - Se **B** → (a) cabeçalho L465: compor `"Redes no raio de 1.5 km"` + sufixo `f" ({total} no total)"`
     quando `total>10`, onde `total = n_conc + n_ultra` (contar `len` dos DataFrames `concorrentes_raio`/`ultra_raio`
     do `result`, com guarda de `None`/empty). (b) Antes de cada `multi_cell` da lista (L477), desenhar bullet:
     `pdf.set_fill_color(*ULTRA_TURQUESA if é_ultra else *ULTRA_MAGENTA); pdf.ellipse(list_x, y+4, 6, 6, "F")`
     e recuar o texto em `list_x+12`. Para saber o tipo, `_point_rows` deve devolver `(texto, is_ultra)`
     (ou prefixar label) — alterar `_point_rows` para retornar tuplas e ajustar o loop. (c) Se a lista truncar
     em 10, acrescentar linha `"... e mais {total-10}"`.
   - Se **C** → `max_w=480` (L448), `list_x=540` (L460), `points.head(14)` em `_point_rows` (L425).
   - **Anti-PII:** manter `name_col` restrito a `("rede","nome_unidade","nome","brand")` (L421) — não tocar.

5. **(D5) Realização.** Em `_credit_page` (L483–527):
   - Se **C (recomendada)** → assinatura passa a usar `assets` (já recebido): no topo, se `assets.get("logo")`,
     `pdf.image(BytesIO(assets["logo"]), x=(_PAGE_W-160)/2, y=90, w=160)` (fallback gracioso: pular se None);
     encurtar o `multi_cell` de método (L504–512) para 1 frase; subir blocos (`y=210`/`y=270`). Texto novo
     deve ser `_ascii(...)`-safe (sem caractere fora de latin-1).
   - Se **B** → trocar `set_fill_color+rect` (L490–491) por `_draw_full_page_background(pdf, assets.get("capa"), ULTRA_TURQUESA)`
     e mover o bloco de texto para a zona limpa (replicar a lógica `has_bg` de `_cover_page`).
   - **Manter** a nota READ-ONLY (L514–523) e a atribuição (L525–527) — contrato/§4.

6. **(Sanidade pós-edição).** Confirmar que `gerar_pdf_relatorio_pontual_censitario` (L548–590) segue
   chamando as 7 funções na ordem Capa→densidade→renda→score→Concorrentes→Big Numbers→Realização
   (L574–580) e que a marca d'água em todas as páginas (L585–587) permanece. **NÃO** mexer em
   `PDF_SECTION_HEADERS` (L18–26), `set_compression(False)` (L198), `pdf_version="1.4"` (L197).

## Passos — `src/motor_expansao/dashboard/censo_map.py`

7. **(D6) Títulos dos mapas.** Em `render_mapas_censitarios_combinados`, editar as 4 strings `titulo=`
   (L751, L759, L769, L777): se **A/B** → remover prefixo `"Relatorio Pontual Censitario - "` deixando
   `"Densidade populacional"`/`"Renda per capita"`/`"Score censitario"`/`"Concorrentes e Ultra"`.
   Se **B** → adicionalmente, em `_render_camada` (L480–481), tornar o subtítulo condicional (parâmetro
   `mostrar_subtitulo: bool = True`) e passar `False` na orquestradora — mas Planner recomenda **A**
   (mantém subtítulo, pois o PNG também serve a UI). Se **C** → sem mudança.

8. **(D7) Legenda.** Em `_draw_legend_camada` (L265–288): se **B/C** → trocar `draw.rectangle` (L281) por
   `draw.rounded_rectangle([x,yy,x+22,yy+16], radius=4, fill=color[:3], outline=(148,163,184))`; e antes do
   bloco de pins (L284), desenhar separador `draw.line([(x, yy_sep),(x+200, yy_sep)], fill=(210,214,222), width=1)`.
   Se **C** → também reduzir a coluna: `legend_x = width - 236` (L484) **em conjunto** com `_map_box`
   right `width - 285` → `width - 269` (L107) para manter a folga. **Atenção (risco G):** `_map_box`
   alimenta `_map_inner_dims`, projeção e `frame_*` — qualquer alteração aqui muda escala; só aplicar a
   parte de `_map_box` se Felipe escolher **C** explicitamente; caso contrário manter `legend_x`/`_map_box`
   e aplicar só arredondado+separador (subset seguro de C).

9. **(D8) Escala/rodapé.** Em `_draw_scale_bar` (L227–243): se **B** → `width=5` nas linhas (L239–241) e,
   antes do label (L243), desenhar um retângulo branco semi-translúcido atrás do texto
   (`draw.rectangle([x0-2, y0+5, x0+px_len, y0+22], fill=(255,255,255,180))`). No rodapé (L601–609):
   se **B** → encurtar `footer` para `"Raio 1,5 km · EPSG:3857 · " + (_atribuição quando drew_basemap else "fundo offline")`.
   Se **C** → remover a atribuição do PNG (DESACONSELHADO — manter quando `drew_basemap=True`).
   **Manter** a regra: atribuição CARTO presente sempre que `drew_basemap=True` (licença DEC-004).

## Passos — testes e docs

10. **Testes.** Atualizar asserts SÓ onde o texto/contagem mudar:
    - `tests/unit/test_relatorio_pontual_censitario_export.py`: `test_pdf_footer_cita_voyager` (L256) e
      `test_pdf_atribuicao_de_tiles_no_rodape` (L247) podem depender de strings — ajustar se D8/D5 mudarem
      o texto. **NÃO** relaxar `test_pdf_sem_pii_de_pessoas` (L195), `/Count 7` (L138/190/217/231),
      `PDF_SECTION_HEADERS` (L135/191), `test_pdf_marca_dagua_*` (L206–247). Se D4=B mudar o cabeçalho da
      lista, adicionar/ajustar assert de "no total"/bullet sem quebrar anti-PII.
    - `tests/unit/test_relatorio_pontual_censitario_mapa.py`: se D6 mudar títulos, ajustar qualquer assert
      que cheque a string de título; manter os asserts de faixas fixas, choropleth na camada `score`,
      pins, fallback offline (`basemap=False`) e estado vazio.
    - Adicionar 1–3 testes novos focados na decisão aprovada (ex.: borda do card em D3=B; bullet/contagem
      em D4=B), sempre com `basemap=False`.

11. **Docs.** Após o gate, refletir as mudanças aprovadas em `docs/relatorio_pontual_censitario.md` §6/§7
    e em `CLAUDE.md §4` (linha de descrição visual do relatório), citando o bloco BLK-EST-02 e as letras
    escolhidas. Não alterar contratos de score/raio/método.

## Sequência de execução sugerida ao Builder (pós-gate)
(1) editar `censo_report.py` D1–D5 → rodar export tests; (2) editar `censo_map.py` D6–D8 → rodar mapa
tests; (3) atualizar/adicionar testes; (4) ruff+mypy; (5) `import streamlit_app`; (6) docs. Commit por path.

## Arquivos afetados
- `src/motor_expansao/dashboard/censo_report.py` (D1–D5)
- `src/motor_expansao/dashboard/censo_map.py` (D6–D8)
- `tests/unit/test_relatorio_pontual_censitario_export.py` (asserts D1–D5)
- `tests/unit/test_relatorio_pontual_censitario_mapa.py` (asserts D6–D8)
- `docs/relatorio_pontual_censitario.md` §6/§7 e `CLAUDE.md §4` (registro pós-gate)
- (condicional, só se D5=C/B com logo) asset `data/ultra/logo_ultra.png` já esperado por `_load_branding_assets` (gitignored, fallback gracioso)

## Dependências
- **Gate humano de Felipe** (escolha das letras D1–D8) ANTES do Builder — bloqueante.
- Helvetica core do fpdf2 (latin-1) — sem dep nova no tema recomendado.
- `assets["logo"]` para D5=C — fallback gracioso se ausente (sem quebra offline).

## Riscos técnicos
- **T (fonte).** Trocar Helvetica→TTF (D1) adiciona asset de fonte + risco de deploy (`_font()`/`add_font`
  falham sem o `.ttf` no volume). DESACONSELHADO neste ciclo; se aprovado, vira sub-bloco com asset versionado.
- **latin-1.** Todo texto novo deve passar por `_ascii()` (já é o padrão); evitar caracteres fora de latin-1.
- **G (`_map_box`).** Alterar `_map_box`/`legend_x` (D7=C largura) muda projeção/escala dos mapas; aplicar
  só se Felipe escolher C com largura; subset seguro (arredondado+separador) não toca `_map_box`.
- **Anti-PII.** `_point_rows` e qualquer texto novo (D4/D5) só podem usar dados de UNIDADE; `image24` jamais
  embutida; `.pptx`/PDF nunca versionados; `set_compression(False)` preservado (auditabilidade).
- **Tamanho do PDF.** Borda/logo extra é desprezível; compressão OFF mantida (PDFs continuam pequenos).
- **Ciclo de feedback lento.** Cada iteração visual exige rebuild de imagem + redeploy por digest na VPS
  (guardrail §6) e cópia manual de assets gitignored ao volume — passo de OPS fora deste ciclo de código.

## Critérios de aceite finais
- Letras D1–D8 aprovadas por Felipe no gate e fixadas no handoff pós-gate.
- PDF com **7 páginas** (`/Count 7`) e os 7 `PDF_SECTION_HEADERS` presentes; ordem inalterada.
- Big Numbers grid **4×2 = 8 métricas** READ-ONLY, mesmas métricas/ordem (só estilo muda).
- Geração offline-safe: `basemap=False` e ausência de assets → PDF válido sem exceção.
- Anti-PII: bytes crus sem PII; `image24` não referenciada; `set_compression(False)` mantido.
- `pytest -n auto` verde; ruff+mypy limpos nos arquivos tocados.
- READ-ONLY M1: diff VAZIO em pipelines/scoring/config/artefatos oficiais.

## Validações obrigatórias
- Comando: `python -m pytest -q tests/unit/test_relatorio_pontual_censitario_export.py tests/unit/test_relatorio_pontual_censitario_mapa.py` — Critério: 0 falhas.
- Comando: `python -c "import streamlit_app"` — Critério: import sem erro.
- Comando: `ruff check src/motor_expansao/dashboard/censo_report.py src/motor_expansao/dashboard/censo_map.py` e `mypy` nos mesmos — Critério: limpos.
- Comando: `python -m pytest -n auto -q` (gate único no QA) — Critério: 0 falhas.
- Verificação: PDF com 7 páginas confirmada (`/Count 7`) e geração com `basemap=False` ok.
- Verificação: `git diff --stat` sem mudança em `src/motor_expansao/pipelines/`, `config.py`, scoring ou `data/outputs/*` oficiais (READ-ONLY M1).

## Fora de escopo
- Recalcular/escrever qualquer score (`score_priorizacao`, `hex_score_estrutural`,
  `score_setor_2022_calibrado`, residual, SAM, carteira, plano, artefatos oficiais).
- Método `setor_censitario_intersecao_area_1p5km` e raio 1.5 km (INTOCADOS).
- Remover/reordenar páginas ou alterar campos/contagem do Big Numbers.
- `pages.py`, `censo_point.py` (motor) — fora do escopo visual.
- Dependência de API ao vivo no dashboard interativo (DEC-004 mantida: tiles só na geração).
- Troca de fonte TTF (DESACONSELHADA; sub-bloco próprio se Felipe quiser).
- Deploy/OPS na VPS (rebuild+redeploy+cópia de assets) — passo gated §6 fora deste ciclo de código.

## Alerta de aprovação
ATENÇÃO: este bloco tem **GATE HUMANO OBRIGATÓRIO** de decisões visuais (Felipe) APÓS o Planner e
ANTES do Builder. O Builder NÃO inicia até Felipe escolher as letras D1–D8 (ou "manter atual" por
decisão). Embora a criticidade seja Média, a esteira exige revisão humana das escolhas visuais
(precedente BLK-CENSO-02/03). NENHUMA mudança toca o M1 — se algum passo derivar para score/artefatos
oficiais, PARAR e escalar com ALERTA EM MAIÚSCULAS + aprovação humana.

## Guardrails ativos
- READ-ONLY sobre o M1 (§2, §5): zero escrita em `score_priorizacao`, `hex_score_estrutural`,
  `score_setor_2022_calibrado`, residual, SAM, carteira, plano, artefatos oficiais.
- Anti-PII §4: `.pptx`/PDF nunca versionados; `image24.png` NUNCA embutido; `set_compression(False)` preservado.
- DEC-004: basemap online só na geração do relatório; fallback offline `basemap=False` obrigatório.
- Método `setor_censitario_intersecao_area_1p5km` e raio 1.5 km INTOCADOS.
- 7 páginas (ordem Capa→Pop→Renda→Score→Concorrentes→Big Numbers→Realização) e Big Numbers grid 4×2
  de 8 métricas READ-ONLY são CONTRATOS invioláveis.
- Guardrail §6 SSH: nenhum comando no VPS sem confirmação explícita do usuário.
- Paths pré-sujos NÃO commitar: `data/outputs/setores_censitarios_2022_geo/_metadata.json`,
  `data/reports/relatorio_pontual_censitario_base_geo.md`.
