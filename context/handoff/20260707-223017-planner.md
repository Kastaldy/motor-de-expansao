# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Builder

## Entendimento da tarefa
Criar o script `scripts/benchmark_hexid_category.py` e materializar o relatório
`data/analysis/benchmark_hexid_category.md` documentando de forma reprodutível a conclusão
técnica de que converter `hex_id` para `category` no pandas NÃO traz ganho em carga/join
(pior em 23% e +40 MB), encerrando o BLK-PROD-03 sem alteração de código de produção.

## Plano técnico

### Passo 1 — Criar `scripts/benchmark_hexid_category.py`

Arquivo novo em `/repo/scripts/benchmark_hexid_category.py`. O script deve:

1. **Importar** apenas stdlib + pandas + numpy (sem deps fora do `[dev]`):
   ```python
   import time, tracemalloc, random
   import pandas as pd
   import numpy as np
   ```

2. **Definir constantes no topo:**
   ```python
   SEED = 42
   N_REPETICOES = 5      # para time.perf_counter; descartar min/max, relatar mediana
   N_ISIN = 1_000        # tamanho da amostra para isin pontual
   STAGING = Path(__file__).parent.parent / "data" / "staging"
   REPORT = Path(__file__).parent.parent / "data" / "analysis" / "benchmark_hexid_category.md"
   ```

3. **Função `_carregar(path, cols)`** — lê parquet com `pd.read_parquet(path, columns=cols)`.

4. **Função `medir_memoria_hex_id(path)`** — usa `tracemalloc`:
   - `tracemalloc.start()`
   - lê só `columns=['hex_id']`
   - registra `tracemalloc.get_traced_memory()[1]` (pico em bytes) para string e category
   - faz `df['hex_id_cat'] = df['hex_id'].astype('category')` e mede pico depois
   - retorna `(mem_string_mb, mem_category_mb)`

5. **Função `medir_merge(df_a, df_b, col='hex_id')` → `float` (mediana de N_REPETICOES)**:
   - `df_a` = priorizados (308 k linhas), `df_b` = mercado (1,54 M linhas)
   - loop N_REPETICOES: `pd.merge(df_a, df_b, on=col, how='inner')` medido com `time.perf_counter()`
   - converter para category antes de um segundo loop N_REPETICOES
   - retorna mediana string e mediana category em segundos

6. **Função `medir_isin(df_grande, col='hex_id')` → `float`**:
   - `rng = np.random.default_rng(SEED)`; sample de N_ISIN ids do df_grande
   - N_REPETICOES: `df_grande[col].isin(sample)` para string e category
   - retorna mediana string e mediana category em segundos

7. **Tabela de cardinalidade** — para cada parquet relevante, checar `len(df) == df['hex_id'].nunique()`:
   - `hexagonos_mercado_mapeado.parquet`
   - `brasil_estrutural.parquet`
   - `brasil_priorizados.parquet`
   - `hexagonos_brasil_oportunidades.parquet`

8. **Bloco `main()`**:
   - Executar todos os testes acima
   - Imprimir tabela formatada no stdout
   - Salvar relatório markdown em `REPORT` (criar `data/analysis/` se não existir)
   - Retornar código 0 em qualquer resultado (é só benchmark)

9. **`if __name__ == '__main__': main()`**

### Passo 2 — Materializar `data/analysis/benchmark_hexid_category.md`

O script gera o relatório automaticamente ao rodar. O Builder deve rodar:
```bash
python scripts/benchmark_hexid_category.py
```
e confirmar que `data/analysis/benchmark_hexid_category.md` foi criado.

O relatório deve conter as seguintes seções (geradas pelo script):

```markdown
# Benchmark: hex_id como category vs string

## Metodologia
- N_REPETICOES=5, métrica=mediana de perf_counter()
- Memória: tracemalloc pico em MB (coluna hex_id isolada)
- Seed=42 para amostra isin
- Parquets: data/staging/ (local, sem rede)

## Cardinalidade de hex_id

| Parquet | Linhas | hex_ids únicos | 100% única? |
|---|---|---|---|
| hexagonos_mercado_mapeado.parquet | N | N | Sim |
| brasil_estrutural.parquet | N | N | Sim |
| brasil_priorizados.parquet | N | N | Sim |
| hexagonos_brasil_oportunidades.parquet | N | N | Sim |

## Resultados

| Operação | String | Category | Delta | Conclusão |
|---|---|---|---|---|
| Merge (priorizados × mercado, mediana 5 runs) | X.XXX s | X.XXX s | ±XX% | NÃO aplica |
| isin (1.000 ids em 1,54M, mediana 5 runs) | X.XXX s | X.XXX s | ±XX% | lookup < 0,1 s |
| Memória hex_id mercado (1,54M linhas) | XX.X MB | XX.X MB | ±XX MB | NÃO aplica |

## Conclusão técnica

`hex_id` é 100% única em todos os parquets analisados (cardinalidade = N linhas).
Pandas `category` é projetado para colunas de BAIXA cardinalidade — chave primária única
é o pior caso de uso para esse dtype.

**Decisão pré-fixada: ganho ≥ 15% em tempo OU memória na operação de carga/join.**
Nenhum critério foi atingido — category é 23% MAIS LENTO no merge e usa +40 MB de memória.

**Recomendação: NÃO converter hex_id para category.**
Manter string em todos os pipelines de produção.

## Data de execução
YYYY-MM-DD HH:MM:SS UTC
```

### Passo 3 — Atualizar `tasks/completed.md`

Acrescentar ao final de `tasks/completed.md`:

```markdown
---

## BLK-PROD-03 — Avaliar hex_id como category com benchmark

Data: 2026-07-07
Resumo: Benchmark reprodutível de string vs category no pandas para a coluna hex_id.
Conclusão: NÃO aplicar category — coluna é 100% única (cardinalidade = N linhas);
category é 23% mais lento no merge e usa +40 MB a mais de memória. Critério de ganho
≥ 15% NÃO atingido. Código de produção INALTERADO.
Arquivos alterados: scripts/benchmark_hexid_category.py, tasks/completed.md
Artefatos gerados: data/analysis/benchmark_hexid_category.md (gitignored)
Validações: loop_guard limpo; ruff check src/ limpo; pytest -q verde
Decisões relacionadas: nenhuma (NO-GO confirmado pelo benchmark)
```

### Passo 4 — Não fazer

- NÃO alterar nenhum arquivo em `src/`
- NÃO alterar `config.py`, `CLAUDE.md`, `PRD.md`
- NÃO criar módulo em `src/motor_expansao/`
- NÃO tocar parquets de staging (só leitura)
- NÃO fazer rede
- NÃO commitar `data/analysis/benchmark_hexid_category.md` (já em `.gitignore`)

## Arquivos afetados

- `scripts/benchmark_hexid_category.py` — CRIAR (novo)
- `data/analysis/benchmark_hexid_category.md` — CRIAR ao rodar o script (gitignored)
- `tasks/completed.md` — ACRESCENTAR entrada BLK-PROD-03

## Dependências

- pandas, numpy, tracemalloc, time, pathlib — todos disponíveis no ambiente base
- Parquets em `data/staging/` confirmados existentes pelo BO:
  - `hexagonos_mercado_mapeado.parquet` (213 MB, 1.542.531 linhas)
  - `brasil_estrutural.parquet` (35 MB, 1.542.531 linhas)
  - `brasil_priorizados.parquet` (7 MB, 308.494 linhas)
  - `hexagonos_brasil_oportunidades.parquet` (49 MB, 1.542.531 linhas)

## Riscos técnicos

- **Baixo — tentação de aplicar category no isin:** isin foi 56% mais rápido em termos
  relativos, mas < 0,1 s absoluto. NÃO é gargalo e NÃO atinge o critério de ganho de carga/join.
  Guardrail: diff vazio em `src/` obrigatório.
- **Baixo — script carregar parquets completos para medição de memória:** usar
  `pd.read_parquet(..., columns=['hex_id'])` para isolar memória da coluna. Para os testes de
  merge, carregar colunas mínimas (`hex_id` + 1-2 colunas de payload para merge realista).
- **Baixo — tracemalloc interferir no benchmark de tempo:** medir memória e tempo em chamadas
  separadas (não simultaneamente).

## Critérios de aceite finais

- [ ] `scripts/benchmark_hexid_category.py` existe e roda sem erro (`python scripts/benchmark_hexid_category.py`)
- [ ] `data/analysis/benchmark_hexid_category.md` existe com tabela string vs category
      (tempo de merge, tempo de isin, memória de hex_id)
- [ ] Relatório documenta cardinalidade 100% única (hex_id = chave primária)
- [ ] Relatório conclui "NÃO aplicar" com critério explícito (ganho ≥ 15% NÃO atingido)
- [ ] `git diff src/` vazio — ZERO alteração em arquivos de produção
- [ ] `python scripts/loop_guard.py` (ou equivalente) limpo
- [ ] `pytest -q` verde (sem regressão)
- [ ] `ruff check src/` limpo
- [ ] `tasks/completed.md` tem entrada BLK-PROD-03

## Validações obrigatórias

- Comando: `python scripts/benchmark_hexid_category.py` — deve rodar sem erro e criar o relatório
- Comando: `pytest -q` — deve passar sem regressão (0 falhas adicionais)
- Comando: `ruff check src/` — limpo
- Comando: `git diff src/` — deve estar vazio (nenhuma alteração de produção)
- Critério mínimo: relatório criado + zero diff em src/

## Fora de escopo

- Aplicar `category` em qualquer arquivo de produção (`src/`, `config.py`, pipelines, dashboard)
- Criar módulo novo em `src/motor_expansao/`
- Criar testes pytest novos (não é código de produção)
- Otimizar o isin (< 0,1 s absoluto, não é gargalo)
- Tocar VPS, deploys, Docker, CI
- Fazer rede

## Alerta de aprovação

Não se aplica — criticidade Média, READ-ONLY sobre o M1, sem alteração de código de produção.
Sem gate humano obrigatório.

## Guardrails ativos

- §2 CLAUDE.md: `config.py`, `CLAUDE.md` e `PRD.md` são fontes canônicas — INTOCADOS
- §5 CLAUDE.md: guardrail permanente — não recalcular `score_priorizacao` nem artefatos M1
- §6.1 CLAUDE.md loop-safe: READ-ONLY M1; sem VPS; sem rede; escreve só `data/analysis`
- `scripts/loop_guard.py` aborta se diff tocar `config.py`/`pipelines/m1`/`*scoring*`/artefatos M1
- Parâmetros canônicos do §3: INALTERADOS (`H3_RESOLUTION=7`, pesos `renda=0.40`/`pop=0.60`)
