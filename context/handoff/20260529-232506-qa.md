# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (Passo 6.0: housekeeping via helper + commit/merge). Documentation Skill opcional (atualizar baseline pytest 542→541 no CLAUDE.md, fora do escopo do Builder).

## VEREDITO
APROVADO

## Justificativa
A FATIA-1 é uma migração mecânica de import-rewiring que preserva 100% dos valores M1 (4/4 hashes sha256 byte-idênticos), mantém a suíte verde (541 passed, 1 skipped, 0 failed), ruff limpo, mypy Success, `import streamlit_app` ok, e ambos os greps de import legado vazios. Os 3 desvios declarados pelo Builder foram auditados via `git diff` e são todos rewiring mínimo necessário (não expansão de escopo material): remoção de teste obsoleto dos wrappers removidos, reaponte de strings de `patch`, e correção de 1 bloco de import em 2 jobs/pipelines que o próprio Passo 7 quebrou.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- Ao fechar o ciclo, atualizar a baseline pytest documentada no CLAUDE.md (hoje "532 passed") para 541 (a real era 542 por merges BLK-OPS-09/10; cai p/ 541 pela remoção do teste obsoleto). Fora do escopo do Builder, como ele mesmo registrou.

## Testes faltantes
- nenhum. O único teste removido (`test_legacy_m1_wrappers_export_modules`) validava exatamente os wrappers de raiz que esta fatia remove por design; mantê-lo seria uma falha garantida. Os caminhos canônicos `motor_expansao.pipelines.m1.*` já são cobertos por `test_m1_pipeline_modules_importable` no mesmo arquivo e por toda a suíte reapontada.

## Riscos remanescentes
- `pythonpath = ["."]` mantido nesta fatia (jobs/ ainda depende). Mascara eventuais imports de raiz remanescentes; mitigado pelo grep explícito (vazio). Limpeza fica em BLK-ARCH-01a.
- `fora_primeira_fase/*` mantém `from config import settings` órfãos por design (não coletados). Esperado, não consertar.
- BLK-ARCH-01a (migração estrutural de `jobs/pipelines/*`, 21 módulos) permanece pendente no backlog.

## Saída literal das validações (re-executadas pelo QA)

### 1. pytest -q
```
541 passed, 1 skipped, 9 warnings in 93.23s (0:01:33)
```
(0 failed. Os warnings são FutureWarning/UserWarning de geopandas/pandas em jobs/pipelines, pré-existentes, não relacionados a esta fatia.)

### 2. python -c "import streamlit_app; print('ok')"
```
...
2026-05-29 23:23:24.094 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
ok
```
(Warnings de runtime do Streamlit em modo bare são esperados; imprimiu `ok`.)

### 3. ruff check .
```
All checks passed!
```

### 4. mypy src/
```
src\motor_expansao\pipelines\m1\ibge_censo.py:158: note: By default the bodies of untyped functions are not checked, consider using --check-untyped-defs  [annotation-unchecked]
src\motor_expansao\pipelines\m1\ibge_censo.py:159: note: ... [annotation-unchecked]
src\motor_expansao\pipelines\m1\poi_enrichment.py:51: note: ... [annotation-unchecked]
Success: no issues found in 23 source files
```
(Só notas `annotation-unchecked`, toleradas. Success.)

### 5a. rg legacy imports (excl. fora_primeira_fase/, context/)
```
$ rg -n "^\s*from (base_h3_brasil|hex_enrichment|fase1_bi_exports|ibge_censo|poi_enrichment|config) import" --glob '!fora_primeira_fase/**' --glob '!context/**'
EXIT1=1   (sem matches — VAZIO)
```

### 5b. rg dashboard imports (excl. context/)
```
$ rg -n "from dashboard(\.|\s+import)|import dashboard" --glob '!context/**'
EXIT2=1   (sem matches — VAZIO)
```

### 6. Hashes sha256 dos 4 artefatos M1 (re-calculados pelo QA)
```
c226954945ad0757a0429c84c43f410492c0ea7d15ca1d9b6a15f68727806567 data/staging/brasil_priorizados.parquet
7baa07a2cbc0b7d8f2a8878932cae0ebf9400fce00e9c48c366c9903215f131b data/staging/brasil_estrutural.parquet
805c65e28adfd800c7d5524e73fa9b0a044b992f17d617d0f2c6e40f9bbf61ca data/staging/hexagonos_brasil_oportunidades.parquet
0cfb1015fc9df8b63776eb07ae3e666766905f768a4a78a406ae4d6b7cb6f618 data/outputs/hexagonos_brasil_dashboard.parquet
```
Comparação com o Passo 0 do Builder: 4/4 BYTE-IDÊNTICOS. Não-mutação M1 provada.

### Params canônicos (grep src/motor_expansao/config.py + core/constants.py)
```
config.py:59  H3_RESOLUTION: int = 7
config.py:60  DIST_MIN_ULTRA_KM: float = 1.0
config.py:61  RENDA_MIN: float = 4500.0
config.py:77  AREA_MIN_M2: float = 1200.0
(+ defaults espelhados em 124-140)
core/constants.py:11  H3_RESOLUTION = settings.H3_RESOLUTION
core/constants.py:32  "renda_per_capita": 0.40,
core/constants.py:33  "populacao_proxy": 0.60,
```
`config.py` é rename PURO (git diff HEAD do par config.py↔src/.../config.py = vazio; nenhum valor tocado).

## Auditoria dos 3 desvios

**Desvio 1 — remoção de `test_legacy_m1_wrappers_export_modules` (LEGÍTIMO).**
`git diff HEAD tests/unit/test_motor_expansao_import.py` mostra que o teste removido importava exatamente `base_h3_brasil`, `fase1_bi_exports`, `hex_enrichment` (os 3 wrappers de raiz) e asseverava `callable(...main)`. Validava SÓ os wrappers que a fatia remove por design. Removê-lo é correto; mantê-lo falharia. Contagem 542→541 explicada e correta.

**Desvio 2 — reaponte de strings `patch(...)`/imports em 3 arquivos de teste (LEGÍTIMO).**
`git diff HEAD` em `test_fontes_gratuitas.py`, `test_base_h3_brasil.py`, `test_hex_enrichment_brasil.py`: TODAS as linhas alteradas são rewiring mecânico de `hex_enrichment`/`poi_enrichment`/`ibge_censo`/`base_h3_brasil` → `motor_expansao.pipelines.m1.*`, incluindo os alvos de `patch(...)` (que resolvem por string e DEVEM acompanhar a nova localização do símbolo). Mesmos símbolos, mesmos mocks, mesma lógica — só o caminho mudou. Nenhuma assertion/return/valor alterado.

**Desvio 3 — edição de import em 2 módulos `jobs/pipelines/*` (ACEITÁVEL — rewiring mínimo forçado).**
(a) `git diff HEAD` de `fase_a_censo2022_setores.py` e `teste_setor_censitario_2010.py`: a mudança é APENAS a substituição do bloco try/except de import por `from motor_expansao.pipelines.m1.hex_enrichment import (...)`. Nada estrutural/lógico; `log = structlog.get_logger()` e o resto intactos.
(b) NECESSIDADE comprovada: os dois branches do try/except antigo referenciavam `from hex_enrichment import` (wrapper de raiz REMOVIDO no Passo 7) e `from jobs.pipelines.hex_enrichment import` — e confirmei via `ls` que NENHUM dos dois módulos existe no working tree (`jobs/pipelines/hex_enrichment.py` ausente; raiz `hex_enrichment.py` ausente). Logo, sem a edição, ambos os caminhos do fallback levantariam ModuleNotFoundError. Esses 2 módulos jobs são importados por 4 testes EM ESCOPO (`tests/unit/test_pop_censo_v0001.py`, `tests/integration/test_fase_a_censo2022.py`, `test_validar_fase_a_censo2022.py`, `test_teste_setor_censitario_2010.py`) — a coleta da suíte QUEBRARIA sem a correção. (Esses testes inclusive aparecem rodando na saída do pytest.)
(c) VEREDITO sobre o Desvio 3: ACEITÁVEL. A regra "não expandir escopo" proíbe MOVER/REESTRUTURAR `jobs/pipelines/*` (isso continua integralmente em BLK-ARCH-01a, intocado). O que o Builder fez foi corrigir 1 bloco de import que o PRÓPRIO Passo 7 (remoção do wrapper) tornou inválido — rewiring necessário, aditivo e mínimo, para manter a suíte verde, não uma expansão de escopo gratuita. Reprovar por isso penalizaria a única ação que mantém a barra "CI verde a cada passo". Justo e rigoroso: APROVADO com registro explícito de que a migração estrutural de jobs/ NÃO ocorreu.

## Dry-check de housekeeping
- `scripts/housekeeping_move_block.py` existe; `tests/unit/test_housekeeping_helper.py` existe e passou (incluído nos 541).
- `### BLK-ARCH-01` presente em `tasks/backlog.md:115` (heading do bloco confirmado).
- Execução: `python scripts/housekeeping_move_block.py BLK-ARCH-01 --check` →
  ```
  FALHA --check: stub ausente no backlog para BLK-ARCH-01
  EXITCODE=1
  ```
- Interpretação: exit 1 (PENDENTE, stub ainda ausente) = o move ainda NÃO ocorreu — ESPERADO neste momento. O helper RECONHECE BLK-ARCH-01 como bloco existente no backlog: NÃO retornou EXIT_AD_HOC=3 (que só ocorre no modo MOVE quando `BlockNotFound` é levantado, i.e. heading ausente). O move em si é responsabilidade do Passo 6.0 do orquestrador no FECHAMENTO, não do QA.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico de produção. Esta fatia NÃO envolve tooling SOPS/segredos — a regra de no-bypass de tooling é trivialmente N/A aqui. O uso de `tests/fixtures/` pela suíte pytest é fixture legítima de teste unitário (isolamento de lógica), explicitamente NÃO classificado como bypass pelo prompt. Hashes recalculados pelo QA sobre os artefatos reais do working tree (não fixture).

## Conferência cruzada com log do Builder
Bate. pytest 541/1 (igual), 4 hashes idênticos aos registrados ANTES/DEPOIS pelo Builder, ruff limpo, mypy Success em 23 arquivos, import ok, ambos os greps vazios. Os 3 desvios declarados conferem com o `git diff` real (nada estrutural fora do declarado; `git status` confirma só os arquivos listados + os 3 snapshots de handoff e current_task/handoff esperados).

## Guardrails verificados
- score_priorizacao não alterado: sim (nenhuma lógica/peso/parâmetro tocado; pesos 0.40/0.60 intactos)
- hex_score_estrutural não alterado: sim
- Artefatos M1 preservados: sim (4/4 sha256 byte-idênticos pré/pós)
- H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0: sim (config.py rename puro; constants.py só trocou import morto)
- Validações re-executadas pelo QA (sem bypass): pytest→541/1/0fail | import streamlit_app→ok | ruff→limpo | mypy src/→Success | grep legacy→vazio | grep dashboard→vazio | hashes→idênticos
- Escopo respeitado: sim — `jobs/pipelines/*` NÃO foi movido/reestruturado (só 2 linhas de import corrigidas, forçadas pela remoção do wrapper); migração estrutural permanece em BLK-ARCH-01a.

## Decisão recomendada
Fechar ciclo. No fechamento (Passo 6.0), executar `python scripts/housekeeping_move_block.py BLK-ARCH-01 --date 2026-05-29` (modo move) via helper versionado, validar `--check` OK + byte-identidade contra `git show HEAD:tasks/backlog.md`, e então commit/merge. Nenhum bloco de correção necessário.
