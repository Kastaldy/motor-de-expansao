# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
**Builder** (criticidade Média; D1/D2 já fechadas, D3 fechada aqui — o Builder não decide escopo).

## Entendimento da tarefa
Desacoplar, nos dois painéis de hexágono do Relatório Pontual (`socioeconomia` e `residual`, ambos
gerados por `_render_camada_residual_hex`), o raio do **círculo azul desenhado** (passa a 1,5 km) do
raio de **enquadramento do mapa** (permanece 5 km), atualizando o rótulo do rodapé para o texto fechado
por Vinicius e mantendo motor censitário, `config.py`, `_RESIDUAL_GRID_DISK_K`, grid 2x2, satélite e
concorrentes **INTOCADOS**. READ-ONLY sobre o M1.

---

## 0. Decisões (estado)

- **D1 — FECHADA (orquestrador).** Constante de RENDER nova `RAIO_CIRCULO_DISPLAY_KM = 1.5` em
  `censo_map.py`. Não reusar `RAIO_CENSITARIO_DEFAULT_KM`.
- **D2 — FECHADA (Vinicius, produto).** Rodapé dos dois painéis, texto **IMUTÁVEL**:
  `Raio 1,5 km - escala 5 km - EPSG:3857 - fundo de ruas offline`
  (variante online: `... - EPSG:3857 - (c) OpenStreetMap, (c) CARTO`). Mecanismo escolhido pelo
  Planner: **reusar o parâmetro `rotulo_escala=` que `_render_camada` já aceita** (mesmo caminho da
  camada `entorno`, `censo_map.py:700/872/1196`), injetando uma constante de módulo **derivada das duas
  constantes de raio** — ver passo 2. Motivo: não mexe no cálculo de `raio_txt`/`prefixo` (que serve as
  4 camadas de 1,5 km e tem de sair byte-idêntico), reusa caminho já testado, e a derivação impede que
  o rótulo dessincronize das constantes.
- **D3 — FECHADA AQUI (Planner).** Os títulos dentro do PNG passam de `"... - raio 5 km"` para
  `"... - escala 5 km"`:
  - `"Residual Fitness - raio 5 km"` → **`"Residual Fitness - escala 5 km"`** (`censo_map.py:1021`)
  - `"Socioeconomia - raio 5 km"` → **`"Socioeconomia - escala 5 km"`** (`censo_map.py:1533`)
  **Justificativa:** depois da mudança a palavra "raio" ao lado de "5 km" contradiz o desenho (círculo
  de 1,5 km) **e** o rodapé novo, que passa a reservar "Raio" para 1,5 km e "escala" para 5 km. Mesmo
  vocabulário em título e rodapé = zero ambiguidade para o leitor do PDF. Custo: 2 strings + 3 asserts
  em testes que já serão tocados.
  **Risco de overflow MEDIDO por mim (não estimado)** — `_FS_TITULO=44`, budget estrito
  `width - _LEGEND_COL_W - 33 - 28` = **609 px** @1000 (889 px @1280):
  | título | largura |
  |---|---|
  | `Socioeconomia - raio 5 km` (hoje) | 520 px |
  | `Socioeconomia - escala 5 km` (novo) | **573 px** ✓ |
  | `Residual Fitness - raio 5 km` (hoje) | 533 px |
  | `Residual Fitness - escala 5 km` (novo) | **586 px** ✓ (folga 23 px) |
  Folga pequena no pior caso → **teste de largura obrigatório** (passo 3.4).

---

## 1. Plano técnico — diff conceitual por `arquivo:linha`

### `src/motor_expansao/dashboard/censo_map.py`

**1.1 — Constante nova (inserir logo APÓS `_RESIDUAL_GRID_DISK_K`, hoje `censo_map.py:126`,
antes do bloco `RAIO_ENTORNO_DISPLAY_KM` da L128).** Precisa vir depois da L120 para a derivação do
rótulo (passo 1.2) enxergar `RAIO_RESIDUAL_DISPLAY_KM`. Seguir o padrão de comentário das L115-135:

```python
# BLK-RELPON-14 (pedido de Vinicius, 2026-07-28): raio do CIRCULO AZUL desenhado nas camadas de
# HEXAGONO (`socioeconomia`/`residual`). Constante de RENDER PROPRIA, no mesmo padrao de
# `RAIO_RESIDUAL_DISPLAY_KM`/`RAIO_ENTORNO_DISPLAY_KM`: NAO entra em `config.py` nem no §3 do
# CLAUDE.md. Antes deste bloco o circulo herdava `RAIO_RESIDUAL_DISPLAY_KM` (5 km) porque UM
# parametro governava DUAS coisas — enquadramento e desenho. Agora: o frame segue em 5 km
# (`_frame_box_metric`, L1047) e SO o circulo cai para 1,5 km. Coincide NUMERICAMENTE com
# `RAIO_CENSITARIO_DEFAULT_KM` (motor censitario) de proposito, mas NAO importa aquele simbolo:
# acoplar RENDER a MOTOR quebraria a separacao que este arquivo pratica (D1 do ciclo).
RAIO_CIRCULO_DISPLAY_KM = 1.5
```

**1.2 — Rótulo do rodapé (D2), imediatamente abaixo da constante acima:**

```python
# BLK-RELPON-14 (D2, decisao de produto de Vinicius): prefixo do rodape das camadas de hexagono.
# O formato automatico ("Raio X km", derivado de `raio_km` em `_render_camada`) passaria a MENTIR
# sobre o desenho: o circulo tem 1,5 km e o frame cobre 5 km. Injetado via `rotulo_escala` (o mesmo
# mecanismo DEFAULT-PRESERVING que a camada `entorno` ja usa) -> as 4 camadas de 1,5 km continuam
# byte-identicas. DERIVADO das duas constantes para o texto nunca dessincronizar do desenho.
# ASCII puro (excecao de RENDER do §2 do CLAUDE.md: o font embutido do Pillow nao tem glifo
# acentuado). NB: `:.0f` na escala e' deliberado ("escala 5 km", nao "5,0 km"); se algum dia o
# enquadramento deixar de ser inteiro, o teste do texto exato quebra e obriga revisao.
_HEX_ROTULO_ESCALA = (
    f"Raio {RAIO_CIRCULO_DISPLAY_KM:.1f} km - escala {RAIO_RESIDUAL_DISPLAY_KM:.0f} km"
).replace(".", ",")
```
**Verificado por mim:** essa expressão produz exatamente `"Raio 1,5 km - escala 5 km"` (medido, não
presumido). Somado ao sufixo de `_render_camada:874/876`, sai exatamente o texto imutável da D2.

**1.3 — `censo_map.py:1048` (o coração do bloco):**
```python
# ANTES
circle_metric = Point(0, 0).buffer(RAIO_RESIDUAL_DISPLAY_KM * 1000.0, quad_segs=96)
# DEPOIS
# BLK-RELPON-14: o CIRCULO usa a sua propria constante (1,5 km); o FRAME (L1047) continua em
# `RAIO_RESIDUAL_DISPLAY_KM` (5 km). Sao responsabilidades diferentes desde este bloco.
circle_metric = Point(0, 0).buffer(RAIO_CIRCULO_DISPLAY_KM * 1000.0, quad_segs=96)
```
**`censo_map.py:1047` (`frame_metric = _frame_box_metric(RAIO_RESIDUAL_DISPLAY_KM, ...)`) NÃO MUDA
UMA LETRA.**

**1.4 — `censo_map.py:1091-1125` (chamada de `_render_camada`):** acrescentar UM kwarg, logo após
`valor_ponto=...` (L1119-1124):
```python
        rotulo_escala=_HEX_ROTULO_ESCALA,  # BLK-RELPON-14 (D2)
```
e manter `raio_km=RAIO_RESIDUAL_DISPLAY_KM` (L1112) **como está**, com comentário curto de 1 linha:
com `rotulo_escala` setado, `raio_km` deixa de alimentar o rodapé e permanece só como a escala
declarada do frame. (Não trocar por `RAIO_CIRCULO_DISPLAY_KM`: seria mentir sobre o enquadramento e
quebraria a leitura do teste novo do passo 3.1.)

**1.5 — D3, títulos:**
- `censo_map.py:1021`: `titulo: str = "Residual Fitness - escala 5 km"`.
- `censo_map.py:1533`: `titulo="Socioeconomia - escala 5 km",  # ASCII (excecao de RENDER, §2)`.

**1.6 — Comentários/docstrings que ficam STALE (obrigatório atualizar, é a dívida que o
BLK-RELPON-12 já pagou nesta família):**
- `censo_map.py:865-870` (bloco dentro de `_render_camada`): a frase *"a camada de residual sai
  'Raio 5,0 km'"* deixa de ser verdade. Trocar por: as camadas de hexágono passam `rotulo_escala`
  (BLK-RELPON-14) e o `:.1f` de `raio_km` continua servindo só as camadas de 1,5 km.
- `censo_map.py:1028` (1ª linha da docstring de `_render_camada_residual_hex`): "Choropleth de
  `value_col` por hexagono H3 no raio de EXIBICAO de 5 km." → deixar explícito **frame 5 km, círculo
  desenhado 1,5 km** (`RAIO_CIRCULO_DISPLAY_KM`).
- `censo_map.py:96-99` (comentário de `CAMADAS_CENSITARIAS`) e `censo_map.py:1238-1241` (docstring de
  `render_mapas_censitarios_combinados`): acrescentar a cláusula "(círculo desenhado: 1,5 km; os 5 km
  são o ENQUADRAMENTO)". Mudança de 1 cláusula em cada, não reescrever o parágrafo.

### `docs/relatorio_pontual_censitario.md`
- **L80** (§ visão geral): `"...; \`residual\` (5 km) e \`entorno\` (escala de quadra) tem frame
  proprio e NAO desenham pins"` → citar que `socioeconomia`/`residual` têm frame de 5 km **com círculo
  de 1,5 km**.
- **L177** (§6, "Exceções das camadas com frame proprio"): hoje afirma que *"`residual` desenha o
  circulo do raio de EXIBICAO de 5 km"* — **fica FALSO**. Reescrever para: `socioeconomia`/`residual`
  desenham o círculo de **1,5 km** (`RAIO_CIRCULO_DISPLAY_KM`) dentro do frame de **5 km**
  (`RAIO_RESIDUAL_DISPLAY_KM`), com prefixo de rodapé `"Raio 1,5 km - escala 5 km"` injetado via
  `rotulo_escala`, e continuam sem pins/legenda de pins.
- **L204** (§6, "Validacao minima"): acrescentar ao fim `**BLK-RELPON-14** adiciona <nomes dos 4 testes
  novos do passo 3>`, no mesmo padrão das frases de BLK-RELPON-10/-11.
- **L214-215** (§7, página 3 do PDF): a menção a `RAIO_RESIDUAL_DISPLAY_KM = 5.0` **continua correta**
  (é o enquadramento) — acrescentar só a cláusula "(o círculo azul desenhado é de 1,5 km desde o
  BLK-RELPON-14)".
- **L253** (parágrafo histórico do BLK-RELPON-13): **NÃO reescrever** (é registro histórico). Em vez
  disso, **acrescentar um parágrafo novo logo depois**, no padrão do arquivo:
  `**Correcao BLK-RELPON-14 (pedido de Vinicius 2026-07-28):** ...` documentando D1 (constante nova),
  D2 (texto exato do rodapé), D3 (títulos) e o invariante de que o enquadramento/`_RESIDUAL_GRID_DISK_K`
  /motor não mudaram.

---

## 2. Os 2 testes que quebram por construção — atualização legítima, não afrouxamento

Arquivo: `tests/unit/test_relatorio_pontual_censitario_mapa.py`.

**2.1 `test_socioeconomia_e_hexagono_nao_setor_a_5km` (L826-858).**
- L855: `"Socioeconomia - raio 5 km"` → `"Socioeconomia - escala 5 km"`.
- L856: `"Raio 5,0 km - EPSG:3857 - fundo de ruas offline"` →
  `"Raio 1,5 km - escala 5 km - EPSG:3857 - fundo de ruas offline"`.
- L827-830 (docstring): ajustar a frase final para descrever o novo título/rodapé.
- **Manter o nome do teste** — ele fala do FRAME ("a 5km"), que continua em 5 km.

**2.2 `test_rodape_do_png_deriva_do_raio_km_1p5_identico_e_5p0_novo` (L922-949).**
- L943 (`"Raio 1,5 km - EPSG:3857 - fundo de ruas offline"`): **NÃO MUDA** — é a trava das 4 camadas
  de setor a 1,5 km, que têm de sair byte-idênticas.
- L944: passa a asserir o rodapé novo **e** a proibir o antigo:
  ```python
  assert "Raio 1,5 km - escala 5 km - EPSG:3857 - fundo de ruas offline" in textos
  assert not any(t.startswith("Raio 5,0 km") for t in textos), (
      "nenhuma camada pode voltar a anunciar 'Raio 5,0 km': o circulo tem 1,5 km (BLK-RELPON-14)"
  )
  ```
- L946/947: títulos → `"Socioeconomia - escala 5 km"` / `"Residual Fitness - escala 5 km"`.
- L948 (`"Residual disponivel (alunos)"`) e a legenda: **não mudam**.
- **Renomear** para `test_rodape_1p5_por_raio_km_nas_camadas_de_setor_e_rotulo_escala_nas_de_hexagono`
  (o nome atual promete um "5p0" que deixou de existir). Verifiquei: nenhum outro arquivo `.py` nem a
  doc citam esse nome — o rename é seguro. Atualizar a docstring junto.

**Por que isso é atualização de expectativa e não afrouxamento:**
1. Os dois asserts travavam **texto voltado ao usuário**, e D2/D3 são exatamente decisões de produto
   que mudam esse texto — mudar o dado esperado é o único desfecho correto; manter seria travar uma
   mentira (o rodapé descreveria um círculo de 5 km que não existe mais).
2. **Nenhuma asserção foi removida ou relaxada:** as strings continuam comparadas por igualdade exata,
   a trava das camadas de 1,5 km (L943) segue intocada, e o conjunto ficou **mais estrito** (assert
   negativo novo proibindo `"Raio 5,0 km"` + 4 testes novos, incluindo dois geométricos).
3. Os testes que travam o **invariante** deste bloco continuam byte-idênticos e verdes:
   `test_frame_box_metric_puro_reproduz_o_calculo_do_caller` (L992-1006) e
   `test_raio_de_exibicao_nao_toca_o_raio_do_motor` (L1009-1018).

---

## 3. Testes NOVOS (critério de aceite) — com a asserção concreta, não a ideia

### 3.1 `test_circulo_desenhado_tem_1_5_km_com_frame_de_5_km(monkeypatch)` — **geométrico, primário**
Mede o círculo **antes do desenho**, em metros, de forma determinística. Duas espiãs de módulo
(ambas resolvidas por global, então `monkeypatch.setattr` funciona), chamando
`_render_camada_residual_hex` **direto** (assim há exatamente 1 chamada de cada):

```python
capt: dict = {}
real_frame, real_camada = censo_map._frame_box_metric, censo_map._render_camada

def _spy_frame(raio_km, width, height):
    capt.setdefault("raios_frame", []).append(float(raio_km))
    return real_frame(raio_km, width, height)

def _spy_camada(**kwargs):                       # `_render_camada` e' keyword-only
    capt.update(circle=kwargs["circle_3857"], bounds=kwargs["bounds"], raio_km=kwargs["raio_km"])
    return real_camada(**kwargs)

monkeypatch.setattr(censo_map, "_frame_box_metric", _spy_frame)
monkeypatch.setattr(censo_map, "_render_camada", _spy_camada)
png = censo_map._render_camada_residual_hex(
    LAT_C, LNG_C, _hexes_sinteticos(), basemap=False, width=1000, height=760
)
assert png is not None

# (a) ENQUADRAMENTO INTOCADO: o frame continua sendo pedido a 5 km
assert capt["raios_frame"] == [censo_map.RAIO_RESIDUAL_DISPLAY_KM] == [5.0]
assert capt["raio_km"] == censo_map.RAIO_RESIDUAL_DISPLAY_KM

# (b) CIRCULO = 1,5 km: desprojeta 3857 -> CRS metrico local e mede o raio de CADA vertice
inv = censo_map._transformer(censo_map.CRS_WEB_MERCATOR, _local_metric_crs(LAT_C, LNG_C))
circulo_metrico = censo_map._project_geometry(capt["circle"], inv)
xs, ys = circulo_metrico.exterior.coords.xy
raios = np.hypot(np.asarray(xs), np.asarray(ys))
esperado_m = censo_map.RAIO_CIRCULO_DISPLAY_KM * 1000.0
assert abs(float(raios.max()) - esperado_m) <= 1.0
assert abs(float(raios.min()) - esperado_m) <= 1.0
# (c) anti-vacuo: nao e' mais o raio do enquadramento
assert float(raios.max()) < 0.5 * censo_map.RAIO_RESIDUAL_DISPLAY_KM * 1000.0
```
**Por que é determinístico (medido por mim, não estimado):** `Point(0,0).buffer(r)` põe TODOS os
vértices exatamente a `r`; o round-trip aeqd→3857→aeqd via `_project_geometry` tem erro máximo
**2,6e-09 m** (medido para r=1500 e r=5000). A tolerância de 1,0 m é ~9 ordens acima do erro real e
~3 ordens abaixo da diferença que se quer detectar (3.500 m). `_local_metric_crs` já é importado no
topo do arquivo de teste (L18); `np` idem (L5) — sem import novo.

### 3.2 `test_circulo_em_pixel_encolheu_nos_dois_paineis_de_hexagono()` — **fim-a-fim, no PNG**
Prova o artefato desenhado (não só o insumo), nos DOIS painéis, pelo caminho público. Helper novo ao
lado de `_conta_pixels_do_circulo` (L1149-1153), mesmo predicado de azul:
```python
def _bbox_dos_pixels_do_circulo(png: bytes) -> tuple[int, int, int, int]:
    arr = np.array(Image.open(BytesIO(png)).convert("RGB"))
    r, g, b = arr[:, :, 0], arr[:, :, 1], arr[:, :, 2]
    ys, xs = np.nonzero((b > 200) & (r < 80) & (g < 160))
    return int(xs.min()), int(xs.max()), int(ys.min()), int(ys.max())
```
Asserção (o fator 2 do diâmetro cancela nos dois lados):
```python
mapas = render_mapas_censitarios_combinados(
    LAT_C, LNG_C, _setores_um_quadrado(), width=1000, height=760,
    basemap=False, hexes_df=_hexes_sinteticos(),
)
_inner_w, inner_h = censo_map._map_inner_dims(1000, 760)          # inner_h = 550
esperado_px = inner_h * censo_map.RAIO_CIRCULO_DISPLAY_KM / (
    censo_map.RAIO_RESIDUAL_DISPLAY_KM * (1.0 + censo_map._MAP_FRAME_MARGIN)
)                                                                  # 550 * 1,5/5,4 = 152,8 px
for chave in ("socioeconomia", "residual"):
    x0, x1, y0, y1 = _bbox_dos_pixels_do_circulo(mapas[chave])
    assert abs((x1 - x0) - esperado_px) <= 6, chave                # tolerancia: linha de 3 px + Mercator
    assert abs((y1 - y0) - esperado_px) <= 6, chave
# trava anti-vacuo do predicado: as camadas de 1,5 km (frame de 1,5 km) seguem com o circulo GRANDE
x0, x1, _y0, _y1 = _bbox_dos_pixels_do_circulo(mapas["densidade"])
assert (x1 - x0) > 0.8 * inner_h
```
**Calibração medida por mim no código de hoje** (com o círculo ainda em 5 km): extensão azul em x =
**509 px** em `1000x760` **e** em `1280x760` (o frame acompanha o aspect, então o diâmetro em px é
invariante ao canvas); a mesma fórmula prevê 509,2 px — erro de 0,2 px. Depois da mudança o esperado
é ~153 px (analítico 151,9 + ~3 px da linha de 3 px). Tolerância de 6 px cobre com folga e ainda
reprova qualquer volta ao círculo de 5 km (509 px). Se por algum motivo a medição sair fora, **subir
a tolerância para 8 px é aceitável** (justificativa: largura de linha + distorção Mercator);
**abaixar o esperado, não** — isso seria afrouxar o teste.

### 3.3 `test_raio_do_circulo_e_constante_de_render_propria(...)` — **D1 + nenhuma constante de motor**
Espelha `test_raio_de_exibicao_nao_toca_o_raio_do_motor` (L1009-1018), no padrão
`assert not hasattr(config, ...)` das L1017/1216:
```python
import motor_expansao.config as config
from motor_expansao.dashboard.censo_point import RAIO_CENSITARIO_DEFAULT_KM

assert censo_map.RAIO_CIRCULO_DISPLAY_KM == 1.5
assert not hasattr(config, "RAIO_CIRCULO_DISPLAY_KM")     # constante de RENDER, nunca de motor
assert censo_map.RAIO_RESIDUAL_DISPLAY_KM == 5.0          # enquadramento preservado
assert censo_map._RESIDUAL_GRID_DISK_K == 5               # disco de candidatos preservado
assert RAIO_CENSITARIO_DEFAULT_KM == 1.5                  # motor censitario INTOCADO
assert censo_map._HEX_ROTULO_ESCALA == "Raio 1,5 km - escala 5 km"   # D2, texto imutavel
# Coincidencia NUMERICA documentada, SEM acoplamento: o circulo de RENDER nao deriva do motor.
# Se o motor um dia mudar de 1,5 km, este assert cai e forca uma decisao explicita sobre o
# circulo, em vez de o valor ser arrastado junto em silencio.
assert censo_map.RAIO_CIRCULO_DISPLAY_KM == RAIO_CENSITARIO_DEFAULT_KM
```
**Nota para o Builder:** NÃO tente provar a ausência de acoplamento com
`assert not hasattr(censo_map, "RAIO_CENSITARIO_DEFAULT_KM")` — **falharia**: `censo_map` já importa
esse símbolo do motor para o default de `render_mapas_censitarios_combinados` (`censo_map.py:1205`),
o que é legítimo e fora de escopo. A garantia de D1 vem do teste 3.1 (o círculo mede o valor da
constante NOVA) + deste espelho.

### 3.4 `test_titulos_das_camadas_de_hexagono_cabem_sem_invadir_a_legenda()` — **D3, overflow**
No padrão de `test_textos_do_entorno_cabem_sem_invadir_a_coluna_da_legenda` (L1228-1247), porque a
folga do pior título caiu para 23 px:
```python
image = Image.new("RGBA", (1000, 760)); draw = ImageDraw.Draw(image, "RGBA")
budget = 1000 - censo_map._LEGEND_COL_W - 33 - 28      # 609 px (respeita o respiro ate a legenda)
for titulo in ("Socioeconomia - escala 5 km", "Residual Fitness - escala 5 km"):
    assert censo_map._text_width(draw, titulo, censo_map._font(censo_map._FS_TITULO)) <= budget
# rodape novo (61 chars) e a variante ONLINE (mais longa) cabem no canvas
for rodape in (
    f"{censo_map._HEX_ROTULO_ESCALA} - EPSG:3857 - fundo de ruas offline",
    f"{censo_map._HEX_ROTULO_ESCALA} - EPSG:3857 - {censo_map._ATRIBUICAO_TILES}",
):
    assert censo_map._text_width(draw, rodape, censo_map._font(censo_map._FS_FOOTER)) <= 1000 - 36
# ASCII puro (excecao de RENDER do §2): o font embutido do Pillow nao tem glifo acentuado
assert all(ord(c) < 128 for c in censo_map._HEX_ROTULO_ESCALA)
```
**Medido por mim:** títulos 573/586 px vs budget 609; rodapé offline 614 px e online 703 px vs 964 px
de canvas útil. Todos passam com folga suficiente para não virar teste instável.

---

## 4. Ordem de execução (Builder)

1. `censo_map.py`: constante + rótulo (1.1, 1.2). Rodar `python -c "import motor_expansao.dashboard.censo_map as m; print(m._HEX_ROTULO_ESCALA)"` e conferir `Raio 1,5 km - escala 5 km`.
2. `censo_map.py`: L1048 (círculo), kwarg `rotulo_escala` na chamada (1.3, 1.4). **Não tocar a L1047.**
3. `censo_map.py`: D3, títulos L1021/L1533 (1.5).
4. `censo_map.py`: comentários/docstrings stale (1.6).
5. Testes: atualizar os 2 que quebram (§2) e adicionar os 4 novos + o helper de bbox (§3).
6. `docs/relatorio_pontual_censitario.md` (L80, L177, L204, L214-215 + parágrafo novo após L253).
7. Validações do §6 abaixo. Só então entregar ao QA.

---

## 5. Recorte de commits por path (o orquestrador commita; esta é a lista)

- **Commit 1 — `feat(relpon-14)`:**
  `src/motor_expansao/dashboard/censo_map.py`
  `tests/unit/test_relatorio_pontual_censitario_mapa.py`
- **Commit 2 — `docs(relpon-14)`:** `docs/relatorio_pontual_censitario.md`
- **Commit 3 — `docs(backlog)`:** `tasks/backlog.md` (o bloco BLK-RELPON-14 já está no worktree, não
  commitado — **é do ciclo**), e no fechamento `tasks/completed.md`
- **Handoffs:** `context/handoff/*.md` (append-only, versionados)

**NÃO COMMITAR, em nenhum commit:** `graphify-out/*` (regerado pelo hook `post-checkout`), `PRD.md`,
`context/handoff.md` (corrente, não versionado neste ciclo), `tasks/current_task.md`, `constraints.txt`.

---

## 6. Validações obrigatórias (Builder e QA)

> **`pytest -n auto` está QUEBRADO nesta máquina (BLK-QA-XDIST-01)** — rodar **SERIAL** ou `-n 2`.
> Nunca rodar a suíte completa dentro do Builder; o QA decide se roda.

| # | Comando | Critério mínimo |
|---|---|---|
| 1 | `python -m pytest -q tests/unit/test_relatorio_pontual_censitario_mapa.py` | 0 falhas; os 4 testes novos passam; `test_frame_box_metric_puro_reproduz_o_calculo_do_caller` e `test_raio_de_exibicao_nao_toca_o_raio_do_motor` verdes **sem edição** |
| 2 | `python -m pytest -q tests/unit/test_relatorio_pontual_censitario_export.py tests/unit/test_relatorio_pontual_orquestracao.py tests/unit/test_relatorio_pontual_viabilidade.py tests/unit/test_api_skeleton.py` | 0 falhas — trava `/Count 8` e `/Count 12`, chaves das camadas e o caminho da API |
| 3 | `ruff check . --no-cache` | 0 erros (step bloqueante no CI) |
| 4 | `mypy src/` | 0 erros (step bloqueante no CI) |
| 5 | (QA) `python -m pytest -q` **serial** ou `-n 2` | 0 falhas; baseline medida no momento, não comparada com contagem histórica |

Verifiquei por grep que **nenhum outro** `.py` do repo referencia `"Raio 5,0"`, `"raio 5 km"` ou
`RAIO_RESIDUAL_DISPLAY_KM` além de `censo_map.py` e do arquivo de teste alvo — não há alcance
colateral em `pages.py`, `censo_report.py` (cujos `MAP_LAYER_TITLES` são títulos de PÁGINA do PDF,
`"Socioeconomia"`/`"Residual Fitness"`, sem raio) nem na API.

---

## 7. Riscos e mitigação

| # | Risco | Mitigação |
|---|---|---|
| R1 | Mexer no círculo mexer em RECORTE DE DADO | Confirmado pelo BO e reconferido por mim na leitura de `censo_map.py:1047-1064/1091-1125` e `_render_camada:824-831`: o clip dos hexes é `.intersection(frame_3857)` (L963); `circle_3857` só alimenta `draw.line`. Teste 3.1 assere que o frame continua pedido a 5 km |
| R2 | Título novo (D3) transbordar para a coluna da legenda (folga de só 23 px) | Medido: 573/586 px vs 609 px de budget; **teste 3.4** trava permanentemente |
| R3 | Rodapé de 61 chars transbordar do canvas | Medido: 614 px (offline) / 703 px (online) vs 964 px; coberto pelo teste 3.4 |
| R4 | Rótulo dessincronizar das constantes numa mudança futura | `_HEX_ROTULO_ESCALA` é **derivado** das duas constantes + assert de texto exato em 3.3 |
| R5 | Regressão silenciosa (alguém religar o círculo ao raio do frame) | Testes 3.1 (geométrico) + 3.2 (pixel) + assert negativo de `"Raio 5,0 km"` no teste 2.2 |
| R6 | Doc ficar stale (dívida que o BLK-RELPON-12 já pagou) | Passo 1 do §1 lista `docs/relatorio_pontual_censitario.md` L80/L177/L204/L214-215 + parágrafo novo; entra **no mesmo PR** |
| R7 | Tolerância do teste de pixel virar teste instável | Calibrada com medição real (erro de 0,2 px na fórmula); teto explícito de 8 px, e **nunca** mexer no valor esperado |
| R8 | `n_setores` (L1113) refletir o círculo | Verificado: `n_setores=len(hex_records)` conta hexes recortados pelo FRAME e **não é usado no corpo de `_render_camada`**; nenhum teste o assere. Sem impacto |

**Premissas do backlog que NÃO se sustentam / preciso registrar:**
- O backlog oferecia "reusar `RAIO_CENSITARIO_DEFAULT_KM`" como alternativa de D1 — **descartada** (D1
  fechada). Registro aqui porque a prova "o módulo não importa o símbolo do motor" é **impossível**:
  `censo_map.py:1205` já o importa legitimamente. A separação é garantida por **de onde o círculo
  deriva**, não pela ausência do import (ver nota em 3.3).
- O backlog dizia que o rótulo do rodapé sairia de `raio_km`; com a D2 ele passa a **não** derivar mais
  de `raio_km` nessas duas camadas (vai por `rotulo_escala`), e `raio_km=5.0` permanece na chamada só
  como declaração da escala do frame. Isso é intencional, não sobra de código.
- Confirmado (não repetindo o trabalho do BO): 2 chamadores, ambos em `censo_map.py`; nenhum alcance
  em `pages.py`/`api/service.py`.

---

## 8. Critério de aceite reescrito de forma verificável

| # | Critério | Como se verifica | Quem |
|---|---|---|---|
| A1 | O círculo desenhado mede 1,5 km nos DOIS painéis | teste 3.1 (raio em metros, tol. 1,0 m) + teste 3.2 (pixel, ambas as chaves) | QA (automático) |
| A2 | O enquadramento continua em 5 km | teste 3.1(a) (`_frame_box_metric` recebe `RAIO_RESIDUAL_DISPLAY_KM`) + L992-1006 e L1009-1018 **sem edição** | QA |
| A3 | `censo_map.RAIO_RESIDUAL_DISPLAY_KM == 5.0` e `_RESIDUAL_GRID_DISK_K == 5` | teste 3.3 | QA |
| A4 | Nenhuma constante de motor tocada / nada foi para `config.py` | teste 3.3 (`assert not hasattr(config, ...)`, `RAIO_CENSITARIO_DEFAULT_KM == 1.5`); diff sem `config.py`/`pipelines/m1`/`censo_point.py` | QA |
| A5 | Rodapé = exatamente `Raio 1,5 km - escala 5 km - EPSG:3857 - fundo de ruas offline` | testes 2.1/2.2 (igualdade exata) + assert negativo de `"Raio 5,0 km"` | QA |
| A6 | Títulos coerentes (D3) e sem overflow | testes 2.2 e 3.4 | QA |
| A7 | `/Count` do PDF e demais páginas inalterados | comando 2 do §6 (`/Count 8` e `/Count 12`) | QA |
| A8 | `ruff`/`mypy` limpos, suíte relevante verde | comandos 1-5 do §6 | QA |
| A9 | **Gate visual: o círculo menor "lê" bem no PDF, no mesmo ponto, antes/depois** | **Comparação visual de PDF por Vinicius — é do HUMANO, não do QA.** Nenhum teste automático julga aparência; o QA **não** pode marcar este item como aprovado nem reprovado, só registrar "pendente de gate humano" | **Vinicius (antes do merge)** |

---

## 9. Arquivos afetados
- `src/motor_expansao/dashboard/censo_map.py` (constante nova, rótulo, L1048, kwarg, títulos, comentários)
- `tests/unit/test_relatorio_pontual_censitario_mapa.py` (2 atualizados + 4 novos + 1 helper)
- `docs/relatorio_pontual_censitario.md` (L80, L177, L204, L214-215 + parágrafo novo)
- `tasks/backlog.md` / `tasks/completed.md` (bookkeeping, no fechamento)

## 10. Dependências
- Nenhuma dependência nova. `h3`, `shapely`, `pyproj`, `Pillow`, `numpy` já são usados pelos caminhos
  tocados; `basemap=False` mantém tudo offline em CI.
- BLK-RELPON-10 e -13 já mergeados (pré-requisito satisfeito).

## 11. Fora de escopo (não tocar)
- Motor censitário (`setor_censitario_intersecao_area_1p5km`, `censo_point.py:23`
  `RAIO_CENSITARIO_DEFAULT_KM`), `config.py`, `src/motor_expansao/pipelines/m1/`, scoring, artefatos M1.
- `_frame_box_metric`/`frame_metric`/`frame_3857`, `RAIO_RESIDUAL_DISPLAY_KM`, `_RESIDUAL_GRID_DISK_K`.
- Camadas `densidade`/`renda`/`score`/`renda_domiciliar`/`concorrentes`/`entorno` (byte-idênticas),
  página de satélite, grid 2x2, `censo_report.py` (`MAP_LAYER_TITLES` intocado), `pages.py`,
  `api/service.py`.
- Deploy/VPS. Nenhuma DEC nova é necessária (mudança de RENDER, sem reverter decisão aprovada).

## 12. Alerta de aprovação
Criticidade **Média**, READ-ONLY sobre o M1 — **não** exige Approver antes do Builder. Exige, sim, o
**gate visual humano de Vinicius antes do merge** (A9). Bloco **não** é loop-safe.

## 13. Guardrails ativos
- READ-ONLY M1: nada em `config.py`, `pipelines/m1/`, scoring ou artefatos oficiais
  (`score_priorizacao`, `hex_score_estrutural`, carteira, plano curto prazo, plano domínio).
- Motor censitário INTOCADO (CLAUDE.md §4).
- CLAUDE.md §5 (guardrail permanente): visualização/render não recalcula nem altera score/carteira/
  plano/artefatos.
- Acentuação (§2): texto de PNG segue a **exceção de RENDER** — ASCII puro, hífen `-` e nunca travessão
  `—`, porque o font embutido do Pillow não tem glifo acentuado. O texto desta doc/handoff, sim, é
  acentuado.

---

## Nota ao orquestrador
Não editei `tasks/current_task.md` (proibido pelo escopo que recebi). Ajustar lá:
`Próxima Skill: Builder` e `D3: FECHADA no Planner — títulos passam a "escala 5 km"`.
