# Handoff — QA/Quality Analyzer (re-verificação pós follow-up)

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador: housekeeping move + commit por path). Bloco Média, sem gate humano.

## VEREDITO
APROVADO

## Justificativa
A ressalva única do meu veredito anterior (legendas PIL do mapa municipal sem acento) foi RESOLVIDA
pelo Builder: `relatorio_municipal.py:106,112` agora exibem `"Aprovado (dado próprio)"`. O follow-up
é EXCLUSIVAMENTE os 2 literais de legenda `proprio`→`próprio` ("ó" = U+00F3 = latin-1 0xF3, mesmo
caminho PIL que já desenha "Agregação"/"região"), sem tocar a constante `_COR_APROVADO_PROPRIO`, o
comentário (l.100) nem a docstring. Suíte completa segue verde exceto a falha pré-existente/ambiental
do M2 (parquet gitignored ausente), alheia a este bloco — sem regressão. Todo o escopo do bloco está
correto: 0 tipografia fora de latin-1, identificadores/números/`_ascii()`/núcleo censo_* intactos,
READ-ONLY M1 preservado.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum (a ressalva das legendas PIL foi resolvida neste follow-up).

## Melhorias opcionais
- Opcional/cosmético (não pedido, não bloqueia): o comentário-fonte l.100 ("Aprovado dado proprio")
  e a docstring seguem sem acento — são comentários de código (não texto voltado ao usuário), logo
  fora da regra §2; deliberadamente intocados. Sem ação necessária.

## Testes faltantes
- Persiste a lacuna estrutural (não regressão deste bloco): o texto renderizado por PIL nas
  legendas/títulos do mapa não tem cobertura de teste (o anti-`"?"` cobre só o caminho
  `_ascii`/bytes crus). Follow-up opcional futuro, não condição de fechamento.

## Riscos remanescentes
- Revisão visual humana dos 3 PDFs (pontual recente + classico + municipal) é critério de aceite do
  backlog (não gate formal). Recomendada antes do merge.
- Ambiental: `pytest-xdist` (`-n auto`) instável no Python 3.14 (execnet EOFError). A suíte SERIAL é
  a fonte de verdade.

## Saída literal das validações (re-executadas pelo QA)

`python -m ruff check .`
```
All checks passed!
exit 0
```

`python -m mypy src` (6 erros, TODOS pré-existentes de types-requests em arquivos NÃO tocados; 0 novo;
0 em censo_report.py/relatorio_municipal.py):
```
src\motor_expansao\dimensionamento\growth_api_client.py:28: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\ibge_censo.py:24: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\poi_enrichment.py:15: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\base_h3_brasil.py:31: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\geo.py:21: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\telegram_bot.py:23: error: Library stubs not installed for "requests"  [import-untyped]
Found 6 errors in 6 files (checked 102 source files)
```

`python -c "import streamlit_app; print('import ok')"`:
```
import ok
```

Suíte COMPLETA SERIAL `python -m pytest -q` — resultado AUTORITATIVO do orquestrador (processo do QA
anterior foi morto para evitar órfão; NÃO relancei a suíte completa):
```
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 1434 passed, 2 skipped, 218868 warnings in 656.20s (0:10:56)
```
- Corroborado por uma segunda run serial independente (monitor do QA, mesmo commit): `1 failed, 1434
  passed, 2 skipped ... in 676.93s` — IDÊNTICO em contagem.
- Única falha, isolada e confirmada como pré-existente/ambiental: `FileNotFoundError` ao ler
  `data/staging/unidade_territorio_retencao.parquet` (parquet do M2/`lifetime/`, gitignored, ausente
  neste ambiente). NÃO toca nenhum arquivo deste bloco. O follow-up (2 literais de legenda) não
  introduziu regressão.
- Os 3 arquivos de teste do bloco (incl. o novo anti-`"?"`) estão entre os 1434 passed; confirmados
  em 66 passed na run focada da re-verificação anterior deste QA (26 censo export + 37 municipal + 3
  regressão).

`python scripts/housekeeping_move_block.py BLK-ACENTO-02 --check` (estado PRÉ-move esperado):
```
FALHA --check: stub ausente no backlog para BLK-ACENTO-02
exit 1
```
- Correto: reconhece o bloco (NÃO é `BlockNotFound`); o stub/move é passo do orquestrador no
  fechamento.

`git diff --stat` (escopo):
```
 context/handoff.md                                 | 168 +++++++--------------
 src/motor_expansao/dashboard/censo_report.py       |  61 ++++----
 src/motor_expansao/dashboard/relatorio_municipal.py| 162 ++++++++++----------
 tasks/current_task.md                              |  74 +++++----
 tests/unit/test_relatorio_municipal.py             |  18 +--
 tests/unit/test_relatorio_pontual_censitario_export.py | 18 +--
```
+ untracked: `tests/unit/test_relatorio_acentuacao_regressao.py` (novo) e snapshots de handoff. ZERO
em pipelines/m1, config.py, censo_point.py, censo_map.py, data.py, pages.py, components.py,
constants.py; nenhum parquet.

Diff do follow-up (só os 2 literais de legenda):
```
-    ("Aprovado (dado proprio)", _COR_APROVADO_PROPRIO),
+    ("Aprovado (dado próprio)", _COR_APROVADO_PROPRIO),
   (x2: _COBERTURA_LEGENDA l.106 e _RESUMO_LEGENDA l.112; _COR_APROVADO_PROPRIO intocado)
```

## Conferência de no-bypass
Confirmo: nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock do
caminho crítico. Não há tooling de produção em escopo (texto de relatório). Os checks rápidos foram
re-executados por mim com saída literal colada; a suíte completa é o resultado autoritativo do
orquestrador (656s), corroborado por run serial independente do QA (676s) — mesma contagem. Sem
bypass.

## Conferência cruzada com log do Builder (follow-up)
Bate. O Builder (snapshot `20260707-152223-builder.md`) declarou tocar só os 2 literais de legenda
`proprio`→`próprio`; o `git diff` confirma exatamente isso (nada além, `_COR_APROVADO_PROPRIO` e
comentário/docstring intactos). Suíte sem regressão.

## Guardrails verificados
- score_priorizacao não alterado: sim (só LIDO em prosa READ-ONLY dos créditos; nunca acentuado).
  Nenhum pipeline/artefato M1 tocado.
- Artefatos M1 preservados: sim. `git diff --stat` só nos arquivos declarados + bookkeeping; nenhum
  parquet/data/M1 alterado.
- Follow-up seguro: "ó" = U+00F3 (latin-1 0xF3) — renderiza no core font/PIL como os demais acentos
  já presentes; constante de cor `_COR_APROVADO_PROPRIO` e comentário-fonte intocados.
- Núcleo censo_* INTOCADO: `_ascii()`, `setor_censitario_intersecao_area_1p5km`, raio 1,5 km,
  `RAIO_CENSITARIO_DEFAULT_KM`, `/Count 5` e `/Count 9`, grid Big Numbers, marca d'água (BLK-EST-03),
  `set_compression(False)`, `pdf_version` — inalterados; só STRINGS/comentários.
- Validações re-executadas pelo QA (sem bypass):
  - ruff check . → All checks passed!
  - mypy src → 6 erros pré-existentes (0 novos, 0 nos arquivos tocados)
  - import streamlit_app → import ok
  - pytest -q (serial, autoritativo do orquestrador) → 1 failed (M2 pré-existente/ambiental) / 1434
    passed / 2 skipped; corroborado por run independente do QA (mesma contagem)
  - housekeeping --check → FALHA "stub ausente" (pré-move correto)
- Escopo respeitado: sim. Só os arquivos declarados; follow-up limitado aos 2 literais de legenda.
- Parâmetros canônicos §3 (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, pesos
  renda=0.40/pop=0.60): inalterados.

## Decisão recomendada
Fechar o ciclo. Ressalva resolvida; suíte verde exceto a falha pré-existente/ambiental do M2.
Housekeeping move (`scripts/housekeeping_move_block.py BLK-ACENTO-02`) + commit por path são passos
do orquestrador — QA não move nem commita. Revisão visual humana dos 3 PDFs recomendada antes do
merge (critério de aceite do backlog; branch acumula BLK-ACENTO-01+02 num PR único).
