# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador: Passo 6.0 housekeeping via `scripts/housekeeping_move_block.py BLK-LTV-04` — mover stub backlog→completed — depois merge humano)

## VEREDITO
APROVADO

## Justificativa
O BLK-LTV-04 entregou um **NO-GO honesto, mecânico e reprodutível** — desfecho LEGÍTIMO
(DEC-014 decisão 2 / DEC-008). O critério de GO era realmente alcançável (não afrouxado), o
eixo retenção não bate o baseline out-of-fold (melhor modelo `score_priorizacao` sozinho:
R²_oof=+0.0399 com IC cruzando zero; rho_oof=−0.0730 < 0.30 e IC cruza zero), o módulo NÃO
gerou o parquet de score nem degradou para proxy, e o M1 ficou READ-ONLY (mtimes idênticos).
Suíte full verde (1193 passed, 1 skipped, 0 failed), ruff/mypy limpos, imports proibidos
(incl. `sklearn`) ausentes, DEC-001/DEC-009 intactas.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- nenhuma (escopo estritamente aderente ao gate; nada a gold-platear num NO-GO).

## Testes faltantes
- nenhum. Os 13 testes cobrem: schema exato + dtypes + versao_contrato; determinismo de score
  e de IC (seed); protocolo kfold_5x5 (N≥25) vs LOO (N<25); OOF sem dobra vazia; NO-GO
  alcançável sem exceção; `run()` NO-GO não gera parquet mas escreve relatório; imports
  proibidos (incl. `sklearn`); READ-ONLY M1 por mtime; clip 0–100; flag_extrapolação; não
  muta inputs.

## Riscos remanescentes
- N=56 pequeno (variância alta) e colinearidade captação↔retenção — DECLARADOS no §3 do
  relatório (confounds 1, 2, 4, 5) e no docstring do módulo. Não é defeito; é o motivo
  técnico do NO-GO.
- Ambiente local tem 3 dos 4 artefatos M1 (`hexagonos_brasil_dashboard.parquet` ausente); os 3
  presentes tiveram mtime idêntico antes/depois. O teste de mtime faz `skip` gracioso se nenhum
  existir e valida só os existentes — cobertura correta.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte FULL (gate único) — `python -m pytest -n auto -q`
```
1193 passed, 1 skipped, 307060 warnings in 502.58s (0:08:22)
```
0 failed / 0 collection errors. Baseline anterior ~1180+ + 13 testes novos do módulo = coerente.

### 2. Smoke import — `python -c "import streamlit_app; print('import ok')"`
```
import ok
```
(precedido só dos WARNINGs benignos de cache do Streamlit fora de runtime)

### 3. ruff — `ruff check src/ tests/`
```
All checks passed!
```

### 3b. mypy — `mypy src/`
```
Success: no issues found in 88 source files
```
(apenas notes `annotation-unchecked` pré-existentes em `pipelines/m1/*.py`, não erros)

### 4. Reprodução determinística + READ-ONLY M1 — `python -m motor_expansao.lifetime.score_retencao_territorial` (2×)
```
MTIME BEFORE (ns):
  brasil_estrutural.parquet             1780501621733768300
  brasil_priorizados.parquet            1780501631900401600
  hexagonos_brasil_oportunidades.parquet 1780501655559567700
  hexagonos_brasil_dashboard.parquet    MISSING (ausente neste ambiente)

RUN 1 -> BLK-LTV-04 NO-GO: score NAO gerado (DEC-014 decisao 2). veredito=NO-GO
SHA1 relatorio run1: 2e554413d961faadd6e0737312fa5e034f532541
RUN 2 -> BLK-LTV-04 NO-GO: score NAO gerado (DEC-014 decisao 2). veredito=NO-GO
SHA1 relatorio run2: 2e554413d961faadd6e0737312fa5e034f532541

MTIME AFTER (ns):
  brasil_estrutural.parquet             1780501621733768300  (IDÊNTICO)
  brasil_priorizados.parquet            1780501631900401600  (IDÊNTICO)
  hexagonos_brasil_oportunidades.parquet 1780501655559567700 (IDÊNTICO)

score parquet ABSENT (correct NO-GO)
```
(a) NO-GO reproduzível; (b) relatório byte-estável (SHA1 idêntico entre as 2 runs);
(c) mtimes M1 IDÊNTICOS antes/depois — zero escrita em artefato M1; (d) parquet de score
NÃO gerado (DEC-014 decisão 2).

### 4b. gitignore dos outputs
```
data/analysis/relatorio_score_retencao.md      -> IGNORADO
data/staging/score_retencao_territorial.parquet -> IGNORADO
```

### 5. Housekeeping pré-close — `python scripts/housekeeping_move_block.py BLK-LTV-04 --check`
```
FALHA --check: stub ausente no backlog para BLK-LTV-04
```
FALHA é o estado PRÉ-close CORRETO — o helper RECONHECE o bloco (não é BlockNotFound); o move
backlog→completed é do orquestrador no Passo 6.0. NÃO movido pelo QA.

## Conferência de no-bypass
Confirmo que NENHUMA validação usou `--config /dev/null`, fixture fora do caminho real, ou
mock do caminho crítico. O gate real foi executado de verdade: a suíte full rodou 1× aqui
(`-n auto`, 1193 passed), e o `run()` do módulo foi executado 2× sobre os **parquets REAIS de
staging** (`unidade_territorio_retencao.parquet`, `growth_api_historico.parquet`) com captura
de mtime dos artefatos M1 reais antes/depois. Os testes unitários usam fixtures sintéticas em
memória — prática legítima e esperada (isolam a lógica), NÃO bypass do caminho de produção, que
foi exercitado à parte pelo `run()` real.

## Conferência cruzada com log do Builder
BATE. Números out-of-fold idênticos aos do handoff do Builder (score_priorizacao R²_oof=+0.0399
IC[−0.1013,+0.1203], rho_oof=−0.0730; as 5 variantes conferem linha a linha com o §2 do
relatório). Contagem de testes do subset do Builder (13 no módulo) coerente com o total. Builder
reportou 3 de 4 artefatos M1 presentes — reproduzido (dashboard ausente neste ambiente). Veredito
NO-GO, ausência do parquet de score e mtime inalterado: todos confirmados por evidência própria.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (só LIDO como feature; `config.py`/`pipelines/m1`/
  `scoring` NÃO tocados no diff; pesos renda=0.40/pop=0.60 intactos → DEC-001 INTACTA).
- Artefatos M1 preservados: **sim** (mtime ns idêntico nos 3 presentes; nenhum path M1 escrito;
  CLAUDE.md diff = +16 linhas puramente aditivas de DEC-014).
- Metodologia honesta (DEC-008): **sim** — R²/rho são OUT-OF-FOLD (k-fold 5×5, padronização só no
  treino de cada fold), baseline da média presente (`_r2_oof` vs `y.mean()`), IC bootstrap
  seed=42, `fit(X,y)→predict(X)` ausente (o `_fit_final` só projeta aos hexes em GO, jamais como
  desempenho, e nunca roda em NO-GO); critério de NO-GO mecânico e alcançável; maturidade só como
  covariável (variante `score_priorizacao+maturidade`, nunca feature por hex).
- Imports proibidos em `lifetime/`: **sim, ausentes** (teste `test_no_extra_m1_imports` inclui
  `sklearn`; grep confirma que os únicos matches de `sklearn/pipelines.m1/dashboard/censo/api`
  estão no DOCSTRING descrevendo a restrição, não em imports reais; Ridge é numpy puro).
- DEC-009 intacta: **sim** — score de RETENÇÃO territorial; §5 do relatório proíbe uso de coluna
  do M2 como preditor de demanda; nenhuma coluna do M2 alimenta regressão geográfica de demanda.
- Escopo por PATH: **sim** — diff só toca arquivos do ciclo (módulo + teste novos, CLAUDE.md
  DEC-014, handoff/current_task); paths pré-sujos (`data/raw/ibge/*.geojson` D,
  `scripts/backtest_smartfit_scores.py` ??) e `PRD.md` NÃO arrastados.
- Validações re-executadas pelo QA (sem bypass):
  - pytest -n auto → 1193 passed, 1 skipped, 0 failed
  - import streamlit_app → import ok
  - ruff check src/ tests/ → All checks passed
  - mypy src/ → Success (88 files)
  - run() módulo 2× sobre parquets reais → NO-GO reproduzível, relatório byte-estável, mtime M1 idêntico, sem parquet de score
  - housekeeping --check → FALHA (estado pré-close correto; bloco reconhecido)

## Julgamento metodológico (NO-GO honesto?)
SIM, honesto. (a) O critério de GO (R²_oof>0 IC↛0 E rho_oof≥0.30 IC↛0 E supera base) é
mecânico e ERA alcançável — os testes `_df_calibracao_forte` exercitam o caminho GO com sinal
recuperável, provando que a régua não está travada em NO-GO; não foi afrouxado. (b) O Builder
NÃO forçou GO nem degradou para proxy: decisão 1 (variante A 50/50) e decisão 2 (encerrar sem
score) respeitadas; nenhum parquet gerado. (c) Todos os 5 confounds declarados no §3 (N=56;
colinearidade captação↔retenção ambos derivando de `score_priorizacao`; maturidade atributo de
unidade; seleção de sobreviventes + 32 sem hex_id 36%; TICKET_MEDIO×LTV +0.626 exógeno
proibido). (d) O gap in-sample→out-of-fold é tecnicamente sólido: o +0.391 do LTV-03 é
correlação de rank in-sample da feature bruta; o modelo Ridge validado fora-de-fold não
generaliza (R²_oof e rho_oof com IC cruzando zero), exatamente o esperado sob N=56 +
colinearidade. Um NO-GO honesto e reprodutível = APROVADO.

## Decisão recomendada
Fechar ciclo. Orquestrador executa o housekeeping (Passo 6.0) movendo o stub BLK-LTV-04
backlog→completed via `scripts/housekeeping_move_block.py`; depois merge humano. Nenhum bloco de
correção necessário.
