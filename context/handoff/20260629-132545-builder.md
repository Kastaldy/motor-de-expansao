# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-UI-11 — Aprimoramento estético e clareza de conteúdo do dashboard (caminho de produção). READ-ONLY sobre o M1.

## Escopo aprovado (gate Vinicius, 2026-06-29)
TODOS os Quick Wins (G2,G3,G4,S1,S2,S3,M1,M2,E1,E2,E3,D1,D2,C1,C2,V1,V2), TODOS os Médio
(G5,G6,M3,M4,M5,E4,E5,C3,C4,V3), G1 como SUBSET SEGURO (só strings não-testadas), aba
Viabilidade incluída, E5/M3 em components.py autorizado (só title=/labels), sem remover código
morto, sem acentuação em massa.

## O que foi feito (por ID)
**GLOBAL / TEMA**
- **G1 (subset seguro):** acentuei strings visíveis NÃO fixadas por teste (headers `####`/`#####`,
  captions, cards). Mantive SEM acento as substrings travadas: expander "Analise Territorial" e
  "Analise Pontual de Entorno" (test L2509 checa substring "Analise"); rótulos de tabela travados.
- **G2:** `render_header()` — título trocado para "Ultra Academia — Inteligência de Expansão" +
  subtítulo de operador; 3 pills mantidas e acentuadas.
- **G3:** `inject_styles` — `block-container` padding-top 1.4rem→0.8rem; `.hero` padding
  1.35/1.5→0.95/1.3, margin-bottom 1rem→0.6rem, border-radius 22→18, sombra reduzida.
- **G4:** `streamlit_app.py` — `page_title` "Ultra Academia | Mapa Territorial" → "Ultra Academia | Expansão".
- **G5:** `.stApp` de 3 radiais+linear → 1 radial sutil + linear; sombras dos cards/dataframe/metric
  baixadas (`0 16px 36px`/`0 22px 48px` → `0 8px 20px`/`0 8px 22px`). TOKENS do GUARDRAIL de CSS
  preservados (verificado pelos 2 testes de inject_styles, verdes).
- **G6:** `.section-card` — removido `min-height:160px`, padding consistente (altura segue conteúdo).

**SIDEBAR / FILTROS**
- **S1:** `render_uf_selectbox` — removido subtítulo "Recorte executivo M1 + Hibrido" e a caption
  redundante; mantido só o header "Filtros globais". (selectbox 1º arg == "UF" e index=None preservados.)
- **S2:** `render_coord_search_sidebar` — `help` de ~6 linhas → 2 linhas (4 formatos resumidos).
- **S3:** `render_sidebar_filters` — removida a 2ª caption duplicada ("M1=decisão municipal…");
  mantida a 1ª (acentuada). Contrato de 8 retornos preservado.

**ABA MAPA**
- **M1:** caption do topo do Mapa Territorial dividida (instrução curta + dica; guardrail em caption menor).
- **M2:** `_CENTROID_DISCLAIMER` já centralizado; sem ação extra necessária além do existente.
- **M3:** rótulos de legenda em `_render_unified_legend` e `render_modelo_hibrido_v2` simplificados
  (de "Score Residual (score_oportunidade_residual)" para "Score Residual" etc.). Tooltip de hover
  ("Score Censitario 2022:") INTOCADO.
- **M4:** sub-cabeçalhos "**Leitura territorial**" e "**Análise de ponto**" agrupando os 6 expanders;
  labels de expander preservados (substrings "Analise"/"Ranking" mantidas; demais acentuados).
- **M5:** cards "Como interpretar os três modelos" em `render_modelo_hibrido_v2` enxugados (5→2-3
  linhas cada) + header/badges acentuados.

**ABA EXECUTIVO**
- **E1:** caption do mapa de presença Ultra encurtada (2 linhas).
- **E2:** removido o trio redundante de baixo (card "UFs a priorizar" duplicado); 2 figuras de UF
  remanescentes rebalanceadas em `st.columns(2)`.
- **E3:** headers acentuados ("Presença Ultra Academia", "Comparativo por UF", "Indicadores médios");
  "Onde evitar expansão" acentuado.
- **E4:** sub-caption agrupando "Rede Ultra e Mercado" e "Comparativo por UF".
- **E5 (components.py):** títulos/labels de figuras legibilizados — "Distribuição de score",
  "Renda × população", "Score médio por faixa de oportunidade", "Top cidades por score médio",
  "Top UFs por oportunidades viáveis", "Melhores e piores UFs por score médio", "Residual potencial
  por UF (alunos)", "Distribuição do score residual", "Top cidades: alto residual, baixa presença
  Ultra", "Comparativo M1 vs Censitário 2022 por hex". SÓ title=/labels; ZERO lógica/tooltip.

**ABA EXPANSÃO DE DOMÍNIO**
- **D1:** caption do topo encurtada; rodapé reduzido a UM card-glossário ("Teses de domínio");
  removido o card "Como ler o plano de dominio". Headers "Hex Ancora"/"Tese" preservados.
- **D2:** removida da caption do mapa a linha "Âncoras coloridas por ordem de abertura…"; cor/ordem
  ficam na legenda (`render_dominio_tese_legend`).

**ABA CARTEIRA E PLANO**
- **C1:** cards de rodapé (Carteira: "Como usar"/"Restrições"; Plano: idem) condensados a ≤4 bullets.
- **C2:** headers acentuados ("Carteira de Expansão — Decisão prática…", "Plano de Expansão — Curto Prazo").
- **C3:** label não-testado "Score Hibrido (apoio)" → "Score Híbrido (apoio)" (atualizado no dict
  display_cols_order E no loop de formatação que o referencia). NÃO toquei "Dens. < 5k"/"Consumo
  Conc. (est.)"/"Consumo Ultra (real)" (travados por teste).
- **C4:** barra de filtros da Carteira de 5 colunas → 3 (UF/Município/Prioridade); "Quartil residual"
  e "Ordenação" movidos para expander "Opções avançadas".

**ABA VIABILIDADE**
- **V1:** dedupe do guardrail "demanda é premissa…": removido da caption do topo e mantido na caption
  do form (perto do input de demanda).
- **V2:** headers encurtados/acentuados ("Viabilidade no cenário pedido", "Faixa de alunos pela metragem").
- **V3:** headers de seção acentuados ("Sensibilidade demanda × aluguel", "Projeção financeira (60 meses)");
  agrupamento já existente preservado.

## Arquivos alterados
- `streamlit_app.py` — só `page_title` (G4).
- `src/motor_expansao/dashboard/pages.py` — inject_styles (G3/G5/G6), render_header (G2),
  render_uf_selectbox (S1), render_sidebar_filters (S3+G1), render_coord_search_sidebar (S2),
  render_visao_executiva (E1/E2/E3/E4), render_analise_territorial (G1), render_modelo_hibrido_v2
  (M3/M5), _render_unified_legend (M3), render_carteira_expansao (C1/C2/C3/C4), render_expansao_dominio
  (D1/D2), render_plano_expansao (C1/C2), render_viabilidade_ponto (V1/V2/V3), render_mapa_territorial
  (M1/M4). SÓ textos/CSS/layout.
- `src/motor_expansao/dashboard/components.py` — SÓ title=/labels de 10 figuras (E5). ZERO lógica.

## Asserts atualizados em tests/integration/test_streamlit_app.py
NENHUM. Todos os itens foram aplicados evitando strings travadas por teste, então não foi preciso
editar nenhum assert. Os 211 testes passam sem modificação no arquivo de teste.

## Validações executadas
- ruff (`pages.py`, `components.py`, `streamlit_app.py`): **All checks passed**.
- mypy (`src/motor_expansao/dashboard`): **Success: no issues found in 12 source files**.
- `import streamlit_app`: **import ok**.
- pytest `tests/integration/test_streamlit_app.py`: **211 passed** (serial via `-p no:xdist`;
  `-n auto`/xdist falha por INTERNALERROR de ambiente Windows/Py3.14 — execnet "cannot send",
  não relacionado ao escopo). Confirmado por grep que nenhum outro arquivo de teste exercita os
  builders de figura tocados (E5).

## Problemas encontrados
- `-n auto` (pytest-xdist) crasha com INTERNALERROR (execnet/OSError "cannot send (already closed?)")
  neste ambiente local (Py3.14/Windows). Rodei serial com `-p no:xdist` conforme instrução do prompt.
  Não é regressão de código.

## Pendências
- Paths pré-sujos (NÃO commitar): `data/outputs/setores_censitarios_2022_geo/_metadata.json`,
  `data/outputs/setores_censitarios_2022_geo/_report.md`.

## Riscos remanescentes
- QA deve rodar a suíte FULL (gate único). Em ambiente onde xdist funcione, validar `-n auto`.
- Mudanças são visuais/texto: revisão visual humana opcional para confirmar o "polimento".

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; nenhuma função de cálculo/builder de dados tocada).
- Artefatos M1 preservados: sim (zero escrita em parquet/artefato).
- Dashboard offline mantido: sim (DEC-004/010/011 inalteradas; nenhuma dependência de rede nova).
- Contratos Blocos 4–6 intocados: sim (load_uf_slice/read_enriched_uf_partition/build_dashboard_dataset/
  render_tab_selector/_downsample_map_index/build_pop_cut_lookup sem mudança).
- Tokens do GUARDRAIL de CSS preservados: sim (test_inject_styles_tab_selector_sticky e
  test_inject_styles_cobre_componentes_baseweb verdes).
- DASHBOARD_TAB_LABELS inalterado: sim.
- Tooltips de hover do mapa inalterados: sim.
