# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador roda o housekeeping no Passo 6.0)

## VEREDITO
APROVADO

## Justificativa
Bloco de DOCS/design (ZERO código de produção, READ-ONLY M1): contrato + OpenAPI + DEC-005 estão escritos, coerentes entre si e com as 6 decisões do gate humano (1=(c), 2=(b), 3=(a), 4=(b), 5=(a), 6=carimbo); nenhum placeholder `<<DECISÃO N>>` remanesceu nos entregáveis; as 4 assinaturas `censo_*` citadas batem exatamente com o código real; git scope só em docs/tasks/CLAUDE.md/README/PRD/context (ZERO em `src/motor_expansao/`); suíte full (679 passed / 1 skipped / 0 falhas / 0 erros de coleta), import, ruff, mypy e YAML well-formed verdes.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- O contrato §5 deixa a "resolução coord→(uf, cod_municipio)" como detalhe de G2/G3 (ponto-em-polígono na malha municipal IBGE). As duas malhas IBGE (`data/raw/ibge/malha_*.geojson`) estão DELETADAS no worktree pré-sujo (não relacionado a este ciclo, não commitar). Apenas um lembrete para os blocos sucessores (BLK-API-03/06): garantir que o artefato de malha usado para essa resolução esteja materializado/restaurado antes de implementar. Não bloqueia o contrato.

## Testes faltantes
- nenhum (bloco de docs; os testes de implementação nascem em BLK-API-02..04). O `tests/unit/test_housekeeping_helper.py` exigido para o fechamento já passa (10 passed).

## Riscos remanescentes
- Ambiente local Python 3.14 difere do CI (3.11): a contagem absoluta de testes varia (679 local vs baseline histórica ~532). Critério aplicado = 0 falhas / 0 erros de coleta, atendido. Sem flakiness por paralelismo observada.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte FULL (gate único do ciclo) — `python -m pytest -n auto -q`
```
679 passed, 1 skipped, 130223 warnings in 122.29s (0:02:02)
```
(0 falhas / 0 erros de coleta. Warnings são DeprecationWarning de ambiente Python 3.14 / pytest-asyncio e UserWarning de CRS geográfico pré-existentes, não defeito.)

### 2. Smoke import — `python -c "import streamlit_app; print('import ok')"`
```
2026-06-10 10:35:16.542 WARNING streamlit.runtime.scriptrunner_utils.script_run_context: Thread 'MainThread': missing ScriptRunContext! This warning can be ignored when running in bare mode.
2026-06-10 10:35:16.579 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
... (warnings de bare-mode esperados) ...
import ok
```

### 3. Ruff — `ruff check src/ tests/`
```
All checks passed!
```

### 4. Mypy — `mypy src/`
```
src\motor_expansao\pipelines\m1\ibge_censo.py:158: note: By default the bodies of untyped functions are not checked, consider using --check-untyped-defs  [annotation-unchecked]
src\motor_expansao\pipelines\m1\ibge_censo.py:159: note: By default the bodies of untyped functions are not checked, consider using --check-untyped-defs  [annotation-unchecked]
src\motor_expansao\pipelines\m1\poi_enrichment.py:51: note: By default the bodies of untyped functions are not checked, consider using --check-untyped-defs  [annotation-unchecked]
Success: no issues found in 46 source files
```
(3 notes informativos pré-existentes; não são erros.)

### 5. YAML well-formed — `python -c "import yaml; yaml.safe_load(open('docs/api_geoespacial_openapi.yaml',encoding='utf-8')); print('yaml ok')"`
```
yaml ok
```

### 6. Git scope / prova READ-ONLY — `git status --porcelain` + `git diff HEAD --name-only`
```
 M CLAUDE.md
 M PRD.md
 M README.md
 M context/handoff.md
 D data/raw/ibge/malha_brasil.geojson
 D data/raw/ibge/malha_uf_brasil.geojson
 M tasks/backlog.md
 M tasks/current_task.md
?? context/handoff/20260610-101519-block-orchestrator.md
?? context/handoff/20260610-101938-planner.md
?? context/handoff/20260610-103258-builder.md
?? docs/api_geoespacial_contrato.md
?? docs/api_geoespacial_openapi.yaml
----DIFF----
CLAUDE.md
PRD.md
README.md
context/handoff.md
data/raw/ibge/malha_brasil.geojson
data/raw/ibge/malha_uf_brasil.geojson
tasks/backlog.md
tasks/current_task.md
```
Scope SÓ em docs/tasks/CLAUDE.md/README/PRD/context. ZERO em `src/motor_expansao/` (nenhum `api/__init__.py`; pasta `api/` NÃO criada, como o Planner recomendou e Felipe não pediu). As 2 deleções `data/raw/ibge/*.geojson` são o worktree pré-sujo (não relacionadas ao ciclo); NÃO fazem parte da entrega e NÃO devem ser commitadas.

### Housekeeping (Passo 6.0) — esperado FALHAR antes do fechamento
`python scripts/housekeeping_move_block.py BLK-API-01 --check`:
```
FALHA --check: stub ausente no backlog para BLK-API-01
EXITCODE=1
```
FALHA "stub ausente" ESPERADA antes do fechamento (precedente BLK-CENSO-02/03; o orquestrador roda o helper no Passo 6.0). Confirmado: o heading `### BLK-API-01` ainda existe em `tasks/backlog.md` (linha 444) e NÃO há entrada dele em `tasks/completed.md` (grep sem match) — o Builder NÃO moveu o bloco manualmente.

`python -m pytest -n auto -q tests/unit/test_housekeeping_helper.py`:
```
10 passed, 842 warnings in 2.01s
```

## Auditoria de conteúdo / coerência dos entregáveis
- **6 decisões registradas e coerentes** em DEC-005 (CLAUDE.md §8, linhas 203-216), `docs/api_geoespacial_contrato.md` e `docs/api_geoespacial_openapi.yaml`: 1=(c) JSON+`?formato=pdf`; 2=(b) token por consumidor; 3=(a) MVP `/health`+`/analisar`; 4=(b) `{lat,lng}` E maps_url; 5=(a) raio fixo 1.5 km; 6=carimbo de versão (`versao_contrato`/`versao_score`/`gerado_em`). Sem nenhum `<<DECISÃO N>>` remanescente nos entregáveis (as únicas ocorrências do token estão no snapshot do Planner como INSTRUÇÃO, não como placeholder pendente).
- **Assinaturas REAIS da fronteira importa-não-edita** — conferidas LINHA A LINHA contra o código (leitura, sem editar) e batem 100%:
  - `analisar_ponto_censitario_setores(lat, lng, setores_df, raio_km=RAIO_CENSITARIO_DEFAULT_KM, competitors_df=None, ultra_df=None) -> dict` (censo_point.py:145) ✓
  - `gerar_pdf_relatorio_pontual_censitario(result, mapas=None, *, residual=None, ultra_dir=None) -> bytes` (censo_report.py:511) ✓
  - `render_mapas_censitarios_combinados(lat, lng, setores_df, *, raio_km=..., competitors_df=None, ultra_df=None, width=1000, height=760, basemap=True, logos_dir=None, ultra_logo_dir=None) -> dict[str,bytes]` (censo_map.py:630) ✓
  - `read_censo_geo_partition(base_dir, uf, cod_municipio=None) -> pd.DataFrame` (data.py:131) ✓
- **Decomposição BLK-API-02..07** explícita no contrato (§13) E em `tasks/backlog.md` (headings linhas 528/545/562/577/589/602), com escopo e critérios por bloco; nota de decomposição inserida após BLK-API-01 sem mover blocos não relacionados.
- **Fronteiras registradas**: "importa-não-edita `censo_*`" (contrato §2.2/§4/§14; DEC-005 (ii)) e "on-demand, PostGIS FORA do MVP" (contrato §2.1/§11; DEC-005 (i)).
- **OpenAPI 3.1.0** coerente: GET /health (sem security) + POST /analisar (JSON+PDF, `?formato`, anyOf lat/lng|maps_url, securityScheme bearer, ErrorResponse com enum de códigos). YAML válido.
- **DEC-001..004 INTOCADAS** (DEC-005 é append puro); "Pesos/fórmula/artefatos do M1: INALTERADOS" presente (DEC-005 + contrato §2.5/§2.6/§14). Parâmetros canônicos (H3=7, renda=0.40/pop=0.60, raio 1.5 km, método de interseção) apenas DESCRITOS, não alterados.
- Referência ao scaffold legado `fora_primeira_fase/api_postgis/main.py` (contrato §12): arquivo existe (verificado), não movido nem editado.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock de caminho crítico. Bloco de DOCS — não há tooling de produção a contornar; a suíte full, o import, ruff, mypy e o parse YAML rodaram contra os artefatos reais do repositório, sem flags de bypass. O git scope foi medido contra o HEAD real.

## Conferência cruzada com log do Builder
Bate. O Builder reportou subconjunto (`test_streamlit_app.py` 176 passed) + import + ruff + mypy + git scope; o QA re-executou a suíte FULL (679 passed/1 skipped, superset que inclui o subconjunto do Builder) e as demais validações com resultados idênticos no veredito. A divergência de contagem (176 do subconjunto, 679 do full) é esperada por escopo de coleta, não defeito. Builder declarou "Pasta api/ NÃO criada" — confirmado por git scope.

## Guardrails verificados
- score_priorizacao não alterado: sim (bloco só docs; git diff sem nenhum arquivo em `src/`).
- Artefatos M1 preservados: sim (nenhum artefato tocado; READ-ONLY comprovado por git scope).
- Validações re-executadas pelo QA (sem bypass):
  - pytest -n auto (full) → 679 passed, 1 skipped, 0 falhas
  - import streamlit_app → import ok
  - ruff check src/ tests/ → All checks passed!
  - mypy src/ → Success: no issues found in 46 source files
  - yaml.safe_load openapi.yaml → yaml ok
  - git status/diff → scope só em docs/tasks/CLAUDE/README/PRD/context
  - housekeeping --check → FALHA "stub ausente" (esperada pré-fechamento)
  - test_housekeeping_helper.py → 10 passed
- Escopo respeitado: sim (ZERO código de produção; pasta `api/` não criada; DEC-001..004 intocadas).

## Decisão recomendada
Fechar ciclo. No fechamento (Passo 6.0), o orquestrador DEVE rodar `python scripts/housekeeping_move_block.py BLK-API-01` (mover o bloco de `tasks/backlog.md` para `tasks/completed.md` via helper versionado, NÃO move manual) e em seguida confirmar `--check` OK. Commitar SÓ os paths do ciclo por path (docs/api_geoespacial_*, CLAUDE.md, PRD.md, README.md, tasks/, context/) — NUNCA `git add -A`, para não arrastar as 2 deleções `data/raw/ibge/*.geojson` do worktree pré-sujo.
