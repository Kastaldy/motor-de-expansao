# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-MAP-02 — Filtro de marcas de concorrentes do mapa em menu expansível (fechado por padrão)

## O que foi feito
Em `render_mapa_territorial` (`src/motor_expansao/dashboard/pages.py`), o `st.multiselect("Redes de concorrentes", ...)` (bloco `_show_rede_filter`, antigas linhas 4382–4388) foi envolvido em `with st.expander("Redes de concorrentes", expanded=False):`, com o corpo do multiselect reindentado um nível a mais. Nenhum outro caractere do multiselect foi alterado: `options=_all_redes`, `default=_all_redes`, `format_func=lambda r: COMPETITOR_BRANDS.get(r, {}).get("label", r)` e `key="mapa_territorial_redes_concorrentes"` permanecem idênticos. A lógica BLK-MAP-01 logo abaixo (linhas ~4390–4396: seleção vazia ⇒ `competitors_df_filtered = None`) permanece fora do `with`, no mesmo nível de indentação de antes, consumindo `selected_redes` normalmente. O `if _show_rede_filter:` existente continua controlando se o expander aparece.

## Arquivos alterados
- `src/motor_expansao/dashboard/pages.py` — bloco `_show_rede_filter` em `render_mapa_territorial` (~linhas 4373–4389): multiselect envolvido em `st.expander("Redes de concorrentes", expanded=False)`. Diff isolado a essas linhas (ver `git diff` — 7 linhas removidas / 8 adicionadas, só reindentação + wrapper).

## Validações executadas
- `python -m ruff check .` → **All checks passed!** (0 erros).
- `python -m mypy src` → **Found 7 errors in 7 files** — todos pré-existentes, todos `Library stubs not installed for "requests" [import-untyped]` em arquivos não tocados neste bloco (`dimensionamento/growth_api_client.py`, `pipelines/m1/ibge_censo.py`, `pipelines/m1/poi_enrichment.py`, `pipelines/m1/base_h3_brasil.py`, `api/geo.py`, `api/telegram_bot.py`, `api/service.py`). Zero erros em `dashboard/pages.py`. (Nota: o handoff do BO citava "~6" pré-existentes; a contagem real hoje é 7, mas todos da mesma causa raiz de stub ausente e nenhum em `pages.py` — não é regressão introduzida por este bloco.)
- `python -c "import streamlit_app; print('import ok')"` → **import ok** (só warnings esperados de Streamlit rodando fora de runtime).
- `python -m pytest -q tests/integration/test_streamlit_app.py` → **232 passed, 0 failed, 0 skipped** (serial, sem `-n auto` conforme instrução da máquina). Nenhum assert existente travava label/posição do widget (`grep` por "mapa_territorial_redes_concorrentes"/"Redes de concorrentes" no arquivo de teste não retornou resultados) — teste não precisou ser alterado.
- `git diff --stat` restrito a `pages.py`: 1 arquivo de código alterado, mudança de ~15 linhas (wrapper `with st.expander` + reindentação); zero mudança em pipelines/config/censo/artefatos.

## Problemas encontrados
Nenhum.

## Pendências
Nenhuma.

## Riscos remanescentes
Nenhum identificado. A lógica BLK-MAP-01 (seleção vazia ⇒ esconde concorrentes) foi preservada byte-a-byte fora do `with`; nenhum teste travava a posição/estrutura do widget, então `tests/integration/test_streamlit_app.py` não foi tocado.

## Guardrails verificados
- score_priorizacao não alterado: sim (nenhum arquivo de score/pipeline/artefato tocado).
- Artefatos M1 preservados: sim (mudança 100% em `dashboard/pages.py`, camada de visualização).
- Dashboard offline mantido: sim (nenhuma dependência de rede/API introduzida; `st.expander` é widget nativo do Streamlit).
- Identificadores não acentuados: sim (`key="mapa_territorial_redes_concorrentes"`, `_all_redes`, `selected_redes`, `_show_rede_filter`, `competitors_df_filtered` inalterados; label visível "Redes de concorrentes" já acentuada, mantida).
