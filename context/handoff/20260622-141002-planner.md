# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Builder

## Entendimento da tarefa
Corrigir a constante `RENDA_ZONA_MORTA_MIN` de `3_000.0` para `1_600.0` em `viabilidade_ponto.py` e adicionar dois testes de fronteira do novo limiar para tornar o critério de aceite verificável.

## Plano técnico

1. **Alterar constante em `src/motor_expansao/dimensionamento/viabilidade_ponto.py`, linha 38:**
   - De: `RENDA_ZONA_MORTA_MIN: float = 3_000.0`
   - Para: `RENDA_ZONA_MORTA_MIN: float = 1_600.0`
   - Nenhuma outra linha do arquivo é alterada (a função `flag_zona_morta` e o orquestrador `analisar_viabilidade_ponto` referenciam a constante por nome, não por valor literal — logo propagam automaticamente).

2. **Confirmar ausência de hardcodes de `3000`/`3_000` relativos a `RENDA_ZONA_MORTA_MIN` em `src/` e `tests/`:**
   - Executar: `grep -rn "3[_]0\{3\}\|3000" src/ tests/ | grep -v ".pyc"` e inspecionar cada ocorrência.
   - Verificação esperada: nenhuma ocorrência asserta ou compara com o valor antigo em contexto de zona-morta. Se houver, corrigir para `1600`.

3. **Adicionar dois testes de fronteira em `tests/unit/dimensionamento/test_viabilidade_ponto.py`** (depois do último teste existente):

   ```python
   # ---------------------------------------------------------------------------
   # BLK-DIM-17 — Testes de fronteira do novo limiar RENDA_ZONA_MORTA_MIN = 1600
   # ---------------------------------------------------------------------------

   def test_flag_zona_morta_renda_abaixo_novo_limiar() -> None:
       """renda=1500 < 1600 -> flag_zona_morta=True (fronteira inferior)."""
       out = flag_zona_morta({"pop_captacao": 50000.0, "renda_per_capita_captacao": 1500.0})
       assert out["flag_zona_morta"] is True
       assert "renda<1600" in out["motivo_zona_morta"]


   def test_flag_zona_morta_renda_no_limiar_nao_dispara() -> None:
       """renda=1600 == limiar -> flag_zona_morta=False (limiar inclusivo: < dispara, >= nao dispara)."""
       out = flag_zona_morta({"pop_captacao": 50000.0, "renda_per_capita_captacao": 1600.0})
       assert out["flag_zona_morta"] is False
       assert out["motivo_zona_morta"] == "ok"
   ```

   - Os testes usam `pop_captacao=50000.0` (bem acima de `POP_ZONA_MORTA_MIN=5000`) para isolar o efeito de renda.
   - O motivo esperado em `test_flag_zona_morta_renda_abaixo_novo_limiar` é `"renda<1600"` porque `flag_zona_morta` formata como `f"renda<{int(renda_min)}"` (linha 212 do arquivo fonte).

4. **Validar localmente antes de fechar:**
   - `python -c "from motor_expansao.dimensionamento.viabilidade_ponto import RENDA_ZONA_MORTA_MIN; assert RENDA_ZONA_MORTA_MIN == 1600.0, RENDA_ZONA_MORTA_MIN"`
   - `pytest -q tests/unit/dimensionamento/test_viabilidade_ponto.py` — deve passar todos os testes (existentes + 2 novos).
   - `ruff check src/motor_expansao/dimensionamento/viabilidade_ponto.py tests/unit/dimensionamento/test_viabilidade_ponto.py`
   - `mypy src/motor_expansao/dimensionamento/viabilidade_ponto.py`

## Arquivos afetados
- `/repo/src/motor_expansao/dimensionamento/viabilidade_ponto.py` — linha 38 (constante `RENDA_ZONA_MORTA_MIN`)
- `/repo/tests/unit/dimensionamento/test_viabilidade_ponto.py` — adicionar 2 testes de fronteira ao final

## Dependências
- Nenhuma dependência externa nova. A constante já é exportada em `__all__` (linha 402); a exportação não muda.
- `flag_zona_morta` já formata o motivo com `int(renda_min)` (linha 212), portanto os testes podem assertar `"renda<1600"` sem alteração de lógica.

## Riscos técnicos
- **Risco baixo:** a mudança é uma constante de alerta/exibição; não altera cálculo de margem, payback, ROIC nem qualquer parâmetro do M1.
- Os testes existentes (`test_flag_zona_morta_true` usa `renda=5000.0`; `test_flag_zona_morta_false` usa `renda=6000.0`) não asseriam o valor `3000` — não quebram automaticamente com a mudança, mas os 2 novos testes de fronteira garantem cobertura verificável do limiar correto.
- Confirmar na etapa 2 que não há hardcode residual de `3000` referente à zona morta.

## Critérios de aceite finais
- `RENDA_ZONA_MORTA_MIN == 1600.0` no módulo (verificável via `python -c`).
- `flag_zona_morta({"pop_captacao": 50000.0, "renda_per_capita_captacao": 1500.0})` retorna `flag_zona_morta=True`.
- `flag_zona_morta({"pop_captacao": 50000.0, "renda_per_capita_captacao": 1600.0})` retorna `flag_zona_morta=False`.
- Nenhum assert sobre o valor `3000`/`3_000` referente a `RENDA_ZONA_MORTA_MIN` permanece nos testes sem atualização.
- Suite `pytest -q` verde sem alteração de score, pesos ou artefatos M1.
- `ruff check` e `mypy` limpos nos arquivos alterados.

## Validações obrigatórias
- Comando: `python -c "from motor_expansao.dimensionamento.viabilidade_ponto import RENDA_ZONA_MORTA_MIN; assert RENDA_ZONA_MORTA_MIN == 1600.0"`
- Comando: `pytest -q tests/unit/dimensionamento/test_viabilidade_ponto.py`
- Comando: `pytest -q` (suite completa — critério de fechamento do QA)
- Critério mínimo: 0 falhas, ruff limpo, mypy limpo nos arquivos alterados.

## Fora de escopo
- `src/motor_expansao/config.py` — `RENDA_MIN = 4_500.0` (parâmetro canônico do M1, intocado).
- `src/motor_expansao/dimensionamento/config.py` — intocado.
- Qualquer pipeline, script de scoring, artefato M1 (`brasil_estrutural.parquet`, `brasil_priorizados.parquet`, etc.).
- `score_priorizacao`, `hex_score_estrutural`, carteira, plano curto prazo, plano de domínio.
- Lógica interna de `flag_zona_morta()` — a assinatura e implementação ficam inalteradas; só a constante que lhe é passada como default muda.
- VPS, deploy, segredos, ingestão ao vivo.

## Alerta de aprovação
Não aplicável. Bloco de criticidade **Média**, loop-safe, READ-ONLY sobre o M1. Sem gate humano obrigatório.

## Guardrails ativos
- READ-ONLY sobre M1: não tocar `score_priorizacao`, `hex_score_estrutural`, carteira, plano, artefatos oficiais (CLAUDE.md §5 guardrail permanente).
- `RENDA_MIN = 4_500.0` em `config.py` do M1: INTOCADO (parâmetro canônico §3).
- Nenhum PR sobe com CI quebrado (CLAUDE.md §2).
- Modo loop autônomo: bloco marcado `loop-safe`; sem gate humano interativo.
