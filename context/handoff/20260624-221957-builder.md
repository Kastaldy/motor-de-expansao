# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer (opus 4.8)

## Bloco executado
BLK-TP-01 — Ingestão e contrato da camada de Demanda Revelada (H3, sem PII)

## Gate humano
**APROVADO POR FELIPE SILVA EM 2026-06-24** — adotadas TODAS as recomendações do Planner
para as 6 perguntas; DEC-012 aprovada; **licença plenamente liberada** (procedência confirmada
inclusive para produtização). Sem essa aprovação o ciclo seria abortado (R1) — satisfeita.

## O que foi feito
Camada paralela, READ-ONLY sobre o M1, agregada em H3 res-7 e SEM PII, casável por `hex_id`
com `hexagonos_mercado_mapeado.parquet`. Implementação dos 10 passos do plano do Planner:

1. **Pacote disjunto** `src/motor_expansao/demanda_revelada/` (`__init__.py`, `contrato.py`,
   `ingestao.py`) — NUNCA importa de `pipelines/m1/`, `censo_*` nem `dashboard/`; usa só deps
   da base (pandas/numpy/pyarrow/h3) + `re`/`json` stdlib. **Sem extra `[demanda]`** (pergunta 4).
2. **`contrato.py`** — `VERSAO_CONTRATO="demanda_revelada_v1"`, `H3_RES_CONTRATO=7`,
   `CONTRATO_COLUNAS` = **9 colunas genéricas do backlog** (incl. `n_concorrente_lc`, que faltava
   no rascunho do Planner — corrigido p/ casar o backlog), `COLUNAS_PII_PROIBIDAS` (rede anti-PII).
3. **`ingestao.py`** — `_parse_fonte_html(fonte)` reusa a lógica `grab` do protótipo (varredura
   balanceada de `[`/`{` com escape) e desserializa SOMENTE `CELLS/GYMS_PTS/SF/BANDS` (drop-PII na
   fronteira; qualquer outra `var` é ignorada). `_agregar_h3` agrega por `h3.latlng_to_cell(...,7)`
   (h3 v4) com nomes genéricos do contrato, `gt5km = faixa_idx>=5`, coerção de dtypes, ordenação
   por `hex_id` (reprodutibilidade). `ingerir_demanda_revelada(fonte, destino, escrever=True)`
   valida `_assert_sem_pii` + `_assert_res7` e grava o parquet. Caminho da fonte parametrizável
   (default `NAO_ABRA/...`, gitignored).
4. **Testes** `tests/unit/test_demanda_revelada_ingestao.py` (8 testes) sobre **fixture sintética
   SEM PII** `tests/fixtures/demanda_revelada_fake.html` (versionável via `!tests/fixtures/**`;
   inclui var de ruído `RAW_FAKE` com PII falsa que o parser NÃO lê): parquet legível, contrato
   9 colunas + dtypes, zero PII, res-7, join por `hex_id` (com asserção do valor agregado),
   reprodutibilidade (frames idênticos + ordenados), agregação correta (3 hexes controlados),
   e não-leitura da var de ruído.
5. **Materializado** `data/staging/demanda_revelada_h3.parquet` rodando a ingestão sobre o dump
   real (gitignored — NÃO commitado): **16.575 hexes**, 9 colunas, `membros` total 1.960.742,
   todos res-7, **zero PII**. Join read-only com o Motor: **16.411/16.575 = 99,0% da demanda**
   casa; cobertura **~1,06%** do universo (1.542.531 hexes). Cross-check vs `concorrentes_mapeados`
   (`smart_fit`): 690 hexes SF em ambas as fontes.
6. **Relatório de qualidade** gitignored `data/reports/scratch/demanda_revelada_qualidade.md`
   com os caveats obrigatórios (R3 cobertura SP ~1% do universo; R4 arredondamento ~1 km;
   "não substitui M1/censitário"; anti-PII; procedência/licença liberada).
7. **DEC-012 registrada em CLAUDE.md §8** (status APROVADA por Felipe Silva em 2026-06-24;
   licença plenamente liberada; cláusula de procedência NÃO trava produtização), no estilo das
   DECs existentes.

## Arquivos alterados
- `src/motor_expansao/demanda_revelada/__init__.py` (novo) — API pública do pacote.
- `src/motor_expansao/demanda_revelada/contrato.py` (novo) — contrato 9 colunas + versão + PII proibida.
- `src/motor_expansao/demanda_revelada/ingestao.py` (novo) — parser + agregação H3 + drop-PII + parquet.
- `tests/unit/test_demanda_revelada_ingestao.py` (novo) — 8 testes sobre fixture sintética.
- `tests/fixtures/demanda_revelada_fake.html` (novo) — fixture sintética SEM PII (versionada).
- `CLAUDE.md` §8 — DEC-012 (APROVADA).
- `data/staging/demanda_revelada_h3.parquet` (saída, gitignored — NÃO commitar).
- `data/reports/scratch/demanda_revelada_qualidade.md` (relatório, gitignored — NÃO commitar).
- `context/handoff.md`, `context/handoff/20260624-221957-builder.md`, `tasks/current_task.md`.

## Validações executadas
- pytest (subconjunto, `-n auto`): **217 passed, 2 failed** em
  `tests/unit/test_demanda_revelada_ingestao.py tests/integration/test_streamlit_app.py`.
  - **8/8 dos NOVOS testes** (`test_demanda_revelada_ingestao.py`) **PASSARAM**.
  - As 2 falhas são `test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline`
    e `...plus_code_curto_usa_localidade_mock` — **PRÉ-EXISTENTES e ALHEIAS a este bloco**:
    `pages.render_coord_search_sidebar()` retorna `None` porque o módulo `openlocationcode`
    (extra Plus Code, BLK-UI-09-FU2) **não está instalado neste ambiente local** (Python 3.14).
    Causa confirmada: `import openlocationcode` → `ModuleNotFoundError`. Meu pacote é DISJUNTO
    (`grep demanda_revelada` em dashboard/api/streamlit_app = 0 ocorrências) → não pode afetar
    esse teste. Lacuna de ambiente (CLAUDE.md §5 nota baseline), não defeito. QA confirma no
    ambiente de CI com `.[dev]`/`.[api_mvp]` completo.
- import `streamlit_app`: **ok**.
- import `motor_expansao.demanda_revelada`: **ok** (não puxa `[basemap]`/`[api]`/scraping).
- ruff `src/motor_expansao/demanda_revelada/ tests/unit/test_demanda_revelada_ingestao.py`: **All checks passed** (corrigidos B905 zip-strict + I import-sort).
- mypy `src/motor_expansao/demanda_revelada/`: **Success: no issues found in 3 source files**.

## Problemas encontrados
- 2 falhas pré-existentes do Plus Code em `test_streamlit_app.py` por `openlocationcode` ausente
  no ambiente local (não relacionadas ao escopo; ver acima). Nenhum problema no código do bloco.

## Pendências
- Para o QA reproduzir os 2 testes de Plus Code, instalar `openlocationcode` (já está na base do
  `pyproject.toml`, linha `openlocationcode>=1.0.1`) no ambiente — então passam.
- Regeneração dos parquets paralelos NÃO se aplica (camada nova, autocontida; não toca o pipeline
  de mercado existente).

## Riscos remanescentes
- **R3/R4 (cobertura SP ~1% + arredondamento ~1 km):** documentados no relatório; camada de refino,
  não cobertura nacional; não substitui M1/censitário.
- A fonte real vive em `NAO_ABRA/` (gitignored) — nunca versionada; testes blindados por fixture.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (pacote disjunto, READ-ONLY; nenhuma escrita em artefato M1).
- Artefatos M1 preservados: **sim** (só escreve `data/staging/demanda_revelada_h3.parquet`, gitignored, não oficial).
- Dashboard offline mantido: **sim** (sem API ao vivo; ingestão offline; nenhuma alteração em dashboard/).
- Anti-PII: **sim** (só células agregadas; `COLUNAS_PII_PROIBIDAS` + teste; fonte real não versionada; fixture sintética).
- DEC-001 / DEC-009: **INTACTAS** (não recalibra M1; demanda = insumo observado, não preditor).
