# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento pelo Orquestrador (Passo 6.0, modo AUTO-MERGE com a ressalva de backlog.md resolvida abaixo).

## VEREDITO
APROVADO COM RESSALVAS

## Justificativa
O entregável do Builder (`docs/vulnerabilidade_ma_contrato.md`, 344 linhas / 15 seções, + 1 linha no
índice `docs/README.md`) está 100% correto e completo: cobre os critérios de aceite do Planner, registra
D1–D8 fielmente (D3=Não e D4 com a aritmética de renormalização conferida), preserva os guardrails
READ-ONLY M1 / DEC-012 / DEC-013 / §2, não cria DEC, e as duas âncoras de código citadas conferem no
repositório real. É trabalho SÓ-DOC — nenhuma linha de código de produção. **Ressalva de fechamento (não
é defeito do Builder, não toca o M1):** a branch `ciclo/BLK-MA-01` carrega um commit humano de setup
(`0c2e344`, Vinicius, 11:00, ANTES do Builder) que ADICIONA o epic BLK-MA-01 ao `tasks/backlog.md`; por
isso `git diff main...HEAD` lista `tasks/backlog.md`, o que (i) faz a prova de escopo "só-doc" incluir um
caminho de governança no nível COMMITTED e (ii) quebraria o AUTO-MERGE assumido (o `guard` cai em
governança ao ver `tasks/backlog.md`). Deve ser resolvido pelo Orquestrador no fechamento — ou movendo o
epic-add para um PR de backlog separado (deixando o PR de ciclo puro-doc → auto-merge funciona), ou
mergeando por humano (Média; o guard bloquearia o auto-merge de qualquer modo).

## Problemas críticos (bloqueadores)
- nenhum. (O `score_priorizacao`, `hex_score_estrutural`, pesos `renda=0.40`/`pop=0.60`, carteira, plano
  e artefatos oficiais do M1 estão INTOCADOS; o diff não contém `src/`, `config.py`, `pipelines/m1` nem
  artefato oficial.)

## Problemas médios (não bloqueadores)
- **`tasks/backlog.md` está no diff committed da branch (`git diff main...HEAD` → `tasks/backlog.md`),
  via `0c2e344` — commit de SETUP humano (Vinicius, 11:00) que cria o epic BLK-MA-01 no backlog, ANTES do
  Builder.** Impacto: (a) a prova de escopo "só-doc" só é verdadeira no nível do WORKING TREE (o
  entregável do Builder — `docs/README.md` modificado + `docs/vulnerabilidade_ma_contrato.md` novo — está
  todo em docs/); no nível COMMITTED a branch também mexe em `tasks/backlog.md`; (b) o modo AUTO-MERGE
  assumido NÃO dispara — o `guard` cai em governança ao ver `tasks/backlog.md` (o epic nasceu na branch, e
  não estava em main: `BLK-MA-01` ausente do `main:tasks/backlog.md`, count=0). **Não é defeito do
  Builder** (ele não fez esse commit; seu entregável é 100% doc e ainda está uncommitted) e **não é
  problema de segurança do M1** (é markdown de bookkeeping, não código/score/artefato). Ação a cargo do
  Orquestrador no Passo 6.0: separar o epic-add num PR de backlog (deixando o ciclo puro-doc p/
  auto-merge) OU human-merge do branch.
- **Lacuna na prova de escopo do Builder (referência cruzada).** O Builder validou o escopo só com
  `git status --porcelain` + `git diff --stat` (WORKING TREE) e por isso não enxergou o commit de
  `tasks/backlog.md` já presente na branch. A conclusão "NENHUMA linha de tasks/..." vale para o working
  tree, mas omite o estado committed. Sem impacto no conteúdo do entregável; registrado para o fechamento.

## Melhorias opcionais
- 3 spans de inline-code contêm palavra acentuada em NOTAÇÃO natural (não identificadores):
  `1 = máxima vulnerabilidade` (L84/L157) e `f(score_vulnerabilidade da academia, demanda do hex,
  saturação do hex)` (L235). Não violam o §2 (não são `key=`/enum/coluna/slug/arquivo; os identificadores
  reais — `score_vulnerabilidade`, `score_priorizacao`, `flag_serie_imatura`, `semanas_sem_mudanca` etc. —
  estão todos em ASCII correto). Para higiene máxima do §2, poderiam sair dos backticks (virar prosa), mas
  é puramente estético — sem risco de código e sem regressão.

## Testes faltantes
- nenhum. Bloco SÓ-DOC (ZERO código executável); não há unidade a cobrir. `test_claude_md_size.py`
  (guarda de inchaço do CLAUDE.md) passa e é a checagem relevante.

## Riscos remanescentes
- Riscos de PRODUTO já documentados no próprio contrato (não são riscos de código): sinal 2 (rating)
  `n/d` PERMANENTE no Plano B; ramp-up da série de snapshots (`MIN_SEMANAS=8`/`STALE_SEMANAS=12`;
  `flag_serie_imatura` neutraliza); reconciliação "28 scrapers legados vs 90 coletores" a fazer no
  BLK-MA-02. Nenhum risco de M1.

## Saída literal das validações (re-executadas pelo QA)

### 1. `python -m pytest -q tests/unit/test_claude_md_size.py` — critério: 0 falhas
```
..                                                                       [100%]
============================== warnings summary ===============================
...(101 DeprecationWarning de pytest_asyncio, ambientais — Python 3.14 local)...
2 passed, 101 warnings in 1.79s
```
OK — 0 falhas. O doc novo não incha nem toca o CLAUDE.md.

### 2. Prova de escopo SÓ-DOC (vs base `main`)
```
=== git diff --name-only main...HEAD ===
tasks/backlog.md

=== git diff --name-status main -- . ':(exclude)context/handoff/' ===
M	docs/README.md
M	tasks/backlog.md

=== git status --porcelain ===
 M docs/README.md
?? context/handoff/20260723-111857-block-orchestrator.md
?? context/handoff/20260723-112503-planner.md
?? context/handoff/20260723-114548-builder.md
?? docs/vulnerabilidade_ma_contrato.md
```
Leitura: o ENTREGÁVEL do Builder (working tree) = `docs/README.md` (M) + `docs/vulnerabilidade_ma_contrato.md`
(untracked) → 100% em `docs/`. Os `context/handoff/*.md` são snapshots de bookkeeping do próprio ciclo
(à parte). NENHUM `src/`, `config.py`, `pipelines/m1`, artefato oficial, `PRD.md` ou `tasks/current_task.md`.
**Exceção (ressalva médio):** `tasks/backlog.md` aparece no diff COMMITTED (`main...HEAD`) e no `git diff
main` — via `0c2e344`, commit de setup humano que cria o epic; ver Problemas médios.

Proveniência do commit de backlog:
```
=== commit 0c2e344 ===
VinhoAbencoado | viniciusmateuscostacruz@gmail.com | 2026-07-23 11:00:58 -0300 | docs(backlog): BLK-MA-01 — epic "Score de vulnerabilidade de concorrentes (M&A)", Plano B
files: tasks/backlog.md   (apenas)
BLK-MA-01 em main:tasks/backlog.md  -> count 0 (epic nasceu na branch)
git log main..HEAD --oneline -> 0c2e344 (único commit; o doc do Builder ainda NÃO foi commitado)
```

### 3. `python -c "import streamlit_app; print('import ok')"` — smoke mínimo
```
2026-07-23 11:49:59 WARNING streamlit.runtime.caching...: No runtime found, using MemoryCacheStorageManager
...
import ok
```
OK — nada quebrou no repo.

### 4. Acentuação §2 (revisão do doc)
```
grep -nE '[áàâãéêíóôõúüçÁÀÂÃÉÊÍÓÔÕÚÜÇ]' docs/vulnerabilidade_ma_contrato.md    -> só prosa acentuada
grep -noE '`[^`]*[a-z]_[^`]*[acentos][^`]*`' docs/vulnerabilidade_ma_contrato.md  ->
   235:`f(score_vulnerabilidade da academia, demanda do hex, saturação do hex)`   (notação natural, NÃO identificador)
```
Resultado: prosa em português corretamente ACENTUADA; TODOS os identificadores/enum/nomes de coluna em
ASCII (`score_oportunidade_residual`, `oferta_efetiva_disponivel`, `sam_fitness_potencial`, `hex_id_res7`,
`concorrente_id`, `abrir_agora`, `independente`, `flag_serie_imatura`, `semanas_sem_mudanca`,
`hash_campos_raspados`, `pressao_concorrencial_score_2km`, `score_priorizacao`, `score_vulnerabilidade`).
Nenhum identificador acentuado por engano. Os únicos acentos dentro de backticks são notação de pseudo-
fórmula/anotação (`1 = máxima vulnerabilidade`, `saturação do hex`) — não identificadores; ver Melhorias
opcionais. **§2: PASS.**

### Extras (evidência de conteúdo / housekeeping)
```
wc -l docs/vulnerabilidade_ma_contrato.md           -> 344
grep -cE '^## [0-9]+\.' (...)                        -> 15   (15 seções)
git diff main...HEAD --name-only | grep -c CLAUDE.md -> 0    (CLAUDE.md intocado)

python scripts/housekeeping_move_block.py BLK-MA-01 --is-done -> "BLK-MA-01: ABERTO em tasks/completed.md" exit=1
python scripts/housekeeping_move_block.py BLK-MA-01 --check   -> "FALHA --check: stub ausente no backlog" exit=1
```
`--is-done` exit 1 é ESPERADO: o append `## Fechamento de ciclo — BLK-MA-01` a `tasks/completed.md` é
passo do ORQUESTRADOR, roda DEPOIS deste veredito (pendência de fechamento, não defeito do Builder).
`--check` NÃO é gate no modo AUTO-MERGE (stub diferido por design, BLK-ORQ-24) — a falha "stub ausente"
é o comportamento esperado.

## Aderência de conteúdo (contra os critérios de aceite + gate D1–D8)
- **15 seções previstas:** presentes (## 1..## 15). Contrato dos 6 sinais (tabela seção 4): direção
  ↑vuln, fonte, coluna/artefato, maturidade, tratamento n/d, condicional — 1–4 obrigatórios, 5/6
  opcionais. OK.
- **D3=Não propagado:** sinal 2 = `n/d` PERMANENTE no Plano B, DEFINIDO mas INATIVO no MVP (seção 7 +
  linha 90/142), movido a BLK-MA-07 (seção 13, BLK-MA-03 explicita "Rating NÃO entra aqui (D3=Não)");
  score roda em S1/S3/S4. OK.
- **D4 aritmética conferida:** pesos-alvo `0,15/0,25/0,35/0,25`; efetivos renormalizados no Plano B
  `S1=0,15/0,75≈0,20`, `S3=0,35/0,75≈0,467`, `S4=0,25/0,75≈0,333`; soma 0,20+0,467+0,333 = 1,000. Tabela
  (L191–195) e Registro D4 (L340) corretos. NÃO-preditivo declarado (L153–155). OK.
- **D5:** hex quente = `sam_fitness_potencial` top quartil AND `score_oportunidade_residual < 25`;
  distância k=1 (`h3.grid_disk(k=1)`); INVERSÃO (demanda alta + residual baixo, oposto de `abrir_agora`)
  em destaque (seção 2 blockquote + seção 9); join READ-ONLY no molde
  `enriquecer_outputs_residual_mercado.py:68-82` com asserts (`len`/`validate="many_to_one"`,
  `score_priorizacao` + ranks `rank_brasil`/`rank_uf`/`rank_carteira_brasil`/`rank_carteira_uf` `.equals`).
  OK.
- **D1=Faseado:** MVP hex-level anti-PII; nomeação por-academia deferida (BLK-MA-03) com a nuance factual
  (o `nome` EXISTE nos CSVs brutos, dropado em `_ler_csv_tp_wh` linha 127; dado de negócio vs PII
  DEC-012). Decomposição BLK-MA-02..07 ajustada pelo D3=Não (rating fora de MA-03 → MA-07). OK.
- **Guardrails §5/DEC-012/DEC-013/§2 + SEM DEC:** explícitos (seções 1 e 14). Exemplo de CSV (L250) com
  `sep=";"`; texto reafirma `sep=";"`/`utf-8-sig`. OK.
- **Âncoras de código conferidas no repo real:**
  - `_ler_csv_tp_wh` em `src/motor_expansao/demanda_revelada/concorrentes_densos.py:127` — CONFIRMADO;
    lê `nome`/`latitude`/`longitude`, saída (linhas 139–141) só `hex_id_res7`/`rede_normalizada`/`fonte`,
    sem rating. A citação do doc ("lê `nome` (linhas 130–138), descarta na fronteira, saída linhas
    139–141") bate.
  - `enriquecer_outputs_residual_mercado.py:68-82` — CONFIRMADO; bloco de merge `how="left"` +
    `validate="many_to_one"`, assert de `len`, assert `score_priorizacao.equals`, asserts dos 4 ranks
    `.equals`. Os asserts load-bearing estão dentro de 68-82 (a chamada `base.merge(` abre em 65; sem
    defeito — citação idêntica à do BO/Planner).
- **`docs/README.md`:** 1 linha adicionada na seção "## 1. Modelo, scores e relatórios" (L15), marcada
  `[canônico]`, apontando o novo contrato. OK.

## Conferência de no-bypass
Confirmo: nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock de caminho
crítico. Não há tooling dependente de config/artefatos de produção neste bloco (SÓ-DOC). O
`test_claude_md_size.py` é teste unitário legítimo (guarda de tamanho do CLAUDE.md), não bypass. A suíte
FULL não foi executada porque o bloco é SÓ-DOC (ZERO linha executável alterada) — precedente
BLK-RELPON-12; isto NÃO é bypass (o NO-BYPASS mira verde de tooling obtido por contorno da config real, o
que não se aplica). Escopo só-doc PROVADO por conta própria (item 2 acima).

## Conferência cruzada com log do Builder
Bate no essencial (doc + índice + validações 1/3/4 idênticas: `2 passed`, `import ok`, §2 sem
identificador acentuado). **DIVERGE em 1 ponto:** o Builder provou o escopo com `git status --porcelain`
+ `git diff --stat` (WORKING TREE) e concluiu "NENHUMA linha de tasks/...". Isso é verdadeiro para o
working tree, mas o Builder NÃO checou `git diff main...HEAD` (COMMITTED), que lista `tasks/backlog.md`
via o commit de setup `0c2e344`. A divergência não muda a qualidade do entregável (o Builder não fez esse
commit; é setup humano pré-Builder), mas a prova de escopo do Builder foi incompleta — daí a ressalva de
fechamento.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (bloco SÓ-DOC; nenhum código/artefato/pipeline do M1 tocado; o
  diff committed é `tasks/backlog.md` [markdown] + entregável em `docs/`).
- Artefatos M1 preservados: **sim** (nada em `data/staging`/`data/outputs`/`pipelines/m1`/`config.py`).
- Parâmetros canônicos preservados: **sim** (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0,
  pesos renda=0.40/pop=0.60 apenas CITADOS no doc como INTOCADOS; nenhuma escrita).
- Validações re-executadas pelo QA (sem bypass): `pytest test_claude_md_size` → 2 passed · escopo
  `git diff main...HEAD`/`git diff main`/`git status` → só-doc no entregável + ressalva backlog.md ·
  `import streamlit_app` → import ok · §2 grep → PASS · âncoras de código → confirmadas · helper
  `--is-done`/`--check` → estados esperados.
- Escopo respeitado: **sim quanto ao entregável do Builder** (100% doc); **ressalva** no nível committed
  da branch (`tasks/backlog.md` via commit de setup humano `0c2e344`, não do Builder) — a resolver pelo
  Orquestrador.

## Housekeeping (Passo 6.0) — modo AUTO-MERGE (Média, DEC-016)
- (a) O PR de ciclo NÃO deve tocar `tasks/backlog.md`: **FALHA parcial** — `tasks/backlog.md` está no
  diff committed (`0c2e344`, setup humano). Ação do Orquestrador: separar o epic-add num PR de backlog
  (deixando o ciclo puro-doc → auto-merge) OU human-merge do branch. Não é defeito do Builder.
- (b) Append de fechamento em `tasks/completed.md` com heading `## Fechamento de ciclo — BLK-MA-01`:
  **pendência do Orquestrador** (roda depois deste veredito). `--is-done` retorna "ABERTO" (exit 1) até lá.
- (c) `--check` NÃO é gate aqui (stub diferido por design). A suíte relevante (item 1) está **verde**.

## Decisão recomendada
**Fechar ciclo** (o entregável está aprovado e completo), com o Orquestrador resolvendo no Passo 6.0 a
ressalva de `tasks/backlog.md`: mover o commit `0c2e344` (epic-add) para um PR de backlog separado —
deixando o PR de ciclo puro-doc para auto-mergear — OU mergear o branch por humano (Média; o guard
bloquearia o auto-merge enquanto `tasks/backlog.md` estiver no diff). Em seguida: commit por path do doc
(`docs/vulnerabilidade_ma_contrato.md` + `docs/README.md`) e append `## Fechamento de ciclo — BLK-MA-01`
em `tasks/completed.md`. Não reabrir para o Builder (sem defeito no entregável).
