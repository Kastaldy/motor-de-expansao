# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-DIM-00 — Fundação de dados (Growth API + catchment censitário + base de calibração das maduras + parse do simulador). Camada PARALELA, READ-ONLY sobre o M1. Plano APROVADO no gate humano 2026-06-13 (D1–D7).

## VERIFICAÇÃO DE ESCOPO MASTER — VEREDITO: **CONFIRMADO**
1º fetch real de `/historico-dash-view` (janela `2026-06-01 → 2026-06-13`):
- **88 unidades** retornadas (≫ 1 → login É master de dados; a contradição doc §10.1 vs pré-ciclo está RESOLVIDA a favor do master).
- **54/54** unidades do performance parquet casadas = **100%** após normalização.
- `inauguracao` presente; 1.068 linhas na janela curta.
- **Descoberta de normalização (Risco 3 confirmado e mitigado):** a Growth API anexa sufixo `" - XX"` (sigla UF) ao nome da unidade (ex.: `"AGUAS LINDAS - GO"`), que NÃO existe no performance parquet (`"AGUAS LINDAS"`). `normalizar_unidade()` remove esse sufixo (regex `\s*-\s*[A-Z]{2}$`) → join 1:1 perfeito (54/54). Sem isso o match cru era só 35%.

## Parquets materializados (rede real + geometria real — APROVADO)
Todos em `data/staging/` (gitignored, NÃO commitados):

1. **`growth_api_historico.parquet`** — ingestão `2022-04-01 → hoje`, 51 janelas mensais de `/historico-dash-view` (D7), cache idempotente.
   - **61.844 linhas**, **93 unidades**, range **2022-04-01 → 2026-06-12**.
   - 11/11 colunas mínimas presentes (`unidade, data, faturamento, pagantes, ticket_medio, cancelados, churn, ativos_total, inadimplente, uf, inauguracao`); **0 ausentes**.
   - **% inauguracao real = 100,0%** (meta ≥80%, superada).
   - Backoff 429 disparou e RECUPEROU em produção (validação real do rate limit; alguns windows atingiram o cap de 4 retries mas completaram).
   - 29 colunas no total; `assert_sem_pii` OK (ZERO PII).

2. **`unidades_ultra_catchment.parquet`** — catchment geométrico de 1,5 km (D3) via `analisar_ponto_censitario_setores` (helper NÃO alterado; setores reais por UF via `read_censo_geo_partition`, cache por UF).
   - **54 unidades**, **53 com catchment válido**, **1 sem** (CAMPO LIMPO, lat/lng NULL no perf → NaN + lacuna; comportamento esperado).
   - `pop_captacao` min=1.403 / mediana=35.517 / max=128.326 (realista).
   - Colunas: `unidade, unidade_norm, uf, lat, lng, pop_captacao, renda_per_capita_captacao, n_setores_captacao, raio_km`. `assert_sem_pii` OK.

3. **`base_calibracao_maduras.parquet`** — consolidação por unidade (chave `unidade_norm`).
   - **54 unidades**, **100% inauguracao real**, **54 maduras** (≥8 meses, D4).
   - `meses_desde_inauguracao`: **nunique=29** (NÃO constante → fecha o critério #6 / parte do G1 da DEC-001 na camada paralela), min=15, max=65.
   - Steady-state = mediana dos últimos **6 meses** (D6): `pagantes_steady_state, churn_steady, ticket_steady` (+ `cancelados/ativos_total/inadimplente_steady` auxiliares).
   - Coluna **`lacunas`** presente; **1 unidade com lacunas** (CAMPO LIMPO: `pop_captacao`, `renda_per_capita_captacao`).
   - 22 colunas. `assert_sem_pii` OK.

4. **`simulador_estrutura.json`** — parse do `.xlsx` real (gitignored), metadado paramétrico (sem PII, sem dado financeiro real de unidade). **COMMITABLE (D2)** — versionado por path (exceção no `.gitignore`).
   - **19 drivers** (Simulador E9–E13/J9–J12/N9–N12/R9–R12/G7/I7), **5 ratios DRE**, **5 impostos presumido**.
   - Células-fórmula conferidas: `mensalidade` (J9) e `capex_total` (R9) → `valor_default=None` + `formula` string; `royalties_pct_receita` (F61) → `=Simulador!N11`; `ir_aliquota` (E44) → `=32%*25%`.
   - Valores estáticos conferidos célula a célula contra o arquivo real (E12=0.06, E13=8, N11=0.08, R10=140000, R12='presumido'; ratios F30=0.005, F63=0.02, F67=0.02, F79=0.0105; impostos E38=0.0065, E40=0.03, E42=0.03).
   - **Correção vs handoff do Planner:** os ratios DRE NÃO estão na célula imediatamente abaixo do rótulo; estão na **coluna F** da linha `% Receita Bruta`/`Alíquota` (F30/F61/F63/F67/F79). Mapeamento verificado no `.xlsx` real e codificado.

## O que foi feito (por passo)
- **PASSO 0** — `src/motor_expansao/dimensionamento/{__init__.py, config.py}`: constantes locais (NÃO toca config.py raiz). Valores D1/D3/D4/D6 + `PII_COLUNAS_PROIBIDAS` + diretórios + rate-limit.
- **PASSO 1** — `growth_api_client.py`: `GrowthAPIClient` (login, `_headers`, `_throttle` janela deslizante, `_request` com relogin 401 + backoff 429/5xx via `tenacity`, cache idempotente), `assert_sem_pii`, `normalizar_unidade` (com strip do sufixo UF), `to_dataframe`. Credenciais via `.env` (`load_dotenv`), nunca hardcoded.
- **PASSO 2** — `ingestao.py` (funções puras: `gerar_janelas_mensais`, `iter_janelas`, `concatenar_e_dedup`, `auditar_historico`) + `scripts/ingerir_growth_api.py` (CLI offline: verificação de escopo master que ABORTA se ≤1 unidade; janelamento mensal; `assert_sem_pii` antes do `to_parquet`; auditoria).
- **PASSO 3** — `catchment_batch.py` (`calcular_catchment_unidade`/`calcular_catchment_batch`, raio parametrizável, trata lat/lng NULL, cache de setores por UF) + `scripts/calcular_catchment_maduras.py` (CLI offline).
- **PASSO 4** — `calibracao_maduras.py` (`consolidar_base_calibracao` com steady-state, maturação real, `lacunas`; `resumo_consolidacao`) + `scripts/consolidar_base_calibracao.py` (CLI offline).
- **PASSO 5** — `simulador_parser.py` (`parse_simulador` via openpyxl `data_only=False`; drivers + ratios + impostos; fórmulas preservadas) + `scripts/parse_simulador.py` (CLI offline).
- **PASSO 6** — `.gitignore` (ignore de conteúdo de `data/staging/*` + exceção `!data/staging/simulador_estrutura.json` para o JSON commitable D2; comentário). `pyproject.toml` INTOCADO (todas as deps já na base). Testes-pacote `tests/unit/dimensionamento/` (5 arquivos).

## Arquivos alterados
### Novos
- `src/motor_expansao/dimensionamento/__init__.py` — docstring do módulo paralelo.
- `src/motor_expansao/dimensionamento/config.py` — constantes locais (D1/D3/D4/D6 + PII + rate-limit + diretórios).
- `src/motor_expansao/dimensionamento/growth_api_client.py` — cliente API (auth/throttle/backoff/cache) + `assert_sem_pii` + `normalizar_unidade`.
- `src/motor_expansao/dimensionamento/ingestao.py` — funções puras de janelamento/concat/dedup/auditoria.
- `src/motor_expansao/dimensionamento/catchment_batch.py` — catchment via helper geométrico (não alterado).
- `src/motor_expansao/dimensionamento/calibracao_maduras.py` — consolidação da base de maduras.
- `src/motor_expansao/dimensionamento/simulador_parser.py` — parse paramétrico do `.xlsx`.
- `scripts/ingerir_growth_api.py`, `scripts/calcular_catchment_maduras.py`, `scripts/consolidar_base_calibracao.py`, `scripts/parse_simulador.py` — CLIs offline/manuais.
- `tests/unit/dimensionamento/{__init__.py, test_growth_api_client.py, test_ingestao.py, test_catchment_batch.py, test_calibracao_maduras.py, test_simulador_parser.py}` — 47 testes (mocks; CI-safe, sem rede; `time.sleep` mockado).
- `data/staging/simulador_estrutura.json` — metadado COMMITABLE (D2), versionado por path.
### Alterados
- `.gitignore` — `data/staging/` → `data/staging/*` + exceção do JSON do simulador.
- `tasks/current_task.md` — status Builder concluído.
- `context/handoff.md` + snapshot em `context/handoff/`.

## Validações executadas (saída literal)
- `python -m pytest -q tests/unit/dimensionamento -n auto` → **47 passed**, 4944 warnings (warnings são DeprecationWarning de `pytest_asyncio`/Python 3.14 do ambiente, não do código).
- `python -m ruff check src/motor_expansao/dimensionamento scripts tests/unit/dimensionamento` → **All checks passed!**
- `python -m mypy src/motor_expansao/dimensionamento` → **Success: no issues found in 7 source files**.
- `python -c "import streamlit_app"` → **import streamlit_app ok** (smoke obrigatório).
- `python -m pytest -q tests/integration/test_streamlit_app.py -n auto` → **183 passed** (sem regressão).
- `assert_sem_pii` nos 3 parquets materializados → **ZERO PII** (29/9/22 colunas).
- `git status --porcelain --untracked-files=all | grep -iE "\.parquet|\.env|\.xlsx|growth_api/"` → **nenhum** (só o `simulador_estrutura.json` aparece, por D2).

## Problemas encontrados
- **Normalização de `unidade` (Risco 3):** API anexa sufixo ` - UF`; resolvido em `normalizar_unidade` (verificado 54/54).
- **Mapeamento dos ratios DRE divergia do handoff:** valores estão na coluna F da linha `% Receita Bruta`, não na célula imediatamente abaixo do rótulo. Corrigido contra o `.xlsx` real (F30/F61/F63/F67/F79).
- **Rate limit real:** alguns windows atingiram o cap de retentativas do `tenacity` (5 tentativas) durante a ingestão; mesmo assim a série completou (windows posteriores foram buscados; cache idempotente). Não houve perda de dados (range contínuo 2022-04 → 2026-06).

## Pendências
- Nenhuma para o fechamento do BLK-DIM-00. (DIM-01 aderência, DIM-02 Huff, DIM-03 calculadora seguem fora de escopo.)

## Riscos remanescentes
- A `view` tem 93 unidades na série completa vs 54 no perf; a base de calibração usa as 54 do perf como universo (join `how=left` a partir do perf). As unidades extras da API (incl. `ADMINISTRACAO`) ficam fora — esperado.
- Interpretação semântica de `churn` (valores ~2–5, possivelmente pontos percentuais/contagem, não fração) e de `ticket_medio` é responsabilidade do DIM-01; aqui há pass-through fiel do agregado da API. Documentado.
- 1 unidade (CAMPO LIMPO) sem lat/lng → sem catchment (NaN + lacuna registrada). Insumo de auditoria para o DIM-01.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (READ-ONLY; DEC-001; pesos 0.40/0.60 intocados).
- Artefatos M1 preservados: **sim** (nenhum artefato M1 tocado; `unidades_ultra_performance_hex.parquet` lido READ-ONLY, não sobrescrito).
- Dashboard offline mantido: **sim** (ingestão é offline/batch; nenhuma dependência de API ao vivo no dashboard; `analisar_ponto_censitario_setores`/raio 1,5 km/`setor_censitario_intersecao_area_1p5km` INTOCADOS).
- ZERO PII em disco: **sim** (`assert_sem_pii` antes de cada `to_parquet`; endpoints com PII §8.4–8.10 nunca chamados).
