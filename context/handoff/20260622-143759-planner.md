# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Builder

## Entendimento da tarefa
Corrigir o teto de payback de 60 para 36 meses em `ViabilidadeResult.flag_viavel` (`simulador.py`), atualizar docstring, substituir o texto `"> 60 / nunca"` por `"> 60 meses"` no card de payback do dashboard (`pages.py`), e ajustar os testes afetados (CA-07d: `flag_viavel` passa a ser `False` para payback ~57 meses; novos testes de fronteira e de display).

## Plano técnico

### Passo 1 — `simulador.py` linha 74: atualizar docstring de `ViabilidadeResult`
- Arquivo: `/repo/src/motor_expansao/dimensionamento/simulador.py`
- Localizar a docstring do campo `flag_viavel` (linha 73-74):
  ```
      flag_viavel:
          True se margem_ebitda_pct >= 0.10 E payback_meses <= 60.
  ```
- Substituir `payback_meses <= 60` por `payback_meses <= 36`.

### Passo 2 — `simulador.py` linha 302: corrigir constante de viabilidade
- Arquivo: `/repo/src/motor_expansao/dimensionamento/simulador.py`
- Localizar (linha 302):
  ```python
  flag_viavel = (margem_ebitda_pct >= 0.10) and (payback_meses <= 60)
  ```
- Substituir `60` por `36`:
  ```python
  flag_viavel = (margem_ebitda_pct >= 0.10) and (payback_meses <= 36)
  ```

### Passo 3 — `pages.py` linha 3377: corrigir texto do card "Payback"
- Arquivo: `/repo/src/motor_expansao/dashboard/pages.py`
- Localizar (linha 3377):
  ```python
  f"{format_int(int(payback))} meses" if payback != float("inf") else "> 60 / nunca",
  ```
- Substituir a string do `else` de `"> 60 / nunca"` para `"> 60 meses"`:
  ```python
  f"{format_int(int(payback))} meses" if payback != float("inf") else "> 60 meses",
  ```
- **Semântica preservada:** a janela de simulação continua sendo 60 meses (inalterada); o texto `"> 60 meses"` é semanticamente correto (payback não ocorreu nos 60 meses simulados). Não alterar a janela de simulação.

### Passo 4 — `test_simulador.py`: atualizar CA-07d
- Arquivo: `/repo/tests/unit/dimensionamento/test_simulador.py`
- Localizar `test_flag_viavel_verdadeiro_com_capex_menor` (linhas 214-217):
  ```python
  def test_flag_viavel_verdadeiro_com_capex_menor() -> None:
      """CA-07d: com capex=600k, margem ~18% e payback ~57 meses => flag_viavel=True."""
      r = viabilidade(**VIAVEL)
      assert r.flag_viavel is True
  ```
- Atualizar docstring e assert — payback ~57 meses > 36 ⇒ `flag_viavel` agora é `False`:
  ```python
  def test_flag_viavel_verdadeiro_com_capex_menor() -> None:
      """CA-07d: com capex=600k, margem ~18% e payback ~57 meses => flag_viavel=False (57 > 36)."""
      r = viabilidade(**VIAVEL)
      assert r.flag_viavel is False
  ```
- **Não alterar** `test_payback_finito_com_capex_menor` (CA-07a, linhas 189-193): o assert `r.payback_meses <= 60` verifica o valor numérico do payback (~57), não a flag — permanece correto e intocado.

### Passo 5 — `test_simulador.py`: adicionar testes de fronteira (CA-07e, CA-07f)
- Arquivo: `/repo/tests/unit/dimensionamento/test_simulador.py`
- Inserir APÓS o bloco CA-07 existente (após linha 217, antes do bloco CA-08):
  ```python
  def test_flag_viavel_com_payback_menor_igual_36() -> None:
      """CA-07e: payback <= 36 meses => flag_viavel=True (fronteira do novo limite).

      capex=150_000 com FCF steady ~13k/mes cobre em ~12 meses, bem dentro de 36.
      """
      r = viabilidade(**{**VIAVEL, "capex": 150_000})
      assert r.payback_meses <= 36
      assert r.flag_viavel is True


  def test_flag_nao_viavel_com_payback_entre_37_e_60() -> None:
      """CA-07f: payback entre 37 e 60 meses => flag_viavel=False (zona antes 'viavel', agora nao).

      capex=600_000 => payback ~57 meses (> 36, <= 60) => flag_viavel=False com novo limite.
      """
      r = viabilidade(**VIAVEL)
      assert 36 < r.payback_meses <= 60
      assert r.flag_viavel is False
  ```
  **Nota:** o cenário `VIAVEL` (capex=600_000, payback ~57) cobre CA-07f naturalmente. O capex=150_000 deve produzir payback finito ≤ 36 — verificar numericamente que `150_000 / ~13_000 ≈ 11,5 meses`. Se o valor real diferir, ajustar o capex do CA-07e para garantir payback < 36 (ex.: usar `capex=400_000` e checar; meta: payback ≤ 36). O Builder deve executar `viabilidade(**{**VIAVEL, "capex": 150_000})` mentalmente ou via REPL para confirmar antes de escrever o assert.

### Passo 6 — `test_simulador.py`: adicionar teste de display do card (CA-09)
- Arquivo: `/repo/tests/unit/dimensionamento/test_simulador.py`
- O teste de display NÃO deve importar o Streamlit. Testar a lógica de formatação isoladamente via função pura local ao arquivo de teste.
- A lógica em `pages.py:3377` é:
  ```python
  f"{format_int(int(payback))} meses" if payback != float("inf") else "> 60 meses"
  ```
- Adicionar ao final do arquivo de testes do simulador:
  ```python
  # ---------------------------------------------------------------------------
  # CA-09: display de payback (lógica pura, sem Streamlit)
  # ---------------------------------------------------------------------------

  def _format_payback_display(payback: float) -> str:
      """Replica a lógica de pages.py:3377 de forma pura para teste."""
      if payback != float("inf"):
          return f"{int(payback)} meses"
      return "> 60 meses"


  def test_payback_inf_exibe_60_meses_sem_nunca() -> None:
      """CA-09a: payback=inf => display '> 60 meses', sem 'Nunca' nem '/'."""
      display = _format_payback_display(float("inf"))
      assert display == "> 60 meses"
      assert "Nunca" not in display
      assert "/" not in display


  def test_payback_finito_exibe_numero_meses() -> None:
      """CA-09b: payback finito (ex: 57) => display '57 meses'."""
      display = _format_payback_display(57.0)
      assert display == "57 meses"
  ```
  **Nota de implementação:** `_format_payback_display` é uma função auxiliar LOCAL ao arquivo de teste — não deve ser importada de `pages.py` nem de qualquer módulo de produção (evita dependência do Streamlit nos testes unitários). Ela replica apenas a lógica de formatação de `pages.py:3377`. O Builder não deve mover nem exportar essa função.

### Passo 7 — Validações finais
1. Executar `pytest -q tests/unit/dimensionamento/test_simulador.py` — 0 falhas.
2. Executar `pytest -q` (suite completa) — 0 falhas, 0 regressões.
3. Executar `ruff check src/motor_expansao/dimensionamento/simulador.py src/motor_expansao/dashboard/pages.py tests/unit/dimensionamento/test_simulador.py` — limpo.
4. Executar `mypy src/motor_expansao/dimensionamento/simulador.py src/motor_expansao/dashboard/pages.py` — limpo.

## Arquivos afetados
- `/repo/src/motor_expansao/dimensionamento/simulador.py` (linhas 74, 302)
- `/repo/src/motor_expansao/dashboard/pages.py` (linha 3377)
- `/repo/tests/unit/dimensionamento/test_simulador.py` (linhas 214-217 CA-07d; novos CA-07e, CA-07f, CA-09a/b após linha 217 e no final do arquivo)

## Dependências
- Nenhuma dependência externa nova.
- `ViabilidadeResult` é um `@dataclass` em `simulador.py` — a mudança na constante da linha 302 e na docstring (linha 74) são as únicas alterações de produção.
- A lógica de display em `pages.py:3377` é autossuficiente (expressão ternária inline).

## Riscos técnicos
1. **Confusão entre os dois `flag_viavel`**: o `flag_viavel` nos datasets de hexágonos M1 (`hex_enrichment.py`, `calcular_colunas_mercado.py`, etc.) é completamente distinto de `ViabilidadeResult.flag_viavel`. O Builder deve tocar SOMENTE `simulador.py` — nenhum pipeline M1 deve ser alterado.
2. **CA-07e: capex=150_000 precisa gerar payback ≤ 36 meses**: FCF steady-state com `VIAVEL` é ~13k/mês; `150_000 / 13_000 ≈ 11,5` meses (seguro). Porém a rampa de maturação pode alterar levemente; o Builder deve confirmar com `viabilidade(**{**VIAVEL, "capex": 150_000}).payback_meses` antes de escrever o assert fixo. Alternativa segura: usar `capex=300_000` (~23 meses, ainda < 36) se preferir margem maior.
3. **Janela de simulação de 60 meses não muda**: o loop interno do simulador (`for t in range(1, 61)`) permanece intocado. A alteração é só no critério `flag_viavel`. O texto `"> 60 meses"` no display refere-se à janela, não ao critério.
4. **Nenhum teste de integração (test_streamlit_app.py) asserta a string `"> 60 / nunca"`**: confirmado por grep — nenhum ajuste necessário em `tests/integration/`.

## Critérios de aceite finais
- `viabilidade(**VIAVEL)` (capex=600_000, payback ~57): `flag_viavel is False` (57 > 36).
- `viabilidade(**{**VIAVEL, "capex": 150_000})` (payback ≤ 36): `flag_viavel is True`.
- `payback == float("inf")` → display card = `"> 60 meses"` (sem "Nunca", sem "/").
- Docstring de `ViabilidadeResult.flag_viavel` menciona `payback_meses <= 36`.
- `pytest -q` verde (zero falhas, sem regressões).
- `ruff check` e `mypy` limpos nos arquivos alterados.
- `config.py` (M1 e dimensionamento) intocado; `flag_viavel` dos hexágonos intocado; artefatos oficiais M1 inalterados.

## Validações obrigatórias
- Comando: `pytest -q tests/unit/dimensionamento/test_simulador.py`
- Critério mínimo: 0 falhas (todos os CA-07a..f e CA-09a/b passam)
- Comando: `pytest -q`
- Critério mínimo: 0 falhas, sem regressões na suite completa
- Comando: `ruff check src/motor_expansao/dimensionamento/simulador.py src/motor_expansao/dashboard/pages.py tests/unit/dimensionamento/test_simulador.py`
- Critério mínimo: sem erros
- Comando: `mypy src/motor_expansao/dimensionamento/simulador.py src/motor_expansao/dashboard/pages.py`
- Critério mínimo: sem erros de tipo

## Fora de escopo
- `src/motor_expansao/config.py` (parâmetros M1) — não tocar.
- `src/motor_expansao/dimensionamento/config.py` — não tocar.
- `src/motor_expansao/dimensionamento/viabilidade_ponto.py` — não tocar.
- `src/motor_expansao/pipelines/m1/hex_enrichment.py` e qualquer outro arquivo de pipeline M1 — não tocar (o `flag_viavel` dos hexágonos é independente).
- `src/motor_expansao/pipelines/calcular_colunas_mercado.py` — não tocar.
- Janela de simulação de 60 meses no loop interno do simulador — não alterar.
- Artefatos Parquet oficiais — não regenerar.
- Qualquer arquivo não listado em "Arquivos afetados".

## Alerta de aprovação
Não aplicável — criticidade Média, loop-safe, READ-ONLY sobre M1. Nenhuma aprovação humana necessária antes do Builder.

## Guardrails ativos
- READ-ONLY sobre M1: não recalcula `score_priorizacao`, `hex_score_estrutural`, carteira, plano, artefatos oficiais.
- Visualizações e interações de mapa não podem recalcular ou alterar artefatos oficiais do M1 sem aprovação explícita (§5 CLAUDE.md).
- `config.py` e `PRD.md` são fontes canônicas de parâmetros — não alterar.
- Bloco loop-safe: sem toque em VPS/deploy/segredos/PII/ingestão ao vivo.
- Pesos canônicos: `renda=0.40`, `pop=0.60` — inalterados.
