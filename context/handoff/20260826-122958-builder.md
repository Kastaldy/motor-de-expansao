# Handoff — Builder

## Skill que gerou este handoff
Builder (rodada curta de correção pós-QA)

## Próxima Skill recomendada
Fechamento manual (merge humano — bloco de criticidade **Alta**, exige a label `aprovado-humano`)

## Bloco executado
**BLK-MA-21 — Cron SEMANAL dos agregadores**, branch `ciclo/BLK-MA-21-v2`. Esta rodada NÃO reabriu
escopo nem repensou decisão nenhuma: corrigiu **os cinco problemas médios** do QA
(`context/handoff/20260826-120702-qa.md`) e mais nada. As quatro decisões aprovadas por Felipe em
2026-08-26 seguem exatamente como o Builder anterior (`2f1e415`) as implementou.

## O que foi feito, médio a médio

### M1 — `CLAUDE.md:209` (o conserto OBRIGATÓRIO antes do merge)
A linha-índice do §8 descrevia a DEC-039 pela premissa **mensal**, morta. Reescrita para o que a DEC
decide HOJE, com as **seis** afirmações falsas eliminadas (`grep -c "Cron MENSAL" CLAUDE.md` -> `0`;
`5,98`, `1a terca do mes`, `cronometro de 8 meses`, `>7 dias`, `26 -> 78` -> todos fora):

- `**Cron MENSAL**` -> `**Cron SEMANAL dos agregadores**`, e "o cron semanal fotografa `unidades`" ->
  "o cron **de domingo** fotografa `unidades`" (com as duas cadências semanais, "semanal" deixou de
  identificar qual é qual);
- **(D5)** `RETENCAO_SEMANAS` = **`26`** (piso duro medido **13**; `26` = 2x o piso);
- **(D4)** RECUSA feed velho **>3 dias**, pela régua `min(data_coleta)`;
- **(D1)** TODA terça, 02:00 UTC (`0 2 * * 2`, sem guarda de dia do mês);
- **(D9)** "desde a **1ª SEMANA** (o cronômetro de **8 semanas**)";
- `**Emendada (2026-08-25...)**` -> `**Emenda 1 (2026-08-25...)**`, e entra a **Emenda 2 (2026-08-26,
  aprovada item a item por Felipe)** com D1/D4/D5 + o `MONITOR_AGREGADOR_MAX_DIAS` `45` -> `9` na
  quinta, a maturação em `~8-12 semanas` e o "sem bump de contrato";
- status `APROVADA (emendada)` -> **`APROVADA (emendada 2x)`**.

**Regra do §8 respeitada:** continua **1 linha** por DEC (o arquivo tem 209 linhas, teto 230), o
corpo e as emendas seguem só em `docs/decisions/DEC-039.md`. Por essa mesma regra, **não** coloquei
na linha-índice a blindagem do fallback de `mtime` (M3): é detalhe de implementação, não item de
decisão — ele está documentado no código e no runbook. Nada foi acrescentado ao corpo da DEC:
emendar decisão aprovada não é objeto desta rodada.

### M2 — a trava agora ancora na INSTRUÇÃO, não em qualquer menção ao número
`test_wrapper_e_runbook_citam_a_mesma_retencao`:

```python
- assert f"`{esperado}`" in runbook and "retencao_semanas" in runbook, (...)
+ assert f"| `retencao_semanas` | tem de ser `{esperado}`" in runbook, (...)
```

A substring genérica era satisfeita pela **prosa** de `docs/infra_producao.md:436` e `:449` (a seção
que conta a história do `78` -> `26`), então a linha do checklist podia regredir sozinha. O docstring
passou a registrar por que o assert é ancorado. Prova de mutação abaixo.

### M3 — o fallback de `mtime` passa a ser FAIL-CLOSED contra o futuro
`curadoria_agregadores.py`. Nasce `_idade_do_feed_detalhada(diretorio, agora) -> (idade, régua,
procedência)`, e `idade_do_feed` vira uma casca fina sobre ela — **a assinatura pública NÃO mudou**
(segue `(float | None, str)`), então nenhum chamador nem teste existente quebra. O contrato novo:

| caso | idade | régua |
|---|---|---|
| nenhum CSV | `None` | `indisponivel` |
| `data_coleta` legível (futuro descartado) | `>= 0` | `data_coleta_min` |
| sem data legível, `mtime` no passado | `>= 0` | `mtime` |
| **sem data legível, `mtime` no FUTURO** | **`None`** | **`indisponivel`** |

`_decidir` ganhou o ramo `elif idade is None:` — obrigatório, porque a formatação `{idade:.1f}` do
ramo "feed velho" estouraria com `None`. A mensagem é acionável e nomeia o arquivo, o mtime lido e o
"agora". **Clampar em `0.0` foi rejeitado e o motivo está escrito no código:** `0.0` é exatamente a
leitura de "coleta recém-terminada", e a corrupção passaria por saúde.

Medido antes/depois no mesmo cenário (feed 100% no futuro + mtime 40 dias no futuro):

```
ANTES: idade_do_feed -> (-40.0, 'mtime')          feed_esta_fresco -> (True, -40.0)   => PUBLICAVA
AGORA: idade_do_feed -> (None, 'indisponivel')    feed_esta_fresco -> (False, None)   => RECUSA
```

### M4 — a recusa diz QUAL arquivo e QUAL data
`_data_coleta_mais_antiga` passa a devolver `(date, Path)` e `_csv_mais_novo` devolve
`(mtime, Path)` (`_mtime_mais_novo` virou casca, para `escolher_diretorio_fonte` não mudar). A
procedência nasce **dentro** de `_idade_do_feed_detalhada`, onde se sabe qual régua respondeu — fora
dali, o texto poderia divergir do número. Recusa real, com 28 CSVs e uma linha `1970-01-01` no
`uf13.csv`:

```
feed velho: idade 20691.0 dia(s) pela regua `data_coleta_min`, limite 3.0. `data_coleta` mais
antiga: 1970-01-01 em <...>\TotalPass\csvs\unidades_totalpass_uf13.csv. Fotografar feed nao
recoletado faz o S4 marcar o universo inteiro desta fonte como parado
```

E a do M3:

```
idade INDISPONIVEL em <...>\TotalPass\csvs: mtime no FUTURO: 2026-10-05T12:00:00+00:00 em
<...>\unidades_totalpass_sp.csv, contra 2026-08-26T12:00:00+00:00 de agora. Idade negativa e'
estado impossivel, entao a curadoria recusa em vez de publicar. Confira o relogio da maquina que
coletou, corrija e recolete
```

Nos dois casos a outra fonte **continua publicando** (`fontes_publicadas=['wellhub']`): recusar uma
não derruba a outra.

### M5 — `tasks/backlog.md:1793`
`então ela é **certa, todo mês**` -> `então ela é **certa, toda semana**`. Deixa de contradizer a
linha 1943 do próprio arquivo (`é CERTA, toda semana, para sempre`).

### A correção de premissa do spread — varredura feita, nada a corrigir
Varri `src/`, `docs/`, `scripts/`, `tests/`, `tasks/` e `CLAUDE.md` por `spread`. **Nenhuma
justificativa do limiar `3` fala em "~22h de spread"** — as quatro ocorrências no perímetro do bloco
já usam a medição certa (`0 dia em 83.685 linhas`, `data_coleta` carimbada uma vez antes do
`ThreadPoolExecutor`): `curadoria_agregadores.py:228`, `DEC-039.md:412`, `infra_producao.md:412` e
`current_task.md:85` (que é justamente a nota dizendo que a premissa não se sustenta). As menções a
`~22h`/`~21h45` em `tasks/backlog.md:1881,1918` e nos wrappers são a **duração da coleta**, não o
spread da coluna — corretas, não tocadas.

Aproveitei para escrever a premissa certa onde ela faltava: o docstring de `idade_do_feed` agora diz
que a granularidade de DIA é a razão de o limiar de 3 comportar com folga, e que **não há "spread de
horas" a acomodar**.

### Um extra de 1 linha, dentro do escopo do M3
`docs/infra_producao.md:512` (linha `regua_idade` do checklist do `DRY_RUN`): `indisponivel` **com**
CSVs no diretório passa a ser explicado como "mtime no futuro, relógio torto na máquina que coletou;
corrija e recolete". Sem isso o estado novo apareceria na saída do modo seco sem nenhum significado
documentado para o operador — que é quem aplica na VPS comando a comando.

## Prova de mutação do M2 (colada literalmente)

```
===== BASELINE (sem mutacao) =====
1 passed, 1181 warnings in 0.38s

===== MUTACAO 1: docs/infra_producao.md:515  `26` -> `78` (o risco operacional #1) =====
1 failed, 1181 warnings in 0.58s

===== MUTACAO 2: run_snapshot_agregadores.sh  'tem de ser 26' -> 78 =====
1 failed, 1181 warnings in 0.53s

===== MUTACAO 3: run_snapshot_agregadores.sh  'retencao_semanas=26' -> 78 =====
1 failed, 1181 warnings in 0.53s

===== MUTACAO 4: contrato.py  RETENCAO_SEMANAS = 26 -> 30 =====
1 failed, 1181 warnings in 0.54s

===== ARVORE RESTAURADA =====
(vazio acima = revertido)
```

**As 4 ficam vermelhas.** Antes desta rodada, a mutação 1 — a que o QA chamou de risco operacional
#1 — passava **verde**. Cada mutação foi aplicada por script com `newline="\n"`, medida, e revertida
com `git checkout --`; a árvore voltou ao estado original (`git status --porcelain` dos três
caminhos: vazio).

## Arquivos alterados (7)

| arquivo | o quê |
|---|---|
| `CLAUDE.md` | M1: a linha-índice 209 da DEC-039 (1 linha, `+1/-1`) |
| `tests/unit/test_wrappers_cron_agregadores.py` | M2: assert ancorado na linha do checklist + docstring do porquê |
| `src/motor_expansao/vulnerabilidade/curadoria_agregadores.py` | M3 + M4: `_idade_do_feed_detalhada`, `_csv_mais_novo`, `_data_coleta_mais_antiga -> (date, Path)`, ramo `idade is None` em `_decidir`, procedência nas recusas |
| `tests/unit/vulnerabilidade/test_curadoria_agregadores.py` | 3 testes novos (M3 unitário, M3 ponta a ponta, M4) |
| `tasks/backlog.md` | M5: `todo mês` -> `toda semana` (1 linha) |
| `docs/infra_producao.md` | M3: `indisponivel` explicado na linha `regua_idade` do checklist (1 linha) |
| `docs/decisions/README.md` | M1 (mesma classe): a linha-indice da DEC-039 no indice IRMAO, que carregava as MESMAS afirmacoes mortas (`Cron MENSAL`, `1a terca do mes`, `26 -> 78`, `5,98`) |

`git diff --stat`: **7 arquivos, 184 inserções, 27 deleções**. Nada em `config.py`, `pipelines/m1/`,
artefatos M1, `graphify-out/`, `PRD.md`, VPS ou `../GymScraping`. **Nada commitado** — o commit é
passo do orquestrador.

**Um 7º arquivo, achado durante o M1 e da mesma classe.** `docs/decisions/README.md:52` — o índice
IRMÃO das DECs, apontado por `docs/README.md` — carregava as **mesmas** afirmações mortas que o
`CLAUDE.md:209`: `**Cron MENSAL dos agregadores**`, `1a terca do mes 02:00 UTC`, `retencao 26 ->
78` e `com 26 um feed mensal para em 5,98 observacoes`. O QA não o pegou porque a varredura de
"mensal" dele cobriu `src/motor_expansao/vulnerabilidade/`. Consertar só o `CLAUDE.md` deixaria a
premissa morta viva no índice que o `docs/README.md` manda ler — é literalmente o defeito do M1, no
arquivo ao lado. Reescrito para a cadência SEMANAL, com a Emenda 2 resumida. `grep -c "MENSAL|5,98|
1a terca do mes" docs/decisions/README.md` → `0`.

## Validações executadas

### 1. pytest SERIAL no subconjunto impactado
```
$ python -m pytest tests/unit/test_wrappers_cron_agregadores.py tests/unit/vulnerabilidade/ \
    tests/unit/test_claude_md_size.py tests/contracts/ -q -p no:randomly
862 passed, 5 skipped, 0 failed in 78.72s
```
Mais os leitores de backlog/governança (mudei `tasks/backlog.md`):
```
$ python -m pytest tests/unit/test_garimpeiro_select_block.py tests/unit/test_housekeeping_helper.py \
    tests/unit/test_loop_guard.py tests/unit/test_loop_guard_paths.py -q -p no:randomly
215 passed
```
`-n auto` não foi usado (quebra nesta estação); `-p no:xdist` não foi usado; nada deselecionado.
**A suíte COMPLETA não foi re-rodada** — o QA a mediu em `3768 passed / 34 skipped / 0 failed` há
poucas horas, e este diff acrescenta **3 testes** e não toca nenhum outro caminho (esperado:
`3771 / 34 / 0`). Se o fechamento exigir o gate completo, ele leva ~26 min.

### 2. `python -m ruff check .`
```
All checks passed!
```

### 3. `ruff format` — só nos arquivos que toquei
`python -m ruff format --diff` nos 3 arquivos de código apontou **2 hunks, ambos exclusivamente em
linhas minhas** (uma assinatura que cabia em 1 linha, uma chamada longa em teste). Apliquei os dois
**à mão**, e não rodei `ruff format` sobre nenhum arquivo — o repo tem 269 arquivos fora do format e
o CI roda só `ruff check`. Estado final: `3 files already formatted`.

### 4. `bash -n` nos 3 shells
```
OK  scripts/cron/run_snapshot_agregadores.sh
OK  scripts/cron/run_snapshot_concorrentes.sh
OK  scripts/healthcheck_vps.sh
```
(Não editei shell nenhum; rodei para provar que as mutações 2 e 3 foram revertidas de verdade.)

### 5. CRLF — DEC-017
Medido no **conteúdo versionado** (`git ls-files --eol`), que é o que a DEC rege:
```
i/lf  w/crlf  CLAUDE.md
i/lf  w/lf    docs/decisions/README.md
i/lf  w/lf    docs/infra_producao.md
i/lf  w/lf    src/motor_expansao/vulnerabilidade/curadoria_agregadores.py
i/lf  w/lf    tasks/backlog.md
i/lf  w/lf    tests/unit/test_wrappers_cron_agregadores.py
i/lf  w/lf    tests/unit/vulnerabilidade/test_curadoria_agregadores.py
```
**`i/crlf` = 0** nos 7 arquivos. O `w/crlf` do `CLAUDE.md` é o artefato de checkout desta estação que o QA já havia
registrado (`autocrlf`), não do repositório — e o `Edit` preservou o EOL existente, por isso o diff
do arquivo é de **1 linha** e não do arquivo inteiro. Todo arquivo que escrevi por Python usou
`newline="\n"`.

### 6. `python scripts/loop_guard.py --base origin/main --json`
```
CRITICOS: []
```
Nove violações, **todas `governanca`** (`CLAUDE.md`, `tasks/backlog.md` e os 7 módulos de
`vulnerabilidade/`) — exatamente o mesmo conjunto que o QA mediu, sem nenhuma entrada nova.

### 7. `data/staging/snapshots_concorrentes/` intacta (SHA-256)
```
ANTES:  semana=2026-33/parte-0.parquet  3454901 bytes  ITENS: 1
        TREE_SHA256: e886620e32b959079a57a35b805607cbbba7ac5bb4899d00a795e3e1ce4a9aff
DEPOIS: TREE_SHA256: e886620e32b959079a57a35b805607cbbba7ac5bb4899d00a795e3e1ce4a9aff
```
**IDÊNTICO.** O valor difere do `878d287f...` do QA porque o método de hash é outro (eu incluo o
caminho relativo no digest); o que é comparável — **1 item e 3.454.901 bytes** — bate com a medição
dele exatamente. A partição viva não foi lida por nada além do hash.

### 8. Smoke das duas mensagens novas
Rodado contra clone **sintético** em diretório temporário (nunca contra `../GymScraping`, nunca
contra `data/`). Saídas coladas nas seções M3 e M4 acima.

## Problemas encontrados
- **Nenhum bloqueador.** Um efeito colateral obrigatório que vale registrar: fazer o fallback devolver
  `idade = None` com CSVs presentes tornou alcançável um estado que `_decidir` não previa, e o ramo
  "feed velho" teria estourado com `TypeError` ao formatar `{None:.1f}`. Foi por isso que o ramo
  `elif idade is None:` entrou junto — não é enfeite.
- **`idade_do_feed` teve de ser refatorada, não só emendada.** A mensagem do M4 precisa do arquivo e
  da data, e recomputá-los em `_decidir` criaria dois caminhos que podem divergir — o mesmo defeito
  que o campo `regua_idade` existe para fechar. A saída foi o núcleo detalhado com casca fina, que
  preserva a assinatura pública e não mexeu em nenhum teste existente.

## Pendências
- **Melhoria opcional do QA, NÃO feita** (fora dos 5 médios): `docs/infra_producao.md:317-318` mede a
  maturação em `~2 meses` enquanto `churn_staleness.py:29` e `docs/vulnerabilidade_ma_contrato.md:354`
  medem a mesma grandeza em `~8 semanas`. Não é falso; é mistura de unidades. Padronizar em semanas
  cabe num follow-up.
- **Mojibake** (2 sequências `C3 83 C2` no snapshot de handoff do Builder anterior): cosmético, em
  snapshot de sessão, não tocado.
- Suíte completa não re-rodada nesta pernada (justificativa na validação 1).

## Riscos remanescentes
Os do QA seguem válidos e nenhum foi criado por esta rodada:
- a régua `min` é fail-closed **por decisão** — o custo hoje é zero (spread real `0` dia), e agora a
  recusa aponta o arquivo, que era a parte que faltava para o custo continuar baixo em produção;
- `26 = 2x` o piso `13`: se a taxa de falha semanal por fonte na VPS passar de ~50%, as 13
  observações não chegam. Não é medível daqui;
- baixar a retenção é destrutivo e irreversível na VPS, e o `--dry-run` não mostra o que a poda
  apagaria (inócuo hoje: nenhuma partição acumulada);
- a imagem em produção não é a `v4`; a ordem do runbook (imagem primeiro) segue sendo a defesa;
- **novo, pequeno:** o estado `regua_idade = indisponivel` com CSVs no diretório só aparece com
  relógio torto na origem. É improvável, está documentado no runbook, e a direção de falha é a
  segura (recusa).

## Guardrails verificados
- **score_priorizacao não alterado:** sim — nenhum arquivo de `config.py`, `pipelines/m1/`,
  `*scoring*`, `normalizar_concorrentes.py` ou `calcular_colunas_mercado.py` no diff.
- **Artefatos M1 preservados:** sim — camada READ-ONLY; `tests/contracts/` verde dentro dos 862;
  árvore de staging byte-idêntica por SHA-256 antes e depois.
- **Parâmetros canônicos:** preservados.
- **Nenhum comando na VPS:** confirmado — nada remoto, por via nenhuma (MCP, ssh, scp).
- **`../GymScraping` READ-ONLY:** não foi lido nem escrito nesta rodada; os smokes usaram clone
  sintético em `TEMP`.
- **Acentuação §2:** prosa de operador e docstrings acentuados; **nenhum identificador acentuado**.
  As mensagens de recusa novas seguem o registro ASCII dos vizinhos no mesmo módulo (elas vão para
  log de shell na VPS, exceção de RENDER do §2); nenhum valor bruto novo foi criado — `regua_idade`
  continua com o vocabulário `data_coleta_min` / `mtime` / `indisponivel`.
- **EOL (DEC-017):** `i/crlf` = 0 nos 7 arquivos.
- **Escopo respeitado:** sim — só os 5 médios, mais 1 linha de runbook que é consequência direta do
  M3 e 1 linha do índice irmão das DECs, que é o próprio M1 no arquivo ao lado. Nenhuma decisão reaberta, nenhum bump de contrato, nenhuma alteração no corpo da DEC-039.
- **Nada commitado; `graphify-out/*` e `PRD.md` intocados.**
