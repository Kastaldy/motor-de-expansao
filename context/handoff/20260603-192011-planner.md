# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Approver (criticidade **Alta** — gate humano obrigatório antes do Builder; o fix LÊ colunas de score para colorir/ordenar, CLAUDE.md §2)

## Entendimento da tarefa
Fazer os hexes da orla (Praia Grande/Mongaguá/litoral) APARECEREM visivelmente no dashboard corrigindo APENAS o caminho de render/display — os parquets servidos já contêm a orla (BLK-FIX-06-B/DEC-003); nada de dados, M1, score ou artefatos é tocado.

## Diagnóstico confirmado por leitura de código (file:line)
Três mecanismos de display ocultam a orla. Todos verificados no código atual:

- **(A) Cinza alpha-70 invisível por corte de 5k** — `components.py:1107` `_DISCARDED_FILL=[120,120,140,70]`; `_apply_pop_cut_colors` (`components.py:1111-1125`) pinta com esse cinza todo hex com `flag_pop_min_5k=False`. Orla tem pop baixa (Mongaguá centro ~2.416) → `flag_pop_min_5k=False` → cinza alpha-70 quase invisível no fundo CARTO_DARK. **Afeta TODOS os modos** (M1 chama em `:1414`, Híbrido em `:1665`, Residual em `:1793`). Hex EXISTE no deck mas é invisível.
- **(B) Descarte por NaN de setor censitário ANTES do cap** — `build_hybrid_map_figure` scope `hdf["score_setor_2022_calibrado"].notna()` (`components.py:1582`) e `build_residual_heatmap_figure` scope idem (`components.py:1754`). Hexes costeiros sem setor censitário (NaN) são REMOVIDOS do `scope` → somem dos modos Híbrido, Censitário e Residual (o dispatcher `build_unified_map_figure:2942-2954` roteia Censitário e Híbrido para `build_hybrid_map_figure`). M1 NÃO sofre disso (scope é `score_priorizacao.notna()`, `:1339`).
- **(B') NaN também recai no cinza invisível mesmo se o scope for relaxado** — `score_band_to_color` (`utils.py:37-38`) retorna o MESMO `[120,120,140,70]` para score NaN. Logo, relaxar o scope de (B) sem tratar a cor de NaN só troca "ausente" por "invisível". O fix de (B) precisa vir acompanhado de uma cor de fallback visível para NaN.
- **(C) Filtro hard nas TABELAS de carteira/domínio** — `pages.py:1279` e `pages.py:1666` fazem `view = view[view["flag_pop_min_5k"]]`, removendo a orla das tabelas (não do mapa). Texto "removidas pela régua de 5.000" em `pages.py:1273-1278`.
- **(D) Cap `MAP_POINT_LIMIT_LARGE=18000`** (`constants.py:103`): só morde quando o recorte tem >35k linhas (`MAP_POINT_LIMIT=35000`, `constants.py:98`). Com filtro de cidade ativo o recorte é pequeno → cap NÃO morde. O BO confirma esse cenário. **Não alterar o cap** (fora de escopo salvo prova de que morde; o sort por `score_priorizacao` é LEITURA, não recálculo).

## Plano técnico

### Parte 1 — Cor do "descartado <5k" visível e distinta (mecanismo A) [DECISÃO DE PRODUTO i]
1. Em `src/motor_expansao/dashboard/components.py:1107`, substituir `_DISCARDED_FILL = [120, 120, 140, 70]` pela cor recomendada **`_DISCARDED_FILL = [150, 150, 170, 150]`** (cinza-azulado, alpha 150 ≈ 0.59 — visível no CARTO_DARK, ainda nitidamente distinto das faixas de score saturadas). Manter `_DISCARDED_LINE = [120, 120, 140, 180]` ou subir para `[170, 170, 190, 200]` para reforçar contorno (recomendado subir junto). ANTES: alpha 70 (invisível). DEPOIS: alpha 150 (visível como "descartado").
2. Em `_apply_pop_cut_colors` (`components.py:1111-1125`): nenhuma mudança de lógica — continua marcando `discarded = ~flag_pop_min_5k.fillna(True)` e aplicando `_DISCARDED_FILL`/`_DISCARDED_LINE`. Só a constante muda. A distinção acionável-vs-descartado é PRESERVADA (descartado segue com cor própria, agora visível).
3. Em `render_pop_cut_legend` (`components.py:1128-1136`): atualizar o `background` do dot e a opacidade do chip de legenda para casar com a nova cor (ex.: `background:#9696AA;opacity:0.59;`), mantendo o texto "Descartado &lt;5k hab (mapa)". ANTES: `#78788C;opacity:0.7`. DEPOIS: cor/opacidade espelhando `_DISCARDED_FILL`.

### Parte 2 — Não descartar hexes costeiros sem setor censitário nos modos operacionais (mecanismos B/B') [DECISÃO DE PRODUTO ii]
4. Em `build_hybrid_map_figure` (`components.py:1576-1592`): relaxar o gate de entrada.
   - Manter o guard `if "score_setor_2022_calibrado" not in hdf.columns: return None, 0` (`:1576-1577`) — isso é "coluna ausente", não "valor NaN".
   - Alterar o `scope` em `:1581-1585` de
     `scope = hdf["score_setor_2022_calibrado"].notna() & hdf["lat"].notna() & hdf["lng"].notna()`
     para `scope = hdf["hex_id"].notna() & hdf["lat"].notna() & hdf["lng"].notna()`
     (deixa de exigir setor censitário; passa a exigir só geometria válida). ANTES: orla sem setor sai do scope. DEPOIS: orla entra no scope e é renderizada.
5. Em `build_residual_heatmap_figure` (`components.py:1753-1757`): mesma troca de `scope` (de `score_setor_2022_calibrado.notna()` para `hex_id/lat/lng notna()`). ANTES→DEPOIS idêntico ao passo 4.
6. **Cor de fallback para score NaN** (necessário por causa de B'): introduzir em `components.py` um `_NAN_SCORE_FILL` visível (recomendado **`[110, 116, 140, 150]`** — azul-acinzentado neutro, alpha 150, distinto tanto das faixas de score quanto do "descartado <5k") e aplicá-lo onde hoje o `score_band_to_color`/mapeamento de fill gera cor a partir de score que pode ser NaN:
   - M1 (`components.py:1407`): `map_df["fill_color"] = map_df["score_priorizacao"].map(score_band_to_color)` — `score_priorizacao` da orla NÃO é NaN, então M1 não precisa de fallback; **não alterar** este ponto (evita mexer onde não há problema).
   - Híbrido (`components.py:1620-1621`): `_color_src` vem de `color_col` (`score_expansao_hibrido` ou `score_setor_2022_calibrado`) que pode ser NaN na orla. Após o `.map(score_band_to_color)`, sobrescrever as linhas de score NaN com `_NAN_SCORE_FILL` (ex.: `_nan_mask = _color_src.isna(); map_df.loc[_nan_mask, "fill_color"] = [_NAN_SCORE_FILL]*int(_nan_mask.sum())` — atribuição linha-a-linha para preservar lista RGBA por célula, seguindo o padrão de `_apply_pop_cut_colors`).
   - Residual (`components.py:1781-1784`): idem para `score_oportunidade_residual` NaN — substituir o ramo `else [[120,120,140,70]]` por `_NAN_SCORE_FILL` e cobrir o caso de coluna presente com valores NaN.
   - **Ordem importa:** aplicar o fallback de NaN ANTES de `_apply_pop_cut_colors` (que roda em `:1665`/`:1793`), para que um hex de orla NaN+pop<5k receba a cor de "descartado <5k" (mecanismo A já o cobre e tem prioridade visual de produto). Documentar essa precedência em comentário.
7. **Tooltip honesto para NaN**: onde o tooltip formata score (`format_score` já devolve "-" para NaN — confirmar em `build_hybrid_map_figure:1630-1632`), garantir que o hex de orla sem setor mostra "Score Censitário: -" em vez de erro. Nenhuma mudança esperada se `format_score` já trata NaN; o Builder deve confirmar e, se necessário, ajustar SÓ a string.

### Parte 3 — Tabelas (mecanismo C) [DECISÃO DE PRODUTO iii — default: NÃO mexer nas tabelas neste ciclo]
8. **Recomendação default: NÃO alterar `pages.py:1279`/`:1666`.** As tabelas de carteira/domínio são a régua operacional acionável de 5k e o texto explicativo (`pages.py:1273-1278`) é honesto sobre isso. O critério de aceite primário é o MAPA. Deixar registrado como decisão de produto: se Felipe quiser a orla também nas tabelas, abrir como toggle em ciclo separado (evita inchar o escopo Alta atual). Se aprovado AGORA, a alternativa é trocar o filtro hard por um `st.toggle("Incluir <5k hab", value=False)` controlando `if not incluir: view = view[view["flag_pop_min_5k"]]` — mas isso é UX nova e fica fora do default.

### Parte 4 — Testes
9. **Atualizar** `tests/integration/test_streamlit_app.py::test_build_map_figure_pinta_hex_descartado_por_pop_com_cor_neutra` (`:1235-1260`): os asserts `== [120,120,140,70]` (`:1254`) ficam obsoletos. Trocar para asserir a NOVA `_DISCARDED_FILL` importando a constante do módulo (`from motor_expansao.dashboard.components import _DISCARDED_FILL`) e asserindo `rendered.loc[hex_bad, "fill_color"] == _DISCARDED_FILL` e que `_DISCARDED_FILL[3] >= 140` (alpha visível) e `!= [120,120,140,70]`. Manter os asserts de tooltip "Descartado".
10. **Novo teste** `test_build_map_figure_orla_baixa_pop_renderiza_visivel` em `test_streamlit_app.py`: monta `df` com um hex de orla `flag_pop_min_5k=False`, `score_priorizacao` válido; chama `build_map_figure`; assere (a) o hex está em `deck.layers[0].data` (NÃO descartado do dataset), e (b) `fill_color` tem alpha `>= 140` (visível), `!= [120,120,140,70]`.
11. **Novo teste** `test_build_hybrid_map_figure_renderiza_hex_sem_setor_censitario` em `test_streamlit_app.py`: `df` com um hex `score_setor_2022_calibrado=pd.NA`, `score_expansao_hibrido=pd.NA`, geometria válida, `flag_pop_min_5k=True` (isolar B' de A); chama `build_hybrid_map_figure(...)`; assere (a) `points >= 1` e o `hex_id` presente no layer (NÃO mais descartado por NaN), e (b) `fill_color == _NAN_SCORE_FILL` com alpha visível `>= 140`.
12. **Novo teste** `test_build_residual_heatmap_figure_renderiza_hex_sem_score_residual`: análogo ao 11 para `build_residual_heatmap_figure` com `score_oportunidade_residual=pd.NA`; assere presença no layer + `fill_color == _NAN_SCORE_FILL`.
13. **Novo teste** `test_apply_pop_cut_colors_usa_alpha_visivel`: chama `_apply_pop_cut_colors` num `df` com `flag_pop_min_5k=False` e `fill_color` pré-setado; assere que o resultado é `_DISCARDED_FILL` e `_DISCARDED_FILL[3] >= 140`.
14. **Sanidade de não-regressão de score** (já coberto por `test_pop_cut.py::test_nao_altera_score_priorizacao`): garantir que nenhum teste novo escreve em `score_priorizacao`/`hex_score_estrutural`; os builders só LEEM.

## Arquivos afetados
- `src/motor_expansao/dashboard/components.py` (constantes `_DISCARDED_FILL`/`_DISCARDED_LINE`, novo `_NAN_SCORE_FILL`, `render_pop_cut_legend`, `build_hybrid_map_figure` scope+fallback, `build_residual_heatmap_figure` scope+fallback)
- `tests/integration/test_streamlit_app.py` (1 teste atualizado + 4 novos)
- `CLAUDE.md` §5 (nota de fechamento de ciclo — APENAS o QA/Builder ao concluir)
- `tasks/current_task.md` (tracking — já atualizado pelo Planner)

NÃO afetados (confirmado): `constants.py` (cap/POP_MIN_ACIONAVEL inalterados), `data.py` (`derive_pop_cut_columns` inalterada — a lógica do flag está correta; só a COR muda), `pages.py` (default = não mexer nas tabelas), `utils.py` (`score_band_to_color` inalterado — o fallback de NaN é aplicado nos builders, não dentro do helper, para não mudar outros consumidores), pipelines M1, artefatos.

## Dependências
- Nenhuma dependência de dados nova: os parquets servidos já têm a orla (verificado via docker exec, 2026-06-03). Nenhuma regeneração/redeploy.
- `score_band_to_color` (`utils.py`) e `format_score` permanecem como estão; o fallback de NaN vive nos builders.

## Riscos técnicos
1. **Trocar o scope de notna() para hex_id/lat/lng pode aumentar o nº de hexes nos modos Híbrido/Residual** em UFs grandes, podendo encostar no cap `MAP_POINT_LIMIT_LARGE`. Mitigação: o cap continua ativo e ordena por relevância; com filtro de cidade (caso do Felipe) o recorte é pequeno. NÃO alterar o cap. Builder deve rodar os testes de payload enxuto existentes (`:1303`, `:1370`) para confirmar sem regressão de serialização.
2. **Atribuição de lista RGBA por célula no pandas**: usar o padrão linha-a-linha de `_apply_pop_cut_colors` (list-comprehension com `zip`), NÃO `df.loc[mask, "fill_color"] = [r,g,b,a]` direto (pandas tenta broadcast e quebra com coluna de listas). Especificado no passo 6.
3. **Precedência de cor** (NaN-score vs descartado-<5k): definido que `_apply_pop_cut_colors` roda DEPOIS do fallback de NaN, então um hex NaN+pop<5k termina com a cor de "descartado". Builder não deve inverter a ordem.
4. **Distinção visual**: `_DISCARDED_FILL` (descartado <5k) e `_NAN_SCORE_FILL` (sem score) devem ser cores DIFERENTES e ambas distintas das 10 faixas de `RESIDUAL_SCORE_BANDS`. As cores recomendadas (`[150,150,170,150]` vs `[110,116,140,150]`) atendem; se Felipe preferir uma só "cor neutra de exceção", colapsar para uma só (decisão de produto i+ii).

## Critérios de aceite finais
1. **(primário)** No modo **M1**, com filtro em Praia Grande/Mongaguá, os hexes da orla aparecem com cor VISÍVEL (não cinza alpha-70, não ausentes) após `Ctrl+Shift+R`. Verificável por teste: hex de orla `flag_pop_min_5k=False` presente no layer com `fill_color` alpha `>= 140`.
2. Nos modos **Híbrido/Censitário/Residual**, hexes costeiros SEM setor censitário (`score_*` NaN) aparecem com cor de fallback visível em vez de sumir. Verificável pelos testes 11-12.
3. `score_priorizacao`, `hex_score_estrutural`, carteira, plano e artefatos oficiais M1 INALTERADOS (nenhuma escrita; só leitura para colorir/ordenar).
4. Suite verde: `pytest -q` sem novos falhos (baseline 2026-05-28: `532 passed, 1 skipped`; local pode reportar contagem diferente por ambiente — critério é 0 falhas / 0 erros de coleção).
5. `import streamlit_app` sem erro.
6. Sem regressão de performance: cap e downsample inalterados; testes de payload enxuto (`:1272`, `:1303`, `:1370`) seguem verdes.

## Validações obrigatórias
- Comando: `python -m pytest -q tests/integration/test_streamlit_app.py` | Critério mínimo: 0 falhas, incluindo os testes atualizados/novos (passos 9-13).
- Comando: `python -m pytest -q tests/unit/test_pop_cut.py` | Critério mínimo: 0 falhas (garante que `derive_pop_cut_columns`/flag não regrediram).
- Comando: `python -c "import streamlit_app"` | Critério mínimo: import sem erro (smoke).
- Comando: `python -m pytest -q` | Critério mínimo: 0 falhas / 0 erros de coleção (gate full no QA; baseline `532 passed, 1 skipped`).
- Lint/tipos (paridade CI): `python -m ruff check src tests` e `python -m mypy src` | Critério mínimo: limpos (instalar `types-requests` antes do mypy — lição feedback_mypy_paridade_ci).

## Fora de escopo
- `base_h3_brasil.py`, qualquer pipeline M1, artefatos oficiais (BLK-FIX-06-B fechou; NÃO reabrir).
- Regenerar parquets / redeploy de dados ao VPS (dados já corretos e servidos).
- Mudar pesos/fórmula de `score_priorizacao`/`hex_score_estrutural`/qualquer score; `config.py`.
- Alterar `MAP_POINT_LIMIT`/`MAP_POINT_LIMIT_LARGE`/`POP_MIN_ACIONAVEL` (cap e régua de negócio inalterados).
- Alterar `score_band_to_color` em `utils.py` (o fallback de NaN vive nos builders para não afetar outros consumidores).
- Tabelas de carteira/domínio (`pages.py:1279`/`:1666`) — default é NÃO mexer; se Felipe pedir, vira ciclo/toggle separado.

## Decisões de produto para o gate humano (Felipe decide ANTES do Builder)
- **(i) Cor do "descartado <5k" no mapa.** Default recomendado: subir `_DISCARDED_FILL` de alpha 70 → **alpha 150** (`[150,150,170,150]`), mantendo cor cinza-azulada distinta das faixas de score. Justificativa: torna a orla visível preservando a semântica "descartado". Alternativas: (a) manter cinza mas alpha ~110 (mais discreto); (b) usar uma cor de borda/outline forte em vez de fill (chama atenção sem competir com score). 
- **(ii) Tratamento de score NaN nos modos operacionais.** Default recomendado: **relaxar o scope** (parar de descartar por `score_setor_2022_calibrado` NaN) **+ cor de fallback** `_NAN_SCORE_FILL=[110,116,140,150]`. Justificativa: o critério de aceite 2 pede a orla visível nos modos operacionais mesmo sem censo. Alternativa: manter o descarte (orla aparece SÓ no M1) — mais conservador, mas não atende o aceite 2.
- **(iii) Corte de 5k nas TABELAS de carteira/domínio.** Default recomendado: **NÃO mexer** neste ciclo (mapa é o aceite primário; tabela é régua acionável honesta). Alternativa: trocar o filtro hard por um `st.toggle("Incluir <5k hab", default off)` — UX nova, sugerido para ciclo separado.
- **Opcional (i+ii):** colapsar `_DISCARDED_FILL` e `_NAN_SCORE_FILL` numa única "cor neutra de exceção" se Felipe preferir simplicidade visual a distinguir os dois casos.

## Alerta de aprovação
ATENÇÃO: esta tarefa é **ALTA** — o fix LÊ colunas de score (`score_priorizacao` no sort/cap via `MAP_SORT_COLUMNS`; `score_setor_2022_calibrado`/`score_expansao_hibrido`/`score_oportunidade_residual` para colorir e definir scope nos builders). Conforme CLAUDE.md §2, "LEITURA/ANÁLISE de score sem escrita em artefato M1 → Alta (revisão humana antes do Builder)". APROVAÇÃO HUMANA OBRIGATÓRIA ANTES DO BUILDER. O usuário deve ler este handoff, escolher as decisões de produto (i/ii/iii) e confirmar. NENHUMA escrita em `score_priorizacao`, `hex_score_estrutural`, carteira, plano ou artefatos M1 ocorre — se qualquer escrita desse tipo surgir na execução, a tarefa VIRA CRÍTICA e deve parar para aprovação + DEC.

## Guardrails ativos
- CLAUDE.md §2: "LEITURA/ANÁLISE de score sem escrita em artefato M1 → Alta (revisão humana antes do Builder)"; "ALTERAÇÃO de fórmula, pesos, ou qualquer artefato M1 → Crítica (aprovação obrigatória + DEC)".
- CLAUDE.md §3: pesos `renda=0.40`/`pop=0.60` e fórmula `score_priorizacao`/`hex_score_estrutural` INALTERADOS (DEC-001 vigente).
- CLAUDE.md §5 guardrail permanente: "visualizações, análise radial e interações de mapa não podem recalcular ou alterar score_priorizacao, hex_score_estrutural, carteira, plano curto prazo, plano domínio ou artefatos oficiais do M1 sem aprovação explícita."
- CLAUDE.md §6: nunca executar comandos no VPS sem confirmação explícita por comando (este ciclo NÃO toca o VPS).
- NÃO reintroduzir falso diagnóstico de "dados faltando" — os dados ESTÃO nos parquets servidos; o trabalho é de RENDER.
