# Handoff — Builder (BLK-RELPON-10, reconstruido no fechamento)

## Nota de procedencia
O Builder do BLK-RELPON-10 foi INTERROMPIDO a pedido de Vinicius em 2026-07-21, no passo de gerar
os PDFs de amostra, e por isso nao gravou o proprio snapshot. Este arquivo o reconstroi a partir do
codigo commitado e das validacoes reexecutadas em 2026-07-22, para o rastro versionado nao ficar com
buraco entre o Planner (`20260721-152739-planner.md`) e o QA (`20260722-095101-qa.md`). O QA
registrou essa ausencia como pendencia; e este o fechamento dela.

## Skill que gerou este handoff
Builder (reconstruido pelo orquestrador no fechamento do ciclo)

## Proxima Skill
Nenhuma — ciclo fechado. QA APROVADO + gate visual de Vinicius APROVADO em 2026-07-22.

## O que foi implementado
Slide novo "Socioeconomia e Residual Fitness" ANTES do "Mapas de calor" do Relatorio Pontual
Censitario, com 2 mapas lado a lado e o raio ROTULADO em cada um. PDF: 6 -> 7 paginas nas duas
variantes (`censitario` e `classico`).

- `censo_map.py`: camada `socioeconomia` (mesmo choropleth de `score_setor_2022_calibrado`, 1,5 km)
  e camada `residual` (`oferta_efetiva_disponivel` em ALUNOS, por hexagono H3 res-7, raio de
  EXIBICAO `RAIO_RESIDUAL_DISPLAY_KM = 5.0`, disco `h3.grid_disk` k=5, clip ao frame). Projecao por
  `pyproj`/`_transformer`, a convencao que o modulo ja usa; `h3` com import lazy.
- `constants.py`: `OFERTA_DISPONIVEL_ALUNOS_BANDS` — 6 faixas absolutas, cortes
  0 / 1.250 / 2.500 / 5.000 / 10.000 / inf, ancoradas em 2.500 alunos = 1 unidade (§4).
- `censo_report.py`: slide novo + gemea `_classico_*`; `_draw_maps_grid` generalizado com
  `cols`/`rows` (defaults preservam o 2x2 byte-a-byte); `PDF_SECTION_HEADERS` de 6 -> 7.
- `api/service.py`: `_hexes_vizinhos_do_ponto` (filtro pyarrow por 91 chaves, nao carrega 1,5 M).
- `pages.py`: passa o `df` ja em escopo como `hexes_df` nas 3 chamadas.
- `CLAUDE.md` §4: corrigido (dizia "5 paginas / tira 1x3"; a realidade ja era 6 paginas / grid 2x2
  ANTES deste bloco, e passa a 7 depois dele).

## Decisoes do gate humano (Vinicius, 2026-07-21) — ver `context/handoff.md`
- **S1 = A:** o `score` PERMANECE tambem no grid 2x2 -> churn ZERO em `_mapas_calor_page`.
- **S2 = B:** regua de 6 faixas, 3o corte em 2.500 (nao 2.000). A alternativa de 5 faixas foi
  recusada por saturacao medida (Manaus: p75 7.588 e max 13.472 na MESMA faixa de topo).
- Reversao do BLK-CENSO-02 registrada como **emenda a DEC-011** (nao DEC nova).

## Achado que evitou churn grande
`_tema_bicolor`: o slide novo e **ordinal 0**, e `0 % 2 == 0` ja devolve magenta -> **ZERO inversao
em cascata**. As 4 paginas existentes mantem exatamente a cor de hoje, sem tocar o corpo da funcao.

## Excecao de RENDER (regra de acentuacao, §2)
A fonte do PNG do mapa **nao tem glifo acentuado** — medido: `i` e `c` acentuados renderizam o mesmo
tofu box que um ideograma CJK. Todo texto novo do PNG e ASCII PURO. O texto do PDF (fpdf2/latin-1)
segue acentuado normalmente. O QA confirmou por espiao em `_draw_text`: 0 nao-ASCII em 86 textos.

## FU1 (pos-gate visual, 2026-07-22)
A camada `residual` deixou de desenhar pins: a area a 5 km e ~11x a de 1,5 km (r^2) e a densidade de
logos cobria o choropleth (medido na amostra da Av. Paulista). `_draw_legend_camada`/`_render_camada`
ganharam `mostrar_legenda_pins` com **default `True`** — de proposito: a legenda NAO pode reagir a
`pins` estar vazio, senao um ponto sem concorrentes no raio perderia a linha nas 5 camadas
pre-existentes e quebraria a byte-identidade que o QA provou por sha256.

## Validacoes (reexecutadas em 2026-07-22, serial — `-n auto` aborta com INTERNALERROR/execnet)
- Subconjunto impactado: `172 passed`; apos o FU1, `126 passed` no recorte relevante.
- Suite FULL: `1 failed, 1952 passed, 2 skipped` (18m18s). A falha e
  `test_score_retencao_territorial::test_run_readonly_m1_por_mtime` (parquet de staging gitignored
  ausente), **pre-existente** — o diff em `lifetime/` vs a baseline e vazio.
- `ruff`: All checks passed. `mypy src/`: 7 erros no HEAD == 7 na baseline (stubs de `requests`,
  ruido do ambiente local), sem regressao.
- Amostras com dado real: `data/reports/scratch/blk_relpon_10/RELPON10_{paulista_sp,manaus_am,
  chapeco_sc}.pdf` (7 paginas cada) + `SLIDE2_*.png`.

## Guardrail
READ-ONLY sobre o M1 (§5): render puro. `score_priorizacao`, `hex_score_estrutural`, pesos, scores
censitarios, `flag_sam`, carteira, plano e artefatos oficiais INTOCADOS (mtime e tamanho verificados
pelo QA). `setor_censitario_intersecao_area_1p5km` e `RAIO_CENSITARIO_DEFAULT_KM` idem.
