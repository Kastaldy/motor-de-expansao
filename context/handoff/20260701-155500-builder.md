# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-RELPON-03 — Eliminar a barra cinza (letterbox) dos mapas do Relatório Municipal

## Plano aprovado
Plano do Planner APROVADO PELO USUÁRIO (Vinicius) EM 2026-07-01, com as recomendações:
- **D1 = re-proporcionar o PNG para 1000×704** (aspect 1,4205 ≈ painel 1,4211).
- **D2 = padronizar os 5 painéis em 540×380** (alinhar a página Score de `max_w=560`→`540`).

## O que foi feito
RENDER-only em `relatorio_municipal.py` (READ-ONLY sobre o M1; nenhum score/pesos/SAM/carteira/
plano/artefato tocado). Executados os passos 1–9 do handoff do Planner à risca:

1. **`render_mapas_municipio`** (~1166): default `height: int = 620` → `704`. `width=1000`
   inalterado.
2. **`_render_mapa_municipio`** (~862): default `height: int = 620` → `704`. Nenhuma lógica
   interna mudou (o `map_box` interno e o `scale=min` da projeção da geografia continuam iguais;
   a geografia é contida no `map_box`, então maior altura só dá mais respiro vertical, sem
   distorcer — insight-chave do Planner confirmado no código).
3. **`_score_page`** (~1643, D2): a chamada `_draw_framed_map(..., max_w=560.0, ...)` → `max_w=540.0`
   (padroniza os 5 painéis no aspect-alvo). Painel lateral de legenda (`px=626`) INTOCADO; folga
   de 42 pt garante ausência de sobreposição.
4. **`_draw_map`** (~1296) higiene: defaults NÃO exercitados `max_w=600.0, max_h=430.0` →
   `max_w=540.0, max_h=380.0` (casa o aspect-alvo; remove armadilha dormente).
5. As 4 chamadas de `_draw_framed_map` que já usavam `max_w=540.0` (~1528, ~1586, ~1693, ~1755):
   NÃO alteradas.
6. SEM novo parâmetro opcional de modo/fit — o novo comportamento vira o default (só há um
   consumidor de produção). A assinatura pública não mudou (só o valor default de `height`).
7. **Passo 7a (teste):** extraída função pura `_fit_contain(img_w, img_h, max_w, max_h, *,
   x_anchor, y_anchor) -> (draw_w, draw_h, x, y)` de dentro de `_draw_map` (refatoração
   byte-idêntica do cálculo `scale=min` + centralização; `_draw_map` agora delega a ela).

## Testes novos (tests/unit/test_relatorio_municipal.py)
- `test_fit_contain_pnl_1000x704_no_painel_540x380_sem_letterbox`: para img=(1000,704) e
  max=(540,380) exige `abs(draw_h−380) <= 1.0` E `abs(draw_w−540) <= 1.0` (sem letterbox nos
  dois eixos) + verifica a centralização a partir das âncoras.
- `test_fit_contain_documenta_painel_560x380_sobra_lateral`: documenta o fallback (não usado em
  produção) — no painel 560×380 a altura domina e sobra ~20 pt de largura (por isso a
  padronização em 540).
- `test_render_mapas_municipio_png_altura_704` (parametrizado nas 5 camadas): confirma que
  `render_mapas_municipio(...)` gera PNGs `(1000, 704)` por padrão (`_png_dimensions(...)==(1000,704)`).

## Arquivos alterados
- `src/motor_expansao/dashboard/relatorio_municipal.py`
  - `render_mapas_municipio`: default `height` 620→704.
  - `_render_mapa_municipio`: default `height` 620→704.
  - `_score_page`: `_draw_framed_map(..., max_w=560.0)` → `max_w=540.0` (+ comentário BLK-RELPON-03).
  - `_draw_map`: defaults `max_w=600.0/max_h=430.0` → `540.0/380.0`; passa a delegar o encaixe a
    `_fit_contain`.
  - Novo helper puro `_fit_contain(...)` (extração byte-idêntica do encaixe contain).
- `tests/unit/test_relatorio_municipal.py`: import de `_fit_contain`/`_png_dimensions`/`pytest`;
  3 testes novos (letterbox + fallback documentado + smoke de altura 704).

## Validações executadas
- `python -m pytest -n auto -q ...`: **INTERNALERROR (xdist/execnet instável no Python 3.14)** —
  conforme antecipado no prompt. Reexecutado **serial** (`-p no:xdist`).
- `python -m pytest -q -p no:xdist tests/unit/test_relatorio_municipal.py tests/integration/test_streamlit_app.py`:
  **246 passed, 0 failed, 0 skipped** (30,45s). Cobre os 3 testes novos + todos os existentes do
  Relatório Municipal (`/Count 9`, headers `PDF_SECTION_HEADERS`, `BLK-RELMUN-01`,
  `OpenStreetMap`/`CARTO`, marca d'água ≥8, anti-PII, `test_coexistencia_relatorio_pontual_intocado`
  byte-identity do Pontual) + o smoke de integração do Streamlit. NENHUM teste existente regrediu.
- `python -m ruff check src/motor_expansao/dashboard/relatorio_municipal.py tests/unit/test_relatorio_municipal.py`:
  **All checks passed!**
- `python -m mypy src/motor_expansao/dashboard/relatorio_municipal.py`: **Success: no issues found
  in 1 source file**.
- `python -c "import streamlit_app; print('import ok')"`: **import ok**.
- NÃO rodei a suíte FULL (gate único do QA).

## Problemas encontrados
- `-n auto` (pytest-xdist) aborta com INTERNALERROR/EOFError de execnet no ambiente Python 3.14
  local (problema de ambiente, não de código). Validação feita serial (`-p no:xdist`), verde.

## Pendências
- **Gate visual humano do PDF**: a eliminação da barra cinza é uma mudança de RENDER; recomenda-se
  ao QA/humano abrir o PDF gerado (5 mapas) e confirmar visualmente: (i) barra cinza sumiu top/base
  nas 5 páginas; (ii) sem distorção da geografia/hexes; (iii) sem sobreposição de moldura/título/
  legenda lateral/rodapé/marca d'água; (iv) enquadramento vertical mais alto é aceitável (esperado).
- Fechamento do ciclo (`tasks/completed.md`, `tasks/backlog.md`) é passo do orquestrador/QA, não do
  Builder.

## Riscos remanescentes
- **Enquadramento vertical percebido**: com `map_box` mais alto, a geografia ganha mais respiro
  vertical (contain); NÃO distorce (forma dos hexes intacta, `focus_bounds` compartilhado entre as
  4 camadas de foco), mas o zoom aparente pode parecer levemente menor na vertical. Aceitável e
  esperado — item do gate visual.
- Se o QA/humano preferir manter Score em 560 (fallback documentado), reaparece barra lateral fina
  (~10 pt/lado) só nessa página. Não é o padrão implementado (padrão = 540, sem sobra).

## Guardrails verificados
- score_priorizacao não alterado: sim (RENDER-only; zero leitura/escrita de artefato M1).
- Artefatos M1 preservados: sim (nenhum pipeline/artefato tocado).
- Dashboard offline mantido: sim (nenhuma chamada de rede nova; DEC-011/`_fetch_basemap_municipio`
  inalterados; `basemap=False` default em teste).
- 8 páginas (`/Count 9`), `set_compression(False)`, `pdf_version="1.4"`, marca d'água anti-PII,
  atribuição OpenStreetMap/CARTO: preservados (testes verdes).
- `censo_map.py`/`censo_point.py`/`censo_report.py` (Relatório Pontual): INTOCADOS
  (`test_coexistencia_relatorio_pontual_intocado` verde — byte-identity).
