# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer (Opus 4.8)

## Próxima Skill recomendada
Fechamento manual (orquestrador): Passo 6.0 (housekeeping via helper versionado) + gate de
REVISÃO VISUAL HUMANA (dashboard + slides 2/3 do PDF) → merge.

## VEREDITO
**APROVADO COM RESSALVAS**

## Justificativa
A entrega cumpre integralmente D1/D3/D4 do plano REVISADO, com escopo exato (6 arquivos; `pages.py`,
`Dockerfile.streamlit` e `test_streamlit_app.py` NÃO tocados), READ-ONLY sobre o M1, choropleth/faixas
de cor/pins/círculo/basemap/estrutura do PDF intocados, `ruff`/`mypy` limpos e suíte full sem nenhuma
falha atribuível ao bloco (a única falha é PRÉ-EXISTENTE e provada no baseline via `git stash`).
Verifiquei por conta própria (não só por teste) que `_font(N)` escala de fato, que a densidade nova usa
a **área válida** e que o texto ficou legível no PNG e nas páginas 2/3 do PDF, sem colisão nem vazamento.
As ressalvas são **não-bloqueantes para o código**: (a) o housekeeping (Passo 6.0) está PENDENTE por
instrução do orquestrador — e a regra de `prompts/qa_analyzer.md` exige que ele seja confirmado antes de
um APROVADO puro; (b) o gate formal de revisão visual humana ainda não ocorreu; (c) `pyproject.toml`
declara `pillow>=10.0.0` mas o código passou a exigir `>=10.1` (sem risco em produção/CI, que instalam
com `-c constraints.txt` → `pillow==12.3.0`).

## Problemas críticos (bloqueadores)
- nenhum.

## Problemas médios (não bloqueadores)
- **Piso de dependência desatualizado:** `_font()` agora depende de `ImageFont.load_default(size=...)`,
  API introduzida no **Pillow 10.1.0**, mas `pyproject.toml:22` declara `pillow>=10.0.0`. Em produção e
  em CI não há risco (ambos instalam com `-c constraints.txt` → `pillow==12.3.0`; `Dockerfile.streamlit:29`
  e `.github/workflows/ci.yml:43`), mas uma instalação SEM constraints que resolva 10.0.0 levantaria
  `TypeError` em runtime ao gerar qualquer mapa. Correção = bumpar o piso para `pillow>=10.1.0`. NÃO fiz
  nem pedi ao Builder porque `pyproject.toml` é path **CRÍTICO** no `loop_guard` (DEC-016) e elevaria a
  criticidade deste bloco — deve virar bloco próprio (sugestão: BLK-RELPON-06-FU1).
- **Falha PRÉ-EXISTENTE na suíte full (não do bloco):** `tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime`
  falha com `FileNotFoundError: data/staging/unidade_territorio_retencao.parquet`. **Provado pré-existente:**
  com o diff inteiro em `git stash`, o mesmo teste falha igual no baseline. Causa-raiz: o teste só faz
  `pytest.skip` quando NENHUM artefato M1 existe (caso do CI); nesta máquina os artefatos M1 existem mas o
  insumo LTV (gitignored) não → o `run()` real quebra. Não toca `censo_*` nem o diff deste ciclo e não
  quebra o check `test` do CI.

## Melhorias opcionais
- `test_font_nao_depende_de_ttf_do_sistema` é fraco (só assere `getbbox("A") is not None`); o contrato real
  já está travado pelos outros dois testes de fonte (`test_font_escala_com_o_size` e a inspeção de código-fonte).
- Margem da faixa superior: medi o pior caso realista `"Densidade no raio: 25.000 hab/km2"` → termina em
  x=634, contra `legend_x=670` (folga 36 px ≈ 2 dígitos). Uma densidade de 7 dígitos (irreal: exigiria
  >1.000.000 hab/km²) colidiria com o título "Legenda". Sem teste cobrindo o comprimento máximo da string —
  risco desprezível, mas registrado.
- Na camada `concorrentes` (só-pins, 0 entradas de faixa) o subtítulo da legenda e a caption repetem
  "Pins: Ultra e concorrentes" (visível na página 3 do PDF). Duplicação **pré-existente** (o `legenda_titulo`
  dessa camada já era essa string), agora mais evidente com a fonte maior. Cosmético.

## Testes faltantes
- nenhum bloqueador. Os 4 testes de contrato de legibilidade/overflow e os 3 de fonte cobrem o que a
  revisão visual não trava em código. Verifiquei que **não** foram relaxados para passar (ver evidência
  numérica abaixo): os 2 bugs de overflow eram reais (subtítulo 418 px vs orçamento 320 px; caption 362 px
  vs 252 px) e o alvo de legibilidade tem margem real (`32 × 0,29867 = 9,56 pt ≥ 9,0`).

## Riscos remanescentes
- **Gate de revisão visual humana ainda não ocorreu** (é o único gate do bloco). Fiz uma verificação visual
  INDEPENDENTE (gerei PNGs + PDF de 5 páginas com fixture sintética e inspecionei): aprovaria, mas o gate é
  do humano.
- Os números da faixa MUDAM em relatórios já gerados (esperado, é o ponto do bloco): densidade nova ≥ antiga.
  No meu repro sintético: `densidade_pop_raio_hab_km2 = 2875.36` → `densidade_pop_raio_valida_hab_km2 = 4324.24`.
- Byte-identidade dos PNGs quebra de propósito em Windows (Arial → fonte embutida do Pillow) — decisão D3.
- Área útil do mapa encolheu (~663×590 → ~585×550 px inner) para acomodar título/faixa/legenda maiores.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte COMPLETA (gate único do ciclo) — `python -m pytest -q` (serial)
```
-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 1747 passed, 2 skipped, 259964 warnings in 1356.46s (0:22:36)
```
Rodei a suíte full **duas vezes, de forma independente**, com resultado IDÊNTICO (sem flakiness):
```
run A: 1 failed, 1747 passed, 2 skipped, 259964 warnings in 1356.46s (0:22:36)
run B: 1 failed, 1747 passed, 2 skipped, 259964 warnings in 1245.09s (0:20:45)
```
A ÚNICA falha (`test_run_readonly_m1_por_mtime`) é pré-existente e provada no baseline em §1c.

### 1b. `pytest -n auto` (xdist) — INFRA quebrada neste ambiente (Windows/Py3.14), confirmado
```
INTERNALERROR>   File "...\site-packages\execnet\gateway_base.py", line 899, in waitclose
INTERNALERROR>     raise error
INTERNALERROR>   File "...\site-packages\execnet\gateway.py", line 87, in _rinfo
INTERNALERROR>     self._cache_rinfo = RInfo(ch.receive())
INTERNALERROR>   File "...\site-packages\execnet\gateway_base.py", line 934, in receive
INTERNALERROR>     raise self._getremoteerror() or EOFError()
INTERNALERROR>   File "...\site-packages\execnet\gateway_base.py", line 571, in from_io
INTERNALERROR>     raise EOFError("couldn't load message header, " + e.args[0]) from None
INTERNALERROR> EOFError: couldn't load message header, expected 9 bytes, got 0
```
Falha no *gateway* do `execnet`, ANTES de coletar/rodar qualquer teste → é INFRA, não teste. Por isso o
gate rodou serial. **Não foi usado para mascarar falha de teste.**

### 1c. Causa-raiz da única falha — PROVA de que é PRÉ-EXISTENTE (`git stash` do diff + re-run no baseline)
```
$ python -m pytest -q tests/unit/test_score_retencao_territorial.py   # com o diff aplicado
>       run(root=root)  # roda de verdade sobre os parquets reais
E       FileNotFoundError: [Errno 2] No such file or directory:
        'C:\...\motor-de-expansao\data\staging\unidade_territorio_retencao.parquet'
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime

$ git stash push -- src tests docs      # BASELINE, sem NENHUMA linha do BLK-RELPON-06
$ python -m pytest -q tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 651 warnings in 2.21s
$ git stash pop                          # diff restaurado intacto (6 arquivos modificados)
```
Mesma falha, mesmo erro, SEM o diff → pré-existente. Arquivo em `lifetime/` (camada LTV/DEC-014), zero
relação com `censo_*`. O teste faz `pytest.skip` só quando NENHUM artefato M1 existe (caso do CI) → o
check `test` do CI não é afetado.

### 1d. Subconjunto impactado (serial, evidência adicional)
```
$ python -m pytest -q tests/unit/test_relatorio_pontual_censitario_motor.py tests/unit/test_relatorio_pontual_censitario_mapa.py tests/unit/test_relatorio_pontual_censitario_export.py
61 passed, 3211 warnings in 61.70s (0:01:01)

$ python -m pytest -q tests/integration/test_streamlit_app.py
249 passed, 12700 warnings in 54.21s
```

### 2. `python -c "import streamlit_app; print('import ok')"`
```
2026-07-14 13:07:20.806 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
2026-07-14 13:07:22.665 WARNING streamlit.runtime.scriptrunner_utils.script_run_context: Thread 'MainThread': missing ScriptRunContext! ...
(...warnings de runtime do Streamlit em modo bare...)
import ok
```

### 3. `python -m ruff check src/motor_expansao/dashboard/{censo_point,censo_map,censo_report}.py`
```
All checks passed!
```

### 4. `python -m mypy src/motor_expansao/dashboard/{censo_point,censo_map,censo_report}.py`
```
Success: no issues found in 3 source files
```

### 5. D3 — `_font()` ESCALA de fato (verificação própria, não só teste)
```
$ python -c "from motor_expansao.dashboard import censo_map as cm; [print(f' _font({s}).getbbox(A) =', cm._font(s).getbbox('A')) for s in (20,44)]"
 _font(20).getbbox(A) = (0, 6, 13, 20)
 _font(44).getbbox(A) = (0, 13, 28, 43)
```
13×14 px → 28×30 px. O `size` é respeitado (o bug de produção era exatamente os dois renderizarem igual).
Confirmado no diff: `_font()` NÃO chama mais `ImageFont.truetype(...)` nem `arial.ttf` — só
`ImageFont.load_default(size=size)`.

### 6. D1 — matemática da densidade (verificação própria, fixture sintética de 3 setores)
```
densidade_pop_raio_hab_km2       = 2875.36     (denominador = pi*r^2 FIXO, inclui agua/vazio)
densidade_pop_raio_valida_hab_km2= 4324.24     (denominador = area_intersecao_total_m2, area VALIDA)
renda_per_capita_media_raio      = 3925.91
score_setor_medio                = 68.98
```
Confirmado no código: usa `result["area_intersecao_total_m2"]` (NÃO `area_km2`), com guarda
`if area_valida_m2 and area_valida_m2 > 0:` dentro do bloco `if pop_weights.notna().any():` → cobre os
DOIS caminhos de "n/d" (sem setores intersectados; área válida 0/None). Nova ≥ antiga, como esperado.

### 7. Contrato de legibilidade/overflow — os testes provam comportamento REAL (não foram relaxados)
```
rotulo mais longo:  "R$ 1.001-2.000" | largura@32px = 220 | orcamento = 252   -> cabe
subtitulo@22px (_FS_LEGENDA_SUBTITULO) = 296 | orcamento = 320                -> cabe
subtitulo@32px (fonte ANTIGA proposta)  = 418 | orcamento = 320                -> ESTOURAVA (bug real)
caption@20px  (_FS_LEGENDA_CAPTION)    = 224 | orcamento = 252                -> cabe
caption@32px  (fonte ANTIGA proposta)  = 362 | orcamento = 252                -> ESTOURAVA (bug real)
legibilidade PDF: _FS_LEGENDA_CORPO(32) * 298.67/1000 = 9.557 pt  >= 9.0      -> alvo batido
```
Os 2 bugs de overflow relatados pelo Builder são **reais e reproduzidos por mim**, e ambos têm teste de
regressão dedicado (`test_legenda_subtitulo_mais_longo_nao_transborda_do_canvas`,
`test_legenda_caption_pins_nao_transborda_do_canvas`). O teste corrigido
`test_valor_raio_nao_e_nd_quando_setor_nao_cobre_o_ponto_mas_intersecta_raio` está CORRETO sob D1 (o
cenário tem `n_setores=1` → a faixa mostra valor real do raio, não "n/d") e o "n/d" de verdade ganhou
teste próprio (`test_valor_raio_e_nd_quando_setor_fora_do_raio`, `n_setores=0`). Nenhum assert foi
afrouxado para passar.

### 8. Os 5 campos `*_setor_ponto` continuam no `result` (D1 exige)
```
censo_point.py:206-210 (defaults, INTOCADOS):
    "cod_setor_ponto", "renda_per_capita_setor_ponto", "densidade_pop_setor_ponto",
    "score_setor_2022_calibrado_ponto", "flag_setor_ponto_encontrado"
```

### 9. VERIFICAÇÃO VISUAL INDEPENDENTE (obrigatória — feita pelo QA, não herdada do Builder)
Gerei **eu mesmo** (fixture sintética: 3 setores, 2 concorrentes, 1 Ultra; `basemap=False`; artefatos no
scratchpad, NÃO commitados) as 4 camadas PNG (1000×760) e o PDF completo, e renderizei as páginas 2 e 3
com PyMuPDF (150 dpi) para inspeção:
```
densidade/renda/score/concorrentes: (1000, 760) PNG ok
PDF: 5 paginas (960x540 pt)  |  pag 2 = "Mapas de calor"  |  pag 3 = "Concorrentes"
```
Observado:
- **(a) Nenhum texto colide ou vaza do canvas.** Título (44 px) e faixa "no raio" (38 px) terminam bem à
  esquerda de `legend_x=670`; legenda inteira dentro da coluna de 330 px; rodapé e barra de escala dentro
  do canvas. Confirmei também por medição (`_text_width`) e não só a olho.
- **(b) A legenda cabe:** 5 faixas + separador + bloco de pins ocupam até ~y=600 de 760 px (folga).
- **(c) O texto está de fato MAIOR:** título 20→44 px, faixa 17→38 px, corpo da legenda 13→32 px. Nas
  páginas 2 e 3 do PDF (tamanho real 960×540 pt) título, linha de dado e rótulos de faixa estão nítidos e
  perfeitamente legíveis — vs. os ~3,9–6,0 pt borrados de antes.
- Rótulo correto em todas as 3 camadas: `Densidade no raio: 4.324 hab/km2`, `Renda no raio: R$ 3.926`,
  `Score no raio: 69`. Camada `concorrentes` sem faixa (como especificado).
- Choropleth, faixas de cor, pins, círculo e a estrutura de 5 páginas do PDF: visualmente idênticos.

## Conferência de no-bypass
Confirmo que **nenhuma** validação usou `--config /dev/null`, mock do caminho crítico ou qualquer
contorno da config/artefatos reais. A suíte rodou SERIAL e COMPLETA (`python -m pytest -q`, sem `-p
no:xdist`, sem `-k`, sem desabilitar plugin). O `-n auto` foi tentado e falha no gateway do `execnet`
ANTES de coletar qualquer teste (saída literal em §1b) — INFRA do ambiente Windows/Py3.14, registrada,
NÃO usada para mascarar falha de teste. A única falha da suíte foi investigada até a causa-raiz e PROVADA
pré-existente por re-execução no baseline com o diff inteiro em `git stash` (§1c). A verificação visual foi
feita com fixture sintética por exigência ANTI-PII do próprio bloco (PNG/PDF com dado real nunca são
versionados/gerados aqui) — não é bypass: o caminho de render exercitado (`render_mapas_censitarios_combinados`
→ `gerar_pdf_relatorio_pontual_censitario`) é exatamente o de produção, sem stub.

## Conferência cruzada com log do Builder
**Bate.** `ruff`/`mypy`/`import streamlit_app` idênticos. O subconjunto do Builder (310 passed) reproduz
como 61 + 249 = 310 nas minhas duas execuções. Os 2 bugs de overflow e a correção do
`test_valor_ponto_nd_quando_ponto_fora_da_malha` que ele relata foram **reproduzidos e confirmados por mim
com números próprios**. Divergência única (esperada): o Builder não rodou a suíte FULL (por instrução do
orquestrador) — rodei aqui, e ela revelou a falha pré-existente do `test_score_retencao_territorial`, que
o subconjunto do Builder não alcançava.

## Guardrails verificados
- **score_priorizacao não alterado:** sim. `git diff -U0` filtrado por `score_priorizacao|hex_score_estrutural|PESOS_|carteira|plano_expansao`
  → **nenhuma linha `+`/`-`** toca esses termos. `config.py` e `pipelines/m1/` fora do diff.
- **Artefatos M1 preservados:** sim. `git status --short data/` vazio; nenhum parquet/CSV oficial regerado.
- **Escopo respeitado:** sim — `git diff --name-only` = EXATAMENTE os 6 arquivos esperados
  (`censo_point.py`, `censo_map.py`, `censo_report.py`, os 2 testes unitários, `docs/relatorio_pontual_censitario.md`).
  **`pages.py`, `Dockerfile.streamlit`, `tests/integration/test_streamlit_app.py`, `pyproject.toml`,
  `constraints.txt` e `test_relatorio_pontual_censitario_export.py` NÃO estão no diff** (verificado com
  `git diff --name-only HEAD --` sobre cada um → vazio). Sem plumbing `mapas_pdf` e sem parâmetro `escala`
  por caminho (D4 respeitado: UM único render).
- **`setor_censitario_intersecao_area_1p5km` / raio 1,5 km / `RAIO_CENSITARIO_DEFAULT_KM`:** intocados
  (nenhuma linha `+`/`-` os menciona).
- **Choropleth e faixas de cor INALTERADOS:** sim — `color_fn`, `source_values`, `DENSIDADE_POP_BANDS`,
  `RENDA_PER_CAPITA_BANDS`, `score_band_to_color`, `_CHOROPLETH_ALPHA`, `_STREET_*` não aparecem em nenhuma
  linha `+`/`-` do diff (só como contexto e num `import` de teste que os LÊ). Só texto/fonte/layout mudou.
- **Estrutura do PDF (5 páginas, grid Big Numbers 4x2, marca d'água, `set_compression(False)`, `pdf_version`):**
  intocados — `censo_report.py` só teve docstrings/comentários alterados (2 blocos, zero lógica); confirmei
  as 5 páginas no PDF que gerei; `test_relatorio_pontual_censitario_export.py` passa sem edição.
- **Parâmetros canônicos:** `H3_RESOLUTION=7`, `DIST_MIN_ULTRA_KM=1.0`, `RENDA_MIN=4500.0`, pesos
  `renda=0.40`/`pop=0.60` — todos fora do diff, INALTERADOS.
- **Acentuação (§2):** texto de usuário novo ("no raio") sem acento necessário; identificadores sem acento;
  faixa em ASCII-safe (`hab/km2`, `R$` sem centavos, `n/d`) — OK.
- **Validações re-executadas pelo QA (sem bypass):**
  - `python -m pytest -q` (full, serial) → 1 failed (PRÉ-EXISTENTE, provado), 1747 passed, 2 skipped
  - `python -m pytest -q -n auto` → INTERNALERROR/EOFError no gateway do execnet (INFRA, registrado)
  - `python -c "import streamlit_app"` → import ok
  - `ruff check` (3 arquivos) → All checks passed!
  - `mypy` (3 arquivos) → Success: no issues found in 3 source files
  - Verificação visual independente (4 PNGs + PDF 5 páginas, gerados e inspecionados pelo QA) → OK

## Housekeeping (Passo 6.0)
**PENDENTE** — o bloco BLK-RELPON-06 VEM de `tasks/backlog.md` e continua lá. Por instrução explícita do
orquestrador, NÃO executei o move (é passo de fechamento do orquestrador).
Comando a executar no fechamento: `python scripts/housekeeping_move_block.py BLK-RELPON-06`
(seguido de `--check`). É a razão formal do "COM RESSALVAS" em vez de "APROVADO" puro
(regra de `prompts/qa_analyzer.md` §Housekeeping).

**Nota obrigatória para o `completed.md`:** o texto do bloco no backlog ainda descreve o **D2 antigo**
("fontes maiores SÓ no PDF, via parâmetro opcional" + critério "Dashboard e API: render byte-a-byte
idêntico"). Esse D2 foi **REVERTIDO pelo D4** (Vinicius, 2026-07-14: UM único render, fonte maior nas duas
superfícies) — registrar a supersessão junto com (a) a **reversão do D3 do BLK-RELPON-05** (faixa volta a
ser do RAIO) e (b) a **decisão D3** (fonte embutida do Pillow; achado de que o texto do mapa estava
quebrado em produção no dashboard *e* no PDF).

## Decisão recomendada
**Fechar ciclo** após: (1) `scripts/housekeeping_move_block.py BLK-RELPON-06` + registro das reversões
no `completed.md`; (2) **gate de revisão visual humana** (dashboard + slides 2/3 do PDF) — minha inspeção
independente aprovaria, mas o gate é do humano. Abrir **BLK-RELPON-06-FU1** (criticidade Crítica, pois
toca `pyproject.toml` — path CRÍTICO do `loop_guard`/DEC-016) para bumpar `pillow>=10.0.0` → `>=10.1.0`.
