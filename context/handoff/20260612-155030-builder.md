# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-UI-06 — Corrigir o GAP do seletor de telas (flex-pai real + zerar margem negativa do baseweb)

## O que foi feito
Inseridas duas novas regras CSS no bloco do seletor de telas dentro de `inject_styles()` em `pages.py`:

1. `[data-baseweb="button-group"] { gap: 8px !important; }` — sobrescreve o `gap: 4px 0px` nativo do Streamlit 1.58 no flex-pai real dos botões do seletor, forçando 8px de espaçamento horizontal.
2. `[data-testid="stBaseButton-segmented_control"], [data-testid="stBaseButton-segmented_controlActive"] { margin: 0 !important; }` — remove o `margin-right: -1px` injetado pelo baseweb que "colava" os botões eliminando o gap.

As duas regras foram inseridas imediatamente após a regra inerte `stSegmentedControl`/gap (L304–308) e antes da regra de cor dos botões inativos. Todas as regras existentes foram mantidas intactas.

Adicionados 3 asserts de regressão em `test_inject_styles_cobre_componentes_baseweb`:
- `assert 'data-baseweb="button-group"' in css`
- `assert "gap: 8px" in css`
- `assert "margin: 0 !important" in css`

## Arquivos alterados
- `src/motor_expansao/dashboard/pages.py` — inseridas 7 linhas de CSS (2 novas regras) entre as linhas 308 e 309 originais; zero alteração de lógica Python; `render_tab_selector` intocado.
- `tests/integration/test_streamlit_app.py` — adicionados 3 asserts ao final de `test_inject_styles_cobre_componentes_baseweb` (L4485–4487).

## Validações executadas
- pytest `tests/integration/test_streamlit_app.py` (serial): **183 passed, 0 failed, 0 skipped**
- import smoke: `python -c "import streamlit_app"` → **import ok**
- ruff: `ruff check src/motor_expansao/dashboard/pages.py tests/integration/test_streamlit_app.py` → **All checks passed!**
- mypy: `mypy src/motor_expansao/dashboard/pages.py` → **Success: no issues found in 1 source file**

## Problemas encontrados
Nenhum.

## Pendências
- Verificação RENDER (playwright): o orquestrador deve medir o bounding box dos 4 botões do seletor e confirmar ~8px de distância horizontal (conforme critério de aceite do handoff do Planner).

## Riscos remanescentes
- `[data-baseweb="button-group"]` é seletor mais amplo que o seletor de telas; risco baixo (confirmado no diagnóstico DOM que apenas o seletor de telas usa esse componente no app). A verificação RENDER confirma visualmente.
- `gap: 8px` (sem eixo explícito) uniformiza row-gap e column-gap; intencional, seletor de telas é linha única.

## Guardrails verificados
- score_priorizacao não alterado: sim (só CSS, zero lógica Python)
- Artefatos M1 preservados: sim (não aplicável — só CSS)
- Dashboard offline mantido: sim (`render_tab_selector` intocado; Bloco 5 intocado)
