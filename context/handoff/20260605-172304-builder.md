# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-CENSO-01 — Relatório censitário: camadas combinadas + fundo de ruas + faixas GeoFusion + pins com logo (DEC-004 APROVADA por Felipe em 2026-06-05)

## O que foi feito
Repaginação READ-ONLY do Relatório Pontual Censitário seguindo os Passos 0–7 do handoff do Planner. O motor (`analisar_ponto_censitario_setores`, `setor_censitario_intersecao_area_1p5km`, raio 1.5 km) ficou 100% INTOCADO; toda a mudança é de RENDER/visualização/export.

Passo 0 (`pyproject.toml`): adicionado extra dedicado `[project.optional-dependencies].basemap = ["contextily>=1.5.0"]`, fora das deps base do deploy. `contextily` é importado LAZY dentro de `_fetch_basemap` sob `try/except ImportError`; cache em `data/cache/basemap_tiles/` (mkdir parents/exist_ok + `ctx.set_cache_dir`).

Passo 5 (`.gitignore`): adicionado `data/cache/`.

Passo 1 (`constants.py`): adicionados `DENSIDADE_POP_BANDS` (Reds-5, cortes 1k/5k/10k/25k/inf hab/km², alpha 150) e `RENDA_PER_CAPITA_BANDS` (rampa fria→quente adaptada a PER CAPITA, cortes 1k/2k/3,5k/5k/inf R$/pessoa, alpha 150). Docstrings registram que per capita ≠ domiciliar (não confundir com RENDA_MIN=4500). Nada de M1 tocado.

Passo 2 (`censo_map.py`): nova orquestradora `render_mapas_censitarios_combinados(...) -> dict[str,bytes]` com chaves `densidade/renda/concorrentes`. Composição em EPSG:3857 (reprojeta setores+círculo+centro do aeqd local → 3857 SÓ para render, via `_to_mercator`/`_point_to_mercator` reusando os transformers do `censo_point`). Basemap CartoDB Positron No-Labels via contextily lazy em `_fetch_basemap` (zoom automático por bbox, `ll=False`); fallback offline gracioso (basemap=False OU import/fetch falha → canvas branco sem ruas, sem exceção — qualquer exceção do contextily vira None). Choropleth por faixa fixa: `_color_for_densidade`/`_color_for_renda`/`_color_for_score` (este via `score_band_to_color(..., alpha=150)`), substituindo o quartil `_build_breaks` no caminho novo (helpers de quartil mantidos para o legado). Pins com logo via `competitors._render_pin_tile` colados por `_paste_logo_pin` (ancorando a ponta do balão; Ultra=`"__ultra__"`, concorrente pela coluna `rede`, fallback de sigla já coberto). Legenda por camada (`_draw_legend_camada`). `render_mapa_censitario_estatico_png` mantido como wrapper fino legado (devolve uma camada pelo antigo `metric_column`; basemap=False default).

Passo 3 (`pages.py`, `render_relatorio_pontual_censitario`): removidos o `st.selectbox` de métrica e a variável `metric_column`; chamada única a `render_mapas_censitarios_combinados(..., basemap=True)`; os 3 mapas são exibidos juntos via 3 `st.image` (sem dropdown que esconda camadas); o dict de mapas é repassado a `render_downloads_relatorio_censitario`. `preload_logos` já roda no boot do `streamlit_app.py:206` (confirmado) → `_ICON_CACHE` populado; a UI não precisa repassar logos.

Passo 4 (`censo_report.py`): `gerar_pdf_relatorio_pontual_censitario` agora aceita `mapas: dict[str,bytes] | bytes | None` (retrocompat: bytes = 1 mapa legado, via `_normalize_mapas`). `_build_pdf` generalizado de 1 XObject para N imagens (mapas page-idx → image obj; `page_xobjects` liga página↔XObject); `_map_content(w,h,*,title,im_name)` por mapa; uma página de mapa por camada com títulos "Mapa - Densidade"/"Mapa - Renda per capita"/"Mapa - Concorrentes" inseridas entre KPIs e detalhes. CSV inalterado. Offsets/xref do writer manual recomputados a partir de `objects` (sem hardcode de índice de página).

Passo 6 (testes): os 5 do motor NÃO mudaram. Reescritos os 2 de mapa + 3 novos (`test_mapa_censitario_faixas_fixas_nao_quartil`, `test_mapa_censitario_pin_com_logo_concorrente_e_ultra`, `test_mapa_censitario_fallback_offline_sem_tiles`) + 1 extra (`test_fetch_basemap_sem_contextily_devolve_none`). Ajustados os 3 de export (usam `render_mapas_censitarios_combinados(..., basemap=False)` → dict) + 2 novos (`test_pdf_embute_tres_camadas`, `test_pdf_retrocompat_aceita_bytes_unico_legado`). Integração: removido o patch do selectbox e o de `render_mapa_censitario_estatico_png`, agora mocka `render_mapas_censitarios_combinados` retornando dict e assere 3 `st.image`.

Passo 7 (docs+DEC): `docs/relatorio_pontual_censitario.md` §6/§7/§8 reescritos; `CLAUDE.md` §4 (linha "Motor/UI censitário") atualizada; **DEC-004 inserida em CLAUDE.md §8 com Status "APROVADA por Felipe Silva em 2026-06-05"**.

## Arquivos alterados
- `pyproject.toml` — extra `[basemap]` (contextily) fora das deps base.
- `.gitignore` — adicionado `data/cache/`.
- `src/motor_expansao/dashboard/constants.py` — adição de `DENSIDADE_POP_BANDS` e `RENDA_PER_CAPITA_BANDS` (visualização; nada de M1).
- `src/motor_expansao/dashboard/censo_map.py` — orquestradora 3 camadas + basemap 3857 lazy/fallback + faixas fixas + pins com logo; wrapper legado.
- `src/motor_expansao/dashboard/pages.py` — remove selectbox/metric_column, 3 `st.image`, repassa dict aos downloads.
- `src/motor_expansao/dashboard/censo_report.py` — PDF com N mapas; assinatura `mapas: dict|bytes|None`; `_build_pdf` generalizado.
- `tests/unit/test_relatorio_pontual_censitario_mapa.py` — reescritos 2 + 4 novos.
- `tests/unit/test_relatorio_pontual_censitario_export.py` — ajustados 3 + 2 novos.
- `tests/integration/test_streamlit_app.py` — teste do relatório ajustado (sem selectbox; mocka orquestradora; assere 3 imagens).
- `docs/relatorio_pontual_censitario.md` — §6/§7/§8.
- `CLAUDE.md` — §4 (linha Motor/UI censitário) + §8 DEC-004 (APROVADA).

## Validações executadas
- pytest (subconjunto impactado): `python -m pytest -n auto -q tests/unit/test_relatorio_pontual_censitario_motor.py tests/unit/test_relatorio_pontual_censitario_mapa.py tests/unit/test_relatorio_pontual_censitario_export.py tests/integration/test_streamlit_app.py` → **192 passed, 0 failed, 0 skipped** (18.658 warnings, todos DeprecationWarning de pytest-asyncio/Python 3.14, pré-existentes e fora de escopo).
  - Por quê esse subconjunto: cobre o motor (garantia READ-ONLY), o mapa (orquestradora/faixas/pins/fallback), o export (PDF multi-camada/retrocompat) e a integração do `render_relatorio_pontual_censitario` — exatamente os arquivos tocados + o smoke do app exigido.
- ruff: `python -m ruff check .` → **All checks passed!**
- mypy (escopo tocado): `python -m mypy censo_map.py censo_report.py pages.py constants.py` → **Success: no issues found in 4 source files**.
- import: `python -c "import streamlit_app"` → **ok**.
- Os testes usam `basemap=False` (offline) exceto o de fallback, que mocka `_fetch_basemap`/falha do contextily. O ambiente NÃO tem `contextily` instalado (extra `[basemap]` não instalado) — o caminho default passa SEM contextily, confirmando o fallback offline.
- NÃO rodei a suíte full (`pytest -n auto` completo) — é gate do QA.

## Problemas encontrados
nenhum

## Pendências
- Validação VISUAL real do basemap com tiles (círculo não-elipse, ruas legíveis) exige `pip install .[basemap]` + internet — não disponível no ambiente do Builder; coberto por critério de aceite a confirmar manualmente em deploy. O caminho com tiles está implementado e o offline está testado.
- A suíte full é responsabilidade do QA (gate único).

## Riscos remanescentes
- `_fetch_basemap` depende de `contextily.providers.CartoDB.PositronNoLabels` e `bounds2img`; se a API do contextily mudar, o `try/except` cai no fallback offline (sem quebra), mas o basemap some silenciosamente — aceitável por design (DEC-004 mitigação b).
- O writer manual de PDF foi generalizado para N imagens; validado com 3 camadas e com 1 bytes legado (count de `/Subtype /Image` = 3 e 1, respectivamente).

## Guardrails verificados
- score_priorizacao não alterado: sim (zero escrita em score/pesos; motor e fórmula intocados)
- Artefatos M1 preservados: sim (nenhum artefato oficial tocado; mudança 100% de render/export/visualização)
- Dashboard offline mantido: sim (contextily em extra `[basemap]`, import lazy + fallback offline default; dashboard interativo não depende de internet)
- Método de interseção e raio 1.5 km: INTOCADOS (`censo_point.py` não alterado)
