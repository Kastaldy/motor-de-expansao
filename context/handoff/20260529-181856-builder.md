# Handoff — Builder (BLK-OPS-02)

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA — verificar os checks do PR `ciclo/BLK-OPS-02` → `main` (CI completo + Docker Publish).

## Gate de aprovação
APROVADO POR Felipe Silva EM 2026-05-29 — plano ajustado (PR de ciclo, negação .gitignore
para fixtures, ruff/mypy escopo limitado com BLK-OPS-02b para refatoração maior).

## Bloco executado
BLK-OPS-02 — CI completo + build via registry (fora da prod). Branch `ciclo/BLK-OPS-02`.

## O que foi feito (por fase)

### Fase A — Destravar a suíte (skip-guards, sem fixtures de dados)
- `.gitignore`: adicionada negação `!tests/fixtures/` + `!tests/fixtures/**` após `*.csv`
  (destrava futuras fixtures sintéticas de BLK-OPS-02a; sem efeito colateral agora).
- `test_normalizar_concorrentes.py`: `pytestmark = skipif(not PARQUET.exists())` + removido o
  `assert PARQUET.exists()` do fixture `df`.
- `test_modelo_mercado_hexagonos.py`: skip granular — 3 fixtures (`mercado_guardrails_df`,
  `mercado_score_df`, `hibrido_score_df`) e 3 testes (`test_csvs_concorrentes_legiveis`,
  `test_ultra_loader_...`, `test_artefatos_oficiais_m1_mantem_invariantes`) trocaram
  `assert exists()` por `pytest.skip(...)`. Os 5 testes sintéticos (151-320) intocados.
  NENHUM assert de scoring/contrato M1 tocado (`score_oficial==score_priorizacao`,
  `score_oficial_nome`, `osm_status`, schema, len permanecem).
- `test_calcular_penetracao_ultra_hex.py`: guard no fixture `df` por
  `UNIDADES_PATH=data/staging/unidades_ultra_performance.parquet`; sintético intocado; `len==54` intocado.
- `test_comparar_geofusion_vs_hex.py`: guard no fixture `df` por
  `PERF_HEX_PATH=data/staging/unidades_ultra_performance_hex.parquet`. **CORREÇÃO vs plano:** o
  plano sugeria `unidades_ultra_performance.parquet`; ao ler `jobs/pipelines/comparar_geofusion_vs_hex.py`
  confirmei que `gerar_comparacao()` lê `PERF_HEX_PATH = unidades_ultra_performance_hex.parquet`
  (com sufixo `_hex`). Usei o path REAL. `test_calcular_comparacao_unit` sintético intocado; `len==54` intocado.
- `test_validar_penetracao_ultra_hex.py`: `pytestmark` de módulo por
  `PERF_HEX_PATH=unidades_ultra_performance_hex.parquet` (confirmado lendo
  `jobs/pipelines/validar_penetracao_ultra_hex.py`: `gerar_analise()` lê esse parquet). Os 6
  testes são todos acoplados a dado real; `len==54` intocado. Adicionado `import pytest`.
- `test_normalizar_unidades_ultra.py`: guard no fixture `df` + no `test_loader_...` por
  `ULTRA_RAW_PATH=data/ultra/Ultra.csv`; `len==54`/`len==150` intocados.
- `test_pop_censo_v0001.py` e `test_expansao_dominio.py`: NÃO alterados (já têm skip).

### Fase B — Gate CI
- `.github/workflows/ci.yml`: gatilhos mantidos (push main/codex-dashboard-m1-streamlit +
  pull_request main) + `workflow_dispatch`. setup-python com `cache: pip` +
  `cache-dependency-path: pyproject.toml`. Step de testes trocado de 2 arquivos para
  `python -m pytest -q` (suíte completa). Smoke import mantido. Adicionados steps `ruff check .`
  e `mypy src/` — ver "Desvio ruff/mypy" abaixo.

### Fase C — Build + push GHCR
- `.github/workflows/docker-publish.yml` (NOVO): `push: main` + `workflow_dispatch`;
  `permissions: contents:read, packages:write`; login GHCR com `GITHUB_TOKEN`;
  `docker/metadata-action@v5` (`type=sha` + `latest` no default branch);
  `docker/build-push-action@v6` com `context:.`, `file:Dockerfile.streamlit`, cache gha.
  Imagem: `ghcr.io/${{ github.repository }}/motor-expansao-streamlit`.
- `docker-compose.prod.yml`: removido o bloco `build:`; `image:` agora
  `${STREAMLIT_IMAGE:-ghcr.io/kastaldy/motor-de-expansao/motor-expansao-streamlit:latest}`
  (owner/repo = Kastaldy/motor-de-expansao, lowercase p/ GHCR). Build local de sanity continua
  via `docker build -f Dockerfile.streamlit` (independe do compose).
- `.dockerignore`: já existia e cobre `.git/`, `.env`, `data/`, `secrets/`, `tests/`,
  `authelia/`, `Caddyfile`, `tmp_codex_runtime/`. Adicionado `concorrentes/` explícito
  (defesa em profundidade; CSVs já caíam por `*.csv`).

### Fase D — Runbook
- `docs/deploy.md` (NOVO): runbook modo pull (`export STREAMLIT_IMAGE` → `pull` → `up -d` sem
  `--build`), login GHCR para pacote privado (PAT read:packages), rollback por SHA, atualização
  de Parquets, troubleshooting, guardrail §6.
- `docs/deploy_vps_streamlit.md`: nota de cruzamento na seção "Atualizacao" apontando para
  `docs/deploy.md` (diff mínimo, fluxo build legado preservado).

### Controle
- `tasks/current_task.md`: status → aguardando QA.
- `tasks/backlog.md`: BLK-OPS-02 marcado EM EXECUÇÃO; criado bloco **BLK-OPS-02b** (saneamento ruff/mypy).

## DESVIO RELEVANTE — ruff/mypy NÃO atingem 0 erros neste bloco (movido p/ BLK-OPS-02b)
As validações 3 e 4 do prompt (`ruff check .`→0, `mypy src/`→0) **NÃO foram atingidas** e isso
é uma decisão consciente alinhada à regra de escopo do plano ("violação que exija refatoração
maior → NÃO corrigir; registrar BLK-OPS-02b; NÃO refatorar produção; NUNCA supressão em massa;
NÃO desabilitar regra no pyproject").

Estado real medido nesta máquina:
- `ruff check .` → **286 erros** (exit 1). `ruff check . --fix` resolveria 228 mecanicamente
  (I001/F401/UP045/UP037/F541/UP035) MAS tocaria ~52 arquivos de produção — revertido por ser
  refatoração de produção fora de escopo. Restariam 68 não-autofixáveis em ~22 arquivos,
  incluindo **bug latente real** `pages.py:2511 F821 Undefined name pdk`, 3× B019 lru_cache em
  `ibge_censo.py`, e **14× F601 dict-key repetida em `test_hex_enrichment_brasil.py` (teste de
  fixture M1)** — tocar isso muda semântica de teste M1 (proibido).
- `mypy src/` → **23 erros** em 6 arquivos, todos produção: `dashboard/pages.py` (8),
  `pipelines/m1/hex_enrichment.py` (6 — M1), `config.py` (5), `dashboard/censo_map.py` (2),
  `components.py` (1), `pipelines/m1/base_h3_brasil.py` (1 — M1).

Decisão tomada (para manter o gate VERDE — CLAUDE.md §2 "nenhum PR sobe com CI quebrado" — sem
refatorar produção/M1 e sem mass-suppress): os steps `ruff check .` e `mypy src/` foram
adicionados ao `ci.yml` como **`continue-on-error: true`** (informativos no log), com comentário
explicando que a obrigatoriedade (remover `continue-on-error`) é o entregável de **BLK-OPS-02b**.
NENHUMA regra foi desabilitada no `pyproject.toml`; NENHUM `# noqa`/`# type: ignore` foi
espalhado. Isto está documentado no `ci.yml` e em `tasks/backlog.md` (BLK-OPS-02b).
**Sinalização ao QA/humano:** se a expectativa for ruff/mypy BLOQUEANTES já neste PR, isso requer
executar BLK-OPS-02b (nova aprovação) — não cabe no escopo trivial de BLK-OPS-02.

## Arquivos alterados
Modificados: `.gitignore`, `.github/workflows/ci.yml`, `docker-compose.prod.yml`,
`.dockerignore`, `docs/deploy_vps_streamlit.md`, `tasks/backlog.md`, `tasks/current_task.md`,
`tests/integration/test_normalizar_concorrentes.py`, `test_modelo_mercado_hexagonos.py`,
`test_calcular_penetracao_ultra_hex.py`, `test_comparar_geofusion_vs_hex.py`,
`test_validar_penetracao_ultra_hex.py`, `test_normalizar_unidades_ultra.py`.
Novos: `.github/workflows/docker-publish.yml`, `docs/deploy.md`, este handoff + snapshot
`context/handoff/20260529-181856-builder.md`.

## Validações executadas (saída completa)
1. `git check-ignore tests/fixtures/sample.parquet` → saída vazia, **exit 1** (NÃO ignorado). OK.
2. `python -m pytest -q` (LOCAL, dados reais presentes) → **`532 passed, 1 skipped, 9 warnings in 133.74s`**.
   Sem regressão; baseline mantido — prova de que os skip-guards NÃO mascaram nada localmente.
   (Rodado 2×: pré-Fase A 532/1 em 153s; pós-todas-edições 532/1 em 134s.)
3. `ruff check .` → **286 errors (exit 1)** — NÃO zerado; movido a BLK-OPS-02b (ver Desvio acima).
4. `mypy src/` → **23 errors in 6 files (exit 1)** — NÃO zerado; movido a BLK-OPS-02b.
5. `python -c "import streamlit_app; print('import ok')"` → **`import ok`** (2 warnings de cache do
   streamlit sem runtime, esperados). OK.
6. `python -m pytest -q tests/integration/test_streamlit_app.py` → **`147 passed in 19.45s`**. OK.
7. `docker build -f Dockerfile.streamlit -t test:ci .` → **docker indisponível localmente nesta
   máquina Windows** (não há `docker` no PATH em PowerShell nem Bash) — **sanity de build
   delegada ao CI** (workflow Docker Publish no PR). Não é bloqueador. YAML de todos os workflows
   e do compose validado via `yaml.safe_load` (all yaml parsed OK).

## Nº/URL do PR
**Branch commitada e PUSHADA; PR ainda NÃO criado** (`gh` ausente + sem token via credential helper).
- Commit: `383859d` (20 arquivos) na branch `ciclo/BLK-OPS-02`.
- `git push -u origin ciclo/BLK-OPS-02` → **SUCESSO** (`* [new branch] ciclo/BLK-OPS-02`,
  tracking `origin/ciclo/BLK-OPS-02`). O push DISPARA o `pull_request`? Não — push da branch do
  ciclo NÃO dispara o `ci.yml` (gatilho é `pull_request: main`); é o PR que dispara. Por isso o
  PR é obrigatório para a verificação do QA.
- **URL para criar o PR (fornecida pelo GitHub no push):**
  `https://github.com/Kastaldy/motor-de-expansao/pull/new/ciclo/BLK-OPS-02`
- `gh` CLI **não instalado** (ausente em PowerShell e Bash; não em Program Files/LOCALAPPDATA/WinGet)
  e `git credential fill` não expôs token utilizável → não consegui abrir o PR nem disparar o
  workflow Docker Publish via API/CLI nesta máquina.

**Pendência para o orquestrador/humano (só falta isto da Fase E):**
1. Abrir o PR `ciclo/BLK-OPS-02` → `main` (via a URL acima ou `gh pr create`), título:
   "BLK-OPS-02 — CI suite completa + ruff/mypy (informativo) + Docker Publish GHCR + runbook deploy pull".
   Abrir o PR é o que dispara o CI (`pull_request: main`) nos commits da branch — pré-requisito da
   verificação do QA. NÃO mergear (merge é passo humano no fechamento).
2. Disparar o Docker Publish na branch para o QA ver verde ANTES do merge:
   `gh workflow run "docker-publish.yml" --ref ciclo/BLK-OPS-02`
   (ou via Actions → Docker Publish (GHCR) → Run workflow → branch ciclo/BLK-OPS-02).
3. Registrar nº/URL do PR e URL do run de Docker Publish aqui para o QA.

> **Atenção QA sobre o check CI do PR:** os steps ruff/mypy aparecerão como "executados mas com
> erros internos" no log (são `continue-on-error: true`) — isso é ESPERADO e NÃO reprova o check
> CI. O check CI fica VERDE se `pytest` (suíte completa) e o smoke import passarem. ruff/mypy
> bloqueantes = BLK-OPS-02b.

## Count de skips local + nota sobre o runner limpo
- **Local (dados reais presentes):** 532 passed, **1 skipped** (o skip pré-existente de
  `test_pop_censo_v0001.py::test_v0001_e_populacao_total_nao_domicilios`). Nenhum dos novos
  skip-guards disparou — todos os parquets/CSVs reais existem nesta máquina.
- **Runner limpo (CI, sem `data/` nem `concorrentes/`):** os testes acoplados a dado real viram
  `skipped`. Tentei simular escondendo `data/`+`concorrentes/`, mas o `Rename-Item` foi negado
  (handle aberto/permissão) — NÃO consegui medir o count exato de skips no runner aqui. Contagem
  máxima possível de tests nos arquivos guardados (collect-only): test_normalizar_concorrentes=8,
  test_modelo_mercado_hexagonos=17 (5 sintéticos NÃO skipam → ~12 skipam), test_calcular_penetracao=6
  (1 sintético NÃO skipa → 5 skipam), test_comparar_geofusion=12 (1 sintético NÃO skipa → 11 skipam),
  test_validar_penetracao=6 (todos skipam), test_normalizar_unidades=8 (todos skipam),
  test_expansao_dominio=22 (já guardado). Estimativa: ~50-72 skips no runner limpo, conforme quais
  dirs faltam. **O count exato no runner é verificado pelo QA via o log do step de testes do PR**
  (critério de aceite: 0 failed / 0 errors; todos os não-rodados são skips justificados por path ausente).

## Problemas / desvios
- `gh` e `docker` ausentes nesta máquina → Fase E e validação 7 não executadas localmente
  (delegadas a humano/CI). Documentado acima.
- Path real dos pipelines GeoFusion/penetração = `unidades_ultra_performance_hex.parquet`
  (não `..._performance.parquet` como o plano sugeria) — corrigido com base na leitura do código.
- ruff/mypy muito acima do escopo trivial → BLK-OPS-02b criado; steps não-bloqueantes no CI.
- Durante a investigação, um `ruff check . --fix` exploratório + `git checkout -- .` reverteu
  temporariamente as edições de controle pré-existentes (current_task/handoff/backlog do estado
  pré-bloco). `current_task.md` foi restaurado para o conteúdo BLK-OPS-02; `backlog.md` está no
  HEAD + as edições deste bloco; nenhum código de produção ficou alterado (revert total dos .py).

## Pendências
- Fase E (branch + commit por path + push + PR + dispatch Docker Publish) — passo do
  orquestrador/humano (sem `gh`/push aqui).
- BLK-OPS-02b — saneamento ruff/mypy (registrado no backlog).
- BLK-OPS-02a — fixtures sintéticas (já previsto; `.gitignore` já destravado).

## Riscos
- Docker Publish nunca rodou: primeira execução pode expor ajuste de permissões de Packages no
  repo (Settings → Actions → Workflow permissions / package visibility). Mitigação: `workflow_dispatch`
  na branch para validar antes do merge.
- Pacote GHCR privado exige `docker login` no VPS (PAT read:packages) — documentado em deploy.md.
- Suíte completa no runner pode ser mais lenta (geoprocessamento). Cache de pip ajuda no install.

## Guardrails verificados
- **Score NÃO alterado:** nenhum arquivo de scoring/`config.py`/`core` tocado; nenhum assert de
  `score_oficial`/`score_oficial_nome`/`osm_status`/`hex_score_estrutural`/`len==54` alterado —
  só `assert PATH.exists()` → skip-guard.
- **Artefatos M1 preservados:** nenhum parquet/output M1 gerado, lido para escrita ou alterado.
- **Dashboard offline mantido:** nenhuma dependência de API ao vivo introduzida; compose lê
  Parquets por volume read-only.
- **VPS intocado:** nenhum comando SSH/MCP no servidor (CLAUDE.md §6).
- **Commit por path:** sem `git add -A`/`.`; PRD.md NÃO incluído.
