# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-RELMUN-05 — Cores otimistas (verde) para aprovados na Visão Geral do Município

## O que foi feito
Troca de DISPLAY no Relatório Municipal (READ-ONLY M1), conforme plano do Planner e gate D1 pré-aprovado por Vinicius (2026-07-08):
1. Cores dos hexágonos APROVADOS de amarelo/laranja → 2 tons de verde:
   - `_COR_APROVADO_PROPRIO = (255,210,28)` → `(20,170,80)` (verde forte)
   - `_COR_APROVADO_MUNICIPAL = (245,140,30)` → `(90,190,120)` (verde médio)
   - `_COR_REPROVADO = (150,156,170)` (cinza): INALTERADO.
   - Derivados (`_HEX_DESTAQUE_RGBA`, `_HEX_DESTAQUE_MUNICIPAL_RGBA`, `_HEX_APROVADO_RGBA`, `_HEX_APROVADO_MUNICIPAL_RGBA`, `_COBERTURA_LEGENDA`, `_RESUMO_LEGENDA`) herdam automaticamente das 2 constantes-fonte (verificado — nenhuma edição direta).
2. Texto VISÍVEL no PDF "amarelos" → "destacados":
   - `_resumo_page`: "Soma dos hexágonos amarelos / 2.500" → "...destacados / 2.500"
   - `_espaco_academias_page` (rodapé Método): "Espaço = soma dos hexágonos amarelos / 2.500." → "...destacados / 2.500."
   - Confirmado por grep que eram as ÚNICAS 2 strings visíveis com "amarelo".
3. Comentários/docstrings de COERÊNCIA (não user-facing) que descreviam a cor dos hexes aprovados/destacados atualizados de "amarelo/dourado/laranja" para "verde/destacados" (blocos ~85-88, ~100, docstrings ~880/~884, comentário ~1071/~1073, docstring ~1203). Line ~883 ("dominio ... fallback amarelo") deixada INTOCADA de propósito: o fallback de domínio usa `_HEX_NEUTRO_RGBA` (cinza-neutro), então nem "amarelo" nem "verde" seria acurado — fora do escopo de cor deste bloco.
4. Emenda BLK-RELMUN-05 inserida na DEC-011 do CLAUDE.md (após a emenda BLK-RELMUN-03), registrando que é DISPLAY LOCAL: critério de destaque (`oferta_efetiva_disponivel >= 2000`), identificadores (`n_hex_amarelos`/`soma_oferta_amarelos`/`parcelas_amarelos`/chaves de `result`), `flag_sam`/score/pesos/artefatos M1 permanecem INTOCADOS.
5. Testes: import das 3 constantes de cor; novo `test_cores_aprovados_verdes_blk_relmun_05`; 2 asserts em `test_pdf_municipal_8_paginas_e_secoes` (nenhum "amarelo" no PDF gerado + presença de "hexágonos destacados").

## Arquivos alterados
- `src/motor_expansao/dashboard/relatorio_municipal.py` — 2 constantes de cor (verde); 2 strings visíveis de PDF; comentários/docstrings de coerência. Identificadores com "amarelo" INTOCADOS.
- `tests/unit/test_relatorio_municipal.py` — import de `_COR_APROVADO_PROPRIO`/`_COR_APROVADO_MUNICIPAL`/`_COR_REPROVADO`; novo teste de cor; 2 asserts de wording no PDF. (isort aplicado por `ruff --fix` no bloco de import.)
- `CLAUDE.md` — emenda 2026-07-08 (BLK-RELMUN-05) na DEC-011.
- `tasks/current_task.md` — status → aguardando QA.

## Validações executadas
- `python -m ruff check .` → **All checks passed!** (0 erros; 1 fix de isort aplicado no import do teste).
- `python -m mypy src` → **6 erros PRÉ-EXISTENTES** de `types-requests` (import-untyped) em arquivos NÃO tocados (`growth_api_client.py`, `ibge_censo.py`, `poi_enrichment.py`, `base_h3_brasil.py`, `api/geo.py`, `api/telegram_bot.py`). Zero erro novo.
- `python -c "import streamlit_app"` → **import ok**.
- `python -m pytest -q tests/unit/test_relatorio_municipal.py tests/unit/test_relatorio_acentuacao_regressao.py` (serial; xdist instável no Python 3.14) → **41 passed, 0 failed, 0 skipped** (warnings = deprecations pré-existentes de pytest_asyncio). O 2º arquivo gera o PDF municipal e valida ausência de "?" (fora de latin-1) — verde.
- `git diff --stat` restrito aos alvos + bookkeeping; **nenhum parquet**; zero alteração em `pipelines/`, `config.py`, `censo_point/map/report`, artefatos oficiais.

## Problemas encontrados
Nenhum. (ruff pediu isort do import novo no teste; resolvido via `ruff --fix` só no bloco de import.)

## Pendências
- QA: suíte completa (`pytest -q`, serial — gate único do ciclo).
- Revisão VISUAL do PDF (não automatizável por assert): 2 tons de verde distinguíveis entre si e do cinza de reprovado sobre o basemap claro.

## Riscos remanescentes
- 2 tons de verde pouco distinguíveis — mitigado pela revisão visual do QA (constantes distintas e assert de inequalidade no teste).

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY M1; só cores/texto de display).
- Artefatos M1 preservados: sim (nenhum parquet/artefato tocado).
- Dashboard offline mantido: sim (nenhuma dependência de rede adicionada).
- Identificadores com "amarelo" INTOCADOS: sim (`n_hex_amarelos`/`soma_oferta_amarelos`/`parcelas_amarelos`/chaves de `result` — testes `:126-127`/`:173-174`/`:387` verdes sem edição).
- Cores de ZONA da página Domínio (`_ZONA_CORES_*`) intocadas: sim.
- Critério de destaque DEC-011 (`oferta_efetiva_disponivel >= 2000`) / `_hex_destacado_mask` / `flag_sam`: intocados.
- Núcleo censo_*, estrutura de páginas (`/Count 9`), marca d'água, `set_compression`, `pdf_version`, `_ascii()`: intocados.

## Resumo (1 linha)
BLK-RELMUN-05: aprovados do Relatório Municipal agora em VERDE (2 tons) + texto "amarelos"→"destacados"; READ-ONLY M1; ruff/import OK, 41 passed / 0 failed no subconjunto.
