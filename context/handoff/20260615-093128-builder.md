# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-EST-03 — Fonte real do solicitante (token→consumidor da API) para a marca d'água do PDF

## O que foi feito
Liguei o `consumidor` já resolvido pela autenticação da API (`auth.resolver_consumidor`, token→consumidor) ao parâmetro `solicitante` da geração do PDF, de modo que todo PDF emitido por `POST /analisar?formato=pdf` carregue o nome do consumidor na marca d'água, preservando o fallback seguro `None` → só "Ultra Academia".

### Passo 1 — `service.py` (uma linha funcional)
Na função `gerar_pdf_ponto`, adicionei `solicitante=consumidor` à chamada final de `gerar_pdf_relatorio_pontual_censitario` (antes linhas 306-308). `consumidor` já estava no escopo (parâmetro `consumidor: str | None`). A assinatura de `gerar_pdf_ponto` NÃO foi estreitada (`consumidor` permanece `str | None`, R5). `censo_report.py` NÃO foi tocado (`solicitante: str | None = None` e `_watermark_text` já estavam prontos).

### Passo 2 — 3 testes em `tests/integration/test_api_analisar.py`
Adicionados após o último teste de PDF existente (`test_analisar_accept_pdf_header`):
- **(a) `test_analisar_pdf_carimba_consumidor`**: `POST /api/v1/analisar?formato=pdf` com `Bearer dev-token`. Spy patcha a função de ORIGEM `motor_expansao.dashboard.censo_report.gerar_pdf_relatorio_pontual_censitario` (o import em `gerar_pdf_ponto` é local por-nome a partir de `censo_report`, então a origem é o alvo correto — patchar `service.<nome>` não funcionaria). Asserts: status 200, content-type `application/pdf`, e o spy capturou `solicitante == get_settings().tokens["dev-token"]` (nome fictício `dev-local`). Spy retorna `b"%PDF-1.4 fake"` — nenhum PDF real em disco.
- **(b) `test_analisar_pdf_solicitante_none_fallback`**: chama DIRETAMENTE `service.gerar_pdf_ponto(lat, lng, None, get_settings(), rotulo=None)` com o mesmo spy → assert spy capturou `solicitante is None` (fallback retrocompatível, independe da rota).
- **(c) Anti-PII**: ambos usam exclusivamente o nome fictício `dev-local` do mapa `tokens` default; spies retornam bytes sintéticos; nenhum PDF/PII real escrito.

### Desvios do plano
- **Runner `-n auto` (xdist) indisponível neste ambiente**: `pytest -n auto` aborta com `INTERNALERROR ... OSError: connection already closed` no setup dos workers execnet (problema de ambiente Python 3.14 local, não defeito de teste). Rodei o subconjunto SERIALMENTE — resultado idêntico ao esperado. O QA roda o gate full conforme política.
- **`ruff`/`mypy` não estão no PATH do shell** → rodei via `python -m ruff` / `python -m mypy`.
- **Base geo materializada localmente**: o gate autouse `_exige_base_geo` NÃO forçou skip, então os testes (a)/(b) rodaram de fato (não ficaram skipped). O spy curto-circuita o render do PDF, mas o restante de `gerar_pdf_ponto` (resolução de município + render de mapas) executa com a base real — mesmo padrão dos testes de PDF já existentes (linhas 93-110). O objetivo do bloco (`consumidor→solicitante`) fica provado pela captura do spy.

## Arquivos alterados
- `src/motor_expansao/api/service.py` — em `gerar_pdf_ponto`, adicionado `solicitante=consumidor` à chamada de `gerar_pdf_relatorio_pontual_censitario` (1 linha funcional).
- `tests/integration/test_api_analisar.py` — 2 testes novos (`test_analisar_pdf_carimba_consumidor` via rota; `test_analisar_pdf_solicitante_none_fallback` direto), com spy por-nome na origem `censo_report` e fixtures fictícias anti-PII.
- `context/handoff.md` (este) + `context/handoff/20260615-093128-builder.md` (snapshot) + `tasks/current_task.md` (housekeeping de esteira).

## Validações executadas
- pytest (serial, `-n auto` indisponível no ambiente): `python -m pytest -q tests/integration/test_api_analisar.py tests/unit/test_relatorio_pontual_censitario_export.py tests/integration/test_streamlit_app.py` → **208 passed, 0 failed, 0 skipped** (104.95s). Os 2 testes novos passam; nenhum dos testes de export/integração regrediu.
- import: `python -c "import streamlit_app; print('import ok')"` → **import ok**.
- ruff: `python -m ruff check src/motor_expansao/api/service.py tests/integration/test_api_analisar.py` → **All checks passed!**
- mypy: `python -m mypy src/motor_expansao/api/service.py` → **1 erro PRÉ-EXISTENTE** em service.py:222 (`Library stubs not installed for "requests"`, no `_reuse_contextily_session`, intocado por este bloco). Confirmado via `git stash` que o erro existe sem minhas mudanças → **ZERO erro novo introduzido**. `consumidor` (`str | None`) é compatível com `solicitante: str | None`.

## Problemas encontrados
- Ambiente local não roda `pytest -n auto` (xdist/execnet falha no setup de workers no Python 3.14) — contornado rodando serialmente. Sinalizo para o QA usar o runner que funcionar no ambiente dele.
- mypy aponta stub ausente de `requests` (pré-existente, fora de escopo). Não corrigido por estar fora do escopo do bloco; candidato a `types-requests` em tooling separado se desejado.

## Pendências
- Nenhuma dentro do escopo do bloco. (Fora de escopo, inalterado: trilha do dashboard `pages.py` bloqueada por ausência de Authelia; logging LGPD novo não introduzido — premissa R3 mantida: o JSON `AnalisarResponseJSON.consumidor` segue carimbado e inalterado.)

## Riscos remanescentes
- O wiring grava identidade do consumidor (token→consumidor) na marca d'água do PDF — esperado e aprovado (gate humano por Vinicius em 2026-06-15). Sem nomes reais nas fixtures.
- Discrepância de runner (serial vs `-n auto`) é de ambiente, não de produto.

## Guardrails verificados
- score_priorizacao não alterado: sim (não aplicável ao escopo; READ-ONLY M1, DEC-001)
- Artefatos M1 preservados: sim (nenhum tocado; pesos `renda=0.40`/`pop=0.60` inalterados)
- Dashboard offline mantido: sim (não aplicável; nada tocado no dashboard; geração de PDF pela API é on-demand, DEC-005)
- `censo_report.py` NÃO alterado: sim
- `AnalisarResponseJSON` NÃO alterado: sim
- Anti-PII: sim (fixtures com nome fictício `dev-local`; nenhum PDF/PII real versionado)
- Sem logging LGPD novo: sim (premissa R3 preservada)
