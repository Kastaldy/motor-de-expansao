# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-FIX-01 — Corrigir validação de score_expansao_hibrido (alternativa iii)

## O que foi feito
Solução só-validador (read-only), seguindo à risca o plano do Planner (alternativa iii). Diff conceitual:

**`schemas.py` (constantes):**
- Removido `"score_expansao_hibrido"` de `_SCORE_RANGE_OPTIONAL`, que agora contém só scores genuínos [0,100]: `score_setor_2022_calibrado`, `score_oportunidade_residual`, `score_dominio_hibrido`.
- Criada nova tupla `_SCORE_NUMERIC_OPTIONAL = ("score_expansao_hibrido",)` com comentário de 3 linhas explicando que é chave de ordenação lexicográfica (M1 primário + micro-desempate censitário <=0.001), pode exceder 100 (~100.001) por desenho, e que se valida só como numérico/não-nulo, sem faixa.

**`schemas.py` (`validate_dashboard_frame`, invariante (e)):**
- Extraído helper local `_coerce_numeric(col)` que reaproveita EXATAMENTE a checagem de dtype/conversibilidade que estava inline (mesma mensagem `"nao e conversivel a numerico"`, citando source+coluna), coage a numérico em variável local (nunca reatribuída ao df) e retorna a série.
- O laço de faixa [0,100] (scores obrigatórios + opcionais presentes de `_SCORE_RANGE_OPTIONAL`) ficou idêntico, agora chamando `_coerce_numeric(col)` antes do `between(0,100)`.
- Adicionado laço final que, para cada coluna de `_SCORE_NUMERIC_OPTIONAL` presente no df, chama só `_coerce_numeric(col)` (conversibilidade), SEM `between(0,100)`. NaN tolerado. Sem duplicação de lógica.
- Docstring da invariante (e) atualizada para mencionar a chave de ordenação.

**`tests/unit/test_schema_validation.py`:**
- Importado `_SCORE_NUMERIC_OPTIONAL` no topo (junto de `_SCORE_RANGE_OPTIONAL`/`_SCORE_RANGE_REQUIRED`).
- `test_score_opcional_fora_de_faixa_levanta`: trocado de `score_expansao_hibrido = [70,200,40]` para `score_oportunidade_residual = [70.0,200.0,40.0]` com `match="score_oportunidade_residual"` (preserva a intenção: opcional [0,100] fora de faixa levanta).
- Adicionado `test_score_expansao_hibrido_acima_de_100_passa` (`[100.001, 100.0, 99.999]` → não levanta).
- Adicionado `test_score_expansao_hibrido_nao_conversivel_levanta` (`["x","y","z"]` → levanta `match="nao e conversivel a numerico"`, mensagem cita o campo).
- Adicionado `test_score_expansao_hibrido_com_nan_tolerado` (`[100.001, np.nan, 99.5]` → não levanta).
- Adicionado `test_score_expansao_hibrido_fora_de_required` (sanidade: in `_SCORE_NUMERIC_OPTIONAL`, not in `_SCORE_RANGE_OPTIONAL`/`_SCORE_RANGE_REQUIRED`).
- Não tocados `test_score_fora_de_faixa_superior_levanta` / `test_score_fora_de_faixa_inferior_levanta` (scores obrigatórios seguem estritos).

## Arquivos alterados
- `src/motor_expansao/dashboard/schemas.py` — removeu hibrido de `_SCORE_RANGE_OPTIONAL`, criou `_SCORE_NUMERIC_OPTIONAL`, extraiu helper `_coerce_numeric` e adicionou laço numérico-sem-faixa; docstring atualizada (arquivo: 105 linhas).
- `tests/unit/test_schema_validation.py` — import + 1 teste ajustado + 4 testes novos (arquivo: 192 linhas).

## Validações executadas
$ python -m pytest -q tests/unit/test_schema_validation.py
....................                                                     [100%]
20 passed in 0.19s

$ python -m pytest -q
567 passed, 1 skipped, 9 warnings in 198.89s (0:03:18)

$ python -c "import streamlit_app; print('import ok')"
(warnings de streamlit "missing ScriptRunContext"/"No runtime found" — esperados em bare mode)
import ok

$ python -m pytest -q tests/integration/test_streamlit_app.py
147 passed in 12.16s

$ python -m ruff check src/motor_expansao/dashboard/schemas.py tests/unit/test_schema_validation.py
All checks passed!

$ python -m mypy src/motor_expansao/dashboard/schemas.py
Success: no issues found in 1 source file

## Problemas encontrados
nenhum

## Pendências
nenhuma

## Riscos remanescentes
- Perda de cobertura de faixa do híbrido é por desenho (chave de ordenação, não score [0,100]); mitigado pelos testes de teto técnico ~100.001 e de não-conversível. O teto real depende de `score_setor_2022_calibrado` ∈ [0,100], que é validado com faixa estrita no mesmo frame (`_SCORE_RANGE_OPTIONAL`).

## Guardrails verificados
- score_priorizacao/hex_score_estrutural não alterados (seguem estritos [0,100]): sim
- Fórmula híbrida/LOCAL_BONUS_DIVISOR/modelo_hibrido_expansao.py intocados: sim
- score_dominio_hibrido intocado (segue faixa estrita): sim
- Nenhum Parquet regenerado; artefatos M1 preservados: sim
- Validação read-only (df não mutado): sim (coerção só em variável local via `_coerce_numeric`)
- CLAUDE.md NÃO commitado/alterado: sim
