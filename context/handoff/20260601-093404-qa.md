# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA / Quality Analyzer

## Próxima Skill recomendada
**Orquestrador (fechamento)** — commit por path, `housekeeping_move_block.py BLK-SEC-01`
(backlog→completed), merge da `ciclo/BLK-SEC-01` em `main`. Nível 3 (run real no Actions
provando publish/build-sanity skipped com test vermelho) é **prova diferida ao humano** no
fechamento — RESSALVA NÃO bloqueante.

## Bloco auditado
BLK-SEC-01 — Gate de publicação no CI (publish só com CI verde) + pin de imagem e rollback.
Criticidade: Alta. Esteira: Block Orchestrator → Planner → [REVISÃO HUMANA] → Builder → QA.

## VEREDITO
**APROVADO COM RESSALVAS**

Ressalva única (NÃO bloqueante, esperada pelo enunciado): a prova dinâmica Nível 3 (disparar run
real no GitHub Actions e comprovar que `publish`/`build-sanity` ficam skipped com `test` vermelho)
é diferida ao fechamento humano. O gate é provado neste ciclo por prova estática (Nível 1, A-G) +
sintaxe (Nível 2, parsers YAML). Não há bypass, escopo excedido nem M1 tocado.

## Justificativa
- As 7 validações obrigatórias foram RE-EXECUTADAS pelo QA por conta própria (saída literal abaixo)
  e TODAS passam: YAML do CI parseia, `docker-publish.yml` removido, compose com pin fail-closed
  `${STREAMLIT_IMAGE:?...}` (sem `:latest`), 150 testes de integração verdes, smoke import ok,
  suíte completa **626 passed, 1 skipped, 0 failed, 0 collection errors**, e o diff fica
  estritamente no escopo permitido (workflows/compose/docs/tasks/handoff).
- Prova estática do gate (Nível 1, A-G) confirmada por inspeção do YAML real — não por mock.
  Os três únicos pontos de risco (`push: true`, `docker/login-action`, `packages: write`) existem
  cada um UMA vez e SEMPRE dentro do job `publish`, que é duplamente travado por `needs: [test]` E
  `if: github.event_name == 'push' && github.ref == 'refs/heads/main'`.
- O ajuste humano aprovado por Felipe Silva em 2026-06-01 (pin fail-closed em vez do `sha-<commit>`
  original do D2) está corretamente implementado e rastreável nos snapshots; a Validação #3 reflete
  a nova forma. Onde o plano original divergia, o banner venceu — comportamento correto.
- Zero alteração em M1: `src/motor_expansao/config.py` UNCHANGED (confirmado por `git diff --quiet`);
  nenhum `*.parquet`, `PRD.md`, `src/` ou `dashboard/` no diff.

## Problemas críticos
Nenhum.

## Problemas médios
Nenhum bloqueante. Observações (não exigem ação neste ciclo):
- O job `test` em `workflow_dispatch` na própria `main` rodaria `build-sanity` (sem push) — correto,
  dispatch nunca publica. Comportamento documentado e intencional.

## Melhorias opcionais
- (Follow-up BLK-SEC futuro, fora de escopo): assinatura de imagem (cosign)/attestation/SBOM e
  varredura de vulnerabilidades como gate — já roteirizados em outros blocos BLK-SEC.
- `docs/deploy.md` cita a tag `latest` como existente em push na main (linha ~19). É verdadeiro
  (metadata-action gera `latest` só na default branch), mas o runbook canônico orienta pin por
  digest — coerente. Sem ação.

## Testes faltantes
- Prova dinâmica Nível 3 (run real no Actions) — diferida ao humano no fechamento (ver ressalva).
  Nenhum teste unitário/integração faltante para o escopo (CI/CD + docs não são cobríveis por
  pytest; cobertos por prova estática + parser YAML).

## Riscos remanescentes
- Pin de produção depende de um digest real publicado (só existe após push real ao GHCR pelo job
  `publish`); documentado como obter sem fabricar. Fora do escopo de execução (passo humano §6).
- Histórico append-only (`tasks/completed.md`, snapshots antigos) ainda cita `docker-publish.yml` —
  registro histórico intencionalmente não reescrito; `tasks/backlog.md` cita o nome só como
  "arquivos prováveis" forward-looking. Sem impacto no gate ativo.

## Saída literal das validações (re-executadas pelo QA)

### 1. `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml', encoding='utf-8')); print('ci.yml ok')"`
```
ci.yml ok
```

### 2. `python -c "import os; assert not os.path.exists('.github/workflows/docker-publish.yml'); print('removido')"`
```
removido
```

### 3. `python -c "... img=d['services']['streamlit']['image']; ... assert img.startswith('${STREAMLIT_IMAGE:?'); assert ':latest' not in img ..."`
```
${STREAMLIT_IMAGE:?defina STREAMLIT_IMAGE com o digest imutavel ghcr.io/kastaldy/motor-de-expansao/motor-expansao-streamlit@sha256:<digest> — ver docs/infra_producao.md (Atualizar/Rollback)}
compose fail-closed ok
```

### 4. `python -m pytest -q tests/integration/test_streamlit_app.py`
```
........................................................................ [ 48%]
........................................................................ [ 96%]
......                                                                   [100%]
150 passed in 6.35s
```

### 5. `python -c "import streamlit_app; print('import ok')"`
```
(warnings de ScriptRunContext/MemoryCacheStorageManager — esperados em bare mode)
import ok
```

### 6. `python -m pytest -q` (suíte completa)
```
626 passed, 1 skipped, 9 warnings in 83.04s (0:01:23)
```
Critério atendido: 0 failed / 0 collection errors. (Baseline histórico ~532 é menor; o repo cresceu —
o que importa é zero falhas.)

### 7. Escopo do diff — `git status --porcelain` + `git diff --name-only`
```
 M .github/workflows/ci.yml
D  .github/workflows/docker-publish.yml
 M context/handoff.md
 M docker-compose.prod.yml
 M docs/deploy.md
 M docs/infra_producao.md
 M tasks/current_task.md
?? context/handoff/20260601-091229-block-orchestrator.md
?? context/handoff/20260601-091547-planner.md
?? context/handoff/20260601-092740-builder.md
```
Apenas `.github/workflows/*`, `docker-compose.prod.yml`, `docs/*.md`, `tasks/*`, `context/handoff*`.
NENHUM `src/`, `dashboard/`, `*.parquet`, `config.py`, `PRD.md`. Escopo CONFORME.

### Housekeeping — `python scripts/housekeeping_move_block.py BLK-SEC-01 --check`
```
FALHA --check: stub ausente no backlog para BLK-SEC-01
EXIT=0
```
**FALHA ESPERADA pré-move** — o move backlog→completed é do ORQUESTRADOR no fechamento (Passo 6.0),
DEPOIS deste veredito. O `--check` deve passar nesse momento. NÃO é reprovação.

### Housekeeping — `python -m pytest -q tests/unit/test_housekeeping_helper.py`
```
10 passed in 0.04s
```

## Conferência de no-bypass
- As validações foram rodadas pelo QA contra os arquivos REAIS (YAML do `ci.yml`/compose,
  importação real do app, suíte completa real) — sem mock do caminho crítico, sem fixture que não
  casa o real.
- O caminho crítico (estrutura dos workflows e do compose) foi inspecionado linha a linha no YAML
  real, não inferido de log. Os asserts da Validação #3 testam a string `image` materializada pelo
  parser, não um valor fabricado.
- Confirmado: NÃO sobrou nenhum job/step com `push: true`, `docker/login-action` ou `packages:
  write` fora do job `publish` gated (grep no `ci.yml`: cada um aparece 1x, todos dentro de
  `publish`).

## Conferência cruzada com log do Builder
- Builder reportou: V1 `ci.yml ok`; V2 `removido`; V3 fail-closed; V4 `150 passed`; V5 `import ok`;
  V6 `626 passed, 1 skipped`; V7 escopo limpo. **Bate 1:1** com a re-execução do QA (V4 do QA:
  150 passed; V6: 626 passed, 1 skipped). Diferença trivial de tempo de execução (6.35s vs 6.90s)
  é irrelevante.
- Nota técnica do Builder sobre o `run:` do echo do digest (aspas simples por causa do `: ` literal)
  CONFIRMADA: o `ci.yml` parseia sem exceção (V1) e o valor é
  `run: 'echo "Digest imutavel publicado: ${{ steps.build.outputs.digest }}"'` — YAML válido para o
  Actions, comportamento idêntico (o GitHub re-interpola `${{ }}` independentemente das aspas YAML).
- O log do Builder foi usado só como referência cruzada; a prova é a re-execução própria acima.

## Prova estática do gate (Nível 1 — A-G), com trechos do YAML real
- **(A) Sem publish/push sem CI verde.** Job `publish` (ci.yml L54-56):
  `publish:` / `needs: [test]` / `if: github.event_name == 'push' && github.ref == 'refs/heads/main'`.
  Sem `test` verde, `publish` é pulado. Grep confirma `push: true` (L85), `docker/login-action`
  (L64) e `packages: write` (L60) SÓ dentro de `publish`. CONFORME.
- **(B) build-sanity não publica.** L95-113: `build-sanity` com `push: false` (L110), sem
  `docker/login-action`, sem `type=raw,value=latest`. Dispatch manual não publica. CONFORME.
- **(C) docker-publish.yml REMOVIDO.** Validação #2 = `removido`; `git status` mostra
  `D  .github/workflows/docker-publish.yml` (deleção staged). Sem workflow paralelo publicando em
  `push:[main]` sem gate. CONFORME.
- **(D) metadata-action mantém type=sha + latest só na default branch.** L74-76:
  `type=sha` e `type=raw,value=latest,enable={{is_default_branch}}`. CONFORME.
- **(E) packages: write least-privilege.** `permissions: contents: read` global (L15-16); `packages:
  write` SÓ no job `publish` (L58-60). `test` e `build-sanity` ficam com `contents: read`. CONFORME.
- **(F) Compose fail-closed coerente; override preservado.** `image: ${STREAMLIT_IMAGE:?...}`
  (compose L28) — `up` sem `STREAMLIT_IMAGE` falha de propósito. O mecanismo de override por
  `STREAMLIT_IMAGE` (digest imutável) está preservado e é o pin canônico documentado. Sem `:latest`,
  sem `sha-<commit>` hardcodado, sem `@sha256:` fabricado. CONFORME.
- **(G) Runbook PULL + rollback por digest, sem `--build`.** `docs/infra_producao.md` tem
  "## Atualizar o dashboard (modo PULL, sem build)" e "## Rollback (por digest imutavel, SEM
  rebuild)"; grep por `--build` retorna só ocorrências "SEM --build" (negações) — nenhum
  `up -d --build` contraditório sobrou. Referências apontam para o job `publish` do CI
  (`.github/workflows/ci.yml`), não ao arquivo removido (grep `docker-publish` em docs/*.md e *.yml:
  No matches). `docs/deploy.md` coerente com `infra_producao.md`. CONFORME.

## Guardrails verificados
- **M1 intocável:** `src/motor_expansao/config.py` UNCHANGED (`git diff --quiet` → "config.py
  UNCHANGED"). Nenhum `*.parquet`, `PRD.md`, `src/`, `dashboard/` no diff. Score
  `score_priorizacao`/`hex_score_estrutural`/pesos (renda=0.40/pop=0.60)/fórmula/carteira/plano/
  artefatos NÃO alterados. CONFIRMADO.
- **Parâmetros canônicos preservados:** H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0,
  renda=0.40/pop=0.60 — config.py intocado, logo inalterados.
- **§2 commit por path:** working tree pronto; commit é do orquestrador (Builder/QA não fizeram
  `git add`/commit). QA NÃO commitou, NÃO fez `git add`, NÃO alterou código/escopo.
- **§6 VPS:** nada executado no servidor; runbooks são documentação; deploy/rollback = passo humano.
- **Escopo não excedido:** apenas o bloco aprovado (com o ajuste humano do D2); sem refator fora de
  escopo; cosign/gitleaks/SBOM ficam para blocos futuros.

## Decisão recomendada
**APROVADO COM RESSALVAS** — liberar para fechamento pelo orquestrador:
1. Commit por path (`.github/workflows/ci.yml`, deleção de `docker-publish.yml`,
   `docker-compose.prod.yml`, `docs/infra_producao.md`, `docs/deploy.md`, `tasks/*`,
   `context/handoff*`); nunca `git add -A`.
2. Rodar `python scripts/housekeeping_move_block.py BLK-SEC-01` (move backlog→completed) e confirmar
   `--check` PASSANDO após o move.
3. Merge `ciclo/BLK-SEC-01` → `main`.
4. **Pós-merge (humano):** Nível 3 — disparar run controlado no Actions provando que com `test`
   vermelho `publish`/`build-sanity` ficam skipped (sem push ao GHCR); anotar run id no fechamento.
   Ressalva NÃO bloqueante deste ciclo.
