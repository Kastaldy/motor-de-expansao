# Handoff — QA

## Skill que gerou este handoff
QA

## Bloco validado
BLK-DIM-06

## Veredito
APROVADO

## Resultados dos gates

### Suite de testes
872 passed, 4 skipped (gate duro satisfeito: >= 872 passed, <= 4 skipped). 0 failed.
Os 11 testes novos de `test_backtest_dim.py` passam.

### ruff + mypy
PASS.
- `ruff check backtest_dim.py + test_backtest_dim.py`: All checks passed!
- `mypy backtest_dim.py --ignore-missing-imports`: Success: no issues found in 1 source file.

### Guardrails
- M1 intocado: SIM. `git diff HEAD src/motor_expansao/pipelines/m1/` vazio. `config.py`
  (`src/motor_expansao/config.py`) aparece no working tree como `M`, mas é EXCLUSIVAMENTE
  drift de line-endings (CRLF/EOL): `git diff --ignore-all-space --ignore-blank-lines` retorna
  VAZIO; 184/184 linhas substituídas sem mudança de conteúdo. Faz parte do drift repo-wide de
  420 arquivos presente desde o início da sessão — não é alteração deste ciclo. ZERO mudança
  semântica em config.py.
- aderencia.py/simulador.py intocados: SIM. `git diff HEAD` dos dois arquivos retorna VAZIO.
  São apenas importados (funções congeladas reusadas via LOO), nunca editados.
- backtest_dim.md gitignored: SIM. `git check-ignore data/analysis/backtest_dim.md` retorna o
  path.
- Arquivos criados existem: SIM. `backtest_dim.py` (23 KB) + `test_backtest_dim.py` (6.8 KB).

### Anti-circular
PASS. Evidência (leitura de `backtest_dim.py`):
- Camada 1 (linhas 150-152, 213-215): alvo = `pagantes_steady_state` REAL do parquet; predição
  = saída LOO de `prever_aderencia`. Nenhuma saída de simulador como alvo.
- Camada 3+4 (linhas 268, 306-307): alvo = `faturamento` REAL do parquet (`faturamento_real`);
  predição = `res.faturamento_mensal_steady`. NÃO existe nenhum
  `faturamento = viabilidade(...).faturamento_mensal_steady` usado como target.
- End-to-end (linhas 378, 416-417): mesmo padrão — alvo real `faturamento`, predição do
  simulador alimentado por alunos previstos LOO.
- Teste `test_sem_dado_autogerado` confirma: multiplica o `faturamento` real ×10 e exige que o
  MAPE mude — prova que o alvo é LIDO do campo real, não recalculado pelo simulador.

### Armadilha churn
PASS. `_churn_para_fracao()` é chamada ANTES de `viabilidade()` em ambos os caminhos:
- camada34: linha 287 (`churn_fracao = _churn_para_fracao(...)`) → `viabilidade(..., churn=churn_fracao)` na linha 302.
- end_to_end: linha 397 → `viabilidade(..., churn=churn_fracao)` na linha 412.
- Também usado em `_alunos_maturidade_de_pagantes` antes da chamada. Teste
  `test_backtest_camada34_corrige_churn_percentual` exige MAPE finito e < 5.0 (explodiria se o
  churn percentual não fosse convertido).

### Housekeeping check
Código de retorno 1 (`FALHA --check: stub ausente no backlog para BLK-DIM-06`). ESPERADO — o
orquestrador move o bloco para `completed.md` APÓS o QA. Não bloqueia o veredito.

## Observações finais
APROVADO sem ressalvas técnicas. O backtest é honesto: alvo sempre o dado real, baseline da
média exposto, R²/MAPE refletem o erro MEDIDO (end-to-end R²=-0.53, pior que o chute ingênuo —
NO-GO da Camada 1 BLK-DIM-01R propaga, conforme esperado e documentado). READ-ONLY sobre o M1
preservado; `aderencia.py`/`simulador.py` congelados de fato.

Nota de contexto (não-bloqueante): o working tree carrega drift repo-wide de line-endings em
~420 arquivos (incl. `config.py`), presente desde o início da sessão. O commit-por-path do
fechamento deve incluir SOMENTE os 4 paths do ciclo (`backtest_dim.py`, `test_backtest_dim.py`,
`context/handoff.md`, snapshot) — NÃO commitar `config.py` nem demais arquivos de EOL.

Próximo passo: orquestrador move BLK-DIM-06 para `completed.md` (housekeeping), commita por path
e fecha o ciclo. Sem merge/push/deploy autônomo (regra do loop).
