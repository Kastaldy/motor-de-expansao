# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA

## Bloco
BLK-VIAB-06

## Status
IMPLEMENTADO

## Arquivos alterados
- src/motor_expansao/dimensionamento/viabilidade_ponto.py
- tests/unit/dimensionamento/test_viabilidade_ponto.py

## Saída literal das validações

### pytest
```
============================= test session starts ==============================
platform linux -- Python 3.12.13, pytest-8.4.2, pluggy-1.6.0
rootdir: /repo
configfile: pyproject.toml
plugins: xdist-3.8.0, mock-3.15.1, asyncio-0.24.0, cov-7.1.0, anyio-4.55.0
asyncio: mode=Mode.AUTO, default_loop_scope=None
collected 27 items

tests/unit/dimensionamento/test_viabilidade_ponto.py ................... [ 70%]
........                                                                 [100%]

============================== 27 passed in 3.75s ==============================
```

### ruff
```
All checks passed!
```

### loop_guard
```
GUARD OK: 0 caminho(s) alterado(s), nenhum proibido.
```

## Guardrails verificados
- `config.py` — não tocado; `ENVELOPE_MIN`/`ENVELOPE_MAX` são constantes locais do módulo `viabilidade_ponto.py`
- `src/motor_expansao/pipelines/m1/` — não tocado
- `score_priorizacao`, `hex_score_estrutural`, pesos `renda=0.40`/`pop=0.60` — inalterados
- Artefatos oficiais do M1 — inalterados
- VPS / rede — zero acesso externo
- PII — testes usam fixtures sintéticas em memória
- `test_batch_viabilidade.py` e `test_backtest_viabilidade.py` — não modificados
- `loop_guard.py` — GUARD OK (0 caminhos proibidos)

## Notas para o QA
- 21 testes antigos permanecem passando; 6 novos testes da seção BLK-VIAB-06 adicionados, todos passando — total 27 passed.
- O campo `flag_fora_envelope: bool = False` foi adicionado ao final dos campos com default do dataclass `ViabilidadePontoResult`, após `demanda_fonte`. Nenhum `TypeError` — a ordem de campos sem default não foi alterada.
- O cálculo `flag_envelope = not (ENVELOPE_MIN <= m2 <= ENVELOPE_MAX)` foi inserido como passo 2b no orquestrador `analisar_viabilidade_ponto`, após `flag_zona_morta` e antes do passo 3 (faixa de alunos). A flag é puramente informativa: DRE, faixa de alunos e grade de sensibilidade continuam calculados normalmente para qualquer m2.
- `ENVELOPE_MIN` e `ENVELOPE_MAX` foram adicionados ao `__all__` e são importáveis diretamente pelos testes.
- Comportamento byte-idêntico para m2 ∈ [600, 3000]: `flag_fora_envelope=False` (o default).
