# Handoff (snapshot) — QA/Quality Analyzer — BLK-OPS-10

## VEREDITO
APROVADO

## Justificativa
Helper versionado, puro e testado (10 casos verdes), move byte-idêntico (fatia literal, CRLF preservado);
6.0 + 6.c + checklist do QA coerentes; suíte completa verde (542 passed) e smoke de CLI contra conteúdo
REAL (sem bypass) confirma move/--check/ad-hoc.

## Escopo entregue (4 substantivos + bookkeeping)
- scripts/housekeeping_move_block.py (NOVO): move_block/verify_moved (puros) + CLI (--check, EXIT_AD_HOC=3).
- tests/unit/test_housekeeping_helper.py (NOVO): 10 testes.
- .claude/commands/run-cycle.md: Passo 6.0 + bullet 6.c (dummy block na branch, abandona sem merge) + guardrail.
- prompts/qa_analyzer.md: seção de housekeeping exigindo --check + byte-identity vs git HEAD + pytest.

## Notas do humano atendidas
1. moved = backlog[start:end] fatia literal (offset de caractere). 2. CLI newline="" utf-8 (CRLF). 3. idempotência re-entrante.

## Defeito corrigido no QA (no-bypass real)
Smoke CRLF reprovou `--check` inicial: regex do stub ` *$` não tolerava `\r` → corrigido p/ `[ \t\r]*$`
+ assert verify_moved em CRLF no teste. Testes só-LF não pegavam; smoke real pegou.
Também ruff I001 no helper auto-corrigido (reordenação cosmética).

## Validações (re-executadas pelo QA)
- `pytest -q` → 542 passed, 1 skipped, 9 warnings.
- `ruff check` (helper+teste) → All checks passed!
- CLI smoke (cópia REAL CRLF, bloco BLK-OPS-03): move rc=0; --check rc=0; ad-hoc rc=3 (SKIP, sem traceback);
  byte-identity verbatim em completed + stub no backlog + heading removido + completed append-only.

## No-bypass
Sem --config /dev/null, sem mock; smoke roda o CLI REAL sobre cópia do backlog/completed REAIS.

## Guardrails
score/M1 N/A (não tocados); escopo só os 4 arquivos + bookkeeping; guard dry_run:true intacto.

## Decisão
Fechar ciclo → commit por path → merge humano → dry-run autônomo 6.c (ciclo ALTERA orquestração).
