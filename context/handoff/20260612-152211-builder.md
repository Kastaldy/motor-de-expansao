# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-UI-05 — Corrigir CSS do seletor de telas (seletores reais do st.segmented_control + !important)

## O que foi feito
Adicionados os seletores reais do DOM do Streamlit 1.58 ao bloco CSS de `inject_styles()` em `pages.py`, com `!important` para sobrepor o CSS emotion do Streamlit:

1. **GAP**: `gap: 8px` → `gap: 8px !important;` no container `div[data-testid="stSegmentedControl"]`.
2. **INATIVO**: adicionado `[data-testid="stBaseButton-segmented_control"]` como seletor adicional na regra base; props visuais `background`, `color`, `border` e `border-radius` ganharam `!important`. `font-size`, `font-weight` e `padding` mantidos sem `!important` (sem conflito de especificidade nesses).
3. **ATIVO**: adicionado `[data-testid="stBaseButton-segmented_controlActive"]` à lista de seletores da regra ativa (junto de `aria-checked`/`aria-selected` preservados); todas as props do ativo ganharam `!important`; `border-color` mudou de `rgba(25,183,255,0.9)` para `#19B7FF !important` (conforme handoff do Planner).
4. **Ordem CSS preservada**: inativo (linhas 309-320) → hover (321-325) → ativo (326-335). Seletores legados `aria-checked`/`aria-selected` mantidos intactos.
5. **Teste de regressão**: adicionados 2 asserts no final de `test_inject_styles_cobre_componentes_baseweb`:
   - `assert "stBaseButton-segmented_controlActive" in css`
   - `assert "stBaseButton-segmented_control" in css`

## Arquivos alterados
- `src/motor_expansao/dashboard/pages.py` — linhas 304-335: bloco CSS `stSegmentedControl` em `inject_styles()` (gap `!important`, seletor inativo real + `!important`, seletor ativo real + `!important`). Nenhuma outra linha tocada.
- `tests/integration/test_streamlit_app.py` — linhas 4483-4484: 2 asserts de regressão adicionados ao final de `test_inject_styles_cobre_componentes_baseweb`.

## Validações executadas
- **pytest** `tests/integration/test_streamlit_app.py`: **183 passed, 0 failed, 0 skipped** (serial, sem `-n auto`)
  - Subconjunto escolhido: arquivo de integração do dashboard — cobre diretamente `inject_styles()` e os asserts novos.
- **import**: `python -c "import streamlit_app"` → **import ok** (warnings de runtime esperados em bare mode, sem exceção).
- **ruff**: `python -m ruff check src/motor_expansao/dashboard/pages.py tests/integration/test_streamlit_app.py` → **All checks passed!**
- **mypy**: `python -m mypy src/motor_expansao/dashboard/pages.py` → **Success: no issues found in 1 source file**

## Problemas encontrados
- `ruff` não estava no PATH do bash; usado via `python -m ruff` (equivalente, mesma versão). Resultado limpo.

## Pendências
- Confirmação VISUAL no navegador pelo usuário (Felipe): verificar que a aba ativa fica ciano sólido e o gap aparece. O pytest não detecta render no browser — limite documentado.

## Riscos remanescentes
- Testids (`stBaseButton-segmented_control` / `stBaseButton-segmented_controlActive`) podem mudar em versões futuras do Streamlit. Os seletores legados `aria-checked`/`aria-selected` ficam como fallback documentado.
- Branch stack (`ciclo/BLK-UI-05` sobre `ciclo/BLK-UI-04`): sem impacto neste diff (100% CSS + 2 asserts).

## Guardrails verificados
- score_priorizacao não alterado: sim — nenhuma linha de score ou pipeline tocada
- Artefatos M1 preservados: sim — zero escrita em artefatos
- Dashboard offline mantido: sim — mudança 100% CSS em `inject_styles()`; nenhuma dependência de API adicionada
- `render_tab_selector()` / Bloco 5 intocado: sim — diff exclusivamente no bloco CSS ~linhas 304-335
- Paths pré-sujos não tocados: sim — `_metadata.json` e `relatorio_pontual_censitario_base_geo.md` inalterados
