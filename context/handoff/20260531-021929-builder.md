# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-SCORE-02 — Poder preditivo dos scores vs. desfecho

## O que foi feito
Implementado o backtest READ-ONLY fim a fim em `analysis/score_backtest.py` (funções puras testáveis) + testes sintéticos em `tests/unit/test_score_backtest.py`, e gerado `data/analysis/relatorio_backtest.md` (gitignored).

Funções puras criadas (assinaturas do plano): `pairwise_valid`, `correlate` (scipy `from scipy import stats`; N<10 → suficiente=False/coefs None; variância zero → coefs None com motivo, sem quebrar), `correlate_by_cell` (AGG + por rede), `bootstrap_ci_spearman` (rng=default_rng(42); None p/ N<30), `join_componentes` (left-join read-only só `["hex_id","renda_pct_nacional","pop_pct_nacional"]`, não sobrescreve coluna do dataset — usa sufixo `_prio` se houver colisão; devolve match_stats), `decompor_renda_pop` (correlação isolada renda/pop vs outcome — ACHADO DESCRITIVO, sem propor peso), `find_outliers` (percentis within-rede; expõe só rede/uf/nome_municipio/hex_id/hex_precisao/rotulo_confiabilidade + percentis/valores + hipótese; NUNCA `nome_unidade`), `build_report` (markdown string, sem I/O), `load_inputs` (único I/O; priorizados lido só com as 3 colunas), `main(argv)->int` (load→join→correlações→decomposição→outliers→figuras OPCIONAIS sob try/except+Agg→build_report→grava OUT_REPORT utf-8; print só agregados; return 0; guard `__main__`).

Achados-chave (números reais, N=441): no agregado o melhor é `score_setor_2022_calibrado` (Spearman ρ=+0.148, p=0.004, IC95% [+0.052,+0.251]); `score_priorizacao` (M1) ρ=-0.004 (p=0.948, nulo); `score_oportunidade_residual` ρ=-0.257 (p<0.001, negativo — esperado: residual alto = mercado pouco ocupado); `score_dominio_hibrido` ρ=+0.109 mas N=43 (engcorpo N=4 e ultra N=0 caem no piso). Decomposição (357/391=91,3% match): no AGG ρ(renda)=+0.067 vs ρ(pop)=+0.095 → `pop_pct_nacional` carrega ligeiramente mais sinal monotônico (achado descritivo, sem propor peso). Outliers: ≥1 caso de cada direção por score (ex.: setor_2022 score=100 × outcome baixo em Goiânia/ultra e Florianópolis/engcorpo estimado; e Skyfit PA/PI com score baixo × outcome alto).

## Arquivos alterados
- `analysis/score_backtest.py` (NOVO — script de backtest + funções puras read-only)
- `tests/unit/test_score_backtest.py` (NOVO — 15 testes com fixtures sintéticas)
- `data/analysis/relatorio_backtest.md` (gerado/regenerado — gitignored)
- `data/analysis/fig_ranking_spearman_agg.png` (OPCIONAL — gitignored; gerado por matplotlib sob try/except)
- `tasks/current_task.md` (status → aguardando QA)
- `context/handoff.md` + `context/handoff/20260531-021929-builder.md` (este handoff)

## Validações executadas
- pytest tests/unit/test_score_backtest.py: 15 passed
- python analysis/score_backtest.py: exit 0; relatório gerado com seções (a)(b)(c) + Limitações preenchidas; resumo no stdout (ranking AGG, match 357/391=91.3%)
- pytest -q (completo): 617 passed, 0 failed, 1 skipped (baseline 602 + 15 novos = 617; sem regressão)
- import streamlit_app: ok

## Achados do relatório (números reais)
- (a) Ranking AGG (Spearman ρ): 1º `score_setor_2022_calibrado` +0.148 (p=0.004); 2º `score_dominio_hibrido` +0.109 (N=43, frágil); 3º `score_priorizacao` -0.004 (p=0.948, nulo); 4º `score_oportunidade_residual` -0.257 (p<0.001, negativo). Por rede o sinal é fraco e às vezes inverte (heterogeneidade), priorizar leitura within-rede.
- (b) Renda × pop (357 casadas, 91,3%): ρ(renda)=+0.067 vs ρ(pop)=+0.095 no AGG → pop carrega ligeiramente mais sinal monotônico. DESCRITIVO; nenhuma proposta de peso (BLK-SCORE-03).
- (c) Outliers: alto-score×baixo-outcome (ex.: setor_2022=100 em Goiânia/ultra outcome_pct=0.02; Florianópolis/engcorpo estimado precisão cidade) e baixo-score×alto-outcome (Skyfit Redenção/PA e Parnaíba/PI score_pct~0.04 × outcome_pct~0.97). Hipóteses: maturação não observada / saturação de concorrente / rótulo estimado / precisão de hex por centroide.

## Problemas encontrados
- O termo "nome_unidade" aparece no relatório apenas como menção de guardrail no cabeçalho ("sem nome_unidade/PII"), não como vazamento. Ajustei o teste de PII do build_report para verificar que nenhum NOME real de unidade vaza (e o teste de find_outliers garante que `nome_unidade` nunca é chave exposta). Nenhum outro problema.

## Pendências
nenhuma

## Riscos remanescentes
- N pequeno em EngCorpo e cobertura esparsa do `score_dominio_hibrido` (engcorpo N=4, ultra N=0) → conclusões por rede frágeis; declarado nas Limitações. Maturação indisponível (sem proxy inventado) limita a interpretação causal — declarado.

## Guardrails verificados
- score_priorizacao não alterado: sim
- Artefatos M1 preservados (nada em data/outputs/, scoring.py, constants.py): sim
- Saída só em data/analysis/ (gitignored): sim (confirmado via git check-ignore)
- Sem dependência nova / sem PII / sem proposta de peso: sim (scipy já presente; matplotlib opcional sob try/except; relatório descritivo só com achados)
