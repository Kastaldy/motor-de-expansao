# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-UI-01 — Refatoração UX/UI (2º recorte: Densidade/Navegação). Itens implementados: F1-A, F1-B, F1-C, F2-A (REORDER+labels+dispatch), F2-B, F2-C, F2-D, F2-F, F2-G, F2-H, F2-I. F1-D = SEM AÇÃO; F2-E = FUTURO (não tocados).

## O que foi feito
- **F1-A (densidade de tabelas):** Carteira/Plano/Domínio passam a exibir um set PRIMÁRIO reduzido (≤12 cols) por padrão; as secundárias vão para um `st.expander(expanded=False)` que renderiza um 2º `st.dataframe` com o frame COMPLETO já calculado. ZERO coluna removida do DataFrame — só do set exibido por padrão. Mesma `view`/formatadores.
  - `render_carteira_expansao`: primário = Rank Brasil, Rank UF, Score M1, Prioridade, UF, Municipio, Hex ID, Modo Hex, Score Censo, SAM Fitness, Oferta Residual, Motivo. Expander "Mostrar colunas detalhadas (residual, ranks, censo)".
  - `render_plano_expansao`: primário = Rank Brasil, Rank UF, Nivel, UF, Municipio, Hex ID, Score Hibrido, Score M1, Score Censo, SAM Fitness, Oferta Residual. Expander "Mostrar colunas detalhadas (residual, consumo, censo, status)".
  - `render_expansao_dominio`: primário = Rank Brasil, Rank UF, UF, Cidade, Hex Ancora, Ordem na Cidade, Score Residual, Residual Capturado, Oferta Disponivel, Tese, Dist. Ultra (m). Expander "Mostrar colunas detalhadas (consumo, cluster, ranks)".
- **F1-B (KPIs da Análise Pontual):** `_render_analise_pontual_multihex` reduzido de 12 para 9 KPIs em 2 linhas (4 + 5); os 3 KPIs de consumo viraram 1 `st.caption` consolidado. `render_analise_pontual` (simples): consumo (3 `st.metric`) virou 1 `st.caption` consolidado, absorvendo a proveniência; os 8 KPIs primários (2×4) preservados.
- **F1-C (disclaimer centroide):** extraída a constante de módulo `_CENTROID_DISCLAIMER` (perto de `DASHBOARD_TAB_LABELS`); as 2 ocorrências literais (multihex e simples) substituídas por `st.caption(_CENTROID_DISCLAIMER)`. Texto padronizado sem "de hexagono".
- **F2-A (REORDER+renomear abas — ATÔMICO):** `DASHBOARD_TAB_LABELS = ["Mapa","Executivo","Expansão de Domínio","Carteira e Plano","Viabilidade"]`. "Mapa" é a 1ª aba e o default ao carregar (o `render_tab_selector` usa `opts[0]` como default/fallback). Dispatch em `main()` reescrito em LOCKSTEP, indexando `DASHBOARD_TAB_LABELS[0..4]` (evita divergência de acento). Mapeamento aba→render preservado (Executivo→`render_visao_executiva`, Mapa→`render_mapa_territorial`, Expansão de Domínio→`render_expansao_dominio`, Carteira e Plano→`render_carteira_e_plano`, Viabilidade→`render_viabilidade_ponto`). Gate de summaries (`build_city_summary`/`build_uf_summary`) agora condicionado a `(tab_executivo, tab_mapa)`.
- **F2-B (estado vazio sem shell):** as 3 mensagens de `st.warning` das tabelas vazias (Carteira/Domínio/Plano) trocadas por mensagem de produto em 1 frase, sem `python -m jobs`/`jobs/pipelines`.
- **F2-C (filtros avançados colapsados):** os 5 controles avançados movidos para `st.sidebar.expander("Filtros avancados", expanded=False)`. Municipio/Faixa permanecem na primeira dobra. `render_uf_selectbox` segue como 1º elemento da sidebar (é chamado em `main()` antes de `render_sidebar_filters`). Assinatura/return de `render_sidebar_filters` IDÊNTICOS (mesma ordem dos 8 valores).
- **F2-D (heading no Mapa Territorial):** inserido `st.markdown("#### Detalhamento territorial")` logo após o `st.markdown("---")`, antes do 1º `st.expander("Analise Territorial")`.
- **F2-F (download no topo):** em `render_relatorio_pontual_censitario`, o bloco `residual` (lookup READ-ONLY) e a chamada `render_downloads_relatorio_censitario(...)` foram movidos para logo após a geração dos `mapas` e ANTES das 4 `st.image`. Apenas reposicionamento de chamadas de UI; `censo_report.py`/`censo_map.py`/`censo_point.py` INTOCADOS (mesma lógica de mapas/result/CSV/PDF). Os `return` antecipados garantem que o botão só aparece quando há ponto/relatório válido.
- **F2-G (sidebar sempre aberta):** `initial_sidebar_state="expanded"` MANTIDO. Reforço offline por CSS puro em `inject_styles()`: `[data-testid="stSidebar"][aria-expanded="true"] { min-width: 20rem; }` — sem JS de auto-clique, sem componente externo/rede.
- **F2-H (seletor no topo):** em `main()`, `render_tab_selector(DASHBOARD_TAB_LABELS)` movido para ACIMA da `st.caption("Recorte atual...")` e do card de busca, mantido DEPOIS do guard `if filtered_df.empty: return` (não computa summaries em frame vazio).
- **F2-I (info da coordenada após o seletor):** a chamada `render_hex_search_result(...)` reposicionada para DEPOIS do seletor de abas (e da caption de recorte), no corpo de `main()`. Funcionalidade idêntica; só muda a posição vertical.
- **Testes:** 5 testes de label/dispatch atualizados + 1 teste de Domínio (`rendered_dfs[0]`→`rendered_dfs[-1]` + patch de `st.expander` como context manager, pois o frame completo agora é o último, renderizado no expander).

## Arquivos alterados
- `src/motor_expansao/dashboard/pages.py` — F1-A (3 tabelas: set primário + expander), F1-B (KPIs multihex/simples + captions de consumo), F1-C (constante `_CENTROID_DISCLAIMER` + 2 substituições), F2-A (`DASHBOARD_TAB_LABELS` nova ordem/labels), F2-B (3 warnings), F2-C (expander de filtros na sidebar), F2-D (heading "Detalhamento territorial"), F2-F (reposição interna de `residual`+downloads em `render_relatorio_pontual_censitario`), F2-G (CSS de sidebar em `inject_styles`).
- `streamlit_app.py` — F2-A (dispatch de `main()` em lockstep, indexado), F2-H (seletor de abas movido para o topo do corpo), F2-I (`render_hex_search_result` movido para depois do seletor). `set_page_config` já estava em `"expanded"` (confirmado, inalterado).
- `tests/integration/test_streamlit_app.py` — F2-A (5 testes: `test_render_tab_selector_e_exportado`, `_retorna_aba_ativa`, `_fallback_quando_desmarcado`, `test_main_renderiza_apenas_a_aba_ativa`, `test_render_viabilidade_ponto_aba_no_dashboard_tab_labels`) + F1-A Domínio (`test_render_expansao_dominio_exibe_consumo_quando_colunas_presentes` → `rendered_dfs[-1]` + patch de `st.expander`).

## Validações executadas
- **pytest (subconjunto impactado):** `python -m pytest -n auto -q tests/integration/test_streamlit_app.py` falhou com INTERNALERROR de gateway execnet (`OSError [WinError 6]` — bug conhecido do `pytest-xdist`/execnet no Python 3.14 local, NÃO defeito do código). Conforme instrução do handoff, rodei SERIAL (sem mascarar com `-p no:xdist`):
  - `python -m pytest -q tests/integration/test_streamlit_app.py` → **190 passed, 0 failed, 0 skipped** (9501 warnings — DeprecationWarnings pré-existentes de `pytest_asyncio`/`asyncio` no Python 3.14, alheios ao ciclo).
- **ruff:** `python -m ruff check src/motor_expansao/dashboard/pages.py streamlit_app.py tests/integration/test_streamlit_app.py` → **All checks passed!** (0 erros).
- **mypy:** `python -m mypy src/motor_expansao/dashboard/pages.py streamlit_app.py` → **Success: no issues found in 2 source files** (0 erros novos).
- **import smoke:** `python -c "import streamlit_app; print('import ok')"` → **import ok**.
- **F1-C aceite:** `grep "centroide H3 res-7" pages.py` → 1 única linha (a definição de `_CENTROID_DISCLAIMER`).

## Problemas encontrados
- `-n auto` (pytest-xdist) inutilizável neste ambiente Python 3.14 (INTERNALERROR de execnet ao subir o gateway — `OSError [WinError 6] Identificador inválido`). Contornado com execução SERIAL, conforme instrução explícita do handoff. NÃO usei `-p no:xdist`. Não é regressão deste ciclo.

## Pendências
- A suíte FULL (`pytest -n auto -q`) é gate do QA (não rodada aqui). Dívida operacional herdada (3 failures pré-existentes documentados no `current_task.md`: `test_csvs_concorrentes_legiveis[csv_path1-223]`, `[csv_path2-472]`, `test_parquet_final_respeita_guardrails_do_piloto`) NÃO foi tocada por este ciclo.

## Riscos remanescentes
- F2-G: `initial_sidebar_state="expanded"` + CSS `min-width` é o reforço mais robusto offline; o Streamlit não reabre a sidebar se o usuário a colapsar manualmente na mesma sessão (limitação do runtime, não evitável sem JS/componente externo, fora de escopo). Documentado.
- F2-F: o caminho feliz do relatório agora renderiza o download ANTES das 4 imagens; os testes do relatório cobrem só os `return` antecipados (sem ponto/sem base) e seguem verdes. Nenhum teste assevera ordem de `st.image` vs `download_button`.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; só visualização/navegação; nenhuma fórmula/peso/score tocado).
- Artefatos M1 preservados: sim (nenhuma escrita em parquet/artefato; ZERO coluna removida do DataFrame; `config.py`/`pipelines/m1` não tocados).
- Dashboard offline mantido: sim (F2-G é CSS puro, sem JS/rede; nenhuma dependência de API ao vivo; `censo_*` lógica intocada; Blocos 4/5/6 de performance e `components.py` intocados; DEC-005 emenda respeitada — `censo_*` não alterado neste ciclo).
