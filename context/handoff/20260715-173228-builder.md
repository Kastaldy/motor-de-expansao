# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-RELPON-08 — Big Numbers (página 5) do Relatório Pontual: trocar métrica, reordenar e
semáforo verde/vermelho por meta.

## O que foi feito
Implementado EXATAMENTE o plano técnico numerado do Planner, READ-ONLY sobre o M1:

1. **`censo_point.py` — novo campo `domicilios_total_raio`** em
   `analisar_ponto_censitario_setores`:
   - Adicionado `"domicilios_total_raio": None` ao dict default do `result` (logo após
     `"pop_total_raio": None`), garantindo o campo em todos os early-returns.
   - Cálculo do peso de área seguindo EXATAMENTE o padrão de `pop_total_raio`:
     `dom_setor = _numeric(intersectados, "domicilios_particulares_ocupados_setor_2022").clip(lower=0)`
     e `dom_weights = dom_setor * intersectados["peso_area_setor"]`, calculado sobre
     `intersectados` (full columns, antes do corte para `display_cols`). NÃO persistido como
     coluna em `intersectados`/`setores_intersectados`/CSV (schema `_empty_setores_frame()`
     intocado).
   - Agregação com guard "n/d" gracioso:
     `if dom_weights.notna().any(): result["domicilios_total_raio"] = round(float(dom_weights.fillna(0).sum()), 2)`.
     Sem a coluna de domicílios em nenhum setor → `dom_weights` todo NaN → campo permanece
     `None` (default), sem exceção.

2. **`censo_report.py` — constantes de meta/cor + helpers puros + reforma de `_big_numbers_page`**:
   - 6 metas nomeadas de módulo (`_META_POP_TOTAL_RAIO=10000`,
     `_META_RENDA_PER_CAPITA_MEDIA_RAIO=1500`, `_META_DOMICILIOS_TOTAL_RAIO=3000`,
     `_META_SCORE_SETOR_MEDIO=60`, `_META_SAM_FITNESS_POTENCIAL=2000`,
     `_META_RESIDUAL_FITNESS_DISPONIVEL=2000`) e paleta pastel do semáforo
     (`_CARD_VERDE_RGB=(205,236,217)`, `_CARD_VERMELHO_RGB=(248,209,209)`,
     `_CARD_NEUTRO_RGB=(232,233,237)`).
   - Helper PURO `_cor_por_meta(valor, meta)`: `None`/`NaN` → neutro ANTES da comparação;
     `>= meta` → verde; `< meta` → vermelho.
   - Helper PURO `_cor_consumo_concorrentes(sam, residual_disponivel)`: regra assimétrica —
     vermelho SÓ quando `SAM >= 2000 E Residual < 2000`; verde caso contrário; `None`/`NaN`
     em qualquer um → neutro. Usado também para o card "Concorrentes no raio" (espelho).
   - `_big_numbers_page`: linha 1 reordenada para **População total no raio → Renda per capita
     média → Número de domicílios → Score censitário médio**; card "Score censitário máximo"
     REMOVIDO do PDF (`score_setor_max` continua em `result`/CSV para auditoria). Loop de
     desenho desempacota `(label, value, cor_fundo)` e pinta o fundo do card com `cor_fundo`
     (antes `_BRANCO`). Nota de fonte atualizada mencionando o semáforo, ASCII-safe (sem
     caracteres fora de latin-1).

3. **`docs/relatorio_pontual_censitario.md`**: documentado o novo campo `domicilios_total_raio`
   (§3), a nova ordem/semáforo dos cards Big Numbers e a saída de `score_setor_max` do PDF.

## Arquivos alterados
- `src/motor_expansao/dashboard/censo_point.py` — função `analisar_ponto_censitario_setores`:
  novo campo `domicilios_total_raio` (dict default + cálculo de peso de área + agregação com
  guard "n/d").
- `src/motor_expansao/dashboard/censo_report.py` — 6 constantes `_META_*` + 3 cores
  `_CARD_*_RGB`; helpers puros `_cor_por_meta`/`_cor_consumo_concorrentes`; `_big_numbers_page`
  (cards reordenados, troca de card, fundo por meta, nota de fonte).
- `tests/unit/test_relatorio_pontual_censitario_motor.py` — `_sector_record` ganhou kwarg
  `domicilios`; 3 testes novos (`..._domicilios_total_raio_setor_totalmente_dentro_do_raio`,
  `..._setor_parcial_pondera_por_area`, `..._nd_quando_coluna_ausente`); asserção
  `domicilios_total_raio is None` no teste de setor fora do raio.
- `tests/unit/test_relatorio_pontual_censitario_export.py` — imports dos helpers/constantes;
  `test_pdf_big_numbers_com_residual_e_nd` atualizado (rótulo "Número de domicílios" presente,
  "Score censitário máximo" ausente, "4.200"); novos `test_pdf_big_numbers_ordem_linha_1`,
  `test_cor_por_meta_verde_vermelho_neutro`, `test_cor_consumo_concorrentes_regra_assimetrica`.
- `docs/relatorio_pontual_censitario.md` — §3 (campo novo) + §7 (ordem/semáforo dos cards).

## Validações executadas
- pytest (subconjunto impactado, `-n auto`):
  `tests/unit/test_relatorio_pontual_censitario_motor.py`,
  `tests/unit/test_relatorio_pontual_censitario_export.py`,
  `tests/integration/test_streamlit_app.py`
  → **300 passed, 0 failed, 0 skipped** (128.43s). Rodei estes porque cobrem o motor
  (`domicilios_total_raio`), o export/PDF (ordem, rótulos, cores) e o smoke de integração do app.
- import: **ok** (`python -c "import streamlit_app; print('import ok')"`).
- ruff check (4 arquivos tocados): **All checks passed** (após `--fix` de ordenação de imports
  no arquivo de teste de export; nenhum impacto de lógica).
- mypy (`censo_point.py`, `censo_report.py`): **Success: no issues found in 2 source files**.

## Problemas encontrados
Nenhum.

## Pendências
Nenhuma de código. Revisão VISUAL humana do PDF gerado é gate obrigatório no FIM da esteira
(após QA), já registrado em `tasks/current_task.md` — inclui ajuste fino dos tons pastel se a
revisão visual julgar necessário (contraste dos pasteis é hipótese de partida do Planner).

## Riscos remanescentes
- **Contraste dos pasteis** (`_CARD_VERDE_RGB`/`_CARD_VERMELHO_RGB`/`_CARD_NEUTRO_RGB`): tons de
  partida do Planner; luminância alta com texto cinza-escuro (45,45,45)/(40,40,40) deve manter
  contraste; ajuste fino fica para a revisão visual humana.
- **Metas de display** (`_META_*`) são thresholds locais ao PDF; não alteram `flag_sam` nem os
  valores de `sam_fitness_potencial`/`oferta_efetiva_disponivel` (só LIDOS).

## Guardrails verificados
- score_priorizacao não alterado: **sim** (nenhuma escrita em score/pesos/`hex_score_estrutural`;
  `config.py`/`pipelines/m1/` intocados).
- Artefatos M1 preservados: **sim** (nenhum artefato oficial tocado; só leitura/agregação
  censitária e render do PDF).
- Dashboard offline mantido: **sim** (nenhuma dependência de rede nova; `fpdf2`/pandas já em uso;
  método de interseção, raio 1,5 km e marca d'água INTOCADOS).
- Contagem de páginas do PDF: **6** (`/Count 6`), `PDF_SECTION_HEADERS` inalterado — confirmado
  pelos testes de export existentes que seguem verdes.
- Acentuação: texto novo do PDF com acentuação portuguesa correta ("Número de domicílios") e
  nenhum caractere fora de latin-1 introduzido (nota de fonte usa `-`/`...`/aspas retas).
