# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (housekeeping `scripts/housekeeping_move_block.py BLK-RELPON-05` + merge do orquestrador)

## VEREDITO
APROVADO

## Justificativa
O Builder implementou EXATAMENTE o plano aprovado e as decisões de produto D1/D2/D3 confirmadas
pelo humano; ruff/mypy/import limpos, os 284 testes em escopo passam (incluindo 7 testes novos
substantivos) e a única falha da suíte serial (`test_run_readonly_m1_por_mtime`, camada M2/lifetime,
fora do escopo) reproduz IDÊNTICA no baseline limpo com o diff stashed — é gap ambiental
pré-existente (`data/staging/unidade_territorio_retencao.parquet` ausente localmente), NÃO regressão
deste bloco. READ-ONLY sobre o M1 preservado; só os 7 arquivos esperados mudaram.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- O tie-break por `peso_area_setor` (D3) não tem um teste dedicado de "2 setores empatados cobrindo
  o ponto" (cenário raro de ruído de reprojeção); o Builder reconheceu isso no handoff. É coberto
  implicitamente pelo `.sort_values(...).iloc[0]`, mas um teste explícito documentaria o comportamento.
  Não bloqueia (o plano não exigiu esse teste explícito).
- `test_run_readonly_m1_por_mtime` (fora deste bloco) deveria também `skip` quando o input próprio da
  camada de retenção (`unidade_territorio_retencao.parquet`) não existe, não só quando os artefatos
  M1 faltam — hoje ele estoura `FileNotFoundError` em ambiente sem esse staging. Item de tooling
  separado (não é deste ciclo).

## Testes faltantes
- nenhum crítico. (Ver melhoria opcional sobre o tie-break de fronteira.)

## Riscos remanescentes
- `pytest -n auto` (xdist) segue quebrado neste ambiente Windows/Python 3.14 (falha de startup do
  worker execnet), não introduzido por este bloco. A suíte serial é o gate efetivo aqui.
- Revisão visual do PDF/PNG (posicionamento da faixa) foi feita pelo Builder com dados sintéticos e
  é exigência do próprio bloco no backlog; o gate humano de produto (D1/D2/D3) já foi cumprido. Não
  há como o QA re-executar a inspeção visual sem display, mas os testes de repasse/pixels cobrem a
  presença e o conteúdo da faixa.

## Saída literal das validações (re-executadas pelo QA)

### 1a. `python -m pytest -n auto -q` (xdist — infra do worker, NÃO do código)
```
INTERNALERROR> ... xdist/workermanage.py ... node.setup() ... self.gateway._rinfo()
INTERNALERROR>   File ".../execnet/gateway_base.py", line 988, in new
INTERNALERROR>     raise OSError(f"connection already closed: {self.gateway}")
INTERNALERROR> OSError: connection already closed: <Gateway id='gw0' not-receiving, main_thread_only model, 0 active channels>
```
Falha de startup do worker do xdist/execnet (Windows, Python 3.14) — confirmado o MESMO sintoma
reportado pelo Builder. NÃO é flakiness do código; é infra do worker. Gate rodado SERIAL abaixo.

### 1b. `python -m pytest -q` (suíte COMPLETA, serial — gate único)
```
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 1565 passed, 2 skipped, 247732 warnings in 1380.41s (0:23:00)
```
A ÚNICA falha é `test_run_readonly_m1_por_mtime` (camada M2/lifetime, DEC-014 — FORA dos 7 arquivos
deste bloco). Reproduzida em isolamento:
```
FileNotFoundError: [Errno 2] No such file or directory:
'...\data\staging\unidade_territorio_retencao.parquet'
1 failed, 651 warnings in 2.96s
```
Prova de que é PRÉ-EXISTENTE e independente do diff (stash de TODOS os 7 arquivos alterados +
re-run no baseline limpo):
```
Saved working directory and index state WIP on ciclo/BLK-RELPON-05...
===RUN ON BASELINE===
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 651 warnings in 10.67s
===RESTORE===
Dropped refs/stash@{0}
```
Causa: o teste só faz `skip` quando os artefatos M1 faltam; não guarda o input PRÓPRIO da camada de
retenção (`unidade_territorio_retencao.parquet`, gitignored e ausente neste ambiente), então `run()`
estoura `FileNotFoundError`. NÃO é regressão deste bloco (que não toca `lifetime/`) e NÃO é falha
mascarada como infra — é ambiente sem um staging gitignored.

### 1c. `python -m pytest -q` dos 3 arquivos do bloco + `test_streamlit_app.py`
```
284 passed, 15967 warnings in 153.17s (0:02:33)
```

### 2. `python -c "import streamlit_app; print('import ok')"`
```
(WARNINGs esperados de streamlit "missing ScriptRunContext"/"No runtime found")
import ok
```

### 3. `python -m ruff check censo_point.py censo_map.py censo_report.py`
```
All checks passed!
```

### 4. `python -m mypy censo_point.py censo_map.py censo_report.py`
```
Success: no issues found in 3 source files
```

### Housekeeping (`--check`, esperado PENDENTE no QA — move é passo de fechamento)
```
FALHA --check: stub ausente no backlog para BLK-RELPON-05
```
O bloco EXISTE em `tasks/backlog.md` (heading `### BLK-RELPON-05`, ~linha 1604). O move via
`scripts/housekeeping_move_block.py BLK-RELPON-05` é passo de FECHAMENTO do orquestrador (não do QA).
PENDENTE — a ser rodado no fechamento.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do
caminho crítico. A suíte completa rodou contra os parquets reais de `data/staging`; a única falha foi
investigada até a causa-raiz (não mascarada) e provada pré-existente por stash+re-run no baseline.
Os testes do bloco usam fixtures sintéticas legítimas (`_sector_record`/`box(...)`), prática normal e
não em escopo da regra anti-bypass (que mira tooling contra config de produção).

## Conferência cruzada com log do Builder
Bate. O Builder reportou 284 passed nos mesmos arquivos (reproduzido: 284 passed), ruff/mypy/import
limpos (reproduzidos), e o mesmo erro de xdist (reproduzido). O Builder não rodou a suíte completa
(correto — é gate do QA); a única falha da suíte completa é ambiental e fora do escopo, como o próprio
Builder antecipou ("nenhum risco novo introduzido sobre o M1").

## Guardrails verificados
- score_priorizacao não alterado: sim (não aplicável — bloco não toca M1; `git diff --name-only`
  mostra só os 7 arquivos censo_*/tests/docs, nada em `pipelines/m1`/`config.py`/`*scoring*`).
- Artefatos M1 preservados: sim (nenhum artefato oficial no diff; a falha de mtime é ambiental e
  reproduz no baseline sem o diff).
- `setor_censitario_intersecao_area_1p5km`, `RAIO_CENSITARIO_DEFAULT_KM`, raio 1,5 km: intocados
  (diff de `censo_point.py` só ADICIONA `contains_ponto`/lookup; método e raio inalterados).
- Estrutura/contagem/ordem das 5 páginas do PDF, grid de Big Numbers, marca d'água,
  `set_compression(False)`: intocados (`censo_report.py` só ganhou comentário/docstring; teste de
  regressão `test_pdf_estrutura_inalterada_com_faixa_valor_ponto_blk_relpon_05` valida `/Count 5`,
  `>= 4` imagens, todos os `PDF_SECTION_HEADERS` e o método de interseção no PDF).
- DEC-005 emenda 2026-06-12: `valor_ponto: str | None = None` é opcional/default None em
  `_render_camada`; a camada Concorrentes NÃO recebe o kwarg — provado byte-a-byte por
  `test_valor_ponto_muda_pixels_do_png` (`mapas_a["concorrentes"] == mapas_b["concorrentes"]`) e pela
  ausência do kwarg no call site de `concorrentes_png`.
- D1/D2/D3 conforme confirmado pelo humano: D1 = só os 3 choropleths recebem a faixa, Concorrentes
  sem faixa (verificado no código e nos testes de repasse/pixels); D2 = "X no ponto: <valor>" com
  renda `R$ 2.567` (sem centavos), densidade inteira `hab/km2`, score inteiro, `n/d` ausente
  (verificado nos helpers `_format_valor_ponto_*`/`_legenda_valor_ponto`); D3 = `covers(Point(0,0))`
  no CRS métrico local, tie-break por maior `peso_area_setor`, `None`/`False` fora da malha (verificado
  no lookup e nos 3 testes do motor). Textos ASCII-safe; identificadores (`cod_setor_ponto` etc.) sem
  acento.
- Cobertura de teste real: `test_lookup_setor_do_ponto_dentro_da_malha` prova que o valor do ponto
  DIFERE de `renda_per_capita_media_raio`/`score_setor_medio` (não é reciclagem do agregado do raio);
  `test_lookup_setor_do_ponto_fora_da_malha` e `_geometria_invalida` provam `None`/`False`;
  `test_valor_ponto_repassado_aos_3_choropleths_nao_a_concorrentes` prova repasse seletivo via spy de
  `_render_camada`; `test_valor_ponto_nd_quando_ponto_fora_da_malha` prova o `n/d`. Asserts não são
  triviais.
- Validações re-executadas pelo QA (sem bypass): ruff → passed; mypy → success; import → ok; suíte
  completa serial → 1 falha ambiental pré-existente (fora de escopo, provada por stash) + 1565 passed;
  284 passed nos arquivos do bloco.
- Escopo respeitado: sim — `git diff --name-only` = exatamente os 7 arquivos do plano.

## Decisão recomendada
Fechar ciclo (mergear). Passo de fechamento pendente do orquestrador:
`python scripts/housekeeping_move_block.py BLK-RELPON-05` (move do backlog para completed).
