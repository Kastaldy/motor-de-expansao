# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
**[REVISÃO HUMANA OBRIGATÓRIA — LGPD/anonimização + DEC-012]** → Builder (opus) → QA (opus 4.8)

---

## ⚠️ ALERTA DE APROVAÇÃO (MAIÚSCULAS, OBRIGATÓRIO)

**ATENÇÃO: ESTA TAREFA EXIGE REVISÃO HUMANA OBRIGATÓRIA ANTES DO BUILDER (LGPD/ANONIMIZAÇÃO + DEC-012). O BUILDER NÃO PODE EXECUTAR NENHUMA ESCRITA EM `data/staging/` ENQUANTO O HUMANO (FELIPE/VINI) NÃO (1) APROVAR A DEC-012 RASCUNHADA ABAIXO, (2) TRAVAR AS RESPOSTAS DAS "PERGUNTAS PARA O GATE HUMANO" (ESPECIALMENTE O FORMATO/CAMINHO DA FONTE — R2 — E O CONTRATO DE COLUNAS), E (3) CONFIRMAR A PROCEDÊNCIA/LICENÇA DA FONTE EXTERNA. SEM ESSAS TRÊS CONFIRMAÇÕES O CICLO É ABORTADO (R1).**

---

## Entendimento da tarefa
Criar uma camada PARALELA, READ-ONLY sobre o M1, agregada em H3 res-7 e SEM PII (`data/staging/demanda_revelada_h3.parquet`), casável por `hex_id` com `hexagonos_mercado_mapeado.parquet`, ingerindo um dump externo de demanda paga **já agregado por célula** (anti-PII por construção), com testes em fixture sintética e relatório de qualidade — destravando os sucessores BLK-TP-02..05.

---

## Descobertas da investigação (resolvem R2/R5/R6 — base do plano)

A investigação READ-ONLY localizou os artefatos exploratórios reais (em `NAO_ABRA/`, gitignored) que originaram a Epic. Eles **resolvem o R2 (formato de entrada)** e validam o contrato:

1. **Fonte de entrada (R2) = dump HTML** `NAO_ABRA/totalpass_final (72) (1).html`. Contém variáveis JavaScript `var NOME = [...]` extraíveis por parser de chaveamento balanceado (já existe protótipo em `NAO_ABRA/_agg_e_cruzamento.py`, função `grab(name)`):
   - `CELLS` = `[lat, lng, membros, faixa_idx(0–5), dist_sf_m, sf_nome]` — **células JÁ AGREGADAS** (lat/lng arredondados a ~2 casas ≈ 1 km), **NÃO linhas individuais**. `membros` = demanda paga agregada na célula. `faixa_idx==5` = banda `>5km` da Smart Fit (mín. confirmado 5007 m).
   - `GYMS_PTS` = academias parceiras `{n, lat, lng, p(tier tp0–tp7 ou 'sf'), q(alunos)}`.
   - `SF` = unidades Smart Fit `{n, a(lat), o(lng)}`.
   - `BANDS` = rótulos das faixas de distância.
   - **PII NA ORIGEM (employee_id/company_id/lat-lng residencial individual) NÃO está em nenhuma dessas 4 variáveis** — elas já são pré-agregadas. A fronteira anti-PII = o parser só desserializa `CELLS/GYMS_PTS/SF/BANDS`; qualquer outra estrutura do HTML é ignorada.
2. **h3 = v4.4.2** (confirmado). API a usar: `h3.latlng_to_cell(lat,lng,7)`, `h3.get_resolution`, `h3.is_valid_cell`, `h3.cell_to_latlng`, `h3.great_circle_distance`. NÃO usar API v3 (`geo_to_h3`/`h3_to_geo`). `H3_RESOLUTION=7` confirmado em `config.py` / CLAUDE.md §3.
3. **Chave de join (R5 OK)** = `hex_id` (str) em `hexagonos_mercado_mapeado.parquet` (135 colunas; `hex_id`, `score_oportunidade_residual`, `oferta_efetiva_disponivel`, `sam_fitness_potencial`, `populacao_corte_hex` confirmadas presentes). Protótipo casou **16.411 / 16.575 hexes** (≈1,1% do universo do Motor — caveat de cobertura SP, R3).
4. **R6 RESOLVÍVEL JÁ NESTE BLOCO.** `data/staging/concorrentes_mapeados.parquet` existe com colunas `rede`, `lat`, `lng`, `hex_id_res7`. Há **1.000 unidades `rede=='smart_fit'`**. Logo `dist_concorrente_lc_min_m` e `n_concorrente_lc` podem vir de **duas fontes**: (a) do próprio dump (`SF` array + `dist_sf_m` por célula), e/ou (b) cross-check independente contra `concorrentes_mapeados` (`rede=='smart_fit'`, agrupado por `hex_id_res7`). Recomendação do Planner abaixo.
5. **Reconciliação de nomes (decisão de gate).** O backlog (linhas 959–969) usa nomes **genéricos** (`membros`, `membros_gt5km_concorrente_lc`, `dist_concorrente_lc_min_m`, `n_concorrente_lc`); o protótipo `_agg_e_cruzamento.py` usa nomes **SF-específicos** (`tp_membros`, `tp_membros_gt5km_sf`, `dist_sf_min_m`, `n_sf`). **Adoto os nomes genéricos do backlog** como contrato canônico (Smart Fit é o concorrente low-cost de referência, CLAUDE.md §1; nome genérico evita acoplar o schema a uma marca). Mapeamento explícito no passo 4 do plano.
6. **Sem colisão de módulo:** `src/motor_expansao/demanda_revelada/` NÃO existe. Pacotes atuais: `api`, `core`, `dashboard`, `data`, `dimensionamento`, `pipelines`, `config.py`.
7. **`.gitignore` confirma:** `data/staging/*` e `*.parquet` ignorados; `!tests/fixtures/**` é exceção (fixtures sintéticas versionáveis); `NAO_ABRA/` ignorado (fonte real nunca versionada).

---

## Plano técnico (numerado, concreto)

> **PRÉ-CONDIÇÃO DURA:** os passos 3–10 (qualquer escrita em `data/staging/`) só rodam APÓS aprovação humana da DEC-012 e travamento do gate. Passos 1–2 (estrutura de módulo/contrato puro) são neutros, mas o Builder só inicia após o gate.

1. **Criar pacote `src/motor_expansao/demanda_revelada/`** (disjunto; NUNCA importa de `pipelines/m1/`, `censo_*`, `dashboard/`, `config.py` do M1 exceto leitura de constante neutra se necessário):
   - `__init__.py` — exporta a API pública (`ingerir_demanda_revelada`, `CONTRATO_COLUNAS`, `VERSAO_CONTRATO`).
   - `contrato.py` — fonte única do contrato (constantes puras, sem I/O).
   - `ingestao.py` — parser + agregação H3 + drop-PII + escrita do parquet.
   - `qualidade.py` (opcional, pode ficar em `ingestao.py`) — gera o relatório de qualidade.

2. **`contrato.py` — contrato de 9 colunas (canônico, nomes genéricos do backlog) + versão:**
   ```python
   VERSAO_CONTRATO = "demanda_revelada_v1"   # carimbo de reprodutibilidade
   H3_RES_CONTRATO = 7
   # ordem e tipos canônicos do parquet de saída:
   CONTRATO_COLUNAS: dict[str, str] = {
       "hex_id": "str",                          # H3 res-7
       "membros": "int64",                       # demanda paga agregada ao hex
       "membros_gt5km_concorrente_lc": "int64",  # subconjunto a >5km da SF
       "dist_concorrente_lc_min_m": "float64",   # menor dist. à SF no hex (m)
       "n_celulas_agregadas": "int64",           # nº de células de origem
       "n_acad_parceiras": "int64",              # academias parceiras no hex
       "alunos_parceiras": "int64",              # soma de alunos das parceiras
       "n_concorrente_lc": "int64",              # unidades SF no hex
       "versao_contrato": "str",
   }
   COLUNAS_PII_PROIBIDAS: frozenset[str] = frozenset({
       "employee_id","company_id","cpf","email","telefone","nome","lat","lng",
       "lat_residencial","lng_residencial","sf_nome","id","membro_id",
   })  # teste anti-PII falha se QUALQUER uma aparecer no parquet de saída
   ```
   - **DECISÃO DE GATE (ver perguntas):** manter 9 colunas. Versão res-8 = **opcional, fora deste bloco** (recomendo adiar; ver pergunta 2).

3. **`ingestao.py::_parse_fonte_html(path) -> tuple[list, list, list, list]`** — parser puro:
   - Reusar a lógica de `grab(name)` do protótipo (`NAO_ABRA/_agg_e_cruzamento.py`, linhas 27–43): regex `var NOME =` + varredura de chaves/colchetes com escape de strings; `json.loads` do bloco balanceado.
   - Extrai SOMENTE `CELLS`, `GYMS_PTS`, `SF`, `BANDS`. **Drop-PII na fronteira:** nenhuma outra `var` do HTML é lida; cada `CELLS[i]` é uma célula agregada (não-individual) por construção da fonte.
   - Encoding de leitura do HTML: `utf-8`.
   - **Parametrizar o caminho da fonte** (argumento `fonte: Path`), sem hardcode — default configurável (ver pergunta 1).

4. **`ingestao.py::_agregar_h3(cells, gyms, sf) -> pd.DataFrame`** — agregação res-7 SEM PII (espelha `_agg_e_cruzamento.py` linhas 67–96, com nomes do contrato):
   - `cells.hex_id = h3.latlng_to_cell(lat, lng, 7)` por célula; descartar linhas onde `hex_id is None`.
   - `gt5km = faixa_idx >= 5`.
   - `groupby("hex_id")` →
     - `membros` = `soma(membros)`,
     - `membros_gt5km_concorrente_lc` = `soma(membros onde gt5km)`,
     - `dist_concorrente_lc_min_m` = `min(dist_sf_m)`,
     - `n_celulas_agregadas` = `size()`.
   - `GYMS_PTS` (excluir `p=='sf'`) agregadas por `hex7` → `n_acad_parceiras = size()`, `alunos_parceiras = soma(q)`.
   - `SF` agregadas por `hex7` → `n_concorrente_lc = size()`.
   - `merge` left de academias e SF sobre o frame de demanda; `fillna(0)` nas 3 colunas derivadas; coerção aos dtypes do contrato (`int64` sem NaN, `float64` para distância).
   - `versao_contrato = VERSAO_CONTRATO` em todas as linhas.
   - **Ordenar por `hex_id` antes de escrever** (determinismo → reprodutibilidade, critério 6).

5. **R6 — `n_concorrente_lc`/`dist_concorrente_lc_min_m`:** fonte primária = dump (`SF`/`dist_sf_m`). **Cross-check opcional** (recomendado, barato) contra `data/staging/concorrentes_mapeados.parquet` (`rede=='smart_fit'` agrupado por `hex_id_res7`) — **somente para o RELATÓRIO de qualidade** (divergência dump×mapeado), NÃO para sobrescrever o valor do contrato (mantém a camada autocontida no dump). Decisão final no gate (pergunta 3).

6. **`ingestao.py::ingerir_demanda_revelada(fonte, destino=..., gerar_relatorio=True) -> Path`** — orquestra 3→4, valida com `_assert_sem_pii(df)` (colunas ⊆ contrato; nenhuma de `COLUNAS_PII_PROIBIDAS`), valida `h3.get_resolution(hex_id)==7` para todos os `hex_id` (amostra ≤5 inválidos no erro), e escreve `df.to_parquet(destino, index=False)`. Destino default = `data/staging/demanda_revelada_h3.parquet`.

7. **Relatório de qualidade** `data/reports/demanda_revelada_qualidade.md` (Markdown; **gitign:** ver pergunta 5 — recomendo gitignored em `data/reports/scratch/` ou commitável só com agregados sem PII). Conteúdo: nº de hexes cobertos, nº/% de join com `hexagonos_mercado_mapeado`, concentração geográfica (top UFs/cidades via join read-only), e os **caveats obrigatórios** (R3 cobertura SP ~66% / ~1% do universo; R4 arredondamento ~1 km → ruído no join res-7; "não substitui M1/censitário", §1). NÃO recalcula score.

8. **`pyproject.toml` — extra novo `[demanda]`:** o pipeline usa só `pandas`, `numpy`, `pyarrow`, `h3` — **todos já na base**. Portanto o extra `[demanda]` **provavelmente fica VAZIO ou desnecessário**. Recomendo: **NÃO adicionar dependência nova** (parser é `re`+`json` stdlib). Se o gate exigir um marcador de extra para futuros sucessores, criar `demanda = []` (placeholder documentado). Decisão no gate (pergunta 4).

9. **Testes** `tests/unit/test_demanda_revelada_ingestao.py` (fixture sintética SEM PII; nunca o dump real):
   - **Fixture** = um HTML mínimo sintético em string (ou em `tests/fixtures/demanda_revelada_fake.html`, versionável via `!tests/fixtures/**`) com `var CELLS=[...]; var GYMS_PTS=[...]; var SF=[...]; var BANDS=[...]` usando coordenadas reais de hexes SP/RJ (gerar `hex_id` via `h3.latlng_to_cell(...,7)` no teste para casar com o passo de join).
   - `test_parquet_existe_e_legivel` — gera, lê via pandas/pyarrow.
   - `test_contrato_colunas` — colunas == `CONTRATO_COLUNAS.keys()`, dtypes corretos.
   - `test_zero_pii` — **nenhuma** coluna de `COLUNAS_PII_PROIBIDAS` no parquet (critério 3).
   - `test_hex_res7` — `h3.get_resolution(hex_id)==7` para todos (critério 5).
   - `test_join_hex_id` — frame de mercado sintético (ou subset real read-only) casa ≥N hexes por `hex_id` sem erro de tipo (critério 4).
   - `test_reprodutibilidade` — duas execuções sobre a mesma fixture → parquets byte/҂conteúdo idênticos (critério 6).
   - `test_agregacao_correta` — soma de `membros` por hex confere com a fixture controlada.

10. **Atualizar `CLAUDE.md` §8 com DEC-012** — SOMENTE pelo Builder, SOMENTE após aprovação humana (texto rascunhado abaixo). Antes disso, DEC-012 vive só neste handoff.

---

## DELIVERABLE 1 — Rascunho da DEC-012 (NÃO escrever em CLAUDE.md ainda; só para aprovação humana)

> ### DEC-012 — Adoção da camada paralela de Demanda Revelada (H3, sem PII; BLK-TP-01)
> - ID: DEC-012 | Data: 2026-06-24 | Criticidade: **Estratégica/Alta** (nova camada paralela de dados que ingere insumo externo com **PII na origem** + **procedência/licença a confirmar**; **READ-ONLY sobre o M1**). Exige gate humano (LGPD/anonimização).
> - Status: **PENDENTE — aguardando aprovação de Felipe/Vinicius** (gate humano do BLK-TP-01).
> - Contexto: a DEC-009 encerrou a previsão de *magnitude de demanda* pela geografia interna (renda/pop têm sinal nulo no M1 — DEC-001); faltava um sinal de **demanda observada**. Um insumo externo de benefício corporativo de academias (centrado na Smart Fit, concorrente low-cost da Ultra — §1) fornece **demanda paga georreferenciada**. Análise exploratória interna (2026-06-24, `NAO_ABRA/`, gitignored) indicou Spearman **+0,52** vs. `score_oportunidade_residual` e **+0,75** vs. alunos capturados — primeira validação externa positiva forte de uma camada do Motor.
> - Decisão: **adotar a camada de Demanda Revelada** como camada PARALELA, materializada em `data/staging/demanda_revelada_h3.parquet` (H3 res-7), casável por `hex_id` com `hexagonos_mercado_mapeado.parquet`, isolada em `src/motor_expansao/demanda_revelada/`. Contrato canônico de 9 colunas (nomes genéricos: `hex_id`, `membros`, `membros_gt5km_concorrente_lc`, `dist_concorrente_lc_min_m`, `n_celulas_agregadas`, `n_acad_parceiras`, `alunos_parceiras`, `n_concorrente_lc`, `versao_contrato`).
> - **Princípios anti-PII por construção (não-negociáveis):** a ingestão consome **apenas células já agregadas** do insumo; `employee_id`/`company_id`/coordenadas residenciais individuais **nunca** são lidos para o staging nem persistidos em arquivo, log ou cache; a agregação para H3 ocorre na **fronteira de entrada**; o artefato e os testes contêm **zero** PII (teste automatizado lista colunas proibidas e falha se qualquer uma existir); a **fonte real nunca é versionada** (vive em `NAO_ABRA/`, gitignored); os testes usam **fixture sintética**.
> - **Procedência/licença:** a produtização (exposição no dashboard/relatórios — sucessores BLK-TP-02..05) só avança após Felipe/Vini confirmarem a **procedência e a base legal** da fonte. Enquanto não confirmada, a camada permanece de uso interno/exploratório.
> - **Relação com a DEC-001:** **INTACTA** — a camada NÃO recalibra `score_priorizacao`/`hex_score_estrutural`/pesos (`renda=0.40`/`pop=0.60`) nem regenera artefatos oficiais; é READ-ONLY sobre o M1 (§5 guardrail permanente).
> - **Relação com a DEC-009:** **INTACTA** — a demanda entra como **insumo OBSERVADO**, NUNCA como preditor geográfico de magnitude. Proibido usar `membros` ou qualquer coluna desta camada como input em regressão geográfica de demanda ou como ajuste do `score_priorizacao`. (O re-teste honesto demanda→captura é um bloco próprio, BLK-TP-05, sob a metodologia LOO/k-fold vs baseline da DEC-008.)
> - Parâmetros canônicos do §3: **INALTERADOS** (a camada não introduz parâmetro de score; H3_RESOLUTION=7 reutilizado como chave de join).
> - Referências: `tasks/backlog.md` (Epic BLK-TP / BLK-TP-01, linhas ~916–989); `context/handoff.md` (Planner); `docs/modelo_mercado_hexagonos.md` (contrato de join por `hex_id`); CLAUDE.md §1/§2/§4/§5; DEC-001; DEC-009.

---

## DELIVERABLE 2 — PERGUNTAS PARA O GATE HUMANO (humano TRAVA antes do Builder)

**Pergunta 1 (R2 — formato/caminho da fonte). O dump externo `NAO_ABRA/totalpass_final (72) (1).html` (vars JS `CELLS`/`GYMS_PTS`/`SF`/`BANDS`) é a fonte definitiva e única? Confirma que ele já está agregado (sem PII individual)? O parser deve ser parametrizável por caminho?**
- **Recomendação do Planner:** SIM, usar esse dump; parser **parametrizável** (`fonte: Path`, default lendo de uma constante apontando para `NAO_ABRA/...`, fora do versionamento). A fixture sintética é o contrato que blinda o Builder caso o arquivo mude — o teste NÃO depende do dump real. Confirmar formalmente que `CELLS` é célula agregada (a fonte já entrega assim; nenhuma var individual é lida).

**Pergunta 2 (contrato de colunas). Manter as 9 colunas genéricas do backlog (renomeando o protótipo SF-específico)? Gerar também a versão res-8 (`demanda_revelada_h3_res8.parquet`)?**
- **Recomendação:** manter as **9 colunas genéricas** (Smart Fit é o "concorrente low-cost de referência" — nome de marca fora do schema). **Adiar res-8** para um sucessor (BLK-TP-03/04, leitura intraurbana fina); res-7 é a chave de join com o Motor e cobre o aceite. Inclui-la agora amplia superfície sem demanda atual.

**Pergunta 3 (R6 — `dist_concorrente_lc_min_m`/`n_concorrente_lc`). Incluir já neste bloco (dados disponíveis no dump + `concorrentes_mapeados.parquet`) ou adiar?**
- **Recomendação:** **INCLUIR já** — ambos vêm do próprio dump (`SF`/`dist_sf_m`); o `concorrentes_mapeados.parquet` (1.000 SF, `hex_id_res7`) entra **só como cross-check no relatório** (divergência dump×mapeado), não como fonte de verdade do contrato. Mantém a camada autocontida e o aceite completo.

**Pergunta 4 (extra `[demanda]`). Criar o extra no `pyproject.toml`? Com qual conteúdo?**
- **Recomendação:** o pipeline usa só `pandas`/`numpy`/`pyarrow`/`h3` (já na base) + `re`/`json` (stdlib) → **NÃO há dependência nova**. Recomendo **NÃO criar** o extra (ou criar `demanda = []` como placeholder documentado, se o humano quiser o marcador para sucessores). Confirmar o nome `[demanda]` caso seja criado.

**Pergunta 5 (relatório de qualidade — onde versionar). Gitignored (`data/reports/scratch/`) ou commitável em `data/reports/`?**
- **Recomendação:** gerar em `data/reports/demanda_revelada_qualidade.md`. Como `*.csv`/`*.parquet` já são ignorados mas `.md` em `data/reports/` **não** é ignorado por padrão (só `data/reports/scratch/` e `data/analysis/` são), e o relatório conterá só **agregados sem PII**, ele **pode** ser commitável. **Recomendo gitignorá-lo** por cautela de procedência (a fonte ainda não tem licença liberada) — colocá-lo em `data/reports/scratch/` (já gitignored) OU adicionar uma linha de ignore específica. Decisão do humano.

**Pergunta 6 (DEC-012 e procedência). Aprova a DEC-012 acima? Confirma a procedência/base legal da fonte para uso interno/exploratório agora?**
- **Recomendação:** aprovar a DEC-012 para **uso interno/exploratório** (staging + análise), mantendo o gate de **procedência/licença** travado para a etapa de *produtização* (exposição no dashboard, BLK-TP-02..05). Sem aprovação explícita aqui, o ciclo é abortado (R1).

---

## Arquivos afetados (todos NOVOS, exceto pyproject/CLAUDE condicionais)
- `src/motor_expansao/demanda_revelada/__init__.py` (novo)
- `src/motor_expansao/demanda_revelada/contrato.py` (novo)
- `src/motor_expansao/demanda_revelada/ingestao.py` (novo)
- `tests/unit/test_demanda_revelada_ingestao.py` (novo)
- `tests/fixtures/demanda_revelada_fake.html` (novo, fixture sintética; versionável via `!tests/fixtures/**`)
- `data/staging/demanda_revelada_h3.parquet` (saída, gitignored)
- `data/reports/demanda_revelada_qualidade.md` (relatório; local conforme pergunta 5)
- `pyproject.toml` — SOMENTE se gate decidir criar `[demanda]` (pergunta 4)
- `CLAUDE.md` §8 — SOMENTE para DEC-012, APÓS aprovação humana
- `context/handoff.md`, `context/handoff/AAAAMMDD-HHMMSS-<slug>.md`, `tasks/current_task.md`

## Dependências
- **DEC-012 aprovada** (bloqueante absoluto — R1).
- Fonte: `NAO_ABRA/totalpass_final (72) (1).html` (dump JS pré-agregado).
- `data/staging/hexagonos_mercado_mapeado.parquet` (join read-only) e `data/staging/concorrentes_mapeados.parquet` (cross-check R6) — ambos presentes.
- `h3` v4.4.2 (base), `pandas`/`numpy`/`pyarrow` (base).

## Riscos técnicos
- **R1 — DEC-012 não aprovada → ciclo abortado.** Bloqueante.
- **R2 — RESOLVIDO** pela investigação (HTML JS vars já agregadas); parser parametrizável + fixture sintética blindam mudança de fonte.
- **R3 — cobertura SP/~1% do universo:** documentar no relatório; NUNCA tratar como cobertura nacional (§1).
- **R4 — arredondamento ~1 km:** ruído no join res-7; documentar; fixture inclui caso de borda.
- **R5 — RESOLVIDO:** `h3.latlng_to_cell(...,7)` (v4); chave `hex_id`.
- **R6 — RESOLVIDO:** colunas calculáveis do dump + cross-check com `concorrentes_mapeados`.
- **R7 — paths pré-sujos** (`.gitignore`,`CLAUDE.md`,`tasks/backlog.md` M; geojsons D; `scripts/backtest_smartfit_scores.py` ??): o Builder NÃO os inclui no commit do ciclo.
- **Procedência/licença da fonte:** ainda não liberada para produtização — gate humano (pergunta 6).

## Critérios de aceite finais (do handoff do BO + refinos)
1. `data/staging/demanda_revelada_h3.parquet` existe e legível (pandas/pyarrow).
2. Contém as 9 colunas do contrato (`CONTRATO_COLUNAS`), dtypes corretos.
3. **Zero** coluna PII (teste `test_zero_pii` sobre `COLUNAS_PII_PROIBIDAS`).
4. Join `hex_id` com `hexagonos_mercado_mapeado.parquet` demonstrado por teste (≥N hexes casam, sem erro de tipo).
5. `h3.get_resolution(hex_id)==7` para todo `hex_id` da fixture.
6. Reprodutível: mesma entrada → mesmo parquet (ordenação por `hex_id`).
7. READ-ONLY M1: `grep -r "score_priorizacao\|hex_score_estrutural\|to_parquet" src/motor_expansao/demanda_revelada/` não escreve em artefato oficial (só `demanda_revelada_h3.parquet`).
8. `pytest -q` verde (baseline + novos testes).
9. `ruff check .` → 0 erros.
10. `mypy src/motor_expansao/demanda_revelada/` → 0 erros (ou baseline justificado).
11. `import motor_expansao.demanda_revelada` não puxa `[basemap]`/`[api]`/`[api_mvp]`/scraping.
12. DEC-012 aprovada pelo humano e registrada em CLAUDE.md §8 ANTES de qualquer escrita em staging.

## Validações obrigatórias
- Comando: `python -m pytest -q tests/unit/test_demanda_revelada_ingestao.py` → 0 falhas.
- Comando: `python -m pytest -q` → 0 falhas (suíte completa).
- Comando: `ruff check .` → 0 erros; `mypy src/motor_expansao/demanda_revelada/` → 0 erros.
- Comando: `python -c "import motor_expansao.demanda_revelada"` → sem ImportError de extras.

## Fora de escopo
- Score/pesos/`hex_score_estrutural`/carteira/plano/artefatos oficiais do M1.
- Persistir QUALQUER PII (id/nome/cpf/email/telefone/coord individual) em arquivo/log/cache.
- Versionar a fonte real (`NAO_ABRA/`).
- Ingestão ao vivo na carga do dashboard; exposição no dashboard; as análises (BLK-TP-02..05).
- Deploy VPS, Dockerfile, compose, CI, `config.py`; tocar `censo_*`/`pages.py`/`components.py`/`streamlit_app.py`.
- Versão res-8 (adiada — pergunta 2).

## Alerta de aprovação
**ATENÇÃO: TAREFA ALTA — REVISÃO HUMANA OBRIGATÓRIA ANTES DO BUILDER (LGPD/ANONIMIZAÇÃO + DEC-012). O USUÁRIO DEVE LER ESTE HANDOFF, RESPONDER ÀS 6 PERGUNTAS DO GATE, E APROVAR A DEC-012. SEM ISSO O BUILDER NÃO EXECUTA E O CICLO É ABORTADO.**

## Guardrails ativos
- §2 — sem API ao vivo no dashboard (ingestão offline); Parquet em staging; toda mudança com teste.
- §4 — camada PARALELA; não altera M1; `pop_hex_base`/modelo de mercado intocados.
- §5 — guardrail permanente: nada recalcula `score_priorizacao`/`hex_score_estrutural`/carteira/plano/artefatos M1.
- §8/DEC-001 — pesos `renda=0.40`/`pop=0.60` e fórmula INALTERADOS.
- §8/DEC-009 — demanda = insumo OBSERVADO, NUNCA preditor geográfico de magnitude.
- Anti-PII por construção — agregação na fronteira de entrada; fixture sintética nos testes; fonte real nunca versionada.
- DEC-012 obrigatória e aprovada antes de qualquer escrita em staging.
