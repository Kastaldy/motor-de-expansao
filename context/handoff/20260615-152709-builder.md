# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-EST-05 — PDF "Apresentação Clássica Ultra" (template GeoFusion) do Relatório Pontual Censitário

## O que foi feito
Implementada a variante "Apresentação Clássica Ultra" do PDF do Relatório Pontual Censitário como
função pública DEDICADA `gerar_pdf_relatorio_pontual_classico(...)` em `censo_report.py` (estética
GeoFusion antiga sobre o motor censitário novo), sem ramificar nem alterar o gerador recente.
Seguidos os 12 passos do plano do Planner e as decisões do gate humano (Q1/Q2/Q3).

Detalhe por passo:
1. **Branding** — `_ASSET_ICONE = "icone_ultra.png"`; `"icone": None` adicionado ao dict de
   `_load_branding_assets` e ao loop de carga (mesmo try/except + `Image.verify()`). O asset
   `data/ultra/icone_ultra.png` JÁ existe (confirmado). Recente intocado (nunca lê `assets["icone"]`).
2. **Import** — `from motor_expansao.api.maps_geocoder import build_search_url` no topo. Sem ciclo
   (maps_geocoder importa só `re`/`urllib`; selenium só dentro de `MapsGeocoder.__init__`). Smoke OK.
3. **Constantes clássicas** — `_CLASSICO_MARGIN=20.0`, `_CLASSICO_CORNER_RADIUS=16.0`,
   `_CLASSICO_BAND_H=58.0`, `_CLASSICO_MAGENTA_BANDA_H=13.0`, `_CLASSICO_MAGENTA_OFFSET=13.0` +
   `_MESES_PT`. Cores = `ULTRA_TURQUESA=(0,167,157)` / `ULTRA_MAGENTA=(194,60,142)` reusadas (Q1).
   Sem `_CLASSICO_TURQUESA`/`_CLASSICO_MAGENTA`, sem laranja.
4. `_classico_title_band` — banda turquesa margem 20px + `rect(..., style="F", round_corners=True,
   corner_radius=16)` (set_line_width(0)); endereço/nome à esquerda (branco); ícone à direita via
   `pdf.image(BytesIO(assets["icone"]), ...)` em try/except só se `assets.get("icone")`; título da
   seção abaixo da banda.
5. `_classico_cover_page` — endereço ACIMA (baseline y430) do subtítulo (baseline y455, acima da
   linha branca ~y460), via `pdf.text`; zona limpa inferior-direita (x≥478) quando há fundo;
   mês/ano via `_classico_mes_ano` (ASCII-safe).
6. `_classico_map_page` — banda clássica + `_draw_map` (reuso) + `_draw_footer` (reuso); fallback
   "Mapa indisponivel...".
7. `_classico_competitors_page` — banda clássica + mapa à esquerda + lista à direita reusando
   `_point_rows`/`_safe_len`; cabeçalho "(N no total)" e "... e mais N" idênticos ao recente.
8. `_classico_banda_magenta_rodape` — banda magenta full-width flush-baixo (y = `_PAGE_H -13 -13`),
   sem cantos/margem lateral; não cobre a marca d'água.
9. `_classico_credit_page` — corpo igual `_credit_page` (turquesa, "Realizacao" 34pt, crédito,
   método 1 frase, linha READ-ONLY, rodapé CARTO) + bloco "Link para localizacao do ponto:" com
   `pdf.cell(..., link=build_search_url(endereco_ou_coord))` + "Data de geracao: {por extenso}".
   SEM logo, SEM cartão de contato.
10. `gerar_pdf_relatorio_pontual_classico(...)` — monta as 7 páginas na ordem canônica reusando
    `_normalize_mapas_by_key` + `_big_numbers_page` (pág. 6) + banda magenta por cima; mesmo loop de
    marca d'água do gerador recente (capa branca / demais cinza). `now` injetável p/ teste.
11. Params opcionais `template: str | None = None` e `rotulo: str | None = None` em
    `gerar_payloads_download_relatorio_censitario` e `render_downloads_relatorio_censitario`;
    `template == "classico"` roteia ao clássico, default = recente (byte-a-byte).
12. 7 testes novos adicionados (ver Validações).

Calibração Q3 (offsets visuais): banda magenta ~13px com offset ~13px e baseline da capa y430/y455
(acima da linha branca ~y460) entregues como valores razoáveis conforme memória do template; o PDF
de simulação `SIMULACAO_relatorio_caiubi_classico.pdf` NÃO foi alterado nem commitado.

## Arquivos alterados
- `src/motor_expansao/dashboard/censo_report.py` — ÚNICA alteração de produção: import
  `build_search_url` + `datetime`; `_ASSET_ICONE` e chave `"icone"` em `_load_branding_assets`;
  constantes `_CLASSICO_*` + `_MESES_PT`; helpers novos `_classico_data_extenso`,
  `_classico_mes_ano`, `_classico_title_band`, `_classico_banda_texto`, `_classico_cover_page`,
  `_classico_map_page`, `_classico_competitors_page`, `_classico_banda_magenta_rodape`,
  `_classico_credit_page`; função pública `gerar_pdf_relatorio_pontual_classico`; params opcionais
  `template`/`rotulo` nos 2 helpers de download. Tudo o que o template recente usa ficou byte-a-byte
  inalterado (verificado por teste `test_classico_template_recente_inalterado`).
- `tests/unit/test_relatorio_pontual_censitario_export.py` — import da nova função + 7 testes da
  variante (fixtures sintéticas sem PII).

## Validações executadas
- pytest (subconjunto impactado, SERIAL): `205 passed, 0 failed, 0 skipped, 10595 warnings` em
  `tests/unit/test_relatorio_pontual_censitario_export.py tests/integration/test_streamlit_app.py`.
  NOTA: `-n auto` (xdist) crashou com `INTERNALERROR EOFError` na inicialização do worker —
  problema de AMBIENTE (Python 3.14 + execnet/xdist), NÃO defeito do código; rodado serial como
  fallback (mesmo padrão histórico de divergência de ambiente registrado no BLK-ORQ-01). QA deve
  rodar a suíte full (decidir serial vs `-n auto` conforme o ambiente do QA).
- import: ok (`import streamlit_app` + `gerar_pdf_relatorio_pontual_classico`).
- ruff: `All checks passed!` (`src/motor_expansao/dashboard/censo_report.py tests/`).
- mypy: `Success: no issues found in 1 source file` (`censo_report.py`).
- Smoke de geração do clássico: 7 páginas (`/Count 7`), `%PDF-1.4`, link `maps/search` presente,
  8 `/Subtype /Image` (≥4), 255 KB.
- Testes novos: `test_classico_gera_7_paginas_e_secoes`, `test_classico_link_clicavel_na_realizacao`,
  `test_classico_offline_safe_sem_assets`, `test_classico_sem_pii_de_pessoas`,
  `test_classico_marca_dagua_solicitante`, `test_classico_template_recente_inalterado`,
  `test_downloads_roteia_template_classico` — todos verdes.

## Problemas encontrados
- xdist (`-n auto`) instável no ambiente local (Python 3.14): INTERNALERROR na partida do worker.
  Contornado rodando serial; é divergência de ambiente, não regressão. Reportado ao QA.

## Pendências
- QA: rodar a suíte FULL (gate único) sem regressão vs baseline (884 passed, 1 skipped esperados +
  os 7 novos do clássico → ~891 passed; confirmar no ambiente do QA).
- Calibração fina dos offsets visuais (Q3) é "bom senso" conforme o gate; revisão visual final é do
  Felipe/Vini no merge se desejarem ajuste de pixels (não bloqueia o ciclo).

## Riscos remanescentes
- Offsets visuais (banda magenta, baseline da capa) são estimativas calibradas pela memória do
  template; sem inspeção pixel-a-pixel do simulado. Funcionalmente corretos e graciosos; ajuste fino
  é cosmético e revisável no gate visual.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; nenhuma escrita em score/carteira/plano/artefatos).
- Artefatos M1 preservados: sim (nada tocado; DEC-001 intacta).
- Dashboard offline mantido: sim (`build_search_url` é regex puro, sem Selenium no import de módulo;
  geração 100% offline, fallback gracioso de assets testado, incl. `icone_ultra.png` ausente).
- Anti-PII: sim (fixtures com nome fictício e coordenada sintética; `_PII_FORBIDDEN` ausente do PDF
  clássico; sem logo na Realização; sem cartão de contato `image24.png`; PDF/PII real não versionado).
- Template recente byte-a-byte: sim (`gerar_pdf_relatorio_pontual_censitario`, `_cover_page`,
  `_map_page`, `_competitors_page`, `_credit_page`, `_big_numbers_page`, `_draw_title_band`
  inalterados; isolamento verificado por teste).
- Paths pré-sujos/alheios NÃO tocados/commitados: `setores_censitarios_2022_geo/_metadata.json`,
  `relatorio_pontual_censitario_base_geo.md`, `SIMULACAO_relatorio_caiubi_classico.pdf`.
