# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-UI-03 — 4 ajustes finos de UX no dashboard (READ-ONLY M1). FECHA o BLK-FIX-10 (item nº3).

## O que foi feito
Quatro ajustes de UX aprovados no GATE HUMANO (Felipe/usuário, 2026-06-12), todos READ-ONLY sobre o M1 (CSS/markdown/strings/largura de exibição na tela; zero score/pesos/fórmula/artefatos):

- **D1 — Reverter coord** (`render_coord_search_sidebar`, pages.py): removido o bloco `st.sidebar.info(...)` inserido no BLK-UI-02 e restaurado o par exato (texto literal do commit BLK-UI-01 `0862205`):
  ```python
  st.sidebar.markdown("### Busca por coordenada")
  st.sidebar.caption("Localize um hexagono pela coordenada. Offline, sem API externa.")
  ```
  Preservados: `st.sidebar.markdown("---")` acima, `text_input`, `parse_coordinate_input`, `st.sidebar.error`, assinatura `() -> tuple[float, float] | None` e a chamada em `streamlit_app.py:470`.

- **D2 — Tooltip meio-termo** (`_shared_map_tooltip` e `_hybrid_compact_tooltip`, components.py): as 4 chaves de tamanho do `style` em AMBAS as funções passaram de `11px`/`6px 8px`/`260px`/`1.25` para `"fontSize": "13px"`, `"padding": "8px 10px"`, `"maxWidth": "300px"`, `"lineHeight": "1.35"`. Chaves pré-existentes (`backgroundColor`/`color`/`border`/`borderRadius`/`fontFamily`) e o `html` INALTERADOS.

- **D3 — Preview menor [fecha BLK-FIX-10]** (pages.py): adicionada a constante de módulo `_CENSUS_PREVIEW_WIDTH_PX = 720` (comentário `# BLK-FIX-10: preview menor que 100% da largura de conteudo`) logo após `RESIDUAL_SORT_COLUMNS`. Trocado `width="stretch"` por `width=_CENSUS_PREVIEW_WIDTH_PX` nas 4 chamadas `st.image` do preview do Relatório Pontual Censitário (densidade/renda/score/concorrentes), editadas UMA A UMA por contexto único (sem `replace_all`). Captions inalterados; `censo_report.py`/`censo_map.py`/`render_mapas_censitarios_combinados`/`gerar_pdf_relatorio_pontual_censitario`/raio/interseção INTOCADOS — só a largura de exibição na tela muda; PDF e PNGs gerados idênticos.

- **D4 — Destaque do seletor de abas — SÓ CSS** (`inject_styles()`, pages.py): reforçadas SOMENTE as regras CSS já existentes do `stSegmentedControl`. Regra base dos botões ganhou `border-radius: 10px; font-size: 1rem; font-weight: 600; padding: 0.5rem 1.15rem;`. Regra do botão ATIVO (`aria-checked/selected="true"`): `border-color` trocado de `{COLORS["brand_alt"]}` para `rgba(25, 183, 255, 0.9)` + adicionados `box-shadow: 0 0 8px rgba(25, 183, 255, 0.35);` e `font-weight: 700;`. `:hover` INALTERADA. `render_tab_selector` e toda a lógica de `st.segmented_control`/`session_state`/render lazy (Bloco 5) byte-idênticos — mudança 100% dentro do bloco `<style>`. Seletores `stSegmentedControl`/`aria-checked`/`popover`/`select` preservados (test_inject_styles_cobre_componentes_baseweb segue válido).

- **Teste (junto do D2):** `test_map_tooltips_tem_css_de_tamanho` atualizado para asserir `"13px"`/`"8px 10px"`/`"300px"`/`"1.35"` em ambos os tooltips (estrutura de loop mantida).

## Arquivos alterados
- `src/motor_expansao/dashboard/pages.py` — D1 (revert coord em `render_coord_search_sidebar`); D3 (constante `_CENSUS_PREVIEW_WIDTH_PX=720` + 4× `st.image width`); D4 (CSS do `stSegmentedControl` em `inject_styles`).
- `src/motor_expansao/dashboard/components.py` — D2 (4 chaves de tamanho do `style` em `_shared_map_tooltip` e `_hybrid_compact_tooltip`).
- `tests/integration/test_streamlit_app.py` — `test_map_tooltips_tem_css_de_tamanho` atualizado para os novos valores do D2.

## Validações executadas
- pytest: `python -m pytest -q tests/integration/test_streamlit_app.py` (SERIAL — `-n auto` dá INTERNALERROR execnet × Python 3.14 local) → **183 passed, 0 failed, 0 skipped** (9151 warnings, todos pré-existentes de DeprecationWarning do pytest-asyncio em Python 3.14). Rodei este arquivo porque concentra os contratos tocados (tooltips D2 + `inject_styles` D4) e é o smoke do app exigido pelo plano; D1/D3 são strings/largura de exibição sem teste dedicado.
- import: `python -c "import streamlit_app"` → **import ok** (warnings de cache fora de runtime são esperados, sem exceção).
- ruff: `ruff check src/motor_expansao/dashboard/pages.py src/motor_expansao/dashboard/components.py tests/integration/test_streamlit_app.py` → **All checks passed!**
- mypy: `mypy src/motor_expansao/dashboard/pages.py src/motor_expansao/dashboard/components.py` → **Success: no issues found in 2 source files**
- Suíte FULL NÃO rodada aqui (gate único do QA).

## Problemas encontrados
nenhum

## Pendências
- QA: rodar a suíte FULL (`pytest -n auto` — ou serial se INTERNALERROR local). Baseline conhecida do plano: `695 passed, 1 skipped, 3 failed PRÉ-EXISTENTES`; critério = ZERO novas falhas.
- Housekeeping no fechamento: mover BLK-FIX-10 via `scripts/housekeeping_move_block.py BLK-FIX-10`.

## Riscos remanescentes
- R4 (Bloco 5): mitigado — `render_tab_selector` e `session_state`/`st.segmented_control` byte-idênticos; mudança só no `<style>` de `inject_styles`. QA deve confirmar por diff.
- Paths pré-sujos (`data/outputs/setores_censitarios_2022_geo/_metadata.json`, `data/reports/relatorio_pontual_censitario_base_geo.md`) NÃO devem ser commitados — alheios ao ciclo.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY M1; só CSS/markdown/strings/largura de exibição)
- Artefatos M1 preservados: sim (nenhum artefato tocado; PDF/geração de mapas do Relatório Pontual intocados)
- Dashboard offline mantido: sim (Bloco 4/6 intocados; nenhuma dependência de API ao vivo introduzida)
