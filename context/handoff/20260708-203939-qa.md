# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Block Orchestrator (fechamento)

## VEREDITO
APROVADO

## Justificativa
BLK-REV-01 entregou o harness `scripts/perf_baseline_app.py` (único arquivo de
código) e o relatório gitignored `data/analysis/perf_baseline_app_2026.md`,
ambos READ-ONLY sobre o M1. Re-executei TODAS as validações de forma
independente: ruff limpo, loop_guard OK, script sai com EXIT=0 e regenera o
relatório (18 linhas, 6 caminhos, 3 UFs, seção "O que NÃO foi medido"), M1
mtimes inalterados, git diff M1/config VAZIO. As 4 falhas + 1 erro de coleta na
suite FULL são PRÉ-EXISTENTES e de AMBIENTE (dependências opcionais ausentes:
`matplotlib` e `openlocationcode`), NÃO introduzidas por este ciclo — nenhum dos
arquivos de teste/fonte envolvidos foi tocado pelo BLK-REV-01, e nenhum teste
referencia o script novo. Housekeeping executado (--check EXIT=0).

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- Ambiente sem `matplotlib` e `openlocationcode` (deps opcionais): causam 1 erro
  de coleta (`tests/unit/demanda_revelada/test_viz_atratividade.py`, de BLK-ATR-04)
  e 4 falhas de plus-code (`test_coord_search.py` + `test_streamlit_app.py`).
  PRÉ-EXISTENTES vs. este ciclo, NÃO bloqueadores do BLK-REV-01. Recomenda-se ao
  BO/loop registrar como flanco de ambiente (instalar as deps ou marcar skip).

## Melhorias opcionais
- nenhuma

## Saída literal das validações (re-executadas pelo QA)

### 1. Suite FULL
`python -m pytest -q 2>&1 | tail -5` (coleta interrompida por dep ausente):
```
E   ModuleNotFoundError: No module named 'matplotlib'
=========================== short test summary info ============================
ERROR tests/unit/demanda_revelada/test_viz_atratividade.py
!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!
1 skipped, 1 error in 11.62s
```
Suite completa ignorando o arquivo de coleta quebrado por dep ausente
(`--ignore=tests/unit/demanda_revelada/test_viz_atratividade.py`):
```
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_curto_usa_localidade_mock
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_curto_usa_localidade_mockada
4 failed, 1524 passed, 4 skipped, 19 warnings in 599.84s (0:09:59)
```
Diagnóstico: as 4 falhas são de `resolve_plus_code` que depende de
`openlocationcode` (ausente: `ModuleNotFoundError: No module named
'openlocationcode'`); o erro de coleta é `matplotlib` ausente. AMBOS são
ambiente/pré-existentes (arquivos NÃO tocados pelo ciclo; nenhum teste importa
`perf_baseline_app`). ZERO novas falhas atribuíveis ao BLK-REV-01.

### 2. ruff
```
All checks passed!
EXIT=0
```

### 3. loop_guard
```
GUARD OK: 0 caminho(s) alterado(s), nenhum proibido.
EXIT=0
```

### 4. Script executa
`python scripts/perf_baseline_app.py 2>&1 | tail -20` → EXIT=0
```
[B3] Render mapa M1 (build_map_figure)...
   RO: frio 0.653s / quente 0.965s / cap servido 18,000
   SC: frio 0.715s / quente 0.677s / cap servido 20,373
   AM: frio 0.705s / quente 0.755s / cap servido 18,000
[B4] Troca de modo de cor (build_hybrid_map_figure)...
   AM/score_expansao_hibrido: frio 0.693s / quente 3.253s / cap servido 18,000
   AM/score_setor_2022_calibrado: frio 1.282s / quente 0.931s / cap servido 18,000
   AM/score_oportunidade_residual: frio 0.854s / quente 0.881s / cap servido 18,000
   AM/score_priorizacao: frio 0.751s / quente 0.981s / cap servido 18,000
[B5] Cenario multi-hex (agregar_cenario_multihex)...
   SC/1 hex: frio 0.021s / quente 0.010s
   SC/5 hex: frio 0.021s / quente 0.012s
   SC/20 hex: frio 0.013s / quente 0.013s
[B6] PDF Pontual e Municipal (fpdf2, mapas=None)...
   Pontual Floripa/SC: frio 0.188s / quente 0.057s / 175 KB
   Municipal Floripa/SC: frio 0.093s / quente 0.084s / 196 KB

Relatorio gravado em data/analysis/perf_baseline_app_2026.md (18 linhas de benchmark)
EXIT=0
```

### 5. Relatório gerado
```
-rw-r--r-- 1 loop loop 5130 Jul  8 20:39 data/analysis/perf_baseline_app_2026.md
```

### 6. Relatório gitignored
```
.gitignore:123:data/analysis/	data/analysis/perf_baseline_app_2026.md
EXIT=0
```

### 7. M1 mtimes
Antes e depois de re-executar o script (idênticos):
```
1780501621 data/staging/brasil_estrutural.parquet
1780501631 data/staging/brasil_priorizados.parquet
```

### 8. git diff M1/config
```
(vazio)
```

### 9. Conteúdo do relatório
Confirmado por leitura de `data/analysis/perf_baseline_app_2026.md`:
- Tabela com 18 linhas de benchmark (≥ 15 exigidas).
- Os 6 caminhos presentes: 1. Carga inicial, 2. Filtro por município, 3. Render
  mapa M1, 4. Troca de cor, 5. Cenário multi-hex, 6a/6b. PDF Pontual/Municipal.
- As 3 UFs presentes: RO, SC, AM.
- Seção "O que NÃO foi medido e por que" presente (WebGL/paint, clique, PNG dos
  mapas, overlays, "frio não é 100% frio de disco").
- Também traz seção "Ambiente" e "Comparativo com baseline mai/2026".

### 10. Housekeeping
`--check` inicial retornou EXIT=1 (stub ausente). Executado o move e re-check:
```
OK: movido BLK-REV-01 (1325 bytes) -> tasks/completed.md; stub em tasks/backlog.md.
MOVE_EXIT=0
OK: BLK-REV-01 movido (stub no backlog + bloco em completed.md).
CHECK_EXIT=0
```
`pytest tests/unit/test_housekeeping_helper.py -q` → `10 passed in 0.84s`.

## Conferência de no-bypass
Re-executei todas as validações por conta própria (não confiei na saída do
Builder). A suite FULL rodou de fato (~600s no subconjunto executável). As
falhas foram investigadas individualmente e classificadas como ambiente/
pré-existentes com evidência (`ModuleNotFoundError` de `matplotlib` e
`openlocationcode`; arquivos não tocados pelo ciclo via `git status`). Nenhum
gate foi pulado.

## Guardrails verificados
- §5 READ-ONLY M1: git diff M1/config VAZIO; M1 mtimes inalterados antes/depois
  de re-rodar o script; loop_guard GUARD OK (0 caminhos proibidos).
- §6.1 loop-safe: script sem VPS, sem rede de produção; PDFs em memória com
  mapas=None (sem PII persistida); escrita restrita a data/analysis/ (gitignored).
- §2 acentuação: relatório em PT correto ("Florianópolis" acentuado; strings ok).
- Pesos renda=0.40/pop=0.60 e fórmula do score: INTOCADOS.
- Único arquivo de código novo = scripts/perf_baseline_app.py; sem merge/push/deploy.

## Decisão recomendada
Fechar ciclo. As falhas de suite são de ambiente (deps opcionais ausentes),
pré-existentes ao BLK-REV-01 e não bloqueadoras; recomenda-se registrar o flanco
de dependências ausentes para o BO/loop.
