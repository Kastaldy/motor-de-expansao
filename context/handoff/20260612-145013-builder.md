# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-UI-04 — Destaque do seletor de telas (aba ativa em ciano sólido, mais espaço, botões distintos dos cartões)

## O que foi feito

3 mudanças CSS, todas dentro do bloco `<style>` de `inject_styles()` em `pages.py`, sem tocar `render_tab_selector` nem qualquer lógica de render lazy (Bloco 5):

1. **Aba ATIVA — ciano sólido:** na regra `button[aria-checked="true"]`/`button[aria-selected="true"]` (linhas 325-333 após edição): `background: rgba(25,183,255,0.22)` → `background: #19B7FF`; `color: {COLORS["text"]}` → `color: #0A0C18` (contraste ~9.7:1, AAA). `border-color`, `box-shadow` e `font-weight: 700` mantidos.

2. **GAP entre botões — 8px:** inserida nova regra CSS logo antes do bloco dos botões inativos (linhas 304-308 após edição):
   ```css
   div[data-testid="stSegmentedControl"],
   div[data-testid="stSegmentedControl"] > div {
       display: flex;
       gap: 8px;
   }
   ```

3. **Botões INATIVOS distintos dos cartões:** na regra base dos botões inativos (linhas 309-319 após edição): `background: rgba(18,23,42,0.92)` → `background: rgba(30,38,65,0.88)` (slate mais claro); `border: 1px solid {COLORS["border"]}` → `border: 1px solid rgba(25,183,255,0.30)` (borda ciano suave). `color`, `border-radius`, `font-size`, `font-weight`, `padding` mantidos.

4. **Assert adicional no teste** (opcional, conforme sugestão do Planner): `assert "#19B7FF" in css  # aba ativa ciano sólido (BLK-UI-04)` adicionado em `test_inject_styles_cobre_componentes_baseweb`, linha 4482.

## Arquivos alterados
- `src/motor_expansao/dashboard/pages.py` — bloco CSS do `stSegmentedControl` em `inject_styles()` (~linhas 304-333): regra de gap inserida; botões inativos com background e border trocados; botão ativo com background `#19B7FF` e color `#0A0C18`.
- `tests/integration/test_streamlit_app.py` — `test_inject_styles_cobre_componentes_baseweb` (linha 4482): adicionado assert `"#19B7FF" in css`.

## Validações executadas
- **pytest (serial):** `python -m pytest -q tests/integration/test_streamlit_app.py` → **183 passed, 0 failed, 0 skipped** (sem regressões; `-n auto` não usado por INTERNALERROR execnet×Python 3.14 local, conforme instrução do handoff do Planner)
- **smoke import:** `python -c "import streamlit_app; print('import ok')"` → **import ok**
- **ruff:** `python -m ruff check src/motor_expansao/dashboard/pages.py` → **All checks passed!**
- **mypy:** `python -m mypy src/motor_expansao/dashboard/pages.py --ignore-missing-imports` → **Success: no issues found in 1 source file**

## Problemas encontrados
Nenhum.

## Pendências
- Suíte completa (full `pytest -q`) — responsabilidade do QA (gate único por ciclo); não rodada aqui.

## Riscos remanescentes
1. Gap de 8px: `div[data-testid="stSegmentedControl"] > div` pode não casar em versões futuras do Streamlit (mitigado: inclui ambos os seletores; se não casar, gap simplesmente não aplica, sem regressão visual).
2. Seletor `aria-checked`/`aria-selected`: se versão futura usar `aria-pressed`, o ciano sólido pode não aparecer. Mitigado pelos 3 seletores já presentes.

## Guardrails verificados
- score_priorizacao não alterado: não aplicável (CSS puro)
- Artefatos M1 preservados: sim — zero toque em parquets, pipelines, score, carteira, plano
- Dashboard offline mantido: sim — sem nova dependência de API ao vivo
- render_tab_selector (Bloco 5) intocado: sim — zero alteração nas linhas 435-459
- Paths pré-sujos não commitados: sim — `data/outputs/setores_censitarios_2022_geo/_metadata.json` e `data/reports/relatorio_pontual_censitario_base_geo.md` não tocados
