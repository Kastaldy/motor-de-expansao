# Handoff — BLK-DIM-16 Builder → QA

## Bloco
- **ID:** BLK-DIM-16 — fix break-even (margem 0%) + bound do aluguel-teto
- **Branch:** `ciclo/loop-20260617-114955`
- **Skill atual:** Builder → **próxima: QA**

## Arquivos modificados
1. `src/motor_expansao/dimensionamento/simulador.py` — `aluguel_teto()`: bound `alug_sup` corrigido para receita TOTAL (balcão + agregadores + personal) extraída de kwargs com fallback nas constantes
2. `src/motor_expansao/dimensionamento/viabilidade_ponto.py`:
   - Dataclass `ViabilidadePontoResult`: campo novo `alunos_para_margem_alvo: float = 0.0`
   - `analisar_viabilidade_ponto()` passo 5: `alunos_breakeven` calculado com `margem_alvo=0.0` (break-even real); segunda chamada `alunos_margem_alvo` com `margem_alvo=margem_alvo` para o campo informativo
3. `tests/unit/dimensionamento/test_viabilidade_ponto.py` — 5 novos testes (BLK-DIM-16):
   - `test_breakeven_menor_que_alunos_para_margem_alvo`
   - `test_breakeven_resulta_ebitda_zero`
   - `test_aluguel_teto_considera_agregadores_materiais`
   - `test_aluguel_teto_sem_agregadores_nao_regride`
   - `test_alunos_para_margem_alvo_campo_presente`

## Resultado local Builder
- `pytest tests/unit/dimensionamento/test_viabilidade_ponto.py` → **19 passed**
- `ruff check` → limpo
- `mypy` → limpo

## Guardrails confirmados
- READ-ONLY M1: zero escrita em config/pipelines m1/scoring/artefatos/deploy/CI
- `pages.py` NÃO tocado
- `alunos_breakeven` = EBITDA ~0% (tolerância < 5% para xtol=0.5 de brentq)
- `aluguel_teto` com agregadores extremos > bound antigo (2×balcão)
- DEC-001/008/009 intactas; raio 1.5 km INTOCADO

## Próxima skill: QA
