# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Block Orchestrator (fechamento)

## VEREDITO
APROVADO

## Justificativa
O BLK-VIAB-06 adiciona `flag_fora_envelope` a `analisar_viabilidade_ponto`, sinalizando
extrapolação quando `m2` cai fora de [600, 3000] m². A flag é puramente informativa: o
DRE, a faixa de alunos e a grade de sensibilidade seguem calculados normalmente para
qualquer `m2` (byte-idêntico dentro do envelope, `flag=False` = default do dataclass).
Re-executei TODAS as validações de forma independente:
- Os 6 novos testes cobrem exatamente os critérios de aceite: m2>3000→True, m2<600→True,
  m2=3000→False (inclusivo), m2=600→False (inclusivo), dentro do envelope (1500)→False,
  e DRE inalterado com flag=True.
- Suite: 1517 passed, 4 skipped. Os únicos 4 FAILED e 1 ERROR de coleta são
  PRÉ-EXISTENTES e de AMBIENTE (deps opcionais ausentes: `matplotlib` e `openlocationcode`),
  em módulos que este bloco NÃO tocou. Comprovei que falham IDENTICAMENTE na base limpa
  (com as mudanças do bloco stashed). ZERO novas falhas atribuíveis ao bloco.
- ruff: All checks passed. loop_guard: GUARD OK.
- READ-ONLY M1 confirmado: mtimes dos 2 artefatos oficiais idênticos aos do Builder;
  `git diff` de `pipelines/m1/` + root `config.py` vazio.
- Housekeeping executado com sucesso (stub criado, --check EXIT=0, helper 10 passed).

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- Ambiente do loop não tem `matplotlib` nem `openlocationcode` instalados, causando 1 erro
  de coleta (test_viz_atratividade) e 4 falhas de plus-code offline. São PRÉ-EXISTENTES e
  independentes deste bloco, mas convém instalar as deps opcionais (`[basemap]`/plus-code)
  para a suite ficar 100% verde no ambiente do loop.

## Testes faltantes
- nenhum (os 6 casos de aceite estão cobertos)

## Riscos remanescentes
- nenhum novo

## Saída literal das validações (re-executadas pelo QA)

### 1. Suite FULL
Rodada com `python -m pytest -q` interrompe na COLETA por dep ausente (matplotlib),
erro PRÉ-EXISTENTE do BLK-ATR-04 (test_viz_atratividade.py), não deste bloco:
```
E   ModuleNotFoundError: No module named 'matplotlib'
=========================== short test summary info ============================
ERROR tests/unit/demanda_revelada/test_viz_atratividade.py
!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!
1 skipped, 1 error in 13.35s
```
Reexecutada deselecionando SÓ o módulo uncollectable (dep de ambiente):
`python -m pytest -q --ignore=tests/unit/demanda_revelada/test_viz_atratividade.py`
```
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_curto_usa_localidade_mock
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_curto_usa_localidade_mockada
4 failed, 1517 passed, 4 skipped, 19 warnings in 683.46s (0:11:23)
```
Os 4 FAILED são PRÉ-EXISTENTES (dep `openlocationcode` ausente) — reproduzidos IDÊNTICOS
na base limpa (mudanças do bloco stashed):
```
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_curto_usa_localidade_mockada
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_curto_usa_localidade_mock
4 failed in 4.00s
```
Testes do bloco (isolados) — todos verdes:
```
tests/unit/dimensionamento/test_viabilidade_ponto.py ... 27 passed in 1.63s
```

### 2. ruff
```
All checks passed!
```

### 3. loop_guard
```
GUARD OK: 0 caminho(s) alterado(s), nenhum proibido.
```

### 4. mtimes M1
```
1780501621 data/staging/brasil_estrutural.parquet
1780501631 data/staging/brasil_priorizados.parquet
```
Idênticos aos reportados pelo Builder (inalterados após todas as operações do QA).

### 5. git diff M1/config
`git diff HEAD -- src/motor_expansao/pipelines/m1/ config.py` → (vazio)
Nota: `src/motor_expansao/dimensionamento/config.py` aparece no `git diff --stat` geral,
mas é o config LOCAL da camada paralela de Dimensionamento (NÃO o `config.py` raiz do M1),
e o diff é APENAS conversão de fim-de-linha (CRLF→LF): conteúdo byte-idêntico
(`git diff --ignore-all-space` vazio; sem adição de lógica de score/envelope/pesos).

### 6. NO-BYPASS
Confirmado. Nenhuma validação usou `--config /dev/null`, fixture fora do caminho real
nem mock do caminho crítico. A suite full rodou pelo `pyproject.toml` real; os testes do
bloco exercitam `analisar_viabilidade_ponto` real com fixtures sintéticas em memória
(sem parquet real, sem rede). O stash foi usado apenas para PROVAR que as 4 falhas são
pré-existentes; as mudanças do bloco foram restauradas e revalidadas (27 passed).

### 7. Housekeeping
```
python scripts/housekeeping_move_block.py BLK-VIAB-06 --check
  -> FALHA --check: stub ausente no backlog para BLK-VIAB-06 (EXIT=1)
python scripts/housekeeping_move_block.py BLK-VIAB-06 --date 2026-07-08
  -> OK: movido BLK-VIAB-06 (1615 bytes) -> tasks/completed.md; stub em tasks/backlog.md.
python scripts/housekeeping_move_block.py BLK-VIAB-06 --check
  -> OK: BLK-VIAB-06 movido (stub no backlog + bloco em completed.md). (EXIT=0)
python -m pytest tests/unit/test_housekeeping_helper.py -q
  -> 10 passed in 0.64s
```

## Conferência de no-bypass
Confirmo explicitamente: nenhuma validação foi burlada. A suite completa foi
re-executada por mim (não confiei nos números do Builder). O único desvio da suite
"full literal" foi ignorar o módulo com dep de ambiente ausente (matplotlib) para
obter contagem, e a falha de coleta original está reportada literalmente acima. As 4
falhas de plus-code foram comprovadamente reproduzidas na base limpa, isolando-as do
bloco. Zero mock do caminho crítico do motor de viabilidade.

## Guardrails verificados
- §5 READ-ONLY M1: mtimes dos artefatos oficiais inalterados; `git diff` de
  `pipelines/m1/` + root `config.py` vazio; pesos `renda=0.40`/`pop=0.60` e
  `score_priorizacao`/`hex_score_estrutural` intocados.
- §6.1 loop-safe: loop_guard GUARD OK (0 caminhos proibidos); zero rede/VPS/PII
  (testes usam fixtures sintéticas em memória).
- ENVELOPE_MIN/ENVELOPE_MAX são constantes LOCAIS de `viabilidade_ponto.py`,
  não do §3 canônico.
- Flag apenas informativa (não recusa por padrão): DRE/faixa/grade byte-idênticos
  dentro do envelope; comportamento existente preservado exceto a flag nova.

## Decisão recomendada
Fechar ciclo.
