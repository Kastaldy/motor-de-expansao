# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Builder

## Bloco
BLK-REV-01

## Criticidade
Alta

## Sumário executivo (para o Builder)
Criar UM script novo: `scripts/perf_baseline_app.py`. É um harness de medição headless
(sem Streamlit rodando, sem browser) que importa as funções puras de `data.py`/`components.py`/
`censo_*`/`relatorio_municipal.py`, roda os 6 benchmarks frio/quente por UF (RO/SC/AM), mede
tempo (`time.perf_counter`) e memória (`psutil.rss`, fallback `tracemalloc`), e escreve o relatório
`data/analysis/perf_baseline_app_2026.md`. READ-ONLY sobre o M1: **nada** em `src/`, `config.py`,
`streamlit_app.py` ou artefatos oficiais é tocado. O script sai com código 0 e é auto-validante.

**Viabilidade CONFIRMADA por prova headless (rodei localmente antes de planejar):**
- imports (`data.py`+`components.py`) OK em ~2.6s; `pydeck.Deck` constrói offline sem fetch de tiles.
- RO load 0.82s / M1 map 0.66s (cap 18k) / hybrid map 0.53s; RSS ~346 MB.
- multihex 1/5/20 hex ≈ 12–16 ms; **PDF pontual 204 ms / 180 KB, PDF municipal 64 ms / 201 KB** com `mapas=None` (fpdf2 offline). **Os 6 benchmarks são todos mensuráveis headless — nada precisa ser "manual".**
- `psutil` está disponível no ambiente → usar RSS real.

## Fatos de assinatura verificados (NÃO improvisar)
- `read_enriched_uf_partition(base_dir: Path, uf: str) -> DataFrame` — base_dir = `data/outputs/hexagonos_dashboard_enriquecido`.
- `apply_global_filters(df, *, selected_ufs, selected_cities, selected_faixas, ...)` — **todos keyword-only**; filtro de cidade usa `nome_municipio`.
- `build_map_figure(df, *, selected_ufs, selected_cities, competitors_df=None, ultra_df=None, ...)` → **retorna tupla `(deck, n)`**. **NÃO tem `color_col`** — colore fixo por `score_priorizacao` (M1). Cap dinâmico: se linhas no scope > `MAP_POINT_LIMIT` (35000) usa `MAP_POINT_LIMIT_LARGE` (18000). RO (46k) e AM (294k) caem no cap 18k; SC (20k) fica abaixo do cap normal.
- `build_hybrid_map_figure(hdf, *, selected_ufs, selected_cities, selected_faixas=None, color_col="score_expansao_hibrido", ...)` → **retorna `(deck, n)`**. **É AQUI que se troca o modo de cor** (B4), via `color_col`. Retorna `(None, 0)` se `score_setor_2022_calibrado` ausente (presente no enriquecido).
- `agregar_cenario_multihex(df, hex_ids: list[str]) -> dict` — pura, não muta df.
- `analisar_ponto_censitario_setores(lat, lng, setores_df, raio_km=1.5, ...) -> dict`.
- `gerar_pdf_relatorio_pontual_censitario(result, mapas=None, *, ...) -> bytes` — offline com `mapas=None`.
- `agregar_municipio(df, *, nome_municipio, uf=None, ...) -> dict` — usar `nome_municipio="Florianópolis"` (acentuado!).
- `gerar_pdf_relatorio_municipal(municipio_result, mapas=None, *, ...) -> bytes` — offline.
- `read_censo_geo_partition(base_dir, uf, cod_municipio) -> DataFrame` — geo em `data/outputs/setores_censitarios_2022_geo`; Florianópolis = `uf=SC/cod_municipio=4205407` (1004 setores confirmados).
- `.gitignore` cobre `data/analysis/` (linhas 123-124) → relatório NÃO será commitado.

## Decisão de escopo dos overlays (importante)
Nos benchmarks de mapa (B3/B4), passar `competitors_df=None` e `ultra_df=None`. Motivo: isola o custo
Python de render dos hexágonos (downsample+cap+color-map+tooltip), que é o alvo do bloco; os pins são
overlay separado (nota do BO). Isso também evita depender do diretório `concorrentes/` (CSVs) e mantém
o benchmark determinístico. Registrar essa escolha como nota no relatório.

## Plano de implementação

### A. Import headless
```python
from __future__ import annotations
import gc, logging, platform, sys, time
from datetime import datetime
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
for _p in (str(ROOT), str(ROOT / "src")):
    if _p not in sys.path:
        sys.path.insert(0, _p)

# Streamlit emite "missing ScriptRunContext" fora do runtime — esperado, suprimir.
logging.getLogger("streamlit").setLevel(logging.ERROR)

import pandas as pd  # noqa: E402
from motor_expansao.dashboard.data import (  # noqa: E402
    read_enriched_uf_partition, apply_global_filters, agregar_cenario_multihex,
    read_censo_geo_partition,
)
from motor_expansao.dashboard.components import build_map_figure, build_hybrid_map_figure  # noqa: E402
from motor_expansao.dashboard.censo_point import analisar_ponto_censitario_setores  # noqa: E402
from motor_expansao.dashboard.censo_report import gerar_pdf_relatorio_pontual_censitario  # noqa: E402
from motor_expansao.dashboard.relatorio_municipal import (  # noqa: E402
    agregar_municipio, gerar_pdf_relatorio_municipal,
)

MB = 1024 * 1024
ENRIQUECIDO_DIR = ROOT / "data" / "outputs" / "hexagonos_dashboard_enriquecido"
CENSO_GEO_DIR = ROOT / "data" / "outputs" / "setores_censitarios_2022_geo"
REPORT_PATH = ROOT / "data" / "analysis" / "perf_baseline_app_2026.md"
UFS = ["RO", "SC", "AM"]  # pequena / média / grande
```

Sampler de memória (idêntico ao padrão de `profile_dashboard.py`, reusar):
```python
def _make_rss_sampler():
    try:
        import psutil
        proc = psutil.Process()
        return (lambda: proc.memory_info().rss / MB), f"psutil {psutil.__version__} (RSS do processo)"
    except Exception:
        import tracemalloc
        if not tracemalloc.is_tracing():
            tracemalloc.start()
        return (lambda: tracemalloc.get_traced_memory()[0] / MB), "tracemalloc (heap Python; subestima numpy)"
```

Helper central de medição frio/quente (usar em TODOS os benchmarks). Frio = 1ª chamada após
`gc.collect()`; quente = 2ª chamada imediata no mesmo processo (sem re-ler disco / sem @st.cache).
Pico de memória = maior RSS amostrado entre antes e depois de cada fase.
```python
rss, RSS_BACKEND = _make_rss_sampler()

def bench(fn):
    """Roda fn() frio e quente; retorna (t_frio_s, t_quente_s, pico_mem_MB, resultado_quente)."""
    gc.collect()
    m0 = rss()
    t0 = time.perf_counter(); out = fn(); t_frio = time.perf_counter() - t0
    m1 = rss()
    t0 = time.perf_counter(); out = fn(); t_quente = time.perf_counter() - t0
    m2 = rss()
    pico = max(v for v in (m0, m1, m2) if v is not None)
    return t_frio, t_quente, pico, out
```
Registrar cada resultado numa lista `rows: list[dict]` com chaves:
`{"caminho","uf_tam","t_frio_s","t_quente_s","pico_mem_MB","nota"}`.

### B. Benchmarks (código exato por seção)

#### B1. Carga inicial por UF
Para cada uf em UFS, medir `read_enriched_uf_partition`. Guardar o df quente por UF num dict
`dfs[uf]` (reusado por B2–B4). O "frio" aqui NÃO é 100% frio de disco (o SO pode cachear páginas),
mas é o custo de leitura+`_prepare_dataframe`; registrar essa ressalva no relatório.
```python
dfs = {}
for uf in UFS:
    tf, tq, pico, df = bench(lambda uf=uf: read_enriched_uf_partition(ENRIQUECIDO_DIR, uf))
    dfs[uf] = df
    rows.append({"caminho": "1. Carga inicial (read_enriched_uf_partition)", "uf_tam": f"{uf} ({len(df):,} hexes)",
                 "t_frio_s": tf, "t_quente_s": tq, "pico_mem_MB": pico, "nota": ""})
```

#### B2. Troca de UF/município (filtro pós-carga)
`apply_global_filters` com uma cidade real por UF. Todos os kwargs são keyword-only; passar listas
vazias nos filtros não usados.
```python
CIDADES = {"RO": "Porto Velho", "SC": "Florianópolis", "AM": "Manaus"}
for uf in UFS:
    df = dfs[uf]; cidade = CIDADES[uf]
    tf, tq, pico, sub = bench(lambda df=df, uf=uf, cidade=cidade: apply_global_filters(
        df, selected_ufs=[uf], selected_cities=[cidade], selected_faixas=[]))
    rows.append({"caminho": "2. Filtro por município (apply_global_filters)",
                 "uf_tam": f"{cidade}/{uf} ({len(sub):,} hexes)",
                 "t_frio_s": tf, "t_quente_s": tq, "pico_mem_MB": pico, "nota": ""})
```

#### B3. Render do mapa M1 (lado Python)
`build_map_figure` por UF; **desempacotar a tupla `(deck, n)`**. Passar overlays None (ver decisão de
escopo). O cap efetivo (18k p/ RO e AM; SC abaixo do cap) é registrado via `n`. Também medir o mapa
Híbrido por UF (custo comparável, base do B4).
```python
for uf in UFS:
    df = dfs[uf]
    tf, tq, pico, (deck, n) = bench(lambda df=df, uf=uf: build_map_figure(
        df, selected_ufs=[uf], selected_cities=[], competitors_df=None, ultra_df=None))
    rows.append({"caminho": "3. Render mapa M1 (build_map_figure)", "uf_tam": f"{uf} (cap {n:,})",
                 "t_frio_s": tf, "t_quente_s": tq, "pico_mem_MB": pico,
                 "nota": "só custo Python; WebGL/paint = browser (não medido)"})
```

#### B4. Troca de modo de cor
Reinvocar `build_hybrid_map_figure` no df de AM (caso saturado, cap 18k) alternando `color_col` entre
as 4 réguas. **M1 (`build_map_figure`) NÃO tem `color_col`** → o M1 é medido em B3; aqui usamos o
builder híbrido, que é o que de fato recolore. Uma linha por coluna de cor.
```python
df_am = dfs["AM"]
COLOR_COLS = ["score_expansao_hibrido", "score_setor_2022_calibrado",
              "score_oportunidade_residual", "score_priorizacao"]
for col in COLOR_COLS:
    tf, tq, pico, (deck, n) = bench(lambda col=col: build_hybrid_map_figure(
        df_am, selected_ufs=["AM"], selected_cities=[], color_col=col,
        competitors_df=None, ultra_df=None))
    rows.append({"caminho": "4. Troca de cor (build_hybrid_map_figure)", "uf_tam": f"AM · {col} (cap {n:,})",
                 "t_frio_s": tf, "t_quente_s": tq, "pico_mem_MB": pico, "nota": ""})
```
Nota p/ relatório: o rerun real do Streamlit por troca de cor reexecuta o builder inteiro — este
benchmark mede exatamente esse custo integral (o mais honesto).

#### B5. Cenário múltiplo
`agregar_cenario_multihex` com 1/5/20 hex_ids amostrados do df de SC.
```python
df_sc = dfs["SC"]
sample_ids = df_sc["hex_id"].dropna().head(20).tolist()
for k in (1, 5, 20):
    ids = sample_ids[:k]
    tf, tq, pico, r = bench(lambda ids=ids: agregar_cenario_multihex(df_sc, ids))
    rows.append({"caminho": "5. Cenário multi-hex (agregar_cenario_multihex)", "uf_tam": f"SC · {k} hex",
                 "t_frio_s": tf, "t_quente_s": tq, "pico_mem_MB": pico, "nota": ""})
```

#### B6. PDF Pontual e Municipal (headless — MEDIR, não pular)
Ambos são offline com `mapas=None` (fpdf2). Confirmado: pontual ~204 ms/180 KB, municipal ~64 ms/201 KB.
Preparar os `result`/`municipio_result` UMA vez (fora do `bench`, pois montá-los não é o alvo);
medir só a geração do PDF.
```python
# Pontual — Florianópolis (setores geo)
setores = read_censo_geo_partition(CENSO_GEO_DIR, "SC", "4205407")
res_pontual = analisar_ponto_censitario_setores(-27.5954, -48.5480, setores)
tf, tq, pico, pdf = bench(lambda: gerar_pdf_relatorio_pontual_censitario(res_pontual, mapas=None))
rows.append({"caminho": "6a. PDF Pontual (fpdf2, mapas=None)", "uf_tam": f"Floripa/SC ({len(pdf)//1024} KB)",
             "t_frio_s": tf, "t_quente_s": tq, "pico_mem_MB": pico,
             "nota": "offline; composição de mapas PNG (contextily) NÃO medida"})

# Municipal — Florianópolis (df de SC)
mres = agregar_municipio(df_sc, nome_municipio="Florianópolis", uf="SC")
tf, tq, pico, mpdf = bench(lambda: gerar_pdf_relatorio_municipal(mres, mapas=None))
rows.append({"caminho": "6b. PDF Municipal (fpdf2, mapas=None)", "uf_tam": f"Floripa/SC ({len(mpdf)//1024} KB)",
             "t_frio_s": tf, "t_quente_s": tq, "pico_mem_MB": pico,
             "nota": "offline; composição de mapas PNG (contextily) NÃO medida"})
```
Guardar `agregar_municipio` num try/except defensivo: se lançar (ex.: município sem hexes no recorte),
registrar a linha com nota "n/d — {erro curto}" e seguir (não abortar o script). O mesmo para o pontual.

### C. Geração do relatório
Escrever `data/analysis/perf_baseline_app_2026.md` (criar a pasta com `mkdir(parents=True, exist_ok=True)`).
Conteúdo obrigatório:
1. Cabeçalho com data/hora de execução (`datetime.now().isoformat(timespec="seconds")`) e comando de repro.
2. Ambiente: `platform.python_version()`, SO/release, `pd.__version__`, RAM total (`psutil.virtual_memory().total/GB` se psutil; senão "n/d"), CPU (`platform.processor()` + `os.cpu_count()`), backend de memória.
3. Tabela de resultados: colunas `caminho | UF/tamanho | t_frio_s | t_quente_s | pico_mem_MB | nota`
   (uma linha por item de `rows`; formatar tempos com 3 casas em s ou converter para ms quando < 0.01 s — sugestão: sempre em s com 3 casas para uniformidade).
4. Seção "Comparativo com baseline mai/2026": ler `data/reports/perf_baseline_dashboard.md` se existir e
   citar os números-âncora (cold start nacional ~110s eliminado; RSS pico nacional ~865 MB → AM ~439 MB;
   frame nacional 449 MB deep). Deixar claro que aquele baseline media a CARGA nacional; este mede os 6
   caminhos por UF — não são a mesma métrica, é contexto histórico, não regressão.
5. Seção "O que NÃO foi medido e por quê":
   - Render WebGL / paint no browser (o harness constrói o `pdk.Deck` em Python mas não renderiza pixels).
   - Interação/latência de clique no mapa (browser-side).
   - Composição de mapas PNG dos PDFs (contextily/matplotlib) — medimos o backbone fpdf2 com `mapas=None`.
   - Overlays de concorrentes/Ultra nos mapas (passados como None para isolar o custo de hexágonos).
   - "Frio" não é 100% frio de disco (cache de páginas do SO pode estar quente).
Reusar o estilo de `_write_report`/`_fmt` de `profile_dashboard.py` (mesmas convenções de tabela markdown).

### D. Execução e validação (como o Builder valida)
1. `python scripts/perf_baseline_app.py` → deve imprimir um resumo e **sair com código 0**.
2. Conferir que `data/analysis/perf_baseline_app_2026.md` foi criado e contém as 6 seções de benchmark
   (≥ 12 linhas de dados: B1×3, B2×3, B3×3, B4×4, B5×3, B6×2 = 18 linhas).
3. `ruff check scripts/perf_baseline_app.py` limpo.
4. `python -c "import ast; ast.parse(open('scripts/perf_baseline_app.py').read())"` (sanidade sintática).
5. Não é necessário arquivo de teste formal (script auto-validante). NÃO tocar `tests/`.
6. Confirmar via `git status` que só os paths do ciclo mudaram e que `data/analysis/...` está gitignored
   (não deve aparecer como untracked a commitar).

## Paths do ciclo (para commit isolado)
- `scripts/perf_baseline_app.py` (NOVO — único arquivo de código)
- `data/analysis/perf_baseline_app_2026.md` (gerado; GITIGNORED — não commitar)
- `tasks/backlog.md` (stub housekeeping do bloco, se aplicável)
- `tasks/completed.md` (bloco movido no fechamento — QA/fechamento)
- `tasks/current_task.md` (status)
- `context/handoff.md` + `context/handoff/*.md`

## Guardrails verificados
- §5 READ-ONLY M1: o script só IMPORTA e LÊ; zero escrita em `config.py`, `pipelines/m1/`, `src/`,
  `streamlit_app.py` ou artefatos oficiais (`hexagonos_brasil_dashboard.parquet`, etc.). Nenhuma função
  chamada recalcula score (todas documentadas como puras/READ-ONLY).
- §6.1 loop-safe: sem VPS, sem rede de produção, sem PII (PDFs gerados só em memória com `mapas=None`,
  não persistidos). `pydeck.Deck` construído offline (verificado: não faz fetch de tiles na construção).
- Escrita restrita a `data/analysis/` (gitignored, §5). Nenhum parquet de staging/outputs é reescrito.
- Determinístico: hex_ids amostrados por `.head(k)` sobre o df já ordenado por `_prepare_dataframe`
  (ordem estável), coordenadas fixas, mesmas UFs.
- §2 acentuação: strings do relatório em PT com acentuação correta; `nome_municipio="Florianópolis"` acentuado.
- pesos `renda=0.40`/`pop=0.60` e fórmula: INTOCADOS.
