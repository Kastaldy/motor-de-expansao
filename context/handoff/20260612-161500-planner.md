# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Builder (aguardando aprovação humana — criticidade Alta)

## Bloco planejado
BLK-UI-03 — 4 ajustes finos de UX (reverter coord, tooltip meio-termo, preview menor [fecha BLK-FIX-10], destaque seletor de abas)

## Estado confirmado (leitura de código)
- Streamlit do projeto: `streamlit>=1.35.0,<2` (pyproject.toml:15); instalado localmente **1.58.0**. `st.image(..., width=<int>)` é suportado (>=1.35). `width="stretch"` é a forma de string atual usada no preview.
- `render_coord_search_sidebar` (pages.py:523-543): hoje tem `st.sidebar.info(...)` (linhas 526-529), inserido pelo BLK-UI-02.
- Tooltips (components.py): `_shared_map_tooltip` style nas linhas 1067-1077 (4 chaves novas em 1073-1076); `_hybrid_compact_tooltip` style nas linhas 1102-1112 (4 chaves novas em 1108-1111). Valores atuais idênticos: `fontSize=11px`, `padding=6px 8px`, `maxWidth=260px`, `lineHeight=1.25`.
- Preview do Relatório Pontual: 4x `st.image(..., width="stretch")` em pages.py:2596-2615.
- CSS do segmented control em `inject_styles()`: linhas 301-319 (base `button`, `:hover`, `[aria-checked="true"]`). `brand_alt=#19B7FF` = rgb(25,183,255); `border=rgba(120,137,210,0.24)`; `muted=#A7B3D1`; `text=#F3F7FF`.
- `render_tab_selector` (pages.py:426-450): Bloco 5 render lazy — NÃO tocar.
- Teste `test_map_tooltips_tem_css_de_tamanho` (test_streamlit_app.py:341-349) assere `"11px"/"6px 8px"/"260px"/"1.25"`.
- Caption original recuperado via `git show 0862205` (commit BLK-UI-01, parent de cdcd4c9/BLK-UI-02): exatamente
  `st.sidebar.caption("Localize um hexagono pela coordenada. Offline, sem API externa.")`.

## Decisões técnicas

### D1 — Reverter coord (pages.py:523-529)
Reverter o `st.sidebar.info(...)` para o par heading+caption ORIGINAL (recuperado do commit BLK-UI-01 `0862205`, texto exato):
```python
st.sidebar.markdown("### Busca por coordenada")
st.sidebar.caption("Localize um hexagono pela coordenada. Offline, sem API externa.")
```
- Remover INTEGRALMENTE o bloco `st.sidebar.info(` ... `)` (linhas 526-529).
- Preservar intactos: `st.sidebar.markdown("---")` (linha 525) acima; `st.sidebar.text_input(...)` (530-534); `parse_coordinate_input`; `st.sidebar.error(...)`; assinatura `() -> tuple[float, float] | None`; chamada em `streamlit_app.py:470`.

### D2 — Tooltip meio-termo (components.py:1073-1076 e 1108-1111)
Valores APROVADOS (meio-termo entre default deck.gl ~13-14px e o 11px do BLK-UI-02; legível mas menor que default):
- `"fontSize": "13px"` (era 11px)
- `"padding": "8px 10px"` (era 6px 8px)
- `"maxWidth": "300px"` (era 260px)
- `"lineHeight": "1.35"` (era 1.25)

Aplicar IDÊNTICO em ambas as funções. Chaves pré-existentes (`backgroundColor`/`color`/`border`/`borderRadius`/`fontFamily`) e o `html` de ambas INALTERADOS.

Atualizar `test_map_tooltips_tem_css_de_tamanho` (linhas 346-349): trocar os 4 valores asseridos para `"13px"`, `"8px 10px"`, `"300px"`, `"1.35"`. A estrutura do teste (loop sobre ambos os tooltips, asserções de presença das 4 chaves) é mantida.

### D3 — [BLK-FIX-10] Preview menor (pages.py:2596-2615)
Abordagem escolhida: **largura fixa em pixels via constante nomeada** (mais simples e auditável que colunas; não muta layout de outras seções).
- Adicionar constante de módulo em `pages.py`, junto às outras constantes de UI no topo do arquivo (ou imediatamente acima de `render_relatorio_pontual_censitario`):
  ```python
  _CENSUS_PREVIEW_WIDTH_PX = 720  # BLK-FIX-10: preview menor que 100% da largura de conteudo
  ```
- Nas 4 chamadas `st.image` trocar `width="stretch"` por `width=_CENSUS_PREVIEW_WIDTH_PX`. Captions inalterados.
- Confirmação de API: `st.image(image, width=<int>)` aceita inteiro em px desde Streamlit 1.35 (projeto 1.58.0). `width="stretch"` segue sendo a forma string; trocamos por inteiro literal via constante.
- NÃO tocar: `censo_report.py`, `censo_map.py`, `render_mapas_censitarios_combinados`, `gerar_pdf_relatorio_pontual_censitario`, raio/interseção. O conteúdo dos PNGs (`mapas[...]`) não muda; só a largura de exibição na tela.

### D4 — Destaque seletor de abas — SÓ CSS (pages.py:301-319, dentro de inject_styles)
Reforçar SOMENTE as 3 regras CSS que já referenciam `stSegmentedControl` (linhas 301-319). Valores exatos:

Regra base dos botões (era linhas 301-307) — adicionar tipografia/padding/raio:
```css
[data-testid="stSegmentedControl"] button,
div[role="radiogroup"] [data-baseweb="button"],
[data-baseweb="button-group"] button {
    background: rgba(18, 23, 42, 0.92);
    color: {COLORS["muted"]};
    border: 1px solid {COLORS["border"]};
    border-radius: 10px;
    font-size: 1rem;
    font-weight: 600;
    padding: 0.5rem 1.15rem;
}
```
Regra `:hover` (era 308-312) — INALTERADA (já dá feedback de hover suficiente; manter como está).

Regra do botão ATIVO `[aria-checked="true"]/[aria-selected="true"]` (era 313-319) — reforçar destaque:
```css
[data-testid="stSegmentedControl"] button[aria-checked="true"],
[data-testid="stSegmentedControl"] button[aria-selected="true"],
[data-baseweb="button-group"] button[aria-checked="true"] {
    background: rgba(25, 183, 255, 0.22);
    color: {COLORS["text"]};
    border-color: rgba(25, 183, 255, 0.9);
    box-shadow: 0 0 8px rgba(25, 183, 255, 0.35);
    font-weight: 700;
}
```
Notas:
- Os valores rgb(25,183,255) correspondem a `brand_alt=#19B7FF`; usados como literais (já é o padrão do arquivo nessas linhas).
- `box-shadow`/`border-color` reforçado e `font-weight: 700` no ativo separam visualmente a aba atual; `font-size: 1rem` + `font-weight: 600` + `padding` maior tornam o seletor mais evidente como um todo.
- `render_tab_selector` (linha 426-450) NÃO é tocado — confirmado; mudança 100% dentro do bloco `<style>` de `inject_styles()`.
- O teste `test_inject_styles_cobre_componentes_baseweb` (linha 4466) só checa presença de `stSegmentedControl`/`aria-checked`/`popover`/`select` no CSS — segue passando (mantemos esses seletores).

## Plano de execução (Builder deve seguir em ordem)

### Passo 1 — [D1] Reverter coord
Arquivo: `src/motor_expansao/dashboard/pages.py`
- Operação (Edit): substituir
  ```python
  st.sidebar.info(
      "**Busca por coordenada** — localize um hexagono pela coordenada. "
      "Offline, sem API externa."
  )
  ```
  por
  ```python
  st.sidebar.markdown("### Busca por coordenada")
  st.sidebar.caption("Localize um hexagono pela coordenada. Offline, sem API externa.")
  ```
- Verificação: `render_coord_search_sidebar` sem `st.sidebar.info`; com markdown heading e caption; `markdown("---")`, `text_input`, `parse_coordinate_input`, `error`, assinatura intactos.

### Passo 2 — [D2] Tooltip meio-termo
Arquivo: `src/motor_expansao/dashboard/components.py`
- Operação em `_shared_map_tooltip` (linhas 1073-1076): trocar os 4 valores para `"13px"`, `"8px 10px"`, `"300px"`, `"1.35"`.
- Operação em `_hybrid_compact_tooltip` (linhas 1108-1111): mesmas 4 trocas.
- Verificação: ambas as funções com os valores novos; chaves pré-existentes e `html` inalterados.

### Passo 3 — [D3] Preview menor
Arquivo: `src/motor_expansao/dashboard/pages.py`
- Operação 3a: adicionar constante `_CENSUS_PREVIEW_WIDTH_PX = 720` (com comentário BLK-FIX-10) no bloco de constantes de módulo ou logo acima de `render_relatorio_pontual_censitario`.
- Operação 3b: nas 4 chamadas `st.image` (linhas 2596-2615), trocar `width="stretch"` por `width=_CENSUS_PREVIEW_WIDTH_PX`. (4 ocorrências; podem ser feitas com `replace_all` da string `width="stretch",` se ela só ocorrer nessas 4 chamadas — o Builder deve confirmar a contagem antes de usar replace_all; senão editar uma a uma.)
- Verificação: nenhuma `width="stretch"` restante no preview do censitário; `censo_map.py`/`censo_report.py` intocados; captions inalterados.

### Passo 4 — [D4] CSS do seletor
Arquivo: `src/motor_expansao/dashboard/pages.py` (`inject_styles`, linhas 301-319)
- Operação: na regra base do `stSegmentedControl button` adicionar `border-radius: 10px; font-size: 1rem; font-weight: 600; padding: 0.5rem 1.15rem;`. Na regra do ativo adicionar/ajustar `border-color: rgba(25, 183, 255, 0.9); box-shadow: 0 0 8px rgba(25, 183, 255, 0.35); font-weight: 700;` (substituindo a linha `border-color: {COLORS["brand_alt"]};` pelo rgba literal). Regra `:hover` inalterada.
- Verificação: `render_tab_selector` (linha 426-450) SEM alteração; mudança só no bloco `<style>`; seletores `stSegmentedControl`/`aria-checked` mantidos.

### Passo 5 — Testes
Arquivo: `tests/integration/test_streamlit_app.py`
- Atualizar `test_map_tooltips_tem_css_de_tamanho` (linhas 346-349): asserts para `"13px"`, `"8px 10px"`, `"300px"`, `"1.35"`.
- (Opcional, mínimo) NÃO é obrigatório criar testes novos. Se o Builder quiser cobrir D3/D4 com smoke barato:
  - D3: um teste que importa `pages._CENSUS_PREVIEW_WIDTH_PX` e assere `== 720` (ou `< 1000`), evitando renderização Streamlit.
  - D4: o `test_inject_styles_cobre_componentes_baseweb` já cobre presença; opcionalmente asserir `"box-shadow" in css` no contexto do segmented control — só se trivial. Evitar testes frágeis de CSS exato.
  - D1: o teste de coord search existente (mock em streamlit_app:render_coord_search_sidebar) não inspeciona o texto; sem teste novo necessário.

### Passo 6 — Validação pelo Builder (subconjunto)
- `python -m pytest -q tests/integration/test_streamlit_app.py` (alvo; rodar SERIAL — xdist quebra no Python 3.14 local conforme nota do Builder BLK-UI-02).
- `python -c "import streamlit_app"` → ok.
- `ruff check` nos arquivos tocados.
- `mypy src/motor_expansao/dashboard/pages.py src/motor_expansao/dashboard/components.py`.
(suíte FULL é gate do QA.)

## Arquivos alterados (lista para o Builder)
- `src/motor_expansao/dashboard/pages.py` (D1 coord, D3 preview + constante, D4 CSS)
- `src/motor_expansao/dashboard/components.py` (D2 tooltips)
- `tests/integration/test_streamlit_app.py` (atualizar `test_map_tooltips_tem_css_de_tamanho`)

## Critérios de aceite (para QA verificar)
1. **D1:** `render_coord_search_sidebar` SEM `st.sidebar.info`; COM `st.sidebar.markdown("### Busca por coordenada")` e `st.sidebar.caption("Localize um hexagono pela coordenada. Offline, sem API externa.")`; `markdown("---")` acima, `text_input`, `parse_coordinate_input`, `error` e assinatura intactos.
2. **D2:** ambos os tooltips com `fontSize="13px"`, `padding="8px 10px"`, `maxWidth="300px"`, `lineHeight="1.35"`; chaves pré-existentes e `html` inalterados; `test_map_tooltips_tem_css_de_tamanho` atualizado e passando.
3. **D3 (fecha BLK-FIX-10):** as 4 `st.image` do preview usam `width=_CENSUS_PREVIEW_WIDTH_PX` (=720), nenhuma `width="stretch"`; constante definida em `pages.py`; `censo_map.py`/`censo_report.py`/PDF intocados; captions inalterados.
4. **D4:** regras CSS do `stSegmentedControl` em `inject_styles` com `font-size`/`font-weight`/`padding`/`border-radius` reforçados e botão ativo com `border-color` vivo + `box-shadow` + `font-weight:700`; `render_tab_selector` (426-450) sem nenhuma alteração; `:hover` inalterado.
5. **Suíte:** `pytest -q` sem novas falhas (baseline informado: `695 passed, 1 skipped, 3 failed PRÉ-EXISTENTES`); ruff e mypy limpos nos arquivos tocados; `import streamlit_app` ok.
6. **M1 READ-ONLY:** zero alteração em score, pesos, fórmula ou artefatos oficiais.

## Riscos e mitigações
- **R1 (texto original do caption):** RESOLVIDO — recuperado exato via `git show 0862205` (BLK-UI-01): `"Localize um hexagono pela coordenada. Offline, sem API externa."`. Sem ambiguidade.
- **R2 (contrato de teste do tooltip):** `test_map_tooltips_tem_css_de_tamanho` assere `"11px"`. Mitigação: Passo 5 atualiza os 4 valores; sem essa atualização o teste falha. Builder DEVE executar o Passo 5 junto do Passo 2.
- **R3 (API de `st.image(width=int)`):** RESOLVIDO — Streamlit do projeto `>=1.35.0,<2` (instalado 1.58.0); `width` inteiro em px suportado. `width="stretch"` (string) é a forma atual; troca por inteiro literal via constante é válida.
- **R4 (seletor CSS por versão):** `[data-testid="stSegmentedControl"]` já está em uso no arquivo e coberto por teste; reforçamos as MESMAS regras, sem introduzir seletor novo. Risco baixo. Tema dark preservado (cores derivadas de `COLORS`/brand_alt já usados).
- **R5 (Bloco 5 intocável):** Mitigação dura — D4 edita SOMENTE o bloco `<style>` em `inject_styles` (linhas 301-319). `render_tab_selector` (426-450), `session_state`, `st.segmented_control`, `options`, `key`, `default`, `label_visibility` NÃO são tocados. QA deve confirmar via diff que `render_tab_selector` está byte-idêntico.

## Guardrails confirmados
- READ-ONLY sobre o M1: nenhum score (`score_priorizacao`, `hex_score_estrutural`, `score_setor_2022_calibrado`, `score_oportunidade_residual`, SAM), peso, fórmula ou artefato oficial alterado (CLAUDE.md §2/§3/§5).
- Bloco 5 render lazy intocável: `render_tab_selector` e a lógica de `session_state`/`st.segmented_control` — só CSS em `inject_styles` (CLAUDE.md §4).
- Item 3 (BLK-FIX-10) READ-ONLY sobre PDF/geração de mapas: `censo_report.py`, `censo_map.py`, `render_mapas_censitarios_combinados`, `gerar_pdf_relatorio_pontual_censitario`, raio 1,5 km e método de interseção intocados.
- Bloco 4 (carga lazy por UF) e Bloco 6 (fonte de mapa enxuta) intocados; sem dependência de API ao vivo introduzida.
- Nenhum PR sobe com CI quebrado; mudança é só estilo/strings/largura de exibição.
- Paths pré-sujos (`data/outputs/setores_censitarios_2022_geo/_metadata.json`, `data/reports/relatorio_pontual_censitario_base_geo.md`) NÃO devem ser commitados — alheios ao ciclo.
