# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-TP-05 — Re-teste honesto do elo demanda OBSERVADA → captura (k-fold repetido vs baseline)

## O que foi feito
Implementado EXATAMENTE o plano técnico do Planner (itens 1–10), aprovado por Felipe Silva em
2026-06-30 (decisões A1–A8 sem ajustes). Re-teste honesto, READ-ONLY sobre o M1, do elo
demanda OBSERVADA (`membros`, BLK-TP-01) → captura (`alunos_parceiras`), que a DEC-009 marcou
NO-GO com demanda IMPUTADA.

1. **CRIADO `src/motor_expansao/demanda_revelada/backtest_tp05.py`** (pacote disjunto, DEC-012):
   - `@dataclass BacktestTP05Result` com todos os campos do item 8.1 (alpha, coefs, intercepto,
     R²_oof log/alunos, RMSE oof log/alunos, RMSE baseline log/alunos, r2_oof_baseline=0,
     ic95_r2_oof, r2_insample, n_treinamento, n_descartado_zeros/invalidos, range_alunos,
     spearman, pct_extrapolacao, flag_extrapolacao_padrao_global, metodo_validacao,
     auditoria_vazamento, veredito, nota_honesta).
   - `correlacoes_bivariadas(df)` (Spearman das 5 features brutas vs `alunos_parceiras` no
     subset >0, ANTES do modelo).
   - `preparar_dados(df)`: alvo=`log1p(alunos_parceiras)` no subset >0; features=
     `[log1p(membros), log1p(dist_concorrente_lc_min_m), n_concorrente_lc]`; EXCLUI
     `n_acad_parceiras` (circular) e `membros_gt5km` (colinear); limpa NaN/inf; conta descartados.
   - `_kfold_repetido_oof(X, y, alpha, *, n_splits)`: `RepeatedKFold(5, 5, random_state=42)` +
     `Ridge`; R²/RMSE oof por repetição via `_r2`/`_rmse` importados de `backtest_dim`; predição
     oof média por ponto p/ o bootstrap; baseline = média do treino do fold (espaço log e alunos).
   - `_selecionar_alpha_e_oof`: varre `ALPHA_GRID` (importado de `aderencia.py`), escolhe alpha
     por MENOR RMSE oof; suporta k=5×5, k=10×5 e LOO (fallback A4 via `_r2_loo_para_alpha`).
   - `_ic_bootstrap_r2(y, y_pred_oof, rng, n=500)`: IC95 percentis [2,5; 97,5] dos pares
     reamostrados (espelha `_ic_bootstrap_auc` de `residual_discriminacao.py`), seed 42.
   - `_pct_extrapolacao` (envelope min-max das features de treino) + `flag_extrapolacao_padrao_global`
     (n<30, degrada p/ LOO).
   - `backtest_demanda_captura(df, limiar_r2=LIMIAR_R2_GO)`: orquestra; GO se `R²_oof_log > 0,05`
     E `IC95_inferior > 0`; NO-GO NÃO levanta.
   - `r2_insample` SOMENTE como campo rotulado literalmente "apenas auditoria -- NAO usar como
     desempenho" (constante `_ROTULO_INSAMPLE`, testada por substring).
   - `_auditoria_vazamento(df)`: modelo SECUNDÁRIO COM `n_acad_parceiras`, rotulado
     "auditoria -- vazamento estrutural"; NÃO entra no veredito.
   - `relatorio_tp05(result)` + `escrever_relatorio_tp05(result, *, path)`; `if __name__ ==
     "__main__"` (`# pragma: no cover`) lê o parquet real e grava `data/analysis/backtest_tp05.md`.
     NUNCA lê dado real em teste.
   - Docstring do módulo com guardrails (READ-ONLY M1; DEC-008/009/012; sem PII; demanda =
     insumo observado nunca preditor geográfico). Imports: numpy/pandas/scipy.stats/sklearn
     (Ridge, RepeatedKFold) + `motor_expansao.dimensionamento`. ZERO import de
     `pipelines/m1`, `censo_*`, `dashboard`, `config.py` raiz.

2. **CRIADO `tests/unit/test_backtest_tp05.py`** (10 testes, fixture sintética em código, n=600
   com sinal + 150 zeros): (a) GO com sinal forte; (b) NO-GO com ruído puro; (c) IC95 lo≤hi;
   (d) pct_extrapolacao∈[0,100] (=0% pois treino=teste); (e) r2_insample rotulado por substring;
   (f) zero-PII estrutural (nenhuma coluna de `COLUNAS_PII_PROIBIDAS` em coefs/spearman/auditoria
   + sem PII forte na prosa); (g) reprodutibilidade com seed fixa; (h) n_descartado_zeros conta os
   150 zeros; + preparar_dados/correlacoes; + ValueError sem positivos.

3. **ATUALIZADO `src/motor_expansao/demanda_revelada/__init__.py`**: exporta
   `backtest_demanda_captura` e `BacktestTP05Result`, preservando os exports atuais.

4. **GERADO o relatório real** `data/analysis/backtest_tp05.md` (gitignored) via `__main__` sobre
   o parquet real.

### VEREDITO REAL OBTIDO (dado real, parquet `demanda_revelada_h3.parquet`)
**GO honesto.** Números-chave:
- N usados (alunos_parceiras>0, features finitas) = **5.341** | descartados zeros = **11.234** |
  inválidos = 0 | range alunos = [1, 14.332] (total parquet = 16.575 = 5.341 + 11.234 ✓).
- **R²_oof_log = +0,5750** (âncora, gate) | **IC95 = [+0,5576, +0,5959]** (não cruza zero) →
  GO (> 0,05 E IC_inf > 0).
- R²_oof_alunos (auditoria) = +0,4218 | R²_insample (auditoria) = +0,5758.
- RMSE_oof_log = 1,2205 (baseline média 1,8726) | RMSE_oof_alunos = 649,4 (baseline 902,5) →
  o modelo bate o chute da média em ambos os espaços.
- alpha = 10 | método = kfold_5x5 | pct_extrapolacao = 0,0% | flag_global = False.
- Spearman bivariada (subset >0): membros +0,748, n_acad_parceiras +0,759,
  dist_concorrente_lc_min_m −0,448, n_concorrente_lc +0,332, membros_gt5km −0,232.
- Modelo de auditoria (COM `n_acad_parceiras`, circular) salta para R²_oof_log = **+0,6388** —
  evidencia o ganho atribuível à circularidade soma↔contagem que justificou excluí-lo do principal.

Leitura honesta: o GO é com demanda OBSERVADA (não imputada) e fica preservado SEM a feature
circular. Os 6 confounds obrigatórios estão na `nota_honesta`/relatório (cobertura ~1%,
concentração SP, ruído coords ~1 km, viés de seleção das parceiras, multicolinearidade
membros↔alunos_parceiras, circularidade de n_acad_parceiras). A REABERTURA da Camada 2 (Huff) é
gate humano (Felipe), FORA deste bloco — o Builder só gerou o relatório.

## Arquivos alterados
- `src/motor_expansao/demanda_revelada/backtest_tp05.py` — CRIADO (módulo do re-teste honesto).
- `tests/unit/test_backtest_tp05.py` — CRIADO (10 testes, fixture sintética).
- `src/motor_expansao/demanda_revelada/__init__.py` — exporta `backtest_demanda_captura` +
  `BacktestTP05Result` (exports antigos preservados).
- `data/analysis/backtest_tp05.md` — GERADO em runtime (gitignored; não versionado).
- `tasks/current_task.md` — status atualizado para "aguardando QA".

## Validações executadas
- pytest (subconjunto impactado):
  `python -m pytest -q tests/unit/test_backtest_tp05.py tests/unit/test_demanda_revelada_ingestao.py`
  → **18 passed, 0 failed, 0 skipped** (10 do TP-05 + 8 da ingestão).
- ruff: `python -m ruff check src/motor_expansao/demanda_revelada/ tests/unit/test_backtest_tp05.py`
  → **All checks passed!** (0 erros).
- mypy: `python -m mypy src/motor_expansao/demanda_revelada/`
  → **Success: no issues found in 4 source files** (0 erros novos).
- import: `python -c "import streamlit_app; print('import ok')"` → **import ok**.
- relatório real: `python -m motor_expansao.demanda_revelada.backtest_tp05` → gravou
  `data/analysis/backtest_tp05.md` com veredito GO e N reais.

Por quê esse subconjunto: o escopo toca só o pacote `demanda_revelada/` (módulo novo + __init__);
rodei os testes do módulo novo + os da camada (ingestão) para garantir que os exports não
quebraram. Smoke `import streamlit_app` como rede de segurança mínima obrigatória. A suíte FULL
é o gate único do QA — não rodada aqui.

## Problemas encontrados
- 1º run do `test_zero_pii_em_todas_as_saidas` falhou por falso-positivo: o scan substring da
  prosa em PT casava `lat`/`id`/`nome` (colunas PII curtas) dentro de palavras comuns
  ("plataforma", "aleatoria"). Corrigido para checar PII por NOME ESTRUTURAL (colunas/chaves/
  features), igual ao `test_zero_pii` da ingestão, + substring só de PII FORTE inequívoca
  (cpf/email/telefone/employee_id/company_id/residencial) na prosa. O módulo nunca emite coluna
  PII; era só o teste sendo grosseiro demais.
- A contagem real de zeros é **11.234** (não 10.234 citada no plano do Planner — typo do Planner;
  16.575 − 5.341 = 11.234, confirmado no parquet). N modelado (5.341) bate com a inspeção do Planner.

## Pendências
- Nenhuma do escopo. O relatório real é gitignored (não versionado) — regenerável via `__main__`.

## Riscos remanescentes
- Veredito GO com demanda OBSERVADA: a REABERTURA da Camada 2 (Huff) é gate HUMANO (Felipe),
  FORA deste bloco (handoff §9/risco 6). O Builder NÃO implementou a reabertura.
- GO carrega os confounds documentados (especialmente multicolinearidade membros↔alunos_parceiras
  de mesma origem de medição e viés de seleção das parceiras) — não é prova de causalidade
  demanda→captura; ler a `nota_honesta`.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; nenhum artefato M1 tocado; pacote disjunto não
  importa de `pipelines/m1`, `censo_*`, `dashboard`, `config.py` raiz — verificado por mypy/ruff/imports).
- Artefatos M1 preservados: sim (zero escrita em `data/staging/brasil_*`/`hexagonos_brasil_*`; o
  parquet `demanda_revelada_h3.parquet` é consumido READ-ONLY).
- Dashboard offline mantido: sim (`import streamlit_app` ok; nenhuma dependência nova de rede; o
  módulo não é importado pelo dashboard).
- Anti-PII (DEC-012): sim (sem PII em artefato/teste/relatório; testes só com fixture sintética;
  fonte real `NAO_ABRA/` não tocada).
- DEC-008: sim (k-fold repetido vs baseline da média; R² in-sample só auditoria rotulada; NO-GO é
  resultado válido — não forçado).
- DEC-009: sim (demanda = insumo OBSERVADO; `membros` usado só na análise read-only, não como
  ajuste do score).
- Paths pré-sujos (geojson deletados, scripts/backtest_smartfit_scores.py, _report.md): NÃO
  tocados.
