# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA

## Bloco executado
BLK-LTV-04 — Score de retenção territorial (camada paralela M2, READ-ONLY sobre o M1; DEC-014)

## Resultado de negócio (o que importa)
**VEREDITO REAL DO SCORE = NO-GO** (desfecho LEGÍTIMO e esperado — DEC-014 decisão 2 / DEC-008).
O eixo retenção territorial, validado honestamente out-of-fold, **NÃO** bate o critério de GO. O
módulo **NÃO gerou** `data/staging/score_retencao_territorial.parquet` (encerrar sem score, sem
degradar para `score_priorizacao` como proxy). Escreveu apenas o relatório documentando o NO-GO.

## O que foi feito
Implementado o módulo do eixo de score M2 conforme DEC-014 (4 decisões de produto do gate humano
aplicadas exatamente): pesos variante A `w_cap=0.50`/`w_ret=0.50`; fallback NO-GO = encerrar sem
score; modelo em numpy puro (Ridge, SEM scikit-learn); nomenclatura `score_retencao`.

- **`src/motor_expansao/lifetime/score_retencao_territorial.py`** (NOVO):
  - Constantes (SEED=42, N_BOOTSTRAP=1000, PISO_RHO=0.30, W_CAP=0.50, W_RET=0.50, K_FOLDS=5×N_REPS=5,
    N_MIN_KFOLD=25→LOO fallback, RIDGE_LAMBDA=1.0, ALVO=`LTV_PROSPECTIVO_12M_MEDIANO`, SNAPSHOT,
    envelope de clip de maturidade 0–300 meses, `VERSAO_CONTRATO="score_retencao_v1"`).
  - `_normalizar_unidade` LOCAL (maiúscula/sem acento/sufixo `- UF`) — mantém `lifetime/` desacoplado
    (NÃO importa de `dimensionamento/`); casa **56/56** unidades com o histórico de growth.
  - Loaders READ-ONLY: `_carregar_dataset`, `_carregar_universo_aplicacao` (lê `hexagonos_mercado_mapeado.parquet`
    só as colunas necessárias — camada paralela, NÃO artefato oficial M1), `_carregar_maturidade`
    (dedup `groupby('unidade').first()`, parse `inauguracao` DD/MM/YYYY, meses até SNAPSHOT, **clip** de
    outliers negativos/>600m).
  - **Ridge numpy puro** (`_ridge_fit`/`_ridge_pred`, intercepto não penalizado; fallback lstsq).
  - `_oof_predictions`: **k-fold 5×5** (padronização ajustada só no treino de cada fold, sem vazamento;
    LOO quando N<25). `fit(X,y)→predict(X)` **nunca** ocorre. `_r2_oof` vs baseline da média.
  - `_bootstrap_ci_r2_rho`: IC95 (seed fixo, determinista) de R²_oof e Spearman rho_oof.
  - `validar_eixo_retencao(df, *, maturidade)` → `ResultadoValidacao` (dataclass): variantes
    `score_priorizacao` sozinho + multivariadas territoriais + variante COM maturidade como COVARIÁVEL
    de controle. Sem R² in-sample.
  - `_veredito_no_go(res)` mecânico (R²_oof>0 IC↛0 **E** rho_oof≥0.30 IC↛0 **E** supera base) — NUNCA
    levanta exceção.
  - `calcular_score_retencao(df_hexes, df_cal, features)`: schema DEC-014, `retencao_norm` por percentil
    nacional (`rank(pct=True)`), clip 0–100, `flag_extrapolacao` [q05,q95], sem mutar inputs, sem
    escrever em path M1.
  - `_montar_relatorio` (string PURA byte-estável) + `_escrever_relatorio`.
  - `run(root)`: valida → escreve relatório → **em GO** grava o parquet; **em NO-GO NÃO grava** o parquet
    (DEC-014 decisão 2), sem exceção. `main()`.
- **`tests/unit/test_score_retencao_territorial.py`** (NOVO): 13 testes (schema exato+dtypes+versao_contrato;
  determinismo de score e de IC; protocolo kfold 5×5 vs LOO; OOF sem dobra vazia; NO-GO alcançável sem
  exceção; `run()` NO-GO não gera parquet mas escreve relatório; imports proibidos incl. `sklearn`;
  READ-ONLY M1 por mtime nos 4 artefatos; clip 0–100; flag_extrapolação; não muta inputs).

## Saída LITERAL das validações

### `run()` real sobre os parquets REAIS (veredito + números out-of-fold)
```
M1 artefatos presentes: ['brasil_estrutural.parquet', 'brasil_priorizados.parquet', 'hexagonos_brasil_oportunidades.parquet']

===== VEREDITO: NO-GO =====
N calibracao: 56 | protocolo: kfold_5x5 | seed: 42

Modelos out-of-fold:
  score_priorizacao            n=56 proto=kfold_5x5 R2_oof=+0.0399 IC=[-0.1013,+0.1203] rho_oof=-0.0730 IC=[-0.3594,+0.2338] mat=False
  score+renda                  n=56 proto=kfold_5x5 R2_oof=-0.0161 IC=[-0.2024,+0.0692] rho_oof=-0.1322 IC=[-0.4042,+0.1712] mat=False
  score+conc                   n=56 proto=kfold_5x5 R2_oof=+0.0292 IC=[-0.1343,+0.1119] rho_oof=+0.1074 IC=[-0.1842,+0.3751] mat=False
  score+renda+conc             n=56 proto=kfold_5x5 R2_oof=-0.0255 IC=[-0.2311,+0.0631] rho_oof=-0.0221 IC=[-0.2950,+0.2622] mat=False
  score_priorizacao+maturidade n=56 proto=kfold_5x5 R2_oof=+0.0039 IC=[-0.1162,+0.1039] rho_oof=+0.0510 IC=[-0.2201,+0.3210] mat=True

mtime M1 inalterado: True
parquet score existe? False
```
Justificativa mecânica registrada no relatório: melhor modelo (`score_priorizacao` sozinho) tem
R²_oof=+0.0399 com **IC cruzando zero** e rho_oof=-0.0730 (**< 0.30 e IC cruza zero**) → NO-GO honesto.
Nota: só 3 dos 4 artefatos M1 existem neste ambiente; o mtime dos presentes ficou idêntico.

### pytest (subset impactado)
```
python -m pytest -n auto -q tests/unit/test_score_retencao_territorial.py
13 passed, 1302 warnings in 18.25s

python -m pytest -n auto -q tests/unit/test_score_retencao_territorial.py tests/unit/test_correlacao_territorio_retencao.py tests/integration/test_streamlit_app.py
237 passed, 25876 warnings in 141.80s
```

### ruff / mypy / smoke
```
ruff check src/motor_expansao/lifetime/score_retencao_territorial.py tests/unit/test_score_retencao_territorial.py
All checks passed!

mypy src/motor_expansao/lifetime/score_retencao_territorial.py  -> Success: no issues found in 1 source file
mypy src/                                                        -> Success: no issues found in 88 source files

python -c "import streamlit_app; print('import ok')"  -> import ok
```

## Validações executadas
- pytest (subset): **237 passed, 0 failed, 0 skipped** (novo módulo 13 + LTV-03 + streamlit integration).
- import streamlit_app: **ok**.
- ruff (escopo novo): **clean**. mypy `src/`: **Success (88 files)**.
- `run()` real: **NO-GO**; parquet de score NÃO gerado; mtime dos artefatos M1 presentes **inalterado**.

## Arquivos alterados
- `src/motor_expansao/lifetime/score_retencao_territorial.py` (NOVO — módulo do eixo M2 de retenção).
- `tests/unit/test_score_retencao_territorial.py` (NOVO — 13 testes unitários).
- `data/analysis/relatorio_score_retencao.md` (NOVO output do `run()`, gitignored — documenta o NO-GO).
- `context/handoff.md` (este arquivo) + snapshot `context/handoff/20260701-160000-builder.md`.
- `tasks/current_task.md` (Skill atual: Builder; Próxima: QA).
- **NÃO gerado (NO-GO, DEC-014 decisão 2):** `data/staging/score_retencao_territorial.parquet`.

## Problemas encontrados
Nenhum. O NO-GO é o desfecho honesto esperado (o sinal bivariado +0.391 do LTV-03 é correlação de
rank in-sample da feature bruta; o teste out-of-fold do modelo Ridge não generaliza — R²_oof e rho_oof
com IC cruzando zero). Nada a forçar.

## Pendências
Nenhuma para o Builder. Decisão de negócio (do humano, fora deste ciclo): dado o NO-GO honesto, o epic
BLK-LTV encerra a linha de "score de retenção territorial" — os ativos LTV-01/02/03 permanecem.

## Riscos remanescentes
- N=56 pequeno (variância alta) e colinearidade captação↔retenção declarados no relatório (§3).
- O relatório e o (não-)parquet são gitignored; `git check-ignore` confirma ambos ignorados.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (só LIDO; DEC-001 intacta).
- Artefatos M1 preservados: **sim** (mtime dos presentes inalterado; nenhum path M1 escrito).
- Dashboard offline mantido: **sim / não aplicável** (nenhuma integração ao dashboard/API/mercado/VPS).
- `lifetime/` sem imports proibidos: **sim** (teste dedicado inclui `sklearn`; numpy puro).
- DEC-008: R² in-sample e `fit(X,y)→predict(X)` banidos; out-of-fold + IC + flag_extrapolação; NO-GO válido.
- DEC-009: score de RETENÇÃO territorial; nenhuma coluna usada como preditor de demanda.
- Escopo por PATH respeitado: paths pré-sujos (malhas IBGE, backtest_smartfit) e `PRD.md` NÃO tocados.

## Nota para o QA
- Gate full (`pytest -n auto`) + ruff/mypy full + gitleaks são do QA.
- O veredito NO-GO é reproduzível e determinístico (seed=42) sobre os parquets reais; reexecutar `run()`
  não deve gerar o parquet de score nem alterar mtime dos artefatos M1.
