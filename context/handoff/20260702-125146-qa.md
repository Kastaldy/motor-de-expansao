# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador roda Passo 6.0 housekeeping via `scripts/housekeeping_move_block.py BLK-TP-04` + commit por path).

## VEREDITO
APROVADO

## Justificativa
Bloco READ-ONLY sobre o M1 entregue como planejado (D1–D7 = todas A): módulo puro `demanda_revelada/calibracao_curva.py` valida honestamente a curva m²→densidade sob DEC-008 (k-fold 5×5 vs baseline, R² in-sample banido, IC95 bootstrap seed=42, intervalos p10/p50/p90 + flag de extrapolação), com `alunos_parceiras` só como sanity-check (`usado_na_curva=False`) e curva prevendo de `log(metragem)`(+dummy marca) SOMENTE (DEC-009). Suíte FULL `1231 passed, 1 skipped` (+8 dos novos, sem flakiness); mtime dos 4 artefatos M1 idêntico; isolamento DEC-012 e anti-PII verificados; `viabilidade_ponto.py`/`config.py` intocados; ruff+mypy limpos. Veredito do relatório (GO fraco pelo degrau de TIER; pooled/Ultra NO-GO) é honesto e bate byte-a-byte com o log do Builder.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- Em `validar_curva_densidade` a linha `_ = metr_serie` e a variável `p50m`/`metr_serie` são inertes (a série de metragem é recomputada dentro de `_percentis_janela`); poderiam ser removidas para reduzir ruído. Cosmético, não afeta correção nem cobertura.

## Testes faltantes
- nenhum bloqueador. Cobertura adequada: anti-PII (token/word-boundary), R² in-sample banido (campo + relatório), intervalos ordenados + `flag_extrapolacao_m2`, `alunos_parceiras` nunca preditor (inclui grep de source), isolamento de import, determinismo do IC bootstrap, critério GO/NO-GO (sinal forte→GO, ruído→NO-GO) e materialização do .md. (Opcional futuro: teste do fallback LOO `n<N_PISO_LOO`, hoje exercido só indireta/implicitamente.)

## Riscos remanescentes
- N=112 pequeno → IC largo; o GO é fraco e dependente do dummy de marca (degrau de tier Eng 2,31 vs Ultra 1,57), NÃO da metragem sozinha — pooled e Ultra dão NO-GO. Documentado explicitamente no relatório e no `nota_honesta` como caveat central; não há risco de leitura enganosa da metragem como sinal forte. Recalibrar coeficientes da curva em produção é FOLLOW-UP com gate próprio (não feito aqui).

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte COMPLETA (gate único) — `python -m pytest -n auto -q`
```
1231 passed, 1 skipped, 326654 warnings in 192.01s (0:03:12)
```
Baseline BLK-TP-03 ≈ 1223 passed; +8 dos novos testes de `tests/unit/demanda_revelada/test_calibracao_curva.py`. 0 falhas, 0 erros de coleta. Sem flakiness por paralelismo (rodada `-n auto` única, íntegra). Warnings são pré-existentes (CRS geográfico do Fase A censo, não regressão).

### 2. Smoke — `python -c "import streamlit_app; print('import ok')"`
```
(warnings de streamlit "missing ScriptRunContext" / "No runtime found" — esperados em bare mode)
import ok
```

### 3. Isolamento de import (DEC-012)
```
isolamento ok
```
(`import motor_expansao.demanda_revelada.calibracao_curva`; source não contém `pipelines.m1`/`dashboard`/`censo_`/`motor_expansao.api`.)

### 4. mtime dos 4 artefatos oficiais M1 (antes registrado pelo Builder == depois da suíte QA)
```
brasil_estrutural 1780501621.7337682 SAME
brasil_priorizados 1780501631.9004016 SAME
hexagonos_brasil_oportunidades 1780501655.5595677 SAME
hexagonos_brasil_dashboard MISSING SAME
ALL M1 MTIMES IDENTICAL: True
```
`git status --porcelain` → nenhum artefato oficial M1 modificado. (`hexagonos_brasil_dashboard.parquet` não existe localmente e NÃO foi criado — o bloco não materializa artefato M1.)

### 5. Lint/type
```
$ ruff check src/motor_expansao/demanda_revelada/calibracao_curva.py tests/unit/demanda_revelada/
All checks passed!

$ python -m mypy src/motor_expansao/demanda_revelada/calibracao_curva.py
Success: no issues found in 1 source file
```

### Auditoria de mérito (evidência adicional)
```
# DEC-008: in_sample/insample no relatório .md → NENHUM (só a menção "R2 in-sample é BANIDO", não como métrica)
grep -in "in_sample|insample" data/analysis/calibracao_curva_densidade.md → NONE FOUND
# dataclass CurvaValidacaoResult não tem campo r2_insample/in_sample (verificado em source + teste test_loo_honesto_sem_r2_insample)
# in-sample computado e IMEDIATAMENTE del (linhas 449-453), com comentário "nao vaza ao output/relatorio"

# DEC-012 anti-PII: tokens proibidos como word-boundary no .md
PII token hits in .md: []
COLUNAS_PII_PROIBIDAS = {cpf, sf_nome, lat, company_id, telefone, lat_residencial, nome,
  employee_id, longitude, lng_residencial, id, latitude, lng, email, membro_id}
git check-ignore data/analysis/calibracao_curva_densidade.md → gitignored (OK)
# fixtures dos testes são SINTÉTICAS (np.random.default_rng), fonte real NAO_ABRA/ não tocada

# DEC-009: feature = log(metragem) + dummy marca; nenhum lat/lng/pop/renda em X;
#   alunos_parceiras usado_na_curva=False (teste test_alunos_parceiras_nunca_preditor faz grep de source)

# Escopo READ-ONLY M1
git status --porcelain | grep viabilidade_ponto  → NOT MODIFIED
git diff --stat src/motor_expansao/dimensionamento/  → (vazio; nenhum diff)
git status --porcelain | grep config.py  → NOT MODIFIED

# Housekeeping (pré-move, esperado)
python scripts/housekeeping_move_block.py BLK-TP-04 --check → FALHA: stub ausente (EXIT=1)  [ESPERADO: move roda no Passo 6.0 do orquestrador]
grep "### BLK-TP-04" tasks/backlog.md → linha 913 (bloco ÍNTEGRO no backlog p/ o helper mover)
python -m pytest tests/unit/test_housekeeping_helper.py -q → 10 passed
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico. A suíte FULL rodou 1× via `pytest -n auto` (gate único do ciclo), sem `-p no:xdist`. Este bloco é de análise/modelagem (não tooling de produção que dependa de config real tipo SOPS), então a regra de "tooling contra caminho REAL" do §(c) não se aplica; as fixtures sintéticas dos 8 testes unitários são prática legítima e esperada (isolam a lógica de validação), NÃO bypass. As execuções de merit (mtime, isolamento, PII, escopo) rodaram contra os artefatos/código reais do repo.

## Conferência cruzada com log do Builder
Bate. Contagem: Builder rodou subconjunto (221 passed); QA rodou FULL (1231 passed, 1 skipped) — consistente e sem regressão. Números do veredito idênticos ao relatório e ao handoff do Builder: principal (dummy marca) R2_oof=+0.2590 IC95[+0.0973,+0.3840] GO fraco; pooled NO-GO IC95[-0.0227,+0.2065]; Ultra N=54 NO-GO IC95[-0.1651,+0.1144]; densidade mediana Eng 2,31 / Ultra 1,569; alpha=1; método kfold_5x5. mtimes M1 idênticos aos registrados pelo Builder. ruff/mypy limpos como declarado.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; nenhum recálculo; `config.py`/pesos renda=0.40/pop=0.60 intocados)
- Artefatos M1 preservados: sim (mtime idêntico nos 3 existentes; dashboard MISSING não criado; nenhum M1 em `git status`)
- `viabilidade_ponto.py` / `faixa_alunos_por_densidade` / `dimensionamento/`: INTOCADOS (janela reimplementada localmente; zero diff em `dimensionamento/`)
- DEC-008 (in-sample banido, k-fold 5×5 vs baseline, IC95 seed=42, intervalos+flag): sim
- DEC-009 (curva de m² SOMENTE; nenhuma geografia; `alunos_parceiras` só descritivo): sim
- DEC-012 (anti-PII; fixtures sintéticas; isolamento de pacote; fonte real não tocada): sim
- Validações re-executadas pelo QA (sem bypass): pytest full → 1231 passed/1 skipped; import streamlit_app → ok; isolamento → ok; mtime M1 → idêntico; ruff → passed; mypy → success; anti-PII → 0 hits; housekeeping helper test → 10 passed
- Escopo respeitado: sim — só os arquivos do plano (`calibracao_curva.py` novo, `tests/unit/demanda_revelada/{__init__,test_calibracao_curva}.py` novos, `.md` gitignored, `handoff`/`current_task`/snapshots). `backtest_smartfit_scores.py` e os 2 geojsons deletados são pré-existentes do snapshot inicial do repo, fora deste ciclo.

## Decisão recomendada
Fechar ciclo. Orquestrador executa o Passo 6.0 (housekeeping via `scripts/housekeeping_move_block.py BLK-TP-04`) + commit por path. Nenhum bloco de correção necessário.
