# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-UI-08 — Refinos de UX/UI do dashboard (paleta Renda absoluta, tab selector sticky, busca por endereço)

## Decisão do gate humano (já resolvida — implementada)
- D1 = **Alternativa B (fetch HTTP automático)** para a busca por endereço, com **fallback gracioso** para o link clicável da Alternativa A.
- **DEC-010 APROVADA por Vinicius em 2026-06-17** e registrada em `CLAUDE.md §8`.

## O que foi feito

### Mudança 1 — Paleta absoluta da Renda Média (`constants.py`)
- Substituí o bloco de `RENDA_PER_CAPITA_BANDS` pelas 5 faixas pedidas, alpha=150, ordem ASCENDENTE de cutoff:
  `(1000→247,244,139,150 #F7F48B)`, `(2000→255,255,0,150 #FFFF00)`, `(3500→255,210,28,150 #FFD21C)`,
  `(5000→168,255,168,150 #A8FFA8)`, `(inf→0,204,0,150 #00CC00)`.
- Atualizei o comentário acima do bloco (autoria Vini 2026-06-17, cores hex registradas; alpha=150 segue sendo cor de legenda).
- `DENSIDADE_POP_BANDS` e `RESIDUAL_SCORE_BANDS`: INTOCADOS. `censo_map.py` consome `RENDA_PER_CAPITA_BANDS` só via import (linhas 27/182/773/810) — sem override local; a troca se propaga automaticamente.

### Mudança 2 — Tab selector sticky (`pages.py`, `inject_styles`)
- Adicionei, no bloco já existente do `div[data-testid="stSegmentedControl"]`, regras de fixação: `position: sticky; top: 0; z-index: 999;` + fundo opaco (mesmo gradiente do stApp/sidebar `linear-gradient(180deg,#0E1324,#0A0F1F)`), padding vertical 0.4rem e `overflow: visible`.
- Comentário documenta o fallback `position: fixed` + padding compensatório (NÃO ativado por padrão).
- `render_tab_selector` e a ordem de chamada em `streamlit_app.py`: INTOCADOS (mudança puramente de CSS).

### Mudança 3 (Alternativa B) — Busca por endereço (`pages.py` + `maps_geocoder.py`)
- **`maps_geocoder.py`:** novo helper `resolve_endereco_http(query, *, timeout=6.0, cache_dir=None) -> tuple[float,float]|None`, **urllib puro** (sem Selenium), seguindo o redirect da URL de busca do Maps e extraindo a coordenada via `extract_any_coord` (URL final e, em fallback, corpo). Valida bbox Brasil (constantes locais `_BR_*`, espelham `data._validate_brazil_bbox`, p/ não acoplar o módulo `api` ao dashboard). Cache local opcional por consulta (`_cache_key` = sha1 do texto normalizado). `try/except` amplo → qualquer falha/timeout/sem-rede retorna `None`. `import urllib.request` foi promovido ao topo do módulo (stdlib, sem rede no import) p/ ser mockável em teste; a rede só ocorre no `urlopen`.
- **`pages.py` (`render_coord_search_sidebar`):** assinatura `-> tuple[float,float] | None` e `key="coord_search_input"` PRESERVADAS. Fluxo: vazio→None; `parse_coordinate_input(raw)` SEMPRE primeiro (caminho numérico intacto) → se resolver, retorna; senão trata como ENDEREÇO → import lazy de `resolve_endereco_http`, chama com `cache_dir=Path("data/cache/geocode")` e timeout 6s sob `try/except`; sucesso→retorna `(lat,lng)`; falha/None→`_render_endereco_fallback_link` (link clicável via `st.link_button` com fallback `st.markdown` se a versão não tiver) e retorna None. Label/placeholder/caption ampliados (aceita endereço; aviso anti-PII de que o endereço é enviado ao Google).

### DEC-010 registrada (`CLAUDE.md §8`)
- Nova subseção `### DEC-010 — Resolução de endereço por fetch HTTP na barra de busca do dashboard (BLK-UI-08)`, no molde da DEC-004, com Status APROVADA por Vinicius em 2026-06-17, Criticidade Alta, e as cláusulas (a)–(f): escopo restrito ao caminho de busca de endereço; cache `data/cache/geocode/` (gitignored); fallback offline gracioso + default seguro em CI/teste; timeout curto + try/except; I/O isolado no helper `api` (urllib, import lazy no dashboard); nota anti-PII + atribuição Google. READ-ONLY sobre M1 (pesos/score/artefatos INALTERADOS); §5 preservado. Cita a DEC-004 como precedente.

## Arquivos alterados
- `src/motor_expansao/dashboard/constants.py` — `RENDA_PER_CAPITA_BANDS` (5 cores absolutas, alpha 150) + comentário.
- `src/motor_expansao/dashboard/pages.py` — CSS sticky do `stSegmentedControl` em `inject_styles`; `render_coord_search_sidebar` agora aceita endereço (Alt. B) + helper `_render_endereco_fallback_link` e constante `_GEOCODE_CACHE_DIR`.
- `src/motor_expansao/api/maps_geocoder.py` — `import urllib.request` no topo; helper `resolve_endereco_http` + `_cache_key` + constantes de bbox Brasil. `MapsGeocoder`/funções existentes INTOCADAS.
- `CLAUDE.md` — DEC-010 registrada em §8.
- `tests/integration/test_streamlit_app.py` — testes das 3 mudanças (renda bands; sticky CSS; endereço via mock + fallback + caminho numérico não chama HTTP).
- `tests/unit/test_coord_search.py` — testes de `resolve_endereco_http` (redirect mock, falha de rede, fora do Brasil, vazio, cache sem rede); urllib SEMPRE mockado.
- `context/handoff.md`, `context/handoff/20260617-124414-builder.md`, `tasks/current_task.md` — housekeeping.

## Validações executadas
- `python -m pytest -q tests/integration/test_streamlit_app.py tests/unit/test_coord_search.py` → **232 passed**, 0 failed, 0 skipped (11107 warnings de DeprecationWarning do pytest-asyncio/Python 3.14, pré-existentes/ambiente).
- `python -m pytest -q tests/unit/test_coord_search.py` (após rename interno) → **27 passed**.
- `python -m ruff check src/motor_expansao/dashboard/constants.py src/motor_expansao/dashboard/pages.py src/motor_expansao/api/maps_geocoder.py tests/integration/test_streamlit_app.py` → **All checks passed!**
- `python -m mypy src/motor_expansao/dashboard/pages.py src/motor_expansao/dashboard/constants.py src/motor_expansao/api/maps_geocoder.py` → **Success: no issues found in 3 source files**.
- `python -c "import streamlit_app"` → **import ok**.
- `git check-ignore data/cache/geocode/test.txt` → coberto pelo padrão `data/cache/` do `.gitignore`.

## Problemas encontrados
- mypy acusou conflito de tipo (`float` vs `float|None`) por reuso do nome `lat,lng` entre o ramo de cache (floats) e o `extract_any_coord` (Optional). Resolvido renomeando os locais do cache para `c_lat,c_lng`. mypy/ruff limpos depois.

## Pendências
- Nenhuma de código. Verificação VISUAL do sticky (Risco R2: `position: sticky` depende de nenhum ancestral Streamlit cortar o scroll) é critério manual — confirmar no dashboard rodando; fallback `fixed` documentado em comentário caso falhe.

## Riscos remanescentes
- R2 (sticky visual) acima.
- Alt. B faz rede SÓ no caminho de busca de endereço (DEC-010); CI/teste nunca bate na rede (urllib mockado). Carga/interatividade offline do dashboard intacta.

## Guardrails verificados
- score_priorizacao não alterado: sim (não aplicável — nada de score/pesos/artefatos tocado).
- Artefatos M1 preservados: sim.
- Dashboard offline mantido: sim (fetch HTTP isolado e opt-in só na busca de endereço, com fallback offline; import lazy; default seguro sem rede).

## Notas para o QA
- Suite full (`pytest -n auto`) é o gate do QA. **Atenção:** rodar SERIAL se o xdist apresentar `INTERNALERROR` no Python 3.14 deste ambiente (conhecido).
- NÃO commitar os paths pré-sujos (`data/outputs/setores_censitarios_2022_geo/_metadata.json`, `data/reports/relatorio_pontual_censitario_base_geo.md`) — alheios ao ciclo; commit é passo do orquestrador.
- Verifique se o teste `test_inject_styles_cobre_componentes_baseweb` (já existente) e o novo `test_inject_styles_tab_selector_sticky` convivem sem conflito de seletor.
