# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer (Opus 4.8)

## Bloco executado
BLK-TP-07 — Huff/gravitacional de captura de concorrentes com demanda observada (reabertura da Camada 2 do BLK-DIM). Gate humano APROVADO por Felipe em 2026-07-03 ("aprovar"); D1..D6 = recomendações do Planner.

## O que foi feito
Implementado o módulo novo `demanda_revelada/huff_captura.py` (Huff por hexágono) exatamente conforme o plano do Planner (passos 1..15), reusando o núcleo PURO de `dimensionamento/huff.py` (`share_huff`, `_haversine_vec`, `_carregar_concorrentes`, `BETA_GRID`, `DIST_BASE_KM`, `JANELA_CATCHMENT_KM`) e o harness testado de `demanda_revelada/calibracao_residual.py` (k-fold 5×5 seed=42, `_ic_bootstrap_r2`/`_ic_bootstrap_rho`, `_selecionar_alpha_e_oof`, `_metodo_validacao`, `_rho_oof`) + `dimensionamento/backtest_dim._r2/_rmse` e `aderencia.LIMIAR_R2_GO`.

Arquitetura (isolamento DEC-012): centroide do hex via `h3.cell_to_latlng(hex_id)` (NUNCA lê `lat`/`lng`, que são `COLUNAS_PII_PROIBIDAS`). `share_hex` é PURO — só geometria + β, nunca recebe o alvo `membros` (anti-vazamento DEC-009). As 6 decisões implementadas:
- D1: atratividade UNITÁRIA (1.0) no principal; capacidade-por-rede (via `capacidade_clube_validacao.ler_capacidade_clube_por_rede`, import lazy dentro da função) como SENSIBILIDADE rotulada fora do veredito.
- D2: share do centroide do hex.
- D3: β escolhido por MENOR RMSE out-of-fold sobre `BETA_GRID=(0.5,1,1.5,2,3)` (share sempre puro; seleção out-of-fold).
- D4: sem Ultra no share principal + sensibilidade "proximidade Ultra covariável" (via `n_unidades_ultra_1km`/`dist_ultra_mais_proxima_m` do mercado, READ-ONLY) fora do gate.
- D5: `membros` log1p = alvo único do veredito; `alunos_parceiras`/`n_acad_parceiras` só cross-check bivariado (Spearman antes do modelo), com caveat de circularidade.
- D6: baseline da média (âncora do R², embutido no k-fold) + baseline GEOMÉTRICO `log1p(n_conc_no_raio) ~ membros` (contagem-no-raio sem β) reportado ao lado; o GO-âncora EXIGE superar o baseline geométrico.

Rodado o veredito REAL via `executar()` sobre os parquets reais e materializado `data/analysis/huff_captura.md` (gitignored).

## Veredito REAL out-of-fold (dados reais, seed=42)
**GO (âncora R²).** A geometria de distância AGREGA sobre a mera contagem no raio.
- **β_selecionado = 0.5** (out-of-fold; RMSE_oof por β: 0.5→1.4184, 1.0→1.4186, 1.5→1.4251, 2.0→1.4355, 3.0→1.4612).
- **R²_oof_log = +0.4391** | IC95 [+0.4251, +0.4523] (> LIMIAR_R2_GO 0.05; IC acima de zero).
- **rho_oof = +0.4354** | IC95 [+0.4213, +0.4491].
- **R²_oof baseline GEOMÉTRICO (D6b) = +0.2922** | IC95 [+0.2790, +0.3066] → o Huff (com β/distância) supera a contagem-no-raio em ~0.147 de R² ⇒ a distância agrega.
- R²_insample (APENAS auditoria, banido do veredito) = +0.4392.
- alpha (Ridge) = 0.1; método = kfold_5x5; pct_extrapolação = 0.0%; flag_global = False.
- n_join = 16.575 | n_treino = 16.575 | descartados = 0 | cobertura ~1.07% do universo | range membros [1, 8831].
- concentração UF: SP 29.1%, MG 12.7%, RJ 9.2% (viés metropolitano do Sudeste — caveat DEC-012).
- Spearman bruta (vs membros): share_huff −0.58 (coerente: mais/perto de concorrentes ⇒ menor share; o Ridge sobre log1p(share) captura o sinal na direção certa), n_conc_no_raio +0.458, n_concorrente_lc +0.302, dist_concorrente_lc_min_m −0.55, alunos_parceiras +0.637 / n_acad_parceiras +0.624 (cross-check circular, fora do gate).
- Sensibilidade D1b (capacidade/rede, atratividade uniforme ≈2.735, FORA do gate): R²_oof_log +0.357 IC95 [+0.343, +0.370] — não supera o principal unitário (atratividade uniforme só reescala; não agrega).
- Sensibilidade D4c (proximidade Ultra covariável, FORA do gate): R²_oof_log +0.4755 IC95 [+0.462, +0.488] — ganho marginal sobre o principal; não desloca o veredito.

**Interpretação honesta:** GO técnico e materialmente confiável out-of-fold, RESTRITO ao recorte metropolitano (~1% do universo, viés Sudeste). É "hipótese confirmada" (share Huff prevê a demanda observada e a distância agrega sobre a contagem), NÃO execução crua. Integrar a captura ao `score_oportunidade_residual`/carteira/plano é FOLLOW-UP com DEC + gate próprio (BLK-TP-09) — este bloco NÃO integra nada.

## Arquivos alterados
- **CRIADO** `src/motor_expansao/demanda_revelada/huff_captura.py` — módulo novo do Huff por hex (share puro por centroide, seleção de β out-of-fold, validação honesta k-fold, baseline geométrico, sensibilidades D1b/D4c rotuladas, relatório sem PII, `executar()` de disco). Imports só de módulos permitidos.
- **CRIADO** `tests/unit/demanda_revelada/test_huff_captura.py` — fixtures 100% sintéticas (hexes H3 reais + concorrentes sintéticos); 11 testes.
- **GERADO (gitignored, não versionado)** `data/analysis/huff_captura.md` — relatório GO/NO-GO real.
- **ATUALIZADO** `tasks/current_task.md`, `context/handoff.md`, `context/handoff/20260703-180958-builder.md` (snapshot).
- **NÃO ALTERADO:** `dimensionamento/huff.py` (só import); nenhum parquet de staging; nenhum artefato oficial M1.

## Validações executadas (saída real)
- `pytest -q tests/unit/demanda_revelada/test_huff_captura.py`: **11 passed** (0 failed).
- `pytest -n auto -q tests/unit/demanda_revelada/`: **96 passed** (0 failed) — subconjunto impactado.
- `python -c "import streamlit_app"`: **import ok**.
- `ruff check` (módulo + teste): **All checks passed** (2 unused imports + import-sort auto-corrigidos; helper de teste morto removido).
- `mypy src/.../huff_captura.py`: **Success: no issues found**.
- AST/grep isolamento: imports diretos = {__future__, calibracao_residual, capacidade_clube_validacao, contrato, dataclasses, h3, logging, dimensionamento.aderencia, dimensionamento.backtest_dim, dimensionamento.huff, numpy, pandas, pathlib, re, scipy.stats, sklearn.linear_model} → **NENHUM proibido** (pipelines.m1/dashboard/censo/api/config).
- mtime guard dos 4 oficiais M1 — **inalterado**:
  - `brasil_estrutural.parquet` 1780501621.7337682 (antes = depois)
  - `brasil_priorizados.parquet` 1780501631.9004016 (antes = depois)
  - `hexagonos_brasil_oportunidades.parquet` 1780501655.5595677 (antes = depois)
  - `hexagonos_brasil_dashboard.parquet` **MISSING localmente** (não presente no checkout; nada escrito).
- `git check-ignore data/analysis/huff_captura.md` → casado (relatório gitignored, não versionado).

## Problemas encontrados
Nenhum bloqueante. Dois ajustes durante o desenvolvimento: (1) o guard anti-PII do relatório disparava com os tokens literais `lat`/`lng` do header — reescrito para "coordenada residencial" (sem tokens PII isolados); (2) `test_share_nao_recebe_alvo` inspecionava a docstring (que cita `membros` como o alvo) — corrigido para inspecionar SÓ o código executável (docstring removida via AST). Ambos resolvidos; nenhum impacto no comportamento do modelo.

## Pendências
Nenhuma para este bloco. Integração da captura ao residual/carteira/plano = BLK-TP-09 (follow-up com DEC + gate próprio), FORA de escopo.

## Riscos remanescentes
- Generalização: GO restrito a ~1% do universo (viés metropolitano Sudeste) — caveat explícito no relatório; não é validação nacional.
- Ruído de coord ~1 km na fonte da Demanda Revelada domina refino sub-hex (atenua sinal; documentado).
- Circularidade `alunos_parceiras`↔`n_acad_parceiras` (só cross-check, nunca gate).

## Guardrails verificados
- score_priorizacao não alterado: **sim** (READ-ONLY; nenhuma escrita em score/pesos/carteira/plano).
- Artefatos M1 preservados: **sim** (mtime dos 3 oficiais presentes inalterado; nenhum parquet de staging regenerado).
- Dashboard offline mantido: **sim** (`import streamlit_app` ok; módulo isolado, sem dependência de rede/API ao vivo).
- DEC-008 honrada: **sim** (out-of-fold vs baseline; R² in-sample rotulado "apenas auditoria -- NAO usar como desempenho", fora do veredito; IC95 seed=42; β out-of-fold; flag de extrapolação).
- DEC-009 honrada: **sim** (`membros`/`alunos_parceiras` só alvo/cross-check; `share_huff` puro nunca recebe o alvo — teste `test_share_nao_recebe_alvo`).
- DEC-012 (anti-PII): **sim** (`test_zero_pii`; centroide via `hex_id`; `nome_unidade` nunca lido; fixtures sintéticas; fonte real intocada).

## Paths do ciclo para commit (commit por path — NUNCA git add -A)
- `src/motor_expansao/demanda_revelada/huff_captura.py`
- `tests/unit/demanda_revelada/test_huff_captura.py`
- `tasks/current_task.md`
- `context/handoff.md`
- `context/handoff/20260703-180958-builder.md`
(NÃO versionar `data/analysis/huff_captura.md` — gitignored.)
