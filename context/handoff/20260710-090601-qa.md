# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (housekeeping via `scripts/housekeeping_move_block.py` + merge `ciclo/BLK-RELMUN-06` -> `integracao/map02-relmun05-06`).

## VEREDITO
APROVADO (FINAL — suíte completa autoritativa confirmada pelo orquestrador: 1545 passed, 2 skipped, 1 failed pré-existente/ambiental alheia ao bloco)

## Justificativa
O Builder implementou exatamente o plano: helper puro `_texto_zonas_sintese` retornando os 4 textos APROVADOS no gate D1 (conferidos caractere a caractere), checagem por PERTENCIMENTO de rótulo (Cerco > Flancos laterais > Âncora central > fallback), e substituição SÓ do 3º elemento do card 3 em `_sintese_page` (cor `ULTRA_LARANJA` e VALOR intactos). Escopo READ-ONLY sobre o M1 respeitado; ruff/mypy/import/subconjunto de testes verdes.

## Problemas críticos (bloqueadores)
- Nenhum.

## Problemas médios (não bloqueadores)
- Nenhum.

## Melhorias opcionais
- Os asserts de integração checam LINHAS wrapeadas fixas (pontos de quebra do `multi_cell` a 12 pt). Se a largura do card ou a fonte mudar no futuro, esses asserts quebram por dependerem do ponto de quebra — aceitável hoje, mas frágeis a mudanças de layout. Não bloqueante.

## Testes faltantes
- Nenhum. As 4 combinações (0/1/2/3 zonas) estão cobertas tanto no unit direto do helper quanto na integração via PDF, mais o caso defensivo (só "Flancos laterais", sem "Âncora" → texto de 2 zonas, sem exceção) e a regressão de cards 1/2 + VALOR do card 3.

## Riscos remanescentes
- Revisão VISUAL do PDF (o texto não estourar o card) não foi feita interativamente aqui; porém os 4 textos ficam na faixa 91-108 chars (≤ ao texto atual que já cabia em 3 linhas), e os testes de integração confirmam a renderização em 3 linhas dentro do card — risco baixo.
- Suíte completa CONFIRMADA (ver abaixo): 1545 passed / 2 skipped / 1 failed. A única falha (`test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime`) é PRÉ-EXISTENTE/AMBIENTAL — `read_parquet` de `data/staging/unidade_territorio_retencao.parquet` (camada M2 `lifetime/`, parquet gitignored ausente no ambiente local); ALHEIA ao BLK-RELMUN-06, que não toca `lifetime/`. O +9 vs baseline (1536→1545) são exatamente os 9 testes novos deste bloco.

## Saída literal das validações (re-executadas pelo QA)

### `python -m ruff check .`
```
All checks passed!
```

### `python -m mypy src`
```
src\motor_expansao\dimensionamento\growth_api_client.py:28: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\ibge_censo.py:24: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\poi_enrichment.py:15: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\base_h3_brasil.py:31: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\geo.py:21: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\telegram_bot.py:23: error: Library stubs not installed for "requests"  [import-untyped]
Found 6 errors in 6 files (checked 108 source files)
```
Todos os 6 são pré-existentes (`types-requests` ausente); ZERO em `relatorio_municipal.py` ou no teste; 0 erro novo.

### `python -c "import streamlit_app; print('import ok')"`
```
2026-07-10 09:07:25 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
(...warnings normais de contexto Streamlit fora de runtime...)
import ok
```

### `python -m pytest -q tests/unit/test_relatorio_municipal.py tests/unit/test_relatorio_acentuacao_regressao.py` (SERIAL)
```
50 passed, 2311 warnings in 11.87s
```
Warnings todos de `pytest_asyncio`/DeprecationWarning (Python 3.14 local), não relacionados ao bloco.

### `python -m pytest -q` (suíte COMPLETA, serial — gate único, rodada pelo orquestrador)
```
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 1545 passed, 2 skipped, 243703 warnings in 640.03s
```
Única falha = `test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime` (camada M2 `lifetime/`), por `read_parquet` de `data/staging/unidade_territorio_retencao.parquet` — artefato gitignored ausente no ambiente. PRÉ-EXISTENTE/AMBIENTAL, ALHEIA a este bloco (não toca `lifetime/`). Confirmado por re-execução isolada do QA (mesma FileNotFound do parquet do M2). 1545 passed = baseline 1536 + os 9 testes novos do BLK-RELMUN-06.

### `python scripts/housekeeping_move_block.py BLK-RELMUN-06 --check` (estado PRÉ-move)
```
FALHA --check: stub ausente no backlog para BLK-RELMUN-06
```
Comportamento esperado no estado pré-move (o helper RECONHECE o bloco; falha só porque o move ainda não foi executado — housekeeping é passo pós-QA do orquestrador).

### `git diff --stat`
```
 context/handoff.md                                 | 207 +++++++++++----------
 src/motor_expansao/dashboard/relatorio_municipal.py|  31 ++-
 tasks/current_task.md                              |  58 +++---
 tests/unit/test_relatorio_municipal.py             | 166 +++++++++++++++++
 4 files changed, 335 insertions(+), 127 deletions(-)
```
Só `relatorio_municipal.py` + `test` + bookkeeping (`handoff.md`, `current_task.md`). Zero pipelines/config/censo/artefatos; nenhum parquet.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico. Este ciclo é puramente de display (texto de PDF), sem tooling que dependa de config/artefatos de produção; a suíte usa fixtures legítimas de teste (prática normal, não bypass). Suíte completa é o gate único, rodando no orquestrador.

## Conferência cruzada com log do Builder
Bate. Ruff "All checks passed", mypy 6 pré-existentes/0 novo, import ok, 50 passed serial, git diff --stat restrito aos 4 arquivos — todos idênticos ao reportado pelo Builder. A nota técnica do Builder (asserts por LINHA wrapeada em vez de sentença contígua, por causa do `multi_cell` emitir 1 Tj por linha) é razoável, não-tautológica e não altera o texto REAL renderizado.

## Guardrails verificados
- score_priorizacao não alterado: sim (não aplicável — bloco não toca score; helper só LÊ `result["zonas_geo"]`).
- Artefatos M1 preservados: sim (zero escrita/leitura nova em artefatos oficiais; nenhum parquet no diff).
- Conferência dos 4 textos vs gate D1 (current_task.md): IDÊNTICOS caractere a caractere (0/1/2/3 zonas); acentuação correta; só pontuação ASCII (`,` `.` `:`); nenhum caractere fora de latin-1 nos textos de produção (o em-dash existe apenas na docstring do helper, nunca passa por `_ascii()`/PDF).
- `_sintese_page`: só o 3º elemento do card 3 trocado pela chamada do helper; cor `ULTRA_LARANJA` e VALOR `"{n} zonas de atuação"` inalterados; cards 1 (`ULTRA_MAGENTA`, penetração) e 2 (`ULTRA_TURQUESA`, residual) intocados.
- `_dominio_page`, `_zonas_geometricas`, `_zonas_do_municipio`, `agregar_municipio`, `_hex_destacado_mask`, `dominio_df`, `flag_sam`: não tocados.
- Parâmetros canônicos §3 (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, pesos renda=0.40/pop=0.60): INALTERADOS (não tocados por este bloco).
- Escopo respeitado: sim.
- Validações re-executadas pelo QA (sem bypass): ruff→pass; mypy→6 pré-existentes/0 novo; import→ok; subconjunto pytest→50 passed; housekeeping --check→FALHA esperada pré-move; git diff --stat→4 arquivos.

## Decisão recomendada
Fechar ciclo: housekeeping via `scripts/housekeeping_move_block.py BLK-RELMUN-06` + merge de `ciclo/BLK-RELMUN-06` -> `integracao/map02-relmun05-06` (SEM abrir PR, conforme decisão de branch de Vinicius 2026-07-08 — parar na secundária até os 3 ciclos serem revisados). Suíte completa verde (única falha ambiental/pré-existente do M2, alheia ao bloco). VEREDITO FINAL = APROVADO.
