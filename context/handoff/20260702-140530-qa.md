# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador roda Passo 6.0 via `scripts/housekeeping_move_block.py BLK-TP-06` + commit por path)

## VEREDITO
APROVADO

## Justificativa
O GO é honesto (out-of-fold, R² in-sample banido do veredito) e REPRODUZÍVEL bit-a-bit sobre os parquets reais (R²_oof_log=+0,3119 IC95[+0,2977,+0,3250], rho_oof=+0,4615 IC95[+0,4477,+0,4739], n_join=16.411); a suíte full passa (1241 passed / 1 skipped, +10 dos novos), isolamento/anti-PII/READ-ONLY M1 e mercado confirmados por evidência própria, e o escopo bate exatamente com o plano aprovado (só os 3 arquivos + bookkeeping).

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- O confound #1 da `nota_honesta` cita "16.575 de ~1,54 M" (universo bruto do dump DEC-012) enquanto o `n_join`/cobertura efetiva é 16.411 (~1,06%) — números coerentes (o join casa a maioria dos 16.575), mas as duas cifras aparecem no mesmo relatório sem uma frase ligando-as. Cosmético, não afeta o veredito.

## Testes faltantes
- nenhum bloqueador. Os 10 testes cobrem o núcleo (anti-PII, R² in-sample fora do veredito, IC determinístico R2+rho, anti-inversão alvo/preditor, DEC-009 features, isolamento AST+textual, GO/NO-GO forte×ruído, join inner). Opcional futuro: um teste que exercite o caminho `metodo="loo"`/`kfold_10x5` (hoje só `kfold_5x5` é exercitado pelas fixtures de n=400) — não bloqueia.

## Riscos remanescentes
- R2 (cobertura/viés): o GO vale para o recorte metropolitano (~1,06%, SP/MG/RJ ≈ 51%), NÃO é validação nacional — declarado no relatório e na `nota_honesta`. Aceito.
- R6 (guardrail): a "Proposta de recalibração" é análise/recomendação documentada; aplicá-la em produção exige BLK-TP-07 + DEC + gate. NÃO foi aplicada neste bloco (verificado: `calcular_colunas_mercado.py` intocado; nenhum parquet de mercado regenerado).
- `R²_oof_membros` negativo (−0,0110, espaço cru) é esperado/honesto (modelo ancorado em log) e reportado como auditoria, não decide gate.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte COMPLETA (gate único) — `python -m pytest -n auto -q`
```
1241 passed, 1 skipped, 333490 warnings in 195.55s (0:03:15)
```
0 falhas, 0 erros de coleta. Baseline histórica ~1231 (BLK-TP-04) + 10 novos de `test_calibracao_residual.py` = 1241. Sem flakiness por paralelismo (rodou com `-n auto`, sem `-p no:xdist`).

### 2. Smoke — `python -c "import streamlit_app; print('import ok')"`
```
(warnings de streamlit "missing ScriptRunContext"/"No runtime found" — esperados em bare import)
import ok
```

### 3. Isolamento (AST) — `calibracao_residual.py` não importa `pipelines.m1`/`dashboard`/`censo`/`api`/`config`
```
all imports: ['__future__', 'contrato', 'dataclasses', 'logging', 'motor_expansao.dimensionamento.aderencia', 'motor_expansao.dimensionamento.backtest_dim', 'numpy', 'pandas', 'pathlib', 're', 'scipy.stats', 'sklearn.linear_model', 'sklearn.model_selection', 'typing']
bad imports: []
config imported: False
```

### 4. mtime dos 4 artefatos oficiais M1 + mercado (BEFORE == AFTER, e após o repro run)
```
brasil_estrutural: 1780501621
brasil_priorizados: 1780501631
hexagonos_brasil_oportunidades: 1780501655
hexagonos_brasil_dashboard: AUSENTE (artefato derivado grande não presente neste checkout — trivialmente inalterado)
mercado (hexagonos_mercado_mapeado.parquet): 1781205056
```
mtimes idênticos aos do log do Builder e INALTERADOS após a re-execução do `calibrar_residual_demanda` sobre os parquets reais (leitura pura). `git status --porcelain | grep parquet` → "no parquet in git status".

### 5. Lint/type — `ruff check` + `mypy` nos 2 arquivos novos
```
=== RUFF ===
All checks passed!
=== MYPY ===
tests\unit\demanda_revelada\test_calibracao_residual.py:173: note: By default the bodies of untyped functions are not checked, consider using --check-untyped-defs  [annotation-unchecked]
Success: no issues found in 2 source files
```

### 6. Reprodução do número (evidência própria, `calibrar_residual_demanda` sobre os parquets REAIS)
```
veredito: GO tipo_go: ancora_r2
r2_oof_log: 0.3119 IC95: [0.2977, 0.325]
rho_oof: 0.4615 IC95: [0.4477, 0.4739]
r2_insample(auditoria): 0.3121
r2_oof_membros: -0.011
n_join: 16411 cobertura%: 1.0639 metodo: kfold_5x5 alpha: 100.0
top-3 UF: {'SP': 29.1, 'MG': 12.7, 'RJ': 9.2}
```
Bate 4 casas com o log do Builder (determinístico, seed=42).

### 7. Anti-PII do relatório e do módulo
```
PII tokens present in .md: NONE
03_Competidores.xlsx: só citado como adiado (R4=A) — nenhum read_excel/.xlsx executado no módulo
NAO_ABRA: referenciado só em docstring/comentário — fonte real nunca tocada
```

### 8. R² in-sample NÃO entra no veredito (grep)
`r2_insample` aparece em: docstring (l.95), campo do dataclass (l.138), cálculo (l.569), atribuição (l.608), e 2 linhas de relatório/nota com o rótulo literal `"apenas auditoria -- NAO usar como desempenho"` (l.684, l.800). O bloco de veredito (l.574–592) usa SOMENTE `r2_oof_log`/`ic95_r2`/`rho_oof`/`ic95_rho` — R² in-sample banido do gate (DEC-008).

### 9. §5 READ-ONLY — fórmula do residual intocada
```
git status --porcelain src/motor_expansao/pipelines/calcular_colunas_mercado.py → (vazio, clean)
git status --porcelain src/motor_expansao/demanda_revelada/ → só ?? calibracao_residual.py (novo)
```

### 10. Housekeeping
```
tests/unit/test_housekeeping_helper.py → 10 passed
### BLK-TP-06 presente em tasks/backlog.md (linha 919), íntegro
### BLK-TP-06 ausente em tasks/completed.md (move a cargo do orquestrador — Passo 6.0)
```

## Conferência de no-bypass
Confirmo que NENHUMA validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico. O gate GO foi reproduzido pelo próprio QA sobre os parquets REAIS de produção (`demanda_revelada_h3.parquet` × `hexagonos_mercado_mapeado.parquet`), não sobre fixture. As fixtures sintéticas dos testes unitários são uso legítimo (isolar lógica GO/NO-GO forte×ruído), não bypass — este é módulo de modelagem, não tooling que depende de config real de produção.

## Conferência cruzada com log do Builder
Bate integralmente. Suíte: Builder rodou só o subconjunto (231 passed); QA rodou a full (1241 passed / 1 skipped) — gate único. Números GO idênticos (R²_oof_log +0,3119, IC95 [+0,2977,+0,3250]; rho_oof +0,4615, IC95 [+0,4477,+0,4739]; R²_insample +0,3121; R²_oof_membros −0,0110; secundário +0,2610; n_join 16.411; cobertura 1,06%; alpha 100; top-3 UF SP/MG/RJ). mtimes idênticos. Isolamento `bad imports: []` idêntico. Ruff/mypy idênticos.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; pesos renda=0.40/pop=0.60 e fórmula intocados; módulo não importa `pipelines.m1`/`config`).
- Artefatos M1 preservados: sim (mtime dos 3 presentes idêntico antes/depois/pós-repro; 4º ausente no checkout; nenhum parquet no `git status`).
- Artefato de mercado preservado: sim (`hexagonos_mercado_mapeado.parquet` mtime inalterado; `calcular_colunas_mercado.py` clean no git — fórmula do `score_oportunidade_residual` não tocada).
- DEC-008 honesto: sim (k-fold 5×5 seed=42 vs baseline da média; IC95 bootstrap 2000 seed=42 para R² e rho; R² in-sample só auditoria rotulada, fora do veredito; flag_extrapolacao; NO-GO seria válido — testado por `join_ruido`).
- DEC-009: sim (`membros` = ALVO observado; nunca feature; teste anti-inversão + asserção de FEATURES).
- DEC-012 anti-PII: sim (só camada agregada; `COLUNAS_PII_PROIBIDAS` ausentes no `.md`/result; `_assert_sem_pii_no_relatorio` presente; fixtures sintéticas; NAO_ABRA/03_Competidores.xlsx não tocados).
- Aderência ao gate: sim (R4=A sem ingestão da planilha; alvo log1p(membros); GO/NO-GO R²-âncora + rho-suporte; membros=alvo único).
- Escopo respeitado: sim (só `calibracao_residual.py` [novo], `test_calibracao_residual.py` [novo], `calibracao_residual_demanda.md` [gitignored, não aparece no git] + handoffs/bookkeeping; nenhum arquivo fora do plano).
- Validações re-executadas pelo QA (sem bypass): suíte full → 1241 passed/1 skipped; import streamlit_app → ok; isolamento AST → bad imports []; mtime M1/mercado → inalterado; ruff/mypy → limpos; repro GO → bate 4 casas; anti-PII → NONE; housekeeping helper → 10 passed; BLK-TP-06 íntegro no backlog.

## Decisão recomendada
Fechar ciclo. Orquestrador roda Passo 6.0 (`scripts/housekeeping_move_block.py BLK-TP-06`) + commit por path dos 3 arquivos do ciclo + bookkeeping. FOLLOW-UPs registrados: BLK-TP-07 (aplicar recalibração com DEC+gate) e BLK-TP-08 (ingestão anti-PII de `03_Competidores.xlsx`).
