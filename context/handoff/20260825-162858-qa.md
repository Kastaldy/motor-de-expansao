# Handoff — QA/Quality Analyzer

> Gerado em 2026-08-25, na branch `ciclo/BLK-MA-21`. Cópia append-only em
> `context/handoff/20260825-162858-qa.md`. Substitui o handoff do Builder (snapshot preservado em
> `context/handoff/20260825-153631-builder.md`).

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (Passo 6 do orquestrador — modo **merge-humano**, criticidade Alta)

## VEREDITO
**APROVADO COM RESSALVAS**

## Justificativa
Re-executei por conta própria todas as validações obrigatórias contra a config e os artefatos reais:
a suíte COMPLETA passou (**3681 passed / 34 skipped / 0 failed**, serial), e provei de forma
independente — sem reusar fixture do Builder — os quatro pontos onde o dano seria irreversível: a
partição de duas chaves, a migração destrutiva, a integridade da partição viva `semana=2026-33`
(hash SHA-256 idêntico antes e depois de toda a bateria) e a fronteira D9 de ponta a ponta. Nenhum
problema **crítico**. As ressalvas são três problemas **médios** e nenhum deles bloqueia o merge: o
mais relevante é que a fronteira D9, que a DEC-038 declara "imposta por CÓDIGO", depende de um flag
que **duas das três instruções canônicas do repositório não passam** — inócuo hoje, defeito no dia em
que o cron deste bloco gravar a primeira folha `fonte=totalpass`.

## Problemas críticos (bloqueadores)
nenhum

## Problemas médios (não bloqueadores)

1. **A fronteira D9 depende de um flag que o comando canônico não passa.** A DEC-038 (D9) decide que
   o recorte do TotalPass é *imposto por código*, e o mecanismo existe e funciona — provei de ponta a
   ponta numa série sintética de duas fontes: `--fontes wellhub` derruba o entregável de **19.329
   para 9.659 academias** (6.753 → 4.841 hexes). Mas ele só age se o flag for passado, e:
   - `docs/infra_producao.md:468` — o bloco copiável "**Gerar — na estação, nunca na VPS**" da seção
     BLK-MA-19 invoca `alvos_ma` **sem** `--fontes wellhub`, **24 linhas depois** de a seção nova do
     BLK-MA-21 dizer em prosa que "o entregável roda `--fontes wellhub`";
   - `scripts/check_artifacts.py:143` — a dica impressa quando os pins somem do Mapa Territorial, idem.

   **Impacto:** hoje inócuo (a série em disco tem só `wellhub`). Vira defeito silencioso na primeira
   execução mensal com TotalPass — exatamente o "fora de escopo, explicitamente" do bloco. Registrado
   como **BLK-MA-21-FU1**.

2. **O ramo que IMPÕE o recorte não tem teste.** `test_cli_aceita_recorte_de_fontes` cobre só o
   `argparse`; `test_recorte_de_fontes_e_prosa_sem_o_flag` cobre só `inspect.signature`. O ramo de
   `alvos_ma.main()` que lê a série recortada e a **injeta** nos dois extratores (`snapshots=[serie]`)
   não é exercitado por teste nenhum. Provei-o à mão (ver saídas abaixo); não há guarda de regressão.

3. **`ler_snapshots(fontes=)` valida DEPOIS da saída antecipada.** Com base inexistente ou sem
   partição, `ler_snapshots(base, fontes=["wellub"])` devolve frame **vazio** em vez de levantar — e
   esse é precisamente o estado da VPS hoje (zero partições, achado A1 do Planner). O próprio teste
   `test_ler_snapshots_recorta_por_fonte` promete que "o erro de digitação tem de falhar alto", e a
   promessa não vale nesse caso. Medido:
   `[ATENCAO] base SEM particao + fonte INVALIDA -> nao levanta; devolve 0 linhas`.

## Melhorias opcionais

- **`contrato.py:4` ficou com o caminho ANTIGO da partição** (`semana=AAAA-SS/parte-*.parquet`) no
  docstring do módulo, enquanto `snapshots.py:4` e os dois docs foram atualizados. É o arquivo que se
  declara "fonte única de verdade do schema" — vale corrigir a linha.
- **`curar()` publica o TotalPass ANTES de abortar pela ambiguidade do WellHub.** O laço é
  `("totalpass", "wellhub")`, então no estado ambíguo os CSVs do TotalPass já foram copiados para o
  destino quando a exceção sobe. Inócuo (o wrapper sai != 0, o snapshot não roda, e o `copy2`
  preserva o mtime para a guarda do mês seguinte), mas não está escrito no docstring.
- **`prompts/builder.md` (linhas 45/46/49) e `prompts/qa_analyzer.md` (linha 25) estão STALE:** exigem
  `import streamlit_app` e `tests/integration/test_streamlit_app.py`, ambos removidos em `30378a0`
  ("corte(BLK-WEB-20): remove o Streamlit", DEC-022). Confirmei por conta própria que o módulo não
  existe. A substituição do Builder é **legítima** e foi tratada como validação executada, não pulada.
- **`ruff format --check` passou a acusar `tests/unit/vulnerabilidade/test_snapshots.py`**, que estava
  limpo na `origin/main` (3 quebras de linha). `snapshots.py` já estava fora do format na `origin/main`
  — esse **não** é do ciclo. Nenhum dos dois é gate do CI (`ci.yml:54` roda só `ruff check .`).
- **`docs/decisions/README.md` pula de DEC-036 para DEC-038:** a linha da **DEC-037 nunca foi
  acrescentada**. É bookkeeping de outro bloco; o Builder registrou e **corretamente não corrigiu**.
- **Acentuação, consistência:** as linhas de índice da DEC-038 no `CLAUDE.md` §8 e no
  `docs/decisions/README.md` estão SEM acento, enquanto o corpo da `DEC-038.md` está acentuado (e as
  vizinhas DEC-035/036 também). As mensagens de erro de `curadoria_agregadores.py` também estão sem
  acento — mas isso **segue a convenção do próprio `snapshots.py`** ("fonte fora do contrato",
  "particao existente PRESERVADA"), e o `healthcheck_vps.sh` mantém a sua (mensagens de Telegram
  acentuadas). Nenhum identificador acentuado foi introduzido (medido: **0**).

## Testes faltantes

- **Teste do ramo de recorte de `alvos_ma.main()`** (médio 2 acima) — o que prova a fronteira D9.
- **O teste de AST (T3) cobre `ds.dataset`, não `ds.write_dataset`.** O modo de falha destrutivo — a
  segunda execução da semana apagando a folha da primeira — vem do **ESCRITOR**. Hoje os dois
  escritores usam `_particionamento_hive()`, mas um `write_dataset` futuro que não use o helper passa
  despercebido pela varredura. T3 assere `_particionamento_hive()`, o que mitiga mas não fecha.
- Teste para `ler_snapshots(fontes=inválida)` sobre base **sem partição** (médio 3 acima).

## Riscos remanescentes

1. **Aplicação na VPS é MANUAL e a dependência dura NÃO está satisfeita.** O BLK-MA-06 segue
   "aguardando aplicação na VPS": a série lá tem **zero partições** e o `DRY_RUN` que valida o caminho
   dos CSVs nunca rodou. Não bloqueia o merge (é tudo código + teste); **bloqueia a aplicação**.
2. **Imagem antiga na VPS escrevendo com UMA chave apaga a folha do mensal.** Mitigado pela ordem de
   aplicação em 6 passos do runbook e por `versao_contrato` na auditoria do `DRY_RUN` (se vier `v3`,
   não agendar). O risco só some quando o passo 1 (imagem nova) for feito.
3. **O clone `../GymScraping` está no estado AMBÍGUO de D3 nesta estação** — confirmado por mim:
   `Wellhub/csvs` é 11 dias mais novo (`2026-08-18T18:29:19Z`) que `Wellhub/csvs_musculacao`
   (`2026-08-07T19:06:09Z`, as 22.173 linhas que batem com a partição viva). A curadoria **aborta, com
   os dois caminhos e as duas datas na mensagem, e diz o que fazer** ("Confira de que modo o coletor
   rodou e apague o diretorio obsoleto a mao"). É o comportamento aprovado em D3 e está correto —
   mas **exige ação humana antes da primeira execução mensal**. O runbook explica o *porquê* da
   ambiguidade abortar; a instrução de remediação vive só na mensagem de erro.
4. **O wrapper nunca foi executado** (exige Docker, o clone e ~21h de coleta). O que está travado é o
   contrato textual (23 testes) + `bash -n`; a validação de execução é o `DRY_RUN=1` do Felipe.
5. **Bump com série viva.** `semana=2026-33` (22.173 linhas, `v3`, layout legado de 1 chave) **não foi
   regerada** e continua legível sob o leitor novo — provado por mim, com `fonte` íntegra (0 nulos) e
   `fontes_lidas` nula nas 22.173 linhas. Depois de `--migrar-layout` ela passa a viver em
   `fonte=wellhub/`; até lá, a escrita naquela semana é **recusada** (D8).
6. **`pytest -n auto` não roda nesta estação** — falha de INFRAESTRUTURA do execnet/xdist, não
   flakiness de teste (detalhe abaixo). A suíte rodou serial, sem `-p no:xdist`.

---

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte COMPLETA — gate único do ciclo

**Tentativa 1 — `-n auto` (exigido pelo prompt):**
```
$ python -m pytest -n auto -q
INTERNALERROR>   File ".../xdist/dsession.py", line 90, in pytest_sessionstart
INTERNALERROR>     nodes = self.nodemanager.setup_nodes(putevent=self.queue.put)
INTERNALERROR>   File ".../execnet/gateway_io.py", line 34, in __init__
INTERNALERROR>     self.popen = p = execmodel.subprocess.Popen(args, stdout=PIPE, stdin=PIPE)
INTERNALERROR>   File ".../subprocess.py", line 1430, in _make_inheritable
INTERNALERROR>     h = _winapi.DuplicateHandle(
INTERNALERROR> OSError: [WinError 6] Identificador inválido
```

**Tentativa 2 — `-n auto` com handles reais (`Start-Process`), só coleta:** FUNCIONA.
```
EXITCODE=0
3714 tests collected in 111.70s (0:01:51)
```

**Tentativa 3 — `-n auto` com handles reais, suíte completa:**
```
INTERNALERROR>   File ".../subprocess.py", line 1430, in _make_inheritable
INTERNALERROR>     h = _winapi.DuplicateHandle(
INTERNALERROR> OSError: [WinError 50] Não há suporte para o pedido
```

**Tentativa 4 — `-n 4`, sem redirecionar stderr:**
```
EXITCODE=3
INTERNALERROR>   File ".../execnet/gateway_base.py", line 571, in from_io
INTERNALERROR>     raise EOFError("couldn't load message header, " + e.args[0]) from None
INTERNALERROR> EOFError: couldn't load message header, expected 9 bytes, got 0
```

> **Diagnóstico:** três invocações distintas, três erros distintos (`WinError 6`, `WinError 50`,
> `EOFError`), **todos em `pytest_sessionstart`, no `makegateway` do execnet — antes de qualquer
> teste rodar**. É falha de INFRAESTRUTURA do xdist nesta estação Windows, não teste mal-isolado:
> nenhum teste chegou a ser executado nas três tentativas. Confirma o achado (3) do Builder. **Não
> usei `-p no:xdist`.**

**Execução válida — SERIAL:**
```
$ python -m pytest -q
3681 passed, 34 skipped, 828898 warnings in 2016.04s (0:33:36)
```
```
$ python -m pytest --collect-only -q
3714 tests collected in 258.26s (0:04:18)
```
> Baseline do ciclo: **3658** coletados. Agora **3714** (+56 = 19 da curadoria + 23 dos wrappers +
> 12 T1..T9 em `test_snapshots.py` + 2 do D9 em `test_alvos_ma.py`). Executados 3715 (3681+34) — o
> delta de 1 entre coletado e executado é artefato de coleta, **sem nenhuma falha associada**.
> `0 failed, 0 error`.

### 2. Conferência cruzada com os recortes do Builder
```
$ python -m pytest -q tests/unit/vulnerabilidade/ tests/unit/test_wrappers_cron_agregadores.py \
    tests/contracts/ tests/unit/test_claude_md_size.py
826 passed, 5 skipped, 53403 warnings in 119.00s (0:01:58)
```
```
$ python -m pytest -q tests/unit/test_check_artifacts.py tests/unit/test_direcao_score_na_tela.py \
    tests/unit/test_loop_guard_paths.py tests/unit/test_paridade_sinais_web.py \
    tests/unit/test_piloto_web_{api,endpoints,independentes,redes}.py
245 passed, 8090 warnings in 29.41s
```
> **Bate exatamente** com o log do Builder (826/5 e 245).

### 3. `ruff`
```
$ python -m ruff check .
All checks passed!

$ python -m ruff format --check <7 arquivos do ciclo>
Would reformat: src\motor_expansao\vulnerabilidade\snapshots.py
Would reformat: tests\unit\vulnerabilidade\test_snapshots.py
2 files would be reformatted, 5 files already formatted

$ python -m ruff format --check <os MESMOS 2 arquivos, vindos de origin/main>
Would reformat: .../qa_orig/snapshots.py
1 file would be reformatted, 1 file already formatted
```
> Confirmado: `snapshots.py` **já** estava fora do format na `origin/main` (pré-existente);
> `test_snapshots.py` estava limpo e **saiu do format neste ciclo** (3 quebras de linha).
> `ci.yml:53-54` roda só `ruff check . --no-cache` — não é gate.

### 4. Shell: sintaxe e EOL
```
$ bash -n scripts/cron/run_snapshot_agregadores.sh   -> sintaxe OK
$ bash -n scripts/healthcheck_vps.sh                 -> sintaxe OK

$ python -c "<contagem de bytes CRLF/LF>"
  scripts/cron/run_snapshot_agregadores.sh                       CRLF=0  LF=230  -> LF puro
  scripts/healthcheck_vps.sh                                     CRLF=0  LF=217  -> LF puro
  src/motor_expansao/vulnerabilidade/curadoria_agregadores.py    CRLF=0  LF=346  -> LF puro
  docs/decisions/DEC-038.md                                      CRLF=0  LF=231  -> LF puro

$ git check-attr -a scripts/cron/run_snapshot_agregadores.sh
  scripts/cron/run_snapshot_agregadores.sh: text: set
  scripts/cron/run_snapshot_agregadores.sh: eol: lf
```
**Guarda de dia do mês da linha de crontab (`[ "$(date +\%d)" -le 07 ]`), testada contra o octal:**
```
bash: date=+01 -> TRUE      sh: 08 -> FALSE rc=1
bash: date=+07 -> TRUE      sh: 09 -> FALSE rc=1
bash: date=+08 -> FALSE(rc=1)
bash: date=+10 -> FALSE(rc=1)
```
> `08`/`09` **não** são lidos como octal por `test`: a guarda casa exatamente os dias 1-7 (a 1ª terça).

### 5. `loop_guard`
```
$ python scripts/loop_guard.py --base origin/main --json
{"limpo": false, "violacoes": [{"path": "tasks/backlog.md", "classe": "governanca",
 "motivo": "backlog - marcador de autonomia/criticidade dos blocos"}]}
```
> **Nenhum achado CRÍTICO.** Único achado é `governanca` sobre o `backlog.md` — esperado (o Builder
> mudou o Status; eu acrescentei o BLK-MA-21-FU1). Re-rodado DEPOIS da minha edição: idêntico.

### 6. Smokes de import (substituindo o `import streamlit_app`, que é STALE)
```
$ ls streamlit_app.py            -> No such file or directory
$ python -c "import streamlit_app" -> ModuleNotFoundError: No module named 'streamlit_app'
$ git log --oneline -1 --all -- streamlit_app.py
  30378a0 corte(BLK-WEB-20): remove o Streamlit — o piloto web e o unico app (DEC-022)

$ python -c "import motor_expansao.vulnerabilidade; from motor_expansao.vulnerabilidade import
             curadoria_agregadores, snapshots, contrato, alvos_ma"
import ok - vulnerabilidade + curadoria_agregadores + snapshots + contrato + alvos_ma
  migrar_layout_particoes exportada: True

$ (cd web/server && python -c "import app")
import ok - web/server/app.py (app de producao, DEC-022)
```

### 7. VERIFICAÇÃO INDEPENDENTE dos pontos de dano irreversível
> Script próprio do QA, fixtures próprias, sem reusar nada do Builder.

**V1 — duas fontes coexistem na mesma semana (a colisão de partição):**
```
  apos gravar wellhub:   ['semana=2026-33/fonte=wellhub/parte-0.parquet']
  apos gravar totalpass: ['semana=2026-33/fonte=totalpass/parte-0.parquet',
                          'semana=2026-33/fonte=wellhub/parte-0.parquet']
  ler_snapshots -> 3 linhas; fontes: ['totalpass', 'wellhub']; nulos em `fonte`: 0
  colunas: 14 (13 do contrato + semana)
  [OK] wellhub SOBREVIVEU a escrita do totalpass
  -- reescrita PARCIAL --
   -> 2 linhas; {'totalpass': 1, 'wellhub': 1}
  [OK] folha encolheu de 2->1 e a irma sobreviveu
```

**V2 — o defeito do leitor de UMA chave, reproduzido:**
```
  leitor de 1 chave: 2 linhas; nulos em `fonte`: 2
  [OK] defeito REPRODUZIDO (silencioso, sem excecao)
  [OK] ler_snapshots de producao: 0 nulos
```
**Varredura própria por leitores da série (não só o teste de AST):**
```
$ grep -rn "ds.partitioning|pq.ParquetDataset|snapshots_concorrentes" --include=*.py src/ scripts/ web/ dashboard/
src/motor_expansao/pipelines/m1/fase1_bi_exports.py:604:  partitioning=ds.partitioning(... ("uf", ...))   <- M1, outra arvore
src/motor_expansao/vulnerabilidade/snapshots.py:765:      return ds.partitioning(                          <- _particionamento_hive
scripts/check_artifacts.py:144:                           (so' imprime uma dica; nao le a serie)
```
> **Nenhum outro caminho do repositório lê a série** — nem com 1 chave, nem com `pd.read_parquet`.

**V3 — `migrar_layout_particoes()` (código que APAGA arquivo):**
```
  legado: ['semana=2026-30/parte-0.parquet']
  dry_run -> ['2026-30'] | arvore: ['semana=2026-30/parte-0.parquet']
  [OK] dry-run NAO tocou o disco
  migrado -> ['2026-30'] | arvore: ['semana=2026-30/fonte=wellhub/parte-0.parquet']
  [OK] legado convertido, 2 linhas preservadas, legado removido
  -- ESTADO AMBIGUO --
  [OK] levantou: estado ambiguo em semana=2026-30: arquivo legado e folha `fonte=` da(s)
       mesma(s) fonte(s) ['wellhub'] coexistem; resolva a mao antes de migrar
   arvore intacta apos a excecao: ['semana=2026-30/fonte=wellhub/parte-0.parquet',
                                   'semana=2026-30/parte-0.parquet']
```
> Li o corpo da função: `ds.write_dataset(...)` vem **antes** de `arquivo.unlink()`. Se a escrita
> falhar, o legado permanece e a série segue legível pelo caminho misto.

**V4 — a escrita RECUSA semana com layout legado (D8):**
```
  [OK] recusou: particao legada de 1 chave em semana=2026-33 (1 arquivo(s) solto(s));
       rode --migrar-layout antes de gravar
```

**V5 — série MISTA (legado `v3` sem `fontes_lidas` + folha nova):**
```
  linhas: 2 | fontes: ['totalpass', 'wellhub'] | nulos em `fonte`: 0
  fontes_lidas por semana: {'2026-30': None, '2026-34': 'totalpass'}
  [OK] v3 legivel; `fontes_lidas` nula na safra antiga
```

**V6 — `ler_snapshots(fontes=)`:**
```
  sem flag: ['totalpass','unidades','wellhub'] | com fontes=['wellhub']: ['wellhub']
  [OK] fonte invalida levanta: fonte fora do contrato: ['wellub']; aceitas: [...]
  [ATENCAO] base SEM particao + fonte INVALIDA -> nao levanta; devolve 0 linhas   <- médio 3
```

**V7 — guarda de frescor (D4) NO LIMITE:**
```
  [OK] idade=  0.0 dia(s) -> fresco=True (medido 0.000)
  [OK] idade=  6.9 dia(s) -> fresco=True (medido 6.900)
  [OK] idade=  7.0 dia(s) -> fresco=True (medido 7.000)   <- limiar inclusivo
  [OK] idade= 7.01 dia(s) -> fresco=False (medido 7.010)
  [OK] idade= 30.0 dia(s) -> fresco=False
  diretorio SEM csv -> (False, None)  [OK] nunca fresco
```

**V8 — CURADORIA NO CLONE REAL (caminho de produção, sem bypass):**
```
  clone: C:\Users\Vinicius Cruz\Downloads\Projetos\GymScraping  existe: True
  totalpass -> (.../GymScraping/TotalPass/csvs, "TotalPass so' emite `csvs/` ...")
  [OK] wellhub levanta (estado ambiguo D3):
      WellHub em estado ambiguo: `csvs_musculacao/` existe mas e' MAIS ANTIGO que `csvs/`, o que
      indica que a ultima coleta rodou noutro modo. Os dois universos diferem por 2-3x, e escolher
      em silencio produz um numero maior, nao um erro. .../Wellhub/csvs_musculacao (mais novo:
      2026-08-07T19:06:09+00:00) x .../Wellhub/csvs (mais novo: 2026-08-18T18:29:19+00:00).
      Confira de que modo o coletor rodou e apague o diretorio obsoleto a mao.
  [OK] curar() ABORTA a execucao inteira: ValueError
```
> **Mensagem correta e acionável:** nomeia os dois caminhos, as duas datas, a razão e o que fazer.

**V9 — caminho FELIZ da curadoria (clone sintético):**
```
  fontes_publicadas = ['totalpass', 'wellhub']
  wellhub origem   = Wellhub/csvs_musculacao
  wellhub n_linhas = 3   (3 = musculacao; 10 = csvs SEM filtro)  <- escolheu o subset certo
  destino: ['totalpass/csvs/sp.csv', 'wellhub/csvs/sp.csv']      <- minusculo
  [OK] copy2 preservou o mtime (insumo da guarda do mes seguinte)
  [OK] CSV orfao no destino SOBREVIVEU  (nunca apaga no destino)
  [OK] destino NAO foi criado em dry-run
```

**V10 — poda sobre a árvore de 2 chaves (a função que apaga diretório):**
```
  antes: ['semana=2026-01' ... 'semana=2026-05']
  dry_run removeria: ['2026-01','2026-02'] | arvore intacta
  removidas: ['2026-01','2026-02'] | restam: ['2026-03','2026-04','2026-05']
  folhas preservadas na sobrevivente: ['fonte=unidades', 'fonte=wellhub']
  serie legivel: 6 linhas; {'unidades': 3, 'wellhub': 3}
```

**V11 — a PARTIÇÃO VIVA `semana=2026-33` (insumo de produção):**
```
  linhas: 22173 | colunas: 14
  fontes: ['wellhub'] | nulos em `fonte`: 0
  semanas: ['2026-33'] | versao_contrato: ['snapshots_concorrentes_v3']
  fontes_lidas nulos: 22173 de 22173   (esperado: TODOS, safra pre-v4)
  arquivos: ['semana=2026-33/parte-0.parquet']   (layout legado, intacto)
```
**Hash SHA-256 da árvore, ANTES e DEPOIS de toda a bateria do QA (inclusive a suíte completa):**
```
b8812d584f126c5edf60d83317faf2ccd9e99deb748dec4c1d537ca5809335be *.../semana=2026-33/parte-0.parquet
8f942220cb7a0f2924aa7bd1b1efe511573abc4306ba76f3d0523f86e0ca8e8d *- (agregado)   <- IDENTICO
$ git status --porcelain -- data/     (vazio)
```

### 8. FRONTEIRA D9 provada DE PONTA A PONTA (o ramo que não tem teste)

**Contra a série REAL (`wellhub`-only) — com e sem o flag, resultado idêntico:**
```
$ python -m motor_expansao.vulnerabilidade.alvos_ma --base-dir data/staging/snapshots_concorrentes \
    --fontes wellhub --dry-run
INFO:__main__:recorte de fontes ATIVO (wellhub): 22173 linha(s) na serie
INFO:__main__:dry-run: nada gravado. {'academias': 19329, 'hexes_no_csv': 6753, 'linhas_csv': 6753,
              'regimes': 1, 'academias_proximas_de_hex_quente': 947, 'dry_run': True}

$ python -m motor_expansao.vulnerabilidade.alvos_ma --base-dir data/staging/snapshots_concorrentes \
    --dry-run
INFO:__main__:dry-run: nada gravado. {'academias': 19329, 'hexes_no_csv': 6753, 'linhas_csv': 6753,
              'regimes': 1, 'academias_proximas_de_hex_quente': 947, 'dry_run': True}
```

**Contra uma série SINTÉTICA de DUAS fontes (metade relabelada como `totalpass`), em `tmp`:**
```
folhas: ['fonte=totalpass', 'fonte=wellhub']
SEM recorte: {'totalpass': 11087, 'wellhub': 11086}
COM recorte: {'wellhub': 11086}

$ alvos_ma --base-dir <sintetica> --sem-pressao --dry-run
{'academias': 19329, 'hexes_no_csv': 6753, 'linhas_csv': 6753, 'regimes': 1,
 'academias_proximas_de_hex_quente': 947, 'dry_run': True}

$ alvos_ma --base-dir <sintetica> --fontes wellhub --sem-pressao --dry-run
INFO:__main__:recorte de fontes ATIVO (wellhub): 11086 linha(s) na serie
{'academias': 9659, 'hexes_no_csv': 4841, 'linhas_csv': 4841, 'regimes': 1,
 'academias_proximas_de_hex_quente': 458, 'dry_run': True}
```
> **Provado:** sem o flag, comportamento idêntico ao de antes; com o flag, o TotalPass sai de fato do
> entregável (19.329 → 9.659 academias). O mecanismo funciona. O que falta é o flag chegar às
> instruções canônicas (médio 1).

### 9. Acentuação (§2 do CLAUDE.md)
```
Identificadores ACENTUADOS suspeitos: 0
(varredura das linhas ADICIONADAS de .py/.sh: atribuicoes, chaves de dict, colunas de df, flags de CLI)

Strings acentuadas em linhas de codigo: 11 — TODAS mensagens de operador
  (alertas de Telegram do healthcheck e mensagens de assert do pytest).
```

### 10. Housekeeping (modo MERGE-HUMANO)
```
$ python scripts/housekeeping_move_block.py BLK-MA-21 --check
FALHA --check: stub ausente no backlog para BLK-MA-21          <- ESPERADO (o move e' Passo 6.0)

$ grep -n "^### BLK-MA-21" tasks/backlog.md
1850:### BLK-MA-21 — Cron MENSAL dos agregadores: o relógio dos independentes, que nunca foi ligado
2059:### BLK-MA-21-FU1 — A fronteira D9 depende de um flag que o comando canônico não passa

$ grep -n "BLK-MA-21" tasks/completed.md
(vazio — correto, o move ainda nao aconteceu)

Simulacao do corte do helper: cortaria as linhas 1850..1993 (proxima heading `### BLK-MA-07`, 1994).
BLK-MA-21-FU1 esta FORA do corte: True
```

---

## Conferência de no-bypass

**Confirmo que nenhuma validação usou bypass.** Especificamente:

- A **curadoria** foi rodada contra o clone **REAL** `../GymScraping`, não só contra fixture sintética
  — e foi ali que o comportamento aprovado em D3 (abortar) se manifestou. O clone sintético foi usado
  **em adição**, para exercitar o caminho feliz que o clone real não permite hoje.
- A **partição viva de produção** (`semana=2026-33`, 22.173 linhas) foi lida pela função de produção
  `ler_snapshots`, não por um mock. O `alvos_ma` foi rodado contra ela pelo `python -m` real.
- Os **dois wrappers de shell** foram checados com `bash -n` real e com a leitura byte a byte do EOL;
  a linha de crontab foi testada contra o `test` real do bash **e** do `sh`.
- O `-n auto` foi tentado **quatro vezes**, em duas vias diferentes de I/O, antes de eu concluir que
  a falha é do runner. **Não usei `-p no:xdist`.** A prova de que é infraestrutura e não flakiness:
  as três falhas ocorrem em `pytest_sessionstart`/`makegateway`, **antes de qualquer teste rodar** —
  e a mesma invocação com `--collect-only` (que não abre gateway de execução) **termina com exit 0**.
- **Ressalva declarada:** o wrapper `run_snapshot_agregadores.sh` **não foi executado** — exige Docker,
  o clone e ~21h de coleta, e o §6 proíbe comando na VPS. O que foi validado dele é contrato textual
  (23 testes) + `bash -n` + a semântica da linha de cron. Isso é limite físico, não bypass, e está
  declarado como risco remanescente (4).
- A regra de no-bypass **não** se aplica às fixtures de `tests/` — uso legítimo, como o prompt define.

## Conferência cruzada com log do Builder

**BATE.** Item a item:

| afirmação do Builder | minha medição |
|---|---|
| 826 passed / 5 skipped no recorte impactado | **826 passed, 5 skipped** ✅ |
| 245 passed nos 8 consumidores adjacentes | **245 passed** ✅ |
| `ruff check .` limpo | **All checks passed!** ✅ |
| `bash -n` OK nos 2 shells, em LF | **OK, CRLF=0 nos dois** ✅ |
| `loop_guard` sem CRÍTICO, 1 `governanca` | **idêntico** ✅ |
| hash da árvore de snapshots idêntico | **idêntico** (`8f942220…`, medido por mim) ✅ |
| clone no estado ambíguo de D3, 11 dias | **confirmado**, `2026-08-07` × `2026-08-18` ✅ |
| `pytest -n auto` morre com INTERNALERROR | **reproduzido 3×**, em 2 vias de I/O ✅ |
| `streamlit_app` não existe (DEC-022) | **confirmado**, removido em `30378a0` ✅ |
| DEC-037 ausente do `docs/decisions/README.md` | **confirmado** (índice pula 036 → 038) ✅ |
| `snapshots.py` já fora do `ruff format` na main | **confirmado** — mas `test_snapshots.py` **não** estava, e saiu do format neste ciclo (o Builder não citou) ⚠️ |

Divergência única: a nota do Builder sobre `ruff format` (item 5 dos "Problemas encontrados") atribui
**todo** o desvio a arquivos pré-existentes. Um dos dois arquivos (`test_snapshots.py`) estava limpo
na `origin/main` e saiu do format neste ciclo. É cosmético e não é gate do CI.

## Guardrails verificados

- **`score_priorizacao` não alterado:** **sim.** `git status --porcelain -- src/motor_expansao/config.py
  src/motor_expansao/pipelines/` **vazio**; `loop_guard --base origin/main` sem achado CRÍTICO.
  Parâmetros canônicos conferidos em runtime: `H3_RESOLUTION = 7`, `DIST_MIN_ULTRA_KM = 1.0`,
  `RENDA_MIN = 4500.0`, `AREA_MIN_M2 = 1200.0`, `AREA_IDEAL_MIN_M2 = 1500.0`,
  `AREA_IDEAL_MAX_M2 = 2000.0`, `PE_DIREITO_MIN = 3.5`, `M1_SCORE_OFICIAL = score_priorizacao`,
  `M1_PRIORIZACAO_TOP_PCT_POR_UF = 0.2`, `M1_OSM_ENABLED = False`,
  `M1_SETOR_CENSITARIO_OBRIGATORIO = False`, `M1_POP_MINIMA_PROXY = 1`,
  `M1_HEX_LAND_FRACTION_MIN = 0.05`. Pesos `renda=0.40`/`pop=0.60`: `hex_enrichment.py` **intocado**
  (0 linhas de diff); `tests/contracts/` passou inteiro dentro dos 826.
- **Artefatos M1 preservados:** **sim.** Nada em `config.py`, `pipelines/m1/`,
  `normalizar_concorrentes.py`, `calcular_colunas_mercado.py` ou `data/outputs/`. A única escrita
  possível é `data/staging/snapshots_concorrentes/`, e ela **não ocorreu** (hash idêntico).
- **Camada READ-ONLY como CÓDIGO:** `curadoria_agregadores.py` importa **só stdlib** (`argparse`,
  `logging`, `shutil`, `collections.abc`, `datetime`, `pathlib`) — zero acoplamento ao M1, travado por
  `test_modulo_nao_importa_o_m1_nem_dashboard`.
- **Nenhum comando na VPS:** **sim** — não usei MCP `ssh-vps-ultra`, `ssh` nem `scp` em etapa nenhuma.
- **`../GymScraping` READ-ONLY:** **sim** — só leituras (`escolher_diretorio_fonte`, `curar(dry_run=True)`,
  medição de mtime). Nada foi escrito lá.
- **Validações re-executadas pelo QA (sem bypass):**
  - `pytest -q` (suíte COMPLETA, serial) → **3681 passed, 34 skipped, 0 failed**
  - `pytest --collect-only -q` → **3714** coletados (baseline 3658, +56)
  - `pytest -n auto` → **INTERNALERROR do execnet, 3× em 2 vias** (infraestrutura, documentado)
  - `pytest -q <recorte do Builder>` → **826 passed, 5 skipped**
  - `pytest -q <8 consumidores adjacentes>` → **245 passed**
  - `ruff check .` → **All checks passed!**
  - `ruff format --check` → 2 arquivos (1 pré-existente, 1 do ciclo); não é gate do CI
  - `bash -n` nos 2 shells → **OK**, ambos LF puro
  - `loop_guard --base origin/main --json` → **sem CRÍTICO** (1 `governanca`)
  - `import motor_expansao.vulnerabilidade` + `curadoria_agregadores` → **ok**
  - `import app` de `web/server` (app de produção) → **ok**
  - `python -m …curadoria_agregadores --help` / `…snapshots --help` → **ok**, `--migrar-layout` presente
  - `curadoria_agregadores` contra o clone REAL → **aborta corretamente (D3)**
  - `alvos_ma --fontes wellhub --dry-run` contra a série real E contra série sintética de 2 fontes → **recorte efetivo**
  - `housekeeping_move_block.py BLK-MA-21 --check` → falha esperada (move é Passo 6.0)
- **Escopo respeitado:** **sim.** Confrontei os arquivos alterados contra o plano P1..P12 do snapshot
  do Planner: 1:1, sem excesso. `tasks/backlog.md` recebeu **só** o campo Status (autorizado por P12);
  `Autonomia` segue `manual (NÃO loop-safe)` — **intocada**. `graphify-out/*`, `PRD.md` e
  `tests/contracts/` não foram alterados pelo Builder. **Eu acrescentei** o bloco `BLK-MA-21-FU1` ao
  `tasks/backlog.md` (linha 2059) e atualizei `context/handoff.md` + `tasks/current_task.md`.

## Decisão recomendada

**Fechar o ciclo** (modo merge-humano — label `aprovado-humano`), levando junto o bloco de correção
`BLK-MA-21-FU1`, e **não** aplicar nada na VPS antes da ordem de 6 passos do runbook.

**O que o orquestrador precisa fazer no fechamento:**

1. **Rodar o housekeeping** (Passo 6.0): `python scripts/housekeeping_move_block.py BLK-MA-21` e
   depois `--check`. Simulei o corte: ele pega as linhas 1850..1993 e para em `### BLK-MA-07`;
   o `BLK-MA-21-FU1` (linha 2059) fica **fora** do corte, confirmado.
2. **Incluir no commit** o `tasks/backlog.md` com o bloco novo **BLK-MA-21-FU1** — é o registro dos
   três problemas médios e dos cinco achados secundários.
3. Commit por path, **sem** `graphify-out/*`, `PRD.md`, `context/handoff.md` nem `tasks/current_task.md`.
4. Lembrar ao Felipe, no PR: **(a)** resolver o estado ambíguo do clone WellHub antes da 1ª execução
   mensal; **(b)** a ordem de aplicação começa pela **imagem nova**, nunca pelo cron; **(c)** o
   BLK-MA-06 continua pendente na VPS e é dependência dura da aplicação.
