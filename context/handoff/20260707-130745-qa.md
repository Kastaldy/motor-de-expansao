# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador: housekeeping move BLK-ACENTO-01 + commit por path). Opcional: Documentation.

## VEREDITO
APROVADO COM RESSALVAS

## Justificativa
As 3 decisões do gate (D1 FAIXA_LABELS, D1-bis Opção A HYBRID_ELIGIBILITY_LABELS, fallback "Não informado" nas 5 ocorrências) foram implementadas exatamente como aprovadas, com ZERO identificador acentuado, ZERO tipografia esperta, cor do gráfico preservada e READ-ONLY M1 intacto; a única ressalva (não-bloqueadora) é que `build_faixa_comparison_figure` ainda exibe os valores brutos do enum no eixo x (um dos 3 call sites de faixa que o Planner nomeou em 2.2c), deixado assim pelo Builder para evitar regressão de cor.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- `build_faixa_comparison_figure` (`components.py:2296-2303`): o eixo x e a série de cor usam `faixa_label = faixa_oportunidade.astype(str)` (valor BRUTO), então as barras seguem rotuladas com os slugs do enum ("prioridade_maxima", "media", "baixa", ...) sem acento/capitalização. A cor NÃO regrediu (`color_discrete_map=FAIXA_COLORS` continua keyed pelo bruto — correto), mas este era um dos 3 call sites de faixa citados no critério de aceite (Planner 2.2c) e ficou sem a camada de label. Comportamento pré-existente (não é regressão deste bloco). Caminho seguro já indicado pelo Planner: `color="faixa_oportunidade"` (bruto) + coluna de label acentuada só no eixo x, OU `FAIXA_COLORS_POR_LABEL = {FAIXA_LABELS.get(k,k): v ...}`. Recomendo micro follow-up ou dobrar no BLK-ACENTO-02.

## Melhorias opcionais
- Acentuar travessões/`Nao`/etc. remanescentes em comentários e docstrings (fora do escopo "texto voltado ao usuário", deixados de propósito para não inflar o diff) num passe futuro de higiene, se desejado.

## Testes faltantes
- nenhum crítico. O novo teste `test_faixa_e_elegibilidade_labels_via_format_func` cobre D1/D1-bis (label mapping + `format_func` devolve acentuado + `options` seguem BRUTOS). Um assert cobrindo o eixo x acentuado de `build_faixa_comparison_figure` só faria sentido se a ressalva média for corrigida.

## Riscos remanescentes
- xdist/execnet instável no Python 3.14 deste ambiente (crash de worker em `pytest_sessionstart`, ANTES de coletar testes) — risco de mascarar "verde" se alguém usar `-p no:xdist`. Gate rodado SERIAL (correto). Não é defeito do bloco.
- Cabeçalhos de tabela renomeados são acoplados por string a selectores downstream; todos verificados em lockstep (grep por headers pré-acento retornou só `relatorio_municipal.py`, fora de escopo/BLK-ACENTO-02), suíte de dashboard verde. Baixo risco residual.

## Saída literal das validações (re-executadas pelo QA)

```
$ python -m ruff check .
All checks passed!
```

```
$ python -c "import streamlit_app; print('import ok')"
(warnings de streamlit ScriptRunContext/MemoryCacheStorageManager — esperados em bare mode)
import ok
```

```
$ python -m mypy src        # working tree COM as mudanças do bloco
src\motor_expansao\dimensionamento\growth_api_client.py:28: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\ibge_censo.py:24: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\poi_enrichment.py:15: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\base_h3_brasil.py:31: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\geo.py:21: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\telegram_bot.py:23: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\service.py:223: error: Library stubs not installed for "requests"  [import-untyped]
Found 7 errors in 7 files (checked 102 source files)

$ git stash push -- <5 arquivos do bloco> && python -m mypy src   # clean HEAD, sem as mudanças
Found 7 errors in 7 files (checked 102 source files)   # IDÊNTICO -> os 7 são PRÉ-EXISTENTES/ambientais (types-requests ausente),
                                                        # todos em arquivos NÃO tocados pelo bloco. ZERO erro novo em dashboard/* + streamlit_app.py.
```

```
$ python -c "import h3; print('h3', h3.__version__)"
h3 4.5.0                      # sem bloqueio do Smart App Control (WDAC) nesta rodada

$ python -m pytest -n auto -q      # SUÍTE COMPLETA (tentativa paralela)
INTERNALERROR> ... xdist/workermanage.py ... execnet/gateway_base.py:571 ...
INTERNALERROR> EOFError: couldn't load message header, expected 9 bytes, got 0
# Crash de WORKER no setup (pytest_sessionstart), ANTES de qualquer teste rodar -> instabilidade
# AMBIENTAL do xdist/execnet no Python 3.14, NÃO falha de teste. Fallback SERIAL abaixo (NO-BYPASS,
# sem -p no:xdist).
```

```
$ python -m pytest -q               # SUÍTE COMPLETA SERIAL (gate único do ciclo)
1 failed, 1430 passed, 2 skipped, 217834 warnings in 401.26s (0:06:41)
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
# Única falha: FileNotFoundError em data/staging/unidade_territorio_retencao.parquet (parquet
# gitignored da camada M2 lifetime/, ausente neste ambiente). PRÉ-EXISTENTE/ambiental, documentada
# no ciclo anterior BLK-RELPON-04; este bloco NÃO toca lifetime/ -> não é regressão.
# Todos os testes de dashboard/streamlit passam.
```

```
$ python -m pytest -q "tests/integration/test_streamlit_app.py::test_faixa_e_elegibilidade_labels_via_format_func"
1 passed, 11782 warnings in 2.13s     # teste novo do D1/D1-bis
```

```
$ python scripts/housekeeping_move_block.py BLK-ACENTO-01 --check
FALHA --check: stub ausente no backlog para BLK-ACENTO-01
# ESTADO PRÉ-MOVE CORRETO: o helper RECONHECE o bloco (não é BlockNotFound); o move é do
# ORQUESTRADOR no fechamento, DEPOIS deste veredito. Heading íntegro em backlog.md:1247.
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock do caminho crítico. A suíte foi rodada SERIAL como fallback honesto ao crash ambiental do xdist (NÃO usei `-p no:xdist` para forjar verde). mypy foi comparado contra o HEAD limpo via `git stash` para provar que os 7 erros são pré-existentes. A única falha de teste foi diagnosticada individualmente (FileNotFoundError de parquet M2 gitignored) e é ambiental/pré-existente, não do bloco.

## Conferência cruzada com log do Builder
Bate. Builder: ruff limpo (confirmado), mypy 7 erros pré-existentes de types-requests (confirmado idêntico no HEAD), import ok (confirmado), `-n auto` aborta por EOFError do execnet no 3.14 (confirmado), h3 4.5.0 sem bloqueio WDAC (confirmado). Builder rodou o subconjunto focado 234/234; QA rodou a suíte COMPLETA serial (gate único) = 1430 passed / 2 skipped / 1 falha ambiental pré-existente. D1/D1-bis/fallback implementados como descritos. A ressalva do eixo x de `build_faixa_comparison_figure` foi ANTECIPADA e documentada honestamente pelo próprio Builder no handoff ("apenas o eixo x já mostrava o bruto; não foi alterado, evitando qualquer regressão de cor").

## Guardrails verificados
- score_priorizacao não alterado: sim (nenhuma fórmula/peso/artefato tocado; só camada de exibição).
- hex_score_estrutural não alterado: sim.
- Artefatos M1 preservados: sim (`git status --porcelain` filtrado por parquet/config.py/core/constants/pipelines/m1/censo_/relatorio_municipal/calcular_colunas = NONE; nenhum parquet no diff; bloco não roda pipeline).
- Parâmetros canônicos: preservados (H3_RESOLUTION=7, pesos renda=0.40/pop=0.60, RENDA_MIN=4500.0 vivem em config.py/core, intocados; FAIXA_ORDEM e HYBRID_ELIGIBILITY_ORDER com valores BRUTOS inalterados — só adição das dicts LABELS).
- Identificadores intocados: sim — verificado por grep no diff: `key=`/`session_state`/`.st-key-*` só com label acentuado e key intacta; chaves (esquerda) de todos os `.rename(columns={...})` = nomes de coluna reais preservados, só o VALOR acentuado; `options` dos multiselects seguem os valores BRUTOS (`FAIXA_ORDEM`/`HYBRID_ELIGIBILITY_ORDER`), acentuação só via `format_func`; `template="classico"`/`METODO_*` não estão nesta camada (não tocados).
- Tipografia esperta: sweep Python nas linhas ADICIONADAS (5 arquivos + teste) = 0 travessão/en-dash/bullet/seta/reticências/aspas curvas/©; travessões de exibição trocados por " - ".
- Fallback "Não informado": 5 ocorrências trocadas juntas (data.py x3 incl. `categories=FAIXA_ORDEM + ["Não informado"]`, components.py x2); `pd.Categorical` continua casando (suíte verde).
- Lockstep de cabeçalhos renomeados: `style_ranking_table` ("Score Priorização"/"Viável"), listas `for col in [...]` e ordens de coluna atualizadas; grep por headers pré-acento não achou consumidor stale no dashboard.
- Escopo respeitado: sim — diff só em dashboard/{constants,components,data,pages}.py + streamlit_app.py + tests/integration/test_streamlit_app.py + bookkeeping (handoff, current_task). ZERO em core/constants.py, censo_*, relatorio_municipal.py, pipelines/m1, config.py, calcular_colunas_mercado.py.
- Validações re-executadas pelo QA (sem bypass): ruff → ok; import smoke → ok; mypy → 7 pré-existentes (idêntico no HEAD); pytest serial → 1 falha ambiental pré-existente, 1430 passed; teste novo D1/D1-bis → passed; housekeeping --check → estado pré-move correto.

## Decisão recomendada
Fechar ciclo (housekeeping move BLK-ACENTO-01 + commit por path pelo orquestrador). A ressalva média (eixo x de `build_faixa_comparison_figure`) é não-bloqueadora: pode ser um micro follow-up ou dobrada no BLK-ACENTO-02. Revisão visual humana da UI segue recomendada como critério de aceite do Planner (filtro/legenda de faixa, gráfico de comparação por faixa com cores por faixa, Carteira e Plano, Expansão de Domínio).
