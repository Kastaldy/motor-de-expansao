# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Builder

## Entendimento da tarefa
Reposicionar a marca d'água do PDF do Relatório Pontual de faixa diagonal grande centralizada para texto horizontal pequeno e discreto no canto inferior-direito, ajustando somente as constantes `_WATERMARK_*` e o corpo de `_draw_watermark` em `censo_report.py`.

## Plano técnico

### Passo 1 — Ajustar constantes `_WATERMARK_*` (linhas 82–86 de `censo_report.py`)

Alterar os quatro valores numéricos/string nas constantes existentes e acrescentar uma nova constante de margem. Ordem de aparição no arquivo (linhas 82–87):

```
# ANTES
_WATERMARK_BASE = "Ultra Academia"       # linha 82 — INALTERADO
_WATERMARK_RGB = (120, 120, 120)         # linha 83 — INALTERADO
_WATERMARK_ALPHA = 0.16                  # linha 84 — ALTERAR → 0.40
_WATERMARK_ANGLE = 45.0                  # linha 85 — ALTERAR → 0.0
_WATERMARK_FONT_PT = 60                  # linha 86 — ALTERAR → 9

# DEPOIS (adicionar após _WATERMARK_FONT_PT)
_WATERMARK_MARGIN = 20.0                 # nova — margem pt das bordas direita e inferior
```

Justificativas dos valores:
- `_WATERMARK_FONT_PT = 9`: corpo pequeno (8–10 pt range pedido pelo BO); 9 pt é padrão tipográfico de rodapé discreto e legível a ~100% zoom.
- `_WATERMARK_ALPHA = 0.40`: em fonte 60 pt, 0.16 produzia área de cobertura grande com pouca opacidade; em 9 pt a área de texto é ~44x menor — 0.16 seria quase invisível. 0.40 mantém a marca discreta-porém-auditável em corpo pequeno (meio do intervalo 0.30–0.45 recomendado pelo BO).
- `_WATERMARK_ANGLE = 0.0`: horizontal (decisão de produto aprovada).
- `_WATERMARK_MARGIN = 20.0`: 20 pt das bordas direita e inferior. Com `_PAGE_H = 540.0`, baseline `y = 520 pt` — bem dentro da página; sem risco de clipping.

### Passo 2 — Reescrever `_draw_watermark` (linhas 260–275 de `censo_report.py`)

Substituir o corpo atual pelo novo, que:
- Usa peso de fonte `""` (normal) em vez de `"B"` (bold) — discreto em corpo pequeno.
- Calcula `x` e `y` pelo canto inferior-direito com `_WATERMARK_MARGIN`.
- Remove o bloco `with pdf.rotation(...)` (ângulo 0 torna-o um no-op; remoção explícita evita ambiguidade).
- Mantém `with pdf.local_context(fill_opacity=_WATERMARK_ALPHA)` obrigatório para restaurar o graphics state de opacidade.
- Atualiza a docstring para descrever o novo comportamento.

```python
def _draw_watermark(pdf: _UltraPDF, text: str) -> None:
    """Desenha a marca d'agua no canto inferior-direito, horizontal e discreta (BLK-EST-01-FU1).

    Posicao: baseline em (_PAGE_W - _WATERMARK_MARGIN - largura_texto, _PAGE_H - _WATERMARK_MARGIN).
    READ-ONLY de estado logico: `local_context` restaura o graphics state (fill_opacity) ao sair.
    Usa `pdf.text` (baseline) — imune a `set_margins`/auto_page_break OFF. Deve ser chamada
    DEPOIS do conteudo da pagina para ficar POR CIMA do fundo/PNG.
    """
    pdf.set_font("Helvetica", "", _WATERMARK_FONT_PT)
    pdf.set_text_color(*_WATERMARK_RGB)
    w = pdf.get_string_width(text)
    x = _PAGE_W - _WATERMARK_MARGIN - w
    y = _PAGE_H - _WATERMARK_MARGIN
    with pdf.local_context(fill_opacity=_WATERMARK_ALPHA):
        pdf.text(x, y, text)
```

**Diferenças-chave em relação ao código atual:**
| Aspecto | Antes | Depois |
|---|---|---|
| Peso da fonte | `"B"` (bold) | `""` (normal) |
| Tamanho | `_WATERMARK_FONT_PT=60` | `_WATERMARK_FONT_PT=9` |
| Opacidade | `_WATERMARK_ALPHA=0.16` | `_WATERMARK_ALPHA=0.40` |
| Posição x | `cx - w/2 = 480 - w/2` (centro) | `_PAGE_W - _WATERMARK_MARGIN - w` (direita) |
| Posição y | `_PAGE_H / 2 = 270` (centro) | `_PAGE_H - _WATERMARK_MARGIN = 520` (inferior) |
| Rotação | `with pdf.rotation(45.0, x=cx, y=cy)` | removido (ângulo 0, horizontal) |
| `local_context` | preservado | preservado (obrigatório) |

### Passo 3 — Verificar colisão com rodapés existentes

O rodapé técnico ("Raio 1,5 km - EPSG:3857 - {atribuição CARTO}") é texto embutido nos PNGs dos mapas, não em operadores de texto PDF sobrepostos. Portanto não há colisão de camadas PDF. O `y = 520 pt` com fonte 9 pt (altura de glifo ~7 pt) fica entre 513–520 pt — dentro da página (0–540 pt), sem clipping.

Avaliação de margem: `_WATERMARK_MARGIN = 20.0 pt` resulta em:
- Borda direita: `x = 960 - 20 - get_string_width(text)` → texto termina a 20 pt da borda direita. OK.
- Borda inferior: `y = 540 - 20 = 520 pt` → baseline a 20 pt da borda inferior. OK.
- **Sem ajuste necessário** — canto inferior-direito com margem 20 pt é seguro em todas as 7 páginas.

### Passo 4 — Confirmar testes existentes (sem alteração nos testes)

Os testes de marca d'água em `tests/unit/test_relatorio_pontual_censitario_export.py` (linhas 206–244):
- `test_pdf_marca_dagua_com_solicitante` (L206): assere `b"Ultra Academia" in pdf_bytes` e `b"Analista Teste" in pdf_bytes` — **continua verde** (texto inalterado).
- `test_pdf_marca_dagua_sem_solicitante` (L221): assere `b"Ultra Academia" in pdf_bytes` — **continua verde**.
- `test_pdf_marca_dagua_em_todas_as_paginas` (L235): assere `pdf_bytes.count(b"Ultra Academia") >= 7` — **continua verde** (loop por-página inalterado, linhas 635–637).

Nenhum dos três assere posição, ângulo ou tamanho. **O arquivo de testes NÃO precisa ser alterado.**

Opcional (baixíssimo custo, regressão de "horizontal"): se o Builder quiser, pode adicionar um assert no final do `test_pdf_marca_dagua_com_solicitante`:
```python
from motor_expansao.dashboard.censo_report import _WATERMARK_ANGLE
assert _WATERMARK_ANGLE == 0.0, "marca d'agua deve ser horizontal (angulo=0)"
```
Não é obrigatório; registrar apenas se o Builder julgar útil.

## Arquivos afetados
- `src/motor_expansao/dashboard/censo_report.py` — constantes `_WATERMARK_ALPHA`, `_WATERMARK_ANGLE`, `_WATERMARK_FONT_PT` (linhas 84–86) + nova constante `_WATERMARK_MARGIN` (inserir após linha 86) + corpo de `_draw_watermark` (linhas 260–275)
- `tests/unit/test_relatorio_pontual_censitario_export.py` — provavelmente inalterado; alterar só se o Builder incluir o assert opcional de `_WATERMARK_ANGLE == 0.0`

## Dependências
- `fpdf2`: `pdf.local_context(fill_opacity=...)`, `pdf.get_string_width()`, `pdf.text()`, `pdf.set_font()`, `pdf.set_text_color()` — todos já usados no código atual; nenhuma nova dependência.
- `pdf.rotation()` é REMOVIDO — verifica-se que não é usado em nenhum outro lugar do arquivo (uso único era dentro de `_draw_watermark`).

## Riscos técnicos
1. **Opacidade vs legibilidade em fonte pequena**: `_WATERMARK_ALPHA=0.16` (projetada para 60 pt) seria quase invisível em 9 pt. Mitigado: escolhido `0.40` — discreto sem sumir. Se na verificação visual o QA/orquestrador julgar muito visível ou pouco visível, o valor é uma constante fácil de ajustar sem risco.
2. **`local_context` sem `rotation`**: ao remover `pdf.rotation(...)`, o `with pdf.local_context(fill_opacity=...)` permanece obrigatório para restaurar o estado gráfico de opacidade. O Builder NÃO deve remover o `local_context`.
3. **`pdf.rotation()` no escopo global**: confirmar (via Grep) que `pdf.rotation` não é chamado em outros pontos do arquivo que dependam da versão atual de `_draw_watermark` — improvável, mas verificar antes de alterar.
4. **Paths pré-sujos**: `data/outputs/setores_censitarios_2022_geo/_metadata.json` e `data/reports/relatorio_pontual_censitario_base_geo.md` estão sujos no `git status` — NÃO entrar no commit deste ciclo.
5. **Baseline de testes**: BO reportou `696 passed, 1 skipped, 3 failed` pré-existentes. As 3 falhas são alheias a este ciclo; o Builder não deve introduzir novas falhas.

## Critérios de aceite finais
1. `b"Ultra Academia" in pdf_bytes` — continua verdadeiro (texto inalterado).
2. `b"Analista Teste" in pdf_bytes` quando `solicitante="Analista Teste"` — continua verdadeiro.
3. `pdf_bytes.count(b"Ultra Academia") >= 7` — continua verdadeiro (7 páginas, loop inalterado).
4. `b"/Count 7" in pdf_bytes` — 7 páginas preservadas.
5. `_WATERMARK_ANGLE == 0.0` (regressão horizontal — verificável por import).
6. `_WATERMARK_FONT_PT == 9` (corpo pequeno — verificável por import).
7. Verificação visual pelo orquestrador: PDF gerado com `solicitante="Teste"` → marca aparece no canto inferior-direito, pequena e horizontal — não diagonal, não centralizada.
8. Ruff + mypy limpos no escopo alterado.
9. Suite completa sem novas falhas além das 3 pré-existentes.
10. Nenhum artefato M1 alterado; nenhum score recalculado.

## Validações obrigatórias
```
# Suite focada (marcas d'água + mapa; serial):
python -m pytest -q tests/unit/test_relatorio_pontual_censitario_export.py tests/unit/test_relatorio_pontual_censitario_mapa.py

# Smoke de import (censo_report é importado pela app):
python -c "import streamlit_app"

# Linting e tipos (escopo dos arquivos alterados):
ruff check src/motor_expansao/dashboard/censo_report.py
mypy src/motor_expansao/dashboard/censo_report.py --ignore-missing-imports

# Suite completa (gate do QA):
python -m pytest -q
```

Critério mínimo: 0 falhas novas (as 3 pré-existentes são permitidas); smoke sem erro de import; ruff e mypy sem erros no escopo.

## Fora de escopo
- Recalcular qualquer score ou artefato M1
- Reverter a feature BLK-EST-01 / alterar o texto da marca, `_watermark_text()`, ou o parâmetro `solicitante`
- Alterar o loop por-página (linhas 635–637)
- Alterar template/conteúdo das 7 páginas, raio/interseção, geração de mapas
- Tocar componentes do dashboard (`pages.py`, `components.py`) — este ciclo é só do PDF
- `data/outputs/setores_censitarios_2022_geo/_metadata.json` e `data/reports/relatorio_pontual_censitario_base_geo.md` (paths pré-sujos, NÃO commitar)
- Alterar `_WATERMARK_BASE`, `_WATERMARK_RGB`, `_PAGE_W`, `_PAGE_H`

## Alerta de aprovação
Não aplicável. Criticidade Média. Tarefa é READ-ONLY sobre M1 — zero escrita em score, artefatos ou parâmetros canônicos do §3.

## Guardrails ativos (CLAUDE.md §2/§4/§5)
- READ-ONLY sobre o M1: nenhuma visualização, análise ou interação de mapa pode recalcular ou alterar `score_priorizacao`, `hex_score_estrutural`, carteira, plano ou artefatos oficiais do M1.
- Anti-PII §4: `set_compression(False)` e `pdf_version="1.4"` INALTERADOS (auditabilidade); cartão de contato `image24.png` NUNCA embutido; texto da marca permanece "Ultra Academia" ou "Ultra Academia | {solicitante}".
- Método de interseção `setor_censitario_intersecao_area_1p5km` e raio 1,5 km INTOCADOS.
- Parâmetros canônicos §3 INALTERADOS: `H3_RESOLUTION=7`, `DIST_MIN_ULTRA_KM=1.0`, `RENDA_MIN=4500.0`, pesos `renda=0.40`/`pop=0.60`.
