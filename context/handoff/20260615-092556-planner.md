# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
**Approver / REVISÃO HUMANA** — criticidade **Alta** (passa a gravar identidade real do consumidor no documento PDF; rastreabilidade/LGPD). Gate humano obrigatório por Felipe ANTES do Builder.

## Entendimento da tarefa
Ligar o `consumidor` já resolvido pela autenticação da API (`auth.resolver_consumidor`, token→consumidor) ao parâmetro `solicitante` na geração do PDF em `service.gerar_pdf_ponto`, para que todo PDF emitido por `POST /analisar?formato=pdf` carregue o nome real do consumidor na marca d'água, mantendo o fallback seguro `None` → só "Ultra Academia".

## Plano técnico

### 1. Mudança cirúrgica em `service.py` (uma linha)
Arquivo: `src/motor_expansao/api/service.py`, função `gerar_pdf_ponto`.
- Estado atual confirmado (linhas **306-308**):
  ```python
  return gerar_pdf_relatorio_pontual_censitario(
      result, mapas, residual=residual, ultra_dir=ultra_dir, rotulo=rotulo
  )
  ```
- Mudança: adicionar `solicitante=consumidor` aos kwargs. `consumidor` já está no escopo (parâmetro `consumidor: str | None` na linha 244). Resultado:
  ```python
  return gerar_pdf_relatorio_pontual_censitario(
      result, mapas, residual=residual, ultra_dir=ultra_dir,
      solicitante=consumidor, rotulo=rotulo
  )
  ```
- NÃO estreitar a assinatura de `gerar_pdf_ponto` (R5): `consumidor` permanece `str | None` para flexibilidade de testes; no caminho real da rota ele é sempre `str` (`resolver_consumidor` retorna `str`, levanta 401 antes).
- `censo_report.py` NÃO é alterado: `gerar_pdf_relatorio_pontual_censitario(..., solicitante: str | None = None, ...)` (linha 629) e `_watermark_text` (linhas 144-151) já existem prontos e seguros. Fora de escopo (handoff do BO).
- Cadeia confirmada (R1): rota `analisar.py` linha 56 já passa `consumidor` posicional para `gerar_pdf_ponto(lat, lng, consumidor, settings, rotulo=...)`; o `consumidor` vem de `Depends(resolver_consumidor)` (linha 41). A única lacuna é o repasse interno em `service.py`.

### 2. Testes a adicionar/estender
**Cobertura existente mapeada (R4 — não duplicar):**
- `tests/unit/test_relatorio_pontual_censitario_export.py` linhas 206-244: já cobre `gerar_pdf_relatorio_pontual_censitario` com `solicitante="Analista Teste"` (watermark contém o nome), com `solicitante=None` (só "Ultra Academia", anti-PII), e em todas as 7 páginas (`count >= 7`). **NÃO duplicar** o nível unitário do `censo_report`.
- `tests/integration/test_api_analisar.py` linhas 93-110: já cobrem `?formato=pdf` e `Accept: application/pdf` retornando `%PDF`, mas **NÃO inspecionam a watermark** nem verificam o wiring `consumidor → solicitante`. **ESTA é a lacuna a fechar.**

**Onde adicionar (arquivo exato):** `tests/integration/test_api_analisar.py` (junto dos testes de PDF existentes, após a linha 110).

**Abordagem de assert (escolhida, justificada): monkeypatch de `gerar_pdf_relatorio_pontual_censitario` no namespace de `service`, capturando o kwarg `solicitante`.**
Justificativa de robustez:
- Determinístico e hermético: não depende da base geo materializada (os testes de PDF reais já têm `_exige_base_geo` autouse de módulo, mas o wiring deve ser testável sem render de PDF caro/online de tiles).
- Verifica o contrato exato do bloco (o kwarg `solicitante` recebe o `consumidor` resolvido), não um efeito colateral indireto.
- Evita gravar PDF real em disco (anti-PII) e evita falso-positivo por substring "Ultra Academia" que aparece independentemente do solicitante.
- O import dentro de `gerar_pdf_ponto` é `from motor_expansao.dashboard.censo_report import gerar_pdf_relatorio_pontual_censitario` (linha 264, local à função). Portanto o monkeypatch deve mirar **a origem**: `monkeypatch.setattr("motor_expansao.dashboard.censo_report.gerar_pdf_relatorio_pontual_censitario", _spy)`. (Como o import é local e por-nome dentro da função, patchar o atributo no módulo de origem `censo_report` é o alvo correto; patchar `service.gerar_pdf_relatorio_pontual_censitario` NÃO funcionaria porque o nome não existe no namespace de `service` no import-time.)
- O spy deve retornar `b"%PDF-1.4 fake"` (bytes válidos mínimos) e registrar `kwargs["solicitante"]`. Também é necessário mockar/curto-circuitar a parte cara de `_resolver_e_carregar` + render de mapas, OU manter o `_exige_base_geo` autouse e mockar apenas a geração final do PDF (mais simples: o spy só substitui a função final; o resto roda com a base real do módulo, igual aos testes 93-110). **Recomendação: reusar o gate `_exige_base_geo` já autouse e mockar só `gerar_pdf_relatorio_pontual_censitario`** — minimiza superfície de mock.

**Testes concretos a adicionar (3):**
- **(a) `test_analisar_pdf_carimba_consumidor`**: token `dev-token` → consumidor `dev-local` (mapa estático de `settings.tokens`, nome **fictício**, sem PII). `POST /api/v1/analisar?formato=pdf` com `headers=AUTH`. Assert: `resp.status_code == 200`, `content-type == "application/pdf"`, e o spy capturou `solicitante == get_settings().tokens["dev-token"]` (== `"dev-local"`).
- **(b) `test_analisar_pdf_solicitante_none_fallback`**: chamar **diretamente** `service.gerar_pdf_ponto(lat, lng, consumidor=None, settings, rotulo=None)` com o mesmo spy → assert spy capturou `solicitante is None` (cobre o fallback retrocompatível sem depender da rota, que sempre injeta `str`). Reaproveita `monkeypatch` + a fixture de base geo.
- **(c) Fixtures/anti-PII**: usar EXCLUSIVAMENTE o nome fictício `dev-local` do mapa `tokens` default (ou um override de `settings.tokens` com nome explicitamente fictício, ex. `{"tok-fake": "bot-telegram"}` via `dependency_overrides[get_settings]`, padrão já usado no teste `test_analisar_municipio_sem_base_404` linha 68-69). NENHUM PDF real escrito em disco; NENHUM nome real de pessoa; spy retorna bytes sintéticos.

**Variante de fallback (apenas se o monkeypatch por-nome se mostrar frágil no ambiente):** inspeção de bytes — `assert bytes(consumidor, "latin-1") in resp.content` no PDF real (stream OFF / `set_compression(False)` garante texto legível, confirmado pelos testes unitários existentes que fazem `b"Analista Teste" in pdf_bytes`). É menos robusto (render caro + dependência de base/tiles) e por isso é fallback, não primário.

### 3. Validações obrigatórias
Comandos concretos no escopo (Builder roda o subconjunto; suite full fica para o QA):
- `python -m pytest -q tests/integration/test_api_analisar.py tests/unit/test_relatorio_pontual_censitario_export.py -n auto`
  - Critério mínimo: 0 falhas; os 3 novos testes passam; nenhum dos 244+ testes de export regride.
- `python -m pytest -q -k "import_streamlit_app or test_api_skeleton" -n auto` (smoke de import dos módulos da API)
  - Critério: import limpo, sem erro.
- `ruff check src/motor_expansao/api/service.py tests/integration/test_api_analisar.py`
  - Critério: 0 erros no escopo alterado.
- `mypy src/motor_expansao/api/service.py`
  - Critério: 0 erros novos; tipo de `consumidor` (`str | None`) compatível com `solicitante: str | None`.
- **QA (gate único, suite full):** `python -m pytest -n auto` — critério: **780 passed, 4 skipped** preservados (+3 novos = **783 passed, 4 skipped**), 0 falhas. Confirmar contagem com o QA; sem regressão.

### 4. Tratamento do R3 (logs LGPD — ClickUp 86e1rtezm) — PREMISSA a confirmar no gate humano
PREMISSA (a ser confirmada por Felipe ANTES do Builder): **se os logs LGPD já leem o campo `consumidor` do JSON de resposta (`AnalisarResponseJSON.consumidor`, já carimbado e inalterado), então o `solicitante=consumidor` no PDF é suficiente e nenhuma mudança adicional de logging é necessária.** Qualquer log estruturado novo no servidor, header de resposta extra, ou campo adicional no PDF/JSON **amplia o escopo** deste bloco e exige aprovação explícita no gate. O plano NÃO inclui logging novo. `AnalisarResponseJSON.consumidor` permanece INALTERADO.

## Arquivos afetados
- `src/motor_expansao/api/service.py` — uma linha (adiciona `solicitante=consumidor` na chamada em `gerar_pdf_ponto`, linhas 306-308).
- `tests/integration/test_api_analisar.py` — 3 testes novos (wiring consumidor→solicitante via spy; fallback None; fixtures fictícias).
- `context/handoff.md` (este arquivo) e `tasks/current_task.md` (housekeeping de esteira).

## Dependências
- Base geo materializada (`data/outputs/setores_censitarios_2022_geo/uf=SP/cod_municipio=3500402`) para os testes de integração reais — gate `_exige_base_geo` autouse já trata com `pytest.skip` quando ausente (preserva os 4 skipped esperados).
- `httpx` (extra de teste) — `pytest.importorskip("httpx")` já presente no arquivo de integração.
- Extra `[api]` (`fastapi`/`pydantic`) instalado para rodar os testes da API.

## Riscos técnicos
- **R1 — Lacuna confirmada (corrigível em 1 linha):** `service.py` 306-308 não passa `solicitante`. Severidade média/funcional; é a razão do bloco. Mitigado pelo passo 1.
- **R2 — Trilha do dashboard BLOQUEADA (Authelia ausente):** `pages.py` linha 2656 (`render_downloads_relatorio_censitario`) não tem fonte de identidade no Streamlit (zero hits para `authelia`/`X-Remote-User`/`st.user`/identidade em `st.session_state`). Fase 2 só após Authelia (ou equivalente) operacional. `solicitante=None` / fallback "Ultra Academia" deve ser preservado indefinidamente como default seguro. **FORA deste ciclo.**
- **R3 — Logs LGPD:** ver passo 4 (premissa a confirmar no gate; sem logging novo sem aprovação).
- **R4 — Cobertura de teste:** mapeada acima; os novos testes vão em `test_api_analisar.py` (wiring API), sem duplicar os unitários de `censo_report` já existentes.
- **R5 — `consumidor` é `str` em `auth.py`:** `resolver_consumidor` retorna `str` (linha 28). O parâmetro de `gerar_pdf_ponto` segue `str | None` (correto, flexível para testes); o Builder NÃO deve estreitar a assinatura.

## Critérios de aceite finais
- `gerar_pdf_ponto(lat, lng, consumidor="bot-telegram", settings, ...)` produz watermark contendo "Ultra Academia | bot-telegram" (verificado via spy: `solicitante == "bot-telegram"`).
- `gerar_pdf_ponto(lat, lng, consumidor=None, settings, ...)` → `solicitante=None` → watermark só "Ultra Academia" (fallback retrocompatível).
- `POST /analisar?formato=pdf` com token válido encadeia `consumidor → solicitante` (novo teste de integração verde).
- O campo `consumidor` em `AnalisarResponseJSON` permanece INALTERADO.
- Suite full verde sem regressão de contagem (780 passed, 4 skipped → 783 passed, 4 skipped); ruff + mypy limpos no escopo.
- READ-ONLY M1: `score_priorizacao`, `hex_score_estrutural`, pesos (`renda=0.40`/`pop=0.60`), carteira, plano, artefatos oficiais INALTERADOS.
- Nenhum PDF real nem fixture com PII real versionado; fixtures usam nome fictício (`dev-local` / `bot-telegram`).

## Validações obrigatórias
- Comando: `python -m pytest -q tests/integration/test_api_analisar.py tests/unit/test_relatorio_pontual_censitario_export.py -n auto`
- Critério mínimo: 0 falhas; 3 testes novos passam; 0 regressão.
- Comando: `ruff check src/motor_expansao/api/service.py tests/integration/test_api_analisar.py` — 0 erros.
- Comando: `mypy src/motor_expansao/api/service.py` — 0 erros novos.
- Comando (QA): `python -m pytest -n auto` — 783 passed, 4 skipped (ou equivalente sem regressão), 0 falhas.

## Fora de escopo
- Trilha do dashboard (`pages.py` / `render_downloads_relatorio_censitario`): bloqueada (Authelia ausente). NÃO tocar.
- Authelia, proxy headers, `X-Remote-User`, JWT externo, `st.user`: inexistentes; NÃO introduzir.
- Alterar `censo_report.py` (assinatura `solicitante` e `_watermark_text` já prontos — NÃO tocar).
- Redefinir template/marca d'água (entregue no BLK-EST-01).
- Logging LGPD novo no servidor / header / campo extra (sem aprovação — ver R3).
- Score, pesos, artefatos M1 (READ-ONLY; DEC-001). `config.py` raiz.
- BLK-API-05 e fases posteriores da API.
- Versionar PDFs reais ou fixtures com PII real.

## Alerta de aprovação
**ATENÇÃO: ESTA TAREFA É DE CRITICIDADE ALTA. PASSA A GRAVAR A IDENTIDADE REAL DO CONSUMIDOR (TOKEN→CONSUMIDOR) NA MARCA D'ÁGUA DO PDF EMITIDO PELA API — IMPACTO DE RASTREABILIDADE / LGPD. APROVAÇÃO HUMANA OBRIGATÓRIA ANTES DO BUILDER. FELIPE DEVE LER ESTE HANDOFF E CONFIRMAR EXPLICITAMENTE: (1) O REPASSE `solicitante=consumidor` NO PDF; (2) A PREMISSA DO R3 — SE OS LOGS LGPD (CLICKUP 86e1rtezm) JÁ LEEM `consumidor` DO JSON DE RESPOSTA, NENHUM LOGGING NOVO É NECESSÁRIO; QUALQUER LOG/CAMPO EXTRA AMPLIA O ESCOPO E EXIGE APROVAÇÃO SEPARADA. NÃO INICIAR O BUILDER SEM ESSA CONFIRMAÇÃO.**

## Guardrails ativos
- READ-ONLY sobre M1: `score_priorizacao`, `hex_score_estrutural`, pesos (`renda=0.40`/`pop=0.60`), carteira, plano, artefatos oficiais INALTERADOS (DEC-001).
- Anti-PII (§2/§4 CLAUDE.md): nenhum PDF real ou fixture com PII real versionado; fixtures usam nomes fictícios.
- Sem dependência de API ao vivo no dashboard de produção (§2); geração do PDF pela API já é on-demand (DEC-005).
- `censo_report.py` NÃO alterado (assinatura `solicitante` já pronta).
- Sem regressão de suite: baseline corrente 780 passed, 4 skipped (último QA BLK-DIM-00).
- Esteira Alta: gate humano obrigatório entre Planner e Builder.
- Nenhuma alteração em `config.py` raiz (parâmetros canônicos §3).
