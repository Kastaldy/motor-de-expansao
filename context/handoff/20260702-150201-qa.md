# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador roda Passo 6.0 via `scripts/housekeeping_move_block.py BLK-TP-08` + commit por path)

## VEREDITO
APROVADO

## Justificativa
A entrega do BLK-TP-08 é READ-ONLY sobre o M1, anti-PII por construção (PII dropada na fronteira; parquet real com ZERO colunas PII; `Total_Alunos_Cluster` nunca somado), reproduz exatamente os números do Builder (24.045 acad / 1.920.955 alunos / 6.785 hexes; 1.425 overlap; 39,4%/62,7%), e a suíte completa passa (1251 passed, 1 skipped) sem tocar artefatos M1/mercado.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- O grep bruto por "total_alunos_cluster" no relatório casa a FRASE de guardrail ("`Total_Alunos_Cluster` NUNCA é somado…"), não um valor PII. Se quiser blindagem cosmética total, a nota poderia usar um placeholder, mas é irrelevante (não há dado PII, só o nome da coluna numa explicação). Não bloqueia.

## Testes faltantes
- nenhum crítico. Cobertura boa: contrato/dtypes, ZERO PII (parquet+relatório), agregação (Alunos somado / Total_Alunos_Cluster nunca), res-7, determinismo, DEDUP por hex, isolamento AST. (Opcional: um teste garantindo que uma coluna PII injetada na fixture faz `_assert_sem_pii` levantar — hoje a garantia é indireta pela fixture limpa.)

## Riscos remanescentes
- Dupla-contagem de oferta (39,4% acad / 62,7% alunos em hex já coberto) é conhecida e apenas QUANTIFICADA (D1/D4) — a subtração/integração ao residual é follow-up sob gate (BLK-TP-09). Aceito.
- Coords ~1 km (~2 casas) geram ruído no join res-7 — documentado no relatório (D2); camada é refino, não verdade fina.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte COMPLETA (gate único) — `python -m pytest -n auto -q`
```
1251 passed, 1 skipped, 338862 warnings in 250.88s (0:04:10)
```
(0 falhas / 0 erros de coleta. Baseline histórica ~1241 do BLK-TP-06 + os 10 testes novos do bloco = 1251, dentro do esperado. Warnings são deprecations pré-existentes de libs/CRS, não regressões.)

### 2. Smoke — `python -c "import streamlit_app; print('import ok')"`
```
import ok
```

### 3. Isolamento (AST) de `oferta_academias_menores.py`
```
imports (AST): ['__future__', 'pathlib', 'h3', 'pandas', 'contrato']
proibidos encontrados: []
ISOLAMENTO OK
```
(NÃO importa `pipelines.m1`/`dashboard`/`censo`/`motor_expansao.api`.)

### 4a. `ruff check` (módulo + teste novos)
```
All checks passed!
```

### 4b. `mypy` (módulo novo)
```
Success: no issues found in 1 source file
```

### 5. git status --porcelain (nenhum artefato M1 nem mercado modificado)
```
 M context/handoff.md
 D data/raw/ibge/malha_brasil.geojson        (pré-existente no snapshot inicial, alheio ao bloco)
 D data/raw/ibge/malha_uf_brasil.geojson     (pré-existente no snapshot inicial, alheio ao bloco)
 M docs/modelo_mercado_hexagonos.md
 M src/motor_expansao/demanda_revelada/__init__.py
 M tasks/current_task.md
?? context/handoff/20260702-143126-block-orchestrator.md
?? context/handoff/20260702-143912-planner.md
?? context/handoff/20260702-145330-builder.md
?? scripts/backtest_smartfit_scores.py        (pré-existente no snapshot inicial, alheio ao bloco)
?? src/motor_expansao/demanda_revelada/oferta_academias_menores.py
?? tests/fixtures/oferta_academias_menores_fake.xlsx
?? tests/unit/demanda_revelada/test_ingestao_oferta_menores.py
```
Nenhum de `brasil_estrutural`/`brasil_priorizados`/`hexagonos_brasil_*`/`hexagonos_mercado_mapeado`/`calcular_colunas_mercado.py` aparece.

### 5b. gitignore dos gerados
```
.gitignore:26:*.parquet   data/staging/oferta_academias_menores_h3.parquet
.gitignore:70:data/reports/scratch/   data/reports/scratch/oferta_academias_menores_qualidade.md
```

### 6. mtime dos 4 artefatos M1 — ANTES e DEPOIS de rodar a ingestão real pelo QA
```
ANTES:
1780501621 data/staging/brasil_estrutural.parquet
1780501631 data/staging/brasil_priorizados.parquet
1780501655 data/staging/hexagonos_brasil_oportunidades.parquet
1781271963 data/outputs/hexagonos_brasil_dashboard.parquet
DEPOIS (idênticos):
1780501621 data/staging/brasil_estrutural.parquet
1780501631 data/staging/brasil_priorizados.parquet
1780501655 data/staging/hexagonos_brasil_oportunidades.parquet
1781271963 data/outputs/hexagonos_brasil_dashboard.parquet
hexagonos_mercado_mapeado.parquet: 1781205056 (inalterado)
```

### 7. Prova anti-PII do parquet REAL (rodado pelo QA sobre `NAO_ABRA/03_Competidores.xlsx`)
```
COLUNAS DO PARQUET REAL (e em disco, 15):
['hex_id', 'n_academias_menores', 'alunos_academias_menores', 'n_plano_tp0', 'n_plano_tp1',
 'n_plano_tp1_plus', 'n_plano_tp2', 'n_plano_tp2_plus', 'n_plano_tp3', 'n_plano_tp4',
 'n_plano_tp5', 'n_plano_tp5_plus', 'n_plano_tp6', 'n_plano_tp7', 'versao_contrato']
PII PRESENTE: []   (nenhuma de latitude/longitude/nome_academia/cluster_id/total_alunos_cluster)
```
Confirmado no CÓDIGO: `_ler_e_derivar_hex` deriva `hex_id` das coords e retorna SÓ `hex_id`/`Plano`/`Alunos_Academia` — `Latitude`/`Longitude`/`Nome_Academia`/`Cluster_ID` são dropadas na fronteira e `Total_Alunos_Cluster` nunca é selecionado (logo nunca somado; `_agregar_h3` soma só `Alunos_Academia`). `_assert_sem_pii` bloqueia qualquer coluna fora do contrato/PII.

### 8. Reprodução dos números (relatório real gerado pelo QA)
```
n_hexes: 6785
sum_academias: 24045
sum_alunos: 1920955
- Hexes que casam com o universo: 6.731 de 6.785 (99,2%)
- Hexes de concorrentes mapeados (únicos): 1.659
- Hexes em SOBREPOSIÇÃO: 1.425
- Academias menores em hex já coberto: 9.482 (39,4%)
- Alunos em hex já coberto: 1.203.919 (62,7%)
```
Batem exatamente com o log do Builder.

### 9. Grep PII no relatório gerado
```
NO COORDINATE DATA IN REPORT
```
(O único match textual de "Total_Alunos_Cluster" é a FRASE de guardrail que diz que ele NUNCA é somado — não é dado PII. Sem coordenadas, sem nomes, sem cluster ids.)

### 10. Fixture sintética (sem PII real)
```
Nome_Academia: FAKE GYM A..E; coords de landmark SP/RJ (não PII individual);
Total_Alunos_Cluster=500 (fabricado, p/ testar que NUNCA é somado);
1 linha NaN coord (testa drop). Header corrompido 'Munic?pio' reproduz o real.
```
Os testes leem SEMPRE esta fixture, NUNCA `NAO_ABRA/`.

### 11. Housekeeping
```
### BLK-TP-08 — ... (linha 960 de tasks/backlog.md, ÍNTEGRO)
BLK-TP-08 em completed.md: 0 (ainda não movido — correto, é o Passo 6.0 do orquestrador)
housekeeping_move_block.py BLK-TP-08 --check → FALHA (stub ausente): esperado ANTES do move
tests/unit/test_housekeeping_helper.py → 10 passed
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico. A prova anti-PII do artefato foi feita rodando a ingestão real sobre `NAO_ABRA/03_Competidores.xlsx` (caminho de produção da camada) e lendo o parquet do disco — não sobre a fixture. A suíte completa rodou 1× em `-n auto` (gate único). Uso de `tests/fixtures/oferta_academias_menores_fake.xlsx` nos testes unitários é fixture legítima de isolamento (não bypass — regra (b) do prompt).

## Conferência cruzada com log do Builder
Bate integralmente. Colunas do parquet (15, zero PII), cobertura (6.785 hexes, 99,2% casam), DEDUP (1.425 overlap, 39,4%/62,7%), 24.045 acad / 1.920.955 alunos — todos idênticos aos números reportados. A contagem de suíte do Builder foi de SUBCONJUNTO (241) — o gate único de suíte completa (1251 passed, 1 skipped) é meu, e passa. O ponto do Builder sobre falso-positivo da checagem de isolamento por substring (docstring contém "dashboard"/"censo") confere: a verificação correta é por AST, que passa.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; módulo isolado não toca M1; fórmula/pesos renda=0.40/pop=0.60 intactos)
- Artefatos M1 preservados: sim (mtime dos 4 oficiais IDÊNTICO antes/depois de rodar a ingestão real; nenhum aparece no git status)
- `oferta_efetiva_disponivel`/`score_oportunidade_residual` NÃO recompostos: sim (`hexagonos_mercado_mapeado.parquet` mtime inalterado; `calcular_colunas_mercado.py` NÃO tocado)
- Anti-PII (DEC-012): sim — PII dropada na fronteira; parquet real com ZERO colunas PII; `Total_Alunos_Cluster` nunca somado; fonte/artefatos gerados gitignored; testes com fixture sintética
- Isolamento de pacote (AST): sim (não importa pipelines.m1/dashboard/censo/api)
- Parâmetros canônicos §3: preservados (H3_RESOLUTION=7 reutilizado como chave de join; nenhum parâmetro de score introduzido)
- Escopo respeitado: sim — só os arquivos do plano (módulo novo, __init__ exports, teste novo, fixture, doc §7); D1/D3 (dedup só relatório sem flag no parquet; distribuição por Plano presente), D4 (só ingere, não integra)

## Decisão recomendada
Fechar ciclo. Orquestrador executa o Passo 6.0 (housekeeping_move_block.py BLK-TP-08 + commit por path). Integração ao residual = BLK-TP-09 sob gate próprio.
