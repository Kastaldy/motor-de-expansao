# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA (sempre em Opus 4.8). Sem gate humano adicional — o gate de UX (cor/toggle/tooltip) já foi
aprovado por Felipe em 2026-07-15 antes deste ciclo (ver `tasks/current_task.md`).

## Entendimento da tarefa
BLK-TP-03-FU1 implementado à risca conforme o plano técnico numerado (passos 1-14) do handoff
do Planner (`context/handoff/20260715-172232-planner.md`), com os 3 itens do gate humano já
confirmados por Felipe (cor roxa `#7C5CFF`, rótulo "Vazio competitivo LC", tooltip
título+4 linhas) substituindo os placeholders do plano. Overlay opcional (default OFF) do
Mapa Territorial que expõe os 229 hexes de "vazio competitivo" do concorrente low-cost
(`data/staging/vazios_competitivos_lc.parquet`, contrato `vazios_competitivos_v1`, BLK-TP-03).

## O que foi feito

### `src/motor_expansao/dashboard/components.py`
1. Constantes `_VAZIOS_LC_FILL_RGBA = [124, 92, 255, 60]` e `_VAZIOS_LC_LINE_RGBA = [124, 92, 255, 230]`
   perto de `_DISCARDED_FILL`/`_NAN_SCORE_FILL`, com comentário citando o gate humano (Felipe,
   2026-07-15).
2. Nova função `_build_vazios_competitivos_layer(vazios_df)` perto de `_build_ancoras_dominio_layer`:
   no-op silencioso (retorna `None`) se `vazios_df` é `None`/vazio/sem `hex_id`; dedup defensivo
   por `hex_id`; projeta `layer_df` com `hex_id` + `tooltip_title`/`tooltip_line_1..14` (linhas 1-4
   preenchidas via `format_int`/`format_score`/`_clean_tooltip_value`, já existentes no arquivo;
   linhas 5-14 vazias); retorna `pdk.Layer("H3HexagonLayer", ...)` com a cor do gate.
3. Nova função `render_vazios_competitivos_legend()` perto de `render_ancoras_dominio_legend`
   (item opcional do plano — **implementado**, não pulado).
4. `build_unified_map_figure`: novo parâmetro `vazios_df: pd.DataFrame | None = None`; injeção
   da camada logo depois do bloco `ancoras_dominio`, antes de `_stabilize_layer_ids`, gated por
   `"vazios_competitivos_lc" in enabled_overlays`.
5. `build_unified_map_figure_cached`: 2 novos parâmetros keyword-only com default
   (`_vazios_df: pd.DataFrame | None = None`, `vazios_token: str = "none"`), repassando
   `vazios_df=_vazios_df` à chamada interna. Ordem dos 4 posicionais `_df`/`_competitors_df`/
   `_ultra_df`/`_dominio_df` preservada — compatível com qualquer chamador existente.

### `src/motor_expansao/dashboard/pages.py`
6. Constante `_VAZIOS_COMPETITIVOS_LC_PATH = Path("data/staging/vazios_competitivos_lc.parquet")`
   perto de `_GEOCODE_CACHE_DIR`.
7. Loader `_load_vazios_competitivos_lc_cached(path_str: str)` (`@st.cache_data`), lazy + offline,
   `None` em qualquer falha (arquivo ausente/vazio/sem `hex_id`/exceção de leitura) — nunca lança.
8. Checkbox splice dentro de `_render_mapa_fragment`, logo após o `st.multiselect` "Overlays" e
   ANTES dos early-returns de modo/domínio e do bloco `_show_rede_filter`/expander "Legenda":
   `st.checkbox("Vazio competitivo LC", value=False, key="mapa_territorial_vazios_lc", help=...)`
   quando o parquet existe; senão `st.caption("Vazio competitivo LC indisponível neste ambiente
   (parquet não encontrado).")`. Quando ligado, `enabled_overlays = [*enabled_overlays,
   "vazios_competitivos_lc"]` em memória (não registrado em `OVERLAYS`/`constants.py`).
9. Chamada a `build_unified_map_figure_cached(...)` ganhou `_vazios_df=` e `vazios_token=`
   (usando `_map_frame_token`, mesmo padrão dos demais tokens).
10. `render_vazios_competitivos_legend` importado de `components` e conectado em
    `_render_unified_legend` (`if "vazios_competitivos_lc" in enabled_overlays: ...`).
11. **Não tocados**: `render_sidebar_filters`, `render_coord_search_sidebar`,
    `_render_multihex_panel`, `render_mapa_pydeck_fragment`, `render_mapa_territorial`,
    `constants.py`, `streamlit_app.py`, `demanda_revelada/` (confirmado via `git diff --stat`
    filtrado — zero linhas tocadas nesses arquivos).

### `tests/integration/test_streamlit_app.py` (6 testes novos, cobrindo os passos 11-14 do plano)
- `test_build_unified_map_figure_overlay_vazios_competitivos_ligado_vs_desligado` (builder-level,
  padrão do teste `ancoras_dominio`): liga/desliga + `None`/vazio → no-op silencioso.
- `test_overlay_vazios_competitivos_nao_altera_score_nem_mutacao_dos_frames` (READ-ONLY M1):
  `df` de entrada não mutado (`assert_frame_equal`); `fill_color`/`line_color` do hex_layer
  principal idênticos com/sem overlay.
- `test_toggle_vazios_competitivos_existe_e_default_off`: parquet mockado presente → checkbox
  chamado com `value=False`/`key="mapa_territorial_vazios_lc"`; deck com 1 layer (OFF).
- `test_toggle_vazios_competitivos_ligado_adiciona_layer`: checkbox `True` → deck com 2 layers;
  última layer tem `get_fill_color == _VAZIOS_LC_FILL_RGBA` e tooltip formatado corretamente
  (`tooltip_line_1..4`).
- `test_toggle_vazios_competitivos_parquet_ausente_oculta_checkbox`: loop nos 3 casos (ausente/
  vazio/sem `hex_id`, todos mockados como `None` no loader) → checkbox NUNCA chamado com a key
  canônica + caption "indisponível" emitido.
- Import novo: `_VAZIOS_LC_FILL_RGBA` de `motor_expansao.dashboard.components` no bloco de import
  do topo do arquivo (junto de `_DISCARDED_FILL`/`_NAN_SCORE_FILL`).

**Nota de correção durante a implementação:** a primeira versão do teste de "parquet ausente"
usava um `side_effect=lambda` capturando uma variável (`captions_captured`) reatribuída a cada
iteração do loop `for _caso in (...)`, o que o `ruff` sinalizou como **B023** (closure sobre
variável de loop). Corrigido substituindo o `side_effect` por `mock.patch("streamlit.caption")
as caption_mock` e extraindo as mensagens de `caption_mock.call_args_list` depois do bloco `with`
— sem `lambda` fechando sobre estado mutável do loop. `ruff check` limpo depois da correção.

## Validações executadas (resultados exatos)
- `python -m pytest -q tests/integration/test_streamlit_app.py -k "vazios"` →
  **5 passed, 249 deselected** (0 falhas).
- `python -m pytest -q tests/integration/test_streamlit_app.py` (suíte de integração completa) →
  **254 passed** (0 falhas, 0 erros de coleta) — nenhum dos 4 modos de mapa/overlays existentes
  regrediu.
- `python -c "import streamlit_app; print('import ok')"` → `import ok` (warnings de bare-mode
  do Streamlit são ruído esperado/pré-existente, não erro).
- `python -m ruff check src/motor_expansao/dashboard/components.py
  src/motor_expansao/dashboard/pages.py tests/integration/test_streamlit_app.py` →
  **All checks passed!**
- `python -m mypy src/motor_expansao/dashboard/components.py
  src/motor_expansao/dashboard/pages.py` → **Success: no issues found in 2 source files**
  (0 issues novos; nenhum issue pré-existente fora do escopo a documentar).
- Ambiente local é Python 3.14 (não houve necessidade de cair para serial por WinError 6 — a
  suíte de integração completa acima já rodou em modo serial `pytest -q` diretamente).
- Suíte FULL (`pytest -n auto`/serial) **NÃO** foi rodada pelo Builder — é o gate único do QA,
  conforme instrução do orquestrador.

## Verificação dos guardrails
- **Aditivo puro**: `git diff --stat` mostra só inserções (+413/-0) nos 3 arquivos editáveis;
  `git diff --stat` filtrado para `constants.py`/`streamlit_app.py`/`demanda_revelada/` retorna
  vazio (zero linhas tocadas).
- **`_downsample_map_index`, `MAP_POINT_LIMIT`, `MAP_POINT_LIMIT_LARGE`,
  `MAP_SOURCE_COLUMNS_M1`, `MAP_SOURCE_COLUMNS_HYBRID`**: não tocados (confirmado por leitura do
  diff — o overlay nunca passa por eles, camada de 229 hexes sempre desenhada inteira).
- **READ-ONLY M1**: `score_priorizacao` só é LIDO (via o parquet de vazios já enriquecido no
  BLK-TP-03) para formatar `tooltip_line_4`; nenhuma coluna de score/carteira/plano é recalculada.
  Coberto por teste (`test_overlay_vazios_competitivos_nao_altera_score_nem_mutacao_dos_frames`).
- **mtime dos 4 artefatos oficiais do M1**: os 4 parquets (`hexagonos_brasil_dashboard.parquet`,
  `brasil_estrutural.parquet`, `brasil_priorizados.parquet`,
  `hexagonos_brasil_oportunidades.parquet`) **não existem neste worktree** (staging é gitignored
  e não foi materializado aqui — condição já sinalizada em `tasks/current_task.md` antes do
  início deste bloco). Nenhum comando de regeneração de pipeline foi executado pelo Builder;
  `git status --porcelain` confirma que só os 3 arquivos editáveis (+ os 2 handoffs versionados
  já existentes de ciclos anteriores) aparecem como alterados/novos — nada em `data/`. QA deve
  verificar mtime no próprio ambiente (onde os parquets existirem) para fechar este critério.
- **Sem rede**: loader é `Path.exists()` + `pd.read_parquet` local, sem chamada de rede.
- **Confirmação do risco "acoplamento com `enabled_overlays`/`OVERLAYS`"**: reconfirmado por
  leitura direta do código antes de editar — os 3 consumidores em `pages.py`
  (`_render_unified_legend`, a chamada a `build_unified_map_figure_cached` via
  `enabled_overlays=tuple(sorted(enabled_overlays))`, e a lógica de caption de pins amostrados
  ~linha 4454, agora ~4520) fazem só checagem de string (`"x" in enabled_overlays`), nunca
  `OVERLAYS[...]` — splice seguro, `constants.py` intocado.
- **DEC-001 (pesos M1)**: nada neste bloco toca `renda=0.40`/`pop=0.60` nem a fórmula.
- **DEC-012 (anti-PII)**: nenhuma coluna nova adicionada ao artefato de vazios (só leitura/
  exibição); nenhuma coluna de `COLUNAS_PII_PROIBIDAS` entra no `layer_df` do overlay.

## Passo opcional (legenda) — decisão
Implementado (não pulado): `render_vazios_competitivos_legend()` em `components.py` + hook em
`_render_unified_legend` (`pages.py`). Segue o padrão visual de `render_ancoras_dominio_legend`
(chip com dot colorido + rótulo), cor `#7C5CFF` alinhada ao overlay.

## Arquivos afetados (finais)
- `src/motor_expansao/dashboard/components.py`
- `src/motor_expansao/dashboard/pages.py`
- `tests/integration/test_streamlit_app.py`
- `context/handoff.md` (este arquivo) + cópia versionada
  `context/handoff/20260715-183906-builder.md`
- `tasks/current_task.md` (status atualizado para "aguardando QA")

## Fora de escopo (não tocado, conforme handoff do Planner)
- `constants.py`, `streamlit_app.py`, `demanda_revelada/{vazios_competitivos,contrato,ingestao}.py`.
- Nenhum recálculo/alteração de `score_priorizacao`/pesos/carteira/plano/artefatos oficiais.
- Regeneração do parquet `vazios_competitivos_lc.parquet` (consumido como está — nem sequer
  existe neste worktree, só mockado em testes).
- `tasks/backlog.md`/`tasks/completed.md` — deixado para o orquestrador no fechamento (housekeeping
  não é responsabilidade do Builder neste ciclo, por instrução explícita do prompt).

## Pendências para o QA
1. Rodar a suíte FULL (`pytest -n auto`, fallback serial `pytest -q` se WinError 6) — gate único,
   0 falhas/0 erros de coleta esperado.
2. Verificar `mtime` dos 4 artefatos oficiais do M1 antes/depois do ciclo, no ambiente onde eles
   existirem (não existem neste worktree do Builder — ver nota acima).
3. Confirmar visualmente (se aplicável) que o checkbox aparece nos controles do Mapa Territorial
   e que a cor/tooltip batem com o gate humano de Felipe (2026-07-15).
