# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-RELPON-06 — Legibilidade dos mapas (dashboard + PDF) + linha de dado por RAIO (densidade sobre área válida)

## O que foi feito
Executado o plano REVISADO do Planner (v2, pós D3/D4 de Vinicius 2026-07-14) sem expansão de escopo.

**D1 (`censo_point.py`)** — campo novo `densidade_pop_raio_valida_hab_km2` no `result` de
`analisar_ponto_censitario_setores`: `pop_total_raio / (area_intersecao_total_m2 / 1e6)`
(população do raio ÷ área de espaço VÁLIDO, excluindo água/vazio), calculado dentro do bloco
`if pop_weights.notna().any():`, com guarda de divisão por zero (`area_valida_m2 and area_valida_m2
> 0`) — fica `None` quando não há setores intersectados ou a área válida é 0. Os 5 campos
`*_setor_ponto` do BLK-RELPON-05 **permanecem** intocados no `result`. Docstring da função atualizada.

**D3 (`censo_map.py`, `_font()`)** — reescrita para usar **exclusivamente**
`ImageFont.load_default(size=size)` (fonte TrueType embutida do Pillow), removendo
`ImageFont.truetype("arial.ttf", size)` e o fallback `load_default()` sem `size`. Verificado
empiricamente ANTES de codar: `PIL.__version__ = 12.2.0` (ambiente local; `constraints.txt` pina
`12.3.0`), `load_default(size=40)` devolve `FreeTypeFont` com bbox proporcional ao `size` (escala de
fato — o bug de produção era `_font(20)` e `_font(60)` renderizarem IGUAL no `python:3.11-slim` sem
fontes do sistema).

**D4 (`censo_map.py`, fontes+layout na BASE)** — novas constantes de tamanho (`_FS_TITULO=44`,
`_FS_VALOR_RAIO=38`, `_FS_LEGENDA_TITULO=34`, `_FS_LEGENDA_CORPO=32`, `_FS_BODY=26`,
`_FS_FOOTER=22`, `_FS_ESCALA=24`) e de layout (`_LEGEND_COL_W=330`, `_MAP_TOP=132`, `_VALOR_Y=78`)
aplicadas em `_map_box`, `_render_camada` e `_draw_legend_camada`/`_draw_scale_bar`. **Sem parâmetro
de escala por-caminho** — UM único render vale para dashboard, PDF e API; `pages.py` e
`tests/integration/test_streamlit_app.py` NÃO tocados.

**Achados do ajuste visual (2 bugs reais, corrigidos, fora do "ponto de partida" do Planner):**
ao gerar e inspecionar os PNGs/PDF de exemplo, dois textos secundários da legenda **transbordavam
do canvas** a `_FS_LEGENDA_CORPO`/`_FS_LEGENDA_TITULO` (362px e 448px de largura vs. orçamento de
~252px/320px da coluna): (a) a caption "Pins: Ultra e concorrentes"; (b) o subtítulo mais longo
("Renda per capita (R$/pessoa)"). Corrigido com **duas constantes de fonte novas, dedicadas e
menores** — `_FS_LEGENDA_CAPTION=20` (para "Ponto central"/"Pins: Ultra e concorrentes") e
`_FS_LEGENDA_SUBTITULO=22` (para o subtítulo da legenda) — mantendo `_FS_LEGENDA_CORPO=32` só para
os rótulos de faixa (o "pior caso" ancorado no alvo de legibilidade do PDF). Coberto por 2 testes
novos de regressão (`test_legenda_caption_pins_nao_transborda_do_canvas`,
`test_legenda_subtitulo_mais_longo_nao_transborda_do_canvas`).

Também ajustado (sem mudar critério de aceite): `test_fallback_offline_canvas_claro` amostrava o
pixel EXATO do centro do `_map_box`, que — com o `_MAP_TOP`/`_LEGEND_COL_W` novos — passou a cair
na ponta do pin vermelho do ponto central; corrigido para amostrar um canto do interior da caixa
(fora do círculo/pin), mantendo o objetivo original do teste (canvas de fallback é claro).

**D1 no mapa (`censo_map.py`)** — `_legenda_valor_ponto` mudou o rótulo de "no ponto" para "no
raio"; os 3 valores da faixa superior (densidade/renda/score) passaram a ler
`densidade_pop_raio_valida_hab_km2` / `renda_per_capita_media_raio` / `score_setor_medio` do
`result` (agregados do RAIO) em vez dos 5 campos `*_setor_ponto` do BLK-RELPON-05. Variáveis locais
renomeadas `valor_ponto_*` → `valor_raio_*`; o kwarg `valor_ponto=` de `_render_camada` foi mantido
(identificador, não texto de usuário). Docstrings/comentários que diziam "valor BRUTO do setor que
CONTÉM o ponto" atualizados para refletir a reversão.

**`censo_report.py`** — só docstring/comentários (2 blocos) atualizados para "no raio"; **zero
mudança de lógica** (confirmado pela suíte de export, 27/27 verdes sem edição de asserts).

**Testes** — motor: 3 novos asserts (densidade nova vs. antiga nos 2 casos de interseção +
"n/d" no caso sem setores). Mapa: reversão da faixa travada
(`test_valor_ponto_repassado_aos_3_choropleths_nao_a_concorrentes` + novo bloco "no raio"/"no
ponto"); `test_valor_ponto_nd_quando_ponto_fora_da_malha` **renomeado e corrigido** para
`test_valor_raio_nao_e_nd_quando_setor_nao_cobre_o_ponto_mas_intersecta_raio` (sob D1 esse cenário
NÃO é mais "n/d" — é valor real do raio); teste novo `test_valor_raio_e_nd_quando_setor_fora_do_raio`
cobre o "n/d" de verdade (sem setores intersectados); 3 testes novos de fonte
(`test_font_escala_com_o_size`, `test_font_nao_chama_truetype_do_sistema_no_proprio_codigo`,
`test_font_nao_depende_de_ttf_do_sistema`); 4 testes novos de contrato de legibilidade/layout em
código (`test_legenda_corpo_atinge_o_alvo_de_legibilidade_no_pdf`,
`test_rotulo_mais_longo_da_legenda_cabe_na_coluna`,
`test_legenda_subtitulo_mais_longo_nao_transborda_do_canvas`,
`test_legenda_caption_pins_nao_transborda_do_canvas`).

**`docs/relatorio_pontual_censitario.md`** — §3 documenta o campo novo e a reversão do destino dos
5 campos `*_setor_ponto`; §6 reescreve o parágrafo da faixa ("no raio"), corrige a imprecisão
pré-existente `(28,52)`/`_font(14)` e documenta a fonte embutida (D3), a tabela de tamanhos
antigo→novo e as novas constantes de layout/lista de testes de legibilidade.

**Ajuste visual (obrigatório, feito antes do handoff)** — gerado localmente, via script em
`data/cache`-equivalente do scratchpad (NÃO commitado): as 4 camadas PNG (1000×760, `basemap=False`)
e o PDF completo (5 páginas) com dado sintético (3 setores, 2 concorrentes, 1 Ultra), renderizados
com `PyMuPDF` para inspecionar as páginas 2 e 3 no tamanho real do PDF. Resultado: título/linha de
dado/legenda nitidamente legíveis nas duas superfícies (antes ~6pt borrado no PDF; agora
legenda-corpo ≈9,6pt, título ≈13,1pt); os 2 bugs de overflow acima foram encontrados e corrigidos
NESTA etapa. Nenhum artefato de exemplo foi commitado (anti-PII, dado sintético).

## Arquivos alterados
- `src/motor_expansao/dashboard/censo_point.py` — campo novo `densidade_pop_raio_valida_hab_km2` (D1) + docstring.
- `src/motor_expansao/dashboard/censo_map.py` — `_font()` reescrita (D3); constantes `_FS_*`/`_LEGEND_COL_W`/`_MAP_TOP`/`_VALOR_Y` (D4, incl. `_FS_LEGENDA_SUBTITULO`/`_FS_LEGENDA_CAPTION` adicionadas no ajuste visual); `_map_box`, `_render_camada`, `_draw_legend_camada`, `_draw_scale_bar` atualizados; faixa "no raio" (D1) com fontes de origem trocadas para os agregados do raio.
- `src/motor_expansao/dashboard/censo_report.py` — só docstrings/comentários ("no raio"), sem mudança de lógica.
- `tests/unit/test_relatorio_pontual_censitario_motor.py` — 3 asserts novos de D1.
- `tests/unit/test_relatorio_pontual_censitario_mapa.py` — reversão da faixa travada; teste renomeado/corrigido; teste novo de "n/d" real; 7 testes novos (fonte D3 + legibilidade/overflow D4); `test_fallback_offline_canvas_claro` ajustado ao novo layout.
- `docs/relatorio_pontual_censitario.md` — §3 (campo novo) e §6 (faixa "no raio", fonte embutida, tabela de tamanhos, constantes de layout, lista de testes) reescritos.
- `context/handoff.md` (sobrescrito) / `context/handoff/20260714-123550-builder.md` (snapshot novo).
- `tasks/current_task.md` (status atualizado para aguardando QA).

**NÃO alterados** (conforme escopo/D3/D4): `pages.py`, `tests/integration/test_streamlit_app.py`,
`Dockerfile.streamlit`, `tests/unit/test_relatorio_pontual_censitario_export.py` (só executado),
`tasks/backlog.md`, `tasks/completed.md` (ver Pendências — mover o bloco é passo pós-QA, seguindo o
precedente do fechamento do BLK-RELPON-05).

## Validações executadas
```
python -m pytest -q tests/unit/test_relatorio_pontual_censitario_motor.py tests/unit/test_relatorio_pontual_censitario_mapa.py tests/unit/test_relatorio_pontual_censitario_export.py tests/integration/test_streamlit_app.py
→ 310 passed, 0 failed, 0 skipped (59.22s, serial — não tentei -n auto pois o subconjunto já roda rápido)
```
```
python -c "import streamlit_app; print('import ok')"
→ import ok
```
```
python -m ruff check src/motor_expansao/dashboard/censo_point.py src/motor_expansao/dashboard/censo_map.py src/motor_expansao/dashboard/censo_report.py
→ All checks passed!
```
```
python -m mypy src/motor_expansao/dashboard/censo_point.py src/motor_expansao/dashboard/censo_map.py src/motor_expansao/dashboard/censo_report.py
→ Success: no issues found in 3 source files
```
Premissa do D3 confirmada ANTES de codar:
```
python -c "import PIL; print(PIL.__version__); from PIL import ImageFont; f=ImageFont.load_default(size=40); print(type(f).__name__, f.getbbox('Densidade'))"
→ 12.2.0 FreeTypeFont (0, 9, 195, 40)
```
Suíte FULL: **NÃO executada** — por instrução explícita do orquestrador nesta tarefa ("NÃO rode a
suíte full, é do QA"), que tem precedência sobre a menção à suíte completa no plano do Planner. O
subconjunto acima cobre 100% dos arquivos tocados + o smoke de import.

## Problemas encontrados
2 bugs reais de overflow de texto descobertos no ajuste visual obrigatório (não presentes no plano
do Planner como "ponto de partida", mas antecipados como risco): a caption de pins e o subtítulo
mais longo da legenda transbordavam do canvas às fontes originalmente propostas
(`_FS_LEGENDA_CORPO=32`/`_FS_LEGENDA_TITULO=34`). Corrigidos com 2 constantes de fonte novas e
dedicadas, menores, documentadas acima — nenhuma mudança de escopo, cores, choropleth ou geometria
de faixa. `test_fallback_offline_canvas_claro` precisou de ajuste de amostragem de pixel (não de
critério) porque o pixel central passou a coincidir com o pin do ponto central sob o layout novo.

## Pendências
- **Revisão visual humana** (gate que resta, conforme o plano): inspecionar o PNG no dashboard
  (`st.image`, aba Mapa Territorial) e os slides 2/3 do PDF em produção/staging. Os artefatos de
  exemplo gerados pelo Builder (4 PNGs + PDF completo, dado sintético) estão no scratchpad local,
  NÃO commitados (anti-PII) — reproduzíveis com o script usado, se o QA/humano quiser regenerar.
- **Mover BLK-RELPON-06 de `tasks/backlog.md` para `tasks/completed.md`**: NÃO feito neste passo —
  seguindo o precedente do fechamento do BLK-RELPON-05 (`tasks/completed.md`, "Fechamento de ciclo
  — BLK-RELPON-05"), essa movimentação é escrita DEPOIS do veredito do QA, não pelo Builder.
- `CLAUDE.md` §5 pode ganhar uma linha de sumário do ciclo após aprovação do QA (padrão dos ciclos
  anteriores) — não fiz essa edição por não estar no escopo/arquivos-alvo autorizados do handoff.

## Riscos remanescentes
- Os números da faixa mudam nos relatórios já gerados (esperado, é o ponto do bloco): a densidade
  nova é sempre `>=` a antiga (denominador menor, exclui água/vazio) — coberto por teste.
- Byte-identidade dos PNGs quebra de propósito em relação ao estado anterior em Windows (fonte Arial
  → fonte embutida do Pillow) — decisão explícita de Vinicius (D3), documentada.
- A área útil do mapa encolheu (~663×590 → ~585×550 px inner) para acomodar título/faixa/legenda
  maiores; o choropleth continua cobrindo o frame inteiro (sem mudança de cor/faixa). Se o gate
  visual humano achar o mapa pequeno demais, a alavanca é reduzir os `_FS_*` (custo: menos
  legibilidade) — aumentar `width`/`height` não ajuda (insight matemático do plano, não mexido).

## Guardrails verificados
- score_priorizacao não alterado: sim (não tocado; `censo_*` é camada paralela READ-ONLY sobre o M1).
- Artefatos M1 preservados: sim (nenhum artefato oficial tocado nesta mudança de render/semântica da camada censitária).
- Dashboard offline mantido: sim (`pages.py` intocado; nenhuma nova dependência de rede introduzida).
- `setor_censitario_intersecao_area_1p5km`/raio 1,5 km/`RAIO_CENSITARIO_DEFAULT_KM`: intocados.
- Estrutura das 5 páginas/grid Big Numbers 4x2/marca d'água/`set_compression(False)`: intocados (confirmado pela suíte de export, 27/27 verdes sem edição de asserts).
- Choropleth (cor por setor)/faixas de cor/pins/círculo/basemap: inalterados — só texto/fonte/layout mudaram.
