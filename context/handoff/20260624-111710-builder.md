# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-RELMUN-02 — "Bairros por Zona" com nomes reais de bairro (resolve o D9 do BLK-RELMUN-01). **Direção A2** (gate humano: APROVADO POR Vinicius EM 2026-06-24) + parte comum.

## O que foi feito
Direção A2: a malha geo IBGE 2022 passa a materializar `nome_bairro`/`cod_bairro` (fonte interna/offline, já no DBF), e o Relatório Municipal LÊ a partição geo on-demand para listar bairros reais por zona. NÃO toca a cadeia hot do dashboard (Blocos 4–6, `enrich_dashboard_data`, `censo_extra_cols`, dashboard parquet — isso seria A1). Tudo READ-ONLY sobre o M1 e 100% offline.

**Materialização (A2.1/A2.2) — `materializar_setores_censitarios_geo.py`:**
- `carregar_malha_uf`: força `encoding="utf-8"` em `gpd.read_file` (o `.cpg` diz UTF-8 mas houve mojibake na amostra do Planner com decodificação latin-1) e seleciona `CD_BAIRRO`/`NM_BAIRRO`. Mapeia `nome_bairro` (via novo `_normalizar_nome_bairro`: vazio/"nan"/sentinelas → `pd.NA`, preservando acentos UTF-8) e `cod_bairro` (zfill 13). Sem as colunas no shapefile → NA gracioso.
- `COLUNAS_ARTEFATO`: adiciona `cod_bairro` e `nome_bairro` (após `nome_municipio`).
- `montar_base_setorial_uf`: garante defensivamente `nome_bairro`/`cod_bairro` = NA quando ausentes (robustez para qualquer caller, ex.: malha sintética de teste), antes da seleção final de `COLUNAS_ARTEFATO`.
- **NÃO re-materializei os parquets reais** (~1,17 GB) — job operacional pós-merge, faseável por UF (`materializar_base_geo(ufs=[...])`); fechamento de testes independe deles (DataFrames sintéticos). Contagem de setores esperada inalterada (468.099).

**Relatório (A2.3/A2.4/A2.5 + parte comum) — `relatorio_municipal.py`:**
- Novo helper `_carregar_bairros_por_hex(uf, cod_municipio, censo_geo_dir) -> dict[str,str]`: lê a partição via `ler_particao_setores` com `nome_bairro`+bbox+pop, deriva centróide do bbox do setor, mapeia a `hex_id` res-7 (h3, import lazy), resolve bairro DOMINANTE por hex (maior população). Fallback `{}` se `censo_geo_dir`/cod/partição/coluna ausentes (offline, sem exceção).
- Novo helper PURO `_bairros_por_zona(hex_zona_geo, bairros_por_hex) -> list[dict]`: agrupa bairros distintos por zona (0/1/2), ordena por frequência desc (desempate estável por nome). Sem rede.
- `agregar_municipio` ganha `bairros_por_hex: dict[str,str] | None = None` (default None = comportamento IDÊNTICO ao atual — preserva todos os testes que não passam o param). Popula `result["bairros_por_zona"]` + `result["n_bairros_total"]`.
- `_bairros_page` reescrita: lista bairros reais por zona (cap `_BAIRROS_CAP_POR_ZONA=10` + "... e mais N"), cabeçalho de fonte IBGE quando há bairros. **Fallback gracioso**: sem bairro → zonas geométricas + descrição/tese, SEM a string "Bairros indisponiveis na base atual" como texto principal, sem `raise`. Mantém **9 páginas** e `/Count 9`.
- Docstring do módulo (D9) atualizada: SIMPLIFICADA → IMPLEMENTADA (BLK-RELMUN-02).

**Fiação (`pages.py` + `streamlit_app.py`):**
- Novo helper `_resolve_bairros_por_hex_municipio(df, nome_municipio, uf, censo_geo_dir)` em `pages.py`: resolve `cod_municipio` (coluna do `df` ou fallback `resolve_cod_municipio_from_geo_dir`) e chama `_carregar_bairros_por_hex`; fallback `{}` gracioso.
- `render_relatorio_municipal_download_topo` e `render_relatorio_municipal_expander` ganham `censo_geo_dir: Path | None = None`; ambas resolvem `bairros_por_hex` e passam a `agregar_municipio`.
- Call sites: `streamlit_app.py` passa `CENSO_GEO_DIR` ao topo; `render_mapa_territorial` passa `censo_geo_dir` ao expander.

**Doc/testes:**
- `docs/relatorio_municipal_template.md` D9: "SIMPLIFICADA" → "IMPLEMENTADA" (fonte IBGE `NM_BAIRRO`, heterogeneidade de cobertura, fallback gracioso).
- `tests/unit/test_relatorio_municipal.py`: asserts de fallback da Página 6 atualizados conscientemente (`b"Bairros indisponiveis na base atual"` agora NÃO presente → nova mensagem `b"Bairros nao mapeados na base IBGE 2022"`); novos testes: bairros reais nos bytes do PDF agrupados por zona (`/Count 9`, sem PII), `bairros_por_zona` populado pela fonte, fallback do helper de leitura sem dir, fallback sem bairros sem PII. `test_coexistencia_relatorio_pontual_intocado` mantido verde.
- `tests/integration/test_materializar_setores_censitarios_geo.py`: `_malha_fake` ganha `nome_bairro`/`cod_bairro` (1 setor sem bairro = NA, exercendo cobertura heterogênea) + assert de fluxo da coluna.

## Arquivos alterados
- `src/motor_expansao/pipelines/materializar_setores_censitarios_geo.py` — `_normalizar_nome_bairro` (novo), `carregar_malha_uf` (encoding utf-8 + NM_BAIRRO/CD_BAIRRO → nome_bairro/cod_bairro), `montar_base_setorial_uf` (default NA defensivo), `COLUNAS_ARTEFATO` (+cod_bairro/+nome_bairro).
- `src/motor_expansao/dashboard/relatorio_municipal.py` — `_carregar_bairros_por_hex` (novo), `_bairros_por_zona` (novo), `_BAIRROS_CAP_POR_ZONA` (novo), `agregar_municipio` (param `bairros_por_hex` + `bairros_por_zona`/`n_bairros_total` no result), `_bairros_page` (reescrita real + fallback), docstring D9.
- `src/motor_expansao/dashboard/pages.py` — import `_carregar_bairros_por_hex`, `_resolve_bairros_por_hex_municipio` (novo), `censo_geo_dir` nos 2 renders + fiação a `agregar_municipio` + call site do expander.
- `streamlit_app.py` — `censo_geo_dir=CENSO_GEO_DIR` no `render_relatorio_municipal_download_topo`.
- `docs/relatorio_municipal_template.md` — D9 IMPLEMENTADA.
- `tests/unit/test_relatorio_municipal.py` — asserts da Página 6 atualizados + 4 testes novos.
- `tests/integration/test_materializar_setores_censitarios_geo.py` — `_malha_fake` com bairro + assert de fluxo.

## Validações executadas
- `pytest -p no:xdist tests/unit/test_relatorio_municipal.py`: **27 passed** (era 23; +4 novos). POR QUÊ: módulo central do bloco.
- `pytest -p no:xdist tests/integration/test_materializar_setores_censitarios_geo.py`: **3 passed**. POR QUÊ: pipeline tocado (nova coluna `nome_bairro`).
- `pytest -p no:xdist tests/integration/test_streamlit_app.py`: **211 passed**. POR QUÊ: fiação em `pages.py`/`streamlit_app.py` (smoke do app).
- `pytest -p no:xdist tests/ -k "censo or censitario or streamlit_app or relatorio or materializar or setores_geo"`: **346 passed, 0 failed** (após o fix do pipeline; coexistência do Relatório Pontual verde).
- `python -c "import streamlit_app; print('import ok')"`: **ok**.
- `python -m ruff check src tests`: **All checks passed!**
- `python -m mypy src/motor_expansao/dashboard/relatorio_municipal.py src/motor_expansao/pipelines/materializar_setores_censitarios_geo.py`: **Success, no issues**. (+ `pages.py`: Success.)

### Total impacto (Builder): pytest = 0 failed em todos os subconjuntos rodados; nenhum skip novo.
### NOTA ao QA — ambiente:
- `pytest -n auto` (xdist) está **quebrado neste ambiente local** (Windows/Python 3.14): `INTERNALERROR ... execnet OSError: cannot send (already closed?)` na criação dos workers — é falha de ambiente do xdist, NÃO defeito do código. Rodei tudo com `-p no:xdist` (serial). O QA deve rodar a suíte full do jeito que conseguir (serial se o `-n auto` falhar igual).
- `ruff format --check` reporta "would reformat" em `relatorio_municipal.py`/`pages.py`/`materializar_setores_censitarios_geo.py`/`streamlit_app.py` — **CONFIRMADO PRÉ-EXISTENTE** (via `git stash` antes das minhas edições, os mesmos 4 arquivos já estavam não-conformes). O gate do projeto é `ruff check` (passa). NÃO rodei `ruff format` para não reescrever centenas de linhas pré-existentes fora do escopo do bloco; minhas linhas seguem o estilo do entorno.

## Problemas encontrados
- 2 falhas iniciais em `test_materializar_setores_censitarios_geo.py` (KeyError: `nome_bairro` ausente na malha sintética ao selecionar `COLUNAS_ARTEFATO`). Corrigido com default NA defensivo em `montar_base_setorial_uf` + bairro no `_malha_fake`. Resolvido.

## Pendências
- **Job operacional pós-merge (não bloqueia testes):** re-materializar `data/outputs/setores_censitarios_2022_geo/` para popular `nome_bairro` em todos os municípios. Recomendado faseado por UF, validando RJ (~73% cobertura) e DF (0%) antes do batch, e checando contagem de setores inalterada (468.099) e acentos preservados. Sem isso, em produção a Página 6 cai no fallback gracioso (zonas geométricas) até a partição ter a coluna.

## Riscos remanescentes
- Cobertura de `NM_BAIRRO` é HETEROGÊNEA por município (medido pelo Planner: RJ 73%, AC 10%, DF 0%) → o fallback gracioso é o comportamento esperado, não defeito, para municípios sem bairro.
- Resolução intra-hex: um hex res-7 cobre vários setores/bairros; resolvemos bairro DOMINANTE por população (escolha A2, preserva resolução do setor melhor que A1).

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; nenhum toque em `config.py`/`pipelines/m1/`/pesos/score).
- Artefatos M1 preservados: sim (só artefato derivado censitário geo recebe coluna nova; M1 oficial intocado; 100% das colunas/linhas do M1 preservadas).
- Dashboard offline mantido: sim (A2 é 100% offline; `_carregar_bairros_por_hex` lê parquet local; nenhuma dependência de rede nova).
- Relatório Pontual Censitário BYTE-A-BYTE intocado: sim (`censo_report.py`/`censo_map.py`/`analisar_ponto_censitario_setores`/`setor_censitario_intersecao_area_1p5km` não tocados; `test_coexistencia_relatorio_pontual_intocado` verde).
- Blocos 4–6 intocados: sim (`load_uf_slice`/`read_enriched_uf_partition`/`build_dashboard_dataset`/`render_tab_selector` não tocados; A2 não mexe na cadeia hot).
- PDF segue 9 páginas / `/Count 9` / mesma ordem: sim.
