# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-DIM-12 — UI da esteira property-first: ferramenta de viabilidade do imóvel no dashboard (Opção B — nova aba "Viabilidade do Imovel")

## O que foi feito
Implementada a 5ª aba do dashboard ("Viabilidade do Imovel"), property-first, que stress-testa a viabilidade de um imóvel real reusando o engine `analisar_viabilidade_ponto` (BLK-DIM-11, INTOCADO). Conforme o gate humano (Felipe, 2026-06-15) e o "Delta para Opção B" do Planner.

1. **`render_viabilidade_ponto` (nova) em `pages.py`** — Seções 1-8 do design:
   - **Seção 1 (captura própria, Opção B):** helper `_resolve_viab_ponto` lê o campo `viab_ponto_coord_raw` (coordenada `lat,lng` OU link do Google Maps) em cascata 100% offline: `parse_coordinate_input` → `extract_any_coord` (import lazy de `api.maps_geocoder`, string pura) + `_validate_brazil_bbox`; fallback para `search_pin` (clique/sidebar). Sem ponto → `st.info` e `return`.
   - **Seção 2:** `st.form(key="viab_ponto_form")` com `m2`, `aluguel_pedido`, toggle `usar_p50`, `demanda_premissa` (number_input visível/editável), e expander "Parametros avancados" (`ticket_medio`, `margem_alvo_pct`). Engine roda **só on-submit** (`st.form_submit_button`).
   - **Toggle p50 NUNCA silencioso:** pré-calcula `faixa_alunos_por_densidade(m2, base)` fora do form só para preencher o `value=` do number_input de demanda; o engine recebe SEMPRE o valor da caixa.
   - **Seções 3-8:** guardrail visível (`demanda_fonte`), cards do cenário pedido (break-even, aluguel-teto, margem, payback, ROIC, faturamento, EBITDA, viável), faixa p10/p50/p90 por densidade (com "n/d" se sem comparáveis), contexto do entorno (catchment + zona morta com modo degradado), grade de sensibilidade (pivot `margem_liq` com `background_gradient` nativo do pandas Styler — sem dep nova), e caption do pino.
   - Catchment: resolve `setores_df` via `_resolve_censo_context` + `censo_geo_loader` (igual ao censitário); ausência → modo degradado sem exceção (`setores_df=None`).
   - **Prefixo `viab_ponto_*` em TODAS as keys.**

2. **Imports em `pages.py`:** `_validate_brazil_bbox` (de `dashboard.data`), `analisar_viabilidade_ponto`, `RAIO_CATCHMENT_KM`, `SIM_MENSALIDADE_BALCAO`. `extract_any_coord` e `faixa_alunos_por_densidade` importados lazy dentro das funções.

3. **`DASHBOARD_TAB_LABELS`** em `pages.py`: adicionada 5ª aba `"Viabilidade do Imovel"`.

4. **`streamlit_app.py`:** `BASE_CALIBRACAO_PATH` + `load_base_calibracao()` com `@st.cache_data` derivando `alunos_por_m2 = alunos_reais/metragem` (achado load-bearing 1 — sem isso a faixa sai sempre "n/d"); import de `render_viabilidade_ponto`; branch `elif active_tab == "Viabilidade do Imovel":` chamando `render_viabilidade_ponto(search_pin, filtered_df, censo_geo_loader=load_censo_geo_setores, censo_geo_dir=CENSO_GEO_DIR, base_calibracao_df=load_base_calibracao())`.

5. **Testes** em `tests/integration/test_streamlit_app.py` (7 novos + 1 ajuste): nomes do plano de testes + helpers `_ctx_mgr`/`_viab_ponto_patches`/`_viab_ponto_df`/`_fake_viab_result`. Ajustado `test_render_tab_selector_e_exportado` (constante de labels agora tem 5 entradas — mudança aprovada).

## Arquivos alterados
- `src/motor_expansao/dashboard/pages.py` — nova `render_viabilidade_ponto` + helper `_resolve_viab_ponto`; imports do engine/parser/config; `"Viabilidade do Imovel"` em `DASHBOARD_TAB_LABELS`. (`render_mapa_territorial` NÃO alterado — Opção B.)
- `streamlit_app.py` — `BASE_CALIBRACAO_PATH`, `load_base_calibracao()` (deriva `alunos_por_m2`), import + branch da nova aba.
- `tests/integration/test_streamlit_app.py` — 7 testes novos (guardrail anti-geográfico, toggle p50, faixa n/d, loader, smoke sem pino, parse link Maps, aba registrada) + ajuste do teste de labels.
- `context/handoff.md` + snapshot `context/handoff/20260615-174958-builder.md`.
- `tasks/current_task.md` — status: aguardando QA.

## Validações executadas
- ruff (`pages.py streamlit_app.py test_streamlit_app.py`): **All checks passed!**
- mypy (`pages.py streamlit_app.py`): **Success: no issues found in 2 source files**
- pytest subset `-k "viabilidade or base_calibracao"` (`-n auto`): **7 passed**
- pytest full do arquivo `tests/integration/test_streamlit_app.py` (`-n auto`): **190 passed** (rodado por ser o arquivo impactado + smoke obrigatório do app; cobre import + render + tab selector + nova aba)
- `python -c "import streamlit_app"`: **import ok**

POR QUÊ esse subconjunto: o escopo toca `pages.py`, `streamlit_app.py` e o test file de integração do app; `test_streamlit_app.py` é o gate de regressão do dashboard (import + render lazy + abas + nova função). A suíte FULL é responsabilidade do QA.

## Problemas encontrados
- `test_render_tab_selector_e_exportado` regrediu (assert hardcoded da lista de 4 labels). Resolvido atualizando a expectativa para as 5 abas (mudança aprovada do bloco). Nenhum outro problema.

## Pendências
- Nenhuma de código. Operacional (fora do fechamento de testes): nenhuma — não há regeneração de parquet (tela é só leitura/cálculo em memória).

## Riscos remanescentes
- Baixo — os testes de UI mockam `st.form`/`st.expander`/`st.spinner` como context managers (`_ctx_mgr`); o comportamento real do Streamlit (form só submete no botão) é exercido em runtime, não pelos testes. Mitigado: o teste de guardrail captura o arg real passado ao engine.
- Baixo — `load_base_calibracao` depende de `data/staging/base_calibracao_multirede.parquet` (426×9, confirmado presente; 112 linhas com densidade válida após derivação). Ausência → faixa "n/d" sem travar.

## Guardrails verificados
- score_priorizacao não alterado: sim (não tocado)
- Artefatos M1 preservados: sim (zero escrita; tela só lê/calcula em memória)
- Dashboard offline mantido: sim (parser de link Maps é regex/`unquote` puro; selenium só em `MapsGeocoder.__init__`, nunca importado no caminho da aba; sem geocoding ao vivo)
- DEC-009 (demanda SEMPRE premissa explícita): sim — engine recebe sempre o valor da caixa; toggle p50 só preenche `value=`; `demanda_fonte="premissa_explicita"` exibido; teste `test_viabilidade_ponto_demanda_e_sempre_premissa_do_operador` asserta isso
- Performance (Blocos 4-6): sim — `base_calibracao_df` carregado 1× via `@st.cache_data`; engine roda só on-submit; render lazy de abas preservado (nova aba só executa quando ativa)
