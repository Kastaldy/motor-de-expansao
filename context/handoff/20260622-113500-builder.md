# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-RELMUN-01 — Relatório Municipal (novo PDF de 8 páginas por município; READ-ONLY sobre M1)

## O que foi feito
Implementado o **Relatório Municipal** conforme o gate humano (D1–D9 travadas; DEC-011), em um
módulo NOVO e disjunto, COEXISTINDO com o Relatório Pontual Censitário (intocado).

- **Módulo novo `src/motor_expansao/dashboard/relatorio_municipal.py`** (helpers de layout/PDF
  reimplementados localmente — NÃO importa nenhum helper `_`-prefixado de `censo_report.py`/
  `censo_map.py`; isolamento total). API pública:
  - `agregar_municipio(df, *, nome_municipio, uf=None, dominio_df=None, competitors_df=None,
    ultra_df=None) -> dict` — único ponto que toca dados, READ-ONLY (só lê colunas). Computa o
    dicionário canônico das 8 páginas:
    - **D1** hex destacado ⇔ `sam_fitness_potencial>=3000` E `oferta_efetiva_disponivel>=2000`;
      `espaco_para_academias = round(Σ oferta dos destacados / 2500)`; rótulo do hex = oferta.
    - **D4** Mercado/Residual = `Σ oferta_efetiva_disponivel` do município (alunos).
    - **D5** faixas Score Censitário (médio/máx NaN-safe). **D6** pins por H3 res-7
      (reusa `hex_id_res7`, senão deriva via `h3`; só `rede`/contagem — anti-PII).
    - **D2/D7** zonas via `dominio_df` por `cluster_id`, ordenadas por residual desc, cap 3,
      rótulos 1 Âncora central / 2 Flancos laterais / 3 Cerco; fallback `[]` se ausente/vazio.
  - `render_mapas_municipio(...)` → 4 PNGs (resumo/score/residual/dominio) via Pillow.
    **D3 tiles online** (`_fetch_basemap_municipio`: import lazy de `contextily`, EPSG:3857, cache
    `data/cache/basemap_tiles/`, fallback offline → canvas claro; `basemap=False` default em
    CI/teste). Cap de desenho de hexes `_HEX_DRAW_CAP=8000` (R5/perf). Reusa
    `competitors._render_pin_tile` por import (sem modificar o arquivo).
  - `gerar_pdf_relatorio_municipal(...)` → 8 páginas 16:9 (`%PDF-1.4`, `set_compression(False)`,
    `/Count 8`), marca d'água em todas as páginas (anti-PII), **D8** carimbo de versão no rodapé +
    breakdown só das redes mapeadas, **D9** Página 6 simplificada (zona/cluster + tese + nota
    "Bairros indisponíveis na base atual"; sem `NM_BAIRRO`).
  - `gerar_payloads_download_relatorio_municipal(...)` + `render_download_relatorio_municipal(...)`.
- **Toque pontual em `src/motor_expansao/dashboard/pages.py`**: import passivo do módulo novo +
  função `render_relatorio_municipal_expander(...)` + novo expander "Relatorio Municipal" em
  `render_mapa_territorial`, habilitado SÓ com exatamente 1 município (`len(selected_cities)==1`;
  senão `st.info`). Nenhuma assinatura existente mudou; sem carga nova de parquet (usa `df`/
  `dominio_df`/`competitors_df`/`ultra_df` já em escopo — Blocos 4–6 preservados).
- Confirmação de viabilidade D1: `sam_fitness_potencial` e `oferta_efetiva_disponivel` PRESENTES
  no slice enriquecido por UF (`hexagonos_dashboard_enriquecido`) — verificado por inspeção de
  schema. Nenhum bloqueio.

## Arquivos alterados
- `src/motor_expansao/dashboard/relatorio_municipal.py` — CRIADO (módulo novo; agregação + mapas +
  PDF 8 páginas + payloads/streamlit helper).
- `tests/unit/test_relatorio_municipal.py` — CRIADO (16 testes: agregação/D1/D4/D5/D6/zonas,
  8 páginas/`/Count 8`/`%PDF-1.4`/headers, fallback sem dominio, nota Página 6, sem assets,
  anti-PII, mapas `basemap=False`, pins por H3, marca d'água, payloads, **CA2 coexistência
  bytes idênticos do pontual**). NENHUM teste bate na rede.
- `src/motor_expansao/dashboard/pages.py` — import passivo + `render_relatorio_municipal_expander`
  + novo expander (toque pontual; nenhuma assinatura existente alterada).
- `docs/relatorio_municipal_template.md` — anotadas as decisões D1–D9 do gate (seção nova).
- `tasks/current_task.md` — status → aguardando QA.

## Validações executadas
- pytest (subconjunto impactado, SERIAL): **246 passed, 0 failed, 0 skipped** em
  `tests/unit/test_relatorio_municipal.py tests/unit/test_relatorio_pontual_censitario_export.py
  tests/integration/test_streamlit_app.py`.
  - NB: `-n auto` (xdist) ABORTOU com `INTERNALERROR ... execnet ... OSError [WinError 6]`
    (problema conhecido de ambiente no Python 3.14 local — NÃO é defeito, NÃO é bypass).
    Refeito SERIAL (`python -m pytest -q ...`) conforme instrução. A suíte FULL com `-n auto`
    é responsabilidade do QA.
  - Subconjunto escolhido: o módulo novo + o teste do Relatório Pontual (prova de coexistência
    CA2) + o smoke de integração do app (expander novo em `pages.py`).
- import: **ok** (`python -c "import streamlit_app"`).
- ruff: **All checks passed!** (`python -m ruff check .`).
- mypy: **Success: no issues found** (`python -m mypy src/motor_expansao/dashboard/relatorio_municipal.py`).

## Problemas encontrados
- Inicialmente o título canônico da capa ("Potencial de Entrada de Novas Unidades") era emitido via
  `multi_cell` (quebra de linha) e não aparecia contíguo nos bytes crus do PDF. Corrigido: título
  emitido em UMA linha via `cell` (string canônica contígua; compressão OFF). Os 8 headers passam.
- Ajustes de lint (ruff): removidos um helper/variável locais não usados e reordenado o import do
  módulo novo em `pages.py` (após `data`, antes de `utils`). Sem impacto funcional.

## Pendências
- Validação visual humana do PDF municipal (estética GeoFusion/Ultra) — fora do escopo do Builder.
- Atualizar `tasks/backlog.md` (estado do BLK-RELMUN-01) — recomendado ao QA/orquestrador (não
  toquei para não conflitar com o estado pré-sujo listado em `current_task.md`).

## Riscos remanescentes
- R-tiles: a geração de mapas com `basemap=True` faz fetch online sob demanda (DEC-011); em runtime
  sem internet cai no fallback offline (canvas claro). A suíte usa `basemap=False`.
- R-dominio: municípios sem `dominio_df` caem no modo simplificado (Páginas 5–6) — testado.
- R-perf: cap de desenho de hexes `_HEX_DRAW_CAP=8000` por município (mantém geração < ~5 s);
  QA pode confirmar tempo em município grande.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; só leitura de colunas).
- Artefatos M1 preservados: sim (nenhuma escrita; nenhum recálculo de score/carteira/plano).
- Relatório Pontual intocado: sim (CA2 — `censo_report.py`/`censo_map.py`/`censo_point.py` sem
  edição; teste prova bytes idênticos do pontual antes/depois de gerar o municipal).
- Dashboard offline mantido: sim (default `basemap=False` em CI/teste; sem carga nova de parquet;
  Blocos 4–6 de performance preservados).
- Anti-PII: sim (sem `image24.png`; só `rede`/contagem nos pins; PDF de saída não versionado).
