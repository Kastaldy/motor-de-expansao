# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Block Orchestrator (fechamento)

## VEREDITO
APROVADO

## Justificativa
O benchmark `scripts/benchmark_hexid_category.py` é reprodutível, conclui NO-GO (category
é pior em merge e memória; ganho ≥ 15% não atingido) e não altera código de produção
(zero diff real em `src/`; M1 e artefatos oficiais intocados). Todas as 7 validações
obrigatórias re-executadas pelo QA passam; housekeeping via helper versionado byte-idêntico.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- O `merge_conclusao` no gerador de relatório rotula sempre "Category MAIS LENTO" quando
  `delta_pct >= 0`, o que casa com o resultado observado, mas a lógica ficaria mais clara
  se o ramo `>= GANHO_MINIMO_PCT` (redundante com `>= 0`) fosse removido. Cosmético, não bloqueia.

## Testes faltantes
- nenhum — script de benchmark/relatório read-only, coberto por execução reprodutível; sem
  lógica de produção nova que exija teste unitário.

## Riscos remanescentes
- nenhum — script read-only sobre `data/staging`; relatório gitignored; sem rede/VPS.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suite FULL (gate único) — `python -m pytest -n auto -q`
```
=========================== short test summary info ============================
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_curto_usa_localidade_mock
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_curto_usa_localidade_mockada
ERROR tests/unit/demanda_revelada/test_viz_atratividade.py
4 failed, 1493 passed, 4 skipped, 19 warnings, 1 error in 436.27s (0:07:16)
```
As 4 falhas são Plus Code (`ModuleNotFoundError: No module named 'openlocationcode'`) e o
ERROR é `test_viz_atratividade` (`ModuleNotFoundError: No module named 'matplotlib'`) —
todas PRÉ-EXISTENTES/ambientais e listadas como aceitas. Confirmado por conta própria:
```
$ python -c "import openlocationcode"  -> ModuleNotFoundError: No module named 'openlocationcode'
$ python -c "import matplotlib"        -> ModuleNotFoundError: No module named 'matplotlib'
```
Zero falhas NOVAS. O benchmark não toca produção, logo não pode introduzir regressão.

### 2. ruff — `python -m ruff check scripts/benchmark_hexid_category.py`
```
All checks passed!
```

### 3. src/ intocado — `git diff --ignore-cr-at-eol HEAD -- src/`
```
warning: in the working copy of 'src/motor_expansao/dimensionamento/config.py', CRLF will be replaced by LF the next time Git touches it
```
Diff de CONTEÚDO vazio; única alteração em `src/` é CRLF→LF pré-existente em
`dimensionamento/config.py` (aceito pelo escopo). Nenhuma linha de conteúdo alterada.

### 4. Reprodutibilidade do benchmark — `python scripts/benchmark_hexid_category.py`
```
  hexagonos_mercado_mapeado.parquet: 1,542,531 linhas, 1,542,531 únicos, 100%=True
  brasil_estrutural.parquet: 1,542,531 linhas, 1,542,531 únicos, 100%=True
  brasil_priorizados.parquet: 308,494 linhas, 308,494 únicos, 100%=True
  hexagonos_brasil_oportunidades.parquet: 1,542,531 linhas, 1,542,531 únicos, 100%=True
  Memória:  String 94.1 MB | Category 132.3 MB | Delta +38.1 MB
  Merge:    String 0.838 s | Category 0.887 s | Delta +5.9% (Category MAIS LENTO)
  isin:     String 62.805 ms | Category 30.763 ms | Delta -51.0% (< 0,1 s, não é gargalo)
  Recomendação: NÃO converter hex_id para category. NO-GO confirmado.
```
Completa sem erro e conclui NO-GO. Nota: o merge delta varia entre runs (Builder mediu
+12.9%, QA mediu +5.9%) mas o SINAL é estável (category sempre mais lento no merge; nunca
atinge o ganho de -15%); memória e cardinalidade são determinísticas e batem exatamente.

### 5. loop_guard — `python scripts/loop_guard.py --base HEAD~2`
```
GUARD OK: 7 caminho(s) alterado(s), nenhum proibido.
```

### 6. M1 mtimes — `stat -c "%Y %n" brasil_estrutural.parquet brasil_priorizados.parquet`
```
1780501621 /repo/data/staging/brasil_estrutural.parquet
1780501631 /repo/data/staging/brasil_priorizados.parquet
```
Iguais aos valores esperados (1780501621 e 1780501631). Artefatos M1 NÃO regerados.

### 7. housekeeping — `scripts/housekeeping_move_block.py BLK-PROD-03`
O `--check` inicial falhou (código 1: "stub ausente no backlog") porque o Builder havia
adicionado uma entrada MANUAL `## BLK-PROD-03` em completed.md (não via helper) e o bloco
seguia com heading `###` no backlog. Conforme a regra de housekeeping (não aceitar move
manual), removi a entrada manual e executei o move pelo helper versionado:
```
$ python scripts/housekeeping_move_block.py BLK-PROD-03 --date 2026-07-07
OK: movido BLK-PROD-03 (1352 bytes) -> tasks/completed.md; stub em tasks/backlog.md.
EXIT=0
$ python scripts/housekeeping_move_block.py BLK-PROD-03 --check
OK: BLK-PROD-03 movido (stub no backlog + bloco em completed.md).
EXIT=0
```
Byte-identidade verificada contra `git show HEAD:tasks/backlog.md`:
```
orig len: 1352 | comp len: 1352 | BYTE-IDENTICAL: True
```
Teste do helper verde: `pytest -n auto tests/unit/test_housekeeping_helper.py` -> `10 passed`.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou
mock do caminho crítico. O benchmark roda contra os parquets REAIS de `data/staging/`
(1.542.531 linhas confirmadas); a suíte usa fixtures legítimas de teste (não é tooling de
produção). Housekeeping executado contra `tasks/backlog.md`/`tasks/completed.md` reais.

## Conferência cruzada com log do Builder
Bate no essencial (NO-GO, cardinalidade 100% única, memória +38.1 MB, merge sempre mais
lento). Divergências não-bloqueantes: (a) o delta de merge medido pelo QA foi +5.9% vs
+12.9% do Builder — ruído de wall-clock esperado em benchmark, sinal idêntico (category
pior, longe do -15%); (b) o Builder adicionou a entrada de completed.md manualmente — QA
corrigiu para o move byte-idêntico via helper, como exige a regra de housekeeping.

## Guardrails verificados
- score_priorizacao não alterado: sim (zero diff de conteúdo em `src/`)
- Artefatos M1 preservados: sim (mtimes inalterados; script só LÊ staging)
- Validações re-executadas pelo QA (sem bypass):
  - suite full → 4 failed + 1 error PRÉ-EXISTENTES (Plus Code/matplotlib ausentes), 1493 passed
  - ruff → All checks passed
  - src/ content diff → vazio (só CRLF)
  - benchmark → executa, NO-GO
  - loop_guard → GUARD OK
  - M1 mtimes → inalterados
  - housekeeping → move via helper, byte-idêntico, --check OK, teste do helper 10 passed
- Escopo respeitado: sim — só `scripts/benchmark_hexid_category.py` (novo) + relatório
  gitignored + housekeeping/handoff. Nenhum código de produção alterado (decisão pré-fixada
  NO-GO honrada).

## Decisão recomendada
fechar ciclo
