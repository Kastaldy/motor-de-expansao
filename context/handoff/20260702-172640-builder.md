# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-TP-06-FU1 — Re-validação do residual com candidatos de recalibração (baseline + Candidato A APENAS; Candidato C e A+C ADIADOS por decisão do gate humano)

## GATE HUMANO — REGISTRO
APROVADO pelo usuário em 2026-07-02: rodar **baseline + Candidato A APENAS**. Candidato C (capacidade POR REDE) e A+C **NÃO implementados** neste ciclo (adiados — o Candidato C exigirá capacidade de CLUBE real de `data/validacao/`: `Sky Fit dados.xlsx`, `academias_engenharia_do_corpo.xlsx`, `KPIs_Smart_2025_02 (1).xlsx`; as medianas ~340 do TP-08-FU são footprint de bairro, não capacidade de clube). READ-ONLY sobre o M1.

## O que foi feito
Módulo novo, PURO e isolado (`demanda_revelada/revalidacao_residual_candidatos.py`) que:
- Reproduz o baseline do BLK-TP-06 (`residual_baseline = score_oportunidade_residual`) → **R²_oof_log = +0,3119, rho_oof = +0,4615, n=16.411** (bate exato o TP-06).
- Constrói EM MEMÓRIA o **Candidato A** (oferta enriquecida com dedup FINO por par `(hex_id, rede_menor)`): soma à oferta consumida os `alunos_academias_menores` das academias menores NÃO já cobertas pela mesma rede naquele hex (independentes sempre somam; rede conhecida já em `concorrentes_mapeados[(hex_id_res7, rede)]` é dedupada). `residual_cand_A = clip(100·max(sam_fitness_potencial − (oferta_consumida_total_estimada + alunos_menores_add), 0)/2500, 0, 100)` (cap_ref=2500, denominador do clip INTOCADO). Dedup medido no join: **+1.748.710 alunos somados (91,7%) / −157.840 dedupados (8,3%)** — bate o handoff do Planner.
- Valida cada vetor com o MESMO harness out-of-fold de `calibracao_residual.py` (k-fold 5×5 seed=42, alpha por menor RMSE oof, IC95 bootstrap R²+rho). R² in-sample BANIDO (nenhum campo in-sample no fluxo de veredito).
- Compara A vs baseline por **IC95 do Δ pareado (bootstrap PAREADO, mesmos índices nos 2 vetores oof, seed=42)** nos 3 recortes: completo (n=16.411), metropolitano SP/MG/RJ (n=8.367), fora (n=8.044).
- Estrutura EXTENSÍVEL para o Candidato C futuro: o dedup fino é função própria (`alunos_menores_add_por_hex`) e o C somaria uma coluna `residual_cand_C` sem tocar as existentes — mas C **não** foi implementado.
- Gera relatório gitignored `data/analysis/revalidacao_residual_candidatos.md` (via `__main__`, sem PII, com seção "Candidato C — ADIADO" citando a fonte futura `data/validacao/`).

## VEREDITO REAL do Candidato A (dados reais, seed=42)
**NAO_APLICAR (NO-GO honesto, DEC-008).** O Candidato A **piora** o baseline:
- Baseline (completo): R²_oof_log = **+0,3119**, IC95 [+0,2977, +0,3250], rho_oof +0,4615.
- Candidato A (completo): R²_oof_log = **+0,2692**, IC95 [+0,2553, +0,2819], rho_oof +0,4327.
- **Δ pareado (A − baseline) COMPLETO = −0,0427**, IC95 [−0,0477, −0,0379] → inteiramente **abaixo de zero** → vence = **False**.
- **Δ pareado FORA (não-metro, n=8.044) = −0,0193**, IC95 [−0,0232, −0,0157] → inteiramente abaixo de zero → vence = **False**.
- (Metropolitano n=8.367: Δ também negativo.)
- **Não vence em NENHUM recorte** → **NÃO recomendar ao BLK-TP-09** aplicar o Candidato A. Enriquecer a oferta consumida com as academias menores REDUZ o poder preditivo do residual out-of-fold (satura hexes que tinham demanda observada, derrubando o residual justamente onde há `membros`). NO-GO é resultado VÁLIDO (DEC-008).

## Arquivos alterados
- `src/motor_expansao/demanda_revelada/revalidacao_residual_candidatos.py` (NOVO) — módulo puro/isolado: dedup fino, construção baseline+Candidato A, validação oof (reuso do harness), comparação pareada, orquestrador `revalidar_candidatos`, relatório, `__main__`.
- `tests/unit/demanda_revelada/test_revalidacao_residual_candidatos.py` (NOVO) — 18 testes com fixtures SINTÉTICAS.
- `data/analysis/revalidacao_residual_candidatos.md` (NOVO, gitignored, via `__main__`, sem PII) — baseline reproduzido, Candidato A, tabela R²/rho/IC95, Δ pareado nos 3 recortes, VEREDITO, Candidato C ADIADO, confounds.
- `context/handoff.md`, `context/handoff/20260702-172640-builder.md`, `tasks/current_task.md` (bookkeeping).

## Validações executadas
- pytest (subset): `python -m pytest -n auto -q tests/unit/demanda_revelada/ tests/integration/test_streamlit_app.py` → **278 passed, 0 failed, 0 skipped** (Python 3.14 local; warnings de deprecação do pytest-asyncio, não-defeitos).
  - Só o arquivo novo: `test_revalidacao_residual_candidatos.py` → **18 passed** (dedup fino: rede já mapeada não soma / independente sempre soma / rede não mapeada soma / independente soma com concorrente de outra rede no hex; residual A cai onde soma / em [0,100]; baseline == score do parquet; flag_metropolitano; R² in-sample banido; IC determinístico seed; bootstrap pareado mesmos índices (cand==base ⇒ Δ≈0, IC cobre 0) + determinístico; NO-GO válido; vence exige completo E fora; anti-PII; isolamento AST; CAP_REF=2500).
- import: `python -c "import streamlit_app"` → **ok**.
- ruff: `ruff check` nos 2 arquivos novos → **All checks passed**.
- Isolamento (AST): módulo novo NÃO importa de `pipelines.m1`/`dashboard`/`censo`/`api`/`config` raiz/`pipelines.calcular_colunas_mercado`/`pipelines.enriquecimento_espacial_hexagonos` → **exit 0**.
- mtime (antes/depois): `hexagonos_mercado_mapeado.parquet` + 4 oficiais M1 (`brasil_estrutural`, `brasil_priorizados`, `hexagonos_brasil_oportunidades`, `hexagonos_brasil_dashboard`) → **INALTERADO** (diff vazio).
- git status: nenhum artefato de produção modificado; relatório em `data/analysis/` é gitignored (confirmado por `git check-ignore`).

## Problemas encontrados
Nenhum. (Ruff apontou 1 variável morta `add_total` numa 1ª passada; removida — o valor correto `add_join` é computado no recorte de dedup.)

## Pendências
- **Candidato C ADIADO** (decisão do gate): implementar em bloco próprio quando a capacidade de CLUBE real de `data/validacao/` estiver disponível na fronteira anti-PII. A estrutura do módulo já está preparada (dedup fino isolado; coluna `residual_cand_C` a adicionar).
- Fechamento pós-merge (operacional, fora do core do Builder): atualizar "Depende de" do BLK-TP-09 em `tasks/backlog.md` citando este FU1 concluído.

## Riscos remanescentes
- Nenhum sobre produção (READ-ONLY; zero escrita em artefato M1/mercado). O veredito NO-GO significa que o BLK-TP-09 NÃO deve aplicar o Candidato A; a decisão sobre reabrir o Candidato C (fonte de clube) fica para gate futuro.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; nunca tocado)
- Artefatos M1 preservados: sim (mtime dos 4 oficiais + `hexagonos_mercado_mapeado.parquet` inalterado; nenhum parquet de mercado novo/regenerado; `calcular_colunas_mercado.py`/`enriquecimento_espacial_hexagonos.py` intocados)
- Dashboard offline mantido: sim (nenhuma dependência de rede; módulo só lê parquets locais no `__main__`)
- DEC-008: out-of-fold vs baseline da média; R² in-sample banido do veredito; IC95 seed=42; Δ pareado; NO-GO válido — sim
- DEC-009: `membros` = ALVO OBSERVADO, nunca preditor geográfico — sim
- DEC-012 (anti-PII/isolamento): pacote disjunto; zero coluna PII em frame/relatório; fixtures sintéticas; `NAO_ABRA/`/`data/validacao/` não tocados — sim
