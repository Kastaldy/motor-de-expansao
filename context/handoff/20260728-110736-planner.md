# Handoff — Planner

## Skill que gerou este handoff
Planner (plano consolidado **A + B**, pós-gate humano de 2026-07-28)

## Próxima Skill recomendada
**Approver (aprovação humana)** — criticidade **CRÍTICA**. Só depois: Builder → QA.

## Entendimento da tarefa
Fazer o grafo do graphify chegar ao agente como **ferramenta** (`.mcp.json` versionado e **funcional,
provado por handshake real**), como **norma** (§2 do `CLAUDE.md`, não §7) e como **dependência declarada**
(`pyproject.toml`), com o hook de rebuild revisável em `.githooks/`, runbook em `docs/` e a DEC-019 registrada
como APROVADA — tudo READ-ONLY sobre o M1.

---

## Medições que EU fiz nesta sessão (venv descartável no scratchpad, fora do repo)

Ambiente: `python -m venv` + `uv 0.11.33` + `pip 25.3` no scratchpad. **Nada instalado no Python global,
nada escrito no repo** (`git status --porcelain` no fim = só os 3 handoffs não-rastreados de sempre).

| # | Pergunta | Resultado MEDIDO |
|---|---|---|
| M1 | `uv pip compile --all-extras` enxerga `[dependency-groups]`? | **NÃO.** Compilar o `pyproject.toml` com e sem o bloco devolve **os mesmos 214 pins, byte a byte** (`diff` vazio). A premissa central da sonda 2 estava NÃO MEDIDA — agora está **CONFIRMADA**. |
| M2 | E como **extra** em `[project.optional-dependencies]`? | **250 pins (+36)**, incluindo 25 gramáticas `tree-sitter-*`. A "bomba-relógio" é real. |
| M3 | `uv pip compile` **preserva** os pins existentes ao regenerar? | **NÃO — re-resolve tudo.** Com o `pyproject.toml` **INTOCADO**, o refresh de hoje muda **30 pacotes**, incluindo `streamlit 1.59.2→1.60.0` e `fastapi 0.139.0→0.140.9` (ambos nas **imagens de produção**), `prefect`, `matplotlib`, `sentry-sdk`, `gitpython`, `ruff`, e um **downgrade** `anyio 4.14.2→4.14.1`. |
| M4 | `hatchling` quebra com `[dependency-groups]` presente? | **NÃO.** `pip wheel --no-deps` builda o pacote, exit 0 (`motor_expansao-0.1.0-py3-none-any.whl`). O `pip install -e .` do CI está a salvo. |
| M5 | `pip install --group graph` funciona? | **SIM**, exit 0 (pip 25.3 no venv; o global tem 26.1.2). |
| M6 | **`graphifyy[mcp]` sem pin funciona hoje?** | **NÃO — ESTÁ QUEBRADO.** Ver abaixo. |
| M7 | Com `mcp<2`? | **FUNCIONA.** Handshake JSON-RPC completo, `VERDICT: PASS`. |

### M6/M7 — A DESCOBERTA QUE JUSTIFICA O ESCOPO B INTEIRO

`mcp` subiu para **2.0.0** (é a `latest` no PyPI **hoje**). A sonda 4 mediu `1.28.1` há poucas horas — o
alvo se moveu **durante o ciclo**. E `mcp 2.0.0` **removeu `AnyUrl` de `mcp.types`**:

```
$ python -c "from mcp.types import AnyUrl"
ImportError: cannot import name 'AnyUrl' from 'mcp.types'
```

`graphify/serve.py:1116` faz exatamente `from mcp.types import AnyUrl`, dentro de um `try/except ImportError`
que **mascara o erro real**:

```
serve.py:1118 -> raise ImportError('mcp not installed. Run: pip install "graphifyy[mcp]"')
```

Ou seja: com `graphifyy[mcp]` **sem pin**, o servidor instala com sucesso e depois morre dizendo que **não
está instalado**. Handshake com `mcp 2.0.0` → `TOOLS (0)`, `EXIT 1`, `VERDICT: FAIL`.

Com `mcp<2` (resolveu `1.29.0`), o mesmo driver:

```
PROTOCOL   : 2025-06-18
SERVERINFO : {'name': 'graphify', 'version': '1.29.0'}
TOOLS (10) : ['get_community','get_neighbors','get_node','get_pr_impact','god_nodes',
              'graph_stats','list_prs','query_graph','shortest_path','triage_prs']
graph_stats: Nodes: 7633 | Edges: 15362 | Communities: 424 | EXTRACTED: 97% | INFERRED: 3% | AMBIGUOUS: 0%
EXIT       : 0     STDERR: (vazio)     VERDICT: PASS
```

**Consequência para o plano:** declarar a dependência deixou de ser burocracia. Sem o pin `mcp<2`, o critério
de aceite "funcional" é **inalcançável** e a próxima pessoa recebe uma mensagem de erro que mente. Este é o
argumento que o revisor Crítico precisa ver.

---

## Decisões fechadas por este Planner (o Builder NÃO decide nenhuma)

### DECISÃO ZERO — base do PR: **`main`**. Regra de EOL no `.gitattributes` **RAIZ**.
Medido: `git diff --numstat main...HEAD` → **14 arquivos, 2.312 adições, 30 remoções**. O `graph.json` sai
como `-` (binário) por causa do `-diff linguist-generated=true` do commit `a7c4754`.
**A premissa do backlog de que "o diff combinado passa de 265k linhas e afoga o `claude-review`" está
OBSOLETA** — ela motivou o `a7c4754` e foi resolvida por ele. A base `main` está reaberta, e é o que o humano
pediu: um PR final único, entregue quando o grafo estiver completo (BLK-GRAPH-01 + 02 juntos; o PR #150 segue
fechado).

Como `.gitattributes` **já está** no diff vs `main` (8 linhas do `a7c4754`), o PR **já é CRÍTICO por caminho**
antes de qualquer coisa do Escopo B. Logo o custo marginal de acrescentar a regra de EOL no arquivo **raiz** é
**ZERO** — e o `.githooks/.gitattributes` aninhado (rota de fuga da sonda 3) fica **descartado**: só fazia
sentido com base `graph-01`.

**Labels obrigatórias do PR (medidas no `guard.yml`):**
| Label | Por quê | Quem aplica |
|---|---|---|
| `criticidade:critica` | `review-gate` cobra o **MÁXIMO** da faixa do backlog (`guard.yml:684-695`) | autor |
| `critica-aprovada` | diff toca `.gitattributes` + `pyproject.toml` (classe `critico`) | `@Kastaldy` **ou** `@VinhoAbencoado` (DEC-019) |
| `aprovado-humano` | diff toca `CLAUDE.md`/`.claude/`/`tasks/` (classe `governanca`) **e** `critica` ∈ `EXIGEM_HUMANO` | humano com write/admin, **≠ autor** |

**Portão adicional, não-label:** `.github/CODEOWNERS:29-34` exige review nativo de
`@Kastaldy @VinhoAbencoado @juancalu` para `/CLAUDE.md` e `/pyproject.toml`. São **dois** portões, não um.

### D8 — mecanismo da dependência: **`[dependency-groups]` (PEP 735) + pin `mcp<2`**
As sondas 1 e 2 se contradiziam; resolvi por **medição**, e a sonda 2 está certa:
- **M1** prova que o grupo fica fora do `--all-extras` → o lock permanece intocado **para sempre**, inclusive
  no próximo bump de CVE. Um extra entraria sozinho no lock (**M2**), que é o defeito que a sonda 2 chamou de
  bomba-relógio.
- **M4** derruba o único risco sério da opção: `hatchling` builda normalmente com a tabela presente.
- **M5** confirma a ergonomia: `python -m pip install --group graph`, um comando.
- `uvx --from "graphifyy[mcp]"` (alternativa da sonda 1 que evitaria o Escopo B inteiro) fica **descartada
  conscientemente**: `uv` não está instalado nesta máquina nem no CI, e resolveria em runtime **sem pin** —
  cairia exatamente no `mcp 2.0.0` quebrado do M6.

### D9 — `constraints.txt`: **NÃO tocar**
**M3** é decisivo e vai muito além do que a sonda 2 estimou: regenerar o lock hoje com o comando canônico,
**sem mudar nada no `pyproject.toml`**, bumpa **30 pacotes** — entre eles `streamlit` e `fastapi`, que rodam
nas imagens de produção. Um PR que deveria ser "adiciona ferramenta de dev" viraria "muda o runtime de
produção". E editar o lock à mão está vedado pelo próprio cabeçalho (`autogenerated by uv`).
Com o `[dependency-groups]`, a questão não volta: o lock nunca vê o pacote.
**Registrar no PR** que a divergência é **intencional e estável**, não esquecimento.

### D10 — `command` do `.mcp.json`: **`"python"` literal**
Reverto o `${GRAPHIFY_PYTHON:-python}` do plano A. Três razões, em ordem de peso:
1. **O critério de aceite virou FUNCIONAL.** O `scripts/verify_mcp_graphify.py` lê `command` do próprio
   `.mcp.json` e o passa a `subprocess` — é isso que faz a prova valer para a **config**, não para uma
   variante digitada à mão. Com `${...}`, ou o subprocess falha, ou o script expande e passa a provar **outra
   coisa** do que o harness roda. Trocar prova por promessa, justamente no item que o humano exigiu provar.
2. **Medido (sonda 4):** o `python` do PATH **é** o interpretador com o graphify
   (`AppData/Local/Python/pythoncore-3.14-64`). Funciona hoje, sem indireção.
3. A expansão `${VAR:-default}` é documentada, mas ficou **NÃO MEDIDA**; e ela **não resolve portabilidade**
   de verdade — em Linux o problema não é o nome do binário, é o pacote não estar instalado. Isso quem
   resolve é o runbook (`pip install --group graph`), não o campo `command`.
**Follow-up registrado:** se surgir uma segunda máquina, reavaliar `GRAPHIFY_PYTHON` **com medição**.

### D11 — reescrever a `Criticidade` do bloco (`tasks/backlog.md:114`): **SIM**
Com o Escopo B dentro, a faixa "Alta no Escopo A; Crítica se..." deixou de descrever a realidade, e o
`review-gate` cobra o máximo de qualquer forma. É GOVERNANÇA e o PR já toca governança. Texto literal no
passo 9.

### D12 — corrigir `graphify` → `graphifyy` no backlog: **SIM**
`tasks/backlog.md:143` e `:164` escrevem o nome errado. Medido pela sonda 1: **`graphify` está UNCLAIMED no
PyPI** — se alguém registrar o nome, um `pip install` copiado do backlog executa código de terceiro. Custo de
corrigir: 2 linhas. Textos literais no passo 9.

### D13 — DEC-019 `PROPOSTA` → `APROVADA`, em **commit próprio**
Duas edições de 1 linha. **Quem aprovou hoje foi Vinicius Cruz (`@VinhoAbencoado`), em 2026-07-28** — não
Felipe; o texto atual ("aguardando aprovacao de Felipe") seria mentira se copiado.
**Ressalva do orquestrador, registrada:** a DEC-019 é o que autoriza `@VinhoAbencoado` a conceder
`critica-aprovada`, e este PR é Crítico → o poder de aprovação nasceria no mesmo PR que ele aprova.
**Mitigação adotada:** commit separado por path (passo 1, **primeiro de todos**), e recomendação de **PR
próprio** — a decisão de separar é do humano no fechamento.
**Atenuante medido, que reduz a circularidade a papelada:** o `guard.yml:200` da **`main`** já executa
`DONOS = {"kastaldy", "vinhoabencoado"}`. A label já seria aceita hoje, com a DEC em `PROPOSTA`. A edição
alinha o **registro** ao código; **não muda comportamento**.

### D14 — prova funcional: script **versionado** + teste de contrato que **NÃO skipa**
- `scripts/verify_mcp_graphify.py` **VERSIONADO** (`scripts/` é livre no `loop_guard`). Motivo: o M6 é
  exatamente a classe de defeito que volta em silêncio — quando o graphify suportar `mcp 2.x`, alguém precisa
  reconferir o pin em **um comando**. Deixar no scratchpad joga fora a ferramenta de regressão.
- **Guardrail NO-BYPASS, tratado de frente.** Um teste com `pytest.importorskip("mcp")` **SKIPa no CI**, e
  teste que skipa não é validação executada. Por isso a validação se divide:
  - **`tests/contracts/test_grafo_ferramenta.py` — NÃO skipa, roda no CI sempre.** Lê o `pyproject.toml` com
    `tomllib` (stdlib) e trava o pin `mcp<2` e a escolha de `[dependency-groups]`. **É aqui que a descoberta
    M6 vira regressão automática.**
  - `tests/integration/test_mcp_graphify_server.py` — **skipa** onde o extra não existe (CI, container do
    loop). É rede de segurança para quem tem o extra, **não** é o gate.
  - **A validação REAL é a execução de `python scripts/verify_mcp_graphify.py` na máquina do Builder, com a
    saída colada no handoff. É OBRIGATÓRIA** e está nos Critérios de aceite (nº 2) e nas Validações.

### D1 (do plano A) — **SUPERSEDED pelo gate humano**
O critério de aceite volta a ser o **original do backlog**: `.mcp.json` **funcional, testado de verdade**.
O "BLK-GRAPH-03 / Escopo B como bloco próprio" **deixa de existir**.

### D2–D7 (do plano A) — **mantidas**, com os ajustes acima
D2 (`.mcp.json` literal, agora com `command: "python"`), D3 (`enabledMcpjsonServers` + 5 verbos read-only),
D4 (só `post-commit` viaja; `post-checkout` fora), D5 (`/graphify . --update` é a forma correta;
`python -m graphify . --update` é ignorado em silêncio), D6 (runbook `docs/grafo_conhecimento.md`),
D7 (testes de contrato entram).

---

## Plano técnico

> **Passo 0 (antes de qualquer edição).** `git status --porcelain`. Se `graphify-out/*` aparecer modificado,
> **não é seu** — é o hook. `git checkout -- graphify-out/`. Repetir imediatamente antes de **cada** commit.
> **NUNCA `git add -A`.** Commit sempre por path. **A ORDEM DOS PASSOS IMPORTA**: o passo 1 é commitado
> sozinho, antes de o `CLAUDE.md` receber as outras edições.

### 1. DEC-019 — 2 edições + **COMMIT A, isolado** (fazer PRIMEIRO)

**1a.** `docs/decisions/DEC-019.md`, **linha 4** — substituir a linha inteira por:
```
- Status: **APROVADA** por Vinicius Cruz (`@VinhoAbencoado`) em 2026-07-28, no gate humano do BLK-GRAPH-02. Ja estava implementada em `.github/workflows/guard.yml` (job `guard`, `DONOS = {"kastaldy", "vinhoabencoado"}`) desde 2026-07-23 — esta edicao alinha o REGISTRO ao codigo e nao muda comportamento. Ressalva registrada: a DEC amplia quem pode conceder `critica-aprovada` e foi aprovada por um dos donos que ela habilita; a separacao de deveres segue preservada pelo `review-gate`, que exige `aprovado-humano` de um humano **diferente do autor do PR**.
```

**1b.** `CLAUDE.md`, **linha 189** — trocar **somente a última célula**, `| PROPOSTA |` → `| APROVADA |`.
Não mexer na data (`2026-07-23` é a data da decisão; a DEC-018 também preserva a original). Edição in-place:
o arquivo continua com **189 linhas**.

**1c. COMMIT A** (por path, só estes dois arquivos):
```
git checkout -- graphify-out/
git add docs/decisions/DEC-019.md CLAUDE.md
git commit -m "docs(dec-019): PROPOSTA -> APROVADA -- alinha o registro ao guard.yml ja em vigor"
```

### 2. `pyproject.toml` — declarar a dependência (**CRÍTICO**) → **COMMIT B, isolado**
Acrescentar **no fim do arquivo** (após a linha 149, `packages = ["src/motor_expansao"]`), precedido de uma
linha em branco. **Conteúdo literal** — o comentário **não é decorativo**: é o que responde ao achado 4 do
`REVIEW.md:56` ("dependência nova fora do `constraints.txt`") dentro do próprio diff, que é tudo o que o
`claude-review` lê:

```toml
[dependency-groups]
# Ferramenta de ESTACAO DE TRABALHO (grafo de conhecimento, §2/§7): serve o MCP declarado no
# `.mcp.json`. NAO roda no CI, nem nas imagens de producao (Dockerfile.streamlit/.api), nem no
# container do loop -- nenhum arquivo de src/, scripts/ ou tests/ faz `import graphify`.
# POR QUE [dependency-groups] (PEP 735) E NAO UM EXTRA: o comando canonico de refresh do lock
# (ci.yml:45, `uv pip compile pyproject.toml --all-extras ...`) NAO enxerga dependency-groups.
# MEDIDO em 2026-07-28: compilar com e sem este bloco devolve os MESMOS 214 pins, byte a byte;
# como extra, o mesmo comando devolve 250 (+36, dos quais 25 gramaticas tree-sitter). Logo o
# `constraints.txt` fica INTOCADO de proposito -- nao e' esquecimento.
# ATENCAO AO NOME: o pacote no PyPI e' `graphifyy`, com DOIS 'y'. O nome nu `graphify` esta
# UNCLAIMED no PyPI -- declara-lo abriria typosquatting.
# O PIN `mcp<2` E' OBRIGATORIO, NAO COSMETICO: o mcp 2.0.0 removeu `AnyUrl` de `mcp.types` e
# `graphify/serve.py:1116` ainda o importa; sem o pin o servidor sobe e morre com a mensagem
# ENGANOSA 'mcp not installed'. Medido 2026-07-28: mcp 2.0.0 = ImportError / mcp 1.29.0 =
# handshake OK com 10 tools. Reavaliar quando o graphify suportar mcp 2.x.
# Instalar: python -m pip install --group graph
graph = [
    "graphifyy[mcp]>=0.9.29,<0.10",
    "mcp>=1.28,<2",
]
```

**COMMIT B** (sozinho — é a mudança de supply chain, merece isolamento na revisão):
```
git checkout -- graphify-out/
git add pyproject.toml
git commit -m "build(graph-02): declara graphifyy[mcp] em [dependency-groups] com pin mcp<2"
```

### 3. Instalar e **provar** (antes de escrever o resto)
```
python -m pip install --group graph
python -c "import importlib.metadata as m; print('graphifyy', m.version('graphifyy'), '| mcp', m.version('mcp'))"
```
**Critério:** `mcp` tem de sair **1.x** (`<2`). Se sair `2.x`, o pin não pegou — **parar e reportar**.
Guardar a saída: ela vai para o corpo do PR.

### 4. Criar `.mcp.json` na raiz (novo, **livre** no `loop_guard`)
Conteúdo **literal** (UTF-8 **sem BOM**, LF, newline final; usar a tool Write, **nunca** `Out-File`/
`Set-Content` sem `-Encoding utf8` — BOM quebra parser JSON estrito):

```json
{
  "mcpServers": {
    "graphify": {
      "type": "stdio",
      "command": "python",
      "args": ["-m", "graphify.serve", "graphify-out/graph.json"],
      "env": {
        "GRAPHIFY_QUERY_LOG_DISABLE": "1"
      }
    }
  }
}
```
- `python` literal (D10). `graphify-mcp` nu está **descartado**: medido fora do PATH.
- Caminho **relativo** posicional = o default de `_default_graph_json()`, explícito e testável.
- `GRAPHIFY_QUERY_LOG_DISABLE` é redundante hoje (o log já é opt-in) mas **trava por contrato** que nada é
  escrito em `~/.cache` se o default upstream mudar.
- Nome do servidor `graphify` = **identificador ASCII, sem acento** (§2).

### 5. Criar `scripts/verify_mcp_graphify.py` (novo, **livre**) — a prova funcional versionada
Conteúdo literal (o script lê `command`/`args` **do próprio `.mcp.json`** — é isso que faz a prova valer para
a config, e não para uma linha digitada à mão):

```python
"""Prova funcional do servidor MCP do graphify (BLK-GRAPH-02).

Sobe o processo exatamente como o `.mcp.json` manda e faz initialize + tools/list +
tools/call por JSON-RPC sobre stdio. Nao depende do harness do Claude Code.

Uso:  python scripts/verify_mcp_graphify.py     (exit 0 = PASS)

Requer o grupo `graph` do pyproject: python -m pip install --group graph
"""

from __future__ import annotations

import json
import os
import subprocess
import sys
import tempfile
import time
from pathlib import Path

RAIZ = Path(__file__).resolve().parents[1]
ESPERADO = [
    "get_community", "get_neighbors", "get_node", "get_pr_impact", "god_nodes",
    "graph_stats", "list_prs", "query_graph", "shortest_path", "triage_prs",
]


def main() -> int:
    srv = json.loads((RAIZ / ".mcp.json").read_text(encoding="utf-8"))["mcpServers"]["graphify"]
    cmd = [srv["command"], *srv.get("args", [])]
    env = {**os.environ, **srv.get("env", {})}
    print("LAUNCH:", cmd, "\nCWD   :", RAIZ)

    errf = tempfile.TemporaryFile(mode="w+", encoding="utf-8", errors="replace")
    p = subprocess.Popen(
        cmd, cwd=RAIZ, env=env, stdin=subprocess.PIPE, stdout=subprocess.PIPE,
        stderr=errf, text=True, encoding="utf-8", bufsize=1,
    )

    def send(o: dict) -> None:
        assert p.stdin is not None
        p.stdin.write(json.dumps(o) + "\n")
        p.stdin.flush()

    send({"jsonrpc": "2.0", "id": 1, "method": "initialize", "params": {
        "protocolVersion": "2025-06-18", "capabilities": {},
        "clientInfo": {"name": "blk-graph-02-probe", "version": "1"}}})
    send({"jsonrpc": "2.0", "method": "notifications/initialized"})
    send({"jsonrpc": "2.0", "id": 2, "method": "tools/list"})
    send({"jsonrpc": "2.0", "id": 3, "method": "tools/call",
          "params": {"name": "graph_stats", "arguments": {}}})
    send({"jsonrpc": "2.0", "id": 4, "method": "tools/call", "params": {
        "name": "query_graph",
        "arguments": {"question": "score_priorizacao", "depth": 2, "token_budget": 600}}})

    got: dict[int, dict] = {}
    deadline = time.time() + 120
    assert p.stdout is not None
    while time.time() < deadline and len(got) < 4:
        line = p.stdout.readline()
        if not line:
            break
        try:
            m = json.loads(line)
        except json.JSONDecodeError:
            continue
        if isinstance(m, dict) and "id" in m:
            got[m["id"]] = m

    assert p.stdin is not None
    p.stdin.close()
    try:
        rc = p.wait(timeout=20)
    except subprocess.TimeoutExpired:
        p.kill()
        rc = p.wait()

    def texto(i: int) -> str:
        c = (got.get(i, {}).get("result", {}) or {}).get("content") or [{}]
        return (c[0] or {}).get("text", "")

    init = got.get(1, {}).get("result", {})
    tools = [t["name"] for t in got.get(2, {}).get("result", {}).get("tools", [])]
    print("PROTOCOL   :", init.get("protocolVersion"))
    print("SERVERINFO :", init.get("serverInfo"))
    print("TOOLS (%d) : %s" % (len(tools), sorted(tools)))
    print("graph_stats:", " | ".join(texto(3).splitlines())[:400])
    print("query_graph:", " | ".join(texto(4).splitlines())[:300])
    print("EXIT       :", rc)
    errf.seek(0)
    print("STDERR     :", errf.read()[-800:] or "(vazio)")

    ok = (
        sorted(tools) == ESPERADO
        and "error" not in got.get(3, {})
        and "error" not in got.get(4, {})
        and texto(3).strip() != ""
        and texto(4).strip() != ""
    )
    print("VERDICT    :", "PASS" if ok else "FAIL")
    return 0 if ok else 1


if __name__ == "__main__":
    sys.exit(main())
```

**Rodar agora** e **colar a saída inteira no handoff do Builder e no corpo do PR**:
```
python scripts/verify_mcp_graphify.py
```
Esperado: `TOOLS (10)` com os 10 nomes, `graph_stats` com `Nodes: 7633 | Edges: 15362 | Communities: 424`,
`query_graph` não vazio, `EXIT: 0`, `STDERR: (vazio)`, `VERDICT: PASS`.

**Se vier `TOOLS (0)` + `ImportError: mcp not installed`:** o pin não pegou — conferir `pip show mcp`. É o
modo de falha do M6, e a mensagem **mente**.

### 6. Criar `.githooks/post-commit` (novo, **livre**)
- Copiar **byte a byte** de `.git/hooks/post-commit` (9.376 bytes, **LF puro**). **Não editar o conteúdo** —
  arquivo upstream que a gente edita é arquivo que a gente mantém para sempre; o comentário do projeto vai
  no runbook.
- **NÃO** criar `.githooks/post-checkout` (D4).
- **Bit de execução (obrigatório):** `core.fileMode=false` aqui → entraria como `100644` e o
  `core.hooksPath` ficaria **mudo em Linux**:
  ```
  git add .githooks/post-commit
  git update-index --chmod=+x .githooks/post-commit
  git ls-files -s .githooks/post-commit     # tem de imprimir 100755
  ```

### 7. `.gitattributes` — regra de EOL dos hooks (**CRÍTICO**, edição estritamente **ADITIVA**)
Inserir **logo após a linha 31** (`docs/*.md           text eol=lf`), como um bloco novo precedido de linha em
branco. Texto literal (alinhamento na coluna 21, como os vizinhos):

```
# Hooks versionados (BLK-GRAPH-02): `.githooks/post-commit` nao tem extensao, entao o
# `*.sh` da L12 NAO o cobre. Roda sob o `sh` do Git for Windows e, com core.autocrlf=true,
# o checkout entregaria CRLF e quebraria o shebang `#!/bin/sh`.
.githooks/*         text eol=lf
```

Uso `.githooks/*` (não `**`): a sonda 3 registrou `**` como NÃO MEDIDO em `.gitattributes`, e `*` é 100%
suficiente para hooks planos. **Validar logo após:**
```
git check-attr -a -- .githooks/post-commit     # deve listar: text: set / eol: lf
```

**NÃO TOCAR** no resto do arquivo: L5-8 (`secrets/*.enc*`), L12 (`*.sh`), L17 (`*.py`), L25
(`tasks/completed.md merge=union`), L30-31 (DEC-017) e **sobretudo a L39**
(`graphify-out/graph.json -diff linguist-generated=true`) — removê-la refaz o afogamento do `claude-review`
que reprovou o PR #150, e é o que mantém este PR revisável.
**Nunca reintroduzir** `graphify-out/graph.json merge=graphify` (revertida em `1ebef60`). Se em algum momento
rodar `python -m graphify hook install`, **conferir o `.gitattributes` depois**: o instalador re-adiciona
essa linha.

### 8. `.claude/settings.json` — pré-aprovar o servidor e liberar os verbos read-only (GOVERNANÇA)
Adicionar a chave `enabledMcpjsonServers` **no topo do objeto** e **5 entradas** no início de
`permissions.allow` (o resto da lista fica **inalterado**):

```json
{
  "enabledMcpjsonServers": ["graphify"],
  "permissions": {
    "allow": [
      "Bash(python -m graphify query*)",
      "Bash(python -m graphify path*)",
      "Bash(python -m graphify explain*)",
      "Bash(python -m graphify god-nodes*)",
      "Bash(python -m graphify affected*)",
      "Bash(python -m pytest*)",
      "... (resto da lista existente, na ordem atual, inalterado)"
    ]
  }
}
```
**NÃO** adicionar o glob `Bash(python -m graphify*)` (D3): liberaria `extract`/`update` (rebuild caro) e,
pior, `hook install` — que reescreve o `.gitattributes` (CRÍTICO).
Usar `enabledMcpjsonServers` (estrito), **não** `enableAllProjectMcpServers` (abriria qualquer servidor
futuro do arquivo).

### 9. `tasks/backlog.md` — 3 correções literais (GOVERNANÇA)

**9a — linha 114** (D11), substituir a linha inteira por:
```
| **Criticidade** | **Crítica** — o Escopo B entrou neste ciclo (gate humano de 2026-07-28): o PR toca `pyproject.toml` e `.gitattributes`, ambos `critico` no `loop_guard`. |
```

**9b — linha 143** (D12), substituir a linha inteira por:
```
| Pacote instalado | `graphifyy` (DOIS 'y' — o nome nu `graphify` está UNCLAIMED no PyPI) não consta de `pyproject.toml` nem de `constraints.txt` |
```

**9c — linha 164** (D12), substituir **apenas o nome** na linha, mantendo o resto do parágrafo:
```
**Escopo B — Crítica, SEPARÁVEL.** Declarar `graphifyy[mcp]` como dependência (extra opcional em
```

### 10. `CLAUDE.md` §2 — inserir a NORMA (**+1 linha**)
Inserir como **primeiro bullet** logo após a linha 38 (`**Regras operacionais rapidas (dia a dia):**`),
empurrando `- **Resposta ao Felipe:**` para baixo. Texto **literal**:

```
- **Grafo antes de varredura:** para "como X funciona", "o que chama Y", "onde vive Z", consultar o grafo PRIMEIRO — tool MCP `graphify` (`.mcp.json` na raiz) ou `python -m graphify query "<pergunta>"`. Grep/Read amplo so' depois que o grafo nao resolver. Instalacao (`pip install --group graph`), limites e armadilhas: §7 + `docs/grafo_conhecimento.md`.
```

> **Acentuação — decisão explícita:** o bullet entra **sem acentos**, no mesmo estilo dos 5 vizinhos do bloco
> `Regras operacionais rapidas` (todos sem acento, inclusive o `so'` com apóstrofo, já usado no arquivo). É
> **movimento de texto legado**, não texto novo de usuário; misturar acentuação dentro de uma mesma lista
> seria pior, e "corrigir" o arquivo inteiro é expansão de escopo vedada. Todo texto **novo**
> (`docs/grafo_conhecimento.md`, passo 12) vai com **acentuação correta do português**.

### 11. `CLAUDE.md` §7 — substituir as linhas **144–149** (6 linhas) por estas **5**
Saldo: §2 +1 / §7 −1 → **189 linhas, igual a agora** (teto de 230 travado por
`tests/unit/test_claude_md_size.py`). A §7 perde a NORMA (fica só na §2, sem duplicação), ganha o comando de
instalação novo, o **pin `mcp<2`** e o comando de update **corrigido** (D5). Texto **literal**:

```
- **Grafo de conhecimento (graphify) — detalhe tecnico** (a NORMA de consultar antes de varrer esta na §2): `python -m graphify query "<pergunta>"`, `... path "A" "B"`, `... explain "<no>"`. **Sempre `python -m graphify`, NUNCA o `graphify` nu** — o pacote instala `graphify.exe` em `<python>/Scripts/`, normalmente fora do PATH (verificado 2026-07-27). Instalar: `python -m pip install --group graph` (grupo `graph` do `pyproject.toml`, PEP 735 — fora do `constraints.txt` DE PROPOSITO, ver comentario no bloco). **O pin `mcp>=1.28,<2` e' obrigatorio:** o `mcp` 2.0.0 removeu `AnyUrl` de `mcp.types` e quebra o servidor com o erro ENGANOSO `'mcp not installed'` (medido 2026-07-28). Cobre 421 arquivos (290 de codigo por AST + 131 docs/contratos/DECs) em 7.633 nos e 15.362 arestas; ~58x menos tokens por consulta que ler o corpus. Artefatos versionados: `graphify-out/graph.json` e `graphify-out/GRAPH_REPORT.md`. Runbook: `docs/grafo_conhecimento.md`.
- **Como o grafo se mantem atualizado (2 caminhos, NAO se substituem):**
  - **CODIGO — automatico.** Hook `post-commit` versionado em `.githooks/` dispara rebuild AST em segundo plano a cada commit (sem LLM, nao bloqueia o `git commit`; log em `~/.cache/graphify-rebuild.log`; pular com `GRAPHIFY_SKIP_HOOK=1`). **Nao viaja sozinho:** o git NAO aceita `core.hooksPath` vindo do repo — cada clone roda `git config core.hooksPath .githooks` uma vez (mesma pegadinha do `.mcp.json`, que pede aprovacao na 1a sessao para quem nao usa `enabledMcpjsonServers`). Equivalente manual: `python -m graphify update .`. Container do loop e CI nao tem o pacote — la o grafo nao atualiza.
  - **DOCS/DECs/backlog — MANUAL.** O hook ignora a camada semantica (precisa de LLM). Depois de mexer em `docs/`, `tasks/`, `CLAUDE.md` ou nas DECs, rodar **numa sessao Claude o SLASH-COMMAND** `/graphify . --update`. **`python -m graphify . --update` NAO faz isso** (verificado 2026-07-28): o CLI reescreve para `extract . --update`, o flag e' ignorado em silencio e vira uma extracao COMPLETA com LLM.
- **LIMITES do grafo, ler antes de confiar:** (a) entre um commit de doc e o `/graphify . --update` manual, a camada semantica esta DESATUALIZADA — se a resposta depender de doc recem-alterado, ler o arquivo; (b) a consulta trunca por orcamento de tokens (usar `--budget`); (c) `context/handoff/` (566 logs) e as imagens ficaram **fora** do corpus de proposito, mas o rebuild automatico NAO respeita esse recorte — por isso o `post-checkout` (rebuild total) ficou FORA do `.githooks/`; (d) e' INDICE de navegacao, **nao** fonte canonica — parametro, formula e guardrail valem pelo §3/§8 e pelos contratos em `docs/`.
```

**Nota sobre os números:** `7.633 nos / 15.362 arestas` é o que o próprio `graph_stats` reporta sobre o
`graph.json` versionado (medido hoje, e igual ao que a sonda 4 mediu). O `7.560 / 15.351` que está lá hoje
está defasado. Reconferir com `python scripts/verify_mcp_graphify.py` (linha `graph_stats:`).

Conferir depois: `wc -l < CLAUDE.md` → **189**.

### 12. Criar `docs/grafo_conhecimento.md` (novo, **livre**) — **acentuação correta do português**
O Builder escreve a prosa; abaixo é o **contrato do que não pode faltar**:
1. **O que é / para que serve** — índice de navegação, ~58x menos tokens por consulta; **não** é fonte canônica.
2. **Instalar** — `python -m pip install --group graph`. Explicar **por que é um `[dependency-groups]` e não
   um extra** (o lock não o enxerga; medição dos 214 vs 250 pins) e **por que o `mcp<2` está pinado**
   (o `AnyUrl` removido no 2.0.0 e a mensagem de erro que mente).
3. **Consultar** — CLI (`query`/`path`/`explain`, sempre `python -m graphify`) e MCP. Listar as **10 tools
   reais**: `query_graph`, `get_node`, `get_neighbors`, `get_community`, `god_nodes`, `graph_stats`,
   `shortest_path`, `list_prs`, `get_pr_impact`, `triage_prs`. Registrar que as 3 de PR shellam o `gh`
   (dependem de rede/auth) e **por isso ficam fora** do smoke-test.
4. **`.mcp.json`** + **LIMITAÇÃO GÊMEA 1**: servidor de `.mcp.json` **exige aprovação do usuário na primeira
   sessão**; `enabledMcpjsonServers` em `.claude/settings.json` pré-aprova, mas quem não usar a chave aprova
   por `/mcp`. Registrar o **risco de cwd** e as duas redes de segurança medidas: o servidor não morre se o
   grafo default não carregar (`serve.py:1176-1182`) e as 10 tools aceitam `project_path` absoluto.
5. **Verificar** — `python scripts/verify_mcp_graphify.py`, com a saída esperada.
6. **Hooks (`.githooks/`)** + **LIMITAÇÃO GÊMEA 2**: o git **não aplica `core.hooksPath` vindo do
   repositório**; **cada clone roda `git config core.hooksPath .githooks` uma vez**. O ganho é o hook ser
   revisável e igual para todos, **não** auto-instalável.
7. **Por que o `post-checkout` NÃO foi versionado** (seção obrigatória). Tabela curado × rebuild automático:
   **7.560 nós / 15.351 arestas / 10.157.850 bytes** contra **13.599 nós / 21.063 arestas / 15.299.593
   bytes**; `context/handoff/` dentro do corpus contra o recorte da §7; `.graphify_labels.json` curado
   sobrescrito. Causa: `post-checkout` chama `_rebuild_code` **sem `changed_paths`** = corpus inteiro, e
   `_rebuild_code` **não tem parâmetro de exclusão**. `git config core.hooksPath .githooks` neutraliza o
   `post-checkout` local.
8. **Se o repo aparecer sujo em `graphify-out/`** — é o hook, não você: `git checkout -- graphify-out/`,
   commit **por path**, nunca `git add -A`.
9. **Armadilhas conhecidas** — (a) `graphify` nu fora do PATH; (b) **não rodar `python -m graphify hook
   install`**: re-adiciona `graphify-out/graph.json merge=graphify` ao `.gitattributes` (CRÍTICO); (c)
   resíduo `merge.graphify.*` no git config **local**, órfão e inerte desde `1ebef60`, mas volta a valer no
   primeiro `hook install`; (d) **não remover** o `-diff linguist-generated=true` da L39 do `.gitattributes`;
   (e) o nome no PyPI é `graphifyy`, e `graphify` está **UNCLAIMED** — typosquatting real.
10. **Dívida registrada** — o `constraints.txt` **não** pina o grafo, por decisão medida; e a pergunta de
    fundo: `graphify-out/graph.json` deve continuar versionado ou virar artefato de CI?

### 13. `docs/README.md` — 1 linha no índice (seção "5. Orquestração, Loop e Governança")
Inserir logo após a linha do `garimpeiro.md` (linha 40):
```
- [grafo_conhecimento.md](grafo_conhecimento.md) — **[canônico]** grafo de conhecimento (graphify): instalação (`--group graph`), consulta via MCP/CLI, `.mcp.json`, hooks em `.githooks/` e as duas limitações de instalação por clone.
```

### 14. Criar `tests/contracts/test_grafo_ferramenta.py` (novo, **livre**) — **NÃO skipa**
Seguir o estilo de `tests/contracts/test_docs_vs_codigo.py` (`from __future__ import annotations`,
`RAIZ = Path(__file__).resolve().parents[2]`, `-> None`, mensagens que ensinam o conserto, e o princípio
**âncora ausente é FALHA, nunca skip**). Uma função por item:

1. `test_mcp_json_e_json_valido_e_declara_o_servidor_graphify` — existe; `json.loads` de
   `read_text(encoding="utf-8")` OK; **bytes não começam com `b"\xef\xbb\xbf"`**; chaves de topo
   `== {"mcpServers"}`; `"graphify"` presente; `type == "stdio"`; `command` str não vazia; `args` lista de str.
2. `test_mcp_json_invoca_o_MODULO_serve_e_nao_um_subcomando` — `args[:2] == ["-m", "graphify.serve"]`.
   Mensagem cita a armadilha: **não existe subcomando `serve`** — `python -m graphify serve` cai no fallback
   de ajuda e **sai 0 sem servir nada**.
3. `test_mcp_json_usa_command_literal_e_nao_expansao_de_variavel` — `command == "python"`; falhar se contiver
   `"${"`. Mensagem explica D10: `scripts/verify_mcp_graphify.py` executa esse valor via `subprocess`; uma
   expansão faria a prova divergir do que o harness roda.
4. `test_mcp_json_aponta_para_o_caminho_canonico_do_grafo` — `args[2] == "graphify-out/graph.json"`.
   **Comentar no código por que NÃO se asserta `is_file()`**: um gate que quebrasse numa base sem o artefato
   viraria ruído e acabaria desligado.
5. `test_nome_do_servidor_mcp_e_identificador_ascii` — cada chave de `mcpServers` casa `^[a-z][a-z0-9_-]*$`.
   Trava a §2: identificador **nunca** leva acento.
6. `test_settings_json_pre_aprova_exatamente_os_servidores_do_mcp_json` —
   `set(.claude/settings.json["enabledMcpjsonServers"]) == set(.mcp.json["mcpServers"])`. Trava o drift:
   renomear o servidor sem atualizar a pré-aprovação volta a pedir aprovação manual **em silêncio**.
7. **`test_dependency_group_graph_declara_graphifyy_e_fixa_mcp_abaixo_de_2`** — ler `pyproject.toml` com
   `tomllib` (stdlib no 3.11+, versão do CI). Asserir: existe `dependency-groups.graph`; alguma entrada
   começa com `graphifyy[mcp]`; **nenhuma** entrada é o nome nu `graphify` (typosquatting); e existe uma
   entrada de `mcp` cujo specifier contém `<2`. **Mensagem de erro obrigatória**, citando a medição:
   *"mcp 2.0.0 removeu AnyUrl de mcp.types; graphify/serve.py:1116 ainda importa. Sem o pin o servidor morre
   com a mensagem enganosa 'mcp not installed'. Medido 2026-07-28."* **Este é o teste que transforma a
   descoberta M6 em regressão automática — ele roda no CI sem o extra instalado.**
8. `test_graphify_nao_e_extra_para_o_lock_nao_incha` — `"graph" not in
   pyproject["project"]["optional-dependencies"]`. Mensagem: como extra, o
   `uv pip compile --all-extras` do `ci.yml:45` puxaria +36 pins (medido).
9. `test_githooks_versiona_o_post_commit_com_lf_e_sem_o_post_checkout` — `.githooks/post-commit` existe,
   `read_bytes()` começa com `b"#!/bin/sh"`, contém `b"graphify-hook-start"`, e **`b"\r\n" not in dados`**;
   e `not (RAIZ / ".githooks" / "post-checkout").exists()`, com mensagem explicando D4 (rebuild de corpus
   inteiro diverge do grafo curado) para que reintroduzi-lo seja um ato **consciente**.
10. `test_gitattributes_protege_o_graph_json_e_fixa_eol_dos_hooks` — nenhuma linha contém `merge=graphify`;
    toda linha que mencione `graphify-out/graph.json` contém `-diff`; e existe uma linha casando
    `.githooks/` com `eol=lf`.
11. `test_norma_do_grafo_esta_na_secao_2_e_nao_duplicada_na_secao_7` — fatiar o `CLAUDE.md` entre
    `## 2. Regras operacionais` e `## 3. Nucleo oficial M1`, e entre `## 7. Onde aprofundar` e
    `## 8. Decisoes registradas`; falhar explicitamente se um heading sumiu. Asserir: §2 contém
    `python -m graphify query`; §7 **não** contém `Grafo antes de varredura`; §2 **não** contém
    `LIMITES do grafo`.
12. `test_claude_md_nao_recomenda_a_forma_de_update_que_o_cli_ignora` — `"python -m graphify . --update"`
    **ausente** do `CLAUDE.md`, e `"/graphify . --update"` **presente**.
13. `test_runbook_do_grafo_existe_e_documenta_as_duas_limitacoes_gemeas` — `docs/grafo_conhecimento.md`
    existe e contém `core.hooksPath`, `enabledMcpjsonServers`, `--group graph` e `mcp`; e `docs/README.md`
    referencia `grafo_conhecimento.md`.
14. `test_dec_019_registrada_como_aprovada` — `docs/decisions/DEC-019.md` **não** contém `**PROPOSTA**`; a
    linha da DEC-019 no §8 do `CLAUDE.md` termina em `| APROVADA |`. Trava o drift registro-vs-código que
    este bloco veio consertar.

### 15. Criar `tests/integration/test_mcp_graphify_server.py` (novo, **livre**) — **skipa sem o extra**
Abrir com `pytest.importorskip("mcp", reason="grupo `graph` nao instalado (CI/loop nao instalam)")`.
Corpo: importar `main` de `scripts/verify_mcp_graphify.py` (ou reexecutá-lo via `subprocess`) e asserir
exit 0. **Declarar no docstring**, sem eufemismo: *"Este teste SKIPa no CI e no container do loop, porque
nenhum dos dois instala o grupo `graph`. Ele NÃO é o gate. A validação executada é (a)
`tests/contracts/test_grafo_ferramenta.py`, que roda sempre e trava o pin `mcp<2`, e (b) a execução manual
obrigatória de `python scripts/verify_mcp_graphify.py`, cuja saída vai colada no PR."*

### 16. `tests/unit/test_loop_guard_paths.py` — 3 entradas em `test_caminhos_limpos`
Acrescentar ao `parametrize` que termina na linha 200:
```python
        # BLK-GRAPH-02: o tooling do grafo e' LIVRE no guard (o que escala o PR e' .gitattributes
        # + pyproject.toml, nao estes).
        ".mcp.json",
        ".githooks/post-commit",
        "scripts/verify_mcp_graphify.py",
```

### 17. Commits por path (**nunca `git add -A`**)

Já feitos nos passos 1 e 2 (**COMMIT A** = DEC-019; **COMMIT B** = `pyproject.toml`). Faltam:

**COMMIT C — a ferramenta:**
```
git checkout -- graphify-out/
git add .mcp.json .githooks/post-commit .gitattributes .claude/settings.json CLAUDE.md \
        scripts/verify_mcp_graphify.py docs/grafo_conhecimento.md docs/README.md \
        tests/contracts/test_grafo_ferramenta.py tests/integration/test_mcp_graphify_server.py \
        tests/unit/test_loop_guard_paths.py
git update-index --chmod=+x .githooks/post-commit
git status --porcelain          # graphify-out/ NAO pode estar staged
git commit -m "feat(graph-02): grafo como FERRAMENTA -- .mcp.json funcional, norma na §2 e hook versionado"
```

**COMMIT D — bookkeeping do backlog:**
```
git add tasks/backlog.md
git commit -m "docs(backlog): BLK-GRAPH-02 vira Critica e corrige o nome do pacote (graphifyy)"
```

**Conferência final:**
```
git diff --name-only main...HEAD | grep graphify-out    # so' pode listar os artefatos do GRAPH-01
git diff --numstat main...HEAD
```

**Base do PR: `main`** (DECISÃO ZERO). Entrega BLK-GRAPH-01 + 02 juntos, como o humano pediu.

## Arquivos afetados
- `C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao\docs\decisions\DEC-019.md` (livre)
- `C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao\pyproject.toml` (**CRÍTICO** + CODEOWNERS)
- `C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao\.gitattributes` (**CRÍTICO**)
- `C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao\CLAUDE.md` (§2 +1 / §7 −1 / L189 status — **GOVERNANÇA** + CODEOWNERS)
- `C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao\.claude\settings.json` (**GOVERNANÇA**)
- `C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao\tasks\backlog.md` (**GOVERNANÇA**)
- `C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao\.mcp.json` (**novo** — livre)
- `C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao\.githooks\post-commit` (**novo** — livre, `100755`)
- `C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao\scripts\verify_mcp_graphify.py` (**novo** — livre)
- `C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao\docs\grafo_conhecimento.md` (**novo** — livre)
- `C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao\docs\README.md` (livre)
- `C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao\tests\contracts\test_grafo_ferramenta.py` (**novo** — livre)
- `C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao\tests\integration\test_mcp_graphify_server.py` (**novo** — livre)
- `C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao\tests\unit\test_loop_guard_paths.py` (livre)
- Fechamento (Passo 6, não do Builder): `tasks/completed.md` (livre)

**NÃO commitar:** `constraints.txt` (D9), `graphify-out/*`, `.githooks/post-checkout` (D4),
`.github/workflows/guard.yml` (a DEC-019 já está implementada lá), `PRD.md`, `context/handoff.md`,
`tasks/current_task.md`.

## Dependências
- Branch `ciclo/BLK-GRAPH-02`, criada de `graph-01` @ `be7787a`. **PR com base `main`.**
- `graphify-out/graph.json` versionado nesta linhagem (alvo do `.mcp.json`).
- `.git/hooks/post-commit` presente (fonte da cópia): 9.376 bytes, LF puro.
- **Rede** para `python -m pip install --group graph` (passo 3). Sem ela, os passos 3 e 5 não rodam e o
  critério "funcional" não fecha.
- Nenhuma dependência de dado M1 ou de artefato de `data/`.

## Riscos técnicos
- **R-MCP2 (NOVO, o mais importante) — `mcp` 2.0.0 quebra o servidor com mensagem enganosa.** Mitigação em
  três camadas: pin `mcp>=1.28,<2` no grupo; teste 7 do passo 14 (roda no CI, **não** skipa); e o comentário
  no `pyproject.toml` explicando por que o pin existe, para ninguém "limpar" depois.
- **R-LOCK (NOVO) — regenerar o `constraints.txt` hoje bumpa 30 pacotes**, incluindo `streamlit` e `fastapi`
  de produção (M3). Mitigação: `[dependency-groups]` mantém o lock fora do caminho; **não tocar** o arquivo.
- **R-REVIEW — `claude-review` pode disparar o achado 4 do `REVIEW.md:56`** ("dependência nova fora do
  `constraints.txt`"). Mitigação: o comentário no diff do `pyproject.toml` responde à objeção onde o revisor
  lê. Atenuante: a lista "Não reportar" (`REVIEW.md:63-70`) já isenta *tocar* em `pyproject.toml`/
  `constraints.txt` como achado em si.
- **R-PUBLISH — tocar `pyproject.toml` dispara o job `publish-api`** (`ci.yml:236` casa `pyproject\.toml$`):
  o merge na `main` reconstrói e republica a imagem pesada da API/bot no GHCR. **Não é deploy** (deploy segue
  manual por digest, §6), mas **declarar no corpo do PR** para não parecer acidente.
- **R2 — `hook install` re-adiciona `merge=graphify` ao `.gitattributes`.** Mitigação tripla: proibido no
  plano; `permissions.allow` não libera o verbo (D3); teste 10 falha se a linha aparecer.
- **R3 — resíduo `merge.graphify.*` no git config LOCAL.** Órfão e inerte desde `1ebef60`; reaparece no
  primeiro `hook install`. Documentar no runbook, **não mexer**.
- **R5 — `-diff linguist-generated=true` (L39 do `.gitattributes`).** Não encostar; travado pelo teste 10.
  É o que mantém este PR (2.312 linhas) revisável em vez de 265k.
- **R10 — RESOLVIDO por D4** (só `post-commit` viaja) e registrado no runbook, na §7 e no teste 9.
- **Bit de execução.** `core.fileMode=false`: sem `git update-index --chmod=+x`, o hook entra como `100644` e
  o `core.hooksPath` fica mudo em Linux. Passo 6 cobre; `git ls-files -s` verifica.
- **Portabilidade — NÃO prometer.** Não há venv no repo; `graphifyy` e `motor-expansao` convivem no Python
  global 3.14, enquanto o CI roda 3.11. O grupo torna a dependência **declarada e instalável em um comando**;
  ele **não** faz o `.mcp.json` funcionar sozinho num clone novo. O runbook precisa do passo explícito.
- **Baixo — `enabledMcpjsonServers` não reconhecido** por harness antigo: chave ignorada, cai no fluxo de
  aprovação manual. Sem dano.
- **Baixo — cwd do servidor.** Se o Claude Code lançar de um cwd inesperado, o caminho relativo não resolve;
  o servidor **não morre** (`serve.py:1176-1182`) e as tools aceitam `project_path` absoluto. Documentado.

## Critérios de aceite finais
1. `.mcp.json` na raiz, JSON válido, **sem BOM**, `mcpServers.graphify`, `type: "stdio"`,
   `command: "python"` (literal, sem `${...}`), `args: ["-m", "graphify.serve", "graphify-out/graph.json"]`.
2. **PROVA FUNCIONAL (o critério original do backlog).** `python scripts/verify_mcp_graphify.py` imprime
   `TOOLS (10)` com os 10 nomes, `graph_stats` não vazio, `query_graph` não vazio, `EXIT: 0`,
   `STDERR: (vazio)`, `VERDICT: PASS`. **Saída inteira colada no handoff do Builder e no corpo do PR.**
3. `pyproject.toml` declara `[dependency-groups].graph` com `graphifyy[mcp]` (**dois `y`**) **e** `mcp<2`.
   `constraints.txt` **byte-a-byte inalterado**.
4. Norma de consultar o grafo **na §2**; §7 só com detalhe técnico; **sem duplicação** (teste 11).
5. `.githooks/post-commit` versionado, `100755`, LF, byte-idêntico ao instalado. **`.githooks/post-checkout`
   ausente por decisão registrada** (D4).
6. `.gitattributes` ganha **apenas** o bloco `.githooks/* text eol=lf`; L39 e todo o resto **intactos**;
   `git check-attr -a -- .githooks/post-commit` mostra `eol: lf`.
7. `docs/grafo_conhecimento.md` documenta as **duas limitações gêmeas**, o **porquê do `mcp<2`**, o **porquê
   de não pinar no lock** e a divergência do rebuild automático (tabela 7.560 × 13.599). Linkado no
   `docs/README.md`.
8. `CLAUDE.md` continua com **189 linhas**, sem a string `python -m graphify . --update`, e com a DEC-019
   como `| APROVADA |`.
9. `tasks/backlog.md`: L114 declara só **Crítica**; L143 e L164 dizem `graphifyy`, não `graphify`.
10. `ruff` e `mypy` limpos; suíte verde contra a baseline medida **no momento** (não confiar no número do §5).
11. `loop_guard` no diff vs `main`: exatamente as classes esperadas — `critico` para `.gitattributes` e
    `pyproject.toml`; `governanca` para `CLAUDE.md`, `.gitignore`, `tasks/backlog.md`, `.claude/settings.json`.
    Nenhum caminho de M1/score/pipeline/artefato.
12. `graphify-out/*` sem mudanças novas além do que a linhagem `graph-01` já trazia.

## Validações obrigatórias
- Comando: `python -c "import json,pathlib;json.loads(pathlib.Path('.mcp.json').read_text(encoding='utf-8'));print('ok')"`
  · Critério mínimo: imprime `ok`, exit 0.
- Comando: `python -c "print(open('.mcp.json','rb').read()[:3])"` · Critério mínimo: **não** é `b'\xef\xbb\xbf'`.
- Comando: `python -m pip install --group graph` seguido de
  `python -c "import importlib.metadata as m; print(m.version('graphifyy'), m.version('mcp'))"`
  · Critério mínimo: `mcp` em **1.x** (`<2`). Se vier `2.x`, **parar**.
- Comando: **`python scripts/verify_mcp_graphify.py`** · Critério mínimo: `VERDICT: PASS`, exit **0**,
  `TOOLS (10)`. **Colar a saída inteira no handoff e no PR.** ← é o critério de aceite nº 2.
- Comando: `git diff --stat constraints.txt` · Critério mínimo: **saída vazia**.
- Comando: `git check-attr -a -- .githooks/post-commit` · Critério mínimo: contém `eol: lf`.
- Comando: `git ls-files -s .githooks/post-commit` · Critério mínimo: modo **100755**.
- Comando: `python -c "d=open('.githooks/post-commit','rb').read(); print(len(d), d.count(b'\r\n'))"`
  · Critério mínimo: `9376 0`.
- Comando: `wc -l < CLAUDE.md` · Critério mínimo: **189**.
- Comando: `python -m pytest -q tests/contracts/test_grafo_ferramenta.py tests/contracts/test_docs_vs_codigo.py tests/unit/test_claude_md_size.py tests/unit/test_loop_guard_paths.py`
  · Critério mínimo: 0 falhas, sem erro de import, **nenhum skip** em `test_grafo_ferramenta.py`.
- Comando: `python -m ruff check .` e `python -m mypy src` · Critério mínimo: 0 erros.
- Comando: `python -m pytest --collect-only -q | tail -1` **antes e depois** · Critério mínimo: só cresce, na
  quantidade dos testes novos.
- Comando: `git diff --name-only main...HEAD | python scripts/loop_guard.py --stdin --json`
  · Critério mínimo: as classes do critério de aceite 11 e **nada** de M1.
- Comando: `git status --porcelain` · Critério mínimo: `graphify-out/` não staged em nenhum commit.

## Fora de escopo
- **`constraints.txt`** — decisão medida (D9/M3), não esquecimento.
- `.githooks/post-checkout` (D4), reconstruir o grafo, `--update`, `hook install`, mudar o recorte do corpus,
  regenerar `graph.json`.
- `.github/workflows/guard.yml` — a DEC-019 já está implementada lá; nada a mudar.
- Limpar o resíduo `merge.graphify.*` do git config local (R3) — só documentar.
- Decidir se `graphify-out/graph.json` continua versionado — follow-up registrado, não resolvido aqui.
- Reavaliar `GRAPHIFY_PYTHON`/`${VAR:-default}` no `command` — follow-up, só com medição e segunda máquina.
- Corrigir o `Dockerfile.loop:27`, único ponto de instalação sem `-c constraints.txt` (achado colateral da
  sonda 2) — **bloco próprio**, não este.
- Qualquer coisa no M1: score, pesos, pipelines, artefatos. **READ-ONLY sobre o M1.**

## Alerta de aprovação
**ATENÇÃO: esta tarefa toca `pyproject.toml` e `.gitattributes` — a classe CRÍTICA do `loop_guard`
(supply chain e eol/binary dos `.enc.*`) — além de `CLAUDE.md`, `.claude/settings.json` e `tasks/backlog.md`
(GOVERNANÇA). Criticidade CRÍTICA. Aprovação humana obrigatória antes do Builder. O usuário deve ler este
handoff e confirmar.**

`score_priorizacao`, `hex_score_estrutural`, pesos, pipelines e artefatos oficiais do M1 **não são tocados** —
a criticidade vem da superfície de governança e supply chain, não do M1.

**Merge exigirá TRÊS portões cumulativos:** label `criticidade:critica` (autor) + label `critica-aprovada`
(`@Kastaldy` ou `@VinhoAbencoado`) + label `aprovado-humano` (humano ≠ autor) + review nativo de **CODEOWNER**
para `/CLAUDE.md` e `/pyproject.toml`. **Deploy não se aplica** — nada vai para a VPS; o `publish-api` só
republica a imagem no GHCR (R-PUBLISH).

Pontos que pedem decisão explícita do humano na aprovação:
1. **DECISÃO ZERO** — confirmar a base **`main`** (um PR final único entregando GRAPH-01 + GRAPH-02), agora
   que a premissa das "265k linhas" foi medida como obsoleta (2.312 linhas reais).
2. **D8/D9** — aceitar `[dependency-groups]` com o `constraints.txt` **intocado**, sabendo que regenerar o
   lock hoje bumparia 30 pacotes, incluindo `streamlit` e `fastapi` de produção.
3. **D13** — separar a DEC-019 em **PR próprio**? O commit já sai isolado (COMMIT A); a separação em PR é
   decisão do humano, pela circularidade registrada.
4. **R-PUBLISH** — ciente de que o merge republica a imagem da API/bot no GHCR (sem deploy).

## Guardrails ativos
- **READ-ONLY sobre o M1** (`CLAUDE.md` §5): nada aqui recalcula ou altera `score_priorizacao`,
  `hex_score_estrutural`, carteira, plano de curto prazo, plano de domínio ou artefato oficial. É tooling.
- **Parâmetros canônicos imutáveis sem DEC:** `H3_RESOLUTION=7`, `DIST_MIN_ULTRA_KM=1.0`, `RENDA_MIN=4500.0`,
  pesos `renda=0.40` / `pop=0.60`. Não tocados.
- **§6.1 / DEC-016:** bloco **não é loop-safe** (sem marcador no backlog; toca `CLAUDE.md` e `pyproject.toml`).
  Crítica → merge **nunca** automático. Deploy nunca automático.
- **§2 (acentuação):** texto **novo** de usuário (`docs/grafo_conhecimento.md`) com acentuação correta;
  **nunca** acentuar identificadores — chaves do `.mcp.json`, nome do servidor `graphify`, nome do grupo
  `graph`, nomes de tool (`query_graph`, `god_nodes`, …) e nomes de arquivo. O bullet movido para a §2 do
  `CLAUDE.md` mantém o estilo sem acento dos vizinhos (decisão registrada no passo 10).
- **Regra de manutenção do `CLAUDE.md` (topo):** manter curto. Saldo de linhas **neutro** (189 → 189); teto
  de 230 travado por `tests/unit/test_claude_md_size.py`.
- **Nunca declarar em `[dev]`:** o CI (`ci.yml:46`) e o `Dockerfile.loop:27` instalam `.[dev,api_mvp]`, e o
  `pip-audit` é **bloqueante** — 61 pacotes (29 gramáticas tree-sitter) passariam a poder reprovar o CI por
  um código que não roda nem em produção nem no CI.
- **Higiene de commit:** `git checkout -- graphify-out/` antes de commitar; **commit por path**, nunca
  `git add -A`; `graphify-out/*` não pode ganhar mudanças novas no diff do PR.
