# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Builder

## Entendimento da tarefa
Ajustar `_draw_watermark` em `censo_report.py` para que a marca d'água fique visível no conteúdo (alpha 0.65, 10 pt, cinza) e legível na capa turquesa (branco puro via `_WATERMARK_RGB_COVER`), sem alterar texto, template, mapas ou qualquer artefato M1.

## Plano técnico

### Passo 1 — Atualizar constantes `_WATERMARK_*` (linha 84 e 86)
Arquivo: `src/motor_expansao/dashboard/censo_report.py`

- Linha 84: `_WATERMARK_ALPHA = 0.40` → `_WATERMARK_ALPHA = 0.65`
- Linha 86: `_WATERMARK_FONT_PT = 9` → `_WATERMARK_FONT_PT = 10`
- Após linha 87 (`_WATERMARK_MARGIN = 20.0`), adicionar nova linha:
  ```python
  _WATERMARK_RGB_COVER = (255, 255, 255)
  ```
  (cor branca para a capa — visível sobre o fundo turquesa)

Manter inalterados: `_WATERMARK_BASE`, `_WATERMARK_RGB`, `_WATERMARK_ANGLE`, `_WATERMARK_MARGIN`.

### Passo 2 — Atualizar assinatura e corpo de `_draw_watermark` (linhas 261–275)
Arquivo: `src/motor_expansao/dashboard/censo_report.py`

- Assinatura nova:
  ```python
  def _draw_watermark(pdf: _UltraPDF, text: str, *, rgb: tuple[int, int, int] = _WATERMARK_RGB) -> None:
  ```
- No corpo, linha 270, substituir:
  ```python
  pdf.set_text_color(*_WATERMARK_RGB)
  ```
  por:
  ```python
  pdf.set_text_color(*rgb)
  ```
- Atualizar a docstring para mencionar o parâmetro `rgb` e a cor condicional por página.
- Manter inalterados: cálculo de `x`/`y`, `local_context(fill_opacity=_WATERMARK_ALPHA)`, `pdf.text(x, y, text)`.

### Passo 3 — Atualizar o loop por-página (linhas 635–637)
Arquivo: `src/motor_expansao/dashboard/censo_report.py`

Substituir o loop atual:
```python
for page_number in range(1, pdf.pages_count + 1):
    pdf.page = page_number
    _draw_watermark(pdf, wm_text)
```
por:
```python
for page_number in range(1, pdf.pages_count + 1):
    pdf.page = page_number
    rgb = _WATERMARK_RGB_COVER if page_number == 1 else _WATERMARK_RGB
    _draw_watermark(pdf, wm_text, rgb=rgb)
```
(Página 1 = capa; páginas 2-7 = cinza padrão.)

### Passo 4 — Verificar testes de marca d'água (SEM alterar o arquivo de testes)
Arquivo: `tests/unit/test_relatorio_pontual_censitario_export.py`

Confirmar que os 3 testes `test_pdf_marca_dagua_*` (linhas 206–244) assertam apenas:
- presença de texto `b"Ultra Academia"` / `b"Analista Teste"` no stream de bytes
- `/Count 7` (número de páginas)
- contagem `>= 7` de ocorrências de `b"Ultra Academia"`

Nenhum deles asserta cor (`_WATERMARK_RGB`) nem alpha (`_WATERMARK_ALPHA`), portanto **o arquivo de testes NÃO deve ser alterado**.

Opcional: o Builder pode adicionar um assert barato de constante em teste existente ou novo:
```python
assert _WATERMARK_RGB_COVER == (255, 255, 255)
```
Isso é de baixo risco e documenta a intenção; incluir somente se não quebrar nada.

## Arquivos afetados
- `src/motor_expansao/dashboard/censo_report.py` — único arquivo de produção a tocar
- `tests/unit/test_relatorio_pontual_censitario_export.py` — somente se o Builder optar pelo assert opcional acima; caso contrário, intocado

## Dependências
- Nenhuma dependência nova: `_WATERMARK_RGB_COVER` é constante local; o parâmetro `rgb` com default preserva compatibilidade com qualquer chamada existente.
- A única chamada a `_draw_watermark` está no loop das linhas 635–637 (o Builder verifica se há outras com `grep`).

## Riscos técnicos
- **Baixo.** A assinatura de `_draw_watermark` ganha parâmetro keyword-only com default; qualquer chamada existente sem `rgb=` continua funcionando sem alteração.
- Os 3 testes `test_pdf_marca_dagua_*` não assertam cor nem alpha, portanto seguem verdes sem toque.
- A cor branca `(255,255,255)` na capa não afeta o anti-PII (`set_compression(False)` e `pdf_version="1.4"` não são tocados; texto da marca não muda).
- `ruff`/`mypy`: a anotação `tuple[int, int, int]` é idioma padrão Python 3.9+; verificar se o projeto usa `from __future__ import annotations` ou Python ≥ 3.9 (esperado: sim).

## Critérios de aceite finais
1. `_WATERMARK_ALPHA == 0.65` no código após a mudança.
2. `_WATERMARK_FONT_PT == 10` no código após a mudança.
3. `_WATERMARK_RGB_COVER = (255, 255, 255)` presente junto às demais constantes `_WATERMARK_*`.
4. `_draw_watermark` aceita parâmetro keyword-only `rgb` (default `_WATERMARK_RGB`) e o usa em `set_text_color`.
5. Loop (635–637): página 1 → `rgb=_WATERMARK_RGB_COVER`; páginas 2–7 → `rgb=_WATERMARK_RGB`.
6. Posição (canto inferior-direito), orientação (horizontal) e desenho em todas as 7 páginas: preservados.
7. `_WATERMARK_BASE`, `_WATERMARK_RGB`, `_WATERMARK_ANGLE`, `_WATERMARK_MARGIN`, texto da marca e parâmetro `solicitante`: inalterados.
8. `test_pdf_marca_dagua_com_solicitante`, `test_pdf_marca_dagua_sem_solicitante`, `test_pdf_marca_dagua_em_todas_as_paginas` → 3 passed.
9. Suíte FULL sem regressão (baseline BO: `696 passed, 1 skipped, 3 failed pré-existentes`).
10. `ruff check` e `mypy` limpos em `censo_report.py`.
11. Verificação final por render: orquestrador gera PDF de amostra e confirma visualmente capa (marca branca sobre turquesa) e página de conteúdo (cinza, legível no canto inferior-direito).

## Validações obrigatórias
- Comando (serial, escopo restrito):
  ```
  python -m pytest -q tests/unit/test_relatorio_pontual_censitario_export.py tests/unit/test_relatorio_pontual_censitario_mapa.py
  ```
- Comando (import smoke):
  ```
  python -c "import streamlit_app"
  ```
- Comando (linting):
  ```
  ruff check src/motor_expansao/dashboard/censo_report.py
  mypy src/motor_expansao/dashboard/censo_report.py
  ```
- Critério mínimo: 0 falhas nos testes do escopo; import sem erro; ruff/mypy limpos.

## Fora de escopo
- Qualquer score, artefato ou parâmetro M1 (`score_priorizacao`, `hex_score_estrutural`, carteira, plano, parquets oficiais).
- Texto da marca d'água (`_WATERMARK_BASE`, `_watermark_text`, parâmetro `solicitante`): INALTERADOS.
- Template (7 páginas, ordem, `/Count 7`, `set_compression(False)`, `pdf_version="1.4"`).
- Mapas / raio 1,5 km / método de interseção.
- Dashboard (`pages.py`, `components.py`).
- Posição (canto inferior-direito), orientação (horizontal), `local_context`: PRESERVADOS.
- Paths pré-sujos (`data/outputs/setores_censitarios_2022_geo/_metadata.json`, `data/reports/relatorio_pontual_censitario_base_geo.md`): NÃO commitar.

## Alerta de aprovação
Criticidade MÉDIA — sem gate humano antes do Builder. Nenhum componente M1 é tocado.

## Guardrails ativos
- READ-ONLY sobre o M1: não recalcular score nem alterar artefatos oficiais.
- Guardrail permanente (CLAUDE.md §4): visualizações e o relatório não podem alterar `score_priorizacao`, `hex_score_estrutural`, carteira, plano ou artefatos oficiais do M1 sem aprovação explícita.
- Anti-PII §4: `.pptx`/PDF nunca versionados; cartão de contato `image24.png` nunca embutido. `set_compression(False)` preservado para auditabilidade.
- Paths pré-sujos alheios ao ciclo não devem ser commitados.
- Verificação final por RENDER (orquestrador confirma o efeito visual ao fechar o ciclo — pytest não cobre cor).
