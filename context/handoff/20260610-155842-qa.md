# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (housekeeping Passo 6.0 + commit por path pelo orquestrador)

## VEREDITO
APROVADO

## Justificativa
O gate do SAM foi afrouxado exatamente como aprovado (DEC-007): `flag_sam = faixa_elegivel & flag_pop_ok`, sem `flag_viavel` nem `~canibal`; as variáveis/colunas seguem existindo para os labels; o assert de canibalização saiu de `validar()` e do guardrail de parquet. Todas as 7 validações obrigatórias re-executadas por mim passaram (suíte full 688 passed / 4 skipped, ruff/mypy limpos, import ok), o escopo não tocou o M1 (diff só em `calcular_colunas_mercado.py` no `src/`), e a sanidade READ-ONLY sobre o parquet real reproduz o impacto 27.996 → 479.568.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- `FutureWarning` de pandas (downcast em `.fillna` na linha 395 de `calcular_colunas_mercado.py`) é pré-existente, fora do escopo do ciclo; pode ser endereçada num hardening futuro.

## Testes faltantes
- nenhum. Cobertura adequada: gate de 2 critérios (parametrize), 2 repros de admissão (dropar `flag_viavel`, dropar `~canibal`), reescrita do teste de canibalização e remoção do assert obsoleto do guardrail de parquet.

## Riscos remanescentes
- R4 (operacional, não regressão): os parquets paralelos de staging ainda servem `flag_sam` ANTIGO (27.996) até a regeneração pós-merge na ordem canônica. Conforme plano aprovado, regeneração é passo operacional pós-merge — não é requisito de fechamento de testes. Não toca M1.

## Saída literal das validações (re-executadas pelo QA)

`python -m pytest -q -p no:cacheprovider tests/integration/test_modelo_mercado_hexagonos.py`
```
sss.......................                                               [100%]
...
23 passed, 3 skipped, 11 warnings in 6.50s
```
(3 skips = fixtures de parquet híbrido/score real cobertas por guard de existência — esperado.)

`python -m pytest -q -p no:cacheprovider tests/integration/test_streamlit_app.py`
```
........................................................................ [ 40%]
........................................................................ [ 80%]
...................................                                      [100%]
179 passed, 1 warning in 15.61s
```

`python -m pytest -q -n auto -p no:cacheprovider tests/integration/test_modelo_mercado_hexagonos.py`  (tentativa xdist — erro de AMBIENTE documentado, BLK-ORQ-01)
```
INTERNALERROR> ... execnet/gateway_base.py ...
INTERNALERROR> EOFError: couldn't load message header, expected 9 bytes, got 0
```
(Falha no sessionstart do xdist em Windows + Python 3.14; não é defeito do código. Gate = SERIAL abaixo.)

`python -m pytest -q -p no:cacheprovider`  (SUÍTE COMPLETA — gate único, serial)
```
688 passed, 4 skipped, 19 warnings in 95.99s (0:01:35)
EXIT: 0
```

`python -c "import streamlit_app; print('import ok')"`
```
import ok
```

`python -m ruff check src/ tests/`
```
All checks passed!
```

`python -m mypy src/motor_expansao/pipelines/calcular_colunas_mercado.py`
```
Success: no issues found in 1 source file
```

`git diff --name-only HEAD` + scope check (paths proibidos)
```
CLAUDE.md
context/handoff.md
data/outputs/setores_censitarios_2022_geo/_metadata.json   <- NÃO é do ciclo (nota de escopo)
data/reports/relatorio_pontual_censitario_base_geo.md       <- NÃO é do ciclo (nota de escopo)
docs/modelo_mercado_hexagonos.md
src/motor_expansao/pipelines/calcular_colunas_mercado.py
tasks/backlog.md
tasks/current_task.md
tests/integration/test_modelo_mercado_hexagonos.py

grep por pipelines/m1/|scoring.py|config.py|dashboard/constants.py|pop_corte.py|.parquet:
=> VAZIO (nenhum path proibido)
git diff --stat HEAD -- src/  =>  só calcular_colunas_mercado.py (4 insertions, 10 deletions)
```

`python scripts/housekeeping_move_block.py BLK-SAM-02 --check`  (Passo 6.0 — executado pelo orquestrador APÓS o veredito; esperado FALHAR agora)
```
FALHA --check: stub ausente no backlog para BLK-SAM-02
```
(Esperado: o bloco ainda está no backlog; o move é feito pelo orquestrador no fechamento.)

`python -m pytest -q -p no:cacheprovider tests/unit/test_housekeeping_helper.py`
```
10 passed, 1 warning in 0.04s
```

Sanidade READ-ONLY do novo gate sobre o parquet real (sem reescrever o parquet):
```
Gate ANTIGO (DEC-006) recomputado: 27996
Gate NOVO  (DEC-007) recomputado: 479568
delta: 451572
```
(Bate exatamente com a evidência da DEC-007 / backlog: 27.996 → 479.568, +451.572.)

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico. O caminho crítico `calcular()` é exercitado de verdade em todos os testes de gate (DataFrames in-memory são fixtures de teste legítimas — regra (b)). A suíte full rodou contra o repositório real; a sanidade do volume foi computada sobre o `data/staging/hexagonos_mercado_mapeado.parquet` REAL (que já contém `flag_pop_min_5k`/`populacao_corte_hex` da DEC-006), reproduzindo a mesma lógica do gate sem reescrever o artefato. A regeneração de parquets NÃO foi exigida (passo operacional pós-merge, conforme plano aprovado). xdist FALHOU por ambiente (execnet EOFError, documentado em BLK-ORQ-01); fallback SERIAL é o gate equivalente.

## Conferência cruzada com log do Builder
Bate. O Builder reportou 23 passed/3 skipped no arquivo de mercado, 179 passed no streamlit, ruff/mypy limpos e import ok — reproduzido idêntico aqui. A suíte FULL (não rodada pelo Builder, por design) deu 688 passed / 4 skipped sob meu gate único. Os números do impacto (27.996 → 479.568) batem com o backlog e a DEC-007. A nota de escopo dos 2 arquivos dirty não-relacionados (`_metadata.json`, `relatorio_pontual_censitario_base_geo.md`) confere — efeito colateral de materialização anterior, fora do commit do ciclo (o orquestrador commita por path).

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; nenhuma escrita em score/pesos; `renda=0.40`/`pop=0.60`, H3=7, DIST=1.0, RENDA_MIN=4500 inalterados — `config.py`/`scoring.py`/`pipelines/m1/` ausentes do diff).
- Artefatos M1 preservados: sim (nenhum `.parquet` no diff; `git diff --stat src/` = só `calcular_colunas_mercado.py`).
- `pop_corte.py` intocado: sim (ausente do diff; só consumido via `derive_confianca_geografica`/`derive_pop_cut_columns`).
- DEC-006 NÃO editada: sim (diff do CLAUDE.md só ADICIONA linhas da DEC-007; nenhuma linha `-` na seção DEC-006).
- DEC-007 registrada após DEC-006: sim (linha 233, data 2026-06-10, aprovador Vinicius, reverte 2 sub-decisões, DEC-006 SUPERSEDED nessas 2, impacto 27.996→479.568).
- Gate = `faixa_elegivel & flag_pop_ok` (linha 298): sim, sem `flag_viavel` e sem `~canibal`. Variáveis `flag_viavel`/`flag_canibal` mantidas (linhas 284-285) e usadas em `sam_granularidade`/`tese_entrada`. `flag_sam_fitness`/`sam_fitness_potencial` inalterados.
- Assert de canibalização removido: sim, de `validar()` (produção) e do `test_parquet_final_respeita_guardrails_do_piloto`.
- Validações re-executadas pelo QA (sem bypass): mercado tests → 23p/3s; streamlit tests → 179p; xdist → erro de ambiente; suíte full → 688p/4s; import → ok; ruff → clean; mypy → clean; housekeeping helper → 10p; sanidade volume → 27.996→479.568.
- Escopo respeitado: sim (só `calcular_colunas_mercado.py` em `src/`; demais alterações em testes/docs/CLAUDE.md/tasks/handoff, todos do ciclo).

## Decisão recomendada
Fechar ciclo. Orquestrador executa o housekeeping (`scripts/housekeeping_move_block.py BLK-SAM-02`) e commita por path (excluindo os 2 arquivos dirty fora de escopo). Regeneração dos parquets paralelos é passo operacional pós-merge na ordem canônica.
