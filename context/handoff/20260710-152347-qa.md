# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (housekeeping do BLK-PERF-01b via `scripts/housekeeping_move_block.py` + commit por
path + PR). Pendência PÓS-QA não bloqueante: validação VISUAL humana por Felipe.

## VEREDITO
APROVADO

## Justificativa
Bloco 100% display/cache/interação, READ-ONLY sobre o M1: diff restrito aos paths do ciclo
(components.py/pages.py/tests/), zero mudança em pipelines/m1, config.py, constants.py; suíte FULL
verde (1571 passed, 1 skipped), ruff+mypy limpos, loop_guard OK; a chave de cache captura todos os
parâmetros que afetam o deck (impossível false-hit entre UFs) e o append da layer multi-hex ocorre
sobre a cópia-por-chamada do cache_data (sem vazamento), coberto por testes que exercitam o caminho
real (spy delega ao dispatcher real; dados do deck inspecionados diretamente).

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- `_map_frame_token(df, ["hex_id"])` usa apenas o conjunto de `hex_id` como fingerprint do recorte.
  É correto e seguro no runtime atual (parquets `:ro`, score estático por hex dentro do processo, como
  o Planner documentou), mas se algum dia o `df` do mapa passar a carregar valores mutáveis por hex
  sem mudar o conjunto de `hex_id`, o token precisaria incluir as colunas de score. Não é defeito hoje;
  registrar como premissa.

## Testes faltantes
- nenhum — cobertura adequada: invalidação por parâmetro (UF/cidade/modo/overlay/busca/rede),
  HIT ao revisitar modo, HIT em multihex + cópia segura, anti-vazamento A→B→A, D1 (rerun scope=app +
  caso sem clique), D2 (ausência de `ttl=`), picklability guard, token none/vazio/sensível ao conteúdo,
  e verificação de que ambos são `@st.fragment`.

## Riscos remanescentes
- **Interação de reruns entre fragments aninhados/irmãos** (mapa + painel multi-hex + pydeck interno)
  validada só headless (corpos via `__wrapped__`). Comportamento visual real (clique no mapa, troca de
  modo, add/remove hex, botão "Atualizar mapa", destaque laranja) depende da validação visual PÓS-QA.
- **HIT ~0,14s (não 0)** por cópia-por-chamada do `cache_data` (unpickle do deck) — inerente e
  desejado (isolamento de mutação do append multi-hex); ganho real ~14× sobre o rebuild de ~2s.
- **Memória sem TTL:** `max_entries=24` limita o teto (~360 MB); tunável se a medição de produção pedir.
  Não viola D2 (D2 proíbe TTL, não tamanho máximo).

## PENDÊNCIA ABERTA PÓS-QA (registrada, não bloqueante do gate)
- **Validação VISUAL humana por Felipe** — cache + fragment do mapa + fragment do painel multi-hex +
  destaque laranja + botão "Atualizar mapa" no app real. Fora da esteira automatizada.

## Saída literal das validações (re-executadas pelo QA)

### Suíte FULL (gate único) — `python -m pytest -q -n auto`
```
1571 passed, 1 skipped, 494971 warnings in 690.55s (0:11:30)
```
(exit code 0. Warnings são pré-existentes — CRS geográfico, downcasting fillna, read_json literal —
nenhum introduzido pelo ciclo.)

### ruff — `ruff check components.py pages.py test_streamlit_app.py`
```
All checks passed!
```

### mypy — `mypy src/motor_expansao/dashboard/`
```
Success: no issues found in 14 source files
```

### Smoke import — `python -c "import streamlit_app"`
```
(warnings esperados "No runtime found, using MemoryCacheStorageManager" do @st.cache_data em bare mode)
import ok
```

### loop_guard — `python scripts/loop_guard.py --base HEAD`
```
GUARD OK: 0 caminho(s) alterado(s), nenhum proibido.
```

### git diff guardrail (deve ser vazio) — `git diff --stat HEAD -- pipelines/m1/ config.py constants.py`
```
--- fim (vazio acima = OK) ---
```
(diff VAZIO em `src/motor_expansao/pipelines/m1/`, `src/motor_expansao/config.py` e
`src/motor_expansao/dashboard/constants.py`.)

### grep D2 — `grep -n "ttl=" src/motor_expansao/dashboard/components.py`
```
grep_exit=1   (nenhuma ocorrência de ttl= em components.py)
```
Decorator confirmado: `@st.cache_data(show_spinner=False, max_entries=MAP_FIGURE_CACHE_MAX_ENTRIES)  # sem expiracao por tempo (D2)`

### git status --porcelain (escopo)
```
 M src/motor_expansao/dashboard/components.py
 M src/motor_expansao/dashboard/pages.py
 M tests/integration/test_streamlit_app.py
?? context/handoff/20260710-143736-block-orchestrator.md
?? context/handoff/20260710-150412-planner.md
?? context/handoff/20260710-150907-builder.md
?? scripts/_scratch_atr_alvo_alunos_totais.py
?? scripts/backtest_smartfit_scores.py
```
(Mudanças restritas aos 3 paths do ciclo. Untracked não relacionados `scripts/_scratch*` e
`scripts/backtest_smartfit*` NÃO tocados. Nota: o `M src/motor_expansao/dimensionamento/config.py`
citado no snapshot inicial do ambiente NÃO aparece no working tree atual — não é do ciclo e não está
sujo.)

### Housekeeping (pré-fechamento) — `python scripts/housekeeping_move_block.py BLK-PERF-01b --check`
```
FALHA --check: stub ausente no backlog para BLK-PERF-01b
```
Esperado FALHAR neste momento (bloco ainda no backlog; o move é passo do fechamento, ainda não
executado). Correto. Registrado como pendência do fechamento, não do QA.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do
caminho crítico. A suíte FULL rodou 1× aqui (gate único) com `-n auto` → 1571 passed, 1 skipped. Os
testes de cache exercitam o CAMINHO REAL: `_install_map_cache_spy` instala um spy que INCREMENTA o
contador e DELEGA ao dispatcher real (`build_unified_map_figure`), e o wrapper `@st.cache_data` real é
exercitado com construção de deck real; a inspeção é feita sobre `deck.layers[0].data` (dados reais do
deck), não sobre um retorno mockado. As fixtures sintéticas de recorte (`_perf_df_sp`/`_perf_df_rj`)
são isolamento de dados legítimo (prática normal, não bypass — não há tooling de produção contornado).

## Conferência cruzada com log do Builder
Bate. Builder declarou subset 247 passed / ruff+mypy limpos / import ok / loop_guard OK / grep ttl=
vazio / diff vazio nos paths M1 — todos reproduzidos por conta própria. A suíte FULL (não rodada pelo
Builder, por desenho) confirmou verde aqui: 1571 passed, 1 skipped. Números B3/B4 e MISS/HIT do
Builder são medição de performance (não gate); coerentes com o plano (HIT ~0,14s por cópia do
cache_data, ~14× sobre o rebuild).

## Guardrails verificados
- score_priorizacao não alterado: sim (diff VAZIO em pipelines/m1 e config.py; builders puros e
  dispatcher `build_unified_map_figure` INALTERADOS — o cache é um wrapper novo que delega).
- Artefatos M1 preservados: sim (loop_guard OK; nenhum artefato oficial tocado).
- Parâmetros canônicos preservados: sim (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0,
  pesos renda=0.40/pop=0.60 vivem em config.py — diff VAZIO).
- `MAP_POINT_LIMIT*` e `_downsample_map_index`: INTOCADOS (constants.py sem diff; caps não referenciados
  no diff além do fluxo existente).
- D1 verificado: botão "Atualizar mapa" (`key="btn_multihex_refresh_map"`) → `st.rerun(scope="app")`,
  testado (com clique → rerun; sem clique → sem rerun, KPIs via `st.metric`) + caption acentuado.
- D2 verificado: decorator sem `ttl=`, `max_entries=24` (bound de memória, não TTL).
- Chave de cache: captura df_token/competitors_token/ultra_token/dominio_token + color_mode +
  enabled_overlays (tupla ORDENADA) + ufs/cities/faixas + search_pin/search_hex_id; `multihex_cenario`
  FORA da chave. Append da layer de destaque ocorre APÓS o cache, sobre a cópia-por-chamada do
  cache_data (verificado no diff pages.py:4402-4404 e no teste de cópia segura).
- Acentuação: strings novas de UI acentuadas; identificadores/keys sem acento. OK.
- Escopo respeitado: sim (só components.py/pages.py/tests/; nenhum path fora da lista do ciclo).

## Decisão recomendada
Fechar ciclo. Executar o housekeeping do fechamento (`scripts/housekeeping_move_block.py BLK-PERF-01b`
+ suíte com o teste do helper), commit por path nos 3 paths do ciclo + snapshots, e abrir PR. Manter a
validação visual humana de Felipe como item aberto até ele confirmar no app real.
