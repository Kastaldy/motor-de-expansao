# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (ciclo pronto para merge humano)

## VEREDITO
APROVADO

## Justificativa
A causa do flaky foi PROVADA por conta própria (timestamp `/CreationDate` + `/ID` do fpdf2 atravessando a virada de segundo, NÃO estado global) e a correção é determinismo legítimo em `tests/` (congela só o relógio do fpdf2, input irrelevante ao que o teste valida) — não é máscara, skip, xfail, no:xdist nem reordenação. Suíte full serial verde e reproduzível (964 passed, 1 skipped, 0 failed em 2 execuções independentes); produção (`src/`) byte-intocada; housekeeping byte-idêntico.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- A fixture `_freeze_fpdf_clock` é autouse global. Hoje nenhum teste depende do `/CreationDate` refletir o relógio real (grep em `tests/` só encontra a própria conftest), então não há colateral. Se algum dia um teste precisar validar timestamp real de PDF, ele deve sobrescrever/optar-out da fixture — observação para o futuro, não bloqueia.

## Testes faltantes
- nenhum. O próprio teste-alvo (`test_classico_template_recente_inalterado`) é o teste de isolamento; a fixture o torna determinístico sem perder o que ele valida (gerar o template clássico no meio não muta estado compartilhado que afete os bytes do recente).

## Riscos remanescentes
- nenhum material. A fixture faz 2 `monkeypatch.setattr` por teste (auto-revertidos no teardown), custo desprezível; patcha apenas o símbolo `datetime` DENTRO de `fpdf.fpdf`/`fpdf.output`, não o `datetime` global.

## Saída literal das validações (re-executadas pelo QA)

### 1a. Prova da CAUSA — diff de bytes é o timestamp (SEM a fixture, cruzando 1 segundo)
```
identical without sleep-cross? False
num diff bytes: 61
 idx 648 b'0260617144602Z)\n>>\ne' -> b'0260617144604Z)\n>>\ne'
 idx 880 b'7 0 R\n/ID [<4F6FB10A' -> b'7 0 R\n/ID [<1E35445B'
 ... (restante do bloco /ID)
CreationDate present: [b'/CreationDate (D:20260617144602Z)']
```
Único delta entre 2 PDFs de conteúdo idêntico = dígito de segundos do `/CreationDate` (4602 -> 4604) + hash `/ID` derivado. Não há estado global envolvido.

### 1b. Prova de que a FIXTURE elimina o diff (mesmo símbolo que o conftest patcha)
```
fpdf.fpdf has datetime: True
fpdf.output has datetime: True
identical WITH frozen clock + 1.2s sleep: True
CreationDate frozen: [b'/CreationDate (D:20260101000000Z)']
```

### 1c. Repro pelo caminho REAL de produção (gerar_pdf_relatorio_pontual_censitario), forçando virada de segundo
```
=== WITHOUT fixture (--noconftest) -> expect FAIL ===
FAILED tests/unit/test_repro_blkfix14_scratch.py::test_repro_forca_virada_de_segundo
1 failed, 45 warnings in 3.10s
=== WITH conftest fixture -> expect PASS ===
1 passed, 51 warnings in 3.01s
```
(arquivo de scratch criado para esta prova e REMOVIDO em seguida; não entra na suíte.)

### 2. Teste-alvo isolado
```
$ python -m pytest -q tests/unit/test_relatorio_pontual_censitario_export.py::test_classico_template_recente_inalterado
1 passed, 1101 warnings in 1.08s
```

### 3. Arquivo export inteiro
```
$ python -m pytest -q tests/unit/test_relatorio_pontual_censitario_export.py
22 passed, 1101 warnings in 12.33s
```

### 4. Suíte FULL serial — gate único — RUN 1
```
$ python -m pytest -q --cache-clear tests/
964 passed, 1 skipped, 113221 warnings in 173.09s (0:02:53)
```
Nota -n auto: usei SERIAL conforme instrução explícita do orquestrador (risco documentado de INTERNALERROR execnet no Python 3.14 deste ambiente); Builder também rodou serial. Contagem idêntica à do Builder (964/1).

### 4b. Suíte FULL serial — RUN 2 (reprodutibilidade do isolamento)
```
$ python -m pytest -q --cache-clear tests/
964 passed, 1 skipped, 113221 warnings in 173.22s (0:02:53)
```
Duas execuções full independentes -> mesmo verde. O teste deixou de ser flaky.

### 5. import streamlit_app
```
$ python -c "import streamlit_app; print('import ok')"
import ok
```
(precedido só pelos warnings esperados de "missing ScriptRunContext"/"No runtime found" do Streamlit em modo bare.)

### 6. ruff + mypy
```
$ python -m ruff check tests/conftest.py
All checks passed!

$ python -m mypy tests/conftest.py
Success: no issues found in 1 source file
```

### 7. git diff/status — escopo
```
$ git diff -- src/        -> (vazio)
$ git diff --stat -- src/motor_expansao/dashboard/competitors.py src/motor_expansao/dashboard/censo_report.py src/motor_expansao/config.py
(vazio = intocados)

$ git status --porcelain (estado final pós-housekeeping)
 M context/handoff.md
 M data/outputs/setores_censitarios_2022_geo/_metadata.json   <- pré-sujo (NÃO commitar)
 M data/reports/relatorio_pontual_censitario_base_geo.md      <- pré-sujo (NÃO commitar)
 M tasks/backlog.md          <- housekeeping (stub)
 M tasks/completed.md        <- housekeeping (bloco movido)
 M tasks/current_task.md     <- housekeeping
?? context/handoff/20260617-111443-block-orchestrator.md
?? context/handoff/20260617-112801-planner.md
?? context/handoff/20260617-114247-builder.md
?? tests/conftest.py         <- A CORREÇÃO (novo)
```

### 8. Housekeeping (BLK-FIX-14 é bloco completo)
```
$ python scripts/housekeeping_move_block.py BLK-FIX-14 --date 2026-06-17
OK: movido BLK-FIX-14 (2297 bytes) -> tasks/completed.md; stub em tasks/backlog.md.   (EXIT=0)

$ python scripts/housekeeping_move_block.py BLK-FIX-14 --check
OK: BLK-FIX-14 movido (stub no backlog + bloco em completed.md).                       (EXIT=0)

$ python -m pytest -q tests/unit/test_housekeeping_helper.py
10 passed, 501 warnings in 0.39s
```
Byte-identidade (boundary EXATO do helper, vs `git show HEAD:tasks/backlog.md`):
```
orig_block bytes (utf-8): 2321
completed_block bytes (utf-8): 2321
BYTE-IDENTICAL: True
```
(Um check meu inicial com regex ingênua `\n### BLK` deu falso-negativo porque o conteúdo após BLK-FIX-14 era `## Novos blocos`/`## Projeto API`, não outro `### BLK`; ao replicar o boundary real do helper `^#{2,3} `, a identidade byte-a-byte se confirma. Zero perda.)

## Conferência de no-bypass
Confirmo que NENHUMA validação usou `--config /dev/null`, fixture fora do caminho real, nem mock do caminho crítico. A regra de bypass (escopo: tooling que depende de config/artefatos de produção) NÃO se aplica a este ciclo — é correção de teste com fixture de teste legítima. NO-BYPASS de flakiness: NÃO há `-p no:xdist`, `skip`, `xfail`, desabilitação nem reordenação; a suíte full rodou de verdade (serial, autorizado) e a causa foi atacada na raiz (input não-determinístico de relógio removido só nos testes). A prova da causa foi feita pelo caminho REAL de produção (`gerar_pdf_relatorio_pontual_censitario`), não por mock.

## Conferência cruzada com log do Builder
BATE integralmente.
- Causa real = timestamp `/CreationDate`+`/ID` do fpdf2 (não estado global `_ICON_CACHE`/`_ATLAS_CACHE`): confirmado por reprodução própria.
- Fix = `tests/conftest.py` novo com `_freeze_fpdf_clock` autouse scope=function patchando `fpdf.fpdf.datetime` e `fpdf.output.datetime`: confirmado por leitura e por prova de que esses dois símbolos existem e que congelá-los zera o diff.
- Contagens: target 1 passed, export 22 passed, full 964 passed/1 skipped, ruff/mypy limpos, import ok — TODAS reproduzidas idênticas.
- O `--cache-clear` incompatível com `-p no:cacheprovider` (nota do Builder) não afeta o veredito; rodei full com `--cache-clear` sem o segundo flag, verde.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY M1; `config.py`/`pipelines/m1`/scoring/artefatos intocados — `git diff -- src/` vazio)
- Artefatos M1 preservados: sim
- Parâmetros canônicos (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, renda=0.40/pop=0.60): intactos (config.py não tocado)
- Validações re-executadas pelo QA (sem bypass):
  - prova causa (diff timestamp) -> 61 bytes, só CreationDate+ID
  - prova fixture (frozen clock) -> diff = 0
  - repro caminho real (--noconftest FAIL / conftest PASS) -> conforme
  - target isolado -> 1 passed
  - export file -> 22 passed
  - full serial RUN 1 -> 964 passed, 1 skipped
  - full serial RUN 2 -> 964 passed, 1 skipped
  - import streamlit_app -> ok
  - ruff -> All checks passed
  - mypy -> Success
  - housekeeping move/check -> OK/OK; helper test 10 passed; byte-identidade True
- Escopo respeitado: sim — única alteração de código é `tests/conftest.py`; nada em `src/`; pré-sujos não commitados.

## Decisão recomendada
Fechar ciclo. Para o merge humano: commitar `tests/conftest.py`, `tasks/backlog.md`, `tasks/completed.md`, `tasks/current_task.md`, `context/handoff.md` e os 3 snapshots de `context/handoff/`; NÃO commitar os 2 paths pré-sujos (`data/outputs/.../_metadata.json`, `data/reports/relatorio_pontual_censitario_base_geo.md`).
