# Handoff — BLK-DIM-16 QA → (fechamento)

## Bloco
- **ID:** BLK-DIM-16 — fix break-even (margem 0%) + bound do aluguel-teto
- **Branch:** `ciclo/loop-20260617-114955`
- **Skill atual:** QA → **próxima: housekeeping/fechamento**
- **Status:** **APROVADO**

## Veredito
**APROVADO** — todos os critérios de aceite satisfeitos; suite full verde; lint/types limpos; READ-ONLY sobre M1 confirmado; `pages.py` NÃO tocado por este bloco.

## Arquivos modificados (BLK-DIM-16, working tree vs HEAD)
1. `src/motor_expansao/dimensionamento/simulador.py` — `aluguel_teto()`: bound `alug_sup` agora usa receita TOTAL (balcão + agregadores + personal) via kwargs com fallback nas constantes `SIM_*`, em vez de `alunos*ticket*2`
2. `src/motor_expansao/dimensionamento/viabilidade_ponto.py` — campo `alunos_para_margem_alvo`; break-even calculado com `margem_alvo=0.0` (definição canônica EBITDA=0%) + segunda chamada informativa para a margem-alvo
3. `tests/unit/dimensionamento/test_viabilidade_ponto.py` — 5 novos testes (19 total)

## Evidências de QA
- `pytest -q` (suite FULL): **987 passed, 4 skipped, 18 warnings, 0 failed** (313s). Warnings pré-existentes (CRS geográfico / downcasting pandas nos pipelines M1), não introduzidos por este bloco.
- `pytest tests/unit/dimensionamento/test_viabilidade_ponto.py`: **19 passed**.
- `ruff check .`: **All checks passed!**
- `mypy viabilidade_ponto.py simulador.py`: **Success: no issues found in 2 source files**.
- Critérios de aceite (todos VERDES):
  - `test_breakeven_menor_que_alunos_para_margem_alvo` ✓ (break-even < alunos p/ 10%)
  - `test_breakeven_resulta_ebitda_zero` ✓ (|EBITDA| < 5% no break-even)
  - `test_aluguel_teto_considera_agregadores_materiais` ✓ (teto > bound antigo 2×balcão)
  - `test_aluguel_teto_sem_agregadores_nao_regride` ✓
  - `test_alunos_para_margem_alvo_campo_presente` ✓

## Guardrails verificados
- READ-ONLY M1: `git diff -w main -- config.py / pipelines/m1/` → **só EOL/whitespace** (artefato da branch, ZERO conteúdo). `git diff -w HEAD` confirma que os únicos arquivos com mudança REAL de conteúdo deste bloco são os 3 declarados + 2 deleções `data/raw/ibge/*.geojson` pré-existentes na branch (não dimensionamento, não introduzidas aqui).
- `pages.py`: NÃO tocado por BLK-DIM-16 (mudanças de conteúdo de `pages.py` vs main pertencem a bloco anterior já commitado — split-da-premissa BLK-DIM-13/14).
- Sem VPS/deploy/segredos/PII. DEC-001/008/009 intactas; raio 1.5 km e `setor_censitario_intersecao_area_1p5km` INTOCADOS.

## Housekeeping
- `python scripts/housekeeping_move_block.py BLK-DIM-16 --check` → **FALHA (exit 1)**: "stub ausente no backlog para BLK-DIM-16" — esperado; housekeeping (mover bloco backlog→completed) é passo de fechamento pendente, fora do escopo de QA.

## Próximo passo
Housekeeping/fechamento: criar stub no backlog, mover BLK-DIM-16 para `completed.md`, commit por path. Loop NÃO faz merge/push/deploy — revisão e merge humanos.
