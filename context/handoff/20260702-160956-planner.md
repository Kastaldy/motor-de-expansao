# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
**REVISÃO HUMANA (gate)** — Alta com gate humano OBRIGATÓRIO (anti-PII / anti-reidentificação + vocabulário/matching de rede). Depois: Builder → QA (Opus 4.8).

## Entendimento da tarefa
Estender a ingestão anti-PII das academias menores (TP-08) para classificar cada academia numa CATEGORIA de rede (`rede_menor`) na FRONTEIRA (antes do drop do `Nome_Academia`), produzindo dois insumos gitignored/NÃO oficiais — (a) oferta por `hex_id × rede_menor` e (b) capacidade média/mediana de alunos por rede — que fecham as 2 lacunas de dado do BLK-TP-06-FU1, tudo READ-ONLY sobre o M1.

## Plano técnico

### 0. Preparação (sem código de produção)
0.1. Registrar o bloco AD-HOC **BLK-TP-08-FU** em `tasks/backlog.md` (Autonomia: NÃO loop-safe — envolve gate humano anti-PII; Depende de: BLK-TP-08 concluído).

### 1. Novo módulo irmão `classificacao_rede_menor.py` (funções PURAS, sem I/O de produção no core)
Criar `src/motor_expansao/demanda_revelada/classificacao_rede_menor.py` (módulo IRMÃO, não editar a lógica de `oferta_academias_menores.py` — só reusa/estende), isolado (sem import de `pipelines/m1`, `dashboard`, `censo_*`, `api`).

1.1. **Vocabulário curado** — constante `VOCABULARIO_REDES: dict[str, tuple[str, ...]]` que mapeia cada `rede_menor` (categoria canônica = valor da coluna `rede` de `concorrentes_mapeados`) para uma tupla de TOKENS de matching curados. As 28 redes de `concorrentes_mapeados` são a base; o mapeamento por rede deve usar tokens compostos/de alta especificidade e EXCLUIR/curar tokens curtos ambíguos:
   - Redes com token específico seguro (matching por token com word-boundary): `smart fit`/`smartfit` → `smart_fit`; `panobianco`; `bluefit`/`blue fit` → `bluefit`; `selfit`; `pratique`; `bodytech`/`body tech` → `bodytech`; `phd sports`/`phd` (curar) → `phd_sports`; `velocity`; `allp fit`/`allp` → `allp_fit`; `26fit`/`26 fit` → `26fit`; `engenharia do corpo` → `engenharia_do_corpo`; `vidya`; `contorno do corpo` → `contorno_do_corpo`; `evoque`; `tonus`/`tonus gym` → `tonus_gym`; `bio ritmo`/`bioritmo` → `bio_ritmo`; `aera` (pilates) → `aera_pilates`; `alpha fitness`/`alpha fit` → `alpha_fitness`; `greenlife`/`green life` → `greenlife`; `xprime`/`x prime` → `xprime`; `kore`; `cia athletica`/`cia atletica` → `cia_athletica`; `world gym` → `world_gym`; `red fitness` → `red_fitness`.
   - **Tokens ambíguos a CURAR (precisão > recall):** `live` (casa "vida"/"olive"/"delivery"), `race` (casa "grace"/"terrace"), `jab` (casa substrings genéricas), `gavioes` (nome próprio comum de torcida/bairro), `phd`, `kore`, `evoque`. Regra: só casar por **token whole-word com boundary** (não substring); tokens de comprimento < 4 caracteres OU sabidamente ambíguos exigem ao menos um segundo token de contexto OU são REMOVIDOS do vocabulário (viram `independente`) — a lista final de quais entram é **DECISÃO A do gate**.
1.2. **Normalização** — helper puro `_normalizar_nome(nome: str) -> str`: `lower()`, remoção de acentos (`unicodedata.normalize("NFKD")` + strip de combining marks — stdlib, sem dep nova), remoção de pontuação (regex `re`), colapso de espaços. Retorna string normalizada tokenizável.
1.3. **Matching por token com word-boundary** — helper puro `classificar_rede(nome: str) -> str`: normaliza; para cada rede na ordem determinística de `VOCABULARIO_REDES` (dict é ordem-estável → precedência **first-match** documentada, redes mais específicas primeiro), testa cada token com regex `\b<token>\b` (word-boundary; tokens multi-palavra viram `\btoken1\s+token2\b`); primeiro match vence; sem match → `"independente"`. Nunca retorna/loga o nome cru.
1.4. **Constantes de contrato** — no mesmo módulo:
   - `VERSAO_CONTRATO_REDE_MENOR = "oferta_menores_rede_v1"` (Saída a) e `VERSAO_CONTRATO_CAP_REDE = "capacidade_media_rede_v1"` (Saída b).
   - `CATEGORIA_INDEPENDENTE = "independente"`.
   - `N_MINIMO_ANTI_REIDENTIFICACAO` (limiar de colapso — **DECISÃO B do gate**, recomendação abaixo).
   - `N_MINIMO_CONFIABILIDADE` (flag de capacidade confiável — **DECISÃO D do gate**, recomendação abaixo).

### 2. Anti-PII / anti-reidentificação na fronteira (CRÍTICO)
2.1. **Colapso de baixa cardinalidade**: helper puro `_colapsar_baixa_cardinalidade(rede_por_academia: pd.Series, n_min: int) -> pd.Series` — conta filiais classificadas por `rede_menor` (no dump inteiro), e qualquer rede com `N_filiais < N_MINIMO_ANTI_REIDENTIFICACAO` é reescrita para `"independente"` ANTES de qualquer agregação/persistência. Justificativa: uma categoria de baixíssima cardinalidade (1-2 filiais) + hex pode aproximar a identidade de um estabelecimento — o colapso quebra a reidentificação. `rede_menor` é sempre CATEGORIA, nunca o nome.
2.2. **Ordem de operações anti-PII (fronteira)**: no leitor (passo 3): (i) ler xlsx; (ii) derivar `hex_id` das coords; (iii) `rede_menor = classificar_rede(Nome_Academia)` — o nome é lido SÓ em memória aqui; (iv) DROPAR imediatamente `Nome_Academia`/`Latitude`/`Longitude`/`Cluster_ID` (reusa `_COLUNAS_DROP_FRONTEIRA` do módulo TP-08); (v) aplicar `_colapsar_baixa_cardinalidade`. O nome nunca é persistido/logado; `Total_Alunos_Cluster` nunca é somado (blindado, como no TP-08).
2.3. **Blindagem automatizada**: estender `_COLUNAS_PII_LOCAIS` (reusar a de `oferta_academias_menores.py`) para os dois novos artefatos via um `_assert_sem_pii_rede`. `rede_menor` NÃO é PII (é categoria), mas o `Nome_Academia`/coords não podem aparecer em NENHUM dos 2 parquets nem no relatório/log/teste.

### 3. Leitor de fronteira com rede (novo, no módulo novo)
3.1. `_ler_e_derivar_hex_rede(fonte: Path, *, n_min_anti_reid: int) -> pd.DataFrame` — variante do `_ler_e_derivar_hex` do TP-08 que ADICIONA a coluna `rede_menor` (passo 2.2) e mantém `Plano`/`Alunos_Academia`. Retorna frame SEM PII com colunas `hex_id`, `rede_menor`, `Alunos_Academia` (Plano opcional — não é exigido nas 2 saídas deste FU).

### 4. Saída (a) — oferta por hex × rede (formato LONGO)
4.1. Contrato `CONTRATO_COLUNAS_OFERTA_MENORES_REDE: dict[str, str]`:
   - `hex_id`: `"string"` (H3 res-7)
   - `rede_menor`: `"string"` (categoria; `"independente"` para não-classificáveis/colapsadas)
   - `n_academias_menores`: `"int64"` (nº de academias no par hex×rede)
   - `alunos_academias_menores`: `"int64"` (Σ `Alunos_Academia`; NUNCA `Total_Alunos_Cluster`)
   - `versao_contrato`: `"string"` (= `oferta_menores_rede_v1`)
4.2. `_agregar_h3_rede(df) -> pd.DataFrame`: `groupby(["hex_id","rede_menor"])` → `size()` e `sum(Alunos_Academia)`; coerção de dtypes; `sort_values(["hex_id","rede_menor"])`; `reset_index`. Formato LONGO (robusto a novas redes — **DECISÃO C do gate**, recomendado).
4.3. `ingerir_oferta_menores_por_rede(fonte=FONTE_DEFAULT, destino=DESTINO_REDE_DEFAULT, *, n_min_anti_reid=N_MINIMO_ANTI_REIDENTIFICACAO, escrever=True) -> pd.DataFrame` — orquestra 3→4; valida `_assert_sem_pii_rede` + `_assert_res7` (reusa do TP-08 ou reimplementa localmente para não importar privado cross-módulo); grava `data/staging/oferta_academias_menores_rede_h3.parquet` (gitignored).

### 5. Saída (b) — capacidade média/mediana por rede
5.1. Contrato `CONTRATO_COLUNAS_CAP_REDE: dict[str, str]`:
   - `rede_menor`: `"string"`
   - `media_alunos`: `"float64"`
   - `mediana_alunos`: `"float64"`
   - `n_filiais`: `"int64"`
   - `flag_confiavel`: `"bool"` (`n_filiais >= N_MINIMO_CONFIABILIDADE`)
   - `versao_contrato`: `"string"` (= `capacidade_media_rede_v1`)
5.2. `capacidade_media_por_rede(df_fronteira) -> pd.DataFrame`: agrega `Alunos_Academia` por `rede_menor` (média, mediana, count); marca `flag_confiavel`; EXCLUI a linha `"independente"` da tabela de capacidade (a média de "independente" mistura redes heterogêneas — não é capacidade de rede; registrar decisão). Ordena por `n_filiais` desc.
5.3. `gerar_capacidade_media_por_rede(fonte, destino=DESTINO_CAP_DEFAULT, escrever=True) -> pd.DataFrame` — grava `data/staging/capacidade_media_por_rede.parquet` (gitignored). **Valor recomendado ao FU1 = MEDIANA** (robusta a outliers); a média fica na tabela como referência.

### 6. Relatório de qualidade
6.1. `gerar_relatorio_classificacao(df_fronteira, df_oferta_rede, df_cap, concorrentes_path=CONCORRENTES_DEFAULT, destino_md=RELATORIO_REDE_DEFAULT, escrever=True) -> str` → `data/reports/scratch/rede_menor_classificacao_qualidade.md` (gitignored). Só agregados/contagens, ZERO PII. Conteúdo:
   - Cobertura da classificação: % `independente` vs % conhecida; nº total de academias.
   - Tabela de **N por rede** (só redes conhecidas após colapso) + `flag_confiavel`.
   - Quantas redes têm capacidade confiável (N≥`N_MINIMO_CONFIABILIDADE`).
   - **Dedup FINO vs `concorrentes_mapeados`**: para cada `(hex_id, rede_menor)` da Saída (a), medir sobreposição contra os pares `(hex_id_res7, rede)` de `concorrentes_mapeados` — quantos hexes/alunos são REALMENTE filial de rede já mapeada (join por chave composta). Comparar com os **62,7%** de dedup grosseiro por hex-overlap do TP-08, demonstrando que o dedup fino (hex×rede) corrige a super-dedução (só conta como duplicado quando a MESMA rede aparece no MESMO hex).
   - Caveat: 97% independentes → capacidade por rede útil só para ~13 redes N≥10; oferta menor é majoritariamente adição legítima à saturação.

### 7. Exports do pacote
7.1. Adicionar ao `__init__.py` de `demanda_revelada/`: `ingerir_oferta_menores_por_rede`, `gerar_capacidade_media_por_rede`, `gerar_relatorio_classificacao`, `classificar_rede`, os 2 contratos e as 2 `VERSAO_CONTRATO_*`. Sem quebrar os exports do TP-08 (`oferta_menores_v1` intacto).

### 8. Testes (`tests/unit/demanda_revelada/test_classificacao_rede_menor.py`)
Fixtures SINTÉTICAS (nomes FABRICADOS carregando tokens de rede; jamais a fonte real). Reusar o padrão de `test_ingestao_oferta_menores.py` (xlsx sintético em `tests/fixtures/`, ou construir o frame de fronteira in-test para os testes de classificação puros). Casos:
   8.1. `test_classificacao_por_token_boundary` — nomes fabricados casam a rede certa (`"Academia Smart Fit Centro" → smart_fit`, `"Panobianco Vila X" → panobianco`).
   8.2. `test_falso_positivo_evitado` — nome com substring ambígua NÃO casa: `"Studio Live Well" → independente` (não `live`), `"Terrace Fit" → independente` (não `race`).
   8.3. `test_colapso_baixa_cardinalidade` — rede com N < limiar vira `independente` no artefato.
   8.4. `test_zero_pii` — nenhuma coluna PII (`nome_academia`/`latitude`/`longitude`/`cluster_id`/`total_alunos_cluster`) nos 2 parquets nem no relatório.
   8.5. `test_agregacao_hex_rede` — soma de `Alunos_Academia` correta por `(hex_id, rede_menor)`; `Total_Alunos_Cluster` nunca somado.
   8.6. `test_capacidade_media_mediana` — média/mediana/`n_filiais`/`flag_confiavel` corretos; `independente` excluída da tabela de capacidade.
   8.7. `test_isolamento_import` — AST sobre o módulo novo: sem `pipelines.m1`/`dashboard`/`censo`/`motor_expansao.api`.
   8.8. `test_determinismo` — 2 execuções → frames idênticos (ordenação estável).

### 9. Documentação
9.1. Documentar o novo contrato (2 artefatos) em `docs/modelo_mercado_hexagonos.md` (aditivo — join por `hex_id`/`rede_menor`), se aprovado no gate.
9.2. Ao fechar: mover BLK-TP-08-FU para `tasks/completed.md` e desbloquear o BLK-TP-06-FU1 (retomável) no backlog.

## Arquivos afetados
- `src/motor_expansao/demanda_revelada/classificacao_rede_menor.py` (NOVO)
- `src/motor_expansao/demanda_revelada/__init__.py` (aditivo — novos exports)
- `tests/unit/demanda_revelada/test_classificacao_rede_menor.py` (NOVO)
- `tests/fixtures/` (fixture sintética com nomes fake carregando tokens de rede, se necessária — ex.: `oferta_menores_rede_fake.xlsx`)
- `data/reports/scratch/rede_menor_classificacao_qualidade.md` (NOVO, gitignored)
- `data/staging/oferta_academias_menores_rede_h3.parquet` (NOVO, gitignored — gerado sob demanda, não versionado)
- `data/staging/capacidade_media_por_rede.parquet` (NOVO, gitignored — gerado sob demanda, não versionado)
- `docs/modelo_mercado_hexagonos.md` (aditivo, se aprovado)
- `tasks/backlog.md`, `tasks/current_task.md`, `tasks/completed.md`
- `context/handoff.md`, `context/handoff/`

## Dependências
- `pandas`, `h3`, `openpyxl` (leitura xlsx) — já na base; `re`, `unicodedata` (stdlib). **Sem dependência nova.**
- Insumos de leitura: `NAO_ABRA/03_Competidores.xlsx` (fonte real, gitignored), `data/staging/concorrentes_mapeados.parquet` (vocabulário `rede` = 28 redes; dedup fino por `hex_id_res7`×`rede`). `base_calibracao_multirede.parquet` só como referência do relatório (cross-check: skyfit/engenharia/ultra já têm média real).
- Reuso do TP-08: `_COLUNAS_DROP_FRONTEIRA`, `_COLUNAS_PII_LOCAIS`, `_assert_res7`, `FONTE_DEFAULT`, `CONCORRENTES_DEFAULT` (import intra-pacote — permitido; ainda isolado do M1).

## Riscos técnicos
- **Reidentificação por categoria de baixa cardinalidade** — mitigado por `_colapsar_baixa_cardinalidade` (passo 2.1); limiar é DECISÃO B do gate.
- **Falsos positivos de matching** (`live`/`race`/`jab`/`phd`/`gavioes`) — mitigado por word-boundary + curadoria de tokens (precisão > recall; falso `independente` aceitável).
- **Chave de dedup divergente** — `concorrentes_mapeados` usa `hex_id_res7`; o TP-08/este FU usam `hex_id`; ambos são H3 res-7 → o join composto `(hex, rede)` é consistente; garantir cast `str`.
- **Precedência de matching** — nome com 2 tokens de redes distintas (raro) resolve por first-match na ordem estável do dict; documentar.
- **Média de `independente`** — heterogênea; excluída da tabela de capacidade (não é capacidade de rede).
- **Cobertura ~3%** — 97% `independente`; a tabela de média por rede só serve o FU1 para ~13 redes N≥10 (caveat, não falha).
- **Determinismo do openpyxl/mediana** — mediana de N par usa interpolação padrão do pandas; ordenação estável garante reprodutibilidade.

## Critérios de aceite finais
- `rede_menor` derivada de `Nome_Academia` e o nome/coords/cluster DROPADOS na fronteira; nenhum aparece em parquet/relatório/log/teste.
- `test_zero_pii` FALHA se qualquer coluna de `_COLUNAS_PII_LOCAIS` (incl. `nome_academia`/`latitude`/`longitude`/`cluster_id`/`total_alunos_cluster`) surgir nas 2 saídas.
- Saída (a): parquet gitignored `hex_id × rede_menor × n_academias_menores × alunos_academias_menores` + `versao_contrato`; hex_id res-7 válido; join por `hex_id` reproduzível.
- Saída (b): parquet gitignored `rede_menor → media_alunos, mediana_alunos, n_filiais, flag_confiavel` + `versao_contrato`; cobre ≥ ~13 redes com N suficiente; `independente` fora da tabela.
- Redes com N < limiar colapsam em `independente`; a categoria não reidentifica estabelecimento.
- Relatório documenta cobertura (% conhecida vs independente), N por rede, redes confiáveis e o **dedup fino** (hex×rede) vs os 62,7% grosseiros do TP-08.
- Nenhum artefato oficial M1 alterado (mtime dos 4 oficiais inalterado); nenhum import de `pipelines/m1`/`dashboard`/`censo_*`/`api`.
- Testes com fixtures sintéticas; `NAO_ABRA/` não versionada. Suíte verde (ruff + mypy + pytest do escopo).

## Validações obrigatórias
- Comando: `python -m pytest -n auto -q tests/unit/demanda_revelada/ tests/integration/test_streamlit_app.py`
  - Critério mínimo: 0 falhas.
- Comando: `python -c "import streamlit_app"`
  - Critério mínimo: 0 erro de import.
- Comando (isolamento): `python -m pytest -q tests/unit/demanda_revelada/test_classificacao_rede_menor.py::test_isolamento_import`
  - Critério mínimo: passa (AST sem `pipelines.m1`/`dashboard`/`censo`/`api`).
- Comando (ruff+mypy): `python -m ruff check src/motor_expansao/demanda_revelada/ tests/unit/demanda_revelada/ && python -m mypy src/motor_expansao/demanda_revelada/`
  - Critério mínimo: 0 erro.
- Comando (anti-PII dos 2 artefatos reais — só colunas): após gerar sob demanda, `python -c "import pandas as pd; [print(sorted(pd.read_parquet(p).columns)) for p in ('data/staging/oferta_academias_menores_rede_h3.parquet','data/staging/capacidade_media_por_rede.parquet')]"`
  - Critério mínimo: nenhuma coluna PII (`nome_academia`/`latitude`/`longitude`/`cluster_id`/`total_alunos_cluster`).
- Comando (mtime M1 inalterado): comparar mtime de `data/outputs/{hexagonos_brasil_dashboard.parquet, top_oportunidades_resumo.csv, resumo_por_uf.csv}` + `data/staging/brasil_priorizados.parquet` antes/depois.
  - Critério mínimo: inalterado.

## Fora de escopo
- Qualquer escrita/recálculo no M1 (score_priorizacao, hex_score_estrutural, carteira, plano, artefatos oficiais). READ-ONLY.
- Recompor o residual (`score_oportunidade_residual`/`oferta_efetiva_disponivel`) ou integrar a capacidade por rede ao pipeline de mercado — é o BLK-TP-06-FU1 / BLK-TP-09, sob gate próprio.
- APLICAR o dedup fino (subtrair oferta): este bloco só HABILITA (produz o rótulo + o par hex×rede); a subtração é follow-up.
- Alterar o contrato `oferta_menores_v1` do TP-08 (novo contrato/arquivo — não quebrar o existente).
- Persistir/logar QUALQUER PII (`Nome_Academia`/Lat/Lng/`Cluster_ID`/`Total_Alunos_Cluster`); versionar `NAO_ABRA/`.
- Res-8 / novas cadências de coleta / integração WellHub-TotalPass ao residual.

## Alerta de aprovação
**ATENÇÃO: ESTA TAREFA CLASSIFICA REDE A PARTIR DE NOME CRU SOB ANTI-PII (DEC-012). APROVAÇÃO HUMANA OBRIGATÓRIA ANTES DO BUILDER. O USUÁRIO DEVE LER ESTE HANDOFF E CONFIRMAR AS 4 DECISÕES DO GATE (A VOCABULÁRIO/MATCHING, B LIMIAR ANTI-REIDENTIFICAÇÃO, C FORMATO DOS 2 ARTEFATOS, D MÉDIA VS MEDIANA + N MÍNIMO). NÃO É CRÍTICA: READ-ONLY SOBRE O M1, ZERO ESCRITA EM SCORE/PESOS/CARTEIRA/PLANO/ARTEFATOS OFICIAIS. O GATE É POR CAUSA DA CLASSIFICAÇÃO DE NOME CRU + RISCO DE REIDENTIFICAÇÃO POR CATEGORIA, NÃO POR TOQUE NO M1.**

## DECISÕES PARA O GATE HUMANO
- **(A) Lista curada de redes + método de matching.** As 28 redes de `concorrentes_mapeados` (`smart_fit` 1000, `panobianco` 458, `bluefit` 226, `selfit` 221, `pratique` 165, `bodytech` 156, `phd_sports` 141, `gavioes` 120, `velocity` 117, `allp_fit` 71, `26fit` 68, `engenharia_do_corpo` 64, `vidya_studio` 59, `contorno_do_corpo` 55, `evoque` 48, `live` 42, `tonus_gym` 41, `bio_ritmo` 36, `aera_pilates` 33, `race_bootcamp` 30, `alpha_fitness` 27, `greenlife` 25, `xprime` 23, `kore` 22, `cia_athletica` 19, `world_gym` 19, `red_fitness` 6, `jab_house` 4). Matching = **normalização (lower + sem acento + sem pontuação) + TOKEN com word-boundary `\b...\b`** (não substring). **RECOMENDAÇÃO:** manter as redes com token específico/composto seguro; para os tokens ambíguos curtos (`live`, `race`, `jab`) usar SÓ o token composto da rede-mãe (`race bootcamp`, `jab house`) e, se não houver contexto, deixar `independente` — precisão > recall. Curar `phd`, `kore`, `evoque`, `gavioes` (exigir 2º token ou marca completa). Precedência = first-match na ordem do dict (redes mais específicas primeiro).
- **(B) Limiar de N que colapsa rede em `independente` (anti-reidentificação).** Qualquer `rede_menor` com `N_filiais` no dump abaixo do limiar é reescrita para `independente` antes de persistir. **RECOMENDAÇÃO: `N_MINIMO_ANTI_REIDENTIFICACAO = 3`** — colapsa redes com 1-2 filiais (baixíssima cardinalidade → maior risco de reidentificação hex+categoria) mantendo as ~20 redes com match real. Alternativa mais conservadora: 5 (colapsaria também `jab_house`/`red_fitness`-nível). Justificativa: 3 equilibra utilidade (preserva o vocabulário útil para dedup) e privacidade (categoria com ≥3 estabelecimentos não singulariza um).
- **(C) Formato dos 2 artefatos.** **RECOMENDAÇÃO: Saída (a) LONGO** `hex_id × rede_menor × n_academias_menores × alunos_academias_menores × versao_contrato` (robusto a novas redes, evita esparsidade de uma coluna por rede, casa direto com o par `(hex_id_res7, rede)` de `concorrentes_mapeados` no dedup fino). **Saída (b)** = tabela `rede_menor → media_alunos, mediana_alunos, n_filiais, flag_confiavel, versao_contrato`. Ambos parquet gitignored em `data/staging/`, NÃO oficiais do M1.
- **(D) Média vs mediana + N mínimo de confiabilidade.** **RECOMENDAÇÃO: reportar AMBAS (média e mediana), mas o valor RECOMENDADO ao consumo (Candidato C do FU1) é a MEDIANA** (robusta a outliers de alunos por unidade). **N mínimo de confiabilidade `N_MINIMO_CONFIABILIDADE = 10`** (`flag_confiavel = n_filiais >= 10`) → cobre ~13 redes (panobianco 149, velocity 114, gavioes 91, 26fit 67, evoque 48, vidya 46, kore 37, bio_ritmo 33, smart_fit 29, live 25, etc.); redes 5 ≤ N < 10 entram na tabela com `flag_confiavel=False`; N < limiar anti-reidentificação (B) já viraram `independente` e não aparecem. Alternativa: N≥20 (11 redes, mais rígido).

## Guardrails ativos
- **§5 (READ-ONLY M1):** zero recálculo de score/pesos/carteira/plano/artefatos oficiais; NÃO recompor o residual; pesos `renda=0.40`/`pop=0.60` e `score_priorizacao` INALTERADOS.
- **DEC-012 (anti-PII POR CONSTRUÇÃO):** rede classificada na FRONTEIRA; `Nome_Academia`/Lat/Lng/`Cluster_ID`/`Total_Alunos_Cluster` descartados na entrada, nunca persistidos/logados; `rede_menor` é CATEGORIA (não o nome) e, por (B), não pode reidentificar; zero PII em artefato/log/teste; fonte real em `NAO_ABRA/` (gitignored); fixtures sintéticas.
- **DEC-013 (parte 3):** dedup + capacidade por tipo é o que este bloco HABILITA (rótulo + média por rede); integração ao residual permanece follow-up sob gate.
- **DEC-009:** oferta é insumo OBSERVADO, NUNCA preditor geográfico de magnitude.
- **Isolamento:** pacote `demanda_revelada/` NÃO importa de `pipelines/m1/`, `dashboard/`, `censo_*` nem `api`.
- **Parâmetros canônicos §3 INALTERADOS:** `H3_RESOLUTION=7` reutilizado como chave; nenhum parâmetro de score introduzido (`N_MINIMO_*` são constantes da camada paralela, não do M1).
