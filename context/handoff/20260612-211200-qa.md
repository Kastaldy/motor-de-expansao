# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador executa o Passo 6.0 — housekeeping move — e fecha o ciclo)

## VEREDITO
APROVADO (condicionado ao move de housekeeping no fechamento — Passo 6.0)

## Justificativa
Bloco puramente documental: `docs/api_geoespacial_uso.md` foi criado, é tecnicamente
exato contra os fontes da API (15 vars `API_*`, 4 slugs de erro, 17 campos do
`AnalisarResponseJSON`, porta 8077, versão `api-geoespacial/v1`, bounding box, 4 padrões
de `maps_url`, fluxo do bot e negociação JSON/PDF — todos conferidos linha-a-linha), o
escopo foi respeitado (só `docs/` + handoffs/current_task, NENHUM código), e a suíte
completa passou sem regressão (`733 passed, 4 skipped`, 0 falhas, 0 erros de coleção).

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- Seção 3b (Performance) cita "10 a 30 s"; a mensagem real do bot (`_GERANDO`) diz "cerca de
  2 minutos" e o timeout do cliente é 240 s. Não é contradição (uma é perf típica, a outra é
  estimativa conservadora ao usuário), mas alinhar os números evitaria ruído. Opcional.
- A seção 8 lista as vars na ordem do `settings.py`, mas o nome de env `API_API_PREFIX` e
  `API_API_CALL_TOKEN` (duplo `API_`) está correto e documentado — bom; poderia ganhar uma nota
  de 1 linha explicando que o duplo prefixo vem do `env_prefix="API_"` + campo `api_prefix`/
  `api_call_token`. Opcional.

## Testes faltantes
- nenhum (bloco documental; nenhum código novo a cobrir. A suíte existente — incl.
  `tests/unit/test_housekeeping_helper.py` e os testes da API — permanece verde.)

## Riscos remanescentes
- O `--check` de housekeeping ainda FALHA (estado pré-move esperado): o stub no `backlog.md`
  e o bloco em `completed.md` serão criados pelo orquestrador no Passo 6.0 do fechamento via
  `scripts/housekeeping_move_block.py BLK-API-08` (sem `--check`). A APROVAÇÃO é condicionada a
  esse move ser executado no fechamento. NÃO executei o move (apenas `--check`), conforme regra.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suite completa (gate único do ciclo): `python -m pytest -n auto -q`
```
tests/integration/test_modelo_mercado_hexagonos.py: 10 warnings
  .../calcular_colunas_mercado.py:395: FutureWarning: Downcasting object dtype arrays on .fillna ...
-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
733 passed, 4 skipped, 153206 warnings in 198.99s (0:03:18)
```
0 falhas, 0 erros de coleção. Warnings são deprecações pré-existentes (asyncio 3.16,
geopandas CRS geográfico em `area`, pandas downcasting) — sem relação com o doc.

### 1-bis. Teste do helper de housekeeping (incluído na suite full, conferido standalone)
```
10 passed, 491 warnings in 0.12s
```
(`tests/unit/test_housekeeping_helper.py`)

### 2. Smoke import: `python -c "import streamlit_app; print('import ok')"`
```
2026-06-12 21:11:12.823 WARNING streamlit ...: missing ScriptRunContext! ...
2026-06-12 21:11:12.904 WARNING streamlit.runtime.caching.cache_data_api: No runtime found ...
... (warnings de runtime Streamlit fora do servidor — esperados) ...
import ok
```

### 3a. Escopo: `git status --porcelain`
```
 M context/handoff.md
 D data/raw/ibge/malha_brasil.geojson
 D data/raw/ibge/malha_uf_brasil.geojson
 M tasks/current_task.md
?? context/handoff/20260612-120000-block-orchestrator.md
?? context/handoff/20260612-203000-planner.md
?? context/handoff/20260612-210800-builder.md
?? docs/api_geoespacial_uso.md
```

### 3b. Escopo: `git diff --name-only`
```
context/handoff.md
data/raw/ibge/malha_brasil.geojson
data/raw/ibge/malha_uf_brasil.geojson
tasks/current_task.md
```
Tocados pelo ciclo: `docs/api_geoespacial_uso.md` (novo), `tasks/current_task.md`,
`context/handoff.md` + snapshots `context/handoff/*`. NENHUM arquivo de código (`src/`,
`tests/`, `config.py`, `streamlit_app.py`). Os 2 geojson deletados em `data/raw/ibge/` são
estado PRÉ-EXISTENTE do worktree (reconhecidos; não são do ciclo, não contam como violação).

### 4. Housekeeping `--check` (estado pré-move ESPERADO falhar)
```
FALHA --check: stub ausente no backlog para BLK-API-08
EXIT=1
```
Esperado no estado pré-fechamento. Helper presente em `scripts/housekeeping_move_block.py`.
BLK-API-08 ainda é bloco completo em `tasks/backlog.md` (linha 260 — pré-move correto). O move
é responsabilidade do orquestrador no Passo 6.0; NÃO executei o move.

## Conferência de no-bypass
Confirmo: nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do
caminho crítico. A suíte é `pytest -n auto` puro (sem `-p no:xdist`, sem flag de contorno). Como
o bloco é doc puro, não há tooling-de-produção em escopo (não é o caso SOPS/BLK-OPS-01). Verde real.

## Conferência cruzada com log do Builder
Bate. O Builder afirmou ter conferido versão, rotas, 15 vars, Bearer/401, negociação JSON/PDF,
campos dos schemas, 4 slugs, bounding box, cadeia de geocoding e fluxo do bot — RE-CONFERI cada
um contra os fontes e todos batem (sem divergência material). O Builder não rodou `pytest`
(correto — gate é do QA); rodei aqui: verde. Smoke import bate ("import ok"). Escopo bate.

## Auditoria de exatidão do doc (amostragem mínima exigida + cobertura completa)
- **Versão** `api-geoespacial/v1` — bate `src/.../api/__init__.py`. ✓
- **Porta 8077** — bate `api_base_url` default + exemplos curl. ✓
- **15 vars `API_*`** — cada default conferido contra `settings.py`: environment=development,
  api_prefix=/api/v1, censo_geo_dir, ibge_dir, ultra_dir, competitors_logos_dir=None,
  staging_dir, cors_origins=["*"], tokens={"dev-token":"dev-local"}, telegram_token="",
  api_base_url=http://127.0.0.1:8077, api_call_token="dev-token", bot_senha="trocar-esta-senha",
  google_maps_api_key="", bot_sessoes_path=bot_sessoes.json. Todos batem. ✓
- **4 slugs de erro** — 400 `coordenada_invalida` / 401 `nao_autenticado` ("Token invalido") /
  404 `base_geo_ausente` (msg "Materialize setores_censitarios_2022_geo/ para SP/3550308") /
  500 `erro_interno` ("Erro interno ao gerar o estudo"). Batem `service.py`/`coord.py`/`auth.py`/
  `errors.py`. Nota 422 (validação Pydantic sem `codigo`) correta. ✓
- **17 campos do `AnalisarResponseJSON`** — batem `schemas/__init__.py` (lat/lng/raio_km/area_km2/
  metodo/n_setores/pop_total_raio/renda_per_capita_media_raio/densidade_pop_raio_hab_km2/
  score_setor_medio/score_setor_max/n_concorrentes/n_ultra/versao_contrato/versao_score/
  gerado_em/consumidor). ✓
- **`AnalisarRequest`** lat/lng/maps_url/formato/rotulo + regra "lat,lng OU maps_url". Bate. ✓
- **Negociação JSON/PDF (3 formas + precedência Accept→query→body)** — bate `routes/analisar.py`
  (linha 52-53). ✓
- **4 padrões `maps_url` + ordem de prioridade** + query params (q|query|ll|sll|center|destination)
  — batem `coord.py` `_PADROES`. ✓
- **Bounding box** lat[-34.0;5.5] lng[-74.0;-28.0] — bate `coord.py`. ✓
- **Fluxo do bot** (senha→login→menu→localização→PDF), 3 formas de localização, persistência
  `bot_sessoes_path`, comandos, cadeia de geocoding Selenium→Google→Nominatim, timeout 240s —
  batem `telegram_bot.py`/`geo.py`/`maps_geocoder.py`. ✓
- **Cols SAM** (sam_fitness_potencial/oferta_efetiva_disponivel/score_oportunidade_residual/
  oferta_consumida_mercado_estimada) — batem `service.py` `_SAM_COLS`. ✓
- **Checklist Passo 12 (12 seções)** — todas presentes e corretas. ✓

## Guardrails verificados
- score_priorizacao não alterado: **sim** (não aplicável a código; bloco doc — nenhum artefato/
  pipeline/`config.py` tocado; `git diff` confirma só `docs/` + handoffs)
- Artefatos M1 preservados: **sim** (nenhum parquet/CSV oficial nem `config.py` tocado)
- Parâmetros canônicos intactos: **sim** — o doc é READ-ONLY; H3_RESOLUTION=7, RENDA_MIN=4500,
  pesos renda=0.40/pop=0.60 não são tocados (e o doc não os contradiz; cita raio 1.5 km e método
  `setor_censitario_intersecao_area_1p5km` como INTOCÁVEIS, coerente com §4)
- Validações re-executadas pelo QA (sem bypass):
  - `pytest -n auto` → 733 passed, 4 skipped, 0 falhas
  - `import streamlit_app` → import ok
  - `git status --porcelain` / `git diff --name-only` → escopo só doc + handoffs
  - `housekeeping_move_block.py BLK-API-08 --check` → FALHA esperada (pré-move)
- Escopo respeitado: **sim** (nenhum arquivo de código; 2 geojson deletados são pré-existentes do worktree)

## Decisão recomendada
Fechar ciclo: orquestrador executa o Passo 6.0 (`scripts/housekeeping_move_block.py BLK-API-08`,
sem `--check`) movendo o bloco de `backlog.md` → `completed.md` byte-idêntico, commita por path e
encerra. APROVAÇÃO condicionada a esse move.
