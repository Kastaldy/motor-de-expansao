# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (housekeeping via `scripts/housekeeping_move_block.py BLK-VIAB-02` pós-veredito)

## VEREDITO
APROVADO

## Justificativa
O módulo `demanda_premissa.py` e seus 28 testes passam integralmente (ruff, mypy, subset e smoke import limpos); é READ-ONLY sobre o M1 (config/pipelines/m1/artefatos oficiais intocados, mtimes de 2026-06-03 preservados, `viabilidade_ponto.py` com diff vazio) e honra a DEC-009 (alvo = alunos reais; zero coluna geográfica/PII no artefato). As 4 falhas + 1 erro da suíte full são pré-existentes e de ambiente (Plus Code/`openlocationcode` em `api/` e matplotlib ausente em `demanda_revelada/`), sem qualquer ligação com este ciclo.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- O `config.py` do dimensionamento aparece como modificado no working-tree por conversão CRLF→LF (0 mudança de conteúdo; `--ignore-space-at-eol` = 0 linhas). Já estava assim antes do ciclo e NÃO entrou no commit da feature (HEAD `76b6e39`). Sugestão: normalizar line-endings do repo (`.gitattributes`) para eliminar esse ruído recorrente em ambiente Windows/WSL.

## Testes faltantes
- nenhum. Cobertura adequada: fronteiras de tier, N por tier, monotonicidade de percentis, flag_extrapolacao true/false, NaN em tier vazio, ausência de PII/geográfico, determinismo, materialização e run end-to-end.

## Riscos remanescentes
- Tier `>=3000` com N=2 (flag_extrapolacao=True) → percentis instáveis; devidamente sinalizado no artefato e no relatório de qualidade, a ser exibido como aviso no BLK-VIAB-03 (contrato preservado).
- Discrepância backlog "~1.100 academias" vs. N=112 real; documentada no relatório e corrigida no backlog pelo Builder. Não é defeito.

## Saída literal das validações (re-executadas pelo QA)

### 1. Verificação de código (grep de padrões proibidos)
```
8:    - PROIBIDO usar membros/agregadores corporativos ou qualquer preditor geografico
9:      (lat, lng, hex_id) como alvo de demanda.
71:        Nenhuma coluna de PII (nome, lat, lng, hex_id) e propagada.
259:        " PROIBIDO usar `membros`/agregadores corporativos ou qualquer preditor geografico como alvo.",
```
Todos os matches são docstrings/comentários que DOCUMENTAM a proibição da DEC-009 — nenhum uso de `membros`/`lat`/`lng` como preditor. `from motor_expansao.config` ausente. `viabilidade_ponto` não importado.

### 2. ruff
```
$ python -m ruff check src/motor_expansao/dimensionamento/demanda_premissa.py tests/unit/dimensionamento/test_demanda_premissa.py
All checks passed!
```

### 3. mypy
```
$ python -m mypy src/motor_expansao/dimensionamento/demanda_premissa.py
Success: no issues found in 1 source file
```

### 4. pytest subset
```
$ python -m pytest tests/unit/dimensionamento/test_demanda_premissa.py -q
............................                                             [100%]
28 passed in 1.75s
```

### 5. pytest FULL (-n auto)
```
$ python -m pytest -n auto -q
=========================== short test summary info ============================
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_curto_usa_localidade_mock
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_curto_usa_localidade_mockada
ERROR tests/unit/demanda_revelada/test_viz_atratividade.py
4 failed, 1464 passed, 4 skipped, 18 warnings, 1 error in 406.49s (0:06:46)
```
As 4 falhas são Plus Code (`resolve_plus_code` retorna None → `openlocationcode` não instalado). O 1 erro é `ModuleNotFoundError: No module named 'matplotlib'` em `test_viz_atratividade.py` (coleta). Todas são de AMBIENTE/dependência em módulos `api/`+`demanda_revelada/`, NENHUMA em `dimensionamento/`, NENHUMA introduzida por este ciclo (os módulos são independentes de `demanda_premissa.py`). O Builder rodou subset (`dimensionamento/ + integration`), por isso reportou só 2; o gate full surfaceia 4+1 — todas pré-existentes.

### 6. Smoke import
```
$ python -c "from motor_expansao.dimensionamento.demanda_premissa import run, calcular_tiers; print('ok')"
ok
```

### 7. Guardrails M1
```
$ git diff HEAD -- src/motor_expansao/dimensionamento/viabilidade_ponto.py
[vazio — INTOCADO]

$ git diff HEAD -- src/.../dimensionamento/config.py  (word-diff --ignore-space-at-eol)
0 linhas de conteúdo alteradas (apenas CRLF→LF no working-tree; NÃO no commit HEAD)

$ git -c core.whitespace=cr-at-eol diff --ignore-space-at-eol HEAD~1 HEAD -- .../config.py
0  [config.py NÃO faz parte do commit da feature]

$ stat -c %Y data/staging/brasil_estrutural.parquet   -> 1780501621 (2026-06-03)
$ stat -c %Y data/staging/brasil_priorizados.parquet  -> 1780501631 (2026-06-03)
```
Nota: os valores esperados no prompt do QA (622/632) estão off-by-1 vs. os reais (621/631). Os reais batem com o handoff do Builder e o current_task, e datam de 2026-06-03 (semanas antes do ciclo) → artefatos M1 demonstravelmente INTOCADOS. Único parquet recém-escrito em staging: `demanda_premissa_por_tier.parquet` (saída pretendida).

### 8. Escopo do ciclo (commit HEAD 76b6e39)
```
$ git show HEAD --stat
demanda_premissa.py | 320 ++  | test_demanda_premissa.py | 386 ++
tasks/{backlog,completed,current_task}.md | context/handoff*.md
```
O commit da feature contém APENAS: `demanda_premissa.py`, o teste, os handoffs e os arquivos de tarefa. SEM `config.py` de conteúdo, SEM `pipelines/m1/`, SEM artefatos M1. (`config.py` aparece só no working-tree por CRLF, não no commit.)

### 9. Housekeeping pré-check
```
$ python scripts/housekeeping_move_block.py BLK-VIAB-02 --check
FALHA --check: stub ausente no backlog para BLK-VIAB-02
EXIT=1
```
Correto — o move do bloco para `completed.md` ocorre PÓS-veredito. O heading `### BLK-VIAB-02` ainda está em `backlog.md` (linha 897).

### Verificação do artefato materializado
```
cols: ['tier_label','metragem_min','metragem_max','n','p10','p50','p90','flag_extrapolacao','versao_contrato']
versao_contrato: ['demanda_premissa_v1']
n por tier: {'<1000':17,'1000-1499':46,'1500-1999':36,'2000-2999':11,'>=3000':2}  total=112
flag_extrapolacao: {'<1000':False,...,'>=3000':True}  (True só onde N=2<5)
colunas PII/geograficas presentes: NENHUMA
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock do caminho crítico. As fixtures sintéticas dos testes unitários são uso LEGÍTIMO (isolamento de lógica pura), explicitamente fora do escopo da regra de no-bypass (que mira tooling dependente de config de produção). A suíte full rodou 1× aqui com `-n auto` (gate único). O artefato de produção foi inspecionado diretamente (dados reais, N=112).

## Conferência cruzada com log do Builder
Bate no essencial e diverge apenas em detalhes esperados:
- ruff/mypy/subset/smoke/import: idênticos.
- N por tier (17/46/36/11/2 = 112), versao_contrato, flag_extrapolacao: idênticos.
- mtimes M1 621/631: idênticos.
- Contagem de falhas: Builder reportou "2 failed" (rodou subset); o gate full expõe "4 failed + 1 error" — TODAS pré-existentes de ambiente (Plus Code/openlocationcode + matplotlib ausente), fora de `dimensionamento/`. Não é regressão nem divergência de qualidade.

## Guardrails verificados
- score_priorizacao não alterado: sim (nenhum toque em pipelines/m1/, config raiz, score)
- Artefatos M1 preservados: sim (mtimes 621/631 de 2026-06-03 inalterados; loop_guard limpo no Builder)
- Validações re-executadas pelo QA (sem bypass):
  - ruff → All checks passed
  - mypy → Success: no issues found
  - pytest subset → 28 passed
  - pytest full -n auto → 4 failed (pré-existentes Plus Code) + 1 error (matplotlib ausente) + 1464 passed
  - smoke import → ok
  - guardrail diffs (viabilidade_ponto/config) → sem conteúdo alterado
  - housekeeping --check → FALHA esperada (stub ausente, move pós-veredito)
- Escopo respeitado: sim (commit contém só os 2 arquivos de código do ciclo + tarefas/handoffs)
- Parâmetros canônicos (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, renda=0.40/pop=0.60): INALTERADOS (bloco não toca §3)

## Decisão recomendada
Fechar ciclo. Executar o housekeeping via `scripts/housekeeping_move_block.py BLK-VIAB-02` (pós-veredito) para mover o bloco de `backlog.md` para `completed.md`.
