# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Builder (criticidade Média — SEM gate humano)

## Campos de controle
- Skill atual: Planner
- Próxima Skill: Builder
- Bloco: BLK-ARCH-01b — Tipar os 14 módulos migrados e remover o override de mypy
- Criticidade: Média
- Branch ativo: `ciclo/BLK-ARCH-01b`

## Entendimento da tarefa
Corrigir mecanicamente os 50 erros de tipo dos 14 módulos de `src/motor_expansao/pipelines/` hoje silenciados pelo bloco `[[tool.mypy.overrides]]` (`ignore_errors = true`) e remover esse override, sem alterar comportamento/valores/score/artefatos M1 (não-mutação provada por sha256 idêntico pré/pós).

## RECON — confirmado pelo Planner
Procedimento: backup do `pyproject.toml`, remoção TEMPORÁRIA do bloco `[[tool.mypy.overrides]]`, `python -m mypy src/`, depois RESTAURAÇÃO (git diff de `pyproject.toml` confirmado LIMPO, EXIT_DIFF=0).
- **Resultado real:** `Found 50 errors in 12 files (checked 44 source files)` — CONFIRMA a contagem do Block Orchestrator (QA de 01a).
- Ambiente: `mypy 2.1.0`, `pandas 2.3.3`, **SEM `pandas-stubs`** (não instalado), **COM `types-requests` 2.33.x**. Os 50 erros aparecem mesmo sem pandas-stubs (são erros de `object`/`float|None`/numpy, não de Series tipada).
- Os 12 arquivos com erro são subconjunto dos 14 do override. `validar_fase_a_censo2022` E `comparar_geofusion_vs_hex` TÊM erro (12 arquivos no total; 2 dos 14 ficam sem erro — provavelmente nenhum, conferir: os 12 são os abaixo). Os módulos `validar_modelo_ultra` e `enriquecimento_espacial_hexagonos` NÃO têm teste dedicado, mas têm erro.

> PARIDADE CI/LOCAL (lição de memória — "mypy local sem stubs dá falso verde"): o Builder DEVE verificar `.github/workflows/*` e o ambiente do CI antes de declarar limpo. Se o CI instalar `pandas-stubs`, podem surgir erros ADICIONAIS de Series; reaplicar os mesmos padrões. NÃO adicionar `pandas-stubs` como dependência do projeto (fora de escopo) — apenas garantir paridade no ambiente de validação.

## Os 50 erros enumerados (arquivo:linha:código) — confirmados
**normalizar_unidades_ultra.py**
- `:158` `[arg-type]` — `_coord_valida`: `_coord_in_brazil(float(lat),...)` com `lat: object`.

**gerar_carteira_acionavel.py**
- `:146` `[arg-type]` — `_coerce_score_value`: `return float(score_value)` (`score_value` é célula `object`). JÁ há `# type: ignore[arg-type]` em outros `float()` do módulo (ex.: `_safe_int`/`calibrar` usam o padrão).
- `:155` `[arg-type]` — `_safe_float`: `return float(value)` (`value: object`).
  NOTA: a linha 157 (`except ... return default`) referencia `default` que não é parâmetro de `_safe_float` — é BUG de runtime LATENTE (NameError se exceção), mas NÃO é erro de tipo e está FORA de escopo (não consertar; ver Grupo C).

**comparar_geofusion_vs_hex.py**
- `:123` `[arg-type]` — `_fmt`: `f"{float(value):...}"` (`value: object`, já guardado por `pd.isna`).

**validar_modelo_ultra.py**
- `:265` `[var-annotated]` — `resultados = {}` em `_calcular_correlacoes`. Anotar `resultados: dict[str, dict[str, dict[str, float | int | None]]] = {}` (valores são dicts `{"rho","p","n"}`; rho/p podem ser None, n é int). Builder confirme o shape exato e use a anotação mais simples que feche (ex.: `dict[str, dict[str, dict[str, Any]]]` se necessário, importando `Any`).
- `:286` `[var-annotated]` — `resultado = {}` em função de quartis. Anotar `resultado: dict[str, Any] = {}` (ou shape exato). Builder inspeciona o que é populado.

**modelo_hibrido_expansao.py**
- `:102` `[arg-type]` — `_coerce_pair`: `return float(coerced_a), float(coerced_b)`. `coerced_a = a if a is not None else fallback_a` → mypy NÃO narrowa para float (a é `float|None`, fallback é float; o resultado fica `float|None` para o mypy). Linha 102 = `return float(coerced_a), ...`.
- `:110` `[arg-type]` — segundo site análogo no módulo (outro `float(...)` de `float|None`). Builder localiza o par.

**enriquecimento_espacial_hexagonos.py**
- `:134` `[call-overload]` — `np.where(n_2km > 0, redes_abc[argmax], None)`: o ramo "falso" é `None` (não array/escalar numérico) → numpy stubs não têm overload. ESTE É O ÚNICO `np.where` do módulo com erro (1 erro, NÃO 5). Tratamento abaixo (Grupo A).

**materializar_setores_censitarios_geo.py**
- `:29` `[no-redef]` (×2: `ajuste_executivo`, `percentile_in_distribution`) — fallback `try: from motor_expansao... except ModuleNotFoundError: from calibrar_renda...` na linha 28-29. mypy vê o segundo `from` como redefinição. (2 erros)

**fase_a_censo2022_setores.py**
- `:36` `[no-redef]` — `_percentil_em_distribuicao_referencia` no fallback `try/except import` (linhas 23-40). (1 erro; este import tem `# noqa: F401`.)
- `:720,:723,:730,:738` `[assignment]` (×4) — atribuições `var = Path(...)` onde a var foi inicializada como `None` e mypy inferiu o tipo `None`. Tratamento: anotar a inicialização como `var: Path | None = None` (Grupo A). Builder lê o bloco 712-742 para achar as inicializações `= None`.
- `:778,:1192` `[var-annotated]` (×2) — `info_processamento = {}` em duas funções. Anotar `info_processamento: dict[str, Any] = {}` (Grupo A).

**validar_penetracao_ultra_hex.py**
- `:110,:116,:123` `[arg-type]` (×3) — `_coerce` (ou helper de coerção) com `float(value)` sobre `object`. Builder confirma a função (provável `_coerce`/`_coerce_alt`). Grupo B.

**validar_fase_a_censo2022.py**
- `:34` `[no-redef]` (×11) — fallback `try/except import` (linhas 31-40+) reimporta 11 nomes de `fase_a_censo2022_setores`. mypy flagga cada um. (11 erros; o import tem `# noqa: F401`.)
- `:982` `[arg-type]` — `"".join(x)` onde `x: object` (provável `", ".join(summary[...])`). Builder lê 975-986; Grupo C ou B conforme o site.

**fase_a_piloto_expandido.py** (função `gerar_relatorio`/helpers — CÓDIGO VIVO, testado por `test_fase_a_piloto_expandido.py`)
- `:142,:157,:159` `[call-overload]` (×3) — `int(x)` onde `x: object` (valores de `summary`/dict tipado como `dict[str, object]`). Grupo B/A.
- `:570` `[str-format]` — f-string `f"{x}"` com placeholder int/float e `x: object`. Grupo B.
- `:610,:803` `[attr-defined]` (×2) — `for ... in x` onde `x: object` não-iterável estaticamente. Grupo B.
- `:805` `[index]` — `x[i]` onde `x: object`. Grupo B.

**fase_a_nacional_completo.py** (função `gerar_relatorio`/`calcular_qualidade_join_uf` — CÓDIGO VIVO, testado por `test_fase_a_nacional_completo.py`)
- `:59` `[return-value]` — `calcular_qualidade_join_uf`: `return calcular_qualidade_join(summary["mismatch_pct"])` retorna `object` esperado `str`. Causa-raiz: `summary: dict[str, object]` → `summary["mismatch_pct"]` é `object`. Grupo A/B.
- `:185` `[misc]` — generator com item `object` esperado `str`.
- `:193` `[arg-type]` + `:193` `[return-value]` (×2) — `sorted(..., key=lambda r: r[...])` com key retornando `object`.
- `:195` `[str-format]` — f-string object em placeholder int/float.
- `:207,:208` `[arg-type]` (×2) — `sorted(list[object])` esperado `Iterable[str]`.
- `:403` `[misc]` + `:403` `[attr-defined]` (×2) — generator/iteração sobre `object`.
- `:404` `[arg-type]` — `len(object)`.

> Total conferido: 1+2+1+2+2+1+(2+1+4+2)+3+(11+1)+(3+1+2+1)+(1+1+2+1+2+2+1)=50. Builder DEVE re-rodar `mypy src/` (override removido) para travar os números exatos antes de editar (linhas podem deslizar ±2).

## POLÍTICA DE CORREÇÃO (decisão do Planner — vincula o Builder)
Correção MECÂNICA; NÃO altera runtime/valores/dtype/ordenação. Três grupos:

### Grupo A — anotação real (sem ignore) onde o tipo verdadeiro é claro
- **`[no-redef]` de `try/except import`** (`materializar_setores_censitarios_geo.py:29` ×2, `fase_a_censo2022_setores.py:36`, `validar_fase_a_censo2022.py:34` ×11): este é o padrão idiomático de fallback import. A correção MECÂNICA preferida que NÃO muda runtime: adicionar `# type: ignore[no-redef]` ao `from ... import ...` do RAMO `except` (o segundo import). NÃO mexer no `try`/no fluxo. Justificativa: dual-import fallback é runtime-correto; mypy não modela `except ImportError`. Alternativa (se Builder preferir e ruff aceitar): converter para `import ... as ...` — porém o PADRÃO é `# type: ignore[no-redef]` pontual no import do except (mínima superfície, idiomático). Confirmar que ruff/F811 segue limpo (o import já tem `# noqa: F401` onde aplicável; pode ser preciso `# noqa: F811` também — Builder ajusta o noqa sem remover nomes).
- **`fase_a_censo2022_setores.py:720/723/730/738` `[assignment]`**: anotar a INICIALIZAÇÃO `var = None` como `var: Path | None = None` (reflete que recebe Path depois). Sem mudança de runtime.
- **`fase_a_censo2022_setores.py:778/1192` e `validar_modelo_ultra.py:265/286` `[var-annotated]`**: anotar o dict acumulador (`dict[str, Any] = {}` ou shape exato). `from typing import Any` se ainda não importado. Sem mudança de runtime.
- **`enriquecimento_espacial_hexagonos.py:134` `[call-overload]`**: `np.where(cond, redes_abc[argmax], None)`. O ramo `None` é intencional (produz array object com None onde não há concorrente). NÃO trocar `None` por outro valor (mudaria dados/dtype). Tratamento: `# type: ignore[call-overload]` PONTUAL na linha do `np.where(` (numpy stubs não cobrem ramo `None`). VALIDAR via hash M1 que nada mudou (este módulo alimenta enriquecimento; embora não-oficial, o gate de hash cobre os 4 artefatos).

### Grupo B — coerção `float/int(object)` viva (helpers e relatórios chamados por testes)
Sites: `normalizar_unidades_ultra:158`, `gerar_carteira_acionavel:146,155`, `comparar_geofusion_vs_hex:123`, `modelo_hibrido_expansao:102,110`, `validar_penetracao_ultra_hex:110,116,123`, `fase_a_piloto_expandido:142,157,159,570,610,803,805`, `fase_a_nacional_completo:59,185,193(×2),195,207,208,403(×2),404`.
Esses fazem coerção defensiva sobre células `object`/`dict[str,object]`, já guardadas por `pd.isna`/lógica. O repo JÁ adota a convenção `# type: ignore[code]` pontual nesses casos (ver outros `float()` nos próprios módulos). Tratamento PADRÃO: `# type: ignore[CÓDIGO]` PONTUAL na linha (com o código exato: `[arg-type]`, `[call-overload]`, `[str-format]`, `[attr-defined]`, `[index]`, `[misc]`, `[return-value]`), seguindo a convenção existente.
- PREFERIR, quando trivial e SEM mudar runtime, a alternativa de tipar a fonte: em `fase_a_nacional_completo`/`fase_a_piloto_expandido`, vários erros têm causa-raiz comum `summary: dict[str, object]` / `resumos: list[dict]`. Se o Builder conseguir tipar o dict de resumo com chaves conhecidas (ex.: `dict[str, float | int | str | None]`) ou usar `cast(float, x)`/`cast(str, x)` PONTUAL nos acessos, MUITOS erros caem de uma vez SEM ignore e SEM mudar runtime. DECISÃO: Builder tenta primeiro `cast`/anotação da fonte (preferível); cai para `# type: ignore[code]` pontual onde o cast não for trivial ou puder mascarar. `from typing import cast` quando usar cast. JAMAIS reescrever a lógica (ex.: trocar `int(x)` por outra expressão).

### Grupo C — sites onde a anotação não pode mudar comportamento e o código tem cheiro de legado
- `validar_fase_a_censo2022.py:982` (`join(object)`): se o valor for de fato iterável de str em runtime mas tipado `object`, usar `cast` ou `# type: ignore[arg-type]` pontual. NÃO mudar a lógica do join.
- `gerar_carteira_acionavel.py` linha 157 (`default` indefinido): BUG de runtime latente, NÃO é erro de tipo, FORA de escopo — NÃO consertar neste ciclo (registrar como observação ao QA; mexer nele seria mudança de comportamento). Se o Builder julgar imprescindível, abrir nota — mas o default é deixar intacto.
> Regra geral do Grupo C: silenciar o TIPO, nunca reescrever a lógica buggy.

## Plano técnico (lotes; após CADA lote: `mypy src/` parcial + `pytest -q` + `ruff check .`)
**Passo 0 (Builder — baseline e não-mutação):**
0.1 Confirmar branch `ciclo/BLK-ARCH-01b`, worktree limpo.
0.2 Recapturar sha256 dos 4 artefatos M1 (esperados na seção de aceite) como baseline PRÉ.
0.3 Verificar paridade de stubs com o CI (`.github/workflows`). Re-rodar `mypy src/` com o override removido temporariamente para travar a lista de 50 erros; restaurar o override (só remover de vez no Passo final, ou remover por módulo já-limpo).
0.4 `pytest -q` baseline (~541 passed, 1 skipped).

**Lote 1 — Trio Fase A + validar_fase_a (concentra os no-redef e os relatórios vivos):**
- `fase_a_censo2022_setores.py`: no-redef :36 (ignore no except import); assignment :720/723/730/738 (`Path | None`); var-annotated :778/1192 (`dict[str, Any]`).
- `validar_fase_a_censo2022.py`: no-redef :34 (×11, ignore no except import + noqa F811 se preciso); :982 (cast/ignore).
- `fase_a_piloto_expandido.py`: :142/157/159/570/610/803/805 (Grupo B — cast preferido / ignore pontual).
- `fase_a_nacional_completo.py`: :59/185/193×2/195/207/208/403×2/404 (Grupo B — preferir tipar `summary`/`resumos` ou cast pontual).
Validar com `test_fase_a_*` verdes.

**Lote 2 — Geo censitário:** `materializar_setores_censitarios_geo.py:29` (×2 no-redef — ignore no except import). Não tocar `try/except` nem helpers. Confirmar ruff/F811 limpo.

**Lote 3 — Dupla penetração:** `validar_penetracao_ultra_hex.py:110/116/123` (Grupo B). `calcular_penetracao_ultra_hex` NÃO aparece na lista de 50 — confirmar limpo (pode ter sido tipado corretamente já). Se limpo, só remover do override.

**Lote 4 — Mercado/enriquecimento/geofusion:** `enriquecimento_espacial_hexagonos.py:134` (Grupo A — `# type: ignore[call-overload]`, validar hash), `comparar_geofusion_vs_hex.py:123` (Grupo B). `calcular_colunas_mercado` NÃO aparece nos 50 — confirmar limpo e só remover do override.

**Lote 5 — Ultra/carteira/híbrido/validação:** `normalizar_unidades_ultra.py:158` (Grupo B), `gerar_carteira_acionavel.py:146/155` (Grupo B), `modelo_hibrido_expansao.py:102/110` (Grupo B — `# type: ignore[arg-type]` nos `float()` de `float|None`), `validar_modelo_ultra.py:265/286` (Grupo A — anotar dicts).

**Passo final:**
F.1 Remover o bloco `[[tool.mypy.overrides]]` INTEIRO de `pyproject.toml` (`[[tool.mypy.overrides]]` + `module=[...]` + `ignore_errors=true`, linhas ~107-124) e o comentário-cabeçalho órfão (linhas ~102-106). NÃO relaxar `[tool.mypy]` global.
F.2 `mypy src/` → 0 errors (no ambiente de stubs do CI).
F.3 `pytest -q` → contagem igual ao baseline.
F.4 `ruff check .` → limpo.
F.5 Recapturar sha256 dos 4 artefatos M1 e comparar com PRÉ (idênticos).
F.6 `python -c "import streamlit_app"` → ok.
F.7 Commit POR PATH (nunca `git add -A`): módulos tocados + `pyproject.toml` + arquivos de controle. Atualizar `tasks/current_task.md` (Skill atual: Builder → Próxima: QA), `tasks/completed.md`/`backlog.md`.

## Arquivos afetados
- `src/motor_expansao/pipelines/normalizar_unidades_ultra.py`
- `src/motor_expansao/pipelines/gerar_carteira_acionavel.py`
- `src/motor_expansao/pipelines/comparar_geofusion_vs_hex.py`
- `src/motor_expansao/pipelines/modelo_hibrido_expansao.py`
- `src/motor_expansao/pipelines/enriquecimento_espacial_hexagonos.py`
- `src/motor_expansao/pipelines/validar_modelo_ultra.py`
- `src/motor_expansao/pipelines/validar_penetracao_ultra_hex.py`
- `src/motor_expansao/pipelines/materializar_setores_censitarios_geo.py`
- `src/motor_expansao/pipelines/fase_a_censo2022_setores.py`
- `src/motor_expansao/pipelines/validar_fase_a_censo2022.py`
- `src/motor_expansao/pipelines/fase_a_piloto_expandido.py`
- `src/motor_expansao/pipelines/fase_a_nacional_completo.py`
- `src/motor_expansao/pipelines/calcular_colunas_mercado.py` (provável só confirmar limpo + remover do override)
- `src/motor_expansao/pipelines/calcular_penetracao_ultra_hex.py` (idem)
- `pyproject.toml` (remover o bloco `[[tool.mypy.overrides]]`)
- `tasks/current_task.md`, `tasks/backlog.md`, `tasks/completed.md`
- `context/handoff.md`, `context/handoff/`
- (eventuais testes em `tests/` SÓ se anotação exigir narrowing — improvável)

## Dependências
- `mypy` (ambiente 2.1.0), `pandas` 2.3.3, `types-requests` instalado. NÃO adicionar stub novo ao `pyproject` (fora de escopo). Paridade CI/local é responsabilidade do Builder.
- BLK-ARCH-01a já mergeado (satisfeito).

## Riscos técnicos
- **Paridade `pandas-stubs` CI vs local**: localmente os 50 já aparecem sem pandas-stubs; se o CI usa pandas-stubs, podem surgir erros ADICIONAIS de Series. Mitigação: Builder replica o ambiente do CI antes de declarar limpo.
- "Consertar tipos" mudar runtime (cast que altera valor, narrowing que muda fluxo). Mitigação: gate sha256 + `pytest -q`; cast/anotação apenas, nunca reescrever lógica.
- Anotação errada mascarar bug (ex.: `gerar_carteira_acionavel:157` `default` indefinido). Mitigação: política Grupo C — não consertar lógica; só silenciar tipo onde aplicável.
- `# type: ignore[no-redef]` no except import poderia esconder import quebrado. Mitigação: pytest cobre o import real; o fallback é runtime-correto e idiomático.
- Remover módulo do override antes de limpo quebra `mypy src/`. Mitigação: remover por último (F.1) ou só após passar isolado.

## Critérios de aceite finais
- `mypy src/` LIMPO (0 errors) COM `[[tool.mypy.overrides]]` REMOVIDO — no MESMO ambiente de stubs do CI.
- `pytest -q` verde, mesma contagem do baseline (~541 passed, 1 skipped).
- `ruff check .` → All checks passed.
- `python -c "import streamlit_app"` → sem erro.
- sha256 IDÊNTICO pré/pós nos 4 artefatos M1:
  - `data/staging/brasil_priorizados.parquet` = `c226954945ad0757a0429c84c43f410492c0ea7d15ca1d9b6a15f68727806567`
  - `data/staging/brasil_estrutural.parquet` = `7baa07a2cbc0b7d8f2a8878932cae0ebf9400fce00e9c48c366c9903215f131b`
  - `data/staging/hexagonos_brasil_oportunidades.parquet` = `805c65e28adfd800c7d5524e73fa9b0a044b992f17d617d0f2c6e40f9bbf61ca`
  - `data/outputs/hexagonos_brasil_dashboard.parquet` = `0cfb1015fc9df8b63776eb07ae3e666766905f768a4a78a406ae4d6b7cb6f618`
- `# type: ignore` apenas PONTUAL e justificado; ZERO blanket ignore; config global de mypy inalterada.

## Validações obrigatórias (Builder e QA)
- `python -m mypy src/` (override REMOVIDO) → `Success: no issues found` (0 errors), no ambiente do CI.
- `python -m pytest -q` → mesma contagem do baseline (~541 passed, 1 skipped), 0 falhas.
- `ruff check .` → All checks passed.
- `python -c "import streamlit_app"` → sem exceção.
- Recomputar sha256 dos 4 parquets → idênticos aos esperados.
- `pyproject.toml` sem bloco `[[tool.mypy.overrides]]`; `[tool.mypy]` global intacto.

## Fora de escopo
- Alterar comportamento, valores, dtypes de saída, ordenação.
- Tocar `core/scoring.py`, `score_priorizacao`, `hex_score_estrutural`, pesos, carteira, plano curto prazo, plano de domínio, artefatos M1.
- Consertar bugs de runtime latentes (ex.: `gerar_carteira_acionavel:157` `default`); reescrever lógica de relatórios; remover funções dead.
- Adicionar `pandas-stubs`/`types-*` como dependência versionada.
- `# type: ignore` em massa; relaxar config global de mypy.
- Avançar para outro bloco.

## Alerta de aprovação
Criticidade MÉDIA — SEM gate humano. Próxima Skill: Builder diretamente. (Bloco NÃO toca score/carteira/plano/artefatos M1; não-mutação é critério de aceite duro via sha256.)

## Guardrails ativos (do CLAUDE.md)
- Correção mecânica: cada anotação/cast reflete o tipo REAL em runtime. Sem mudança de comportamento/valores/dtype/ordenação.
- NÃO recalcular nem alterar `score_priorizacao`, `hex_score_estrutural`, carteira, plano curto prazo, plano de domínio ou artefatos oficiais do M1.
- Ler o repositório real antes de editar; `config.py`, `CLAUDE.md`, `PRD.md` canônicos.
- Toda mudança relevante entra com teste; nenhum PR sobe com CI quebrado.
- Preservar 100% das linhas/colunas oficiais do M1.
- Commit POR PATH; NUNCA `git add -A`.
- Parâmetros canônicos imutáveis: H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, pesos renda=0.40/pop=0.60.

## Nota operacional do Planner
Recon executado: override removido temporariamente → `Found 50 errors in 12 files (checked 44 source files)` (CONFIRMA o Block Orchestrator). `pyproject.toml` RESTAURADO ao original (git diff LIMPO, EXIT_DIFF=0). Nenhum arquivo de código alterado pelo Planner. Único output: este handoff (2 cópias).
