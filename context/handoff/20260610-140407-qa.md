# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador executa housekeeping `scripts/housekeeping_move_block.py BLK-SAM-01` APÓS este veredito)

## VEREDITO
APROVADO

## Justificativa
Os 6 critérios de aceite do BLK-SAM-01 estão atendidos pelo código; a suíte completa passou (685 passed,
5 skipped, 0 failed), ruff e mypy limpos, DEC-006 registrada, e o gate do SAM foi reescrito exatamente como
`flag_viavel & faixa∈{baixa,media,alta,prioridade_maxima} & flag_pop_min_5k(≥5000) & ~flag_canibalizacao_ultra_1km`
com a régua de população EXTRAÍDA byte-a-byte para `pipelines/pop_corte.py` (fonte única, sem ciclo de import).
O M1 está intocado: o `git diff` não toca `pipelines/m1/`, `scoring.py`, `config.py`, `dashboard/constants.py`
nem artefatos `.parquet` oficiais; pesos/H3/distância/renda canônicos inalterados.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- `calcular_colunas_mercado.py:395` emite `FutureWarning` de `.fillna` (downcasting object dtype). É código
  PRÉ-EXISTENTE (`som = df["som_indice_mapeado"].fillna(0.0)`), NÃO tocado por este bloco; sugestão de limpeza
  futura, fora de escopo do BLK-SAM-01.

## Testes faltantes
- nenhum. Os 3 testes novos cobrem (i) repro de zeragem granular `87a91b18dffffff`, (ii) hex que passa a
  calcular fora do `top_municipio` antigo, (iii) o gate como conjunção paramétrica dos 4 critérios. O guardrail
  de schema sobre o parquet real (`test_parquet_final_tem_schema_minimo`) skipa as 3 colunas pendentes de
  regeneração de forma EXPLÍCITA (não silenciosa), exigindo as colunas estáveis — comportamento correto.

## Riscos remanescentes
- Enquanto os parquets paralelos não forem regenerados (passo OPERACIONAL pós-merge, fora do fechamento), o
  dashboard de produção continua servindo o `flag_sam` antigo (196.715). O CÓDIGO já está correto e verificado;
  é pendência operacional documentada na DEC-006 e no handoff do Builder. Sem risco ao M1.
- `tese_entrada`/`sam_granularidade` ainda referenciam `top_municipio` (intacto de propósito; `top_municipio`
  saiu apenas do gate do SAM, segue existindo como coluna). Coerente com o plano aprovado.

## Saída literal das validações (re-executadas pelo QA)

### `-n auto` (tentativa única para registrar o erro de ambiente — Windows + Python 3.14)
```
python -m pytest -q -p no:cacheprovider -n auto tests/integration/test_modelo_mercado_hexagonos.py
→ processo TRAVOU no sessionstart, 0 bytes de stdout (gateway execnet/xdist não inicializa);
  terminado via TaskStop. Falha de AMBIENTE documentada no BLK-ORQ-01 (execnet EOFError em Win/Py3.14),
  NÃO defeito do bloco. Fallback SERIAL abaixo é o gate equivalente.
```

### `python -m pytest -q -p no:cacheprovider tests/integration/test_modelo_mercado_hexagonos.py`
```
20 passed, 4 skipped, 9 warnings in 5.52s
(4 skips: 3 guardrails que dependem do parquet real pré-regen + 1 fixture de score que exige
 oportunidades_expansao_hibrido.parquet; warnings pré-existentes, incl. FutureWarning na linha 395 NÃO tocada)
```

### `python -m pytest -q -p no:cacheprovider tests/integration/test_streamlit_app.py`
```
179 passed, 1 warning in 11.13s
```

### `python -m pytest -q -p no:cacheprovider`  (SUÍTE COMPLETA — gate único do ciclo, serial)
```
685 passed, 5 skipped, 17 warnings in 94.51s (0:01:34)
exit code 0
```

### `python -c "import streamlit_app; print('import ok')"`
```
import ok
(precedido por WARNINGs benignos de "missing ScriptRunContext"/"No runtime found" do Streamlit em modo bare)
```

### `python -m ruff check src/ tests/`
```
All checks passed!
```

### `python -m mypy src/motor_expansao/pipelines/pop_corte.py src/motor_expansao/pipelines/calcular_colunas_mercado.py src/motor_expansao/dashboard/data.py`
```
Success: no issues found in 3 source files
```

### `git diff --name-only HEAD` + scope check
```
CLAUDE.md
context/handoff.md
docs/modelo_mercado_hexagonos.md
src/motor_expansao/dashboard/data.py
src/motor_expansao/pipelines/calcular_colunas_mercado.py
tasks/current_task.md
tests/integration/test_modelo_mercado_hexagonos.py
(untracked: src/motor_expansao/pipelines/pop_corte.py — NOVO helper esperado; + snapshots de handoff)

SCOPE CHECK — grep por pipelines/m1/, scoring.py, config.py, dashboard/constants.py, *.parquet → VAZIO.
Nenhum arquivo M1/oficial ou artefato .parquet alterado. ESCOPO RESPEITADO.
```

### Verificação independente de volume (read-only, parquet real, SEM escrita)
```
total linhas = 1542531
flag_sam ANTES (parquet atual)        = 196715
flag_sam DEPOIS (régua nova in-memory)= 27996   (delta −168719)
fonte_populacao_corte = {'total_municipal': 802007, 'setor_2022': 740524}
```
Reproduzido pelo QA aplicando `derive_confianca_geografica` + `derive_pop_cut_columns(pop_min=5000)` +
gate `flag_viavel & faixa∈{…} & flag_pop_min_5k & ~canibal` sobre o parquet real. Bate EXATAMENTE com a
sanidade do Builder e com a DEC-006.

### Housekeeping (informativo — NÃO bloqueia; o move é do orquestrador no fechamento)
```
python scripts/housekeeping_move_block.py BLK-SAM-01 --check
→ FALHA --check: stub ausente no backlog para BLK-SAM-01   (ESPERADO — o move ainda não ocorreu)

python -m pytest -q -p no:cacheprovider tests/unit/test_housekeeping_helper.py
→ 10 passed, 1 warning in 0.03s
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho
crítico. A suíte `pytest` usa fixtures de teste LEGÍTIMAS (regra (b) do prompt: fixture de teste comum NÃO é
bypass). O caminho crítico real (`calcular()` do pipeline de mercado) é exercitado diretamente pelos testes —
o repro `87a91b18dffffff` roda `calcular(df).iloc[0]` e verifica `confianca_geografica=='granular'`,
`populacao_corte_hex≈35.4`, `flag_sam=False`, `sam_fitness_potencial=0`. Além disso, a verificação de volume
foi feita contra o PARQUET REAL de produção (1.542.531 linhas), não fixture. A não-regeneração dos parquets é
passo operacional pós-merge explicitamente fora do fechamento (conforme plano aprovado e DEC-006), NÃO um
contorno do caminho crítico.

## Conferência cruzada com log do Builder
BATE. Builder reportou: mercado 20 passed/4 skipped, streamlit 179 passed, import ok, ruff limpo, mypy limpo,
volume 196.715 → 27.996 (740.524 setor_2022 + 802.007 total_municipal). QA re-executou tudo por conta própria
e obteve resultados idênticos. A suíte FULL (gate do QA, não rodada pelo Builder) deu 685 passed / 5 skipped /
0 failed. A extração byte-a-byte foi confirmada comparando contra `git show HEAD:.../data.py`: os corpos de
`has_censo_signal`/`derive_confianca_geografica`/`derive_pop_cut_columns` em `pop_corte.py` são idênticos ao
original (só mudou o default do `pop_min` para o literal `5_000` == `POP_MIN_ACIONAVEL`; o wrapper do dashboard
preserva `POP_MIN_ACIONAVEL`). A delegação em `data.py` é puramente import + retorno de 1 linha (lógica do
dashboard preservada; assinaturas públicas intactas). Sem ciclo de import (`pop_corte` importa só numpy/pandas).

## Guardrails verificados
- score_priorizacao não alterado: **sim** (git diff não toca scoring/config/m1/constants; `faixa_oportunidade`
  é LIDA, não recalculada — DEC-001 respeitada).
- hex_score_estrutural não alterado: **sim**.
- Artefatos M1 preservados: **sim** (nenhum `.parquet` no diff; volume verificado em modo read-only, zero escrita).
- Parâmetros canônicos §3: H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, pesos renda=0.40/pop=0.60
  **INALTERADOS** (`config.py` fora do diff). `POP_MIN_SAM_GATE=5_000` é constante LOCAL da camada de mercado,
  não entra no §3.
- Dashboard offline mantido: **sim** (`pop_corte.py` só numpy/pandas; smoke import + 179 testes de streamlit verdes).
- Validações re-executadas pelo QA (sem bypass):
  - test_modelo_mercado_hexagonos.py → 20 passed, 4 skipped
  - test_streamlit_app.py → 179 passed
  - SUÍTE COMPLETA → 685 passed, 5 skipped, 0 failed
  - import streamlit_app → ok
  - ruff src/ tests/ → All checks passed!
  - mypy (3 arquivos) → Success: no issues found
  - git diff scope check → VAZIO em m1/scoring/config/constants/.parquet
  - volume real read-only → 196715 → 27996 (bate com DEC-006)
  - test_housekeeping_helper.py → 10 passed
- Escopo respeitado: **sim** (7 arquivos versionados + 1 novo helper, todos previstos no plano; nenhum excesso).

## Decisão recomendada
fechar ciclo (após o orquestrador rodar `scripts/housekeeping_move_block.py BLK-SAM-01` e a regeneração
operacional pós-merge dos parquets paralelos na ordem canônica).
