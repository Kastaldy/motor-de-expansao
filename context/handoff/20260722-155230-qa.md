# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer (Opus 4.8, regra dura do orquestrador)

## Próxima Skill recomendada
**GATE VISUAL do Vinicius** (z19 vs z18 / R1) e, depois dele, **Fechamento manual** — commit por path
no branch `ciclo/BLK-RELPON-11`, sem PR (RELPON-09/10/11 entram num PR único).

## VEREDITO
**APROVADO**

## Justificativa
Auditei o ciclo por evidência própria (não pelos logs do Builder): a suíte FULL serial fecha em
`1 failed, 1963 passed, 2 skipped`, com a ÚNICA falha sendo a ambiental pré-existente
(`test_run_readonly_m1_por_mtime`, parquet de staging gitignored ausente, diff ZERO sobre
`lifetime/`); provei por worktree limpo em `HEAD` que as **7 camadas pré-existentes saem
byte-idênticas (sha256)** e que `entorno` é a única chave nova; gerei eu mesmo os 2 PDFs offline e
confirmei `/Count 8` nas duas variantes; e o `loop_guard` sobre o diff acusa **zero violação de
classe `critico`** (nenhum artefato M1, `config.py`, `pipelines/` ou `constants.py` tocado). Os 2
problemas que encontrei são de precisão de relato e de dívida documental, não de comportamento.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
1. **O relato de `mypy` do Builder está errado — o número real é 7, não 6, e é EXATAMENTE a
   baseline.** O Builder reportou "6 erros, nenhum nos módulos tocados" e sugeriu que o
   `current_task.md` (que dizia 7) estivesse desatualizado. Medi os dois lados: com o **cache
   incremental do repo** (`.mypy_cache` existente) dá 6; com **cache limpo** dá **7**, e o worktree
   limpo de `HEAD` também dá **7**, com a lista `file:line` IDÊNTICA. O erro que "sumia" era
   `src/motor_expansao/dimensionamento/growth_api_client.py:28`, suprimido pelo cache stale. Além
   disso, **um dos 7 ESTÁ num arquivo tocado** (`src/motor_expansao/api/service.py:269`) — mas é
   pré-existente, mesma linha e mesma classe da baseline, então **zero erro NOVO**. Impacto: nenhum
   sobre o código; é acurácia de evidência. O `current_task.md` estava certo e não deve ser
   "corrigido" para 6.
2. **3 docs da API passaram a mentir a contagem de páginas** (achado do próprio Builder, que
   confirmei): `docs/api_geoespacial_contrato.md:14,66`, `docs/api_geoespacial_openapi.yaml:37,56` e
   `docs/api_geoespacial_uso.md:15,110` dizem "PDF de 7 páginas" (agora são 8). Confirmei também que
   o argumento do Builder para deixá-los fora procede: `uso.md:166` já listava a estrutura
   pré-BLK-RELPON-01 ("Capa → População → Renda → Score censitário → Concorrentes") e
   `docs/relatorio_pontual_censitario.md` já dizia "estrutura de 6 paginas" / "tira 1x3" /
   "4 camadas" ANTES deste bloco — um conserto parcial os deixaria internamente contraditórios. Não
   bloqueia, mas **este ciclo aumenta a dívida** e o fechamento TEM de registrar o
   **BLK-RELPON-12 (de-staling) com escopo ampliado para os 4 arquivos**.

## Melhorias opcionais
1. **5 nomes de teste ficaram mais stale ainda** (dizem "6 paginas"/"count_6" quando a realidade já
   é 8): `test_classico_gera_6_paginas_e_secoes` (export:492),
   `test_classico_sem_fotos_mantem_6_paginas` (fotos:185),
   `test_classico_sem_info_mantem_6_paginas` (info_imovel:73),
   `test_dispatcher_regressao_sem_params_novos_count_6` (orquestracao:73) e
   `test_sem_viabilidade_mantem_6_paginas` (viabilidade:83). O Planner os excluiu EXPLICITAMENTE do
   escopo ("dívida de higiene separada"), então o Builder agiu certo — mas vale juntar ao
   BLK-RELPON-12.
2. **T9 é a prova mais fraca do lote.** `test_camadas_existentes_ficam_byte_identicas_com_os_defaults_novos`
   só compara "default explícito == default implícito" na MESMA árvore, o que é quase tautológico:
   ele não pegaria uma mudança que afetasse os dois caminhos igualmente. O Planner ofereceu as duas
   variantes e o Builder escolheu a permitida, então não é violação de plano — e eu fechei a lacuna
   por fora (sha256 contra worktree de `HEAD`, ver abaixo). Sugestão: numa próxima inserção de
   camada, congelar os sha256 do pai como literais.

## Testes faltantes
- nenhum bloqueador. Todos os 15 itens T1..T15 do plano existem; os pontos que o ciclo declarou
  travar por teste estão travados, incluindo a armadilha do `MAP_LAYER_TITLES`
  (`assert "entorno" in chaves` + `dict(_normalize_mapas_by_key({"entorno": b"E"})) == {"entorno": b"E"}`)
  e as travas anti-vácuo de T2 (`socioeconomia` DEVE reagir a pontos) e T4 (`densidade` DEVE ter
  pixels de círculo).

## Riscos remanescentes
- **R1 — legibilidade em z19 (vai ao gate visual; ver meu parecer abaixo).** Quantifiquei: o rótulo
  cai a **3,0-3,3 pt** na variante recente e a **2,6-2,9 pt** na CLÁSSICA (que é a que produção
  entrega). Não bloqueia o merge; bloqueia a utilidade da página.
- **R2** — Voyager esparso fora de capital (limitação do provedor).
- **R4** — +~25-36 tiles por relatório a frio no bot (~+10%).
- **R5** — `data/cache/basemap_tiles/` não é volume no `docker-compose.prod.yml` (pré-existente).
- **R6** — ordem narrativa com fotos + informações do imóvel presentes (UX, gate visual).
- **R7** — barra de escala derivada de 3857, levemente otimista (PRÉ-EXISTENTE, não corrigir).
- **PR em lote:** o `loop_guard` acusa 4 paths de classe `governanca` neste ciclo
  (`CLAUDE.md`, `api/service.py`, `censo_map.py`, `censo_report.py`) — **nenhum `critico`**. O PR
  único vai herdar de BLK-RELPON-09 o `relatorio_municipal.py`, que É `critico` por path
  (DEC-011) → o PR combinado exigirá a label mais alta. Já previsto pelo BO; registrado aqui só para
  não ser surpresa no fechamento.

---

## Saída literal das validações (re-executadas pelo QA)

### 0. Nota de ambiente — a suíte rodou SERIAL, não `-n auto`
O `prompts/qa_analyzer.md` manda `python -m pytest -n auto`. Neste ambiente (Windows 11 /
Python 3.14) o `xdist` **aborta** com `INTERNALERROR`/`execnet`, então **substituí por
`python -m pytest -q` (SERIAL)**, conforme instrução explícita do orquestrador. Registro a
substituição de forma aberta: **isto não é bypass**. Bypass, na definição do §(b) da minha Skill, é
contornar config/artefatos REAIS de produção para fabricar verde; rodar serial em vez de paralelo é
uma restrição de ambiente que **amplia** o rigor (elimina flakiness de paralelismo) e não troca
nenhuma entrada real por fixture. Custo: 17 min 23 s em vez de ~5.

### 1. Suíte FULL, serial (gate único do ciclo)
```
$ python -m pytest -q
...
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 1963 passed, 2 skipped, 318742 warnings in 1043.12s (0:17:23)
```
A falha é a **ambiental pré-existente**, confirmada por evidência própria:
```
E   FileNotFoundError: [Errno 2] No such file or directory:
    'C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao\data\staging\unidade_territorio_retencao.parquet'

$ ls data/staging/unidade_territorio_retencao.parquet
ls: cannot access '...': No such file or directory      (parquet gitignored, ausente)

$ git diff --name-only | grep -E "lifetime|retencao"
  ZERO (nao tocado por este ciclo)
```

### 2. Reconciliação da contagem (nenhum teste sumiu ou foi silenciado)
```
$ python -m pytest --collect-only -q                 # working tree (BLK-RELPON-11)
1966 tests collected in 38.91s
$ python -m pytest --collect-only -q                 # worktree limpo em HEAD (091e002)
1955 tests collected in 40.95s

delta = +11  ==  11 funcoes de teste NOVAS
  (+def test_ = 16, -def test_ = 5, e cada uma das 5 removidas tem o seu par renomeado)
soma: 1 failed + 1963 passed + 2 skipped = 1966 coletados  [FECHA]
```
*(A baseline "1951 passed" citada pelo QA do RELPON-10 é de outra árvore; a comparação válida é
`HEAD` deste branch → working tree, medida acima.)*

### 3. Lint
```
$ python -m ruff check src tests
All checks passed!
```

### 4. Smoke de import/UI
```
$ python -c "import streamlit_app; print('import ok')"
2026-07-22 12:33:03.924 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
2026-07-22 12:33:03.926 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
import ok
```

### 5. Tipos — working tree vs baseline, com CACHE LIMPO dos dois lados
```
$ python -m mypy src --cache-dir <tmp>          # WORKING TREE (BLK-RELPON-11)
src\motor_expansao\dimensionamento\growth_api_client.py:28: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\ibge_censo.py:24: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\poi_enrichment.py:15: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\base_h3_brasil.py:31: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\geo.py:21: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\telegram_bot.py:23: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\service.py:269: error: Library stubs not installed for "requests"  [import-untyped]
Found 7 errors in 7 files (checked 113 source files)

$ python -m mypy src                            # BASELINE (worktree HEAD 091e002, cache novo)
  ... lista file:line IDENTICA ...
Found 7 errors in 7 files (checked 113 source files)
```
**Número real: 7 (== baseline), todos `types-requests`. Zero erro NOVO. Zero erro em
`censo_map.py`/`censo_report.py`.** O "6" do Builder veio do `.mypy_cache` stale do repo:
```
$ python -m mypy src                            # working tree, cache do repo (stale)
Found 6 errors in 6 files (checked 113 source files)     <- growth_api_client.py suprimido
```

### 6. PDF REAL offline, nas 2 variantes, gerado PELO QA (`basemap=False`, canvas 1280x760)
```
metodo do motor censitario : 'setor_censitario_intersecao_area_1p5km'
RAIO_CENSITARIO_DEFAULT_KM : 1.5
camadas produzidas         : ['concorrentes', 'densidade', 'entorno', 'renda', 'renda_domiciliar', 'score', 'socioeconomia']
CAMADAS_CENSITARIAS        : ['densidade', 'renda', 'score', 'renda_domiciliar', 'socioeconomia', 'residual', 'concorrentes', 'entorno'] (len=8)

[censitario] /Count=['8'] | 'Imagem do Entorno' nos bytes: True | imagens=16 | bytes=465954 | headers ausentes=[]
[classico  ] /Count=['8'] | 'Imagem do Entorno' nos bytes: True | imagens=18 | bytes=471999 | headers ausentes=[]

PDF_SECTION_HEADERS (8):
  [0] Relatório Pontual Censitário
  [1] Imagem do Entorno
  [2] Socioeconomia e Residual Fitness
  [3] Mapas de calor
  [4] Concorrentes
  [5] Perfil do Bairro/Distrito
  [6] Big Numbers
  [7] Realização
```
**8 páginas nas DUAS variantes, sem opcionais, 100% offline.** ✔

### 7. Byte-identidade das camadas PRÉ-EXISTENTES — prova INDEPENDENTE (worktree em `HEAD` + sha256)
Não aceitei o T9 do Builder como prova (ele só compara default explícito vs implícito na mesma
árvore). Criei um worktree limpo em `HEAD` (091e002), rodei o MESMO script de render nos dois `src`
e comparei sha256:
```
  concorrentes       IDENTICO
  densidade          IDENTICO
  renda              IDENTICO
  renda_domiciliar   IDENTICO
  residual           IDENTICO
  score              IDENTICO
  socioeconomia      IDENTICO
  so no HEAD : []
  so no WT   : ['entorno']
  VEREDITO byte-identidade das camadas pre-existentes: OK
```
As 2 mudanças de `_render_camada` (`circle_3857: BaseGeometry | None`, `rotulo_escala=None`) são de
fato **default-preserving**, e `entorno` é a **única** chave nova.

### 8. `_BASEMAP_ZOOM_BUMP` global e os call-sites de `_fetch_basemap`
```
$ grep -n "_BASEMAP_ZOOM_BUMP" src/motor_expansao/dashboard/censo_map.py
52:_BASEMAP_ZOOM_BUMP = 1                                   <- GLOBAL INALTERADA
635:    BLK-RELPON-11: `zoom_bump` sobrescreve o `_BASEMAP_ZOOM_BUMP` GLOBAL apenas nesta chamada
655:        bump = _BASEMAP_ZOOM_BUMP if zoom_bump is None else int(zoom_bump)

$ grep -n "_fetch_basemap(" src/motor_expansao/dashboard/censo_map.py
622:def _fetch_basemap(
1040:    basemap_tiles = _fetch_basemap(frame_3857.bounds, width) if basemap else None      <- pre-existente, SEM o param
1124:    basemap_tiles = _fetch_basemap(frame_3857.bounds, width, zoom_bump=0) if basemap else None   <- NOVO
1322:        basemap_tiles = _fetch_basemap(bounds_t, width)                                <- pre-existente, SEM o param
```
`zoom_bump` é keyword-only com default `None`; os 2 call-sites antigos não o passam → comportamento
idêntico (confirmado também pelo T5 e pela byte-identidade acima).

### 9. §5 READ-ONLY M1 — verificação própria, não a afirmação do Builder
```
$ git diff --name-only | grep -E "config\.py|pipelines/|scoring|constants\.py|relatorio_municipal|deploy/|Dockerfile|compose|Caddy|authelia|\.env|secrets/|\.github/"
NENHUM arquivo proibido no diff

$ git diff -U0 | grep -nE "^[+-].*(score_priorizacao|hex_score_estrutural|0\.40|0\.60|renda_pct_nacional|pop_pct_nacional)"
NENHUMA ocorrencia no diff

$ git status --porcelain src/motor_expansao/config.py src/motor_expansao/pipelines/
(vazio = intocados)

$ ls -la --time-style=full-iso <7 artefatos oficiais>
49109457 2026-06-10 10:24:10  data/outputs/hexagonos_brasil_dashboard.parquet
14779071 2026-06-10 10:24:11  data/outputs/hexagonos_mapa_sample.parquet
    1213 2026-06-10 10:24:10  data/outputs/resumo_por_uf.csv
   57068 2026-06-10 10:24:10  data/outputs/top_oportunidades_resumo.csv
35496625 2026-06-10 09:50:32  data/staging/brasil_estrutural.parquet
 7179409 2026-06-10 09:50:32  data/staging/brasil_priorizados.parquet
49278753 2026-06-10 09:50:34  data/staging/hexagonos_brasil_oportunidades.parquet
```
Todos com mtime **2026-06-10** (hoje é 2026-07-22) e tamanhos idênticos aos declarados. Parâmetros
canônicos intactos em `src/motor_expansao/config.py` (arquivo **não** consta do diff):
`H3_RESOLUTION=7`, `DIST_MIN_ULTRA_KM=1.0`, `RENDA_MIN=4500.0`, `M1_SCORE_OFICIAL="score_priorizacao"`.

### 10. `loop_guard` sobre os paths do ciclo (a classificação por CÓDIGO, não por prosa)
```
$ git diff --name-only | python scripts/loop_guard.py --stdin --json
{"limpo": false, "violacoes": [
  {"path": "CLAUDE.md",                                  "classe": "governanca", ...},
  {"path": "src/motor_expansao/api/service.py",          "classe": "governanca", ...},
  {"path": "src/motor_expansao/dashboard/censo_map.py",  "classe": "governanca", ...},
  {"path": "src/motor_expansao/dashboard/censo_report.py","classe": "governanca", ...}]}
```
**Zero violação de classe `critico`** (nada de `config.py`, `pipelines/m1/`, `*scoring*`,
`constants.py`, artefatos M1, `relatorio_municipal.py`, `deploy/`, compose, `secrets/`).

### 11. Motor censitário INTOCADO
```
$ git status --porcelain src/motor_expansao/dashboard/censo_point.py     -> (vazio)
$ grep -rn "RAIO_CENSITARIO_DEFAULT_KM = " src/
src/motor_expansao/dashboard/censo_point.py:23:RAIO_CENSITARIO_DEFAULT_KM = 1.5

Em runtime, no PDF que EU gerei:
  metodo do motor censitario : 'setor_censitario_intersecao_area_1p5km'
  RAIO_CENSITARIO_DEFAULT_KM : 1.5
```
Não há confusão entre os dois raios no código nem no texto do PDF: `RAIO_ENTORNO_DISPLAY_KM=0.14` é
constante de RENDER (`censo_map.py:133`, **fora** de `config.py` — confirmei
`hasattr(config,'RAIO_ENTORNO_DISPLAY_KM') is False`), o rodapé do PNG diz **"Escala de quadra"**
(não "Raio 0,1 km") e nenhum texto da camada começa com `"Raio "` (travado por T3).

### 12. `MAP_LAYER_TITLES` / `CAMADAS_CENSITARIAS` / faixas de §2
```
len(PDF_SECTION_HEADERS) = 8   |  [1] == 'Imagem do Entorno'
MAP_LAYER_TITLES chaves  = ['densidade','renda','score','renda_domiciliar','socioeconomia','residual','concorrentes','entorno']
MAP_LAYER_TITLES[0]      = ('densidade', 'População - Densidade')      <- contrato legado preservado
CAMADAS_CENSITARIAS      = (... ,'entorno')  len=8
_BASEMAP_ZOOM_BUMP       = 1
RAIO_ENTORNO_DISPLAY_KM  = 0.14   ->  lado curto do frame = 302.4 m   (janela 250-400, alvo ~300)
```
Cobertura de teste da armadilha do `_normalize_mapas_by_key` **existe**
(`test_map_layer_titles_inclui_as_chaves_novas_senao_os_pngs_sumiriam`, agora com
`assert "entorno" in chaves` e `dict(_normalize_mapas_by_key({"entorno": b"E"})) == {"entorno": b"E"}`).

### 13. §2 acentuação — strings novas, uma a uma
```
--- PDF (latin-1 safe, sem tipografia fora de latin-1) ---
  'Imagem do Entorno'   latin1=True  proibidos=[]
  PDF_SECTION_HEADERS[0..7]: latin1=True e proibidos=[] em TODAS as 8
  (checagem de — – • → … “ ” ‘ ’ © em cada string)
--- PNG (ASCII puro, excecao de RENDER) ---
  'Entorno - mapa de quadra'                              ascii=True
  'Ruas e quadras do entorno'                             ascii=True
  'Entorno imediato do ponto'                             ascii=True
  'Escala de quadra'                                      ascii=True
  'Escala de quadra - EPSG:3857 - fundo de ruas offline'  ascii=True
```
Identificadores novos **sem acento**: `"entorno"`, `RAIO_ENTORNO_DISPLAY_KM`, `_ENTORNO_TITULO_PNG`,
`_ENTORNO_VALOR_LINHA`, `_ENTORNO_LEGENDA_TITULO`, `_ENTORNO_ROTULO_ESCALA`, `_ENTORNO_TITULO`,
`_entorno_page`, `_classico_entorno_page`, `_render_camada_entorno`, `zoom_bump`, `rotulo_escala`,
`circle_3857`.

### 14. Rótulo não promete o que o Voyager não entrega
```
$ git diff -U0 -- src/ | grep -inE "^\+.*(satelit|satélite|aére|aere|ortofoto|POI|estabelecimento comercial|fachada)"
(so o COMENTARIO que diz "Nenhuma string promete satelite, imagem aerea ou POI comercial")
```
As 4 strings do PNG e o título do PDF descrevem **morfologia urbana** ("mapa de quadra",
"Ruas e quadras do entorno", "Entorno imediato do ponto", "Imagem do Entorno"). Nenhuma promete
satélite, imagem aérea, ortofoto nem POI comercial. ✔

### 15. Offline / dashboard sem rede
```
$ grep -n "import contextily" src/motor_expansao/dashboard/censo_map.py
645:        import contextily as ctx  # lazy: so existe com o extra [basemap]
(nenhum import de contextily em nivel de modulo em src/motor_expansao/dashboard/*.py)
```
`_render_camada_entorno` **não** tem `try/except` (correto: o fallback já é o canvas claro
incondicional dentro de `_fetch_basemap`) e devolve SEMPRE `bytes` → a chave nunca falta. Provado em
runtime pelo item 6 (PDF gerado com `basemap=False`, sem exceção, `/Count 8`).

### 16. Divergência "35 vs 39 asserts de `/Count`" — RESOLVIDA a favor do Builder
Contei os dois lados. A **prosa** do plano (risco R3) dizia "35 asserts", mas a **tabela** do próprio
plano lista **39 linhas**. O real no arquivo é:
```
arquivo                                        HEAD                       AGORA
export.py                                      19x '/Count 7'             22x '/Count 8'   (19 trocados + 3 asserts novos)
orquestracao.py                                2x7, 1x8, 1x9, 1x11        2x8, 1x9, 1x10, 1x12
info_imovel.py                                 1x7, 3x8, 1x9              1x8, 3x9, 1x10
fotos.py                                       1x7, 4x8                   1x8, 4x9
ui_relviab06.py                                1x11                       1x12
viabilidade.py                                 1x7, 1x8, 3x9, 1x11        1x8, 1x9, 3x10, 1x12
test_relatorio_municipal.py                    1x8, 7x9                   1x8, 7x9   <- INTOCADO
```
Total de substituições = **19+5+5+5+1+6 = 41 = 39 asserts + 2 docstrings**, exatamente o que o
Builder declarou. Inspecionei **todos** os pares `-`/`+` do diff: **todos são +1 exato**, nenhum
duplo-incremento, e **não sobrou nenhum `/Count 7`**. A tabela do Planner (39) contava só asserts;
os 2 extras são docstrings (1 em `export.py`, 1 em `orquestracao.py`). **Nada sobrou nem faltou.**

### 17. `test_relatorio_municipal.py` — intocado, isolado e verde
```
$ git status --porcelain tests/unit/test_relatorio_municipal.py
(vazio = INTOCADO)
$ grep -n "^from motor_expansao" tests/unit/test_relatorio_municipal.py
22:from motor_expansao.dashboard.relatorio_municipal import (   <- PDF_SECTION_HEADERS PROPRIA
```
Importa a constante do **próprio módulo** `relatorio_municipal`, não a de `censo_report` → zero
contaminação. `/Count 8` e `/Count 9` dele permaneceram intactos. Verde na suíte FULL.

### 18. EOL — nenhum CRLF espúrio sobrou
Medi byte a byte (o `grep -c $'\r'` em Git Bash dá falso positivo neste ambiente):
```
CLAUDE.md                                              CRLF= 187  LF-only=    0   <- estado ORIGINAL do working tree
src/motor_expansao/api/service.py                      CRLF=   0  LF-only=  600
src/motor_expansao/dashboard/censo_map.py              CRLF=   0  LF-only= 1529
src/motor_expansao/dashboard/censo_report.py           CRLF=   0  LF-only= 2317
tests/.../censitario_export.py                         CRLF=   0  LF-only=  923
tests/.../censitario_mapa.py                           CRLF=   0  LF-only= 1179
tests/.../fotos.py / info_imovel.py / orquestracao.py  CRLF=   0
tests/.../ui_relviab06.py / viabilidade.py             CRLF=   0
```
Os 6 arquivos que o script de churn converteu **estão de volta em LF** — a renormalização do Builder
funcionou. E o `--stat` reflete só as linhas realmente mudadas, nunca arquivos inteiros:
```
 CLAUDE.md                                        |   2 +-
 src/motor_expansao/api/service.py                |   2 +-
 src/motor_expansao/dashboard/censo_map.py        | 161 +++++++++++-
 src/motor_expansao/dashboard/censo_report.py     | 115 +++++++--
 tests/.../censitario_export.py                   | 137 +++++++---
 tests/.../censitario_mapa.py                     | 275 +++++++++++++++++++-
 tests/.../fotos.py                               |  10 +-
 tests/.../info_imovel.py                         |  10 +-
 tests/.../orquestracao.py                        |  10 +-
 tests/.../ui_relviab06.py                        |   2 +-
 tests/.../viabilidade.py                         |  12 +-
 11 files changed, 641 insertions(+), 95 deletions(-)
```

### 19. Escopo, commits e PR
```
$ git status --porcelain --untracked-files=all
 M CLAUDE.md
 M src/motor_expansao/api/service.py
 M src/motor_expansao/dashboard/censo_map.py
 M src/motor_expansao/dashboard/censo_report.py
 M tests/unit/test_relatorio_pontual_censitario_export.py
 M tests/unit/test_relatorio_pontual_censitario_mapa.py
 M tests/unit/test_relatorio_pontual_fotos.py
 M tests/unit/test_relatorio_pontual_info_imovel.py
 M tests/unit/test_relatorio_pontual_orquestracao.py
 M tests/unit/test_relatorio_pontual_ui_relviab06.py
 M tests/unit/test_relatorio_pontual_viabilidade.py
?? context/handoff/20260722-133000-block-orchestrator.md
?? context/handoff/20260722-134500-planner.md
?? context/handoff/20260722-152045-builder.md

$ git log --oneline -1
091e002 docs(BLK-RELPON-11): reabre o escopo com a pesquisa de alternativas
```
**Nenhum commit** feito pelo Builder, **nenhum PR** aberto, **nenhuma dependência nova**, **nenhuma
DEC nova**. Os 11 arquivos batem EXATAMENTE com a lista declarada (`context/handoff.md` e
`tasks/current_task.md` são gitignored, por isso não aparecem). `tasks/backlog.md` e
`tasks/completed.md` **intocados** (regime de fechamento em lote, ver Housekeeping).

### 20. Amostras do gate visual — existem, foram abertas, NÃO estão no repo
```
$ ls <scratchpad>
entorno_paulista_z19_bump0.png    440.517 B
entorno_paulista_z18_bump-1.png   548.127 B
amostra_relpon11_paulista_recente.pdf / _classico.pdf

$ git status --porcelain | grep -iE "png|pdf"      -> NENHUM
$ find . -iname "entorno_paulista*" -o -iname "amostra_relpon11*"  -> vazio (nao estao no repo)
$ ls data/reports/scratch | grep -i entorno         -> nada
```
Confirmo: os 2 PNGs e os 2 PDFs **não foram commitados nem colocados em `data/`**. Abri os dois PNGs
com a ferramenta Read (parecer abaixo).

---

## Parecer visual do QA sobre o R1 (z19 vs z18) — entra na recomendação, não decide

**O que eu efetivamente VI nas duas imagens:**

- **Campo de visão é o mesmo nos dois.** A claim do Builder procede e é estrutural: o frame é fixo em
  METROS por `RAIO_ENTORNO_DISPLAY_KM`; só o zoom do tile muda. Confirmei nos PNGs — "Praça Yoshiya
  Takaoka", "Praça Antônio Benetazzo" e o pin central caem praticamente nos mesmos pixels nas duas
  imagens. z18 **não** perde área.
- **z19 (`zoom_bump=0`, o que o código entrega hoje):** consigo ler os nomes de rua **só porque estou
  vendo o PNG em 1280 px nativos**, e ainda assim com esforço ("Rua Carlos Comenale", "Rua Plínio
  Figueiredo" estão no limite). Os **números de porta são ilegíveis já no PNG nativo** — e eles são
  justamente a justificativa declarada do z19. Há mais rótulos, sim, mas informação que não se lê não
  é informação.
- **z18 (`zoom_bump=-1`):** "Avenida Paulista", "Alameda Santos", "Rua Itapeva", "Avenida Nove de
  Julho", "Ciclovia da Avenida Paulista", "PRAÇA ANTÔNIO BENETAZZO" e os números de porta (724, 707,
  1776, 1682, 1578, 1600, 1500, 1445) são **confortavelmente legíveis**. Menos rótulos, todos
  utilizáveis. Footprints mais "blocados", porém a leitura de quadra/via fica intacta — que é o
  objetivo declarado da página.

**Aritmética que fecha com o que vejo** (render = 1,82 px/m; tile z19 = 3,65 px/m → redução 2,0x;
tile z18 = 1,83 px/m → 1:1). Medi a célula real de cada variante:
```
celula RECENTE  (x,y,w,h) = (92.6, 58.0, 774.7, 460.0)   escala PNG->PDF = 0.6053 pt/px
celula CLASSICA (x,y,w,h) = (146.5, 122.0, 666.9, 396.0) escala PNG->PDF = 0.5211 pt/px

  RECENTE   z19: glifo 10-11px no tile -> 3.0 - 3.3 pt no PDF
  RECENTE   z18: glifo 10-11px no tile -> 6.0 - 6.6 pt no PDF
  CLASSICO  z19: glifo 10-11px no tile -> 2.6 - 2.9 pt no PDF     <- variante que PRODUCAO entrega
  CLASSICO  z18: glifo 10-11px no tile -> 5.2 - 5.7 pt no PDF
```

**Meu parecer técnico:** **z18 é o legível no PDF final; z19 não é.** O argumento decisivo não estava
explícito no plano: a variante **CLÁSSICA** é a que produção entrega (download do dashboard + bot
Telegram) e é a **pior** das duas — a 2,6-2,9 pt, o rótulo de rua fica abaixo de qualquer limiar
utilizável em papel ou tela a 100%, e a página vira textura decorativa em vez de mapa de quadra. O
que se paga por z18 é densidade de rótulos (Voyager rotula por zoom), o que é muito mais barato do
que perder a legibilidade de todos eles. **A decisão é do Vinicius**; o código entrega z19 como
aprovado e a troca é `zoom_bump=0` → `zoom_bump=-1` em `censo_map.py:1124` (1 caractere), sem
nenhuma outra mudança e sem tocar a constante global.

*Nota para o gate:* como z19 e z18 têm o MESMO campo de visão, trocar não altera o enquadramento
nem `RAIO_ENTORNO_DISPLAY_KM`; muda só a nitidez do texto do tile. E se o Vini quiser manter z19, a
alternativa honesta é reduzir o campo (raio menor) para reaproximar render e tile — mas isso reabre a
janela de 250-400 m e não é escopo deste bloco.

---

## Housekeeping (Passo 6.0) — modo e razão
**Modo: entrega por COMMIT NO BRANCH, SEM PR** (decisão de Vinicius em 2026-07-21: RELPON-09/10/11
entram num PR único). O housekeeping de `backlog.md`/`completed.md` é do **fechamento em lote dos 3
blocos**, não deste ciclo isolado. Rodei o helper só para documentar o estado:
```
$ python scripts/housekeeping_move_block.py BLK-RELPON-11 --is-done
BLK-RELPON-11: ABERTO em tasks/completed.md          (exit 1)
$ python scripts/housekeeping_move_block.py BLK-RELPON-11 --check
FALHA --check: stub ausente no backlog para BLK-RELPON-11   (exit 1)

Precedente (mesmo lote, ja aceitos):
  BLK-RELPON-09 --check: FALHA (stub ausente)   --is-done: DONE
  BLK-RELPON-10 --check: FALHA (stub ausente)   --is-done: DONE
  headings ainda no backlog: L149 (09), L188 (10), L230 (11)
$ git status --porcelain tasks/     -> (vazio: backlog.md e completed.md INTOCADOS)
```
**Não reprovo por isso** — é exatamente o regime dos dois predecessores já aprovados: o stub do
backlog está **diferido de propósito** para o PR de lote, e a entrada de fechamento em
`completed.md` é acrescentada pelo **orquestrador no fechamento** (como foi feito para 09 e 10).
Ação para o orquestrador: acrescentar o heading `## Fechamento de ciclo — BLK-RELPON-11` em
`tasks/completed.md` e, no PR de lote, aplicar os 3 stubs de `backlog.md`.

## Conferência de no-bypass
**Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, nem mock do
caminho crítico.** Detalhando o que poderia parecer bypass e não é:
- **Suíte serial em vez de `-n auto`:** substituição de AMBIENTE, declarada no item 0. Não troca
  config/artefato real por nada; reduz flakiness. A suíte roda 1x, completa, como gate único.
- **`basemap=False` nos testes e no meu smoke de PDF:** é o **caminho de produção offline**
  (fallback gracioso da DEC-004), não um mock — o mesmo código que roda sem rede na VPS. As amostras
  do gate visual usaram **tiles REAIS** do Voyager.
- **`_FakeContextily` nos testes T5/T6:** stub do provedor EXTERNO de rede para capturar o `zoom`.
  Não é mock do caminho crítico deste bloco (a composição do PNG roda inteira, de verdade); é o
  padrão já usado pelos testes de basemap pré-existentes. E o zoom real foi verificado à parte, com
  tiles reais, nas amostras (`zoom` capturado = 19 e = 18).
- **Artefatos M1:** verifiquei mtime/tamanho dos 7 arquivos REAIS em disco, não uma fixture.
- **Byte-identidade:** comparei contra um **worktree git real de `HEAD`**, não contra hashes que o
  Builder tivesse escrito.

## Conferência cruzada com log do Builder
**Bate, com 1 divergência (favorável ao `current_task.md`) e 1 imprecisão:**

| Item | Builder | QA (evidência própria) | Veredito |
|---|---|---|---|
| `/Count` nas 2 variantes | 8 / 8 | 8 / 8 (PDF gerado pelo QA) | bate |
| Camadas produzidas | 7 chaves offline | idênticas | bate |
| `CAMADAS_CENSITARIAS` | 8 | 8 | bate |
| Artefatos M1 (mtime/tamanho) | inalterados, 2026-06-10 | idênticos, byte a byte de tamanho | bate |
| `ruff` | limpo | `All checks passed!` | bate |
| `import streamlit_app` | ok | ok | bate |
| 39 asserts + 2 docstrings de `/Count` | 41 substituições | 41, todas +1 exato, nenhuma sobrando | bate |
| EOL renormalizado p/ LF | sim | sim (0 CRLF nos 10 `.py`) | bate |
| Suíte FULL esperada | `1 failed, ~1966 passed, 2 skipped` | `1 failed, 1963 passed, 2 skipped` | bate (falha ambiental) |
| **`mypy`** | **6 erros, nenhum em módulo tocado** | **7 (== baseline); 1 deles em `api/service.py:269`, mas PRÉ-EXISTENTE** | **diverge — ver Problema médio 1** |
| "campo de visão idêntico z19/z18" | sim | sim (confirmado nos PNGs e por construção) | bate |
| T9 como prova de byte-identidade | suficiente | insuficiente por si; supri com sha256 vs `HEAD` | ressalva (ver Melhoria 2) |

## Guardrails verificados
- **score_priorizacao não alterado:** sim — zero ocorrência no diff; `config.py` e `pipelines/` fora
  do diff; `loop_guard` sem violação `critico`.
- **Artefatos M1 preservados:** sim — 7/7 com mtime `2026-06-10` e tamanhos idênticos.
- **Parâmetros canônicos preservados:** sim — `H3_RESOLUTION=7`, `DIST_MIN_ULTRA_KM=1.0`,
  `RENDA_MIN=4500.0`, pesos `renda=0.40`/`pop=0.60` (arquivo intocado).
- **Motor censitário INTOCADO:** sim — `censo_point.py` fora do diff,
  `RAIO_CENSITARIO_DEFAULT_KM=1.5`, método `setor_censitario_intersecao_area_1p5km` confirmado em
  runtime. O raio novo é de EXIBIÇÃO e está fora de `config.py`.
- **Camadas pré-existentes byte-idênticas:** sim — sha256 das 7 contra worktree de `HEAD`.
- **Offline/dashboard sem rede:** sim — `import contextily` lazy, fallback incondicional, PDF gerado
  sem rede com `/Count 8`.
- **§2 acentuação:** sim — PDF latin-1-safe sem tipografia proibida; PNG ASCII puro; identificadores
  novos sem acento.
- **Validações re-executadas pelo QA (sem bypass):** `pytest -q` (FULL, serial) → `1 failed
  (ambiental), 1963 passed, 2 skipped` · `pytest --collect-only -q` (2x, working tree e `HEAD`) →
  1966 vs 1955 · `ruff check src tests` → limpo · `mypy src` (2x, cache limpo, working tree e `HEAD`)
  → 7 == 7 · `import streamlit_app` → ok · geração de PDF real offline nas 2 variantes → `/Count 8` ·
  sha256 das 7 camadas vs `HEAD` → idênticas · `loop_guard --stdin --json` → 0 `critico` ·
  `housekeeping_move_block.py --is-done/--check` → estado de lote documentado.
- **Escopo respeitado:** sim — 11 arquivos, exatamente os declarados; nenhum commit, nenhum PR,
  nenhuma dependência nova, DEC-018 não aberta, `relatorio_municipal.py`/`pages.py`/`constants.py`
  intocados.

## Decisão recomendada
**Fechar ciclo** (após o GATE VISUAL do Vinicius), com 2 registros obrigatórios no fechamento:
1. **Criar `BLK-RELPON-12 — de-staling da documentação do Relatório Pontual`** com escopo AMPLIADO
   para 4 arquivos: `docs/relatorio_pontual_censitario.md` (já stale desde RELPON-01/05/07/10) +
   `docs/api_geoespacial_contrato.md` + `docs/api_geoespacial_openapi.yaml` +
   `docs/api_geoespacial_uso.md` (os 3 passaram a dizer "7 páginas" com este bloco). Somar a esse
   bloco a renomeação dos 5 nomes de teste stale ("6 paginas"/"count_6").
2. **Registrar no `completed.md`** o heading `## Fechamento de ciclo — BLK-RELPON-11` e deixar o stub
   de `backlog.md` para o PR de lote (mesmo tratamento de 09/10).

**Se o gate visual escolher z18:** é 1 caractere em `censo_map.py:1124` (`zoom_bump=0` →
`zoom_bump=-1`) + atualizar o comentário e o teste T6
(`test_entorno_pede_z19_em_todo_o_brasil` passaria a esperar 18 e a mudar de nome). Não precisa de
bloco de correção nem de novo ciclo — cabe como FU1 do próprio BLK-RELPON-11.
