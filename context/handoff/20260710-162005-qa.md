# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (merge humano do branch `ciclo/loop-20260710-123418`)

## VEREDITO
APROVADO

## Justificativa
As duas correções agora existem de fato no working tree e no diff: Fix 1 (transformer pyproj
compartilhado em `censo_map.py`, `_to_mercator`/`_point_to_mercator` preservadas para pins) e Fix 2
(`df_pre_filtrado` opcional em `agregar_municipio` + 4 callers pré-filtrando). Suíte completa verde
(1550 passed, 4 skipped = 1548 baseline + 2 testes novos), ruff/import/loop_guard limpos, housekeeping
feito via helper versionado, M1 INTOCADO. O único arquivo alheio (`dimensionamento/config.py`) é
CRLF→LF puro (zero diff com `--ignore-all-space`), incluído no commit com ressalva.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- `dimensionamento/config.py` regrediu CRLF→LF fora do escopo do ciclo. É whitespace-only e foi
  normalizado (benigno), mas idealmente o Builder teria feito `git restore` para manter o diff cirúrgico.
  Incluído no commit como normalização de line-ending (não bloqueia).
- Dois scripts scratch pré-existentes ficaram untracked (`scripts/_scratch_atr_alvo_alunos_totais.py`,
  `scripts/backtest_smartfit_scores.py`) — fora do escopo deste ciclo, deixados sem stage.

## Testes faltantes
- nenhum. Fix 1 coberto por byte-identity de PNG (`test_shared_transformer_bytes_identicos`); Fix 2
  por equivalência pré-filtro vs full-scan em 6 métricas (`test_agregar_municipio_prefiltrado_identico_ao_nacional`).

## Riscos remanescentes
- O harness de perf B6 (`scripts/perf_baseline_app.py`) não foi re-rodado neste ciclo (medição de ganho
  86× documentada é responsabilidade de validação de perf posterior / visual humana pós-merge, não
  bloqueia o gate funcional). A correção é matematicamente equivalente e coberta por testes de identidade.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte completa (`python -m pytest -q -n auto`)
```
1550 passed, 4 skipped, 19 warnings in 319.39s (0:05:19)
```

### 2. Smoke import (`python -c "import streamlit_app; print('import ok')"`)
```
...WARNING streamlit.runtime... (bare-mode, benignos)
import ok
```

### 3. Ruff (`ruff check censo_map.py relatorio_municipal.py pages.py service.py`)
```
All checks passed!
```

### 4. Loop guard (`python scripts/loop_guard.py --base HEAD`)
```
GUARD OK: 0 caminho(s) alterado(s), nenhum proibido.
```

### 5. Diff arquivo alheio (`git diff --ignore-all-space HEAD -- dimensionamento/config.py`)
```
(vazio — apenas CRLF→LF, ZERO diff de conteúdo com --ignore-all-space; git diff normal só warn de line-ending)
```

### 6a. Housekeeping check ANTES do move
```
FALHA --check: stub ausente no backlog para BLK-PERF-01a
```

### 6b. Housekeeping move + check DEPOIS
```
OK: movido BLK-PERF-01a (3278 bytes) -> tasks/completed.md; stub em tasks/backlog.md.
OK: BLK-PERF-01a movido (stub no backlog + bloco em completed.md).
```

### 7. Testes novos (targeted)
```
tests/unit/test_relatorio_pontual_censitario_mapa.py::test_shared_transformer_bytes_identicos PASSED
tests/unit/test_relatorio_municipal.py::test_agregar_municipio_prefiltrado_identico_ao_nacional PASSED
2 passed in 1.21s
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do
caminho crítico. A suíte roda a lógica real; os 2 testes novos exercitam os caminhos exatos das
correções (transformer compartilhado real e `df_pre_filtrado` real em `agregar_municipio`). Não há
tooling dependente de config de produção neste ciclo.

## Conferência cruzada com log do Builder
Bate. Builder reportou 295 passed no subconjunto impactado, ruff limpo, import ok — consistente. A
contagem completa (1550 passed vs 1548 baseline + 2 novos) confere. O diff dos 6 arquivos-alvo casa
exatamente o descrito; config.py confirmado CRLF-only como o Builder anotou.

## Guardrails verificados
- score_priorizacao não alterado: sim (root `config.py` e `pipelines/m1/` com ZERO diff)
- Artefatos M1 preservados: sim (nenhum parquet de staging/output tocado)
- Validações re-executadas pelo QA (sem bypass):
  - `pytest -q -n auto` → 1550 passed, 4 skipped
  - `import streamlit_app` → import ok
  - `ruff check` (4 arquivos) → All checks passed!
  - `loop_guard --base HEAD` → GUARD OK
  - `housekeeping --check` → OK (pós-move)
  - 2 testes novos targeted → 2 passed
- Escopo respeitado: sim — Fix 1 e Fix 2 conforme plano; `_to_mercator`/`_point_to_mercator`
  preservadas; único desvio é CRLF benigno em `dimensionamento/config.py` (incluído com ressalva).
- Parâmetros canônicos (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, renda=0.40/pop=0.60):
  INALTERADOS (nenhum arquivo de score/pesos tocado).

## Decisão recomendada
Fechar ciclo. Commit por path feito pelo QA. Merge do branch é passo humano.
