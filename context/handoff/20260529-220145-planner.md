# Handoff (snapshot) — Planner — BLK-OPS-10 (plano ajustado 2x: helper versionado + 3 notas)

## Skill que gerou este handoff
Planner (consolidado no orquestrador). Plano AJUSTADO 2x pelo humano.

## Próxima Skill recomendada
[APROVAÇÃO HUMANA] → Builder (somente após "aprovar")

## Bloco
BLK-OPS-10 — Alta; ALTERA A ORQUESTRAÇÃO + tooling versionado. Branch `ciclo/BLK-OPS-10`. dry_run: false.

## Arquivos-alvo (4 substantivos + bookkeeping)
- scripts/housekeeping_move_block.py        (NOVO — helper byte-idêntico, funções puras + CLI)
- tests/unit/test_housekeeping_helper.py    (NOVO — teste unitário)
- .claude/commands/run-cycle.md             (passo 6.0 + bullet no 6.c)
- prompts/qa_analyzer.md                     (checklist exigindo o helper)
NÃO tocar: M1, CLAUDE.md, conteúdo do backlog (além do move), score/artefatos.

## Plano (resumo)
- Helper: move_block(...)->(novo_backlog,novo_completed,moved); verify_moved(...); BlockNotFound se ausente.
  stub = "- {id} (concluído {date}) — ver tasks/completed.md".
- 6.0 (antes de 6.a): se bloco existe, chama helper + --check + pytest; falha → git restore + reportar.
  Ad-hoc → no-op. Entra no commit 6.a. 6.a–6.d preservados.
- 6.c: dry-run com bloco DUMMY na branch, valida, abandona sem merge. Guard dry_run:true intacto.
- qa_analyzer.md: exige --check + byte-identity vs git HEAD + pytest (com novo teste). Ad-hoc → N/A.

## NOTAS DE IMPLEMENTAÇÃO AO BUILDER (exigidas pelo humano)
1. BYTE-IDENTITY POR FATIA LITERAL: `moved = backlog[start:end]` (offsets de caractere), NÃO
   round-trip join/split. start = offset do heading `### {id}`; end = offset do próximo `### `/`## `
   (ou EOF) recuando sobre `---`/brancos finais.
2. CLI abre arquivos com `newline=''` (ou binário) p/ preservar CRLF no Windows (byte-identity disco↔memória).
3. TESTE CASO 6 (idempotência re-entrante): `move_block` 2x sobre o mesmo id → `BlockNotFound` na 2ª.

## Veredito do Planner
Apresentado ao humano para aprovação. Nenhuma edição nos arquivos-alvo ainda.
