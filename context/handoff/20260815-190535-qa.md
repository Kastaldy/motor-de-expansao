# Handoff — QA/Quality Analyzer

> Gerado em 2026-08-15, na branch `ciclo/BLK-MA-17`. Substitui o handoff do Builder (preservado em
> `context/handoff/20260815-181334-builder.md`). Cópia append-only deste em
> `context/handoff/20260815-190535-qa.md`.

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (Passo 6 do orquestrador, modo **MERGE-HUMANO**) — **com uma decisão pendente sobre
o housekeeping**, porque o BLK-MA-17 está entregue **pela metade** (ver "Pendências do Passo 6").

## VEREDITO
**APROVADO COM RESSALVAS**

## Justificativa

Re-executei **todas** as validações por conta própria contra o insumo real, e **cada número material
do Builder reproduziu** — inclusive os dois Spearman (`0,9911994` e `0,9865793`), as 12 trocas no
top-100 do entregável e as três réguas visíveis (`7.237` / `7.218` / `773`), aos dígitos publicados.
O escopo foi respeitado ao pé da letra (só a metade 2; nada em `web/`), o M1 ficou intacto e o
`test_14b` **provou ser trava de verdade**: com o `k` mutado para 1 ele falha, e no insumo real esse
`k` errado deixaria **193 unidades** de deduplicar em silêncio. As ressalvas são um defeito
**latente e fora do escopo ratificado** (DES-5, confirmado) e dois defeitos de **processo** nos
prompts da esteira — nenhum deles bloqueia o merge desta entrega.

## Problemas críticos (bloqueadores)

**Nenhum.**

## Problemas médios (não bloqueadores)

1. **DES-5 CONFIRMADO — `dedup_independentes` sub-cobre o bucket H3.** `pressao_competitiva.py:469`
   usa `h3.grid_disk(celulas[i], 1)` com `DEDUP_INDEPENDENTES_M = 50,0` m, quando a própria fórmula
   que o Builder introduziu neste bloco dá `_k_do_bucket(50) = 4`. **Efeito hoje: provadamente
   nulo** — medi `0 de 19.329` colapsos, porque a função só colapsa entre `fonte` DIFERENTES e o
   snapshot só tem WellHub. Na primeira coleta com TotalPass o defeito entra em massa, e o modo de
   falha é o pior possível: a duplicata NÃO colapsa, as duas linhas sobrevivem, e a gêmea a ~0 m
   soma `peso(0) x 0,5` -> até **+33,3 pontos** de pressão fantasma na academia — exatamente o
   fantasma que a DEC-033 criou aquela dedup para matar. **Não é regressão deste bloco** (o defeito
   é anterior, do BLK-MA-16) e o plano o pôs explicitamente fora de escopo. **Vira bloco de
   correção** — texto pronto ao final deste handoff (`BLK-MA-17-FU1`).

2. **Defeito de PROCESSO — a validação obrigatória `import streamlit_app` está MORTA há 12 dias.**
   Verifiquei: `30378a0` (`corte(BLK-WEB-20): remove o Streamlit — o piloto web e o unico app
   (DEC-022)`, 2026-08-03) removeu `streamlit_app.py`, `.streamlit/`, `Dockerfile.streamlit` e
   `tests/integration/test_streamlit_app.py`. `git ls-files | grep -c streamlit_app` = **0**. Mesmo
   assim, **três prompts da esteira ainda a exigem**: `prompts/builder.md` (L45, L46 e L49 — "o
   smoke `import streamlit_app` é OBRIGATÓRIO em todo ciclo"), `prompts/planner.md` (L74) e
   `prompts/qa_analyzer.md` (L25). É uma validação obrigatória que **não pode passar em nenhum
   ciclo** desde o corte — o Builder acertou em declarar (DES-4) em vez de silenciar. Rodei o
   equivalente vivo (pacote da camada + `web/server/app.py`), ambos OK.

3. **Defeito de PROCESSO — `prompts/qa_analyzer.md` manda rodar a suíte com `-n auto`, que é
   INEXECUTÁVEL neste ambiente.** Provei: `python -m pytest -n auto` termina em `INTERNALERROR` no
   `execnet` (`_winapi.DuplicateHandle` -> `OSError: [WinError 50] Não há suporte para o pedido`;
   xdist 3.8.0, pytest 8.4.2). O prompt vai além e exige *"confirme que a contagem é IDÊNTICA à do
   serial"* — instrução **insatisfazível**, porque não há execução paralela para comparar. Rodei
   **serial**, conforme instruído pelo orquestrador. Registro a divergência aqui porque ela é
   permanente e afeta todo QA futuro. (Nota: o orquestrador citou `WinError 6`; aqui o código é
   `WinError 50`. Mesma raiz — o gateway do execnet não consegue herdar o handle de stderr quando o
   processo roda sem console real —, código diferente conforme o modo de lançamento.)

## Melhorias opcionais

- **`dedup_cadeias_do_feed` não guarda `(fonte, chave_snapshot)` duplicado.** Provei em sonda: com
  a chave repetida a função não levanta, e `posicao_por_chave` guarda **só a última** posição — a
  auto-exclusão da outra linha apontaria para o índice errado. Efeito hoje **nulo**, porque o frame
  é uma fatia do mesmo frame de origens que `calcular_pressao_por_academia` **já** guarda
  (`if bool(base.duplicated(...)): raise`). É a mesma forma da `dedup_independentes` pré-existente.
  Uma linha de guarda simétrica fecharia.
- **O carimbo `universo_oferta` mente numa rodada só-cadeias-do-feed.** Sonda: passando **apenas**
  `cadeias_do_feed`, a saída sai `cadeias_e_independentes` com `oferta_independentes = 0,0`. É
  assimetria **declarada e testada** (DEC-034 item 6, comentário do enum, `test_12`), e a CLI sempre
  passa os dois frames juntos — mas a função pura permite a leitura errada.
- **`docs/decisions/DEC-033.md` ainda cita `com_independentes=True`** na seção "Execução da aprovação
  (2026-08-14)". É registro histórico correto, mas hoje é um símbolo morto (virou
  `com_oferta_do_feed` neste bloco) — uma nota de renomeação ao lado evitaria o grep frustrado.
- **`.gitattributes` `docs/*.md` é ANCORADO e não cobre `docs/decisions/*.md`.** A garantia de LF da
  DEC-017 para os arquivos de DEC depende hoje do `core.autocrlf` de cada máquina, não do repo.
  Medi que os blobs sairão em LF **nesta** estação, mas a regra não está no repositório.
- **Cross-reference off-by-one do Builder:** ele reporta *"448 passed (eram 427 antes do bloco)"*.
  Contei os testes por arquivo contra a base `16f8243`: `test_auditoria_pressao_no_pin.py` +1,
  `test_oferta_cadeias_do_feed.py` +21, os outros dois +0, **sem `parametrize`** em nenhum -> delta
  exato **+22**, logo o recorte era **426**, não 427. O número autoritativo (+22 na coleta) está
  certo; só a lembrança do "antes" estava errada em 1.

## Testes faltantes

- **Nenhum bloqueador.** A cobertura do bloco é a mais forte que já auditei nesta epic: os 21 testes
  novos cobrem os 4 sentidos do critério de dedup, os 2 casos da auto-exclusão, a dedup contra os
  pontos VÁLIDOS, a decomposição disjunta, o carimbo, o default, o grão hex, o determinismo, os dois
  contratos e a posição das colunas — mais 3 guardrails (universo do score, ausência de coordenada
  na saída, coluna obrigatória).
- **Sugestão (não bloqueia):** um teste para o caso de `(fonte, chave_snapshot)` duplicado em
  `cadeias_do_feed` (hoje passa em silêncio com o mapeamento errado) e um que trave
  `_k_do_bucket(DEDUP_INDEPENDENTES_M) == k` efetivamente usado por `dedup_independentes` — este
  segundo só faz sentido junto do `BLK-MA-17-FU1`.

## Riscos remanescentes

1. **A shortlist comercial reordena** — confirmei por medição própria: **12 das 100 primeiras** e
   **5 das 50 primeiras** linhas do `alvos_ma_priorizados.csv` trocam; `Spearman 0,9865793` no grão
   `(hex, regime)`. Anunciado na DEC-034 e no §8.1, com os 4 bumps.
2. **Contagem sem pin.** `n_conc` inclui unidades sem pin desenhado; a coluna nova DECLARA a lacuna,
   não a fecha. Só a metade 1 fecha.
3. **Artefatos em disco continuam na régua antiga.** `vulnerabilidade_ma_academias.parquet`,
   `vulnerabilidade_ma_nomeadas.parquet` e `alvos_ma_priorizados.csv` estão de **15/08 16:07**, antes
   do trabalho do Builder (18:03-18:05) — ou seja, **não foram regenerados**, e seguem em
   `v5`/`v2`/`v3`. Regenerar é ato deliberado, fora deste ciclo.
4. **DES-5 armado para a primeira coleta com TotalPass** (ver Problemas médios #1).
5. **Falso zero quase não se move** (5,53% -> 5,31%) — confirmado por medição própria. Quem vender o
   bloco como "acaba com o falso zero" estará errado.
6. **Pilha sem PR.** `git rev-list --left-right --count origin/main...HEAD` continua acusando a pilha
   herdada (23 commits locais); este ciclo empilha por cima. Risco assumido no Passo 0.
7. **Camada semântica do grafo desatualizada.** Este ciclo mexeu em `docs/`, `tasks/`, `CLAUDE.md` e
   nas DECs; o hook `post-commit` só reconstrói a camada AST. O `/graphify . --update` (slash-command
   numa sessão Claude) segue pendente — CLAUDE.md §7.

## Saída literal das validações (re-executadas pelo QA)

### 1) Suíte COMPLETA, SERIAL — `python -m pytest -q`

> Gate único do ciclo. **`-n auto` NÃO roda neste ambiente** (ver Problemas médios #3); rodei serial,
> conforme instrução do orquestrador.

```
$ python -m pytest -q
............................................................. [ 15%]
... (3.206 itens)
[100%]
3178 passed, 28 skipped, 664398 warnings in 2542.73s (0:42:22)
EXIT=0
```

**VERDE. `3178 passed + 28 skipped = 3206`** — exatamente a contagem da coleta, ou seja **nenhum
teste sumiu no caminho** entre coletar e executar. **Zero falha, zero erro** (`grep -cE "^F|^E"` no
log = **0**), exit **0**. 42 min 22 s, serial.

### 2) `python -m pytest -q tests/unit/vulnerabilidade/ tests/unit/test_piloto_web_independentes.py`

```
448 passed, 27543 warnings in 63.89s (0:01:03)
```

### 3) `python -m pytest -q tests/contracts/test_parametros_canonicos.py` (prova de M1 intacto)

```
4 passed, 205 warnings in 0.51s
```

### 4) `python -m ruff check src tests`

```
All checks passed!
exit=0
```

### 5) `python -m pytest --collect-only -q`

```
$ python -m pytest --collect-only -q
...
3206 tests collected in 243.62s (0:04:03)
EXIT=0
```

**3206** — exatamente a contagem do Builder, e **+22** sobre a baseline de **3184** medida hoje pelo
Block Orchestrator. Confirmei o delta por contagem de funções contra a base `16f8243`, sem
`parametrize` em nenhum dos arquivos: `test_oferta_cadeias_do_feed.py` **+21** e
`test_auditoria_pressao_no_pin.py` **+1**; `test_score.py` e `test_universo_oferta_s6.py` **+0**.
Zero erro de coleta.

### 6) `python scripts/loop_guard.py --stdin`

> **Primeira invocação minha foi ERRADA e produziu um FALSO VERDE**, e registro isso porque é
> exatamente o tipo de coisa que a regra de no-bypass existe para pegar: pipei o `git diff` COMPLETO
> em vez de `--name-only`, o `loop_guard` leu cada linha do diff como um caminho ("1097 caminho(s)
> alterado(s), nenhum proibido") e devolveu `GUARD OK`. O contrato do `--stdin` é
> `gh pr diff --name-only`. Invocação correta abaixo.

```
$ { git diff --name-only 16f8243 -- . ':!graphify-out'; git status --porcelain | grep '^??' | sed 's/^?? //'; } | sort -u | python scripts/loop_guard.py --stdin

GUARD: caminho(s) PROTEGIDO(s) tocado(s):
  - [GOVERNANCA] CLAUDE.md  ->  fonte canonica de regras e DECs
  - [GOVERNANCA] src/motor_expansao/vulnerabilidade/alvos_ma.py  ->  camadas paralelas com insumo de PII na origem (DEC-012)
  - [GOVERNANCA] src/motor_expansao/vulnerabilidade/alvos_nomeados.py  ->  camadas paralelas com insumo de PII na origem (DEC-012)
  - [GOVERNANCA] src/motor_expansao/vulnerabilidade/contrato.py  ->  camadas paralelas com insumo de PII na origem (DEC-012)
  - [GOVERNANCA] src/motor_expansao/vulnerabilidade/pressao_competitiva.py  ->  camadas paralelas com insumo de PII na origem (DEC-012)
  - [GOVERNANCA] tasks/backlog.md  ->  backlog - marcador de autonomia/criticidade dos blocos
exit=1
```

**6 violações, TODAS `GOVERNANCA`, ZERO `CRITICO`** — idêntico ao que o Builder reportou. É o
esperado para um bloco Alta desta camada; resolve pela label `aprovado-humano`.

### 7) Smoke de import — o comando do `prompts/builder.md` e o equivalente VIVO

```
$ git log --oneline -1 30378a0
30378a0 corte(BLK-WEB-20): remove o Streamlit — o piloto web e o unico app (DEC-022)

$ git show 30378a0 --stat | head
Author: Felipe Castaldi | Date: Mon Aug 3 13:38:21 2026 -0300
    CODIGO REMOVIDO (subgrafo fechado, nada compartilhado o importava):
    - streamlit_app.py, .streamlit/, Dockerfile.streamlit;
    ...
    - testes UI-only: test_streamlit_app.py (255; ...)

$ git ls-files | grep -c "streamlit_app"
0

$ python -c "import streamlit_app"
ModuleNotFoundError: No module named 'streamlit_app'

$ python -c "import motor_expansao.vulnerabilidade; from motor_expansao.vulnerabilidade import contrato as c; print(c.VERSAO_CONTRATO_SCORE, ...)"
ok: score_vulnerabilidade_v6 pressao_competitiva_v3 alvos_ma_v3 alvos_ma_nomeados_v4
exit=0

$ python -c "sys.path.insert(0,'web/server'); import app as pilot_app"
import ok: app -> C:\...\web\server\app.py
tem FastAPI app: True
exit=0
```

**DES-4 do Builder PROCEDE** e vira achado de processo (Problemas médios #2).

### 8) Guardrail READ-ONLY M1 — `git diff --name-status 16f8243`

```
M	CLAUDE.md
R086	docs/decisions/DEC-030.md	docs/decisions/DEC-033.md
M	docs/decisions/README.md
M	docs/vulnerabilidade_ma_contrato.md
M	graphify-out/.graphify_labels.json      <- PRE-SUJO (16:19/16:26, ANTES do Builder)
M	graphify-out/GRAPH_REPORT.md            <- PRE-SUJO
M	graphify-out/graph.json                 <- PRE-SUJO
M	src/motor_expansao/vulnerabilidade/alvos_ma.py
M	src/motor_expansao/vulnerabilidade/alvos_nomeados.py
M	src/motor_expansao/vulnerabilidade/contrato.py
M	src/motor_expansao/vulnerabilidade/pressao_competitiva.py
M	tasks/backlog.md
M	tasks/completed.md
M	tests/unit/vulnerabilidade/test_auditoria_pressao_no_pin.py
M	tests/unit/vulnerabilidade/test_score.py
M	tests/unit/vulnerabilidade/test_universo_oferta_s6.py
?? context/handoff/20260815-{164027-block-orchestrator,171256-planner,181334-builder}.md
?? docs/decisions/DEC-034.md
?? tests/unit/vulnerabilidade/test_oferta_cadeias_do_feed.py
```

**NÃO aparecem:** `config.py`, `src/motor_expansao/pipelines/m1/`, `normalizar_concorrentes.py`,
`calcular_colunas_mercado.py`, nenhum artefato oficial do M1, `deploy/`, `Dockerfile.*`, compose,
Caddy, `.env`, CI. **`git status --porcelain data/` = VAZIO** — nada foi escrito em `data/`.

### 9) SONDA PRÓPRIA — o `test_14b` pega mesmo o `k` errado?

> O pedido central do Builder ao QA. Mutei o `k` **em memória** (monkeypatch do atributo de módulo),
> **sem alterar o repositório**, e reexecutei o teste.

```
SONDA 1 — _k_do_bucket: o k e' DERIVADO?
DEDUP_H3_RES=11  aresta media=28.66 m
  _k_do_bucket(50 m) = 4
  _k_do_bucket(150 m) = 6
  _k_do_bucket(300 m) = 10
  _k_do_bucket(1000 m) = 26
k para DEDUP_CADEIA_FEED_M (150 m) = 6
k para DEDUP_INDEPENDENTES_M (50 m) = 4   <-- DES-5: a funcao usa grid_disk(...,1)

SONDA 2 — test_14b com o k REAL (deve PASSAR)
RESULTADO: PASSOU (esperado)

SONDA 3 — test_14b com o k MUTADO PARA 1 (o teste tem de FALHAR)
RESULTADO: FALHOU (esperado) — o teste PEGA o k errado
   File ".../test_oferta_cadeias_do_feed.py", line 427, in test_14b_...
     assert obtidos == esperado_colapsos
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
   AssertionError

SONDA 4 — quanta dedup o k=1 perderia no INSUMO REAL
sobreviventes com k derivado (=6) = 1171
sobreviventes com k=1             = 1364
DIFERENCA (unidades que o k errado deixaria de colapsar) = 193
```

**Veredito da sonda: `test_14b` É trava de verdade.** Ele falha pelo assert de **EQUIVALÊNCIA**
(`obtidos == esperado_colapsos`), não pelo assert cosmético `_k_do_bucket(...) > 1` — que aliás nem
dispara na sonda, porque o teste importa o nome direto e eu mutei o atributo do módulo de propósito,
justamente para isolar a equivalência. E o defeito que ele impede é **material**: no insumo real, o
`k = 1` deixaria **193 unidades** de deduplicar em silêncio (1.364 entrariam em vez de 1.171),
inflando a oferta de todas elas em dobro.

### 10) SONDA PRÓPRIA — auto-exclusão nos DOIS casos (`test_5` e `test_6`)

```
SONDA 5 — auto-exclusao: os DOIS casos
caso A — SOBREVIVENTE (nenhum pin por perto)
  [sobrevivente] oferta=0.000000  pressao=0.0000  n_conc=0  n_feed=0  dist=nan
caso B — COLAPSADA contra um pin do funil a 20 m
  [colapsada]    oferta=0.000000  pressao=0.0000  n_conc=0  n_feed=0  dist=nan

SONDA 5b — a auto-exclusao NAO apaga concorrente de terceiro
  [colapsada + 1 terceiro a 300 m] oferta=0.850168  pressao=45.9509  n_conc=1  n_feed=0  dist=299.66

SONDA 5c — CONTROLE: desligando a auto-exclusao, quanto seria o fantasma?
  [sobrevivente SEM auto-exclusao] oferta=1.000000  pressao=50.0000  n_conc=1  n_feed=1  dist=0.0
  [colapsada    SEM auto-exclusao] oferta=1.000000  pressao=50.0000  n_conc=1  n_feed=0  dist=0.0
```

**Os DOIS casos estão fechados**, e o controle prova que o fantasma seria **exatamente `50,0`
pontos** nos dois — inclusive no caso 2, que o Block Orchestrator não tinha visto. A 5b mostra que a
exclusão é cirúrgica: o concorrente de terceiro a 300 m continua contando.

### 11) SONDA PRÓPRIA — DES-1: o número é 1.171, 1.179 ou 1.134?

> Re-medi do zero, e validei a função contra **varredura completa** escrita por mim (sem bucket H3).

```
[feed] linhas=22173  independentes=19329  unidades_de_rede=2844  redes=83
[mapeados] cru=4499  validos=4366  redes=104

ADOTADO (rede<=150m OU d<=50m): sobreviventes=1171  colapsadas=1673

sensibilidade (ENTRAM = sem representante):
| limiar | pura | (rede,d) | ADOTADO |
| 30 m   | 1629 | 1637     | 1418    |
| 50 m   | 1418 | 1433     | 1418    |
| 100 m  | 1239 | 1268     | 1257    |
| 150 m  | 1134 | 1179     | 1171    |
| 200 m  | 1046 | 1121     | 1113    |
| 300 m  |  834 |  946     |  938    |

forca bruta a 150 m (ADOTADO) = 1171   funcao = 1171   IGUAIS=True
```

**O Builder está CERTO: o número do critério ratificado é `1.171`.** A tabela reproduz a do
`contrato.py`/DEC-034 **em todas as 18 células**. O `1.179` do Planner é a coluna do meio (variante
`(rede, d)` **sem** o piso) e o `1.134` do Block Orchestrator é a coluna da esquerda (distância
pura). Nenhum dos dois está "errado": são outras variantes. Casar a rede salva `1171 - 1134 = 37`
concorrentes reais — o número da DEC fecha.

### 12) SONDA PRÓPRIA — a premissa que ninguém tinha medido: o vocabulário de `rede` casa?

```
redes no feed        = 83
redes nos mapeados   = 104
INTERSECAO           = 81
so' no feed          = 2  ex.: ['ctrc', 'mansao_maromba']
cobertura das unidades do feed cuja rede EXISTE nos mapeados: 2832 de 2844 (99.6%)

so' o piso de 50 m (rede desligada) -> entram 1418
so' o ramo de rede a 150 m          -> entram 1178
criterio ADOTADO (os dois)          -> entram 1171
 => o ramo de rede colapsa 247 unidade(s) ALEM do piso
 => o piso colapsa 7 unidade(s) ALEM do ramo de rede
```

Auditei isto porque o ramo "casar a `rede`" só funciona se os dois lados usarem o **mesmo slug** — se
não usassem, o critério degeneraria silenciosamente no piso de 50 m e a DEC estaria vendendo uma
precisão que não existe. **A premissa se sustenta: 81 das 83 redes do feed existem no insumo mapeado,
cobrindo 99,6% das unidades.** (O `7` acima e o `8` da DEC não divergem: rodei o contrafactual com
`piso_m=0.0`, e `d <= 0,0` ainda captura o caso de **0,0 m** que a própria DEC cita como o menor —
`7 + 1 = 8`.)

### 13) A13 — medição antes/depois, INDEPENDENTE (insumo real, nacional)

```
--- A13: antes (v5, so independentes) x depois (v6, + cadeias do feed) ---
universo do score = 19329

pressao media  antes=61.415  depois=62.775
zeros antes=1068 (5.53%)  depois=1026 (5.31%)
mudam de valor = 7237 (37.44%)
delta (dos que mudam) medio=3.634 p90=8.542 max=52.879
delta (universo inteiro) p90=4.066
mudam de faixa de 10 = 2150 (11.12%)

score medio antes=54.566 depois=55.110
score mudam de valor = 7227
delta score (dos que mudam) medio=1.456 p90=3.417 max=21.152
score muda de faixa = 1082 (5.60%)
SPEARMAN score   = 0.9911994
SPEARMAN pressao = 0.9911994
regimes: {'s1,s6': 19329}

--- entregavel (hex, regime) ---
linhas antes=6753 depois=6753
coluna=score_vulnerabilidade_medio  comuns=6753  linhas alteradas=2096 (31.04%)
SPEARMAN entregavel = 0.9865793
top-50  do entregavel: trocam=5
top-100 do entregavel: trocam=12
top-200 do entregavel: trocam=20
top-500 do entregavel: trocam=43
top-100 de academias: trocam=9
top-200 de academias: trocam=18
top-500 de academias: trocam=33

versoes: score=score_vulnerabilidade_v6  alvos=alvos_ma_v3
universo_oferta depois = ['cadeias_e_independentes']
colunas pressao depois = 15

--- REGUAS VISIVEIS, restritas ao universo do SCORE ---
  pressao_competitiva              mudam=7237  max_delta=+52.879
  n_concorrentes_no_raio           mudam=7218  max_delta=+9.000
  dist_concorrente_mais_proximo_m  mudam=773

--- decomposicao nova (universo do score) ---
  oferta_cadeias_do_feed > 0 em 7218 linhas
  n_cadeias_do_feed_no_raio > 0 em 7218
  soma das partes <= total? True
  colunas de saida = 15  contrato = 15
  ordem == contrato? True
  colunas PROIBIDAS na saida: nenhuma
```

**Os dois Spearman e as 12 trocas no top-100 — os três números que sustentam a DEC-034 — reproduzem
aos 7 decimais e ao inteiro.** As três réguas visíveis também (`7.237` / `7.218` / `773`).

> Nota metodológica: na minha primeira passada eu medi `n_conc` e `dist_m` sobre o **feed inteiro**
> (22.173) e obtive `8842` / `2465`, divergindo do Builder. Re-medi restringindo ao **universo do
> score** (19.329 independentes), que é o recorte da DEC, e bateu exato. **A divergência era minha.**

### 14) Contratos e carimbos — medidos importando `contrato.py`

```
CONTRATO_COLUNAS_PRESSAO_ACADEMIA = 15   (era 13)
CONTRATO_COLUNAS_PRESSAO          = 14   (era 12)
CONTRATO_COLUNAS_ALVOS_NOMEADOS   = 24   (era 23)
CONTRATO_COLUNAS_SCORE            = 26   (INALTERADO)
CONTRATO_COLUNAS_ALVOS_MA         = 18   (INALTERADO)
CONTRATO_COLUNAS_ACADEMIAS_MA     = 26   (INALTERADO)

VERSAO_CONTRATO_PRESSAO        = 'pressao_competitiva_v3'
VERSAO_CONTRATO_SCORE          = 'score_vulnerabilidade_v6'
VERSAO_CONTRATO_ALVOS_MA       = 'alvos_ma_v3'
VERSAO_CONTRATO_ALVOS_NOMEADOS = 'alvos_ma_nomeados_v4'
DEDUP_CADEIA_FEED_M = 150.0   DEDUP_CADEIA_FEED_PISO_M = 50.0   DEDUP_K_MARGEM_ANEIS = 1
UNIVERSOS_OFERTA = ('cadeias', 'cadeias_e_independentes')   <- sem terceiro valor
```

**Os 4 bumps e as 3 mudanças de contrato conferem; os 3 contratos que não deviam mudar não mudaram.**
Confirmei ainda, lendo o código, que `_juntar_pressao` (`score.py`) projeta explicitamente
`pressao[[*chaves, coluna, "universo_oferta"]]` — as duas colunas novas são **inertes** para o score
por desenho, não por acaso.

### 15) `_filtrar_universo_sinal_1` e o universo do SCORE

```
$ git diff 16f8243 -- presenca_agregador.py score.py snapshots.py
-> 0 bytes (INTACTOS)
```

A trava de identidade (`test_score.py:1028`: `m._filtrar_universo_sinal_1 is
mpresenca._filtrar_universo_sinal_1`) está intacta, e o teste novo confirma que só `independente`
sobrevive ao filtro. `n_academias_independentes_totalpass`/`_wellhub` inalteradas.

### 16) G3 — renumeração da DEC

```
$ git diff --name-status 16f8243 | grep DEC
R086	docs/decisions/DEC-030.md	docs/decisions/DEC-033.md     <- git mv, histórico preservado

$ rg "DEC-030" (excluindo graphify-out/ e context/handoff/)
CLAUDE.md:200                  -> linha da DEC-033 (a citação é a NOTA de renumeração)
docs/decisions/README.md:43    -> nota de renumeração
docs/decisions/DEC-033.md:3,4  -> cabeçalho de renumeração
tasks/completed.md:12731       -> nota de renumeração
```

**Zero citação órfã.** As 4 ocorrências restantes de `DEC-030` são todas a *nota de renumeração*
("renumerada de DEC-030"), que tem de existir. As ocorrências em `context/handoff/*` são snapshots
**append-only** e não podem ser editadas por regra do `context/handoff/README.md`. `DEC-033` aparece
em 12 arquivos do repo; `DEC-034.md` existe, tem a seção **"QUEBRA DE COMPARABILIDADE COM A SÉRIE
`v5` — anunciada"** (exigência do G5) e não colide com `origin/main` (que ocupa 030/031/032).

### 17) EOL (DEC-017)

```
arquivo                                | BLOB @16f8243  | WORKTREE hoje | BLOB que seria COMMITADO
tasks/backlog.md                       | CRLF=0 LF=3209 | CRLF=3231     | CRLF=0 LF=3231
tasks/completed.md                     | CRLF=0 LF=12783| CRLF=0        | CRLF=0 LF=12783
docs/vulnerabilidade_ma_contrato.md    | CRLF=0 LF=837  | CRLF=0        | CRLF=0 LF=895
docs/decisions/DEC-033.md              | (novo)         | CRLF=0        | CRLF=0 LF=190
docs/decisions/DEC-034.md              | (novo)         | CRLF=0        | CRLF=0 LF=213
CLAUDE.md                              | CRLF=0 LF=200  | CRLF=201      | CRLF=0 LF=201
pressao_competitiva.py                 | CRLF=0 LF=738  | CRLF=1056     | CRLF=0 LF=1056
```

Medi com `git hash-object -w --path <p>` (aplica os filtros reais). **Todos os blobs que entrarão no
commit são LF puro.** O CRLF do working tree é do `core.autocrlf=true` e é normalizado na entrada.
`git diff --stat` também não mostra churn de arquivo inteiro (`backlog.md` 40 linhas,
`completed.md` 8). **DEC-017 satisfeita.**

### 18) Acentuação (CLAUDE.md §2, nos DOIS sentidos)

```
SENTIDO 1 — IDENTIFICADORES com acento (PROIBIDO)
  -> 0 identificador(es)/chave(s) com acento
     (AST sobre os 5 arquivos .py do ciclo: Name/arg/Attribute/FunctionDef/ClassDef + chaves de dict)

SENTIDO 1b — valores dos CONTRATOS
  CONTRATO_COLUNAS_PRESSAO_ACADEMIA: colunas com acento = NENHUMA
  CONTRATO_COLUNAS_PRESSAO:          colunas com acento = NENHUMA
  CONTRATO_COLUNAS_ALVOS_NOMEADOS:   colunas com acento = NENHUMA
  UNIVERSOS_OFERTA = ('cadeias', 'cadeias_e_independentes')  ascii=True

SENTIDO 2 — PROSA acentuada nos .md novos
  docs/decisions/DEC-034.md            caracteres_acentuados=341
  docs/decisions/DEC-033.md            caracteres_acentuados=337
  docs/vulnerabilidade_ma_contrato.md  caracteres_acentuados=1812
```

**Limpo nos dois sentidos.** Identificadores novos (`oferta_cadeias_do_feed`,
`n_cadeias_do_feed_no_raio`, `oferta_cadeias_do_feed_no_hex`, `DEDUP_CADEIA_FEED_M`,
`DEDUP_CADEIA_FEED_PISO_M`, `DEDUP_K_MARGEM_ANEIS`, `com_oferta_do_feed`, `dedup_cadeias_do_feed`,
`_k_do_bucket`, `_pontos_validos_frame`) 100% ASCII; prosa da DEC, do §8.1, do backlog e do CLAUDE.md
em português acentuado. **Não é regressão** o `help` do `--oferta-so-cadeias` estar sem acento: o
arquivo `alvos_ma.py` já era uniformemente ASCII nessas strings antes do bloco, e `--help` de CLI
interna não está na enumeração de superfícies de usuário do §2 (piloto web / bot / relatórios).

### 19) SONDA PRÓPRIA — os testes de dedup são VACUOS? (teste de mutação)

> Um teste que passa com o código quebrado não é teste. Mutei o comportamento **em memória** e
> reexecutei os 5 casos que dependem da dedup.

```
--- BASELINE (codigo real) ---
  PASSOU  test_2 (mesma rede a 150 m NAO dobra)
  PASSOU  test_4 (piso de 50 m colapsa)
  PASSOU  test_6 (colapsada nao se auto-pressiona)
  PASSOU  test_1 (unidade longe ENTRA)
  PASSOU  test_3 (outra rede CONTINUA contando)

--- MUTADO: dedup DESLIGADA (nada colapsa) ---
  FALHOU  test_2 (mesma rede a 150 m NAO dobra)      (AssertionError)
  FALHOU  test_4 (piso de 50 m colapsa)              (AssertionError: 30 m e' menos que o piso...)
  FALHOU  test_6 (colapsada nao se auto-pressiona)   (AssertionError: a unidade absorvida se auto-pressionou...)
  PASSOU  test_1 (unidade longe ENTRA)               <- correto: ela DEVE entrar nos dois mundos
  PASSOU  test_3 (outra rede CONTINUA contando)      <- correto: idem

--- MUTADO: dedup por DISTANCIA PURA (sem casar a rede) ---
  FALHOU  test_3 (outra rede CONTINUA contando)      (AssertionError)
  (os outros 4 passam, como esperado)
```

**Os testes NÃO são vacuosos.** Desligar a dedup derruba 3 deles; trocar o critério ratificado pela
distância pura — a variante que o Block Orchestrator havia medido e que **apagaria 37 concorrentes
reais** — derruba o `test_3`. O critério do gate está travado por teste, não só por comentário.

### 20) A coluna nova é MESMO inerte para o piloto? (verificado no código, não aceito por palavra)

O Builder afirma (A9) que `web/server/app.py` não precisou ser tocado porque "lê o schema
defensivamente". **Confere, e é mais forte do que ele disse:** `carregar_independentes()`
(`web/server/app.py:424-441`) faz `pq.read_schema(...)` e **interseta com uma whitelist explícita**,
`_COLS_NOMEADAS` (L401-422). `n_cadeias_do_feed_no_raio` **não está na whitelist**, então a 24ª
coluna é ignorada na leitura — não é "tolerada", é **não lida**.

```python
disponiveis = set(pq.read_schema(NOMEADAS_PATH).names)
cols = [c for c in _COLS_NOMEADAS if c in disponiveis]
```

**Corolário para a metade 1:** surfaceá-la na tela exige acrescentar o nome a `_COLS_NOMEADAS` — o
que confirma que a superfície ficou de fora de propósito, e não por esquecimento.

### 21) `-n auto` neste ambiente (divergência de instrução, registrada)

```
$ python -m pytest -n auto -q tests/contracts/test_parametros_canonicos.py
INTERNALERROR>   File ".../xdist/workermanage.py", line 106, in setup_node
INTERNALERROR>     gw = self.group.makegateway(spec)
INTERNALERROR>   File ".../execnet/gateway_io.py", line 34, in __init__
INTERNALERROR>     self.popen = p = execmodel.subprocess.Popen(args, stdout=PIPE, stdin=PIPE)
INTERNALERROR>   File ".../subprocess.py", line 1430, in _make_inheritable
INTERNALERROR>     h = _winapi.DuplicateHandle(...)
INTERNALERROR> OSError: [WinError 50] Não há suporte para o pedido

pytest 8.4.2 | xdist 3.8.0
```

## Conferência de no-bypass

**Confirmo que nenhuma validação usou bypass** — nenhum `--config /dev/null`, nenhum mock do caminho
crítico, nenhuma substituição de artefato de produção por fixture para produzir verde. Detalhes:

- As medições de A13/DES-1 rodaram contra os **artefatos reais**: `coordenadas_por_chave()` sobre
  `data/staging/snapshots_concorrentes/semana=2026-33/`, `concorrentes_mapeados.parquet` (4.499
  pontos) e `carteira_expansao_acionavel.parquet`. Nada sintético.
- A validação da dedup foi feita por **varredura completa escrita por mim** (força bruta, sem bucket
  H3), não pela função sob teste. `IGUAIS=True`.
- A sonda do `k` mutou o valor **em memória** (`m._k_do_bucket = lambda: 1`) e restaurou no `finally`
  — **o repositório não foi alterado**, e o `git status` ao fim do QA é o mesmo do início mais os
  arquivos de handoff.
- Os scripts de sonda/medição vivem em `%TEMP%\qa_ma17\` e **nada foi gravado em `data/`** nem no
  repo (`git status --porcelain data/` = vazio).
- A suíte `pytest` usa `tests/fixtures/` legitimamente — isso **não é bypass** pela própria
  delimitação do §(b) do `prompts/qa_analyzer.md`.
- **Registro de auto-crítica:** minha PRIMEIRA invocação do `loop_guard` foi errada e devolveu um
  falso `GUARD OK` (pipei o diff completo em vez de `--name-only`). Corrigi e colei a saída correta.
  Se eu tivesse aceitado o primeiro verde, teria reportado "zero violações" onde há 6.

## Conferência cruzada com log do Builder

**BATE — em tudo que é material.** Reproduzi de forma independente:

| item | Builder | QA (medição própria) | bate? |
|---|---|---|---|
| sobreviventes da dedup (DES-1) | 1.171 | **1.171** (+ força bruta `IGUAIS=True`) | **sim** |
| tabela de sensibilidade (18 células) | — | **idêntica em 18/18** | **sim** |
| pressão média antes -> depois | 61,415 -> 62,775 | **61,415 -> 62,775** | **sim** |
| mudam de valor | 7.237 (37,44%) | **7.237 (37,44%)** | **sim** |
| Spearman do score | 0,9911994 | **0,9911994** | **sim** |
| Spearman da pressão | 0,9911994 | **0,9911994** | **sim** |
| Spearman do entregável | 0,9865793 | **0,9865793** | **sim** |
| entregável alterado | 2.096 / 6.753 (31,04%) | **2.096 / 6.753 (31,04%)** | **sim** |
| top-100 do CSV trocam | 12 | **12** | **sim** |
| top-50 / top-100 acad. / top-200 / top-500 | 5 / 9 / 18 / 33 | **5 / 9 / 18 / 33** | **sim** |
| réguas `pressao`/`n_conc`/`dist_m` | 7.237 / 7.218 / 773 | **7.237 / 7.218 / 773** | **sim** |
| zeros | 5,53% -> 5,31% | **5,53% -> 5,31%** | **sim** |
| regime único `{s1,s6}` | 100% | **19.329 de 19.329** | **sim** |
| contratos 15/14/24, 26/18/26 | idem | **idem** | **sim** |
| 4 bumps | idem | **idem** | **sim** |
| recorte da camada | 448 passed | **448 passed** | **sim** |
| `parametros_canonicos` | 4 passed | **4 passed** | **sim** |
| `ruff` | All checks passed | **All checks passed** | **sim** |
| `loop_guard` | 6 governança, 0 crítico | **6 governança, 0 crítico** | **sim** |
| DES-4 (`import streamlit_app` morto) | procede | **PROCEDE** (`30378a0`, 0 arquivos) | **sim** |
| DES-5 (`dedup_independentes` k=1) | procede, nulo hoje | **PROCEDE**, `0 de 19.329` hoje | **sim** |
| coleta | 3206 | **3206 collected in 243.62s, exit 0** | **sim** |
| suíte completa | **não rodada** (correto: é o gate do QA) | **3178 passed + 28 skipped = 3206, exit 0** | **n/a** |

**Única divergência encontrada:** o Builder escreve *"448 passed (eram 427 antes do bloco)"*; o
delta real por contagem de funções é **+22**, logo o recorte era **426**. Erro de 1 numa referência
histórica, sem consequência (o número autoritativo é a coleta).

**Desvios do Builder, auditados um a um:** DES-1 **procede e ele está certo** (o `1.179` do Planner e
o `1.134` do BO são outras variantes). DES-2 (`Int64` em vez de `Float64`) **procede, e o
Builder acertou em desviar do plano** — medi os dtypes das 5 colunas de auditoria no contrato
nomeado: as **três contagens** são `Int64` (`n_concorrentes_no_raio`, `n_independentes_no_raio`,
`n_cadeias_do_feed_no_raio`) e as **duas grandezas** são `Float64` (`oferta_ponderada`,
`dist_concorrente_mais_proximo_m`). Seguir o A2 ao pé da letra teria criado uma contagem `Float64`
ao lado de duas irmãs `Int64`. `Int64` é nulável do mesmo jeito, e a invariante "ausência é nula,
nunca zero" continua travada por teste. DES-3 (p90 condicionado)
**procede**: medi o p90 sobre o universo inteiro e deu `4,066`, compatível com o `~4,08` do Planner;
é diferença de definição, declarada. DES-4 e DES-5 **procedem** e viraram achados (acima).

## Guardrails verificados

- **`score_priorizacao` não alterado:** **sim** — `tests/contracts/test_parametros_canonicos.py`
  4 passed; `H3_RESOLUTION=7`, `DIST_MIN_ULTRA_KM=1.0`, `RENDA_MIN=4500.0` e os pesos
  `renda=0.40`/`pop=0.60` preservados; `config.py` e `pipelines/m1/` **não aparecem no diff**.
- **`hex_score_estrutural` não alterado:** **sim** — nenhum arquivo do M1 no diff.
- **Artefatos M1 preservados:** **sim** — `git status --porcelain data/` vazio; nenhum parquet
  oficial escrito; as medições são leitura pura com scripts em `%TEMP%`.
- **Camada READ-ONLY sobre o M1:** **sim** — `loop_guard` com **zero** `CRITICO`.
- **Dashboard/piloto offline mantido:** **sim** — `web/` intocado; nenhuma dependência de rede.
- **Anti-PII (DEC-012/§11):** **sim** — a saída da pressão foi medida e **não tem** `lat`, `lng`,
  `nome` nem `rede` (sonda 13, "colunas PROIBIDAS na saida: nenhuma"); nenhum artefato novo persiste
  coordenada.
- **Validações re-executadas pelo QA (sem bypass):**
  - `pytest -q` (suíte completa, serial) -> **3178 passed, 28 skipped, 0 falhas, exit 0** (42m22s)
  - `pytest -q tests/unit/vulnerabilidade/ tests/unit/test_piloto_web_independentes.py` -> **448 passed**
  - `pytest -q tests/contracts/test_parametros_canonicos.py` -> **4 passed**
  - `ruff check src tests` -> **All checks passed!**
  - `pytest --collect-only -q` -> **3206 collected, exit 0**
  - `loop_guard.py --stdin` -> **6 governança, 0 crítico** (exit 1, esperado)
  - smoke de import (equivalente vivo) -> **pacote OK + `web/server/app.py` OK**
  - `git diff --name-status 16f8243` -> **nenhum caminho do M1**
  - sondas próprias 1-15 (k mutado, auto-exclusão nos 2 casos + controle, DES-1 por força bruta,
    vocabulário de rede, chave duplicada, carimbo, **2 testes de mutação da dedup**) -> **todas
    registradas acima**
- **Escopo respeitado:** **sim** — a metade 1 / Plano B ficou **integralmente fora**: `git diff`
  não lista **nada** em `web/`, não existe `redes_diagnostico.py` (confirmei: o arquivo não foi
  criado), nenhum artefato novo nomeado de redes, nenhum pin. O único ponto de contato com a metade 1
  é a coluna `n_cadeias_do_feed_no_raio` no artefato nomeado, que **declara** a lacuna sem
  surfaceá-la — e isso foi explicitamente fechado pelo Planner (F9) e ratificado no gate.
- **Ratificações do gate, conferidas uma a uma:** critério `(rede igual E d <= 150 m) OU (d <= 50 m)`
  **implementado como ratificado**; `DEDUP_CADEIA_FEED_M = 150.0` **presente**; `DEC-030` local ->
  **`DEC-033`** por `git mv`; DEC nova = **`DEC-034`**; **4 bumps** presentes; quebra de
  comparabilidade com a série `v5` **anunciada em seção própria da DEC-034** e no §8.1.

## Housekeeping (Passo 6.0) — modo MERGE-HUMANO, medido e NÃO usado como reprovação

Criticidade **Alta** + PR toca `tasks/backlog.md` (governança) -> **MERGE-HUMANO**. O housekeeping é
passo do orquestrador (Passo 6), **posterior ao QA**, então por instrução explícita do orquestrador
eu **meço e registro** em vez de reprovar — no molde do QA do BLK-MA-04.

```
$ python scripts/housekeeping_move_block.py BLK-MA-17 --check
FALHA --check: stub ausente no backlog para BLK-MA-17
exit=1

$ python scripts/housekeeping_move_block.py BLK-MA-17 --is-done
BLK-MA-17: ABERTO em tasks/completed.md
exit=1
```

**Estado esperado** (o helper ainda não rodou). **MAS há uma decisão real a tomar antes de rodá-lo —
ver a pendência #1 abaixo.**

## Pendências do Passo 6 (para o orquestrador)

1. **NÃO rodar `housekeeping_move_block.py BLK-MA-17` sem decidir o que fazer com a metade 1.**
   Este é o achado de processo mais importante do fechamento. O helper **recorta o bloco inteiro**
   do `backlog.md` e o move para `completed.md`, deixando um stub — mas o BLK-MA-17 está **entregue
   pela metade**: a metade 1 (exibição, pins, DEC própria) segue PENDENTE e vive **dentro do mesmo
   heading `### BLK-MA-17`**. Mover o bloco **apagaria do backlog** o trabalho que ainda falta.
   Duas saídas limpas:
   - **(a)** fatiar antes: criar `### BLK-MA-17-B — metade 1 (exibição)` com o conteúdo pendente e
     só então mover o `BLK-MA-17`; ou
   - **(b)** **não mover**: manter o bloco no backlog (já anotado pelo Builder com "Metade 2
     EXECUTADA / Metade 1 PENDENTE") e só **acrescentar** o registro de fechamento parcial ao
     `completed.md`.
   Lembrar ainda que o helper **engole stubs de 1 linha** adjacentes (ele recorta até o próximo
   heading) — conferir e restaurar depois de rodar.
2. **Commit por PATH.** `graphify-out/*` está sujo desde **16:19/16:26**, antes do Builder
   (18:03-18:05) — é regeneração do hook `post-commit`, **não** parte do ciclo. `PRD.md`,
   `context/handoff.md` e `tasks/current_task.md` também não entram, por instrução do Passo 0.
3. **Label `aprovado-humano`** é necessária (guard = 6 violações de governança, exit 1). Atenção à
   armadilha conhecida: **o guard derruba a label a cada push** — atualizar a branch com a base
   ANTES de pedir a aprovação, não depois.
4. **`/graphify . --update`** numa sessão Claude: este ciclo mexeu em `docs/`, `tasks/`, `CLAUDE.md`
   e nas DECs, e o hook só cobre a camada AST.
5. **Bloco de correção `BLK-MA-17-FU1`** (texto pronto abaixo) — **não o inseri em
   `tasks/backlog.md`**, conforme instrução; a inserção é do orquestrador no Passo 6.
6. **Artefatos em disco continuam na régua `v5`/`v2`/`v3`.** Regenerar é ato deliberado, e a DEC-033
   registra que a janela de estreia dos pins na VPS já foi usada.
7. **Pilha sem PR** (23 commits) — risco herdado, inalterado por este ciclo.
8. **O §8 do `CLAUDE.md` VAI conflitar no merge, e o conflito é de conteúdo, não de EOL.** Medi as
   duas pontas: `origin/main` termina em `...026, 030, 031, 032`; a pilha local termina em
   `...026, 027, 028, 029, 033, 034`. Nenhuma das duas tem a tabela completa. O merge tem de
   produzir **021-034 em ordem** — se alguém resolver "pegando um lado", some metade do índice
   canônico de decisões. Mesma observação para `docs/decisions/README.md`, cujo índice em
   `origin/main` para na DEC-026. **A renumeração deste ciclo está CORRETA e é o que torna essa
   reconciliação possível** (confirmei que `origin/main` tem mesmo DEC-030 "dois selos", DEC-031
   "régua do percentil de renda" e DEC-032 "k nacional", e que 033/034 estão livres lá).

## Bloco de correção proposto (TEXTO PRONTO — NÃO inserido no backlog)

```markdown
### BLK-MA-17-FU1 — `dedup_independentes` sub-cobre o bucket H3: o `k` é cravado em 1, e 50 m exigem 4

| | |
|---|---|
| **Criticidade** | **Média** — corrige uma dedup que hoje tem efeito **provadamente nulo** (`0 de 19.329` colapsos, porque só há WellHub), mas que passa a errar **em massa** na primeira coleta com TotalPass. READ-ONLY sobre o M1. |
| **Prioridade** | Alta dentro da epic — tem de estar corrigido **ANTES** de o BLK-MA-06 ligar a segunda fonte, senão o defeito estreia junto com o dado. |
| **Esteira** | Builder → QA (sem gate: não muda universo, não muda decisão; muda um detalhe de PERFORMANCE que hoje é detalhe de RESULTADO). |
| **Status** | Pendente — **achado do QA do BLK-MA-17 (2026-08-15)**, confirmando o DES-5 declarado pelo Builder. |
| **Depende de** | BLK-MA-17 (metade 2) — é ele que introduz `_k_do_bucket`, a função que expõe e resolve o defeito. |
| **Autonomia** | **manual (NÃO loop-safe)** |

**O defeito, medido.** `pressao_competitiva.py:469` busca o candidato da dedup em
`h3.grid_disk(celulas[i], 1)` — `k` **cravado** — com `DEDUP_INDEPENDENTES_M = 50,0` m. A fórmula que
o BLK-MA-17 introduziu no mesmo arquivo dá `_k_do_bucket(50) = 4`: na `DEDUP_H3_RES = 11` a aresta
média é **28,66 m**, e o anel `k = 1` não garante cobertura nenhuma para 50 m. O comentário do
`contrato.py` dizia "aresta ~24 m" — a medição real é **28,66 m**, e o BLK-MA-17 já corrigiu esse
comentário.

**Por que não levanta hoje, e por que isso é o perigoso.** A função só colapsa entre `fonte`
DIFERENTES, e o snapshot `2026-33` só tem WellHub: medi **`0 de 19.329`** colapsos. O modo de falha
de uma dedup sub-coberta é **indistinguível** do caso correto — ela devolve "nenhum colapso", que é
exatamente o que uma dedup correta devolve quando não há duplicata. Ninguém percebe até alguém
comparar contra varredura completa.

**O custo quando o TotalPass entrar.** A duplicata que não colapsa vira duas linhas na oferta; a
gêmea a ~0 m soma `peso(0) x PESO_OFERTA_INDEPENDENTE` -> `sat(0,5)` = até **+33,3 pontos** de
pressão fantasma na academia — o mesmo fantasma que a DEC-033 criou a dedup para matar. Para calibrar
a ordem de grandeza: no lado das cadeias, onde o dado JÁ existe, o `k` errado deixaria **193 de 2.844
unidades (6,8%)** de deduplicar (medido pelo QA: 1.364 sobreviventes com `k=1` contra 1.171 com o `k`
derivado).

**Escopo.** (1) Trocar `h3.grid_disk(celulas[i], 1)` por `h3.grid_disk(celulas[i], _k_do_bucket(distancia_m))`.
(2) **Medir o efeito** numa fixture de duas fontes e registrar (é mudança de COMPORTAMENTO da função,
ainda que hoje sobre conjunto vazio). (3) Teste de **equivalência contra varredura completa**, no
molde exato do `test_14b` do BLK-MA-17 — que é a única forma de provar que o bucket não mudou
resultado. (4) Nota de emenda na DEC-033, que é a dona daquela dedup. (5) Considerar, no mesmo passo,
a guarda de `(fonte, chave_snapshot)` duplicado nas DUAS funções de dedup (achado leve do mesmo QA:
com chave repetida o `dict` guarda só a última posição, e a auto-exclusão da outra linha aponta para
o índice errado).

**Fora de escopo.** Recalibrar o limiar de 50 m (isso é BLK-MA-06, com par verdade-terreno); mexer em
`dedup_cadeias_do_feed` (já usa `k` derivado); qualquer artefato/peso/score do M1.

**Alternativa considerada e recusada:** absorver como sub-item do BLK-MA-06. Recusada porque o
BLK-MA-06 é o bloco que **liga a segunda fonte** — se a correção viajar junto, o defeito e o dado que
o ativa estreiam no mesmo PR, e não haverá régua "antes" para medir.
```

## Decisão recomendada

**Fechar o ciclo** (Passo 6, modo MERGE-HUMANO), com três ações amarradas:

1. **Resolver a pendência #1** (metade 1 dentro do heading que o helper moveria) **antes** de rodar
   o housekeeping.
2. **Inserir `BLK-MA-17-FU1`** no `tasks/backlog.md` com o texto acima.
3. **Corrigir os prompts da esteira**: `prompts/builder.md` (L45/L46/L49), `prompts/planner.md` (L74)
   e `prompts/qa_analyzer.md` (L25) — trocar o smoke morto `import streamlit_app` pelo equivalente
   vivo, e substituir o `pytest -n auto` obrigatório por `-n auto` **com fallback serial declarado**,
   já que o paralelo é inexecutável nesta estação. São 2 linhas de prompt que hoje tornam duas
   validações obrigatórias impossíveis de cumprir.
