# Handoff — Builder (BLK-FIX-06, Fase A concluída)

## Skill que gerou este handoff
Builder (Fase A — MEDIÇÃO em scratch)

## Próxima Skill recomendada
**[CHECKPOINT — aprovação humana + DEC-002] → depois Fase B do Builder → QA**
(NÃO ir direto para QA: a Fase A termina no checkpoint. A Fase B — regeneração dos
artefatos oficiais do M1 — só pode iniciar APÓS a DEC-002 ser registrada/aprovada por
Felipe Silva com os números reais abaixo.)

## Aprovação humana registrada
**Fase A APROVADA por Felipe Silva em 2026-06-03** (medição em scratch). Esta aprovação
NÃO autoriza a Fase B nem qualquer regeneração de artefato oficial do M1 — isso depende
da DEC-002, registrada DEPOIS, no checkpoint, com os números reais produzidos aqui.
Ajuste humano v2: limiar candidato = **0.20**; Fase A mediu o leque {0.15, 0.20, 0.25, 0.30}.

## O que foi feito (somente Fase A — medição, sem tocar oficiais)
1. **Critério HÍBRIDO implementado** em `src/motor_expansao/pipelines/m1/base_h3_brasil.py`:
   - Fast path de centróide PRESERVADO (interior sem regressão por construção).
   - Para hexes de centróide FORA: novo helper `_fracao_terra_hex(hex_id, brasil_geom)`
     calcula `fracao_terra = brasil_geom.intersection(_hex_polygon(hex_id)).area /
     hex_poly.area` (razão de áreas em graus EPSG:4674) e MANTÉM se
     `fracao_terra >= settings.M1_HEX_LAND_FRACTION_MIN`; senão descarta.
   - Interseção polígono×polígono aplicada SÓ aos hexes hoje-descartados (centróide fora).
   - `gerar_hexagonos_validos_uf` agora retorna `(validos, removidos, recuperados_costeiros,
     fracao_terra_vec)`; novo parâmetro opcional `coletar_fracao_terra=False` (quando True,
     retorna o vetor `[(hex_id, fracao_terra), ...]` dos hexes hoje-descartados que
     sobrepõem terra, para re-filtragem barata do leque) e `land_fraction_min` opcional.
   - `UFMetrica` ganhou `recuperados_costeiros`; caller interno `executar_base_h3_brasil`
     atualizado (desempacota 4-tupla); relatório e dict de retorno incluem o novo contador.
   - Docstring (topo) atualizada para descrever o critério híbrido (o comentário enganoso
     anterior afirmava que costeiros já eram preservados — corrigido).
2. **`src/motor_expansao/config.py`**: adicionado `M1_HEX_LAND_FRACTION_MIN: float = 0.20`
   nas DUAS classes `Settings` (pydantic e fallback). Nenhum parâmetro canônico alterado.
3. **Testes** em `tests/integration/test_base_h3_brasil.py` (mockam h3 + shapely.box/Polygon):
   - existente atualizado p/ 4-tupla (mar aberto descartado);
   - costeiro mantido pelo limiar (frac 0.25 >= 0.20);
   - mar aberto descartado (frac 0 < 0.20);
   - interior NÃO avalia polígono (`cell_to_boundary` não chamado — custo zero);
   - borda no limiar exato 0.20 (regra `>=`);
   - re-filtragem do leque (vetor: 0.22 entra em 0.15/0.20, sai em 0.25/0.30).
4. **Script de medição** `scripts/medir_impacto_litoral_blk_fix_06.py` (NOVO, scratch):
   regenera base H3 no piso 0.15 em `data/staging/brasil_litoral_tmp/uf=XX/`, persiste o
   vetor de `fracao_terra` (`fracao_terra_descartados.parquet`), monta o leque por
   re-filtragem, e roda o DELTA CARO (estrutural+priorização+camada de oportunidade em
   SCRATCH, todos paths `*.tmp`) para 0.20 e 0.30. NÃO usa `rodar_pipeline_fase1_brasil`
   (que escreveria oficiais via BI exports); chama as funções de baixo nível com paths
   scratch explícitos. Tempo total: ~265 s, exit 0.
5. **Relatório** `data/reports/base_h3_litoral_impacto.md` (NOVO) + CSV auxiliar
   `data/reports/base_h3_litoral_leque.csv` (`sep=";"`, `utf-8-sig`).

## Arquivos alterados/criados
- `src/motor_expansao/pipelines/m1/base_h3_brasil.py` — critério híbrido, `_fracao_terra_hex`,
  retorno 4-tupla, `UFMetrica.recuperados_costeiros`, docstring, relatório/totais.
- `src/motor_expansao/config.py` — NOVO `M1_HEX_LAND_FRACTION_MIN = 0.20` (2 classes).
- `tests/integration/test_base_h3_brasil.py` — teste existente atualizado + 5 casos novos.
- `scripts/medir_impacto_litoral_blk_fix_06.py` — NOVO (medição em scratch).
- `data/reports/base_h3_litoral_impacto.md` — NOVO (relatório de impacto, insumo da DEC).
- `data/reports/base_h3_litoral_leque.csv` — NOVO (CSV auxiliar do leque).

## Números-chave do relatório (para o orquestrador apresentar ao humano)
- **Total oficial atual:** 1.537.950 hexes (critério centróide).
- **Leque (recuperados / total após):** 0.15→494 (1.538.444); **0.20→474 (1.538.424)**;
  0.25→428 (1.538.378); 0.30→383 (1.538.333).
- **DELTA CARO (impacto nos hexes EXISTENTES, 0.20 E 0.30 — REAL, base nacional completa):**
  mediana de delta = **0.0** em `renda_pct_nacional`, `pop_pct_nacional` e
  `score_priorizacao`; deslocamento MÁXIMO de `score_priorizacao` = **0.01**; **0 hexes**
  mudam além de ±0.5. O risco ALTO do plano (deslocamento de percentis) NÃO se materializou
  nesta escala (474/383 novos sobre 1,54 M).
- **Top-20%/UF (`brasil_priorizados`):** 0.20 → entram 136 / saem 41; 0.30 → entram 104 /
  saem 28 (entrantes incluem os próprios costeiros recuperados; saídas são hexes de borda
  deslocados pela leve mudança de contagem de floor(20%)).
- **Quartis de `fracao_terra` dos que entram em 0.20:** min 0.20 / q25 0.33 / mediana 0.40 /
  q75 0.46 / max 0.86 (maioria bem acima do piso — não são "esquinas" oceânicas).
- **Repro do litoral (hexes hoje AUSENTES recuperados):**
  - Praia Grande (SP): hex `87a810c02ffffff`, fracao_terra=**0.231**, reaparece a partir de
    **0.15/0.20** (SAI em 0.30).
  - Litoral RJ (Rio): hex `87a8a078cffffff`, fracao_terra=**0.401**, reaparece em
    **0.15/0.20/0.25/0.30**.
- **CAVEAT (honestidade):** as colunas `soma_pop_total`/`soma_populacao_proxy` do leque são
  SUPERESTIMADAS (não são massa real). A `populacao_proxy` do M1 divide a pop municipal pelo
  nº de hexes do município; ao enriquecer só os recuperados em isolamento, o denominador
  colapsa e cada hex recebe ~a pop do município inteiro (ex.: hex do litoral RJ ~6,2 M). Use
  só como ordenação relativa. O número CONFIÁVEL para a DEC é o DELTA CARO (base completa),
  que mostra impacto ~nulo. `renda_media_entram` (per capita) não sofre esse viés.

## Delta caro: NÃO ficou pendente
O delta caro RODOU integralmente em scratch para 0.20 E 0.30 (enriquecimento nacional
completo, percentis + score + top-20%/UF). Números reais acima. 0.15 e 0.25 ficaram só com
contagem/massa (leque), por design do plano v2.

## Validações (saída literal)
- `python -m pytest -n auto -q tests/integration/test_base_h3_brasil.py tests/integration/test_streamlit_app.py tests/unit/test_config_fallback.py`
  → **`181 passed, 16256 warnings in 20.64s`** (rodado 2x, idêntico). Escolhi esse subconjunto:
  base_h3 (alvo direto do critério), streamlit_app (smoke do app que lê config/artefatos),
  config_fallback (blindagem do novo parâmetro no ramo sem pydantic-settings). A SUÍTE FULL é
  do QA — NÃO rodada aqui (regra do run-cycle).
- `python -c "import streamlit_app; print('import ok')"` → `import ok` (warnings esperados de
  bare mode).
- `python scripts/medir_impacto_litoral_blk_fix_06.py` → exit 0; base scratch (1.538.444 no
  piso 0.15), vetor (577 hexes), leque, delta caro 0.20 e 0.30, repro do litoral, relatório
  escrito. Tempo total ~265 s.

## Guardrails confirmados
- `score_priorizacao` / `hex_score_estrutural` / fórmula / pesos (renda=0.40 / pop=0.60):
  **NÃO alterados** (Fase A não toca score oficial).
- Parâmetros canônicos (`H3_RESOLUTION`, `DIST_MIN_ULTRA_KM`, `RENDA_MIN`, `AREA_*`,
  `M1_*` existentes): **inalterados**. Apenas ADICIONADO `M1_HEX_LAND_FRACTION_MIN = 0.20`.
- **NENHUM artefato oficial escrito.** Verificado: `brasil_estrutural.parquet`,
  `brasil_priorizados.parquet`, `hexagonos_brasil_oportunidades.parquet`,
  `hexagonos_brasil_dashboard.parquet`, `hexagonos_mapa_sample.parquet`,
  `top_oportunidades_resumo.csv`, `resumo_por_uf.csv` e `data/reports/base_h3_brasil.md`
  mantêm mtime de 2026-05-26 (intactos).
- **Prova de scratch** (tudo gravado fora dos caminhos oficiais):
  `data/staging/brasil_litoral_tmp/` (base piso 0.15 + `fracao_terra_descartados.parquet`),
  `data/staging/brasil_litoral_tmp_020/`, `.../brasil_litoral_tmp_030/`,
  `data/staging/brasil_estrutural.020.tmp.parquet`, `.030.tmp.parquet`,
  `data/staging/brasil_priorizados.020.tmp.parquet`, `.030.tmp.parquet`,
  `data/staging/hexagonos_brasil_oportunidades.0NN.tmp.parquet`,
  `data/staging/hexagonos_brasil_dashboard_base.0NN.tmp.parquet`,
  `data/staging/brasil_estrutural_tmp_0NN/`, `data/reports/camada_oportunidade_fase1.0NN.tmp.md`,
  `data/reports/base_h3_litoral_impacto.md`, `data/reports/base_h3_litoral_leque.csv`.
  (Estes scratch serão removidos na Fase B / housekeeping, conforme plano.)

## Para o CHECKPOINT (preencher a DEC-002 com os números acima)
O esboço da DEC-002 está no handoff do Planner (campos LEQUE + DELTA CARO + repro).
Recomendação técnica do Builder (NÃO é decisão): com impacto ~nulo nos existentes em ambos
os limiares, a escolha 0.20 vs 0.30 é puramente sobre cobertura do litoral povoado vs.
conservadorismo de borda; 0.20 (candidato humano) inclui Praia Grande e a maioria dos
recuperados tem fracao >= 0.33 na mediana. A decisão final é do humano na DEC-002.
