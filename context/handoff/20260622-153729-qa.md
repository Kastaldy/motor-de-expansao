# Handoff — QA → (ciclo concluído)

## Skill que gerou este handoff
QA

## Próxima Skill recomendada
(nenhuma — bloco aprovado; merge/housekeeping humano)

## Bloco
BLK-DIM-21 — UI: gráficos financeiros e curva de maturidade na aba de viabilidade

## Criticidade
**Média** — enriquecimento visual; READ-ONLY sobre M1. Esteira: BO → Planner → Builder → QA (sem gate humano; bloco loop-safe).

---

## VEREDITO: APROVADO

---

## 1. Gate obrigatório — evidências

| Etapa | Comando | Resultado |
|---|---|---|
| 1. Dep BLK-UI-09 | `pip install "openlocationcode>=1.0.1" -q` | OK |
| 2. Suite COMPLETA (serial) | `python -m pytest -q` | **1046 passed, 4 skipped, 18 warnings** em 171s |
| 3. Lint | `ruff check .` | **All checks passed!** |
| 4. Tipos | `mypy simulador.py pages.py --ignore-missing-imports` | **Success: no issues found in 2 source files** |
| 5. Import | `python -c "import streamlit_app"` | **import ok** |

Nota: suite serial autorizada (pytest-xdist falha com INTERNALERROR no Python 3.14). Os warnings são pré-existentes (CRS geográfico em fase_a_censo2022_setores, downcasting em calcular_colunas_mercado), não relacionados ao bloco.

## 2. Verificações específicas do BLK-DIM-21

- **CA-11a..h: 8 PASS** (`pytest test_simulador.py -k "serie_mensal"` → 8 passed, 35 deselected). Suite do simulador completa = 43 testes (35 CA-01..10 + 8 CA-11).
- **`gerar_serie_mensal()` existe** em `simulador.py` (linha 340), retorna `list[dict]` com 60 elementos (meses 1..60), campos `mes`/`alunos_balcao`/`faturamento_mensal`/`ebitda_mensal`/`fcf_acumulado`.
- **Delegação sem duplicação confirmada**: `viabilidade()` (linha 282) substituiu o loop `for t in range(1,61)` por uma chamada a `gerar_serie_mensal()` e extrai o payback da série (primeiro mês com `fcf_acumulado >= 0`). `capex_efetivo` para ROIC permanece local. CA-11f valida que o payback da série == payback de `viabilidade()`.
- **`viabilidade_ponto.py` NÃO alterado pelo BLK-DIM-21**: `git show b016733 -- viabilidade_ponto.py` vazio. (O diff vs `main` que mostra esse arquivo pertence a ciclos anteriores do branch — BLK-DIM-19/20 — não a este bloco.)
- **`config.py` (dimensionamento) NÃO alterado pelo BLK-DIM-21**: `git show b016733 -- config.py` vazio.
- **Artefatos M1 intocados**: commit `b016733` toca apenas `pages.py`, `simulador.py`, `test_simulador.py` (+ handoff/current_task). ZERO arquivos em `pipelines/m1`, ZERO parquets, ZERO `config.py` raiz / score / pesos.
- **Seção 8 em pages.py confirmada**: import `plotly.graph_objects as go` (linha 8), import `gerar_serie_mensal` (linha 103) e `SIM_MATURACAO_MESES` (linha 100); Seção 8 dos gráficos a partir da linha 3506 com `gerar_serie_mensal(...)` e vline de maturação `min(SIM_MATURACAO_MESES, 60)`.

## 3. Housekeeping (6.0)
- `housekeeping_move_block.py BLK-DIM-21 --date 2026-06-22` → OK: movido para `completed.md`, stub no `backlog.md`.
- `housekeeping_move_block.py BLK-DIM-21 --check` → OK (EXIT 0).

## 4. Guardrails
READ-ONLY sobre M1 confirmado. Pesos `renda=0.40`/`pop=0.60`, `score_priorizacao`, `hex_score_estrutural`, carteira, plano e artefatos oficiais INALTERADOS. Sem dependências novas (plotly já é base). DEC-001 intacta.

---

**VEREDITO: APROVADO**
