# Handoff — Builder (follow-up BLK-ACENTO-01)

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer (re-verificação focada)

## Bloco executado
BLK-ACENTO-01 — follow-up do QA (mesmo bloco): acentuar o eixo x / legenda do gráfico
`build_faixa_comparison_figure` (item 2.2c do plano do Planner), que ainda exibia os SLUGS BRUTOS
do enum (`prioridade_maxima`, `media`, `baixa`, ...) em vez dos labels acentuados.

## O que foi feito (display-only, READ-ONLY M1)
Aplicada a receita exata do item 2.2c, preservando as cores por faixa:
- **`dashboard/constants.py`:** criado `FAIXA_COLORS_POR_LABEL = {FAIXA_LABELS.get(k, k): v for k, v
  in FAIXA_COLORS.items()}` logo após `FAIXA_COLORS` — dict de cores keyed pelo LABEL acentuado,
  DERIVADO de `FAIXA_COLORS` + `FAIXA_LABELS` (não duplica cores à mão; acompanha mudanças de
  `FAIXA_COLORS`). `FAIXA_COLORS` e `FAIXA_ORDEM` (valores brutos) INTOCADOS.
- **`components.py` `build_faixa_comparison_figure`:** a coluna de EXIBIÇÃO `faixa_label` passou a
  conter o LABEL acentuado (`FAIXA_LABELS.get(f, f)`), como `pd.Categorical` ORDENADO por
  `_faixa_label_order = [FAIXA_LABELS.get(f, f) for f in FAIXA_ORDEM]` (o eixo não desordena). O
  `px.bar` agora usa `color_discrete_map=FAIXA_COLORS_POR_LABEL` + `category_orders={"faixa_label":
  _faixa_label_order}`. `x`/`color`/`labels` continuam apontando para `faixa_label` (agora
  acentuado). O **valor bruto de `faixa_oportunidade` na origem NÃO foi alterado** — a coluna
  acentuada é derivada só para o gráfico. Cada barra mantém a MESMA cor por faixa (o mapa novo é
  keyed pelo mesmo label que a coluna).
- **Teste novo** `test_build_faixa_comparison_figure_eixo_e_cores_acentuados` em
  `tests/integration/test_streamlit_app.py`: confirma que (a) o eixo x / legenda usam labels
  acentuados e nunca os slugs brutos, e (b) a cor de cada faixa é preservada
  (`FAIXA_COLORS_POR_LABEL[label] == FAIXA_COLORS[valor_bruto]`).

Nenhum teste existente travava o eixo bruto desse gráfico (grep confirmou: a única ocorrência de
`"faixa_label"` em teste é o payload-do-layer do MAPA — coluna auxiliar do tooltip m1, sem relação
com este gráfico); nenhum assert precisou ser atualizado em lockstep.

## Arquivos alterados (apenas este follow-up)
- `src/motor_expansao/dashboard/constants.py` — +`FAIXA_COLORS_POR_LABEL` (derivado).
- `src/motor_expansao/dashboard/components.py` — import de `FAIXA_COLORS_POR_LABEL`; eixo x/legenda
  do `build_faixa_comparison_figure` acentuados via coluna de exibição categórica + mapa de cores
  por label + `category_orders`.
- `tests/integration/test_streamlit_app.py` — +teste
  `test_build_faixa_comparison_figure_eixo_e_cores_acentuados`.

## Validações executadas (saída literal)
- `python -m ruff check .` → **All checks passed!**
- `python -m mypy src` → **Found 7 errors in 7 files** — TODOS pré-existentes/ambientais
  ("Library stubs not installed for requests" em `api/*` e `pipelines/m1/*`, arquivos NÃO tocados).
  **ZERO erro novo** (constants.py/components.py limpos).
- `python -c "import streamlit_app; print('import ok')"` → **import ok**.
- `python -m pytest -q tests/integration/test_streamlit_app.py` (serial) → **232 passed, 0 failed**
  (inclui os 2 novos testes de faixa; warnings = DeprecationWarning do pytest-asyncio no Py 3.14).
- Testes novos isolados (figura + format_func) → **2 passed**.

## Problemas encontrados
- Nenhum. Confirmado que o gráfico exibe as barras com a MESMA cor de antes, agora com rótulos
  acentuados.

## Pendências
- QA: re-verificação focada. Rodar a suíte completa como gate único; recomendado SERIAL
  (`pytest -q`) — o `-n auto`/xdist segue instável (execnet EOFError) no Python 3.14 desta máquina.
- Revisão visual do gráfico "Score médio por faixa de oportunidade" (eixo/legenda acentuados,
  cores por faixa inalteradas).

## Riscos remanescentes
- Nenhum novo. `FAIXA_COLORS_POR_LABEL` é derivado de `FAIXA_COLORS`; se algum dia um label de
  `FAIXA_LABELS` colidir (dois valores brutos → mesmo texto), o dict colapsaria uma cor — hoje os
  6 labels são únicos, sem risco.

## Guardrails verificados
- score_priorizacao não alterado: sim (só camada de visualização; nenhuma fórmula/peso/artefato).
- Artefatos M1 preservados: sim (diff deste follow-up restrito a `dashboard/constants.py`,
  `dashboard/components.py`, teste; nada de pipelines/censo/config/core/parquet).
- Dashboard offline mantido: sim (nenhuma dependência de rede).
- Identificadores/valor bruto intocados: sim (`faixa_oportunidade`, `FAIXA_ORDEM`, `FAIXA_COLORS`
  preservados; a coluna acentuada é derivada só para o gráfico).
