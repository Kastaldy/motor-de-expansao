# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA

## Bloco
BLK-ATR-03-FU1 — Re-rodar o teste de estrutura (matriz vs composto) sobre o Huff DENSO.

## O que foi implementado

### 1. Módulo novo `src/motor_expansao/demanda_revelada/estrutura_funil_densa.py`
Refaz o teste de ESTRUTURA do ATR-03 substituindo SÓ o eixo `share_captura_huff` pelo share
recomputado geometricamente sobre a base DENSA (`concorrentes_densos.parquet`), reusando as funções
PURAS existentes SEM alterá-las:
- `substituir_share_denso(df_join, conc_lat, conc_lng, beta)` — função PURA: recomputa o share por
  hex via `calcular_share_por_hex` (puro, sem alvo) e SUBSTITUI a coluna `share_captura_huff` numa
  CÓPIA do frame; NÃO toca `membros`.
- `avaliar_estrutura_densa(df_join, df_denso)` — função PURA: extrai `(conc_lat, conc_lng)` via
  `_coords_densas`; seleciona beta OUT-OF-FOLD via `calibrar_huff_captura` (recomputado, não
  hardcoded); substitui o share denso; roda `avaliar_estrutura_funil` (harness ATR-03 INTOCADO).
  Retorna `(EstruturaFunilResult, beta_selecionado)`.
- `executar(...)` — caminho de disco (`# pragma: no cover`): carrega o join via `_carregar_join` de
  `estrutura_funil`, lê a base densa, chama `avaliar_estrutura_densa`, grava o `.md` via
  `escrever_relatorio` (rede anti-PII embutida).

`__all__`: `substituir_share_denso`, `avaliar_estrutura_densa`, `executar`,
`RELATORIO_ESTRUTURA_DENSA_DEFAULT`, `DENSO_DEFAULT`.
ZERO import de `pipelines/`, `pipelines/m1/`, `censo_*`, `dashboard/`, `api/`, `config.py` raiz.

### 2. Teste novo `tests/unit/demanda_revelada/test_estrutura_funil_densa.py`
6 testes obrigatórios, fixtures 100% sintéticas com hexes res-7 REAIS (`h3.latlng_to_cell`):
1. `test_substituir_share_denso_recomputa` — share antigo 1.0 + concorrentes → share < 1.0; `membros` intocado; original não mutado.
2. `test_share_nao_usa_membros` (DEC-009) — dois frames só com `membros` diferente → share idêntico (`np.array_equal(equal_nan=True)`).
3. `test_avaliar_estrutura_densa_roda` — retorna `(EstruturaFunilResult, beta)` com `beta in BETA_GRID`, `veredito in {GO-composto, matriz}`, `composto.n > 0`.
4. `test_relatorio_denso_sem_pii` — `COLUNAS_PII_PROIBIDAS` não aparecem como tokens isolados.
5. `test_isolamento_imports_ast` — AST: nenhum import começa com `motor_expansao.pipelines/dashboard/api/censo`.
6. `test_membros_intocado` — `pd.testing.assert_series_equal` em `membros` após substituição.

### 3. Relatório real gerado (gitignored, NÃO commitado)
`data/analysis/estrutura_funil_densa.md` materializado via `executar()`.

## Resultado real (base densa) — números do relatório

| métrica | ATR-03 original | ATR-03-FU1 (Huff DENSO) |
| --- | ---: | ---: |
| Beta denso selecionado (OOF) | (0.5) | **0.5** |
| R²_oof composto | +0.48 | **+0.5653** (IC95 [+0.5450, +0.5839]) |
| R²_oof melhor eixo isolado | +0.37 (disputa) | **+0.4885** (disputa) |
| ganho_material do composto | — | **+0.0768** (> 0.01) |
| redundante | — | **False** |
| cobertura Huff (% share < 1.0) | ~62% | **96.0%** |
| n_join / n_pos_gate | — | 16.411 / 4.630 |
| método | — | kfold_5x5 |
| **Veredito** | GO-composto | **GO-composto** |

Eixos isolados (OOF): sociodemo +0.2592, mercado +0.1054, **disputa +0.4885**, censitário (auditoria) +0.0082, pesos_iguais (auditoria) +0.5021.
Spearman bruto vs membros: share_captura_huff **−0.696**, score_priorizacao +0.506, residual +0.320, censitário +0.091.
Coefs Ridge do composto: disputa +0.0383 (dominante), sociodemo +0.0166, mercado +0.0113.

**Comparação com ATR-03 original:** cobertura do Huff subiu de ~62% → **96%** (a base densa
preenche a maioria dos hexes viáveis com concorrente na janela), o eixo `disputa` denso ficou mais
forte (+0.37 → +0.4885) e o composto subiu (+0.48 → +0.5653). O ganho material sobre o melhor eixo
(+0.0768) e a não-redundância confirmam **GO-composto** honesto sobre a base densa. Beta reproduziu
0.5 (não hardcoded — recomputado OOF no mesmo runtime). Veredito preservado (GO-composto), agora com
sinal mais robusto.

## Validações (saída literal)

**ruff** — `All checks passed!`
**mypy** — `Success: no issues found in 1 source file`
**pytest subset** (`test_estrutura_funil_densa.py` + `test_estrutura_funil.py` + `test_huff_captura.py`):
```
................................                                         [100%]
32 passed in 102.41s (0:01:42)
```
**smoke import** — `import ok`
**mtime dos artefatos READ-ONLY (antes → depois, INALTERADO):**
```
data/staging/concorrentes_densos.parquet: 1783429833 → 1783429833
data/staging/brasil_estrutural.parquet:   1780501622 → 1780501622
data/staging/brasil_priorizados.parquet:  1780501632 → 1780501632
```
`data/analysis/estrutura_funil_densa.md` confirmado gitignored (`git check-ignore` casa).

## Arquivos afetados
- `src/motor_expansao/demanda_revelada/estrutura_funil_densa.py` (NOVO)
- `tests/unit/demanda_revelada/test_estrutura_funil_densa.py` (NOVO)
- `data/analysis/estrutura_funil_densa.md` (NOVO, gitignored — NÃO commitado)
- `tasks/current_task.md`, `context/handoff.md`, `context/handoff/20260707-164919-builder.md`

## Guardrails honrados
- §5 READ-ONLY M1: zero recálculo de score/pesos/carteira/plano/artefatos; mtimes INALTERADOS.
- DEC-008: beta e alpha out-of-fold; R² in-sample só auditoria (fora do veredito); IC95 seed=42; matriz seria veredito válido.
- DEC-009: `membros` só ALVO; share PURO/geométrico (testado que independe de `membros`).
- DEC-012: pacote disjunto (teste AST verde); sem PII; fixtures sintéticas; fonte real nunca tocada.
- `estrutura_funil.py`, `huff_captura.py`, `concorrentes_densos.py` NÃO modificados (só importados).

## Para o QA
- Rodar suite full (gate único) com `pytest -n auto`.
- Confirmar ruff+mypy limpos e smoke import.
- Confirmar mtimes dos 4 artefatos M1 + `concorrentes_densos.parquet` inalterados.
- O `.md` é gitignored; NÃO deve entrar no commit (só o módulo + o teste).
- Veredito honesto: GO-composto na base densa (composto +0.5653 vence disputa +0.4885 por +0.0768, não redundante, cobertura 96%).
