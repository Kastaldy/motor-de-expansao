# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
REVISÃO HUMANA do plano (gate Alta) → depois Builder (opus) → QA (opus 4.8)

## Entendimento da tarefa
BLK-UI-02: três ajustes de UX READ-ONLY no Mapa Territorial — (1) destacar o campo de busca por coordenada na sidebar, (2) reduzir o corte do tooltip na borda inferior via CSS de fonte/padding/largura, (3) comunicar que filtrar por município densifica o mapa — sem tocar score, pesos, cap anti-OOM (Bloco 6), carga lazy (Bloco 4) ou render lazy (Bloco 5).

## Confirmações de leitura do código (file:line REAIS, conferidos agora)
- `render_coord_search_sidebar` em `pages.py:523-541` — hoje: `st.sidebar.markdown("---")` + `### Busca por coordenada` + `st.sidebar.caption(...)` + `st.sidebar.text_input(... key="coord_search_input")`. Assinatura `() -> tuple[float,float] | None`. Chamada única em `streamlit_app.py:470`, SEMPRE (fora do branch de aba) — Bloco 5 preservado. NÃO mudar a assinatura (o teste `test_streamlit_app.py:4270` mocka `render_coord_search_sidebar` sem args).
- `_shared_map_tooltip` em `components.py:1047-1073` — 14 linhas; `style` SEM `fontSize`/`padding`/`maxWidth`/`lineHeight`. **Usado SOMENTE por `build_map_figure` (modo M1)**, que preenche `tooltip_line_1..14` integralmente (`_apply_hex_tooltip_fields` modo "m1", linhas 512-526).
- `_hybrid_compact_tooltip` em `components.py:1083-1104` — 8 linhas; mesmo `style` sem CSS de tamanho. **Usado pelos modos híbrido E residual** (`build_hybrid_map_figure:1832`, `build_residual_heatmap_figure:1977`, e o builder unificado), pois `_HYBRID_TOOLTIP_SHOW_DETAIL=False` (default). O modo "hybrid" de `_apply_hex_tooltip_fields` preenche exatamente `tooltip_line_1..8` (linhas 501-504).
- **Achado decisivo sobre "enxugar linhas vazias":** na config default (`_HYBRID_TOOLTIP_SHOW_DETAIL=False`), AMBOS os templates são preenchidos por completo por seus consumidores — `_shared_map_tooltip` (14) só roda no modo M1 que preenche 1..14; `_hybrid_compact_tooltip` (8) roda nos modos que preenchem 1..8. **Não há linhas sistematicamente vazias para remover sem perder informação.** Portanto a mitigação do #2 é PURAMENTE CSS (sem mexer no HTML/linhas), exatamente o caminho que o BO marcou como mais seguro.
- `build_map_scope_caption` em `components.py:383-400` — ramo `capped=True` já diz "Aplique filtros de municipio para ver o recorte completo"; ramo `capped=False` (linhas 397-400) NÃO menciona município. Consumido em `pages.py:2671-2675` dentro de `render_mapa_pydeck_fragment`.
- `render_mapa_territorial` em `pages.py:2704-2818+` — caption introdutório em `2730-2733`; seletor de Modo de cor em `2746-2752`; multiselect de Overlays em `2757-2763`. Bom ponto para a dica de densificação.
- Caps em `constants.py:98-103` (`MAP_POINT_LIMIT=35000`, `MAP_POINT_LIMIT_LARGE=18000`) — NÃO TOCAR.
- **`selected_cities` entra no `scope` ANTES do downsample em TODOS os builders (confirmado lendo cada um):**
  - `build_map_figure`: scope+`selected_cities` em `1418-1427`; `_downsample_map_index` em `1466`.
  - `build_hybrid_map_figure`: `selected_cities` em `1679-1681`; downsample em `1702`.
  - `build_unified_map_figure` (caminho de score híbrido): scope idêntico, downsample em `1880`.
  - `build_residual_heatmap_figure`: `selected_cities` em `1867-1869`; downsample em `1880-1885`.
  - Logo, selecionar um município já restringe o universo ANTES do cap → densidade total do recorte. O #3 é COMUNICAÇÃO, não nova lógica. O cap fica intocado.
- Teste-contrato existente: `test_streamlit_app.py:329-334` exige a substring exata `"todos os hexagonos validos da UF selecionada"` no ramo não-capped de `build_map_scope_caption` → a alteração do #3 deve ANEXAR a dica preservando essa substring, ou atualizar o teste (ver Passo 5).

## Decisões de produto para o GATE HUMANO (D1..D4)
> Esteira Alta: o humano (Felipe/Vini) confirma estas decisões ANTES do Builder. Para cada uma há recomendação do Planner.

- **D1 — Estilo do destaque do campo de coordenada (#1).**
  - (A) Trocar `### Busca por coordenada` por `st.sidebar.info("**Busca por coordenada** — localize um hexágono pela coordenada. Offline, sem API.")` ENVOLVENDO só o cabeçalho/caption, mantendo o `text_input` logo abaixo. Container azul nativo, destaque claro, ruído mínimo.
  - (B) Adicionar ícone/emoji ao heading (ex.: `### 📍 Busca por coordenada`) + manter caption. Mais leve, menos destaque.
  - (C) `st.sidebar.success(...)` (container verde) em vez de info.
  - **Recomendação: D1=A** (`st.sidebar.info` no cabeçalho). Destaque consistente com o restante do app, sem somar mais um `---`. O BO alertou contra ruído visual (já há vários `---`); manter o `---` atual de `pages.py:525` e substituir apenas o `markdown("### ...")`+`caption` por um único `st.sidebar.info`. NÃO mover a chamada para dentro de aba (Bloco 5).

- **D2 — Valores de CSS do tooltip (#2).**
  - Adicionar ao `style` dict de AMBOS os tooltips (`_shared_map_tooltip` e `_hybrid_compact_tooltip`): `"fontSize"`, `"padding"`, `"maxWidth"`, `"lineHeight"`.
  - (A) Conservador: `fontSize="11px"`, `padding="6px 8px"`, `maxWidth="260px"`, `lineHeight="1.25"`.
  - (B) Mais compacto: `fontSize="10px"`, `padding="5px 7px"`, `maxWidth="240px"`, `lineHeight="1.2"`.
  - **Recomendação: D2=A** (11px / 6px 8px / 260px / 1.25). 11px equilibra legibilidade e altura; o tooltip de 14 linhas (modo M1) cai ~30% de altura vs. atual sem fonte definida, suficiente para caber em 1080p ao hover na borda inferior, sem sacrificar leitura dos valores (risco apontado pelo BO de "fonte pequena demais" mitigado). NÃO enxugar linhas do HTML (achado acima: não há linha sistematicamente vazia na config default).

- **D3 — Texto e local da dica de densificação por município (#3).**
  - Parte (a) `build_map_scope_caption` ramo não-capped: anexar uma frase ao final do caption atual, preservando a substring testada. Texto proposto: `" Para densidade total de uma area, selecione um municipio no filtro lateral."`
  - Parte (b) `render_mapa_territorial`: acrescentar UMA frase ao caption introdutório de `pages.py:2730-2733` (NÃO um novo widget/`st.info` para não poluir os controles). Texto proposto: `" Dica: filtre por municipio na barra lateral para ver o recorte com densidade total."`
  - Alternativas: (B) só a parte (a); (C) só a parte (b); (D) parte (b) como `st.info` separado.
  - **Recomendação: D3 = (a)+(b) anexando frase aos captions existentes** (não criar widgets novos). Comunica em dois pontos onde o usuário olha (caption do mapa e controles), sem ruído extra e sem tocar cap/downsample. Mantém a substring exigida pelo teste (a frase é ANEXADA).

- **D4 — Atualização do teste `test_build_map_scope_caption_reflete_todos_os_hexes_da_uf` (#3, parte a).**
  - (A) Manter o teste como está (a substring `"todos os hexagonos validos da UF selecionada"` continua presente por anexação) e ADICIONAR uma asserção nova de que a dica de município aparece no ramo não-capped.
  - (B) Só manter sem nova asserção.
  - **Recomendação: D4=A** — adicionar asserção positiva da nova dica para travar o comportamento (TDD leve), sem remover a asserção existente.

## Plano técnico (numerado — Builder segue sem decidir escopo)
1. **(#1) `pages.py:525-527` em `render_coord_search_sidebar`** — manter `st.sidebar.markdown("---")` (linha 525). Substituir as linhas 526-527 (`st.sidebar.markdown("### Busca por coordenada")` + `st.sidebar.caption("Localize um hexagono pela coordenada. Offline, sem API externa.")`) por **um único** `st.sidebar.info("**Busca por coordenada** — localize um hexagono pela coordenada. Offline, sem API externa.")` (D1=A). Manter intactos: o `text_input` (528-532, mesmo `key="coord_search_input"`), o parse e o `st.sidebar.error` (535-541), e a assinatura `() -> tuple[float,float] | None`. NÃO mover a chamada de `streamlit_app.py:470`.

2. **(#2) `components.py:1066-1072` em `_shared_map_tooltip`** — adicionar ao dict `style` as chaves (D2=A): `"fontSize": "11px"`, `"padding": "6px 8px"`, `"maxWidth": "260px"`, `"lineHeight": "1.25"`. Preservar as chaves atuais (`backgroundColor`, `color`, `border`, `borderRadius`, `fontFamily`) e o `html` (14 linhas) INALTERADO.

3. **(#2) `components.py:1097-1103` em `_hybrid_compact_tooltip`** — adicionar ao dict `style` as MESMAS chaves do Passo 2 (mesmos valores). Preservar `html` (8 linhas) e demais chaves de `style`.

4. **(#3a) `components.py:397-400` em `build_map_scope_caption` (ramo `capped=False`)** — ANEXAR ao final da string retornada a frase da dica (D3): `" Para densidade total de uma area, selecione um municipio no filtro lateral."`. **NÃO** alterar a substring `"todos os hexagonos validos {scope_label}"` (testada). NÃO alterar o ramo `capped=True` (já tem a orientação de município). NÃO alterar a assinatura.

5. **(#3b) `pages.py:2730-2733` em `render_mapa_territorial`** — ANEXAR ao texto do `st.caption(...)` introdutório a frase (D3): `" Dica: filtre por municipio na barra lateral para ver o recorte com densidade total."`. NÃO criar widget novo; NÃO tocar nos controles de modo/overlay; NÃO tocar `build_unified_map_figure`, scope, downsample ou caps.

6. **(testes) `tests/integration/test_streamlit_app.py`**:
   - 6a. `test_build_map_scope_caption_reflete_todos_os_hexes_da_uf` (linha 329) — manter as asserções existentes (substring `"todos os hexagonos validos da UF selecionada"` e `"1.234"`) e ADICIONAR (D4=A) `assert "municipio" in caption` (ou substring da dica nova) no ramo não-capped.
   - 6b. Adicionar um teste novo de CSS do tooltip: `assert _shared_map_tooltip()["style"]["fontSize"] == "11px"` e `"maxWidth" in _shared_map_tooltip()["style"]`; idem para `_hybrid_compact_tooltip()`. (Importar via `streamlit_app.` ou do módulo `components`, conforme padrão do arquivo de teste.)
   - 6c. (Opcional, se houver helper de smoke da sidebar já existente) — não é obrigatório criar teste de `st.sidebar.info`; o destaque é cosmético. NÃO criar teste frágil que dependa de internals do Streamlit. Se desejável, um teste mínimo que mocke `st.sidebar.info` e verifique que `render_coord_search_sidebar` ainda retorna `None`/coord conforme input já cobre a não-regressão de fluxo.

## Arquivos afetados
- `src/motor_expansao/dashboard/pages.py` (Passos 1, 5)
- `src/motor_expansao/dashboard/components.py` (Passos 2, 3, 4)
- `tests/integration/test_streamlit_app.py` (Passo 6)

## Dependências
- Nenhuma dependência nova. Tudo é CSS-em-dict / markdown / strings. Sem novas libs, sem API ao vivo.

## Riscos técnicos
- **R1 (#2 legibilidade):** `fontSize` muito pequeno cortaria leitura. Mitigado por D2=A (11px, não 10px). Critério de aceite #2 cobre.
- **R2 (#3 teste-contrato):** o teste em `test_streamlit_app.py:329-334` casa substring exata. Mitigado anexando a frase (substring preservada) + Passo 6a. Se o Builder reescrever o caption em vez de anexar, o teste quebra — instrução explícita é ANEXAR.
- **R3 (Bloco 6 anti-OOM):** NENHUM passo toca `MAP_POINT_LIMIT`, `MAP_POINT_LIMIT_LARGE`, `_downsample_map_index`, o `scope` dos builders ou o payload serializado. O payload global ao cliente é IDÊNTICO ao atual (mudanças são CSS de tooltip + strings de caption + um `st.sidebar.info`). Risco de OOM = nulo.
- **R4 (Bloco 5 render lazy):** `render_coord_search_sidebar` continua sendo chamada em `streamlit_app.py:470`, fora do branch de aba; assinatura inalterada → mock do teste 4270 segue válido.
- **R5 (pydeck recorte residual):** o recorte do tooltip na borda é limitação nativa do deck.gl no iframe; a redução de altura ATENUA, não elimina em todos os zooms/posições. O critério de aceite #2 é "cabe sem recorte ao hover próximo da borda inferior em 1080p" — atingível com 14 linhas a 11px/lineHeight 1.25, mas o humano deve estar ciente de que casos extremos (cursor colado no fundo absoluto) podem ainda recortar 1-2 linhas. Sem solução nativa adicional sem trocar o componente (fora de escopo, decisão BLK-UI-01).

## Critérios de aceite finais
- #1: a seção "Busca por coordenada" na sidebar exibe um container `st.sidebar.info` (destaque azul), visualmente diferenciado dos demais filtros; `text_input`, parse e mensagem de erro inalterados; fluxo de retorno preservado.
- #2: ambos os tooltips (`_shared_map_tooltip` 14 linhas e `_hybrid_compact_tooltip` 8 linhas) têm `fontSize`/`padding`/`maxWidth`/`lineHeight` no `style`; tooltip cabe sem recorte ao hover próximo à borda inferior em 1080p no caso típico.
- #3: ramo não-capped de `build_map_scope_caption` e o caption introdutório do Mapa Territorial mencionam o filtro de município como caminho para densidade total; NENHUM cap/downsample/scope alterado; nenhum OOM introduzido.
- READ-ONLY M1: `score_priorizacao`, `hex_score_estrutural`, `score_setor_2022_calibrado`, `score_expansao_hibrido`, `score_oportunidade_residual`, carteira, plano, SAM e artefatos oficiais INALTERADOS.
- ruff + mypy limpos no escopo tocado; smoke import `python -c "import streamlit_app"` ok.

## Validações obrigatórias
- Comando: `python -m pytest -q tests/integration/test_streamlit_app.py` — Critério: não INTRODUZIR novas falhas (a suíte deste arquivo deve passar; ver baseline abaixo).
- Comando (gate único no QA): `python -m pytest -q` (SERIAL — `-n auto` dá INTERNALERROR execnet×Python 3.14 local). Critério: baseline atual `695 passed, 1 skipped, 3 failed PRÉ-EXISTENTES` (drift de CSVs locais gitignored + gate DEC-006 do SAM); o critério é **não introduzir falhas novas**, não bater número.
- Comando: `python -c "import streamlit_app"` — Critério: sem erro de import.
- Comando: `ruff check` + `mypy` no escopo tocado — Critério: limpos.

## Fora de escopo
- Alterar/elevar/remover `MAP_POINT_LIMIT` / `MAP_POINT_LIMIT_LARGE`, `_downsample_map_index` ou o `scope` dos builders.
- Zoom-awareness / eventos de zoom (pydeck não suporta; decisão de produto pré-fixada não inclui).
- Enxugar/remover linhas do HTML dos tooltips (não há linha sistematicamente vazia na config default — achado confirmado; mitigação é só CSS).
- Tocar `render_sidebar_filters`, `apply_global_filters`, carga lazy (Bloco 4), render lazy de abas (Bloco 5).
- Recalcular qualquer score ou artefato M1; reabrir a decisão de produto nº3; dependência de API ao vivo.
- PRD.md e os paths pré-sujos (`data/outputs/setores_censitarios_2022_geo/_metadata.json`, `data/reports/relatorio_pontual_censitario_base_geo.md`).

## Alerta de aprovação
ATENÇÃO: esteira **ALTA — REVISÃO HUMANA DO PLANO OBRIGATÓRIA ANTES DO BUILDER.** Embora a tarefa seja READ-ONLY sobre o M1 (apenas CSS de tooltip, markdown de sidebar e strings de caption — NÃO toca score, pesos, cap anti-OOM, carga/render lazy), ela mexe na navegação do dashboard de produção e em contratos visuais. O humano (Felipe/Vini) deve ler este handoff e confirmar as decisões de produto **D1, D2, D3, D4** antes de o Builder executar. Recomendações do Planner: D1=A (`st.sidebar.info`), D2=A (11px / 6px 8px / 260px / 1.25), D3=(a)+(b) anexando frases aos captions existentes, D4=A (asserção nova mantendo a existente).

## Guardrails ativos
- **Bloco 6 anti-OOM:** `MAP_POINT_LIMIT=35000` / `MAP_POINT_LIMIT_LARGE=18000` INTOCADOS; payload global ao cliente inalterado. Densificação do #3 é via filtro de município já nativo (scope antes do downsample).
- **Bloco 4 carga lazy por UF:** `df` passado aos builders é a partição `uf=XX`; nenhum passo força carga de UFs adicionais.
- **Bloco 5 render lazy de abas:** `render_coord_search_sidebar` segue chamada SEMPRE em `streamlit_app.py:470`, fora do branch de aba; assinatura inalterada.
- **READ-ONLY M1 (§5):** visualizações, CSS de tooltip e dicas de UX não recalculam nem alteram `score_priorizacao`, `hex_score_estrutural`, `score_setor_2022_calibrado`, `score_expansao_hibrido`, `score_oportunidade_residual`, carteira, plano, SAM ou artefatos oficiais.
- **Decisão de produto nº3 pré-fixada:** filtro de município/área (já nativo); sem zoom-awareness, sem mexer no cap.

## Paths pré-sujos (NÃO tocar — alheios ao ciclo)
- data/outputs/setores_censitarios_2022_geo/_metadata.json
- data/reports/relatorio_pontual_censitario_base_geo.md
