# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-SAM-01 — Redefinir o gate do SAM (Faixa M1 elegível AND população ≥ 5000), removendo `top_municipio`
e o piso `tam_populacao_hex > 0`. Camada PARALELA de mercado/residual. READ-ONLY sobre o M1.

**Aprovação humana registrada:** decisões de PRODUTO por Felipe em 2026-06-09; PLANO TÉCNICO aprovado por
Felipe/usuário (vinicius.cruz@ultraacademia.com.br) em 2026-06-10.

## O que foi feito
Executei literalmente os Passos 1–7 do plano técnico aprovado (handoff do Planner):

1. **Passo 1 — Helper compartilhado (fonte única).** Criado `src/motor_expansao/pipelines/pop_corte.py`
   com 4 funções PURAS movidas BYTE-A-BYTE de `dashboard/data.py`: `normalized_join_quality`,
   `has_censo_signal`, `derive_confianca_geografica`, `derive_pop_cut_columns(df, pop_min=5_000)`. Importa
   só `numpy`/`pandas` (sem ciclo: `data.py`→`pop_corte`, `pop_corte`→numpy/pandas). `dashboard/data.py`
   passou a DELEGAR `_normalized_join_quality`/`_has_censo_signal`/`_derive_confianca_geografica`/
   `derive_pop_cut_columns` ao impl compartilhado, preservando 100% as assinaturas públicas e o default
   `POP_MIN_ACIONAVEL`. `build_pop_cut_lookup` permaneceu em `data.py`. Lógica preservada (extração mecânica).

2. **Passo 2 — `SOURCE_REQUIRED_COLS`.** Adicionados SOMENTE `faixa_oportunidade` e `pop_total` (os 3
   drivers do `granular` ficam OPCIONAIS; o helper degrada graciosamente para municipal quando faltam).

3. **Passo 3 — Constante local.** `POP_MIN_SAM_GATE = 5_000` no pipeline (após a linha de
   `SCORE_RESIDUAL_CAPACIDADE_REFERENCIA`), com comentário de espelhamento de `POP_MIN_ACIONAVEL`. NÃO
   importei de `dashboard.constants`; §3/`config.py` intocados.

4. **Passo 4 — Reescrita do gate §5.4.** Antes do gate:
   `df["confianca_geografica"] = derive_confianca_geografica(df)` e
   `df = derive_pop_cut_columns(df, pop_min=POP_MIN_SAM_GATE)`. Gate novo:
   `flag_sam = flag_viavel & faixa_elegivel & flag_pop_ok & ~flag_canibal`
   (`faixa_elegivel = faixa_oportunidade ∈ {baixa,media,alta,prioridade_maxima}`,
   `flag_pop_ok = flag_pop_min_5k`). `flag_sam_fitness = flag_sam` (piso `tam_populacao_hex>0` REMOVIDO).
   `sam_fitness_potencial = np.where(flag_sam_fitness, pd.to_numeric(tam_fitness_potencial, errors="coerce").fillna(0.0), 0.0)`
   (sem NaN, não-negativo). `tese_entrada` e `sam_granularidade` deixados INTACTOS de propósito
   (`top_municipio` segue existindo como coluna; só saiu do gate do SAM).

5. **Passo 5 — Schema.** Adicionadas `populacao_corte_hex`/`fonte_populacao_corte`/`flag_pop_min_5k` ao set
   `required` de `validar()` e ao `MERCADO_REQUIRED_COLS` do teste. `docs/modelo_mercado_hexagonos.md` §5.4
   reescrita com o gate novo, as 3 colunas de corte e a diferença `granular` vs `flag_censo_elegivel`/`mask_hex_censo`.

6. **Passo 6 — Testes.** Fixtures que chamam `calcular()` direto receberam `faixa_oportunidade` (+`pop_total`
   onde o teste espera SAM>0); criados os 3 testes novos: `test_sam_zera_quando_pop_corte_granular_abaixo_de_5000`
   (repro `87a91b18dffffff`: granular, setor 2022 = 35.4, pop_total = 27272, faixa prioridade_maxima,
   flag_viavel, não canibal, qualidade A + flag_censo_disponivel + score notna → granular →
   populacao_corte_hex≈35.4 → flag_pop_min_5k=False → flag_sam=False → sam_fitness_potencial=0),
   `test_sam_calcula_para_faixa_elegivel_pop_corte_acima_5000_fora_top_municipio`
   (top_municipio=False, faixa alta, municipal, pop_total=8000 → flag_sam=True, SAM>0) e
   `test_flag_sam_e_conjuncao_dos_quatro_criterios` (paramétrico: cada 1 critério ausente zera o SAM).

7. **DEC-006.** Texto pronto colado em `CLAUDE.md §8` (após DEC-005), registrando a aprovação do PRODUTO
   (Felipe 2026-06-09) e do PLANO TÉCNICO (Felipe/usuário 2026-06-10).

8. **Regeneração de parquets — NÃO executada** no fechamento (passo OPERACIONAL pós-merge, conforme Passo 7).
   Rodei a sanidade de volume read-only sobre o parquet real (sem escrita): `flag_sam` 196.715 → 27.996.

### Tratamento de friction no schema do parquet real (não previsto literalmente, dentro do espírito do Passo 7)
O parquet real `hexagonos_mercado_mapeado.parquet` ainda NÃO contém as 3 colunas novas (só após a regen
operacional pós-merge). A fixture `mercado_guardrails_df` lia por projeção a lista completa, o que
quebrava com `ArrowInvalid`. Tornei a fixture resiliente (lê só as colunas presentes via `pq.read_schema`)
e `test_parquet_final_tem_schema_minimo` passou a exigir as colunas ESTÁVEIS (pré-BLK-SAM-01) e a
`pytest.skip` claro nas 3 colunas pendentes de regeneração. Constante documental `MERCADO_COLS_PENDENTES_REGEN`.
Guardrail de canibalização sobre o parquet real permanece válido. Nenhum valor de SAM hard-coded no parquet.

## Arquivos alterados
- `src/motor_expansao/pipelines/pop_corte.py` — **NOVO**. Helper puro compartilhado (fonte única): 4 funções
  (`normalized_join_quality`, `has_censo_signal`, `derive_confianca_geografica`, `derive_pop_cut_columns`).
- `src/motor_expansao/dashboard/data.py` — import do helper; `_normalized_join_quality`/`_has_censo_signal`/
  `_derive_confianca_geografica`/`derive_pop_cut_columns` agora DELEGAM ao impl (lógica preservada byte-a-byte).
- `src/motor_expansao/pipelines/calcular_colunas_mercado.py` — import do helper; `POP_MIN_SAM_GATE=5_000`;
  `SOURCE_REQUIRED_COLS` (+`faixa_oportunidade`,+`pop_total`); gate §5.4 reescrito (DEC-006); `validar()`
  required (+3 colunas de corte).
- `tests/integration/test_modelo_mercado_hexagonos.py` — `MERCADO_REQUIRED_COLS` (+3 cols);
  `MERCADO_COLS_PENDENTES_REGEN`; fixture `mercado_guardrails_df` resiliente; fixtures atualizadas
  (faixa/pop_total); 3 testes novos; `test_parquet_final_tem_schema_minimo` tolerante a regen pendente.
- `docs/modelo_mercado_hexagonos.md` — §5.4 reescrita (gate novo + 3 colunas + `granular` vs `flag_censo_elegivel`).
- `CLAUDE.md` — §8 DEC-006 registrada (aprovação humana 2026-06-09 produto / 2026-06-10 plano técnico).
- `tasks/current_task.md` — status → aguardando QA.
- `context/handoff.md` (este) + `context/handoff/20260610-135423-builder.md` (snapshot append-only).

## Validações executadas
Host Windows + Python 3.14 (xdist/`-n auto` falha por execnet EOFError) → execução SERIAL conforme instruído.

- `python -m pytest -q -p no:cacheprovider tests/integration/test_modelo_mercado_hexagonos.py`
  → **20 passed, 4 skipped, 9 warnings** (os 4 skips: 3 guardrails que dependem do parquet real
  pré-regen + 1 fixture de score; warnings pré-existentes, incl. FutureWarning de `.fillna` na linha 395
  que NÃO toquei).
- `python -m pytest -q -p no:cacheprovider tests/integration/test_streamlit_app.py`
  → **179 passed, 1 warning** (garante que a extração em `data.py` não regrediu o dashboard).
- `python -c "import streamlit_app; print('import ok')"` → **import ok**.
- `python -m ruff check src/ tests/` → **All checks passed!**
- `python -m mypy src/motor_expansao/pipelines/pop_corte.py src/motor_expansao/pipelines/calcular_colunas_mercado.py src/motor_expansao/dashboard/data.py`
  → **Success: no issues found in 3 source files**.
- **Sanidade de volume (read-only, parquet real, SEM escrita):**
  - ANTES (`flag_sam` no parquet atual): **196.715** de 1.542.531.
  - DEPOIS (régua nova aplicada in-memory): **27.996** (delta −168.719).
  - `fonte_populacao_corte`: **740.524 `setor_2022` + 802.007 `total_municipal`**.
  - Bate exatamente com os números verificados no plano.

NÃO rodei a suíte FULL (é gate único do QA, por instrução).

## Problemas encontrados
- A projeção de colunas da fixture `mercado_guardrails_df` quebrava com `ArrowInvalid` porque o parquet real
  ainda não tem as 3 colunas novas (regen é pós-merge). Resolvido tornando a fixture/teste resilientes
  (skip claro das colunas pendentes), sem forçar regeneração no fechamento e sem afrouxar o guardrail de
  canibalização. Nenhum outro bloqueio.

## Pendências
- **OPERACIONAL pós-merge (não é requisito de fechamento / QA):** regenerar os parquets paralelos de staging
  na ordem canônica `híbrido → mercado → calcular_colunas_mercado → carteira → plano → domínio → residual →
  fase1_bi_exports` para materializar `populacao_corte_hex`/`fonte_populacao_corte`/`flag_pop_min_5k` e o novo
  `flag_sam` (196.715 → 27.996) no parquet de produção. ZERO escrita em artefatos M1 oficiais. Após a regen,
  os 3 guardrails hoje skipados voltam a rodar plenamente.

## Riscos remanescentes
- Enquanto o parquet real não for regenerado, o dashboard de produção continua exibindo o `flag_sam` antigo
  (196.715). O CÓDIGO já está correto; só falta a regeneração operacional (pendência acima). Sem risco ao M1.
- `tese_entrada`/`sam_granularidade` ainda referenciam `top_municipio` (intacto de propósito; fora do escopo
  do gate). Coerente com o plano; documentado.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (nenhuma escrita; `git diff` não toca `pipelines/m1/`, `scoring.py`,
  `config.py`, `dashboard/constants.py` nem artefatos `.parquet` oficiais).
- Artefatos M1 preservados: **sim** (nenhum parquet foi escrito; sanidade de volume foi read-only).
- Dashboard offline mantido: **sim** (`pop_corte.py` importa só numpy/pandas; nenhuma dependência de API ao
  vivo; smoke import + 179 testes de streamlit verdes).
- §3 / pesos `renda=0.40`/`pop=0.60` INALTERADOS; `POP_MIN_SAM_GATE` é constante da camada de mercado.
- `faixa_oportunidade` LIDA, não recalculada (DEC-001).
