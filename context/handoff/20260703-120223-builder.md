# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer (Opus 4.8)

## Bloco executado
BLK-TP-06-FU2 — Candidato C do residual (C1 = re-capacitar concorrentes point-level por
capacidade de CLUBE real por rede; C2 = C1 + academias de bairro decaídas por k-ring H3),
validados out-of-fold vs baseline pelo mesmo harness do FU1. READ-ONLY sobre o M1.

## GATE (registrado)
Decisão AUTÔNOMA do orquestrador em 2026-07-02 (usuário delegou os gates desta sessão):
(A) capacidade real Smart ~2.363 / Engenharia ~3.106,5 de `data/validacao/`, fallback **2.500**
p/ as ~26 redes sem dado, Sky só âncora documentada; (B) decay das bairro = k=1 **ponderado por
anel** (1,0 central / 0,5 anel-1) com **normalização Σ pesos = 4,0 (conserva massa)** — NÃO infla
~4×; (C) rodar C1 e C2; (D) vence = IC95 do Δ pareado (Cx−baseline, seed=42) sem cruzar zero no
completo **E** fora de SP/MG/RJ; (E) leitor de `data/validacao/` anti-PII em módulo irmão.

## O que foi feito
1. **Módulo irmão anti-PII** `src/motor_expansao/demanda_revelada/capacidade_clube_validacao.py`
   (NOVO): funções puras que leem os 3 xlsx de `data/validacao/` e extraem NA FRONTEIRA só a
   mediana de alunos/CLUBE por rede (`dict[str,float]`), descartando todo DataFrame com PII
   imediatamente. `REDES_VALIDACAO_ARQUIVOS` casa só `smart_fit` (KPIs Base, última obs por
   `Sigla`) e `engenharia_do_corpo` (Academias). `FALLBACK_CAPACIDADE=2500.0`.
   `capacidade_por_rede_com_fallback` monta o dict completo das 28 redes. `ancora_capacidade_sky`
   lê a mediana Sky (Alunos EVO) só como âncora. `_assert_sem_pii_em_dict` garante retorno só
   `{rede:float}`. Sem import de m1/dashboard/censo/api/config/pipelines.
2. **Extensão de `revalidacao_residual_candidatos.py`** (baseline + Candidato A INTOCADOS):
   - `_haversine_m` (vetorizado local) + `oferta_concorrentes_recapacitada_por_hex` — re-deriva a
     oferta consumida dos concorrentes point-level (decay `peso_2km_i=max(0,1−dist_m/2000)`) de
     `concorrentes_mapeados` (lat/lng, `status_registro=='valido'`), ponderando por `cap[rede]`
     (real ou fallback 2.500) no lugar do `·2500` uniforme; bounding-box antes do Haversine
     exato; index único por hex (dedup) → base do **C1**.
   - `oferta_bairro_decaida_kring_por_hex` — k-ring H3 k=1 ponderado por anel (1,0/0,5)
     NORMALIZADO por Σ_pesos=4,0 sobre `alunos_menores_add_por_hex` (dedup fino, IDÊNTICO ao A) →
     termo do **C2**. Conserva massa (testado: 1000→250 central + 6×125 = 1000; NÃO infla 4×).
   - `construir_residuais_candidatos` ganhou params opcionais (`capacidade_por_rede`, `conc_df`,
     `k_ring`, `pesos_anel`; default `None` = FU1 byte-a-byte). Colunas novas:
     `residual_cand_C1 = clip(100·max(sam − [oferta_conc_C1 + ultra],0)/2500,0,100)`;
     `residual_cand_C2 = clip(100·max(sam − [oferta_conc_C1 + ultra + bairro_kring],0)/2500,0,100)`.
     **CAP_REF=2500 do clip INTOCADO.**
   - `comparar_por_recorte` valida C1/C2 quando as colunas existem; `RevalidacaoResult` ganhou
     `vence_candidato_c1`/`vence_candidato_c2` (regra (D)); `revalidar_candidatos` computa veredito
     POR SUB-CANDIDATO (A + C1 + C2) quando C ativo, preservando o formato legado FU1 quando
     inativo. `_nota_honesta` estendida com linhas C1/C2 sem quebrar o FU1.
   - Relatório dedicado `relatorio_revalidacao_candidato_c` + `escrever_relatorio_candidato_c`
     (seções baseline/capacidades/C1/C2/tabela/Δ recortes/veredito+recomendação/confounds; assert
     anti-PII). `__main__` roda FU1 (preservado) e depois FU2 (lê validacao + concorrentes
     point-level) e escreve o novo relatório.
3. **Relatório** `data/analysis/revalidacao_residual_candidato_c.md` (gitignored) gerado sobre os
   parquets + validacao REAIS.
4. **Testes** `tests/unit/demanda_revelada/test_revalidacao_residual_candidato_c.py` (NOVO,
   fixtures sintéticas): leitor anti-PII (só `{rede:float}`, real vs fallback, arquivo ausente),
   decay point-level (dist~0→cap, 2 km→0, 1 km→0,5·cap, por rede, fallback), k-ring conserva massa
   / não infla 4× / k=0 / flat difere, residual C1/C2 em [0,100] e não-negativo, CAP_REF intocado,
   C ausente preserva FU1, R² in-sample banido, veredito por sub-candidato token-exato, Δ pareado
   determinístico (seed), isolamento AST dos 2 módulos, anti-PII no relatório.

## Prova anti-PII (colada do run REAL, sem PII)
Retorno do leitor de `data/validacao/` = **só `{rede: float}`**:
`{'smart_fit': 2363.0, 'engenharia_do_corpo': 3106.5}` (2 redes reais; type-check `str→float` ok).
Âncora Sky Fit (só documentada, sem peso) = ~944,5 (mediana de Alunos EVO do próprio clube).
Fallback 2.500 aplicado às 26 redes restantes. Ordem de grandeza das capacidades reais:
Engenharia ~3,1 mil > Smart ~2,4 mil > fallback 2,5 mil — nenhum nome/endereço/coord/linha saiu.
Cobertura: ~33% dos PONTOS de `concorrentes_mapeados` (Smart 999 + Eng 63 de 3.179) / ~7% das
redes; o resto é fallback → capacidade ~uniforme na prática.

## VEREDITO REAL (out-of-fold, k-fold repetido, seed=42, IC95 bootstrap; n=16.411)
Baseline reproduzido byte-a-byte: **R²_oof_log=+0,3119** IC95[+0,2977,+0,3250] | rho_oof=+0,4615.

- **C1 = APLICAR_C1 (vence o critério (D), mas ganho DESPREZÍVEL):**
  R²_oof=**+0,3138** IC95[+0,2995,+0,3269] | rho_oof=+0,4629.
  Δ pareado (C1−baseline) COMPLETO=**+0,0019** IC95[+0,0015,+0,0023] → vence=True;
  FORA (não-metro)=**+0,0013** IC95[+0,0007,+0,0019] → vence=True.
  → Passa formalmente (IC não cruza zero no completo E fora), mas o ganho é ~+0,002 de R²_oof
  sobre +0,3119 (segunda casa decimal). **Recomendação honesta ao BLK-TP-09: NÃO aplicar C1** —
  com capacidade real cobrindo só ~33% dos pontos (fallback 2.500 domina), re-capacitar quase não
  move o residual; o ganho não paga o custo/risco de recalibrar a fórmula em produção.
- **C2 = NAO_APLICAR_C2 (NO-GO honesto):**
  R²_oof=**+0,2807** IC95[+0,2666,+0,2936] | rho_oof=+0,4422.
  Δ pareado (C2−baseline) COMPLETO=**−0,0312** IC95[−0,0349,−0,0278] → vence=False;
  FORA=**−0,0120** IC95[−0,0143,−0,0099] → vence=False.
  → Piora o baseline em ambos os recortes. Espalhar bairro por k-ring (mesmo com decay +
  normalização que conserva massa) REPETE o padrão do Candidato A (co-localização bairro↔demanda).
- **Referência Candidato A (FU1, reproduzido):** Δ COMPLETO=−0,0427 / FORA=−0,0193 → NAO_APLICAR.
- **Veredito consolidado FU2:** `NAO_APLICAR_A; APLICAR_C1; NAO_APLICAR_C2`.
- **Atribuição de causa (objetivo do FU2):** o NO-GO do A NÃO era só crudeza de execução — nem
  re-capacitar (C1, neutro) nem incluir bairro com decay fino (C2, negativo) supera o baseline de
  forma útil. O residual atual (baseline) segue como está em produção.

## Arquivos alterados
- `src/motor_expansao/demanda_revelada/capacidade_clube_validacao.py` (NOVO — leitor anti-PII +
  fallback + âncora Sky).
- `src/motor_expansao/demanda_revelada/revalidacao_residual_candidatos.py` (ESTENDIDO — C1/C2,
  geometria, validação por sub-candidato, relatório C; baseline+A e API FU1 preservados).
- `tests/unit/demanda_revelada/test_revalidacao_residual_candidato_c.py` (NOVO).
- `data/analysis/revalidacao_residual_candidato_c.md` (runtime, gitignored — NÃO é código).
- `context/handoff.md` + snapshot `context/handoff/20260703-120223-builder.md`;
  `tasks/current_task.md` (status → aguardando QA).

## Validações executadas
- `python -m pytest -n auto -q tests/unit/demanda_revelada/ tests/integration/test_streamlit_app.py`
  → **304 passed, 0 failed, 0 skipped** (inclui os 85 de demanda_revelada + 219 do streamlit).
- `python -c "import streamlit_app"` → **import ok**.
- `ruff check` (2 módulos src + teste) → **All checks passed**.
- `mypy` (2 módulos src) → **Success: no issues found**.
- Isolamento de import (AST) dos 2 módulos → verde (testes `test_isolamento_import_*`).
- Run REAL end-to-end sobre os parquets + `data/validacao/` (n=16.411) → relatório escrito.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (não tocado; só LIDO nenhum peso).
- Artefatos M1 preservados: **sim** — mtime dos 4 oficiais + `hexagonos_mercado_mapeado.parquet`
  IDÊNTICO antes/depois:
  brasil_estrutural=1780501621, brasil_priorizados=1780501631,
  hexagonos_brasil_oportunidades=1780501655, hexagonos_mercado_mapeado=1781205056,
  hexagonos_brasil_dashboard=1781271963. `git status` sem artefato de produção modificado.
- Dashboard offline mantido: **sim** (nenhuma dependência de rede; sem dep nova).
- Anti-PII (DEC-012): validacao NUNCA versionada/alterada; leitor retorna só `{rede:float}`;
  relatório e testes sem PII; fixtures sintéticas; relatório gitignored.
- DEC-008: R² in-sample banido do veredito; Δ pareado; seed=42; NO-GO é válido (C2 NO-GO honesto).
- DEC-009: `membros` só ALVO observado; nunca preditor.

## Problemas encontrados
Nenhum (2 bugs de fixture/asserção do próprio teste — nome de sheet "Base" e `in` de substring em
veredito — corrigidos; 1 InvalidIndexError por hex duplicado no join sintético corrigido com dedup
de índice no recompositor).

## Pendências
Nenhuma para o QA. Follow-up de PRODUTO (fora deste ciclo): o BLK-TP-09 deve registrar o veredito —
C1 tecnicamente GO mas com ganho desprezível (recomendação: NÃO aplicar); C2 NO-GO; residual
segue como está. Nenhuma regeneração de parquet paralelo requerida (nenhum candidato aplicável).

## Riscos remanescentes
- C1 passa o critério formal por margem de ~+0,002 — decisão de aplicar/não é de produto
  (documentada como NÃO recomendar). Não há mudança em produção.
- Cobertura de capacidade real baixa (~33% dos pontos) → C1 é ~neutro por construção; um bloco
  futuro só faria sentido com capacidade real por rede cobrindo a maioria dos pontos.
