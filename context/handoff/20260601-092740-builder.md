# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada (QA)
**QA** — validar o gate (Nível 1 estático + Nível 2 sintaxe), confirmar zero-M1 e a forma
fail-closed do pin. Nível 3 (run real no Actions) é prova DIFERIDA ao fechamento humano.

## Bloco executado
ID: BLK-SEC-01 — Gate de publicação no CI (publish só com CI verde) + pin de imagem e rollback.
Criticidade: Alta. Executado conforme o **handoff do Planner (itens 1-6)** + o **ajuste humano
aprovado por Felipe Silva em 2026-06-01** (pin do compose fail-closed `${STREAMLIT_IMAGE:?...}` no
lugar do `sha-<commit>` original; Validação #3 atualizada). O banner de aprovação humana original
(`⚠ AJUSTE APROVADO PELO HUMANO`) permanece rastreável nos snapshots append-only
`context/handoff/20260601-091547-planner.md` e na cópia deste handoff em
`context/handoff/20260601-092740-builder.md`.

### Registro do ajuste humano aprovado (preservado)
> ⚠ AJUSTE APROVADO PELO HUMANO (Felipe Silva, 2026-06-01) — SOBREPÕE O D2 ORIGINAL.
> O default do `image:` em `docker-compose.prod.yml` NÃO é mais `sha-<commit>` hardcodado; passa a
> ser a forma fail-closed `${STREAMLIT_IMAGE:?<mensagem apontando docs/infra_producao.md>}`. Motivo:
> ciclos entram na `main` por merge commit, então `sha-<commit-da-branch>` nunca é publicada — um
> default `sha-<...>` apontaria para tag inexistente. Com `:?`, `docker compose up` SEM
> `STREAMLIT_IMAGE` falha de propósito (fail-closed) com mensagem clara. A Validação obrigatória #3
> foi atualizada para checar essa forma fail-closed. Onde o corpo do plano divergir, o banner vence.

## O que foi feito
1. **`.github/workflows/ci.yml`** — job `test` mantido intacto. Adicionados:
   - `permissions: contents: read` global (least-privilege).
   - input `dispatch_build_sanity` (boolean, default true) em `workflow_dispatch`.
   - job `publish` (`needs: [test]`, `if: github.event_name == 'push' && github.ref ==
     'refs/heads/main'`, `permissions: contents: read / packages: write`): checkout → login GHCR →
     metadata-action (`type=sha` + `latest` só na default branch) → buildx → build-push
     (`push: true`, id `build`) → step que ecoa `${{ steps.build.outputs.digest }}`.
   - job `build-sanity` (`needs: [test]`, `if: github.event_name == 'workflow_dispatch' &&
     inputs.dispatch_build_sanity`, `permissions: contents: read`): checkout → buildx → build-push
     com `push: false`. NUNCA faz login no GHCR nem gera tag `:latest`.
   - Nota técnica: o step que ecoa o digest teve o valor `run:` colocado entre aspas simples porque
     o PyYAML (Validação #1) tropeça no `: ` literal de um escalar não-citado; é YAML válido para o
     GitHub Actions e não muda o comportamento do step.
2. **`.github/workflows/docker-publish.yml`** — removido via `git rm` (sem caminho de publish
   desacoplado; com o publish acoplado em `ci.yml`, manter o arquivo reabriria o gap).
3. **`docker-compose.prod.yml` (linha do `image:`)** — AJUSTE DO BANNER aplicado:
   `image: ${STREAMLIT_IMAGE:?defina STREAMLIT_IMAGE com o digest imutavel ghcr.io/kastaldy/motor-de-expansao/motor-expansao-streamlit@sha256:<digest> — ver docs/infra_producao.md (Atualizar/Rollback)}`.
   Comentários acima reescritos para o modelo PULL + pin obrigatório por digest fail-closed. Resto
   do serviço/arquivo preservado. Sem `:latest`, sem `sha-<commit>` hardcodado, sem `@sha256:` real.
4. **`docs/infra_producao.md`** — seção antiga "## Atualizar o código do dashboard" (`git pull` +
   `up -d --build`) substituída pelas duas seções do plano (conteúdo literal): "## Atualizar o
   dashboard (modo PULL, sem build)" e "## Rollback (por digest imutavel, SEM rebuild)". Nenhuma
   instrução `up -d --build` contraditória sobrou; referência ao workflow aponta para o job
   `publish` do CI.
5. **`docs/deploy.md`** — referências de `docker-publish.yml` atualizadas para "job `publish` do
   workflow `CI` (`.github/workflows/ci.yml`)" com menção ao gate `needs: [test]`; visão geral do
   compose reescrita para o pin fail-closed obrigatório; "Atualizacao" agora exige `STREAMLIT_IMAGE`
   (preferindo digest); seção "Rollback (por SHA)" renomeada para "Rollback (por digest imutavel)".
6. Housekeeping: `tasks/current_task.md` atualizado (status: aguardando QA).

### Trecho YAML final dos jobs do gate (Nível 1 — prova estática)
```yaml
  publish:
    needs: [test]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v5
      - name: Login no GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Metadata (tags/labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/motor-expansao-streamlit
          tags: |
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}
      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build e push
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.streamlit
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Exibir digest publicado (pin de prod)
        run: 'echo "Digest imutavel publicado: ${{ steps.build.outputs.digest }}"'

  build-sanity:
    needs: [test]
    if: github.event_name == 'workflow_dispatch' && inputs.dispatch_build_sanity
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build (sanity, SEM push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.streamlit
          push: false
          tags: motor-expansao-streamlit:sanity
          cache-from: type=gha
          cache-to: type=gha,mode=max
```

## Arquivos alterados
- `.github/workflows/ci.yml` — ALTERADO (jobs `publish` + `build-sanity`, `workflow_dispatch.inputs`,
  `permissions: contents: read` global).
- `.github/workflows/docker-publish.yml` — REMOVIDO (`git rm`).
- `docker-compose.prod.yml` — ALTERADO (pin fail-closed `${STREAMLIT_IMAGE:?...}`; comentários).
- `docs/infra_producao.md` — ALTERADO (seção PULL + seção Rollback; remoção do `--build`).
- `docs/deploy.md` — ALTERADO (referências ao job `publish` do CI; pin fail-closed; rollback digest).
- `tasks/current_task.md` — ALTERADO (status aguardando QA).
- `context/handoff.md` — ALTERADO (este handoff).
- `context/handoff/20260601-092740-builder.md` — NOVO (cópia append-only idêntica).

Nenhum `src/`, `dashboard/`, `*.parquet`, `config.py` nem `PRD.md` tocado.

## Validações executadas (saída literal)
1. **Sintaxe YAML do CI** — `ci.yml ok` (exit 0).
2. **Remoção do docker-publish.yml** — `removido`.
3. **Compose fail-closed (AJUSTE)** —
   `${STREAMLIT_IMAGE:?defina STREAMLIT_IMAGE com o digest imutavel ghcr.io/kastaldy/motor-de-expansao/motor-expansao-streamlit@sha256:<digest> — ver docs/infra_producao.md (Atualizar/Rollback)}`
   + `compose fail-closed ok` (começa com `${STREAMLIT_IMAGE:?`; sem `:latest`).
4. **Integração streamlit** — `150 passed in 6.90s` (0 falhas / 0 erros de coleta).
5. **Smoke import** — `import ok`.
6. **Suíte completa** — `626 passed, 1 skipped, 9 warnings in 80.99s` (0 failed / 0 collection
   errors). Baseline ~532 era a contagem histórica; o repo cresceu desde então — o que importa é 0
   falhas.
7. **Diff só no escopo** — `git status --porcelain`: `M .github/workflows/ci.yml`,
   `D .github/workflows/docker-publish.yml`, `M docker-compose.prod.yml`, `M docs/deploy.md`,
   `M docs/infra_producao.md`, `M context/handoff.md`, `M tasks/current_task.md` (+ snapshots
   `context/handoff/*` untracked). Nenhum `src/`/`dashboard/`/`*.parquet`/`config.py`/`PRD.md`.
   (`git diff main...HEAD` vazio porque ainda não há commit — o orquestrador faz o commit por path.)

## Problemas encontrados
- PyYAML (Validação #1) falhou na 1ª passada por causa do `: ` literal dentro do escalar não-citado
  do step "Exibir digest publicado". Corrigido envolvendo o valor `run:` em aspas simples (YAML
  válido para Actions; comportamento idêntico). Demais validações sem intercorrência.

## Pendências
- **QA:** revisão estática do gate (Nível 1), confirmar sintaxe (Nível 2) e zero-M1.
- **Nível 3 (diferido ao humano):** disparar run controlado no Actions de uma branch de ciclo com
  teste quebrado e comprovar que `publish`/`build-sanity` ficam skipped (sem push ao GHCR); anotar
  run id no handoff de fechamento. NÃO executado pelo Builder (não dispara runs reais).
- **Commit por path:** o orquestrador faz o commit (Builder não fez `git add`/commit).
- **Deploy/rollback no VPS:** passo humano (§6); runbook é documentação.

## Riscos remanescentes
- Pin de produção depende de uma imagem real publicada (digest só existe após push real ao GHCR);
  fora do escopo de execução — documentado como obter o digest sem fabricá-lo.
- `workflow_dispatch` na `main`: `build-sanity` roda (sem push) e `publish` NÃO (exige
  `event_name == 'push'`). Comportamento correto: dispatch nunca publica.
- Histórico append-only (`tasks/completed.md`, snapshots antigos de handoff) ainda cita
  `docker-publish.yml` — é registro histórico, intencionalmente não reescrito. `tasks/backlog.md`
  cita o arquivo apenas como "arquivos prováveis" de blocos futuros (forward-looking) — sem ação.

## Guardrails verificados
- **M1 intocável:** zero alteração em `score_priorizacao`/`hex_score_estrutural`/pesos
  (renda=0.40/pop=0.60)/fórmula/carteira/plano/artefatos. Diff só toca workflows/compose/docs/
  housekeeping. CONFIRMADO pela Validação #7.
- **Sem API ao vivo no dashboard / staging Parquet:** inalterado (bloco não toca dashboard/dados).
- **§2 commit por path:** Builder não fez commit/`git add`; working tree deixado pronto.
- **§6 VPS:** nada executado no servidor; runbooks são documentação; deploy/rollback = passo humano.
- **Escopo:** apenas o bloco aprovado; sem refator fora de escopo; cosign/gitleaks/SBOM ficam para
  blocos BLK-SEC futuros.
