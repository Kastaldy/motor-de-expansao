# Handoff — QA

## Skill que gerou este handoff
QA

## Próxima Skill recomendada
Fechamento (housekeeping): converter BLK-ATR-01 em stub no backlog + mover para completed.md.
Veredito: **APROVADO COM RESSALVAS** (bloco loop-safe; suíte verde exceto 4 falhas PRÉ-EXISTENTES).

## Veredito: APROVADO COM RESSALVAS (BLK-ATR-01)

Base DENSA de concorrentes do Huff (TotalPass/WellHub/Unidades) + re-validação out-of-fold do GO
do BLK-TP-07. Validação independente do QA confirma READ-ONLY sobre o M1, isolamento e testes verdes.

### Gate único — suíte FULL
- `python -m pytest -q`: **1329 passed, 4 failed, 4 skipped** (737s).
- As **4 falhas são PRÉ-EXISTENTES** e de AMBIENTE: `openlocationcode` (Plus Code) NÃO instalado
  (`ModuleNotFoundError: No module named 'openlocationcode'` confirmado). São elas:
  `test_render_coord_search_plus_code_completo_offline`,
  `test_render_coord_search_plus_code_curto_usa_localidade_mock`,
  `test_resolve_plus_code_completo_decodifica_offline`,
  `test_resolve_plus_code_curto_usa_localidade_mockada`.
- **Regressões novas do BLK-ATR-01 = 0** (o bloco só toca `demanda_revelada/`; os testes de Plus Code
  são de `coord_search`/`streamlit_app`, sem relação).
- Os 11 testes novos (`tests/unit/demanda_revelada/test_concorrentes_densos.py`) passam.

### ruff
- `ruff check concorrentes_densos.py test_concorrentes_densos.py`: **All checks passed!**

### Guardrails (verificados pelo QA, independentemente)
- **Imports proibidos:** `grep` de `pipelines.m1|dashboard|censo_|api` no módulo → **VAZIO**.
  (O único import de nível de função é `from motor_expansao.dimensionamento.huff import _carregar_concorrentes`
  dentro de `executar()` — pacote `dimensionamento` é PARALELO/permitido, não M1/dashboard/api/censo.)
  Teste AST `test_isolamento_imports` cobre isso.
- **loop_guard:** `python scripts/loop_guard.py --base ciclo/loop-20260706-152137` → **GUARD OK**
  (15 caminhos alterados — inclui os staged do BLK-ATR-01 —, nenhum proibido).
- **`concorrentes_mapeados.parquet`:** NÃO modificado (ausente do `git status`) — só LIDO.
- **mtime dos 4 artefatos M1 oficiais:** inalterado (2026-06-03 / 2026-06-12, muito antes de hoje 2026-07-06).
- **`huff_captura.py`:** NÃO alterado (`git diff base..HEAD` do arquivo → vazio).
- **`import streamlit_app`:** ok.

### Corretude (leitura do código + testes)
- lat/lng = centroide via `h3.cell_to_latlng` (`_centroide_hex`, l.118), NUNCA coord GPS bruta — teste
  `test_lat_lng_centroide` (l.196-204) confirma que difere da coord original.
- Chave de dedup = `(hex_id_res7, rede_normalizada)` (nível 1 intra-fonte + nível 2 inter-fonte com
  precedência `unidades > totalpass > wellhub` + contra base atual). Chave única no total (asserção
  `_assert_schema` + teste).
- PII descartada na fronteira (`_COLUNAS_DROP_FRONTEIRA`: slug/nome/coords/CEP/endereço/cidade/uf/…);
  só `hex_id_res7`/`rede_normalizada`/`fonte` sobrevivem à leitura de cada CSV.
- `versao_contrato="concorrentes_densos_v1"` carimbado em todas as linhas do parquet.

## Ressalvas (não bloqueiam o veredito; para ciência do humano no fechamento)
1. **Re-validação com DADOS REAIS não foi executada** (parquet denso + `data/analysis/relatorio_huff_densa.md`
   NÃO materializados). O Builder deferiu `executar()` (operação cara; artefatos gitignored). Consequência:
   os NÚMEROS antes/depois (R²_oof, cobertura, IC95) do GO re-validado NÃO existem ainda — a lógica de
   re-validação está testada por smoke, mas o VEREDITO empírico do BLK-TP-07 densificado fica pendente de
   uma rodada real (o QA/humano pode rodar `python -m motor_expansao.demanda_revelada.concorrentes_densos`).
2. **Critério de aceite "cruzar com `NAO_ABRA/` (01_SmartFit / 03_Competidores)" NÃO implementado.** O dedup
   é inter-fonte + contra `concorrentes_mapeados.parquet`; a aferição de overlap/precisão contra as unidades
   reais do `NAO_ABRA/` não entrou. Gap de escopo vs o backlog; o núcleo (base densa + harness de
   re-validação, isolado e READ-ONLY) está completo.
3. **Housekeeping:** `housekeeping_move_block.py BLK-ATR-01 --check` → "stub ausente no backlog". O bloco
   ainda é entrada completa no backlog (l.1001), não convertido em stub. Passo de fechamento pendente.

## Conclusão
Critérios de APROVAÇÃO satisfeitos: suíte verde exceto as 4 falhas pré-existentes de openlocationcode
(regressões novas = 0), ruff limpo, isolamento confirmado (sem M1/dashboard/censo/api), READ-ONLY M1
confirmado (mtime oficiais + concorrentes_mapeados + huff_captura intocados). As ressalvas 1-2 são de
COMPLETUDE de escopo empírico (não de corretude/guardrail) — por isso **APROVADO COM RESSALVAS**.
