# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
[REVISÃO HUMANA / Approver] — exige aprovação humana (produto + emenda DEC-011) ANTES do Builder.
Depois da aprovação: Builder → QA.

## Entendimento da tarefa
Remover o termo de SAM Fitness (`sam_fitness_potencial >= 3000`) de `_hex_destacado_mask` no
Relatório Municipal, deixando o critério de "hexágono destacado/válido" = SOMENTE
`oferta_efetiva_disponivel >= OFERTA_DESTAQUE_MIN (2000)`; propagar a todos os textos/legendas/
docstrings; adicionar teste que PROVE a remoção; e registrar emenda à DEC-011. READ-ONLY sobre o M1.

---

## DECISÕES DE PRODUTO PARA O GATE HUMANO (não decidir sem aprovação)

### D-PROD-1 — `SAM_DESTAQUE_MIN`: remover a constante OU mantê-la inerte?
**Recomendação do Planner: REMOVER a constante (`SAM_DESTAQUE_MIN = 3000.0`, linha 50).**
- Justificativa: após remover o termo de SAM do corpo de `_hex_destacado_mask` (linha 264) e dos
  dois textos de PDF que a referenciam (linhas 1613 e 1670), a constante fica **100% sem uso**.
  Verificado por grep: `SAM_DESTAQUE_MIN` só aparece em `relatorio_municipal.py` (linhas 50, 264,
  1613, 1670 — todas editadas neste bloco) e em docs/handoffs; **nenhum outro módulo ou teste a
  importa** (o `test_relatorio_municipal.py` não a importa). Removê-la é mais limpo e não deixa
  dead-code que sugira um critério ainda vigente.
- Nota de lint: manter a constante inerte **NÃO** quebraria o ruff (regras padrão não flagam
  constante de módulo não usada — F841 é só p/ variável local), então "inerte" é possível, mas
  gera código morto/enganoso e risco de reintrodução futura silenciosa do filtro.
- Rastro histórico preservado sem a constante: git history + emenda à DEC-011 + `completed.md`.
- **Alternativa (se o gate preferir):** MANTER `SAM_DESTAQUE_MIN = 3000.0` com comentário explícito
  `# mantido só por rastro histórico; NÃO usado no critério após BLK-RELMUN-03 (DEC-011 emenda)`.
  Trade-off: dead-code + confusão de revisor.
- **Impacto no plano abaixo:** o Passo 3 assume REMOÇÃO. Se o gate escolher "inerte", o Builder
  pula a deleção da linha 50 e adiciona o comentário; os textos 1613/1670 mudam igual (não citam
  mais SAM), então a constante fica de fato inerte.

### D-PROD-2 — Cobertura de teste que PROVA a mudança (recomendado ENTRAR no escopo)
A fixture `_sample_df` move os DOIS limiares juntos (destacados têm sam=4000 **e** oferta=4451;
não-destacados têm sam=1000 **e** oferta=500), então os testes atuais passariam mesmo se o filtro
de SAM continuasse. **Recomendação: adicionar 2 testes dedicados** (ver Passo 5) com o caso
`sam < 3000 E oferta >= 2000` (que ANTES não era destacado e AGORA é) e o caso `sam alto E
oferta < 2000` (que continua NÃO destacado) — assim a suíte FALHA se alguém reintroduzir o SAM.

### D-PROD-3 — Redação exata dos textos novos (ver literais no Passo 4)
Proposta de substituição literal para cada ocorrência textual (rodapés/legendas/docstrings).

### D-PROD-4 — Emenda à DEC-011 (rascunho pronto no Passo 6)

---

## Plano técnico

### Passo 1 — Corpo de `_hex_destacado_mask` (linhas 258-264, `relatorio_municipal.py`)
Substituir o corpo (mantendo o guard de `df_muni.empty`):
- ANTES (262-264):
  ```
      sam = pd.to_numeric(df_muni.get("sam_fitness_potencial"), errors="coerce")
      oferta = pd.to_numeric(df_muni.get("oferta_efetiva_disponivel"), errors="coerce")
      return (sam >= SAM_DESTAQUE_MIN) & (oferta >= OFERTA_DESTAQUE_MIN)
  ```
- DEPOIS:
  ```
      oferta = pd.to_numeric(df_muni.get("oferta_efetiva_disponivel"), errors="coerce")
      return oferta >= OFERTA_DESTAQUE_MIN
  ```
- Nota técnica: `oferta >= 2000` devolve Series[bool] com o mesmo índice; `NaN >= 2000 == False`
  (mesma semântica NaN-safe de antes). O guard `if df_muni.empty` (260-261) permanece.

### Passo 2 — Docstring de `_hex_destacado_mask` (linha 259)
- ANTES: `"""D1: destacado <=> sam_fitness_potencial>=3000 E oferta_efetiva_disponivel>=2000."""`
- DEPOIS: `"""D1 (emenda BLK-RELMUN-03): destacado <=> oferta_efetiva_disponivel>=2000 (Residual Fitness; termo de SAM removido)."""`

### Passo 3 — Remover a constante `SAM_DESTAQUE_MIN` (linha 50) — **condicional a D-PROD-1**
- Se REMOVER (recomendado): apagar a linha `SAM_DESTAQUE_MIN = 3000.0` e atualizar o comentário do
  bloco (linhas 46-49) acrescentando uma linha:
  `# BLK-RELMUN-03 (DEC-011 emenda): criterio de destaque passou a ser SO Residual Fitness (>=2000).`
- Se MANTER INERTE: preservar a linha 50 com comentário
  `# mantido so por rastro historico; NAO usado no criterio apos BLK-RELMUN-03 (DEC-011 emenda)`.

### Passo 4 — Propagar textos/docstrings (todas em `relatorio_municipal.py`)
Substituições LITERAIS:

**(a) Docstring do módulo, D1 (linhas 10-11):**
- ANTES:
  ```
  - D1: hex DESTACADO ("amarelo") <=> `sam_fitness_potencial >= 3000` E
    `oferta_efetiva_disponivel >= 2000`. Rotulo sobre o hex = `oferta_efetiva_disponivel`.
  ```
- DEPOIS:
  ```
  - D1 (emenda BLK-RELMUN-03, 2026-07-02): hex DESTACADO ("amarelo") <=>
    `oferta_efetiva_disponivel >= 2000` (Residual Fitness; termo de SAM Fitness REMOVIDO).
    Rotulo sobre o hex = `oferta_efetiva_disponivel`.
  ```

**(b) Docstring de `_zonas_geometricas` (linhas 463-464):**
- ANTES:
  ```
      Hexes relevantes = SOMENTE os hexes DESTACADOS/aprovados (`_hex_destacado_mask`:
      sam_fitness_potencial>=3000 E oferta_efetiva_disponivel>=2000). Decisao do produto
  ```
- DEPOIS:
  ```
      Hexes relevantes = SOMENTE os hexes DESTACADOS/aprovados (`_hex_destacado_mask`:
      oferta_efetiva_disponivel>=2000; termo de SAM removido em BLK-RELMUN-03). Decisao do produto
  ```

**(c) Nota de rodapé da página "Potencial de Entrada" (linhas 1612-1614):**
- ANTES:
  ```
          f"As paginas seguintes consideram apenas as {_format_number(n_aprov, 0)} regioes aprovadas "
          f"(SAM Fitness >= {_format_number(SAM_DESTAQUE_MIN, 0)} e "
          f"Residual Fitness >= {_format_number(OFERTA_DESTAQUE_MIN, 0)}).",
  ```
- DEPOIS:
  ```
          f"As paginas seguintes consideram apenas as {_format_number(n_aprov, 0)} regioes aprovadas "
          f"(Residual Fitness >= {_format_number(OFERTA_DESTAQUE_MIN, 0)} alunos).",
  ```

**(d) Comentário + nota de rodapé da página "Resumo da Regiao" (linhas 1666-1671):**
- ANTES (comentário 1666-1667):
  ```
      # AJUSTE 2: legenda BREVE do criterio de inclusao do hexagono (limiares reais do
      # _hex_destacado_mask: SAM_DESTAQUE_MIN / OFERTA_DESTAQUE_MIN — fonte unica, nao diverge).
  ```
  DEPOIS:
  ```
      # AJUSTE 2 (BLK-RELMUN-03): legenda BREVE do criterio de inclusao do hexagono (limiar real
      # do _hex_destacado_mask: OFERTA_DESTAQUE_MIN — SO Residual Fitness, termo de SAM removido).
  ```
- ANTES (nota 1670-1671):
  ```
          f"Hexagono considerado quando SAM Fitness >= {_format_number(SAM_DESTAQUE_MIN, 0)} "
          f"e Residual Fitness >= {_format_number(OFERTA_DESTAQUE_MIN, 0)} (alunos).",
  ```
  DEPOIS:
  ```
          f"Hexagono considerado quando Residual Fitness >= {_format_number(OFERTA_DESTAQUE_MIN, 0)} (alunos).",
  ```

**(e) Confirmar exaustividade:** após editar, rodar grep no arquivo (ver Validações) — NÃO pode
sobrar nenhuma ocorrência de `SAM`, `sam_fitness_potencial`, `SAM_DESTAQUE_MIN` em
`relatorio_municipal.py`. (Ocorrências de `sam_fitness_potencial` em `calcular_colunas_mercado.py`,
`censo_report.py`, `pages.py` etc. são de OUTRAS trilhas e ficam **fora de escopo**.)

### Passo 5 — Testes (`tests/unit/test_relatorio_municipal.py`)

**(5.1) Atualizar asserção literal do PDF (linhas 324-327):**
- ANTES:
  ```
      # AJUSTE 2 (FU1): legenda do criterio de inclusao do hexagono no Slide 2, com os
      # limiares REAIS do _hex_destacado_mask (SAM>=3000 & oferta>=2000).
      assert b"SAM Fitness >= 3.000" in pdf_bytes
      assert b"Residual Fitness >= 2.000" in pdf_bytes
  ```
- DEPOIS:
  ```
      # BLK-RELMUN-03: legenda do criterio de inclusao do hexagono, com o limiar REAL do
      # _hex_destacado_mask (SO Residual Fitness >= 2.000; termo de SAM removido).
      assert b"SAM Fitness" not in pdf_bytes
      assert b"Residual Fitness >= 2.000" in pdf_bytes
  ```
  (o `assert b"SAM Fitness" not in pdf_bytes` é o guard forte de ausência de texto residual.)

**(5.2) Atualizar wording de docstrings de fixture (só texto; SEM mudar valores):**
- Linha 47: `"""4 hexes em SAO PAULO; 2 destacados (sam>=3000 & oferta>=2000), 2 nao."""`
  → `"""4 hexes em SAO PAULO; 2 destacados (oferta>=2000), 2 nao (oferta<2000)."""`
- Linha 367: `"""3 hexes APROVADOS (sam>=3000 & oferta>=2000) em celulas res-7 DISTINTAS ...`
  → `"""3 hexes APROVADOS (oferta>=2000) em celulas res-7 DISTINTAS ...` (resto do texto igual).
  Os valores de `sam_fitness_potencial` nas fixtures podem PERMANECER (são inócuos agora); não
  alterar números para não mexer nos asserts numéricos existentes.

**(5.3) NOVO teste unitário do critério (guard de regressão do SAM) — adicionar import
`_hex_destacado_mask` na lista de imports do topo (linhas 18-30):**
```python
def test_hex_destacado_criterio_so_residual_sem_sam():
    """BLK-RELMUN-03: destacado <=> oferta_efetiva_disponivel>=2000, INDEPENDENTE de SAM.
    Prova que o filtro de SAM foi REMOVIDO: hex com sam<3000 mas oferta>=2000 agora conta
    (antes NAO contava); hex com oferta<2000 NAO conta mesmo com sam alto."""
    from motor_expansao.dashboard.relatorio_municipal import _hex_destacado_mask

    df = pd.DataFrame(
        [
            {"sam_fitness_potencial": 1000.0, "oferta_efetiva_disponivel": 2500.0},  # NOVO -> True
            {"sam_fitness_potencial": 9000.0, "oferta_efetiva_disponivel": 500.0},   # -> False
            {"sam_fitness_potencial": 4000.0, "oferta_efetiva_disponivel": 4451.0},  # -> True
        ]
    )
    assert list(_hex_destacado_mask(df)) == [True, False, True]
```

**(5.4) NOVO teste de nível `agregar_municipio` (prova o efeito no número):**
```python
def test_agregar_municipio_conta_hex_sam_baixo_oferta_alta():
    """BLK-RELMUN-03: hex com sam<3000 e oferta>=2000 entra em n_hex_amarelos e no espaco
    (antes do drop do SAM ele NAO entrava)."""
    df = pd.DataFrame(
        [
            {
                "hex_id": _hex(-23.55, -46.63), "lat": -23.55, "lng": -46.63,
                "nome_municipio": "SAO PAULO", "cidade": "SAO PAULO", "uf": "SP",
                "sam_fitness_potencial": 1000.0, "oferta_efetiva_disponivel": 3000.0,
                "score_setor_2022_calibrado": 50.0, "score_oportunidade_residual": 40.0,
                "pop_total_setor_2022": 1500.0, "pop_total": 2000.0, "renda_per_capita": 3000.0,
                "penetracao_fitness_mercado_estimada": 12.5,
                "oferta_consumida_mercado_estimada": 200.0,
            }
        ]
    )
    res = agregar_municipio(df, nome_municipio="SAO PAULO", uf="SP")
    assert res["n_hex_amarelos"] == 1
    assert res["soma_oferta_amarelos"] == 3000.0
    assert res["espaco_para_academias"] == round(3000.0 / CAPACIDADE_UNIDADE)  # == 1
```

**(5.5) Confirmar por cascata** (sem mudança esperada de valores nos testes existentes, pois
`_sample_df` move os dois limiares juntos): `test_agregar_municipio_formula_espaco_d1` (n=2,
soma=8902, espaço=4), `_focus_bounds_mercator`, `_zonas_geometricas`, camada de mapa, contagem
aprovados/reprovados — todos derivam de `_hex_destacado_mask` (fonte única), então não divergem.
Rodar a suíte inteira do módulo para confirmar.

### Passo 6 — Emenda à DEC-011 (`CLAUDE.md` §8) — rascunho pronto (ver "Emenda DEC-011" abaixo)
Inserir como último parágrafo da DEC-011 (padrão das emendas DEC-010/DEC-014). Ajustar a frase
sobre `SAM_DESTAQUE_MIN` conforme a decisão D-PROD-1.

### Passo 7 — Housekeeping do ciclo
Atualizar `tasks/backlog.md` (status BLK-RELMUN-03 → concluído no fechamento), `tasks/current_task.md`
e `tasks/completed.md` no fechamento do ciclo (fluxo padrão da esteira).

---

## Emenda DEC-011 (rascunho a inserir em CLAUDE.md §8)
> **Emenda 2026-07-02 (BLK-RELMUN-03, APROVADA por Vinicius): critério do "hexágono destacado/válido"
> do Relatório Municipal passa a ser SÓ Residual Fitness.** O item de critério de destaque da parte 2
> muda de `sam_fitness_potencial >= 3000` **E** `oferta_efetiva_disponivel >= 2000` para **somente
> `oferta_efetiva_disponivel >= OFERTA_DESTAQUE_MIN (2000)`** — o termo de SAM Fitness (≥ 3.000) é
> REMOVIDO de `_hex_destacado_mask` em `relatorio_municipal.py`. Consequência de produto (aprovada,
> esperada, NÃO é bug): passam a contar também os hexes com `oferta_efetiva_disponivel ≥ 2000` e
> `sam_fitness_potencial < 3000` → mais hexágonos destacados/"amarelos", "Espaço para academias" igual
> ou MAIOR, e contagem "Aprovados" maior em todos os relatórios já gerados/futuros. O rótulo sobre cada
> hex destacado (= `oferta_efetiva_disponivel`) e a fórmula "Espaço para academias" = round(Σ
> oferta_efetiva_disponivel dos destacados ÷ 2.500, capacidade 2.500 §4) permanecem. Constante
> `SAM_DESTAQUE_MIN`: **[REMOVIDA / mantida inerte — preencher conforme decisão do gate D-PROD-1]**.
> É mudança de DISPLAY LOCAL ao Relatório Municipal (limiares em `relatorio_municipal.py`): **NÃO toca
> `flag_sam`/DEC-006/DEC-007** (gate do SAM no pipeline de mercado, `calcular_colunas_mercado.py`), nem
> `score_priorizacao`/pesos/`hex_score_estrutural`/carteira/plano/artefatos oficiais do M1 (READ-ONLY,
> §5). A parte 1 da DEC-011 (tiles online no Relatório Municipal) e as demais decisões do gate
> (D2, D4–D9) permanecem INTACTAS. Referências: `tasks/backlog.md` (BLK-RELMUN-03);
> `context/handoff/20260702-121318-planner.md`; `src/motor_expansao/dashboard/relatorio_municipal.py`
> (`_hex_destacado_mask`); `tests/unit/test_relatorio_municipal.py`.

---

## Arquivos afetados
- `src/motor_expansao/dashboard/relatorio_municipal.py` (mask, constante condicional, docstrings, 2 notas de PDF, 2 comentários)
- `tests/unit/test_relatorio_municipal.py` (asserção PDF, 2 docstrings de fixture, 2 testes novos, 1 import)
- `CLAUDE.md` (emenda à DEC-011 em §8)
- `tasks/backlog.md`, `tasks/current_task.md`, `tasks/completed.md` (fechamento do ciclo)
- `context/handoff.md`, `context/handoff/` (este handoff)

## Dependências
- Nenhuma dependência técnica nova. Sem rede nova. Só edição de módulo já existente + testes.
- `_hex_destacado_mask` é a FONTE ÚNICA do critério; consumidores (`agregar_municipio`,
  `_zonas_geometricas`, `_focus_bounds_mercator`, `_render_mapa_municipio` camadas resumo/cobertura)
  herdam a mudança automaticamente — não há segunda implementação paralela do critério (grep
  confirmou: `sam_fitness_potencial >= 3000` só existe dentro dessa função).

## Riscos técnicos
- **Fixture que não distingue o comportamento novo:** mitigado pelos testes novos (5.3/5.4).
- **Texto residual de "SAM" no PDF:** mitigado pelo `assert b"SAM Fitness" not in pdf_bytes` (5.1) +
  grep de fechamento no módulo.
- **Constante órfã (se REMOVER):** verificado que não há import externo — remoção segura.
- **Contagem de páginas do PDF:** NÃO tocar. O código está em **9 páginas** (`PDF_SECTION_HEADERS`
  tem 9 entradas; testes asseram `b"/Count 9"`); o texto "8 páginas" em docstrings/guardrails é
  resquício histórico. Invariante real = "estrutura/contagem de páginas inalterada". NÃO forçar 8.
- **Não confundir com o Relatório Pontual:** `tests/unit/test_relatorio_pontual_censitario_export.py`
  linha 156 (`b"SAM Fitness"`) é do Big Numbers do Relatório PONTUAL (`censo_report.py`) — **fora de
  escopo, NÃO alterar**.

## Critérios de aceite finais
- `_hex_destacado_mask` retorna `oferta_efetiva_disponivel >= OFERTA_DESTAQUE_MIN` apenas (sem termo de SAM).
- Zero ocorrência de `SAM`/`sam_fitness_potencial`/`SAM_DESTAQUE_MIN` em `relatorio_municipal.py`
  (grep vazio) e nenhuma citação a "SAM Fitness >= 3.000" no módulo ou nos testes do municipal.
- Novos testes (5.3, 5.4) verdes e FALHAM se o filtro de SAM for reintroduzido.
- Suíte `tests/unit/test_relatorio_municipal.py` verde; suíte completa sem regressões; ruff+mypy limpos.
- Emenda à DEC-011 registrada em `CLAUDE.md` §8, datada 2026-07-02 e citando aprovação de Vinicius.
- `score_priorizacao`, artefatos oficiais do M1 e `flag_sam` no pipeline de mercado byte-a-byte inalterados.
- Revisão visual humana do PDF (Vinicius) aprovada antes do merge.

## Validações obrigatórias
- Comando: `python -m pytest -q tests/unit/test_relatorio_municipal.py`
  - Critério mínimo: 0 falhas; os 2 testes novos presentes e verdes.
- Comando: `python -m pytest -q`
  - Critério mínimo: sem regressões (gate único no QA).
- Comando: `ruff check src/motor_expansao/dashboard/relatorio_municipal.py tests/unit/test_relatorio_municipal.py`
  - Critério mínimo: 0 erros.
- Comando: `mypy src/motor_expansao/dashboard/relatorio_municipal.py`
  - Critério mínimo: sem novos erros.
- Comando (grep de ausência no módulo): `rg -n "SAM Fitness|SAM_DESTAQUE_MIN|sam_fitness_potencial" src/motor_expansao/dashboard/relatorio_municipal.py`
  - Critério mínimo: **nenhuma linha** (saída vazia).
- Comando (grep de ausência nos testes do municipal): `rg -n "SAM Fitness >= 3.000|SAM_DESTAQUE_MIN" tests/unit/test_relatorio_municipal.py`
  - Critério mínimo: saída vazia.

## Fora de escopo
- `flag_sam`/gate do SAM no pipeline de mercado (`calcular_colunas_mercado.py`, DEC-006/DEC-007) — NÃO tocar.
- `score_priorizacao`, `hex_score_estrutural`, pesos, carteira, plano, plano de domínio, artefatos M1.
- Relatório Pontual Censitário (raio 1,5 km) e seu Big Numbers "SAM Fitness (alunos)" em `censo_report.py`
  / `test_relatorio_pontual_censitario_export.py:156` — INTOCADOS.
- Método de intersecção/raio, tiles/basemap (parte 1 da DEC-011), estrutura/contagem de páginas do PDF,
  marca d'água anti-PII, `set_compression(False)`.

## Alerta de aprovação
ATENÇÃO: esta tarefa altera o CRITÉRIO de "hexágono destacado/válido" fixado formalmente na DEC-011 e
muda números visíveis do Relatório Municipal (hexágonos destacados, "Espaço para academias",
Aprovados/Reprovados). Criticidade **ALTA**. Aprovação HUMANA obrigatória ANTES do Builder, cobrindo:
(1) decisão de produto D-PROD-1 (remover vs. manter inerte `SAM_DESTAQUE_MIN`); (2) redação da emenda
à DEC-011. Não é fórmula/peso/artefato do M1 (por isso Alta, não Crítica), mas exige DEC por alterar
um número decidido formalmente. O usuário deve ler este handoff e confirmar.

## Guardrails ativos
- §2: nenhuma trilha paralela altera o M1 sem aprovação explícita; toda mudança relevante entra com
  teste; nenhum PR sobe com CI quebrado.
- §5 (guardrail permanente): visualizações/relatórios não recalculam nem alteram `score_priorizacao`,
  `hex_score_estrutural`, carteira, plano curto prazo, plano de domínio ou artefatos oficiais do M1.
- DEC-011 (§8): fixou o critério original (SAM≥3000 E oferta≥2000); este bloco exige EMENDA formal,
  não reescrita silenciosa.
- Parâmetros canônicos imutáveis (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, pesos
  renda=0.40/pop=0.60): INALTERADOS.
