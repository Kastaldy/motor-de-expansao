# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-DIM-19 — Fix: flag de viável (payback 60 → 36 meses) e exibir payback real (remover "Nunca")

## O que foi feito

1. **`simulador.py` linha 74 — docstring:** `payback_meses <= 60` → `payback_meses <= 36` na docstring de `ViabilidadeResult.flag_viavel`.
2. **`simulador.py` linha 302 — constante:** `flag_viavel = (margem_ebitda_pct >= 0.10) and (payback_meses <= 60)` → `<= 36`. O loop de simulação de 60 meses (`for t in range(1, 61)`) foi mantido intocado.
3. **`pages.py` linha 3377 — display:** `"> 60 / nunca"` → `"> 60 meses"`. Semântica preservada: a janela de simulação é 60 meses; o texto refere-se à janela, não ao critério de viabilidade.
4. **`test_simulador.py` — CA-07d:** docstring atualizada e `assert r.flag_viavel is True` → `is False` (payback ~57 meses > 36 → inviável com novo limite).
5. **`test_simulador.py` — CA-07e (novo):** `capex=150_000` → `payback_meses=22.0` (verificado numericamente via REPL), `flag_viavel is True`.
6. **`test_simulador.py` — CA-07f (novo):** `capex=600_000` → `payback_meses=57.0`, `36 < payback <= 60`, `flag_viavel is False`.
7. **`test_simulador.py` — CA-09a/b (novos):** helper local `_format_payback_display` (réplica pura da lógica de `pages.py:3377`, sem importar Streamlit) + testes de display: `inf → "> 60 meses"` (sem "Nunca", sem "/"), `57.0 → "57 meses"`.

**Verificação numérica prévia (REPL):**
- `viabilidade(**{**VIAVEL, "capex": 150_000}).payback_meses = 22.0` (≤ 36 ✓)
- `viabilidade(**VIAVEL).payback_meses = 57.0` (> 36, ≤ 60 ✓)
- `viabilidade(**VIAVEL).margem_ebitda_pct = 18.27%` (≥ 10% ✓, só payback falha)

## Arquivos alterados
- `/repo/src/motor_expansao/dimensionamento/simulador.py` — linhas 74 (docstring) e 302 (constante `<= 60` → `<= 36`)
- `/repo/src/motor_expansao/dashboard/pages.py` — linha 3377 (display `"> 60 / nunca"` → `"> 60 meses"`)
- `/repo/tests/unit/dimensionamento/test_simulador.py` — CA-07d atualizado; CA-07e, CA-07f, CA-09a/b adicionados

## Validações executadas
- `pytest -n auto -q tests/unit/dimensionamento/test_simulador.py tests/integration/test_streamlit_app.py`: **240 passed, 0 failed, 0 skipped** em 35.56s
- `import streamlit_app`: **import ok** (warnings de runtime esperados em modo bare)
- `ruff check simulador.py pages.py test_simulador.py`: **All checks passed**
- `mypy simulador.py pages.py`: **Success: no issues found in 2 source files**

## Problemas encontrados
Nenhum.

## Pendências
Nenhuma.

## Riscos remanescentes
Nenhum. A suite completa (gate do QA) validará ausência de regressões nos demais módulos.

## Guardrails verificados
- score_priorizacao não alterado: sim (apenas `ViabilidadeResult.flag_viavel` do simulador; `flag_viavel` dos hexágonos M1 em `hex_enrichment.py` e `calcular_colunas_mercado.py` não foram tocados)
- Artefatos M1 preservados: sim (nenhum parquet regenerado)
- Dashboard offline mantido: sim (nenhuma dependência de API ao vivo adicionada)
- config.py M1 e dimensionamento: intocados
- Loop de 60 meses em `simulador.py`: intocado
- `flag_viavel` dos hexágonos (pipelines M1/mercado): intocado
