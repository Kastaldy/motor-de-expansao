# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador executa Passo 6.0 housekeeping via `scripts/housekeeping_move_block.py` + merge após revisão visual humana do PDF por Vinicius).

## VEREDITO
APROVADO COM RESSALVAS

## Justificativa
A entrega do BLK-RELMUN-03 está correta, no escopo e READ-ONLY sobre o M1: `_hex_destacado_mask` passou a ser SÓ `oferta_efetiva_disponivel >= OFERTA_DESTAQUE_MIN`, a constante `SAM_DESTAQUE_MIN` foi removida, todos os textos/docstrings propagados, 2 testes-guard de regressão adicionados (verdes), greps de ausência vazios, ruff/mypy limpos e emenda à DEC-011 registrada. As ressalvas são NÃO-bloqueadoras: (1) 1 falha PRÉ-EXISTENTE e comprovadamente independente na suíte (`test_score_retencao_territorial` — artefato de staging gitignored ausente no ambiente, DEC-014), e (2) a revisão visual humana do PDF (Vinicius) e o move de housekeeping seguem pendentes por design do fluxo.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- Suíte completa não fecha 100% verde no ambiente: `tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime` FALHA com `FileNotFoundError` em `data/staging/unidade_territorio_retencao.parquet`. PROVADO pré-existente e fora de escopo: (a) o parquet é gitignored/gerado e não existe neste ambiente; (b) `src/motor_expansao/lifetime/` e o próprio teste NÃO foram tocados por este ciclo; (c) com as mudanças do ciclo STASHED, o teste falha IDENTICAMENTE. É defeito de disponibilidade de dado do ambiente (camada DEC-014), não regressão de BLK-RELMUN-03. Recomenda-se tratar/skip-guard separadamente (o teste já tem skip para artefatos M1 ausentes, mas não para o insumo `unidade_territorio_retencao.parquet`).

## Melhorias opcionais
- No teste `test_run_readonly_m1_por_mtime`, estender o `pytest.skip` para também cobrir a ausência de `data/staging/unidade_territorio_retencao.parquet` (insumo do `run()`), tornando-o robusto a ambientes sem os parquets de staging da camada de retenção. Fora do escopo deste bloco.

## Testes faltantes
- nenhum. Os 2 testes novos (`test_hex_destacado_criterio_so_residual_sem_sam` e `test_agregar_municipio_conta_hex_sam_baixo_oferta_alta`) são guards de regressão corretos: ambos FALHAM se o filtro de SAM for reintroduzido (o 1º espera `[True, False, True]` com um hex `sam=1000, oferta=2500` → True que seria False sob `sam>=3000 & oferta>=2000`; o 2º espera `n_hex_amarelos==1` com `sam=1000, oferta=3000` que seria 0 sob o filtro antigo).

## Riscos remanescentes
- Consequência de produto ESPERADA (não bug, documentada na emenda DEC-011): mais hexágonos "amarelos" / "Aprovados" e "Espaço para academias" igual ou MAIOR em relatórios já gerados/futuros. Requer a revisão visual humana do PDF (Vinicius) prevista no critério de aceite do Planner antes do merge.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte COMPLETA (gate único do ciclo) — `python -m pytest -q`
```
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 1224 passed, 1 skipped, 159123 warnings in 285.88s (0:04:45)
```
NOTA DE AMBIENTE: `-n auto` (xdist) foi evitado — o Builder relatou abort do execnet/xdist no Python 3.14 local. Rodei SERIAL, que é a evidência válida. A única falha é pré-existente e fora de escopo (ver abaixo). Baseline histórico do §5 (~532) e recentes (~660+) são de ambientes distintos; aqui coletaram-se 1226 itens (Python 3.14 local).

### 1b. Prova de que a falha é PRÉ-EXISTENTE (mudanças do ciclo STASHED)
```
Saved working directory and index state WIP on ciclo/BLK-RELMUN-03
=== running failing test at pre-change baseline ===
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 651 warnings in 1.40s
=== restoring ===
Dropped refs/stash@{0}
```
Causa raiz (isolada): `FileNotFoundError: ...\data\staging\unidade_territorio_retencao.parquet`. `git check-ignore` confirma o parquet é gitignored; `git status` confirma `src/motor_expansao/lifetime/` e `test_score_retencao_territorial.py` intocados neste ciclo.

### 2. Foco do bloco — `python -m pytest -q tests/unit/test_relatorio_municipal.py`
```
37 passed, 1655 warnings in 6.20s
```
Confirmados os 2 testes novos: `test_hex_destacado_criterio_so_residual_sem_sam` e `test_agregar_municipio_conta_hex_sam_baixo_oferta_alta`.

### 3. Smoke import — `python -c "import streamlit_app; print('import ok')"`
```
(WARNINGs de cache do Streamlit sem runtime — inócuos)
import ok
```

### 4. Ruff — `python -m ruff check src/.../relatorio_municipal.py tests/.../test_relatorio_municipal.py`
```
All checks passed!
```

### 5. Mypy — `python -m mypy src/motor_expansao/dashboard/relatorio_municipal.py`
```
Success: no issues found in 1 source file
```

### 6. Grep de ausência (módulo) — `rg -n "SAM Fitness|SAM_DESTAQUE_MIN|sam_fitness_potencial" src/motor_expansao/dashboard/relatorio_municipal.py`
```
(saída vazia; rg EXIT=1 = No matches found)
```

### 7. Grep de ausência (testes municipal) — `rg -n "SAM Fitness >= 3.000|SAM_DESTAQUE_MIN" tests/unit/test_relatorio_municipal.py`
```
(saída vazia; rg EXIT=1 = No matches found)
```

### 8. Escopo — `git status --porcelain`
```
 M CLAUDE.md
 M context/handoff.md
 M src/motor_expansao/dashboard/relatorio_municipal.py
 M tasks/current_task.md
 M tests/unit/test_relatorio_municipal.py
?? context/handoff/20260702-120858-block-orchestrator.md
?? context/handoff/20260702-121318-planner.md
?? context/handoff/20260702-122035-builder.md
```
config.py / src/motor_expansao/config.py: (git status vazio = INTOCADOS).

### 9. Housekeeping (pré-condição) — heading do bloco em backlog.md
```
123:### BLK-RELMUN-03 — Validar hexágono só por Residual Fitness (remover o filtro de SAM Fitness)
```
`python scripts/housekeeping_move_block.py BLK-RELMUN-03 --check` → `FALHA --check: stub ausente no backlog` — ESPERADO: o MOVE é o Passo 6.0 do orquestrador APÓS este veredito. Pré-condição confirmada: o bloco existe como bloco COMPLETO (heading presente) em `tasks/backlog.md`. O `--check` será validado no fechamento pelo orquestrador (não executado por mim — instrução do prompt).

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico. Não há tooling que dependa de config/artefatos de produção neste bloco (é módulo de display + testes unitários). A suíte usa fixtures sintéticas legítimas (permitido pela regra (b)). A única falha foi investigada de forma REAL (não mascarada com `-p no:xdist` nem skip): re-executada isolada, causa-raiz identificada (parquet ausente), e provada pré-existente por stash das mudanças do ciclo.

## Conferência cruzada com log do Builder
Bate. Builder relatou 250 passed no subconjunto impactado (`test_relatorio_municipal.py` + `test_streamlit_app.py`), import ok, ruff/mypy limpos e greps vazios — todos reproduzidos por conta própria (municipal isolado = 37 passed; import/ruff/mypy idênticos; greps vazios). O Builder rodou só o subconjunto; a suíte FULL (gate único) rodou aqui e revelou a falha PRÉ-EXISTENTE de `test_score_retencao_territorial` (fora do subconjunto do Builder), que não é regressão deste ciclo.

## Guardrails verificados
- score_priorizacao não alterado: sim (módulo de display do Relatório Municipal; nenhum recálculo). config.py INTOCADO.
- hex_score_estrutural / artefatos M1 preservados: sim (nenhum artefato oficial tocado; git status só toca relatorio_municipal.py + testes + CLAUDE.md + orquestração).
- `flag_sam` / `calcular_colunas_mercado.py` (DEC-006/DEC-007): INTOCADOS (não aparecem no git status).
- `censo_report.py` / `test_relatorio_pontual_censitario_export.py` (Relatório Pontual): INTOCADOS.
- Emenda à DEC-011 em CLAUDE.md §8: PRESENTE, datada 2026-07-02, cita aprovação de Vinicius, declara `SAM_DESTAQUE_MIN` = REMOVIDA e "NÃO toca flag_sam/DEC-006/DEC-007 nem score_priorizacao/pesos/artefatos M1".
- Parâmetros canônicos (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, pesos renda=0.40/pop=0.60): INALTERADOS (config.py intocado).
- Critério de aceite: `_hex_destacado_mask` retorna só `oferta >= OFERTA_DESTAQUE_MIN` (semântica NaN-safe preservada); estrutura/contagem de páginas do PDF inalterada (9 páginas; NÃO forçado "8"); textos coerentes.
- Validações re-executadas pelo QA (sem bypass): pytest full → 1 falha pré-existente/1224 passed; municipal → 37 passed; import → ok; ruff → clean; mypy → clean; greps → vazios.
- Escopo respeitado: sim.

## Decisão recomendada
Fechar ciclo (após revisão visual humana do PDF por Vinicius e Passo 6.0 de housekeeping pelo orquestrador). A falha pré-existente de `test_score_retencao_territorial` deve ser tratada em bloco separado (não é regressão de BLK-RELMUN-03); sugestão de melhoria: estender o skip-guard do teste para o insumo `unidade_territorio_retencao.parquet`.
