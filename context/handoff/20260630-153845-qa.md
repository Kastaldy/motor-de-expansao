# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (mover BLK-TP-05 backlog→completed via `scripts/housekeeping_move_block.py` + commit pelo humano)

## VEREDITO
APROVADO

## Justificativa
O re-teste honesto demanda OBSERVADA→captura (BLK-TP-05) está implementado em pacote disjunto READ-ONLY sobre o M1; reproduzi o veredito GO de forma independente do parquet real (R²_oof_log=+0,5750, IC95 [+0,5576, +0,5959], n=5.341) e auditei que o GO é HONESTO — métrica out-of-fold vs baseline da média sem vazamento, R² in-sample só como campo rotulado de auditoria, feature circular `n_acad_parceiras` de fato excluída do modelo principal, 6 confounds na nota e nenhuma reabertura da Camada 2 (Huff). Suíte full verde (1117 passed, 1 skipped, 0 failed), ruff/mypy limpos, import ok, anti-PII e escopo confirmados.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- O `RuntimeWarning` benigno do `runpy` ("found in sys.modules ... prior to execution") ao rodar `python -m motor_expansao.demanda_revelada.backtest_tp05` é cosmético (módulo também exportado no `__init__`); não afeta o resultado. Poderia ser silenciado movendo o `__main__` para um wrapper, mas é irrelevante para o gate.

## Testes faltantes
- nenhum essencial. Cobertura adequada: GO com sinal, NO-GO com ruído, IC95 ordenado, pct_extrapolação∈[0,100], R²_insample rotulado, zero-PII estrutural, reprodutibilidade com seed, contagem de zeros, preparar_dados/correlações, ValueError sem positivos.

## Riscos remanescentes
- GO é estatístico com demanda OBSERVADA, NÃO prova de causalidade: carrega os 6 confounds documentados (cobertura ~1%, concentração SP, ruído de coords ~1 km, viés de seleção das parceiras, multicolinearidade membros↔alunos_parceiras de mesma origem de medição, circularidade de n_acad_parceiras). A `nota_honesta` registra todos.
- A REABERTURA da Camada 2 (Huff) é gate HUMANO (Felipe), FORA deste bloco — o Builder corretamente NÃO a implementou.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte COMPLETA (gate único) — `python -m pytest -n auto -q`
```
1117 passed, 1 skipped, 273162 warnings in 309.08s (0:05:09)
```
NOTA DE AMBIENTE: o `-n auto` (xdist) rodou normalmente neste ambiente (Windows/Py3.14) — SEM o bug execnet "cannot send" citado como possível. Não foi preciso fallback serial. ZERO failed.

### 2. Subconjunto impactado — `python -m pytest -q tests/unit/test_backtest_tp05.py tests/unit/test_demanda_revelada_ingestao.py`
```
18 passed, 925 warnings in 5.80s
```
(10 do TP-05 + 8 da ingestão — bate com o log do Builder.)

### 3. ruff (escopo do ciclo) — `python -m ruff check src/motor_expansao/demanda_revelada/ tests/unit/test_backtest_tp05.py`
```
All checks passed!
```

### 3b. ruff (repo todo, paridade CI) — `python -m ruff check .`
```
All checks passed!
```

### 4. mypy — `python -m mypy src/motor_expansao/demanda_revelada/`
```
Success: no issues found in 4 source files
```

### 5. import — `python -c "import streamlit_app; print('import ok')"`
```
import ok
```

### 6. Reprodutibilidade do relatório real — `python -m motor_expansao.demanda_revelada.backtest_tp05`
```
INFO:__main__:BacktestTP05: n=5341 metodo=kfold_5x5 alpha=10 r2_oof_log=0.5750 ic95=(0.5576,0.5959) gate=GO
INFO:__main__:relatorio BLK-TP-05 escrito: data\analysis\backtest_tp05.md
  R2_oof_log (ancora, gate) = +0.5750 | IC95 = [+0.5576, +0.5959]
  R2_oof_alunos (auditoria) = +0.4218
  R2_insample = +0.5758 (apenas auditoria -- NAO usar como desempenho)
  n_treinamento = 5341 | descartados: zeros=11234, invalidos=0 | range alunos = [1, 14332]
  metodo_validacao = kfold_5x5 | alpha = 10 | pct_extrapolacao = 0.0% | flag_global = False
```
(GO, R²/IC95/n/zeros idênticos ao relatório commitado e ao log do Builder.)

### 7. git status / diff (escopo) — `git status --porcelain` + `git diff --stat`
```
 M context/handoff.md
 D data/raw/ibge/malha_brasil.geojson        <- pré-sujo, NÃO tocado pelo Builder
 D data/raw/ibge/malha_uf_brasil.geojson     <- pré-sujo, NÃO tocado pelo Builder
 M src/motor_expansao/demanda_revelada/__init__.py
 M tasks/current_task.md
?? context/handoff/20260630-*.md (snapshots do ciclo)
?? data/outputs/setores_censitarios_2022_geo/_report.md  <- pré-sujo, NÃO tocado
?? scripts/backtest_smartfit_scores.py        <- pré-sujo, NÃO tocado
?? src/motor_expansao/demanda_revelada/backtest_tp05.py
?? tests/unit/test_backtest_tp05.py
```
Nenhum commit feito (correto — commit é fechamento humano). Paths pré-sujos intactos.

### 8. Housekeeping — `python scripts/housekeeping_move_block.py BLK-TP-05 --check`
```
FALHA --check: stub ausente no backlog para BLK-TP-05
```
Estado CORRETO pré-fechamento: o move backlog→completed acontece no fechamento, não agora. O heading `### BLK-TP-05` está íntegro em `tasks/backlog.md` (linha 953) e o bloco AINDA não está em `completed.md` (0 ocorrências) → pronto para move byte-idêntico.

### 8b. Teste do helper — `python -m pytest -q tests/unit/test_housekeeping_helper.py`
```
10 passed, 501 warnings in 0.45s
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock do caminho crítico. A suíte `pytest` usa fixture sintética LEGÍTIMA (`_fixture_sintetica` construída em código, n=600+150 zeros) para isolar a lógica do modelo — prática normal, NÃO bypass; verificado que o teste NÃO referencia dado real (`grep` por `NAO_ABRA|read_parquet|.html|data/staging|data/raw` em `test_backtest_tp05.py` → nenhuma referência). A VALIDAÇÃO DO RESULTADO REAL (GO/N/IC95) foi re-executada por mim contra o parquet REAL `data/staging/demanda_revelada_h3.parquet` (passo 6), reproduzindo os números do Builder — não houve contorno do caminho de produção.

## Conferência cruzada com log do Builder
Bate integralmente. Subset 18 passed = log do Builder; ruff/mypy/import idênticos; GO, R²_oof_log=+0,5750, IC95 [+0,5576,+0,5959], n=5.341, zeros=11.234 (Builder corrigiu o typo do Planner 10.234→11.234; 16.575−5.341=11.234 ✓), alpha=10, kfold_5x5, modelo de auditoria com circular salta para +0,6388 — todos reproduzidos por mim. Diferença vs Builder: rodei a suíte FULL (1117 passed) que o Builder não roda (gate único do QA), e o ruff repo-wide para paridade CI.

## Guardrails verificados
- score_priorizacao não alterado: sim (camada paralela; `git status` de `data/staging`/`data/outputs` → "NENHUM artefato M1 oficial modificado"; pesos renda=0.40/pop=0.60 intactos).
- Artefatos M1 preservados: sim (zero escrita em `brasil_*.parquet`/`hexagonos_brasil_*.parquet`/`top_oportunidades_resumo.csv`/`resumo_por_uf.csv`; o parquet de demanda revelada é consumido READ-ONLY).
- DEC-012 pacote disjunto: sim — `backtest_tp05.py` importa SÓ de `motor_expansao.dimensionamento.aderencia`/`.backtest_dim` (camada irmã) e `.contrato`; NÃO importa de `pipelines/m1`, `censo_*`, `dashboard`, `config.py` raiz (as ocorrências de "pipelines/m1"/"censo_"/"dashboard" no grep são linhas de DOCSTRING/COMENTÁRIO, não imports — verificado por leitura das linhas 49-57).
- Anti-PII (DEC-012): sim — nenhuma coluna de `COLUNAS_PII_PROIBIDAS` em coefs/spearman/auditoria/relatório; scan do relatório real por PII forte → "NENHUMA"; testes 100% fixture sintética; fonte real `NAO_ABRA/` não tocada; teste estrutural `test_zero_pii_em_todas_as_saidas` verde.
- DEC-008: sim — métrica de desempenho é OOF (k-fold repetido 5×5) vs baseline da média; R² in-sample existe SÓ rotulado "apenas auditoria -- NAO usar como desempenho" e NÃO entra no gate; NO-GO é resultado válido (teste `test_no_go_com_ruido_puro` confirma que ruído puro dá NO-GO — GO não foi forçado).
- DEC-009: sim — `membros` usado só como insumo OBSERVADO em análise read-only; NÃO é ajuste do score nem preditor geográfico de magnitude; nota honesta reafirma DEC-009.
- GO HONESTO (auditoria especial): (i) R² in-sample não usado como desempenho ✓; (ii) `n_acad_parceiras` ausente de `FEATURES_PRINCIPAIS`/`preparar_dados`, só no modelo de auditoria rotulado ✓; (iii) métrica OOF sem vazamento — `Ridge.fit(X[train])` prediz só `X[test]`, baseline = média do treino ✓; (iv) 6 confounds na `nota_honesta` ✓; (v) sem reabertura da Camada 2 (Huff) — só gerou relatório ✓.
- Validações re-executadas pelo QA (sem bypass): pytest full → 1117 passed/1 skipped/0 failed; subset → 18 passed; ruff escopo+repo → All checks passed!; mypy → 0 issues; import → ok; relatório real → GO reproduzido; helper test → 10 passed.
- Escopo respeitado: sim (só os 5 paths do ciclo + snapshots; paths pré-sujos não tocados; nenhum commit).
- Housekeeping (Passo 6.0): VERIFICADO pré-fechamento — helper sadio (10 passed), bloco íntegro no backlog (linha 953), `--check` falha como esperado (stub ainda ausente; move é passo do orquestrador no fechamento). NÃO executei o move.

## Decisão recomendada
Fechar ciclo: mover BLK-TP-05 backlog→completed via `scripts/housekeeping_move_block.py` (com stub) e commitar pelo humano. Veredito do bloco = GO honesto, READ-ONLY sobre o M1; a REABERTURA da Camada 2 (Huff) é gate humano de Felipe, separado.
