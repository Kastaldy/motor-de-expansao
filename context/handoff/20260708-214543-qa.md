# Handoff — QA

## Skill que gerou este handoff
QA

## Próxima Skill recomendada
Block Orchestrator (ciclo BLK-REV-04 concluído)

## Bloco
BLK-REV-04 — Diagnóstico de gargalo: troca de modos de cor / heat maps

## Status
APROVADO — Fechar ciclo

## Decisão
**Fechar ciclo.** Bloco de diagnóstico READ-ONLY; relatório gerado, gitignored, com
conteúdo completo (Partes A/B/C/D + hipóteses H1..H4), coerente com BLK-REV-01/REV-03.
Zero regressões atribuíveis ao bloco. Housekeeping aplicado (stub no backlog + bloco em completed).

## Validações re-executadas (independentes)

### 1. Suite FULL
A coleta da suite completa é INTERROMPIDA por 1 erro de ambiente PRÉ-EXISTENTE
(`ModuleNotFoundError: No module named 'matplotlib'` em
`tests/unit/demanda_revelada/test_viz_atratividade.py`, do BLK-ATR-04, commit dea3913).
Rodando com o teste bloqueado deselecionado:

```
$ python -m pytest -q --ignore=tests/unit/demanda_revelada/test_viz_atratividade.py 2>&1 | tail -8
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_curto_usa_localidade_mock
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_curto_usa_localidade_mockada
4 failed, 1524 passed, 4 skipped, 19 warnings in 594.32s (0:09:54)
```

**Diagnóstico das 4 falhas + 1 erro: TODAS são de AMBIENTE (dependências opcionais
ausentes), PRÉ-EXISTENTES, NÃO atribuíveis ao BLK-REV-04:**
- `test_viz_atratividade.py` (erro de coleta): `import matplotlib` no topo — matplotlib não instalado.
- 4× `plus_code`: `resolve_plus_code` faz `from openlocationcode import ...` num try/except
  que retorna `None` quando o módulo falta → `assert out is not None` falha. openlocationcode não instalado.

**Prova de que são de ambiente, não do bloco:** após `pip install matplotlib openlocationcode`,
os 5 testes passam:
```
$ python -m pytest <os 5 testes> -q
14 passed in 36.88s
```
E o bloco não tocou `src/` nem `tests/` (só um `.md` gitignored) — ver git status abaixo.

### 2. loop_guard
```
$ python scripts/loop_guard.py --base HEAD
GUARD OK: 0 caminho(s) alterado(s), nenhum proibido.
EXIT=0
```

### 3. git diff READ-ONLY M1
```
$ git diff HEAD -- src/motor_expansao/pipelines/m1/ src/motor_expansao/config.py
(vazio)
```

### 4. Relatório existe, gitignored, com conteúdo
```
$ ls -la data/analysis/diagnostico_troca_cor.md
-rw-r--r-- 1 loop loop 17593 ... data/analysis/diagnostico_troca_cor.md
$ git check-ignore -v data/analysis/diagnostico_troca_cor.md
.gitignore:123:data/analysis/	data/analysis/diagnostico_troca_cor.md
$ grep -c "^##" ... = 17 seções
```
Seções presentes: Partes A/B/C/D (linhas 54/62/75/83) e hipóteses H1/H2/H3/H4 (100/110/127/138).

### 5. Coerência de conteúdo
- **H4 verificado no código:** `@st.fragment` existe em `render_mapa_pydeck_fragment`
  (pages.py:4099); o seletor de modo (`mapa_territorial_color_mode`) está FORA do fragmento,
  em `render_mapa_territorial` → troca de modo = rerun da aba inteira. Relatório correto.
- **H2 (`@st.cache_data`) coerente com BLK-REV-03:** REV-03 isolou `deck.to_json()` (0,5–1,5 s,
  payload 21–24 MB, ~65% tooltips) como gargalo dominante; REV-04 confirma que na troca de modo
  só ~8,5% do payload (`fill_color`) muda e recomenda cachear o builder (elimina B+C em cache-hit).
  Recomendação consistente.
- **H1/H3 descartadas** com justificativa técnica (stack pydeck/Streamlit; Parte A < 5 ms).

### 6. Housekeeping
```
$ python scripts/housekeeping_move_block.py BLK-REV-04 --check   # (antes) EXIT=1 stub ausente
$ python scripts/housekeeping_move_block.py BLK-REV-04 --date 2026-07-08
OK: movido BLK-REV-04 (910 bytes) -> tasks/completed.md; stub em tasks/backlog.md.
$ python scripts/housekeeping_move_block.py BLK-REV-04 --check   # EXIT=0
$ python -m pytest tests/unit/test_housekeeping_helper.py -q → 10 passed
```

## Observações (não bloqueantes)
- Working tree tem `src/motor_expansao/dimensionamento/config.py` com mudança PURA de CRLF→LF
  (`git diff --ignore-all-space` = vazio). Artefato de line-ending do checkout Windows; não é do
  BLK-REV-04 (diagnóstico READ-ONLY), não toca M1, sem efeito em teste. Registrado para higiene futura.
- Ambiente do loop está SEM `matplotlib` e `openlocationcode` (deps opcionais). Isso mascara a suite
  full de qualquer bloco. Recomenda-se instalá-las na imagem do loop para o gate de suite full ser limpo.

## Guardrails confirmados
- READ-ONLY sobre o M1: git diff M1/config vazio; loop_guard GUARD OK; só escreveu `.md` gitignored.
- Pesos `renda=0.40`/`pop=0.60`, `score_priorizacao`, `hex_score_estrutural`, caps INALTERADOS.
