# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-UI-02 — Follow-up UX do dashboard (coord destaque, tooltip cortado, densificação por filtro de município)

## O que foi feito
Implementados os 4 ajustes aprovados no gate humano (Felipe/usuário, 2026-06-12), todos READ-ONLY sobre o M1 (apenas CSS-em-dict, markdown de sidebar e strings de caption):

- **D1=A (#1 coord):** em `render_coord_search_sidebar` (`pages.py`), o heading markdown `### Busca por coordenada` + `st.sidebar.caption(...)` foram substituídos por um único `st.sidebar.info("**Busca por coordenada** — localize um hexagono pela coordenada. Offline, sem API externa.")`. Preservados: `st.sidebar.markdown("---")` acima, o `st.sidebar.text_input("Coordenada (lat, lng)", ... key="coord_search_input")`, o parse `parse_coordinate_input`, o `st.sidebar.error(...)`, a assinatura `() -> tuple[float,float] | None` e a chamada única em `streamlit_app.py:470` (fora do branch de aba — Bloco 5 intacto).
- **D2=A (#2 tooltip):** adicionadas as 4 chaves `"fontSize": "11px"`, `"padding": "6px 8px"`, `"maxWidth": "260px"`, `"lineHeight": "1.25"` ao dict `style` de AMBOS `_shared_map_tooltip` (14 linhas, modo M1) e `_hybrid_compact_tooltip` (8 linhas, modos híbrido/residual) em `components.py`. Chaves existentes (`backgroundColor`/`color`/`border`/`borderRadius`/`fontFamily`) e o `html` de ambos INALTERADOS.
- **D3=(a)+(b) (#3 densificação — só comunicação):**
  - (a) anexada a frase `" Para densidade total de uma area, selecione um municipio no filtro lateral."` ao ramo NÃO-capped de `build_map_scope_caption` (`components.py`); a substring testada `"todos os hexagonos validos {scope_label}"` foi preservada (anexação pura). Ramo capped INALTERADO.
  - (b) anexada a frase `" Dica: filtre por municipio na barra lateral para ver o recorte com densidade total."` ao caption introdutório de `render_mapa_territorial` (`pages.py`). Nenhum widget novo; controles de modo/overlay/scope/downsample/cap INTOCADOS.
- **D4=A (testes):** atualizado `test_build_map_scope_caption_reflete_todos_os_hexes_da_uf` mantendo as asserções existentes (`"todos os hexagonos validos da UF selecionada"`, `"1.234"`, `"100 melhores" not in`) e ADICIONADA `assert "municipio" in caption`. Criado teste novo `test_map_tooltips_tem_css_de_tamanho` que assere `fontSize=="11px"`, `padding=="6px 8px"`, `maxWidth=="260px"`, `lineHeight=="1.25"` no `style` de ambos os tooltips. Importados `_shared_map_tooltip` e `_hybrid_compact_tooltip` no bloco de import já existente de `motor_expansao.dashboard.components`.

Caminho seguido no risco de teste-contrato (R2): ANEXAÇÃO — a substring exata exigida pelo teste do ramo não-capped permaneceu intacta; nenhum teste do ramo capped foi tocado.

## Arquivos alterados
- `src/motor_expansao/dashboard/pages.py` — (#1) `render_coord_search_sidebar`: heading+caption → `st.sidebar.info`; (#3b) `render_mapa_territorial`: frase de dica de município anexada ao caption introdutório.
- `src/motor_expansao/dashboard/components.py` — (#2) 4 chaves de CSS adicionadas ao `style` de `_shared_map_tooltip` e `_hybrid_compact_tooltip`; (#3a) frase de dica de município anexada ao ramo não-capped de `build_map_scope_caption`.
- `tests/integration/test_streamlit_app.py` — import de `_shared_map_tooltip`/`_hybrid_compact_tooltip`; nova asserção `"municipio" in caption` no teste de scope caption; novo teste `test_map_tooltips_tem_css_de_tamanho`.

## Validações executadas
- pytest: `python -m pytest -q tests/integration/test_streamlit_app.py` → **183 passed, 0 failed, 0 skipped** (9151 warnings pré-existentes de DeprecationWarning asyncio/pytest-asyncio em Python 3.14). Rodei este subconjunto porque é o arquivo de testes que cobre exatamente os 3 arquivos tocados (pages/components do dashboard + os contratos de caption/tooltip). Rodei SERIAL — `-n auto` (xdist) dá INTERNALERROR de gateway no Python 3.14 local (execnet×3.14), conforme nota de ambiente; não mascarei com `-p no:xdist`. A suíte full é gate do QA.
- import: `python -c "import streamlit_app; print('import ok')"` → **ok** (apenas warnings esperados de cache do Streamlit sem runtime).
- ruff: `ruff check` nos 3 arquivos tocados → **All checks passed!**
- mypy: `mypy src/motor_expansao/dashboard/pages.py src/motor_expansao/dashboard/components.py` → **Success: no issues found in 2 source files**.

## Problemas encontrados
Nenhum.

## Pendências
- Suíte FULL é responsabilidade do QA (gate único). Baseline informado pelo Planner: `695 passed, 1 skipped, 3 failed PRÉ-EXISTENTES` (drift de CSVs locais gitignored + gate DEC-006 do SAM). Critério: não introduzir falhas novas.

## Riscos remanescentes
- **R5 (limitação nativa do pydeck):** a redução de altura do tooltip (11px/lineHeight 1.25) ATENUA o recorte na borda inferior no caso típico em 1080p, mas o recorte do tooltip no iframe é limitação nativa do deck.gl; com o cursor colado no fundo absoluto pode ainda recortar 1-2 linhas. Sem solução nativa adicional sem trocar o componente (fora de escopo, decisão BLK-UI-01).

## Guardrails verificados
- score_priorizacao não alterado: sim (nenhuma mudança em score/pesos/fórmula).
- Artefatos M1 preservados: sim (mudanças são CSS-em-dict, markdown e strings; zero escrita em artefato).
- Dashboard offline mantido: sim (nenhuma dependência de API ao vivo introduzida).
- Bloco 6 anti-OOM: `MAP_POINT_LIMIT`/`MAP_POINT_LIMIT_LARGE`, `_downsample_map_index`, scope/cap dos builders e payload global ao cliente INTOCADOS — o #3 é só texto.
- Bloco 4 (carga lazy por UF) e Bloco 5 (render lazy de abas): intactos; `render_coord_search_sidebar` segue chamada SEMPRE em `streamlit_app.py:470` com assinatura inalterada.
- READ-ONLY M1: `score_setor_2022_calibrado`, `score_expansao_hibrido`, `score_oportunidade_residual`, carteira, plano, SAM, artefatos oficiais INALTERADOS.

## Resumo de 1 linha
BLK-UI-02 implementado (D1=A coord destaque via `st.sidebar.info`, D2=A CSS dos 2 tooltips, D3=(a)+(b) dica de filtro por município anexada, D4=A testes); 183 passed no test_streamlit_app.py, import/ruff/mypy limpos.
