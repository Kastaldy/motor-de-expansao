# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer (BLK-RELPON-10 — gate único da suíte FULL)

## Próxima Skill recomendada
**GATE VISUAL de Vinicius** sobre as 4 amostras em `data/reports/scratch/blk_relpon_10/` →
depois `/registrar-decisao` (emenda à DEC-011, S3) → fechamento em commit no branch
(**sem PR** — estratégia de PR único ao fim do RELPON-11).

## VEREDITO
**APROVADO**

## Justificativa
A suíte FULL fecha em `1 failed, 1951 passed, 2 skipped` e a única falha
(`test_score_retencao_territorial::test_run_readonly_m1_por_mtime`) roda sobre código com **diff
ZERO** neste ciclo e cai por `FileNotFoundError` de um parquet de staging *gitignored* ausente do
ambiente — causalmente impossível de ser regressão do bloco. O gate humano foi implementado ao pé
da letra (S1=A: `score` nos DOIS lugares; S2=B: 6 faixas com os cortes exatos 0/1.250/2.500/5.000/
10.000/inf), o M1 está intocado (mtime dos 7 artefatos oficiais idêntico antes/depois, `config.py`/
`pipelines/`/`censo_point.py` fora do diff) e provei **byte-identidade sha256** das 5 camadas
pré-existentes do PNG entre baseline e HEAD — o refactor `_frame_box_metric` e o rodapé
parametrizado por `raio_km` não mudaram um único byte do caminho existente.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
1. **Poluição visual dos pins no mapa de 5 km em metrópole densa (Risco 5 do Planner, materializado).**
   Na amostra `SLIDE2_paulista_sp.png` os pins de concorrentes cobrem quase todo o choropleth de
   residual — o mapa vira um tapete de logos e a leitura das faixas se perde. O Planner previu isto
   e deixou o rollback de 1 linha (`pins=[]`/`ultra_pins=[]` em `_render_camada_residual_hex`),
   explicitamente **para decidir no gate visual, não antes**. Não bloqueia o QA; é decisão de
   produto do Vinicius. Manaus e Chapecó não sofrem do problema.
2. **`docs/relatorio_pontual_censitario.md` ficou stale.** Continua dizendo "estrutura de 6
   paginas" (linha 212), "4 camadas" sem `renda_domiciliar` (linha 171), `/Count 6` e o nome antigo
   `test_slide_unico_count_5_...` (linha 253). Parte já estava stale ANTES do bloco (o RELPON-07 não
   atualizou), e o Planner limitou o escopo de docs a "CLAUDE.md **só §4**" — então é dívida
   herdada + agravada, não desvio de escopo do Builder. `CLAUDE.md` §4 foi corrigido corretamente.
3. **Snapshot do Builder ausente.** Não existe `context/handoff/2026072*-builder.md` para o
   BLK-RELPON-10 (o último é `20260721-131000-builder.md`, do RELPON-09) — o Builder foi
   interrompido antes de gravá-lo. A trilha de auditoria do ciclo tem um buraco no papel Builder;
   recomendo gravar um snapshot retroativo no fechamento.
4. **Amostras só da variante clássica.** O Builder deixou 3 PDFs, todos `classico`; a validação
   obrigatória #6 do Planner pedia **6** (as duas variantes). **Fechei a lacuna eu mesmo**: gerei
   `QA_RECENTE_chapeco_sc.pdf` (7 páginas, dado real, basemap online) + a página 2 rasterizada —
   a gêmea `_socioeconomia_residual_page` funciona. Fica só o registro de que a evidência não veio
   do Builder.

## Melhorias opcionais
- **Espaço vertical vazio no slide-hero da variante RECENTE.** Com `rows=1`, o clamp de largura de
  `_map_grid_cells_packed` (`max_total_w = _PAGE_W - 40`) limita `cell_w` a ~455 pt, e a altura
  derivada (~270 pt) fica centralizada numa faixa de 460 pt → ~95 pt de branco em cima e embaixo
  (visível em `QA_RECENTE_SLIDE2_chapeco_sc.png`). É cosmético e a variante recente **não é o
  default de produção** (o clássico é). Se incomodar no gate, um `bottom`/`top` dedicado para o
  grid 2x1 resolve.
- Nomes de teste stale herdados (`test_classico_sem_fotos_mantem_6_paginas`,
  `test_sem_viabilidade_mantem_6_paginas`, `test_classico_sem_info_mantem_6_paginas`,
  `test_dispatcher_regressao_sem_params_novos_count_6`, `test_classico_gera_6_paginas_e_secoes`)
  agora asserem 7. O Builder corrigiu o que o Planner mandou (`..._count_5_...` → `..._count_7_...`);
  estes cinco ficaram. Renomear num passe de higiene futuro.

## Testes faltantes
- nenhum bloqueador. Cobertura nova é substantiva: 16 `def test_` adicionados (15 líquidos + 1
  rename), incluindo os 7 itens que o Planner exigiu na Fase E (a–g) — `_tema_bicolor(0..4)` e o
  contrato do ordinal `-1` do RELPON-11, spy de título/cor das 5 páginas nas 2 variantes,
  `cols/rows` 2x1 vs default 2x2, rodapé `1,5`/`5,0`, faixa de cor por valor absoluto + NaN
  distinto de `sem residual`, labels sem acento, `hexes_df=None` → fallback textual com `/Count 7`,
  e a prova anti-regressão `RAIO_CENSITARIO_DEFAULT_KM == 1.5` + `RAIO_RESIDUAL_DISPLAY_KM` fora do
  `config.py`.
- Sugestão (não bloqueia): um teste para o helper novo `_hexes_vizinhos_do_ponto` de
  `api/service.py` — é o único código novo do ciclo sem teste próprio (é `try/except → None`,
  espelho literal de `_residual_do_ponto`, e o caminho de PDF da API já é coberto de outra forma).

## Riscos remanescentes
1. **Ponto na divisa de UF** (dashboard): o `df` é a fatia `uf=XX` (CLAUDE.md §4) → disco truncado,
   meio-mapa sem hexes. Aceito, documentado na docstring, degradação graciosa (verifiquei: hex
   ausente vira `_FILL_SEM_DADO`, não exceção).
2. **Emenda à DEC-011 ainda NÃO registrada.** O bloco reverte de fato o BLK-CENSO-02 (residual
   passa a choropleth no Pontual). É passo de gate (`/registrar-decisao`), fora do escopo do
   Builder — mas **obrigatório antes do merge**.
3. Custo de render: medido, **desprezível** (ver saída literal) — o basemap é o caro e é
   compartilhado.

## Saída literal das validações (re-executadas pelo QA)

### V0 — SUÍTE FULL (gate único; **serial**, `-n auto` aborta neste Windows/Python 3.14)
```
$ python -m pytest -q
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 1951 passed, 2 skipped, 315744 warnings in 1576.91s (0:26:16)
```

**Análise da única falha — pré-existente e AMBIENTAL, não regressão:**
```
def test_run_readonly_m1_por_mtime():
    ...
>       run(root=root)  # roda de verdade sobre os parquets reais
tests\unit\test_score_retencao_territorial.py:263:
src\motor_expansao\lifetime\score_retencao_territorial.py:730: in run
    df = _carregar_dataset(staging / "unidade_territorio_retencao.parquet")
src\motor_expansao\lifetime\score_retencao_territorial.py:169: in _carregar_dataset
    return pd.read_parquet(path)
...
path_or_buf = 'C:\\...\\motor-de-expansao\\data\\staging\\unidade_territorio_retencao.parquet'
```
```
$ ls data/staging/unidade_territorio_retencao.parquet data/staging/growth_api_historico.parquet
ls: cannot access 'data/staging/unidade_territorio_retencao.parquet': No such file or directory
ls: cannot access 'data/staging/growth_api_historico.parquet': No such file or directory

$ git diff --stat 228fa4a 791035d -- src/motor_expansao/lifetime/ tests/unit/test_score_retencao_territorial.py
(vazio = diff ZERO)
```
Conclusão: o código exercitado pelo teste é **byte-idêntico ao baseline**; a falha depende só de um
artefato de staging *gitignored* ausente do disco. Nenhuma outra falha na suíte → **zero regressões
do ciclo**.

**Contagem de coleta (prova de que nada foi perdido):**
```
baseline 228fa4a : 1939 tests collected
HEAD     791035d : 1954 tests collected      (delta +15 = 16 def test_ novos - 1 removido no rename)
1954 = 1951 passed + 1 failed + 2 skipped
```

### V1 — Subconjunto impactado
```
$ python -m pytest -q tests/unit/test_relatorio_pontual_censitario_export.py tests/unit/test_relatorio_pontual_censitario_mapa.py tests/unit/test_relatorio_pontual_censitario_motor.py tests/unit/test_relatorio_pontual_fotos.py tests/unit/test_relatorio_pontual_info_imovel.py tests/unit/test_relatorio_pontual_orquestracao.py tests/unit/test_relatorio_pontual_ui_relviab06.py tests/unit/test_relatorio_pontual_viabilidade.py tests/unit/test_uplift_composicao_setor.py
148 passed, 8103 warnings in 78.50s (0:01:18)
```

### V2 — Não-regressão cruzada do Relatório Municipal (`/Count 9` do OUTRO módulo)
```
$ python -m pytest -q tests/unit/test_relatorio_municipal.py
50 passed, 2305 warnings in 28.43s

$ git diff --name-only 228fa4a 791035d | grep test_relatorio_municipal
(vazio = arquivo INTOCADO)
```

### V3 — Smoke de import do app
```
$ python -c "import streamlit_app; import motor_expansao.dashboard.censo_map as m; print(len(m.CAMADAS_CENSITARIAS)); print(m.CAMADAS_CENSITARIAS)"
7
('densidade', 'renda', 'score', 'renda_domiciliar', 'socioeconomia', 'residual', 'concorrentes')
```

### V4 — Lint e tipos
```
$ python -m ruff check src tests
All checks passed!

$ python -m mypy src --no-incremental          # HEAD 791035d
src\motor_expansao\dimensionamento\growth_api_client.py:28: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\ibge_censo.py:24:  error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\poi_enrichment.py:15: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\base_h3_brasil.py:31: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\geo.py:21:          error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\telegram_bot.py:23: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\service.py:269:     error: Library stubs not installed for "requests"  [import-untyped]
Found 7 errors in 7 files (checked 113 source files)

$ python -m mypy src                            # BASELINE 228fa4a (via git worktree)
... mesmíssimos 7 arquivos ...
Found 7 errors in 7 files (checked 113 source files)
```
**Sem regressão de contagem: 7 no HEAD == 7 na baseline**, conjunto IDÊNTICO, todos da mesma classe
(`types-requests` não instalado — ruído de ambiente local; o CI usa `ignore_missing_imports`).
*Nota:* a medição de "6 erros" citada no briefing vem de execução com cache quente; com
`--no-incremental` os dois lados dão 7. Em qualquer das duas medições, HEAD == baseline.

### V5 — Integração do dashboard
```
$ python -m pytest -q tests/integration/test_streamlit_app.py
255 passed, 13006 warnings in 54.44s
```

### V6 — PDFs de amostra com dado real
```
$ (inspeção das amostras do Builder — data/reports/scratch/blk_relpon_10/, gitignored)
RELPON10_chapeco_sc.pdf: [b'/Count 7'] imgs= 18 hero= 1 mapas_calor= 1
RELPON10_manaus_am.pdf:  [b'/Count 7'] imgs= 18 hero= 1 mapas_calor= 1
RELPON10_paulista_sp.pdf:[b'/Count 7'] imgs= 18 hero= 1 mapas_calor= 1
$ git check-ignore -v data/reports/scratch/blk_relpon_10/RELPON10_manaus_am.pdf
.gitignore:76:data/reports/scratch/   (anti-PII OK: nenhum PDF versionado)
```
Inspeção VISUAL das 3 páginas 2 rasterizadas: 2 mapas lado a lado, banda de título MAGENTA
(= `_tema_bicolor(0)`), raio rotulado no título E no rodapé de cada PNG, legenda de 6 faixas
completa, escala e atribuição CARTO presentes. Valores do hex central batem com a verificação
independente: Paulista `0` (avenida saturada), Manaus `0`, **Chapecó `1.541`**.

**Lacuna da variante RECENTE fechada pelo QA (o Builder só deixou `classico`):**
```
$ python <script do QA> ...
setores: 495
hexes no disco k=5: 91 | valores: [0.0 x77, 977, 1042, 1158, 1211, 1262, 1541, 1878, 2333, 2349, 2765, 2987, 3143, 3757, 3908]
camadas: ['concorrentes','densidade','renda','renda_domiciliar','residual','score','socioeconomia']
PDF: data\reports\scratch\blk_relpon_10\QA_RECENTE_chapeco_sc.pdf 1258335 bytes | /Count 7 = True | hero = True
paginas: 7
PNG: data\reports\scratch\blk_relpon_10\QA_RECENTE_SLIDE2_chapeco_sc.png
```

### V7 — (evidência PRÓPRIA do QA) Byte-identidade das camadas pré-existentes
Mesmo insumo sintético determinístico, `basemap=False`, sha256 de cada PNG:
```
===== HEAD 791035d =====                      ===== BASELINE 228fa4a (git worktree) =====
concorrentes     52b6d5d4...96dbac            concorrentes     52b6d5d4...96dbac   IDÊNTICO
densidade        53b5b8bb...565e92            densidade        53b5b8bb...565e92   IDÊNTICO
renda            5845d3ae...acc08c            renda            5845d3ae...acc08c   IDÊNTICO
renda_domiciliar c1b2b3cc...1b0b5d            renda_domiciliar c1b2b3cc...1b0b5d   IDÊNTICO
score            ef5c651f...0f78d8            score            ef5c651f...0f78d8   IDÊNTICO
socioeconomia    691977f2...dfd62e            (nova neste bloco)
```
Prova dura de que (a) a extração de `_frame_box_metric` é refactor sem mudança de comportamento e
(b) o rodapé parametrizado por `raio_km` sai byte-a-byte igual com `raio_km=1.5`.

### V8 — (evidência PRÓPRIA do QA) ASCII no PNG (RD-2)
Spy em `_draw_text` sobre um render COMPLETO das 7 camadas:
```
CAMADAS: ['concorrentes','densidade','renda','renda_domiciliar','residual','score','socioeconomia']
TOTAL textos desenhados: 86 | distintos: 49
NAO-ASCII ( 0 ):
TEXTOS NOVOS (slide-hero):
   [ASCII-OK] 'Raio 5,0 km - EPSG:3857 - fundo de ruas offline'
   [ASCII-OK] 'Residual Fitness - raio 5 km'
   [ASCII-OK] 'Residual disponivel (alunos)'
   [ASCII-OK] 'Residual no hexagono: 0'
   [ASCII-OK] 'Socioeconomia - raio 1,5 km'
```
**Zero** caractere não-ASCII em qualquer texto desenhado no PNG. No **PDF** a string nova
`"Socioeconomia e Residual Fitness"` é ASCII puro (latin-1-safe, sem travessão/bullet/reticências)
e o `_MAPA_INDISPONIVEL` acentuado ("Mapa indisponível…") segue intocado — os dois regimes não
foram confundidos.

### V9 — (evidência PRÓPRIA do QA) Robustez do `hexes_df` (fallback gracioso)
```
sem coluna de valor -> camadas: [... 'residual' ...]      # sem exceção
valores lixo        -> camadas: [... 'residual' ...]      # sem exceção
hex_id invalido     -> camadas: [... 'residual' ...]      # sem exceção
```

### V10 — (evidência PRÓPRIA do QA) Custo de render (Risco 7 do Planner)
Chapecó/SC, dado real, `basemap=True` (tiles em cache), sob carga da suíte:
```
SEM hexes_df: 13.43s | camadas=6   (capa/censitárias + socioeconomia)
COM hexes_df: 12.85s | camadas=7   (+ residual)
```
A camada de residual custa **~0** em wall clock: o caro é o basemap, e o de 5 km vem do cache.
*(Uma medição da baseline via worktree deu 334 s porque o worktree não enxerga
`data/cache/basemap_tiles/` e refez o fetch de rede — descartada por não ser comparável.)*

## Conferência de no-bypass
Confirmo: **nenhuma** validação usou `--config /dev/null`, fixture que contorna config real, ou mock
do caminho crítico. A suíte FULL rodou sobre o repositório real, serial, sem `-p no:xdist`, sem
`-k`/deselect e sem `--lf`. Além disso auditei explicitamente o NO-BYPASS do diff de testes:
```
$ git diff 228fa4a 791035d -- tests/ | grep -E "^\+.*(skip|xfail|@pytest.mark|pytest.skip)"
(vazio = nenhum skip/xfail/mark introduzido)

$ git diff 228fa4a 791035d -- tests/ | grep -E "^-.*def test_"
-def test_slide_unico_count_5_e_titulo_mapas_de_calor()     # ÚNICO removido = o RENAME que o Planner pediu

asserts removidos: 41   |   asserts adicionados: 104   (líquido +63)
```
As 41 asserções removidas são exatamente as linhas `/Count N` substituídas por `/Count N+1` (mais as
do teste renomeado). **A atualização 6→7 é CORRETA, não afrouxamento**: os 3 PDFs de amostra com
dado real, o meu 4º PDF e `pdf.page_count` do PyMuPDF mostram **7 páginas reais**, e o código insere
a página incondicionalmente nos DOIS geradores. `assert pdf_bytes.count(b"/Subtype /Image") >= 5`
**não foi relaxado**. Em `test_relatorio_pontual_censitario_mapa.py` o `set(mapas) == _CAMADAS`
virou `== _CAMADAS_SEM_HEXES` — igualdade EXATA mantida e **fortalecida** (passa a exigir
`socioeconomia` presente E `residual` ausente sem `hexes_df`).

## Conferência cruzada com log do Builder
**Bate.** O `current_task.md` registrou, na pausa: smoke import ok, ruff limpo, e
`_censitario_export.py` + `_censitario_mapa.py` = **75 passed**. Re-executei os dois isoladamente
dentro do subconjunto (V1, 148 passed no total dos 9 arquivos) e o smoke/ruff (V3/V4) — tudo
confere. O commit `791035d` tem mensagem "WIP / NAO VALIDADO" por causa da interrupção do Builder,
**não** por defeito: o código e os testes estão completos e a validação retomada aqui fecha verde.
Divergência única: o Builder não gerou as amostras da variante `recente` nem o snapshot de handoff
(itens 3 e 4 dos problemas médios).

## Guardrails verificados
- **score_priorizacao não alterado:** SIM. `git diff --name-only 228fa4a 791035d` não lista
  `config.py`, `src/motor_expansao/pipelines/`, nem qualquer path sob `data/`. `PESOS_HEX_SCORE_ESTRUTURAL`,
  `hex_score_estrutural` e `score_priorizacao` fora do diff. `oferta_efetiva_disponivel` e
  `score_setor_2022_calibrado` são apenas **LIDOS**.
- **Artefatos M1 preservados:** SIM — mtime E tamanho **idênticos** antes e depois de TODA a
  execução do QA (suíte FULL inclusa):
  ```
  brasil_estrutural.parquet             2026-06-10 09:50:32.131  35496625   (antes == depois)
  brasil_priorizados.parquet            2026-06-10 09:50:32.208   7179409   (antes == depois)
  hexagonos_brasil_oportunidades.parquet 2026-06-10 09:50:34.599 49278753   (antes == depois)
  hexagonos_brasil_dashboard.parquet    2026-06-10 10:24:10.093  49109457   (antes == depois)
  hexagonos_mapa_sample.parquet         2026-06-10 10:24:11.842  14779071   (antes == depois)
  top_oportunidades_resumo.csv          2026-06-10 10:24:10.131     57068   (antes == depois)
  resumo_por_uf.csv                     2026-06-10 10:24:10.134      1213   (antes == depois)
  ```
- **Motor censitário INTOCADO (RD-1):** `censo_point.py` **fora do diff**;
  `RAIO_CENSITARIO_DEFAULT_KM = 1.5` (censo_point.py:23) e
  `METODO_RELATORIO_PONTUAL_CENSITARIO = "setor_censitario_intersecao_area_1p5km"` (censo_point.py:22)
  inalterados; `analisar_ponto_censitario_setores` sem diff. `RAIO_RESIDUAL_DISPLAY_KM = 5.0` vive
  em `censo_map.py:113` e **NÃO existe em `config.py`** (`grep` → ausente; travado pelo teste
  `test_raio_de_exibicao_nao_toca_o_raio_do_motor`, que assere `not hasattr(config, "RAIO_RESIDUAL_DISPLAY_KM")`).
- **Parâmetros canônicos §3 preservados:** `H3_RESOLUTION=7` apenas LIDO (literal `7` comentado como
  tal nos 3 pontos novos); `DIST_MIN_ULTRA_KM=1.0`, `RENDA_MIN=4500.0`, `renda=0.40`/`pop=0.60`
  intocados (`config.py` fora do diff).
- **Fidelidade ao gate humano:**
  - **S1 = A — CUMPRIDO.** `layers.get("score")` continua nas 4 células do grid 2x2 nas DUAS
    variantes (`censo_report.py:563` e `:1761`) **E** `layers.get("socioeconomia")` aparece no slide
    novo (`:528` e `:1736`). `_mapas_calor_page` não perdeu camada — o diff só acrescentou comentário
    ao redor dela; a prova dura é a byte-identidade sha256 do PNG `score` (V7).
  - **S2 = B — CUMPRIDO, número a número.** `OFERTA_DISPONIVEL_ALUNOS_BANDS` tem **exatamente 6**
    faixas com os cortes `0.0 / 1_250.0 / 2_500.0 / 5_000.0 / 10_000.0 / inf` e as cores aprovadas
    `(150,156,170)`, `(247,244,139)`, `(255,255,0)`, `(255,210,28)`, `(168,255,168)`, `(20,170,80)`,
    todas alpha 150 — batem 1:1 com a tabela do gate. Labels ASCII: `sem residual`, `1-1.250`,
    `1.251-2.500`, `2.501-5.000`, `5.001-10.000`, `> 10.000`. O 3º corte é **2.500** (não 2.000),
    como decidido.
  - **DT-4 (ordinal 0) — CUMPRIDO e travado por teste com spy:** novo=MAGENTA → Mapas de
    calor=turquesa → Concorrentes=magenta → Perfil=turquesa → Big Numbers=magenta. As 4 páginas
    existentes mantêm a cor de hoje; `_tema_bicolor` teve só a docstring alterada. O contrato do
    ordinal `-1` para o BLK-RELPON-11 está asserido.
- **Acentuação (§2):** identificadores novos sem acento (`"socioeconomia"`, `"residual"`,
  `OFERTA_DISPONIVEL_ALUNOS_BANDS`, `RAIO_RESIDUAL_DISPLAY_KM`, `_RESIDUAL_GRID_DISK_K`); texto do
  PNG 100% ASCII (V8, exceção de RENDER já precedida e testada); texto do PDF latin-1-safe.
- **DEC-004/DEC-011 (rede):** nenhum provedor novo. `_render_camada_residual_hex` reusa
  `_fetch_basemap` só com `bounds` diferente; import lazy de `contextily` e `basemap=False` como
  default seguro em teste, inalterados. Import de `h3` **lazy** dentro das funções, como planejado —
  `censo_map.py` não ganhou import de topo de `h3`.
- **Escopo respeitado:** SIM. 16 arquivos no diff, todos previstos pelo Planner (5 de `src/`, 7 de
  `tests/`, `CLAUDE.md` §4 e 3 snapshots de handoff). **Nada arrastado**: `tasks/backlog.md` e
  `tasks/completed.md` intocados (correto — housekeeping é passo de fechamento),
  `test_relatorio_municipal.py` intocado, `censo_point.py`/`config.py`/`competitors` intocados,
  nenhum PR aberto, working tree limpo em `791035d`.
- **Housekeeping de concluídos:** **N/A neste gate.** A estratégia aprovada por Vinicius é
  **PR ÚNICO ao fim dos 3 blocos (RELPON-09/10/11)**; o ciclo termina em commit no branch, e o
  `completed.md`/stub do backlog entram no fechamento em lote. Não é modo merge-humano nem
  auto-merge de bloco isolado.

## Decisão recomendada
**Fechar o ciclo técnico e seguir para o GATE VISUAL de Vinicius.** Levar ao gate, explicitamente:
(a) a decisão sobre os **pins no mapa de 5 km** (Paulista/SP é o caso extremo — manter ou aplicar o
rollback de 1 linha); (b) o branco vertical do slide na variante recente. Depois do gate:
`/registrar-decisao` com a **emenda à DEC-011** (S3) — sem ela o bloco reverte o BLK-CENSO-02 sem
registro. Não abrir PR.
