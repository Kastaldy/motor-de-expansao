# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-UI-01 — Refatoração UX/UI (recorte: sidebar mais aparente, spinners de carregamento, limpeza visual). Esteira Alta com GATE HUMANO: decisões D1..D9 aprovadas por Felipe/usuário em 2026-06-12.

## O que foi feito
Mudanças puramente visuais (CSS/markdown/spinner/strings), READ-ONLY sobre o M1, ancoradas por TEXTO. Decisões executadas exatamente conforme o gate:

### Bloco A — Sidebar mais aparente
- **A1 (D1=A)** `streamlit_app.py`: `initial_sidebar_state="collapsed"` → `"expanded"` em `st.set_page_config`.
- **A2 (D2=B)** `pages.py inject_styles()` bloco `[data-testid="stSidebar"]`: borda lateral reforçada `border-right: 2px solid rgba(25, 183, 255, 0.45)` + `box-shadow: 4px 0 24px rgba(0, 0, 0, 0.35)`. Sem `!important`; especificidade por `data-testid` (padrão do arquivo).
- **A3 (D3=B)** `pages.py render_uf_selectbox()`: cabeçalho `### Filtros globais` substituído por bloco HTML estilizado (`st.sidebar.markdown(..., unsafe_allow_html=True)`) com título "Filtros globais" + sublinha "Recorte executivo M1 + Hibrido" (barra lateral ciano `COLORS["brand_alt"]`), MANTENDO o caption explicativo logo abaixo. INVARIANTE preservada: `st.sidebar.selectbox("UF", …)` permanece o PRIMEIRO widget de input da sidebar (gate da carga lazy); o cabeçalho é markdown decorativo ANTES do selectbox.

### Bloco B — Spinners de carregamento (decorativos)
- **B1 (D4)** `streamlit_app.py`: `df = load_uf_slice(selected_uf)` envolto em `with st.spinner("Carregando dados da UF..."):` DENTRO do `try` existente (o `except (FileNotFoundError, ValueError)` segue capturando).
- **B2 (D4)** `pages.py`: atribuição `deck, n_points = build_unified_map_figure(...)` envolta em `with st.spinner("Construindo mapa..."):`. O `if deck is None:` e o render permanecem FORA do `with`.
- **B3 (D4)** `pages.py`: chamada `render_mapas_censitarios_combinados(...)` envolta em `with st.spinner("Gerando mapas censitarios..."):`.
- Spinners são context managers decorativos: corpo de `load_uf_slice`, `build_unified_map_figure` e `render_mapas_censitarios_combinados` INTOCADO. Sem efeito em cache, carga lazy (Bloco 4), render lazy (Bloco 5) ou mapa enxuto (Bloco 6).

### Bloco C — Limpeza de poluição visual
- **C1 (D5=A)** `streamlit_app.py`: removido o 2º `st.caption()` ("Base oficial preservada: …"). 1º caption "Recorte atual: …" PRESERVADO intacto. Proveniência segue coberta por `render_manifest_footer` no rodapé.
- **C2 (D6=A)** `pages.py render_header()`: 5 pills → 3 pills SEM nomes internos de coluna: "Onde expandir (M1)", "Qual bairro (Censitario)", "Fila operacional (Hibrido)". Removidas a pill da lista de UFs e as pills com nome de coluna (`score_priorizacao`/`score_setor_2022_calibrado`/`score_expansao_hibrido`).
- **C3 (D7=A)** `pages.py` (estado vazio da Análise Pontual): removido o `st.caption()` de limitações do pydeck ("Clique em objeto de mapa: … Botao direito: …"). O `st.info(...)` acima segue orientando. Caption de centroide H3 (estado COM ponto) NÃO tocado — guardrail de produto PRESERVADO.
- **C4 (D8=A)** `pages.py render_modelo_hibrido_v2()`: dois `st.caption()` consolidados em UM: "M1 decide o municipio; o Censitario e leitura local e o Hibrido organiza a fila operacional."
- **D9=A**: legendas/expander fora de escopo — nada feito (conforme aprovado).

### Bloco D — Testes
- `tests/integration/test_streamlit_app.py`: adicionado `mock.patch("streamlit.spinner")` ao `test_main_renderiza_apenas_a_aba_ativa` (simetria com os demais mocks de UI). Confirmado por execução da suíte alvo que NENHUM teste asseria texto de caption/pill removido (nada mais a atualizar).

## Arquivos alterados
- `streamlit_app.py` — A1 (sidebar expanded), B1 (spinner load_uf_slice), C1 (remoção do 2º caption de proveniência).
- `src/motor_expansao/dashboard/pages.py` — A2 (CSS sidebar), A3 (cabeçalho sidebar), B2/B3 (spinners mapa/censo), C2 (pills), C3 (caption pydeck vazio), C4 (consolidação captions híbrido).
- `tests/integration/test_streamlit_app.py` — D-test (mock de `streamlit.spinner`).

## Validações executadas
- pytest (alvo): `python -m pytest -q tests/integration/test_streamlit_app.py` → **182 passed, 0 failed, 0 skipped** (9101 warnings — apenas DeprecationWarnings de pytest_asyncio, ambiente Python 3.14). Rodado SERIAL: `-n auto` (xdist) faz INTERNALERROR de gateway no ambiente local Python 3.14 (problema de ambiente, não do código); a execução serial é equivalente e verde.
- Por que esse subconjunto: todas as mudanças são de UI no `streamlit_app.py`/`pages.py`/teste de integração; `test_streamlit_app.py` é a suíte que exercita `main()`, header, sidebar, mapa territorial e relatório censitário — cobre os pontos tocados. Suíte full é gate do QA.
- import: `python -c "import streamlit_app"` → **import ok**.
- ruff: `ruff check streamlit_app.py pages.py test_streamlit_app.py` → **All checks passed!**
- mypy: `mypy streamlit_app.py pages.py` → **Success: no issues found in 2 source files**.

## Problemas encontrados
- `pytest -n auto` (xdist) dispara INTERNALERROR de gateway (`OSError: connection already closed`) no ambiente local Python 3.14 — é problema de AMBIENTE (xdist/execnet × Python 3.14), não regressão do código. Contornado rodando SERIAL; resultado verde. QA deve estar ciente ao rodar a full.

## Pendências
- nenhuma.

## Riscos remanescentes
- Sidebar expandida por padrão (D1=A) consome espaço horizontal em telas estreitas — trade-off de produto aceito no gate (usuário pode colapsar manualmente).
- Nenhum risco funcional: mudanças são CSS/markdown/spinner/strings.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (nenhuma escrita em score/pesos).
- Artefatos M1 preservados: **sim** (`score_priorizacao`, `hex_score_estrutural`, `score_setor_2022_calibrado`, `score_expansao_hibrido`, `score_oportunidade_residual`, carteira, plano, SAM, artefatos oficiais — ZERO alteração).
- Dashboard offline mantido: **sim** (spinners não fazem fetch; nenhuma dependência de API ao vivo introduzida; DEC-004 do basemap inalterada).
- Contratos de performance intactos: **sim** — `load_uf_slice`/`read_enriched_uf_partition`/`load_uf_catalog` (Bloco 4), `render_tab_selector`/`st.segmented_control` (Bloco 5), `_downsample_map_index`/`MAP_SOURCE_COLUMNS_*` (Bloco 6) com corpo INTOCADO; spinners só por fora.
- `render_uf_selectbox` segue o PRIMEIRO widget/gate da sidebar: **sim**.

## Paths pré-sujos (NÃO commitados pelo Builder — alheios ao ciclo)
- data/outputs/setores_censitarios_2022_geo/_metadata.json
- data/reports/relatorio_pontual_censitario_base_geo.md
