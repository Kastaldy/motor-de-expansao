# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-ORQ-01 — Otimização de tempo de execução do /run-cycle (Fase 1 / Tier 1) + tiering de modelo por agente (expansão v2, instrução humana de Felipe em 2026-06-02)

## O que foi feito
Executados os 9 passos do plano v2 do Planner (escopo único autorizado), com mudanças MÍNIMAS e rastreáveis, preservando a pureza de line-endings de cada arquivo (3 PURE-CRLF, 3 PURE-LF) e toda a maquinaria NO-BYPASS/handoff-versionado/commit-por-path/rollback/housekeeping.

1. `.claude/commands/run-cycle.md` Passo 4 (item 3 de "Como construir o prompt"): trocado "incluir conteúdo dos arquivos" por "passar a LISTA DE CAMINHOS (o sub-agente lê via Read)". Item 1, item 2, subseção "Contexto isolado por Skill" e bloco "Handoff versionado" INTACTOS.
2. `prompts/block_orchestrator.md` "Leitura obrigatória": acrescentado item 5 explicitando leitura pelos CAMINHOS recebidos do orquestrador (via Read). Objetivo/guardrails/saída inalterados.
3. `prompts/planner.md` "Leitura obrigatória" item 4: reforçado "ler pelos CAMINHOS listados no handoff (via Read)". Demais inalterado.
4. `prompts/builder.md`: (a) "Leitura obrigatória" item 4 → leitura pelos CAMINHOS via Read; (b) "Validação obrigatória" → validação por SUBCONJUNTO impactado + smoke `import streamlit_app`, com `pytest -n auto`; MANTIDOS o smoke obrigatório e o registro do resultado completo; full é responsabilidade do QA.
5. `prompts/qa_analyzer.md`: (a) "Leitura obrigatória" item 4 → leitura pelos CAMINHOS via Read; (b) item (a) da "Re-execução obrigatória" ganhou a frase "suíte COMPLETA roda 1× aqui com `python -m pytest -n auto` ... contagem IDÊNTICA ao serial ... nunca mascarar com `-p no:xdist`"; (c) housekeeping item 3 `pytest -q` → `pytest -n auto`. PRESERVADOS integralmente: itens (b)/(c)/(d) (episódio dos 5 defeitos do BLK-OPS-01), proibições (`--config /dev/null`, fixture-fora-do-caminho-real, mock do caminho crítico), re-execução própria, `--check`.
6. `pyproject.toml`: adicionado `"pytest-xdist>=3.6.0,<4",` ao array `dev`, junto aos demais `pytest-*` (indentação de 4 espaços, vírgula, estilo de bound coerente).
7. Revisão de coerência + verificador de line-endings (passo 7.2): exit 0; todos os 6 arquivos mantêm a pureza original.
8. [NOVO v2] `.claude/commands/run-cycle.md` tiering: (8.1) inserida a subseção "### Seleção de modelo por agente (tiering de complexidade)" no Passo 4, DEPOIS de "Contexto isolado por Skill" e ANTES do `---` que fecha o Passo 4, com a TABELA (4 agentes × 4 colunas de criticidade), IDs dos modelos e as regras (QA sempre Opus 4.8; override ±1 com pisos QA-Opus/≥haiku; dry-run na coluna Baixa). (8.2) adicionado o bullet "**Tiering de modelo / QA sempre Opus 4.8:** ..." em "Guardrails permanentes do orquestrador", logo após o bullet "Branch/commit isolado por ciclo".
9. [NOVO v2] `prompts/qa_analyzer.md`: inserido o blockquote "> **Modelo obrigatório:** este agente roda SEMPRE em Opus 4.8 (`claude-opus-4-8`) ..." logo após a linha 3 (intro).

## Arquivos alterados
- `.claude/commands/run-cycle.md` — Passo 4 item 3: orquestrador passa a LISTA DE CAMINHOS (não conteúdo); NOVA subseção "Seleção de modelo por agente (tiering de complexidade)" com tabela + regras; NOVO bullet de guardrail "Tiering de modelo / QA sempre Opus 4.8". PURE-CRLF preservado.
- `prompts/block_orchestrator.md` — "Leitura obrigatória": novo item 5 (leitura pelos CAMINHOS via Read). PURE-LF preservado.
- `prompts/planner.md` — "Leitura obrigatória" item 4 reforçado (CAMINHOS via Read). PURE-LF preservado.
- `prompts/builder.md` — "Leitura obrigatória" item 4 (CAMINHOS via Read) + "Validação obrigatória" por SUBCONJUNTO impactado com `pytest -n auto`, smoke `import streamlit_app` mantido, full delegada ao QA. PURE-LF preservado.
- `prompts/qa_analyzer.md` — "Leitura obrigatória" item 4 (CAMINHOS via Read); suíte COMPLETA 1× com `pytest -n auto` (item (a)); housekeeping item 3 → `pytest -n auto`; blockquote de modelo Opus 4.8 obrigatório após a intro. NO-BYPASS integralmente preservado. PURE-CRLF preservado.
- `pyproject.toml` — array `dev`: `"pytest-xdist>=3.6.0,<4",` adicionado. PURE-CRLF preservado.
- `context/handoff.md` (corrente) + `context/handoff/20260602-152357-builder.md` (snapshot append-only, segundos).
- `tasks/current_task.md` — status atualizado para "aguardando QA".

## Validações executadas
- `pip install -e ".[dev]"`: SUCESSO (EXIT=0). Instalou `pytest-xdist-3.8.0` + `execnet-2.1.2` (entre outros). Confirma a dep nova do `pyproject.toml`.
- `python -m pytest -n auto -q tests/integration/test_streamlit_app.py`: **171 passed, 0 failed, 0 skipped** (EXIT=0). `-n auto` reconhecido ("bringing up nodes..."). Subconjunto escolhido por ser o smoke de integração do dashboard, mandatório em todo ciclo, e por exercitar o caminho mais sensível (carga/dashboard) deste ciclo de orquestração/prompts. As 14708 warnings são DeprecationWarnings de `pytest-asyncio`/Python 3.14 (pré-existentes, fora de escopo).
- `python -c "import streamlit_app; print('import ok')"`: **import ok** (EXIT=0). Warnings de bare-mode Streamlit esperados.
- Verificador de line-endings (passo 7.2): **EXIT=0** — `run-cycle.md`/`qa_analyzer.md`/`pyproject.toml` = CRLF; `builder.md`/`planner.md`/`block_orchestrator.md` = LF. Nenhum MIXED.
- 3 greps de tiering: OK — `run-cycle.md` contém "Seleção de modelo por agente" (linha 128) e o bullet "Tiering de modelo / QA sempre Opus 4.8" (linha 232); `qa_analyzer.md` contém `claude-opus-4-8` (linha 5).
- Suíte COMPLETA (serial vs `-n auto` com contagem idêntica) NÃO foi rodada aqui — é o gate único do QA por design (validação do Builder é por subconjunto).

## Problemas encontrados
nenhum

## Pendências
- Gate do QA: rodar a suíte COMPLETA 1× serial (`python -m pytest -q`) e 1× `python -m pytest -n auto -q`, confirmando contagem IDÊNTICA (sem flakiness por paralelismo); re-executar por conta própria todas as "Validações obrigatórias" e colar a saída literal; confirmar `--check` do housekeeping no fechamento.

## Riscos remanescentes
- Assimetria local(paralelo)/CI(serial): CI (`.github/workflows/ci.yml`) permanece `python -m pytest -q` serial e NÃO foi tocado (fora de escopo) — documentado.
- `pytest -n auto` ≠ contagem serial (flakiness por paralelismo): a confirmar no QA; flakiness = teste mal-isolado a REPORTAR, nunca mascarar com `-p no:xdist`.
- Tiering é INSTRUÇÃO (sem teste de runtime para escolha de modelo); a demonstração FUNCIONAL ocorre no dry-run autônomo do Passo 6.c (BO=haiku, Builder=sonnet na tarefa dummy Baixa, sem QA → sem Opus).

## Guardrails verificados
- score_priorizacao não alterado: não aplicável (nenhuma mudança em M1/score/artefatos)
- Artefatos M1 preservados: não aplicável (nada de M1 tocado)
- Dashboard offline mantido: sim (nenhuma dependência de API ao vivo introduzida; só prompts/orquestração/pyproject)
- NO-BYPASS / handoff versionado / commit por path / rollback / housekeeping: preservados integralmente
- Line-endings: pureza original de cada arquivo preservada (passo 7.2 exit 0)
- NÃO commitei, NÃO rodei housekeeping, NÃO fiz merge (são do fechamento, pelo orquestrador)
