# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-RELPON-01 — Três mapas de calor (população/renda/score) num único slide do Relatório Pontual

## O que foi feito
Consolidação dos 3 choropleths censitários (Densidade/População, Renda, Score) — antes 1 slide cada (páginas 2–4 do PDF de 7 páginas) — em UM único slide "Mapas de calor" (tira 1×3 lado a lado), nas duas variantes (`censitario` e `classico`), levando o PDF de 7→5 páginas. Plano aprovado por Vinicius em 2026-07-01 (D1–D5 conforme recomendado). READ-ONLY sobre o M1.

Parte A (`censo_report.py`):
- `PDF_SECTION_HEADERS`: de 7 → **5** strings: `("Relatorio Pontual Censitario", "Mapas de calor", "Concorrentes", "Big Numbers", "Realizacao")`. Comentário-cabeçalho reescrito para 5 páginas.
- Novos helpers do template recente: `_map_grid_cells(top,bottom,margin_x,gap)` (geometria PURA das 3 células, testável sem PDF), `_draw_maps_grid(pdf, pngs, *, top, bottom, margin_x, gap)` (desenha os 3 PNGs `[densidade,renda,score]` numa tira 1×3, cada um escalado por `min(w,h)` e centralizado na sua célula, embutidos SEPARADAMENTE; fallback textual "Mapa indisponivel para esta camada." por camada `None`; retorna os bounding boxes) e `_mapas_calor_page(pdf, layers, assets, *, primary)` (`top=56`, `bottom=_PAGE_H-30`, `margin_x=20`, `gap=12`).
- Novos helpers do template clássico: `_classico_draw_maps_grid(pdf, pngs)` (delega a `_draw_maps_grid` com `top=_CLASSICO_MAPS_TOP` ~122, `margin_x=_CLASSICO_MARGIN`) e `_classico_mapas_calor_page(pdf, layers, assets, *, banda_texto, primary)`. Constante nova `_CLASSICO_MAPS_TOP`. Constante `_MAPA_INDISPONIVEL` extraída (fonte única do literal de fallback).
- Reescrita das 2 orquestrações: `gerar_pdf_relatorio_pontual_censitario` e `gerar_pdf_relatorio_pontual_classico` — os 5 cálculos de tema (`p1..p5/s4/s5`) viraram 3 (`p1`, `p2/s2`, `p3/s3`); as 3 chamadas de página de mapa viraram 1 ao novo montador; temas reatribuídos por ordinal (Mapas de calor=1 turquesa, Concorrentes=2 magenta, Big Numbers=3 turquesa). Docstrings 7→5 páginas.
- Helpers órfãos REMOVIDOS (confirmado por grep: sem chamador e sem import em teste — os testes só importam os 6 símbolos públicos): `_map_page`, `_classico_map_page`, `_draw_map`, `_classico_draw_map`. `_normalize_mapas`, `_normalize_mapas_by_key` e `MAP_LAYER_TITLES` PRESERVADOS (ainda usados pela normalização de chaves).

Parte B (`tests/unit/test_relatorio_pontual_censitario_export.py`):
- Todos os `b"/Count 7"` → `b"/Count 5"` (7 ocorrências, censitario + classico).
- `_count_layer_titles` REMOVIDO (os 3 títulos de página deixaram de ser bytes raw — viraram pixels dentro dos PNGs). Call sites migrados para `pdf_bytes.count(b"/Subtype /Image") >= 4`.
- `test_pdf_embute_tres_camadas` → renomeado `test_pdf_embute_tres_choropleths_no_slide_unico` (agora conta imagens).
- Watermark: `>= 7` → `>= 5` por página (censitario e classico) + docstring 7→5.
- Retrocompat de bytes único: `b"Populacao - Densidade"` (título de página, extinto) → `b"Mapas de calor"` + `b"Mapa indisponivel para esta camada."` + `/Count 5`.
- `test_classico_gera_7_paginas_e_secoes` → renomeado `test_classico_gera_5_paginas_e_secoes`.
- 4 TESTES NOVOS adicionados: `test_slide_unico_tres_mapas_sem_sobreposicao` (bounding boxes das 3 células sem interseção e contidas na área de conteúdo, recente + clássico), `test_slide_unico_count_5_e_titulo_mapas_de_calor`, `test_slide_unico_offline_safe_por_camada` (renda+score ausentes → fallback, sem exceção, `/Count 5`), `test_slide_unico_tres_imagens_embutidas` (`/Subtype /Image >= 4`, 3 separadas + pins).

Parte C (docs):
- `docs/relatorio_pontual_censitario.md`: estrutura de 7→5 páginas, `/Count 7`→`/Count 5`, novo parágrafo BLK-RELPON-01, seção de validação mínima atualizada (5 headers, `/Subtype /Image >= 4`, testes novos).
- `CLAUDE.md` §4 (parágrafo "Motor/UI censitario"): "em 7 paginas (...)" → "em 5 paginas (Capa -> Mapas de calor -> Concorrentes -> Big Numbers -> Realizacao)" mencionando BLK-RELPON-01 e os helpers substituídos; parentético BLK-EST-02 ajustado (contagem/ordem/`/Count`/`PDF_SECTION_HEADERS` agora atualizados pelo BLK-RELPON-01). Só texto; parâmetros canônicos §3 INTOCADOS.

## Arquivos alterados
- `src/motor_expansao/dashboard/censo_report.py` — `PDF_SECTION_HEADERS` 7→5; novos helpers `_map_grid_cells`/`_draw_maps_grid`/`_mapas_calor_page`/`_classico_draw_maps_grid`/`_classico_mapas_calor_page` + `_CLASSICO_MAPS_TOP`/`_MAPA_INDISPONIVEL`; 2 orquestrações reescritas (3 temas, 1 slide de mapas); remoção de `_map_page`/`_classico_map_page`/`_draw_map`/`_classico_draw_map`; docstrings 7→5.
- `tests/unit/test_relatorio_pontual_censitario_export.py` — `/Count 5`, watermark `>= 5`, remoção de `_count_layer_titles`, migração para contagem de imagens, retrocompat ajustado, renomeações + 4 testes novos.
- `docs/relatorio_pontual_censitario.md` — estrutura 5 páginas + `/Count 5` + parágrafo BLK-RELPON-01.
- `CLAUDE.md` §4 — parágrafo "Motor/UI censitario" 7→5 páginas (texto).
- `tasks/current_task.md`, `context/handoff.md`, `context/handoff/20260701-115000-builder.md` — governança.

## Validações executadas
- ruff (`src/motor_expansao/dashboard/censo_report.py` + teste): **All checks passed!** (2 achados auto-corrigidos: `B905` zip→`strict=False`; `I001` import sort no teste).
- mypy (`src/motor_expansao/dashboard/censo_report.py`): **Success: no issues found in 1 source file.**
- pytest (subconjunto, serial `-p no:xdist` por crash de ambiente do xdist/execnet no Python 3.14 local — NÃO é defeito de teste): `tests/unit/test_relatorio_pontual_censitario_export.py` + `tests/unit/test_relatorio_municipal.py` + `tests/integration/test_streamlit_app.py` = **265 passed, 0 failed, 0 skipped** (13832 warnings = DeprecationWarnings pré-existentes de asyncio/pytest-asyncio no Python 3.14).
- pytest (regressão render de mapa, não tocado): `tests/unit/test_relatorio_pontual_censitario_mapa.py` = **14 passed, 0 failed**.
- import: `python -c "import streamlit_app"` → **import ok** (só WARNINGs de cache sem runtime Streamlit, esperado fora do server).

## Problemas encontrados
- `pytest -n auto` (xdist) aborta com `INTERNALERROR ... OSError: connection already closed` no worker `gw0` — falha de ambiente do execnet/xdist no Python 3.14 local, NÃO um defeito de teste. Contornado rodando serial (`-p no:xdist`). QA roda a suíte full como gate único; se o `-n auto` continuar instável no ambiente do QA, usar serial (é o mesmo conjunto de testes).

## Pendências
- **Gate visual humano (parte do critério de aceite do bloco):** suíte verde não basta — o layout das 3 colunas em 960×540 com legenda embutida à direita de cada PNG pode ficar pequeno; exige inspeção do PDF gerado (legibilidade + tom bicolor dos slides seguintes). Não é bloqueio de QA de testes, mas é requisito de fechamento do bloco (Alta).
- Fechamento de `tasks/completed.md` (governança de ciclo) fica para o passo pós-QA.

## Riscos remanescentes
- Legibilidade das 3 colunas (risco central declarado pelo Planner) — mitigado por `min(w,h)` sem distorção, mas só validável visualmente.
- `zip(..., strict=False)`: `pngs` e `cells` são sempre 3×3 nas chamadas atuais; `strict=False` apenas trunca ao menor caso um chamador futuro passe lista menor (defensivo, sem risco atual).

## Guardrails verificados
- score_priorizacao não alterado: sim (não aplicável — camada de visualização/relatório, zero toque em score/pesos/carteira/plano).
- Artefatos M1 preservados: sim (nenhum artefato oficial tocado; método `setor_censitario_intersecao_area_1p5km` e raio 1,5 km INTOCADOS).
- Dashboard offline mantido: sim (nenhuma dependência de rede nova; DEC-004/DEC-005 inalteradas; `censo_map.py`, `api/service.py`, `pages.py` INTOCADOS — assinaturas públicas dos geradores idênticas).
- `%PDF-1.4`, `set_compression(False)`, marca d'água em todas as 5 páginas (`>= 5`), atribuição de tiles no rodapé: mantidos.
