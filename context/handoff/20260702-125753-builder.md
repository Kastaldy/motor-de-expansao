# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-RELMUN-04 — Relatório Municipal em lote (gerar um relatório por município quando vários
selecionados)

## O que foi feito
Estendi os DOIS pontos de geração do Relatório Municipal em `pages.py`
(`render_relatorio_municipal_download_topo` e `render_relatorio_municipal_expander`) para suportar
seleção de vários municípios, preservando o fluxo de 1 município BYTE-A-BYTE. Seguido o plano do
Planner (D1–D4) à risca:

- **2 helpers privados novos** ANTES de `render_relatorio_municipal_download_topo`:
  - `_relmun_key_slug(text)` — slug minimalista para `key`s de widget únicos (sem novo import).
  - `_gerar_payload_relatorio_municipal(df, nome_municipio, uf, *, competitors_df, ultra_df,
    dominio_df, censo_geo_dir)` — reproduz na MESMA ordem e com os MESMOS kwargs as chamadas do
    ramo `== 1` (`_resolve_bairros_por_hex_municipio` → `agregar_municipio` → filtro `df_muni`
    (nome_municipio/fallback cidade) → `render_mapas_municipio(basemap=True)` →
    `gerar_payloads_download_relatorio_municipal`). Retorna `None` quando `n_hex_total == 0`.
    NÃO chama `st.*` (unidade testável).
- **`render_relatorio_municipal_download_topo`**: gate trocado de `if len != 1: return` por
  ramificação `not selected_cities` (return silencioso) / `== 1` (bloco ATUAL literal, INTOCADO:
  mesmos keys `btn_gerar_relmun_topo`/`dl_relmun_topo`, mesma cache key `relmun_topo_payload::<nome>`,
  mesmos textos/spinner/warning) / `> 1` (novo ramo de lote): botão único `Gerar Relatorios (N)`
  (key `btn_gerar_relmun_lote_topo`), `st.progress` i/N (D4), loop chamando o helper, warning
  individual por município sem hexágono (`continue`, não aborta), cache em
  `session_state["relmun_lote_topo_payloads"]` (dict `f"{uf or ''}::{nome}"` → payload), e 1
  `st.download_button` por município presente na seleção atual E no cache, label `"Baixar PDF — <nome>"`,
  key `dl_relmun_lote_topo_{i}_{slug(uf)}_{slug(nome)}`.
- **`render_relatorio_municipal_expander`**: ramificação `== 0` (`st.info` com o texto atualizado
  do D1: "Selecione um ou mais municipios...") / `== 1` (bloco ATUAL literal INTOCADO: spinner →
  agregação → `st.columns(3)` + 3 `st.metric` → `render_download_relatorio_municipal`) / `> 1`
  (ramo de lote análogo ao topo, com keys `btn_gerar_relmun_lote_expander`,
  `session_state["relmun_lote_expander_payloads"]`, `dl_relmun_lote_expander_{i}_...`). A
  assimetria auto-gera (1 muni) vs botão (>1) foi mantida conforme D1.
- Docstrings das 2 funções atualizadas para descrever o comportamento multi-município.

Nenhum novo import (usei `Any`/`Path`/`pd`, já importados no topo do arquivo). ZERO alteração em
`relatorio_municipal.py`, no critério de hexágono destacado (DEC-011), no pipeline de mercado
(`flag_sam`), no Relatório Pontual ou na estrutura/páginas do PDF. READ-ONLY sobre o M1.

## Arquivos alterados
- `src/motor_expansao/dashboard/pages.py` — 2 helpers privados novos (`_relmun_key_slug`,
  `_gerar_payload_relatorio_municipal`) + ramificação `0 / ==1 / >1` nos 2 pontos de geração
  (ramo `== 1` preservado literal em ambos; único ajuste de texto no `st.info` do expander).
- `tests/integration/test_streamlit_app.py` — 6 testes novos (`test_relmun_lote_helper_gera_payload_e_none`,
  `test_render_relatorio_municipal_topo_lote_botao_e_downloads`,
  `test_render_relatorio_municipal_topo_lote_municipio_sem_hex_nao_aborta`,
  `test_render_relatorio_municipal_topo_gate_zero_e_um`,
  `test_render_relatorio_municipal_expander_lote_downloads`,
  `test_render_relatorio_municipal_expander_um_municipio_auto_gera`) + ajuste do teste de gate
  existente (`..._sem_municipio_unico_nao_renderiza` migrado de `["A","B"]`→`[]`, pois `> 1` agora
  renderiza botão). Os 2 testes existentes de 1 município permanecem verdes SEM alterar asserções.
- `tasks/current_task.md` — status → "aguardando QA".

## Validações executadas
- pytest (subconjunto impactado): `python -m pytest -q tests/integration/test_streamlit_app.py`
  → **219 passed, 0 failed, 0 skipped** (17,04s). Todos os 6 testes novos verdes.
  - Observação: `python -m pytest -n auto -q ...` (xdist) ABORTOU com `INTERNALERROR ...
    execnet ... OSError: cannot send (already closed?)` — mesmo bug de ambiente do Python 3.14
    local do ciclo anterior; rodei SERIAL conforme instrução.
- import: `python -c "import streamlit_app; print('import ok')"` → **ok** (só warnings de
  cache do Streamlit fora de runtime, esperados).
- ruff: `python -m ruff check src/motor_expansao/dashboard/pages.py tests/integration/test_streamlit_app.py`
  → **All checks passed!**
- mypy: `python -m mypy src/motor_expansao/dashboard/pages.py`
  → **Success: no issues found in 1 source file** (sem erros novos).
- NÃO rodei a suíte completa (gate do QA).

## Problemas encontrados
Nenhum de código. Nota: `render_relatorio_municipal_expander` NÃO é re-exportada em `streamlit_app`
(só `render_relatorio_municipal_download_topo` está na lista de import de `streamlit_app.py`); por
isso os 2 testes novos do expander a importam diretamente de `motor_expansao.dashboard.pages`
(padrão já usado por outros testes do arquivo).

## Pendências
- Revisão VISUAL humana da UI nova (botão "Gerar Relatorios (N)" + downloads por município nos 2
  pontos) — critério de aceite do backlog; o QA deve sinalizar esse passo antes do fechamento.

## Riscos remanescentes
- Custo de gerar N PDFs é sequencial, sob botão explícito, com progresso i/N (D4) — sem
  paralelização de rede; tiles/fallback offline (DEC-011) já internos a
  `render_mapas_municipio(basemap=True)`.
- Homônimos entre UFs: mitigado por `key` com índice + UF + slug do nome.

## Guardrails verificados
- score_priorizacao não alterado: sim (não aplicável — camada de visualização/relatório)
- Artefatos M1 preservados: sim (não aplicável — zero escrita em artefatos oficiais)
- Dashboard offline mantido: sim (nenhuma dependência de rede nova; só reusa tiles já aprovados
  DEC-011 via `render_mapas_municipio(basemap=True)`)
