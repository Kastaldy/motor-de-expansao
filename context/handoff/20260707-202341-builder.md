# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-VIAB-01 — Validação/limpeza da base de imóveis candidatos

## O que foi feito

Criados 2 arquivos novos conforme o plano técnico do Planner:

1. `src/motor_expansao/dimensionamento/imoveis_candidatos.py` — módulo com 4 funções puras:
   - `ler_imoveis_xlsx(path)`: lê aba "Imóveis" com `header=2`, valida COLUNAS_ESPERADAS (19 colunas)
   - `validar_e_limpar(df)`: aplica 3 filtros sequenciais (área < 500 → placeholder → aluguel fora de range), adiciona `flag_sem_coord`, retorna `(df_limpo, resumo)`
   - `materializar(df_limpo, resumo, staging_dir, analysis_dir)`: grava parquet + .md, retorna `(path_parquet, path_relatorio)`
   - `run(xlsx_path, ...)`: orquestra as 3 acima; ponto de entrada CLI

2. `tests/unit/dimensionamento/test_imoveis_candidatos.py` — 13 testes com fixture sintética em memória (sem tocar o xlsx real):
   - Fixture de 7 linhas cobrindo: SOBREVIVE (IDs 1, 6), DESCARTA por área (IDs 2, 3), DESCARTA por placeholder (ID 4), DESCARTA por aluguel fora de range (IDs 5, 7)
   - Todos os 13 testes passaram (0 falhas, 0 skipped)

Materialização executada contra o xlsx real:
- `data/staging/imoveis_candidatos_limpos.parquet`: 23 linhas (28 entrada - 4 área - 1 placeholder - 0 fora de range), todos com `flag_sem_coord=True` (snapshot atual sem coordenadas)
- `data/analysis/imoveis_qualidade.md`: relatório de qualidade com tabela de contagens por regra

## Arquivos alterados

- `src/motor_expansao/dimensionamento/imoveis_candidatos.py` (NOVO — módulo principal)
- `tests/unit/dimensionamento/test_imoveis_candidatos.py` (NOVO — 13 testes unitários)
- `data/staging/imoveis_candidatos_limpos.parquet` (gerado, gitignored, NÃO commitado)
- `data/analysis/imoveis_qualidade.md` (gerado, gitignored, NÃO commitado)
- `context/handoff.md` (este arquivo)
- `context/handoff/20260707-202341-builder.md` (cópia versionada)
- `tasks/current_task.md` (status atualizado para aguardando QA)

**NÃO tocados:**
- `src/motor_expansao/dimensionamento/__init__.py` (intocado)
- `src/motor_expansao/dimensionamento/config.py` (intocado — diferença CRLF→LF é artefato pré-existente de checkout Windows, não alteração de conteúdo)
- `src/motor_expansao/config.py` (M1 — intocado)
- `pipelines/m1/` (intocado)
- Artefatos M1 oficiais (mtimes Jun 3 15:47 — não tocados)

## Validações executadas

- **ruff check** (imoveis_candidatos.py + test_imoveis_candidatos.py): 0 erros (1 auto-fixado: ordenação de imports I001)
- **mypy** (imoveis_candidatos.py --ignore-missing-imports): `Success: no issues found in 1 source file`
- **pytest** (subconjunto impactado + integration): `241 passed, 2 failed (pré-existentes, ver abaixo), 0 skipped`
  - Subconjunto: `tests/unit/dimensionamento/test_imoveis_candidatos.py` (13 testes) + `tests/integration/test_streamlit_app.py`
  - 13/13 testes do módulo novo: PASSARAM
  - 2 falhas em `test_streamlit_app.py`: `test_render_coord_search_plus_code_completo_offline` e `test_render_coord_search_plus_code_curto_usa_localidade_mock` — **confirmados como pré-existentes** (falham idênticos com stash sem meu trabalho; não são regressões deste bloco; são sobre Plus Code no geocoder — fora do escopo BLK-VIAB-01)
- **smoke import do módulo**: `ok`
- **smoke import streamlit_app**: `import ok`
- **materialização real**: executada sem exceção; parquet 23 linhas, md presente
- **mtime artefatos M1**: `brasil_estrutural.parquet` e `brasil_priorizados.parquet` — Jun 3 15:47 (INALTERADOS)

## Resultado do parquet real (snapshot 16/06/2026)

| Métrica | Valor |
|---|---|
| Total de entrada | 28 |
| Descartados (área < 500 m²) | 4 |
| Descartados (placeholder 11.111,11) | 1 |
| Descartados (aluguel fora de range) | 0 |
| Total limpo | 23 |
| Sem coordenada (flag_sem_coord=True) | 23 (100%) |
| STATUS: PROSPECÇÃO | 21 |
| STATUS: HISTÓRICO COMITÊ | 1 |
| STATUS: APROVADOS | 1 |

## Problemas encontrados

- 2 testes pré-existentes falham no `test_streamlit_app.py` (Plus Code). Confirmados como pré-existentes via stash. Não são regressões deste bloco.
- Diferença CRLF→LF em `config.py` do dimensionamento: artefato pré-existente de ambiente Windows, não alteração de conteúdo. O `loop_guard.py` não disparará pois o arquivo não foi commitado.

## Pendências

- Nenhuma deste bloco. Os 2 testes pré-existentes do Plus Code precisam de atenção em ciclo futuro.

## Riscos remanescentes

- Nenhum para este bloco.

## Guardrails verificados

- score_priorizacao não alterado: sim (READ-ONLY sobre M1)
- Artefatos M1 preservados: sim (mtimes Jun 3 15:47, inalterados)
- Dashboard offline mantido: sim (não aplicável — nenhuma mudança no dashboard)
- config.py (M1 e dimensionamento) não alterados: sim
- pipelines/m1/ não tocados: sim
- Sem chamadas HTTP / geocoding: sim
- Saída em staging/analysis (gitignored): sim — NÃO commitados
- loop_guard.py: não disparará (diff toca apenas os 2 arquivos novos de dimensionamento)
