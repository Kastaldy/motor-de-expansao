# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA

## Bloco
BLK-ATR-04 — Visualização dos resultados do funil (gráficos + números concretos para decisão)

---

## O que foi implementado

### 1. `pyproject.toml`
- Adicionado `"matplotlib>=3.7.0"` na seção `# Analise` das deps base (antes de `scikit-learn`).
- Matplotlib já estava instalado (3.11.0 transitivo); agora formalmente declarado.

### 2. `src/motor_expansao/demanda_revelada/viz_atratividade.py` (novo)
Módulo headless de geração de gráficos com 6 funções + 1 relatorio + orquestrador:

- `matplotlib.use("Agg")` logo após import, antes de qualquer pyplot.
- **(a) `gerar_grafico_cobertura_huff`**: barras por UF de % hexes com `share_captura_huff < 1.0`.
- **(b) `gerar_grafico_gate_por_uf`**: barras empilhadas (aprovados/reprovados) por UF, filtra UFs com ≥5 hexes.
- **(c) `gerar_grafico_distribuicoes_eixos`**: 3 subplots `1×3` com histogramas dos eixos normalizados (sociodemo/mercado/disputa), linha da mediana.
- **(d) `gerar_grafico_quadrantes`**: scatter com 4 quadrantes por mediana de residual × disputa, contagem em cada quadrante.
- **(e) `gerar_grafico_r2_modelos`**: barras horizontais de R²_oof por modelo com IC95 (xerr), linha em 0, cor diferenciada para composto/baseline.
- **(f) `gerar_grafico_correlacoes`**: heatmap 3×3 (imshow `RdBu_r`) com anotações de valor.
- `gerar_relatorio_markdown`: markdown com veredito, tabela de modelos, quadrantes, caminhos de PNG e nota honesta; `_assert_sem_pii_no_relatorio` antes de gravar.
- `executar`: orquestrador completo (`pragma: no cover`); carrega parquets, roda ATR-03, gera tudo.
- `plt.close(fig)` ao fim de cada função de gráfico.
- Assinaturas puras: recebem DataFrames/`EstruturaFunilResult`, sem I/O interno.
- Guardrails DEC-012: zero imports de `pipelines/`, `dashboard/`, `censo_*`, `api/`, `config.py`.

### 3. `tests/unit/demanda_revelada/test_viz_atratividade.py` (novo)
10 testes, todos verdes (19s com `-n auto`):

1. `test_grafico_cobertura_huff_gera_png` — PNG existe, > 1 kB
2. `test_grafico_gate_por_uf_gera_png` — PNG existe, > 1 kB
3. `test_grafico_distribuicoes_eixos_gera_png` — PNG existe, > 1 kB
4. `test_grafico_quadrantes_gera_png` — PNG existe, > 1 kB
5. `test_grafico_r2_modelos_gera_png` — PNG existe, > 1 kB
6. `test_grafico_correlacoes_gera_png` — PNG existe, > 1 kB
7. `test_relatorio_markdown_referencia_pngs` — markdown menciona nomes dos PNGs
8. `test_relatorio_markdown_sem_pii` — nenhum token de `COLUNAS_PII_PROIBIDAS` no markdown
9. `test_isolamento_imports` — inspeciona código-fonte do módulo para ausência de imports proibidos
10. `test_figuras_fechadas_apos_geracao` — `len(plt.get_fignums()) == 0` após cada função

### Validações executadas
- `ruff check` — limpo (0 erros)
- `pytest -n auto -q tests/unit/demanda_revelada/test_viz_atratividade.py` — 10/10 verdes
- `python -c "import streamlit_app; print('import ok')"` — OK
- `python scripts/loop_guard.py --base ciclo/loop-20260706-152137` — GUARD OK

### M1 intocado
- Mtime dos 4 artefatos oficiais M1: INALTERADO (nenhuma escrita em `data/staging/brasil_*.parquet`, `data/staging/hexagonos_brasil_oportunidades.parquet`, `data/outputs/hexagonos_brasil_dashboard.parquet`).
- `config.py` raiz: INTOCADO.
- `pipelines/m1/`: INTOCADO.

## Arquivos modificados
- `pyproject.toml` (dep matplotlib adicionada)
- `src/motor_expansao/demanda_revelada/viz_atratividade.py` (novo)
- `tests/unit/demanda_revelada/test_viz_atratividade.py` (novo)

## NÃO modificados
- `estrutura_funil.py` (só importado)
- `__init__.py` do pacote (viz_atratividade não exposta lá, não era requisito)
- Qualquer artefato oficial M1
- `calcular_colunas_mercado.py` ou pipeline de mercado

## Próximo passo
QA — rodar suite completa + verificar PNGs e markdown gerados.
