# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-REV-07 — Avaliação de fundação: Streamlit vs. alternativas (matriz de decisão)

## O que foi feito
Criado o relatório único `data/analysis/avaliacao_stack.md` (gitignored) consolidando os achados
dos REV-01..06 numa matriz de decisão **4 opções (a,b,c,d) × 7 critérios** + coluna de espectro,
partindo da topologia multi-container REAL de produção (`docker-compose.prod.yml`). Seguidas
EXATAMENTE as 8 seções do plano técnico do Planner (Passos 1–8), com cada número ancorado na
fonte REV correspondente. ZERO código de produção alterado — é READ-ONLY sobre o M1.

Conteúdo do relatório (8 seções):
1. Cabeçalho + metadados (bloco `>`: ID, data 2026-07-10, READ-ONLY, consolida REV-01..06,
   decisão final é do REV-12, fontes lidas).
2. Sumário executivo — premissa da topologia multi-container real (`api`/Caddy/volumes `:ro`
   já existem; custo de b/d = trocar o container streamlit, não rebuild do zero); "offline §2 =
   sem serviço externo ao vivo, não sem backend" (NÃO re-litigado); recomendação preliminar em 1 frase.
3. As 4 opções em 1 frase cada (a,b,c,d — exatamente como o BO; nada adicionado/removido).
4. **Matriz de decisão** (núcleo): 4 linhas × 7 critérios + coluna de espectro, células +/0/− com
   âncoras numéricas (payload 21–24 MB, tooltip 65%, `to_json` 0,6–1,5 s, rerun 700–900 ms,
   troca de cor 0,7–3,3 s, PDF 86×, ~1,21 KB/hex, render 0,65–0,97 s).
5. Espectro incremental (a) → cirúrgico (mapa client-side, via REV-08) → rebuild (b/d);
   (c) = rebuild-lateral.
6. Recomendação PRELIMINAR fundamentada (não veredito): incremental (a) primeiro; teto real de (a);
   (d) como evolução (a confirmar no REV-08); (c) pior custo/benefício; honestidade nos dois sentidos.
7. O que fica para o REV-12 decidir (escala-alvo, apetite JS/TS, teto de d medido pelo REV-08,
   requisito de UX além do mapa).
8. O que NÃO foi medido (WebGL/paint, WebSocket/parse, latência de rede, fetch de tiles, teto
   empírico de d, loop não enxerga a UI) + nota de guardrails READ-ONLY M1.

## Arquivos alterados
- **CRIADO:** `data/analysis/avaliacao_stack.md` — relatório de avaliação de stack (gitignored via
  `data/analysis/`; único entregável de conteúdo).
- `context/handoff.md` (este arquivo) + snapshot `context/handoff/20260710-134041-builder.md`.
- `tasks/current_task.md` — Status: aguardando QA.

NENHUM arquivo em `src/`, `config.py`, `pipelines/`, `data/outputs|staging` ou artefato M1.

## Validações executadas
- `git check-ignore data/analysis/avaliacao_stack.md` → imprime o caminho (exit 0): gitignored. OK
- `python scripts/loop_guard.py --base HEAD` → **GUARD OK: 0 caminho(s) alterado(s), nenhum proibido.**
- `git diff HEAD -- src/motor_expansao/pipelines/m1/ src/motor_expansao/config.py` → **vazio** (exit 0).
- `python -c "import streamlit_app; print('import ok')"` → **import ok** (warnings de cache do Streamlit
  sem runtime são benignos, esperados fora do servidor).
- pytest: NÃO rodado (bloco não toca código; suíte full é gate do QA). Não há subconjunto de testes
  impactado — o único entregável é um relatório Markdown gitignored.

## Problemas encontrados
Nenhum.

## Pendências
Nenhuma. (A DECISÃO rebuild vs refactor é do BLK-REV-12 — fora do escopo deste bloco.)

## Riscos remanescentes
Nenhum de código. O relatório é honesto nos dois sentidos (anti-viés pró-status-quo e pró-rebuild),
parte da topologia multi-container real e não re-litiga o offline §2 nem re-mede performance.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; nada em `pipelines/m1/`/`config.py`).
- Artefatos M1 preservados: sim (diff M1/config vazio; loop_guard OK).
- Dashboard offline mantido: sim (nenhuma mudança de código; smoke import ok).
