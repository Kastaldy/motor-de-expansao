# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-RELPON-07 — Slide de perfil do Bairro/Distrito no Relatório Pontual Censitário (PDF)

## O que foi feito
Implementado o plano técnico do Planner (itens A-G) ao pé da letra, sem desvio de escopo:

- **A/B (`censo_point.py`):** `analisar_ponto_censitario_setores` ganhou 5 campos novos no
  `result` (`cod_bairro_ponto`, `nome_bairro_ponto`, `nome_distrito_ponto`,
  `unidade_ponto_tipo`, `unidade_ponto_rotulo`) preenchidos por leitura direta do MESMO setor
  que contém o ponto (reaproveita o lookup do BLK-RELPON-05, sem geometria/CRS novos).
  Constante nova `METODO_RENDA_PERFIL_BAIRRO`. Novo helper público
  `agregar_perfil_bairro_distrito(setores_df, *, cod_bairro=None, nome_bairro=None,
  nome_distrito=None, nome_municipio=None, uf=None)` que agrega população, domicílios, área
  (→ densidade) e renda média domiciliar (D3.5: `renda_responsavel_media_setor_2022`
  ponderada por `domicilios_particulares_ocupados_setor_2022`, exclusão simétrica) sobre
  TODOS os setores da unidade (bairro, fallback distrito) que contém o ponto — não o raio de
  1,5 km. Função pura, "n/d" gracioso (sem exceção) em todo cenário defensivo (DataFrame
  vazio/None, identificador ausente, colunas ausentes, unidade sem setores).
- **C (`pages.py`):** import de `agregar_perfil_bairro_distrito` adicionado; os 2 pontos de
  chamada (`gerar_payloads_relatorio_pontual_para_pin` e
  `render_relatorio_pontual_censitario`) agora calculam `perfil_bairro` logo após
  `analisar_ponto_censitario_setores` e o repassam ao downstream
  (`gerar_payloads_download_relatorio_censitario`/`render_downloads_relatorio_censitario`).
- **D/E (`censo_report.py`):** `PDF_SECTION_HEADERS` 5→6 strings, com "Perfil do
  Bairro/Distrito" entre "Concorrentes" e "Big Numbers". Novas funções `_perfil_bairro_page`
  (variante recente) e `_classico_perfil_bairro_page` (variante clássica), com 4 cards em
  grid 2x2 (População, Densidade demográfica, Domicílios, Renda média) reaproveitando o
  estilo visual de `_big_numbers_page`; texto de indisponibilidade gracioso quando
  `perfil_bairro` é `None`/`flag_perfil_disponivel` falso. `_tema_bicolor` ganhou o 4º ordinal
  nas duas funções geradoras (Big Numbers passa do ordinal 3→4: troca de cor
  turquesa→magenta, efeito ACEITO da alternância, conforme o plano). Assinaturas de
  `gerar_pdf_relatorio_pontual_censitario`, `gerar_pdf_relatorio_pontual_classico`,
  `gerar_payloads_download_relatorio_censitario` e `render_downloads_relatorio_censitario`
  ganharam o parâmetro keyword-only `perfil_bairro: dict[str, Any] | None = None`, propagado
  ao PDF nas duas variantes. Docstrings atualizadas (5→6 páginas). Laço da marca d'água
  (`for page_number in range(1, pdf.pages_count + 1)`) confirmado dinâmico — cobre a 6ª
  página automaticamente, sem mudança de código (validado pelos testes de contagem
  `>= 6`).
- **F (testes novos):**
  - `test_relatorio_pontual_censitario_motor.py`: `_sector_record` ganhou kwargs opcionais
    `cod_bairro`/`nome_bairro`/`nome_distrito` (default `None`, não quebra chamadas
    existentes); `test_lookup_bairro_ponto_quando_setor_tem_bairro`,
    `test_lookup_distrito_ponto_fallback_quando_bairro_ausente`,
    `test_lookup_setor_do_ponto_fora_da_malha` estendido com 3 asserts novos, e 6 testes de
    `agregar_perfil_bairro_distrito` (agregação por `cod_bairro`, fallback para distrito,
    exclusão simétrica de renda, "n/d" sem identificador, "n/d" com `setores_df` vazio).
  - `test_relatorio_pontual_censitario_export.py`: `_PERFIL_BAIRRO_OK` (dict sintético) +
    `test_perfil_bairro_page_presente_com_4_metricas_recente`,
    `test_perfil_bairro_page_nd_quando_perfil_bairro_none`,
    `test_classico_perfil_bairro_page_presente_e_nd`.
- **F.2 (testes existentes atualizados):** as 12 ocorrências literais de `b"/Count 5"` em
  `test_relatorio_pontual_censitario_export.py` trocadas por `b"/Count 6"` (via
  substituição literal exaustiva no arquivo); as 2 contagens de marca d'água `>= 5` → `>= 6`
  (`test_pdf_marca_dagua_em_todas_as_paginas`, `test_classico_marca_dagua_solicitante`);
  `test_classico_gera_5_paginas_e_secoes` renomeado para
  `test_classico_gera_6_paginas_e_secoes`; comentários/docstrings "5 páginas"/"5" atualizados
  nos pontos citados pelo plano. `test_slide_unico_tres_mapas_sem_sobreposicao` e os 11
  testes listados no item 24 do plano permaneceram sem mudança (confirmado).
- **G (docs):** `docs/relatorio_pontual_censitario.md` atualizado: §4 (nota sobre colunas
  `cod_bairro`/`nome_bairro`/`nome_distrito`/`domicilios_particulares_ocupados_setor_2022`/
  `renda_responsavel_media_setor_2022`/`area_setor_m2` já existentes no artefato geo), §7
  (estrutura de 6 páginas com a nova página 4, parágrafo dedicado `BLK-RELPON-07:` com
  D1/D2/D3/D3.5, nota de escopo sobre `api/service.py` ficar sem `perfil_bairro`, parágrafo
  de "Inserção BLK-RELPON-07" documentando a mudança 5→6 e a troca de cor aceita do Big
  Numbers, e atualização da linha "Validação mínima" citando os testes novos).

## Nota sobre a diferença de contagem "13 asserts" do plano vs. 12 encontrados
O plano (item 22) menciona "13 no arquivo"; a busca exaustiva por `b"/Count 5"` no arquivo
encontrou exatamente **12** ocorrências literais, todas trocadas para `b"/Count 6"` (nenhuma
ficou para trás — confirmado por novo grep pós-edição, zero ocorrências residuais de
`/Count 5`, `5 paginas` ou `>= 5` relacionadas a página no arquivo). Não bloqueante: a lista
de funções do item 22 do plano foi conferida uma a uma e todas foram cobertas.

## Arquivos alterados
- `src/motor_expansao/dashboard/censo_point.py` — 5 campos novos no `result` de
  `analisar_ponto_censitario_setores`, constante `METODO_RENDA_PERFIL_BAIRRO`, novo helper
  `agregar_perfil_bairro_distrito` + `_perfil_bairro_default`, docstrings atualizadas.
- `src/motor_expansao/dashboard/pages.py` — import de `agregar_perfil_bairro_distrito`; 2
  pontos de chamada calculam e propagam `perfil_bairro`.
- `src/motor_expansao/dashboard/censo_report.py` — `PDF_SECTION_HEADERS` 5→6;
  `_perfil_bairro_page`/`_classico_perfil_bairro_page` novas; `_tema_bicolor` com 4º ordinal
  nas 2 funções geradoras; assinaturas de 4 funções públicas ganharam `perfil_bairro`;
  docstrings atualizadas.
- `tests/unit/test_relatorio_pontual_censitario_motor.py` — `_sector_record` com kwargs
  opcionais de bairro/distrito; 2 testes novos de lookup; 1 teste estendido; 6 testes novos
  de `agregar_perfil_bairro_distrito`.
- `tests/unit/test_relatorio_pontual_censitario_export.py` — `_PERFIL_BAIRRO_OK`; 3 testes
  novos da página nova; 12 ocorrências de `/Count 5`→`/Count 6`; 2 contagens de marca
  d'água `>= 5`→`>= 6`; 1 teste renomeado; comentários/docstrings atualizados.
- `docs/relatorio_pontual_censitario.md` — §4, §7 (estrutura de páginas, parágrafo
  BLK-RELPON-07, nota de escopo API, validação mínima).

## Validações executadas
- `python -m pytest -n auto -q <subconjunto>`: falhou por INFRA conhecida deste ambiente
  Windows (`execnet`/`OSError: [WinError 6] Identificador inválido` no
  `pytest-xdist`/`execnet` ao criar os workers — mesmo sintoma documentado em ciclos
  anteriores, não relacionado ao código deste bloco).
- `python -m pytest -q tests/unit/test_relatorio_pontual_censitario_motor.py
  tests/unit/test_relatorio_pontual_censitario_export.py
  tests/unit/test_relatorio_acentuacao_regressao.py tests/unit/test_relatorio_municipal.py
  tests/integration/test_streamlit_app.py` (SERIAL, fallback documentado): **345 passed, 0
  failed, 0 skipped** em 60,74s.
- Re-execução isolada de `tests/unit/test_relatorio_pontual_censitario_motor.py` +
  `tests/unit/test_relatorio_pontual_censitario_export.py` após o fix de ruff (import não
  usado): **45 passed**.
- `ruff check . --no-cache`: 1 erro encontrado na primeira passada (`F401` — import não
  usado de `agregar_perfil_bairro_distrito` em `test_relatorio_pontual_censitario_export.py`,
  pois o teste usa um dict literal `_PERFIL_BAIRRO_OK` em vez do helper); corrigido removendo
  o import. Segunda passada: **All checks passed!**
- `mypy src/`: 6 erros pré-existentes (`import-untyped` de `requests`, em
  `ibge_censo.py`/`poi_enrichment.py`/`base_h3_brasil.py`/`api/geo.py`/`api/telegram_bot.py`/
  `api/service.py`) — nenhum nos 3 arquivos tocados por este ciclo.
  `mypy src/motor_expansao/dashboard/censo_point.py
  src/motor_expansao/dashboard/censo_report.py src/motor_expansao/dashboard/pages.py`
  isolado: **Success: no issues found in 3 source files**.
- Smoke import: `python -c "import motor_expansao.dashboard.streamlit_app"` falhou
  (`ModuleNotFoundError` — esse caminho de import não existe no repo, mesmo antes desta
  mudança); `PYTHONPATH=src python -c "import streamlit_app; print('import ok')"` (padrão
  usado em ciclos anteriores, arquivo `streamlit_app.py` na raiz): **`import ok`** (com
  warnings esperados de cache do Streamlit sem runtime, inofensivos).

## Problemas encontrados
- `pytest -n auto` falha por infraestrutura conhecida deste ambiente Windows (execnet/
  `OSError [WinError 6]`), não pelo código do bloco — mitigado rodando serial, conforme
  instrução do orquestrador.
- 1 import não usado detectado pelo ruff (`agregar_perfil_bairro_distrito` em
  `test_relatorio_pontual_censitario_export.py`) — corrigido durante a validação (o teste usa
  um dict literal em vez de chamar o helper, decisão explicitamente permitida pelo plano:
  "OU um dict literal com os campos do item B").

## Pendências
- Gate visual humano pós-QA (Vinicius revisa o PDF renderizado antes do merge) — geometria
  fina da nova página (cards, subtítulo, espaçamento) é ajustável nesse gate, conforme
  "Critérios de aceite finais" do handoff do Planner.
- `tasks/completed.md`: entrada de fechamento fica a cargo do Builder/QA no fechamento do
  ciclo (explicitamente fora do escopo deste handoff do Planner, item 27).

## Riscos remanescentes
- Nenhum risco técnico novo identificado além dos já documentados pelo Planner (troca de cor
  aceita do Big Numbers por alternância de ordinal; fallback bairro→distrito depende de dado
  real do artefato geo, coberto por teste dedicado; `api/service.py` intencionalmente sem
  `perfil_bairro`, gera a página em "n/d" — aceito e documentado).

## Guardrails verificados
- score_priorizacao não alterado: sim (não tocado; nenhuma alteração em
  `pipelines/m1/`, pesos, `hex_score_estrutural` ou artefatos oficiais).
- Artefatos M1 preservados: sim.
- Dashboard offline mantido: sim (nenhuma dependência de rede nova; página nova é
  texto/número puro, sem mapa, sem tocar `censo_map.py`).
- Método de interseção `setor_censitario_intersecao_area_1p5km`, raio 1,5 km,
  `RAIO_CENSITARIO_DEFAULT_KM`: intocados (confirmado por leitura e pelos testes existentes
  que continuam verdes byte-a-byte na lógica de interseção).
- `set_compression(False)`/`pdf_version="1.4"`: intocados.
- Acentuação: texto novo do PDF com acentuação correta em português e pontuação ASCII (sem
  travessão/bullet/seta/reticências/aspas curvas/©); identificadores crus
  (`unidade_ponto_tipo`/`unidade_tipo` = `"bairro"`/`"distrito"`) permanecem sem acento; a
  camada de rótulo acentuado ("Bairro"/"Distrito") vive só em `censo_report.py`.
- Anti-PII: nenhum dado de pessoa física introduzido; só agregados estatísticos de Censo por
  bairro/distrito.
