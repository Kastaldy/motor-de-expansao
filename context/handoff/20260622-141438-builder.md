# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-DIM-17 — Fix: limiar de renda da zona morta (3.000 → 1.600)

## O que foi feito

1. **Constante alterada** em `src/motor_expansao/dimensionamento/viabilidade_ponto.py`, linha 38:
   - De: `RENDA_ZONA_MORTA_MIN: float = 3_000.0`
   - Para: `RENDA_ZONA_MORTA_MIN: float = 1_600.0`
   - A função `flag_zona_morta` e o orquestrador `analisar_viabilidade_ponto` referenciam a constante por nome — propagam automaticamente sem mais alterações.

2. **Grep de hardcodes** confirmado: nenhuma ocorrência de `3000`/`3_000` em `src/` ou `tests/` asserta ou compara com o valor antigo em contexto de zona morta. As demais ocorrências de `3000` no repositório referem-se a outros contextos (faturamento, alunos_reais, fixtures de renda de outros módulos).

3. **2 testes de fronteira adicionados** ao final de `tests/unit/dimensionamento/test_viabilidade_ponto.py`:
   - `test_flag_zona_morta_renda_abaixo_novo_limiar`: renda=1500 < 1600 → `flag_zona_morta=True`, motivo contém `"renda<1600"`.
   - `test_flag_zona_morta_renda_no_limiar_nao_dispara`: renda=1600 == limiar → `flag_zona_morta=False`, motivo `"ok"`.
   - Confirmado antes de escrever os testes que `flag_zona_morta` formata o motivo como `f"renda<{int(renda_min)}"` (linha 212 do arquivo fonte) — portanto `"renda<1600"` é a string correta.

## Arquivos alterados
- `src/motor_expansao/dimensionamento/viabilidade_ponto.py` — linha 38: `RENDA_ZONA_MORTA_MIN` alterada de `3_000.0` para `1_600.0`
- `tests/unit/dimensionamento/test_viabilidade_ponto.py` — 2 testes de fronteira BLK-DIM-17 adicionados ao final (seção claramente marcada com comentário)

## Validações executadas
- `python -c "from motor_expansao.dimensionamento.viabilidade_ponto import RENDA_ZONA_MORTA_MIN; assert RENDA_ZONA_MORTA_MIN == 1600.0"` → ok (`RENDA_ZONA_MORTA_MIN = 1600.0`)
- `pytest -q tests/unit/dimensionamento/test_viabilidade_ponto.py` → **21 passed in 3.19s** (19 existentes + 2 novos)
- `ruff check src/motor_expansao/dimensionamento/viabilidade_ponto.py tests/unit/dimensionamento/test_viabilidade_ponto.py` → **All checks passed!**
- `mypy src/motor_expansao/dimensionamento/viabilidade_ponto.py` → **Success: no issues found in 1 source file**
- `python -c "import streamlit_app; print('import ok')"` → **import ok**

## Problemas encontrados
Nenhum.

## Pendências
- Suite completa (`pytest -q` ou `pytest -n auto`) — responsabilidade do QA (gate único por ciclo).

## Riscos remanescentes
Nenhum. Mudança é uma constante de alerta/exibição; não altera cálculo de margem, payback, ROIC nem qualquer parâmetro do M1.

## Guardrails verificados
- score_priorizacao não alterado: sim (mudança é READ-ONLY sobre M1)
- Artefatos M1 preservados: sim (nenhum parquet ou artefato oficial tocado)
- Dashboard offline mantido: sim (nenhuma dependência de rede adicionada)
- `RENDA_MIN = 4_500.0` em `config.py` do M1: INTOCADO
- `config.py`, pipelines M1, artefatos oficiais: INTOCADOS
