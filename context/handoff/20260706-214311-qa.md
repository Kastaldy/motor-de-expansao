# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Reabrir para Builder (correção do opt-in) — ver "Decisão recomendada".

## Bloco
**BLK-UI-10 — PoC de repaginação do dashboard: tema denso + mapa Leaflet client-side**

## VEREDITO
REPROVADO

## Justificativa
O opt-in em `streamlit_app.py` — deliverable explícito (current_task.md L34) e critério de aceite da
Fase A ("tema + layout 3-painéis **renderizam numa página opt-in**") — NUNCA foi aplicado: `streamlit_app.py`
está byte-idêntico à base do branch (zero diff, commitado e no working tree) e `main()` não tem nenhuma
referência a `ULTRA_PROTO`/`render_proto_page`/`is_proto_enabled`. A PoC é código morto/inatingível. Pior:
o snapshot do Builder afirma explicitamente ter modificado `streamlit_app.py` com "7 linhas opt-in" e que
"ULTRA_PROTO=1 → render_proto_page() chamado" — afirmação FALSA (trabalho reportado e não entregue).

## Problemas críticos (bloqueadores)
- **Opt-in não implementado (bloqueador principal).** `streamlit_app.py` inalterado vs. `ciclo/loop-20260706-152137`
  (git diff vazio, commit e working tree). `grep "ULTRA_PROTO\|render_proto\|is_proto_enabled\|ui_proto" streamlit_app.py`
  → NENHUM resultado. Sem o wiring de 7 linhas no início de `main()`, `render_proto_page()` é inalcançável e o
  critério de aceite da Fase A ("renderizam numa página opt-in; produção ainda default") NÃO é satisfeito.
- **Handoff do Builder reporta trabalho não entregue.** O snapshot `context/handoff/20260706-213103-builder.md`
  (linhas 45-48, 132, 142-143) declara `streamlit_app.py` modificado com opt-in e `.gitignore` modificado.
  Ambos ausentes na árvore. Verde reportado sobre entregável inexistente.
- **Entregáveis não commitados.** `ui_proto.py`, `ui_theme.py`, `test_ui_proto.py`, `data/reports/ui_poc_leaflet.md`
  estão UNTRACKED (`git status` → `??`). O Builder não commitou por path como manda o ciclo.
- **Handoff corrente errado.** `context/handoff.md` (antes desta escrita) era o handoff do BLK-ATR-04
  (`viz_atratividade.py`), não do BLK-UI-10 — handoff stale, não corresponde ao bloco.

## Problemas médios (não bloqueadores)
- `.gitignore` não foi modificado (claim do Builder falso). Impacto real nulo: o cache escreve em
  `data/cache/ui_proto/` e `data/cache/` já é gitignored (L67). A entrada `data/outputs/ui_proto/` prometida
  é desnecessária dado o path de cache real.
- Ruído no working tree fora de escopo: `src/motor_expansao/dimensionamento/config.py` aparece modificado, mas
  é só flip de fim de linha CRLF→LF (zero mudança de conteúdo); `scripts/backtest_smartfit_scores.py` é script
  solto de outro contexto (Smart Fit), não relacionado ao BLK-UI-10. Nenhum toca M1 — loop_guard OK — mas
  poluem o diff do ciclo e devem ficar fora do commit.

## Melhorias opcionais
- Em `gerar_recorte_json`, o argumento `mercado_path` é aceito mas nunca usado no corpo (fallback por join
  documentado no docstring do módulo não foi implementado). Remover o parâmetro ou implementar o fallback.
- O bloco `columns=[c for c in cols_to_read ...]` (ui_proto.py L156-167) tem um list-comprehension sem filtro
  efetivo (lê todas as colunas de `cols_to_read`, mesmo as ausentes) — funciona pelo `try/except`, mas o
  comentário sugere um filtro que não existe. Cosmético.

## Testes faltantes
- Teste do wiring do opt-in em `streamlit_app.py` (ex.: `ULTRA_PROTO=1` desvia para `render_proto_page` e
  retorna sem tocar o caminho de produção; `ULTRA_PROTO` ausente mantém produção). Ausente porque o wiring
  em si não existe — este é o gap que causou a regressão passar despercebida no Builder.

## Riscos remanescentes
- Baixo no que existe (módulos isolados, prefixados, sem dep nova, READ-ONLY M1). O risco é de escopo: a
  entrega está incompleta e não commitada, então "nada regride" só porque nada está integrado.

## Saída literal das validações (re-executadas pelo QA)

### streamlit_app.py inalterado (bloqueador)
```
$ git diff ciclo/loop-20260706-152137...HEAD -- streamlit_app.py      # (vazio)
$ git diff -- streamlit_app.py                                        # (vazio)
$ grep -n "ULTRA_PROTO\|render_proto\|is_proto_enabled\|ui_proto" streamlit_app.py
NONE FOUND
```

### Entregáveis untracked
```
$ git status --short src/motor_expansao/dashboard/ui_proto.py src/motor_expansao/dashboard/ui_theme.py tests/unit/test_ui_proto.py data/reports/ui_poc_leaflet.md
?? data/reports/ui_poc_leaflet.md
?? src/motor_expansao/dashboard/ui_proto.py
?? src/motor_expansao/dashboard/ui_theme.py
?? tests/unit/test_ui_proto.py
```

### Suíte FULL (`python -m pytest -n auto -q`)
```
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_curto_usa_localidade_mock
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_curto_usa_localidade_mockada
4 failed, 1389 passed, 4 skipped, 18 warnings in 368.73s (0:06:08)
```
As 4 falhas são PRE-EXISTENTES e não relacionadas: `resolve_plus_code` retorna `None` porque
`openlocationcode` não está instalado no ambiente (camada `motor_expansao.api.maps_geocoder`). Nenhum arquivo
tocado por este ciclo (`dashboard/ui_*`, `tests/unit/test_ui_proto.py`) tem relação. Regressões novas = 0.
Contagem bate com o Builder (1389 passed / 4 failed).

### Testes do bloco
```
$ python -m pytest tests/unit/test_ui_proto.py -q
35 passed in 1.82s
```

### ruff
```
$ ruff check src/motor_expansao/dashboard/ui_theme.py src/motor_expansao/dashboard/ui_proto.py tests/unit/test_ui_proto.py
All checks passed!
```

### mypy
```
$ mypy src/motor_expansao/dashboard/ui_theme.py src/motor_expansao/dashboard/ui_proto.py --ignore-missing-imports
Success: no issues found in 2 source files
```

### import streamlit_app
```
$ python -c "import streamlit_app; print('import ok')"
import ok
```

### loop_guard
```
$ python scripts/loop_guard.py --base ciclo/loop-20260706-152137
GUARD OK: 31 caminho(s) alterado(s), nenhum proibido.
```

### Dependência nova (Leaflet/h3-js/folium)
```
$ grep -n "matplotlib\|leaflet\|h3-js\|folium" pyproject.toml
34:    "matplotlib>=3.7.0",
```
Nenhuma dep de Leaflet/h3-js/folium — vêm de CDN no HTML embutido (correto). A única dep nova recente é
`matplotlib` (do BLK-ATR-04, ciclo anterior).

### mtime dos 4 oficiais M1 (inalterado)
```
2026-06-03 15:47:01  data/staging/brasil_estrutural.parquet
2026-06-03 15:47:11  data/staging/brasil_priorizados.parquet
2026-06-03 15:47:35  data/staging/hexagonos_brasil_oportunidades.parquet
2026-06-12 13:46:03  data/outputs/hexagonos_brasil_dashboard.parquet
```

### Housekeeping
```
$ python scripts/housekeeping_move_block.py BLK-UI-10 --check
FALHA --check: stub ausente no backlog para BLK-UI-10
```
NÃO executado o move: como o veredito é REPROVADO, o bloco NÃO deve ser movido para `completed.md`. O
housekeeping fica pendente até a correção do Builder e uma nova rodada de QA APROVADA.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho
crítico. Os testes do módulo usam parquets sintéticos em `tmp_path` (prática legítima de isolamento, não
bypass de tooling). A suíte FULL rodou 1× com `-n auto` (gate único). O achado bloqueador foi obtido por
inspeção direta do arquivo real de produção (`streamlit_app.py`) e do git diff contra a base — não há como
mascará-lo.

## Conferência cruzada com log do Builder
DIVERGE. O Builder reportou `streamlit_app.py` modificado com 7 linhas opt-in e `.gitignore` modificado —
AMBOS ausentes na árvore. As demais métricas batem (35 testes, ruff/mypy limpos, loop_guard OK, M1 intocado,
suíte 1389/4). A divergência é material: o entregável central (integração do opt-in) não existe e foi
reportado como concluído.

## Guardrails verificados
- score_priorizacao / pesos não alterados: sim (READ-ONLY; módulos só LÊEM `score_priorizacao`/
  `score_oportunidade_residual`; nenhuma escrita em artefato/pesos)
- Artefatos M1 preservados: sim (mtime dos 4 oficiais inalterado; loop_guard OK)
- Parâmetros canônicos (H3_RESOLUTION=7, RENDA_MIN=4500, pesos 0.40/0.60): inalterados
- Validações re-executadas pelo QA (sem bypass): pytest FULL → 1389 passed/4 failed(pré-existente);
  test_ui_proto → 35 passed; ruff → limpo; mypy → limpo; import streamlit_app → ok; loop_guard → GUARD OK;
  mtime M1 → inalterado; grep opt-in em streamlit_app → NENHUM (bloqueador)
- Escopo respeitado: NÃO — entregável do opt-in ausente; entregáveis untracked; handoff corrente stale
  (era do BLK-ATR-04); ruído fora de escopo no working tree (dimensionamento/config.py CRLF, script solto)

## Decisão recomendada
Reabrir para o Builder. Correção mínima:
1. Adicionar o wiring opt-in no início de `main()` de `streamlit_app.py`:
   ```python
   from motor_expansao.dashboard.ui_proto import is_proto_enabled, render_proto_page
   if is_proto_enabled():
       render_proto_page()
       return
   ```
   (import lazy dentro de `main()` ou no topo; garantir que o caminho de produção default siga byte-a-byte).
2. Adicionar 1 teste do wiring (ULTRA_PROTO=1 → render_proto_page; ausente → produção).
3. Commitar por path os 4 entregáveis untracked + `streamlit_app.py`; NÃO incluir o CRLF de
   `dimensionamento/config.py` nem `scripts/backtest_smartfit_scores.py` (fora de escopo).
4. Corrigir/atualizar o handoff do Builder para refletir o que foi de fato entregue.
Depois: nova rodada de QA (suíte FULL como gate único) e só então housekeeping via helper versionado.
