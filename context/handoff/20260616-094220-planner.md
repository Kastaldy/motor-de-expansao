# Handoff — Planner (revisado pós-gate)

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
**Builder** — GATE APROVADO COM AJUSTES por Felipe/Vini. As 3 decisões de produto (F1-A sets, F2-A labels+reorder, F2-C expander) e os 4 ajustes novos de UX (F2-F/G/H/I) foram aprovados. Não há novo gate humano antes do Builder; o humano revisa as colunas/labels antes do merge.

## Entendimento da tarefa
2º recorte do BLK-UI-01: reduzir densidade visual (colunas/KPIs/captions) e melhorar navegação/fluxo das 5 abas do dashboard de produção — REORDENANDO as abas (Mapa como 1ª/padrão), colapsando filtros avançados, reposicionando o botão de download do relatório, garantindo sidebar sempre aberta e o seletor de abas sempre no topo do corpo. READ-ONLY sobre o M1, offline, sem tocar score/artefatos/builders de mapa/lazy-load/`censo_*` (apenas reposicionar chamadas de UI existentes).

---

## Plano técnico consolidado (núcleo + ajustes novos)

### F1-A — Reduzir colunas-padrão nas tabelas (Carteira, Plano, Domínio) — APROVADO
Princípio: NENHUMA coluna sai do DataFrame/parquet; só reduz o set EXIBIDO por padrão. As secundárias passam para um `st.expander("Mostrar colunas detalhadas ...", expanded=False)` que renderiza um SEGUNDO `st.dataframe` com o set completo (mesma `view`, mesmos formatadores). Em cada função: construir `tbl` COMPLETO (formatação inalterada), renderizar `st.dataframe(tbl[primárias].head(TABLE_ROW_LIMIT), ...)`; abaixo `with st.expander(...): st.dataframe(tbl.head(TABLE_ROW_LIMIT), ...)`.

1. **`render_carteira_expansao`** (`pages.py`, dict `display_cols_order`). SET PRIMÁRIO (12 cols, ordem preservada):
   `rank_brasil` (Rank Brasil), `rank_uf` (Rank UF), `score_priorizacao` (Score M1), `prioridade_abertura` (Prioridade), `uf` (UF), `nome_municipio` (Municipio), `hex_id` (Hex ID), `modo_selecao_carteira` (Modo Hex), `score_setor_2022_calibrado` (Score Censo), `sam_fitness_potencial` (SAM Fitness), `oferta_efetiva_disponivel` (Oferta Residual), `motivo_priorizacao` (Motivo).
   SECUNDÁRIAS (expander): `rank_hex_intraurbano`, `score_expansao_hibrido`, `score_oportunidade_residual`, `quartil_oportunidade_residual`, `share_ultra_estimado_hex`, `oferta_consumida_mercado_estimada`, `oferta_consumida_ultra_real`, `rank_municipio_uf`, `rank_municipio_brasil`, `qualidade_join_uf`, `coverage_pct_setor_2022`, `flag_outlier_espacial`, `flag_baixa_pop_setor`, `flag_join_uf_restrito`.
   Texto do expander: `"Mostrar colunas detalhadas (residual, ranks, censo)"`.
2. **`render_plano_expansao`** (`pages.py`, dict `tbl_cols`). SET PRIMÁRIO (11 cols):
   `rank_carteira_brasil` (Rank Brasil), `rank_carteira_uf` (Rank UF), `nivel_prioridade_final` (Nivel), `uf` (UF), `nome_municipio` (Municipio), `hex_id` (Hex ID), `score_expansao_hibrido` (Score Hibrido), `score_priorizacao` (Score M1), `score_setor_2022_calibrado` (Score Censo), `sam_fitness_potencial` (SAM Fitness), `oferta_efetiva_disponivel` (Oferta Residual).
   SECUNDÁRIAS (expander): `modo_selecao_carteira`, `score_oportunidade_residual`, `quartil_oportunidade_residual`, `share_ultra_estimado_hex`, `oferta_consumida_mercado_estimada`, `oferta_consumida_ultra_real`, `qualidade_join_uf`, `coverage_pct_setor_2022`, `motivo_priorizacao`, `flag_outlier_espacial`, `flag_baixa_pop_setor`, `status_pipeline`.
3. **`render_expansao_dominio`** (`pages.py`, dict `display_cols`). SET PRIMÁRIO (11 cols):
   `rank_dominio_brasil` (Rank Brasil), `rank_dominio_uf` (Rank UF), `uf` (UF), `nome_municipio` (Cidade), `hex_id` (Hex Ancora), `ordem_expansao_cidade` (Ordem na Cidade), `score_oportunidade_residual` (Score Residual), `residual_incremental_capturado` (Residual Capturado), `oferta_efetiva_disponivel` (Oferta Disponivel), `tese_dominio` (Tese), `dist_ultra_mais_proxima_m` (Dist. Ultra (m)).
   SECUNDÁRIAS (expander): `cluster_id`, `oferta_consumida_mercado_estimada`, `oferta_consumida_ultra_real`, `n_concorrentes_mapeados_2km`, `rank_dominio_cidade`.
   - **ATENÇÃO TESTE**: `test_render_expansao_dominio_exibe_consumo_quando_colunas_presentes` (linha 4091–4133) lê `rendered_dfs[0]` e exige "Consumo Conc. (est.)"/"Consumo Ultra (real)" nesse frame. Como o set primário NÃO contém Consumo, o 1º dataframe muda. → TROCAR `rendered_dfs[0]` por `rendered_dfs[-1]` (o frame COMPLETO do expander é o último; o mapa usa `pydeck_chart`, não `dataframe`). Asserts dos valores ("3.000"/"500") permanecem.

### F1-B — Colapsar KPIs da Análise Pontual — APROVADO (Média)
4. **`_render_analise_pontual_multihex`** (`pages.py`, hoje 4+5+3 = 12 KPIs):
   - 2 linhas, ≤9 KPIs. Linha 1 (4): Hexes selecionados, Habitantes, Renda per capita med., Residual fitness. Linha 2 (5): Score M1 med., Score Residual med., Score Dominio Hibrido med., Presenca Ultra, Concorrentes (hexes). Os 3 de consumo (Consumo concorrentes / Consumo Ultra / Consumo total instalado) viram 1 `st.caption` consolidado abaixo (ex.: `Consumo instalado no cenario — concorrentes: X | Ultra: Y | total: Z alunos.`). Reusa `_fi`/`_fs`.
5. **`render_analise_pontual` (simples)** (`pages.py`, hoje 4+4+3 = 11 KPIs):
   - Manter 2 linhas de 4 (8 KPIs primários: Hexes no raio, Populacao no raio, Renda per capita med., Residual total, Score residual med., Score M1 med., Concorrentes no raio, Ultra no raio). Converter o bloco de consumo (3 `st.metric`) para 1 `st.caption` consolidado, removendo as colunas `ka/kb/kc`; a caption de proveniência é absorvida nessa caption única.
   - **TESTE**: `test_render_analise_pontual_com_coordenada_exibe_kpis` (3202+) só exige que algum label contenha "hex" e algum "residual/score" → preservado. Safe.

### F1-C — Disclaimer de centroide em 1 lugar só — APROVADO (Baixa)
6. Extrair constante de módulo em `pages.py` (perto de `DASHBOARD_TAB_LABELS`, ~444):
   `_CENTROID_DISCLAIMER = ("Leitura por centroide H3 res-7: precisao aproximada ~0.5-1 km. Nao altera score_priorizacao, carteira, plano ou artefatos do M1.")`
   - Substituir as 2 ocorrências literais (multihex e simples) por `st.caption(_CENTROID_DISCLAIMER)`. Padronizar para a versão sem "de hexagono".
   - Aceite: `grep "centroide H3 res-7" pages.py` retorna só a definição da constante (1 linha lógica).

### F1-D — Formatação de números — SEM AÇÃO (evidência registrada)
7. `format_int` (`utils.py:8-9`) é o único helper de inteiro e sempre usa ponto como separador. A 1 ocorrência inline `f"{int(v):,}".replace(",", ".")` (`pages.py:1606`) produz EXATAMENTE o mesmo resultado. Scores usam `f"{v:.1f}"` consistente nas tabelas/KPIs. **NÃO alterar `utils.py`.** Fechado sem ação.

### F2-A — REORDENAR + renomear as 5 abas — APROVADO (Alta, decisão de produto + acoplamento crítico)
8. **NOVA ORDEM E LABELS (com acentos UTF-8), em `DASHBOARD_TAB_LABELS` (`pages.py:444–450`):**
   ```python
   DASHBOARD_TAB_LABELS = [
       "Mapa",
       "Executivo",
       "Expansão de Domínio",
       "Carteira e Plano",
       "Viabilidade",
   ]
   ```
   - Consequência: **"Mapa" passa a ser a 1ª aba e a aba PADRÃO** (default do `render_tab_selector` = `opts[0]` = "Mapa" quando nada selecionado; o código já usa `opts[0]` como default/fallback — basta a reordenação para que o default vire "Mapa").
   - **ACOPLAMENTO CRÍTICO — dispatch em `main()` (`streamlit_app.py:531–588`)**: o dispatch compara `active_tab` contra strings literais. Atualizar TODOS os branches em lockstep com os NOVOS labels (acentos inclusos), mantendo o mapeamento aba→render:
     - `if active_tab in ("Visao Executiva", "Mapa Territorial"):` (linha 531, gate dos summaries) → `if active_tab in ("Executivo", "Mapa"):`
     - `if active_tab == "Visao Executiva":` (535) → `if active_tab == "Executivo":` → `render_visao_executiva(...)`
     - `elif active_tab == "Mapa Territorial":` (549) → `elif active_tab == "Mapa":` → `render_mapa_territorial(...)`
     - `elif active_tab == "Expansao de Dominio":` (565) → `elif active_tab == "Expansão de Domínio":` → `render_expansao_dominio(...)`
     - `elif active_tab == "Carteira e Plano":` (573) → INALTERADO (label não muda) → `render_carteira_e_plano(...)`
     - `elif active_tab == "Viabilidade do Imovel":` (581) → `elif active_tab == "Viabilidade":` → `render_viabilidade_ponto(...)`
     - **Atenção acentos**: o dispatch atual usa strings SEM acento (`"Visao"`, `"Dominio"`); os novos labels TÊM acento em `"Expansão de Domínio"`. As comparações em `main()` DEVEM bater byte-a-byte com `DASHBOARD_TAB_LABELS`. Recomenda-se ao Builder referenciar por índice (`DASHBOARD_TAB_LABELS[0]`..`[4]`) OU copiar as strings com acento exatamente — não misturar acentuado/não-acentuado.
   - **TESTES QUE QUEBRAM (todos em `tests/integration/test_streamlit_app.py`)** — valores novos exatos:
     - `test_render_tab_selector_e_exportado` (4223–4232): a lista literal asseverada deve virar exatamente:
       ```python
       assert streamlit_app.DASHBOARD_TAB_LABELS == [
           "Mapa", "Executivo", "Expansão de Domínio", "Carteira e Plano", "Viabilidade",
       ]
       ```
     - `test_render_tab_selector_retorna_aba_ativa` (4235–4245): `return_value="Mapa Territorial"` → `return_value="Mapa"`; `assert result == "Mapa Territorial"` → `assert result == "Mapa"`.
     - `test_render_tab_selector_fallback_quando_desmarcado` (4248–4266): 1º caso `assert result == "Visao Executiva"` → **`assert result == "Mapa"`** (default agora é a 1ª aba = Mapa). 2º caso: `session_state {"dashboard_active_tab_last": "Carteira e Plano"}` + `assert result == "Carteira e Plano"` → INALTERADO (label não muda).
     - `test_main_renderiza_apenas_a_aba_ativa` (4269–4313): `render_tab_selector` mockado `return_value="Expansao de Dominio"` → `return_value="Expansão de Domínio"`; e o `main()` reescrito roteia para `render_expansao_dominio` (mock `dominio_mock.called`). Demais asserts inalterados.
     - `test_render_viabilidade_ponto_aba_no_dashboard_tab_labels` (4867–4870): `assert "Viabilidade do Imovel" in DASHBOARD_TAB_LABELS` → `assert "Viabilidade" in streamlit_app.DASHBOARD_TAB_LABELS`; `assert len(...) == 5` INALTERADO.

### F2-B — Estado vazio sem comando shell — APROVADO (Média)
9. Trocar as 3 mensagens de `st.warning` que expõem comandos de shell (empty-states das tabelas):
   - `render_carteira_expansao` (~1222–1225, contém `python -m jobs.pipelines.gerar_carteira_acionavel`): → `"Carteira acionavel indisponivel neste recorte. Ela e gerada no ciclo de regeneracao dos parquets — contate o time de dados se esta tela estiver vazia em producao."`
   - `render_expansao_dominio` (~1489–1492, contém `python jobs/pipelines/gerar_plano_expansao_dominio.py`): → `"Plano de Expansao de Dominio indisponivel neste recorte. Ele e gerado no ciclo de regeneracao dos parquets — contate o time de dados se esta tela estiver vazia em producao."`
   - `render_plano_expansao` (~1647–1650, contém `python -m jobs.pipelines.gerar_plano_expansao_curto_prazo`): → `"Plano de curto prazo indisponivel neste recorte. Ele e gerado no ciclo de regeneracao dos parquets — contate o time de dados se esta tela estiver vazia em producao."`
   - **NÃO tocar** a linha ~3089 (`render_mapa_territorial`, warning de "modo dominio indisponivel" — outro contexto, fora do escopo deste item).
   - **TESTE**: `test_render_expansao_dominio_exibe_warning_sem_dados` (1895–1903) exige `"Expansao de Dominio" in msg or "plano" in msg.lower()` → o texto proposto contém ambos. PASSA sem alterar o teste.

### F2-C — Filtros avançados em expander colapsado — APROVADO (Média, decisão de produto)
10. **`render_sidebar_filters`** (`pages.py:480–543`). Mover os 5 controles avançados (`Elegibilidade hibrida` 507, `Cobertura censitaria` 512, `Qualidade da camada` 517, `Apenas top_municipio` 522, `Apenas top_hex_intraurbano` 526) para `with st.sidebar.expander("Filtros avancados", expanded=False):`. Os controles `Municipio`/`Faixa de oportunidade` permanecem na primeira dobra.
    - Mover/absorver `st.sidebar.markdown("---")` + `### Camada Hibrida` + a caption (504–506) para o título/corpo do expander.
    - O `return` (534–543) e a assinatura permanecem IDÊNTICOS (mesma ordem dos 8 valores) — `main()` não muda.
    - **INVARIANTE**: `render_uf_selectbox` é chamado em `main()` (streamlit_app.py:457) ANTES de `render_sidebar_filters` (490), então continua sendo o 1º elemento da sidebar; o expander não pode vir antes do bloco Municipio/Faixa dentro de `render_sidebar_filters`.
    - **TESTE**: nenhum teste mocka o conteúdo interno de `render_sidebar_filters` (só a função inteira em `test_main_*`). Safe.

### F2-D — Separadores/headings no Mapa Territorial — APROVADO (Baixa)
11. **`render_mapa_territorial`** (`pages.py:3214–3219`). Já há `st.markdown("---")` antes do bloco de expanders (3215). Inserir `st.markdown("#### Detalhamento territorial")` logo após esse `---`, antes do 1º `with st.expander("Analise Territorial"...)`. 1 linha, sem teste afetado.

### F2-F — Botão de download do relatório de ponto NO TOPO da seção do mapa — NOVO, APROVADO
12. **`render_relatorio_pontual_censitario`** (`pages.py:2529–2668`). Hoje `render_downloads_relatorio_censitario(st, result, mapas, ...)` é a ÚLTIMA chamada da função (linha 2660), após gerar mapas, KPIs, captions, imagens e a tabela de setores. Mover essa chamada para o TOPO do conteúdo do relatório, **logo após o bloco que computa `mapas` + `residual`** e ANTES das 4 imagens `st.image` (atualmente linhas 2621–2640).
    - Ordem-alvo dentro da função: (a) guards/`return` antecipados (search_pin/loader/context/setores) — INALTERADOS; (b) `result = analisar_ponto_censitario_setores(...)`; (c) `with st.spinner(...): mapas = render_mapas_censitarios_combinados(...)`; (d) computar o dict `residual` (lookup `lookup_hex_by_coord`) — mover este bloco (hoje 2645–2658) para ANTES dos downloads se necessário, pois `render_downloads_...` recebe `residual=`; (e) **`render_downloads_relatorio_censitario(st, result, mapas, filename_prefix=..., residual=residual, ultra_dir=Path("data/ultra"), template="classico")`** ← agora no TOPO; (f) `st.markdown("**Ponto analisado:**...")` + KPIs + captions + as 4 `st.image` + tabela de setores.
    - GUARDRAIL: só REPOSICIONAR a chamada de UI. NÃO alterar `censo_report.py`/`censo_map.py` nem a lógica de geração (mapas/result/CSV/PDF idênticos). A condição de só aparecer quando há relatório/ponto é PRESERVADA pelos `return` antecipados (sem `search_pin`/loader/context/setores a função retorna antes; logo o botão só renderiza quando há relatório válido).
    - **TESTES**: os testes do relatório (`test_render_relatorio_pontual_censitario_sem_coordenada_exibe_info` 4316, `_sem_base_setorial_exibe_warning` 4330+) cobrem só os caminhos de `return` antecipado (sem ponto/sem base) e não chegam a renderizar downloads — não afetados pela reordenação interna do caminho feliz. Se algum teste do caminho feliz assertar ORDEM relativa de chamadas de `st.image` vs `download_button`, atualizar a expectativa de ordem; caso contrário, sem mudança de teste.

### F2-G — Sidebar SEMPRE aberta no load/reload — NOVO, APROVADO
13. **`st.set_page_config`** (`streamlit_app.py:178–182`) já usa `initial_sidebar_state="expanded"`. Investigação: `expanded` é o estado inicial em cada carga/reload do script; o Streamlit NÃO força reabertura se o usuário colapsar manualmente na mesma sessão, mas em todo LOAD/RELOAD (rerun completo) o estado inicial é `expanded`. Não há flag pública mais forte que `"expanded"` sem hack frágil.
    - **Abordagem de MENOR risco (recomendada):** (a) MANTER `initial_sidebar_state="expanded"`; (b) reforço offline opcional via CSS injetado em `inject_styles()` (`pages.py`) que garante a sidebar visível/com largura mínima no load — CSS puro, sem API ao vivo, sem JS de auto-clique (frágil e quebra entre versões do Streamlit). Se o Builder optar por não adicionar CSS, registrar que `"expanded"` já é o máximo viável robusto.
    - **Critério de aceite (verificável):** `st.set_page_config(... initial_sidebar_state="expanded")` presente; se CSS adicionado, ele NÃO usa rede/JS de auto-clique e o app importa sem erro. Não há teste novo obrigatório (config é efeito de runtime do Streamlit); cobrir por `python -c "import streamlit_app"` sem exceção.
    - GUARDRAIL: offline preservado; nenhuma dependência de API ao vivo; nada de `streamlit-javascript`/componentes externos.

### F2-H — Seletor de abas SEMPRE no topo do corpo principal — NOVO, APROVADO
14. **`main()`** (`streamlit_app.py:496–529`). Hoje, ANTES de `render_tab_selector` (linha 529), são renderizados no corpo: a `st.caption("Recorte atual: ...")` (508–512) e o card de busca `render_hex_search_result(...)` (514–520, condicional a `search_pin`). O usuário quer o seletor de abas ACIMA de qualquer conteúdo condicional do corpo.
    - **Mudança:** mover a chamada `active_tab = render_tab_selector(DASHBOARD_TAB_LABELS)` para ANTES da `st.caption("Recorte atual...")` e de `render_hex_search_result`. Ordem-alvo em `main()` após `filtered_df` estar pronto e `if filtered_df.empty: ... return` resolvido: (1) `render_tab_selector` → `active_tab`; (2) `st.caption("Recorte atual...")`; (3) demais conteúdos por aba.
    - ATENÇÃO ao gate de summaries: o bloco `if active_tab in (...): city_summary=...; uf_summary=...` (531–533) depende de `active_tab` e deve permanecer DEPOIS do seletor (já é o caso). Garantir que mover o seletor para cima não rode `build_city_summary` antes do `if filtered_df.empty: return` — manter o seletor APÓS o guard de `filtered_df.empty` (linha 522–524) para não computar summaries num frame vazio.
    - **Interação com F2-I:** ver item 15 (a `st.caption` de recorte pode ficar logo após o seletor; o card de busca vai para depois do seletor, conforme F2-I).
    - **TESTE**: `test_main_renderiza_apenas_a_aba_ativa` mocka `render_tab_selector`, `streamlit.caption` e `streamlit.markdown` — a reordenação não quebra os asserts (que checam qual `render_*` foi chamado). Safe, desde que o dispatch continue correto.

### F2-I — Info da coordenada pesquisada não pode ofuscar o seletor — NOVO, APROVADO
15. **`render_hex_search_result`** chamada em `main()` (`streamlit_app.py:514–520`), HOJE renderizada ACIMA do seletor de abas (529). Esse card emite `st.markdown("---")` + `#### Hexagono pesquisado para ...` + status (ver `pages.py:571–...`), empurrando o seletor para baixo quando há busca.
    - **Mudança:** REPOSICIONAR a chamada `if search_pin is not None: render_hex_search_result(...)` para DEPOIS de `active_tab = render_tab_selector(...)` (consistente com F2-H). Opção preferida: renderizar o card logo após o seletor e a `st.caption("Recorte atual...")`, mantendo-o no corpo (não dentro de uma aba específica) para preservar 100% a funcionalidade (continua acessível em qualquer aba). Funcionalidade idêntica — só muda a posição vertical: seletor primeiro, depois a info da coordenada.
    - Mantém `search_pin`/`_search_result`/`search_hex_id` computados ANTES (continuam em 492–494, pois alimentam filtros e os `render_*` das abas). Só a CHAMADA de render do CARD muda de lugar.
    - **TESTE**: nenhum teste assevera a posição vertical do card vs seletor; `render_hex_search_result` não é mockado em `test_main_*` (não está na lista de mocks de 4287–4305). Verificar: se `main()` chamar `render_hex_search_result` com `search_pin=None` (mock `render_coord_search_sidebar` retorna `None`), o branch não dispara — Safe. Caso o Builder note um teste que cubra ordem, atualizar a expectativa.

---

## Fatiamento final
- **NÚCLEO (entregar):** F1-A (3 tabelas), F1-B (KPIs), F1-C (constante), F2-A (REORDER+labels+dispatch+5 testes), F2-B (3 mensagens), F2-C (expander sidebar), F2-D (1 heading), **F2-F (download no topo), F2-G (sidebar aberta), F2-H (seletor no topo), F2-I (info da coord após o seletor)**.
- **SEM AÇÃO (fechado com evidência):** F1-D.
- **FUTURO (fora deste ciclo):** F2-E (hero header contextual com UF).

## Arquivos afetados
- `src/motor_expansao/dashboard/pages.py` — F1-A, F1-B, F1-C, F2-A (`DASHBOARD_TAB_LABELS`), F2-B, F2-C, F2-D, F2-F (reposição interna em `render_relatorio_pontual_censitario`), F2-G (CSS opcional em `inject_styles`).
- `streamlit_app.py` — F2-A (dispatch `main()` em lockstep), F2-G (`set_page_config` já está em "expanded"; só confirmar), F2-H (mover `render_tab_selector` para o topo do corpo), F2-I (mover `render_hex_search_result` para depois do seletor).
- `tests/integration/test_streamlit_app.py` — F2-A (5 testes: `test_render_tab_selector_e_exportado`, `_retorna_aba_ativa`, `_fallback_quando_desmarcado`, `test_main_renderiza_apenas_a_aba_ativa`, `test_render_viabilidade_ponto_aba_no_dashboard_tab_labels`) + F1-A Domínio (1 teste: `test_render_expansao_dominio_exibe_consumo_quando_colunas_presentes` → `rendered_dfs[-1]`).
- `src/motor_expansao/dashboard/constants.py` — NÃO precisa mudar (`DASHBOARD_TAB_LABELS` vive em `pages.py`). Confirmar; não tocar.
- `src/motor_expansao/dashboard/utils.py` — NÃO tocar (F1-D sem ação).
- `src/motor_expansao/dashboard/censo_report.py`, `censo_map.py`, `censo_point.py` — NÃO tocar (F2-F só reposiciona a CHAMADA de UI).

## Dependências
- F2-A é ATÔMICO: `DASHBOARD_TAB_LABELS` + dispatch em `main()` + 5 testes — não fazer parcial (label sem dispatch = aba em branco silenciosa).
- F2-H e F2-I são acoplados (ambos reordenam o corpo de `main()` em torno de `render_tab_selector`): aplicar juntos.
- F1-A Domínio depende de trocar `rendered_dfs[0]`→`rendered_dfs[-1]` no teste de consumo.
- F2-F depende de mover o bloco `residual` para antes da chamada de downloads (a chamada usa `residual=`).

## Riscos técnicos
- **F2-A silencioso**: labels sem `main()` atualizado = aba em branco; só `test_main_renderiza_apenas_a_aba_ativa` pega. Mitigar: Builder atualiza dispatch + roda esse teste. Acentos: garantir igualdade byte-a-byte entre `DASHBOARD_TAB_LABELS` e as comparações em `main()` (preferir indexar `DASHBOARD_TAB_LABELS[i]`).
- **F2-H/F2-I**: mover o seletor para cima NÃO pode disparar `build_city_summary`/`build_uf_summary` antes do guard `if filtered_df.empty: return`; manter o seletor após esse guard.
- **F2-G**: evitar JS de auto-clique (frágil entre versões do Streamlit e exigiria componente externo/rede). Ficar em `initial_sidebar_state="expanded"` + CSS puro opcional.
- **F1-A expander**: 2º `st.dataframe` aumenta payload de render do frame COMPLETO já calculado — sem novo cálculo de score (READ-ONLY preservado); sem impacto no Bloco 6.
- **Acentos UTF-8** nos labels/dispatch: preservar encoding do arquivo.

## Critérios de aceite finais (verificáveis)
1. Carteira/Plano/Domínio: tabela primária com o set aprovado (≤12 cols); secundárias acessíveis via `st.expander(expanded=False)`; ZERO coluna removida do DataFrame.
2. `_render_analise_pontual_multihex` ≤9 KPIs em ≤2 linhas; `render_analise_pontual` simples ≤8 KPIs + 1 caption de consumo.
3. `grep "centroide H3 res-7" pages.py` retorna apenas a definição de `_CENTROID_DISCLAIMER` (1 linha lógica).
4. `DASHBOARD_TAB_LABELS == ["Mapa","Executivo","Expansão de Domínio","Carteira e Plano","Viabilidade"]`; "Mapa" é a 1ª aba e o default ao carregar; `main()` roteia cada aba ao `render_*` correto; 5 testes de label atualizados e verdes.
5. Empty-states das 3 tabelas sem `python -m jobs`/`jobs/pipelines` visível; mensagem de produto em 1 frase.
6. 5 controles avançados em `st.sidebar.expander("Filtros avancados", expanded=False)`; `render_uf_selectbox` continua o 1º elemento da sidebar; assinatura/return de `render_sidebar_filters` inalterados.
7. Heading "#### Detalhamento territorial" presente antes dos expanders do Mapa Territorial.
8. Botão de download do Relatório Pontual Censitário renderizado no TOPO da seção (antes das 4 imagens), só quando há ponto/relatório; `censo_report.py`/`censo_map.py` INTOCADOS (mesmos bytes de PDF/CSV).
9. `initial_sidebar_state="expanded"` mantido; eventual reforço de F2-G é CSS puro offline, sem JS/rede; app importa sem erro.
10. `render_tab_selector` é renderizado ACIMA da `st.caption("Recorte atual...")` e do card `render_hex_search_result` no corpo de `main()`; info da coordenada pesquisada aparece DEPOIS do seletor.
11. `pytest -q` com ≤3 falhas pré-existentes documentadas (csv_path1-223, csv_path2-472, test_parquet_final_respeita_guardrails_do_piloto).
12. `ruff` limpo; `mypy` sem novos erros; diff não toca `config.py`, `pipelines/m1/`, scores, artefatos, deploy, `censo_*` (lógica), builders de mapa (Bloco 6), lazy-load (Bloco 4).

## Validações obrigatórias (comandos exatos)
- `python -m pytest -q tests/integration/test_streamlit_app.py` → 0 falhas novas.
- `python -m pytest -q` → ≤3 falhas, todas na dívida operacional herdada (csv_path1-223, csv_path2-472, test_parquet_final_respeita_guardrails_do_piloto).
- `ruff check src/motor_expansao/dashboard/pages.py streamlit_app.py tests/integration/test_streamlit_app.py` → 0 erros.
- `mypy src/motor_expansao/dashboard/pages.py streamlit_app.py` → 0 erros novos.
- `python -c "import streamlit_app"` → import sem exceção.

## Fora de escopo
- `score_priorizacao`, `hex_score_estrutural`, pesos M1, artefatos oficiais — ZERO toque.
- Builders de mapa / `components.py` (Bloco 6), lazy-load por UF (Bloco 4), render lazy de abas (Bloco 5) — INTOCADOS.
- `censo_map.py`, `censo_report.py`, `censo_point.py` (lógica de geração), `config.py`, `pipelines/m1`.
- F2-E (recorte futuro). F1-D (sem ação). Warning de "modo dominio indisponivel" em `render_mapa_territorial` (~3089) — não é empty-state de tabela.
- Redesenho de grid/proporções, ag-grid, animações, novas colunas derivadas, JS de auto-clique na sidebar.
- Paths pré-sujos NÃO commitar: `data/outputs/setores_censitarios_2022_geo/_metadata.json`, `data/reports/relatorio_pontual_censitario_base_geo.md`.

## Alerta de aprovação
GATE APROVADO COM AJUSTES por Felipe/Vini. Não há novo gate humano antes do Builder. As decisões de produto (F1-A sets, F2-A reorder+labels, F2-C expander) e os 4 ajustes novos (F2-F/G/H/I) estão pré-aprovados; o humano revisa as colunas/labels antes do merge.

## Guardrails ativos
- §2: offline preservado; sem dependência de API ao vivo; CSV sep=";" encoding="utf-8-sig".
- §4 (Blocos 4/5/6): carga lazy por UF, render lazy de abas, fonte de mapa enxuta INTOCADOS. INVARIANTE: `render_uf_selectbox` é o 1º elemento da sidebar.
- §5 guardrail permanente: visualizações não recalculam `score_priorizacao`, `hex_score_estrutural`, carteira, plano ou artefatos oficiais do M1.
- DEC-005 emenda: `censo_*` só pode ser ESTENDIDO com params opcionais pela API; neste ciclo NÃO se altera `censo_*` (só reposicionar a chamada de UI no dashboard).
- Loop guard: diff não pode tocar `config.py`, `pipelines/m1/`, `*scoring*`, artefatos M1, `deploy/`, `Dockerfile.*`, compose, Caddy, authelia, `.env`, `secrets/`, CI.
- Paths pré-sujos NÃO commitar.
