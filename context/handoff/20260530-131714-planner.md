# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Approver (aprovação humana) — depois Builder

## Entendimento da tarefa
Gerar `data/outputs/_manifest.json` como passo FINAL ISOLADO de `fase1_bi_exports.main()` registrando a proveniência (vintage IBGE, sha256 dos bytes do `Ultra.csv`, commit do código, timestamp UTC e os parâmetros canônicos lidos de `settings`/`PESOS_HEX_SCORE_ESTRUTURAL`), expô-la read-only no rodapé do dashboard e cobrir com teste de schema — sem tocar um único byte dos artefatos M1.

## Plano técnico

### A) Novo módulo isolado `src/motor_expansao/pipelines/m1/provenance.py` (NOVO)
DECISÃO FIXADA: criar módulo próprio (recomendado pela current_task). Justificativa: mantém o cálculo do manifesto desacoplado do cálculo/escrita do score, dá função pura testável sem rodar o pipeline inteiro, e não polui `fase1_bi_exports.py` com I/O de hash/git. O módulo NÃO importa nada de scoring nem reescreve parquets.

1. Cabeçalho do módulo: `from __future__ import annotations`; imports padrão `hashlib`, `json`, `subprocess`, `datetime` (usar `datetime.now(timezone.utc)`); `from pathlib import Path`; `from typing import Any`. Fonte canônica: `from motor_expansao.config import settings` e `from motor_expansao.core.constants import PESOS_HEX_SCORE_ESTRUTURAL`.

2. Constantes do módulo:
   - `MANIFEST_PATH = Path("data/outputs/_manifest.json")` (irmão de `DASHBOARD_PATH = data/outputs/hexagonos_brasil_dashboard.parquet`). Reusar caminho relativo idêntico ao padrão dos demais `*_PATH` de `fase1_bi_exports.py`.
   - `ULTRA_CSV_PATH = Path("data/ultra/Ultra.csv")` (mesmo caminho relativo que `ULTRA_PATH` resolve em `streamlit_app.py` linha 185).
   - `IBGE_VINTAGE = "censo_2022"` (mesma string usada como default `data_referencia_ibge` em `fase1_bi_exports.py` ~L300 e `hex_enrichment.py` L109/L605; constante local para não inventar valor novo).
   - `SCHEMA_VERSION = 1` (DECISÃO FIXADA: incluir `schema_version` para versionar o manifesto; inteiro).

3. `def _sha256_file(path: Path) -> str | None:` — abre em modo BINÁRIO (`path.read_bytes()` ou leitura em chunks) e retorna `hashlib.sha256(...).hexdigest()`. COMPORTAMENTO DEFINIDO: se `not path.exists()` retorna `None`. Hasheia BYTES BRUTOS — independe de encoding (correto para o legado `Ultra.csv` latin-1 com 1 linha de metadado). NÃO usar `pd.read_csv`.

4. `def _git_commit() -> str | None:` — executa `git rev-parse HEAD` via `subprocess.run([...], capture_output=True, text=True, cwd=<repo_root>)`. COMPORTAMENTO DEFINIDO: retorna o hash `.strip()` quando `returncode == 0` e a saída casa `^[0-9a-f]{7,40}$`; caso contrário (git ausente, não-repo, erro, `FileNotFoundError`) retorna `None`. Envolver em `try/except (OSError, subprocess.SubprocessError)`. cwd = `Path(__file__).resolve().parents[?]` apontando à raiz do repo (validar profundidade: `src/motor_expansao/pipelines/m1/provenance.py` → `parents[4]` é a raiz). Não falhar nunca o pipeline por causa do git.

5. `def build_manifest(*, ultra_csv_path: Path = ULTRA_CSV_PATH, ibge_vintage: str = IBGE_VINTAGE) -> dict[str, Any]:` — função PURA (sem escrita em disco), retorna o dict do manifesto:
   ```python
   {
       "schema_version": SCHEMA_VERSION,            # int 1
       "ibge_vintage": ibge_vintage,                # "censo_2022"
       "ultra_csv_sha256": _sha256_file(ultra_csv_path),   # str | None
       "code_commit": _git_commit(),                # str | None
       "generated_at": datetime.now(timezone.utc).isoformat(),  # ISO 8601 UTC
       "h3_resolution": settings.H3_RESOLUTION,     # 7
       "pesos": {
           "renda": PESOS_HEX_SCORE_ESTRUTURAL["renda_per_capita"],  # 0.40
           "pop": PESOS_HEX_SCORE_ESTRUTURAL["populacao_proxy"],     # 0.60
       },
       "dist_min_ultra_km": settings.DIST_MIN_ULTRA_KM,   # 1.0
       "renda_min": settings.RENDA_MIN,             # 4500.0
   }
   ```
   Mapeamento canônico fixado: `renda ← PESOS_HEX_SCORE_ESTRUTURAL["renda_per_capita"]`, `pop ← PESOS_HEX_SCORE_ESTRUTURAL["populacao_proxy"]`. NUNCA hardcodar 0.40/0.60 — ler da constante. NUNCA redefinir os settings.

6. `def write_manifest(path: Path = MANIFEST_PATH, *, ultra_csv_path: Path = ULTRA_CSV_PATH, ibge_vintage: str = IBGE_VINTAGE) -> Path:` — chama `build_manifest(...)`, garante o diretório-pai (`path.parent.mkdir(parents=True, exist_ok=True)`), escreve com `path.write_text(json.dumps(manifest, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")`. JSON em UTF-8 puro (NÃO utf-8-sig, NÃO CSV). Retorna `path`. Esta função só LÊ os parâmetros/sha e ESCREVE o JSON novo; jamais reescreve qualquer parquet/CSV M1.

### B) Acoplar o passo final em `fase1_bi_exports.py`
7. Adicionar import no topo: `from motor_expansao.pipelines.m1.provenance import write_manifest` (após os imports existentes do pacote, ~L24).
8. Em `main()` (atual L630-647), APÓS o bloco `if HYBRID_PATH.exists(): ... else: ...` (ou seja, depois de TODOS os artefatos oficiais e do enriquecido já escritos em disco — `generate_fase1_bi_artifacts()` já chamou `write_outputs`), inserir como ÚLTIMO passo:
   ```python
   manifest_path = write_manifest()
   print(f"Manifesto de proveniencia gerado: {manifest_path}")
   ```
   O passo NÃO entra em `generate_fase1_bi_artifacts()` nem em `write_outputs()` — fica isolado em `main()`, garantindo que o manifesto só LÊ artefatos já materializados. NÃO alterar nenhuma linha de `build_dashboard_dataset`, `write_outputs`, `materialize_enriched_dashboard` ou qualquer função de score.

### C) Exibição read-only no dashboard
9. DECISÃO FIXADA: criar helper de render em `src/motor_expansao/dashboard/components.py` (segue o idiom dos `render_*_legend`), e chamá-lo no fim de `main()` em `streamlit_app.py`. Mantém `streamlit_app.py` enxuto e o helper testável/importável.
10. Em `components.py`, adicionar:
    ```python
    def render_manifest_footer(manifest_path: Path) -> None:
        """Rodape read-only com a proveniencia dos outputs (BLK-OPS-03)."""
        if not manifest_path.exists():
            return  # COMPORTAMENTO DEFINIDO: ausencia nao quebra o app, nao renderiza
        try:
            data = json.loads(manifest_path.read_text(encoding="utf-8"))
        except (OSError, ValueError):
            return
        with st.expander("Proveniencia dos outputs (read-only)", expanded=False):
            st.caption(
                f"IBGE: {data.get('ibge_vintage')} | H3 res {data.get('h3_resolution')} | "
                f"pesos renda {data.get('pesos', {}).get('renda')} / pop {data.get('pesos', {}).get('pop')} | "
                f"dist_min_ultra_km {data.get('dist_min_ultra_km')} | renda_min {data.get('renda_min')}"
            )
            st.caption(
                f"commit {data.get('code_commit')} | gerado em {data.get('generated_at')} | "
                f"Ultra.csv sha256 {data.get('ultra_csv_sha256')}"
            )
    ```
    Garantir `import json` e `from pathlib import Path` no topo de `components.py` (verificar se já existem; adicionar só o que faltar). Read-only: apenas lê o JSON e exibe; NÃO recalcula nada.
11. Em `streamlit_app.py`: definir constante de caminho `MANIFEST_PATH = Path(__file__).resolve().parent / "data" / "outputs" / "_manifest.json"` junto às demais (~L180-200, ao lado de `DATASET_PATH`/`ESTRUTURAL_PATH`). Importar `render_manifest_footer` na lista de imports de `dashboard.components` (verificar de onde `components` é importado no app; se não há bloco de import de components, importar via `from motor_expansao.dashboard.components import render_manifest_footer`). Como ÚLTIMO statement de `main()` (após o `elif active_tab == "Carteira e Plano": ...` que termina ~L556), chamar `render_manifest_footer(MANIFEST_PATH)`. Está fora dos branches de aba, então roda em qualquer aba; é read-only e idempotente.

### D) Teste `tests/unit/test_manifest.py` (NOVO)
12. Estilo casado ao projeto: `from __future__ import annotations`, `pytest`, `tmp_path`, imports diretos do módulo. Importar `from motor_expansao.pipelines.m1 import provenance` e/ou `from motor_expansao.pipelines.m1.provenance import build_manifest, write_manifest`. Casos:
    - `test_build_manifest_has_all_required_keys`: `m = build_manifest()`; assert que o set de chaves contém exatamente `{"schema_version","ibge_vintage","ultra_csv_sha256","code_commit","generated_at","h3_resolution","pesos","dist_min_ultra_km","renda_min"}`; tipos: `schema_version` int, `ibge_vintage` str, `h3_resolution` int, `pesos` dict com chaves `renda`/`pop`, `dist_min_ultra_km`/`renda_min` float.
    - `test_canonical_values`: `h3_resolution == 7`; `pesos == {"renda": 0.40, "pop": 0.60}`; `dist_min_ultra_km == 1.0`; `renda_min == 4500.0`; `ibge_vintage == "censo_2022"`. (Ler de settings garante consistência; o teste valida que o mapeamento não inverteu renda/pop.)
    - `test_sha256_present_with_file` (usa `tmp_path`): cria um arquivo de bytes conhecidos; `build_manifest(ultra_csv_path=<tmp>)` → `ultra_csv_sha256` igual ao `hashlib.sha256(bytes).hexdigest()` esperado; valida que hasheia BYTES (escrever bytes não-UTF8/latin-1 e conferir que não levanta).
    - `test_sha256_none_when_file_absent`: `build_manifest(ultra_csv_path=tmp_path / "nao_existe.csv")` → `ultra_csv_sha256 is None`.
    - `test_volatile_fields_present_and_typed`: `generated_at` é str e parseável por `datetime.fromisoformat(...)` (e termina com offset/UTC); `code_commit` é `None` OU str casando `^[0-9a-f]{7,40}$`. NÃO comparar valor literal.
    - `test_write_manifest_creates_utf8_json` (usa `tmp_path`): `out = write_manifest(tmp_path / "_manifest.json", ultra_csv_path=tmp_path/"u.csv")`; `out.exists()`; reler com `json.loads(out.read_text(encoding="utf-8"))` e conferir chaves; conferir que o arquivo NÃO tem BOM utf-8-sig (`out.read_bytes()[:3] != b"\xef\xbb\xbf"`).

## Arquivos afetados
- `src/motor_expansao/pipelines/m1/provenance.py` (NOVO)
- `src/motor_expansao/pipelines/m1/fase1_bi_exports.py` (import + 2 linhas no fim de `main()`)
- `src/motor_expansao/dashboard/components.py` (novo `render_manifest_footer` + imports se faltarem)
- `streamlit_app.py` (constante `MANIFEST_PATH`, import do helper, 1 chamada no fim de `main()`)
- `tests/unit/test_manifest.py` (NOVO)
- Processo: `tasks/current_task.md`, `tasks/completed.md`, `context/handoff.md`, `context/handoff/20260530-131714-planner.md`

## Dependências
- Técnicas: `hashlib`, `json`, `subprocess`, `datetime`, `pathlib` (stdlib — sem nova dependência de terceiros). `settings` (`config.py`) e `PESOS_HEX_SCORE_ESTRUTURAL` (`core/constants.py`) como fontes canônicas. `streamlit` (`st.expander`/`st.caption`) já é dependência do dashboard.
- Sem dependência de API ao vivo (respeita guardrail do dashboard de produção).

## Riscos técnicos
- Acoplamento ao score → mitigação: passo final isolado em `main()`, módulo `provenance.py` separado; não importa scoring, não entra em `write_outputs`.
- Mutação acidental de parquet → mitigação: `write_manifest` só escreve `_manifest.json`; nenhuma chamada `to_parquet/to_csv` de artefato M1. QA confirma por sha256 pré/pós.
- Hardcode divergente de pesos → mitigação: ler `PESOS_HEX_SCORE_ESTRUTURAL` e mapear; teste `test_canonical_values` trava 0.40/0.60.
- `Ultra.csv` ausente em CI (gitignored) → mitigação: `ultra_csv_sha256 = None` definido e testado.
- Git indisponível/não-repo → mitigação: `code_commit = None`; pipeline nunca falha por git.
- Não-determinismo (`generated_at`/`code_commit`) quebrando teste → mitigação: teste valida presença/tipo/formato, não valor literal.
- Encoding em Windows → mitigação: JSON gravado em `encoding="utf-8"` (sem BOM), `ensure_ascii=False`; teste confere ausência de BOM. CSVs do projeto continuam `utf-8-sig`; o JSON NÃO usa utf-8-sig.
- Profundidade do `parents[N]` para o cwd do git → mitigação: o Builder deve confirmar o nível correto (`src/motor_expansao/pipelines/m1/` → raiz = `parents[4]` a partir do arquivo) e, em fallback, deixar `cwd=None` (git resolve pelo diretório corrente) com try/except.

## Critérios de aceite finais
- `data/outputs/_manifest.json` gerado ao final do pipeline com as 9 chaves (`schema_version`, `ibge_vintage`, `ultra_csv_sha256`, `code_commit`, `generated_at`, `h3_resolution`, `pesos`, `dist_min_ultra_km`, `renda_min`), valores canônicos corretos.
- Dashboard exibe a proveniência read-only (expander no fim de `main()`); ausência do JSON não quebra o app.
- `pytest -q tests/unit/test_manifest.py` → 0 falhas.
- Suíte completa permanece `532 passed, 1 skipped`.
- PROVA DE NÃO-MUTAÇÃO: sha256 idêntico pré/pós dos parquets M1.
- `python -c "import streamlit_app"` sem erro.

## Validações obrigatórias
- Comando: `pytest -q tests/unit/test_manifest.py` / Critério mínimo: 0 falhas (presença + schema + valores + sha presente/ausente + voláteis).
- Comando: `pytest -q` / Critério mínimo: baseline `532 passed, 1 skipped` preservado, nada quebrado.
- Comando (prova de não-mutação): hashear ANTES de rodar o pipeline e DEPOIS — `data/outputs/hexagonos_brasil_dashboard.parquet`, `data/outputs/hexagonos_mapa_sample.parquet`, `data/staging/brasil_estrutural.parquet`, `data/staging/brasil_priorizados.parquet`, `data/staging/hexagonos_brasil_oportunidades.parquet` (ex.: `Get-FileHash -Algorithm SHA256` no Windows ou `sha256sum`). / Critério mínimo: TODOS os hashes idênticos pré/pós; qualquer divergência → REPROVAR.
- Comando: `python -c "import streamlit_app"` / Critério mínimo: importa sem exceção.
- Comando: `mypy src/` / Critério mínimo: o módulo novo `provenance.py` passa limpo (sem novos erros).
- Comando: `ruff check src/motor_expansao/pipelines/m1/provenance.py tests/unit/test_manifest.py streamlit_app.py src/motor_expansao/dashboard/components.py` / Critério mínimo: sem violações nos arquivos tocados.

## Fora de escopo
- Alterar qualquer valor/coluna dentro dos parquets/CSVs/resumo executivo do M1.
- Recalcular `score_priorizacao`, `hex_score_estrutural`, carteira, plano curto prazo, plano de domínio ou artefatos oficiais.
- Mudar pesos, parâmetros canônicos ou regras de negócio — o manifesto apenas LÊ e REGISTRA.
- Adicionar dependência de API ao vivo no dashboard.
- Tocar outras camadas/blocos.

## Alerta de aprovação
ATENÇÃO: ESTA TAREFA É CRÍTICA — TOCA O PIPELINE QUE GERA OS ARTEFATOS OFICIAIS DO M1 (`fase1_bi_exports.py`) E REGISTRA OS PESOS DO SCORE (`{renda:0.40, pop:0.60}`). A EXECUÇÃO PELO BUILDER EXIGE APROVAÇÃO HUMANA EXPLÍCITA ANTES DE INICIAR. O MANIFESTO APENAS LÊ OS PESOS/PARÂMETROS CANÔNICOS DAS FONTES (`settings`, `PESOS_HEX_SCORE_ESTRUTURAL`); NÃO PODE REDEFINI-LOS NEM ALTERAR QUALQUER SCORE OU ARTEFATO.

## Guardrails ativos
- (CLAUDE.md §1) Nenhuma trilha paralela pode alterar o M1 sem aprovação explícita.
- (CLAUDE.md §3/§5) Guardrail permanente: visualizações/interações NÃO podem recalcular ou alterar `score_priorizacao`, `hex_score_estrutural`, carteira, plano curto prazo, plano de domínio ou artefatos oficiais do M1.
- (CLAUDE.md §2) `config.py`, `CLAUDE.md` e `PRD.md` são fontes canônicas; ler o repo real antes de editar.
- (CLAUDE.md §2) Toda mudança relevante entra com teste; nenhum PR sobe com CI quebrado.
- (CLAUDE.md §2) `data/ultra/Ultra.csv` é legado sep=";", encoding="latin-1", 1 linha de metadado — o sha256 hasheia os BYTES BRUTOS (modo binário), independe de encoding.
- (CLAUDE.md §2) JSON em UTF-8 puro; `utf-8-sig`/`sep=";"` é só para os CSVs do projeto, não para `_manifest.json`.
- NÃO-MUTAÇÃO POR HASH (específico do bloco): sha256 dos parquets M1 IDÊNTICO pré/pós; divergência → REPROVAR.
- MANIFESTO AO LADO, NUNCA DENTRO: `_manifest.json` é arquivo separado em `data/outputs/`; jamais campo/coluna dentro do conteúdo de scoring.
- Parâmetros canônicos imutáveis sem DEC: H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, pesos renda=0.40/pop=0.60 — o manifesto só LÊ e REGISTRA.
- Commit por path (current_task): NUNCA `git add -A`; nunca arrastar `PRD.md` ou edições não relacionadas. Branch isolado `ciclo/BLK-OPS-03`.
- Este ciclo NÃO altera a orquestração → dry-run 6.c NÃO dispara.
