# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA

## Bloco
BLK-REV-02 — Inventário arquitetural e mapa de dependências do app atual

## Status
IMPLEMENTADO (documento verificado)

## Arquivos criados
- docs/arquitetura_app_atual.md (445 linhas, 30.239 bytes / ~29,5 KB)

## Saída literal das verificações

### wc -l / ls
```
445 /repo/docs/arquitetura_app_atual.md
-rw-r--r-- 1 loop loop 30239 Jul  8 20:48 /repo/docs/arquitetura_app_atual.md
```

### seções presentes (17 headings "##", 8 de nível 2)
```
## 1. Sumário executivo
## 2. Modelo de rerun do Streamlit
## 3. Camadas da arquitetura
## 4. Grafo de dependências (interno)
## 5. Tamanho dos artefatos (disco e timing)
## 6. O que é cacheado vs. recomputado (tabela)
## 7. Pontos de acoplamento e fragilidades
## 8. Decisões arquiteturais relevantes
```

### loop_guard
```
GUARD OK: 0 caminho(s) alterado(s), nenhum proibido.
```

### git diff M1 (src/motor_expansao/pipelines/m1/ e config.py)
```
(vazio)
```

### git diff src/
```
Apenas src/motor_expansao/dimensionamento/config.py — diff CRLF→LF pré-existente
(warning do Git, nenhuma linha de conteúdo alterada; não pertence a este ciclo;
não é pipelines/m1/ nem config.py raiz).
```

## Guardrails verificados
- READ-ONLY M1: zero mudanças em src/motor_expansao/pipelines/m1/ e src/motor_expansao/config.py
- loop_guard: GUARD OK (0 caminhos alterados, nenhum proibido)
- Loop-safe: sem VPS, sem rede, sem PII
- Escreve só docs/ (commitado pelo Planner antes deste handoff)
- O único diff em src/ é CRLF pré-existente em dimensionamento/config.py — não deste ciclo

## Notas para o QA
- Confirmar que o documento cobre os 8 tópicos do escopo (todos presentes — ver seções acima)
- Confirmar que referências a funções/módulos estão corretas: o handoff do Planner registra
  3 correções factuais aplicadas (fragment em pages.py:4099 confirmado; MAP_POINT_LIMIT=35000/
  MAP_POINT_LIMIT_LARGE=18000 corretos; cache_resource vs cache_data corretos sem TTL)
- Documento é READ-ONLY sobre o M1: descreve arquitetura sem alterar código/score/artefatos
- loop_guard GUARD OK — nenhum arquivo proibido tocado
- Sem testes de unidade novos necessários (bloco de documentação pura, loop-safe)
- Sugestão de verificação do QA: fazer grep de algum nome de função crítica do doc
  (ex.: `render_mapa_pydeck_fragment`, `build_map_figure`, `_downsample_map_index`) em
  src/motor_expansao/dashboard/ para confirmar que as funções existem com esses nomes
