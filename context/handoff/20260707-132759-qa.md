# Handoff — QA/Quality Analyzer (re-verificação do follow-up)

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador: housekeeping move BLK-ACENTO-01 + commit por path). Opcional: Documentation.

## VEREDITO
APROVADO

## Justificativa
A ressalva média da rodada anterior foi RESOLVIDA: `build_faixa_comparison_figure` agora exibe os labels acentuados de faixa no eixo x/legenda via coluna de EXIBIÇÃO derivada (`FAIXA_LABELS`), com a MESMA cor por faixa preservada por `FAIXA_COLORS_POR_LABEL` (dict derivado) e a ORDEM categórica seguindo `FAIXA_ORDEM`; o valor bruto de `faixa_oportunidade`, `FAIXA_ORDEM` e `FAIXA_COLORS` ficam intocados. Escopo segue READ-ONLY M1, sem identificador acentuado, e a suíte completa passa exceto a falha pré-existente/ambiental da camada M2.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum (a ressalva anterior sobre o eixo x do gráfico de faixa foi corrigida e verificada).

## Melhorias opcionais
- Acentuar travessões/`Nao`/etc. remanescentes em comentários e docstrings (fora do escopo "texto voltado ao usuário", deixados de propósito) num passe futuro de higiene, se desejado.

## Testes faltantes
- nenhum. O follow-up adicionou `test_build_faixa_comparison_figure_eixo_e_cores_acentuados` (eixo/legenda com labels acentuados, nunca os slugs brutos; cor de cada faixa preservada: `FAIXA_COLORS_POR_LABEL[label] == FAIXA_COLORS[valor_bruto]`), somando ao `test_faixa_e_elegibilidade_labels_via_format_func` do D1/D1-bis.

## Riscos remanescentes
- xdist/execnet instável no Python 3.14 desta máquina (crash de worker em `pytest_sessionstart`, ANTES de coletar testes); gate rodado SERIAL (correto, sem `-p no:xdist`). Não é defeito do bloco.
- `FAIXA_COLORS_POR_LABEL` é derivado por comprehension; se dois valores brutos mapeassem para o MESMO label, uma cor colapsaria — hoje os 6 labels são únicos (sem risco); o teste novo cobre a preservação de cor.

## Saída literal das validações (re-executadas pelo QA)

```
$ git diff --stat
 context/handoff.md                         | 237 +++-------
 src/motor_expansao/dashboard/components.py | 183 ++++----
 src/motor_expansao/dashboard/constants.py  |  29 +-
 src/motor_expansao/dashboard/data.py       |   8 +-
 src/motor_expansao/dashboard/pages.py      | 668 +++++++++++++++--------------
 streamlit_app.py                           |   4 +-
 tasks/current_task.md                      | 101 ++---
 tests/integration/test_streamlit_app.py    | 132 +++++-
 8 files changed, 686 insertions(+), 676 deletions(-)
# + snapshots untracked em context/handoff/. Filtro por parquet/config.py/core/constants/
# pipelines/m1/censo_/relatorio_municipal/calcular_colunas = NONE (clean).
```

```
$ python -m ruff check .
All checks passed!
```

```
$ python -m mypy src
src\motor_expansao\dimensionamento\growth_api_client.py:28: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\ibge_censo.py:24: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\poi_enrichment.py:15: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\base_h3_brasil.py:31: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\geo.py:21: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\telegram_bot.py:23: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\service.py:223: error: Library stubs not installed for "requests"  [import-untyped]
Found 7 errors in 7 files (checked 102 source files)
# Mesmos 7 erros PRÉ-EXISTENTES/ambientais (types-requests ausente), todos em arquivos NÃO tocados.
# ZERO erro novo em dashboard/constants.py e dashboard/components.py (arquivos do follow-up).
```

```
$ python -c "import streamlit_app; print('import ok')"
import ok
```

```
$ python -m pytest -q "tests/integration/test_streamlit_app.py::test_build_faixa_comparison_figure_eixo_e_cores_acentuados" "tests/integration/test_streamlit_app.py::test_faixa_e_elegibilidade_labels_via_format_func"
2 passed, 11833 warnings in 1.72s
```

```
$ python -m pytest -n auto -q      # SUÍTE COMPLETA (tentativa paralela)
INTERNALERROR> ... execnet/gateway_base.py:571 ...
INTERNALERROR> EOFError: couldn't load message header, expected 9 bytes, got 0
# Crash de WORKER no setup (pytest_sessionstart), ANTES de qualquer teste -> instabilidade AMBIENTAL
# do xdist/execnet no Python 3.14, NÃO falha de teste. Fallback SERIAL abaixo (NO-BYPASS).
```

```
$ python -m pytest -q               # SUÍTE COMPLETA SERIAL (gate único)
1 failed, 1431 passed, 2 skipped, 217942 warnings in 360.61s (0:06:00)
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
# Passed 1430 -> 1431 (+1 = teste novo do gráfico). Única falha = FileNotFoundError em
# data/staging/unidade_territorio_retencao.parquet (parquet gitignored da camada M2 lifetime/,
# ausente no ambiente). PRÉ-EXISTENTE/ambiental (documentada no BLK-RELPON-04); bloco não toca
# lifetime/ -> não é regressão. Todos os testes de dashboard/streamlit passam.
```

```
$ python scripts/housekeeping_move_block.py BLK-ACENTO-01 --check
FALHA --check: stub ausente no backlog para BLK-ACENTO-01
# ESTADO PRÉ-MOVE CORRETO: helper RECONHECE o bloco (não é BlockNotFound); move é do ORQUESTRADOR
# no fechamento, DEPOIS deste veredito.
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock do caminho crítico. A suíte foi rodada SERIAL como fallback honesto ao crash ambiental do xdist (sem `-p no:xdist`). A única falha foi diagnosticada individualmente (FileNotFoundError de parquet M2 gitignored) e é pré-existente/ambiental, não do bloco.

## Conferência cruzada com log do Builder
Bate. Builder (follow-up): ruff limpo, mypy 7 pré-existentes de types-requests, import ok, `test_streamlit_app.py` serial verde com os 2 testes de faixa. QA confirmou: ruff limpo, mypy IDÊNTICO (7 pré-existentes, zero novo), import ok, 2 testes novos passam, e a SUÍTE COMPLETA serial (gate único) = 1431 passed / 2 skipped / 1 falha ambiental pré-existente. A receita do item 2.2c foi aplicada como descrita: `FAIXA_COLORS_POR_LABEL` derivado + `faixa_label` acentuado categórico ordenado por `FAIXA_ORDEM` + `category_orders`.

## Guardrails verificados
- score_priorizacao não alterado: sim (só camada de visualização; nenhuma fórmula/peso/artefato).
- hex_score_estrutural não alterado: sim.
- Artefatos M1 preservados: sim (nenhum parquet no diff; bloco não roda pipeline; filtro por out-of-scope = NONE).
- Parâmetros canônicos: preservados (H3_RESOLUTION=7, pesos renda=0.40/pop=0.60, RENDA_MIN=4500.0 em config.py/core intocados; `FAIXA_ORDEM` e `FAIXA_COLORS` com valores BRUTOS inalterados — só adição de `FAIXA_LABELS`/`HYBRID_ELIGIBILITY_LABELS`/`FAIXA_COLORS_POR_LABEL` DERIVADO).
- Correção da ressalva (item 2.2c) verificada por leitura do código (`components.py:2282-2319`): `faixa_oportunidade` bruto usado só no groupby/sort (origem intocada); `faixa_label` = `FAIXA_LABELS.get(f,f)` como `pd.Categorical(categories=_faixa_label_order, ordered=True)`; `x="faixa_label"`, `color="faixa_label"`, `color_discrete_map=FAIXA_COLORS_POR_LABEL`, `category_orders={"faixa_label": _faixa_label_order}` → label acentuado no eixo/legenda, MESMA cor por faixa, ORDEM = FAIXA_ORDEM.
- Identificadores intocados: sim (nenhum `key=`/`.st-key-*`/coluna/valor bruto/slug acentuado; a coluna acentuada `faixa_label` é DERIVADA só para o gráfico, não persiste no dado).
- Tipografia esperta: sweep Python nas linhas ADICIONADAS (constants + components + test) = 0 travessão/en-dash/bullet/seta/reticências/aspas curvas/©.
- Escopo respeitado: sim — diff só em dashboard/{constants,components,data,pages}.py + streamlit_app.py + tests/integration/test_streamlit_app.py + bookkeeping. ZERO em core/constants.py, censo_*, relatorio_municipal.py, pipelines/m1, config.py, calcular_colunas_mercado.py.
- Validações re-executadas pelo QA (sem bypass): ruff → ok; import → ok; mypy → 7 pré-existentes (zero novo); pytest serial → 1 falha ambiental pré-existente, 1431 passed; 2 testes novos de faixa → passed; housekeeping --check → estado pré-move correto.

## Decisão recomendada
Fechar ciclo — orquestrador faz o housekeeping move do BLK-ACENTO-01 (`scripts/housekeeping_move_block.py`) + commit por path. Ressalva anterior resolvida; sem pendências bloqueadoras. Revisão visual humana da UI segue recomendada (gráfico "Score médio por faixa de oportunidade" com eixo/legenda acentuados e cores por faixa inalteradas, filtro/legenda de faixa, Carteira e Plano, Expansão de Domínio).
