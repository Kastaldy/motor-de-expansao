# Handoff — QA

## Skill que gerou este handoff
QA (Quality Analyzer)

## Veredito
**APROVADO COM RESSALVAS**

O bloco BLK-SCORE-01 atende a spec da REVISAO 2 do Planner. Testes verdes, script roda fim a fim,
dataset gerado e correto e conforme o esquema, auditoria de rotulo bem-formada e sem PII, e o M1
permanece READ-ONLY. A unica ressalva relevante e uma imprecisao na contagem de testes do handoff
do Builder (ver Ressalvas) — sem impacto funcional.

## Validacoes executadas (resultados REAIS)
1. `python -m pytest -q tests/unit/test_validation_dataset.py` -> **21 passed** (0.19s).
   - `--collect-only`: **21 tests collected**. O Builder afirmou **23**; o numero REAL e **21**.
   - Todos usam SOMENTE fixtures sinteticas; os que dependem de h3 usam
     `pytest.importorskip("h3")`. Nenhum le parquet/xlsx real. Sem bypass do caminho critico.
2. `python -m pytest -q` (suite completa) -> **591 passed, 1 skipped, 9 warnings** (83s).
   - Bate exatamente com o numero do Builder (591/1).
   - Reconciliacao do "+23": a suite SEM o arquivo novo da **570 passed, 1 skipped**; 570 + 21 = 591.
     A baseline de 532 citada (2026-05-28) estava desatualizada (cresceu para 570 em ciclos
     intermediarios). Portanto NAO ha regressao; o delta vem so dos 21 testes novos.
3. `python -m analysis.build_validation_dataset` -> **exit 0**; grava os 2 artefatos.
   - `data/analysis/dataset_validacao.parquet` EXISTE (51.902 bytes).
   - `data/analysis/relatorio_auditoria_rotulo.md` EXISTE (1.893 bytes, 64 linhas, BEM-FORMADO).
   - Saida agregada (sem PII): ultra 53/54 hex_ok, skyfit 301/326, engcorpo 31/61; dominio presente (500 hexes).
4. Inspecao do dataset (read-only, sem despejo de PII):
   - 441 linhas / 31 colunas; `rede` = {ultra:54, skyfit:326, engcorpo:61}; entrada==linhas por rede.
   - `unidade_id` 0 duplicatas; `cod_municipio` dtype = string; `maturacao_status` == `maturacao_indisponivel` (441/441).
   - Ordem de colunas == `SCHEMA_COLUMNS` (inclui `hex_origem`/`hex_precisao`).
   - Coerencia de flags `score_*_disponivel`: 0 incoerencias nos 4 scores
     (nem flag True com nulo, nem flag False com valor). `hex_resolvido` coerente (0 casos hex nulo & True).
   - Disponibilidade de score (prioriz/setor/residual/dominio): 358/376/384/42. Dominio=42
     (cobertura parcial dos 500 hexes do plano, esperada).
   - alunos_recorrentes nulos: ultra=0, skyfit=28, engcorpo=3.
     alunos_medido: ultra/skyfit=True, engcorpo=False. rotulo_confiabilidade: ultra/skyfit=medido,
     engcorpo=estimado. alunos_origem: ultra=alunos_total, skyfit=Alunos EVO, engcorpo=Alunos Totais.
   - Cascata Skyfit (hex_origem): nome_exato=175, cidade_centroide=110, nome_fuzzy=16, nao_resolvido=25.
   - rotulo_nao_casado: engcorpo=30 (esperado; sem fuzzy para EngCorpo por design).
5. Auditoria de rotulo (criterio de aceite): `relatorio_auditoria_rotulo.md` traz, por rede, nulos
   de alunos, outliers (min/mediana/max), cobertura de hex por origem/precisao, scores disponiveis e
   nota de maturacao + fonte do dominio. Distingue Skyfit/Ultra (medido) de EngCorpo (estimado) via
   `rotulo_confiabilidade`. NAO contem PII (nenhum nome/endereco; so agregados). Bem-formado:
   1 bloco `hex_precisao` por rede (verificado: fonte tem 1 cabecalho de cada; report vivo = 62
   linhas com 3 blocos hex_precisao).
6. Guardrails READ-ONLY do M1 (git): tracked-modificados = `.gitignore`, `CLAUDE.md` (pre-existente,
   fora do ciclo), `context/handoff.md`, `tasks/current_task.md`. Untracked = `analysis/`,
   `tests/unit/test_validation_dataset.py`, snapshots de `context/handoff/`. NENHUM path do M1
   (`data/outputs/*`, `data/staging/*` oficiais, `src/`, `constants.py`, `scoring.py`,
   `hexagonos_*`, `brasil_*`, `carteira_*`, `plano_*`) alterado. Por leitura de codigo: o script so
   escreve em `data/analysis/` (`--out-parquet`/`--out-report`) e nao recalcula score.
   `git check-ignore`: ambos os artefatos ignorados (parquet via `*.parquet`; md via `data/analysis/`).
7. Housekeeping: `python scripts/housekeeping_move_block.py BLK-SCORE-01 --check` -> **exit 0**;
   helper reconhece o bloco completo em `tasks/backlog.md`. (Move real e do orquestrador no fechamento.)

## Conformidade com escopo
- Esquema, fontes Skyfit (somente `concorrentes/unidades_skyfit.csv` + cascata
  nome_exato -> fuzzy 0.84 -> cidade_centroide), colunas novas `hex_origem`/`hex_precisao`,
  `cod_municipio` string, dominio como fonte opcional, maturacao constante — TUDO conforme a
  REVISAO 2 do Planner (SUPERSEDE). Sem over-engineering aparente; escopo respeitado.
- Os 2 desvios relatados pelo Builder foram validados:
  (a) Caminho do dominio = `data/outputs/plano_expansao_dominio.parquet` (arquivo EXISTE; tratado
      como leitura/opcional; run real reporta "presente (500 hexes)"). ACEITAVEL.
  (b) EngCorpo cobertura de hex 31/61 com 30 nao-casados marcados via flag (sem fuzzy por design).
      Limitacao de cobertura CONHECIDA e marcada, nao descartada. NAO e bug. ACEITAVEL como ressalva.

## Guardrails verificados
- READ-ONLY do M1: OK (nenhum artefato/codigo M1 alterado; script so le M1 e grava em data/analysis/).
- `data/analysis/` gitignored: OK (parquet e md). Sem recalculo de score: OK.
- PII: dataset parquet contem `nome_unidade` (uso interno, gitignored) — OK; relatorio so agregados.

## Ressalvas
1. **Contagem de testes imprecisa no handoff do Builder:** ele afirma **23** testes novos; o real e
   **21** (confirmado por `--collect-only`). A suite completa (591/1) bate, e NAO ha regressao
   (570 baseline real do branch + 21). Recomendo o Builder corrigir o numero (ou, se quiser fechar
   em 23, adicionar 2 testes). Nao bloqueia a aprovacao.
2. EngCorpo: cobertura de hex/score baixa (31/61) e 28 nulos de alunos no Skyfit — ambos esperados
   e marcados; documentar para o backtest (BLK-SCORE-02) como limitacao de cobertura.

## Recomendacao final
APROVADO COM RESSALVAS. Entregavel funcional e conforme a spec; dataset e auditoria corretos;
M1 intacto. Corrigir a contagem de testes do handoff e seguir para fechamento.

## Proxima Skill recomendada
**Orquestrador** (fechamento do ciclo; aplicar housekeeping move e merge conforme a esteira).

## Snapshot deste veredito
`context/handoff/20260531-010148-qa.md` (copia identica deste handoff; append-only).
