# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-TP-02 — Validação: Demanda Revelada × Residual Fitness (relatório)

## O que foi feito

1. **Criado `src/motor_expansao/demanda_revelada/validacao.py`** com 7 funções públicas:
   - `carregar_par_validacao()` — inner join demanda × mercado (16.411 hexes), left join priorizados, anti-PII.
   - `calcular_spearman_ic()` — Spearman + IC 95% via Fisher analítico (N>10k) ou bootstrap (N≤10k).
   - `mapear_quadrantes()` — Q1..Q4 por mediana de `score_oportunidade_residual` e `membros`.
   - `calcular_divergencias()` — hexes Q1 fora do top-20%/UF e vice-versa.
   - `gerar_relatorio_validacao()` — Markdown 7 seções, escrito em `data/reports/demanda_revelada_validacao.md`.
   - `salvar_quadrantes_parquet()` — parquet zero-PII com 7 colunas canônicas.
   - `executar_validacao_completa()` — orquestra tudo, retorna dict com 6 chaves.

2. **Atualizado `src/motor_expansao/demanda_revelada/__init__.py`**: exporta `executar_validacao_completa` em `__all__`.

3. **Criado `tests/unit/test_demanda_revelada_validacao.py`** com 11 testes unitários (fixtures sintéticas em tmp_path, zero dados reais).

4. **Gerado `data/reports/demanda_revelada_validacao.md`** executando a análise real (N=16.411 hexes, dados locais disponíveis):
   - `membros` × `score_oportunidade_residual`: rho = **0.517**, IC [0.506, 0.529], p≈0 *** — confirma o sinal +0,52 da exploração.
   - `membros` × `oferta_efetiva_disponivel`: rho = **0.525**, IC [0.513, 0.536], p≈0 ***.
   - Nota: `score_oportunidade_residual` tem mediana=0.0 no join → Q1 (8.352) + Q2 (8.059) presentes; Q3/Q4 ausentes pois todos os hexes têm residual ≥ 0 e a mediana do limiar é 0.0. Comportamento correto, documentado no relatório.
   - Divergências: 1.997 hexes Q1 fora do top-20%/UF; 3.500 hexes top-20%/UF fora de Q1.

## Arquivos alterados

- `src/motor_expansao/demanda_revelada/validacao.py` — NOVO (módulo de validação, 7 funções públicas)
- `src/motor_expansao/demanda_revelada/__init__.py` — MODIFICADO (exporta `executar_validacao_completa`)
- `tests/unit/test_demanda_revelada_validacao.py` — NOVO (11 testes unitários)
- `data/reports/demanda_revelada_validacao.md` — NOVO (relatório gerado com dados reais)
- `tasks/current_task.md` — MODIFICADO (Skill: Builder, Próxima: QA)
- `context/handoff.md` — SOBRESCRITO (corrente)
- `context/handoff/20260624-232000-builder.md` — NOVO (este snapshot append-only)

## Validações executadas

- **pytest subconjunto impactado:** `tests/unit/test_demanda_revelada_validacao.py` + `tests/unit/test_demanda_revelada_ingestao.py`
  → **19 passed, 0 failed, 0 errors** (sem FutureWarning do código próprio após fix)
- **import smoke:** `python -c "import streamlit_app; print('import ok')"` → **ok**
- **ruff:** `ruff check src/motor_expansao/demanda_revelada/validacao.py` → **All checks passed!**
- **mypy:** `mypy src/motor_expansao/demanda_revelada/validacao.py` → **Success: no issues found in 1 source file**

## Problemas encontrados

1. **Ruff F841 + UP017:** duas variáveis não-usadas e `timezone.utc` — corrigidos inline.
2. **FutureWarning pandas fillna em object-dtype:** `.fillna(False)` em coluna booleana após merge left. Corrigido com `.where(notna, other=False).astype(bool)`.
3. **RuntimeWarning arctanh(1.0):** em teste de correlação perfeita. Corrigido via clip de rho para `(-1+eps, 1-eps)` antes do arctanh.
4. **Q3/Q4 ausentes nos dados reais:** `score_oportunidade_residual` tem mediana=0.0 no join → limiar=0.0 → todos hexes ficam em Q1 ou Q2. NÃO é bug — é característica dos dados. Documentado no relatório.

## Pendências

- QA deve rodar a suíte full (`pytest -q`) para confirmar 0 novas falhas vs. baseline (532+).
- QA deve marcar `Autonomia: loop-safe` no `tasks/backlog.md` (BLK-TP-02) e mover para `completed.md`.
- Parquet `data/staging/quadrantes_demanda_residual.parquet` gerado localmente (gitignored); não necessita commit.

## Riscos remanescentes

- `hexagonos_mercado_mapeado.parquet` é gitignored → em CI ou em máquinas sem os parquets de staging, o relatório real não é gerado (funcionalidade esperada; testes unitários passam com fixtures sintéticas).
- Q3/Q4 com zero hexes nos dados reais: resultado correto mas pode causar estranhamento na leitura do relatório. A Seção 5 do relatório documenta os limiares explicitamente.

## Guardrails verificados

- score_priorizacao não alterado: **sim** — lido de `brasil_priorizados.parquet` apenas para derivar flag `top_m1_20pct`; nenhuma escrita.
- Artefatos M1 preservados: **sim** — nenhuma escrita em artefatos oficiais.
- Dashboard offline mantido: **sim** — nenhuma dependência de API ao vivo introduzida.
- DEC-001 intacta: **sim** — pesos `renda=0.40`/`pop=0.60` inalterados.
- DEC-009 intacta: **sim** — `membros` é insumo OBSERVADO; proibição documentada na Seção 7 do relatório.
- DEC-012 intacta: **sim** — `validacao.py` não importa de `pipelines/m1/`, `censo_*` nem `dashboard/`.
- Anti-PII: **sim** — validação automática ativa após join e antes de salvar parquet.
