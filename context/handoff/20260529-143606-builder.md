# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-OPS-01-FU4 — Corrigir encrypt_one no setup_secrets_vps.sh

## O que foi feito
Reescrito o corpo do `case` da função `encrypt_one` em `scripts/setup_secrets_vps.sh` para o padrão in-place (`cp "${src}" "${dst}"` seguido de `if ! sops [flags] -e -i "${dst}"; then rm -f "${dst}"; echo "ERRO..." >&2; return 1; fi`) nos 3 modos, eliminando o antigo `sops -e "${src}" > "${dst}"`. Flags por modo: dotenv = `--input-type dotenv --output-type dotenv`; binary = `--input-type binary --output-type binary` (obrigatório); yaml = sem flags. A assinatura, o early-return de SRC ausente, o ramo `*)` e a linha de log `echo "  OK: ${src} -> ${dst}"` permanecem inalterados. As 5 chamadas a `encrypt_one` ficaram byte-idênticas.

No `docs/backup_restore.md`, o code-fence "Comandos exatos por arquivo (manual)" passou a usar `cp SRC DST && sops [flags] -e -i DST` com as mesmas flags por modo (comando 1 dotenv; 2 e 5 binary; 3 e 4 yaml), e foi adicionada a nota técnica curta da quirk imediatamente após o code-fence, antes do parágrafo "**Importante:**". Nenhuma outra seção do doc foi alterada. O `cd`, o trecho de `git add/status/diff/commit/push` permanecem.

## Arquivos alterados
- `scripts/setup_secrets_vps.sh` — corpo do `case` dentro de `encrypt_one` (linhas ~138-152 originais) reescrito para padrão in-place com guarda `if ! ... ; then rm -f "${dst}"; return 1; fi` nos 3 modos. Resto do script inalterado.
- `docs/backup_restore.md` — code-fence dos 5 comandos manuais reescrito para `cp SRC DST && sops [flags] -e -i DST`; nota técnica da quirk `cp + sops -e -i` adicionada após o code-fence.

## Validações executadas
- bash -n: `bash -n scripts/setup_secrets_vps.sh` → EXIT 0 (sintaxe válida, sem erro de parse).
- shellcheck: não disponível (`command -v shellcheck` vazio).
- inspeção estática (grep):
  - busca por `sops -e "${src}" >` e `sops ... > "${dst}"` no script → No matches found (nenhum redirecionamento residual).
  - busca por `rm -f "${dst}"` no script → 3 ocorrências (linhas 142, 150, 158) — uma por ramo dotenv/binary/yaml, confirmando a guarda.
  - busca por `-e .* > secrets/` no doc → No matches found (nenhum `sops ... -e SRC > DST` residual).
- pytest/import streamlit: N/A (mudança não toca Python).

## Problemas encontrados
nenhum

## Pendências
nenhuma

## Riscos remanescentes
nenhum — sob `set -euo pipefail` a falha do `sops` é capturada pelo `if !`, removendo o DST e retornando 1 antes que o `set -e` mate o script; o `cp` direto (sem `if !`) não deixa plaintext órfão porque ocorre antes do `sops` e, se falhar, o DST não chega a ser criado/encriptado.

## Guardrails verificados
- score_priorizacao não alterado: não aplicável (só shell/markdown)
- Artefatos M1 preservados: não aplicável
- Dashboard offline mantido: não aplicável
- Nenhum segredo em texto puro: sim
- 5 chamadas a encrypt_one inalteradas: sim
