# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-DIM-18 — Fix: faixa de alunos pela metragem ausente em produção (fallback para parquet de unidades)

## O que foi feito
Implementado o fallback em `load_base_calibracao()` (streamlit_app.py) conforme os Passos 1–5 do handoff do Planner, literalmente:

- **Passo 1:** nova constante `UNIDADES_ULTRA_PERF_PATH = BASE_CALIBRACAO_PATH.parent / "unidades_ultra_performance_hex.parquet"` logo após `BASE_CALIBRACAO_PATH` (derivada de `.parent` = `data/staging/`).
- **Passo 2:** bloco de fallback dentro de `if not BASE_CALIBRACAO_PATH.exists():` — quando o multirede não existe, tenta `UNIDADES_ULTRA_PERF_PATH`; se este também não existe, retorna `pd.DataFrame()` vazio (segundo `return`). Deriva `alunos_por_m2 = alunos_total / metragem` com o guard `(alunos > 0) & (metragem > 0)`. Usa `alunos_total` (NÃO `alunos_reais`, inexistente no parquet de fallback) e variável local `df_fb`. Caminho principal (`df = pd.read_parquet(...)`) e `@st.cache_data(show_spinner=False)` INTOCADOS.
- **Passo 3:** docstring atualizado com a linha documentando o fallback (`alunos_por_m2 = alunos_total / metragem`).
- **Passo 4:** 2 testes inseridos imediatamente após `test_load_base_calibracao_deriva_alunos_por_m2` — `test_load_base_calibracao_fallback_unidades_ultra` e `test_load_base_calibracao_fallback_ambos_ausentes`, com fixture sintética via `tmp_path` e monkeypatch das duas constantes. Sem duplicar imports (`pytest`/`pandas` já presentes).
- **Passo 5:** rodado `mypy streamlit_app.py --ignore-missing-imports` além dos demais.

Risco crítico do handoff (copy-paste cego de `alunos_reais`) evitado: teste `test_load_base_calibracao_fallback_unidades_ultra` verifica `alunos_por_m2` = 2.0 e 2.5 nos registros válidos (não NaN), o que só passa com `alunos_total`.

## Arquivos alterados
- `streamlit_app.py` — nova constante `UNIDADES_ULTRA_PERF_PATH` (~linha 210); bloco de fallback + docstring em `load_base_calibracao()` (~linhas 344–370). Nenhuma outra função tocada.
- `tests/integration/test_streamlit_app.py` — 2 novos testes após o `test_load_base_calibracao_deriva_alunos_por_m2` (após ~linha 5350).

## Validações executadas
- pytest (`python -m pytest -n auto -q tests/integration/test_streamlit_app.py`): **213 passed, 0 failed, 0 skipped** (subconjunto impactado; escolhido por ser o único arquivo de teste que exercita `load_base_calibracao`; suíte full é do QA).
- import (`python -c "import streamlit_app"`): **ok** (só warnings de cache Streamlit fora de runtime, esperados).
- ruff (`ruff check streamlit_app.py tests/integration/test_streamlit_app.py`): **All checks passed! (0 erros)**.
- mypy código novo (`mypy streamlit_app.py --ignore-missing-imports`): **Success: no issues found in 1 source file**.
- mypy regressão (`mypy src/`): **Success: no issues found in 83 source files** (só notes preexistentes `annotation-unchecked`, sem regressão).

Nota de ambiente: Python 3.14 local (a contagem 213 é do arquivo de integração; a suíte full/baseline é do QA).

## Problemas encontrados
Nenhum.

## Pendências
- Suíte full (`pytest -n auto`) é o gate único do QA — não rodada aqui por regra.
- Operacional pós-deploy (fora do código): restartar o Streamlit ou aguardar expiração do `@st.cache_data` para invalidar o DataFrame vazio cacheado.

## Riscos remanescentes
- Cache Streamlit em produção pode servir o DataFrame vazio antigo até restart/expiração (documentado; sem ação de código).
- `unidades_ultra_performance_hex.parquet` precisa existir em `data/staging/` em produção para o fallback funcionar (confirmado pelo BO no handoff).

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; só leitura de parquet de unidades para derivar alunos/m²).
- Artefatos M1 preservados: sim (nenhum artefato oficial tocado; nenhuma dependência de API ao vivo).
- Dashboard offline mantido: sim (fallback lê parquet local; sem rede).
