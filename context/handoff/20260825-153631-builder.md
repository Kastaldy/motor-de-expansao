# Handoff — Builder

> Gerado em 2026-08-25, na branch `ciclo/BLK-MA-21`. Cópia append-only em
> `context/handoff/20260825-153631-builder.md`. Substitui o handoff do Planner (snapshot preservado
> em `context/handoff/20260825-143321-planner.md`).

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
**BLK-MA-21 — Cron MENSAL dos agregadores: o relógio dos independentes, que nunca foi ligado.**
Gate humano APROVADO por Felipe em 2026-08-25 ("Pode seguir com o plano"), com as nove recomendações
D1..D9 tais como recomendadas. Plano P1..P12 do Planner executado por inteiro, **num único conjunto
de mudanças** — a curadoria e a correção da colisão de partição não podiam sair separadas, porque
corrigir o caminho de leitura é o que ATIVA o `delete_matching` destrutivo.

---

## O que foi feito

### P1 — `contrato.py` (o contrato primeiro)
- `VERSAO_CONTRATO_SNAPSHOT`: `snapshots_concorrentes_v3` → **`v4`** (D7).
- `RETENCAO_SEMANAS`: `26` → **`78`** (D5), com a **aritmética das duas cadências escrita em
  comentário** (tabela `26 → 5,98` / `53 → 12,2` / `78 → 17,9` observações mensais) e o custo de
  disco medido (155,8 B/linha ⇒ ~310 MB).
- `COLUNA_PARTICAO = "semana"` (escalar) → **`COLUNAS_PARTICAO: tuple = ("semana", "fonte")`**;
  `__all__` atualizado.
- `CONTRATO_COLUNAS_SNAPSHOT`: **13 colunas** — `"fontes_lidas": "string"` inserida imediatamente
  antes de `versao_contrato`. Fora de `COLUNAS_SNAPSHOT_NULAVEIS` e de `CAMPOS_HASH_POR_FONTE`.
- Docstring do módulo: bump `v3 → v4` no histórico + a política de convivência (a janela grátis de
  bump **fechou**; partição antiga fica legível e sai com `fontes_lidas` nula).

### P2/P3/P4 — `snapshots.py`: partição de 2 chaves, leitura, migração
- `_particionamento_hive()` — **fonte única** das chaves, usada pela escrita, pela leitura e pela
  migração (se divergirem, o modo de falha é silencioso).
- `_arquivos_legados_da_semana()` + guarda em `escrever_particao_semana`: **recusa** semana com
  `parte-*.parquet` solto (D8), com mensagem que aponta para `--migrar-layout`.
- `escrever_particao_semana`: docstring reescrito — a idempotência deixa de ser **por partição** e
  passa a ser **por folha** ("a semana encolheu" → "a folha encolheu"). Corpo inalterado salvo o
  `partitioning=`.
- `ler_snapshots`: duas chaves + parágrafo do risco medido (leitor de 1 chave ⇒ `fonte` 100% nula,
  sem erro e sem log) + novo parâmetro **`fontes: Sequence[str] | None`** (D9), validado contra
  `FONTES_VALIDAS` no mesmo molde de `ler_feeds`.
- **`migrar_layout_particoes()`** nova, logo **depois** de `podar_snapshots` (é a 2ª função do módulo
  que apaga arquivo — o agrupamento torna isso visível). Grava as folhas **antes** de remover o
  legado; `dry_run=True` não toca disco; estado ambíguo (folha `fonte=` da mesma fonte já existente)
  **levanta**. CLI `--migrar-layout`, tratado em `main()` **antes** de `executar()`.

### P5 — `fontes_lidas` no parquet e auditoria mais honesta
- `montar_snapshot(df, *, fontes_lidas: str)` — kwarg **obrigatório, sem default** (2 call sites).
- `materializar`: `fontes_lidas` calculado **uma vez** (`",".join(sorted(...))`), usado na coluna
  **e** na auditoria — que passou de `list` para a **mesma string** que vai ao parquet.
- `executar`: auditoria ganha **`retencao_semanas`** e **`versao_contrato`** (é como o `DRY_RUN` na
  VPS prova qual imagem está rodando antes de agendar).

### P6 — Curadoria versionada (módulo Python, D6)
`src/motor_expansao/vulnerabilidade/curadoria_agregadores.py`, com CLI `python -m …`:
- `escolher_diretorio_fonte()` (D3): TotalPass sempre `csvs`; WellHub prefere `csvs_musculacao`, cai
  em `csvs` se ele não existir e **aborta** se `csvs_musculacao/` for estritamente mais antigo.
- `feed_esta_fresco()` (D4): mtime do CSV mais novo, `agora` **injetado** (teste determinístico).
- `curar()`: publica em `destino/{wellhub,totalpass}/csvs/` (minúsculo), **nunca apaga** no destino,
  `shutil.copy2` (preserva o mtime — que é o insumo da guarda do mês seguinte), devolve
  `fontes_publicadas`. Recusa de uma fonte **não** derruba a outra.
- `main()` imprime o relatório e, na **última linha**, `fontes_publicadas=<a,b>` em formato fixo.

### P7 — `scripts/cron/run_snapshot_agregadores.sh` (novo)
Wrapper que faz os **três** passos (D2) sob um `flock -n` único: coleta (pulável por `PULAR_COLETA=1`
e em `DRY_RUN=1`) → curadoria → snapshot. Pré-checagens com caixa exata (`TotalPass/`, `Wellhub/`),
leitura do `API_IMAGE` com `tr -d '\r'`, `--user 0:0`, `tee` + symlink `_latest.log`. **`FONTES`
vazio ⇒ `exit 3`.** Cabeçalho com runbook completo e a linha de crontab de D1.

### P8 — `scripts/healthcheck_vps.sh agregadores`
`check_agregadores()` nova, **por fonte** (chave de estado `agregador_wellhub` / `agregador_totalpass`),
com `MONITOR_SNAPSHOTS_DIR` e `MONITOR_AGREGADOR_MAX_DIAS` (default 45). `FAIL` quando a partição
**nunca existiu** — que é o estado real hoje. Registrada no `case`, na string de uso e no cabeçalho.

### P9 — Fronteira com o BLK-MA-20, imposta por CÓDIGO (D9)
`alvos_ma`: `--fontes` novo (`choices=sorted(FONTES_VALIDAS)`, default `None`). Com o flag, a série é
lida **uma vez** já recortada e **injetada** nos dois extratores (`snapshots=[serie]`), e
`coordenadas_por_chave(fontes=)` recebe o mesmo recorte. **Sem o flag, o comportamento é bit a bit o
de antes** (o ramo `--saida-redes` virou `elif`, intacto).

### P11/P12 — Documentação e DEC
- `docs/infra_producao.md`: o cron mensal sai de "Pendentes (futuro)"; seção nova **"Coleta mensal
  dos agregadores (BLK-MA-21 / DEC-038)"** com as tabelas de decisão da curadoria, a aritmética da
  retenção, a **ordem de aplicação em 6 passos**, o `DRY_RUN` obrigatório com os 4 campos a conferir,
  a linha de crontab e a do healthcheck; `RETENCAO_SEMANAS = 26` corrigido.
- `docs/vulnerabilidade_ma_contrato.md`: §6 "Local / retenção" reescrito (2 chaves, 78, leitor de 1
  chave, série mista); payload de 12 → **13 colunas**; tabela de versões → `v4` / 2026-08-25.
- `docs/decisions/DEC-038.md` novo + 1 linha em `docs/decisions/README.md` + 1 linha no §8 do
  `CLAUDE.md`.
- `tasks/backlog.md`: **só** o Status do BLK-MA-21 (**`Autonomia` NÃO foi tocada** — segue
  `manual (NÃO loop-safe)`).

---

## Arquivos alterados

**Novos**
- `src/motor_expansao/vulnerabilidade/curadoria_agregadores.py` — curadoria (D3/D4/D6), 305 linhas.
- `scripts/cron/run_snapshot_agregadores.sh` — wrapper do cron mensal (LF, `bash -n` OK).
- `tests/unit/vulnerabilidade/test_curadoria_agregadores.py` — 19 testes.
- `tests/unit/test_wrappers_cron_agregadores.py` — 23 testes (contrato textual dos 2 shells).
- `docs/decisions/DEC-038.md`.

**Alterados**
- `src/motor_expansao/vulnerabilidade/contrato.py` — versão, retenção, `COLUNAS_PARTICAO`,
  `fontes_lidas`, docstring.
- `src/motor_expansao/vulnerabilidade/snapshots.py` — escrita/leitura em 2 chaves, guarda de legado,
  `migrar_layout_particoes`, `fontes_lidas`, `fontes=` na leitura, auditoria, CLI.
- `src/motor_expansao/vulnerabilidade/alvos_ma.py` — `--fontes` e o ramo de recorte no `main`.
- `scripts/healthcheck_vps.sh` — subcomando `agregadores`.
- `docs/infra_producao.md`, `docs/vulnerabilidade_ma_contrato.md`, `docs/decisions/README.md`,
  `CLAUDE.md` (§8, 1 linha), `tasks/backlog.md` (só Status).
- Testes: `test_snapshots.py`, `test_churn_staleness.py`, `test_contrato.py`, `test_score.py`,
  `test_presenca_agregador.py`, `test_alvos_ma.py`.

---

## Validações executadas

| comando | resultado |
|---|---|
| `pytest -q tests/unit/vulnerabilidade/ tests/unit/test_wrappers_cron_agregadores.py tests/contracts/ tests/unit/test_claude_md_size.py` | **826 passed, 5 skipped, 0 failed** (77,5 s) |
| `pytest -q` sobre os 8 consumidores adjacentes (`test_check_artifacts`, `test_direcao_score_na_tela`, `test_loop_guard_paths`, `test_paridade_sinais_web`, `test_piloto_web_{api,endpoints,independentes,redes}`) | **245 passed, 0 failed** |
| `ruff check .` | **All checks passed** |
| `ruff format` nos 3 arquivos NOVOS | aplicado (diffs triviais). **`ruff format --check .` NÃO é gate do CI** (`ci.yml:54` roda só `ruff check`) e acusa **274 arquivos pré-existentes**, `snapshots.py` da `origin/main` inclusive — **não** foi introduzido por este ciclo |
| `bash -n scripts/cron/run_snapshot_agregadores.sh` e `bash -n scripts/healthcheck_vps.sh` | sintaxe válida (os dois em **LF**) |
| `python scripts/loop_guard.py --base origin/main --json` | **nenhum achado CRÍTICO** — só 1 `governanca` (`tasks/backlog.md`), esperado |
| import smoke | `motor_expansao.vulnerabilidade` + `curadoria_agregadores` **ok**; `web/server/app.py` (app de produção) **ok** |

**Por que os testes rodaram nesse recorte:** o pacote `vulnerabilidade/` inteiro porque o contrato de
snapshot mudou (13 colunas) e as fixtures de 4 arquivos de teste montam o frame por
`columns=list(CONTRATO_COLUNAS_SNAPSHOT.keys())`; `tests/contracts/` porque `RETENCAO_SEMANAS` é
constante canônica; `test_claude_md_size.py` porque o §8 cresceu; e os 8 consumidores adjacentes por
grep de `vulnerabilidade|alvos_ma|snapshots_concorrentes` fora do pacote. A suíte **completa** é gate
do QA (baseline do ciclo: **3658** testes coletados).

### Smokes `--dry-run` — nada foi criado nem apagado no disco
Hash SHA-256 da árvore `data/staging/snapshots_concorrentes/` **idêntico antes e depois** dos três
smokes (`473bcaac…c3293`); `git status` sem nenhum arquivo de dados novo.

1. `snapshots … --fontes wellhub --dry-run` →
   `fontes_lidas='wellhub'`, `retencao_semanas=78`,
   `versao_contrato='snapshots_concorrentes_v4'`, `linhas_snapshot=22173`.
2. `snapshots --migrar-layout --dry-run` → `{'migrar_layout': ['2026-33'], 'dry_run': True}` —
   lista exatamente a partição legada viva, sem tocar o disco.
3. `curadoria_agregadores --origem ../GymScraping --dry-run` → **abortou pela guarda de D3**. Ver
   "Problemas encontrados" (1) — é comportamento correto e um achado real. O caminho feliz foi
   provado num clone sintético no scratchpad: `fontes_publicadas=totalpass,wellhub`, escolhendo
   `csvs_musculacao` e `TotalPass/csvs`, sem criar o destino.

---

## Testes quebrados DE PROPÓSITO e ajustados (é assim que se prova que a mudança foi deliberada)

| teste | por quê | ajuste |
|---|---|---|
| `test_parquet_sem_pii_relendo_do_disco` | o pyarrow **remove `fonte` de dentro do arquivo** quando ela vira chave (A2/achado 3): 12 colunas físicas, 13 lógicas | `glob` → `rglob`; asserção passa a ser `CONTRATO_COLUNAS_SNAPSHOT` **sem `fonte`**, com comentário. A prova anti-PII (colunas **e** bytes) ficou intacta |
| `test_schema_snapshot_12_colunas_em_ordem` | 12 → 13 colunas | renomeado para `…_13_colunas_em_ordem`; assere `fontes_lidas` |
| `test_materializar_semana_menor_na_segunda_execucao` | a semântica mudou de partição para folha | reescrito e renomeado `test_reescrita_encolhe_a_folha_e_preserva_as_irmas`: a folha do TotalPass encolhe de 2→1 e as irmãs **sobrevivem** (`{totalpass:1, wellhub:2, unidades:2}`) |
| `test_podar_mantem_26_de_30` | dependia do literal `26` | `test_podar_mantem_a_retencao_configurada`, com `RETENCAO_SEMANAS + 4` semanas |
| `test_churn_staleness.py:425` | trava de valor `== 26` | `== 78`, com comentário de 1 linha sobre as 5,98 observações |
| `test_contrato.py::test_identificadores_do_contrato_sao_ascii` | `COLUNA_PARTICAO` virou tupla | `*c.COLUNAS_PARTICAO` |
| `test_m2_montar_snapshot_nao_recebe_mais_data_referencia` | assinatura ganhou kwarg | assere `["df", "fontes_lidas"]`, `KEYWORD_ONLY` e **sem default** |
| `test_materializar_propaga_fontes_e_audita_o_recorte` | a auditoria virou `str` (era `list`) | assere a string e que ela bate com a coluna do frame |
| `test_particao_nomeada_por_semana_iso`, `…_toda_nula_nasce_com_o_tipo`, `test_m1_frame_vazio_preserva_a_particao` | os arquivos passaram a viver sob `fonte=` | `rglob`/`iterdir`; o 1º passou a exigir as 3 folhas e **nenhum** `parte-*.parquet` solto |
| fixtures `_linha` de `test_score`/`test_presenca_agregador`/`test_churn_staleness` | montam por `CONTRATO_COLUNAS_SNAPSHOT.keys()` | ganharam `"fontes_lidas"` |
| `test_ler_snapshots_sobrevive_a_esquema_misto` | gravava partição de 1 chave à mão | grava folhas `fonte=` por grupo (o propósito — safra antiga — está preservado) |

## Testes NOVOS (T1..T9 do plano + extras)
`test_duas_fontes_coexistem_na_mesma_semana` (T1) · `test_leitor_de_uma_chave_devolve_fonte_nula_em_silencio`
(T2) · `test_nenhum_leitor_do_pacote_usa_particionamento_de_uma_chave` (T3, varredura AST) ·
`test_serie_mista_legado_e_folha_nova` (T4) · `test_escrita_recusa_semana_com_particao_legada` (T5) ·
`test_migrar_layout_converte_legado_em_folhas` + `…_dry_run_nao_toca_disco` + `…_recusa_estado_ambiguo`
(T6) · `test_fontes_lidas_carimbada_no_parquet` (T7) · `test_fontes_lidas_distingue_tentado_de_vazio`
(T8) · `test_retencao_serve_as_duas_cadencias` (T9) · `test_ler_snapshots_recorta_por_fonte` (D9) ·
`test_cli_aceita_recorte_de_fontes` e `test_recorte_de_fontes_e_prosa_sem_o_flag` (D9, em
`test_alvos_ma.py`) · 19 em `test_curadoria_agregadores.py` · 23 em `test_wrappers_cron_agregadores.py`.

**Ajuste deliberado em T2:** o defeito só é reproduzível com `schema=` declarado **e**
`partitioning` incompleto. Sem o `schema=`, a coluna `fonte` simplesmente não existe no resultado
(KeyError) — o que não é o modo de falha silencioso que a DEC descreve. O teste usa a cópia
**plausível** do defeito (alguém reusa o schema e esquece a 2ª chave), que é a que produz
`fonte = None` em 100% das linhas.

---

## Problemas encontrados

1. **ACHADO REAL, medido nesta estação: o clone `../GymScraping` está no estado AMBÍGUO de D3.**
   `Wellhub/csvs` (27 arquivos, **45.526** linhas, mais novo `2026-08-18T18:29:19Z`) é **11 dias mais
   novo** que `Wellhub/csvs_musculacao` (27 arquivos, **22.173** linhas, `2026-08-07T19:06:09Z`). As
   22.173 batem exatamente com a partição viva `semana=2026-33` — ou seja, a série em disco veio de
   `csvs_musculacao/`. A curadoria **aborta**, com os dois caminhos e as duas datas na mensagem, que
   é o comportamento aprovado em D3. **Ação humana necessária antes da primeira execução mensal:**
   confirmar de que modo o coletor rodou em 18/08 e apagar o diretório obsoleto. Publicar `csvs/`
   nesse estado dobraria o universo do funil, **em silêncio**. (`TotalPass/csvs`: 27 arquivos,
   15.986 linhas, `2026-08-25T13:35:05Z` — fresco.)
2. **O smoke obrigatório `import streamlit_app` do prompt do Builder está STALE.**
   `streamlit_app.py` foi removido em `30378a0` ("corte(BLK-WEB-20): remove o Streamlit", DEC-022) e
   `tests/integration/test_streamlit_app.py` também não existe. Substituí por
   `import motor_expansao.vulnerabilidade` + `import app` a partir de `web/server` (o app de
   produção, como `tests/unit/test_piloto_web_oportunidades.py:34` faz). **Recomendo atualizar
   `prompts/builder.md`** — o smoke atual é impossível de satisfazer.
3. **`pytest -n auto` (xdist) morre com `INTERNALERROR / EOFError` nesta estação** (execnet no
   Windows). Todas as execuções foram **seriais**. O QA deve contar com isso ao rodar a suíte
   completa — ver a nota de memória "suíte completa em blocos".
4. **`docs/decisions/README.md` não tinha a linha da DEC-037** (o índice pulava de 036 para 038).
   **Não corrigi** — é bookkeeping de outro bloco e está fora do meu escopo. Fica registrado para o
   orquestrador/QA decidir.
5. `ruff format --check .` acusa 274 arquivos, **pré-existentes** (inclusive o `snapshots.py` da
   `origin/main`). Não é gate do CI. Formatei apenas os 3 arquivos que criei.

## Pendências

1. **Aplicação na VPS — MANUAL, comando a comando, do Felipe.** Nenhum comando foi executado lá, por
   nenhuma via. A ordem está em `docs/infra_producao.md` §"Coleta mensal dos agregadores": (1) imagem
   nova **antes** de qualquer cron; (2) `--migrar-layout` se o semanal já rodou com a imagem antiga;
   (3) os 3 passos pendentes do **BLK-MA-06**; (4) atualizar `/opt/gymscraping`; (5) instalar o
   wrapper + `DRY_RUN=1`; (6) só então crontab + healthcheck.
2. **Dependência operacional NÃO satisfeita:** o BLK-MA-06 segue "aguardando aplicação na VPS"
   (`tasks/backlog.md:1723` e o corpo do bloco). A série na VPS tem **zero partições**.
3. Resolver o estado ambíguo do clone WellHub (item 1 acima).
4. Poda **por fonte** dentro da partição — bloco próprio, adiado em D5.

## Riscos remanescentes

1. **Imagem antiga na VPS escrevendo com UMA chave apaga a folha do mensal.** Mitigado pela ordem de
   aplicação e por `versao_contrato` na auditoria do `DRY_RUN` (se vier `v3`, **não agendar**).
2. **Bump com série viva.** `semana=2026-33` é insumo dos parquets nomeados em produção. Ela **não
   foi regerada** neste ciclo; a convivência é provada por T4, e o `--migrar-layout` só converte por
   ato explícito. Depois de migrada, ela passa a ter `fontes_lidas` **nula** (lê-se "gravada antes do
   `v4`").
3. **A auditoria de `materializar` mudou de tipo** (`list` → `str`). Nenhum consumidor de produção a
   parseia (o wrapper só imprime; o healthcheck não a lê), mas é mudança observável em log.
4. **`coordenadas_por_chave(fontes=args.fontes)`** faz o universo da **pressão** também respeitar o
   recorte quando `--fontes` é passado. É o que o plano especificou; vale notar que com
   `--fontes wellhub` a oferta do S6 vinda do feed passa a ser só-WellHub.
5. **Janela de ~21h45 numa KVM4 com 6 containers permanentes** — decisão de produto (D1/D2). Espaço
   livre real e grade horária **não medidos**: §6 proíbe comando na VPS por agente.
6. O wrapper **nunca foi executado** (exige Docker, o clone e ~21h de coleta). O que está travado é o
   contrato textual + `bash -n`; a validação de execução é o `DRY_RUN=1` do Felipe.

## Guardrails verificados
- **score_priorizacao não alterado:** sim — `loop_guard --base origin/main` sem achado CRÍTICO.
- **Artefatos M1 preservados:** sim — nada em `config.py`, `pipelines/m1/`,
  `normalizar_concorrentes.py` ou `calcular_colunas_mercado.py`; escrita só em
  `data/staging/snapshots_concorrentes/`.
- **Dashboard offline mantido:** sim — nenhuma dependência de API ao vivo; o piloto web não foi
  tocado e importa OK.
- **Nenhum comando na VPS por agente:** sim, em nenhuma etapa (nem MCP, nem `ssh`, nem `scp`).
- **`../GymScraping` READ-ONLY:** sim — apenas leituras (`ls`, `sed`, medição de mtime/linhas).
- **Partição viva não regerada:** sim — hash SHA-256 da árvore idêntico antes e depois dos smokes.
- **Não commitado:** nada. `graphify-out/*`, `PRD.md` e `tasks/current_task.md` (fora do status)
  intactos.
- **`Autonomia` do bloco:** intocada — segue `manual (NÃO loop-safe)`.
- **Acentuação (§2):** prosa acentuada em runbook, DEC, docstrings e mensagens de operador;
  identificadores (`fontes_lidas`, `fonte=wellhub`, `agregadores`, `fontes_publicadas`) em ASCII.
