# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-SAM-02 — Afrouxar o gate do SAM: apenas Faixa M1 elegível AND população ≥ 5000 (remover `flag_viavel` e `~flag_canibalizacao_ultra_1km`). READ-ONLY sobre o M1. Plano técnico aprovado pelo usuário Vinicius em 2026-06-10.

## O que foi feito
Implementação literal do plano técnico aprovado (passos 1–21):

### Produção — `src/motor_expansao/pipelines/calcular_colunas_mercado.py`
1. **Gate do SAM (linha ~298):** `df["flag_sam"] = faixa_elegivel & flag_pop_ok` — removidos `flag_viavel &` e `& ~flag_canibal`. Linha `flag_sam = df["flag_sam"].astype(bool)` INALTERADA.
2. **Comentários atualizados** para citar a DEC-007: bloco da régua (`DEC-006` → `DEC-006/DEC-007`) e bloco do gate (reescrito: "Gate DEC-007 (reverte 2 sub-decisoes da DEC-006): apenas faixa M1 elegivel AND populacao_corte_hex >= 5000. flag_viavel e ~canibalizacao SAIRAM do gate (Vinicius 2026-06-10).").
3. **Variáveis `flag_viavel` (~285) e `flag_canibal` (~284): MANTIDAS** (ainda usadas em `sam_granularidade` e `tese_entrada`). `flag_sam_fitness = flag_sam` e `sam_fitness_potencial`: INALTERADOS.
4. **`sam_granularidade` e `tese_entrada`: lógica INALTERADA** (só labels; precedência do `np.select` resolve o paradoxo canibal+SAM → `municipio_priorizado`).
5. **Assert obsoleto removido** em `validar()`: bloco `n_violacao = (flag_sam & flag_canibalizacao_ultra_1km).sum(); assert n_violacao == 0` + `print("Regra canibalizacao OK")`. Demais validações de `validar()` permanecem.

### Testes — `tests/integration/test_modelo_mercado_hexagonos.py`
6. `test_calcular_bloqueia_sam_quando_ha_canibalizacao` → **renomeado/reescrito** para `test_canibalizacao_nao_bloqueia_mais_o_sam`. h1 (canibal=True) agora `flag_sam=True`; `sam_indice_operavel == 99.5` (RECOMPUTADO de `mask_hibrido`: h1 é híbrido com `score_expansao_hibrido=99.5` notna → `tam_indice_demanda=99.5`), `flag_sam_fitness=True`, `sam_fitness_potencial>0`; labels mantidos: `tese_entrada="proteger_rede_atual"`, `sam_granularidade="hex_censo"` (precedência híbrida). Asserts de h2 inalterados.
7. `test_flag_sam_e_conjuncao_dos_quatro_criterios` → **renomeado** para `test_flag_sam_e_conjuncao_dos_dois_criterios`; parametrize reduzido (canibal=True→True; flag_viavel=False→True; demais como o plano). Docstring cita DEC-007.
8. **Novo** `test_dropar_flag_viavel_admite_hex_no_sam` (flag_viavel=False, faixa alta, pop=8000, canibal=False, renda<RENDA_MIN → flag_sam=True, sam_fitness_potencial>0).
9. **Novo** `test_dropar_canibal_admite_hex_no_sam` (canibal=True, flag_viavel=True, faixa alta, pop=8000 → flag_sam=True, sam_fitness_potencial>0, tese_entrada="proteger_rede_atual", sam_granularidade="municipio_priorizado").
10. `test_parquet_final_respeita_guardrails_do_piloto`: **removido** o assert `(flag_sam & flag_canibalizacao).sum()==0`; demais asserts intactos.
11. `test_sam_zera_*`, `test_sam_calcula_*_fora_top_municipio`, schema/`MERCADO_REQUIRED_COLS`: INALTERADOS.

### Documentação — `docs/modelo_mercado_hexagonos.md`
12. §5.4: nova regra de `flag_sam` (só faixa+`flag_pop_min_5k`, com nota DEC-007); nota explicativa em `sam_granularidade` (precedência); reescrita da Leitura (DEC-006/DEC-007); nota em `tese_entrada` (precedência de `proteger_rede_atual`).

### Decisão — `CLAUDE.md §8`
13. **DEC-007** colada literalmente após a DEC-006 (texto pronto do handoff). DEC-006 NÃO editada.

### Housekeeping
14. `tasks/current_task.md` (status: aguardando QA); `tasks/backlog.md` (BLK-SAM-02 em execução → aguardando QA).

## Arquivos alterados
- `src/motor_expansao/pipelines/calcular_colunas_mercado.py` — gate (linha ~298), comentários (régua + gate), remoção do assert obsoleto em `validar()`.
- `tests/integration/test_modelo_mercado_hexagonos.py` — 1 teste reescrito+renomeado, 1 parametrize reduzido+renomeado, 1 assert removido, 2 testes novos.
- `docs/modelo_mercado_hexagonos.md` — §5.4 (regra `flag_sam`, nota `sam_granularidade`, Leitura, nota `tese_entrada`).
- `CLAUDE.md` — §8: nova DEC-007 (após DEC-006).
- `tasks/current_task.md`, `tasks/backlog.md` — housekeeping.
- `context/handoff.md` (este) + `context/handoff/20260610-155218-builder.md` (snapshot).

## Validações executadas (saída LITERAL)
Host Windows + Python 3.14.2 → SERIAL, `-p no:cacheprovider`, sem `-n auto`/xdist.

`python -m pytest -p no:cacheprovider tests/integration/test_modelo_mercado_hexagonos.py`
```
collected 26 items
tests\integration\test_modelo_mercado_hexagonos.py sss.................. [ 80%]
.....                                                                    [100%]
================= 23 passed, 3 skipped, 11 warnings in 7.01s ==================
```
(3 skips = fixtures de parquet real `hexagonos_mercado_mapeado.parquet`/híbrido ausentes/pendentes de regeneração — esperado.)

`python -m pytest -p no:cacheprovider tests/integration/test_streamlit_app.py`
```
collected 179 items
======================= 179 passed, 1 warning in 11.11s =======================
```

`python -c "import streamlit_app; print('import ok')"`
```
import ok
```

`python -m ruff check src/ tests/`
```
All checks passed!
```

`python -m mypy src/motor_expansao/pipelines/calcular_colunas_mercado.py`
```
Success: no issues found in 1 source file
```

NÃO rodei a suíte FULL (gate único do QA). NO-BYPASS respeitado.

## Problemas encontrados
nenhum

## Pendências
- **Regeneração de parquets paralelos = PASSO OPERACIONAL PÓS-MERGE (Builder NÃO regenera).** Enquanto não regenerado, `hexagonos_mercado_mapeado.parquet` serve `flag_sam` ANTIGO (27.996); os testes que leem o parquet real têm skip/guard de coluna e seguem coerentes com os valores presentes. Ordem canônica pós-merge: híbrido → mercado → `calcular_colunas_mercado` → carteira → plano → domínio → residual → `fase1_bi_exports` (+ enriquecido derivado do dashboard). Volume esperado pós-regeneração: `flag_sam` True ≈ 479.568 (sanidade de PR, não teste rígido).

## Riscos remanescentes
- R4 do plano (parquets paralelos servem `flag_sam` antigo até a regeneração) — pendência operacional, não regressão; não toca M1.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; nenhuma escrita em score/pesos `renda=0.40`/`pop=0.60`/`hex_score_estrutural`).
- Artefatos M1 preservados: sim (nenhum `.parquet` oficial M1 tocado; `git diff` só nos paths do ciclo).
- Dashboard offline mantido: sim (nenhuma mudança de carga/render; smoke import ok).
- `pop_corte.py` INTOCADO: sim (só consumido via `derive_confianca_geografica`/`derive_pop_cut_columns`).
- Escopo de diff: `calcular_colunas_mercado.py`, `test_modelo_mercado_hexagonos.py`, `docs/modelo_mercado_hexagonos.md`, `CLAUDE.md`, `tasks/*`, `context/handoff*`. NADA em `pipelines/m1/`, `scoring.py`, `config.py`, `dashboard/constants.py`, `pop_corte.py`, nem `.parquet`.
