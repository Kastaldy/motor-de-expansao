# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
**[GATE] revisão humana do outline → depois Builder**
O outline proposto (seção abaixo) DEVE ser aprovado/ajustado pelo humano antes que o
Builder escreva qualquer linha em `PRD.md`. Só o Builder escreve `PRD.md`, e somente
após esse gate. Após o Builder: QA (re-executa `pytest -q` e confere `git diff --stat`).

## Entendimento da tarefa
Reescrever `PRD.md` (hoje contendo o "Programa de Melhorias", já migrado para
`tasks/backlog.md`) como um PRD padrão de produto, canônico mas SUBORDINADO ao
`CLAUDE.md`, que REFERENCIA (nunca redefine) score/pesos/parâmetros do `CLAUDE.md` §3/§5
e REFERENCIA (não copia) o roadmap de `tasks/backlog.md`. Doc-only.

## Plano técnico
Builder: execute na ordem. Não tome decisões de escopo — a estrutura, o que cada seção
contém e o que é REFERÊNCIA vs. texto próprio estão definidos abaixo.

1. **Substituir 100% do conteúdo de `PRD.md`** (sobrescrever o "Programa de Melhorias —
   Referência do Master Orchestrator" e absorver a edição pré-existente `M PRD.md`).
   Use a estrutura de seções do "Outline proposto" abaixo, na ordem dada (0 a 10).
   Idioma PT-BR sem acentuação especial obrigatória — siga o estilo dos demais docs do
   repo (texto corrido, sem emojis).

2. **Cabeçalho/front-matter** (seção 0): título "PRD — Motor de Expansao Ultra Academia";
   linha de versão/data (`Versao: 2026-05-29`); responsável (Felipe Silva | Estrategia e
   Growth); e um bloco curto declarando explicitamente: "Fonte de verdade: `CLAUDE.md`.
   Este PRD e SUBORDINADO ao `CLAUDE.md`; em qualquer conflito, `CLAUDE.md` prevalece."
   NÃO repita valores numéricos canônicos aqui.

3. **Seções 1–4 (visão, público, escopo, camadas)**: texto próprio descritivo. Pode citar
   o NOME do score oficial (`score_priorizacao`) e o PAPEL de cada camada (M1 decide
   municípios/ranking oficial; censitário refina intraurbano; híbrido é operacional;
   mercado/residual e Expansão de Domínio apoiam estratégia, não substituem o M1). NÃO
   escreva pesos nem parâmetros numéricos — esses ficam para a seção 5 por referência.
   Público-alvo: 18–45 anos (pode citar a faixa, é contexto de produto, não parâmetro de
   score). Para os papéis das camadas, espelhar `CLAUDE.md` §1 e §4 sem copiar valores.

4. **Seção 5 (score oficial e guardrails) — APENAS REFERÊNCIA**: NÃO redefina nada.
   Escreva que o score oficial é `score_priorizacao`, com fórmula, pesos (`renda`/`pop`),
   parâmetros canônicos (`H3_RESOLUTION`, `DIST_MIN_ULTRA_KM`, `RENDA_MIN`, `AREA_MIN_M2`
   etc.) e guardrails permanentes definidos em **`CLAUDE.md` §3 (Núcleo oficial M1) e §5
   (guardrail permanente de não-recálculo)**. Aponte para essas seções por nome. Pode
   listar os NOMES dos campos/artefatos oficiais como referência, mas sem valores
   numéricos. Inclua frase explícita: "Os valores canônicos vivem somente no `CLAUDE.md`
   §3; este PRD nao os duplica para evitar deriva."

5. **Seção 6 (requisitos funcionais e não-funcionais)**: texto próprio. Funcionais:
   dashboard com 4 abas (Visao Executiva, Mapa Territorial, Expansao de Dominio, Carteira
   e Plano), Análise Pontual H3, Relatório Pontual Censitário 1.5 km, multi-hex, etc.
   (descrever capacidades, referenciando `docs/streamlit_dashboard_m1.md` para detalhe).
   Não-funcionais: dashboard offline com Parquets locais em `data/outputs/`, SEM API ao
   vivo em produção, performance (carga lazy por UF, render lazy de abas, fonte de mapa
   enxuta), staging em Parquet, CI verde como gate. Referenciar `CLAUDE.md` §2/§4 e
   `data/reports/perf_baseline_dashboard.md` em vez de reescrever os detalhes.

6. **Seção 7 (métricas de sucesso)**: texto próprio, conciso. Ex.: aderência do score ao
   desfecho real (apontar para a trilha BLK-SCORE-* no backlog como o mecanismo de
   validação), cobertura territorial, baseline de testes verde, dashboard utilizável
   offline. Não inventar metas numéricas não suportadas pelo repo; manter qualitativo
   onde não houver número canônico.

7. **Seção 8 (roadmap/fases) — APENAS REFERÊNCIA AO BACKLOG**: NÃO copie blocos. Liste as
   FRENTES por nome e prefixo (Frente A `BLK-OPS-*`, Frente B `BLK-ARCH-*`, Frente C
   `BLK-SCORE-*`, produto `BLK-PROD-*`, orquestração `BLK-ORQ-*`) com 1 linha de intenção
   cada, e aponte: "Detalhe, escopo, criterios de aceite e mapa de dependencias de cada
   bloco vivem em `tasks/backlog.md`." Mencionar que ciclos concluídos ficam registrados
   em `tasks/completed.md` e `CLAUDE.md` §5.

8. **Seção 9 (dependências e restrições)**: infra/VPS — referenciar `docs/infra_producao.md`
   e o GUARDRAIL ABSOLUTO de `CLAUDE.md` §6 (nenhum comando no VPS sem confirmação
   explícita por comando). Citar que API/FastAPI, PostGIS, Prefect, M2/M3 e Power BI estão
   fora do deploy inicial (espelha `CLAUDE.md` §4). Backup/DR: referenciar
   `docs/backup_restore.md`.

9. **Seção 10 (referências canônicas)**: lista de ponteiros — `CLAUDE.md` (fonte de
   verdade), `tasks/backlog.md` (roadmap), `tasks/completed.md` (histórico), `docs/`
   (contratos técnicos), `PRD.md` (este, subordinado). Reforçar a hierarquia.

10. **Verificação de não-duplicação antes de fechar**: reler o `PRD.md` escrito e confirmar
    que NENHUM valor numérico canônico foi escrito (0.40, 0.60, 7, 1.0, 4500, 1200, 1500,
    2000, 3.5, 0.20, 2500, 1.6, 1.5). Onde algum aparecer, substituir por referência ao
    `CLAUDE.md` §3 (exceção permitida: a faixa de público "18-45" e o raio descritivo das
    features, que são contexto de produto, não parâmetros de score — manter apenas se
    citados como descrição de feature, não como parâmetro canônico).

## Arquivos afetados
- `C:\Users\Felipe Silva\Downloads\motor-de-expansao\motor-de-expansao\PRD.md` (ÚNICO
  entregável de conteúdo; escrito SOMENTE pelo Builder, pós-gate).
- Controle (não-conteúdo): `tasks/current_task.md`, `tasks/completed.md`,
  `context/handoff.md`, `context/handoff/`.

## Dependências
- Gate de revisão humana do outline (obrigatório) ANTES do Builder.
- `tasks/backlog.md` deve permanecer a fonte do roadmap (referência, não cópia).
- `CLAUDE.md` permanece a fonte canônica (referência, não cópia de valores).

## Riscos técnicos
- Deriva de valores canônicos: risco de o Builder redigitar pesos/parâmetros no PRD.
  Mitigado pela regra "referenciar, não redefinir" e pelo passo 10 de verificação.
- Escopo de commit: `tasks/backlog.md` está pré-sujo (`M`, ~127 linhas de migração
  alheia). NÃO incluir no commit por path; `git add` apenas `PRD.md`. Marcação de
  conclusão vai em `tasks/completed.md`.
- Desalinhamento de formato com a expectativa do usuário — mitigado por este gate.

## Critérios de aceite finais
- `PRD.md` é um PRD padrão de produto coerente, subordinado ao `CLAUDE.md`, sem duplicar
  o backlog nem valores canônicos.
- Nenhum valor canônico contradiz o `CLAUDE.md` (PRD referencia §3/§5, não redefine).
- Roadmap aponta para `tasks/backlog.md` em vez de copiar blocos.
- `git --no-pager diff --stat -- PRD.md` mostra somente `PRD.md` alterado no entregável.
- `pytest -q` segue verde (baseline 532 passed, 1 skipped) — nenhum código tocado.

## Validações obrigatórias
- Comando: `pytest -q`
  - Critério mínimo: suíte verde, baseline `532 passed, 1 skipped` (doc-only, nada de
    código tocado).
- Comando: `git --no-pager diff --stat -- PRD.md`
  - Critério mínimo: apenas `PRD.md` aparece como alterado no entregável de conteúdo.

## Fora de escopo
- Alterar `CLAUDE.md`, código, `config.py`, score (`score_priorizacao`,
  `hex_score_estrutural`, pesos), artefatos M1.
- Reescrever, mover ou re-duplicar blocos de `tasks/backlog.md`.
- Incluir `tasks/backlog.md` no commit por path.
- Qualquer comando no VPS.

## Alerta de aprovação
Criticidade do bloco: **Média** (doc-only — NÃO toca `score_priorizacao`,
`hex_score_estrutural`, código, `config.py`, `CLAUDE.md` nem artefatos M1; não é Crítica
pela regra do score). PORÉM, por ser `PRD.md` um **documento canônico**, o gate
**[revisão humana do OUTLINE]** entre Planner e Builder é **OBRIGATÓRIO** —
independentemente do rótulo de criticidade. O Builder NÃO deve escrever em `PRD.md` antes
de o humano aprovar/ajustar o outline da seção abaixo.

## Outline proposto do PRD (para revisão humana)

> Hierarquia declarada: `CLAUDE.md` (fonte de verdade) > `PRD.md` (este, subordinado).
> Em conflito, `CLAUDE.md` prevalece. Valores canônicos NÃO são redefinidos aqui —
> apontam para `CLAUDE.md` §3/§5. Roadmap aponta para `tasks/backlog.md`.

- **0. Cabeçalho e subordinação ao CLAUDE.md**
  Título, versão/data, responsável e declaração explícita de que `CLAUDE.md` é a fonte de
  verdade e prevalece em conflito. Lista os documentos de apoio por referência.

- **1. Visão e objetivo do produto**
  O que é o Motor de Expansao Ultra Academia: decidir onde a Ultra expande, quem já atua,
  qual mercado residual existe e como ocupar regiões com sequência controlada. Duas
  trilhas complementares (M1 oficial territorial + mercado por hexágono).

- **2. Público-alvo e contextos de uso**
  Público prioritário 18–45 anos. Usuários: estratégia/growth (decisão executiva de
  carteira), análise territorial (leitura intraurbana), operação de mercado (residual e
  Expansão de Domínio). Uso offline via dashboard.

- **3. Escopo do produto / fora de escopo**
  Dentro: motor M1 oficial, camadas paralelas, dashboard offline de 4 abas, análises
  pontuais (H3 e censitária 1.5 km), backup/DR de segredos. Fora: API ao vivo em
  produção, FastAPI/PostGIS/Prefect, M2/M3, Power BI, pesquisas (espelha `CLAUDE.md` §4).

- **4. Camadas e trilhas**
  Papéis (sem valores numéricos): M1 oficial territorial decide municípios e ranking
  oficial; censitário/híbrido refinam leitura intraurbana; mercado/residual e Expansão de
  Domínio apoiam estratégia operacional e NÃO substituem o M1. Nenhuma trilha paralela
  altera o M1 sem aprovação explícita.

- **5. Score oficial e guardrails (REFERÊNCIA — `CLAUDE.md` §3/§5)**
  Score oficial = `score_priorizacao`. Fórmula, pesos (renda/pop), parâmetros canônicos
  (`H3_RESOLUTION`, `DIST_MIN_ULTRA_KM`, `RENDA_MIN`, `AREA_MIN_M2` etc.) e o guardrail
  permanente de não-recálculo vivem em `CLAUDE.md` §3 e §5. O PRD aponta para lá e NÃO
  duplica os valores, para evitar deriva.

- **6. Requisitos funcionais e não-funcionais**
  Funcionais: dashboard de 4 abas, análise pontual H3 (raio descritivo), relatório
  censitário 1.5 km, multi-hex, pins de concorrentes/Ultra como camada visual de apoio.
  Não-funcionais: offline com Parquets locais em `data/outputs/`, sem API ao vivo,
  performance (carga/render lazy, mapa enxuto), staging em Parquet, CI verde como gate.
  Detalhe por referência a `docs/streamlit_dashboard_m1.md` e
  `data/reports/perf_baseline_dashboard.md`.

- **7. Métricas de sucesso**
  Aderência do score ao desfecho real (validada pela trilha `BLK-SCORE-*` no backlog),
  cobertura territorial, suíte de testes verde (baseline em `CLAUDE.md` §5), dashboard
  utilizável offline. Qualitativo onde não há número canônico — sem inventar metas.

- **8. Roadmap e fases (REFERÊNCIA — `tasks/backlog.md`)**
  Frentes por nome/prefixo: A `BLK-OPS-*` (robustez operacional), B `BLK-ARCH-*`
  (arquitetura), C `BLK-SCORE-*` (validação de scores), produto `BLK-PROD-*`,
  orquestração `BLK-ORQ-*`. Uma linha de intenção por frente. Escopo, critérios e mapa de
  dependências por bloco vivem em `tasks/backlog.md`; concluídos em `tasks/completed.md`.

- **9. Dependências e restrições (infra/VPS)**
  VPS de produção, deploy via `git pull` + Docker; GUARDRAIL ABSOLUTO de `CLAUDE.md` §6
  (nenhum comando no VPS sem confirmação por comando). Backup/DR via
  `docs/backup_restore.md`. Manutenção/deploy via `docs/infra_producao.md`.

- **10. Referências canônicas**
  Ponteiros para `CLAUDE.md`, `tasks/backlog.md`, `tasks/completed.md`, `docs/` e o papel
  deste PRD, reforçando a hierarquia (PRD subordinado ao CLAUDE.md).

## Guardrails ativos
- Guardrail permanente (`CLAUDE.md` §5): visualizações/análises/interações NÃO podem
  recalcular ou alterar `score_priorizacao`, `hex_score_estrutural`, carteira, plano curto
  prazo, plano de domínio ou artefatos M1 sem aprovação explícita. Este bloco é doc-only e
  NÃO os toca.
- O PRD apenas REFERENCIA valores canônicos (score, pesos, parâmetros, guardrails) —
  JAMAIS os redefine. `CLAUDE.md` permanece a fonte de verdade; o PRD é subordinado a ele.
- GUARDRAIL ABSOLUTO (`CLAUDE.md` §6): nenhum comando no VPS sem confirmação explícita do
  usuário, por comando. Este bloco não toca o VPS.
- Parâmetros canônicos imutáveis sem DEC registrada: `H3_RESOLUTION=7`,
  `DIST_MIN_ULTRA_KM=1.0`, `RENDA_MIN=4500.0`, pesos `renda=0.40`, `pop=0.60` — o PRD não
  os altera nem os redefine.
- Uma tarefa ativa por vez; não expandir escopo; não implementar nada antes do gate.
