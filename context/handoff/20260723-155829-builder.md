# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Aprovação humana (gate do run-cycle)
Plano técnico APROVADO POR Vinicius EM 2026-07-23 (gate humano/visual do run-cycle, criticidade Alta).
O Builder rodou SOMENTE após essa confirmação, conforme o alerta de aprovação do handoff do Planner.

## Bloco executado
BLK-RELPON-13 — Correção do painel Socioeconomia do slide-hero: hexágono H3 a 5 km (padrão residual), não setor. Feature visual/render do PDF do Relatório Pontual. READ-ONLY M1.

## O que foi feito
Religado o painel `socioeconomia` do slide-hero para desenhar `score_setor_2022_calibrado` por hexágono H3 res-7 num disco `grid_disk(k=5)` a `RAIO_RESIDUAL_DISPLAY_KM=5.0` km (mesma métrica/paleta do `score`, sem pins), CONDICIONAL ao `hexes_df` como o `residual`, reduzindo um pouco as 2 imagens da página. Variante clássica espelhada. `/Count 8`, as 8 chaves de `CAMADAS_CENSITARIAS` e as outras 7 camadas seguem byte-idênticas. Passos executados LITERALMENTE conforme o plano numerado (1–9):

- **Passo 1 (`censo_map._hex_polygons_3857`):** parametrizado `*, value_col="oferta_efetiva_disponivel"` (default-preserving); adicionada guarda `if value_col not in hexes_df.columns: return empty` logo após a guarda de `hex_id`; o literal `row.get("oferta_efetiva_disponivel")` virou `row.get(value_col)`; docstring atualizado.
- **Passo 2 (`_residual_hex_central`):** parametrizado `*, value_col="oferta_efetiva_disponivel"`; subset `{"hex_id", value_col}` e leitura `linha[value_col]`. Default preserva o residual.
- **Passo 3 (`_render_camada_residual_hex`):** adicionados 7 parâmetros keyword-only default-preserving após `choropleth_alpha` (`value_col`, `titulo`, `legenda_titulo`, `legenda_entries`, `color_fn`, `valor_central_rotulo`, `valor_central_fmt=_format_valor_residual`). No corpo: `value_col` propagado ao `_hex_polygons_3857`; `_legenda_entries_eff`/`_color_fn_eff` resolvidos (None → residual); `_render_camada` usa `titulo/legenda_titulo/legenda_entries/color_fn` e `valor_ponto=_legenda_valor_hex(valor_central_rotulo, valor_central_fmt(_residual_hex_central(..., value_col=value_col)))`. O `_residual_fn` inline permanece para o default (byte-identidade).
- **Passo 4 (`render_mapas_censitarios_combinados`):** removido o bloco antigo `socioeconomia_png = _render_camada(...)` e a chave `"socioeconomia"` do dict literal; adicionada chamada CONDICIONAL a `_render_camada_residual_hex(..., value_col="score_setor_2022_calibrado", titulo="Socioeconomia - raio 5 km", legenda_titulo="Score censitario (0-100)", legenda_entries=_score_legend_entries(), color_fn=_score_fn, valor_central_rotulo="Score", valor_central_fmt=_format_valor_ponto_score)` junto ao bloco do `residual`, com `if socioeconomia_png is not None: mapas["socioeconomia"] = socioeconomia_png`. Docstrings da função (retorno + item BLK-RELPON-10) atualizados.
- **Passo 5 (`censo_report`):** constante de módulo `_HERO_MAP_SCALE = 0.92` (calibrável no gate visual); `_map_grid_cells_packed` ganhou `*, scale=1.0` com `cell_w *= scale; cell_h *= scale` APÓS o clamp de `max_total_w` e ANTES de `total_w/total_h`; `_draw_maps_grid` ganhou `*, packed_scale=1.0` repassado como `scale=packed_scale` no ramo `pack=True`; `_classico_draw_maps_grid` ganhou `*, packed_scale=1.0` repassado.
- **Passo 6:** `_socioeconomia_residual_page` e `_classico_socioeconomia_residual_page` passam `packed_scale=_HERO_MAP_SCALE`; docstrings atualizados (Socioeconomia = hex H3 a 5 km). Demais páginas (`_entorno_page`/`_mapas_calor_page`/clássicas) intocadas → default 1.0 preserva byte-identidade.
- **Passos 7–8 (testes):** `_CAMADAS_SEM_HEXES` reduzido a 6 chaves (sem socioeconomia); `_CAMADAS = _CAMADAS_SEM_HEXES | {"socioeconomia", "residual"}` (8); coluna `score_setor_2022_calibrado` adicionada ao fixture `_hexes_sinteticos`; título assert → "Socioeconomia - raio 5 km"; anti-vácuo #1 → `com["socioeconomia"] == sem["socioeconomia"]` + sentinela `com["score"] != sem["score"]`; anti-vácuo #2 → sentinela `com["score"] != sem["score"]`; 3 testes novos (`test_socioeconomia_e_hexagono_nao_setor_a_5km`, `test_socioeconomia_ausente_sem_hexes_df_ou_sem_coluna_score`, `test_socioeconomia_reage_ao_score_setor_2022_calibrado`); export `test_slide_hero_offline_safe_sem_camada_residual`: `assert "socioeconomia" in mapas` → `assert "socioeconomia" not in mapas` + docstring. Nenhuma assert de contagem de imagem relaxada (o `>=6` de export.py:908 continua passando exato).
- **Passo 9 (docs):** `docs/relatorio_pontual_censitario.md` — item 3 do slide-hero, faixa "no raio", contrato da função (linhas 166/171/186) e nova entrada de changelog "Correção BLK-RELPON-13" atualizados para o novo comportamento (hex a 5 km, condicional, `_legenda_valor_hex`, `_HERO_MAP_SCALE`).

## Arquivos alterados
- `src/motor_expansao/dashboard/censo_map.py` — passos 1–4 (helpers hex generalizados default-preserving; socioeconomia religada ao render hex, chave condicional; docstrings).
- `src/motor_expansao/dashboard/censo_report.py` — passos 5–6 (`_HERO_MAP_SCALE`; `scale`/`packed_scale` em `_map_grid_cells_packed`/`_draw_maps_grid`/`_classico_draw_maps_grid`; aplicado só às 2 páginas do slide-hero).
- `tests/unit/test_relatorio_pontual_censitario_mapa.py` — passo 7 (constantes, fixture, travas anti-vácuo, título, 3 testes novos).
- `tests/unit/test_relatorio_pontual_censitario_export.py` — passo 8 (`test_slide_hero_offline_safe_sem_camada_residual`).
- `docs/relatorio_pontual_censitario.md` — passo 9 (contrato do painel + changelog BLK-RELPON-13).

## Validações executadas
Ambiente: Python 3.14 local. `pytest-xdist` (`-n auto`) aborta com INTERNALERROR de execnet/gateway NESTE ambiente (bug de ambiente Windows/3.14, não do código) — rodei SERIAL, exatamente como a validação obrigatória do próprio Planner (`python -m pytest -q ...`).

- pytest `tests/unit/test_relatorio_pontual_censitario_mapa.py` + `tests/unit/test_relatorio_pontual_censitario_export.py` (serial): **90 passed, 0 failed, 0 skipped** (97.58s). Rodados porque são os arquivos de teste diretamente tocados (passos 7–8); inclui `test_camada_residual_*` e `com["residual"] == sem["residual"]` (byte-identidade do residual) verdes.
- pytest `tests/integration/test_streamlit_app.py` (serial): **255 passed, 0 failed, 0 skipped** (50.81s). Obrigatório no subconjunto (smoke de app amplo).
- import: **ok** (`python -c "import streamlit_app"` → "import ok").
- ruff `check censo_map.py censo_report.py <2 test files>`: **All checks passed!**
- mypy `censo_map.py censo_report.py` (projeto tem `[tool.mypy]` em pyproject.toml): **Success: no issues found in 2 source files**.
- READ-ONLY M1 por mtime: `brasil_estrutural.parquet`/`brasil_priorizados.parquet`/`hexagonos_brasil_oportunidades.parquet` = 2026-06-10 (INALTERADOS, anteriores a hoje); `hexagonos_brasil_dashboard.parquet`/`hexagonos_mapa_sample.parquet` ausentes localmente (build de pipeline, não tocados). Nenhum artefato oficial reescrito.

Total do subconjunto Builder: **345 passed, 0 failed, 0 skipped** (2 unit files + integração). A suíte FULL é responsabilidade do QA (gate único) — NÃO rodada aqui.

## Problemas encontrados
- `pytest -n auto` (xdist) aborta com INTERNALERROR (execnet gateway "connection already closed") neste ambiente Windows/Python 3.14. Não é regressão do código: contornado rodando serial (idêntico ao comando de validação do Planner). O QA deve observar o mesmo se usar `-n auto` local; o CI (Linux) não é afetado.

## Pendências
- Gate visual de calibração de `_HERO_MAP_SCALE` (partida 0.92) e da aparência final do hex a 5 km — decisão de PRODUTO de Vinicius, pós-Builder. Nenhum teste trava o número 0.92 (testam a MECÂNICA: `scale<1.0` ⇒ células menores e centradas).

## Riscos remanescentes
- Nenhum de correção. O `>=6` de imagens (export.py:908) passa EXATO (6 = grid 2x2 + concorrentes + entorno; socioeconomia/residual ausentes sem `hexes_df`); se alguma dessas 6 imagens deixar de embutir, cairia para 5 — pré-existente, não relaxado.
- Custo aceito (não refatorado, por escopo): socioeconomia hero faz o próprio fetch de basemap 5 km (mesma bbox do residual; cache de tiles em disco deduplica).

## Guardrails verificados
- score_priorizacao não alterado: sim (nenhuma escrita; `score_setor_2022_calibrado`/`oferta_efetiva_disponivel` apenas LIDOS do `hexes_df` para colorir).
- Artefatos M1 preservados: sim (mtime inalterado; nenhum pipeline rodado).
- Dashboard offline mantido: sim (`basemap=False` → canvas claro; import lazy de h3/contextily; chaves condicionais com fallback textual offline-safe).
- Motor censitário INTOCADO: sim (`setor_censitario_intersecao_area_1p5km`, `RAIO_CENSITARIO_DEFAULT_KM=1.5` não tocados).
- Acentuação/EXCEÇÃO DE RENDER (§2): labels do PNG em ASCII — "Socioeconomia - raio 5 km", "Score censitario (0-100)", "Score no hexagono: NN". Docs/testes em português acentuado correto.
- `/Count 8` e 8 chaves de `CAMADAS_CENSITARIAS` preservados; demais 7 camadas byte-idênticas (defaults default-preserving; travado por `test_camadas_existentes_ficam_byte_identicas_com_os_defaults_novos` e `com["residual"] == sem["residual"]`).
