# Handoff — QA Analyzer

## Skill que gerou este handoff
QA Analyzer

## Próxima Skill recomendada
Block Orchestrator (próximo bloco) — merge humano fica para o usuário

## Bloco revisado
BLK-DIM-20 — UI: parâmetros de fluxo de caixa editáveis (capex parcelado)

## VEREDITO: APROVADO

## Gate executado (todos verdes)
1. `pip install "openlocationcode>=1.0.1"` — OK (EXIT 0)
2. `pytest -q` (suite COMPLETA serial): **1038 passed, 4 skipped, 18 warnings** em 207.95s
3. `ruff check .`: **All checks passed!**
4. `mypy simulador.py pages.py --ignore-missing-imports`: **Success: no issues found in 2 source files**
5. `python -c "import streamlit_app"`: **import ok**

## Verificações específicas BLK-DIM-20 (todas confirmadas)
- CA-10a `test_financiamento_zero_resultado_identico`: PASS — `capex_financiado_pct=0.0` dá payback/margem/flag idênticos.
- CA-10b `test_financiamento_aumenta_payback`: PASS — financiamento 100% aumenta payback.
- CA-10c `test_financiamento_nao_altera_ebitda`: PASS — `margem_ebitda_pct` e `ebitda_mensal` inalterados.
- CA-10d `test_pmt_formula_correta`: PASS — PMT para C=600k, r=0.018, n=36 dentro de [22k, 24k].
- (4 testes CA-10 rodados isolados: **4 passed**.)
- `_pmt` calculado na seção 11a (linhas 287-300) **ANTES** do `for t in range(1, 61):` (linha 302). CONFIRMADO via Read.
- EBITDA (seções 7-8, linhas 249-257) computado antes do loop; PMT só subtrai de `fcf_t` dentro do loop (linhas 318-319) — EBITDA imune. CONFIRMADO (Read + CA-10c).
- `viabilidade_ponto.py` NÃO alterado — fora do commit `83ea888`; propaga `**kwargs`. CONFIRMADO.
- Artefatos M1 (config.py M1 / pipelines/m1 / parquets oficiais) NÃO tocados — commit `83ea888` toca só `simulador.py`, `pages.py`, `test_simulador.py` (+ handoff/task tracking). CONFIRMADO via `git show --stat`.

## Guardrails
- READ-ONLY sobre o M1: confirmado. `score_priorizacao`, pesos (`renda=0.40`/`pop=0.60`), `hex_score_estrutural`, carteira, plano e artefatos oficiais INALTERADOS.
- Dashboard offline preservado: novos widgets são locais ao formulário; nenhuma chamada de rede adicionada.
- Loop guard: diff não toca `config.py` M1/`pipelines/m1`/`*scoring*`/artefatos M1/deploy/Dockerfiles.

## Housekeeping
- `housekeeping_move_block.py BLK-DIM-20 --date 2026-06-22`: OK (movido para completed.md, stub no backlog).
- `housekeeping_move_block.py BLK-DIM-20 --check`: OK.

## Observações
- Nota: os widgets condicionais de `prazo_financiamento_meses`/`juros_am_pct` aparecem só quando `pct_financiado > 0`, com defaults seguros no branch `else` (36 meses / 1,8% a.m.) — bem encadeado com a chamada `analisar_viabilidade_ponto`.
- Baseline de testes maior que o citado no CLAUDE.md §5 (1038 vs 532) por evolução do projeto; divergência de ambiente (Python 3.12 local), não defeito.

VEREDITO: APROVADO
