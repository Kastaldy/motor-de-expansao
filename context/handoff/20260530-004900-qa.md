# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (Passo 6.0: housekeeping via helper + commit/merge). Registrar bloco-filho BLK-ARCH-01b (tipagem dos 14 módulos migrados) no backlog.

## VEREDITO
APROVADO COM RESSALVAS

## Justificativa
Migração estritamente mecânica e correta: 21 arquivos movidos `jobs/pipelines/* -> src/motor_expansao/pipelines/*`, `parents[2]->parents[3]` (14 módulos), `sys.path.insert` removido, imports reapontados, `pythonpath = ["src"]`. Os 4 artefatos M1 são byte-idênticos (hash provado). O desvio (`[[tool.mypy.overrides]] ignore_errors=true`) está CORRETAMENTE escopado por nome aos exatos 14 módulos legados migrados que tinham erros latentes — NÃO mascara nenhum código antes coberto por `mypy src/` (m1/, dashboard, core, constants permanecem checados e limpos). Única ressalva (não bloqueante): dívida de tipagem a registrar como bloco-filho.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- Registrar BLK-ARCH-01b: tipar os 14 módulos migrados e remover o `ignore_errors=true` do `pyproject.toml`, recuperando cobertura de tipo plena.

## Testes faltantes
- nenhum (a suíte reaponta os 16 testes corretamente; 541 passed cobre os módulos migrados em integração/unidade).

## Riscos remanescentes
- O override mascara erros de tipo REAIS nos 14 módulos legados até a tipagem dedicada. Mitigação adequada: escopo restrito por nome de módulo (zero impacto em m1/dashboard/core), comentário explicativo no `pyproject.toml`. Risco baixo: esses módulos já não eram type-checked antes (viviam fora de `src/`), logo é status quo, não regressão.
- Diff git mostra os 21 movidos como D+A (não rename puro) porque o conteúdo mudou acima do limiar; histórico preservado via `git mv`. Cosmético.

## Saída literal das validações (re-executadas pelo QA)

### 1. pytest -q
```
541 passed, 1 skipped, 9 warnings in 79.00s (0:01:18)
```

### 2. python -c "import streamlit_app; print('ok')"
```
(warnings de ScriptRunContext / MemoryCacheStorageManager — esperados em bare mode)
ok
```

### 3. ruff check .
```
All checks passed!
```

### 4. mypy src/  (COM override — estado entregue)
```
src\motor_expansao\pipelines\m1\ibge_censo.py:158: note: By default the bodies of untyped functions are not checked ... [annotation-unchecked]
src\motor_expansao\pipelines\m1\ibge_censo.py:159: note: ... [annotation-unchecked]
src\motor_expansao\pipelines\m1\poi_enrichment.py:51: note: ... [annotation-unchecked]
Success: no issues found in 44 source files
```
(apenas `note:` informativos; ZERO `error:`)

### 5. grep imports de raiz vivos (esperado VAZIO)
```
rg -n "^\s*(from|import)\s+(jobs|api|config|ibge_censo|poi_enrichment|base_h3_brasil|hex_enrichment|fase1_bi_exports)\b" --glob "!fora_primeira_fase/**" --glob "!**/__pycache__/**" .
EXIT=1   (sem matches = VAZIO)
```

### 6. pythonpath em pyproject.toml
```
129:pythonpath = ["src"]
```
(sem `"."`)

### 7. parents[2] / parents[3] em src/.../pipelines/
```
rg -n "parents\[2\]" src/motor_expansao/pipelines/   -> EXIT=1 (VAZIO)
rg -l "parents\[3\]" src/motor_expansao/pipelines/   -> 14 módulos
```
Módulos com parents[3]: calcular_colunas_mercado, calcular_penetracao_ultra_hex, comparar_geofusion_vs_hex, enriquecimento_espacial_hexagonos, enriquecer_outputs_residual_mercado, gerar_carteira_acionavel, gerar_plano_expansao_curto_prazo, gerar_plano_expansao_dominio, gerar_relatorio_expansao_dominio, modelo_hibrido_expansao, normalizar_concorrentes, normalizar_unidades_ultra, validar_modelo_ultra, validar_penetracao_ultra_hex.

### 8. Prova de não-mutação M1 (sha256)
```
c226954945ad0757a0429c84c43f410492c0ea7d15ca1d9b6a15f68727806567 data/staging/brasil_priorizados.parquet
7baa07a2cbc0b7d8f2a8878932cae0ebf9400fce00e9c48c366c9903215f131b data/staging/brasil_estrutural.parquet
805c65e28adfd800c7d5524e73fa9b0a044b992f17d617d0f2c6e40f9bbf61ca data/staging/hexagonos_brasil_oportunidades.parquet
0cfb1015fc9df8b63776eb07ae3e666766905f768a4a78a406ae4d6b7cb6f618 data/outputs/hexagonos_brasil_dashboard.parquet
```
Byte-idênticos aos 4 hashes do Passo 0 do Builder. PASS.

### Housekeeping dry-check
```
python scripts/housekeeping_move_block.py BLK-ARCH-01a --check
FALHA --check: stub ausente no backlog para BLK-ARCH-01a
EXIT=1
```
PENDENTE (esperado: move roda no fechamento, Passo 6.0, DEPOIS do veredito). NÃO retornou EXIT_AD_HOC=3 → o helper RECONHECE BLK-ARCH-01a como bloco existente (heading presente em `tasks/backlog.md:119`). Confere.

## Auditoria do desvio crítico — `[[tool.mypy.overrides]] ignore_errors = true`

### (a) Escopo do override — CORRETO, restrito aos 14 módulos
Há exatamente UM bloco `[[tool.mypy.overrides]]` em `pyproject.toml` (linha 107), com `module = [...]` listando NOMINALMENTE 14 módulos `motor_expansao.pipelines.<x>` e `ignore_errors = true`. NÃO é glob (`*`). Módulos cobertos:
calcular_colunas_mercado, calcular_penetracao_ultra_hex, comparar_geofusion_vs_hex, enriquecimento_espacial_hexagonos, normalizar_unidades_ultra, gerar_carteira_acionavel, modelo_hibrido_expansao, validar_modelo_ultra, validar_penetracao_ultra_hex, materializar_setores_censitarios_geo, fase_a_censo2022_setores, validar_fase_a_censo2022, fase_a_piloto_expandido, fase_a_nacional_completo.
NÃO inclui: `motor_expansao.pipelines.m1.*` (M1 core), dashboard, core, constants, config — todos permanecem sob `mypy src/` e LIMPOS. Nem inclui 6 dos 20 módulos migrados (gerar_plano_expansao_curto_prazo/_dominio, gerar_relatorio_expansao_dominio, enriquecer_outputs_residual_mercado, normalizar_concorrentes, teste_setor_censitario_2010) — esses migraram limpos e seguem checados. NÃO há regressão de cobertura de tipo sobre código antes verificado.

### (b) Os 50 erros são pré-existentes/latentes — CONFIRMADO
Removi temporariamente o bloco de override (backup/restore de `pyproject.toml`) e rodei `mypy src/`: `Found 50 errors in 12 files (checked 44 source files)`. Os 12 arquivos com erro são TODOS subconjunto dos 14 do override (nenhum em m1/dashboard/core). Natureza dos erros é típica de código legado nunca-checado, NÃO de migração quebrada (zero "module not found"/import quebrado). Trecho representativo:
```
normalizar_unidades_ultra.py:158: error: Argument 1 to "float" has incompatible type "object" ... [arg-type]
gerar_carteira_acionavel.py:146: error: Argument 1 to "float" has incompatible type "object" ... [arg-type]
validar_modelo_ultra.py:265: error: Need type annotation for "resultados" ... [var-annotated]
modelo_hibrido_expansao.py:102: error: Argument 1 to "float" has incompatible type "float | None" ... [arg-type]
enriquecimento_espacial_hexagonos.py:134: error: No overload variant of "where" matches ... [call-overload]
materializar_setores_censitarios_geo.py:29: error: Name "ajuste_executivo" already defined (possibly by an import) [no-redef]
```
São annotations faltantes, `float(object)` em dataframes, overloads numpy e redefinições de fallback `try/except import` — clássicos de legado. Após restaurar o override, `mypy src/` volta a `Success: no issues found in 44 source files`.

### (c) Decisão certa para bloco MECÂNICO — SIM, defensável
O mandato proíbe refatorar/corrigir além de mover. Corrigir 50 erros de tipo seria expansão de escopo (e mudança de código fora do mecânico). O override preserva o status quo EXATO: esses módulos nunca foram type-checked (viviam fora de `src/`), e o CI continua verde com a MESMA cobertura efetiva (zero código previamente checado fica descoberto). É a escolha value-neutral correta. Parecer: APROVAR a abordagem e registrar a dívida como bloco-filho (BLK-ARCH-01b — tipar os 14 módulos e remover o override).

## Demais desvios e escopo
- Mudanças de conteúdo nos módulos = SOMENTE `parents[2]->parents[3]`, remoção de `import sys`+`sys.path.insert`, reaponte de imports internos `jobs.pipelines.X -> motor_expansao.pipelines.X` (+ remoção de `# noqa: E402` órfão). Nenhuma função/assinatura/valor/lógica alterada (verificado via `git diff -M`).
- 4 literais/docstrings cosméticos `jobs/pipelines/...` preservados (validar_penetracao_ultra_hex.py:627, comparar_geofusion_vs_hex.py:245, gerar_relatorio_expansao_dominio.py:10, e referências em dashboard/pages.py + docstrings m1). O literal asserido por `test_validar_penetracao_ultra_hex.py:130` está intacto.
- `fora_primeira_fase/*` e `concorrentes/geo_skyfit.py` intocados (git status vazio).
- Params canônicos intactos: H3_RESOLUTION=7, pesos 0.40/0.60, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, AREA_MIN_M2=1200.0 (arquivos não tocados pela migração).

## Conferência de no-bypass
Confirmo: nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico. A suíte pytest usa `tests/fixtures/` de forma legítima (isolamento de lógica) — NÃO é bypass. Não há tooling SOPS neste bloco. O `ignore_errors=true` do mypy NÃO é bypass proibido: não substitui config por uma vazia nem finge verde sobre código antes coberto — está escopado aos 14 módulos legados que JÁ não eram checados, preservando a cobertura anterior (item (a)/(b) provam). `mypy src/` continua exercitando m1/dashboard/core reais.

## Conferência cruzada com log do Builder
Bate integralmente. pytest 541/1 ✓; ruff limpo ✓; streamlit ok ✓; mypy Success com override / 50 erros em 12 arquivos sem override ✓ (Builder declarou 50); 4 hashes byte-idênticos ✓; pythonpath ["src"] ✓; parents[2]=0, parents[3]=14 ✓; grep imports raiz VAZIO ✓; jobs/ removido ✓. A distribuição de erros por módulo declarada pelo Builder é consistente com os 12 arquivos observados.

## Guardrails verificados
- score_priorizacao não alterado: sim (config/constants/m1 intocados; hashes idênticos).
- Artefatos M1 preservados: sim (4/4 sha256 byte-idênticos ao Passo 0).
- Validações re-executadas pelo QA (sem bypass):
  - pytest -q → 541 passed, 1 skipped
  - import streamlit_app → ok
  - ruff check . → All checks passed!
  - mypy src/ (com override) → Success (44 files); (sem override) → 50 errors em 12 files (todos do conjunto de 14)
  - grep imports raiz vivos → VAZIO
  - pythonpath → ["src"]
  - parents[2] em pipelines → VAZIO; parents[3] → 14
  - hashes M1 → 4/4 idênticos
  - housekeeping --check → PENDENTE (EXIT=1, não 3) — bloco reconhecido
- Escopo respeitado: sim. Migração mecânica + override value-neutral; nada de lógica/valor alterado.

## Decisão recomendada
Fechar ciclo (Passo 6.0: housekeeping via `scripts/housekeeping_move_block.py BLK-ARCH-01a` + commit/merge) e registrar bloco-filho BLK-ARCH-01b (tipagem dos 14 módulos migrados, remoção do `ignore_errors=true`).
