# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador faz o housekeeping move BLK-DIM-00 backlog→completed e o commit por path).

## VEREDITO
APROVADO

## Justificativa
Suite full verde e idêntica em paralelo e serial (780 passed, 4 skipped); ruff/mypy limpos; ZERO PII em
disco (assert_sem_pii antes de cada to_parquet + 3 parquets inspecionados sem coluna proibida + nenhum
endpoint PII chamado); M1 INTOCADO (READ-ONLY, DEC-001); nada confidencial commitado (só o JSON
metadado D2, sem PII nem dado financeiro real de unidade). Todos os critérios de aceite atendidos.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- `verificar_escopo_master`/branch de ABORT (`n_unidades_view <= 1`) em `scripts/ingerir_growth_api.py`
  não tem teste unitário próprio (é CLI network-bound, fora do CI). A lógica EXISTE e a normalização da
  qual depende (`normalizar_unidade`, incl. strip do sufixo " - UF") É testada (2 testes). Sugestão p/ um
  bloco futuro: extrair a auditoria de escopo para função pura e cobri-la com fixture sintética.
- A interpretação semântica de `churn`/`ticket_medio` (pass-through fiel do agregado) fica para o DIM-01;
  já documentada nos riscos do Builder.

## Testes faltantes
- Teste do branch de ABORT da verificação de escopo master (opcional, ver acima). Não bloqueia: é caminho
  manual/offline e o cliente, ingestão, catchment, calibração e parser têm 47 testes verdes cobrindo
  auth/throttle/backoff/cache/dedup/janelamento/lacunas/maturação/fórmulas.

## Riscos remanescentes
- 1 unidade (CAMPO LIMPO) sem lat/lng → sem catchment (NaN + lacuna registrada). Esperado; insumo de
  auditoria para o DIM-01.
- Os 3 parquets de staging são gitignored e materializados localmente — a regeneração depende de rede +
  `.env`. Não é requisito de fechamento (CI roda só as funções puras com mock).

## Saída literal das validações (re-executadas pelo QA)

### 1. Suite full — gate único (`-n auto`)
```
780 passed, 4 skipped, 168286 warnings in 144.27s (0:02:24)
```

### 1b. Suite full — serial (paridade xdist; IDÊNTICA)
```
780 passed, 4 skipped, 83406 warnings in 113.58s (0:01:53)
```
Contagem paralela == serial → sem flakiness por paralelismo (nenhum teste mal-isolado).

### 1c. Subset do ciclo (`tests/unit/dimensionamento`)
```
47 passed, 2472 warnings in 1.16s
```

### 2. Lint / type
```
=== RUFF (src/motor_expansao/dimensionamento scripts/*.py tests/unit/dimensionamento) ===
All checks passed!
=== MYPY (src/motor_expansao/dimensionamento) ===
Success: no issues found in 7 source files
```

### 3. Smoke imports
```
streamlit_app ok
growth_api_client ok
```
(warnings de ScriptRunContext do streamlit em modo bare são esperados, não erro.)

### 4. Anti-PII — código + dados
Código: `assert_sem_pii` existe em `growth_api_client.py` (case-insensitive vs `config.PII_COLUNAS_PROIBIDAS`
= nome/cpf/email/celular/data_nascimento/cod_aluno/nome_usuario/usuario/cod_usuario/tel/cel/sexo/situacao/
plano/motivo/cargo). Grep de `to_parquet` na camada: 3 ocorrências, TODAS em scripts, cada uma precedida por
`assert_sem_pii` (catchment L38→L40; consolidação L48→L50; ingestão L135→L137). Nenhum endpoint PII
(`/base-cancelados`, `/cancelamento-*`, `/nps-detalhado`, `/alunos-frequencia`, `/cancelados-frequencia`,
`/relatorio-vendas-operacoes`) referenciado no módulo (grep → No matches found).

Dados materializados (só nomes de coluna; nenhuma linha impressa):
```
growth_api_historico.parquet: 29 cols; PII hits=[]
  unidade, master, uf, inauguracao, data, faturamento, pagantes, conversao, convertidos, visitas,
  novos_alunos, ticket_medio, cancelados, churn, em_cobranca, treino_ativo, passagens_gympass,
  alunos_gympass, inadimplente, vendas, ativos_total, NPS, cancelamento_solicitado, passagens_totalpass,
  alunos_totalpass, ticket_medio_gympass, ticket_medio_totalpass, faturamento_sem_agregador,
  ticket_medio_pagantes
unidades_ultra_catchment.parquet: 9 cols; PII hits=[]
  unidade, unidade_norm, uf, lat, lng, pop_captacao, renda_per_capita_captacao, n_setores_captacao, raio_km
base_calibracao_maduras.parquet: 22 cols; PII hits=[]
  unidade, unidade_norm, uf, cidade, metragem, faturamento, alunos_por_m2, ticket_medio_aluno,
  pagantes_steady_state, churn_steady, ticket_steady, cancelados_steady, ativos_total_steady,
  inadimplente_steady, inauguracao, meses_desde_inauguracao, flag_madura, pop_captacao,
  renda_per_capita_captacao, n_setores_captacao, raio_km, lacunas
```
ZERO PII nos 3. `NPS` e `cancelamento_solicitado` são AGREGADOS por unidade/data (score/contagem), não a
PII granular dos endpoints proibidos. `master` é flag booleana de escopo, não nome de usuário.

### 5. Rate limit / resiliência — por teste (`test_growth_api_client.py`)
- `test_429_backoff_e_retenta`: mocka `time.sleep` (cliente E `tenacity.nap`), confirma espera ≥ `BACKOFF_MIN_S` (30s).
- `test_429_persistente_levanta`: 5×429 → `GrowthAPIRateLimitError` (cap de tentativas).
- `test_401_refaz_login_e_retenta`: 401 → relogin (2 POSTs) + retry → 200.
- `test_throttle_dorme_quando_estoura_janela`: janela cheia (`RATE_LIMIT_REQS`) → dorme ~`RATE_LIMIT_WINDOW_S`.
- `test_cache_hit_evita_rede` / `test_force_refresh_ignora_cache`: cache hit/miss idempotente.
- `test_server_error_eh_retentavel` / `_persistente_levanta`: 5xx retentável + cap.
Testes presentes E verdes (não só código).

### 6. READ-ONLY sobre M1 (`git status --porcelain` filtrado)
```
NONE — M1 untouched
```
Nenhum `config.py` raiz, `brasil_*.parquet`, `hexagonos_*`, `pipelines/m1/`, `censo_*`, `dashboard/data.py`
nem `*performance_hex*` aparece como alterado. `unidades_ultra_performance_hex.parquet` é lido READ-ONLY
(gitignored, não sobrescrito no git).

### 7. Vazamento de dados (git status / check-ignore)
```
git check-ignore data/staging/simulador_estrutura.json  → exit 1 (NÃO ignorado, commitable por D2)
git check-ignore *.parquet (3)                           → exit 0 (todos ignorados)
```
`git status` só expõe `data/staging/simulador_estrutura.json` (D2) entre os artefatos de dados; nenhum
`.parquet`/`.env`/`.xlsx` rastreado. JSON inspecionado: só mapeamento célula→driver, defaults de TEMPLATE
e ratios % — sem PII de aluno e sem dado financeiro real de unidade (E12=0.06, E13=8, N11=0.08, R10=140000,
R12=presumido; fórmulas J9/R9/F61/E44/E46 preservadas como string com valor_default=null).

### 8. Escopo master (lógica de normalização/auditoria)
`normalizar_unidade` (maiúscula + sem acento + colapso de espaços + strip do sufixo " - UF" via regex
`\s*-\s*[A-Z]{2}$`) testada: `test_normalizar_unidade_remove_sufixo_uf` (AGUAS LINDAS - GO → AGUAS LINDAS;
não corta "SE" isolado). `verificar_escopo_master`/abort `n_unidades_view <= 1` EXISTE no script (L109-114)
mas sem teste próprio (CLI offline; ver Melhorias opcionais). Não refiz o fetch real (rate limit) — o
Builder reporta 88 unidades na view e 54/54 match, coerente com a normalização verificada.

### Housekeeping (`--check`, pré-move — deve FALHAR)
```
FALHA --check: stub ausente no backlog para BLK-DIM-00
```
Esperado no pré-move. O move (backlog→completed via `scripts/housekeeping_move_block.py`) é do orquestrador
no fechamento; NÃO executei o move. `tasks/test_housekeeping_helper.py` está incluso na suite verde.

## Conferência de no-bypass
Confirmo que NENHUMA validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho
crítico para mascarar verde. A suite full rodou completa (`-n auto` e serial, contagens idênticas), sem
`-p no:xdist` enganoso. As fixtures sintéticas dos testes do `dimensionamento/` (mock de `requests`, mock
de `analisar_ponto_censitario_setores`, `.xlsx` sintético) são isolamento de teste LEGÍTIMO — não há tooling
dependente de config de produção neste bloco (não é caso SOPS). A conferência anti-PII e READ-ONLY foi feita
por LEITURA do código E por INSPEÇÃO dos dados reais materializados em disco, não só pelo log do Builder.

## Conferência cruzada com log do Builder
Bate. Builder reportou: 47 (dimensionamento) verde → confirmado; ruff/mypy limpos → confirmado; import ok →
confirmado; 3 parquets PII-free (29/9/22 cols) → confirmado coluna a coluna; git sem parquet/env/xlsx → 
confirmado; só JSON D2 aparece → confirmado. A suite full (gate único do QA) deu 780 passed/4 skipped
(o Builder não roda a full; rodou só subsets). Divergência de contagem absoluta vs baselines históricos
(532) é de ambiente (Python local, suites integradas posteriores), não defeito — paralelo==serial fecha.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; DEC-001; pesos 0.40/0.60 intocados; nenhum arquivo M1 no diff)
- Artefatos M1 preservados: sim (config.py raiz, brasil_*, hexagonos_*, pipelines/m1/, censo_*, data.py intocados; perf parquet não sobrescrito)
- Validações re-executadas pelo QA (sem bypass):
  - pytest -n auto → 780 passed, 4 skipped
  - pytest serial → 780 passed, 4 skipped (paridade)
  - pytest tests/unit/dimensionamento → 47 passed
  - ruff check → All checks passed!
  - mypy dimensionamento → Success (7 files)
  - import streamlit_app / growth_api_client → ok
  - anti-PII código+dados → ZERO PII; assert antes de cada to_parquet; 0 endpoint PII
  - git status/check-ignore → M1 intocado; só JSON D2 commitable
  - housekeeping --check → FALHA pré-move (esperado)
- Escopo respeitado: sim (só `src/motor_expansao/dimensionamento/`, `scripts/*`, `tests/unit/dimensionamento/`, `.gitignore`, JSON D2; `pyproject.toml` intocado; nenhuma feature fora do plano D1–D7)

## Decisão recomendada
fechar ciclo (orquestrador executa o housekeeping move de BLK-DIM-00 e o commit por path).
