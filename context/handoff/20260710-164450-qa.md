# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (housekeeping do backlog + validação visual de Felipe, pré-deploy)

## VEREDITO
APROVADO

## Justificativa
O escopo foi respeitado à risca (só `components.py` + `constants.py` + `tests/`), o M1 permaneceu
READ-ONLY (diff vazio em `pipelines/m1/` e `config.py`), a suíte FULL fecha verde (1578 passed, 1
skipped) e a redução de payload é REAL e reproduzida por conta própria (M1 23,49 MB → 12,98 MB,
−44,8%). A meta aspiracional de −50% não foi batida no M1, mas por causa fisicamente correta e
fora de D4 (geometria fixa + overhead de serialização do pydeck não escalam com corte de colunas) —
número honesto, não inflado; critério substancialmente atendido.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- Limpeza cosmética adiada (passo F do plano): `_search_hex_payload`/`_build_search_pin_layer` seguem
  com `range(1,15)`/`range(3,15)`, deixando `tooltip_line_7..14` como strings vazias no layer de
  DESTAQUE de busca (não no layer principal). Sem impacto de payload material; opcional em bloco
  sucessor.
- Follow-up de produto já registrado pelo Builder: decidir se abre bloco para atacar
  geometria/overhead de serialização (fora de D4) caso queira empurrar o payload M1 abaixo de 12 MB.

## Testes faltantes
- nenhum. Os 4 modos têm teste de presença exata (`test_tooltip_conjunto_d4_modo_m1/hibrido/
  censitario/residual`), ausência de campos cortados (`test_tooltip_nao_contem_campos_cortados_*`),
  fallback "Não informado" e ausência de `tooltip_line_7..14` no layer principal — todos verdes na
  suíte FULL.

## Riscos remanescentes
- Validação VISUAL humana do tooltip nos 4 modos (Felipe/Vinicius) segue PENDENTE, pós-QA/pré-deploy
  (interpretação "Título conta como 1 dos 7" + duplicação Score-ativo == Score-Censitário no modo
  Censitário). É rede de segurança já prevista; não bloqueia o veredito de QA.
- Deploy BLOQUEADO por guardrail do dia (autorização explícita de Felipe).

## Saída literal das validações (re-executadas pelo QA)

### git diff --stat + status --porcelain
```
 src/motor_expansao/dashboard/components.py | 212 ++++++++-----------------
 src/motor_expansao/dashboard/constants.py  |   1 +
 tests/integration/test_streamlit_app.py    | 242 ++++++++++++++++++++++++++---
 3 files changed, 282 insertions(+), 173 deletions(-)
=====PORCELAIN=====
 M src/motor_expansao/dashboard/components.py
 M src/motor_expansao/dashboard/constants.py
 M tests/integration/test_streamlit_app.py
?? context/handoff/20260710-162530-block-orchestrator.md
?? context/handoff/20260710-171530-planner.md
?? context/handoff/20260710-173000-builder.md
?? scripts/_scratch_atr_alvo_alunos_totais.py
?? scripts/backtest_smartfit_scores.py
```
Só os 3 arquivos de escopo modificados; untracked = snapshots do ciclo + 2 scripts de scratch NÃO
relacionados (não commitar). Nota: o `M dimensionamento/config.py` do snapshot inicial do ciclo NÃO
está mais no working tree — irrelevante para este bloco (fora do escopo de score M1 canônico).

### git diff --stat HEAD -- pipelines/m1/ e config.py
```
(vazio)
```

### git diff -- constants.py  (SÓ 1 linha)
```
@@ -254,6 +254,7 @@ MAP_SOURCE_COLUMNS_HYBRID = [
     "nome_municipio",
+    "faixa_oportunidade",
     "score_setor_2022_calibrado",
```
Grep no diff (components+constants) por `MAP_POINT_LIMIT|_downsample_map_index|
MAP_FIGURE_CACHE_MAX_ENTRIES|build_unified_map_figure_cached|REQUIRED_COLUMNS` → **zero ocorrências**
(caps/cache do BLK-PERF-01b preservados byte-a-byte).

### python -m pytest -q -n auto  (suíte FULL — gate único)
```
1578 passed, 1 skipped, 496483 warnings in 1052.92s (0:17:32)
```
Exit code 0. (Rodado 1× com `-n auto`; 0 falhas, 0 erros de coleção, nenhuma flakiness observada.
Não re-executei em serial por custo — ~17,5 min/execução — sem sinal de teste mal-isolado.)

### ruff check src/ tests/
```
All checks passed!
```

### mypy src/motor_expansao/dashboard/
```
Success: no issues found in 14 source files
```

### python -c "import streamlit_app"
```
import ok
```
(precedido só de WARNINGs benignos de cache do Streamlit sem runtime — esperado fora do servidor.)

### python scripts/loop_guard.py --base HEAD
```
GUARD OK: 0 caminho(s) alterado(s), nenhum proibido.
```

### python scripts/housekeeping_move_block.py BLK-PERF-01c --check
```
FALHA --check: stub ausente no backlog para BLK-PERF-01c
```
ESPERADO nesta fase — o bloco ainda está em `backlog.md`; o move é passo de FECHAMENTO (Passo 6.0 do
/run-cycle), não do QA. Registrado como pendência de fechamento (não bloqueia o veredito técnico).

### Medição de payload reproduzida pelo QA (deck.to_json(), M1, SC 20.373 hexes)
Método do Builder reproduzido: `read_enriched_uf_partition(ENRIQUECIDO_DIR, "SC")` +
`build_map_figure(df, selected_ufs=["SC"], selected_cities=[])`, `len(deck.to_json())`. BEFORE via
`git stash push -- components.py constants.py` (código pré-bloco), AFTER no working tree, restore com
`git stash pop` (working tree limpo confirmado).
```
BEFORE (pré-bloco): 24.056,0 KB = 23,49 MB — tooltip_line_1..14 + tooltip_title (15 cols)
AFTER  (com diff):  13.289,9 KB = 12,98 MB — tooltip_line_1..6 + tooltip_title (7 cols)
Redução: (24056,0 - 13289,9) / 24056,0 = 44,8%
```
**Bate EXATAMENTE com os números do Builder** (24.056,0 → 13.289,9 KB, −44,8%). A redução é real,
substancial e o layer principal M1 tem exatamente o conjunto D4 (título + 6 linhas), sem
`tooltip_line_7..14`.

## Julgamento do critério de payload (item 7)
**APROVADO (critério substancialmente atendido).** A meta de "−50% ou mais" era uma ESTIMATIVA do
REV-03 calibrada sobre o payload M1 de 14 linhas, onde tooltip = 65% do total. Na prática o corte
14→6 campos derruba o **conteúdo de tooltip em si em ~58%**, mas a **geometria (fixa, ~2,0 MB)** e o
**overhead de serialização do pydeck/deck.gl (~4,7 MB, não escala com nº de colunas)** são pisos que
um corte de campos não toca — por isso o TOTAL cai 44,8%, não 50%+. O número é honesto, reproduzido
por mim de forma independente, sem inflar. Os modos Híbrido/Censitário/Residual (−23%~24%) partiam de
um tooltip compacto de 8 linhas (não 14), então o teto de redução é naturalmente menor — esperado,
documentado, não é falha. Reduzir mais exigiria mexer em geometria/overhead (fora de D4, gate de
Felipe "não reabrir"). Portanto: aprovo o número real, não reprovo por aritmética da meta cuja causa
está corretamente explicada.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock do
caminho crítico. A suíte usa fixtures legítimas de teste (prática normal, fora do escopo da regra de
bypass de tooling). A medição de payload rodou contra os builders REAIS sobre o parquet enriquecido
REAL de SC (20.373 hexes), com BEFORE/AFTER pelo mesmo snippet. loop_guard e housekeeping rodaram
contra o estado real do working tree/backlog.

## Conferência cruzada com log do Builder
Bate. `ruff`/`mypy`/smoke limpos (idênticos ao Builder); payload M1 BEFORE/AFTER reproduzido
byte-a-byte (24.056,0 → 13.289,9 KB); diff restrito aos 3 arquivos; constants = 1 linha; M1/config
diff vazio; caps/cache intocados (grep). A suíte FULL (responsabilidade do QA) = 1578 passed, 1
skipped — superconjunto do subset de 254 do Builder, sem regressão. O desvio documentado do Builder
(manter `score_hibrido_fmt` em `_prepare_hybrid_tooltip_fields`) é JUSTIFICADO e INÓCUO: essa coluna
é o "Score do modo ativo" (`_ACTIVE_SCORE_SPEC["hibrido"]`) consumido pelo fallback de busca do modo
Híbrido; removê-la quebraria o próprio teste do passo 19 do plano — manter uma coluna que É consumida
não é scope creep, é correção de um erro do texto do plano.

## Guardrails verificados
- score_priorizacao não alterado: sim (diff vazio em `pipelines/m1/` + `config.py`; só formatação de
  texto exibido no hover; nenhuma coluna-fonte tocada).
- Artefatos M1 preservados: sim (nenhum artefato regenerado; bloco 100% display; loop_guard OK).
- Parâmetros canônicos (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, renda=0.40,
  pop=0.60): inalterados (config.py com diff vazio).
- Acentuação (§2): labels de exibição acentuadas ("Faixa M1", "Score M1/Híbrido/Censitário/Residual",
  "Habitantes", "Renda per capita", "Residual Fitness", "Não informado", "Score Censitário");
  identificadores NÃO acentuados (`mode="m1"/"hibrido"/"censitario"/"residual"`, `tooltip_line_N`,
  `faixa_oportunidade` valor bruto). Conferido no diff.
- Sufixo "- Descartado <5k hab" preservado: sim (mesma condição `flag_pop_min_5k`, agora bloco único).
- Validações re-executadas pelo QA (sem bypass):
  - `git diff --stat`/`--porcelain`/M1-config → só 3 arquivos, M1/config vazio
  - `git diff constants.py` → 1 linha; grep caps/cache no diff → 0
  - `pytest -q -n auto` → 1578 passed, 1 skipped, exit 0
  - `ruff check src/ tests/` → limpo
  - `mypy src/motor_expansao/dashboard/` → limpo
  - `import streamlit_app` → ok
  - `loop_guard --base HEAD` → GUARD OK
  - `housekeeping --check` → falha esperada (bloco no backlog)
  - payload M1 BEFORE/AFTER reproduzido → 23,49 → 12,98 MB (−44,8%), main layer só tooltip_line_1..6
- Escopo respeitado: sim.

## Decisão recomendada
Fechar ciclo. Passos de fechamento (fora do QA): (1) housekeeping do backlog via
`scripts/housekeeping_move_block.py BLK-PERF-01c` (mover para completed.md); (2) validação visual de
Felipe/Vinicius nos 4 modos, pré-deploy; (3) deploy SÓ com autorização explícita de Felipe (guardrail
do dia). Nenhum bloco de correção necessário.
