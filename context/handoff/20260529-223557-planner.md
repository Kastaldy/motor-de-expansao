# Handoff — Planner — BLK-ARCH-01

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Approver (aprovação humana obrigatória — criticidade Alta)

## Entendimento da tarefa
Consolidar mecanicamente em `src/motor_expansao/` o legado vivo de raiz/flat (wrappers, `dashboard/` flat, `ibge_censo.py`, `poi_enrichment.py`, `config.py`), reapontando imports e removendo o legado morto, sem alterar nenhum valor de output M1.

## Decisão de particionamento
**FATIA-1 (este ciclo) — "Consolidação do núcleo M1 + dashboard", EXCLUINDO `jobs/pipelines/*`.**

Este ciclo executa:
- (A) Remoção dos 3 wrappers de raiz (`base_h3_brasil.py`, `hex_enrichment.py`, `fase1_bi_exports.py`) e reaponte dos imports de teste para `motor_expansao.pipelines.m1.*`.
- (B) Migração de `dashboard/constants.py` + `dashboard/utils.py` para `src/motor_expansao/dashboard/`, inversão dos imports e remoção do pacote `dashboard/` flat (vazio).
- (D) Migração de `ibge_censo.py` + `poi_enrichment.py` para `src/motor_expansao/pipelines/m1/`, limpando os branches mortos `jobs.pipelines.*`.
- (E) Migração de `config.py` para `src/motor_expansao/config.py`, limpando o branch morto `api.config`.

**Justificativa da fatia:** os blocos A/B/D/E formam o cluster do M1 oficial + dashboard, fortemente entrelaçados por imports (`config` é consumido por `core/constants.py`, `base_h3_brasil`, `hex_enrichment`, `poi_enrichment`; `ibge_censo`/`poi_enrichment` são consumidos por `hex_enrichment`/`fase1_bi_exports`; `dashboard.constants` é consumido por `fase1_bi_exports` e por todo `src/.../dashboard/*`). Migrá-los juntos elimina TODAS as dependências de raiz do M1 oficial e do dashboard num único ciclo, com testes verdes a cada passo. `config.py` ENTRA nesta fatia (decisão do ponto 3): seu acoplamento, embora amplo, é inteiramente interno ao cluster M1/dashboard já sendo migrado; deixá-lo para trás manteria uma dependência de raiz justamente nos módulos que estamos consolidando.

**Vira bloco-filho no backlog (NÃO neste ciclo):**
- **BLK-ARCH-01a — Migrar `jobs/pipelines/*` (21 módulos) para `src/motor_expansao/pipelines/`.** Evidência: os 21 módulos de `jobs/pipelines/` importam apenas stdlib, libs de terceiros e `jobs.pipelines.*` entre si (cluster autocontido; grep confirmou que NENHUM importa `config`, `ibge_censo`, `poi_enrichment` ou `dashboard`). Não são importados por `streamlit_app.py`. Volume alto (21 arquivos) + acoplamento interno (`fase_a_*`, `validar_penetracao` ↔ `comparar_geofusion`, `enriquecer_outputs_residual` ↔ `gerar_carteira`) tornam um big-bang arriscado. Separar é a recomendação forte do Block Orchestrator e é seguro porque o cluster é isolado.

**`pythonpath`:** NÃO mexer nesta fatia. Mesmo após FATIA-1, `jobs/` (e `fora_primeira_fase/`) continuam importáveis de raiz; remover `"."` quebraria o sub-bloco seguinte. A limpeza de `"."` fica para o final de BLK-ARCH-01a.

## Plano técnico
Pré-requisito de cada passo: árvore limpa (`git status` sem pendências não relacionadas) e `pytest -q` verde no estado anterior. Sempre `git mv` para preservar histórico. NÃO renomear funções, NÃO mudar assinaturas, NÃO alterar valores.

**Passo 0 — Captura de baseline e hashes (prova de não-mutação, ANTES).**
- Comando:
  ```
  pytest -q
  python -c "import hashlib,pathlib; [print(hashlib.sha256(pathlib.Path(p).read_bytes()).hexdigest(), p) for p in ['data/staging/brasil_priorizados.parquet','data/staging/brasil_estrutural.parquet','data/staging/hexagonos_brasil_oportunidades.parquet','data/outputs/hexagonos_brasil_dashboard.parquet']]"
  ```
- Verificação: `pytest -q` = `532 passed, 1 skipped` (baseline). Salvar a saída de hashes em nota do ciclo (`data/reports/` ou comentário no PR) para recomparar no Passo final. Critério: os 4 arquivos existem e produzem hash (confirmado pelo Planner via `ls`).

**Passo 1 — Mover `dashboard/constants.py` para o pacote.**
- `git mv dashboard/constants.py src/motor_expansao/dashboard/constants.py`
- Editar imports que apontam para `dashboard.constants` → `motor_expansao.dashboard.constants` nos call sites:
  - `streamlit_app.py:11`
  - `src/motor_expansao/dashboard/pages.py:13` e `:2535`
  - `src/motor_expansao/dashboard/data.py:11`
  - `src/motor_expansao/dashboard/components.py:11`
  - `src/motor_expansao/pipelines/m1/fase1_bi_exports.py:11`
  - `src/motor_expansao/dashboard/utils.py:5` (será movido no Passo 2; por ora aponte para o novo caminho)
  - testes: `tests/integration/test_streamlit_app.py:7,1713,1778`, `tests/integration/test_fase1_bi_exports.py:4`, `tests/integration/test_expansao_dominio.py:11`, `tests/unit/test_expansao_dominio.py:599`
- Verificação: `pytest -q` -> verde (`532 passed, 1 skipped`).

**Passo 2 — Mover `dashboard/utils.py` para o pacote.**
- `git mv dashboard/utils.py src/motor_expansao/dashboard/utils.py`
- Em `src/motor_expansao/dashboard/utils.py`: trocar `from dashboard.constants import RESIDUAL_SCORE_BANDS` → `from motor_expansao.dashboard.constants import RESIDUAL_SCORE_BANDS`.
- Editar call sites de `dashboard.utils` → `motor_expansao.dashboard.utils`:
  - `streamlit_app.py:42`
  - `src/motor_expansao/dashboard/pages.py:26` e `:2202`
  - `src/motor_expansao/dashboard/components.py:26`
- Verificação: `pytest -q` -> verde.

**Passo 3 — Remover o pacote `dashboard/` flat.**
- `git rm dashboard/__init__.py` (está vazio) e confirmar que o diretório `dashboard/` ficou sem arquivos rastreados.
- Grep de confirmação: `from dashboard.constants` / `from dashboard.utils` / `import dashboard` não devem retornar nada fora de `context/` e snapshots.
  - Comando: `rg -n "from dashboard(\.|\s+import)|import dashboard" --glob '!context/**'` → vazio.
- Verificação: `pytest -q` -> verde.

**Passo 4 — Migrar `ibge_censo.py` para `src/`.**
- `git mv ibge_censo.py src/motor_expansao/pipelines/m1/ibge_censo.py`
- Em `src/motor_expansao/pipelines/m1/hex_enrichment.py:30-35`: substituir o bloco try/except por import direto `from motor_expansao.pipelines.m1.ibge_censo import IBGECenso, carregar_lookup_municipios_ibge` (remove o branch morto `jobs.pipelines.ibge_censo` e o fallback `from ibge_censo`).
- Em `src/motor_expansao/pipelines/m1/fase1_bi_exports.py:21-24`: substituir o try/except por `from motor_expansao.pipelines.m1.ibge_censo import carregar_lookup_municipios_ibge`.
- Reapontar testes: `tests/integration/test_hex_enrichment_brasil.py:11` e as 9 ocorrências `from ibge_censo import IBGECenso` em `tests/contracts/test_fontes_gratuitas.py` (linhas 51,61,73,92,115,123,143-cluster,297,319,337 — usar grep para listar exatas) → `from motor_expansao.pipelines.m1.ibge_censo import IBGECenso`.
- Verificação: `pytest -q` -> verde.

**Passo 5 — Migrar `poi_enrichment.py` para `src/`.**
- `git mv poi_enrichment.py src/motor_expansao/pipelines/m1/poi_enrichment.py`
- Em `hex_enrichment.py:30-35` (mesmo bloco do Passo 4): incluir `from motor_expansao.pipelines.m1.poi_enrichment import POIEnricher` no import direto.
- Em `poi_enrichment.py:19-22`: trocar o try/except `api.config`/`config` por `from motor_expansao.config import settings` (depende do Passo 6; se executar este passo antes do 6, manter temporariamente `from config import settings` e ajustar no Passo 6). RECOMENDAÇÃO: executar Passo 6 ANTES do ajuste de import de config aqui — ver ordem abaixo.
- Reapontar testes: `tests/integration/test_hex_enrichment_brasil.py:12` e as ~13 ocorrências `from poi_enrichment import POIEnricher` em `tests/contracts/test_fontes_gratuitas.py` → `from motor_expansao.pipelines.m1.poi_enrichment import POIEnricher`.
- Verificação: `pytest -q` -> verde.

**Passo 6 — Migrar `config.py` para `src/` e limpar branches `api.config` mortos.**
- `git mv config.py src/motor_expansao/config.py`
- Substituir o try/except `from api.config import settings` / `from config import settings` por `from motor_expansao.config import settings` em:
  - `src/motor_expansao/core/constants.py:9-12`
  - `src/motor_expansao/pipelines/m1/base_h3_brasil.py:30` (verificar bloco try/except no arquivo)
  - `src/motor_expansao/pipelines/m1/hex_enrichment.py:25-28`
  - `src/motor_expansao/pipelines/m1/poi_enrichment.py:19-22` (já no novo caminho após Passo 5)
- NÃO tocar `fora_primeira_fase/*` (fora de escopo; seus `from config import settings` ficam órfãos por design — `"."` ainda no pythonpath, então não quebram coleta porque não são coletados/são fora de fase).
- Verificação: `pytest -q` -> verde.
  - Nota de ordem: por causa do acoplamento de `config`, executar Passo 6 logo após o 4 e antes/junto do ajuste de config do 5 simplifica. Ordem segura recomendada: 1→2→3→4→6→5. Cada um termina com `pytest -q` verde.

**Passo 7 — Remover os 3 wrappers de raiz e reapontar imports de teste.**
- `git rm base_h3_brasil.py hex_enrichment.py fase1_bi_exports.py` (raiz).
- Reapontar imports:
  - `tests/integration/test_base_h3_brasil.py:6` `from base_h3_brasil import ...` → `from motor_expansao.pipelines.m1.base_h3_brasil import ...`
  - `tests/unit/test_scoring.py:4` `from hex_enrichment import ...` → `from motor_expansao.pipelines.m1.hex_enrichment import ...`
  - `tests/integration/test_hex_enrichment_brasil.py:3` `from hex_enrichment import ...` → idem
  - `tests/contracts/test_fontes_gratuitas.py` (`from hex_enrichment import ...` linhas 365,371,382,392,411,417,431,437,446,463,501) → `from motor_expansao.pipelines.m1.hex_enrichment import ...`
  - `tests/integration/test_fase1_bi_exports.py:5` `from fase1_bi_exports import ...` → `from motor_expansao.pipelines.m1.fase1_bi_exports import ...`
  - NÃO tocar `fora_primeira_fase/tests/test_scoring.py:21` (fora de escopo).
- Grep de confirmação (módulos removidos não podem ser importados em código vivo):
  - `rg -n "^\s*from (base_h3_brasil|hex_enrichment|fase1_bi_exports|ibge_censo|poi_enrichment|config) import" --glob '!fora_primeira_fase/**' --glob '!context/**'` → vazio.
- Verificação: `pytest -q` -> verde.

**Passo 8 — Lint, type-check e prova final de não-mutação.**
- `ruff check .` -> limpo (corrigir imports não usados/ordenação resultantes das movimentações).
- `mypy src/` -> sem erros (notas `annotation-unchecked` toleradas).
- `python -c "import streamlit_app; print('ok')"` -> imprime `ok`.
- Recapturar os 4 hashes (mesmo comando do Passo 0) e comparar idêntico ao baseline.
- Verificação final: `pytest -q` = `532 passed, 1 skipped` e hashes idênticos.

## Arquivos afetados
**Mover (`git mv`):**
- `dashboard/constants.py` → `src/motor_expansao/dashboard/constants.py`
- `dashboard/utils.py` → `src/motor_expansao/dashboard/utils.py`
- `ibge_censo.py` → `src/motor_expansao/pipelines/m1/ibge_censo.py`
- `poi_enrichment.py` → `src/motor_expansao/pipelines/m1/poi_enrichment.py`
- `config.py` → `src/motor_expansao/config.py`

**Remover (`git rm`):**
- `dashboard/__init__.py` (vazio) + diretório `dashboard/`
- `base_h3_brasil.py`, `hex_enrichment.py`, `fase1_bi_exports.py` (wrappers de raiz)

**Editar imports (não-teste):**
- `streamlit_app.py` (linhas 11, 42)
- `src/motor_expansao/dashboard/{pages.py (13,26,2202,2535), data.py (11), components.py (11,26), utils.py (5)}`
- `src/motor_expansao/pipelines/m1/{hex_enrichment.py (25-35), fase1_bi_exports.py (11,21-24), base_h3_brasil.py (30)}`
- `src/motor_expansao/core/constants.py (9-12)`

**Editar imports (testes):**
- `tests/integration/test_streamlit_app.py`, `test_fase1_bi_exports.py`, `test_expansao_dominio.py`, `test_base_h3_brasil.py`, `test_hex_enrichment_brasil.py`
- `tests/unit/test_scoring.py`, `test_expansao_dominio.py`
- `tests/contracts/test_fontes_gratuitas.py` (~22 ocorrências ibge/poi + 11 hex_enrichment)

## Blocos-filhos a registrar no backlog (se particionou)
- **BLK-ARCH-01a — Migrar `jobs/pipelines/*` (21 módulos) para `src/motor_expansao/pipelines/` e limpar `pythonpath`.** Escopo: mover os 21 módulos (sugestão de subpacotes por grupo funcional: `fase_a/`, `mercado/`, `dominio/`, `penetracao/`, `normalizacao/` — Planner do sub-bloco decide), atualizar imports internos `jobs.pipelines.*` e os testes de integração/unit que os consomem; só então remover `"."` de `pythonpath` se grep confirmar zero dependência de raiz viva. Justificativa: cluster autocontido (não importa config/ibge/poi/dashboard) mas com volume alto e acoplamento interno → big-bang arriscado se feito junto da FATIA-1. Criticidade provável: Média/Alta (toca pipelines de mercado/residual/fase-A).

## Dependências
- BLK-OPS-02 (CI verde) já satisfeita.
- Nenhuma dependência externa nova; sem alteração de `pyproject.toml` (exceto possivelmente nada nesta fatia — `packages = ["src/motor_expansao"]` já cobre os novos arquivos; `pythonpath` mantido).
- BLK-ARCH-01a depende da conclusão desta fatia (para que o legado de raiz remanescente seja apenas `jobs/`).

## Riscos técnicos
- **Import circular dashboard** (constants ↔ utils ↔ pages/components): mitigado pela ordem 1→2→3 (constants antes de utils; só remover o pacote flat depois que ambos estão no `src/`).
- **Branches try/except mortos** (`api.config`, `jobs.pipelines.ibge_censo`): ao trocar por import direto, garantir que a implementação real (raiz→`src/`) continua resolvível; rodar `pytest -q` após cada passo.
- **`test_fontes_gratuitas.py`** tem ~33 imports legados (ibge/poi/hex_enrichment) — risco de esquecer ocorrência; mitigar com grep exaustivo antes do Passo 8.
- **`pythonpath = ["."]`** mascara import de raiz remanescente: o Passo 7 inclui grep explícito por módulos removidos para detectar resíduos. NÃO remover `"."` nesta fatia (jobs/ depende).
- **`fora_primeira_fase/*`** referencia `config`/`jobs.*` órfãos: NÃO consertar; permanece importável só por `"."` no pythonpath e não é coletado pelos testes em fase.
- **Caminho do parquet do dashboard**: confirmado em `data/outputs/hexagonos_brasil_dashboard.parquet` (não `data/staging/`); `fase1_bi_exports.py:28` `DASHBOARD_PATH` confirma. Os outros 3 oficiais estão em `data/staging/`.

## Critérios de aceite finais
- Nenhum import vivo aponta para caminho legado removido (grep limpo, excluindo `fora_primeira_fase/` e `context/`).
- `python -c "import streamlit_app; print('ok')"` retorna `ok`.
- `pytest -q` = `532 passed, 1 skipped` (baseline preservado).
- `ruff check .` limpo; `mypy src/` sem erros (notas `annotation-unchecked` toleradas).
- Pacote `dashboard/` flat removido; `jobs/pipelines/*` intocado (vai para BLK-ARCH-01a).
- Hash sha256 idêntico pré/pós nos 4 artefatos oficiais.

## Validações obrigatórias
- Comando: `pytest -q` / Critério mínimo: `532 passed, 1 skipped` ao final de CADA passo.
- Comando: `python -c "import streamlit_app; print('ok')"` / Critério: imprime `ok`.
- Comando: `ruff check .` / Critério: sem erros.
- Comando: `mypy src/` / Critério: sem erros (notas annotation-unchecked OK).
- Comando: `rg -n "^\s*from (base_h3_brasil|hex_enrichment|fase1_bi_exports|ibge_censo|poi_enrichment|config) import" --glob '!fora_primeira_fase/**' --glob '!context/**'` / Critério: vazio.
- Comando: `rg -n "from dashboard(\.|\s+import)|import dashboard" --glob '!context/**'` / Critério: vazio.

## Prova de não-mutação M1
- Artefatos a hashear (todos CONFIRMADOS existentes no working tree via `ls`):
  - `data/staging/brasil_priorizados.parquet` ✓
  - `data/staging/brasil_estrutural.parquet` ✓
  - `data/staging/hexagonos_brasil_oportunidades.parquet` ✓
  - `data/outputs/hexagonos_brasil_dashboard.parquet` ✓ (NÃO existe em `data/staging/`; caminho real é `data/outputs/`, confirmado por `fase1_bi_exports.py:28`)
- Comando (Passo 0 ANTES e Passo 8 DEPOIS):
  ```
  python -c "import hashlib,pathlib; [print(hashlib.sha256(pathlib.Path(p).read_bytes()).hexdigest(), p) for p in ['data/staging/brasil_priorizados.parquet','data/staging/brasil_estrutural.parquet','data/staging/hexagonos_brasil_oportunidades.parquet','data/outputs/hexagonos_brasil_dashboard.parquet']]"
  ```
- Critério: as 4 linhas de hash são byte-idênticas pré/pós. Como esta fatia é puramente import-rewiring (não roda pipeline nem regrava parquet), espera-se que os arquivos sequer sejam tocados — a recomparação é a garantia formal. Os hashes NÃO dependem de regerar o pipeline (fora de alcance deste ciclo), apenas de ler os parquets já materializados no working tree.

## Fora de escopo
- `jobs/pipelines/*` (vai para BLK-ARCH-01a).
- Remover `"."` de `pythonpath` (depende de BLK-ARCH-01a).
- `fora_primeira_fase/*` (API/PostGIS/M2-M3/PowerBI; imports órfãos NÃO consertar).
- `concorrentes/geo_skyfit.py` (script standalone isolado, sem importadores; deixar onde está — baixo acoplamento, não justifica mover nesta fatia).
- Qualquer mudança de comportamento/scoring/assinatura/valores M1; renomeações; otimizações.
- Mudar `H3_RESOLUTION`, pesos `renda=0.40`/`pop=0.60`, `DIST_MIN_ULTRA_KM`, `RENDA_MIN`, etc.

## Alerta de aprovação
ATENÇÃO: tarefa Alta (migração que toca o pipeline M1, ainda que mecanicamente). Aprovação humana obrigatória antes do Builder. O usuário deve ler este handoff e confirmar.

## Guardrails ativos
- **Migração é MECÂNICA: não pode alterar nenhum valor de output M1 (provado por hash). Score, ranking, carteira, plano e artefatos oficiais do M1 não mudam.**
- `score_priorizacao` é o score oficial; nenhuma trilha pode alterá-lo sem aprovação explícita.
- Ler o repositório real antes de editar; preservar 100% das linhas/colunas oficiais do M1.
- `config.py`, `CLAUDE.md` e `PRD.md` são fontes canônicas de parâmetros/guardrails (mover `config.py` NÃO altera seus valores).
- Toda mudança relevante entra com teste; nenhum PR sobe com CI quebrado; avançar só com CI verde a cada passo.
- Não criar dependência de API ao vivo no dashboard de produção.
- Um bloco por vez; não expandir escopo.
- Usar `git mv` para preservar histórico.
