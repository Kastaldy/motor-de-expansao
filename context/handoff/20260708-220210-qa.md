# Handoff — BLK-REV-05 QA (2026-07-08T22:02:10)

## Bloco
BLK-REV-05 — Diagnóstico de gargalo: seleção de hex + cenário múltiplo

## Decisão
**APROVADO — Fechar ciclo.**

## Validações re-executadas de forma independente (saída literal)

### 1. Suite FULL
```
python -m pytest -q 2>&1 | tail -5
→ 1538 passed, 4 skipped, 19 warnings in 528.81s (0:08:48)
```
Zero falhas. (O único warning citado é FutureWarning pré-existente de `read_json` em
`test_batch_viabilidade.py`, não relacionado a este bloco.)

### 2. loop_guard
```
python scripts/loop_guard.py --base HEAD
→ GUARD OK: 0 caminho(s) alterado(s), nenhum proibido.
```

### 3. git diff READ-ONLY M1
```
git diff HEAD -- src/motor_expansao/pipelines/m1/ src/motor_expansao/config.py
→ (vazio; EXIT=0)
```

### 4. Relatório existe e é gitignored
```
ls -la data/analysis/diagnostico_selecao_hex.md
→ -rw-r--r-- 1 loop loop 16136 Jul  8 21:51 data/analysis/diagnostico_selecao_hex.md
git check-ignore -v data/analysis/diagnostico_selecao_hex.md
→ .gitignore:123:data/analysis/  (gitignored — correto)
```

### 5. Housekeeping
```
python scripts/housekeeping_move_block.py BLK-REV-05 --check   (antes) → EXIT=1 (stub ausente)
python scripts/housekeeping_move_block.py BLK-REV-05 --date 2026-07-08
  → OK: movido BLK-REV-05 (883 bytes) -> tasks/completed.md; stub em tasks/backlog.md.
python scripts/housekeeping_move_block.py BLK-REV-05 --check   (depois) → EXIT=0
python -m pytest tests/unit/test_housekeeping_helper.py -q     → 10 passed
```

## Verificação de conteúdo do relatório

- **`agregar_cenario_multihex` NÃO é o gargalo:** CONFIRMADO. Relatório reporta 10–21 ms
  (H1), coerente com o BLK-REV-01 B5 (`perf_baseline_app_2026.md`: SC 1/5/20 hex = 10/12/13 ms).
- **Rebuild do mapa (700–900 ms) é o gargalo real:** CONFIRMADO. Coerente com B3 do REV-01
  (`build_map_figure` SC 677–715 ms; AM híbrido até 3253 ms em B4).
- **Recomendação `@st.fragment` para os controles multi-hex:** COERENTE com a análise. O
  relatório mostra que `_render_multihex_controls`/`_render_multihex_kpis` estão FORA do
  fragmento atual; envolvê-los evita o rebuild. Trade-off de produto (destaque laranja só no
  próximo rerun completo) está honestamente declarado.

### Spot-check do código (validação independente das hipóteses)
```
grep _render_multihex pages.py:
  2602: def _render_multihex_controls(...)
  2667: def _render_multihex_kpis(df, multihex_ids)
  4482: _render_multihex_controls(active_hex_id, multihex_ids)   ← fora do fragmento (H4 ok)

grep @st.cache*/@st.fragment em components.py + pages.py:
  components.py: (nenhum @st.cache_data/@st.cache_resource nos builders de mapa) → H2 confirmada
  pages.py:4099: @st.fragment (único; sobre render_mapa_pydeck_fragment) → H4 confirmada

build_unified_map_figure @ components.py:3028 (sem cache)
_build_multihex_selection_layer @ components.py:3225
render_mapa_pydeck_fragment @ pages.py:4100, on_select="rerun" @ pages.py:4137
```
Todas as afirmações de código (H1, H2, H4) batem com o repo real. H3 é apresentada com
ressalvas honestas (picklabilidade do `pdk.Deck`), sem sobre-afirmar.

## Critérios de aprovação
- [x] Zero novas falhas na suite FULL (1538 passed, 4 skipped)
- [x] loop_guard GUARD OK
- [x] git diff M1/config vazio (READ-ONLY M1)
- [x] Relatório existe, gitignored, conteúdo coerente com REV-01 e com o código
- [x] Housekeeping --check EXIT=0

## READ-ONLY M1
Bloco puramente de diagnóstico. Zero alteração em `pipelines/m1/`, `config.py`, artefatos
oficiais, pesos/fórmula, deploy, secrets ou CI. DEC-001 intacta.

## Recomendação final
**Fechar ciclo BLK-REV-05.** A recomendação técnica (`@st.fragment` para o painel multi-hex)
é candidata natural a um bloco de implementação sucessor (não é escopo deste diagnóstico).
