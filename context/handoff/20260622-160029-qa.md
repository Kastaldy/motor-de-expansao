# Handoff — QA → (loop encerrado)

## Skill que gerou este handoff
QA

## Próxima Skill recomendada
Nenhuma — loop autônomo encerrado (sem mais blocos loop-safe pendentes).

## Bloco
BLK-DIM-22 — UI: exportar simulador de viabilidade como Excel

## Criticidade
**Média** — novo entregável UI; READ-ONLY sobre M1. Loop autônomo, sem gate humano.

---

## VEREDITO: APROVADO

---

## Evidências do gate

| Etapa | Resultado |
|-------|-----------|
| `pip install openlocationcode>=1.0.1` | EXIT 0 |
| `pytest -q` (suite completa serial) | **1054 passed, 4 skipped, 18 warnings** in 179s |
| `ruff check .` | All checks passed! |
| `mypy excel_export.py pages.py --ignore-missing-imports` | Success: no issues found in 2 source files |
| `python -c "import streamlit_app"` | import ok |
| Housekeeping `--check` | OK (stub no backlog + bloco em completed.md), EXIT 0 |

## Verificações específicas BLK-DIM-22 — todas confirmadas

- **CA-12a..g todos PASS** (8 testes verbose verdes): retorna_bytes_nao_vazio, xlsx_valido_abrivel, 4_abas_presentes, aba_resumo_contem_campos_chave, aba_curva_tem_60_linhas, sem_escrita_em_disco, nome_ponto_no_resumo, sensibilidade_valores_corretos.
- **`excel_export.py` usa `openpyxl`** (única dependência Excel; ZERO `xlsxwriter`).
- **Retorna `bytes` via `BytesIO.getvalue()`** — sem escrita em disco (`wb.save(buf)` → `buf.getvalue()`).
- **4 abas**: Resumo (15 KPIs), DRE (waterfall steady-state), Sensibilidade (pivot alunos×fator_aluguel com cor condicional), Curva (60 linhas).
- **`viabilidade_ponto.py` NÃO alterado** (fora do commit BLK-DIM-22 `e3a4749`).
- **`simulador.py` NÃO alterado** (novo módulo criado, não editado).
- **Artefatos M1 não tocados**: `config.py` raiz, `pipelines/m1`, parquets — intactos. (Nota: `dimensionamento/config.py` aparece como modificado no working tree apenas por diferença de EOL CRLF→LF; conteúdo byte-idêntico, nenhum valor alterado — não é o config raiz do M1.)
- **Dashboard button**: `st.download_button` em `pages.py` com import lazy + `try/except` fallback gracioso ("Export Excel indisponivel.").

## Guardrails M1 (DEC-001)
Pesos `renda=0.40`/`pop=0.60`, `score_priorizacao`, `hex_score_estrutural`, carteira, plano e artefatos oficiais: **INALTERADOS**. Camada paralela READ-ONLY.

## Commit do bloco
`e3a4749 feat(BLK-DIM-22)` — toca apenas `pages.py` (+17), `excel_export.py` (novo), `test_excel_export.py` (novo) + docs de handoff/task.

## Estado do loop
Nenhum bloco `loop-safe` pendente em `tasks/backlog.md` (os únicos marcadores Autonomia restantes são explicitamente "manual / NÃO loop-safe"). **LOOP_DONE criado.**
