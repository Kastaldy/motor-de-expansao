# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador): mover BLK-LTV-02 backlog→completed via `scripts/housekeeping_move_block.py` no Passo 6.0.

## VEREDITO
APROVADO

## Justificativa
Camada paralela READ-ONLY sobre o M1 entregue conforme o plano: `unidade_territorio_retencao.parquet` (88 × 36, 56 com hex, 59 prob_absoluta) reproduzido deterministicamente; suíte full verde (1169 passed / 1 skipped, 0 falhas, 0 collection errors); mtime dos 4 artefatos M1 byte-idêntico após re-run; zero imports proibidos; ruff/mypy limpos. Todos os critérios de aceite do Planner satisfeitos.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- O caveat de "7 hexes sem dado censitário" (pop_total_setor_2022 NaN entre os 56 matched) não tem asserção dedicada nos testes — está documentado no docstring e no handoff do Builder, mas um teste explícito distinguindo esses NaN dos 32 sem-hex reforçaria o contrato. Opcional; não bloqueia.

## Testes faltantes
- nenhum bloqueador. Os 9 casos obrigatórios do Planner estão presentes + 2 extras de colagem de schema (`test_output_has_36_columns`, `test_output_columns_count_in_result`). Cobrem: preservação de linhas (LEFT join), NaN por hex ausente, normalização de chave, gate USAR_PROB_ABSOLUTA (Sim/Nao), presença/ordem de OUTPUT_COLUMNS, ausência de imports M1, features presentes com hex, RuntimeError em mismatch de join.

## Riscos remanescentes
- Confound de maturidade (estrutural): sem data de abertura por unidade (`maturacao_status` 100% indisponível), qualquer análise downstream de LTV × território mistura localização com tempo de operação. Registrado no docstring; mitigação = aguardar insumo (DEC-008). Não é defeito deste bloco.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte full (gate único) — `python -m pytest -n auto -q`
```
1169 passed, 1 skipped, 297778 warnings in 237.89s (0:03:57)
```
0 failed, 0 collection errors. (Ciclo anterior viu 1130 passed/1 skipped; +11 unitários novos → 1141 esperado pelo prompt, mas a coleta real deste ambiente é 1169 passed/1 skipped — VERIFICADO, não assumido; a diferença vs. a estimativa do prompt é de contagem de ambiente, todos passam.)

### 2. Smoke import — `python -c "import streamlit_app; print('import ok')"`
```
2026-07-01 12:04:13.884 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
2026-07-01 12:04:13.885 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
import ok
```
(apenas warnings esperados do Streamlit em modo bare)

### 3. ruff — `ruff check src/motor_expansao/lifetime/join_territorio_retencao.py tests/unit/test_join_territorio_retencao.py`
```
All checks passed!
```

### 4. mypy — `mypy src/`
```
src\motor_expansao\pipelines\m1\ibge_censo.py:158: note: By default the bodies of untyped functions are not checked, consider using --check-untyped-defs  [annotation-unchecked]
src\motor_expansao\pipelines\m1\ibge_censo.py:159: note: By default the bodies of untyped functions are not checked, consider using --check-untyped-defs  [annotation-unchecked]
src\motor_expansao\pipelines\m1\poi_enrichment.py:51: note: By default the bodies of untyped functions are not checked, consider using --check-untyped-defs  [annotation-unchecked]
Success: no issues found in 86 source files
```
Apenas notes `annotation-unchecked` preexistentes; zero erros novos.

### 5. Deliverable content check
```
shape (88, 36)
with_hex 56
prob_abs_notna 59
cols 36
missing_expected []
```
Conforme esperado (shape (88,36), with_hex ~56, LTV/derivadas presentes, missing_expected []). `prob_abs_notna=59` bate com o gate USAR_PROB_ABSOLUTA (59 "Sim").

### 6. M1 READ-ONLY — mtime antes/depois de re-executar `python -m motor_expansao.lifetime.join_territorio_retencao`
```
BEFORE {'brasil_priorizados.parquet': 1780501631, 'hexagonos_brasil_oportunidades.parquet': 1780501655, 'brasil_estrutural.parquet': 1780501621, 'hexagonos_mercado_mapeado.parquet': 1781205056}
[BLK-LTV-02] Total linhas: 88 | hex_id notna: 56/88
[BLK-LTV-02] CONFIABILIDADE_UNIDADE: Absoluto OK=59, Apenas Ranking=16, Instavel (N/eventos baixos)=6, Nao usar (descalibrado e ranking fraco)=4, Inconclusivo (poucos eventos p/ AUC)=3
[BLK-LTV-02] prob_cancel_90d_media_absoluta: 59 valores / 29 NaN
[BLK-LTV-02] CAVEAT: 32 unidades sem hex_id têm features territoriais = NaN (confound de maturidade também presente; sem data de abertura no parquet Lifetime)
[BLK-LTV-02] Parquet gravado: .../data/staging/unidade_territorio_retencao.parquet
AFTER  {'brasil_priorizados.parquet': 1780501631, 'hexagonos_brasil_oportunidades.parquet': 1780501655, 'brasil_estrutural.parquet': 1780501621, 'hexagonos_mercado_mapeado.parquet': 1781205056}
```
Os 4 mtimes BEFORE == AFTER (byte-idênticos). Inspeção de código: única escrita = `result.to_parquet(output_path)` (linha 287) → `unidade_territorio_retencao.parquet`. Grep de imports proibidos (`pipelines.m1|dashboard|censo|.api`) só casa a linha 10 do DOCSTRING (comentário do guardrail), nenhum import real.

### 7. Housekeeping (PRE-close) — `python scripts/housekeeping_move_block.py BLK-LTV-02 --check`
```
FALHA --check: stub ausente no backlog para BLK-LTV-02
---exit 1---
### BLK-LTV-02 — Join territorial (pendurar retenção/LTV no hexágono da unidade)   (linha 952 de tasks/backlog.md)
```
Estado PRE-close correto: bloco ainda em backlog (heading `### BLK-LTV-02` íntegro na linha 952), stub ainda ausente. O move backlog→completed acontece NO FECHAMENTO pelo orquestrador (Passo 6.0), precedente BLK-DIM-18/BLK-TP-05. `tests/unit/test_housekeeping_helper.py` passou dentro da suíte full.

### 8. Escopo — `git status --porcelain`
```
 M context/handoff.md
 D data/raw/ibge/malha_brasil.geojson
 D data/raw/ibge/malha_uf_brasil.geojson
 M tasks/current_task.md
?? context/handoff/20260701-112606-block-orchestrator.md
?? context/handoff/20260701-113211-block-orchestrator.md
?? context/handoff/20260701-113655-planner.md
?? context/handoff/20260701-115652-builder.md
?? scripts/backtest_smartfit_scores.py
?? src/motor_expansao/lifetime/join_territorio_retencao.py
?? tests/unit/test_join_territorio_retencao.py
```
Apenas: módulo novo + teste novo + arquivos de orquestração (handoff/current_task) + snapshots untracked. Os paths pré-sujos (2 geojson deletados + `scripts/backtest_smartfit_scores.py` untracked) permanecem no estado original — NÃO tocados pelo Builder. O parquet entregável fica em `data/staging` (gitignored) — não aparece no git status, esperado.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico de produção. Os testes unitários usam `unittest.mock.patch` nos três `_load_*` para injetar DataFrames sintéticos — isso é ISOLAMENTE LEGÍTIMO de teste unitário (a regra de no-bypass mira validação de TOOLING que depende de config/artefatos de produção, ex. SOPS; NÃO se aplica a fixtures de teste). O gate REAL é a suíte full que re-executei sem bypass + a verificação do deliverable e mtime contra os parquets REAIS de produção (`data/staging/*.parquet`), executando o módulo de fato. Nenhum verde por contorno.

## Conferência cruzada com log do Builder
Bate. Builder reportou 224 no subconjunto impactado, ruff "All checks passed", mypy "no issues found in 86 source files", deliverable (88,36)/56 with hex, mtimes M1 inalterados e log de cobertura — todos confirmados na minha re-execução independente. A suíte full (não rodada pelo Builder) rodou 1× aqui: 1169 passed/1 skipped. Única divergência de escopo de teste explicada pelo Builder (ajuste de `test_no_extra_m1_imports` de substring livre para padrões `import`/`from` explícitos, evitando falso positivo pela palavra "dashboard" no docstring): auditado e correto — o espírito do guardrail permanece 100% aplicado e o import check ainda cobre `pipelines.m1`/`dashboard`/`censo`/`api`.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (lido apenas como coluna de feature territorial; M1 intacto; `renda=0.40`/`pop=0.60`; DEC-001 preservada).
- Artefatos M1 preservados: **sim** (mtime dos 4 artefatos oficiais byte-idêntico após re-run; única escrita = parquet novo em data/staging).
- Parâmetros canônicos preservados: **sim** (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0 — nenhum tocado; módulo não importa config de score).
- Validações re-executadas pelo QA (sem bypass): pytest full → 1169 passed/1 skipped/0 falhas; import streamlit_app → ok; ruff → All checks passed; mypy → no issues (86 files); deliverable → (88,36)/56/59; mtime M1 → inalterado; housekeeping --check → PRE-close correto.
- Escopo respeitado: **sim** (só módulo + teste + orquestração + snapshots; pré-sujos intocados; sem nova dependência em pyproject.toml).

## Decisão recomendada
Fechar ciclo. No fechamento, o orquestrador move BLK-LTV-02 backlog→completed via `scripts/housekeeping_move_block.py` (Passo 6.0). Próximo bloco do epic: BLK-LTV-03 (correlação território × retenção + haircut).
