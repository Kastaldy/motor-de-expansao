# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador)

## VEREDITO
APROVADO

## Justificativa
O BLK-DIM-03R substituiu os números mágicos do spike (`pessoal_pct=0.30`,
`outros_custos_pct=0.05`, `custo_fixo_base_mes=5000`) por custos fixos ABSOLUTOS
fundamentados no DRE real (`SIM_PESSOAL_MES=50.128,16` + `SIM_OUTROS_FIXOS_MES=38.150`),
ratios % e impostos vindos do `simulador_estrutura.json` (BLK-DIM-00), todos com
comentário de origem (célula/linha do Excel). Re-executei TODAS as validações
obrigatórias de forma independente (não confiei no log do Builder):

- Suite full verde em paralelo E serial (paridade exata): `833 passed, 4 skipped`.
- ruff + mypy limpos no módulo.
- Anti-circularidade comprovada NUMERICAMENTE e no design dos testes (ver abaixo).
- Benchmark CA-03 validado contra intervalo [18%, 26%] derivado do Excel/§8.2, não de
  constantes auto-ajustadas; margem real medida = 18.3% (dentro do intervalo).
- READ-ONLY sobre M1 confirmado por diff semântico (ignorando EOL/whitespace).
- Sem I/O no `simulador.py`; goal-seek do aluguel-teto > 0 no cenário viável.

Todos os 5 critérios de aceite do backlog satisfeitos.

## Problemas críticos (bloqueadores)
Nenhum.

## Problemas médios (não bloqueadores)
Nenhum. Observações documentadas (não bloqueiam):
- A margem benchmark (18.3%) está próxima do PISO do intervalo [18%, 26%]. Está dentro
  e documentada no docstring/CA-03 como "não ajustada para bater com o código". O Planner
  estimava ~22%; a diferença vem do tratamento de churn (só balcão, não agregadores),
  documentado pelo Builder no risco #1. Aceitável.
- `simulador.py` importa `scipy.optimize.brentq` além de stdlib + config.py. O brief de QA
  citava "só stdlib + config.py", mas o BACKLOG exige explicitamente goal-seek via
  `scipy.optimize.brentq`. scipy é dependência de design requerida, não I/O. Não é violação.
- `m2_otimo` existe e roda mas não tem teste dedicado neste arquivo (o spike tinha 3). O
  plano CA-01..CA-08 do Planner não os incluiu. Cobertura de goal-seek garantida por
  `aluguel_teto` e `alunos_minimos_viaveis`. Melhoria opcional, não bloqueia.

## Melhorias opcionais
- Adicionar 1-2 testes dedicados de `m2_otimo` (monotonicidade / bordas m2_min/m2_max).
- Considerar elevar a margem-alvo do benchmark mais ao centro do intervalo num bloco futuro
  de calibração com as 54 maduras (BLK-DIM-04), sem mexer no escopo deste bloco.

## Saída literal das validações (re-executadas pelo QA)

### Suite full (paralelo, xdist)
```
833 passed, 4 skipped, 18 warnings in 112.00s (0:01:52)
```

### Suite full (serial, -p no:xdist) — PARIDADE
```
833 passed, 4 skipped, 18 warnings in 111.33s (0:01:51)
```
Idênticas em paralelo e serial (mesma contagem, zero divergência).

### ruff + mypy
```
All checks passed!
=== MYPY ===
Success: no issues found in 1 source file
```

### Testes do módulo
```
27 passed in 0.96s
```

### Anti-circularidade (verificação numérica independente)
```
CA-03 margem_ebitda_pct = 0.1827 (18.3%) -> in [18%,26%]? True
CA-02 faturamento = 175694
payback (capex default 2.34M) = inf
CA-08a sem agregadores: faturamento=118380, margem=-11.6% -> NEGATIVA? True
VIAVEL capex=600k: payback=57.0, flag_viavel=True, roic=0.262
aluguel_teto(938,1500,137) = 34529  (>0 e >20k default: True)
aluguel_teto(1,...) inviavel = 0
lucro_liquido_mensal (proxy FCF steady) = 13079
```
- CA-08a (anti-circularidade): sem as 3 fontes de receita a margem é NEGATIVA (−11.6%),
  provando que os custos fixos (R$88k) não são artificialmente baixos.
- CA-03 (benchmark): intervalo [18%, 26%] é literal, derivado do Excel/§8.2; os asserts
  NÃO comparam margem/ROIC contra nenhuma constante do código. Não circular.
- Testes 100% hardcoded — `grep` confirmou ZERO leitura de parquet/json/fixture sintética;
  `DEFAULTS`/`VIAVEL` são dicts de defaults do Excel, sem retroalimentar a equação.

### Números sem magia
```
grep pessoal_pct|outros_custos_pct|custo_fixo_base_mes em simulador.py + config.py:
  -> única ocorrência é no DOCSTRING (explica que foram eliminados); nenhuma constante mágica.
config.py: SIM_PESSOAL_MES e SIM_OUTROS_FIXOS_MES com comentário de origem (Fopag/DRE linhas).
viabilidade() usa custos_fixos = pessoal_mes + outros_fixos_mes + aluguel_mes (absolutos).
```

### Housekeeping --check (pré-move; deve FALHAR — OK)
```
FALHA --check: stub ausente no backlog para BLK-DIM-03R
```

### READ-ONLY M1 (diff semântico ignorando EOL/whitespace)
```
git diff --ignore-all-space HEAD -- src/motor_expansao/config.py            -> VAZIO (só EOL churn)
git diff --ignore-all-space HEAD -- src/motor_expansao/pipelines/m1/*.py     -> VAZIO (só EOL churn)
git diff --ignore-all-space HEAD -- dimensionamento/config.py                -> só ADIÇÃO do bloco SIM_*
Únicos arquivos novos (untracked): simulador.py + test_simulador.py
```
NOTA: `git status` mostra a árvore inteira como "modified", mas é churn de EOL/whitespace
pré-existente no branch (headers `@@ -1,N +1,N @@`, mesma contagem). Diff semântico do
M1 (config.py raiz + pipelines/m1) é VAZIO — M1 intocado.

## Conferência de no-bypass
CONFIRMO: nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, nem
mock do caminho crítico para mascarar verde. A suite full rodou COMPLETA (paralelo E serial),
sem `-k`, sem `--no-cov`, sem skip seletivo. Os testes do simulador são 100% hardcoded e
exercitam a função `viabilidade`/goal-seek reais. A verificação numérica de anti-circularidade
foi feita importando o módulo de produção diretamente, sem mock.

## Guardrails verificados
- [x] Suite full verde (833 passed, 4 skipped) — paralelo == serial
- [x] ruff limpo no módulo
- [x] mypy limpo em simulador.py
- [x] Zero número mágico não-fundamentado (pessoal_pct/outros_custos_pct/custo_fixo_base_mes eliminados)
- [x] SIM_PESSOAL_MES / SIM_OUTROS_FIXOS_MES com comentário de origem (DRE/Fopag)
- [x] viabilidade() usa custos fixos ABSOLUTOS, não ratios
- [x] Anti-circularidade: CA-08a margem negativa sem as 3 fontes; CA-03 benchmark [18%,26%] externo
- [x] Goal-seek aluguel_teto > 0 no cenário viável (R$34.529 > R$20k default)
- [x] Sem I/O em simulador.py (só stdlib + scipy.brentq exigido + config.py)
- [x] READ-ONLY M1: config.py raiz e pipelines/m1 com diff semântico VAZIO
- [x] Payback: capex 600k -> 57 meses (<60, viável); capex 2.34M -> inf (correto)
- [x] DEC-001 preservada: score_priorizacao/pesos/artefatos oficiais INALTERADOS
- [x] Housekeeping --check falha pré-move (esperado)

## Decisão recomendada
FECHAR CICLO. O bloco está completo, correto e fundamentado; anti-circularidade comprovada;
suite full verde sem bypass; M1 intocado. Sem bloqueadores.
