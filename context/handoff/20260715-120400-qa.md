# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (modo MERGE-HUMANO) — gate de REVISÃO VISUAL do PDF por Vinicius, depois
housekeeping (`scripts/housekeeping_move_block.py BLK-RELPON-07`) + commit por path pelo
ORQUESTRADOR. Fase 2 (Documentation) opcional; docs já atualizados pelo Builder.

## Bloco executado
BLK-RELPON-07 — Slide "Perfil do Bairro/Distrito" no PDF do Relatório Pontual Censitário
(entre Concorrentes e Big Numbers; 5→6 páginas nas duas variantes; READ-ONLY M1).

## VEREDITO
APROVADO COM RESSALVAS

## Justificativa
A entrega implementa o plano do Planner ao pé da letra, com escopo exatamente nos 6 arquivos
previstos, guardrails M1/interseção/raio/marca-d'água/compressão intactos, e testes fortes que
provam de fato a exclusão simétrica da renda (D3.5), o fallback bairro→distrito e o "n/d"
gracioso nas 2 variantes. A ÚNICA ressalva é NÃO-BLOQUEANTE do código: (1) o gate de revisão
VISUAL do PDF por Vinicius é pós-QA por design (modo merge-humano); (2) a suíte serial tem 1
falha PRÉ-EXISTENTE (provada por git stash) por parquet gitignored ausente, sem relação com
este ciclo.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- O caminho de subtítulo PREENCHIDO (com `unidade_nome` real, ex.: "Bairro X - Bairro de
  SP/SP") não é auditado pelo teste de latin-1 (5.1) — este só exercita o caminho "n/d". As
  strings ESTÁTICAS novas (labels, nota, "Perfil não disponível...") estão 100% cobertas; o
  subtítulo dinâmico depende de dado real e passa por `_ascii()` (replace). Baixo risco;
  cobertura extra seria só reforço.

## Testes faltantes
- nenhum crítico. Cobertura do motor (lookup bairro/distrito, fora-da-malha, agregação,
  fallback, exclusão simétrica, "n/d" sem id / DF vazio) e do export (4 métricas, headers,
  /Count 6, "n/d", clássico) está completa e alinhada aos itens 19-20 do plano.

## Riscos remanescentes
- Revisão VISUAL do PDF (geometria fina da página nova: cards, subtítulo, espaçamento) ainda
  NÃO feita — gate humano de Vinicius antes do merge (esperado no modo merge-humano; NÃO é
  responsabilidade do QA aprová-la). O layout sugerido pelo Planner é ponto de partida
  ajustável nesse gate.
- Falha pré-existente `test_score_retencao_territorial::test_run_readonly_m1_por_mtime`
  permanece no ambiente (parquet gitignored ausente); não introduzida por este ciclo.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte FULL com `-n auto` (gate único; falhou por INFRA conhecida do ambiente Windows)
```
INTERNALERROR> ...xdist/workermanage.py... node.setup() ... self.gateway._rinfo()
INTERNALERROR>   File ".../execnet/gateway_base.py", line 988, in new
INTERNALERROR>     raise OSError(f"connection already closed: {self.gateway}")
INTERNALERROR> OSError: connection already closed: <Gateway id='gw0' not-receiving, main_thread_only model, 0 active channels>
```
Razão: mesmo defeito execnet/`OSError` de setup de workers já documentado em ciclos
anteriores neste ambiente (Python 3.14 local). NÃO produz contagem; fallback serial abaixo.

### 2. Suíte FULL SERIAL (`python -m pytest -q`) — autoridade da contagem
```
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 1770 passed, 2 skipped, 265213 warnings in 482.45s (0:08:02)
```
A única falha é PRÉ-EXISTENTE (provada abaixo). Excluída ela: 1770 passed / 2 skipped, verde.

### 3. Prova de que a falha é PRÉ-EXISTENTE (git stash → baseline HEAD → mesma falha)
```
Saved working directory and index state WIP on ciclo/BLK-RELPON-07: ...
=== BASELINE (changes stashed) ===
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 651 warnings in 1.52s
... Dropped refs/stash@{0} ...
```
Causa (mesma no tree atual e no baseline): `read_parquet` de
`data/staging/unidade_territorio_retencao.parquet` (gitignored, ausente neste ambiente) na
camada `lifetime/score_retencao` — NÃO tocada por este ciclo (censo_point/censo_report/pages).

### 4. Alvos específicos do Planner
```
python -m pytest -q tests/unit/test_relatorio_pontual_censitario_motor.py \
  tests/unit/test_relatorio_pontual_censitario_export.py \
  tests/unit/test_relatorio_acentuacao_regressao.py tests/unit/test_relatorio_municipal.py
........................................................................ [ 75%]
........................                                                 [100%]
96 passed, 4941 warnings in 42.53s
```
Inclui os 7 testes novos do motor (item 19) + fora-da-malha estendido, os 3 novos do export
(item 20) e a regressão de acentuação verde.

### 5. `ruff check . --no-cache`
```
All checks passed!
```

### 6. `mypy src/`
```
src\...\poi_enrichment.py:15: error: Library stubs not installed for "requests"  [import-untyped]
src\...\base_h3_brasil.py:31: error: Library stubs not installed for "requests"  [import-untyped]
src\...\api\geo.py:21: error: Library stubs not installed for "requests"  [import-untyped]
src\...\api\telegram_bot.py:23: error: Library stubs not installed for "requests"  [import-untyped]
src\...\api\service.py:223: error: Library stubs not installed for "requests"  [import-untyped]
Found 6 errors in 6 files (checked 108 source files)
```
Os 6 erros são `types-requests` ausente em módulos NÃO tocados. Prova de pré-existência
(git stash → baseline): grep `import-untyped` = 5 linhas idênticas no baseline. Nos 3
arquivos tocados, isolado:
```
python -m mypy src/motor_expansao/dashboard/censo_point.py \
  src/motor_expansao/dashboard/censo_report.py src/motor_expansao/dashboard/pages.py
Success: no issues found in 3 source files
```

### 7. Smoke import (`PYTHONPATH=src python -c "import streamlit_app; print('import ok')"`)
```
WARNING streamlit.runtime.caching...: No runtime found, using MemoryCacheStorageManager
import ok
```

### 8. Escopo (`git status --porcelain` / `git diff --stat`) — só os 6 arquivos do plano
```
 M docs/relatorio_pontual_censitario.md
 M src/motor_expansao/dashboard/censo_point.py
 M src/motor_expansao/dashboard/censo_report.py
 M src/motor_expansao/dashboard/pages.py
 M tests/unit/test_relatorio_pontual_censitario_export.py
 M tests/unit/test_relatorio_pontual_censitario_motor.py
```
`api/service.py` NÃO tocado (fora de escopo por design — confirmado). Nenhum arquivo de M1,
`censo_map.py`, `relatorio_municipal.py`, `PRD.md` ou config tocado.

### 9. Correção funcional (evidência própria)
```
# PDF_SECTION_HEADERS (censo_report.py L27-34): 6 strings, "Perfil do Bairro/Distrito"
#   ENTRE "Concorrentes" e "Big Numbers" — CONFIRMADO por leitura direta.
# Sequência dos geradores (recente L1374-1379 e clássico L1291-1302):
#   cover -> mapas_calor -> competitors -> PERFIL -> big_numbers -> credit — CONFIRMADO.
grep -c 'b"/Count 5"' export_test  -> 0   (nenhum remanescente)
grep -c 'b"/Count 6"' export_test  -> 16
# Watermark: laço range(1, pdf.pages_count+1) DINÂMICO (recente L1384 / clássico L1306);
#   testes assert count(b"Ultra Academia") >= 6 nas 2 variantes — verdes.
```

### 10. Housekeeping `--check` (esperado FALHAR neste ponto — move é do orquestrador)
```
python scripts/housekeeping_move_block.py BLK-RELPON-07 --check
FALHA --check: stub ausente no backlog para BLK-RELPON-07
```
Correto para modo MERGE-HUMANO pós-QA: o MOVE do bloco + commit por path é passo de
FECHAMENTO do ORQUESTRADOR, não do QA. Registrado como pendência de fechamento.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock
do caminho crítico. A suíte roda contra o código real; as fixtures sintéticas dos testes
(`_bairro_row`, `_PERFIL_BAIRRO_OK`, `_sector_record`) são isolamento de unidade LEGÍTIMO
(não é tooling de produção). O gate único da suíte full rodou de fato AQUI (serial, por infra
de xdist quebrada — razão documentada, não mascarada com `-p no:xdist`).

## Conferência cruzada com log do Builder
Bate. O Builder relatou 345 passed no subconjunto+integração serial, ruff limpo, mypy limpo
nos 3 arquivos, smoke import ok, `-n auto` quebrado por infra — tudo reproduzido. Divergência
menor e ESPERADA: o Builder achou 12 (não 13) ocorrências literais de `b"/Count 5"`; confirmo
0 remanescentes de `/Count 5` e 16 asserts `/Count 6` no export — nenhum teste "esquecido" em
5 e nenhum relaxado/removido (só atualizados/renomeados, ex.: `test_classico_gera_6_paginas`).

## Guardrails verificados
- score_priorizacao não alterado: sim (grep no diff de censo_point/censo_report: 0 ocorrências
  de score_priorizacao/hex_score; nenhuma mudança em `pipelines/m1/`).
- Artefatos M1 preservados: sim (nenhum artefato oficial tocado; camada de relatório/display).
- Interseção `setor_censitario_intersecao_area_1p5km`, raio 1,5 km,
  `RAIO_CENSITARIO_DEFAULT_KM`: INTOCADOS (grep no diff = 0 modificações).
- `set_compression(False)` / `pdf_version="1.4"`: INTOCADOS (grep no diff = 0 modificações).
- Marca d'água anti-PII / choropleth: INTOCADOS (laço só reutilizado; nenhum dado de pessoa
  física — só agregados de setor por bairro/distrito).
- Acentuação: textos novos em PT com acentos dentro de latin-1 ("População", "Densidade
  demográfica", "Domicílios", "Renda média", "Perfil do Bairro/Distrito", "Perfil não
  disponível...") e SÓ pontuação ASCII (sem —/•/…/aspas curvas/©); identificadores crus
  `unidade_ponto_tipo`/`unidade_tipo` = `"bairro"`/`"distrito"` sem acento; rótulo acentuado
  ("Bairro"/"Distrito") só na camada de display. Regressão de acentuação verde (test scania
  o caminho "n/d" da página nova com ZERO "?").
- D3.5 exclusão simétrica: PROVADA — `test_..._exclui_setor_com_domicilio_zero_ou_renda_nula`
  afirma `renda_media_domiciliar == 2000.0` (só o setor válido), NÃO 2250 (média que
  incluiria o setor de dom=0), e pop/dom somam os 3 setores. Não é média simples.
- Validações re-executadas pelo QA (sem bypass): pytest-full-serial → 1 pré-existente/1770
  ok; 4 alvos → 96 ok; ruff → limpo; mypy 3 arquivos → limpo; smoke import → ok.
- Escopo respeitado: sim (6 arquivos do plano; api/service.py fora por design).
- Parâmetros canônicos §3 (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0,
  renda=0.40/pop=0.60): INALTERADOS.

## Decisão recomendada
Fechar ciclo APÓS o gate de revisão VISUAL do PDF por Vinicius. Housekeeping (MOVE do bloco +
commit por path) é passo do ORQUESTRADOR no modo merge-humano — não executado pelo QA.
