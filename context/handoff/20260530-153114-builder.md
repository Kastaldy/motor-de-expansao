# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-FIX-02 — Corrigir MessageSizeError para UFs grandes

## O que foi feito
Fix de transporte/render (NÃO toca score/carteira/plano/M1). Duas frentes:

1. **`.streamlit/config.toml`** — adicionado `maxMessageSize = 500` dentro do bloco
   `[server]` existente, logo após `enableXsrfProtection = true`. Nenhum bloco novo.

2. **Payload enxuto do `H3HexagonLayer` (pydeck)** — novo helper puro
   `_deck_layer_frame(map_df)` em `components.py` (logo após `_downsample_map_index`),
   com constante `_DECK_RENDER_COLUMNS = ("hex_id", "fill_color", "line_color")`.
   O helper projeta `map_df` para SOMENTE `hex_id/fill_color/line_color` + as colunas
   `tooltip_title`/`tooltip_line_*` REALMENTE presentes (derivadas via
   `startswith("tooltip_line_")`, não lista fixa), com dedup preservando ordem, e
   retorna `.loc[:, ordered].copy()`. NÃO muta `map_df`. (Dedup reescrito em loop
   explícito em vez do idiom `x or seen.add(x)` para passar no mypy — `set.add`
   retorna `None`, que o mypy rejeita em contexto de valor.)

   Aplicado a **4 builders** (todos com `H3HexagonLayer` + `data=map_df` + accessors
   string `get_fill_color="fill_color"`/`get_line_color="line_color"` + tooltip):
   - `build_map_figure` (M1) — `layer_df = _deck_layer_frame(map_df)`; `data=layer_df`.
   - `build_hybrid_map_figure` (híbrido/censitário) — idem.
   - `build_residual_heatmap_figure` (residual) — idem.
   - `build_dominio_map_figure` (Expansão de Domínio) — idem. **CONFIRMADO via grep**:
     o domínio existe e usa o mesmo padrão (passo 5 do plano), então entrou no fix por
     consistência. (Antes do grep o plano apenas hipotetizava sua existência.)

   Em todos, `map_df` permanece intacto e segue alimentando
   `_build_competitor_icon_layer(... map_df)`, `_build_ultra_icon_layer(... map_df)`,
   `resolve_map_view(map_df, ...)` e a busca por hex. Apenas o argumento `data=` do
   `H3HexagonLayer` passou de `map_df` para `layer_df`.

   `build_unified_map_figure` é só um **dispatcher** que delega aos 4 builders acima —
   herda o payload enxuto automaticamente, sem layer próprio. Não foi tocado.
   Layers de pins (competidor/Ultra), busca (`_search_hex_payload` já projeta para
   `hex_id`+tooltip), análise pontual e multi-hex já eram enxutos — não tocados.

3. **Testes**
   - **2 regressões corrigidas** (1 prevista no plano + 1 NÃO prevista, descoberta na
     execução), de forma mínima e SEM reintroduzir coluna-fonte no payload:
     - `test_build_map_figure_mostra_todos_os_hexes_validos_da_uf_sem_cap_editorial`
       (linha ~493): o assert lia `confianca_geografica` do payload. Trocado por
       `assert "confianca_geografica" not in rendered.columns` (o recorte dos 3 hexes
       granulares já é garantido pelas asserts de `hex_id` imediatamente acima).
     - `test_build_map_figure_usa_geometria_granular_em_uf_ab_e_fallback_municipal_em_uf_c`
       (linha ~628) **[regressão NÃO prevista no plano]**: lia
       `confianca_geografica` granular/municipal do payload. Como essa coluna decide a
       `line_color` (granular = cor de borda do mapa; municipal = `[245,158,11,220]`),
       o assert foi reescrito para validar a CONSEQUÊNCIA visível: `confianca_geografica`
       ausente do payload, `ce_municipal` com `line_color == [245,158,11,220]` e
       `sp_granular` com `line_color` distinta. Intenção (classificação granular×municipal)
       preservada.
     - `test_build_residual_heatmap_figure_retorna_deck_com_hexes` (linha ~1154)
       **[regressão NÃO prevista no plano]**: lia `score_oportunidade_residual` do
       payload. Trocado por `assert "score_oportunidade_residual" not in
       map_layer_data.columns`; as asserts de `fill_color == score_band_to_color(85/15)`
       logo abaixo preservam a intenção (cor derivada do residual).
   - **3 testes novos** (NO-BYPASS — exercitam os builders de verdade), inseridos após
     `test_build_map_figure_pinta_hex_descartado_por_pop_com_cor_neutra`:
     - `test_build_map_figure_payload_do_layer_so_tem_colunas_de_render_e_tooltip` (M1)
     - `test_build_hybrid_map_figure_payload_do_layer_enxuto` (híbrido)
     - `test_build_residual_heatmap_figure_payload_do_layer_enxuto` (residual)
     Cada um monta um `df`, chama o builder real, pega
     `pd.DataFrame(deck.layers[0].data)` e assere: (a) `set(columns)` ⊆
     `{hex_id, fill_color, line_color, tooltip_title, tooltip_line_1..14}`;
     (b) auxiliares AUSENTES (M1: `score_priorizacao_fmt`, `confianca_geografica`,
     `tooltip_residual_1`, `faixa_label`, `renda_per_capita`; híbrido: `score_censo_fmt`,
     `densidade_pop_setor_hab_km2`, `tooltip_residual_2`, `score_expansao_hibrido`,
     `renda_per_capita`; residual: `score_oportunidade_residual`,
     `densidade_pop_setor_hab_km2`, `tooltip_residual_1`, `renda_per_capita`);
     (c) render PRESENTES (`hex_id/fill_color/line_color/tooltip_title`);
     (d) `tooltip_title` é string não vazia. Constante de keep-set compartilhada
     `_DECK_LAYER_KEEP_SET` no módulo de teste.
     (Nota: o row helper do teste híbrido novo precisou incluir `causa_outlier_espacial`
     e `motivo_nao_elegivel_censo` — o builder híbrido os consome; descoberto na 1ª run.)

## Arquivos alterados
- `.streamlit/config.toml` — `maxMessageSize = 500` no bloco `[server]`.
- `src/motor_expansao/dashboard/components.py` — novo helper `_deck_layer_frame` +
  constante `_DECK_RENDER_COLUMNS`; troca `data=map_df` → `data=layer_df` nos 4 builders
  de `H3HexagonLayer` (M1, híbrido, residual, domínio).
- `tests/integration/test_streamlit_app.py` — 3 asserts de regressão reescritos (sem
  reintroduzir coluna-fonte) + 3 testes novos + constante `_DECK_LAYER_KEEP_SET`.

## Validações executadas (saída literal)
- pytest test_streamlit_app.py: **150 passed**
```
........................................................................ [ 48%]
........................................................................ [ 96%]
......                                                                   [100%]
150 passed in 10.64s
```
- pytest -q (full): **570 passed, 1 skipped, 9 warnings**
```
570 passed, 1 skipped, 9 warnings in 143.41s (0:02:23)
```
  (Baseline esperado do ciclo anterior: 567 passed, 1 skipped. 567 + 3 testes novos = 570.
  Zero falhas, zero testes a menos.)
- import streamlit_app: **ok** (`import ok`; warnings de ScriptRunContext/cache são
  esperados ao importar em modo bare, fora do runtime Streamlit).
- ruff: **All checks passed!** (`components.py` + `test_streamlit_app.py`)
- mypy `components.py`: **Success: no issues found in 1 source file**
  (1ª run acusou `func-returns-value` no idiom `seen.add(c)` do dedup; reescrito em loop
  explícito; 2ª run limpa.)

## Problemas encontrados
- 2 regressões de teste NÃO previstas no plano do Planner (que só antecipou a do
  `confianca_geografica` na linha ~493):
  1. `confianca_geografica` também era lida na linha ~628 (teste granular×municipal).
  2. `score_oportunidade_residual` era lida na linha ~1154 (teste residual).
  Ambas tratadas de forma mínima, preservando a intenção e SEM reintroduzir a
  coluna-fonte no payload (validando a consequência visível: line_color / fill_color).
- mypy reprovou o idiom de dedup sugerido no plano (`x or seen.add(x)`); reescrito.
- Builder híbrido exige `causa_outlier_espacial`/`motivo_nao_elegivel_censo` no input;
  adicionados ao row helper do teste novo híbrido.

## Pendências
- Nenhuma. (Verificar com QA se a inclusão do builder de **domínio** no fix — autorizada
  pelo passo 5 do plano após grep — está OK; é consistente com o critério "payload do
  layer só com render+tooltip".)

## Riscos remanescentes
- Nenhum funcional identificado. Tooltips visíveis idênticos (mesmas `tooltip_line_*`);
  cores idênticas (fill/line preservados). O fix é seleção de colunas pré-serialização.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (nenhuma coluna de score tocada/recalculada;
  apenas projeção de colunas).
- map_df não mutado / pins/busca intactos: **sim** (`layer_df` é variável nova;
  competitor/ultra/resolve_map_view/busca seguem com `map_df`).
- MAP_POINT_LIMIT e downsample inalterados: **sim** (`_downsample_map_index`,
  `MAP_POINT_LIMIT`, `MAP_SORT_COLUMNS` não tocados).
- Artefatos M1 preservados: **sim** (nenhum Parquet gerado/sobrescrito).
- Dashboard offline mantido: **sim** (nenhuma dependência de API ao vivo).
