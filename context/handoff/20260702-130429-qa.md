# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador executa o Passo 6.0 — housekeeping move do BLK-RELMUN-04 — e o
merge humano). Antes do merge: revisão VISUAL humana da UI nova (ressalva não-bloqueadora abaixo).

## VEREDITO
APROVADO COM RESSALVAS

## Justificativa
A entrega implementa fielmente o plano (D1–D4): ramificação `0 / ==1 / >1` nos dois pontos com o
ramo `==1` preservado (só re-indentado, mesmos keys/cache/warnings), helper testável
`_gerar_payload_relatorio_municipal` (None quando `n_hex_total==0`), lote sob botão com progresso e
1 download rotulado por município. Todas as validações re-executadas passam; a única falha da suíte é
a MESMA pré-existente e fora de escopo (`test_score_retencao_territorial`, parquet gitignored ausente).
A ÚNICA ressalva é a revisão VISUAL humana da UI nova, exigida pelo critério de aceite do backlog
antes do merge (não-bloqueadora do ponto de vista técnico).

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- A geração de N PDFs é sequencial sob botão explícito com progresso i/N (D4). Aceitável e conforme o
  plano; se no uso real N for alto, considerar (fora deste ciclo) um teto/aviso de custo.

## Testes faltantes
- nenhum. Cobertura prova: helper dict/None (`test_relmun_lote_helper_gera_payload_e_none`); lote com
  N botões+downloads rotulados (`..._topo_lote_botao_e_downloads`, 3 downloads "— A/B/C"); município
  sem hex não aborta o lote (`..._topo_lote_municipio_sem_hex_nao_aborta`, warning 1× + N−1 downloads);
  gate 0/1 (`..._topo_gate_zero_e_um`, key `btn_gerar_relmun_topo` + cache `relmun_topo_payload::`);
  espelho do expander lote e 1-município auto-gera (`..._expander_lote_downloads`,
  `..._expander_um_municipio_auto_gera` com `button.assert_not_called`). O teste de gate existente foi
  ajustado corretamente (`["A","B"]`→`[]`) sem enfraquecer o caso de 1 município.

## Riscos remanescentes
- Revisão visual humana da UI nova pendente (ressalva, ver acima) — passo de aceite antes do merge.
- Homônimos de município entre UFs: mitigado por `key` com índice+UF+slug (`_relmun_key_slug`) e por
  chave de cache `f"{uf or ''}::{nome}"`. Na prática `selected_ufs` traz 1 UF só.

## Saída literal das validações (re-executadas pelo QA)

### `python -m pytest -q` (suíte COMPLETA — gate único; serial por bug do xdist no Python 3.14 local)
```
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 1230 passed, 1 skipped, 159771 warnings in 273.55s (0:04:33)
```
Falha única = PRÉ-EXISTENTE e FORA DE ESCOPO (mesma do ciclo BLK-RELMUN-03): o teste roda
`run(root=root)` do pacote `lifetime/` sobre parquets reais e falha ao ler
`data/staging/unidade_territorio_retencao.parquet` (gitignored, ausente no ambiente local). Não toca
o diff deste ciclo (só `pages.py` + `test_streamlit_app.py`). Baseline honrado: 1230 passed + 1
skipped + 1 falha pré-existente conhecida.

### `python -m pytest -q tests/integration/test_streamlit_app.py` (foco)
```
........................................................................ [ 32%]
........................................................................ [ 65%]
........................................................................ [ 98%]
...                                                                      [100%]
219 passed, 11170 warnings in 12.12s
```
Os 6 testes novos de lote/helper verdes; os 2 testes de 1 município seguem verdes.

### `python -c "import streamlit_app; print('import ok')"`
```
... WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
import ok
```

### `ruff check src/motor_expansao/dashboard/pages.py tests/integration/test_streamlit_app.py`
```
All checks passed!
```

### `mypy src/motor_expansao/dashboard/pages.py`
```
Success: no issues found in 1 source file
```

### Housekeeping (heading do bloco no backlog — pré-condição do move; NÃO executei o move)
```
tasks/backlog.md:125:### BLK-RELMUN-04 — Relatório Municipal em lote (um relatório por município selecionado)
$ python scripts/housekeeping_move_block.py BLK-RELMUN-04 --check
FALHA --check: stub ausente no backlog para BLK-RELMUN-04
```
Heading `### BLK-RELMUN-04` PRESENTE (linha 125) — pré-condição do helper satisfeita. O `--check`
retorna FALHA por design ANTES do move (valida o invariante PÓS-move: stub presente + heading ausente
+ bloco em completed.md). O MOVE via `scripts/housekeeping_move_block.py` é passo do ORQUESTRADOR no
fechamento (Passo 6.0), após este veredito; o `--check` OK será validado lá.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do
caminho crítico. A suíte pytest usa mocks de `streamlit.*` e das 3 funções pesadas
(`agregar_municipio`/`render_mapas_municipio`/`gerar_payloads_download_relatorio_municipal`) apenas
para isolar a lógica de UI/roteamento — prática normal de teste unitário, NÃO tooling de produção; a
regra (b) do prompt explicitamente não se aplica a fixtures/mocks legítimos de teste. Não há tooling
dependente de config/artefatos reais neste ciclo.

## Conferência cruzada com log do Builder
Bate. Builder reportou 219 passed no foco (idêntico), import ok, ruff/mypy limpos, e a suíte completa
como gate do QA. Diferença esperada: eu rodei a suíte COMPLETA (1230 passed / 1 skipped / 1 falha
pré-existente), que o Builder não executou por instrução. A observação do Builder sobre o xdist
abortar no Python 3.14 local também se confirma (rodei serial).

## Guardrails verificados
- score_priorizacao não alterado: sim (não aplicável — camada de visualização/relatório; diff só em
  `pages.py` de UI + testes; sem toque em `config.py`, `pipelines/m1`, `*scoring*`, artefatos M1).
- Artefatos M1 preservados: sim (zero escrita; `git status --porcelain` só lista `pages.py`,
  `test_streamlit_app.py` e arquivos de orquestração `context/handoff*`, `tasks/*`).
- Escopo respeitado: sim — NENHUM toque em `relatorio_municipal.py` (motor do PDF),
  `calcular_colunas_mercado.py` (`flag_sam`/DEC-011), Relatório Pontual (`censo_report.py`/
  `render_relatorio_pontual_censitario`), nem em qualquer outro ponto de `pages.py`.
- Preservação do fluxo de 1 município (byte-a-byte): CONFIRMADA. `git diff` mostra que o ramo `==1`
  dos dois pontos é o bloco original re-indentado para dentro de `if len(...) == 1:` — mesmos keys
  (`btn_gerar_relmun_topo`, `dl_relmun_topo`), mesma cache key `relmun_topo_payload::<nome>`, mesmos
  warnings/spinner; no expander: auto-geração + `st.columns(3)` + 3 `st.metric` +
  `render_download_relatorio_municipal`. Lógica do `==1` inalterada (só indentação).
- Parâmetros canônicos preservados: H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, pesos
  renda=0.40/pop=0.60 — inalterados (nenhum arquivo de score/config no diff).
- Ramo `>1` conforme plano: botão único `Gerar Relatorios (N)` (keys distintos por ponto
  `btn_gerar_relmun_lote_topo`/`_expander`); loop reusa o helper; warning individual por município
  sem hexágono NÃO aborta (`continue`); 1 download "Baixar PDF — <nome>" por município com `key`
  único (índice+UF+slug); cache em `relmun_lote_topo_payloads`/`relmun_lote_expander_payloads`
  (distinto do modo 1 município).

## Decisão recomendada
Fechar ciclo após: (1) revisão VISUAL humana da UI nova (ressalva de aceite do backlog); (2)
orquestrador executar o housekeeping move (Passo 6.0) com `--check` OK. Nenhum bloco de correção
necessário.
