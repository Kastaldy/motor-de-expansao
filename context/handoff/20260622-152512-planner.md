# Handoff — Planner → Builder

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Builder

## Bloco
BLK-DIM-21 — UI: gráficos financeiros e curva de maturidade na aba de viabilidade

## Criticidade
**Média** — enriquecimento visual; READ-ONLY sobre M1. Esteira: BO → Planner → Builder → QA (sem gate humano; bloco loop-safe).

---

## 1. Análise do estado atual

### `simulador.py`
- Arquivo: `/repo/src/motor_expansao/dimensionamento/simulador.py`
- `ViabilidadeResult` (dataclass, linhas 52–87) expõe apenas valores de **steady-state** — sem série mensal.
- A função `viabilidade()` (linhas 89–345) tem um loop `for t in range(1, 61)` (linhas 302–322) que:
  - Calcula `al_t` (alunos por mês), `fat_t` (faturamento bruto), `eb_t` (EBITDA), `fcf_t` (FCF livre), `fcf_acum` (FCF acumulado).
  - Atualmente não guarda a série — só usa os valores para detectar o mês de payback.
- Parâmetros novos do BLK-DIM-20 já presentes na assinatura: `capex_financiado_pct`, `prazo_financiamento_meses`, `juros_financiamento_am`.

### `pages.py`
- Arquivo: `/repo/src/motor_expansao/dashboard/pages.py`
- Plotly **não** é importado em `pages.py` — as figuras plotly são geradas em `components.py` (que importa `plotly.express as px`). O padrão no projeto é `import plotly.express as px` + ocasionalmente `plotly.graph_objects`. Os gráficos das funções `build_*` são exibidos via `st.plotly_chart(fig, width="stretch")`.
- `render_viabilidade_ponto()` (linha 3211–3506):
  - Linha 3367: `submitted = st.form_submit_button("Calcular viabilidade")`
  - Linha 3369–3371: early return se não submetido
  - Linha 3388–3403: chama `analisar_viabilidade_ponto()` → `result`
  - Linha 3405: `viab = result.viabilidade`
  - Linhas 3413–3440: **Seção 4 — cards do cenário pedido** (8 métricas em 2 linhas de 4 colunas)
  - Linhas 3442–3463: **Seção 5 — faixa por densidade**
  - Linhas 3465–3483: **Seção 6 — catchment / zona morta**
  - Linhas 3485–3501: **Seção 7 — grade de sensibilidade**
  - Linhas 3503–3506: **Seção 8 — caption do pino**
- **Os 4 gráficos novos devem ser inseridos entre a Seção 7 e a Seção 8** (após linha 3501, antes da linha 3503), como nova **Seção 7.5** (ou renomear sequencialmente para Seção 8, empurrando o pino para Seção 9).

---

## 2. Mudanças em `simulador.py`

### 2a. Nova função `gerar_serie_mensal()`

Deve ser adicionada **após a função `viabilidade()`** (após linha 345) e **antes de `aluguel_teto()`** (linha 348).

A assinatura é **idêntica** à de `viabilidade()`, exceto que retorna `list[dict]` em vez de `ViabilidadeResult`. Retorna uma lista de 60 dicts com campos:
- `mes` (int, 1–60)
- `alunos_balcao` (float — `al_t`, alunos brutos no mês t, antes de churn)
- `faturamento_mensal` (float — `fat_t`, faturamento bruto incluindo balcão + agregadores + personal)
- `ebitda_mensal` (float — `eb_t`)
- `fcf_acumulado` (float — `fcf_acum` ao final do mês t)

**Estratégia de refatoração (SEM duplicar lógica):**
Extrair o corpo do loop de `viabilidade()` para `gerar_serie_mensal()`, e fazer `viabilidade()` chamar `gerar_serie_mensal()` para obter a série e depois pós-processar (extrair payback, calcular ROIC/flag, etc.).

**Detalhe de implementação:**

```python
def gerar_serie_mensal(
    alunos_maturidade: float,
    m2: float,
    aluguel_mes: float,
    ticket_medio: float,
    *,
    # mesmos kwargs de viabilidade() — todos os parâmetros com os mesmos defaults
    alunos_agregadores: float = SIM_ALUNOS_AGREGADORES_MATURIDADE,
    ticket_agregador: float = SIM_TICKET_AGREGADOR,
    personal_mes: float = SIM_PERSONAL_MES_RECEITA,
    churn: float = SIM_CHURN,
    inadimplencia: float = 0.02,
    capex: float | None = None,
    coef_capex_m2: float | None = None,
    capex_financiado_pct: float = 0.0,
    prazo_financiamento_meses: int = 36,
    juros_financiamento_am: float = 0.018,
    royalties_pct: float = SIM_ROYALTIES_PCT,
    marketing_pct: float = SIM_MARKETING_PCT,
    manutencao_pct: float = SIM_MANUTENCAO_PCT,
    cartoes_pct: float = SIM_CARTOES_PCT,
    devolucoes_pct: float = SIM_DEVOLUCOES_PCT,
    pis: float = SIM_PIS,
    cofins: float = SIM_COFINS,
    iss: float = SIM_ISS,
    ir_efetivo: float = SIM_IR_EFETIVO,
    csll_efetivo: float = SIM_CSLL_EFETIVO,
    pessoal_mes: float = SIM_PESSOAL_MES,
    outros_fixos_mes: float = SIM_OUTROS_FIXOS_MES,
    maturacao_meses: int = SIM_MATURACAO_MESES,
    alunos_inicial: float = float(SIM_ALUNOS_INICIAL),
) -> list[dict]:
    """Retorna a série mensal de 60 meses do simulador.

    Campos por mês: mes, alunos_balcao, faturamento_mensal, ebitda_mensal, fcf_acumulado.
    A lógica do loop é idêntica ao loop interno de viabilidade() — esta função é a fonte
    única de verdade; viabilidade() delega para ela.

    READ-ONLY sobre o M1: não recalcula score_priorizacao nem artefatos oficiais (DEC-001).
    """
    # Capex efetivo (mesma lógica de viabilidade())
    capex_efetivo: float
    if capex is None and coef_capex_m2 is not None:
        capex_efetivo = coef_capex_m2 * m2
    elif capex is None:
        capex_efetivo = float(SIM_CAPEX_DEFAULT)
    else:
        capex_efetivo = float(capex)

    # PMT de financiamento (mesma lógica de viabilidade())
    _C = capex_efetivo * capex_financiado_pct
    if _C > 0 and juros_financiamento_am > 0:
        _r = juros_financiamento_am
        _n = prazo_financiamento_meses
        _pmt = _C * _r * (1.0 + _r) ** _n / ((1.0 + _r) ** _n - 1.0)
    elif _C > 0:
        _pmt = _C / max(prazo_financiamento_meses, 1)
    else:
        _pmt = 0.0

    _maturacao_meses = max(maturacao_meses, 1)
    fcf_acum = -capex_efetivo
    serie: list[dict] = []

    for t in range(1, 61):
        frac = min(t / _maturacao_meses, 1.0)
        al_t = alunos_inicial + (alunos_maturidade - alunos_inicial) * frac
        pag_t = al_t * (1.0 - churn)
        fat_balcao_t = pag_t * ticket_medio * (1.0 - inadimplencia)
        fat_agr_t = alunos_agregadores * ticket_agregador * (1.0 - inadimplencia)
        fat_t = fat_balcao_t + fat_agr_t + personal_mes
        dev_t = fat_t * devolucoes_pct
        rl_t = fat_t - dev_t
        imp_t = rl_t * (pis + cofins + iss)
        rpi_t = rl_t - imp_t
        cvar_t = rl_t * (royalties_pct + marketing_pct + manutencao_pct + cartoes_pct)
        cfix_t = pessoal_mes + outros_fixos_mes + aluguel_mes
        eb_t = rpi_t - cvar_t - cfix_t
        ir_t = rl_t * (ir_efetivo + csll_efetivo)
        fcf_t = eb_t - ir_t
        if _pmt > 0 and t <= prazo_financiamento_meses:
            fcf_t -= _pmt
        fcf_acum += fcf_t
        serie.append({
            "mes": t,
            "alunos_balcao": float(al_t),
            "faturamento_mensal": float(fat_t),
            "ebitda_mensal": float(eb_t),
            "fcf_acumulado": float(fcf_acum),
        })

    return serie
```

### 2b. Refatorar `viabilidade()` para delegar ao loop

O loop `for t in range(1, 61)` em `viabilidade()` (linhas 302–322) deve ser substituído por uma chamada a `gerar_serie_mensal()`:

```python
# No lugar do loop atual (linhas 282-322), substituir por:
    serie = gerar_serie_mensal(
        alunos_maturidade, m2, aluguel_mes, ticket_medio,
        alunos_agregadores=alunos_agregadores,
        ticket_agregador=ticket_agregador,
        personal_mes=personal_mes,
        churn=churn,
        inadimplencia=inadimplencia,
        capex=capex,
        coef_capex_m2=coef_capex_m2,
        capex_financiado_pct=capex_financiado_pct,
        prazo_financiamento_meses=prazo_financiamento_meses,
        juros_financiamento_am=juros_financiamento_am,
        royalties_pct=royalties_pct,
        marketing_pct=marketing_pct,
        manutencao_pct=manutencao_pct,
        cartoes_pct=cartoes_pct,
        devolucoes_pct=devolucoes_pct,
        pis=pis,
        cofins=cofins,
        iss=iss,
        ir_efetivo=ir_efetivo,
        csll_efetivo=csll_efetivo,
        pessoal_mes=pessoal_mes,
        outros_fixos_mes=outros_fixos_mes,
        maturacao_meses=maturacao_meses,
        alunos_inicial=alunos_inicial,
    )
    # Extrair payback da série (primeiro mês com fcf_acumulado >= 0)
    payback_meses: float = float("inf")
    for row in serie:
        if row["fcf_acumulado"] >= 0:
            payback_meses = float(row["mes"])
            break
```

**IMPORTANTE:** A lógica do capex_efetivo (linhas 269–275 de `viabilidade()`) ainda deve ser calculada em `viabilidade()` antes de chamar `gerar_serie_mensal()`, porque ela é usada para calcular ROIC (linha 327). Alternativa mais limpa: `gerar_serie_mensal()` também pode retornar `capex_efetivo` como metadata — mas para não quebrar a API `list[dict]`, o Builder pode simplesmente recalcular `capex_efetivo` localmente em `viabilidade()` como já faz hoje (duplicação mínima de 4 linhas de lógica de negócio simples), ou pode ser um campo no primeiro dict da série. **Recomendação: manter o cálculo do capex_efetivo duplicado nas 4 linhas em `viabilidade()` — é lógica de negócio simples e evita complexidade na interface de `gerar_serie_mensal()`.**

### 2c. Exportar `gerar_serie_mensal` no `__all__`

Adicionar `"gerar_serie_mensal"` ao `__all__` se ele existir (ou criar um). O arquivo atualmente não tem `__all__` explícito, mas as importações em `test_simulador.py` e `viabilidade_ponto.py` importam diretamente; basta que a função seja definida no módulo.

---

## 3. Mudanças em `pages.py`

### 3a. Import de plotly (no topo do arquivo)

O arquivo `pages.py` **não** importa plotly diretamente. Os gráficos novos serão criados diretamente em `render_viabilidade_ponto()` com import lazy no início da função (ou adicionar ao topo do arquivo junto com os outros imports). **Recomendação: import lazy dentro da função `render_viabilidade_ponto()`**, após o `if not submitted: return` (linha 3371), como já é feito para outros imports opcionais no dashboard:

```python
# Logo após `with st.spinner(...)` (linha 3388), antes de montar os charts:
import plotly.graph_objects as go
import plotly.express as px  # se não já importado globalmente — verificar
```

**Mais limpo**: adicionar `import plotly.graph_objects as go` no topo do arquivo (junto com os outros imports de biblioteca), ao lado de `import pandas as pd`. Plotly já é dep obrigatória do projeto (`plotly>=5.20.0`).

### 3b. Import de `gerar_serie_mensal` (no topo do arquivo)

Adicionar ao bloco de imports de `motor_expansao.dimensionamento`:

```python
from motor_expansao.dimensionamento.simulador import gerar_serie_mensal
```

(Junto à linha 96–100 de imports de `motor_expansao.dimensionamento.config` e linha 101 de `viabilidade_ponto`.)

### 3c. Inserção dos 4 gráficos (nova Seção 8 em `render_viabilidade_ponto`)

**Posição:** inserir entre a linha 3501 (`st.info("Grade de sensibilidade indisponivel.")`) e a linha 3503 (`# --- Secao 8: pino do imovel ---`). A seção atual "Secao 8" vira "Secao 9".

```python
    # --- Secao 8: gráficos financeiros ---
    st.markdown("##### Projeção financeira (60 meses)")
    import plotly.graph_objects as go  # lazy import; plotly é dep obrigatória

    serie = gerar_serie_mensal(
        result.alunos_balcao_premissa,  # alunos_maturidade = balcão já splitado
        float(m2),
        float(aluguel_pedido),
        float(ticket_medio),
        alunos_agregadores=result.alunos_agregadores_premissa,
        capex=float(capex_total),
        capex_financiado_pct=float(pct_financiado) / 100.0,
        prazo_financiamento_meses=int(prazo_financiamento_meses),
        juros_financiamento_am=float(juros_am_pct) / 100.0,
    )
    df_serie = pd.DataFrame(serie)
    meses = df_serie["mes"].tolist()

    # Constantes de cor Ultra
    _TURQUESA = "#00BFB3"
    _CINZA = "#2E3040"
    _VERDE = "#2ECC71"
    _VERMELHO = "#E74C3C"
    _BG = "white"

    # --- Gráfico 1: Curva de maturidade ---
    steady_state = float(result.alunos_balcao_premissa)
    fig1 = go.Figure()
    fig1.add_trace(go.Scatter(
        x=meses,
        y=df_serie["alunos_balcao"].tolist(),
        mode="lines",
        name="Alunos balcão",
        line=dict(color=_TURQUESA, width=2.5),
    ))
    fig1.add_hline(
        y=steady_state,
        line_dash="dash",
        line_color=_CINZA,
        annotation_text=f"Steady-state: {int(steady_state)}",
        annotation_position="top right",
        annotation_font_color=_CINZA,
    )
    # Anotação do ponto de maturação (mês em que frac >= 1.0)
    from motor_expansao.dimensionamento.config import SIM_MATURACAO_MESES as _MAT
    mes_mat = min(_MAT, 60)  # usar parâmetro real se disponível — ver nota abaixo
    fig1.add_vline(
        x=mes_mat,
        line_dash="dot",
        line_color=_CINZA,
        annotation_text=f"Maturação: mês {mes_mat}",
        annotation_position="top left",
        annotation_font_color=_CINZA,
    )
    fig1.update_layout(
        title="Rampa de alunos (balcão)",
        xaxis_title="Mês",
        yaxis_title="Alunos",
        plot_bgcolor=_BG,
        paper_bgcolor=_BG,
        font=dict(color=_CINZA),
        margin=dict(l=40, r=20, t=50, b=40),
        legend=dict(orientation="h", yanchor="bottom", y=1.02, xanchor="right", x=1),
    )
    st.plotly_chart(fig1, use_container_width=True)
```

**Nota sobre `mes_mat`**: o parâmetro `maturacao_meses` lido pelo formulário está em `viab_ponto_form` (campo avançado não exposto ao usuário — usa o default `SIM_MATURACAO_MESES = 8`). Para o gráfico, usar o default importado da config é adequado; o Builder pode refinar se quiser capturar o valor do formulário (não é campo editável no form atual, portanto o default é suficiente).

```python
    # --- Gráfico 2: Faturamento e EBITDA mensal ---
    fig2 = go.Figure()
    fig2.add_trace(go.Bar(
        x=meses,
        y=df_serie["faturamento_mensal"].tolist(),
        name="Faturamento bruto",
        marker_color=_TURQUESA,
        opacity=0.85,
    ))
    ebitda_colors = [_VERDE if v >= 0 else _VERMELHO for v in df_serie["ebitda_mensal"]]
    fig2.add_trace(go.Scatter(
        x=meses,
        y=df_serie["ebitda_mensal"].tolist(),
        mode="lines+markers",
        name="EBITDA",
        line=dict(color=_CINZA, width=2),
        marker=dict(color=ebitda_colors, size=4),
    ))
    fig2.update_layout(
        title="Faturamento e EBITDA mensal",
        xaxis_title="Mês",
        yaxis_title="R$",
        plot_bgcolor=_BG,
        paper_bgcolor=_BG,
        font=dict(color=_CINZA),
        margin=dict(l=40, r=20, t=50, b=40),
        legend=dict(orientation="h", yanchor="bottom", y=1.02, xanchor="right", x=1),
        barmode="overlay",
    )
    st.plotly_chart(fig2, use_container_width=True)
```

```python
    # --- Gráfico 3: FCF acumulado ---
    fcf_vals = df_serie["fcf_acumulado"].tolist()
    # Colorir área por sinal: positiva em turquesa translúcido, negativa em vermelho translúcido
    # Com Scatter fill, a cor muda conforme sinal — usar fill="tozeroy" com conditional color
    # Estratégia: duas traces — uma para positivo (y >= 0), outra para negativo (y < 0)
    fcf_pos = [v if v >= 0 else 0.0 for v in fcf_vals]
    fcf_neg = [v if v < 0 else 0.0 for v in fcf_vals]

    fig3 = go.Figure()
    fig3.add_trace(go.Scatter(
        x=meses, y=fcf_neg,
        fill="tozeroy",
        fillcolor="rgba(231,76,60,0.25)",
        line=dict(color="rgba(231,76,60,0)", width=0),
        name="FCF acumulado (negativo)",
        showlegend=False,
    ))
    fig3.add_trace(go.Scatter(
        x=meses, y=fcf_pos,
        fill="tozeroy",
        fillcolor="rgba(0,191,179,0.25)",
        line=dict(color="rgba(0,191,179,0)", width=0),
        name="FCF acumulado (positivo)",
        showlegend=False,
    ))
    fig3.add_trace(go.Scatter(
        x=meses, y=fcf_vals,
        mode="lines",
        name="FCF acumulado",
        line=dict(color=_TURQUESA, width=2.5),
    ))
    # Linha de payback
    payback = viab.payback_meses
    if payback != float("inf"):
        fig3.add_vline(
            x=int(payback),
            line_dash="dash",
            line_color=_VERDE,
            annotation_text=f"Payback: mês {int(payback)}",
            annotation_position="top left",
            annotation_font_color=_VERDE,
        )
    fig3.add_hline(y=0, line_color=_CINZA, line_width=1)
    fig3.update_layout(
        title="FCF acumulado",
        xaxis_title="Mês",
        yaxis_title="R$",
        plot_bgcolor=_BG,
        paper_bgcolor=_BG,
        font=dict(color=_CINZA),
        margin=dict(l=40, r=20, t=50, b=40),
        legend=dict(orientation="h", yanchor="bottom", y=1.02, xanchor="right", x=1),
    )
    st.plotly_chart(fig3, use_container_width=True)
```

```python
    # --- Gráfico 4: DRE breakdown (steady-state) — barras horizontais empilhadas ---
    # Cascata: faturamento → -deduções → receita líquida → -impostos → -custos variáveis
    #          → -custos fixos (pessoal + outros) → -aluguel → EBITDA
    # Implementado como waterfall chart do plotly
    fat = viab.faturamento_mensal_steady
    ded = fat - viab.receita_liquida           # deduções absolutas
    # Impostos: receita_liquida - receita_pos_impostos
    imp = viab.receita_liquida - viab.receita_pos_impostos
    # Custos operacionais = receita_pos_impostos - ebitda
    # Quebrar em: custos variáveis, pessoal, outros_fixos, aluguel
    # Para a cascata, usar valores do resultado (sem re-calcular)
    # Os valores absolutos de custos fixos precisam de kwargs. Usamos ViabilidadeResult
    # que não expõe custos individuais — para o breakdown, usar valores derivados:
    #   total_custos = receita_pos_impostos - ebitda
    total_custos = viab.receita_pos_impostos - viab.ebitda_mensal

    waterfall_labels = [
        "Faturamento bruto",
        "Deduções",
        "Impostos",
        "Custos operacionais",
        "EBITDA",
    ]
    waterfall_values = [fat, -ded, -imp, -total_custos, viab.ebitda_mensal]
    waterfall_measures = ["absolute", "relative", "relative", "relative", "total"]
    waterfall_colors = [
        _TURQUESA,   # faturamento
        _VERMELHO,   # deduções
        _VERMELHO,   # impostos
        _VERMELHO,   # custos
        _VERDE if viab.ebitda_mensal >= 0 else _VERMELHO,  # EBITDA
    ]

    fig4 = go.Figure(go.Waterfall(
        orientation="v",
        measure=waterfall_measures,
        x=waterfall_labels,
        y=waterfall_values,
        text=[f"R${v/1000:.0f}k" for v in waterfall_values],
        textposition="outside",
        connector=dict(line=dict(color=_CINZA, width=1, dash="dot")),
        increasing=dict(marker=dict(color=_TURQUESA)),
        decreasing=dict(marker=dict(color=_VERMELHO)),
        totals=dict(marker=dict(color=_VERDE if viab.ebitda_mensal >= 0 else _VERMELHO)),
    ))
    fig4.update_layout(
        title="DRE breakdown (steady-state)",
        xaxis_title="",
        yaxis_title="R$",
        plot_bgcolor=_BG,
        paper_bgcolor=_BG,
        font=dict(color=_CINZA),
        margin=dict(l=40, r=20, t=50, b=40),
        showlegend=False,
    )
    st.plotly_chart(fig4, use_container_width=True)
```

**Nota sobre o Gráfico 4 (DRE breakdown):** `ViabilidadeResult` expõe `faturamento_mensal_steady`, `receita_liquida`, `receita_pos_impostos` e `ebitda_mensal`. Os valores de deduções e impostos são derivados por diferença desses campos (sem re-calcular). Custos são agrupados em "Custos operacionais" (`receita_pos_impostos - ebitda`) porque o `ViabilidadeResult` não quebra custos fixos vs variáveis individualmente. Essa granularidade é suficiente para o gráfico e não requer exposição de novos campos no dataclass.

---

## 4. Dados disponíveis no `ViabilidadeResult`

| Campo | Descrição | Disponível hoje? |
|---|---|---|
| `faturamento_mensal_steady` | Receita bruta no steady-state | Sim |
| `receita_liquida` | Após deduções | Sim |
| `receita_pos_impostos` | Após impostos | Sim |
| `ebitda_mensal` | EBITDA steady-state | Sim |
| `margem_ebitda_pct` | Margem EBITDA | Sim |
| `payback_meses` | Payback em meses | Sim |
| `roic_anual` | ROIC anual | Sim |
| `lucro_liquido_mensal` | Lucro líquido steady-state | Sim |
| `flag_viavel` | Flag de viabilidade | Sim |
| Série mensal (60 meses) | `alunos_balcao`, `faturamento_mensal`, `ebitda_mensal`, `fcf_acumulado` por mês | **NÃO** — requer nova `gerar_serie_mensal()` |

**Conclusão:** o `ViabilidadeResult` **não** precisa ser alterado (sem novos campos no dataclass). A série mensal é obtida chamando `gerar_serie_mensal()` separadamente em `pages.py` com os mesmos parâmetros do formulário. Isso evita qualquer quebra de API.

---

## 5. Testes a adicionar em `test_simulador.py`

Arquivo: `/repo/tests/unit/dimensionamento/test_simulador.py`

Adicionar no final do arquivo, após os testes CA-10:

```python
# ---------------------------------------------------------------------------
# CA-11: gerar_serie_mensal() (BLK-DIM-21)
# ---------------------------------------------------------------------------

from motor_expansao.dimensionamento.simulador import gerar_serie_mensal


def test_serie_mensal_comprimento_60() -> None:
    """CA-11a: gerar_serie_mensal() retorna exatamente 60 registros."""
    serie = gerar_serie_mensal(**DEFAULTS)
    assert len(serie) == 60


def test_serie_mensal_campos_presentes() -> None:
    """CA-11b: todos os campos obrigatórios presentes em cada registro."""
    serie = gerar_serie_mensal(**DEFAULTS)
    campos = {"mes", "alunos_balcao", "faturamento_mensal", "ebitda_mensal", "fcf_acumulado"}
    for row in serie:
        assert campos == set(row.keys()), f"Campos faltando: {campos - set(row.keys())}"


def test_serie_mensal_sem_nan() -> None:
    """CA-11c: nenhum campo contém NaN ou None."""
    import math
    serie = gerar_serie_mensal(**DEFAULTS)
    for row in serie:
        for k, v in row.items():
            assert v is not None, f"Campo {k} é None no mês {row['mes']}"
            if isinstance(v, float):
                assert math.isfinite(v), f"Campo {k} é não-finito no mês {row['mes']}"


def test_serie_mensal_meses_sequenciais() -> None:
    """CA-11d: campo 'mes' vai de 1 a 60 em sequência."""
    serie = gerar_serie_mensal(**DEFAULTS)
    assert [row["mes"] for row in serie] == list(range(1, 61))


def test_serie_mensal_fcf_acumulado_negativo_inicio() -> None:
    """CA-11e: FCF acumulado no mês 1 é negativo (capex a vista e alto)."""
    serie = gerar_serie_mensal(**DEFAULTS)
    assert serie[0]["fcf_acumulado"] < 0, (
        "FCF acumulado no mês 1 deve ser negativo (capex padrão R$2.34M não é coberto em 1 mês)"
    )


def test_serie_mensal_payback_consistente_com_viabilidade() -> None:
    """CA-11f: payback detectado na série corresponde ao payback de viabilidade().

    Usa VIAVEL (capex=600k) onde payback ocorre dentro de 60 meses.
    """
    from motor_expansao.dimensionamento.simulador import viabilidade
    r = viabilidade(**VIAVEL)
    serie = gerar_serie_mensal(**VIAVEL)
    # Encontrar payback na série: primeiro mês com fcf_acumulado >= 0
    payback_serie = next(
        (row["mes"] for row in serie if row["fcf_acumulado"] >= 0),
        float("inf"),
    )
    assert payback_serie == r.payback_meses, (
        f"Payback da série ({payback_serie}) difere de viabilidade() ({r.payback_meses})"
    )


def test_serie_mensal_alunos_cresce_ate_maturidade() -> None:
    """CA-11g: alunos_balcao cresce monotonicamente até o steady-state."""
    serie = gerar_serie_mensal(**DEFAULTS)
    alunos = [row["alunos_balcao"] for row in serie]
    # Deve crescer ou manter (nunca decresce durante a rampa)
    for i in range(1, len(alunos)):
        assert alunos[i] >= alunos[i - 1] - 0.01, (
            f"Alunos decresceu no mês {i + 1}: {alunos[i]} < {alunos[i - 1]}"
        )
    # Deve atingir o steady-state (938 com churn 0.06) ao final
    assert abs(alunos[-1] - DEFAULTS["alunos_maturidade"]) < 0.01, (
        f"Alunos no mês 60 ({alunos[-1]:.2f}) deveria ser próximo de {DEFAULTS['alunos_maturidade']}"
    )


def test_serie_mensal_com_financiamento_fcf_pior() -> None:
    """CA-11h: com financiamento, FCF acumulado no mês 1 é menor (mais negativo) que sem."""
    serie_sem = gerar_serie_mensal(**VIAVEL)
    serie_com = gerar_serie_mensal(**VIAVEL, capex_financiado_pct=1.0, prazo_financiamento_meses=36)
    # Com PMT subtraída, o FCF de cada mês é menor
    # Mas capex inicial não é pago à vista (apenas a parcela financiada entra via PMT)
    # FCF no mês 1 com financiamento: -capex_a_vista(0%) + FCF_op - PMT
    # vs sem: -capex(100% vista) + FCF_op
    # Com 100% financiado e capex=600k: fcf_acum_mes1_com = -0 + FCF_op - PMT
    #                                    fcf_acum_mes1_sem = -600k + FCF_op
    # PMT ~22.9k; FCF_op ~13k => com: ~13k - 22.9k = -9.9k; sem: -600k + 13k = -587k
    # Então o acumulado com financiamento é MAIOR no início (menos negativo).
    # O payback porém é maior porque a PMT drena o FCF nos primeiros 36 meses.
    # Verificar: payback da série com financiamento >= sem financiamento
    payback_sem = next((row["mes"] for row in serie_sem if row["fcf_acumulado"] >= 0), float("inf"))
    payback_com = next((row["mes"] for row in serie_com if row["fcf_acumulado"] >= 0), float("inf"))
    assert payback_com >= payback_sem, (
        f"Payback com financiamento ({payback_com}) deveria ser >= sem ({payback_sem})"
    )
```

---

## 6. Guardrails confirmados

- **READ-ONLY sobre M1:** SIM — nenhum arquivo toca `config.py` do M1 (`src/motor_expansao/config.py`), `pipelines/m1/`, `score_priorizacao`, `hex_score_estrutural`, pesos, artefatos oficiais ou parquets M1.
- **`dimensionamento/config.py` intocado:** SIM — somente importações de constantes, nenhuma alteração.
- **Cards existentes preservados:** SIM — os gráficos são inseridos APÓS a Seção 7 (grade de sensibilidade), sem alterar nenhuma linha das Seções 4–7.
- **Renderização condicional:** SIM — o bloco de gráficos fica após `if not submitted: return` (linha 3371), só renderiza após submit.
- **Sem dependências novas:** SIM — `plotly>=5.20.0` já é dep base em `pyproject.toml` (linha 20).
- **API de `viabilidade()` preservada:** SIM — a função continua com a mesma assinatura e retorna `ViabilidadeResult` idêntico; os testes existentes CA-01 a CA-10 passam sem alteração.
- **Loop guard:** arquivos alterados (`simulador.py`, `pages.py`, `test_simulador.py`) não estão na lista de bloqueio do `loop_guard.py`.

---

## 7. Sequência de implementação para o Builder

1. **`simulador.py`**: Implementar `gerar_serie_mensal()` **antes** do `aluguel_teto()` (inserir após linha 345). Refatorar o loop interno de `viabilidade()` (linhas 282–322) para delegar a `gerar_serie_mensal()`. Manter o cálculo de `capex_efetivo` localmente em `viabilidade()` para calcular ROIC.

2. **`pages.py`**: 
   - Adicionar `import plotly.graph_objects as go` ao topo do arquivo (com os outros imports de biblioteca, após `import pandas as pd`).
   - Adicionar `from motor_expansao.dimensionamento.simulador import gerar_serie_mensal` no bloco de imports de `motor_expansao.dimensionamento`.
   - Inserir nova "Seção 8: gráficos financeiros" com os 4 `st.plotly_chart(...)` **antes** da "Seção 8: pino do imovel" (linha 3503) — renomear o pino para "Seção 9".

3. **`test_simulador.py`**: Adicionar os 8 testes CA-11a..h ao final do arquivo. O import de `gerar_serie_mensal` pode ir no bloco de imports existente no topo do arquivo (junto com `viabilidade`, `ViabilidadeResult`, etc.) ou localmente nos testes.

4. **Verificação**: `pytest -q tests/unit/dimensionamento/test_simulador.py` (deve passar todos os CA-01..CA-11); `ruff check .`; `mypy src/motor_expansao/dimensionamento/simulador.py src/motor_expansao/dashboard/pages.py`; `python -c "import streamlit_app"`.

---

## 8. Arquivos a alterar (resumo)

| Arquivo | Tipo de mudança |
|---|---|
| `/repo/src/motor_expansao/dimensionamento/simulador.py` | Adicionar `gerar_serie_mensal()` (após linha 345); refatorar loop interno de `viabilidade()` (linhas 282–322) para delegar. |
| `/repo/src/motor_expansao/dashboard/pages.py` | Adicionar `import plotly.graph_objects as go` no topo; import de `gerar_serie_mensal`; inserir 4 `st.plotly_chart(...)` na `render_viabilidade_ponto()` após linha 3501. |
| `/repo/tests/unit/dimensionamento/test_simulador.py` | Adicionar 8 testes CA-11a..h ao final do arquivo. |

**Arquivos NÃO tocados:** `config.py` (M1 ou dimensionamento), `viabilidade_ponto.py`, `pipelines/m1/`, artefatos parquet, `pyproject.toml`, `Dockerfile.*`, `.env`, `secrets/`, CI.
