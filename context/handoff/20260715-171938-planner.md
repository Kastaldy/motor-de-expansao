# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Builder

## Entendimento da tarefa
Trocar o card "Score censitário máximo" por "Número de domicílios" (novo `domicilios_total_raio`,
raio 1,5 km) na página Big Numbers do Relatório Pontual Censitário, reordenar a linha 1 e pintar o
fundo de cada um dos 8 cards em verde/vermelho/neutro conforme a tabela de metas D3 — tudo
READ-ONLY sobre o M1.

## Plano técnico

### 1. `src/motor_expansao/dashboard/censo_point.py` — novo campo `domicilios_total_raio`

1.1. **Dict default do `result`** (dentro de `analisar_ponto_censitario_setores`, hoje linha 204,
logo após `"pop_total_raio": None,`): adicionar a chave

```python
"domicilios_total_raio": None,
```

Isso garante que o campo existe mesmo em qualquer early-return (`setores_df` vazio/None, sem
coluna de geometria, sem candidatos após bbox/validade, sem `records`).

1.2. **Cálculo do peso por área** (hoje linhas 283-284, logo após a criação de
`intersectados["pop_estimada_intersecao"]`): adicionar, seguindo EXATAMENTE o padrão de
`pop_setor`/`pop_estimada_intersecao`, mas SEM persistir como coluna em `intersectados`
(o campo não faz parte de `_empty_setores_frame()`/`display_cols`/`CSV_SETOR_COLUMNS` — não
tocar esse schema, é fora de escopo expor domicílios por setor no CSV/tabela auditável neste
bloco):

```python
# BLK-RELPON-08: mesmo padrao de peso de area de pop_total_raio, mas para domicilios
# particulares ocupados. NAO persistido como coluna em `intersectados`/`setores_intersectados`
# (fora do schema de `_empty_setores_frame()`); so o agregado do raio (`domicilios_total_raio`)
# e exposto no result.
dom_setor = _numeric(intersectados, "domicilios_particulares_ocupados_setor_2022").clip(lower=0)
dom_weights = dom_setor * intersectados["peso_area_setor"]
```

Colocar essas 2 linhas imediatamente depois da linha `intersectados["pop_estimada_intersecao"] =
pop_setor * intersectados["peso_area_setor"]` (linha 284 atual), antes do bloco de `renda`
(linha 286 atual). `intersectados["peso_area_setor"]` já existe (setado no loop de construção de
`records`, linha 273 atual) — reusar a mesma coluna, sem recomputar.

1.3. **Agregação com guard "n/d" gracioso** (hoje logo após o bloco `if pop_weights.notna().any():`
que termina na linha 366 atual, ANTES de `result["renda_per_capita_media_raio"] = ...` na linha
368 atual): adicionar bloco independente, mesmo padrão do guard de `pop_weights`:

```python
if dom_weights.notna().any():
    result["domicilios_total_raio"] = round(float(dom_weights.fillna(0).sum()), 2)
```

Sem coluna `domicilios_particulares_ocupados_setor_2022` em nenhum setor intersectado
(`dom_setor` inteiramente NaN) → `dom_weights` inteiramente NaN → `notna().any()` é `False` →
`result["domicilios_total_raio"]` permanece `None` do dict default (1.1), sem exceção. Mesmo
comportamento gracioso quando `setores_df` é vazio/None ou não há `records` (já cobertos pelos
early-returns existentes, que retornam o dict default intacto).

1.4. **NÃO tocar**: `setor_censitario_intersecao_area_1p5km`, `RAIO_CENSITARIO_DEFAULT_KM`,
`circle_metric`, `_bbox_prefilter`, `_decode_geometry`, `contains_ponto`/lookup do ponto (BLK-
RELPON-05), `agregar_perfil_bairro_distrito` (BLK-RELPON-07, escopo bairro/distrito — usa
`domicilios_total`, campo DIFERENTE de `domicilios_total_raio`, não reusar), `_empty_setores_frame()`,
`display_cols`, `CSV_SETOR_COLUMNS` (em `censo_report.py`).

### 2. `src/motor_expansao/dashboard/censo_report.py` — constantes de meta e cor

2.1. **Novas constantes de módulo** — inserir logo após o bloco de paleta existente (hoje linhas
73-78, logo depois de `_CINZA_TEXTO = (60, 60, 60)`, ANTES do bloco `_PERFIL_*` que começa na
linha 80 atual):

```python
# BLK-RELPON-08 (D3/Q4): metas do semaforo de cor dos 8 cards do Big Numbers. Verde quando o
# valor bate a meta; vermelho quando nao bate; neutro quando "n/d" (Q2, indecidivel). Constantes
# nomeadas e auditaveis (nao hardcoded inline dentro de `_big_numbers_page`).
_META_POP_TOTAL_RAIO = 10_000.0
_META_RENDA_PER_CAPITA_MEDIA_RAIO = 1_500.0
_META_DOMICILIOS_TOTAL_RAIO = 3_000.0
_META_SCORE_SETOR_MEDIO = 60.0
_META_SAM_FITNESS_POTENCIAL = 2_000.0
_META_RESIDUAL_FITNESS_DISPONIVEL = 2_000.0

# Paleta pastel do semaforo (Q3): fundo claro o bastante para preservar contraste com o
# rotulo/valor em cinza-escuro (45,45,45)/(40,40,40) e com a borda fina (225,225,228) ja
# existente do card. Neutro reusa a familia de cinza-claro de `_PERFIL_DIVISOR_RGB` (232,233,237)
# para ficar visualmente distinto do branco puro do card sem meta aplicavel.
_CARD_VERDE_RGB = (205, 236, 217)
_CARD_VERMELHO_RGB = (248, 209, 209)
_CARD_NEUTRO_RGB = (232, 233, 237)
```

(Se o Builder preferir declarar essas constantes mais perto de `_big_numbers_page`, junto de
`_PERFIL_*`, é aceitável — o critério é que sejam constantes de módulo nomeadas, não números soltos
dentro da função. Mas preservar a convenção de agrupar cores da paleta Ultra num único bloco.)

2.2. **Helpers PUROS de cor por card** — inserir imediatamente ANTES da definição de
`_big_numbers_page` (hoje linha 513):

```python
def _cor_por_meta(valor: Any, meta: float) -> tuple[int, int, int]:
    """Cor de fundo do card: verde se valor >= meta, vermelho se < meta, neutro se "n/d".

    BLK-RELPON-08 (D3/Q2): "n/d" (None/NaN) e tratado ANTES da comparacao numerica -- condicao
    indecidivel vira neutro, nunca falsa reprovacao (vermelho) nem falso positivo (verde).
    Funcao pura, testavel isoladamente sem depender do PDF.
    """
    if valor is None or pd.isna(valor):
        return _CARD_NEUTRO_RGB
    return _CARD_VERDE_RGB if float(valor) >= meta else _CARD_VERMELHO_RGB


def _cor_consumo_concorrentes(sam: Any, residual_disponivel: Any) -> tuple[int, int, int]:
    """Cor assimetrica do card "Consumo concorrentes (est.)" -- tambem usada para colorir
    "Concorrentes no raio" (D3: esse card ESPELHA a cor do card acima, sem meta propria).

    Regra (D3): VERMELHO quando o mercado ja esta consumido (SAM Fitness >= meta E Residual
    Fitness < meta); VERDE caso contrario. "n/d" em SAM OU em Residual -> neutro (condicao
    indecidivel, mesmo criterio de `_cor_por_meta`). Funcao pura.
    """
    if sam is None or pd.isna(sam) or residual_disponivel is None or pd.isna(residual_disponivel):
        return _CARD_NEUTRO_RGB
    if (
        float(sam) >= _META_SAM_FITNESS_POTENCIAL
        and float(residual_disponivel) < _META_RESIDUAL_FITNESS_DISPONIVEL
    ):
        return _CARD_VERMELHO_RGB
    return _CARD_VERDE_RGB
```

### 3. `_big_numbers_page` (hoje linhas 513-591) — reordenar, trocar card, pintar fundo

3.1. **Construção da lista `cards`** (hoje linhas 531-541) — substituir por:

```python
residual = residual or {}
sam = residual.get("sam_fitness_potencial")
oferta_disponivel = residual.get("oferta_efetiva_disponivel")
cor_consumo = _cor_consumo_concorrentes(sam, oferta_disponivel)

cards = [
    (
        "População total no raio",
        _format_number(result.get("pop_total_raio"), 0),
        _cor_por_meta(result.get("pop_total_raio"), _META_POP_TOTAL_RAIO),
    ),
    (
        "Renda per capita média",
        "R$ " + _format_number(result.get("renda_per_capita_media_raio"), 2),
        _cor_por_meta(result.get("renda_per_capita_media_raio"), _META_RENDA_PER_CAPITA_MEDIA_RAIO),
    ),
    (
        "Número de domicílios",
        _format_number(result.get("domicilios_total_raio"), 0),
        _cor_por_meta(result.get("domicilios_total_raio"), _META_DOMICILIOS_TOTAL_RAIO),
    ),
    (
        "Score censitário médio",
        _format_number(result.get("score_setor_medio"), 2),
        _cor_por_meta(result.get("score_setor_medio"), _META_SCORE_SETOR_MEDIO),
    ),
    (
        "SAM Fitness (alunos)",
        _format_number(sam, 0),
        _cor_por_meta(sam, _META_SAM_FITNESS_POTENCIAL),
    ),
    (
        "Residual Fitness (alunos)",
        _format_number(oferta_disponivel, 0),
        _cor_por_meta(oferta_disponivel, _META_RESIDUAL_FITNESS_DISPONIVEL),
    ),
    ("Concorrentes no raio", _format_number(result.get("n_concorrentes"), 0), cor_consumo),
    (
        "Consumo concorrentes (est.)",
        _format_number(residual.get("oferta_consumida_mercado_estimada"), 0),
        cor_consumo,
    ),
]
```

Nota: `residual = residual or {}` JÁ existe hoje na linha 531 — não duplicar, só mover para antes
de `sam`/`oferta_disponivel`. `"Score censitário máximo"`/`score_setor_max` SAI da lista (não
aparece mais em nenhum card) — o campo pode continuar sendo calculado e devolvido em `result`
(não tocar `analisar_ponto_censitario_setores` quanto a `score_setor_max`) para auditoria em
CSV, só deixa de ser exibido no PDF.

3.2. **Loop de desenho dos cards** (hoje linhas 553-575) — o desempacotamento passa de
`(label, value)` para `(label, value, cor_fundo)`, e o fill do retângulo de fundo usa `cor_fundo`
em vez de `_BRANCO`, ANTES da borda e da barra de acento:

```python
for index, (label, value, cor_fundo) in enumerate(cards):
    col = index % cols
    row = index // cols
    x = margin_x + col * (card_w + gap)
    y = top + row * (card_h + gap)
    # Cartao com fundo por meta (semaforo D3) + borda fina + barra de destaque (D3=B).
    pdf.set_fill_color(*cor_fundo)
    pdf.rect(x, y, card_w, card_h, style="F")
    pdf.set_draw_color(225, 225, 228)
    pdf.rect(x, y, card_w, card_h, style="D")
    accent = accents[index % len(accents)]
    pdf.set_fill_color(*accent)
    pdf.rect(x, y, card_w, 6.0, style="F")
    # Rotulo (D2=B: cinza-escuro 45,45,45; acento so na barra do topo).
    pdf.set_text_color(45, 45, 45)
    pdf.set_font("Helvetica", "", 11)
    pdf.set_xy(x + 14, y + 20)
    pdf.multi_cell(card_w - 28, 14, _ascii(label))
    # Valor grande (D2=B: cinza-escuro 40,40,40, nao no acento; D3=B: 26 pt).
    pdf.set_text_color(40, 40, 40)
    pdf.set_font("Helvetica", "B", 26)
    pdf.set_xy(x + 14, y + 88)
    pdf.multi_cell(card_w - 28, 28, _ascii(value))
```

Resto do loop (posição x/y, `accents`, cores de texto do rótulo/valor) fica INALTERADO — só a
fonte do fill de fundo muda de `_BRANCO` para `cor_fundo`.

3.3. **Nota de fonte auditável** (hoje linhas 577-590) — atualizar o texto para mencionar o
semáforo, mantendo ASCII-safe (sem travessão/reticências fora de latin-1; usar `-`/`...`):

```python
pdf.multi_cell(
    _PAGE_W - 2 * margin_x,
    11,
    _ascii(
        "Fontes: pop/renda/domicilios/score = censo (interseção de setores IBGE 2022 com círculo de "
        f"1.5 km, método {metodo}); SAM Fitness, Residual Fitness (em alunos) e consumo = lookup "
        "READ-ONLY do hex H3 (sem recálculo do M1). Fundo do card: verde = meta atingida, vermelho = "
        "meta não atingida, cinza = 'n/d' (dado ausente para o ponto)."
    ),
)
```

## Arquivos afetados
- `src/motor_expansao/dashboard/censo_point.py` — função `analisar_ponto_censitario_setores`
  (novo campo `domicilios_total_raio`).
- `src/motor_expansao/dashboard/censo_report.py` — novas constantes de meta/cor, novos helpers
  `_cor_por_meta`/`_cor_consumo_concorrentes`, função `_big_numbers_page` (cards, loop de desenho,
  nota de fonte).
- `tests/unit/test_relatorio_pontual_censitario_motor.py` — testes de `domicilios_total_raio`.
- `tests/unit/test_relatorio_pontual_censitario_export.py` — testes de rótulos/ordem/cor dos
  cards do PDF.
- `docs/relatorio_pontual_censitario.md` — seção "Cards Big Numbers" (linha ~244 atual).

## 4. Testes obrigatórios

### 4.1. `tests/unit/test_relatorio_pontual_censitario_motor.py` (agregação `domicilios_total_raio`)

Estender o helper `_sector_record` (hoje linhas 26-61) com um kwarg opcional
`domicilios: float | None = None`, adicionando ao dict retornado:
`"domicilios_particulares_ocupados_setor_2022": domicilios,` (sempre presente na coluna; `None`
vira NaN via `_numeric`/`pd.to_numeric(errors="coerce")` — comportamento gracioso já coberto pelo
motor). Isso NÃO quebra os testes existentes (que não passam `domicilios`, logo continuam com a
coluna toda NaN — exercitando o caso "n/d" por omissão).

Casos novos a adicionar (mesmo padrão/estilo dos testes de `pop_total_raio` já existentes,
linhas 64-119):

- `test_motor_censitario_domicilios_total_raio_setor_totalmente_dentro_do_raio`: setor
  totalmente dentro do raio (ex.: `box(-100, -100, 100, 100)`, mesmo padrão do teste de linha 64),
  `domicilios=300`. Esperado: `result["domicilios_total_raio"] == pytest.approx(300)` (peso de área
  = 1.0).
- `test_motor_censitario_domicilios_total_raio_setor_parcial_pondera_por_area`: setor
  parcialmente dentro do raio (mesmo `box(1000, -500, 2500, 500)` do teste de linha 88),
  `domicilios=1000`. Calcular `expected_weight` do MESMO jeito que o teste existente de
  `pop_total_raio` parcial (linha 93-94, `setor.intersection(Point(0,0).buffer(1500,
  quad_segs=64)).area / setor.area`). Esperado: `result["domicilios_total_raio"] ==
  pytest.approx(1000 * expected_weight, rel=0.01)`.
- `test_motor_censitario_domicilios_total_raio_nd_quando_coluna_ausente`: setor totalmente
  dentro do raio SEM passar `domicilios` (kwarg no default `None`). Esperado:
  `result["domicilios_total_raio"] is None`. (Cobre também implicitamente o caso `setores_df`
  vazio/sem candidatos, já coberto pelo dict default — não precisa de teste redundante, mas pode
  reaproveitar `test_motor_censitario_exclui_setor_fora_do_raio` adicionando a asserção
  `assert result["domicilios_total_raio"] is None` naquele teste já existente, linha 122-132.)
- Adicionar `assert result["domicilios_total_raio"] == pytest.approx(500)` (ou valor coerente
  passado) no teste `test_motor_censitario_setor_totalmente_dentro_do_raio` existente SE o
  Builder optar por reusar esse teste em vez de criar um novo dedicado — qualquer uma das duas
  abordagens é aceitável, desde que o cálculo com peso de área conhecido e o cenário "n/d"
  fiquem cobertos por teste explícito.

### 4.2. `tests/unit/test_relatorio_pontual_censitario_export.py` (ordem/rótulos/cor no PDF)

- Atualizar `test_pdf_big_numbers_com_residual_e_nd` (hoje linhas 162-186): remover
  `"Score censitário máximo".encode("latin-1")` da lista de rótulos esperados presentes; adicionar
  `"Número de domicílios".encode("latin-1")`; adicionar uma asserção NEGATIVA
  `assert "Score censitário máximo".encode("latin-1") not in pdf_com` (o rótulo antigo não deve mais
  aparecer no PDF). Para exercitar o campo `domicilios_total_raio`, adicionar essa chave a `result`
  antes de gerar o PDF (`result["domicilios_total_raio"] = <valor>` no teste, já que `_sample_result()`
  não tem `domicilios_particulares_ocupados_setor_2022` nos setores sintéticos — mais simples
  injetar o valor diretamente no dict de `result` do que alterar `_sector_record` deste arquivo) e
  assertar que o número formatado aparece nos bytes do PDF (`_format_number(..., 0)`, mesmo padrão
  de `assert b"3.000" in pdf_com` já usado).
- Novo teste `test_pdf_big_numbers_ordem_linha_1` (ou nome equivalente): gerar o PDF e verificar,
  via a POSIÇÃO relativa dos rótulos nos bytes (`pdf_bytes.index(label_bytes)`), que
  `"População total no raio"` aparece antes de `"Renda per capita média"`, que aparece antes de
  `"Número de domicílios"`, que aparece antes de `"Score censitário médio"` — replicando o padrão
  de asserção por índice de bytes já usado no módulo (ver `pdf_bytes.count(...)`/`in pdf_bytes`
  nos testes vizinhos). Se comparação de índice de bytes for frágil (streams comprimidos por
  página, ordem de objetos), alternativa aceitável: testar a ORDEM diretamente na lista `cards`
  chamando `_big_numbers_page` isolado ou, preferencialmente, testar a lista de labels retornada
  por uma extração local dos 4 primeiros elementos de `cards` — o Builder deve escolher a
  abordagem mais robusta dado que `set_compression(False)` está ativo neste módulo (texto cru no
  content stream, então a ordem de aparição no stream deveria refletir a ordem de desenho; validar
  empiricamente antes de finalizar o teste).

### 4.3. Novo bloco de testes para os helpers puros de cor (mesmo arquivo
`tests/unit/test_relatorio_pontual_censitario_export.py`, ou um novo arquivo dedicado
`tests/unit/test_relatorio_pontual_censitario_cor_cards.py` — Builder escolhe, mas preferir
reusar o arquivo de export por proximidade temática)

Importar `_cor_por_meta`, `_cor_consumo_concorrentes` e as 6 constantes `_META_*` de
`motor_expansao.dashboard.censo_report`. Casos:

- `_cor_por_meta`: valor acima da meta → `_CARD_VERDE_RGB`; valor exatamente igual à meta →
  `_CARD_VERDE_RGB` (meta é inclusiva, `>=`); valor abaixo da meta → `_CARD_VERMELHO_RGB`;
  `valor=None` → `_CARD_NEUTRO_RGB`; `valor=float("nan")` → `_CARD_NEUTRO_RGB`. Testar para pelo
  menos 2 dos 4 campos com meta simples (`pop_total_raio`/`_META_POP_TOTAL_RAIO` e
  `domicilios_total_raio`/`_META_DOMICILIOS_TOTAL_RAIO`) para cobrir o campo NOVO.
- `_cor_consumo_concorrentes`: (a) SAM alto (`>=2000`) E Residual baixo (`<2000`) →
  `_CARD_VERMELHO_RGB` (regra assimétrica do Consumo); (b) SAM alto E Residual alto (`>=2000`) →
  `_CARD_VERDE_RGB`; (c) SAM baixo (independente do Residual) → `_CARD_VERDE_RGB`; (d) SAM=`None`
  ou Residual=`None` (cada um isoladamente) → `_CARD_NEUTRO_RGB` (2 casos: só SAM ausente, só
  Residual ausente).
- Confirmar por leitura direta do card `cards[6]` (Concorrentes) e `cards[7]` (Consumo) dentro de
  um teste que invoca a construção de `cards` (via `_big_numbers_page` real, inspecionando que
  ambos os cards usam a MESMA cor computada por `_cor_consumo_concorrentes`) — pode ser feito
  chamando `_cor_consumo_concorrentes` diretamente com os mesmos SAM/Residual do teste de PDF e
  comparando o resultado com o card, OU um teste mais direto que constrói `cards` chamando a lógica
  da seção 3.1 isolada. Builder decide a forma mais simples que evite duplicar lógica de produção
  no teste.

## Dependências
- Nenhuma dependência nova de pacote. `pandas`/`fpdf2` já em uso.
- Depende da leitura correta de `domicilios_particulares_ocupados_setor_2022` no parquet de
  setores censitários (já existe hoje — usado por `agregar_perfil_bairro_distrito`, BLK-RELPON-07).

## Riscos técnicos
- **Peso de área divergente**: se `dom_weights` não seguir exatamente `peso_area_setor` (mesma
  coluna de `pop_estimada_intersecao`), o número de domicílios fica inconsistente com pop/renda do
  mesmo raio. Mitigado pelos testes 4.1 com peso conhecido.
- **"n/d" pintado como reprovação falsa**: mitigado por `_cor_por_meta`/`_cor_consumo_concorrentes`
  checarem `None`/`pd.isna` ANTES de qualquer comparação numérica, com testes dedicados 4.3.
- **Contraste ilegível dos pasteis propostos**: `_CARD_VERDE_RGB=(205,236,217)`,
  `_CARD_VERMELHO_RGB=(248,209,209)`, `_CARD_NEUTRO_RGB=(232,233,237)` são hipóteses de partida
  (luminância alta, texto cinza-escuro 45/45/45 e 40/40/40 deve manter contraste >= 4.5:1 nos
  três). Ajuste fino de tom fica para a revisão visual humana do PDF gerado (gate obrigatório no
  fim da esteira, já registrado no `current_task.md`/critérios de aceite do bloco).
- **Teste de ORDEM por posição de bytes pode ser frágil** (item 4.2): validar empiricamente antes
  de fechar; se a ordem de bytes no stream não refletir a ordem de desenho, usar alternativa que
  testa a lista `cards` diretamente.
- **Regressão do rótulo antigo**: sem a asserção negativa explícita (`not in pdf_com`), um Builder
  apressado poderia deixar "Score censitário máximo" residual em algum lugar (ex.: comentário
  dentro de f-string da nota de fonte). Mitigado pela asserção negativa do item 4.2.

## Critérios de aceite finais
- `domicilios_total_raio` existe no dict default de `analisar_ponto_censitario_setores` e é
  calculado sobre `intersectados` (full columns) pelo mesmo padrão de peso de área de
  `pop_total_raio`, com "n/d" gracioso quando não há dado.
- Página Big Numbers exibe, na L1: População total no raio, Renda per capita média, Número de
  domicílios, Score censitário médio (nessa ordem); "Score censitário máximo" NÃO aparece mais no
  PDF (pode permanecer em `result`/CSV).
- Cada um dos 8 cards tem fundo verde/vermelho/neutro conforme a tabela D3, com a regra
  assimétrica do Consumo e o espelhamento do card Concorrentes; "n/d" sempre neutro.
- As 6 metas existem como constantes nomeadas em `censo_report.py` (`_META_*`).
- Todos os testes novos/atualizados listados na seção 4 passam.
- `ruff check` e `mypy` limpos nos 2 arquivos de produção alterados.
- Acentuação correta do português em todo texto novo voltado ao usuário (rótulos, nota de fonte);
  nenhum caractere fora de latin-1 introduzido (sem travessão/bullet/seta/reticências/aspas
  curvas — usar `-`/`...`/aspas retas).
- Método de interseção, raio de 1,5 km, `_empty_setores_frame()`, `CSV_SETOR_COLUMNS`, e qualquer
  artefato/score do M1 permanecem INTOCADOS.
- Contagem de páginas do PDF permanece 6 (`/Count 6`); `PDF_SECTION_HEADERS` inalterado.

## Validações obrigatórias
- Comando: `python -m ruff check src/motor_expansao/dashboard/censo_point.py src/motor_expansao/dashboard/censo_report.py tests/unit/test_relatorio_pontual_censitario_motor.py tests/unit/test_relatorio_pontual_censitario_export.py`
  Critério mínimo: 0 erros.
- Comando: `python -m mypy src/motor_expansao/dashboard/censo_point.py src/motor_expansao/dashboard/censo_report.py`
  Critério mínimo: 0 erros (atenção à paridade de stubs com o CI — instalar `types-requests`/stubs
  do `[dev]` extra localmente antes de validar, conforme lição registrada do projeto).
- Comando: `python -m pytest -q tests/unit/test_relatorio_pontual_censitario_motor.py tests/unit/test_relatorio_pontual_censitario_export.py tests/integration/test_streamlit_app.py`
  Critério mínimo: 0 falhas, 0 erros de coleta.
- Comando (smoke de import): `python -c "import motor_expansao.dashboard.censo_report; import motor_expansao.dashboard.censo_point"`
  Critério mínimo: sem exceção.
- QA roda a suíte FULL (`pytest -q`) como gate único antes de fechar o ciclo — Builder não precisa
  rodar a suíte inteira, mas o subconjunto acima deve estar limpo antes de entregar.

## Fora de escopo
- Método de interseção `setor_censitario_intersecao_area_1p5km`, raio 1,5 km,
  `RAIO_CENSITARIO_DEFAULT_KM`, `circle_metric`.
- `_empty_setores_frame()`, `display_cols`, `CSV_SETOR_COLUMNS` — não adicionar
  `domicilios_particulares_ocupados_setor_2022` como coluna exposta em CSV/tabela auditável neste
  bloco (fora do pedido; só o agregado `domicilios_total_raio` no `result`/PDF).
- Página "Perfil do Bairro/Distrito" (BLK-RELPON-07) e seu `domicilios_total` (escopo
  bairro/distrito) — campo DIFERENTE de `domicilios_total_raio`, não reusar nem tocar.
- Mapas de calor/choropleth, marca d'água anti-PII, `set_compression(False)`.
- `score_priorizacao`, pesos, `hex_score_estrutural`, carteira, plano, artefatos oficiais do M1.
- `flag_sam`/gate do SAM (DEC-006/DEC-007) — os thresholds de D3 são de DISPLAY local ao PDF, não
  alteram `sam_fitness_potencial`/`oferta_efetiva_disponivel` nem o pipeline de mercado.
- Contagem de páginas do PDF (permanece 6). Relatório Municipal e UI do dashboard Streamlit.

## Alerta de aprovação
Não aplicável — criticidade Média confirmada pelo Block Orchestrator (READ-ONLY sobre o M1, sem
alteração de score/pesos/artefatos oficiais). Esteira segue direto para Builder, sem aprovação
humana antes da execução. A revisão humana obrigatória é VISUAL do PDF gerado, no FIM da esteira
(após QA), conforme já registrado em `tasks/current_task.md`.

## Guardrails ativos
- **READ-ONLY M1 (CLAUDE.md §5)**: visualizações e relatórios não podem recalcular ou alterar
  `score_priorizacao`, `hex_score_estrutural`, carteira, plano curto prazo, plano de domínio ou
  artefatos oficiais do M1. Este bloco não toca nenhum desses.
- **Acentuação (CLAUDE.md §2)**: todo texto voltado ao usuário no PDF deve ter acentuação correta
  do português; caracteres fora de latin-1 (travessão `—`, bullet `•`, seta `→`, reticências `…`,
  aspas curvas, `©`) viram `"?"` silenciosamente via `_ascii()` — usar `-`/`"`/`...` em texto novo.
  Rótulo do card novo é "Número de domicílios" (sem travessão).
- **Núcleo `censo_*` como interface estável (DEC-005 emenda 2026-06-12)**: o método de interseção,
  o raio 1,5 km e a lógica de interseção são INTOCADOS; este bloco só ESTENDE leitura/agregação
  (novo campo) e render (cor/ordem).
- **flag_sam/DEC-006/DEC-007 intocados**: as metas de cor de D3 são thresholds de DISPLAY locais a
  `censo_report.py`; não alteram o gate do SAM no pipeline de mercado nem os valores de
  `sam_fitness_potencial`/`oferta_efetiva_disponivel` — o card apenas LÊ esses valores.
- **Parâmetros canônicos imutáveis sem DEC** (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0,
  RENDA_MIN=4500.0, pesos renda=0.40/pop=0.60): nenhum tocado por este bloco.
