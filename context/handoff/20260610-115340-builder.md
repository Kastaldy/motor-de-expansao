# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-FIX-11 — Tornar funcionais os 3 overlays "mortos" do Mapa Territorial (Alternativa A)

## O que foi feito
Fiação dos 3 overlays inertes do Mapa Territorial (`hex_pesquisado`, `descartados_5k`, `ancoras_dominio`), conforme plano numerado do Planner e decisões visuais DV-1 a DV-5 (APROVADAS por Felipe Silva em 2026-06-10). READ-ONLY sobre o M1: nenhum recálculo de score/carteira/plano/artefatos. Modo `dominio` NÃO recebe âncoras (follow-up, fora de escopo por decisão de Felipe).

OVERLAY 1 — `hex_pesquisado` (gate centralizado no dispatcher `build_unified_map_figure`):
- Derivadas `_search_pin`/`_search_hex_id` = valor original só quando `"hex_pesquisado" in enabled_overlays`, senão `None`. Propagadas às 3 chamadas de builder (residual, hibrido/censitario, m1). Os `if search_pin is not None` / `if search_hex_id is not None` já existentes em cada builder atuam como gate final (sem alterar a lógica interna dos builders). `build_dominio_map_figure` não recebe esses parâmetros (inalterado).

OVERLAY 2 — `descartados_5k`:
- `_apply_pop_cut_colors(map_df, *, show_discarded: bool = True)`: com `show_discarded=False` retorna `map_df` sem aplicar `_DISCARDED_FILL`/`_DISCARDED_LINE` (hexes <5k ficam com a cor de score normal — DV-3). Guard adicionado logo no início da função.
- `_apply_hex_tooltip_fields(map_df, *, mode, show_discarded: bool = True)`: os dois blocos que anexam `" — Descartado <5k hab"` ao `tooltip_title` (mode hybrid e mode m1) agora gateados por `and show_discarded`.
- Os 3 builders (`build_map_figure`, `build_hybrid_map_figure`, `build_residual_heatmap_figure`) ganharam `show_discarded: bool = True` na assinatura e o propagam às chamadas de `_apply_pop_cut_colors`/`_apply_hex_tooltip_fields`, incluindo o caminho de `search_tooltip_source` (tooltip do hex pesquisado) nos 3.
- Dispatcher deriva `_show_discarded = "descartados_5k" in enabled_overlays` e passa às 3 chamadas de builder.

OVERLAY 3 — `ancoras_dominio`:
- Nova `_build_ancoras_dominio_layer(dominio_df, selected_ufs, selected_cities) -> pdk.Layer | None`: no-op silencioso (retorna `None`) se `dominio_df` é None/vazio ou não tem `{lat, lng, hex_id}`; filtra por UF (se `uf` presente) e por cidade (`nome_municipio`); descarta lat/lng NaN; retorna `None` se vazio após filtros. ScatterplotLayer âmbar `[245, 158, 11, 220]`, `get_radius=250`, `radius_min_pixels=8`, `radius_max_pixels=20`, `line_color=[255,255,255,200]`, `stroked/filled/pickable=True` (DV-1/DV-2). DataFrame da camada inclui `tooltip_title = "Ancora Dominio: " + hex_id` + `tooltip_line_1..14 = ""` para compatibilidade com `_shared_map_tooltip`.
- Dispatcher: as 3 ramas não-domínio passaram a capturar `deck, n_pontos`; após o builder, se `"ancoras_dominio" in enabled_overlays and deck is not None`, injeta a camada via `deck.layers = [*deck.layers, ancora_layer]` (reconstrução da lista, não `.append`, para robustez). Modo `dominio` retorna direto (sem âncoras — follow-up).

LEGENDA:
- `render_ancoras_dominio_legend()` criada em `components.py` (chip âmbar `#F59E0B`, opacity 0.86).
- `pages.py` `_render_unified_legend`: `render_pop_cut_legend()` agora gateada por `"descartados_5k" in enabled_overlays` nos modos m1/hibrido/censitario/residual; `render_ancoras_dominio_legend()` adicionada ao final, gateada por `"ancoras_dominio" in enabled_overlays`. Sem legenda para `hex_pesquisado` (DV-4). Import de `render_ancoras_dominio_legend` adicionado ao bloco de imports de `components` em `pages.py`.

Nada tocado em `constants.py` (OVERLAYS já corretos, DV-5), `pipelines/`, `scoring.py`, `config.py`.

## Arquivos alterados
- `src/motor_expansao/dashboard/components.py` — gates `_search_pin`/`_search_hex_id`/`_show_discarded` + injeção de âncoras no dispatcher `build_unified_map_figure`; `show_discarded` em `_apply_pop_cut_colors`, `_apply_hex_tooltip_fields` e nos 3 builders (incl. call-sites de `search_tooltip_source`); nova `_build_ancoras_dominio_layer`; nova `render_ancoras_dominio_legend`.
- `src/motor_expansao/dashboard/pages.py` — `_render_unified_legend`: gate de `render_pop_cut_legend` por `descartados_5k` + `render_ancoras_dominio_legend` por `ancoras_dominio`; import do helper.
- `tests/integration/test_streamlit_app.py` — 3 novos testes de overlay (ligado vs desligado + no-op de `dominio_df` None/vazio).

## Validações executadas
- pytest (subconjunto impactado, serial): `python -m pytest -p no:cacheprovider -q tests/integration/test_streamlit_app.py` → **179 passed, 0 failed, 0 skipped, 1 warning** (warning preexistente: `Unknown config option: asyncio_mode`).
  - Nota de ambiente: `-n auto` (xdist) aborta com `OSError WinError 6` (spawn de worker bloqueado neste shell sandbox); rodado serial, que o baseline do projeto (BLK-ORQ-01) documenta como idêntico ao `-n auto`. QA deve confirmar com `-n auto` em ambiente sem essa restrição.
  - 3 testes novos confirmados rodando (-k filtrado): `test_build_unified_map_figure_overlay_hex_pesquisado_ligado_vs_desligado`, `test_build_unified_map_figure_overlay_descartados_5k_ligado_vs_desligado`, `test_build_unified_map_figure_overlay_ancoras_dominio_ligado_vs_desligado` → **3 passed**.
- import: `python -c "import streamlit_app; print('import ok')"` → **ok** (apenas warnings de cache do Streamlit sem runtime, esperados fora de servidor).
- ruff: `python -m ruff check src/ tests/` → **All checks passed!** (1 finding UP037 introduzido por mim — anotação de retorno entre aspas — já corrigido removendo as aspas).
- mypy: `python -m mypy src/motor_expansao/dashboard/components.py src/motor_expansao/dashboard/pages.py --ignore-missing-imports` → **Success: no issues found in 2 source files**.
- NÃO rodada a suíte full (gate do QA).

## Problemas encontrados
- Ambiente local só tem Python 3.14.2 sem pytest/ruff/mypy instalados; instalados na sessão (`pip install pytest pytest-xdist ruff mypy`). `pytest -n auto` falha por restrição de spawn de worker do sandbox (WinError 6) — contornado rodando serial. Sem impacto no código.

## Pendências
- nenhuma (dentro do escopo do bloco). Injeção de âncoras no modo `dominio` permanece como follow-up explícito (Felipe não pediu).

## Riscos remanescentes
- QA deve reconfirmar a suíte FULL com `-n auto` em ambiente sem a restrição de spawn (gate único). Contagem de camadas nos novos testes usa `enabled_overlays` mínimo (só o overlay testado) para isolar; se houver builder que crie camada-base adicional em outro recorte, a contagem é específica do cenário m1 sem concorrentes/ultra (validado: 1 layer base).

## Guardrails verificados
- score_priorizacao não alterado: sim
- Artefatos M1 preservados: sim (nenhum parquet/CSV oficial tocado; só camada de visualização)
- Dashboard offline mantido: sim (nenhuma dependência de API ao vivo introduzida)
