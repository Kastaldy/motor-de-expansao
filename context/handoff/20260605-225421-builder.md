# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-CENSO-02 — Relatório censitário: template e visual padrão do PDF (reescrita do writer sobre fpdf2)

## Decisão do gate humano honrada
- Felipe (2026-06-05) APROVOU com alteração de D1: **PDF reescrito sobre `fpdf2`** (NÃO estendido o
  writer manual PDF-1.4). `fpdf2>=2.7.0,<3` entrou como **dependência BASE** em `pyproject.toml`.
- **Compressão de stream DESATIVADA** no writer (`_UltraPDF.set_compression(False)`) → texto cru
  aparece nos bytes (headers de seção, rótulos do Big Numbers, anti-PII auditável). `pdf_version="1.4"`
  setado para continuidade do assert histórico `%PDF-1.4`.
- D2 (assets do .pptx), estrutura de 7 páginas e as 6 métricas do Big Numbers: implementados como no
  plano. Anti-PII mantido (image24.png NUNCA extraído/embutido; .pptx/PDF nunca versionados).

## O que foi feito
1. **Assets (Fase 0):** extraídos 2 fundos LIMPOS do `data/referencias/Teste Modelo.pptx` (zip) para
   `data/ultra/` (gitignored), otimizados com Pillow `optimize=True`:
   - `data/ultra/relatorio_capa_bg.png` ← `ppt/media/image6.png` (capa turquesa #00A79D), 1360×763.
   - `data/ultra/relatorio_conteudo_bg.png` ← `ppt/media/image1.jpg` → PNG (fundo claro #F8F8F8), 783×437.
   - `ppt/media/image24.png` (cartão de contato com PII) NUNCA tocado; `.pptx` NÃO versionado.
2. **Writer fpdf2 (`censo_report.py` reescrito):** classe `_UltraPDF(FPDF)` (A4 pt, compressão off,
   pdf_version 1.4, sem auto page-break, margens 0). Helpers: `_load_branding_assets` (fallback
   gracioso → `None` por asset em qualquer falha), `_draw_full_page_background` (imagem ou retângulo de
   cor sólida), `_draw_title_band` (faixa turquesa), `_draw_footer` (crédito Ultra + atribuição de
   tiles), `_draw_map`. Builders de página: `_cover_page`, `_map_page` (×3), `_big_numbers_page` (grid
   2×3), `_competitors_page` (mapa + lista de redes sem PII), `_credit_page`.
   - **Estrutura de 7 páginas** na ordem: Capa → População → Renda → Score censitário → Big Numbers →
     Concorrentes → Realização/Crédito.
   - **Paleta:** turquesa (0,167,157), magenta (194,60,142), branco-gelo (248,248,248).
   - **Retrocompat:** `mapas` como `bytes` único → mapa só na página de População; renda/score mostram
     "Mapa indisponivel para esta camada.".
   - `PDF_SECTION_HEADERS` atualizado para os 7 títulos canônicos ASCII (Relatorio Pontual Censitario,
     Populacao, Renda, Score censitario, Big Numbers, Concorrentes, Realizacao).
   - Nova assinatura `gerar_pdf_relatorio_pontual_censitario(result, mapas=None, *, residual=None,
     ultra_dir=None)`; kwargs `residual`/`ultra_dir` propagados por
     `gerar_payloads_download_relatorio_censitario` e `render_downloads_relatorio_censitario`.
   - `gerar_csv_setores_censitarios` INALTERADO (contrato CSV preservado).
3. **Big Numbers (Fase 4, READ-ONLY) — 6 métricas:** pop total / renda média / score médio / score máx
   vêm do `result`; **residual fitness** (`score_oportunidade_residual`) e **consumo de concorrentes**
   (`oferta_consumida_mercado_estimada`) vêm de `lookup_hex_by_coord(lat,lng,df,h3_res=7)` lido em
   `pages.py` (df já em escopo). `None`/NaN/hex-ausente → "n/d" com nota de fonte citando o método
   `setor_censitario_intersecao_area_1p5km`. Sem load novo de parquet; sem recálculo de M1/residual.
4. **`pages.py`:** em `render_relatorio_pontual_censitario`, antes do download: monta `residual` via
   `lookup_hex_by_coord` (leitura pura, guarda contra NaN e `_not_found`) e passa `residual` +
   `ultra_dir=Path("data/ultra")` ao `render_downloads_relatorio_censitario`.
5. **Docs/CLAUDE.md:** `docs/relatorio_pontual_censitario.md` §7 reescrita (fpdf2, 7 páginas, Big
   Numbers e fontes, assets, anti-PII, fallback offline, nota de deploy); `CLAUDE.md` §4 (linha do
   Motor/UI censitário) atualizada com 1 frase sobre o template fpdf2 + Big Numbers READ-ONLY.

## Arquivos alterados
- `src/motor_expansao/dashboard/censo_report.py` — reescrita completa do writer sobre fpdf2 (7 páginas,
  assets, paleta, Big Numbers, fallback gracioso, anti-PII). CSV inalterado.
- `src/motor_expansao/dashboard/pages.py` — `render_relatorio_pontual_censitario`: resolve `residual`
  via `lookup_hex_by_coord` (READ-ONLY) + passa `residual`/`ultra_dir`.
- `pyproject.toml` — `fpdf2>=2.7.0,<3` adicionado em `[project] dependencies` (base).
- `tests/unit/test_relatorio_pontual_censitario_export.py` — novos headers, `/Count 7`, Big Numbers
  com/sem residual ("n/d"), offline-safe sem assets, anti-PII, atribuição de tiles, retrocompat ajustada.
- `docs/relatorio_pontual_censitario.md` — §7 reescrita.
- `CLAUDE.md` — §4 (1 frase).
- `data/ultra/relatorio_capa_bg.png`, `data/ultra/relatorio_conteudo_bg.png` — assets extraídos do
  .pptx (GITIGNORED; ficam no host/volume, NÃO entram no git).

## Validações executadas (saída literal)
```
$ python -m pytest -n auto -q tests/unit/test_relatorio_pontual_censitario_export.py tests/integration/test_streamlit_app.py
185 passed, 16598 warnings in 17.65s

$ python -c "import streamlit_app; print('import ok')"
import ok

$ python -m ruff check src tests
All checks passed!

$ python -m mypy src/motor_expansao/dashboard/censo_report.py src/motor_expansao/dashboard/pages.py
Success: no issues found in 2 source files
```
(Os 9 testes do export passam isoladamente: `9 passed`. Warnings são DeprecationWarnings de
pytest-asyncio sob Python 3.14 local — ambiente, não defeito.)

### Anti-PII manual (git)
```
$ git status --short
 M context/handoff.md
 M pyproject.toml
 M src/motor_expansao/dashboard/censo_report.py
 M src/motor_expansao/dashboard/pages.py
 M tasks/current_task.md
 M tests/unit/test_relatorio_pontual_censitario_export.py
?? data/referencias/   (UNTRACKED — .pptx/PDF de PII NÃO entram no git)

$ git check-ignore data/ultra/relatorio_capa_bg.png data/ultra/relatorio_conteudo_bg.png
data/ultra/relatorio_capa_bg.png
data/ultra/relatorio_conteudo_bg.png   (gitignored — assets não vão ao commit)

$ git ls-files data/referencias/
(vazio — nada de data/referencias/ rastreado)
```
- Teste `test_pdf_sem_pii_de_pessoas` confirma que `b"vinicius"`, `b"Vinicius"`, `b"96346-2974"`,
  `b"@ultraacademia.com.br"` e variantes NÃO aparecem nos bytes do PDF.

## Problemas encontrados
- fpdf2 2.8.7 emite `%PDF-1.3` por padrão → resolvido com `self.pdf_version = "1.4"`.
- Retrocompat: "Renda per capita" colidia com o rótulo do Big Numbers ("Renda per capita media"); o
  teste de bytes-único foi ajustado para checar a presença do marcador "Mapa indisponivel para esta
  camada." (renda/score sem mapa) em vez de ausência da string.

## Pendências
- **OPS (pós-merge, fora deste ciclo de código):** rebuild da imagem Docker (fpdf2 agora é dep base) +
  redeploy por digest na VPS; copiar `data/ultra/relatorio_capa_bg.png` e `relatorio_conteudo_bg.png`
  ao volume `/opt/motor-expansao/data/ultra/` (gitignored → não vão na imagem). Sem eles, o fallback
  sólido garante PDF válido. Guardrail §6: cada comando SSH precisa de confirmação de Felipe.

## Riscos remanescentes
- Sem os assets no volume de produção, o PDF sai com fundos de cor sólida (turquesa/branco) — válido,
  porém sem o branding de imagem. Mitigado pelo fallback testado.
- QA deve rodar a suíte FULL (`pytest -n auto`) — gate único; aqui rodei só o subconjunto impactado.

## Guardrails verificados
- score_priorizacao não alterado: sim (lookup é leitura pura; nenhum recálculo/escrita de M1).
- Artefatos M1 preservados: sim (nenhum parquet/score/pesos tocado).
- Dashboard offline mantido: sim (fpdf2 é offline; writer não faz fetch de rede; tiles seguem só no
  BLK-CENSO-01/DEC-004).
- Anti-PII: sim (.pptx/PDF não versionados; image24.png nunca extraído; teste anti-PII verde).
- `censo_map.py` / `setor_censitario_intersecao_area_1p5km` / raio 1.5 km: INTOCADOS.
```
