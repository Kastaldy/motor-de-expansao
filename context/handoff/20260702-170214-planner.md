# Handoff — Planner

## Skill que gerou este handoff
Planner (REVISÃO da retomada BLK-TP-06-FU1 com os artefatos reais do BLK-TP-08-FU)

## Próxima Skill recomendada
REVISÃO HUMANA (gate) — esteira Alta com gate humano de MODELAGEM antes do Builder.

## Entendimento da tarefa
Módulo novo, isolado e READ-ONLY que reproduz o baseline do TP-06 (+0,3119 out-of-fold) e valida OUT-OF-FOLD (mesmo harness/seed de `calibracao_residual.py`) residuais CANDIDATOS de recalibração contra `log1p(membros)`, comparando por IC95 da DIFERENÇA PAREADA e por recorte metropolitano — para decidir SE e QUAL candidato alimenta o BLK-TP-09. **A retomada substitui as 2 aproximações do plano pausado por matemática exata**, agora que o BLK-TP-08-FU entregou (a) `oferta_academias_menores_rede_h3.parquet` (LONGO, `hex_id×rede_menor`) → **dedup FINO por par (hex,rede)** e (b) `capacidade_media_por_rede.parquet` (mediana/média por rede, `flag_confiavel` N≥10) → **capacidade POR REDE**.

## Achados REAIS dos 2 artefatos novos (verificados pelo Planner via pandas, só agregados/schema, ZERO PII)

### `data/staging/oferta_academias_menores_rede_h3.parquet` (Candidato A)
- Schema: `hex_id (string), rede_menor (string), n_academias_menores (int64), alunos_academias_menores (int64), versao_contrato (string="oferta_menores_rede_v1")`. LONGO por `(hex_id, rede_menor)`.
- 7.250 linhas; 6.785 hex únicos; 14 categorias de `rede_menor`; Σ alunos = 1.920.955.
- Distribuição: `independente` domina (6.758 linhas, 1.680.005 alunos ≈ 87,5% dos alunos); demais 13 redes são conhecidas.
- **No JOIN (16.411 hexes com demanda×mercado):** 6.726 linhas menores-rede; **1.906.550 alunos** de oferta menor caem em hexes do join. Desses: `independente`=1.668.066; rede conhecida=238.484.
- **Dedup FINO medido no join** (par `(hex_id, rede_menor)` casando `concorrentes_mapeados[(hex_id_res7, rede)]`, excluindo `independente`): **duplicados = 157.840 alunos (8,3%)**; **adição legítima (não-duplicada) = 1.748.710 alunos (91,7%)**. Confirma o achado do TP-08-FU (62,7% grosseiro → 8,3% fino).

### `data/staging/capacidade_media_por_rede.parquet` (Candidato C)
- Schema: `rede_menor (string), media_alunos (float64), mediana_alunos (float64), n_filiais (int64), flag_confiavel (bool), versao_contrato (string="capacidade_media_rede_v1")`. 13 redes; `independente` EXCLUÍDA por construção.
- **`flag_confiavel=True` (N≥10) = 10 redes**, com MEDIANAS baixas (não são as grandes low-cost): panobianco 509 (n=149), velocity 301 (n=114), bio_ritmo 723 (n=33), evoque 555,5 (n=48), vidya_studio 379,5 (n=46), tonus_gym 442 (n=10), xprime 178 (n=21), 26fit 53 (n=67), pratique 15 (n=13), alpha_fitness 13 (n=10). **Mediana global das 10 confiáveis ≈ 340,25 alunos.**
- **CORREÇÃO CRÍTICA vs plano pausado:** o plano antigo assumia `{skyfit:2295, engenharia:3283}` de `base_calibracao_multirede`. O artefato REAL do TP-08-FU dá capacidades MUITO menores (13–723, mediana ~340) e para OUTRO conjunto de redes (as academias de bairro do dump WellHub/TotalPass), NÃO Smart/Sky/Engenharia. `smart_fit`, `bluefit`, `selfit`, `bodytech`, `cia_athletica`, `world_gym`, `red_fitness` (as maiores/mais numerosas em `concorrentes_mapeados`) NÃO têm capacidade confiável neste artefato → caem no fallback.

### Estrutura REAL da oferta consumida (relação de fórmula verificada por recompute, max abs diff = 0.0)
- `oferta_consumida_mercado_estimada = oferta_efetiva_mapeada_2km · 2500`, onde `oferta_efetiva_mapeada_2km = Σ_concorrente_no_raio2km max(0, 1 − dist_m/2000)` (soma distância-decaída sobre TODOS os concorrentes em 2 km, `enriquecimento_espacial_hexagonos.calc_comp_metrics`). **NÃO é contagem de rede no mesmo hex** → o plano pausado (`n_unid_rede_por_hex` por hex) estava GEOMETRICAMENTE errado.
- `oferta_consumida_total_estimada = oferta_consumida_mercado_estimada + oferta_consumida_ultra_estimada` (verificado).
- `oferta_efetiva_disponivel = max(sam_fitness_potencial − oferta_consumida_total_estimada, 0)` (verificado).
- `score_oportunidade_residual = clip(100·oferta_efetiva_disponivel / 2500, 0, 100)` (verificado).
- Dois `2500` DISTINTOS: `CAPACIDADE_DEFAULT_CONCORRENTE_ALUNOS` (peso da oferta consumida) e `SCORE_RESIDUAL_CAPACIDADE_REFERENCIA` (denominador do clip). São o MESMO número hoje mas semanticamente separados — o Candidato C só mexe no PRIMEIRO (capacidade de consumo por rede); o denominador do clip fica em 2500.
- `hexagonos_mercado_mapeado` tem `lat`/`lng` por hex → dá para reconstruir a query BallTree 2 km exata só no subset do join (16.411 hexes × 3.296 concorrentes = barato). `concorrentes_mapeados` tem `lat`,`lng`,`rede`,`status_registro`,`hex_id_res7`; `rede` usa o MESMO vocabulário de `rede_menor`.

## Plano técnico (revisado)

### 1. Módulo novo `src/motor_expansao/demanda_revelada/revalidacao_residual_candidatos.py`
Funções PURAS, pacote DISJUNTO. Importa só `numpy`/`pandas`/`scipy`/`sklearn` (`BallTree`) e os irmãos `demanda_revelada.calibracao_residual`, `demanda_revelada.contrato`, `dimensionamento.aderencia`, `dimensionamento.backtest_dim`. **NUNCA** importa `pipelines.m1`, `dashboard`, `censo_*`, `api`, `config.py` raiz, NEM `pipelines.calcular_colunas_mercado`/`pipelines.enriquecimento_espacial_hexagonos` (replica localmente a matemática 2 km-decay para não arrastar dependências de pipeline; constantes `EARTH_RADIUS_M=6_371_000.0`, `RADIUS_2KM=2000.0`, `CAP_DEFAULT=2500.0`, `REF_CLIP=2500.0` como constantes locais).

1.1. **Reuso do harness (não duplicar k-fold):** de `calibracao_residual` importar `_selecionar_alpha_e_oof`, `_metodo_validacao`, `_ic_bootstrap_r2`, `_ic_bootstrap_rho`, `_rho_oof`, `SEED`, `N_BOOTSTRAP`, `LIMIAR_R2_GO`, `LIMIAR_RHO_SUPORTE`; de `dimensionamento.backtest_dim` importar `_r2`, `_rmse`. Cada candidato entra como uma COLUNA de score (0–100). **Preparação:** `preparar_dados` de `calibracao_residual` hoje lê fixo `score_oportunidade_residual`. Para NÃO tocar a API do harness (suíte do TP-06 intocada), o novo módulo tem função local `_preparar_candidato(df, coluna) -> (X, y)`: `y=log1p(clip(membros,0,None))`, `X=coluna.reshape(-1,1)`, limpeza NaN/inf idêntica à do harness. (Recomendo função local em vez de estender `preparar_dados`.)

1.2. **`construir_residuais_candidatos(df_join, of_menores_rede, pares_concorrentes, oferta_mercado_C_por_hex, *, cap_ref=2500.0) -> pd.DataFrame`** — devolve `df_join` + colunas EM MEMÓRIA: `residual_baseline`, `residual_cand_A`, `residual_cand_C`, `residual_cand_AC` (se D=sim), `flag_metropolitano` (uf∈{SP,MG,RJ}). Nunca escreve parquet. `residual_baseline` = `score_oportunidade_residual` do parquet (reproduz o TP-06).

1.3. **Candidato A (oferta ENRIQUECIDA com dedup FINO por rede) — matemática exata:**
   - Para cada hex do join, `alunos_menores_add[hex] = Σ_rede alunos_academias_menores[hex,rede]` sobre pares `(hex, rede_menor)` que **NÃO** casam `pares_concorrentes = {(hex_id_res7, rede)}` de `concorrentes_mapeados` (dedup FINO). `independente` **nunca** casa uma rede específica → sempre SOMA integral (é oferta que o Motor ignora hoje). Só o par `(hex, rede_conhecida)` que JÁ existe em `concorrentes_mapeados` é considerado duplicado → NÃO soma.
   - `oferta_consumida_ajustada[hex] = oferta_consumida_total_estimada[hex] + alunos_menores_add[hex]`.
   - `oferta_efetiva_ajustada[hex] = max(sam_fitness_potencial[hex] − oferta_consumida_ajustada[hex], 0)`.
   - `residual_cand_A[hex] = clip(100 · oferta_efetiva_ajustada[hex] / cap_ref, 0, 100)` (cap_ref=2500, denominador do clip INTOCADO).
   - Colunas extra no join (todas presentes): `sam_fitness_potencial`, `oferta_consumida_total_estimada`. Efeito: residual CAI onde há academias menores não-mapeadas (91,7% da oferta menor no join). **Medido:** 1.748.710 alunos entram na saturação; 157.840 são dedupados.

1.4. **Candidato C (capacidade POR REDE) — matemática exata via BallTree local (2 km-decay):**
   - Reconstrói `oferta_consumida_mercado` do subset do join replicando `calc_comp_metrics` MAS ponderando cada concorrente `i` pela capacidade da sua rede: `oferta_consumida_mercado_C[hex] = Σ_{i: dist<2km} max(0, 1 − dist_i/2000) · cap(rede_i)`, com `cap(rede) = capacidade_media_por_rede.loc[rede & flag_confiavel, "mediana_alunos"]` (gate D: valor recomendado = MEDIANA), fallback declarado para redes sem capacidade confiável (ver Decisão C). BallTree(haversine) sobre `concorrentes_mapeados[status_registro=="valido"][["lat","lng","rede"]]`, query nos `lat/lng` dos 16.411 hexes do join.
   - **Comparabilidade:** recomputar TAMBÉM o baseline de mercado pela MESMA BallTree local (`oferta_consumida_mercado_base[hex] = Σ w2 · 2500`) e usar `residual_baseline` do parquet só como sanity-check (deve bater; se divergir >tolerância, é defeito de reprodução do decay → parar e diagnosticar). O delta A−base e C−base fica puramente na mudança de capacidade, não em ruído de BallTree.
   - `oferta_consumida_total_C[hex] = oferta_consumida_mercado_C[hex] + oferta_consumida_ultra_estimada[hex]` (Ultra INTOCADA).
   - `oferta_efetiva_C[hex] = max(sam_fitness_potencial[hex] − oferta_consumida_total_C[hex], 0)`; `residual_cand_C[hex] = clip(100·oferta_efetiva_C/cap_ref, 0, 100)`.
   - **Caveat central (honestidade):** as 10 redes confiáveis têm mediana ~340 (≪ 2.500) → o C REDUZ a oferta consumida na maioria dos hexes (residual SOBE) onde essas redes de bairro aparecem; mas as redes mais numerosas em `concorrentes_mapeados` (smart_fit, selfit, bodytech, cia_athletica, world_gym, bluefit, red_fitness sem N≥10 aqui) caem no fallback → efeito enviesado. Reportar quantos hexes do join efetivamente mudam e o sinal médio da mudança.

1.5. **Candidato A+C combinado (D=sim, RECOMENDADO):** oferta consumida = `oferta_consumida_mercado_C + oferta_consumida_ultra_estimada + alunos_menores_add` (menores por dedup fino E capacidade por rede das mapeadas). `residual_cand_AC = clip(100·max(sam − total_AC,0)/cap_ref,0,100)`. É o cenário mais completo; barato (mesma BallTree + mesma máscara de dedup). Ver Decisão D.

1.6. **NOTA capacidade GLOBAL (não é candidato próprio):** re-escalar só o denominador do clip (`100·oferta/C`) é rescale MONOTÔNICO de `oferta_efetiva_disponivel`; o Ridge de 1 feature re-absorve a escala no coeficiente → `R2_oof`/`rho_oof` ~idênticos ao baseline. Registrar no relatório, não rodar.

1.7. **`validar_candidato(df_join, coluna_score) -> ResultadoCandidato`** — dataclass leve: `nome, r2_oof_log, ic95_r2_oof, rho_oof, ic95_rho_oof, alpha, n, metodo, y_pred_oof` (guardado p/ bootstrap pareado). Roda `_preparar_candidato` + `_selecionar_alpha_e_oof` + IC95 bootstrap (seed=42). **R² in-sample NUNCA no veredito** (DEC-008); se computado, rótulo literal de auditoria.

1.8. **`comparar_pareado(y, pred_baseline_oof, pred_cand_oof, *, seed=42, n=N_BOOTSTRAP) -> (delta_medio, ic95)`** — bootstrap PAREADO: mesmos índices reamostrados aplicados aos DOIS vetores oof; `Δ = _r2(y_b, pred_cand_b) − _r2(y_b, pred_base_b)`. **Candidato "vence"** ⇔ IC95 de Δ não cruza zero (inferior > 0).

1.9. **`comparar_por_recorte(...)`** — repetir 1.7/1.8 em 3 recortes: completo (n=16.411), metropolitano SP/MG/RJ (n≈8.367 — top-3 UF medidas: SP 4.777, MG 2.082, RJ 1.508), não-metropolitano (n≈8.044). Ganho só é robusto se sobrevive FORA de SP/MG/RJ.

1.10. **Orquestrador `revalidar_candidatos(...) -> RevalidacaoResult`** monta os vetores (baseline, A, C, [A+C]), valida cada um, roda comparação pareada cand−baseline nos 3 recortes, decide veredito honesto por candidato. `__main__` (`# pragma: no cover`) lê os parquets reais, constrói `pares_concorrentes` + a BallTree e escreve o relatório; não exercitado em teste.

1.11. **Anti-PII:** reusar a checagem word-boundary de `COLUNAS_PII_PROIBIDAS` antes de escrever o `.md`; nenhum `Nome_Academia`/coord individual em nenhum frame (os artefatos consumidos já são agregados sem PII).

### 2. Relatório `data/analysis/revalidacao_residual_candidatos.md` (gitignored, sem PII)
Seções: §1 Baseline reproduzido (n=16.411, R2_oof_log≈+0,3119, rho_oof≈+0,4615, top-3 UF); §2 Artefatos do TP-08-FU (dedup fino 8,3%; capacidade por rede — 10 confiáveis, mediana ~340); §3 Candidato A (fórmula do enriquecimento + dedup FINO por (hex,rede) + números medidos: +1.748.710 somados / 157.840 dedupados); §4 Candidato C (capacidade por rede via 2 km-decay + fallback + quantos hexes mudam + sinal médio); §5 Candidato A+C (se D=sim); §6 NOTA capacidade global (rescale monotônico); §7 Tabela comparativa (R2_oof/IC95/rho_oof por candidato); §8 Δ pareado nos 3 recortes; §9 VEREDITO honesto por candidato + recomendação ao TP-09; §10 confounds (7 do TP-06 + dedup fino + capacidade-parcial das 10 redes).

### 3. Testes `tests/unit/demanda_revelada/test_revalidacao_residual_candidatos.py` (fixtures SINTÉTICAS)
3.1 Candidato A: par `(hex, rede)` casando concorrentes NÃO soma; par sem match soma; `independente` sempre soma; residual cai onde soma; `oferta_efetiva_ajustada` nunca negativa; residual em [0,100]. 3.2 Candidato C: 2 km-decay local reproduz o baseline (Σ w2·2500) dentro de tolerância; rede confiável usa mediana; rede sem capacidade usa o fallback escolhido (Decisão C); Ultra intocada. 3.3 A+C combina as duas correções (se D=sim). 3.4 R² in-sample banido do veredito. 3.5 IC determinístico (seed=42 reprodutível). 3.6 Bootstrap pareado usa MESMOS índices nos 2 vetores (cand=base ⇒ Δ≈0, IC cobre 0). 3.7 Isolamento de import (AST-scan: sem m1/dashboard/censo/api/config/pipelines.calcular/pipelines.enriquecimento). 3.8 Anti-PII no relatório/frames. 3.9 NO-GO é resultado válido sem exceção.

### 4. Fechamento (pós-merge, ad-hoc)
Atualizar "Depende de" do BLK-TP-09 em `tasks/backlog.md` para citar este FU1 concluído — passo operacional, o Builder não edita backlog no core.

## Arquivos afetados
- `src/motor_expansao/demanda_revelada/revalidacao_residual_candidatos.py` (NOVO)
- `tests/unit/demanda_revelada/test_revalidacao_residual_candidatos.py` (NOVO)
- `data/analysis/revalidacao_residual_candidatos.md` (NOVO, gitignored, via `__main__`, sem PII)
- `context/handoff.md`, `context/handoff/20260702-170214-planner.md`, `tasks/current_task.md` (bookkeeping)
- (fechamento pós-merge) `tasks/backlog.md` — "Depende de" do BLK-TP-09

## Dependências
- Harness `calibracao_residual.py` (API estável; **não alterar assinaturas** — o candidato usa função local `_preparar_candidato`, não muta `preparar_dados`).
- Parquets READ-ONLY (só no `__main__`/fixture, nunca fonte real em teste): `demanda_revelada_h3`; `hexagonos_mercado_mapeado` (`hex_id, lat, lng, uf, sam_fitness_potencial, oferta_consumida_total_estimada, oferta_consumida_ultra_estimada, oferta_consumida_mercado_estimada, oferta_efetiva_disponivel, score_oportunidade_residual`); `oferta_academias_menores_rede_h3` (`hex_id, rede_menor, alunos_academias_menores`); `capacidade_media_por_rede` (`rede_menor, mediana_alunos, flag_confiavel`); `concorrentes_mapeados` (`lat, lng, rede, status_registro, hex_id_res7`).
- Sem dependência nova no `pyproject.toml` (`sklearn.neighbors.BallTree` já é dep de `dimensionamento`/pipelines).

## Riscos técnicos
- **Capacidade confiável só p/ 10 redes de BAIRRO (mediana ~340), NÃO as grandes low-cost:** o Candidato C move o residual sobretudo onde academias de bairro aparecem; smart_fit/selfit/bodytech (numerosas) caem no fallback → efeito estrutural enviesado. Reportar honestamente; risco de C ser inconclusivo ou de sinal contra-intuitivo.
- **Reprodução do 2 km-decay:** se a BallTree local não reproduzir `oferta_consumida_mercado_estimada` do parquet (tolerância), o delta de C fica contaminado por ruído de recompute → sanity-check obrigatório antes de comparar; parar e diagnosticar se divergir.
- **Ganho dependente do metropolitano:** qualquer vitória precisa sobreviver fora de SP/MG/RJ (n≈8.044).
- **Sobreajuste a ruído (DEC-008):** "vence" só com IC de Δ pareado sem cruzar zero; coords ~1 km atenuam o sinal no join res-7.
- **Divergência do baseline:** se não bater +0,3119 / n=16.411 é defeito de join — parar e diagnosticar antes de comparar.

## Critérios de aceite finais
- Baseline reproduz o TP-06 (seed=42): `R2_oof_log ≈ +0,3119`, `rho_oof ≈ +0,4615`, `n_join = 16.411`.
- Candidatos A, C [e A+C] validados pelo MESMO harness/seed, com R2_oof_log + rho_oof + IC95, e comparação vs baseline pelo IC95 da DIFERENÇA PAREADA (não cruza zero para "vence").
- Δ pareado reportado em 3 recortes; veredito registra se o ganho generaliza fora de SP/MG/RJ.
- Candidato A: dedup FINO por par `(hex, rede)` (não hex-overlap grosseiro); `independente` sempre soma; números medidos (+1.748.710 / −157.840) batem no relatório.
- Candidato C: capacidade por rede = MEDIANA das `flag_confiavel`, via 2 km-decay reproduzido, com sanity-check do baseline e fallback documentado; Ultra intocada.
- ZERO escrita em artefato M1/mercado: `mtime` de `hexagonos_mercado_mapeado.parquet` e dos 4 oficiais M1 inalterado; `calcular_colunas_mercado.py`/`enriquecimento_espacial_hexagonos.py` intocados; nenhum parquet de mercado novo.
- Anti-PII: nenhuma coluna de `COLUNAS_PII_PROIBIDAS` em frame/relatório; testes só fixture sintética; `NAO_ABRA/` não tocado.
- Veredito honesto (DEC-008): "não supera baseline" é VÁLIDO; R² in-sample nunca no veredito; caveat ~1% metropolitano reportado.

## Validações obrigatórias
- Comando: `python -m pytest -n auto -q tests/unit/demanda_revelada/ tests/integration/test_streamlit_app.py` — Critério: 0 falhas.
- Comando: `python -c "import streamlit_app"` — Critério: 0 erro de import.
- Comando (isolamento): `python -c "import ast,sys; src=open('src/motor_expansao/demanda_revelada/revalidacao_residual_candidatos.py').read(); mods=[n.module or '' for n in ast.walk(ast.parse(src)) if isinstance(n,ast.ImportFrom)]; bad=[m for m in mods if any(m.startswith(p) for p in ('motor_expansao.pipelines.m1','motor_expansao.dashboard','motor_expansao.censo','motor_expansao.api','motor_expansao.pipelines.calcular_colunas_mercado','motor_expansao.pipelines.enriquecimento_espacial_hexagonos'))]; sys.exit(1 if bad else 0)"` — Critério: exit 0.
- Comando (mtime): comparar `stat -c %Y` de `hexagonos_mercado_mapeado.parquet` + 4 oficiais M1 (`brasil_estrutural`, `brasil_priorizados`, `hexagonos_brasil_oportunidades`, `hexagonos_brasil_dashboard`) antes/depois do Builder — Critério: mtime inalterado.
- Comando: `ruff check src/motor_expansao/demanda_revelada/revalidacao_residual_candidatos.py tests/unit/demanda_revelada/test_revalidacao_residual_candidatos.py` — Critério: 0 erro.

## Fora de escopo
- APLICAR qualquer candidato: editar `calcular_colunas_mercado.py`/`enriquecimento_espacial_hexagonos.py`, alterar a fórmula de `score_oportunidade_residual`/`oferta_efetiva_disponivel`/capacidade em produção, regenerar `hexagonos_mercado_mapeado.parquet`/derivados — é o BLK-TP-09 (DEC + gate).
- Tocar QUALQUER artefato oficial do M1.
- Gerar parquet de mercado novo/oficial (a saída é relatório + módulo/teste).
- Usar `membros`/qualquer coluna da demanda como PREDITOR geográfico de magnitude (DEC-009): é ALVO OBSERVADO.
- Re-ingerir `03_Competidores.xlsx` / recompor `rede_menor` (já feito pelo BLK-TP-08-FU); tocar `NAO_ABRA/`/PII; expandir res-8.
- Injetar capacidades externas de Smart/Pano sem fonte no repo (o módulo aceita `capacidade_por_rede` como parâmetro; o `__main__` usa só o parquet real).
- Resolver o BLK-TP-09 ou outro bloco.

## Alerta de aprovação
ATENÇÃO: ESTA TAREFA É MODELAGEM READ-ONLY QUE ALIMENTA A DECISÃO DE UM SCORE DE PRODUÇÃO (BLK-TP-09). NÃO ALTERA `score_priorizacao`/`hex_score_estrutural`/PESOS/ARTEFATOS OFICIAIS DO M1 NEM A FÓRMULA DO RESIDUAL EM PRODUÇÃO — LOGO NÃO É CRÍTICA PELO §5. MAS ENVOLVE ESCOPO NÃO-TRIVIAL (DEDUP FINO POR REDE + CAPACIDADE POR REDE VIA 2 KM-DECAY + COMPARAÇÃO PAREADA OUT-OF-FOLD). **REVISÃO HUMANA DE MODELAGEM OBRIGATÓRIA ANTES DO BUILDER.** O USUÁRIO DEVE LER ESTE HANDOFF, RESOLVER AS DECISÕES ABAIXO E CONFIRMAR.

## DECISÕES PARA O GATE HUMANO

### (A) Regra exata do dedup FINO do Candidato A — CONFIRMAR
- **Recomendação:** dedup por par `(hex_id, rede_menor)` contra `concorrentes_mapeados[(hex_id_res7, rede)]`. Um par menor `(hex, rede_conhecida)` que JÁ existe em `concorrentes_mapeados` NÃO soma (assume-se a mesma rede já contada na oferta consumida do Motor); todo o resto — inclusive TODA a categoria `independente` — SOMA integral à oferta consumida (é oferta real que o Motor ignora). Números medidos no join: soma +1.748.710 alunos (91,7%), deduplica −157.840 (8,3%). Esta é a matemática exata que substitui a aproximação hex-overlap do plano pausado.
- **Alternativa (mais conservadora):** também dedupar `independente` que caia em hex com QUALQUER concorrente mapeado (volta ao grosseiro parcial). NÃO recomendo — o achado do TP-08-FU é justamente que independentes são adição legítima (rede distinta), não recontagem.

### (C) Fallback de capacidade para redes SEM média confiável + academias independentes — DECIDIR
- **Contexto real (corrige o plano pausado):** só 10 redes têm `flag_confiavel=True`, com mediana ~340 alunos (academias de bairro), NÃO as grandes low-cost. `smart_fit`/`selfit`/`bodytech`/`cia_athletica`/`world_gym`/`bluefit`/`red_fitness` NÃO têm capacidade confiável no artefato → precisam de fallback. (As academias `independente` do Candidato A NÃO usam capacidade — entram por soma de alunos observados, não por proxy; a Decisão C só afeta o Candidato C.)
- **Recomendação:** fallback = **2.500 (o default atual de produção)** para toda rede sem `flag_confiavel=True`. Mantém neutralidade vs produção nessas redes → o C só re-pondera as 10 redes de bairro medidas. Reportar como caveat que o C mexe pouco nas redes numerosas.
- **Alternativa:** fallback = **mediana global das 10 confiáveis (~340)** — trataria TODA rede como academia de bairro pequena, derrubando drasticamente a oferta consumida das grandes → residual explode artificialmente onde há Smart/Selfit. NÃO recomendo (viés forte, sem fonte para as grandes). Registrar a alternativa mas não usá-la por default.

### (D) Roda o candidato combinado A+C? — DECIDIR
- **Recomendação: SIM, rodar A+C.** É barato (mesma BallTree + mesma máscara de dedup) e é o cenário mais completo/realista (menores somadas por dedup fino E capacidade por rede das mapeadas). Dá ao gate a leitura de qual correção carrega o sinal (A isolado, C isolado, ou a combinação). Se o gate quiser enxugar, roda só A e C.

### (E) Critério de "vence" + recorte metropolitano — CONFIRMAR
- **Recomendação (confirmar):** candidato VENCE ⇔ IC95 do `Δ = R2_oof(cand) − R2_oof(baseline)` por bootstrap PAREADO (mesmos folds/seed=42) não cruza zero (inferior > 0). Reportar Δ em 3 recortes (completo n=16.411; SP/MG/RJ n≈8.367; fora n≈8.044); só recomenda ao TP-09 se o ganho SOBREVIVE fora de SP/MG/RJ; se só no metropolitano, veredito = "não generaliza, limitado ao ~1% metropolitano" e recomendação = NÃO aplicar (ou aplicar só com aceite explícito da limitação). Caveat de cobertura ~1,06% do universo sempre reportado (DEC-012).

## Guardrails ativos
- **§5 (READ-ONLY sobre o M1):** zero recálculo/alteração de `score_priorizacao`, `hex_score_estrutural`, pesos (renda=0,40/pop=0,60), carteira, plano, artefatos oficiais; NÃO alterar a fórmula de `score_oportunidade_residual`/capacidade em produção nem regenerar `hexagonos_mercado_mapeado.parquet`/derivados. Candidatos só EM MEMÓRIA / relatório.
- **DEC-008:** validação out-of-fold vs baseline da média; R² in-sample BANIDO do veredito; IC95 bootstrap seed-fixa; comparação de candidatos out-of-fold; NO-GO é resultado VÁLIDO.
- **DEC-009:** `membros` é ALVO OBSERVADO; PROIBIDO usar `membros`/qualquer coluna da demanda como preditor geográfico de magnitude ou ajuste do score.
- **DEC-012 (anti-PII / isolamento):** pacote `demanda_revelada/` DISJUNTO — nunca importa de `pipelines/m1/`, `censo_*`, `dashboard/`, `api`, `config.py` raiz (nem `pipelines.calcular_colunas_mercado`/`enriquecimento_espacial_hexagonos`); zero coluna de `COLUNAS_PII_PROIBIDAS`; fonte real (`NAO_ABRA/`) nunca tocada; testes só com fixture sintética.
- **DEC-013:** oferta das academias menores entra só na camada de mercado/residual (candidata), COM DEDUP FINO por rede; READ-ONLY sobre o M1 e censitário.
- **Não expandir escopo; um bloco por vez; não implementar (Planner).**
