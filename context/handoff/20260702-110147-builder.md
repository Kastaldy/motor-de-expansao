# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-TP-03 — Vazio competitivo do concorrente low-cost (Opção A: parquet + módulo + testes)

## O que foi feito

Implementação completa da Opção A (aprovada por Felipe Silva em 2026-07-02):

1. **Criado `src/motor_expansao/demanda_revelada/vazios_competitivos.py`** — módulo isolado que:
   - Define constantes `LIMIAR_MEMBROS_GT5KM=200`, `DIST_LC_MIN_M=5000.0`, `VERSAO_CONTRATO_VAZIOS="vazios_competitivos_v1"`, `CONTRATO_COLUNAS_VAZIOS` (13 colunas), e os 3 `Path` defaults.
   - `flag_vazio_competitivo(df, *, limiar_membros_gt5km=200) -> pd.Series[bool]` — função PURA, 3 condições com regra de NaN da Decisão 1 (`dist.isna() | (dist > 5000)`), coerção numérica defensiva.
   - `enriquecer_vazios(vazios, enriquecimento) -> pd.DataFrame` — join READ-ONLY por `hex_id` (left), derivando `hex_lat`/`hex_lng` via `h3.cell_to_latlng(hex_id)` (determinístico, evita colisão com `COLUNAS_PII_PROIBIDAS`). Guard por coluna ausente.
   - `gerar_vazios_competitivos(...)` — orquestrador completo (lê, aplica flag, filtra, enriquece, coage ao contrato, ordena por `hex_id`, verifica PII, grava parquet se `escrever=True`).
   - `_assert_sem_pii_vazios(df)` — falha se `set(df.columns) & COLUNAS_PII_PROIBIDAS` não-vazio.
   - `_coagir_ao_contrato(df)` — coerce dtypes e ordem conforme `CONTRATO_COLUNAS_VAZIOS`.
   - Bloco `if __name__ == "__main__": # pragma: no cover` que roda sobre parquets reais e imprime contagem.
   - NUNCA importa de `pipelines/m1/`, `censo_*`, `dashboard/`, `config.py` raiz.

2. **Atualizado `src/motor_expansao/demanda_revelada/__init__.py`** — appended exports de `flag_vazio_competitivo`, `gerar_vazios_competitivos`, `LIMIAR_MEMBROS_GT5KM`, `VERSAO_CONTRATO_VAZIOS`, `CONTRATO_COLUNAS_VAZIOS` ao `__all__` existente.

3. **Criado `tests/unit/test_vazios_competitivos.py`** — 17 testes com fixture SINTÉTICA em código (zero parquet/HTML real), cobrindo:
   - `TestFlagCasosLimite` (8 casos-limite: n_conc>0→False; dist==5000→False; dist==5000.01→True; dist NaN→True; membros==LIMIAR-1→False, ==LIMIAR→True; membros==0→False; limiar parametrizável)
   - `test_reproducibilidade_determinismo` (mesmo input → mesmo output)
   - `test_contrato_colunas_e_dtypes` (colunas exatas na ordem, dtypes, versao_contrato)
   - `test_zero_pii` + `test_assert_pii_levanta_se_coluna_proibida` (rede anti-PII)
   - `test_enriquecimento_join_readonly` (join correto; hex sem match → NA; score só copiado)
   - `test_enriquecimento_colunas_ausentes` + `test_enriquecimento_frame_vazio`
   - `test_isolamento_import` (regex sobre o fonte; verifica ausência de imports proibidos)
   - `test_gerar_vazios_escrever_false` + `test_gerar_vazios_escrever_true` (orquestrador integrado)

4. **Gerado `data/staging/vazios_competitivos_lc.parquet`** — **229 hexes** (sanity check CONFIRMADO com limiar=200, igual ao esperado pelo Planner). Gitignored. Versao_contrato="vazios_competitivos_v1".

## Arquivos alterados
- `src/motor_expansao/demanda_revelada/vazios_competitivos.py` — NOVO (módulo disjunto BLK-TP-03)
- `src/motor_expansao/demanda_revelada/__init__.py` — append de 5 exports ao `__all__`
- `tests/unit/test_vazios_competitivos.py` — NOVO (17 testes com fixture sintética)
- `data/staging/vazios_competitivos_lc.parquet` — NOVO (gitignored; 229 hexes; NÃO versionar)

## Validações executadas

### pytest (subconjunto impactado + integração):
```
python -m pytest -n auto -q tests/unit/test_vazios_competitivos.py tests/unit/test_backtest_tp05.py tests/integration/test_streamlit_app.py
242 passed, 26836 warnings in 25.91s
```
Executado com `-n auto` (pytest-xdist; sem WinError 6 nesta execução). 0 falhas, 0 erros de coleta.

### Smoke import:
```
python -c "import streamlit_app; print('import ok')"
import ok
```

### Ruff (lint):
```
python -m ruff check src/motor_expansao/demanda_revelada/vazios_competitivos.py src/motor_expansao/demanda_revelada/__init__.py tests/unit/test_vazios_competitivos.py
All checks passed!
```
(3 erros UP037/I001 corrigidos automaticamente com `--fix`)

### Mypy (escopo):
```
python -m mypy src/motor_expansao/demanda_revelada/vazios_competitivos.py
Success: no issues found in 1 source file
```

### Sanity check do dado real:
```
Contagem de hexes vazios competitivos: 229  ← CONFIRMADO (esperado: 229, limiar=200)
Versao contrato: vazios_competitivos_v1
Colunas: ['hex_id', 'membros', 'membros_gt5km_concorrente_lc', 'dist_concorrente_lc_min_m',
           'n_concorrente_lc', 'flag_vazio_competitivo', 'hex_lat', 'hex_lng', 'uf',
           'nome_municipio', 'score_priorizacao', 'oferta_efetiva_disponivel', 'versao_contrato']
```

### Artefatos oficiais M1 (mtime inalterado):
```
data/staging/brasil_estrutural.parquet:          mtime=Wed Jun  3 12:47:01 2026
data/staging/brasil_priorizados.parquet:         mtime=Wed Jun  3 12:47:11 2026
data/staging/hexagonos_brasil_oportunidades.parquet: mtime=Wed Jun  3 12:47:35 2026
```
TODOS com datas de Jun 3 — NÃO tocados pelo ciclo BLK-TP-03.

## Problemas encontrados
- 3 erros de ruff (UP037: aspas em anotação de tipo + I001: ordem de imports) corrigidos automaticamente com `--fix`. Sem problema funcional.

## Pendências
- Housekeeping de backlog/completed (tasks/backlog.md, tasks/completed.md) — passo do orquestrador (Passo 6.0), NÃO do Builder.
- Overlay do dashboard (Opção B) fica para bloco sucessor BLK-TP-03-FU1 conforme decisão humana.
- Regeneração dos parquets paralelos de staging (não é requisito de fechamento de testes deste bloco).

## Riscos remanescentes
- Cobertura ~1% do universo do Motor (16.575 hexes; DEC-012) — os 229 vazios refletem esse viés; NÃO é cobertura nacional, é refino sobre metrópoles.
- Ruído de coordenadas ~1 km na fonte → `n_concorrente_lc==0` no hex pode coexistir com LC a ~800 m fora do hex. Precisão é hex-a-hex (~1,2 km de lado), não de metro.
- NaN em `dist_concorrente_lc_min_m`: 0 no dado real atual; regra definida (Decisão 1: incluir como "muito longe"); caso-limite testado para robustez futura.

## Guardrails verificados
- score_priorizacao não alterado: **sim** — apenas LIDO via join de enriquecimento; mtime dos 3 artefatos oficiais M1 inalterado (Jun 3, 2026)
- Artefatos M1 preservados: **sim** — `brasil_estrutural.parquet`, `brasil_priorizados.parquet`, `hexagonos_brasil_oportunidades.parquet` com mtime de Jun 3
- Dashboard offline mantido: **sim** — Opção A não toca dashboard/; import streamlit_app ok
- Anti-PII (DEC-012): **sim** — `hex_lat`/`hex_lng` em vez de `lat`/`lng`; `set(columns) & COLUNAS_PII_PROIBIDAS == set()`; teste anti-PII obrigatório passa; fixture sintética; fonte real NAO_ABRA/ nunca tocada
- Isolamento do pacote: **sim** — `vazios_competitivos.py` não importa de `pipelines/m1/`, `censo_*`, `dashboard/`, `config.py`; verificado por teste de isolamento (regex sobre o fonte)
- Dependência nova no pyproject.toml: **nenhuma** — só pandas/numpy/pyarrow/h3 + stdlib (já na base)
