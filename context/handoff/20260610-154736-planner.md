# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
[REVISÃO HUMANA do plano técnico] → Builder
(Criticidade Alta + reverte 2 sub-decisões de uma DEC aprovada por Felipe → aprovação humana do PLANO obrigatória antes do Builder. Produto JÁ aprovado por Vinicius 2026-06-10 — NÃO reabrir produto.)

## Entendimento da tarefa
Afrouxar o gate do SAM em `calcular_colunas_mercado.py` removendo `flag_viavel` e `~flag_canibalizacao_ultra_1km`, passando o gate a ser apenas `faixa_oportunidade ∈ {baixa,media,alta,prioridade_maxima} & flag_pop_min_5k(≥5000)`; manter `flag_sam_fitness`/`sam_fitness_potencial` inalterados; atualizar testes do BLK-SAM-01, doc e registrar DEC-007 (reversão parcial da DEC-006). READ-ONLY sobre o M1.

## Plano técnico

### Parte A — código de produção (`src/motor_expansao/pipelines/calcular_colunas_mercado.py`)

1. **Gate do SAM (linha 298).** Trocar a expressão:
   - DE: `df["flag_sam"] = flag_viavel & faixa_elegivel & flag_pop_ok & ~flag_canibal`
   - PARA: `df["flag_sam"] = faixa_elegivel & flag_pop_ok`
   Não alterar a linha 299 (`flag_sam = df["flag_sam"].astype(bool)`).

2. **Comentário do gate (linhas 290-291).** Substituir o bloco de comentário que hoje diz
   `# Gate DEC-006: faixa M1 elegivel AND populacao_corte_hex >= 5000 (substitui top_municipio` /
   `# e o piso trivial tam_populacao_hex>0); mantem flag_viavel e ~canibalizacao.`
   por um comentário que referencie a **DEC-007** e descreva o novo gate, ex.:
   `# Gate DEC-007 (reverte 2 sub-decisoes da DEC-006): apenas faixa M1 elegivel AND`
   `# populacao_corte_hex >= 5000. flag_viavel e ~canibalizacao SAIRAM do gate (Vinicius 2026-06-10).`
   Também ajustar o comentário do bloco da régua (linhas 277-280) trocando a referência `(DEC-006)` por `(DEC-006/DEC-007)` ou `(DEC-007)` na frase "Regua de populacao do corte" — mudança textual mínima, não funcional.

3. **NÃO remover** as variáveis locais `flag_viavel` (linha 285) nem `flag_canibal` (linha 284): ambas continuam sendo usadas — `flag_canibal` em `sam_granularidade` (linha 313) e `tese_entrada` (linha 386); `flag_viavel` em `tese_entrada` (linha 389). Mantê-las como estão. (Caso `flag_viavel` deixe de ser referenciada após qualquer ajuste, ela ainda é usada na linha 389 — confirmado em uso, NÃO remover.)

4. **`flag_sam_fitness` (linha 304) e `sam_fitness_potencial` (linhas 305-309):** INALTERADOS. `flag_sam_fitness = flag_sam` segue válido (propaga automaticamente o novo gate). Atualizar apenas o comentário da linha 303 se ele citar premissa antiga — mudança textual opcional, sem efeito funcional.

5. **`sam_granularidade` (np.select, linhas 312-316): NÃO alterar a lógica.** Confirmar a semântica resultante: a precedência `[flag_hibrid_elig, flag_sam, flag_canibal]` faz com que um hex `flag_sam=True & flag_canibal=True` (sem ser híbrido) receba o label `"municipio_priorizado"` ANTES de cair em `"bloqueado_rede_ultra"`. Consequência aceita (documentada na DEC-007): hex canibal que entra no SAM deixa de ser rotulado `"bloqueado_rede_ultra"` e passa a `"municipio_priorizado"`. Isso é coerente: se o SAM aceita o hex, ele não é "bloqueado". O label `"bloqueado_rede_ultra"` continua existindo apenas para hexes canibais que NÃO entram no SAM (faixa inelegível ou pop<5000). NÃO há mudança de código aqui.

6. **`tese_entrada` (np.select, linhas 384-393): NÃO alterar a lógica.** `flag_canibal` continua sendo o 1º critério → hex canibal ainda recebe `"proteger_rede_atual"` independentemente do `flag_sam`. `flag_viavel` segue usado no 4º critério (`monitorar`). Sem mudança de código.

7. **ATENÇÃO — guardrail interno em `validar()` (linhas 433-436) QUEBRA com o novo gate.**
   O assert `n_violacao = (df["flag_sam"] & df["flag_canibalizacao_ultra_1km"]).sum(); assert n_violacao == 0` torna-se FALSO por design (SAM agora coexiste com canibalização: 76 hexes pela medição real). Este assert está NA função `validar()` do arquivo de produção, executada por `main()` na regeneração operacional — **ele DEVE ser removido/adaptado**, senão a regeneração pós-merge quebra.
   - **Ação requerida (in-scope, mesmo arquivo já autorizado):** remover as linhas 433-436 (o bloco `n_violacao` + assert + `print("Regra canibalizacao OK")`). Não substituir por outro assert de canibalização (a regra deixou de existir). As demais validações de `validar()` permanecem intactas.
   - Nota de escopo: o handoff do BO citou "apenas linha 298 + comentário" para este arquivo, mas a remoção do assert obsoleto em `validar()` é consequência direta e necessária da mesma decisão de produto (sem ela a regeneração canônica do passo 5 da Parte D falha). É correção acoplada, não expansão de escopo. Builder deve aplicá-la e registrar no handoff.

### Parte B — testes (`tests/integration/test_modelo_mercado_hexagonos.py`)

8. **`test_calcular_bloqueia_sam_quando_ha_canibalizacao` (linha 171) — RENOMEAR e REESCREVER.**
   Com o novo gate, o hex `h1` (`faixa=prioridade_maxima`, `flag_canibalizacao_ultra_1km=True`, sem `qualidade_join_uf` → corte cai em `pop_total=10000` → `flag_pop_min_5k=True`) passa a ter `flag_sam=True`. Os asserts atuais de `h1` (`flag_sam False`, `sam_indice_operavel 0`, `flag_sam_fitness False`, `sam_fitness_potencial 0`, `oferta_efetiva_disponivel 0`, `prioridade nula`) ficam FALSOS.
   - Renomear para algo como `test_canibalizacao_nao_bloqueia_mais_o_sam` (reflete a DEC-007).
   - Reescrever os asserts de `h1`: `flag_sam=True`; `sam_indice_operavel == 80.0` (`tam_indice_demanda` = `score_expansao_hibrido`=99.5? — h1 tem `flag_hex_hibrido_elegivel=True` e `score_expansao_hibrido=99.5`, então `tam_indice_demanda=99.5` e `sam_indice_operavel≈99.5`; **o Builder deve recomputar o valor exato a partir de `mask_hibrido`** e não chutar 80.0); `flag_sam_fitness=True`; `sam_fitness_potencial > 0`.
   - **MANTER os asserts de label que NÃO mudam:** `tese_entrada == "proteger_rede_atual"` (flag_canibal é o 1º critério, ainda vence) — manter. `sam_granularidade` de h1 = `"hex_censo"` (flag_hibrid_elig=True tem precedência) — adicionar assert para travar a precedência.
   - `h2` (`flag_canibalizacao_ultra_1km=False`, `faixa=alta`, corte=`pop_total`=10000) segue `flag_sam=True`; manter os asserts de `h2` como estão (`municipio_priorizado`, `sam_indice_operavel≈70.0`, `abrir_com_disputa`, etc.).

9. **`test_flag_sam_e_conjuncao_dos_quatro_criterios` (linha 426) — RENOMEAR para `test_flag_sam_e_conjuncao_dos_dois_criterios` e reduzir o parametrize.**
   Lista de casos nova (remover os 2 casos que deixaram de bloquear):
   ```python
   [
       ({}, True),                                                  # tudo ok
       ({"faixa_oportunidade": "descartado"}, False),               # faixa nao elegivel
       ({"pop_total": 4_999.0, "populacao_proxy": 4_999.0}, False), # pop_corte < 5000
       ({"flag_canibalizacao_ultra_1km": True}, True),              # canibal NAO bloqueia mais (DEC-007)
       ({"flag_viavel": False}, True),                              # nao-viavel M1 NAO bloqueia mais (DEC-007)
   ]
   ```
   Atualizar a docstring para citar a DEC-007 e o gate de 2 critérios (`faixa elegível & flag_pop_min_5k`). Os 2 últimos casos servem como os REPROS exigidos (ver passos 10-11) — mas, para clareza/rastreabilidade, criar também os 2 testes nomeados dedicados abaixo.

10. **NOVO teste `test_dropar_flag_viavel_admite_hex_no_sam` (repro flag_viavel).** Usar `_gate_fixture_row(...)` com `flag_viavel=False`, `faixa_oportunidade="alta"`, `flag_canibalizacao_ultra_1km=False`, `pop_total=8000`/`populacao_proxy=8000` (corte municipal → `flag_pop_min_5k=True`), `renda_per_capita` baixa (documentar a causa: renda < RENDA_MIN reprovaria `flag_viavel`). Asserts: `bool(result["flag_sam"]) is True`; `result["sam_fitness_potencial"] > 0`. Docstring: "antes do BLK-SAM-02 (gate DEC-006) este hex reprovava SÓ por flag_viavel=False; com a DEC-007 entra no SAM."

11. **NOVO teste `test_dropar_canibal_admite_hex_no_sam` (repro ~canibal).** `_gate_fixture_row(...)` com `flag_canibalizacao_ultra_1km=True`, `flag_viavel=True`, `faixa_oportunidade="alta"`, `pop_total=8000`/`populacao_proxy=8000`. Asserts: `bool(result["flag_sam"]) is True`; `result["sam_fitness_potencial"] > 0`; e travar a coerência dos labels: `result["tese_entrada"] == "proteger_rede_atual"` (canibal ainda protege) e `result["sam_granularidade"] == "municipio_priorizado"` (flag_sam vence flag_canibal na precedência, NÃO `"bloqueado_rede_ultra"`). Docstring: "antes reprovava SÓ por ~canibal; DEC-007 inclui áreas Ultra<1km no SAM; labels de proteção/granularidade coerentes."

12. **`test_parquet_final_respeita_guardrails_do_piloto` (linha 456) — REMOVER o assert de canibalização.**
    Remover as linhas 457-459 (`assert int((flag_sam & flag_canibalizacao).sum()) == 0`). O SAM agora pode coexistir com canibalização — o invariante deixou de valer. Manter TODOS os demais asserts do teste (fonte_oferta, n_redes≥3, demanda_granularidade, sam_granularidade ⊆ conjunto, faixas de tam/som). O conjunto permitido de `sam_granularidade` já inclui `bloqueado_rede_ultra` e `municipio_priorizado` — sem mudança nesse assert.

13. **`test_sam_zera_quando_pop_corte_granular_abaixo_de_5000` (linha 378):** NÃO muda — continua válido (corte<5000 ⇒ flag_sam=False independe dos critérios removidos). Confirmar mentalmente; nenhuma edição.

14. **`test_sam_calcula_para_faixa_elegivel_pop_corte_acima_5000_fora_top_municipio` (linha 404):** NÃO muda — `flag_viavel=True`, `flag_canibal=False`, faixa+pop ok ⇒ flag_sam=True nos dois gates. Nenhuma edição.

15. **Demais testes do arquivo** (`test_calcular_tam_sam_residual_absoluto_exemplo_manual`, `test_bloco8_*`, `test_anexar_*`, `test_parquet_final_tem_schema_minimo`, `test_piloto_preserva_score_*`, `test_artefatos_oficiais_m1_*`): NÃO mudam (não dependem dos 2 critérios removidos). `MERCADO_REQUIRED_COLS`/schema: INALTERADOS.

### Parte C — documentação

16. **`docs/modelo_mercado_hexagonos.md`.**
    - Linha 251: trocar a regra de `flag_sam` para
      `faixa_oportunidade in {baixa,media,alta,prioridade_maxima} and flag_pop_min_5k`
      (remover `flag_viavel and` e `and not flag_canibalizacao_ultra_1km`).
    - Linha 252 (`flag_sam_fitness == flag_sam`): manter.
    - Linha 256 (`sam_granularidade`): manter a regra textual, mas adicionar nota de que, com o gate DEC-007, um hex `flag_sam=True & flag_canibal=True` recebe `municipio_priorizado` (precedência), não `bloqueado_rede_ultra`.
    - Linhas 260-264 (Leitura): atualizar a referência "(DEC-006)" para "(DEC-006/DEC-007)" e reescrever a frase explicando que o gate agora desconsidera `flag_viavel` (renda) e canibalização; manter a explicação de que o corte ≥5000 sobre `populacao_corte_hex` é o filtro remanescente.
    - Linha 325 (`tese_entrada`): a regra textual NÃO muda (lógica intacta); opcionalmente anotar que `proteger_rede_atual` tem precedência sobre `flag_sam`.

17. **`CLAUDE.md §8` — registrar DEC-007** (texto pronto na seção "DEC-007 (texto pronto)" abaixo). Inserir logo após o bloco da DEC-006 (após a linha 231). NÃO editar a DEC-006 em si (ela permanece como registro histórico; a DEC-007 a declara SUPERSEDED nas 2 sub-decisões).

### Parte D — housekeeping do ciclo (sem código)

18. `tasks/current_task.md`: atualizar "Skill atual"/"Próxima Skill" conforme o avanço da esteira (Planner → [REVISÃO HUMANA] → Builder).
19. `tasks/backlog.md`: marcar BLK-SAM-02 em execução/concluído conforme a fase (o Builder/QA finaliza o status).
20. `tasks/completed.md`: o Builder/QA registra o fechamento ao concluir.
21. **Regeneração de parquets paralelos = PASSO OPERACIONAL PÓS-MERGE (fora do fechamento; Builder NÃO regenera).** Ordem canônica: híbrido → mercado → `calcular_colunas_mercado` → carteira → plano → domínio → residual → `fase1_bi_exports` (+ enriquecido derivado do dashboard). Enquanto não regenerado, `hexagonos_mercado_mapeado.parquet` serve `flag_sam` antigo; `test_parquet_final_*` que leem o parquet real continuam coerentes com os valores presentes (skip/guardas de coluna já existem). O Builder DEVE deixar essa pendência explícita no handoff.

## DEC-007 (texto pronto para CLAUDE.md §8 — inserir após a DEC-006)

```
### DEC-007 — Afrouxar o gate do SAM: apenas Faixa M1 elegível AND população ≥ 5000 (reverte 2 sub-decisões da DEC-006; BLK-SAM-02)
- ID: DEC-007 | Data: 2026-06-10 | Criticidade: **Alta** (altera o VALOR de um campo ativo do dashboard na camada PARALELA de mercado/residual; reverte 2 sub-decisões de uma DEC aprovada; **não** altera o M1 oficial). READ-ONLY sobre o M1.
- Status: **APROVADA pelo usuário Vinicius em 2026-06-10**, com ciência do impacto (×17 no volume de hexes SAM). PLANO TÉCNICO aprovado pelo humano antes do Builder (esteira Alta).
- Decisão: o gate do SAM em `src/motor_expansao/pipelines/calcular_colunas_mercado.py` passa de
  `flag_sam = flag_viavel & faixa_oportunidade ∈ {baixa,media,alta,prioridade_maxima} & flag_pop_min_5k(≥5000) & ~flag_canibalizacao_ultra_1km`
  para
  `flag_sam = faixa_oportunidade ∈ {baixa,media,alta,prioridade_maxima} & flag_pop_min_5k(≥5000)`.
  **Reverte 2 sub-decisões da DEC-006** (sub-decisão 1): (a) REMOVE `flag_viavel` do gate — junto com ele sai o filtro de renda `renda_target_proxy ≥ RENDA_MIN` e o guard `pop ≥ 1` que vinham "de brinde"; (b) REMOVE `~flag_canibalizacao_ultra_1km` — o SAM passa a INCLUIR áreas já cobertas por unidade Ultra < 1 km. As sub-decisões 2 (campo de população = `populacao_corte_hex`/`flag_pop_min_5k`, régua `pop_corte.py`) e 3 (limiar inclusivo ≥ 5000) da DEC-006 PERMANECEM VIGENTES e intactas.
- `flag_sam_fitness = flag_sam` e `sam_fitness_potencial` (coercido, sem NaN, não-negativo): INALTERADOS — propagam o novo gate automaticamente.
- Labels: `sam_granularidade` e `tese_entrada` NÃO mudam de lógica. Consequência semântica aceita: um hex `flag_sam=True & flag_canibalizacao_ultra_1km=True` (não híbrido) recebe `sam_granularidade = "municipio_priorizado"` (a cláusula `flag_sam` vence `flag_canibal` na precedência do `np.select`), não `"bloqueado_rede_ultra"`; `tese_entrada` continua `"proteger_rede_atual"` para qualquer hex canibal (1º critério). `"bloqueado_rede_ultra"` segue existindo só para hexes canibais que NÃO entram no SAM.
- Guardrail interno obsoleto: o assert `(flag_sam & flag_canibalizacao_ultra_1km).sum() == 0` em `validar()` foi REMOVIDO (a regra deixou de existir).
- Evidência-chave (dados reais, 2026-06-10): `flag_sam` True **27.996 → 479.568** (+451.572, ≈ ×17). Breakdown: ~451.496 entram por dropar `flag_viavel` (filtro de renda); 76 por dropar `~canibalização`.
- Pesos/fórmula/artefatos do M1: **INALTERADOS** (`renda=0.40`/`pop=0.60`, `score_priorizacao`, `hex_score_estrutural`, carteira, plano, artefatos oficiais). Parâmetros canônicos do §3: INALTERADOS. `faixa_oportunidade` é LIDA (DEC-001). Régua `populacao_corte_hex`/`flag_pop_min_5k` e helper `pop_corte.py`: INTOCADOS (só consumidos).
- Relação com a DEC-006: a DEC-006 fica **SUPERSEDED nas 2 sub-decisões revertidas** (manutenção de `flag_viavel` e de `~flag_canibalizacao_ultra_1km` no gate); permanece vigente no restante (campo de corte, régua e limiar ≥5000).
- Regeneração dos parquets paralelos de staging = passo OPERACIONAL pós-merge separado (não requisito de fechamento de testes); ordem canônica `híbrido → mercado → calcular_colunas_mercado → carteira → plano → domínio → residual → fase1_bi_exports` (+ enriquecido derivado). ZERO escrita em artefatos M1 oficiais.
- Referências: `tasks/backlog.md` (BLK-SAM-02); `context/handoff.md` (Planner/Builder); `docs/modelo_mercado_hexagonos.md` §5.4; `src/motor_expansao/pipelines/calcular_colunas_mercado.py`; CLAUDE.md §4/§5; DEC-001; DEC-006.
```

## Arquivos afetados
- `src/motor_expansao/pipelines/calcular_colunas_mercado.py` — linha 298 (gate) + comentários (linhas 277-280, 290-291, opcional 303) + remoção do assert obsoleto em `validar()` (linhas 433-436)
- `tests/integration/test_modelo_mercado_hexagonos.py` — reescrever 2 testes, reduzir 1 parametrize, remover 1 assert, criar 2 testes novos
- `docs/modelo_mercado_hexagonos.md` — §5.4 (linhas 251-264) + nota em `sam_granularidade` (256)
- `CLAUDE.md` — §8: nova DEC-007 (após linha 231)
- `tasks/current_task.md`, `tasks/backlog.md`, `tasks/completed.md` — housekeeping
- `context/handoff.md` + `context/handoff/` — esteira

## Dependências
- Nenhuma dependência nova de pacote. Reusa `numpy`/`pandas` já presentes.
- `pop_corte.py` é dependência CONSUMIDA (helpers `derive_confianca_geografica`/`derive_pop_cut_columns`) — INTOCADO.

## Riscos técnicos
- **R1 (BLOQUEADOR se ignorado):** o assert `n_violacao` em `validar()` (linhas 433-436) quebra a regeneração operacional pós-merge. Mitigação: removê-lo (passo 7). Os testes da suíte não exercitam `validar()` diretamente sobre dados reais canibais, mas a regeneração canônica sim.
- **R2:** `test_calcular_bloqueia_sam_quando_ha_canibalizacao` e o parametrize dos "4 critérios" falham se não reescritos (passos 8-9). O valor exato de `sam_indice_operavel` de h1 depende de `mask_hibrido` (h1 é híbrido com `score_expansao_hibrido=99.5`) — o Builder deve recomputar, não chutar.
- **R3:** paradoxo de label `sam_granularidade` para hex canibal+SAM. Resolvido por precedência do `np.select` (flag_sam antes de flag_canibal) → `"municipio_priorizado"`. Coberto por teste novo (passo 11). Sem mudança de código.
- **R4:** parquets paralelos servem `flag_sam` antigo até a regeneração pós-merge (pendência operacional, não regressão; não toca M1). Documentar no handoff do Builder.
- **R5:** DEC-007 conflita com DEC-006 (Felipe). Vinicius é o aprovador com ciência do impacto; o Builder NÃO questiona produto — só implementa.

## Critérios de aceite finais
1. `df["flag_sam"] = faixa_elegivel & flag_pop_ok` (linha 298), sem `flag_viavel` e sem `~flag_canibal`; comentário cita DEC-007.
2. Assert `(flag_sam & flag_canibalizacao_ultra_1km).sum()==0` removido de `validar()` (produção) e de `test_parquet_final_respeita_guardrails_do_piloto` (teste).
3. Repro flag_viavel: teste `test_dropar_flag_viavel_admite_hex_no_sam` verde (hex reprovado só por `flag_viavel=False` agora `flag_sam=True`).
4. Repro ~canibal: teste `test_dropar_canibal_admite_hex_no_sam` verde (hex reprovado só por canibal agora `flag_sam=True`; labels coerentes).
5. `test_calcular_bloqueia_sam_quando_ha_canibalizacao` (renomeado) e `test_flag_sam_e_conjuncao_dos_dois_criterios` reescritos e verdes.
6. DEC-007 registrada em CLAUDE.md §8 (data 2026-06-10, aprovador Vinicius, status APROVADA, reversão explícita das 2 sub-decisões, impacto 27.996→479.568, DEC-006 SUPERSEDED nessas 2 sub-decisões).
7. `docs/modelo_mercado_hexagonos.md` §5.4 com a nova regra de `flag_sam`.
8. `git diff --name-only` NÃO toca `pipelines/m1/`, `scoring.py`, `config.py`, `dashboard/constants.py`, nem qualquer `.parquet` oficial M1.
9. `sam_granularidade` mantém `"bloqueado_rede_ultra"` como rótulo válido (para canibal fora do SAM); label não removido do código nem do conjunto permitido.
10. Volume ≈ 479.568 é validação de SANIDADE pós-regeneração no PR (não teste rígido; não há teste de contagem 27.996 a ajustar — confirmado: nenhum teste asserta a contagem absoluta).

## Validações obrigatórias
Host Windows + Python 3.14 → rodar SERIAL, `-p no:cacheprovider`, NÃO usar `-n auto`/xdist.
- Comando: `python -m pytest -p no:cacheprovider tests/integration/test_modelo_mercado_hexagonos.py`
  - Critério mínimo: 0 failed (skips de parquet real pré-regeneração são aceitáveis).
- Comando: `python -m pytest -p no:cacheprovider tests/integration/test_streamlit_app.py`
  - Critério mínimo: 0 failed, sem erro de import.
- Comando: `python -m pytest -p no:cacheprovider -q` (suíte completa, gate único)
  - Critério mínimo: 0 failed.
- Comando: `ruff check src/ tests/`
  - Critério mínimo: limpo (0 erros).
- Comando: `mypy src/motor_expansao/pipelines/calcular_colunas_mercado.py`
  - Critério mínimo: limpo nos arquivos tocados.
- Comando: `python -c "import streamlit_app"`
  - Critério mínimo: import OK, sem exceção.

## Fora de escopo
- `score_priorizacao`, `hex_score_estrutural`, pesos (`renda=0.40`/`pop=0.60`), `faixa_oportunidade`: READ-ONLY (DEC-001).
- Artefatos oficiais M1 (`brasil_estrutural`, `brasil_priorizados`, `hexagonos_brasil_oportunidades`, `hexagonos_brasil_dashboard`, `hexagonos_mapa_sample`): nenhum `.parquet` M1 é tocado.
- `pop_corte.py` (`derive_confianca_geografica`/`derive_pop_cut_columns`/`has_censo_signal`): INTOCADO.
- `flag_pop_min_5k`/`populacao_corte_hex`/`POP_MIN_SAM_GATE`/`POP_MIN_ACIONAVEL`: INTOCADOS (só consumidos).
- Colunas `flag_viavel` e `flag_canibalizacao_ultra_1km`: continuam DERIVADAS no pipeline e usadas como LABEL — NÃO são removidas como colunas; só saem do gate do `flag_sam`.
- Lógica de `sam_granularidade` e `tese_entrada`: intacta (só labels, não o gate).
- Regeneração de parquets paralelos: passo operacional pós-merge — Builder NÃO regenera.
- APIs, PostGIS, Prefect, pipelines pesados, qualquer outro bloco (BLK-API-*): fora de escopo.

## Alerta de aprovação
ATENÇÃO: esta tarefa é de criticidade ALTA. Ela (a) altera a semântica do gate do SAM na camada PARALELA de mercado/residual (§4) e (b) REVERTE 2 sub-decisões de uma DEC aprovada por Felipe (DEC-006). APROVAÇÃO HUMANA DO PLANO TÉCNICO É OBRIGATÓRIA ANTES DO BUILDER. A decisão de PRODUTO já foi resolvida pelo usuário Vinicius em 2026-06-10 com ciência do impacto (flag_sam 27.996 → 479.568, ×17) — NÃO reabrir produto. O M1 oficial permanece READ-ONLY: `score_priorizacao`, `hex_score_estrutural`, pesos e artefatos oficiais INALTERADOS.

## Guardrails ativos
- `score_priorizacao`/`hex_score_estrutural`/pesos/`faixa_oportunidade`/artefatos oficiais M1: READ-ONLY (DEC-001; §3; §5).
- `pop_corte.py` INTOCADO (fonte única da régua pop; §4).
- Visualizações e camadas paralelas NÃO recalculam nem alteram o M1 sem aprovação explícita (§5 guardrail permanente).
- Parâmetros canônicos imutáveis: H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, renda=0.40, pop=0.60.
- Não expandir escopo para BLK-API-02 nem qualquer outro bloco.
- Commitar apenas paths do ciclo; nunca `git add -A`.
- Decisão de produto JÁ RESOLVIDA pelo usuário Vinicius (2026-06-10): NÃO reabrir.
