# Handoff — Planner

## Skill que gerou este handoff
Planner (Opus — validação/finalização autoritativa do rascunho de tier inferior)

## Próxima Skill recomendada
REVISÃO HUMANA do plano (esteira Alta) → depois Builder (opus) → QA (opus 4.8)

## Bloco planejado
BLK-UI-03 — 4 ajustes finos de UX no dashboard (READ-ONLY M1), a partir do teste do BLK-UI-02. FECHA também o BLK-FIX-10 (item nº3).

## Entendimento da tarefa
Reverter o campo de coordenadas alterado no BLK-UI-02, dar um meio-termo de tamanho aos dois tooltips do mapa, reduzir a largura de exibição na TELA do preview do Relatório Pontual Censitário (fecha BLK-FIX-10) e destacar o seletor de abas SÓ via CSS — sem tocar score/artefatos M1, sem alterar a lógica de render lazy (Bloco 5) e sem afetar o PDF/geração do Relatório Pontual.

## Estado confirmado por leitura própria do código (Opus — file:line auditados)
- **Streamlit**: pin `streamlit>=1.35.0,<2` (pyproject.toml:15); instalado **1.58.0**. `st.image(width=...)` aceita `"content"|"stretch"|int` — CONFIRMADO pela docstring da versão instalada (`width : "content", "stretch", or int`; inteiro = px com cap na largura do container). API válida para D3.
- **D1 coord** — `render_coord_search_sidebar` (pages.py:523-543): assinatura `() -> tuple[float, float] | None` (523). Hoje tem `st.sidebar.info(...)` em **526-529** (bloco do BLK-UI-02 a reverter). Acima: `st.sidebar.markdown("---")` (525). Abaixo: `text_input` (530-534), `parse_coordinate_input` (537), `st.sidebar.error(...)` (539-542), `return result` (543). Chamada única em `streamlit_app.py:470` (`search_pin = render_coord_search_sidebar()`); o retorno é desempacotado posicionalmente (`lookup_hex_by_coord(*search_pin, df)` em 471) — a assinatura `tuple|None` é contratual e NÃO pode mudar.
- **Caption original (texto EXATO)** — recuperado via `git show 0862205:src/motor_expansao/dashboard/pages.py` (commit BLK-UI-01). Evidência literal:
  ```python
  st.sidebar.markdown("### Busca por coordenada")
  st.sidebar.caption("Localize um hexagono pela coordenada. Offline, sem API externa.")
  ```
- **D2 tooltips** — `_shared_map_tooltip` style dict em components.py:1067-1077 (as 4 chaves de tamanho em **1073-1076**); `_hybrid_compact_tooltip` style dict em **1102-1112** (as 4 chaves em **1108-1111**). Valores ATUAIS idênticos nas duas: `"fontSize": "11px"`, `"padding": "6px 8px"`, `"maxWidth": "260px"`, `"lineHeight": "1.25"`. Chaves pré-existentes (`backgroundColor`, `color=COLORS["text"]`, `border`, `borderRadius`, `fontFamily`) e o `html` de cada uma confirmados — NÃO mexer neles.
- **D3 preview** — 4× `st.image(..., width="stretch")` em pages.py, nos blocos **2596-2600** (densidade), **2601-2605** (renda), **2606-2610** (score), **2611-2615** (concorrentes). As 4 linhas com `width="stretch",` são **2599, 2604, 2609, 2614**. São inequivocamente o preview do censitário: precedidas do `st.caption` do método (2591-2595) e seguidas de `st.markdown("##### Setores intersectados")` (2617). Os PNGs vêm de `mapas[...]` (já gerados; D3 não os toca).
  - **ALERTA DE SEGURANÇA (correção ao rascunho):** existem **>40 ocorrências** de `width="stretch"` em pages.py (linhas 673, 698, 702, ... 2498, 2599, 2604, 2609, 2614, 2683, etc.). NÃO usar `replace_all`. As 4 trocas DEVEM ser feitas uma a uma nas linhas 2599/2604/2609/2614, ou com `old_string` único incluindo a chamada `st.image` completa de cada bloco.
- **D4 CSS seletor** — `inject_styles()` injeta CSS via `st.markdown(unsafe_allow_html=True)`. As 3 regras do segmented control estão em pages.py:301-319:
  - Regra base dos botões: **301-307** (`background`, `color=COLORS["muted"]`, `border=1px solid {COLORS["border"]}`).
  - Regra `:hover`: **308-312** (já dá feedback; manter INALTERADA).
  - Regra do botão ATIVO `[aria-checked="true"]`/`[aria-selected="true"]`: **313-319** (`background: rgba(25,183,255,0.22)`, `color=COLORS["text"]`, `border-color: {COLORS["brand_alt"]}`).
  - `COLORS` (constants.py:274-280): `panel_solid=#12172A`, `border=rgba(120,137,210,0.24)`, `text=#F3F7FF`, `muted=#A7B3D1`, `brand_alt=#19B7FF`. rgb(25,183,255) == `#19B7FF` == `brand_alt`.
- **render_tab_selector** (pages.py:426-450): Bloco 5 render lazy (`st.segmented_control`, `session_state`, `last_key`, `default`, `label_visibility="collapsed"`). NÃO TOCAR — fora do bloco `<style>` de `inject_styles()`.
- **Testes**: `test_map_tooltips_tem_css_de_tamanho` (test_streamlit_app.py:341-349) assere os 4 valores ATUAIS (`"11px"`/`"6px 8px"`/`"260px"`/`"1.25"`) — quebra se D2 não atualizar junto. `test_inject_styles_cobre_componentes_baseweb` (4466-4481) só checa PRESENÇA de `stSegmentedControl`, `data-baseweb="popover"`, `data-baseweb="select"` e (`aria-checked="true"` OR `aria-selected="true"`) no CSS — D4 mantém todos esses seletores, então segue passando.

## Decisões técnicas (valores APROVADOS pelo Planner Opus)

### D1 — Reverter coord (pages.py:526-529)
Substituir o bloco `st.sidebar.info(...)` (526-529) pelo par heading+caption ORIGINAL (texto exato do commit `0862205`):
```python
st.sidebar.markdown("### Busca por coordenada")
st.sidebar.caption("Localize um hexagono pela coordenada. Offline, sem API externa.")
```
Preservar intactos: `st.sidebar.markdown("---")` (525), `text_input` (530-534), `parse_coordinate_input`, `st.sidebar.error`, assinatura `() -> tuple[float, float] | None`, e a chamada `streamlit_app.py:470`.

### D2 — Tooltip meio-termo (components.py:1073-1076 e 1108-1111)
Valores APROVADOS — meio-termo entre o default deck.gl (~13-14px, sem fontSize explícito) e o 11px do BLK-UI-02; legível, porém mais contido que o default:
- `"fontSize": "13px"`   (era `"11px"`)
- `"padding": "8px 10px"` (era `"6px 8px"`)
- `"maxWidth": "300px"`  (era `"260px"`)
- `"lineHeight": "1.35"` (era `"1.25"`)

Justificativa de adequação (validação Opus): 13px fica acima do 11px (corrige a queixa de fonte pequena) e abaixo/igual ao default deck.gl, mantendo o ganho anti-recorte da borda; `maxWidth 300px` evita estouro horizontal; `lineHeight 1.35` melhora a leitura sem inflar o card. É um meio-termo adequado — MANTIDO. Aplicar IDÊNTICO nas duas funções. Demais chaves e o `html` INALTERADOS.

### D3 — [BLK-FIX-10] Preview menor (pages.py:2599, 2604, 2609, 2614)
Largura fixa em px via constante nomeada (auditável, não muta outras seções):
```python
_CENSUS_PREVIEW_WIDTH_PX = 720  # BLK-FIX-10: preview do Relatorio Pontual menor que 100% da largura
```
- Definir a constante no bloco de constantes de módulo do topo de `pages.py` (ou imediatamente acima de `render_relatorio_pontual_censitario`).
- Nas 4 chamadas `st.image` (linhas 2599/2604/2609/2614) trocar `width="stretch"` por `width=_CENSUS_PREVIEW_WIDTH_PX`. **NÃO usar `replace_all`** (>40 `width="stretch"` no arquivo). Captions inalterados.
- INTOCADOS: `censo_report.py`, `censo_map.py`, `render_mapas_censitarios_combinados`, `gerar_pdf_relatorio_pontual_censitario`, raio 1,5 km, método de interseção. Só a largura de exibição na TELA muda; o conteúdo dos PNGs e o PDF não.

### D4 — Destaque seletor de abas — SÓ CSS (pages.py:301-319, dentro de inject_styles)
Reforçar SOMENTE as regras já existentes do `stSegmentedControl` (sem introduzir seletor novo). `:hover` (308-312) INALTERADA.

Regra base dos botões (301-307) — adicionar tipografia/padding/raio (mantendo as 3 props atuais):
```css
[data-testid="stSegmentedControl"] button,
div[role="radiogroup"] [data-baseweb="button"],
[data-baseweb="button-group"] button {
    background: rgba(18, 23, 42, 0.92);
    color: {COLORS["muted"]};
    border: 1px solid {COLORS["border"]};
    border-radius: 10px;
    font-size: 1rem;
    font-weight: 600;
    padding: 0.5rem 1.15rem;
}
```
Regra do botão ATIVO (313-319) — reforçar destaque (substituir `border-color: {COLORS["brand_alt"]};` pelo rgba literal e adicionar box-shadow/peso):
```css
[data-testid="stSegmentedControl"] button[aria-checked="true"],
[data-testid="stSegmentedControl"] button[aria-selected="true"],
[data-baseweb="button-group"] button[aria-checked="true"] {
    background: rgba(25, 183, 255, 0.22);
    color: {COLORS["text"]};
    border-color: rgba(25, 183, 255, 0.9);
    box-shadow: 0 0 8px rgba(25, 183, 255, 0.35);
    font-weight: 700;
}
```
Notas: rgb(25,183,255) == `brand_alt` (#19B7FF), já usado nessas linhas como literal — padrão do arquivo preservado. Tema dark mantido. `render_tab_selector` (426-450) e a lógica de `session_state`/render lazy NÃO são tocados — mudança 100% dentro do bloco `<style>`. Os seletores `stSegmentedControl`/`aria-checked`/`popover`/`select` exigidos por `test_inject_styles_cobre_componentes_baseweb` permanecem presentes.

## Plano de execução (Builder deve seguir em ordem)

### Passo 1 — [D1] Reverter coord — `src/motor_expansao/dashboard/pages.py`
Edit: substituir o bloco
```python
    st.sidebar.info(
        "**Busca por coordenada** — localize um hexagono pela coordenada. "
        "Offline, sem API externa."
    )
```
por
```python
    st.sidebar.markdown("### Busca por coordenada")
    st.sidebar.caption("Localize um hexagono pela coordenada. Offline, sem API externa.")
```
Verificação: sem `st.sidebar.info`; com markdown heading + caption; `markdown("---")`, `text_input`, `parse_coordinate_input`, `error` e assinatura intactos.

### Passo 2 — [D2] Tooltip meio-termo — `src/motor_expansao/dashboard/components.py`
- `_shared_map_tooltip` (1073-1076): trocar os 4 valores para `"13px"`, `"8px 10px"`, `"300px"`, `"1.35"`.
- `_hybrid_compact_tooltip` (1108-1111): mesmas 4 trocas.
- Verificação: ambas com os valores novos; demais chaves e `html` inalterados.

### Passo 3 — [D3] Preview menor — `src/motor_expansao/dashboard/pages.py`
- 3a: adicionar a constante `_CENSUS_PREVIEW_WIDTH_PX = 720` (comentário BLK-FIX-10) no bloco de constantes de módulo do topo (ou logo acima de `render_relatorio_pontual_censitario`).
- 3b: nas 4 chamadas `st.image` (2599/2604/2609/2614) trocar `width="stretch"` por `width=_CENSUS_PREVIEW_WIDTH_PX`, **uma a uma** (proibido `replace_all`).
- Verificação: nenhuma `width="stretch"` restante NESSAS 4 chamadas do preview; `censo_map.py`/`censo_report.py`/PDF intocados; captions inalterados.

### Passo 4 — [D4] CSS do seletor — `src/motor_expansao/dashboard/pages.py` (`inject_styles`, 301-319)
- Na regra base (301-307) adicionar `border-radius: 10px; font-size: 1rem; font-weight: 600; padding: 0.5rem 1.15rem;`.
- Na regra do ativo (313-319) substituir `border-color: {COLORS["brand_alt"]};` por `border-color: rgba(25, 183, 255, 0.9);` e adicionar `box-shadow: 0 0 8px rgba(25, 183, 255, 0.35);` e `font-weight: 700;`.
- `:hover` (308-312) INALTERADA.
- Verificação: `render_tab_selector` (426-450) byte-idêntico; mudança só no `<style>`; seletores `stSegmentedControl`/`aria-checked`/`popover`/`select` mantidos.

### Passo 5 — Testes — `tests/integration/test_streamlit_app.py`
- Atualizar `test_map_tooltips_tem_css_de_tamanho` (346-349): asserts para `"13px"`, `"8px 10px"`, `"300px"`, `"1.35"`. (OBRIGATÓRIO no mesmo passo do D2.)
- Opcional (não obrigatório; só se trivial/barato, sem render Streamlit):
  - D3: assert `pages._CENSUS_PREVIEW_WIDTH_PX == 720`.
  - D4: assert `"box-shadow" in css` em `test_inject_styles_cobre_componentes_baseweb` (evitar asserções de CSS frágil).
  - D1: sem teste novo necessário (mock de `render_coord_search_sidebar` não inspeciona texto).

### Passo 6 — Validação do Builder (subconjunto; suíte FULL é gate do QA)
- `python -m pytest -q tests/integration/test_streamlit_app.py` (SERIAL — `-n auto` dá INTERNALERROR execnet × Python 3.14 local).
- `python -c "import streamlit_app"` → ok.
- `ruff check src/motor_expansao/dashboard/pages.py src/motor_expansao/dashboard/components.py tests/integration/test_streamlit_app.py`.
- `mypy src/motor_expansao/dashboard/pages.py src/motor_expansao/dashboard/components.py`.

## Arquivos afetados
- `src/motor_expansao/dashboard/pages.py` (D1 coord 526-529; D3 constante + 2599/2604/2609/2614; D4 CSS 301-319)
- `src/motor_expansao/dashboard/components.py` (D2 tooltips 1073-1076 e 1108-1111)
- `tests/integration/test_streamlit_app.py` (atualizar `test_map_tooltips_tem_css_de_tamanho`)

## Dependências
- Nenhuma dependência nova. `st.image(width=int)` já suportado no Streamlit do projeto (1.58.0; pin `>=1.35.0,<2`). Sem dependência de API ao vivo introduzida (Bloco 4/6 intocados).

## Riscos técnicos
- **R1 (texto do caption):** RESOLVIDO — exato via `git show 0862205` (`"Localize um hexagono pela coordenada. Offline, sem API externa."`). Sem ambiguidade.
- **R2 (contrato do teste de tooltip):** `test_map_tooltips_tem_css_de_tamanho` assere `"11px"`. Mitigação: Passo 5 atualiza os 4 valores no MESMO passo do D2; senão o teste falha.
- **R3 (`replace_all` em D3):** ALTO se mal-feito — >40 `width="stretch"` no arquivo. Mitigação dura: edição uma a uma nas 4 linhas do preview (2599/2604/2609/2614); `replace_all` PROIBIDO.
- **R4 (seletor CSS por versão / Bloco 5):** baixo — reforçamos as MESMAS regras já em uso e cobertas por teste, sem seletor novo; `render_tab_selector` e `session_state` não tocados. QA deve confirmar via diff que `render_tab_selector` (426-450) está byte-idêntico.
- **R5 (PDF/geração do censitário):** D3 só muda largura de exibição na tela; `censo_map.py`/`censo_report.py`/PDF/raio/interseção intocados.

## Critérios de aceite finais (para QA)
1. **D1:** `render_coord_search_sidebar` SEM `st.sidebar.info`; COM `st.sidebar.markdown("### Busca por coordenada")` + `st.sidebar.caption("Localize um hexagono pela coordenada. Offline, sem API externa.")`; `markdown("---")`, `text_input`, `parse_coordinate_input`, `error`, assinatura `tuple[float,float]|None` e a chamada `streamlit_app.py:470` intactos.
2. **D2:** ambos os tooltips com `fontSize="13px"`, `padding="8px 10px"`, `maxWidth="300px"`, `lineHeight="1.35"`; demais chaves e `html` inalterados; `test_map_tooltips_tem_css_de_tamanho` atualizado e passando.
3. **D3 (fecha BLK-FIX-10):** as 4 `st.image` do preview usam `width=_CENSUS_PREVIEW_WIDTH_PX` (=720); nenhuma `width="stretch"` nessas 4; constante definida em `pages.py`; `censo_map.py`/`censo_report.py`/PDF intocados; captions inalterados.
4. **D4:** regras do `stSegmentedControl` em `inject_styles` com `font-size`/`font-weight`/`padding`/`border-radius` reforçados e botão ativo com `border-color` vivo + `box-shadow` + `font-weight:700`; `render_tab_selector` (426-450) byte-idêntico; `:hover` inalterado; `test_inject_styles_cobre_componentes_baseweb` passando.
5. **Suíte:** sem NOVAS falhas vs baseline conhecida `695 passed, 1 skipped, 3 failed PRÉ-EXISTENTES`; ruff e mypy limpos no escopo tocado; `import streamlit_app` ok.
6. **M1 READ-ONLY:** zero alteração em score, pesos, fórmula ou artefatos oficiais.

## Validações obrigatórias
- Comando: `python -m pytest -q tests/integration/test_streamlit_app.py` (SERIAL; NÃO usar `-n auto` — INTERNALERROR execnet × Python 3.14 local). Critério mínimo: ZERO novas falhas vs baseline `695 passed, 1 skipped, 3 failed PRÉ-EXISTENTES`.
- Comando: `python -c "import streamlit_app"`. Critério: import ok, sem exceção.
- Comando: `ruff check` no escopo tocado. Critério: 0 erros.
- Comando: `mypy src/motor_expansao/dashboard/pages.py src/motor_expansao/dashboard/components.py`. Critério: sem novos erros.

## Fora de escopo (invioláveis)
- Recalcular qualquer score (`score_priorizacao`, `hex_score_estrutural`, `score_setor_2022_calibrado`, residual, SAM) ou artefatos M1.
- Alterar a LÓGICA de render lazy de abas (Bloco 5) — só estilo do seletor (`render_tab_selector` byte-idêntico).
- Alterar o PDF exportado / geração de mapas do Relatório Pontual (item nº3 é só preview na TELA).
- Quebrar Bloco 4 (carga lazy) / Bloco 6 (mapa enxuto); recolocar dependência de API ao vivo.
- Usar `replace_all` para a troca de `width="stretch"` (>40 ocorrências no arquivo).

## Decisões para o GATE HUMANO (D1..D4 — confirmar antes do Builder)
- **D1 — Reverter coord:** RECOMENDO reverter ao par exato `st.sidebar.markdown("### Busca por coordenada")` + `st.sidebar.caption("Localize um hexagono pela coordenada. Offline, sem API externa.")` (texto literal do commit BLK-UI-01 `0862205`). [Recomendação: APLICAR como acima.]
- **D2 — Tooltip meio-termo:** RECOMENDO `fontSize="13px"`, `padding="8px 10px"`, `maxWidth="300px"`, `lineHeight="1.35"` (validado como meio-termo adequado entre 11px e o default deck.gl). [Recomendação: APLICAR esses valores.]
- **D3 — Preview menor (fecha BLK-FIX-10):** RECOMENDO largura fixa via constante `_CENSUS_PREVIEW_WIDTH_PX = 720` nas 4 `st.image`, edição uma a uma. (720 px ≈ menor que 100% da coluna de conteúdo, mantém legibilidade dos mapas.) [Recomendação: APLICAR 720; ajustável pelo humano se preferir outro px.]
- **D4 — Destaque do seletor (SÓ CSS):** RECOMENDO reforçar as MESMAS regras do `stSegmentedControl` (base: `font-size:1rem`/`font-weight:600`/`padding:0.5rem 1.15rem`/`border-radius:10px`; ativo: `border-color: rgba(25,183,255,0.9)` + `box-shadow: 0 0 8px rgba(25,183,255,0.35)` + `font-weight:700`), `:hover` inalterada, `render_tab_selector` intocado. [Recomendação: APLICAR como acima.]

## Alerta de aprovação
ATENÇÃO: esta tarefa é de criticidade **ALTA** (toca o seletor de abas da produção/Bloco 5 e a navegação do dashboard, ainda que SÓ via CSS, e o preview do Relatório Pontual). É READ-ONLY sobre o M1 (não toca score/pesos/fórmula/artefatos), mas a esteira Alta exige **REVISÃO HUMANA OBRIGATÓRIA deste plano antes do Builder**. O usuário deve ler este handoff e confirmar D1..D4 antes de qualquer implementação.

## Guardrails ativos
- READ-ONLY sobre o M1: nenhum score, peso, fórmula ou artefato oficial alterado (CLAUDE.md §2/§3/§5).
- Bloco 5 render lazy intocável: `render_tab_selector` e `session_state`/`st.segmented_control` — só CSS em `inject_styles` (CLAUDE.md §4).
- Item nº3 (BLK-FIX-10) READ-ONLY sobre PDF/geração de mapas: `censo_report.py`, `censo_map.py`, `render_mapas_censitarios_combinados`, `gerar_pdf_relatorio_pontual_censitario`, raio 1,5 km e método de interseção intocados.
- Bloco 4 (carga lazy por UF) e Bloco 6 (mapa enxuto) intocados; sem dependência de API ao vivo.
- Nenhum PR sobe com CI quebrado; mudança é só estilo/strings/largura de exibição.
- Paths pré-sujos (`data/outputs/setores_censitarios_2022_geo/_metadata.json`, `data/reports/relatorio_pontual_censitario_base_geo.md`) NÃO devem ser commitados — alheios ao ciclo.

## Nota de housekeeping
Este ciclo FECHA o BLK-FIX-10 (Baixa). No fechamento, mover via `scripts/housekeeping_move_block.py BLK-FIX-10`.
