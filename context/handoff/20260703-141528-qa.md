# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer (Opus 4.8)

## Próxima Skill recomendada
Fechamento manual (orquestrador: completed.md + commit por path; decidir TP-09 conforme materialidade)

## VEREDITO
APROVADO

## Justificativa
A medição é HONESTA e REPRODUZÍVEL: reproduzi por conta própria, sobre os parquets + `data/validacao/`
REAIS, os números do Builder byte-a-byte (baseline R²_oof=+0.3119; C1 Δ pareado completo +0.0019 /
fora +0.0013, IC95 não cruza zero; C2 Δ −0.0312 / −0.0120). O `APLICAR_C1` passa o critério (D) por
uma margem que é ruído-material (segunda casa decimal, com C1==baseline em 97.3% dos hexes) e o C2 é
NO-GO legítimo — ambos corretamente recomendados a NÃO aplicar. Suíte full verde, READ-ONLY sobre o M1
comprovado (mtime dos 4 oficiais + `hexagonos_mercado_mapeado.parquet` inalterado), anti-PII e
isolamento confirmados.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- Docstring de `ResultadoCandidato` (linha 123) menciona um campo `r2_insample` que NÃO existe no
  dataclass (só `r2_oof_log`). Texto stale/aspiracional, inofensivo (nenhum R² in-sample é
  materializado nem entra no veredito — confirmado por `dataclasses.fields`). Limpar a frase num
  toque futuro.

## Testes faltantes
- nenhum relevante. Cobertura sólida: leitor anti-PII (dict-only, real vs fallback, arquivo ausente,
  zero PII), decay point-level (dist0/1km/2km, por rede, fallback), k-ring (conserva massa / não infla
  4x / k0 / flat), residual C1/C2 em faixa e C2<=C1, CAP_REF intocado, FU1 preservado quando C ausente,
  R² in-sample banido, veredito por sub-candidato token-exato, Δ pareado determinístico, isolamento AST,
  anti-PII no relatório.

## Riscos remanescentes
- C1 passa o critério formal (D) por ~+0.002 de R²_oof — decisão de aplicar/não aplicar é de PRODUTO
  (documentada honestamente como NÃO recomendar). Nenhuma mudança em produção neste ciclo.
- Cobertura de capacidade real baixa (Smart+Engenharia = ~33% dos PONTOS, 2/28 redes) → C1 é ~neutro
  por construção; um bloco futuro só faria sentido com capacidade real cobrindo a maioria dos pontos.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte COMPLETA (gate único) — `python -m pytest -n auto -q`
```
1316 passed, 1 skipped, 371882 warnings in 454.31s (0:07:34)
```
0 falhas, 0 erros de coleta. Acima do baseline ~1290 (FU1) pelos novos testes do Candidato C.
Sem flakiness observada.

### 2. Smoke — `python -c "import streamlit_app; print('import ok')"`
```
... (avisos de cache do Streamlit sem runtime) ...
import ok
```

### 3. Isolamento (AST) dos 2 módulos
```
capacidade_clube_validacao OK; from-imports: ['__future__', 'collections.abc', 'contrato', 'pathlib']
revalidacao_residual_candidatos OK; from-imports: ['__future__', 'collections.abc', 'contrato',
  'dataclasses', 'motor_expansao.demanda_revelada.calibracao_residual',
  'motor_expansao.demanda_revelada.capacidade_clube_validacao',
  'motor_expansao.dimensionamento.backtest_dim', 'pathlib']
```
Nenhum import de `pipelines.m1` / `dashboard` / `censo` / `api` / `config` raiz. `dimensionamento.backtest_dim`
(helper `_r2`) e `demanda_revelada.*` (harness irmão) são camadas paralelas permitidas.

### 4. mtime dos artefatos M1 + `hexagonos_mercado_mapeado.parquet` (ANTES == DEPOIS da suíte)
```
1780501621 data/staging/brasil_estrutural.parquet
1780501631 data/staging/brasil_priorizados.parquet
1780501655 data/staging/hexagonos_brasil_oportunidades.parquet
1781205056 data/staging/hexagonos_mercado_mapeado.parquet
1781271963 data/outputs/hexagonos_brasil_dashboard.parquet
```
Idêntico antes e depois de rodar a suíte + o `__main__` completo. Batem com os do Builder.
`git status` sem nenhum artefato de produção modificado (só `revalidacao_residual_candidatos.py`,
handoff, current_task e os 2 geojson já deletados no início do ciclo).
`data/validacao/`: só `README.md` versionado (`git ls-files`); os xlsx reais gitignored e inalterados.
Relatório `data/analysis/revalidacao_residual_candidato_c.md` gitignored (`git check-ignore` positivo).

### 5. Lint/type
```
=== RUFF === All checks passed!
=== MYPY === Success: no issues found in 2 source files
```

### 6. Reprodução dos NÚMEROS (run REAL end-to-end, `__main__`, n=16411, seed=42)
```
Veredito: NAO_APLICAR_A; APLICAR_C1; NAO_APLICAR_C2
  Baseline: R2_oof_log=0.3119 IC95=[0.2977, 0.3250] | rho_oof=0.4615 | n=16411
  Candidato A: R2_oof_log=0.2692  Δ COMPLETO=-0.0427 [-0.0477,-0.0379]  Δ FORA=-0.0193 [-0.0232,-0.0157]
  Candidato C1: R2_oof_log=0.3138 IC95=[0.2995, 0.3269] | rho_oof=0.4629
     Δ pareado (C1-baseline) COMPLETO = 0.0019 IC95=[0.0015, 0.0023] -> vence=True
     Δ pareado (C1-baseline) FORA     = 0.0013 IC95=[0.0007, 0.0019] -> vence=True
  Candidato C2: R2_oof_log=0.2807 IC95=[0.2666, 0.2936] | rho_oof=0.4422
     Δ pareado (C2-baseline) COMPLETO = -0.0312 IC95=[-0.0349, -0.0278] -> vence=False
     Δ pareado (C2-baseline) FORA     = -0.0120 IC95=[-0.0143, -0.0099] -> vence=False
```
BATE COM O BUILDER byte-a-byte (baseline, A, C1, C2, IC95, n).

### 7. Anti-PII do leitor de `data/validacao/` (run REAL)
```
REAL cap reader output (só {rede:float}): {'smart_fit': 2363.0, 'engenharia_do_corpo': 3106.5}
types: {'smart_fit': 'float', 'engenharia_do_corpo': 'float'}
sky ancora: 944.5
```
Só `{rede:float}`; nenhum nome/endereço/coord atravessa. Report scan de PII (COLUNAS_PII_PROIBIDAS,
word-boundary): `PII tokens found: []`.

### 8. Materialidade do C1 (medida própria — divergência C1 vs baseline)
```
n total join: 16411
hexes onde C1 == baseline (diff<1e-6): 15973 (97.3%)
hexes onde C1 difere (>=1e-6):           438 ( 2.7%)
media |diff| onde difere: 2.96   | max |diff|: 22.4
```

### 9. Mecânica verificada (k-ring conserva massa + decay 2 km)
```
kring sum: 1000.0 (NÃO 4000) | central: 250.0 | vizinho: 125.0
decay dist0: 2000.0 | 1km: 1000.0 | 2km: 0.0
```

### 10. DEC-008 — R² in-sample ausente do veredito
```
ResultadoCandidato fields: ['nome','r2_oof_log','ic95_r2_oof','rho_oof','ic95_rho_oof','alpha','n',
  'metodo','y_oof','y_pred_oof']
any insample field: False
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do
caminho crítico. A suíte full rodou 1× aqui (gate único). A reprodução dos números foi contra os
parquets de `data/staging`/`data/outputs` e os xlsx REAIS de `data/validacao/` (não fixtures). As
fixtures sintéticas do teste unitário são uso legítimo (isolam a lógica; a fonte real de `data/validacao/`
nunca é lida em teste — anti-PII exigido pela DEC-012).

## Conferência cruzada com log do Builder
BATE. Baseline (+0.3119 / rho +0.4615 / n=16411), C1 (+0.3138; Δ +0.0019 completo / +0.0013 fora),
C2 (+0.2807; Δ −0.0312 / −0.0120), A (Δ −0.0427 / −0.0193) e capacidades reais
(smart_fit=2363.0 / engenharia_do_corpo=3106.5; Sky 944.5) reproduzem byte-a-byte o handoff do Builder.
Contagem de suíte: o Builder rodou só o subset (304 passed); o gate full aqui deu 1316 passed, 1 skipped.

## Minha leitura explícita sobre a MATERIALIDADE do C1 (input ao TP-09)
**Ruído estatisticamente-significante-por-N-grande, NÃO material.** O C1 "vence" o critério (D)
porque, com n=16.411, um Δ pareado de +0.0019 tem IC95 estreito que não cruza zero — mas o ganho é de
segunda casa decimal (+0.002 sobre +0.3119, ~0.6% relativo). A medição própria confirma a causa
mecânica: **97.3% dos hexes têm C1 IDÊNTICO ao baseline** (a re-derivação point-level com fallback 2.500
em ~66% dos pontos reproduz a oferta mapeada do parquet); a diferença vive em só 438 hexes (2.7%) perto
de Smart/Engenharia. Recomendação ao BLK-TP-09: **NÃO aplicar C1** — o ganho não paga o custo/risco de
recalibrar a fórmula do residual em produção; a hipótese (A) do gate fica confirmada. C2 é NO-GO honesto
(piora em ambos os recortes; repete o viés de co-localização bairro↔demanda do Candidato A). Residual
segue como está em produção; nenhuma regeneração de parquet paralelo requerida.

## Guardrails verificados
- score_priorizacao não alterado: sim (não tocado; nem lido como peso)
- Artefatos M1 preservados: sim — mtime dos 4 oficiais + `hexagonos_mercado_mapeado.parquet` idêntico
  antes/depois; `calcular_colunas_mercado.py` INTOCADO (fora do `git diff --stat`); fórmula do residual
  em produção NÃO alterada; nenhum parquet regenerado
- Validações re-executadas pelo QA (sem bypass): pytest -n auto → 1316 passed/1 skipped; import
  streamlit_app → ok; ruff → clean; mypy → clean; AST isolamento → ok; run REAL __main__ → números
  reproduzidos; leitor anti-PII REAL → só {rede:float}; PII scan report → []; materialidade → 97.3%
  idêntico; k-ring/decay → corretos; DEC-008 sem campo in-sample
- Escopo respeitado: sim — só `revalidacao_residual_candidatos.py` (estendido, FU1 preservado),
  `capacidade_clube_validacao.py` (novo), teste novo e o relatório gitignored. Parâmetros canônicos
  (H3_RESOLUTION=7, RENDA_MIN=4500, pesos renda=0.40/pop=0.60, DIST_MIN_ULTRA_KM=1.0) intocados;
  CAP_REF=2500 do clip intocado.
- Housekeeping: N/A (tarefa AD-HOC, fora do backlog). `housekeeping_move_block.py --check` retorna stub
  ausente (esperado p/ ad-hoc → move no-op). `tests/unit/test_housekeeping_helper.py` verde na full.

## Decisão recomendada
Fechar ciclo por fechamento manual (orquestrador: resumo → completed.md + commit por path). Registrar no
BLK-TP-09 o veredito honesto: NAO_APLICAR_A; C1 tecnicamente GO mas ganho DESPREZÍVEL → NÃO aplicar;
NAO_APLICAR_C2. Residual em produção inalterado; nenhuma DEC de recalibração necessária.
