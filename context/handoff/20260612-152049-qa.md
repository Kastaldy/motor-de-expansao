# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (tarefa ad-hoc / follow-up; sem bloco BLK-UI-02 no backlog)

## VEREDITO
APROVADO

## Justificativa
Os 4 ajustes aprovados no gate humano (D1=A coord destaque, D2=A CSS dos 2 tooltips, D3=(a)+(b) dica de filtro por município, D4=A testes) estão implementados exatamente como decidido, são puramente CSS-em-dict/markdown/strings, e re-executei todas as validações por conta própria: suíte full `3 failed, 696 passed, 1 skipped` com as 3 falhas PRÉ-EXISTENTES confirmadas por `git stash` (passed subiu 695→696 pelo único teste novo), suíte alvo 183 passed, import ok, ruff/mypy limpos. Guardrail Bloco 6 anti-OOM e READ-ONLY M1 intocados (zero tokens de score/cap/downsample no diff de `src/`).

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- nenhuma

## Testes faltantes
- nenhum (D4 cobre o CSS dos 2 tooltips e a substring de município no caption não-capped; o #3 é só comunicação, sem lógica nova a testar)

## Riscos remanescentes
- R5 (do Builder, aceito): a redução de altura do tooltip (11px / lineHeight 1.25) ATENUA o recorte na borda inferior no caso típico 1080p, mas o recorte do tooltip no iframe é limitação NATIVA do deck.gl; com o cursor no fundo absoluto pode ainda recortar 1-2 linhas. Trocar o componente está fora de escopo (decisão BLK-UI-01). Não bloqueante.
- Os 3 testes pré-existentes que falham (drift de CSVs reais locais gitignored em `concorrentes/` + gate DEC-006 do SAM em parquet de mercado pendente de regeneração) seguem vermelhos — alheios a este ciclo; regeneração é passo operacional separado.

## Saída literal das validações (re-executadas pelo QA)

### 1a. Suíte FULL com `-n auto` (tentativa — bug de ambiente conhecido)
```
INTERNALERROR>   File ".../xdist/dsession.py", line 90, in pytest_sessionstart
INTERNALERROR>     nodes = self.nodemanager.setup_nodes(putevent=self.queue.put)
...
INTERNALERROR>   File ".../subprocess.py", line 1430, in _make_inheritable
INTERNALERROR>     h = _winapi.DuplicateHandle(
INTERNALERROR>         _winapi.GetCurrentProcess(), handle, ...
INTERNALERROR> OSError: [WinError 50] Não há suporte para o pedido
```
NOTA: este é o bug de AMBIENTE conhecido execnet×Python 3.14 LOCAL (gateway/DuplicateHandle no Windows), NÃO regressão. Por isso rodei SERIAL (sem `-p no:xdist`), abaixo.

### 1b. Suíte FULL SERIAL (gate único do ciclo) — `python -m pytest -q`
```
=========================== short test summary info ===========================
FAILED tests/integration/test_modelo_mercado_hexagonos.py::test_csvs_concorrentes_legiveis[csv_path1-223]
FAILED tests/integration/test_modelo_mercado_hexagonos.py::test_csvs_concorrentes_legiveis[csv_path2-472]
FAILED tests/integration/test_modelo_mercado_hexagonos.py::test_parquet_final_respeita_guardrails_do_piloto
3 failed, 696 passed, 1 skipped, 71492 warnings in 191.52s (0:03:11)
```
As 3 falhas são as MESMAS 3 PRÉ-EXISTENTES da baseline informada (drift de CSVs reais locais gitignored em `concorrentes/` + gate DEC-006 do SAM em parquet de mercado pendente de regeneração). `passed` subiu de 695 (baseline BLK-UI-01) para **696**, exatamente o +1 do teste novo `test_map_tooltips_tem_css_de_tamanho`. ZERO falhas novas.

### 1c. Prova de pré-existência via `git stash` (mudanças do BLK-UI-02 removidas)
```
=========================== short test summary info ===========================
FAILED tests/integration/test_modelo_mercado_hexagonos.py::test_csvs_concorrentes_legiveis[csv_path1-223]
FAILED tests/integration/test_modelo_mercado_hexagonos.py::test_csvs_concorrentes_legiveis[csv_path2-472]
FAILED tests/integration/test_modelo_mercado_hexagonos.py::test_parquet_final_respeita_guardrails_do_piloto
3 failed, 1 passed, 739 warnings in 3.96s
```
Com os 3 arquivos do ciclo em stash, os MESMOS 3 testes falham → confirmado: falhas alheias a este ciclo. `git stash pop` restaurou as mudanças com sucesso (working tree íntegro).

### 2. Suíte alvo — `python -m pytest -q tests/integration/test_streamlit_app.py`
```
183 passed, 9151 warnings in 18.00s
```

### 3. Smoke import — `python -c "import streamlit_app; print('import ok')"`
```
... WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
import ok
```

### 4a. Ruff — `python -m ruff check pages.py components.py test_streamlit_app.py`
```
All checks passed!
```

### 4b. Mypy — `python -m mypy pages.py components.py`
```
Success: no issues found in 2 source files
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico. A suíte full rodou SERIAL contra os artefatos/config reais do projeto (a queda para serial foi por bug de ambiente execnet×3.14 documentado, NÃO mascaramento — não usei `-p no:xdist`). Este ciclo não envolve tooling de produção (SOPS/segredos); as regras (b)/(c)/(d) do prompt não se aplicam — é UI/CSS com testes unitários legítimos.

## Conferência cruzada com log do Builder
Bate. Builder reportou suíte alvo 183 passed, import ok, ruff "All checks passed!", mypy "no issues found in 2 source files" — reproduzido idêntico. Builder não rodou a full (gate do QA), como esperado; rodei eu (696 passed / 3 pré-existentes). O diff bate 1:1 com o "O que foi feito": 4 chaves CSS nos 2 tooltips, frase no ramo não-capped de `build_map_scope_caption`, `st.sidebar.info` em `render_coord_search_sidebar`, frase no caption de `render_mapa_territorial`, e 2 imports + 1 assert + 1 teste novo no test file.

## Guardrails verificados
- score_priorizacao não alterado: sim — `git diff HEAD -- src/ tests/ | grep score_priorizacao|hex_score_estrutural|score_setor|score_expansao|score_oportunidade` → NENHUM token. Mudanças são CSS-em-dict/markdown/strings.
- Artefatos M1 preservados: sim — nenhuma escrita em parquet/artefato; diff de código só em `pages.py`/`components.py`.
- **Bloco 6 anti-OOM (CRÍTICO):** sim, intocado — `constants.py` SEM diff (MAP_POINT_LIMIT/MAP_POINT_LIMIT_LARGE inalterados); `_downsample_map_index` e o scope/cap dos builders NÃO aparecem no diff (grep dedicado retornou vazio). O #3 é SÓ texto (2 strings de caption). Sem violação do gate.
- D1 (coord): fluxo intacto — `text_input` (key="coord_search_input"), `parse_coordinate_input`, `st.sidebar.error`, assinatura `() -> tuple[float,float] | None` e chamada ÚNICA fora-de-aba em `streamlit_app.py:470` (`search_pin = render_coord_search_sidebar()`) PRESERVADOS. Só o heading+caption viraram um `st.sidebar.info`. Bloco 4/5 intactos.
- D2 (tooltips): SÓ as 4 chaves (`fontSize`/`padding`/`maxWidth`/`lineHeight`) adicionadas ao `style` dos DOIS tooltips; `backgroundColor`/`color`/`border`/`borderRadius`/`fontFamily` e o HTML (14 linhas / 8 linhas) INALTERADOS.
- D3 (caption): substring testada do ramo CAPPED (`"Aplique filtros de municipio para ver o recorte completo."`) PRESERVADA; substring do ramo não-capped (`"todos os hexagonos validos {scope_label}"`) preservada por anexação pura. Frase de município anexada ao ramo não-capped E ao caption de `render_mapa_territorial`.
- Sem dependência de API ao vivo introduzida: sim (texto offline, "sem API externa").
- Builder NÃO commitou: sim — HEAD ainda em `0862205 BLK-UI-01 (recorte)`, nenhum commit novo.
- Paths pré-sujos NÃO tocados pelo Builder: confirmado — `_metadata.json` e `relatorio_pontual_censitario_base_geo.md` seguem modificados (alheios ao ciclo), mas NÃO commitados e fora do diff de `src/`/`tests/`.
- Validações re-executadas pelo QA (sem bypass): full serial → 696 passed/3 pré-existentes/1 skipped; alvo → 183 passed; import → ok; ruff → All checks passed!; mypy → Success.
- Escopo respeitado: sim — só `pages.py`, `components.py`, `tests/integration/test_streamlit_app.py` (mais os handoffs/current_task da esteira).
- Parâmetros canônicos preservados: sim (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, pesos renda=0.40/pop=0.60 — nenhum tocado).

## Housekeeping de concluídos
N/A (tarefa ad-hoc / follow-up; não há bloco `### BLK-UI-02` em `tasks/backlog.md` para mover — BLK-UI-01 permanece aberto). Não rodei o helper de move; isto NÃO bloqueia o veredito. O orquestrador registra o resumo em `completed.md`.

## Decisão recomendada
Fechar ciclo (APROVADO). Próximo passo: fechamento manual do follow-up BLK-UI-02.
