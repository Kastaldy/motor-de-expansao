# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-PRD-01 — Reescrever PRD.md como PRD padrão do projeto (doc-only)

## O que foi feito
Reescrita COMPLETA de `PRD.md`, sobrescrevendo o conteúdo anterior ("Programa de
Melhorias — Referência do Master Orchestrator") e absorvendo a edição pré-existente
`M PRD.md`. Seguiu o "Plano técnico" (passos 1–10) e o "Outline proposto" (seções 0–10)
do handoff do Planner, APROVADO no gate de revisão humana.

Estrutura escrita (seções 0–10):
- 0. Cabeçalho/front-matter: título "PRD — Motor de Expansao Ultra Academia",
  `Versao: 2026-05-29`, responsável Felipe Silva | Estrategia e Growth | Ultra Academia,
  e declaração explícita de subordinação ("Fonte de verdade: CLAUDE.md ... em qualquer
  conflito, CLAUDE.md prevalece"). Lista de docs de apoio por referência.
- 1. Visão e objetivo: duas trilhas (M1 oficial territorial + mercado por hexágono);
  cita o NOME `score_priorizacao`, sem pesos/valores.
- 2. Público-alvo (18-45, contexto de produto) e contextos de uso (Estratégia/Growth,
  análise territorial, operação de mercado; uso offline).
- 3. Escopo / fora de escopo (espelha CLAUDE.md §4: sem API ao vivo, FastAPI/PostGIS/
  Prefect, M2/M3, Power BI, pesquisas fora do deploy inicial).
- 4. Camadas e trilhas: papéis sem valores (M1 decide municípios/ranking; censitário/
  híbrido refinam intraurbano; mercado/residual e Expansão de Domínio apoiam, não
  substituem o M1).
- 5. Score oficial e guardrails — APENAS REFERÊNCIA a CLAUDE.md §3/§5. Nomes de
  parâmetros e artefatos citados; frase explícita "Os valores canônicos vivem somente
  no CLAUDE.md §3; este PRD nao os duplica para evitar deriva."
- 6. Requisitos funcionais (4 abas, Análise Pontual H3 raio 1.6 km descritivo,
  Relatório Censitário 1.5 km, multi-hex) e não-funcionais (offline, performance lazy,
  staging Parquet, CI verde como gate). Referencia docs/streamlit_dashboard_m1.md e
  data/reports/perf_baseline_dashboard.md.
- 7. Métricas de sucesso (qualitativas; aderência validada por BLK-SCORE-*).
- 8. Roadmap — APENAS REFERÊNCIA ao backlog: frentes por prefixo (BLK-OPS-*, BLK-ARCH-*,
  BLK-SCORE-*, BLK-PROD-*, BLK-ORQ-*), 1 linha cada, aponta para tasks/backlog.md e
  tasks/completed.md. Nenhum bloco copiado.
- 9. Dependências/restrições (infra/VPS): GUARDRAIL ABSOLUTO CLAUDE.md §6, fora de
  escopo, backup/DR via docs/backup_restore.md, deploy via docs/infra_producao.md.
- 10. Referências canônicas: hierarquia CLAUDE.md > backlog/completed > docs > PRD.

## Confirmação do passo 10 (anti-deriva)
Reli o PRD.md gravado e verifiquei via Grep que NENHUM valor numérico canônico proibido
aparece (0.40, 0.60, 7 como H3_RESOLUTION, 1.0 km, 4500, 1200, 1500, 2000, 3.5, 0.20,
2500). O único match de `\b7\b` é o número da seção "## 7. Metricas de sucesso" (não é
parâmetro). As exceções permitidas (faixa de público "18-45" e os raios descritivos de
feature 1.6 km / 1.5 km) aparecem apenas como contexto de produto/descrição de feature,
nunca como parâmetro canônico. Parâmetros/pesos/fórmula apontam para CLAUDE.md §3/§5.

## Arquivos alterados
- `PRD.md` — reescrita completa como PRD padrão de produto subordinado ao CLAUDE.md.

## Validações executadas
- `git --no-pager diff --stat -- PRD.md`: apenas PRD.md alterado
  (1 file changed, 226 insertions(+), 80 deletions(-)). (Aviso LF->CRLF é cosmético.)
- pytest: NÃO re-executado pelo Builder por ser ciclo doc-only (nenhum código tocado).
  Expectativa: suíte segue verde (baseline CLAUDE.md §5: 532 passed, 1 skipped). O QA
  re-executa `pytest -q` por conta própria conforme prompt do QA.
- import: não aplicável (nenhum módulo Python tocado).

## Problemas encontrados
nenhum

## Pendências
- QA deve re-executar `pytest -q` (esperado verde, doc-only) e reconferir
  `git --no-pager diff --stat -- PRD.md` (esperado só PRD.md).
- Commit por path é feito no fechamento pelo orquestrador (não commitar agora).
  `tasks/backlog.md` está pré-sujo e NÃO entra no commit por path (escopo do ciclo).

## Riscos remanescentes
nenhum (doc-only).

## Guardrails verificados
- score_priorizacao não alterado: sim (não aplicável — doc-only; PRD só referencia)
- Artefatos M1 preservados: sim (nenhum artefato/código/config tocado)
- Dashboard offline mantido: sim (não aplicável — nenhum código tocado)
- VPS não tocado: sim
