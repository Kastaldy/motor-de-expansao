# Handoff — Builder

## Skill que gerou este handoff
Builder (opus, override +1) — BLK-MA-03, 2026-07-29

## Próxima Skill recomendada
**QA (opus)** → Passo 6 em modo **MERGE-HUMANO** (o ciclo toca `tasks/backlog.md`, classificado como
`governanca` pelo `loop_guard` — o auto-merge de Média não se aplica, decisão E2).

## Bloco executado
**BLK-MA-03** — Sinal 1 do score de vulnerabilidade: presença em agregador (WellHub/TotalPass),
hex-level. Criticidade **Média**. Gate humano **G1 e G2 ratificados** (registro em
`tasks/current_task.md`, seção "GATE HUMANO"), portanto a emenda ao §8.1 foi escrita neste ciclo.

## O que foi feito

Implementado **exatamente o plano do Planner**, sem redesenho. Os 5 pontos em que o Planner divergiu
do Block Orchestrator foram preservados integralmente (P1, P2, justificativa do R2 reescrita, rename
`*_no_hex`, CA-9 com teste próprio).

**1. Módulo novo `presenca_agregador.py`** (molde direto de `churn_staleness.py`):

- `extrair_presenca_agregador(base_dir=None, *, snapshots=None)` — exatamente um entre os dois
  (`ValueError("informe exatamente um entre \`base_dir\` e \`snapshots\`")`, mensagem **idêntica** à
  de `extrair_churn_staleness:278`, para o teste casar por `match="exatamente um"`); **pura** com
  `snapshots` injetado.
- Ordem do algoritmo, que é o núcleo da correção: série longa validada → **reduzir** por
  `(fonte, chave_snapshot)` à maior `semana` (**P1**: ordenação por `semana`, JAMAIS por
  `snapshot_date`, que congela ou regride quando o coletor falha) → **filtrar** o universo do sinal 1
  (**P2**: filtro DEPOIS da redução, para a classificação mais recente vencer) → agregar por
  `hex_id_res7`.
- `_frame_presenca_vazio`, `_reduzir_para_observacao_mais_recente`, `_filtrar_universo_sinal_1`,
  `_agregar_por_hex`, `_assert_schema_presenca_agregador` — as 6 assinaturas do plano, sem desvio.
- **Reuso, não reimplementação (CA-11):** `_montar_serie_longa` é **importado** de `.churn_staleness`
  (com o comentário de reuso deliberado na linha do import) e `ler_snapshots` de `.snapshots`.
  **Nenhum dos dois arquivos foi editado.**
- `_COLUNAS_DE_SCORE_PROIBIDAS = {v1, v2, score_vulnerabilidade, n_sinais_disponiveis,
  flag_score_provisorio}` — a checagem de SCORE é a **primeira** do `_assert_schema`, antes da
  genérica de "colunas fora do contrato", senão a mensagem genérica venceria.
- Docstring do módulo registra o limite estrutural do **"0 agregadores"** (inalcançável e vacuoso;
  fontes só-positivas; o caso informativo é o **sinal 3** `sumiu_recente`), o viés do grão hex e os
  dois defeitos neutralizados (P1/P2), em português acentuado.

**2. Constantes novas em `contrato.py`** (`VERSAO_CONTRATO_PRESENCA_AGREGADOR`,
`FONTES_AGREGADORES`, `CATEGORIA_INDEPENDENTE` **replicada** com a motivação corrigida do Planner —
"não criar uma SEGUNDA aresta", não economia de tempo de import —, e
`CONTRATO_COLUNAS_PRESENCA_AGREGADOR` com as **10 colunas na ordem fixada**), todas em `__all__`.

**3. `_assert_schema_presenca_agregador`** cobre (a)–(m) do passo 6 do plano: SCORE primeiro, colunas
extra/faltante/ordem, `df.empty` → return, res-7 (amostra de até 5), `hex_id_res7` duplicado,
`n_agregadores_no_hex ∈ {1,2}`, contagens não negativas, **consistência** entre
`fontes_presentes_no_hex` e as contagens (e o comprimento vs. `n_agregadores_no_hex`), **bicondicional
de nulabilidade** dos dois relógios por fonte, `versao_contrato` e `semana_ultima_observacao_*` no
formato ISO.

**4. Emenda G1** escrita nos 4 itens (a, b, c, d) **com o texto do Planner**, em
`docs/vulnerabilidade_ma_contrato.md`.

**5. E2** — bloco `BLK-MA-03` estruturado inserido em `tasks/backlog.md` **antes de
`### BLK-ATR-05`** (âncora corrigida pelo Planner), com
`| **Autonomia** | **manual (NÃO loop-safe)** ... |`.

## Arquivos alterados

**Criados:**
- `src/motor_expansao/vulnerabilidade/presenca_agregador.py` — módulo do sinal 1 (extrator hex-level,
  10 colunas, sem escrita em disco).
- `tests/unit/vulnerabilidade/test_presenca_agregador.py` — **43 testes** (fixtures 100% sintéticas;
  o único I/O é `tmp_path` no round-trip).

**Editados:**
- `src/motor_expansao/vulnerabilidade/contrato.py` — 4 constantes novas + 4 entradas em `__all__`.
  Nada existente foi alterado.
- `src/motor_expansao/vulnerabilidade/__init__.py` — reexport dos 5 nomes + grupo novo em `__all__`
  (`# Sinal 1 (presenca em agregador, hex-level)`) + docstring da fronteira dos módulos atualizado
  (agora são três, não dois).
- `tests/unit/vulnerabilidade/test_snapshots.py` — `test_isolamento_imports`: tupla estendida com
  `mpresenca` + docstring explicando que esta tupla **não** prova o CA-9 e apontando para o teste
  próprio (a prosa do docstring foi acentuada de passagem, atendendo a ressalva `m4` do QA do MA-02
  nas 2 linhas que eu toquei).
- `tests/unit/vulnerabilidade/test_contrato.py` — `alvos` da varredura ASCII estendidos (colunas do
  contrato novo, `FONTES_AGREGADORES`, `CATEGORIA_INDEPENDENTE`,
  `VERSAO_CONTRATO_PRESENCA_AGREGADOR`) + `test_fontes_agregadores_e_subconjunto_de_fontes_validas`.
- `docs/vulnerabilidade_ma_contrato.md` — emenda G1 (itens a, b, c, d).
- `tasks/backlog.md` — bloco BLK-MA-03 estruturado (E2).

**NÃO tocados** (verificado por `git status --porcelain`): `config.py`, `pipelines/m1/`,
`normalizar_concorrentes.py`, `calcular_colunas_mercado.py`, `concorrentes_densos.py`,
`snapshots.py`, `churn_staleness.py`, `scripts/loop_guard.py`, `.gitignore`, `pyproject.toml`,
`PRD.md`. `graphify-out/*` segue **modificado-mas-não-adicionado** (regerado pelo hook).

## Validações executadas (saída REAL, sem bypass)

| # | comando | saída real | veredito |
|---|---|---|---|
| 1 | `python -m ruff check . --no-cache` (**forma exata do CI**, `ci.yml:54`) | `All checks passed!` | OK |
| 2 | `python -m pytest -q -p no:randomly tests/unit/vulnerabilidade/test_presenca_agregador.py` | `43 passed, 2306 warnings in 6.51s` | OK (≥ 28 exigidos) |
| 3 | `python -m pytest -q -p no:randomly tests/unit/vulnerabilidade/` | `173 passed, 8531 warnings in 12.80s` | OK |
| 4 | `python -m pytest -q -p no:randomly tests/unit/vulnerabilidade tests/integration/test_streamlit_app.py` | `428 passed, 27701 warnings in 66.37s` | OK |
| 5 | `python -c "import streamlit_app; print('import ok')"` | `import ok` (só os 2 warnings de `MemoryCacheStorageManager`, pré-existentes) | OK |
| 6 | `python -m pytest --collect-only -q` | `2230 tests collected in 142.54s` | OK — **2186 + 44**, e 44 = 43 novos + 1 em `test_contrato.py` |
| 7 | `python scripts/loop_guard.py --stdin` (lista dos 8 caminhos do ciclo) | 4 violações, **todas `governanca`** (3× `vulnerabilidade/*` "camadas paralelas com insumo de PII na origem (DEC-012)" + `tasks/backlog.md` "marcador de autonomia/criticidade"). **Nenhum `critico`.** | OK |
| 8 | `python scripts/loop_guard.py --base HEAD` | `GUARD OK: 0 caminho(s) alterado(s), nenhum proibido.` (exit 0) | OK |
| 9 | `python scripts/garimpeiro_select_block.py --list` | `BLK-FIX-LTV-01` — **não lista BLK-MA-03** | OK |
| 10 | `python -c "raw=open('tasks/backlog.md','rb').read(); print(raw.count(b'\r\n'))"` | `CRLF: 0` (idem `docs/vulnerabilidade_ma_contrato.md`) | OK (DEC-017) |
| 11 | `grep -n "emenda BLK-MA-03" docs/vulnerabilidade_ma_contrato.md \| wc -l` | `3` | OK (≥ 3 exigidas) |
| 12 | `git status --porcelain` | 9 modificados + 4 não-rastreados; `graphify-out/*` modificado-mas-não-adicionado; `PRD.md` intocado | OK |

**Nota sobre o `loop_guard`:** ele **exige** `--base <ref>` ou `--stdin` (a invocação nua do
enunciado, `python scripts/loop_guard.py`, é erro de argparse). Rodei **os dois modos**. Como nada
foi commitado (instrução do ciclo), o `--base HEAD` acusa 0 caminhos; o modo `--stdin` — que é o do
check `guard` do CI — foi alimentado com a lista exata dos caminhos que o ciclo vai commitar. O
`exit 1` do `--stdin` é **por desenho** (esse modo sai 1 para qualquer classe, e o CI resolve pela
régua de labels); o que importa para o CA-16 é que **nenhuma violação é `critico`**.

**Sobre a falha pré-existente:** `tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime`
foi executado isoladamente e **continua falhando** (`1 failed in 8.51s`) — é o defeito conhecido do
**BLK-FIX-LTV-01**, num arquivo que este ciclo não toca (nada em `lifetime/` nem no M1 entrou no
diff). Não é regressão deste bloco. A suíte COMPLETA é gate único do QA e não foi rodada aqui, por
regra do `prompts/builder.md`.

## Mapeamento CA → teste

Todos em `tests/unit/vulnerabilidade/test_presenca_agregador.py`, salvo indicação.

| CA | testes |
|---|---|
| **CA-1** universo TP/WH × independente | `test_universo_exclui_unidades_mesmo_com_rede_forjada` · `test_universo_exclui_tp_wh_com_marca_conhecida` · `test_hex_so_com_linhas_excluidas_nao_emite_linha` |
| **CA-2** chaves distintas, não linhas | `test_conta_chaves_distintas_nao_linhas` · `test_duas_chaves_no_mesmo_hex_contam_duas` |
| **CA-3** redução p/ observação mais recente | `test_recolocacao_conta_so_no_hex_mais_recente` (assere `{HEX_B}` e a AUSÊNCIA de `HEX_A`) |
| **CA-3b** ordenar por `semana` (**P1**) | `test_reducao_ordena_por_semana_com_snapshot_date_congelado` · `test_reducao_ordena_por_semana_com_snapshot_date_regressivo` (datas `2026-01-20`/`27`/`06`: ordenar por `snapshot_date` escolheria a semana 2 e o hex errado) |
| **CA-3c** reduzir → filtrar (**P2**) | `test_reclassificacao_para_marca_conhecida_exclui_a_chave` · `test_reclassificacao_para_independente_inclui_a_chave` |
| **CA-4** contrato de 10 colunas | `test_schema_10_colunas_em_ordem_e_dtypes` + 9 testes `test_assert_schema_rejeita_*` (coluna extra, ordem trocada, coluna faltante, hex fora da res-7, hex duplicado, `n_agregadores` fora do domínio, `fontes_presentes` inconsistente, **nulabilidade inconsistente**, versão inesperada, semana fora do ISO) |
| **CA-5** trava contra score adiantado | `test_extrator_nao_produz_score` · `test_assert_schema_falha_se_alguem_adiantar_o_score` (**parametrizado nas 5 colunas**, `match="SCORE"`) |
| **CA-6** limite do "0 agregadores" | `test_n_agregadores_no_hex_nunca_e_zero` · `test_hex_sem_observacao_nao_emite_linha` · `test_docstring_registra_o_limite_do_zero_agregadores` (assere `"0 agregadores"` **e** `"sinal 3"` no `__doc__`) |
| **CA-7** isolamento AST compartilhado | `test_snapshots.py::test_isolamento_imports` (tupla estendida com `mpresenca`) |
| **CA-8** zero PII / zero CSV cru | `test_saida_sem_coluna_de_pii` · `test_modulo_nao_le_csv_cru` (5 literais montados por concatenação, para o teste não se auto-acusar) |
| **CA-9** constante replicada, sem import | `test_modulo_nao_importa_demanda_revelada` (**AST próprio do módulo novo** — a tupla compartilhada não prova isto) · `test_categoria_independente_replicada_bate_com_a_origem` (só o TESTE importa a origem) |
| **CA-10** acentuação/ASCII | `test_contrato.py::test_identificadores_do_contrato_sao_ascii` (alvos estendidos) · `test_contrato.py::test_fontes_agregadores_e_subconjunto_de_fontes_validas` |
| **CA-11** delegação de I/O + reuso | `test_extrator_exige_base_dir_xor_snapshots` · `test_reusa_montar_serie_longa_do_churn` (identidade de objeto) · `test_roundtrip_particoes_no_disco` (3 semanas gravadas com `escrever_particao_semana` em `tmp_path`) · `test_base_vazia_frame_vazio_bem_formado` |
| **CA-12** relógios de frescor (R3) | `test_gap_de_fonte_nao_apaga_cobertura` (TP só na semana 1 → 2 agregadores, `_totalpass == "2026-01"`, `_wellhub == "2026-03"`) · `test_frescor_nulo_sse_contagem_zero` · `test_snapshot_date_ultimo_e_o_da_ultima_semana_da_fonte` |
| **CA-13** emenda §8.1 no mesmo PR | itens a–d escritos; 3 ocorrências de `emenda BLK-MA-03` no doc |
| **CA-14** backlog estruturado | bloco antes de `### BLK-ATR-05`; `garimpeiro_select_block.py --list` não o lista; 0 CRLF |
| **CA-15** suíte sem regressão | `2230 = 2186 + 44` coletados; `ruff` limpo |
| **CA-16** READ-ONLY M1 pelo diff | `loop_guard --stdin` sem `critico`; `git status` sem nada de M1 |
| — ordem canônica | `test_fontes_presentes_em_ordem_canonica` (linhas WH antes das TP ainda produzem `"totalpass,wellhub"`) |
| — bordas herdadas | `test_serie_exige_semana_iso_e_chave_unica` (sem `semana`; `"2026-1"`; chave duplicada) |

## Desvios do plano (2, ambos mínimos, com justificativa)

1. **Título do bloco no backlog: `presença` em vez de `presenca`.** O texto do Planner grafava o
   título sem acento; escrevi acentuado. Motivo: CLAUDE.md §2 e o próprio CA-10 exigem prosa
   acentuada, e o restante do bloco (escrito pelo Planner) já é acentuado — manter uma única palavra
   sem acento seria uma regressão de acentuação introduzida de propósito. **Nada mais** do texto do
   Planner foi alterado, no backlog ou na emenda.
2. **`fillna("")` na comparação de `fontes_presentes_no_hex` dentro do `_assert_schema`.** Endurecimento
   não previsto no plano: sem ele, um `fontes_presentes_no_hex` nulo produziria `pd.NA` na comparação
   e a soma booleana mascarada **não** acusaria a inconsistência (falha silenciosa da checagem `j`).
   Não muda nenhum comportamento do caminho feliz.

Além disso, dois **acréscimos** dentro do espírito do plano, ambos aditivos: (a) o docstring do
`__init__.py` foi atualizado de "dois módulos" para os três atuais (ficaria factualmente errado após
este bloco); (b) um teste a mais que o plano listou nominalmente —
`test_assert_schema_rejeita_nulabilidade_inconsistente` — para cobrir a regra (k) do passo 6 com
teste próprio.

## Problemas encontrados

- **`python scripts/loop_guard.py` sem argumento não roda** (argparse exige `--base` ou `--stdin`).
  Contornado rodando os dois modos; ver a nota na tabela de validações. Vale registrar porque o
  enunciado do ciclo (e o `Critério de aceite` do bloco) cita a forma nua.
- Nenhum outro. Nenhum bloqueio, nenhuma decisão de escopo precisou ser reaberta.

## Pendências

- **QA (opus)** — gate único da suíte COMPLETA (`python -m pytest -q`, serial: `pytest -n auto` não
  roda neste ambiente, `WinError 6` com xdist 3.8.0 + Python 3.14).
- **Passo 6 em MERGE-HUMANO** — `tasks/completed.md` e o commit por path são do fechamento, não do
  Builder. **Não commitei, não criei branch, não abri PR, não rodei housekeeping**, conforme a
  instrução.
- Herdadas, sem dono neste bloco: **G2** (score de dois valores no ramp-up) é do **BLK-MA-04**; a
  flag formal de staleness é do **BLK-MA-06**; os itens do `BLK-MA-02-FU1` seguem abertos.

## Riscos remanescentes

- **R2' (ALTO, mitigado)** — viés do grão hex: todas as academias de um hex recebem a mesma leitura.
  Mitigado pela emenda escrita no contrato + sufixo `_no_hex` nas duas colunas que o carregam.
  Direção do erro é falso NEGATIVO (segura para funil de aquisição).
- **R1' (ALTO, aceito e reenquadrado)** — `v1 = 1,0` inalcançável: vacuoso no grão hex e redundante
  com o S3. Sem fonte de universo no repositório.
- **G2 (ALTO, de PRODUTO, dono = BLK-MA-04)** — `score_vulnerabilidade ∈ {0, 50}` durante o ramp-up.
  Ciência registrada pelo gate; nenhuma antecipação autorizada.
- **R3' (MÉDIO, parcialmente mitigado)** — gap de feed: cobertura não é apagada e os 2 relógios por
  fonte estão no contrato, mas se AMBAS as fontes ficarem sem rodar por mais de `RETENCAO_SEMANAS = 26`
  a partição some por poda e o hex deixa de existir na saída.
- **R6' (BAIXO, deliberado)** — acoplamento a `_montar_serie_longa` (nome privado de módulo irmão).
  É a intenção (validação única), travado por teste de identidade para que uma quebra apareça como
  falha e não como divergência silenciosa.
- **Insumo real inexistente** — o cron mensal dos agregadores segue pendente; todo o bloco é validado
  por fixture sintética. Nenhum número de produção foi produzido ou alterado.

## Guardrails verificados

- `score_priorizacao` não alterado: **sim** (nada do M1 no diff; `loop_guard` sem `critico`).
- Artefatos M1 preservados: **sim** (o módulo não escreve NADA em disco — não há `escrever_*`).
- Dashboard offline mantido: **sim** (`import streamlit_app` → `import ok`; nenhuma dependência nova,
  nenhuma chamada de API ao vivo).
- Anti-PII (DEC-012): **sim** — só agregados por hex; saída sem nenhuma coluna de
  `COLUNAS_PII_PROIBIDAS`; o módulo não referencia leitura de feed bruto nem o classificador de rede;
  fixtures 100% sintéticas; nenhum teste lê fonte real (e `test_sem_caminho_real_nos_testes`, que
  varre por AST todos os `test_*.py` do diretório, passa).
- Acentuação (§2): **sim** — prosa e docstrings acentuados (inclusive no arquivo de teste novo);
  identificadores, colunas, enums (`totalpass`, `wellhub`, `independente`, `presenca_agregador_v1`) e
  nomes de arquivo 100% ASCII, travados pela varredura estendida em `test_contrato.py`.
- CSV/Parquet (§2): **não aplicável** — este bloco não materializa artefato algum.
- DEC-017 (LF): **sim** — 0 CRLF em `tasks/backlog.md` e em `docs/vulnerabilidade_ma_contrato.md`.
- Fora de escopo respeitado: `concorrentes_densos.py` (D1-B deferida pela decisão S1),
  `snapshots.py`/`churn_staleness.py` (importados, nunca editados), `scripts/loop_guard.py`
  (`vulnerabilidade/` já está em `_DENY_GOVERNANCA` desde a D5 do MA-02 — nenhuma edição foi
  necessária), `.gitignore`, `pyproject.toml`, `PRD.md`, `graphify-out/*`.
