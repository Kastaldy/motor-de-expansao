# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-OPS-11 — Pinar dependências e restaurar paridade CI/local (CI vermelho nos testes)

## Aprovação humana
APROVADO POR Felipe Silva EM 2026-05-31 (abordagem HÍBRIDA: B cura o `config.py` + A pina deps +
ajuste no `ci.yml`). Executado EXATAMENTE o "Plano técnico" do handoff do Planner, passos 1–7.

## O que foi feito
1. **Cura (Opção B) em `config.py`:** no `__init__` do RAMO FALLBACK de `Settings` (executado no CI,
   onde `pydantic_settings` não está instalado por vir só no extra `api`), inseri o guard
   `if isinstance(default, property): continue` ANTES do `setattr`. Isso impede o `setattr` sobre a
   `@property CORES` (nome `isupper()`), que causava `AttributeError: property 'CORES' ... has no
   setter` e derrubava a collection de 12 módulos via `config` → `core/constants.py`. Ramo pydantic,
   dict `CORES` e `_coerce_env_value` intocados.
2–4. **`pyproject.toml`:** tetos de runtime (`streamlit<2`, `pandas<3`, `pyarrow<24`, `numpy<2.4`,
   `scikit-learn<1.8`) e faixas das ferramentas dev (`ruff>=0.15,<0.16`, `mypy>=1.10.0,<2`,
   `pytest>=8.2.0,<10`, `pytest-asyncio>=0.23.0,<0.25`). Floors preservados. Extra `api` NÃO tocado;
   `mypy` NÃO pinado em `==2.1.0`.
5. **`.github/workflows/ci.yml`:** step de lint passou a `ruff check . --no-cache` (cache dava
   falso-verde). `pip install -e ".[dev]"` mantido; extra `api` NÃO adicionado (o fallback é o
   caminho suportado/testado).
6. **NOVO `tests/unit/test_config_fallback.py`:** 2 testes que forçam o ramo fallback (shim de
   `builtins.__import__` levantando `ModuleNotFoundError` para `pydantic_settings` + `importlib.reload`)
   e validam (a) `BaseSettings is None`, `Settings()` instancia, `CORES["purple"]=="#6B21A8"`,
   `len(CORES)==9`; (b) env-override `H3_RESOLUTION=9` → `int` 9. Fixture restaura
   `builtins.__import__` e `sys.modules['pydantic_settings']` e recarrega o config no ramo normal no
   teardown (sem contaminar a suíte). Nada silenciado.
7. **Tarefas:** corrigida a "Causa raiz" do bloco BLK-OPS-11 em `tasks/backlog.md` (a quebra do CI é
   o ramo FALLBACK, não pydantic novo; pin é higiene de paridade). `tasks/current_task.md` →
   Skill atual: Builder / Status: aguardando QA / Próxima: QA. NÃO movi blocos backlog→completed
   (isso é do orquestrador no fechamento).

## Arquivos alterados
- `src/motor_expansao/config.py` — guard `if isinstance(default, property): continue` no `__init__`
  do ramo fallback de `Settings` (1 mudança mínima; ramo pydantic/`CORES`/`_coerce_env_value`
  inalterados).
- `pyproject.toml` — tetos em runtime (streamlit/pandas/pyarrow/numpy/scikit-learn) e faixas nas
  ferramentas dev (ruff/mypy/pytest/pytest-asyncio). Extra `api` intocado.
- `.github/workflows/ci.yml` — step Lint: `ruff check .` → `ruff check . --no-cache`.
- `tests/unit/test_config_fallback.py` — NOVO; 2 testes que blindam o ramo fallback (CORES + env).
- `tasks/backlog.md` — corrigida a nota de "Causa raiz" do bloco BLK-OPS-11 (apenas esse bloco).
- `tasks/current_task.md` — Skill atual: Builder; Status: aguardando QA.
- `context/handoff.md` — este handoff.
- `context/handoff/20260531-155543-builder.md` — cópia append-only idêntica.

## Validações executadas (saída literal)
- `ruff check . --no-cache`:
  ```
  All checks passed!
  ```
- `mypy src/`:
  ```
  Success: no issues found in 46 source files
  ```
  (apenas notes `annotation-unchecked` informativos; 0 erros. Local tem `types-requests`? lição de
  paridade mypy registrada — QA deve confirmar no CI 3.11 que nenhum stub faltante gera falso-verde.)
- `python -m pytest -q tests/unit/test_config_fallback.py`:
  ```
  ..                                                                       [100%]
  2 passed in 0.91s
  ```
- `python -m pytest -q` (suíte completa):
  ```
  626 passed, 1 skipped, 9 warnings in 207.20s (0:03:27)
  ```
  0 falhas, 0 erros de collection. (Baseline histórico do CLAUDE.md era 532; o número subiu por
  testes adicionados em ciclos posteriores + os 2 novos deste bloco. Ambiente local Python 3.14.)
- `python -m pytest -q tests/integration/test_streamlit_app.py`:
  ```
  150 passed in 22.06s
  ```
- `python -c "import streamlit_app; print('import ok')"`:
  ```
  import ok
  ```
- **PROVA DA CURA no estado do CI (fallback forçado APÓS a correção):**
  ```
  BaseSettings is None: True
  CORES len: 9 purple: #6B21A8
  constants reload ok under fallback
  ```
  e import dos 12 módulos antes afetados sob fallback:
  ```
  12/12 previously-affected test modules import under FALLBACK with no AttributeError
  ```
  Comprova que a collection não quebra mais sem `pydantic_settings`.

## Problemas encontrados
- Nenhum bloqueante. Observação: a `@property CORES` está duplicada (ramos pydantic e fallback) no
  `config.py` — fora de escopo deduplicar agora; a cura não exigiu mexer no ramo pydantic.

## Pendências
- **Confirmação no CI real (GitHub Actions, Python 3.11):** as versões REAIS que o CI 3.11 vai
  resolver dentro das faixas pinadas NÃO foram verificadas localmente (Python local = 3.14, difere
  do CI). QA/orquestrador DEVE confirmar o run verde de ponta a ponta (Lint `--no-cache` → mypy →
  Testes → Smoke) via `gh run watch` no fechamento, e validar que `pip install -e ".[dev]"` resolve
  as faixas (especialmente `ruff>=0.15,<0.16` e `mypy>=1.10.0,<2`) no 3.11.

## Riscos remanescentes
- **Paridade Python:** local 3.14 vs CI 3.11 permanece (fora de escopo). Contagens podem diferir; a
  cura (guard de property) é independente de versão.
- **Tetos amplos** (`<3`, `<2.4`) podem esconder incompat futura (dívida adiada controlada); a cura
  real não depende de versão.
- **mypy/ruff floor:** floors em faixa segura pública; se o CI 3.11 não encontrar versão na faixa, o
  `pip install` reprova — QA confirma no run real.

## Guardrails verificados
- score_priorizacao não alterado: sim (bloco é ambiente/CI/config; `scoring.py` e fórmula intocados).
- Artefatos M1 preservados: sim (nenhum artefato regerado; nenhum recálculo de score).
- Dashboard offline mantido: sim (sem dependência de API ao vivo; smoke import OK; 150 testes
  streamlit verdes).
- Sem skip/xfail amplo, sem bypass, sem commit/`git add` (orquestrador commita por path no
  fechamento); VPS não tocado.
