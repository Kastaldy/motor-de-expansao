# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Approver (gate humano) — esta lista de ajustes deve ser APROVADA item a item por Vinicius antes do Builder.

## Entendimento da tarefa
Levantar e propor uma lista CONCRETA e priorizada de ajustes de estética e clareza/simplificação de conteúdo do dashboard Streamlit de produção (READ-ONLY sobre o M1), organizada por aba + global/tema + sidebar, para o humano aprovar/rejeitar item a item no gate.

## Observação importante de escopo (ler antes de aprovar)
O handoff do Block Orchestrator e o `current_task.md` falam em **4 abas** (Visão Executiva, Mapa Territorial, Expansão de Domínio, Carteira e Plano). A produção REAL (`pages.py::DASHBOARD_TAB_LABELS`) tem **5 abas**, nesta ordem e com estes rótulos exatos (acentuados):
`["Mapa", "Executivo", "Expansão de Domínio", "Carteira e Plano", "Viabilidade"]`.
- "Mapa" = a antiga "Mapa Territorial" (`render_mapa_territorial`), que hoje também contém, em expanders internos, a Analise Territorial, o Ranking, a Camada Hibrida (`render_modelo_hibrido_v2`), a Analise Pontual, o Relatorio Pontual Censitario e o Relatorio Municipal.
- "Executivo" = a antiga "Visão Executiva" (`render_visao_executiva`).
- "Viabilidade" é uma 5a aba (`render_viabilidade_ponto`) que NÃO consta do handoff.
Este plano cobre **as 5 abas** (a Viabilidade entra como aba real do produto). Se o humano quiser restringir o ciclo às 4 originais, basta rejeitar os itens da seção "Aba 5 — Viabilidade".

Há ainda uma duplicação de código: `render_modelo_hibrido` (legado, não chamado por `main()`) e `render_modelo_hibrido_v2` (o que roda). Os cartões "Como interpretar os tres modelos" existem em ambos. Sinalizado abaixo, mas a remoção do morto fica FORA deste ciclo de UI (é limpeza de código, não estética) salvo aprovação explícita.

---

## Como ler esta lista
Cada item: **[ID]** O quê (problema) → mudança proposta (arquivo · função · o que muda) — **Categoria** — **Testes** — **Esforço**.
Categorias: `[estética]` `[clareza/simplificação]` `[remoção de ruído]`.
Testes: "nenhum" = não toca asserts; "ATUALIZAR <teste>" = exige editar um assert literal em `tests/integration/test_streamlit_app.py`.
Prioridade: **QUICK WINS** (baixo esforço, baixo risco) primeiro; depois **MÉDIO**; depois **MAIOR/decisão de produto**.

---

## A. GLOBAL / TEMA  (`dashboard/pages.py::inject_styles`, `streamlit_app.py`, `dashboard/constants.py::COLORS`)

### QUICK WINS
- **[G1]** Acentuação ausente em todo o app (texto "Censitario", "Hibrido", "Priorizacao", "Populacao", "Analise", "Area", "municipios"). A UI é quase toda sem acento. → Padronizar a acentuação correta nas strings VISÍVEIS de captions/headers/legendas/cards (não em chaves de dados nem nomes de coluna). **Decisão de produto:** fazer "tudo de uma vez" é alto volume e mexe em muitos asserts; recomendo subconjunto seguro (headers `st.markdown("#### …")` e captions que NENHUM teste fixa). — `[estética/clareza]` — **Testes: vários asserts fixam substrings SEM acento** (`"Hibrido"`, `"disponivel"`, `"coordenada"`, `"base setorial"`, `"Expansao de Dominio"` em L1903/L2392/L4377/L4406/L1960). ATUALIZAR caso a caso, ou (mais seguro) só acentuar strings não-testadas. — Esforço: médio (alto se "tudo").
- **[G2]** `render_header()` (hero): título técnico "Dashboard Executivo M1 + Modelo Hibrido" + 3 pills "Onde expandir (M1)" / "Qual bairro (Censitario)" / "Fila operacional (Hibrido)". É jargão interno. → Suavizar para linguagem de operador: título "Ultra Academia — Inteligência de Expansão" + subtítulo curto; manter as 3 pills com acento. — `[clareza/estética]` — **Testes: nenhum** fixa o texto do hero. — Esforço: baixo.
- **[G3]** `block-container` `padding-top: 1.4rem` + hero grande + barra sticky empurram muito o conteúdo "above the fold". → Reduzir `padding-top` (~0.8rem) e o `padding`/`margin` do `.hero` (hoje `1.35rem 1.5rem`, `margin-bottom 1rem`) para um hero mais enxuto. — `[estética]` — **Testes: nenhum.** — Esforço: baixo.
- **[G4]** `st.set_page_config(page_title="Ultra Academia | Mapa Territorial")` em `streamlit_app.py` — título da aba do navegador desatualizado (aba default é "Mapa"). → Trocar para "Ultra Academia | Expansão". — `[clareza]` — **Testes: nenhum** (testes de tema checam `.streamlit/config.toml`, não `page_title`). — Esforço: baixo.

### MÉDIO
- **[G5]** Excesso de gradientes radiais no `.stApp` (3 radiais + linear) e sombras pesadas (`box-shadow: 0 22px 48px`) deixam o fundo "ruidoso". → Reduzir para 1 radial sutil + o linear; baixar sombras dos cards (`0 16px 36px` → `0 8px 20px`). Mantém o tema escuro coeso. — `[estética/remoção de ruído]` — **Testes: nenhum** lê esses seletores (ver GUARDRAIL de CSS). — Esforço: médio.
- **[G6]** `.section-card { min-height:160px }` gera caixas com muito espaço vazio quando têm poucas linhas. → Trocar `min-height` por `padding` consistente; altura segue o conteúdo. — `[estética]` — **Testes: nenhum.** — Esforço: baixo.

> **GUARDRAIL de CSS (não quebrar):** `test_inject_styles_tab_selector_sticky` (L4625) e `test_inject_styles_cobre_componentes_baseweb` (L5013) exigem que o CSS CONTINUE contendo: `position: sticky`, `[data-testid="stLayoutWrapper"]:has(> .st-key-nav_search_bar)`, `.st-key-dashboard_active_tab`, `top: 0`, `stMainBlockContainer`, `stSegmentedControl`, `data-baseweb="popover"`, `data-baseweb="select"`, `aria-checked="true"`, `#19B7FF`, `stBaseButton-segmented_controlActive`/`stBaseButton-segmented_control`, `data-baseweb="button-group"`, `gap: 8px`, `margin: 0 !important`, `data-testid="stDownloadButton"`, `.st-key-btn_gerar_pdf_topo`, `width: 260px`. Qualquer refactor de `inject_styles` deve PRESERVAR esses tokens. A cor de aba ativa `#19B7FF` é travada por teste — não trocar sem ATUALIZAR L5029.

---

## B. SIDEBAR / FILTROS GLOBAIS  (`pages.py::render_uf_selectbox`, `render_sidebar_filters`, `render_coord_search_sidebar`)

> Nota: apesar do nome, esses controles vivem HOJE no CORPO (não na sidebar) desde BLK-UI-07. A sidebar do app está praticamente vazia.

### QUICK WINS
- **[S1]** `render_uf_selectbox` injeta bloco HTML "Filtros globais / Recorte executivo M1 + Hibrido" + um `st.caption` redundante ("Refine o recorte executivo do M1 e da camada hibrida sem alterar o score oficial."). Duas mensagens dizendo o mesmo. → Manter só "Filtros globais" e remover/fundir o caption redundante. — `[remoção de ruído]` — **Testes: nenhum** (único teste de `render_uf_selectbox` checa 1o arg do selectbox == `"UF"`, L4512 — preservado). — Esforço: baixo.
- **[S2]** O `help` do campo de busca (`render_coord_search_sidebar`) tem ~6 linhas explicando 4 formatos; o `placeholder` já resume. → Encurtar o `help` (1-2 linhas) sem remover capacidade. — `[clareza/simplificação]` — **Testes: nenhum** (teste só fixa `key="coord_search_input"`, L4578). — Esforço: baixo.
- **[S3]** `render_sidebar_filters` → expander "Filtros avancados" repete DUAS captions de "M1 = decisao municipal / Censitario = intraurbana / Hibrido = combinado" (linhas 682 e 706). → Manter uma só. — `[remoção de ruído]` — **Testes:** `test_render_sidebar_filters_*` (L4520) checa tuple de 8 retornos e ≥2 multiselects — preservado. — Esforço: baixo.

---

## C. ABA "MAPA" (`render_mapa_territorial` + expanders internos)

### QUICK WINS
- **[M1]** Caption do topo do Mapa Territorial é longa e mistura 3 mensagens (instrução + guardrail + dica). → Quebrar: 1 linha de instrução curta + a "Dica" mantida; mover o guardrail "não altera score…" para `help`/caption menor. — `[clareza/remoção de ruído]` — **Testes: nenhum** fixa essa caption. — Esforço: baixo.
- **[M2]** `_CENTROID_DISCLAIMER` e o caption "Clique em um hexagono… Botao direito nao e suportado" aparecem repetidos (fragment + analise pontual single + multihex). → Centralizar a nota de centroide num único ponto por aba. — `[remoção de ruído]` — **Testes: nenhum** fixa essas strings. — Esforço: baixo.
- **[M3]** Rótulos de legenda longos/técnicos: `render_score_bands_legend("Score Residual (score_oportunidade_residual)")` mostra o nome da COLUNA cru. → Simplificar o `mode_label` visível para "Score Residual" / "Score Censitário" / "Score M1" / "Score Híbrido". — `[clareza/simplificação]` — **Testes: nenhum** fixa o texto passado a `render_score_bands_legend`. **CUIDADO:** o teste de tooltip fixa `"Score Censitario 2022:"` no HOVER do mapa (L1799) — é OUTRO texto (tooltip do builder), NÃO mexer. — Esforço: baixo/médio (várias chamadas).

### MÉDIO
- **[M4]** Densidade alta de expanders na aba Mapa: "Analise Territorial", "Ranking de Priorizacao", "Camada Hibrida — Detalhe", "Analise Pontual de Entorno", "Relatorio Pontual Censitario", "Relatorio Municipal" — 6 empilhados sob "Detalhamento territorial". → Agrupar visualmente: sub-cabeçalho separando "Leitura territorial" (Análise+Ranking+Híbrida) de "Análise de ponto" (Pontual+Censitário+Municipal). Sem mudar lógica de render nem os labels testados. — `[clareza/estética]` — **Testes:** `test_render_mapa_territorial_com_city_summary_renderiza_expanders` (L2459) exige um expander com label contendo `"Analise"` e outro `"Ranking"` — manter esses dois. — Esforço: médio.
- **[M5]** `render_modelo_hibrido_v2`: o bloco "Como interpretar os tres modelos" (3 model-cards densos) + 5 KPIs + 5 subtabs é muito conteúdo para um expander de detalhe. → Enxugar os 3 cards (4-6 → 2-3 linhas); revisar se os 5 KPIs cabem em 1 linha. — `[clareza/simplificação]` — **Testes:** `test_build_hybrid_kpis_*` fixa VALOR `"2 -> 2"` (L236) — não muda (é dado). Nenhum teste fixa o texto dos cards. — Esforço: médio.

---

## D. ABA "EXECUTIVO" (`render_visao_executiva`)

### QUICK WINS
- **[E1]** Caption do mapa de presença Ultra é longa ("Mapa institucional da rede propria. Exibe apenas unidades Ultra…. Para analise de hexagonos, scores e residual, use a aba Mapa Territorial."). → Encurtar para 1 linha. — `[remoção de ruído]` — **Testes: nenhum.** — Esforço: baixo.
- **[E2]** Dois blocos de "answer cards" muito parecidos: topo ("Onde expandir / Quais cidades priorizar / Onde evitar expansao") e, mais abaixo, ("UFs a priorizar" + gráficos). `build_business_answers` é chamado 2x. → Manter o trio do topo como resumo executivo e remover/fundir o trio redundante de baixo. — `[remoção de ruído]` — **Testes: nenhum** fixa os títulos dos answer cards (passados em `pages.py`). — Esforço: baixo.
- **[E3]** Títulos de seção sem acento ("#### Presenca Ultra Academia", "#### Rede Ultra e Mercado", "#### Analise Comparativa por UF"). → Acentuar + padronizar Title case PT-BR. — `[estética/clareza]` — **Testes: nenhum** fixa esses headers. — Esforço: baixo.

### MÉDIO
- **[E4]** Muitos gráficos plotly empilhados (residual por UF, dist. residual, top cidades, top city, top uf, 3 figuras de UF) — aba longa, "parede de gráficos". → Agrupar em 2 blocos com sub-cabeçalho ("Rede e mercado" / "Comparativo por UF") e usar `st.columns` para reduzir scroll. Sem tocar os builders. — `[clareza/estética]` — **Testes: nenhum.** — Esforço: médio.
- **[E5]** Títulos internos dos gráficos plotly têm nomes de coluna crus ("Distribuicao de score_priorizacao", "Renda per capita x populacao proxy"). → Trocar por títulos legíveis ("Distribuição de score", "Renda × população"). Vem de `components.py::build_*_figure` (argumento `title=`/labels — área de TEXTO de figura, permitida pelo escopo). — `[clareza]` — **Testes: nenhum** fixa esses títulos de figura. — Esforço: baixo/médio (várias figuras). **Sinalizar ao humano:** toca `components.py` (permitido), só `title=`/labels — zero lógica.

---

## E. ABA "EXPANSÃO DE DOMÍNIO" (`render_expansao_dominio`)

### QUICK WINS
- **[D1]** Caption do topo longa (3 linhas) e 2 `section-card` no rodapé ("Como ler o plano de dominio" + "Teses de dominio") repetem definições já presentes na coluna "Tese" e na legenda. → Encurtar a caption; manter só UM card-glossário de teses. — `[remoção de ruído]` — **Testes: nenhum** fixa esses textos; `test_render_expansao_dominio_exibe_tabela_com_dados` (L1936) só checa headers `"Hex Ancora"`/`"Tese"` (preservados). — Esforço: baixo.
- **[D2]** "Ancoras coloridas por ordem de abertura (cyan = primeira, azul = posterior)" — caption + legenda dizem o mesmo. → Manter na legenda (`render_dominio_tese_legend`), remover da caption. — `[remoção de ruído]` — **Testes: nenhum.** — Esforço: baixo.

---

## F. ABA "CARTEIRA E PLANO" (`render_carteira_e_plano` → `render_carteira_expansao` + `render_plano_expansao`)

### QUICK WINS
- **[C1]** Captions longas e 2 `section-card` de rodapé em CADA sub-aba (Carteira: "Como usar esta carteira" + "Restricoes e rastreabilidade"; Plano: idem). Muito texto técnico-jurídico. → Enxugar as captions; condensar os cards (≤4 bullets cada). — `[clareza/simplificação]` — **Testes: nenhum** fixa esses textos. — Esforço: baixo.
- **[C2]** Headers "Carteira de Expansao — Decisao pratica de onde abrir" e "Plano de Expansao — Curto Prazo" sem acento. → Acentuar. — `[estética]` — **Testes: nenhum.** — Esforço: baixo.
- **[C3]** Labels jargão "Score Hibrido (apoio)", "Dens. < 5k", "Join Restrito". **CUIDADO:** alguns headers de tabela são fixados por teste. → Revisar SÓ os labels não-testados. — `[clareza]` — **Testes:** `"Dens. < 5k"` em assert hibrido (L1112-1118) e `"Consumo Conc. (est.)"`/`"Consumo Ultra (real)"` em domínio (L4140-4143) — NÃO renomear esses. — Esforço: baixo (se evitar os testados).

### MÉDIO
- **[C4]** A barra de filtros da Carteira tem 5 colunas (`st.columns([1.4,2.2,1.2,1.8,1.8])`: UF, Município, Prioridade, Quartil residual, Ordenação) — densa. → Mover "Quartil residual" e "Ordenação" para um expander "Opções avançadas", deixando UF/Município/Prioridade na linha principal. — `[clareza/estética]` — **Testes: nenhum** fixa a contagem de colunas da carteira. — Esforço: médio.

---

## G. ABA "VIABILIDADE" (`render_viabilidade_ponto`)  — 5a aba real

### QUICK WINS
- **[V1]** Repetição do guardrail "a demanda é premissa, nunca prevista pela geografia" — aparece na caption do topo, em 2 captions do form e na Seção 3. → Manter UMA vez com destaque (a do form, perto do input de demanda) e remover as duplicatas. — `[remoção de ruído]` — **Testes: nenhum** fixa essas captions; `test_render_viabilidade_ponto_*` (L5414) só checa que a aba existe em `DASHBOARD_TAB_LABELS`. — Esforço: baixo.
- **[V2]** Headers `##### Viabilidade no cenario pedido`, `##### Faixa de alunos plausivel pela metragem (comparaveis, nao pela localizacao)` — longos/sem acento. → Encurtar + acentuar ("Faixa de alunos pela metragem"). — `[clareza/estética]` — **Testes: nenhum.** — Esforço: baixo.

### MÉDIO
- **[V3]** Grid 2x2 de 4 gráficos + grade de sensibilidade + 8 cards + form com expander = aba longa. → Já razoável; sugestão menor: agrupar os 4 gráficos sob 1 sub-cabeçalho "Projeção financeira" (já existe) e garantir alturas consistentes (já `_CHART_H=300`). Baixa prioridade. — `[estética]` — **Testes: nenhum.** — Esforço: médio.

---

## Resumo de priorização (para o gate)
- **QUICK WINS (recomendados, baixo risco, zero/poucos testes):** G2, G3, G4, S1, S2, S3, M1, M2, E1, E2, E3, D1, D2, C1, C2, V1, V2. (~17 itens — o grosso do "polimento" com risco mínimo.)
- **MÉDIO (mais trabalho ou toca `components.py` só em texto):** G1 (subset de acentuação), G5, G6, M3, M4, M5, E4, E5, C3, C4, V3.
- **DECISÃO DE PRODUTO / fora do polimento puro (sinalizar, não fazer sem OK explícito):** G1 "acentuar tudo" (alto volume + muitos asserts); remoção do `render_modelo_hibrido` legado morto (limpeza de código, não estética).

---

## Arquivos afetados (se aprovados)
- `src/motor_expansao/dashboard/pages.py` — `inject_styles`, `render_header`, `render_uf_selectbox`, `render_sidebar_filters`, `render_coord_search_sidebar`, `render_mapa_territorial`, `render_visao_executiva`, `render_expansao_dominio`, `render_carteira_expansao`, `render_plano_expansao`, `render_modelo_hibrido_v2`, `render_viabilidade_ponto` (SÓ textos/labels/layout).
- `src/motor_expansao/dashboard/components.py` — SÓ `title=`/labels de figuras e textos de legenda (E5, M3 se mexer na legenda); ZERO lógica de cálculo/tooltip de dado.
- `src/motor_expansao/dashboard/constants.py` — SÓ se algum label de `COLOR_MODES`/`OVERLAYS` for renomeado (opcional, não proposto por padrão); NÃO tocar cores/listas de cálculo.
- `streamlit_app.py` — SÓ `st.set_page_config(page_title=...)` (G4).
- `tests/integration/test_streamlit_app.py` — ATUALIZAR apenas os asserts literais dos itens aprovados que toquem strings/cores testadas.

## Dependências
- Nenhuma dependência de dados/pacote nova. Tudo consome o que já existe. Sem rede nova (DEC-004/010/011 inalteradas).

## Riscos técnicos
- `pages.py`/`components.py` misturam lógica de cálculo e visual no mesmo arquivo: risco de editar vizinho de cálculo. Mitigação: Builder só altera strings/CSS/layout listados; nada de `build_map_figure`/`build_*` que retornam dados/camadas.
- Acentuação em massa (G1) pode bater em muitos asserts de substring SEM acento — fazer por subconjunto não-testado ou atualizar teste a teste.
- `inject_styles` injeta CSS global: preservar os tokens do GUARDRAIL de CSS (G5/G6) para não quebrar `test_inject_styles_*`.
- Tooltips de mapa (hover) são fixados por teste e NÃO são alvo deste ciclo — não confundir "rótulo de legenda" (alvo) com "linha de tooltip do builder" (intocável).
- Cor de aba ativa `#19B7FF`, faixa "alta" `#F59E0B`/`[245,158,11,140]`, borda municipal `[245,158,11,220]`, fills descartado/NaN e bandas absolutas renda/densidade são TRAVADOS por teste — não recolorir sem aprovação + atualização de teste.

## Critérios de aceite finais
- As **5 abas** (Mapa, Executivo, Expansão de Domínio, Carteira e Plano, Viabilidade) seguem funcionais sem erro de render/import.
- Apenas os itens APROVADOS no gate foram aplicados (item a item).
- `score_priorizacao`, `hex_score_estrutural`, pesos 0.40/0.60 e artefatos M1 inalterados (READ-ONLY) — zero escrita em parquet/artefato.
- Contratos de performance Blocos 4–6 intocados: `load_uf_slice`, `read_enriched_uf_partition`, `build_dashboard_dataset`, `render_tab_selector`, `_downsample_map_index`, `build_pop_cut_lookup` sem modificação de assinatura/comportamento.
- Funções de cálculo (`build_map_figure`, `build_hybrid_map_figure`, `build_dominio_map_figure`, `build_unified_map_figure`, `agregar_cenario_multihex`, `analisar_entorno_ponto`, `lookup_hex_by_coord`) inalteradas.
- Tooltips de mapa (hover) inalterados (fixados por teste e fora de escopo).
- `DASHBOARD_TAB_LABELS` inalterado (lista/ordem/acentos travados por teste) salvo decisão explícita + atualização de L4236-4242.
- Nenhum path pré-sujo (`_metadata.json`, `_report.md` em `setores_censitarios_2022_geo/`) no commit do ciclo.

## Validações obrigatórias
- Comando: `python -m pytest -q tests/integration/test_streamlit_app.py` — Critério: 0 falhas (atualizando só os asserts dos itens aprovados).
- Comando: `python -c "import streamlit_app"` — Critério: import sem erro.
- Comando: `python -m pytest -q` (suíte completa, no fechamento do QA) — Critério: sem falhas novas vs baseline.
- Comando: `ruff check src/motor_expansao/dashboard streamlit_app.py` — Critério: limpo.
- Comando: `mypy src/motor_expansao/dashboard` — Critério: sem erros novos.

## Fora de escopo
- Score/pesos/artefatos M1 e qualquer função de cálculo/builder de dados ou camada de mapa.
- Contratos de performance Blocos 4–6.
- Tooltips de mapa (hover) e textos de tabela/legenda fixados por teste (a menos que o item aprovado explicite a atualização do teste).
- Remoção do código morto `render_modelo_hibrido` (legado) — limpeza de código, não estética; só com OK explícito.
- PoC BLK-UI-10 (trilha separada opt-in).
- Dependência de API ao vivo nova.
- Paths pré-sujos `data/outputs/setores_censitarios_2022_geo/_metadata.json` e `_report.md`.

## Alerta de aprovação
ATENÇÃO (Alta): este ciclo é READ-ONLY sobre o M1 (visualização/texto), mas a superfície são as **5 abas do dashboard de produção** em uso pelo time executivo, e várias mudanças tocam asserts de texto/cor. **Aprovação humana obrigatória antes do Builder — idealmente item a item** (a lista acima está estruturada para aprovar/rejeitar por ID). O humano (Vinicius) deve, no mínimo, decidir: (1) escopo da acentuação G1 (subset seguro vs "tudo"); (2) se inclui a aba Viabilidade (itens V*); (3) se autoriza tocar `title=`/labels de figuras em `components.py` (E5/M3); (4) quais itens de cor/label travados por teste podem mudar (com atualização de teste).

## Guardrails ativos
- §2: sem dependência de API ao vivo nova no dashboard (DEC-004/010/011 inalteradas).
- §3: `score_priorizacao`, `hex_score_estrutural`, pesos `renda=0.40`/`pop=0.60`, `H3_RESOLUTION=7`, `DIST_MIN_ULTRA_KM=1.0`, `RENDA_MIN=4500.0` — INALTERADOS.
- §5: visualizações/interações de mapa não recalculam nem alteram score, carteira, plano, plano domínio ou artefatos M1.
- Blocos 4–6 de performance: intocados.
- Interpretação de criticidade (2026-05-30): leitura/visual sem escrita em artefato M1 → Alta, revisão humana antes do Builder.
