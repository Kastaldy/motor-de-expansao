# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA

## O que foi implementado
BLK-DIM-01R — Calibração REAL da Camada 1 (aderência) com correção da endogeneidade.
Alvo trocado de `log(penetração)` para `log(pagantes_steady_state)` direto (remove o
`log(pop)` do denominador). R²_LOO honesto calculado em DOIS espaços (log e pagantes).

**Criados:**
- `src/motor_expansao/dimensionamento/aderencia.py` — módulo novo (esqueleto do spike
  adaptado). Alvo = `log(pagantes)`; features `[log(pop), log(renda)]` SEM scaler (log cruas);
  seleção de alpha por MENOR `rmse_loo_log`; `AderenciaModel` com campos do D7; `prever_aderencia`
  retorna ALUNOS absolutos com clamp `[PAGANTES_MIN, pop_captacao]`; `escrever_relatorio_aderencia_real`;
  bloco `__main__` que materializa o relatório real. Constantes: `LIMIAR_R2_GO=0.05`, `ALPHA_GRID`,
  `N_MIN_CALIBRACAO=5`, `PAGANTES_MIN=1.0` (REMOVIDOS `PENETRACAO_MIN/MAX`).
- `tests/unit/dimensionamento/test_aderencia.py` — 19 testes. Fixtures anti-circulares D5/D6
  (`df_log_sinal` gera `log(pagantes)` direto; `df_sem_sinal` pagantes constante → controle negativo).
- `data/analysis/aderencia_real.md` — relatório gitignored (NÃO commitado).

**Não tocado:** `config.py` raiz, `pipelines/m1/`, artefatos M1, parquet de calibração (read-only).

## Resultado das validações locais
- `ruff check` (módulo + teste): **All checks passed**.
- `mypy aderencia.py --ignore-missing-imports`: **Success: no issues found**.
- `pytest tests/unit/dimensionamento/test_aderencia.py -v`: **19 passed**.
- `pytest -q` (suite full): **852 passed, 4 skipped, 18 warnings** (0 failed). Warnings são
  FutureWarnings pré-existentes de pandas em `calcular_colunas_mercado.py`, alheios a este bloco.

## Veredito real nos dados (R²_LOO)
Rodado sobre `data/staging/base_calibracao_maduras.parquet` (54 linhas, 1 removida por outlier):
- `n_treinamento` = **53** (esperado pelo plano)
- `r2_loo_log` (PRINCIPAL, gate) = **-0.0134**
- `r2_loo_pagantes` (auditoria) = **-0.0609**
- `r2_insample_log` = +0.0515
- `alpha_selecionado` = 100 ; `coef_log_pop` = -0.0429 ; `coef_log_renda` = +0.0232
- **Veredito = NO-GO** ← resultado ESPERADO e VÁLIDO. Consistente com a previsão do BO
  (≈-0.028) e com a DEC-001 (M1 Spearman ≈ 0). NENHUM tuning foi feito para forçar GO.

## Desvios do plano (se houver)
Nenhum desvio material. Notas de implementação (escolhas oferecidas pelo plano):
- Sem `StandardScaler` (features log cruas), conforme recomendação explícita do Planner em D2.
- Controle negativo mantido com `pagantes` constante puro (D6, caminho `ss_tot>0 else 0.0`).
- 19 testes (acima dos 13 mínimos): incluí os obrigatórios + alguns do plano (alias, no-n_setores,
  relatório NO-GO, outlier catchment-vazio).

## Arquivos do ciclo (para commit por path)
- `src/motor_expansao/dimensionamento/aderencia.py`
- `tests/unit/dimensionamento/test_aderencia.py`
- `context/handoff.md`
- `context/handoff/20260613-170609-builder.md`
(NÃO commitar `data/analysis/aderencia_real.md` — gitignored, confirmado via `git check-ignore`.)

## Riscos remanescentes
- QA deve confirmar a suite full verde no ambiente de CI e re-rodar ruff/mypy.
- `test_calibrar_com_sinal_go` aceita GO ou NO-GO (não força GO); no fixture sintético atual dá GO.
- O bloco `__main__` lê o parquet real; não roda em teste (testes são offline/determinísticos).
- Confirmar que o diff não toca paths bloqueados pelo `loop_guard.py` (só `dimensionamento/` + testes).
