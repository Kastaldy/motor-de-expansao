# Handoff — QA

## Skill que gerou este handoff
QA / Quality Analyzer

## Próxima Skill recomendada
Block Orchestrator (fechamento do ciclo)

## Bloco
BLK-ATR-02 — Gate de viabilidade absoluto (população ≥ 5.000 E renda per capita ≥ 1.500)
na camada de mercado. Coluna PARALELA `flag_gate_atratividade`. READ-ONLY sobre o M1.

## Veredito
**APROVADO COM RESSALVA** (a ressalva é ambiental/infra, NÃO do bloco — ver abaixo).

## Validações executadas (independentes)

### 1. Suíte FULL — `python -m pytest -q`
Resultado: **1318 passed, 4 failed, 4 skipped, 18 warnings** (590,98s).
As 4 falhas são **pré-existentes e NÃO relacionadas ao BLK-ATR-02**:
- `tests/unit/test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline`
- `tests/unit/test_coord_search.py::test_resolve_plus_code_curto_usa_localidade_mockada`
- `tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline`
- `tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_curto_usa_localidade_mock`

**Causa raiz (verificada):** todas dependem da biblioteca `openlocationcode` (Plus Code,
introduzida no ciclo anterior BLK-UI-09-FU2 em `src/motor_expansao/api/maps_geocoder.py`).
`openlocationcode>=1.0.1` está DECLARADA como dependência base no `pyproject.toml` (linha 31),
mas **NÃO está instalada neste ambiente de QA** (`ModuleNotFoundError: No module named
'openlocationcode'`; `pip show openlocationcode` → not found). São falhas de AMBIENTE (pacote
não instalado), não regressões. O código do BLK-ATR-02 toca APENAS
`calcular_colunas_mercado.py` — nenhuma relação com o caminho de Plus Code/geocoder.

**Subconjunto impactado (100% verde):**
`test_calcular_colunas_mercado_gate.py` + `test_modelo_mercado_hexagonos.py` +
`test_feature_backtest_mercado.py` + `test_pop_cut.py` → **50 passed**.

### 2. ruff — `ruff check calcular_colunas_mercado.py test_calcular_colunas_mercado_gate.py`
**All checks passed!**

### 3. Guardrails (verificados de forma independente)
- `grep RENDA_PER_CAPITA_MIN_ATR src/motor_expansao/config.py` → **VAZIO** (exit 1). ✓
- `grep -rn RENDA_PER_CAPITA_MIN_ATR src/` → aparece **SÓ** em
  `calcular_colunas_mercado.py` (linhas 42 e 295). ✓
- `flag_viavel`/`RENDA_MIN`/`renda_target_proxy`: intocados (o único match no diff é um
  COMENTÁRIO de documentação, não lógica). ✓
- `src/motor_expansao/pipelines/m1/` e `config.py`: **não tocados** (`git status --porcelain` vazio). ✓
- `python scripts/loop_guard.py --base ciclo/loop-20260706-152137` → **GUARD OK**. ✓
- Artefatos oficiais M1 (`brasil_estrutural`/`brasil_priorizados`/`hexagonos_brasil_oportunidades`):
  sem alteração git, mtime 2026-06-03 (anterior ao ciclo). ✓ (Nenhuma escrita de parquet no bloco.)

### 4. Corretude do código (`flag_gate_atratividade`, linhas 292–296)
```python
_renda_pc = pd.to_numeric(df["renda_per_capita"], errors="coerce")
df["flag_gate_atratividade"] = (
    df["flag_pop_min_5k"].fillna(False).astype(bool)
    & _renda_pc.ge(RENDA_PER_CAPITA_MIN_ATR).fillna(False)
)
```
- = `flag_pop_min_5k (pop ≥ 5000)` AND `renda_per_capita >= 1500`. ✓
- NaN em `renda_per_capita` → `.ge(...)` NaN → `.fillna(False)` → **False**. ✓ (teste `test_renda_nan_false`)
- Limiar **inclusivo** `>=` (`.ge`); `renda == 1500` → True. ✓ (teste `test_renda_exatamente_no_limiar_true`)
- Tipo `bool` puro, zero NaN. ✓ (teste `test_acima_dos_dois_pisos_true`)
- Coluna **PARALELA**: não referenciada em `flag_sam` (linha 312), `tese_entrada` (np.select
  linha ~327), nem `prioridade_mercado_mapeado` (linha ~401). Confirmado por leitura do diff. ✓
- `validar()`: imprime contagem + % de aprovados + distribuição por UF (top 10, sob guard
  `if "uf" in df.columns`). ✓

### 5. import — `python -c "import streamlit_app"` → **import ok**.

### 6. Housekeeping — `python scripts/housekeeping_move_block.py BLK-ATR-02 --check`
Reporta "stub ausente no backlog para BLK-ATR-02" (exit 0, informativo). Esperado nesta etapa:
o bloco ainda está com a entrada completa no `backlog.md` (linha 1038); a redução a stub +
movimento para `completed.md` é o passo de FECHAMENTO, posterior ao QA. NÃO é bloqueador.

## Documentação
`docs/modelo_mercado_hexagonos.md` §5.4 (linhas 279–289): nova linha de tabela para
`flag_gate_atratividade` + nota explicando regra, NaN→False, escopo paralelo e READ-ONLY M1. ✓

## Ressalva (única)
As 4 falhas do full suite são por `openlocationcode` não instalado no ambiente de QA — um gap
de INFRA do ambiente (o pacote está no `pyproject.toml`, só falta `pip install`), herdado do
ciclo BLK-UI-09-FU2 e independente deste bloco. Recomendação operacional: instalar a dependência
declarada no ambiente do loop para restaurar suíte 100% verde. O BLK-ATR-02 em si está limpo:
subconjunto impactado 50/50 verde, ruff limpo, guardrails confirmados, corretude confirmada.

## Conclusão
Bloco tecnicamente correto e dentro de todos os guardrails (READ-ONLY M1, coluna paralela,
constante local, DEC-001/DEC-006/DEC-007 intactas). Liberado para fechamento. A ressalva é
ambiental e não deve travar o merge do bloco.
