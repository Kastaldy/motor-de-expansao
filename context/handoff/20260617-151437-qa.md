# Handoff — BLK-DIM-13 QA → Fechamento

## Veredito: APROVADO

Ciclo BLK-DIM-13 (correção do split balcão/agregador no engine de viabilidade
property-first). Todas as validações obrigatórias passaram SEM bypass; gate da
suíte full verde; READ-ONLY sobre o M1 confirmado por diff content-only.

## Resultado das validações (na ordem)

1. **Loop guard** — PASSA. `python scripts/loop_guard.py --base 42857d4` com os 3
   arquivos do ciclo staged: `GUARD OK: 3 caminho(s) alterado(s), nenhum proibido.`
   (exit 0). Nenhum path do ciclo está no conjunto bloqueado (config/m1/scoring/
   artefatos/deploy/secrets/CI).
2. **Ruff** — `ruff check .`: `All checks passed!` (exit 0). Zero erros.
3. **Mypy** — `mypy src/motor_expansao/dimensionamento/viabilidade_ponto.py`:
   `Success: no issues found in 1 source file` (exit 0).
4. **Smoke import** — `ok 0.69` (exit 0). `analisar_viabilidade_ponto` e
   `SHARE_BALCAO_DEFAULT` importam; constante = 0.69.
5. **Testes alvo** — `pytest -x tests/unit/dimensionamento/test_viabilidade_ponto.py -q`:
   **14 passed** in 1.30s (exit 0).
6. **Suíte FULL (gate único, sem bypass)** — `pytest -q`:
   **956 passed, 4 skipped, 18 warnings** in 219.48s (exit 0). ZERO falhas.

## Checklist de conteúdo (1–13)

**viabilidade_ponto.py:**
1. ✓ `SHARE_BALCAO_DEFAULT: float = 0.69` (constante nomeada + em `__all__`).
2. ✓ `ViabilidadePontoResult` tem `alunos_balcao_premissa: float = 0.0` e
   `alunos_agregadores_premissa: float = 0.0` ANTES de `demanda_fonte` (linhas 95–100).
3. ✓ `grade_sensibilidade` tem `share_balcao` na assinatura e aplica o split no loop
   interno (`balcao_cel = alunos*share`, `agr_cel = alunos*(1-share)`; passa
   `alunos_agregadores=agr_cel` a `viabilidade`).
4. ✓ `analisar_viabilidade_ponto` computa `alunos_balcao = demanda_premissa*share_balcao`
   e `alunos_agregadores = demanda_premissa*(1.0 - share_balcao)` (linhas 328–329).
5. ✓ `viabilidade()`, `aluguel_teto()`, `alunos_minimos_viaveis()` usam `alunos_balcao`
   e passam `alunos_agregadores=alunos_agregadores`; `grade_sensibilidade` recebe `share_balcao`.
6. ✓ `simulador.py` NÃO modificado (zero diff de conteúdo — ver item 13).

**pages.py:**
7. ✓ Label: `"Demanda assumida (alunos TOTAIS na maturidade)"` (linha 3018).
8. ✓ Caption do split automático: "Total = balcao (~69%, ticket cheio) + agregadores
   (~31%, ticket reduzido). O split e aplicado automaticamente; o DRE roda com os dois
   tickets." (linhas 3029–3030).

**Testes:**
9. ✓ `test_split_corrige_superestimacao_receita` — assert `fat < 300_000.0` (e faixa 260k–290k).
10. ✓ `test_anti_double_count_agregadores_escalam` — assert `alunos_agregadores_premissa != 651.0`.
11. ✓ `test_grade_aplica_split_internamente` — compara célula da grade ao split manual.
12. ✓ `test_share_balcao_default_aplicado` — usa `SHARE_BALCAO_DEFAULT` no split.

**READ-ONLY M1:**
13. ✓ Diff de CONTEÚDO (ignorando whitespace/EOL — a árvore inteira do branch aparece
    como "M" por normalização de fim-de-linha, ruído pré-existente) mostra mudança real
    APENAS nos 3 arquivos do ciclo:
    - `viabilidade_ponto.py`: +41 / -7
    - `pages.py`: +5 / -1
    - `test_viabilidade_ponto.py`: +56
    ZERO mudança de conteúdo em `simulador.py`, `config.py` (M1 raiz),
    `dimensionamento/config.py`, e todos os `pipelines/m1/*` (base_h3_brasil,
    fase1_bi_exports, hex_enrichment, ibge_censo, poi_enrichment, provenance).
    Pesos `renda=0.40`/`pop=0.60`, `score_priorizacao`, artefatos oficiais: INALTERADOS.

## Housekeeping check (6.0)

`python scripts/housekeeping_move_block.py BLK-DIM-13 --check` →
`FALHA --check: stub ausente no backlog para BLK-DIM-13` (exit 1). **ESPERADO**: nesta
versão o `--check` verifica se o bloco JÁ foi movido (stub no backlog + entrada em
completed.md). BLK-DIM-13 ainda é um bloco ATIVO completo no backlog (linha 539), não um
stub, e não está em completed.md — porque o ciclo ainda não foi fechado/commitado. NÃO é
ad-hoc (o bloco existe no backlog). O housekeeping (mover bloco → completed) é passo do
**Fechamento/commit**, não bloqueador de QA.

## Paths a commitar (APROVADO)

- `src/motor_expansao/dimensionamento/viabilidade_ponto.py`
- `src/motor_expansao/dashboard/pages.py`
- `tests/unit/dimensionamento/test_viabilidade_ponto.py`
- (+ handoffs do ciclo e `tasks/current_task.md` conforme processo do orquestrador)

NÃO commitar (pré-sujos, alheios ao ciclo): `data/outputs/setores_censitarios_2022_geo/_metadata.json`,
`data/reports/relatorio_pontual_censitario_base_geo.md`. Os demais "M" da árvore são ruído
de EOL/whitespace sem mudança de conteúdo — não fazem parte deste ciclo.

---
Skill atual: QA
Próxima Skill: Fechamento (orquestrador)
