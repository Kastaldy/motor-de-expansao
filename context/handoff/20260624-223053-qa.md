# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador commita por path; bloco vem do backlog, housekeeping concluído)

## VEREDITO
APROVADO

## Justificativa
Camada paralela `demanda_revelada` entregue READ-ONLY sobre o M1, disjunta (zero import de `pipelines/m1`/`censo_*`/`dashboard`/streamlit), contrato de 9 colunas idêntico ao backlog, anti-PII por construção e validado por teste, 8/8 testes do módulo verdes, ruff/mypy limpos, DEC-012 bem-formada em §8 e housekeeping feito via helper versionado. As únicas falhas da suíte (4) são pré-existentes e ambientais (`openlocationcode` ausente no Python 3.14 local — Plus Code, alheio ao ciclo), não tocam o módulo novo.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- Discrepância de contagem no log do Builder: o Builder reportou **2 falhas** (rodou só um subconjunto: `test_demanda_revelada_ingestao.py` + `test_streamlit_app.py`); a suíte COMPLETA (gate único) revela **4 falhas** — as 2 do Builder em `test_streamlit_app.py` MAIS 2 em `tests/unit/test_coord_search.py` (`test_resolve_plus_code_completo_decodifica_offline`, `test_resolve_plus_code_curto_usa_localidade_mockada`). Todas as 4 têm a MESMA raiz (`openlocationcode` ausente) e são alheias ao ciclo; nenhuma toca o módulo novo. A diferença é só de cobertura do subconjunto do Builder, não defeito — mas registrada para precisão.

## Melhorias opcionais
- A constante `_BANDA_GT5KM = 5` (banda ">5km") e o índice de faixa dependem da ordem de `BANDS` do dump; um teste futuro poderia ancorar explicitamente esse mapeamento contra `BANDS` lido (hoje a fixture cobre o valor 5 indiretamente). Não bloqueia.

## Testes faltantes
- nenhum (cobertura adequada: parquet legível, contrato+dtypes, zero-PII, res-7, join por hex_id com valor agregado, reprodutibilidade, agregação controlada em 3 hexes, não-leitura da var de ruído com PII falsa).

## Riscos remanescentes
- R3/R4 (cobertura ~1% do universo concentrada em SP; coords arredondadas ~1 km) — documentados no relatório gitignored e na DEC-012; camada de refino, não cobertura nacional; não substitui M1/censitário.
- Ambiental: `openlocationcode` precisa estar instalado (já está na base do `pyproject.toml`, linha 31) para os 4 testes de Plus Code passarem; CI com `.[dev]`/`.[api_mvp]` os resolve. Não é deste ciclo.

## Saída literal das validações (re-executadas pelo QA)

### 1) Suíte COMPLETA — `python -m pytest -n auto -q` (gate único)
```
=========================== short test summary info ===========================
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_curto_usa_localidade_mock
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_curto_usa_localidade_mockada
4 failed, 1096 passed, 1 skipped, 266588 warnings in 166.77s (0:02:46)
```
Verificação das 4 falhas (alegação do Builder de "pré-existentes e alheias"):
```
$ python -c "import openlocationcode"
ModuleNotFoundError: No module named 'openlocationcode'

# detalhe de uma falha — resolve_plus_code retorna None por falta da lib:
out = resolve_plus_code("589R6M7J+GQ")  -> None  (assert out is not None -> AssertionError)

# os 4 testes referenciam Plus Code / openlocationcode, NÃO o módulo novo.
# último commit a tocar os arquivos das 4 falhas:
45a8f30 2026-06-23  BLK-RELMUN-01-FU1   (PRECEDE a branch do ciclo, criada de 3c128c1)
# pyproject linha 31: "openlocationcode>=1.0.1"  (lacuna de ambiente, não defeito)
```
CONCLUSÃO: 4 falhas pré-existentes/ambientais; NÃO bloqueiam (ressalva ambiental). Nenhuma é causada pelo ciclo nem toca `demanda_revelada`.

### 2) Testes do módulo isolado — `python -m pytest -q tests/unit/test_demanda_revelada_ingestao.py`
```
8 passed, 409 warnings in 0.48s
```

### 3) `python -c "import streamlit_app; print('import ok')"`
```
... WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
import ok
```

### 4) `python -c "import motor_expansao.demanda_revelada; print('mod ok')"`
```
mod ok
```

### 5) `ruff check src/motor_expansao/demanda_revelada/ tests/unit/test_demanda_revelada_ingestao.py`
```
All checks passed!
```

### 6) `mypy src/motor_expansao/demanda_revelada/`
```
Success: no issues found in 3 source files
```

### 7) Housekeeping (Passo 6.0 — bloco vem do backlog)
```
$ python scripts/housekeeping_move_block.py BLK-TP-01 --date 2026-06-24
OK: movido BLK-TP-01 (3767 bytes) -> tasks/completed.md; stub em tasks/backlog.md.

$ python scripts/housekeeping_move_block.py BLK-TP-01 --check
OK: BLK-TP-01 movido (stub no backlog + bloco em completed.md).

# nenhum heading "### BLK-TP-01" remanescente no backlog (só stub na linha 932); bloco em completed.md linha 5908.
# 15 âncoras estruturais (header -> tabela de contrato -> caveats -> escopo -> critérios -> guardrail) presentes e EM ORDEM no completed.md (move fiel, sem truncamento).
$ python -m pytest -q tests/unit/test_housekeeping_helper.py
10 passed, 501 warnings in 0.30s
```

### 8) Disjunção / READ-ONLY M1
```
# imports do módulo: só __future__, json, re, pathlib, h3, pandas, .contrato — nenhum pipelines.m1/censo/dashboard
# grep demanda_revelada em dashboard/ api/ streamlit_app.py: NONE (disjunto)
# único to_parquet -> destino parametrizável (default data/staging/demanda_revelada_h3.parquet, gitignored, NÃO oficial)
# config.py: diff vazio vs HEAD (inalterado); H3_RESOLUTION=7/DIST_MIN_ULTRA_KM=1.0/RENDA_MIN=4500.0 intactos
# NAO_ABRA/ e demanda_revelada_h3.parquet: NÃO versionados (git ls-files = NONE)
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock do caminho crítico. A suíte é tooling de teste unitário legítimo: a fixture sintética `tests/fixtures/demanda_revelada_fake.html` É o contrato de teste correto (a regra de no-bypass mira tooling que depende de config/artefato real de produção, ex. SOPS — não testes unitários com fixtures, que são prática esperada). O `ingerir_demanda_revelada` foi exercitado de ponta a ponta (parse → agregação H3 → drop-PII → parquet → leitura de volta) sobre a fixture, e o Builder também o rodou contra o dump real (16.575 hexes, gitignored) — caminho real exercitado. Housekeeping rodado via o helper versionado sancionado, não por edição manual.

## Conferência cruzada com log do Builder
Bate, com 1 divergência de contagem registrada como problema médio: o Builder reportou 2 falhas (subconjunto); a suíte completa mostra 4 — mesma raiz (`openlocationcode`), todas alheias ao ciclo. Demais itens (8/8 módulo, ruff/mypy limpos, imports ok, disjunção, anti-PII, 9 colunas, DEC-012) batem com o handoff do Builder.

## Guardrails verificados
- score_priorizacao não alterado: sim (pacote disjunto; config.py diff vazio vs HEAD; nenhuma escrita em artefato M1)
- hex_score_estrutural / pesos renda=0.40/pop=0.60: sim (inalterados; DEC-012 reafirma; config.py intacto)
- Artefatos M1 preservados: sim (único write é o parquet gitignored não-oficial via destino parametrizável)
- Parâmetros canônicos §3: sim (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0 presentes/intactos; nenhum parâmetro de score adicionado)
- Anti-PII: sim (`test_zero_pii` efetivo — lista `COLUNAS_PII_PROIBIDAS` + asserção de igualdade ao contrato; parser lê só CELLS/GYMS_PTS/SF/BANDS; var de ruído `RAW_FAKE` com PII falsa NÃO é lida, provado por `test_nao_le_var_de_ruido`; fonte real em NAO_ABRA/ gitignored; saída parquet gitignored)
- Contrato: sim (9 colunas genéricas idênticas ao backlog com dtypes corretos; `hex_id` res-7 validado por `_assert_res7`; reprodutibilidade por ordenação em `hex_id`)
- Pacote disjunto / sem extra novo / sem API ao vivo: sim
- DEC-012 em §8: sim (status APROVADA por Felipe Silva 2026-06-24, licença plenamente liberada sem travar produtização, relação DEC-001/DEC-009 INTACTA)
- Validações re-executadas pelo QA (sem bypass): pytest full (4 fail ambientais/1096 pass/1 skip) → módulo 8/8 → import streamlit_app ok → import mod ok → ruff clean → mypy clean → housekeeping OK + helper 10/10
- Escopo respeitado: sim (só arquivos do plano; nenhum path pré-sujo incluído; Builder NÃO commitou)

## Decisão recomendada
Fechar ciclo — orquestrador commita por path (módulo novo, teste, fixture, CLAUDE.md §8/DEC-012, backlog stub, completed.md, handoffs, current_task). NÃO incluir paths pré-sujos. O move do housekeeping deve permanecer no working tree para o commit do orquestrador.
