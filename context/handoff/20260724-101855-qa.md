# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (modo MERGE-HUMANO, criticidade Alta) — Passo 6.0 do orquestrador: mover o bloco via `scripts/housekeeping_move_block.py` e commitar por path.

## VEREDITO
APROVADO COM RESSALVAS

## Justificativa
Re-executei por conta própria todas as validações obrigatórias do plano: subconjunto impactado 90 passed/0 failed, smoke import ok, ruff e mypy limpos, e artefatos M1 com mtime congelado em 2026-06-10 (idêntico antes e depois da suíte). O diff é exatamente o escopo planejado (5 arquivos), default-preserving, e o comportamento novo entrou com 3 testes. As 2 ressalvas são NÃO-BLOQUEANTES e nenhuma é defeito do Builder: (1) a única falha da suíte completa é PRÉ-EXISTENTE, local-only e sem relação com o ciclo; (2) falta um micro-teste da mecânica do `packed_scale`.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- **Falha pré-existente e NÃO-RELACIONADA na suíte completa:** `tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime` falha com `FileNotFoundError` em `data/staging/unidade_territorio_retencao.parquet`. **Confirmei por conta própria** (não aceitei o diagnóstico de terceiros): o dataset de entrada NÃO existe nesta máquina (gitignored); o teste e sua fonte (`src/motor_expansao/lifetime/`) NÃO estão no diff do ciclo; não há qualquer import entre `lifetime/` e `censo_map`/`censo_report`; e a falha REPRODUZ ISOLADA em 15s, sem tocar nenhum caminho do BLK-RELPON-13. **Causa-raiz:** o teste guarda com `pytest.skip` os ARTEFATOS M1 (linhas 260-261) mas NÃO guarda o próprio dataset de ENTRADA que `run(root=root)` (linha 263) carrega. Como 3 dos 5 artefatos M1 existem localmente, o skip não dispara e o `run()` estoura. **Local-only:** num checkout limpo de CI os parquets de `data/staging/` são gitignored → nenhum artefato M1 existe → o skip dispara e o teste é PULADO. Por isso o CI (Linux) segue verde e isto não quebra o portão da `main`.
- **Mecânica do `packed_scale` executada mas não asseverada:** o parâmetro novo `scale`/`packed_scale` (`_map_grid_cells_packed`/`_draw_maps_grid`/`_classico_draw_maps_grid`) não tem teste dedicado. O caminho `scale<1.0` É exercitado pelos testes de PDF (as 2 páginas do slide-hero passam `_HERO_MAP_SCALE=0.92` e os PDFs saem válidos com `/Count 8`), mas o EFEITO geométrico ("células menores e centradas") nunca é asseverado. O teste existente `test_grid_de_mapas_aceita_2x1_e_1x1_e_mantem_o_default_2x2` só cobre o default 1.0. O plano (sub-decisão D / Riscos técnicos) pedia explicitamente testar a MECÂNICA. Risco real baixo: são 2 multiplicações float default-preserving, e o valor final é decisão de PRODUTO sob gate visual.

## Melhorias opcionais
- Adicionar ao `test_relatorio_pontual_censitario_export.py` um micro-teste da mecânica: `_map_grid_cells_packed(aspect, ..., cols=2, rows=1, scale=0.8)` produz `w`/`h` menores que com `scale=1.0` e permanece centrado (`x0 + total_w/2 == _PAGE_W/2`). ~6 linhas, sem travar o número 0.92.
- Custo aceito e documentado pelo Planner (não refatorar agora): a socioeconomia faz o PRÓPRIO fetch de basemap de 5 km. Verifiquei que a bbox é idêntica à do residual (mesmo `_frame_box_metric(RAIO_RESIDUAL_DISPLAY_KM, width, height)` e mesmo `width`), então o cache de tiles em disco deduplica o download; sobra só um re-composite. Sem dependência de rede nova.

## Testes faltantes
- Mecânica do `scale`/`packed_scale` (ver acima) — o único teste que o plano pediu e não foi entregue.
- Guarda de `pytest.skip` para o dataset de entrada em `test_run_readonly_m1_por_mtime` (dívida PRÉ-EXISTENTE, não deste ciclo) — sugiro o bloco de correção **BLK-FIX-LTV-01** abaixo.

## Riscos remanescentes
- A suíte completa não fica 100% verde NESTE ambiente enquanto a guarda de skip faltante não for corrigida. Não afeta o CI (o teste pula lá) nem este ciclo.
- `socioeconomia` passou a ser CONDICIONAL: se a fatia de UF servida não trouxer `score_setor_2022_calibrado`, o painel cai no fallback textual (antes renderizava sempre, pois vinha dos setores). **Verifiquei empiricamente que o risco não se materializa em produção:** os 4 call sites reais passam `hexes_df` (`pages.py:3022/3499/3702` com `hexes_df=df`; `api/service.py:351` com `hexes_df=hexes_df`) e as partições servidas trazem a coluna com densidade alta — SP: 46.389/47.389 não-nulos (97,9%), RJ: 7.789/8.286 (94,0%), faixa 0-100 em ambas; AC também tem a coluna. O painel renderiza choropleth real, não cinza.
- `_HERO_MAP_SCALE = 0.92` é ponto de partida; o valor final é do gate visual de Vinicius. Nenhum teste trava o número (confirmei: `grep` por `0.92`/`_HERO_MAP_SCALE` em `tests/` não retorna nada).

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte COMPLETA (gate único) — `python -m pytest -q` (serial)
Executada pelo orquestrador com o mesmo comando e ambiente; eu confirmei a ÚNICA falha por conta própria (reprodução isolada em §7 abaixo) e revalidei os artefatos M1 depois dela.
```
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 1984 passed, 2 skipped, 326076 warnings in 1520.23s (0:25:20)
EXIT=1
```
Leitura: 1984 passed / 2 skipped / 1 failed, sendo a falha PRÉ-EXISTENTE e não-relacionada (provas em §7). Zero regressão atribuível ao BLK-RELPON-13.

### 2. Tentativa com `-n auto` (xdist) — bug de AMBIENTE, registrado literalmente
```
=== FULL SUITE ATTEMPT: -n auto ===
bringing up nodes...
bringing up nodes...

[gw9] node down: Not properly terminated
F[gw8] node down: Not properly terminated
F[gw7] node down: Not properly terminated
F[gw6] node down: Not properly terminated
F[gw5] node down: Not properly terminated
F[gw4] node down: Not properly terminated
F[gw3] node down: Not properly terminated
F[gw2] node down: Not properly terminated
FTraceback (most recent call last):
  File "<string>", line 1, in <module>
    import sys;exec(eval(sys.stdin.readline()))
               ~~~~^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "<string>", line 5, in <module>
OSError: [Errno 22] Invalid argument
```
Confirmo o relato do Builder: `-n auto` aborta por execnet/gateway neste Windows/Python 3.14.2. Rodar SERIAL é workaround de AMBIENTE, não bypass (a regra (b)/(d) mira verde de TOOLING obtido contornando config/artefatos REAIS de produção — não é o caso).

### 3. Subconjunto impactado — `python -m pytest -q tests/unit/test_relatorio_pontual_censitario_mapa.py tests/unit/test_relatorio_pontual_censitario_export.py`
```
........................................................................ [ 80%]
..................                                                       [100%]
90 passed, 4675 warnings in 107.96s (0:01:47)
```
0 falhas. Inclui, verificados por nome no arquivo: `test_camadas_existentes_ficam_byte_identicas_com_os_defaults_novos` (1250), `test_camada_residual_presente_com_hexes_df_e_png_valido` (728), `test_camada_residual_nao_desenha_pins_blk_relpon_10_fu1` (748, com `com["residual"] == sem["residual"]`), `test_camada_residual_ausente_sem_hexes_df_ou_sem_hex_no_disco` (797), `test_camadas_censitarias_declara_as_8_chaves` (917), os 3 NOVOS (`test_socioeconomia_e_hexagono_nao_setor_a_5km` 826, `test_socioeconomia_ausente_sem_hexes_df_ou_sem_coluna_score` 861, `test_socioeconomia_reage_ao_score_setor_2022_calibrado` 896) e `test_slide_hero_offline_safe_sem_camada_residual` (export 832). Bate com os 90 passed do Builder.

### 4. Smoke — `python -c "import streamlit_app; print('import ok')"`
```
2026-07-24 09:09:52.024 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
(...3 linhas idênticas de warning do Streamlit sem runtime...)
import ok
```

### 5. Lint — `ruff check` nos 4 arquivos do diff
```
All checks passed!
```

### 6. Tipos — `mypy src/motor_expansao/dashboard/censo_map.py src/motor_expansao/dashboard/censo_report.py`
```
Success: no issues found in 2 source files
```

### 7. Confirmação PRÓPRIA do diagnóstico da falha (não aceitei de terceiros)
```
=== 1. dataset de entrada existe? ===
ls: cannot access 'data/staging/unidade_territorio_retencao.parquet': No such file or directory
AUSENTE (confirmado)

=== 2. arquivos do teste/fonte estao no diff? ===
NAO esta no diff (nao relacionado ao ciclo)

=== lifetime importa censo_map/censo_report? ===
SEM ligacao com os modulos do ciclo (falha independente)
```
Reprodução ISOLADA (15s, sem tocar nenhum caminho do ciclo):
```
E    FileNotFoundError: [Errno 2] No such file or directory: 'C:\\Users\\Vinicius Cruz\\Downloads\\Projetos\\motor-de-expansao\\data\\staging\\unidade_territorio_retencao.parquet'
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 664 warnings in 15.00s
```
Causa-raiz no próprio teste (guarda os artefatos M1, não guarda a ENTRADA):
```
255: def test_run_readonly_m1_por_mtime():
259:     existentes = [p for p in artefatos if p.exists()]
260:     if not existentes:
261:         pytest.skip("artefatos M1 oficiais nao presentes no ambiente")
263:     run(root=root)  # <-- carrega unidade_territorio_retencao.parquet SEM guarda
```

### 8. READ-ONLY M1 por mtime (medido DEPOIS da suíte completa; idêntico à leitura de antes)
```
FullName      : ...\data\staging\brasil_estrutural.parquet
LastWriteTime : 10/06/2026 09:50:32

FullName      : ...\data\staging\brasil_priorizados.parquet
LastWriteTime : 10/06/2026 09:50:32

FullName      : ...\data\staging\hexagonos_brasil_oportunidades.parquet
LastWriteTime : 10/06/2026 09:50:34

AUSENTE: data/staging/hexagonos_brasil_dashboard.parquet
AUSENTE: data/staging/hexagonos_mapa_sample.parquet
```
Critério atendido: mtime 2026-06-10 (formato local DD/MM/AAAA), muito anterior ao ciclo; nenhum artefato reescrito. Os 2 ausentes são build de pipeline, não gerados aqui.

### 9. Housekeeping (Passo 6.0, modo MERGE-HUMANO) — pendência do ORQUESTRADOR
```
=== housekeeping --is-done (expect ABERTO / exit 1) ===
BLK-RELPON-13: ABERTO em tasks/completed.md
EXIT=1
```
Esperado nesta fase. Bloco `### BLK-RELPON-13` ÍNTEGRO em `tasks/backlog.md:225` (heading presente, disponível para o helper mover). Não exigi `--check` verde agora: o move + `--check` são passo do orquestrador no fechamento, no MESMO commit.

### 10. Escopo — `git status --porcelain` e `git diff --name-only HEAD`
```
 M CLAUDE.md                                                <-- NÃO é do ciclo (trim do orquestrador)
 M docs/relatorio_pontual_censitario.md
 M src/motor_expansao/dashboard/censo_map.py
 M src/motor_expansao/dashboard/censo_report.py
 M tests/unit/test_relatorio_pontual_censitario_export.py
 M tests/unit/test_relatorio_pontual_censitario_mapa.py
?? context/handoff/20260723-152254-block-orchestrator.md
?? context/handoff/20260723-153430-planner.md
?? context/handoff/20260723-155829-builder.md
```
Último commit = `21e18a9` (commit HUMANO que adicionou o bloco). O Builder NÃO commitou. `context/handoff.md` e `tasks/current_task.md` são gitignored (confirmado por `git check-ignore`). `tests/unit/test_claude_md_size.py` após o trim: `2 passed`.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico. Observações:
- A suíte serial (em vez de `-n auto`) é workaround de AMBIENTE comprovado (§2), não contorno de config/artefato real de produção — a regra (b)/(d) do prompt mira verde de TOOLING obtido contornando `.sops.yaml`/`creation_rules` reais, o que não se aplica aqui.
- Os testes usam fixtures sintéticas (`_hexes_sinteticos`, `_setores_um_quadrado`) para isolar lógica de render — prática normal e explicitamente NÃO-bypass pela regra (b).
- Além dos testes, validei o caminho REAL de produção com dados reais: li o schema e a densidade das partições servidas (`data/outputs/hexagonos_dashboard_enriquecido/uf=SP|RJ|AC`) e os 4 call sites de produção, provando que a chave condicional nova de fato renderiza em produção (ver "Riscos remanescentes").

## Conferência cruzada com log do Builder
Bate. Subconjunto 90 passed idêntico; ruff/mypy/import idênticos; mtime idêntico; relato do INTERNALERROR de xdist reproduzido por mim. O Builder declarou não ter rodado a suíte full (correto — é gate único do QA) e não commitou (correto). Os 9 passos do plano estão implementados literalmente, incluindo os defaults DEFAULT-PRESERVING e a remoção do bloco antigo `socioeconomia_png = _render_camada(...)`. Nenhuma divergência encontrada. Única omissão vs. o plano: o teste da mecânica do `packed_scale` (ressalva 2, não-bloqueante).

## Guardrails verificados
- **score_priorizacao não alterado:** sim. `grep` no diff não encontra NENHUMA escrita em `score_priorizacao`/`hex_score_estrutural`/pesos; as únicas ocorrências desses identificadores no diff são PROSA de documentação afirmando que permanecem intocados. Nenhum arquivo de `config`/`pipelines/m1`/`scoring` está no diff (verificado por `git diff --name-only HEAD | grep -E "config|pipelines/m1|scoring"` → vazio).
- **Artefatos M1 preservados:** sim (mtime 2026-06-10 antes e depois da suíte; §8).
- **Motor censitário INTOCADO:** sim. `setor_censitario_intersecao_area_1p5km` e `RAIO_CENSITARIO_DEFAULT_KM = 1.5` (`censo_point.py:23`) não sofrem alteração — `censo_point.py` sequer está no diff. `score_setor_2022_calibrado` é apenas LIDO do `hexes_df` para colorir.
- **Parâmetros canônicos preservados:** sim — `H3_RESOLUTION=7` (o `7` no `censo_map` é literal LIDO, com comentário), `DIST_MIN_ULTRA_KM=1.0`, `RENDA_MIN=4500.0`, pesos `renda=0.40`/`pop=0.60` intocados (nenhum arquivo de config/pipeline no diff).
- **`/Count 8` e 8 chaves:** sim. `CAMADAS_CENSITARIAS` (`censo_map.py:104-113`) mantém exatamente as 8 chaves com `socioeconomia` e `residual`; `test_camadas_censitarias_declara_as_8_chaves` verde; asserts de `/Count 8` verdes nas 2 variantes.
- **Byte-identidade das outras 7 camadas:** sim. As 6 de setor/entorno usam `_render_camada` com código INALTERADO; o `residual` é travado por `com["residual"] == sem["residual"]` e pelos `test_camada_residual_*`. Os parâmetros novos são todos keyword-only DEFAULT-PRESERVING; `scale=1.0` é no-op float exato.
- **Comportamento novo com teste:** sim — socioeconomia é hex (não setor) a 5 km, CONDICIONAL ao `hexes_df`, SEM pins, e reage a `score_setor_2022_calibrado`, coberto pelos 3 testes novos; `test_slide_hero_offline_safe_sem_camada_residual` agora asserta `"socioeconomia" not in mapas`.
- **ASCII no PNG (exceção de RENDER, §2):** sim — `"Socioeconomia - raio 5 km"`, `"Score censitario (0-100)"` e a faixa `_legenda_valor_hex("Score", ...)` → `"Score no hexagono: NN"`. Nenhum glifo acentuado; `_format_valor_ponto_score` emite inteiro ASCII ou `"n/d"`. Docs e testes em português acentuado correto.
- **Escopo respeitado:** sim — os 5 arquivos do plano, nada além. `PRD.md`, `CLAUDE.md` e `tasks/backlog.md` NÃO foram tocados pelo Builder (o `M CLAUDE.md` no working tree é edição não-relacionada do ORQUESTRADOR, e o `tasks/backlog.md` em `git diff main...HEAD` vem do commit humano `21e18a9`).
- **Validações re-executadas pelo QA (sem bypass):** suíte completa → 1984 passed/2 skipped/1 failed pré-existente | subconjunto impactado → 90 passed | smoke import → ok | ruff → All checks passed | mypy → Success | mtime M1 → inalterado | `-n auto` → INTERNALERROR de ambiente (registrado) | reprodução isolada da falha → confirmada não-relacionada | housekeeping `--is-done` → ABERTO/exit 1 (esperado).

## Decisão recomendada
**Fechar ciclo** (modo MERGE-HUMANO). O orquestrador deve, no Passo 6.0 e no MESMO commit: mover o bloco com `python scripts/housekeeping_move_block.py BLK-RELPON-13`, validar `--check`, e commitar POR PATH apenas os 5 arquivos do ciclo (`censo_map.py`, `censo_report.py`, os 2 testes, `docs/relatorio_pontual_censitario.md`) + o bookkeeping — **sem** `CLAUDE.md`, `context/handoff.md`, `tasks/current_task.md`, `PRD.md`. Gate visual de `_HERO_MAP_SCALE` segue com Vinicius (calibração de produto, não bloqueia o merge).

Bloco de correção sugerido (pequeno, NÃO deste ciclo):
- **BLK-FIX-LTV-01 — guarda de skip faltante em `test_run_readonly_m1_por_mtime`** (criticidade Baixa, loop-safe): adicionar `if not (staging / "unidade_territorio_retencao.parquet").exists(): pytest.skip(...)` antes do `run(root=root)`, para o teste pular graciosamente em ambiente com artefatos M1 presentes mas dataset de retenção ausente. READ-ONLY sobre o M1, só teste.
