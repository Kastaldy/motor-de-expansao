# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Approver / gate humano (revisão VISUAL Felipe+Vini + aprovação técnica humana) — ANTES do Builder.
(Criticidade Alta: PARAR após este plano; Builder só roda após o gate.)

## Entendimento da tarefa
Adicionar uma variante "Apresentação Clássica Ultra" (estética GeoFusion antiga sobre o motor
censitário novo) do PDF do Relatório Pontual Censitário em `censo_report.py`, reusando o motor e
helpers existentes, sem alterar o template recente (byte-a-byte) nem tocar o M1.

## Decisões de escopo já tomadas pelo Planner (Builder NÃO redecide)

### D-A — Forma da API nova = FUNÇÃO DEDICADA `gerar_pdf_relatorio_pontual_classico(...)`
- NÃO usar `template="classico"` em `gerar_pdf_relatorio_pontual_censitario`. Justificativa: o
  contrato exige o caminho recente byte-a-byte inalterado; uma função separada garante zero
  ramificação dentro do gerador recente (risco zero de regressão no stream/ordem/`/Count 7`).
- Assinatura (espelha a recente + `rotulo` que também vira o LINK da Realização):
  ```python
  def gerar_pdf_relatorio_pontual_classico(
      result: dict[str, Any],
      mapas: dict[str, bytes] | bytes | None = None,
      *,
      residual: dict[str, Any] | None = None,
      ultra_dir: Path | str | None = None,
      solicitante: str | None = None,
      rotulo: str | None = None,
  ) -> bytes:
  ```
- `rotulo` é o nome/endereço do ponto (mesmo que já flui da API/bot via `service.gerar_pdf_ponto`
  → `rotulo`). Na capa clássica vira o subtítulo (endereço ACIMA); na Realização vira o texto do
  link clicável. Quando `rotulo` é só coordenada (`_parece_coordenada`) ou None, o link cai em
  `build_search_url("{lat},{lng}")` e o subtítulo cai em "Coordenada: ...".

### D-B — Reuso vs nova variante de cada helper
REUSAR TAL-QUAL (NÃO alterar — usados pelo template recente):
- `_UltraPDF`, `_draw_full_page_background`, `_draw_map`, `_png_dimensions`, `_draw_watermark`,
  `_watermark_text`, `_ascii`, `_format_number`, `_parece_coordenada`, `_point_name`, `_point_rows`,
  `_safe_len`, `_normalize_mapas`, `_normalize_mapas_by_key`, constantes
  (`ULTRA_TURQUESA`, `ULTRA_MAGENTA`, `_BRANCO`, `_CINZA_TEXTO`, `_PAGE_W/_PAGE_H`, `_WATERMARK_*`,
  `_ATRIBUICAO_TILES`, `_CREDITO_ULTRA`, `MAP_LAYER_TITLES`, `PDF_SECTION_HEADERS`).
- `_big_numbers_page` REUSADO TAL-QUAL na pág. 6 (grid 4×2 READ-ONLY já não tem selo). A banda
  magenta de rodapé (item visual do clássico) é desenhada POR CIMA, DEPOIS de `_big_numbers_page`,
  por um helper novo `_classico_banda_magenta_rodape(pdf)` — sem tocar `_big_numbers_page`.
- `_competitors_page` NÃO é reusado tal-qual (a recente usa `_draw_title_band` flush full-width; o
  clássico exige banda turquesa com margem 20px + cantos arredondados + ícone). Criar variante
  `_competitors_page_classico` que REUSA `_point_rows`/`_safe_len` (lógica de lista idêntica) e só
  troca o cabeçalho de banda. NÃO alterar `_competitors_page`.

NOVOS helpers (clássico), prefixo `_classico_`:
- `_classico_title_band(pdf, texto_banda, assets, *, rgb=...)` — banda turquesa com margem 20px,
  cantos arredondados r≈16, altura ≈58, endereço/nome à esquerda (branco) + ícone Ultra à direita
  (fallback gracioso quando `assets["icone"]` é None); título da seção desenhado ABAIXO da banda.
- `_classico_cover_page(pdf, result, assets, *, rotulo)` — capa com endereço ACIMA do subtítulo,
  texto por baseline (`pdf.text`), base do bloco ≈ y 455 (acima da linha branca ~y 460). NÃO
  reusar `_cover_page` (a recente usa `cell`/`multi_cell` e ordem diferente).
- `_classico_map_page(pdf, png, *, banda_texto, titulo_secao, assets)` — usa `_classico_title_band`
  + `_draw_map` (reuso) + `_draw_footer` (reuso). NÃO reusar `_map_page` (banda flush).
- `_classico_competitors_page(pdf, result, png, assets, *, banda_texto)` — banda clássica +
  `_point_rows`/`_safe_len` reusados.
- `_classico_credit_page(pdf, assets, *, rotulo, lat, lng)` — Realização: REUSA o corpo do
  `_credit_page` (turquesa, "Realizacao" 34pt, crédito, método, linha READ-ONLY, rodapé CARTO) e
  ESTENDE com bloco "Link para localizacao do ponto:" + endereço como link clicável + data da
  geração. NÃO alterar `_credit_page` (usado pelo template recente).
- `_classico_banda_magenta_rodape(pdf)` — banda magenta full-width flush-baixo, altura ≈13px com
  offset para cima de ≈13px (não cobrir a marca d'água em y≈`_PAGE_H - _WATERMARK_MARGIN`).
- `_classico_data_extenso(now=None)` — "15 de junho de 2026" (ASCII-safe; injetável p/ teste
  determinístico).

### D-C — Cantos arredondados no fpdf2 (DETERMINÍSTICO)
- fpdf2 instalado = **2.8.7** (`pyproject` pin `>=2.7.0,<3`). `FPDF.rect` tem assinatura
  `rect(x, y, w, h, style=None, round_corners=False, corner_radius=0)` (confirmado por inspeção).
- USAR a API nativa: `pdf.rect(x, y, w, h, style="F", round_corners=True, corner_radius=16)`.
  NÃO há composição manual de arcos. (Se quiser bordas sem stroke, `pdf.set_line_width(0)` e
  style="F" já bastam — não desenhar contorno.)
- Para o ícone à direita dentro da banda: `pdf.image(BytesIO(assets["icone"]), ...)` só quando
  `assets.get("icone") is not None`, dentro de try/except (mesmo padrão de `_draw_full_page_background`).

### D-D — Cor turquesa: QUESTÃO PARA O GATE HUMANO (decisão pendente de Felipe/Vini)
- Divergência: memória/spec do clássico usa `(0,159,160)`; constante existente `ULTRA_TURQUESA=(0,167,157)`.
- DEFAULT PROPOSTO PELO PLANNER (a confirmar no gate): **reusar `ULTRA_TURQUESA=(0,167,157)`** (já é
  a cor do branding atual e dos assets; delta visual mínimo; evita nova constante divergente).
- Se o gate decidir `(0,159,160)`: o Builder adiciona constante local `_CLASSICO_TURQUESA=(0,159,160)`
  e a passa às funções `_classico_*` (NÃO redefinir `ULTRA_TURQUESA`). Magenta da banda de rodapé:
  spec do clássico usa `(199,32,120)`; existe `ULTRA_MAGENTA=(194,60,142)`. MESMA questão de gate —
  default proposto: reusar `ULTRA_MAGENTA`. Laranja `(237,125,49)` da spec NÃO é usado nas 7 páginas
  descritas (só citado na paleta) → não introduzir constante sem uso.

### D-E — Asset `icone_ultra.png` em `_load_branding_assets`
- Adicionar `"icone": None` ao dict inicial e ao loop de carga, com constante
  `_ASSET_ICONE = "icone_ultra.png"`. Carga via `Image.open(...).verify()` no MESMO try/except
  (fallback gracioso → None se ausente/inválido). Isto NÃO altera o comportamento do template
  recente: ele nunca lê `assets["icone"]`, e o dict ganhar uma chave a mais não muda o PDF recente
  (os bytes do PDF não dependem das chaves não lidas). Os testes recentes seguem verdes.
- Teste explícito do fallback: PDF clássico gerado sem `icone_ultra.png` (tmp_path) não levanta
  exceção e produz `/Count 7`.

### D-F — Link clicável na Realização (API exata fpdf2)
- `url = build_search_url(rotulo)` quando `rotulo` é endereço real; senão
  `url = build_search_url(f"{lat:.6f},{lng:.6f}")` (de `result`).
- fpdf2 2.8.7 suporta link em `cell`/`multi_cell` via parâmetro `link=`:
  `pdf.set_xy(x, y); pdf.set_text_color(*_BRANCO); pdf.cell(w, h, _ascii(texto_endereco), link=url)`.
  O `link` aceita URL string diretamente (não precisa `add_link()` para links externos).
- Texto do link = o próprio endereço (`rotulo` truncado p/ caber) precedido da label
  "Link para localizacao do ponto:". Importar `build_search_url` de
  `motor_expansao.api.maps_geocoder` (regex puro, sem Selenium — import no topo do módulo é seguro;
  `maps_geocoder` só importa selenium DENTRO de `MapsGeocoder.__init__`, então o import de módulo
  não arrasta selenium).

### D-G — Roteamento de download = PARÂMETRO `template=` opcional (encapsula, não duplica)
- `gerar_payloads_download_relatorio_censitario` e `render_downloads_relatorio_censitario` ganham
  `template: str | None = None` (default = recente, byte-a-byte inalterado) e `rotulo: str | None = None`.
  Quando `template == "classico"`, roteiam para `gerar_pdf_relatorio_pontual_classico(...)`; caso
  contrário, chamam `gerar_pdf_relatorio_pontual_censitario(...)` exatamente como hoje.
- Justificativa: evita uma 3ª função pública quase idêntica; o default preserva 100% do contrato
  atual desses dois helpers (teste `test_payloads_e_helper_streamlit_expoem_downloads_csv_pdf` segue
  verde sem passar `template`).

## Plano técnico (passos numerados para o Builder)
1. **`censo_report.py` — branding:** adicionar `_ASSET_ICONE = "icone_ultra.png"`; em
   `_load_branding_assets`, incluir `"icone": None` no dict e a tupla `("icone", _ASSET_ICONE)` no
   loop de carga (mesmo try/except + `Image.verify()`).
2. **`censo_report.py` — import:** adicionar no topo `from motor_expansao.api.maps_geocoder import build_search_url`.
   (Confirmar que não cria ciclo de import: `maps_geocoder` não importa `censo_report`.)
3. **Constantes clássicas:** adicionar (a confirmar cor no gate) `_CLASSICO_MARGIN = 20.0`,
   `_CLASSICO_CORNER_RADIUS = 16.0`, `_CLASSICO_BAND_H = 58.0`, `_CLASSICO_MAGENTA_BANDA_H = 13.0`,
   `_CLASSICO_MAGENTA_OFFSET = 13.0`. Cor turquesa/magenta: usar `ULTRA_TURQUESA`/`ULTRA_MAGENTA`
   por default (ver D-D); se gate pedir, `_CLASSICO_TURQUESA=(0,159,160)` / `_CLASSICO_MAGENTA=(199,32,120)`.
4. **`_classico_title_band(pdf, texto_banda, assets, *, rgb)`:** `pdf.rect(_CLASSICO_MARGIN,
   _CLASSICO_MARGIN, _PAGE_W-2*_CLASSICO_MARGIN, _CLASSICO_BAND_H, style="F", round_corners=True,
   corner_radius=_CLASSICO_CORNER_RADIUS)`; texto branco (endereço/nome) à esquerda; ícone à direita
   via `pdf.image(BytesIO(assets["icone"]), ...)` em try/except só se `assets.get("icone")`. Título
   da seção ABAIXO da banda (`pdf.set_xy(_CLASSICO_MARGIN, _CLASSICO_MARGIN+_CLASSICO_BAND_H+12)`).
5. **`_classico_cover_page(pdf, result, assets, *, rotulo)`:** `add_page` + `_draw_full_page_background`
   (asset "capa" → turquesa sólido fallback). Texto branco por BASELINE (`pdf.text`), zona limpa
   inferior-direita (x≥478): endereço (de `rotulo`, se não-coordenada) ACIMA, subtítulo
   "Relatorio Pontual Censitario - Raio 1,5 km | {mês/ano}" ABAIXO; base do bloco ≈ y 455.
   `mês/ano` via `_classico_data_extenso`/strftime ASCII-safe.
6. **`_classico_map_page(pdf, png, *, banda_texto, titulo_secao, assets)`:** `add_page` +
   `_draw_full_page_background(assets["conteudo"]→branco)` + `_classico_title_band` + `_draw_map`
   (reuso) ou texto "Mapa indisponivel..." quando `png` None + `_draw_footer` (reuso).
7. **`_classico_competitors_page(pdf, result, png, assets, *, banda_texto)`:** igual ao 6 mas com
   o layout de `_competitors_page` (mapa à esquerda + lista à direita) usando `_point_rows`/`_safe_len`
   reusados; cabeçalho "(N no total)"/"... e mais N" idêntico ao recente.
8. **`_classico_banda_magenta_rodape(pdf)`:** `pdf.set_fill_color(*ULTRA_MAGENTA)` +
   `pdf.rect(0, _PAGE_H-_CLASSICO_MAGENTA_BANDA_H-_CLASSICO_MAGENTA_OFFSET, _PAGE_W,
   _CLASSICO_MAGENTA_BANDA_H, style="F")` (full-width, sem cantos, sem margem lateral). Calibrar o
   offset vendo o PDF de simulação (ver QUESTÃO ABERTA Q3).
9. **`_classico_credit_page(pdf, assets, *, rotulo, lat, lng)`:** corpo igual ao `_credit_page`
   (turquesa, "Realizacao" 34pt, crédito, método 1 frase, linha READ-ONLY, rodapé CARTO) + bloco
   "Link para localizacao do ponto:" com `pdf.cell(..., link=build_search_url(endereco_ou_coord))`
   + "Data de geracao: {data por extenso}". SEM logo, SEM cartão de contato.
10. **`gerar_pdf_relatorio_pontual_classico(...)`:** monta as 7 páginas na ordem canônica reusando
    `_normalize_mapas_by_key` (layers densidade/renda/score/concorrentes) e `_big_numbers_page`
    (pág. 6) + `_classico_banda_magenta_rodape` por cima; ao final, o MESMO loop de marca d'água do
    gerador recente (`_draw_watermark`, capa branca / demais cinza). Retorna `bytes(pdf.output())`.
11. **`gerar_payloads_download_relatorio_censitario` e `render_downloads_relatorio_censitario`:**
    adicionar `template: str | None = None` e `rotulo: str | None = None`; rotear ao gerador clássico
    quando `template == "classico"` (default = recente, inalterado).
12. **Testes novos** (ver seção Validações).

## Arquivos afetados
- `src/motor_expansao/dashboard/censo_report.py` — ÚNICA alteração de produção (novos helpers
  `_classico_*`, nova função pública, chave `"icone"` em `_load_branding_assets`, import de
  `build_search_url`, params opcionais nos 2 helpers de download). Tudo o que o template recente usa
  fica byte-a-byte inalterado.
- `tests/unit/test_relatorio_pontual_censitario_export.py` (ou novo
  `tests/unit/test_relatorio_pontual_classico_export.py`) — testes da variante (fixtures sintéticas).

## Dependências
- fpdf2 2.8.7 (já instalado; `round_corners`/`corner_radius` nativos; `link=` em cell/multi_cell).
- `motor_expansao.api.maps_geocoder.build_search_url` (regex puro; sem selenium no import de módulo).
- Motor INTOCADO: `analisar_ponto_censitario_setores`, `render_mapas_censitarios_combinados`.

## Riscos técnicos
- **Import `maps_geocoder` em `censo_report`:** confirmar ausência de ciclo (maps_geocoder não
  importa censo_report — verificado: importa só `re`/`urllib`). Selenium só é importado dentro de
  `MapsGeocoder.__init__`, então o import de módulo é seguro no caminho do dashboard.
- **`link=` em `cell`/`multi_cell`:** em fpdf2, para URL externa basta a string; se o Builder
  encontrar atrito, usar `pdf.link(x, y, w, h, url)` desenhando a área sobre o texto. Plano A = `link=`.
- **Offset da banda magenta e baseline da capa:** valores ~13px/y455 são da memória; calibrar
  vendo `data/outputs/SIMULACAO_relatorio_caiubi_classico.pdf` (NÃO commitar). Q3 do gate.
- **`icone_ultra.png` ausente no repo:** asset novo; fallback gracioso obrigatório e testado (D-E).
- **Marca d'água:** reusar `_draw_watermark` BYTE-A-BYTE; a banda magenta NÃO pode cobri-la.

## Critérios de aceite finais
1. `gerar_pdf_relatorio_pontual_censitario` sem novo param → PDF byte-a-byte igual; TODOS os testes
   existentes de `test_relatorio_pontual_censitario_export.py` verdes.
2. `gerar_pdf_relatorio_pontual_classico` produz 7 páginas (`/Count 7`), `%PDF-1.4`, todas as
   `PDF_SECTION_HEADERS` presentes, ≥4 imagens de mapa, banda turquesa com cantos arredondados,
   capa com endereço acima do subtítulo, concorrentes com lista de distâncias, Big Numbers sem selo,
   Realização com link clicável (`https://www.google.com/maps/search/` nos bytes).
3. Fallback gracioso: PDF clássico sem qualquer asset (incl. `icone_ultra.png`) gera sem exceção.
4. Fixtures sem PII (nome fictício, coordenada sintética); `_PII_FORBIDDEN` ausente do PDF clássico.
5. ruff + mypy limpos (sem novos erros; per-file-ignores preservados).
6. READ-ONLY M1: zero escrita em score/carteira/plano/artefatos.
7. Suite full verde (baseline 884 passed, 1 skipped) sem regressão.

## Validações obrigatórias (comandos concretos)
- Módulo censo:
  `python -m pytest -q tests/unit/test_relatorio_pontual_censitario_export.py`
  Critério: 0 falhas (template recente intacto + novos testes do clássico).
- Suite full:
  `python -m pytest -q`
  Critério: sem regressão vs baseline (884 passed, 1 skipped).
- ruff:
  `python -m ruff check src/motor_expansao/dashboard/censo_report.py tests/`
  Critério: 0 erros novos.
- mypy:
  `python -m mypy src/motor_expansao/dashboard/censo_report.py`
  Critério: 0 erros novos.
- Smoke import:
  `python -c "import streamlit_app; from motor_expansao.dashboard.censo_report import gerar_pdf_relatorio_pontual_classico"`
  Critério: import sem erro.

### Testes novos a adicionar (fixtures sintéticas, sem PII — reusar `_sample_result()`/`_RESIDUAL_OK`)
- `test_classico_gera_7_paginas_e_secoes`: `/Count 7`, `%PDF-1.4`, todas `PDF_SECTION_HEADERS`,
  `_count_layer_titles == 3`, `>= 4` `/Subtype /Image`.
- `test_classico_link_clicavel_na_realizacao`: `b"https://www.google.com/maps/search/"` nos bytes;
  com `rotulo="Av Teste, 100"` → query do endereço; sem `rotulo` → query da coordenada.
- `test_classico_offline_safe_sem_assets` (tmp_path, incl. sem `icone_ultra.png`): sem exceção,
  `/Count 7`.
- `test_classico_sem_pii_de_pessoas`: `_PII_FORBIDDEN` ausente.
- `test_classico_marca_dagua_solicitante`: `b"Ultra Academia | Analista Teste"` presente; capa branca.
- `test_classico_template_recente_inalterado`: bytes de
  `gerar_pdf_relatorio_pontual_censitario(...)` idênticos antes/depois de chamar o clássico (mesma
  fixture) — garante isolamento.
- `test_downloads_roteia_template_classico`: `gerar_payloads_..._censitario(..., template="classico")`
  retorna PDF clássico; sem `template` retorna o recente (default inalterado).

## Fora de escopo (explícito)
- Alterar `gerar_pdf_relatorio_pontual_censitario`, `_cover_page`, `_map_page`, `_competitors_page`,
  `_credit_page`, `_big_numbers_page`, `_draw_title_band` (usados pelo template recente).
- `analisar_ponto_censitario_setores`, raio 1,5 km, método de interseção (INTOCADOS).
- `render_mapas_censitarios_combinados` (INTOCADO; consome o mesmo dict de camadas).
- score/pesos/artefatos M1 (READ-ONLY; DEC-001).
- Versionar PDF/PII real; dependência de API ao vivo no dashboard; Selenium no render; selo GO/NO-GO;
  logo na Realização; cartão de contato `image24.png`.
- Constante de laranja `(237,125,49)` (sem uso nas 7 páginas).

## QUESTÕES ABERTAS — O GATE HUMANO (Felipe+Vini) PRECISA DECIDIR ANTES DO BUILDER
- **Q1 (cor turquesa):** `(0,159,160)` (spec/memória) vs `(0,167,157)` (`ULTRA_TURQUESA` existente).
  Default proposto: reusar `ULTRA_TURQUESA=(0,167,157)`. DECISÃO PENDENTE.
- **Q1b (magenta):** `(199,32,120)` (spec) vs `(194,60,142)` (`ULTRA_MAGENTA`). Default proposto:
  reusar `ULTRA_MAGENTA`. DECISÃO PENDENTE.
- **Q2 (`icone_ultra.png`):** o PNG (marca ▶ branca transparente) será fornecido em `data/ultra/`
  ANTES do Builder rodar? Se NÃO, o Builder entrega com fallback gracioso (banda sem ícone) e o
  ícone aparece quando o asset for adicionado no host/volume. CONFIRMAR.
- **Q3 (offsets visuais):** offset da banda magenta (~13px) e baseline da capa (base ~y455 acima da
  linha branca ~y460) são estimativas da memória; precisam de inspeção visual do PDF de simulação
  `data/outputs/SIMULACAO_relatorio_caiubi_classico.pdf` para calibração final. CONFIRMAR valores.

## Alerta de aprovação
ATENÇÃO: ESTA TAREFA É DE CRITICIDADE ALTA — CAMINHO DE GERAÇÃO DO PDF DE PRODUÇÃO E BRANDING/LGPD.
O GATE É VISUAL (FELIPE + VINI) **E** APROVAÇÃO TÉCNICA HUMANA, OBRIGATÓRIO **ANTES** DO BUILDER.
NÃO É CRÍTICA (READ-ONLY SOBRE O M1; NÃO TOCA SCORE/PESOS/CARTEIRA/PLANO/ARTEFATOS), MAS A ESTEIRA
ALTA EXIGE REVISÃO HUMANA DO PLANO E DAS 3 QUESTÕES ABERTAS (Q1/Q1b COR, Q2 ASSET, Q3 OFFSETS)
ANTES DE LIBERAR A EXECUÇÃO. O USUÁRIO DEVE LER ESTE HANDOFF E CONFIRMAR.

## Guardrails ativos
- §2 CLAUDE.md anti-PII: .pptx/PDF nunca versionados; `image24.png` NUNCA embutido; fixtures com
  nome fictício e coordenada sintética.
- §2 CLAUDE.md: não criar dependência de API ao vivo no dashboard — `extract_any_coord`/
  `build_search_url` são regex puros, sem Selenium; contextily já é lazy+cache (DEC-004).
- §4/§5 guardrail permanente: visualizações não recalculam/alteram score_priorizacao,
  hex_score_estrutural, carteira, plano ou artefatos oficiais do M1.
- DEC-001 (pesos/fórmula M1 INALTERADOS), DEC-004 (basemap só no render do relatório),
  BLK-EST-03/FU1 (marca d'água reusa `_draw_watermark`/`_watermark_text` exatos).
- Paths pré-sujos NÃO commitar: `setores_censitarios_2022_geo/_metadata.json`,
  `relatorio_pontual_censitario_base_geo.md`, `SIMULACAO_relatorio_caiubi_classico.pdf`.
