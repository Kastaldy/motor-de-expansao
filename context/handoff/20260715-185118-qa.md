# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento do ciclo pelo orquestrador (modo MERGE-HUMANO): housekeeping do Passo 6.0
(`housekeeping_move_block.py BLK-TP-03-FU1` no MESMO PR) + confirmar o check `test` VERDE no CI
(Python 3.11) antes do merge humano — ver ressalva abaixo.

## VEREDITO
APROVADO COM RESSALVAS

## Justificativa
O bloco está implementado à risca do plano (passos 1-14) e do gate de UX (cor `#7C5CFF`, rótulo
"Vazio competitivo LC", tooltip de 4 linhas): 100% aditivo (+413/-0 em 3 arquivos), READ-ONLY M1,
sem rede, com 5 testes novos verdes, ruff/mypy limpos e `import streamlit_app` ok. A ÚNICA ressalva
é NÃO-BLOQUEANTE e alheia ao bloco: a suíte FULL tem 4 falhas que são **pré-existentes na base
limpa** (provado por `git stash`), em módulos sem qualquer relação com o diff, e são artefatos do
ambiente **Python 3.14 local** (ex.: `nan is None`) — zero regressão introduzida por este ciclo.

## Problemas críticos (bloqueadores)
- nenhum.

## Problemas médios (não bloqueadores)
- **4 falhas PRÉ-EXISTENTES na suíte FULL, alheias ao escopo** (não são regressão deste bloco):
  `tests/unit/test_score_retencao_territorial.py::test_schema_exato_do_output`,
  `tests/unit/test_validation_dataset.py::{test_resolve_hex_valido_e_nulo,test_join_label_by_name_casado_e_nao_casado}`,
  `tests/unit/dimensionamento/test_batch_viabilidade.py::test_rodar_candidato_grade_serializa`.
  Provado pré-existente: rodei os 4 na BASE limpa (mudanças em `git stash`) → falham idênticos;
  módulos `lifetime`/`validation_dataset`/`dimensionamento` não têm caminho de import para
  `dashboard/components.py`/`pages.py`. Natureza = ambiente Python 3.14 local (ex.: `assert nan is
  None` — representação `nan` vs `None`), determinístico (falham igual em serial e em `-n auto`, NÃO
  é flakiness de paralelismo). **Ação p/ o orquestrador:** o gate real de merge (DEC-016) é o check
  `test` no CI (Python 3.11, só `.[dev]`), cuja contagem difere do 3.14 local; confirmar que o
  `test` está VERDE no CI antes do merge humano. Se essas 4 também falharem no CI, é condição
  pré-existente do repo (bloco de correção próprio), não deste PR.

## Melhorias opcionais
- O checkbox é renderizado em largura total (fora do `with ctrl_col2:`), abaixo das duas colunas de
  controle — placement válido e consistente com o plano; se desejarem alinhá-lo dentro da coluna de
  overlays no futuro, é ajuste cosmético de 1 nível de indentação.

## Testes faltantes
- nenhum. Os 5 testes novos cobrem: builder on/off/None/vazio (no-op silencioso), imutabilidade de
  frame + fill/line color do hex_layer principal (READ-ONLY M1), checkbox default OFF + key canônica,
  ativação (deck +1 layer, cor `_VAZIOS_LC_FILL_RGBA`, tooltip formatado) e ocultação com parquet
  ausente/vazio/sem `hex_id` (3 casos) + caption "indisponível".

## Riscos remanescentes
- O parquet `data/staging/vazios_competitivos_lc.parquet` é gitignored e NÃO existe neste worktree;
  a feature só se exibe onde o parquet estiver materializado (VPS/prod). Comportamento correto e
  coberto: ausente → checkbox oculto + caption clara, sem exceção. Deploy de DADOS (parquet) é passo
  operacional separado (fora deste bloco).

## Saída literal das validações (re-executadas pelo QA)

### 1) Suíte FULL — `python -m pytest -n auto` (gate único do ciclo)
```
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_schema_exato_do_output
FAILED tests/unit/test_validation_dataset.py::test_resolve_hex_valido_e_nulo
FAILED tests/unit/test_validation_dataset.py::test_join_label_by_name_casado_e_nao_casado
FAILED tests/unit/dimensionamento/test_batch_viabilidade.py::test_rodar_candidato_grade_serializa
=== 4 failed, 1725 passed, 85 skipped, 546624 warnings in 454.60s (0:07:34) ===
```
(Não houve WinError 6; `-n auto` rodou até o fim, exit 0 do pipeline.)

### 1b) Prova de pré-existência (4 falhas na BASE limpa, mudanças em `git stash`)
```
=== STASHED — rodando os 4 na base limpa ===
FAILED tests/unit/test_score_retencao_territorial.py::test_schema_exato_do_output
FAILED tests/unit/test_validation_dataset.py::test_resolve_hex_valido_e_nulo
FAILED tests/unit/test_validation_dataset.py::test_join_label_by_name_casado_e_nao_casado
FAILED tests/unit/dimensionamento/test_batch_viabilidade.py::test_rodar_candidato_grade_serializa
4 failed, 3071 warnings in 1.69s
=== stash restaurado (git stash pop) ===
```
Determinismo (serial, mesmos 4 falham → não é flakiness de xdist):
```
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_schema_exato_do_output
FAILED tests/unit/test_validation_dataset.py::test_resolve_hex_valido_e_nulo
FAILED tests/unit/test_validation_dataset.py::test_join_label_by_name_casado_e_nao_casado
FAILED tests/unit/dimensionamento/test_batch_viabilidade.py::test_rodar_candidato_grade_serializa
4 failed, 3071 warnings in 2.59s
```
Amostra da natureza (env 3.14, não defeito de lógica):
```
>       assert out.loc[1, "hex_id"] is None
E       assert nan is None
tests\unit\test_validation_dataset.py:267: AssertionError
```

### 2) `python -m pytest -q tests/integration/test_streamlit_app.py -k "vazios"`
```
5 passed, 249 deselected, 12955 warnings in 4.80s
```

### 3) `python -c "import streamlit_app; print('import ok')"`
```
... WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
import ok
```
(Warnings de bare-mode do Streamlit são ruído esperado/pré-existente.)

### 4) `python -m ruff check src/.../components.py src/.../pages.py tests/integration/test_streamlit_app.py`
```
All checks passed!
```

### 5) `python -m mypy src/.../components.py src/.../pages.py`
```
Success: no issues found in 2 source files
```

### Escopo aditivo — `git diff --stat` e filtro de arquivos intocados
```
 src/motor_expansao/dashboard/components.py |  97 +++++++++++
 src/motor_expansao/dashboard/pages.py      |  55 ++++++
 tests/integration/test_streamlit_app.py    | 261 +++++++++++++++++++++++++++++
 3 files changed, 413 insertions(+)
```
`git diff --stat -- constants.py streamlit_app.py demanda_revelada/` → VAZIO (zero linhas tocadas).
`_downsample_map_index`/`MAP_POINT_LIMIT`/`MAP_POINT_LIMIT_LARGE`/`MAP_SOURCE_COLUMNS_*`: não
aparecem como hunk alterado (só o `def _downsample_map_index(` como contexto após a nova função).
`git status --porcelain -- data/` → VAZIO (nada escrito em `data/`).

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do
caminho crítico. Este bloco é feature de UI (dashboard), não tooling que dependa de config/artefatos
reais de produção — a exceção do §(b) da skill não se aplica. Os testes usam mocks de widgets
Streamlit e do loader `_load_vazios_competitivos_lc_cached` (fixture sintética legítima de teste de
UI, prática normal e esperada — não é bypass). A suíte FULL rodou 1× aqui como gate único.

## Conferência cruzada com log do Builder
Bate. Builder reportou `-k vazios` = 5 passed → confirmado (5 passed). Suíte de integração completa
254 passed → não re-rodei isolada (a FULL a engloba). ruff "All checks passed" e mypy "Success: no
issues" → confirmados idênticos. O Builder documentou (corretamente) que a suíte FULL era gate do QA
e que os 4 parquets oficiais não existem neste worktree — verifiquei e confirmei ambos. O Builder
disse "6 testes novos"; o real são **5 funções de teste** novas (a 6ª entrada da lista dele era o
import de `_VAZIOS_LC_FILL_RGBA`) — divergência de contagem cosmética, sem impacto.

## Guardrails verificados
- score_priorizacao não alterado: sim — só LIDO para `tooltip_line_4` ("Score M1: ..."); grep no
  diff por escrita em `PESOS_HEX`/`renda=0.4`/`pop=0.6`/`hex_score_estrutural=` → NENHUMA.
- Artefatos M1 preservados: sim, por construção — os 4 parquets oficiais estão AUSENTES neste
  worktree (staging gitignored, não materializado); nenhum pipeline de regeneração foi executado; e
  `git status -- data/` está VAZIO (zero escrita em `data/`). Não há mtime a comparar porque não
  existem — critério satisfeito por ausência de qualquer escrita (documentado honestamente; sem
  inventar mtime).
- DEC-001 (renda=0.40/pop=0.60): intocados. DEC-012 (anti-PII): nenhuma coluna de PII adicionada ao
  `layer_df` (só `hex_id` + tooltips); grep por `hex_lat/hex_lng/employee/company/cpf/email` no diff
  → nenhuma. §2 (sem rede): loader é `Path.exists()` + `pd.read_parquet` local; zero fetch.
- Aderência ao gate de UX (Felipe, 2026-07-15): cor `#7C5CFF` / `[124,92,255,60]`+`[124,92,255,230]`
  ✓; rótulo "Vazio competitivo LC" ✓; tooltip título + 4 linhas (Membros >5km / UF / Município /
  Score M1) ✓; help text conforme recomendação do Planner ✓; checkbox default OFF, key
  `mapa_territorial_vazios_lc` ✓.
- Ordem do splice: `enabled_overlays` do multiselect (l.4377) → splice do checkbox (l.4385-4406) →
  consumidores (`_render_unified_legend` l.4456, `build_unified_map_figure_cached` l.4467/4477). Todos
  os 3 consumidores só fazem `"x" in enabled_overlays` (checagem de string), nunca `OVERLAYS["x"]` —
  splice seguro sem tocar `constants.py`. ✓
- Validações re-executadas pelo QA (sem bypass): pytest FULL `-n auto` → 4 pré-existentes (base) +
  1725 passed; `-k vazios` → 5 passed; import → ok; ruff → All checks passed; mypy → Success.
- Escopo respeitado: sim — só os 3 arquivos previstos; passo opcional de legenda implementado (não
  pulado); Builder não tocou `tasks/backlog.md`/`tasks/completed.md` (correto p/ este ciclo — move é
  do orquestrador no Passo 6.0).

## Housekeeping (modo MERGE-HUMANO)
Este é ciclo INTERATIVO (merge humano). Por instrução explícita do orquestrador, o housekeeping de
`backlog.md`/`completed.md` foi DIFERIDO ao orquestrador no fechamento (Passo 6.0) — logo NÃO exijo
`--check` verde agora. Confirmei que o Builder NÃO tocou `tasks/backlog.md`/`tasks/completed.md`
(`git status --porcelain -- tasks/backlog.md tasks/completed.md` = VAZIO). O move via
`scripts/housekeeping_move_block.py BLK-TP-03-FU1` (stub no backlog + bloco byte-idêntico em
completed + teste `test_housekeeping_helper.py`) fica a cargo do orquestrador no MESMO PR.

## Decisão recomendada
Fechar o ciclo (merge humano). Antes do merge, o orquestrador deve: (1) executar o housekeeping do
Passo 6.0 (helper), e (2) confirmar o check `test` VERDE no CI (Python 3.11) — as 4 falhas locais
são de ambiente 3.14 e provavelmente não reproduzem no CI 3.11; se reproduzirem, abrir bloco de
correção próprio (condição pré-existente do repo, não deste PR). Nenhuma reabertura para o Builder.
