# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-EST-01-FU2 — Marca d'água visível-porém-discreta + branca na capa (turquesa)

## O que foi feito
Três mudanças cirúrgicas em `src/motor_expansao/dashboard/censo_report.py`:

1. **Constantes `_WATERMARK_*`**: `_WATERMARK_ALPHA` 0.40 → 0.65; `_WATERMARK_FONT_PT` 9 → 10; adicionada `_WATERMARK_RGB_COVER = (255, 255, 255)` junto das demais constantes. `_WATERMARK_RGB`, `_WATERMARK_ANGLE`, `_WATERMARK_MARGIN`, `_WATERMARK_BASE` inalterados.

2. **Assinatura de `_draw_watermark`**: mudada para `def _draw_watermark(pdf: _UltraPDF, text: str, *, rgb: tuple[int, int, int] = _WATERMARK_RGB) -> None:`. O corpo passa a usar `pdf.set_text_color(*rgb)` em vez de `*_WATERMARK_RGB` fixo. Posição (canto inferior-direito), orientação horizontal, `local_context(fill_opacity=_WATERMARK_ALPHA)` e `pdf.text(x, y, text)` preservados. Docstring atualizada para mencionar o parâmetro `rgb` e a cor condicional por página.

3. **Loop por-página (linhas 635-638)**: substituído para escolher a cor antes de chamar: `rgb = _WATERMARK_RGB_COVER if page_number == 1 else _WATERMARK_RGB` e chamar `_draw_watermark(pdf, wm_text, rgb=rgb)`. Página 1 = capa → branco (255,255,255); páginas 2-7 → cinza (120,120,120).

Não há outras chamadas a `_draw_watermark` além do loop (verificado com grep). Arquivo de testes não alterado (os 3 `test_pdf_marca_dagua_*` assertam apenas texto/contagem, sem cor nem alpha).

## Arquivos alterados
- `src/motor_expansao/dashboard/censo_report.py` — constantes `_WATERMARK_ALPHA`/`_WATERMARK_FONT_PT`/`_WATERMARK_RGB_COVER`, assinatura e docstring de `_draw_watermark`, loop por-página (linhas 635-638)

## Validações executadas
- **pytest (subconjunto impactado)**: `tests/unit/test_relatorio_pontual_censitario_export.py tests/unit/test_relatorio_pontual_censitario_mapa.py` → **29 passed, 0 failed, 0 skipped**
  - Os 3 testes `test_pdf_marca_dagua_*` seguem verdes (assertam texto/contagem, não cor nem alpha)
- **import smoke**: `python -c "import streamlit_app"` → **import ok**
- **ruff check** `src/motor_expansao/dashboard/censo_report.py` → **All checks passed!**
- **mypy** `src/motor_expansao/dashboard/censo_report.py` → **Success: no issues found in 1 source file**

## Problemas encontrados
Nenhum.

## Pendências
- Verificação visual por render (orquestrador gera PDF de amostra para confirmar capa com marca branca sobre fundo turquesa e páginas 2-7 com marca cinza legível) — parte do gate do QA/orquestrador, não do Builder.
- Suíte completa fica a cargo do QA (gate único por ciclo).

## Riscos remanescentes
Nenhum técnico. O parâmetro `rgb` tem default = `_WATERMARK_RGB`; qualquer chamada sem `rgb=` continua funcionando sem alteração (retrocompatível). Cor branca na capa não afeta anti-PII (`set_compression(False)` e `pdf_version="1.4"` inalterados; texto da marca inalterado).

## Guardrails verificados
- score_priorizacao não alterado: sim (não aplicável — arquivo de visualização/relatório)
- Artefatos M1 preservados: sim (nenhum parquet, score, carteira ou plano tocado)
- Dashboard offline mantido: sim (nenhum arquivo de dashboard alterado; sem dependência de API ao vivo)
- Anti-PII §4: sim (stream OFF preservado; texto/solicitante inalterados; `image24.png` não embutido; PDF/pptx não versionados)
- Paths pré-sujos não tocados: sim (`data/outputs/setores_censitarios_2022_geo/_metadata.json` e `data/reports/relatorio_pontual_censitario_base_geo.md` intocados)
