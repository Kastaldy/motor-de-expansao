# Handoff (snapshot) — Planner — BLK-OPS-10 (Automatizar housekeeping de concluídos no Passo 6)

## Skill que gerou este handoff
Planner (consolidado no orquestrador; contexto fresco do BLK-OPS-09)

## Próxima Skill recomendada
[APROVAÇÃO HUMANA] → Builder (somente após "aprovar")

## Bloco
BLK-OPS-10 — Alta criticidade; ALTERA A PRÓPRIA ORQUESTRAÇÃO (run-cycle.md + qa_analyzer.md).
Branch `ciclo/BLK-OPS-10` (de `main` pós-merge BLK-OPS-09). dry_run: false.

## Arquivos-alvo (escopo)
- `.claude/commands/run-cycle.md` (adicionar Passo 6.e + ajustar "Ordem de fechamento" + ajustar 6.c)
- `prompts/qa_analyzer.md` (adicionar checagem de housekeeping)
NÃO tocar: código, M1, CLAUDE.md, backlog.md (conteúdo), nenhum script novo em scripts/.

## Tensões de design identificadas (resolvidas na proposta)
1. SEQUÊNCIA: a tarefa pede "QA valida stub+completed ANTES do veredito final", mas o move (6.e)
   fecha o ciclo DEPOIS do QA. Resolução proposta: 6.e roda ANTES do commit (6.a); o QA ganha uma
   checagem de fechamento que o ORQUESTRADOR executa logo após 6.e (mesma lógica do "valida sozinho"
   do 6.c). qa_analyzer.md documenta os 2 checks como spec única de "housekeeping válido".
2. BLOCOS AD-HOC: muitos ciclos (inclusive BLK-OPS-10) NÃO vêm do backlog. 6.e é CONDICIONAL:
   só move se o bloco existir em backlog.md; senão, no-op (resumo já vai a completed.md via 6.2).
3. HELPER: BLK-OPS-09 usou helper Python descartável (byte-level). Como "NÃO tocar código", 6.e
   descreve o helper como procedimento ad-hoc inline (sem criar arquivo em scripts/).
4. DRY-RUN (6.c) vs 6.e: o dry-run dummy Baixa agora precisa exercitar 6.e. Para isso adiciona um
   bloco DUMMY ao backlog NA BRANCH do dry-run, fecha o ciclo (6.e move), valida stub+completed,
   e ABANDONA a branch (git switch) — NUNCA mergeia → main não é poluído com dummy.

## Veredito do Planner
Plano pronto e apresentado ao humano para aprovação. Nenhuma edição feita ainda nos arquivos-alvo.
