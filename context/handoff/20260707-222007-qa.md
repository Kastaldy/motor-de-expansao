# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Block Orchestrator (fechamento manual)

## VEREDITO
APROVADO

## Justificativa
Os 2 arquivos novos (backtest LOO + 15 testes) passam ruff/mypy/smoke; a suíte FULL fica em 1493 passed
com apenas as 4 falhas + 1 erro PRÉ-EXISTENTES/ambientais (Plus Code `openlocationcode` ×4, matplotlib
ausente ×1), nenhuma referenciando o módulo novo; `viabilidade_ponto.py` diff VAZIO, mtimes M1 inalterados
(1780501621/1780501631), loop_guard OK, DEC-009 confirmado em código e housekeeping executado com --check OK.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- `data/analysis/viabilidade_backtest_ultra.md` usa travessão `—` e outros glifos fora de latin-1; como é
  relatório `.md` gitignored (não PDF `fpdf2`), a regra §2 de ASCII-no-PDF não se aplica — sem impacto.

## Testes faltantes
- nenhum (15 testes cobrem carregamento, LOO exclusão/reset, kwargs do motor coordless, DEC-009 premissa,
  faixa-contém-real, backtest, métricas MAE/viés, motor real sem mock, anti-PII e materialização).

## Riscos remanescentes
- N=54 pequeno e LOO instável nos extremos (documentado nos caveats do relatório); é medição honesta, não bug.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suite FULL (gate único) — `python -m pytest -n auto -q`
```
=========================== short test summary info ============================
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_curto_usa_localidade_mock
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_curto_usa_localidade_mockada
ERROR tests/unit/demanda_revelada/test_viz_atratividade.py
4 failed, 1493 passed, 4 skipped, 19 warnings, 1 error in 402.67s (0:06:42)
```
Causa confirmada por re-execução isolada: `ModuleNotFoundError: No module named 'openlocationcode'` (4 falhas
Plus Code) e `ModuleNotFoundError: No module named 'matplotlib'` (erro de coleta test_viz_atratividade). Todas
na lista de aceite do handoff; nenhuma referencia o módulo novo. O flake xdist `test_piloto_...mercado` NÃO
manifestou neste run (passou) — 4 falhas vs 5 do Builder é a ausência desse flake, esperado.

### 2. ruff — `ruff check backtest_viabilidade.py test_backtest_viabilidade.py`
```
All checks passed!
```

### 3. mypy — `mypy src/motor_expansao/dimensionamento/backtest_viabilidade.py`
```
Success: no issues found in 1 source file
```

### 4. Smoke import
```
ok
```

### 5. viabilidade_ponto.py intocado — `git diff HEAD -- viabilidade_ponto.py`
```
(saída vazia; EXIT 0)
```

### 6. Guardrails M1 (mtime dos artefatos oficiais)
```
1780501621 /repo/data/staging/brasil_estrutural.parquet
1780501631 /repo/data/staging/brasil_priorizados.parquet
```
Valores INALTERADOS (== esperados 1780501621 / 1780501631).

### 7. loop_guard — `python scripts/loop_guard.py --base HEAD~1`
```
GUARD OK: 5 caminho(s) alterado(s), nenhum proibido.
```

### 8. DEC-009: demanda como premissa (confirmação em código)
`backtest_viabilidade.py::rodar_unidade_loo`:
- L214 `demanda_premissa=alunos_real` (= `alunos_total` REAL da unidade; nunca lat/lng)
- L218 `setores_df=None` (COORDLESS explícito) em TODAS as chamadas
- L212–213 `lat=LAT_COORDLESS`, `lng=LNG_COORDLESS` (= 0.0, placeholders, não preditores)
- L217 `base_calibracao_df=base_loo` (LOO via `calcular_loo_base` + `reset_index(drop=True)` — nunca inclui i)

### 9. housekeeping --check (após EXECUTAR o move)
`--check` inicial retornou EXIT=1 ("stub ausente no backlog"); executado o move e re-verificado:
```
$ python scripts/housekeeping_move_block.py BLK-VIAB-04 --date 2026-07-07
OK: movido BLK-VIAB-04 (1719 bytes) -> tasks/completed.md; stub em tasks/backlog.md.

$ python scripts/housekeeping_move_block.py BLK-VIAB-04 --check
OK: BLK-VIAB-04 movido (stub no backlog + bloco em completed.md).
EXIT=0
```
Heading `### BLK-VIAB-04` em backlog.md: 0 · em completed.md: 1.

### 10. Testes dos novos módulos dimensionamento — `pytest tests/unit/dimensionamento/ -q`
```
301 passed, 1 warning in 54.03s
```
Inclui os 15 testes de `test_backtest_viabilidade.py` (todos verdes).

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock do caminho
crítico. A suíte FULL rodou 1× como gate único (`-n auto`). Os testes do módulo mockam `analisar_viabilidade_ponto`
APENAS nos casos de fluxo/ranking (fixture legítima de isolamento), e o teste `test_motor_importado_real_nao_mock`
exercita o MOTOR REAL sem mock — não é bypass. O relatório real foi gerado pelo Builder contra o parquet real
das 54 unidades. PII guard usa a constante REAL `PII_COLUNAS_PROIBIDAS` do `config.py`.

## Conferência cruzada com log do Builder
Bate. ruff/mypy/smoke/diff-vazio/mtime/loop-guard idênticos. Suíte: Builder reportou 1492 passed + 5 failed +
1 error; QA obteve 1493 passed + 4 failed + 1 error — a diferença é o flake xdist `test_piloto_...mercado` que
o Builder viu falhar e aqui passou (por isso +1 passed / -1 failed). Todas as demais falhas coincidem e são
ambientais. Housekeeping: o Builder não havia executado o move (esperado — é passo do fechamento); o QA executou.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; nenhuma escrita em score/pesos/artefatos M1)
- Artefatos M1 preservados: sim (mtimes brasil_estrutural=1780501621 / brasil_priorizados=1780501631 inalterados)
- Validações re-executadas pelo QA (sem bypass): pytest FULL → 1493 passed (4+1 env); ruff → passed; mypy → ok;
  smoke import → ok; git diff viabilidade_ponto.py → vazio; loop_guard → GUARD OK; dimensionamento → 301 passed;
  housekeeping --check → OK
- Escopo respeitado: sim — só os 2 arquivos novos (commit e12f0cc) + fechamento (backlog/completed/handoffs).
  `config.py` mostra diff mas é 100% EOL CRLF→LF (`--ignore-all-space` = zero conteúdo; era `M` pré-existente
  no git status inicial do ciclo, não stageado pelo Builder). Relatório `.md` gitignored (confirmado por
  `git check-ignore`).

## Decisão recomendada
Fechar ciclo. BLK-VIAB-04 APROVADO.
