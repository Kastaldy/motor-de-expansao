# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador: Passo 6.0 housekeeping move de BLK-RELMUN-01 + commit por path)

## VEREDITO
APROVADO

## Justificativa
A entrega do BLK-RELMUN-01 implementa o Relatório Municipal em módulo NOVO e disjunto
(`relatorio_municipal.py`), READ-ONLY sobre o M1, com as decisões D1–D9 do gate fielmente
codificadas; suíte completa verde (1055 passed, 1 skipped, 0 failed), ruff/mypy limpos, import ok,
coexistência byte-a-byte do Relatório Pontual provada por teste e nenhum artefato/parâmetro M1 ou
`censo_*`/`config.py`/`pipelines/m1` tocado.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- `_render_mapa_municipio` recebe `zonas=` mas não o usa para colorir a camada "dominio" (usa o
  mesmo esquema destaque/neutro da camada "resumo"). O mapa de domínio, portanto, não distingue
  visualmente as zonas 1/2/3 — a estratégia por zona aparece só no painel textual da Página 5.
  Não viola D2/D7 (texto está correto) nem reprova; melhoria visual para um follow-up.
- A capa emite o título canônico via `cell` em UMA linha (decisão consciente do Builder para o
  header aparecer contíguo nos bytes crus); pode transbordar visualmente em larguras de bloco
  pequenas. Sem impacto funcional; validação visual humana já está listada como pendência.

## Testes faltantes
- nenhum bloqueador. (Opcional: um teste que exercite explicitamente o caminho `basemap=True` com
  `contextily` mockado para cobrir o branch de paste do tile — hoje só o fallback offline é testado,
  o que é o comportamento seguro e correto para CI.)

## Riscos remanescentes
- R-tiles: `basemap=True` (só no expander runtime, sob spinner) faz fetch online sob demanda
  (DEC-011); sem internet cai no fallback offline (canvas claro). A suíte NUNCA bate na rede.
- R-perf: cap `_HEX_DRAW_CAP=8000` por município limita o tempo de geração; municípios muito
  grandes desenham os 8000 hexes de maior oferta/score.
- R-housekeeping: o move de BLK-RELMUN-01 (backlog→completed via helper) é responsabilidade do
  orquestrador no fechamento (Passo 6.0); `--check` pré-move retorna exit≠0 (esperado).

## Saída literal das validações (re-executadas pelo QA)

### `python -m pytest -n auto -q` (gate único — abortou com INTERNALERROR conhecido de ambiente)
```
INTERNALERROR> ... xdist/workermanage.py ... self.gateway._rinfo()
INTERNALERROR> ... execnet/gateway_base.py", line 571, in from_io
INTERNALERROR>     raise EOFError("couldn't load message header, " + e.args[0]) from None
INTERNALERROR> EOFError: couldn't load message header, expected 9 bytes, got 0
```
CONTORNO REGISTRADO: erro de execnet/xdist no Python 3.14 Windows local (NÃO é defeito do código,
NÃO é bypass de validação). Re-executado SERIAL conforme instrução do ciclo.

### `python -m pytest -q` (full suite SERIAL — gate único)
```
1055 passed, 1 skipped, 125746 warnings in 260.68s (0:04:20)
```
0 failed. (Os warnings são pré-existentes: geographic-CRS area, FutureWarning de fillna,
pytest_asyncio DeprecationWarning — alheios ao ciclo.)

### `python -m pytest tests/unit/test_relatorio_municipal.py tests/unit/test_housekeeping_helper.py -q`
```
25 passed, 1271 warnings in 3.78s
```
(16 testes do módulo novo + 9 do helper de housekeeping, todos verdes.)

### `python -c "import streamlit_app; print('import ok')"`
```
... (warnings benignos de Streamlit bare-mode: missing ScriptRunContext / No runtime found) ...
import ok
```

### `python -m ruff check .`
```
All checks passed!
```

### `python -m mypy src/motor_expansao/dashboard/relatorio_municipal.py`
```
Success: no issues found in 1 source file
```

### `python scripts/housekeeping_move_block.py BLK-RELMUN-01 --check`
```
FALHA --check: stub ausente no backlog para BLK-RELMUN-01
EXIT=1
```
ESPERADO (pré-move): o move backlog→completed é do orquestrador no Passo 6.0 do fechamento; o
heading `### BLK-RELMUN-01` está presente em `tasks/backlog.md` (origem do bloco confirmada).
Registrado: "pré-move, move pendente ao orquestrador" — NÃO reprova.

### `git status --porcelain` / `git diff --stat HEAD`
```
 M CLAUDE.md
 M context/handoff.md
 M data/outputs/setores_censitarios_2022_geo/_metadata.json   <- PRÉ-SUJO (cycle start)
 M data/reports/relatorio_pontual_censitario_base_geo.md      <- PRÉ-SUJO (cycle start)
 M src/motor_expansao/dashboard/pages.py
 M tasks/backlog.md
 M tasks/current_task.md
?? context/handoff/20260622-105733-planner.md
?? context/handoff/20260622-113500-builder.md
?? context/handoff/20260622-120000-block-orchestrator.md
?? docs/relatorio_municipal_template.md
?? src/motor_expansao/dashboard/relatorio_municipal.py
?? tests/unit/test_relatorio_municipal.py

 CLAUDE.md                          |  11 +   (DEC-011)
 context/handoff.md                 | 221 (handoffs do ciclo)
 .../_metadata.json                 |  56     <- pré-sujo, NÃO commitar
 .../relatorio_pontual_..._geo.md   |  56     <- pré-sujo, NÃO commitar
 src/.../dashboard/pages.py         |  83 +   (puramente aditivo: 0 linhas removidas)
 tasks/backlog.md                   |  45 +   (heading BLK-RELMUN-01 p/ housekeeping)
 tasks/current_task.md              | 147
```
`git diff HEAD -- pages.py | grep '^-'` → 0 linhas removidas (nenhuma assinatura existente alterada).
NENHUM `config.py`, `censo_report.py`, `censo_map.py`, `censo_point.py` nem `pipelines/m1/` aparece
no diff. Os 2 paths de DADOS pré-sujos seguem pré-sujos (não alterados pelo ciclo). NOTA: o ciclo
regrava `tasks/backlog.md` (estado anterior, declarado em current_task.md §Paths pré-sujos) — a
adição é o heading `### BLK-RELMUN-01`, requisito do helper de housekeeping; não é regressão.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do
caminho crítico. A suíte usa `tests/fixtures`/dados sintéticos para isolar lógica (prática legítima,
fora do escopo da regra de tooling). O contorno `-n auto`→serial é por defeito de ambiente do
execnet no Python 3.14 (documentado no ciclo), com contagem serial registrada integralmente — NÃO é
bypass. Os testes de mapa rodam `basemap=False` (default seguro), `contextily` é import lazy e a
suíte NÃO faz fetch de rede — comportamento offline confirmado por leitura do código + teste
`test_mapas_municipio_offline_sem_rede`.

## Conferência cruzada com log do Builder
Bate. Builder reportou 246 passed no subconjunto serial (módulo + pontual + integração) — o QA
re-rodou a suíte COMPLETA (1055 passed, 1 skipped). ruff "All checks passed!", mypy "Success" e
import ok idênticos. O Builder reportou o mesmo contorno `-n auto`→serial; confirmado de forma
independente. Builder não tocou `tasks/backlog.md` por receio do estado pré-sujo, mas o heading
BLK-RELMUN-01 ESTÁ no working tree (provavelmente do Block Orchestrator/Planner) — necessário e
correto para o housekeeping.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; só leitura de colunas; `grep` no módulo novo só
  acha as menções no docstring de READ-ONLY; nenhuma escrita).
- hex_score_estrutural não alterado: sim.
- Artefatos M1 preservados: sim (nenhum `.to_parquet`/`read_parquet` no módulo novo; nenhum artefato
  oficial no diff; nenhum recálculo de score/carteira/plano).
- Parâmetros canônicos §3 intactos: sim (`config.py` não tocado — H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0,
  RENDA_MIN=4500.0, pesos renda=0.40/pop=0.60 inalterados; os limiares 3000/2000/2500 são constantes
  de DISPLAY locais ao módulo, conforme DEC-011 parte 2, NÃO entram no §3).
- Relatório Pontual intocado (CA2): sim — `censo_*` sem edição (não no diff) + teste
  `test_coexistencia_relatorio_pontual_intocado` prova `antes == depois` byte-a-byte ao gerar o
  municipal no meio.
- Anti-PII: sim (sem `image24.png`; só `rede`/contagem nos pins D6; teste `test_pdf_municipal_sem_pii`
  varre needles de PII reais).
- PDF: `%PDF-1.4`, `/Count 8`, `set_compression(False)`, 8 headers de seção, carimbo de versão D8,
  marca d'água ≥8 ocorrências, fallbacks sem dominio/sem assets — todos cobertos por teste.
- Performance (Blocos 4–6): sim — expander reusa `df`/`dominio_df`/`competitors_df`/`ultra_df` já em
  escopo (sem carga nova de parquet); cap `_HEX_DRAW_CAP=8000`; habilitado só com 1 município.
- Escopo respeitado: sim (módulo novo + teste novo + toque aditivo em `pages.py` + docs + DEC-011 +
  handoffs; nada além).
- Validações re-executadas pelo QA (sem bypass): pytest serial → 1055 passed/1 skipped/0 failed;
  import → ok; ruff → passed; mypy → success; housekeeping --check → exit 1 esperado (pré-move).

## Decisão recomendada
Fechar ciclo. Orquestrador executa o Passo 6.0 (housekeeping_move_block.py BLK-RELMUN-01, sem
`--check` no modo move) + commit por path no branch `ciclo/BLK-RELMUN-01`, sem incluir os 2 paths de
dados pré-sujos.
