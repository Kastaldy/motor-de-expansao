# Handoff — Builder

## Skill que gerou este handoff
Builder (2026-07-28 15:59:19)

## Próxima Skill recomendada
**QA / Quality Analyzer**

## Bloco executado
**BLK-RELPON-14** — Círculo azul de 1,5 km nos mapas de escala 5 km (enquadramento INTOCADO).
Plano do Planner executado na íntegra e na ordem definida (`context/handoff/20260728-154511-planner.md`,
§4 "Ordem de execução"). Criticidade Média, READ-ONLY sobre o M1.

## O que foi feito

Nos dois painéis de hexágono do Relatório Pontual (`socioeconomia` e `residual`, ambos gerados por
`_render_camada_residual_hex`), o **círculo azul desenhado** passou de 5 km para **1,5 km**, enquanto o
**enquadramento do mapa continua derivando de `RAIO_RESIDUAL_DISPLAY_KM = 5.0`**. Antes deste bloco UMA
constante governava DUAS coisas (frame e círculo); agora são duas responsabilidades separadas.

- **D1** — constante de RENDER nova `RAIO_CIRCULO_DISPLAY_KM = 1.5`, inserida logo após
  `_RESIDUAL_GRID_DISK_K`, no padrão de `RAIO_RESIDUAL_DISPLAY_KM`/`RAIO_ENTORNO_DISPLAY_KM`
  (deliberadamente fora de `config.py`). **Não** importa nem deriva de `RAIO_CENSITARIO_DEFAULT_KM`.
- **D2** — rodapé via `_HEX_ROTULO_ESCALA`, **derivado das duas constantes**, injetado pelo parâmetro
  `rotulo_escala=` que `_render_camada` já aceitava (mesmo mecanismo DEFAULT-PRESERVING da camada
  `entorno`). Sai exatamente `Raio 1,5 km - escala 5 km - EPSG:3857 - fundo de ruas offline`.
- **D3** — títulos `"Socioeconomia - escala 5 km"` e `"Residual Fitness - escala 5 km"`.
- **`_frame_box_metric(RAIO_RESIDUAL_DISPLAY_KM, ...)` não mudou uma letra** (provado por teste e por
  medição direta, abaixo).

## Arquivos alterados

### 1. `src/motor_expansao/dashboard/censo_map.py` (+56/-)
- **Constante nova `RAIO_CIRCULO_DISPLAY_KM = 1.5`** (após `_RESIDUAL_GRID_DISK_K`), com o bloco de
  comentário do plano: constante de RENDER, fora de `config.py`/§3, coincidência numérica com o motor
  censitário documentada SEM acoplamento.
- **`_HEX_ROTULO_ESCALA`** (logo abaixo), derivado:
  `f"Raio {RAIO_CIRCULO_DISPLAY_KM:.1f} km - escala {RAIO_RESIDUAL_DISPLAY_KM:.0f} km"` + `.replace(".", ",")`
  → `"Raio 1,5 km - escala 5 km"` (medido, não presumido).
- **Círculo:** `circle_metric = Point(0, 0).buffer(RAIO_CIRCULO_DISPLAY_KM * 1000.0, quad_segs=96)`
  (era `RAIO_RESIDUAL_DISPLAY_KM`). A linha anterior (`frame_metric = _frame_box_metric(
  RAIO_RESIDUAL_DISPLAY_KM, width, height)`) ficou **intacta**.
- **Chamada de `_render_camada`:** acrescentado `rotulo_escala=_HEX_ROTULO_ESCALA,  # BLK-RELPON-14 (D2)`
  ao fim dos kwargs; `raio_km=RAIO_RESIDUAL_DISPLAY_KM` **mantido**, com comentário explicando que agora
  ele declara só a escala do frame.
- **Títulos (D3):** default `titulo: str = "Residual Fitness - escala 5 km"` e, na chamada da
  socioeconomia, `titulo="Socioeconomia - escala 5 km"`.
- **Comentários/docstrings stale atualizados (4):** bloco do rodapé em `_render_camada` (a frase "a
  camada de residual sai 'Raio 5,0 km'" deixou de ser verdade), 1ª linha da docstring de
  `_render_camada_residual_hex` (frame 5 km vs círculo 1,5 km), comentário de `CAMADAS_CENSITARIAS` e
  docstring de `render_mapas_censitarios_combinados` (cláusula "círculo desenhado 1,5 km; os 5 km são o
  ENQUADRAMENTO").

### 2. `tests/unit/test_relatorio_pontual_censitario_mapa.py` (+157/-)
**2 testes atualizados** (quebravam por construção — expectativa de texto de produto, nada afrouxado):
- `test_socioeconomia_e_hexagono_nao_setor_a_5km`: título → `"Socioeconomia - escala 5 km"`, rodapé →
  `"Raio 1,5 km - escala 5 km - EPSG:3857 - fundo de ruas offline"`; docstring ajustada. **Nome mantido**
  (fala do FRAME, que continua em 5 km).
- `test_rodape_do_png_deriva_do_raio_km_1p5_identico_e_5p0_novo` → **renomeado** para
  `test_rodape_1p5_por_raio_km_nas_camadas_de_setor_e_rotulo_escala_nas_de_hexagono`. A trava das 4
  camadas de setor (`"Raio 1,5 km - EPSG:3857 - fundo de ruas offline"`) ficou **intacta**; entrou o
  **assert negativo** proibindo qualquer rodapé iniciado por `"Raio 5,0 km"`; títulos → "escala 5 km".
  → conjunto ficou **mais estrito**, não mais frouxo.

**1 helper novo:** `_bbox_dos_pixels_do_circulo` (ao lado de `_conta_pixels_do_circulo`, MESMO predicado
de azul).

**4 testes novos** (seção `── BLK-RELPON-14 ──` ao fim do arquivo):
- `test_circulo_desenhado_tem_1_5_km_com_frame_de_5_km` — geométrico/primário: espiãs em
  `_frame_box_metric`/`_render_camada`; (a) frame continua pedido a 5 km, (b) círculo desprojetado
  3857→CRS métrico local mede 1500 m ± 1,0 m em todos os vértices, (c) anti-vácuo.
- `test_circulo_em_pixel_encolheu_nos_dois_paineis_de_hexagono` — fim-a-fim no PNG, nas 2 chaves, com a
  camada `densidade` como sentinela anti-vácuo.
- `test_raio_do_circulo_e_constante_de_render_propria` — D1/D2: `RAIO_CIRCULO_DISPLAY_KM == 1.5`,
  `not hasattr(config, "RAIO_CIRCULO_DISPLAY_KM")`, `RAIO_RESIDUAL_DISPLAY_KM == 5.0`,
  `_RESIDUAL_GRID_DISK_K == 5`, `RAIO_CENSITARIO_DEFAULT_KM == 1.5`, `_HEX_ROTULO_ESCALA` exato.
- `test_titulos_das_camadas_de_hexagono_cabem_sem_invadir_a_legenda` — D3/overflow (títulos e as 2
  variantes de rodapé) + ASCII puro.

### 3. `docs/relatorio_pontual_censitario.md` (+14/-)
- **§3 (linha 80):** `socioeconomia`/`residual` = frame próprio de 5 km **com círculo de 1,5 km**.
- **§6 "Exceções das camadas com frame proprio":** reescrito — era **FALSO** dizer que `residual`
  desenha o círculo de 5 km. Agora cita as duas constantes, `_HEX_ROTULO_ESCALA`/`rotulo_escala` e os
  títulos novos.
- **§6 "Validacao minima":** parágrafo com os 4 testes novos + o rename, no padrão dos BLK-RELPON-10/-11.
- **§7 página 3:** a menção a `RAIO_RESIDUAL_DISPLAY_KM = 5.0` continua (é o enquadramento), com a
  cláusula "o círculo azul DESENHADO é de 1,5 km desde o BLK-RELPON-14".
- **Parágrafo histórico novo** `**Correcao BLK-RELPON-14 (pedido de Vinicius 2026-07-28):**`, inserido
  logo APÓS o parágrafo do BLK-RELPON-13 (que **não** foi reescrito), com D1/D2/D3 + a lista de
  INTOCADOS.

## Validações executadas (saídas literais)

**1. Testes do arquivo primário (SERIAL) — VERDE**
```
$ python -m pytest -q tests/unit/test_relatorio_pontual_censitario_mapa.py
52 passed, 2653 warnings in 37.84s
```
Confirmação nominal de que os 4 novos rodaram e de que os 2 invariantes passam **sem edição**:
```
$ python -m pytest tests/unit/test_relatorio_pontual_censitario_mapa.py -v -k "..."
tests/.../test_socioeconomia_e_hexagono_nao_setor_a_5km PASSED                            [ 12%]
tests/.../test_rodape_1p5_por_raio_km_nas_camadas_de_setor_e_rotulo_escala_nas_de_hexagono PASSED [ 25%]
tests/.../test_frame_box_metric_puro_reproduz_o_calculo_do_caller PASSED                  [ 37%]
tests/.../test_raio_de_exibicao_nao_toca_o_raio_do_motor PASSED                           [ 50%]
tests/.../test_circulo_desenhado_tem_1_5_km_com_frame_de_5_km PASSED                      [ 62%]
tests/.../test_circulo_em_pixel_encolheu_nos_dois_paineis_de_hexagono PASSED              [ 75%]
tests/.../test_raio_do_circulo_e_constante_de_render_propria PASSED                       [ 87%]
tests/.../test_titulos_das_camadas_de_hexagono_cabem_sem_invadir_a_legenda PASSED         [100%]
8 passed, 44 deselected
```

**2. Conjunto adicional do plano (§6 comando 2, SERIAL) — VERDE**
```
$ python -m pytest -q tests/unit/test_relatorio_pontual_censitario_export.py \
    tests/unit/test_relatorio_pontual_orquestracao.py \
    tests/unit/test_relatorio_pontual_viabilidade.py tests/unit/test_api_skeleton.py
78 passed, 4122 warnings in 58.45s
```
(trava `/Count 8` e `/Count 12`, chaves das camadas e o caminho da API — inalterados)

**3. Ruff — LIMPO**
```
$ python -m ruff check . --no-cache
All checks passed!
```

**4. Mypy — sem erro NOVO**
```
$ python -m mypy src/
src\motor_expansao\pipelines\m1\ibge_censo.py:24: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\poi_enrichment.py:15: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\base_h3_brasil.py:31: error: Library stubs not installed for "requests"  [import-untyped]
Found 3 errors in 3 files (checked 113 source files)
```
Os **3** erros são os PRÉ-EXISTENTES de `types-requests` nesta máquina (a orientação falava em 4; nesta
execução são 3 `error:` + notas `annotation-unchecked`). Todos em `pipelines/m1/`, **nenhum arquivo
tocado por este ciclo** — `censo_map.py` sai limpo.

**5. Smoke de import — OK**
```
$ python -c "import streamlit_app; print('import ok')"
import ok
```
Extra (exigido pelo prompt da Skill Builder):
```
$ python -m pytest -q tests/integration/test_streamlit_app.py
255 passed, 13261 warnings in 19.48s
```

**6. Prova de que o ENQUADRAMENTO não mudou (medição direta, além do teste)**
```
RAIO_RESIDUAL_DISPLAY_KM   = 5.0
RAIO_CIRCULO_DISPLAY_KM    = 1.5
_RESIDUAL_GRID_DISK_K      = 5
raios pedidos ao _frame_box_metric: [5.0]
diametro previsto (analitico) = 152.8 px
  bbox azul [socioeconomia] = (153, 154) px (dx, dy)
  bbox azul [residual] = (153, 154) px (dx, dy)
  bbox azul [densidade] = (509, 512) px (dx, dy)      <- sentinela: camada de 1,5 km, circulo grande
  titulo 'Socioeconomia - escala 5 km' = 573 px (budget 609)
  titulo 'Residual Fitness - escala 5 km' = 586 px (budget 609)
  rodape (61 ch) = 614 px (canvas util 964)
  rodape (68 ch) = 703 px (canvas util 964)
```
Todas as medições do Planner se confirmaram **exatamente** (573/586 px de título; 614/703 px de rodapé;
diâmetro medido 153 px vs 152,8 previsto — erro de 0,2 px, dentro da tolerância de 6 px com folga de
~4,8 px). Antes do bloco o diâmetro nos dois painéis era 509 px, o mesmo que a `densidade` ainda exibe.

**7. `git status --porcelain` no fim**
```
 M docs/relatorio_pontual_censitario.md
 M graphify-out/.graphify_labels.json
 M graphify-out/GRAPH_REPORT.md
 M graphify-out/graph.json
 M src/motor_expansao/dashboard/censo_map.py
 M tasks/backlog.md
 M tests/unit/test_relatorio_pontual_censitario_mapa.py
?? context/handoff/20260728-153129-block-orchestrator.md
?? context/handoff/20260728-154511-planner.md
```
Diffstat dos paths do ciclo: `censo_map.py` +56, `test_..._mapa.py` +157, `docs/...md` +14.
`graphify-out/*` (hook) e `tasks/backlog.md` (bloco do ciclo, pré-sujo na entrada) **não** foram tocados
por mim. Nenhum `git add`/commit/branch foi executado.

## Desvios do plano
**Nenhum desvio de substância.** Dois ajustes cosméticos, ambos dentro da intenção do plano:
1. Nos comentários novos de `censo_map.py` **omiti os números de linha absolutos** que o plano sugeria
   ("`_frame_box_metric`, L1047" → "`_frame_box_metric`"; "L1047" → "linha acima"), porque a própria
   inserção da constante desloca as linhas e o comentário nasceria stale. O conteúdo é o do plano.
2. Os 4 testes novos foram agrupados numa seção `── BLK-RELPON-14 ──` **ao fim do arquivo** (o helper
   `_bbox_dos_pixels_do_circulo` ficou ao lado de `_conta_pixels_do_circulo`, como o plano pediu). O
   plano descrevia o estilo dos testes-espelho, não a linha de inserção; agrupar deixa o bloco auditável.

A tolerância do teste de pixel ficou nos **6 px** do plano (não foi preciso subir para 8) e o valor
esperado é **derivado das constantes**, nunca hardcoded.

## Problemas encontrados
Nenhum. Os 2 testes que quebrariam por construção foram os únicos, e passaram após a atualização de
expectativa; nenhum teste precisou ser afrouxado, removido ou marcado como skip. Não houve necessidade
de iteração: a suíte primária passou na primeira execução.

## O que o QA precisa saber

- **Rode SERIAL ou `-n 2`:** `pytest -n auto` está QUEBRADO nesta máquina (`INTERNALERROR` do `execnet`,
  bloco BLK-QA-XDIST-01). A suíte completa é decisão do QA; meça a baseline no momento, **não** compare
  com contagem histórica (CLAUDE.md §5).
- **Pré-existência (não é regressão):** os 3 erros de `mypy` (`types-requests` em `pipelines/m1/`) já
  existiam; nenhum arquivo do ciclo aparece na lista.
- **A9 é do HUMANO:** o gate visual (o círculo menor "lê" bem no PDF, mesmo ponto antes/depois) é de
  **Vinicius, antes do merge**. O QA **não** pode aprovar nem reprovar esse item — só registrar
  "pendente de gate humano".
- **Onde olhar a prova do invariante:** `test_frame_box_metric_puro_reproduz_o_calculo_do_caller` e
  `test_raio_de_exibicao_nao_toca_o_raio_do_motor` continuam **byte-idênticos** e verdes; se o QA quiser
  reconferir o desacoplamento, a asserção (a) de `test_circulo_desenhado_tem_1_5_km_com_frame_de_5_km`
  é a que trava `_frame_box_metric` recebendo 5 km.
- **Armadilha registrada pelo Planner (não repetir):** `assert not hasattr(censo_map,
  "RAIO_CENSITARIO_DEFAULT_KM")` **falharia** — o módulo importa esse símbolo legitimamente para o
  default de `render_mapas_censitarios_combinados`. A separação RENDER/MOTOR é provada por **de onde o
  círculo deriva**, não pela ausência do import.
- **Recorte de commits (o orquestrador commita):** commit 1 = `censo_map.py` + o teste; commit 2 =
  `docs/relatorio_pontual_censitario.md`; commit 3 = `tasks/backlog.md`. **NÃO commitar** `graphify-out/*`,
  `PRD.md`, `context/handoff.md`, `tasks/current_task.md`, `constraints.txt`.
- **`tasks/current_task.md` NÃO foi editado** (proibido pelo escopo que recebi). O orquestrador deve
  ajustar lá: `Skill atual: QA`, `Esteira executada: ... → Builder ✅ (15:59)`.

## Pendências
- Gate visual humano de Vinicius (critério A9) **antes do merge**.
- Bookkeeping de fechamento (`tasks/completed.md`) — fora do escopo do Builder.

## Riscos remanescentes
- **Folga de 23 px no título mais longo** (`Residual Fitness - escala 5 km`, 586 px contra budget de
  609 px @1000): travada por `test_titulos_das_camadas_de_hexagono_cabem_sem_invadir_a_legenda`. Se
  alguém alongar esse título ou aumentar `_FS_TITULO`, o teste quebra — é o comportamento desejado.
- **Nenhum risco de dado:** o círculo alimenta apenas `draw.line`; o clip dos hexes é
  `.intersection(frame_3857)`. Confirmado pelo BO, pelo Planner e pela asserção (a) do teste geométrico.

## Guardrails verificados
- `score_priorizacao` não alterado: **sim** (nenhum arquivo de scoring/pipeline tocado).
- Artefatos M1 preservados: **sim** (nada em `config.py`, `pipelines/m1/`, artefatos oficiais).
- Motor censitário INTOCADO: **sim** (`setor_censitario_intersecao_area_1p5km`,
  `RAIO_CENSITARIO_DEFAULT_KM` não tocados; `censo_point.py` fora do diff).
- `_RESIDUAL_GRID_DISK_K`, `_frame_box_metric`, `RAIO_RESIDUAL_DISPLAY_KM`: **intocados** (travados por
  teste e medidos acima).
- Grid 2x2, página de satélite e de concorrentes: **byte-idênticas** (as 4 camadas de setor continuam
  derivando o rodapé de `raio_km`; `rotulo_escala` é DEFAULT-PRESERVING e
  `test_camadas_existentes_ficam_byte_identicas_com_os_defaults_novos` segue verde).
- Dashboard offline mantido: **sim** (`basemap=False` em todo o caminho de teste; nenhuma dependência
  nova, nenhuma chamada de rede).
- Acentuação (§2): **exceção de RENDER aplicada** — todo texto de PNG em ASCII puro (`Raio 1,5 km -
  escala 5 km`, títulos com hífen `-`, sem travessão), travado por assert de `ord(c) < 128`. Este
  handoff e a doc, em português acentuado.
