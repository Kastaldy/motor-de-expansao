# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (Passo 6 do orquestrador — modo **MERGE-HUMANO**, ciclo Crítico)

## VEREDITO
**APROVADO COM RESSALVAS**

## Justificativa
Reexecutei por conta própria **todas** as validações obrigatórias do plano, incluindo a prova funcional do MCP (`VERDICT: PASS`, `TOOLS (10)`, `EXIT 0`, `STDERR: (vazio)`), e o script comprovadamente sobe o processo a partir do `command`/`args` do **próprio `.mcp.json`** — não há bypass. A entrega bate **14/14 arquivos** com o plano, sem um único caminho fora de escopo; `constraints.txt` intocado, `.gitattributes` estritamente aditivo com a linha `-diff linguist-generated=true` preservada e **zero** `merge=graphify`; `loop_guard` devolve exatamente as classes previstas e **nenhum** caminho de M1; os artefatos oficiais do M1 continuam com `mtime` de 2026-06-10. As ressalvas — 1 falha na suíte, `mypy` sujo localmente e `pytest -n auto` quebrado — foram **todas provadas pré-existentes e ambientais** (reproduzidas em `main` pura ou com blobs idênticos aos da `main`), e nenhuma delas nasce deste ciclo.

## Problemas críticos (bloqueadores)
- **nenhum.**

## Problemas médios (não bloqueadores)

1. **Critério de aceite nº 8 do plano literalmente NÃO cumprido — a string `python -m graphify . --update` continua no `CLAUDE.md`.**
   O plano se contradiz em **três** pontos: o passo 11 manda escrever um texto literal que **cita** a string dentro de uma advertência; o teste 12 manda a string estar **ausente**; e o critério de aceite nº 8 repete a ausência. Não há como cumprir os três.
   **Julgamento do QA: a resolução do Builder é DEFENSÁVEL e o teste entregue trava a intenção certa.** Verifiquei: existe **exatamente 1 ocorrência** no `CLAUDE.md`, e ela está dentro de `**`python -m graphify . --update` NAO faz isso**`. O teste `test_claude_md_nao_recomenda_a_forma_de_update_que_o_cli_ignora` percorre **todas** as ocorrências e exige que cada uma esteja a ≤60 caracteres de `"NAO faz isso"`, além de exigir a presença da forma correta `/graphify . --update`. Advertir é o oposto de recomendar, e apagar a advertência perderia informação medida (o CLI reescreve para `extract . --update` e dispara extração completa com LLM).
   **Ação requerida:** por ser ciclo **Crítico**, o humano deve **ratificar explicitamente no corpo do PR** que aceita o desvio do critério nº 8 pela intenção. Não é motivo de reprovação, mas não pode passar em silêncio.

2. **A suíte completa tem 1 falha: `tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime`.**
   Resultado do gate: `1 failed, 2028 passed, 2 skipped in 1397.07s`.
   **PROVADO pré-existente e ambiental, sem relação alguma com este ciclo:** o blob do teste (`30a38c53…`) e o do módulo sob teste `src/motor_expansao/lifetime/score_retencao_territorial.py` (`7a886c9f…`) são **byte-idênticos** entre `HEAD` e `main`; o último commit a tocá-los foi `1b7aac9` (BLK-LTV-04). Causa: a guarda de `pytest.skip` só cobre os artefatos M1 (que existem nesta estação), mas `run()` carrega logo depois `data/staging/unidade_territorio_retencao.parquet`, **ausente** aqui → `FileNotFoundError`. Em CI `data/staging/` é gitignored e vazio → o teste **pula** → a `main` está verde.
   **Já está registrado no backlog como `BLK-FIX-LTV-01`** (achado pelo QA do BLK-RELPON-13 em 2026-07-24), com a correção e o critério de aceite escritos. Nada a criar.
   **Guardrail conferido:** a falha **não** escreveu nada — os artefatos M1 seguem com `mtime` de 2026-06-10 09:50.

3. **`pytest -n auto` está quebrado nesta estação — verificado por mim, e PROVADO pré-existente.**
   Não aceitei a alegação do Builder: reproduzi de quatro formas independentes (detalhe na seção de saídas literais), sendo a decisiva um `git worktree` da **`main` pura**, onde o `-n auto` falhou **2/2** e até o `-n 2` caiu. Também confirmei que os pacotes instalados pelo grupo `graph` não registram entry point `pytest11`.
   **Severidade: média, e NÃO é regressão deste ciclo.** Atenuante que medi: o CI roda `python -m pytest -q` **serial** (`ci.yml:60`), então o portão da `main` não é afetado — o dano é só a ergonomia do gate do QA (23 min em serial).
   **Bloco de correção criado por mim: `BLK-QA-XDIST-01`** em `tasks/backlog.md`.

## Melhorias opcionais

- **Regra de EOL para o `.mcp.json`.** O blob está correto (232 bytes, LF, sem BOM), mas com `core.autocrlf=true` a cópia de trabalho fica CRLF (244 bytes, 12 CRLF). Não quebra nada — o handshake **passou com o arquivo já em CRLF** — e em clone Linux o checkout sai LF (não há regra de `.gitattributes` para o caminho e `autocrlf` é `false` por padrão lá). Uma linha `.mcp.json text eol=lf` fecharia o assunto num próximo PR que já toque `.gitattributes`.
- **Travar por teste a decisão D3 (glob amplo proibido).** Verifiquei que `Bash(python -m graphify*)` está **ausente** do `permissions.allow`, mas **nenhum teste** impede que ele volte. Como esse glob liberaria `hook install` — que re-adiciona `merge=graphify` ao `.gitattributes` (CRÍTICO) — vale um assert no `test_grafo_ferramenta.py`.
- **Blindar os arquivos que escolhem o interpretador do hook.** O `.githooks/post-commit` resolve o Python a partir de `graphify-out/.graphify_python` e a raiz a partir de `graphify-out/.graphify_root`. Ambos estão cobertos pelo `.gitignore:160` (`graphify-out/.graphify_*`, com exceção só para `.graphify_labels.json`), logo não entram por acidente — mas um `git add -f` passaria. Um teste de contrato afirmando que seguem não-versionados é barato.
- **Número defasado no runbook.** `docs/grafo_conhecimento.md` §7 cita "566 logs" em `context/handoff/`; hoje são **571**. O texto se declara medido durante o BLK-GRAPH-01/02, então não é erro — só envelhece.
- **Precisão do handoff do Builder (2 imprecisões que corrigi na conferência cruzada):** (a) "`.gitattributes` — +13 linhas" — este ciclo adicionou **5**; as outras 8 são linhagem `graph-01` (`a7c4754`); (b) sobre o script, "Nenhuma outra linha foi alterada" — o arquivo inteiro foi **reformatado** (lista `ESPERADO` uma-por-linha, chamadas `send(...)` reembrulhadas). Sem impacto funcional, mas a afirmação não se sustenta ao pé da letra.

## Testes faltantes

- Teste que trave a **ausência** do glob `Bash(python -m graphify*)` no `permissions.allow` (D3 hoje é só prosa + runbook).
- Teste que trave `graphify-out/.graphify_python` e `graphify-out/.graphify_root` como **não versionados**.
- Nada trava que o **blob** do `.mcp.json` seja LF (o teste existente só verifica a ausência de BOM).
- *(fora deste ciclo)* `BLK-FIX-LTV-01` — a guarda de skip que falta em `test_run_readonly_m1_por_mtime`.

## Riscos remanescentes

- **`core.hooksPath` NÃO está configurado nesta estação** (`git config --get core.hooksPath` = vazio). Consequência concreta: versionar o hook **ainda não surtiu efeito** aqui — o `.git/hooks/post-checkout` (rebuild de corpus inteiro, que infla o grafo de 7.633 para 13.599 nós) continua **ativo**. A limitação está documentada como "limitação gêmea 2" no runbook, mas ninguém rodou o comando. **Ação para o humano após o merge:** `git config core.hooksPath .githooks`.
- **R3 confirmado presente e inerte:** `merge.graphify.name`/`merge.graphify.driver` seguem no git config **local**, órfãos desde `1ebef60` (nenhuma linha do `.gitattributes` os referencia). Documentado no runbook §9.3, não mexido — como o plano determinou.
- **R-PUBLISH:** o merge toca `pyproject.toml` → dispara `publish-api` (`ci.yml:236`) e **republica a imagem da API/bot no GHCR**. Não é deploy (deploy segue manual por digest, §6). Humano já ciente, registrado na aprovação do plano.
- **R-MCP2:** mitigado em 3 camadas (pin, teste de contrato que **não** skipa, comentário no diff). Reconfirmei que o pin pegou: `mcp 1.29.0` instalado e `from mcp.types import AnyUrl` funciona.
- **Circularidade da DEC-019:** ela habilita `@VinhoAbencoado` a conceder `critica-aprovada` e viaja no PR que ele mesmo autoriza. Atenuante que **medi**: `guard.yml:200` da **`main`** já executa `DONOS = {"kastaldy", "vinhoabencoado"}` — a edição alinha registro a código, não muda comportamento; e `.github/` está intocado neste PR. Separação de deveres preservada: o autor dos 4 commits é `VinhoAbencoado`, então o `aprovado-humano` **tem** de vir de outro humano.
- **Portabilidade não prometida:** o `command: "python"` funciona porque o `python` do PATH é o interpretador que tem o pacote. Correto para esta máquina, declarado como dívida no runbook §10.

## Saída literal das validações (re-executadas pelo QA)

### 1. PROVA FUNCIONAL DO MCP — `python scripts/verify_mcp_graphify.py` (critério de aceite nº 2)

```
LAUNCH: ['python', '-m', 'graphify.serve', 'graphify-out/graph.json']
CWD   : C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao
PROTOCOL   : 2025-06-18
SERVERINFO : {'name': 'graphify', 'version': '1.29.0'}
TOOLS (10) : ['get_community', 'get_neighbors', 'get_node', 'get_pr_impact', 'god_nodes', 'graph_stats', 'list_prs', 'query_graph', 'shortest_path', 'triage_prs']
graph_stats: Nodes: 7633 | Edges: 15362 | Communities: 424 | EXTRACTED: 97% | INFERRED: 3% | AMBIGUOUS: 0%
query_graph: Traversal: BFS depth=2 | Start: ['score_priorizacao (score oficial do M1)', 'score_priorizacao nao deve ser alterado pelo enrich nem pelo load_carteira.', 'score_priorizacao nos dados originais deve ser identico antes e apos analise_ent'] | 7 nodes found |  | [!] TRUNCATED: showing 7 of 7 nodes (~60
EXIT       : 0
STDERR     : (vazio)
VERDICT    : PASS
EXIT_REAL=0
```

**Conferência anti-bypass (li o script antes de rodar):** `scripts/verify_mcp_graphify.py:37-39` faz
`json.loads((RAIZ / ".mcp.json").read_text(...))["mcpServers"]["graphify"]` e monta
`cmd = [srv["command"], *srv.get("args", [])]` / `env = {**os.environ, **srv.get("env", {})}`.
A prova exercita a **config versionada**, não uma variante digitada à mão. ✅

### 2. Pin `mcp<2` e o grupo `[dependency-groups]`

```
=== versoes instaladas ===
graphifyy 0.9.29 | mcp 1.29.0
=== AnyUrl ===
AnyUrl OK: <class 'pydantic.networks.AnyUrl'>
=== pyproject [dependency-groups] via tomllib ===
{'graph': ['graphifyy[mcp]>=0.9.29,<0.10', 'mcp>=1.28,<2']}
optional-deps keys: ['dev', 'api_mvp', 'api', 'ml', 'scraping', 'basemap', 'geocoder']
```

O teste de contrato lê o TOML com `tomllib` (stdlib), exige `graphifyy[mcp]`, proíbe o nome nu
`graphify` e exige `<2` no specifier do `mcp` — e **não skipa** (roda no CI sem o grupo instalado).
`graph` **não** está em `optional-dependencies` → o lock não incha. ✅

### 3. `.mcp.json` — JSON válido, sem BOM, blob LF

```
=== .mcp.json bytes iniciais (BOM?) ===
b'{\r\n'
=== json valido ===
ok
=== blob no git (.mcp.json) ===
len 232 CRLF 0 BOM False
=== working tree .mcp.json ===
len 244 CRLF 12 BOM False
=== check-attr .mcp.json ===
[fim]
```

### 4. `constraints.txt` INTOCADO (decisão D9)

```
=== constraints.txt nos 4 commits (deve ser vazio) ===
[fim]
=== git diff --stat constraints.txt (working) ===
[fim]
=== constraints.txt vs main ===
[fim]
```

### 5. `.gitattributes` — aditivo, L44 preservada, zero `merge=graphify`

Diff do commit `24f4619` (**+5 linhas**, nada removido):

```
@@ -30,6 +30,11 @@ tasks/completed.md  merge=union
 tasks/*.md          text eol=lf
 docs/*.md           text eol=lf

+# Hooks versionados (BLK-GRAPH-02): `.githooks/post-commit` nao tem extensao, entao o
+# `*.sh` da L12 NAO o cobre. Roda sob o `sh` do Git for Windows e, com core.autocrlf=true,
+# o checkout entregaria CRLF e quebraria o shebang `#!/bin/sh`.
+.githooks/*         text eol=lf
+
 # graph.json e' ARTEFATO GERADO (graphify, BLK-GRAPH-01): ~10 MB / 263k linhas de JSON.
```

Linha 44 do arquivo final, **intacta**:
```
graphify-out/graph.json -diff linguist-generated=true
```
Nenhuma ocorrência de `merge=graphify` em todo o arquivo. ✅

### 6. `loop_guard` contra o diff real

```
$ git diff --name-only main...HEAD | python scripts/loop_guard.py --stdin --json
{"limpo": false, "violacoes": [
 {"path": ".claude/settings.json", "classe": "governanca", "motivo": "configuracao do harness Claude (permissoes/hooks/agentes)"},
 {"path": ".gitattributes",        "classe": "critico",    "motivo": ".gitattributes - eol/binary dos .enc.*"},
 {"path": ".gitignore",            "classe": "governanca", "motivo": ".gitignore - o que fica fora do versionamento"},
 {"path": "CLAUDE.md",             "classe": "governanca", "motivo": "fonte canonica de regras e DECs"},
 {"path": "pyproject.toml",        "classe": "critico",    "motivo": "pyproject.toml - config do pytest/ruff + deps da imagem"},
 {"path": "tasks/backlog.md",      "classe": "governanca", "motivo": "backlog - marcador de autonomia/criticidade dos blocos"}]}
EXIT=1

$ git diff --name-only 037c61a~1 acd72d4 | python scripts/loop_guard.py --stdin --json   # SÓ os 4 commits do ciclo
{"limpo": false, "violacoes": [
 {"path": ".claude/settings.json", "classe": "governanca", ...},
 {"path": ".gitattributes",        "classe": "critico",    ...},
 {"path": "CLAUDE.md",             "classe": "governanca", ...},
 {"path": "pyproject.toml",        "classe": "critico",    ...},
 {"path": "tasks/backlog.md",      "classe": "governanca", ...}]}
EXIT=1
```

`critico` = **exatamente** `.gitattributes` + `pyproject.toml`; `governanca` = `.claude/settings.json`,
`CLAUDE.md`, `tasks/backlog.md` (+ `.gitignore`, que é linhagem `graph-01`). **ZERO caminho de M1.** ✅

### 7. `graphify-out/*` — nenhum dos 4 commits o toca

```
$ git diff --name-only 037c61a~1 acd72d4 -- graphify-out/
[fim]

$ git diff --name-only 037c61a~1 acd72d4
.claude/settings.json
.gitattributes
.githooks/post-commit
.mcp.json
CLAUDE.md
docs/README.md
docs/decisions/DEC-019.md
docs/grafo_conhecimento.md
pyproject.toml
scripts/verify_mcp_graphify.py
tasks/backlog.md
tests/contracts/test_grafo_ferramenta.py
tests/integration/test_mcp_graphify_server.py
tests/unit/test_loop_guard_paths.py
```

Os 3 artefatos de `graphify-out/` que aparecem em `main...HEAD` vêm **todos** da linhagem `graph-01`. ✅

### 8. Aderência de escopo — 14/14, zero excesso

```
reais: 14 | plano: 14
EXCESSO (fora do plano): nenhum
FALTANDO (previsto e nao entregue): nenhum
```

### 9. Hook versionado

```
=== git check-attr -a -- .githooks/post-commit ===
.githooks/post-commit: text: set
.githooks/post-commit: eol: lf
=== git ls-files -s .githooks/post-commit ===
100755 dcd9e3cc94c95e02a784519d460c9c3931053125 0	.githooks/post-commit
=== bytes do hook (working tree) ===
9376 0
=== bytes do hook (blob git) ===
9376 0
=== diff byte-a-byte instalado x versionado ===
IDENTICOS
=== post-checkout versionado? (deve ser False) ===
False
```

### 10. `CLAUDE.md` — 189 linhas e a string do critério nº 8

```
=== wc -l CLAUDE.md ===
189
=== CLAUDE.md: string proibida pelo criterio 8 ===
  ocorrencia 1 -> 'SH-COMMAND** `/graphify . --update`. **`python -m graphify . --update` NAO faz isso** (verificado 2'
total ocorrencias: 1
/graphify . --update presente: True
```

### 11. `.claude/settings.json` — pré-aprovação estrita, resto da lista preservado

```
chaves topo novo: ['enabledMcpjsonServers', 'permissions']
allow: velho 34 -> novo 39
as 5 primeiras novas: ['Bash(python -m graphify query*)', 'Bash(python -m graphify path*)', 'Bash(python -m graphify explain*)', 'Bash(python -m graphify god-nodes*)', 'Bash(python -m graphify affected*)']
resto identico ao velho, na ordem: True
enabledMcpjsonServers: ['graphify']
enableAllProjectMcpServers presente: False
glob amplo graphify* presente: False
```

### 12. `ruff` e `import streamlit_app`

```
=== ruff ===
All checks passed!
EXIT=0
=== import streamlit_app ===
(...avisos de "No runtime found" do Streamlit, esperados fora de runtime...)
ok
EXIT=0
```

### 13. `mypy src` — 4 erros, TODOS pré-existentes e LOCAIS

```
src\motor_expansao\pipelines\m1\ibge_censo.py:24: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\poi_enrichment.py:15: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\base_h3_brasil.py:31: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\service.py:310: error: Library stubs not installed for "requests"  [import-untyped]
Found 4 errors in 4 files (checked 113 source files)
EXIT=1
```

**Diagnóstico próprio (a explicação do Builder estava errada no detalhe).** Não é "o CI instala `.[dev]`":
`types-requests` **não existe** no `pyproject.toml` nem no `constraints.txt`. A causa real é **drift do
ambiente local**: aqui está `requests 2.33.1` (sem marcador `py.typed`), enquanto o `constraints.txt:516`
pina `requests==2.34.2`. Confirmei no CI verde da `main` (run `30128322027`):

```
test	Types (mypy src/)	Success: no issues found in 113 source files
```

Nenhum dos 4 arquivos foi tocado por este ciclo (`git diff main...HEAD -- src/` = vazio). Pré-existente. ✅

### 14. `collect-only` e contagem dos testes novos

```
=== collect-only (HEAD) ===
2031 tests collected in 91.93s (0:01:31)

=== testes nos arquivos novos ===
16 tests collected      # 15 (test_grafo_ferramenta.py) + 1 (test_mcp_graphify_server.py)
```

`+3` entradas no `parametrize` de `test_loop_guard_paths.py` → **+19**, batendo com o `2012 → 2031` do Builder.

### 15. Subset obrigatório do plano — 0 falhas, 0 skips no gate novo

```
$ python -m pytest -q -rs tests/contracts/test_grafo_ferramenta.py tests/contracts/test_docs_vs_codigo.py \
      tests/unit/test_claude_md_size.py tests/unit/test_loop_guard_paths.py
161 passed, 3184 warnings in 3.90s
EXIT=0

$ python -m pytest -q -rs tests/contracts/test_grafo_ferramenta.py tests/integration/test_mcp_graphify_server.py
16 passed, 819 warnings in 3.41s
EXIT=0
```

`test_grafo_ferramenta.py` roda **sem nenhum skip**, como o plano exigia. ✅

### 16. SUÍTE COMPLETA — gate único do ciclo (serial)

```
$ python -m pytest -q
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 2028 passed, 2 skipped, 337301 warnings in 1397.07s (0:23:17)
EXIT=1
```

Falha isolada:
```
E  FileNotFoundError: [Errno 2] No such file or directory:
   'C:\Users\Vinicius Cruz\Downloads\Projetos\motor-de-expansao\data\staging\unidade_territorio_retencao.parquet'
```

Prova de que **não** é deste ciclo:
```
=== blob do teste que falhou: HEAD x main ===
30a38c539eb35458141bbc6114f6432c0d311b88
30a38c539eb35458141bbc6114f6432c0d311b88
=== blob do modulo lifetime: HEAD x main ===
7a886c9f48bb93e4db3d0b35c272c5f0ff783c1d
7a886c9f48bb93e4db3d0b35c272c5f0ff783c1d
=== ultimo commit que tocou o teste ===
1b7aac9 BLK-LTV-04 — Score de retenção territorial (M2): gate honesto NO-GO (DEC-014)
```
Já catalogado como **BLK-FIX-LTV-01** no backlog.

### 17. `pytest -n auto` — verificado por mim, PROVADO pré-existente

```
=== cpu_count === 12
pytest 8.4.2 | xdist 3.8.0 | execnet 2.1.2

--- -n auto, no repo, tests/unit/test_claude_md_size.py (blob IDÊNTICO ao da main) ---
run 1: INTERNALERROR> EOFError: couldn't load message header, expected 9 bytes, got 0        EXIT=3
run 2: INTERNALERROR> OSError: [WinError 6] Identificador invalido (_winapi.DuplicateHandle)  EXIT=3
run 3: INTERNALERROR> OSError: [WinError 6] Identificador invalido (_winapi.DuplicateHandle)  EXIT=3
```

**Teste decisivo — worktree da `main` PURA** (`git worktree add --detach ... main`, `GRAPHIFY_SKIP_HOOK=1`):
```
HEAD is now at 2a988f7 Merge pull request #145 from Kastaldy/ciclo/BLK-RELPON-13
--- -n auto EM MAIN PURO ---
INTERNALERROR> EOFError: couldn't load message header, expected 9 bytes, got 0   EXIT=3
INTERNALERROR> EOFError: couldn't load message header, expected 9 bytes, got 0   EXIT=3
--- -n 2 EM MAIN PURO ---
INTERNALERROR> OSError: [WinError 50] Nao ha suporte para o pedido               EXIT=3
```

Suíte completa com `-n 4`, também no HEAD:
```
INTERNALERROR> OSError: connection already closed: <Gateway id='gw0' not-receiving, main_thread_only model, 0 active channels>
EXIT=3
```

Descartada a hipótese de que os pacotes novos sejam a causa:
```
mcp -> nenhum | graphifyy -> nenhum | starlette -> nenhum
sse-starlette -> nenhum | httpx-sse -> nenhum | pyjwt -> nenhum      (entry points pytest11)
```
E fora do repositório, com um teste trivial, `-n auto` **passa** (`1 passed in 3.31s`).
**Conclusão: quebra de ambiente, pré-existente, e o CI não é afetado** (`ci.yml:60` roda serial).
Worktree removido ao fim (`git worktree list` = só o repo principal).

### 18. Guardrail READ-ONLY M1 — verificação direta

```
=== caminhos de M1/score/pipeline/config/artefato nos 4 commits ===
[fim - vazio = nenhum]
=== git diff main...HEAD -- config.py src/ data/ ===
[fim - vazio]
=== parametros canonicos (src/motor_expansao/config.py, leitura literal) ===
H3_RESOLUTION = 7
DIST_MIN_ULTRA_KM = 1.0
RENDA_MIN = 4500.0
M1_HEX_LAND_FRACTION_MIN = 0.05
M1_SCORE_OFICIAL = "score_priorizacao"
=== ultimo commit que tocou config.py e pipelines/m1/ ===
353b2c1 BLK-FIX-06-B: candidatos H3 por overlap + limiar 0.05 (DEC-003)
=== mtime dos artefatos oficiais M1 (HOJE = 2026-07-28 12:19) ===
brasil_estrutural.parquet                     2026-06-10 09:50:32
brasil_priorizados.parquet                    2026-06-10 09:50:32
hexagonos_brasil_oportunidades.parquet        2026-06-10 09:50:34
```

### 19. Governança — `guard.yml` intocado, DEC-019 já em vigor, CODEOWNERS

```
=== DONOS no guard.yml da main ===
200:          DONOS = {"kastaldy", "vinhoabencoado"}
345:              if autor.lower() not in DONOS:
=== guard.yml mudou neste ciclo? ===
[fim - vazio = intocado]
=== CODEOWNERS ===
32:/CLAUDE.md                         @Kastaldy @VinhoAbencoado @juancalu
33:/pyproject.toml                    @Kastaldy @VinhoAbencoado @juancalu
```

### 20. Higiene do repositório

```
$ git status --porcelain
?? context/handoff/20260728-095326-block-orchestrator.md
?? context/handoff/20260728-095544-block-orchestrator.md
?? context/handoff/20260728-100947-planner.md
?? context/handoff/20260728-110736-planner.md
?? context/handoff/20260728-114814-builder.md
```
`graphify-out/` **limpo** durante toda a auditoria (nenhum commit foi feito por mim, logo o hook
`post-commit` não disparou). Só os snapshots de handoff não rastreados.

### 21. Desvio literal (a) — `%`-format reprovaria o `ruff`: CONFIRMADO

```
$ python -m ruff check --no-cache  (sobre o literal do plano)
UP031 Use format specifiers instead of percent format
 --> _up031_probe.py:2:7
2 | print("TOOLS (%d) : %s" % (len(tools), sorted(tools)))
Found 1 error.  EXIT=1
```
`[tool.ruff.lint] select = ["E", "F", "I", "UP", "B"]` e `ruff check .` é passo **bloqueante**
(`ci.yml:53`). A troca por f-string produz saída **idêntica**. Desvio real, mínimo e necessário. ✅
*(Arquivo de sonda removido; não ficou resíduo no repo.)*

## Conferência de no-bypass

**Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico.**

- O tooling em escopo é o **servidor MCP**, e a prova rodou contra a **config real versionada**: li `scripts/verify_mcp_graphify.py` **antes** de executá-lo e verifiquei nas linhas 37-39 que ele carrega `command`, `args` e `env` do próprio `.mcp.json` da raiz e os passa a `subprocess.Popen`. Não há caminho digitado à mão, nem variante alternativa, nem stub.
- A prova rodou com o `.mcp.json` **exatamente como está na working tree** (inclusive em CRLF) — não houve normalização prévia para fazer passar.
- Nenhum teste foi ajustado, afrouxado ou marcado para passar. O gate do bloco (`tests/contracts/test_grafo_ferramenta.py`) **não skipa** e rodou com **0 skips** (`161 passed` no subset). O único teste que skipa (`tests/integration/test_mcp_graphify_server.py`) está declarado no docstring como **não sendo o gate** — e aqui ele nem skipou, rodou e passou.
- A suíte completa rodou **sem** `-p no:xdist`, sem `--deselect` e sem `-k`. A falha e os 2 skips estão reportados de frente, não mascarados.
- O `INTERNALERROR` do `-n auto` **não** foi contornado: foi investigado, reproduzido em `main` pura e reportado como problema com bloco de correção próprio.

## Conferência cruzada com log do Builder

**Bate, com 4 divergências de precisão (nenhuma altera o resultado):**

| Item | Builder | QA (medido) | Situação |
|---|---|---|---|
| Prova funcional MCP | `VERDICT: PASS`, `TOOLS (10)`, `EXIT 0` | **idêntico**, reexecutado | bate |
| `loop_guard` | 2 `critico` + 4 `governanca`, 0 M1 | **idêntico** | bate |
| Hook | `100755`, `eol: lf`, `9376 0` | **idêntico** + byte-idêntico ao instalado | bate |
| `CLAUDE.md` | 189 linhas | **189** | bate |
| `collect-only` | `2012 → 2031 (+19)` | **2031**, +19 conferido por contagem | bate |
| `-n auto` quebrado, pré-existente | provado com `git stash` | **provado melhor**: worktree da `main` pura, 2/2 + `-n 2` caindo lá também | bate, evidência reforçada |
| Suíte completa | não rodou (regra) | `1 failed, 2028 passed, 2 skipped` | **divergência**: o Builder não podia saber; a falha é pré-existente (BLK-FIX-LTV-01) |
| mypy | "4 erros; o CI instala `.[dev]`" | 4 erros, mas a causa é `requests 2.33.1` local **atrás** do lock (`2.34.2`); `types-requests` não existe em lugar nenhum | **divergência de diagnóstico**, conclusão final igual (pré-existente, CI verde) |
| `.gitattributes` | "+13 linhas" | **+5** neste ciclo; 8 são de `a7c4754` | **divergência de contagem** |
| Script | "Nenhuma outra linha foi alterada" | reformatação completa do arquivo (equivalente funcional) | **divergência de precisão** |

## Guardrails verificados

- **score_priorizacao não alterado:** **sim.** `git diff main...HEAD -- config.py src/ data/` = vazio; nenhum dos 4 commits toca `pipelines/`, `*scoring*` ou artefato; `loop_guard` = 0 caminhos de M1; artefatos oficiais com `mtime` de 2026-06-10 (inclusive **após** a falha do teste de retenção, que abortou antes de escrever).
- **hex_score_estrutural não alterado:** **sim** — mesma evidência; nada em `src/motor_expansao/pipelines/m1/`, cujo último commit segue sendo `353b2c1`.
- **Artefatos M1 preservados:** **sim** — `brasil_estrutural.parquet`, `brasil_priorizados.parquet`, `hexagonos_brasil_oportunidades.parquet` intactos por `mtime`; nada em `data/` no diff.
- **Parâmetros canônicos:** **sim** — `H3_RESOLUTION=7`, `DIST_MIN_ULTRA_KM=1.0`, `RENDA_MIN=4500.0`, `M1_HEX_LAND_FRACTION_MIN=0.05`, `M1_SCORE_OFICIAL="score_priorizacao"`; pesos `renda=0.40`/`pop=0.60` fora do diff. `test_parametros_canonicos.py` verde na suíte completa.
- **Dashboard offline mantido:** **sim** — `import streamlit_app` ok; o grupo `graph` é PEP 735, fora do `constraints.txt`, das imagens (`Dockerfile.streamlit`/`.api`) e do container do loop. Nenhuma dependência de API ao vivo criada.
- **Acentuação (§2):** **sim** — `docs/grafo_conhecimento.md` (texto novo) com acentuação correta do português; identificadores 100% ASCII (`.mcp.json`, `scripts/verify_mcp_graphify.py` e `test_mcp_graphify_server.py` sem **nenhum** caractere não-ASCII; no arquivo de contrato os únicos não-ASCII são `§` e `ê` em mensagens de erro, não em identificadores).
- **Regra de manutenção do `CLAUDE.md`:** **sim** — 189 → 189 linhas, teto de 230 verde.
- **§6.1 / DEC-016:** **sim** — bloco **não** loop-safe (sem marcador no backlog), criticidade **Crítica** → merge **humano obrigatório**, auto-merge não se aplica. **Deploy: nada foi para a VPS.**
- **Validações re-executadas pelo QA (sem bypass):**
  - `python scripts/verify_mcp_graphify.py` → **PASS, exit 0, TOOLS (10), STDERR vazio**
  - `json.loads('.mcp.json')` → **ok**; primeiros bytes → **sem BOM**
  - `pip`/`importlib.metadata` → **graphifyy 0.9.29 | mcp 1.29.0** (pin `<2` pegou); `from mcp.types import AnyUrl` → **OK**
  - `git diff --stat constraints.txt` e vs `main` → **vazio**
  - `git check-attr -a -- .githooks/post-commit` → **text: set / eol: lf**
  - `git ls-files -s .githooks/post-commit` → **100755**
  - bytes do hook → **9376 0** e **byte-idêntico** a `.git/hooks/post-commit`
  - `wc -l < CLAUDE.md` → **189**
  - `pytest -q -rs` (subset do plano) → **161 passed, 0 skipped**
  - `pytest -q` (suíte completa, serial) → **1 failed (pré-existente), 2028 passed, 2 skipped**
  - `ruff check . --no-cache` → **All checks passed!**
  - `mypy src` → **4 erros pré-existentes/locais** (CI verde: "Success: no issues found in 113 source files")
  - `pytest --collect-only -q` → **2031**
  - `loop_guard --stdin --json` → **classes exatas, 0 de M1**
  - `git status --porcelain` → **só handoffs não rastreados; `graphify-out/` limpo**
- **Escopo respeitado:** **sim** — 14 caminhos entregues = 14 caminhos do plano; **zero** excesso, **zero** faltando. Nada de `constraints.txt`, `graphify-out/*`, `.githooks/post-checkout`, `guard.yml`, `PRD.md`, `context/handoff.md` ou `tasks/current_task.md` nos commits.

## Housekeeping de concluídos (Passo 6.0) — PENDÊNCIA DO ORQUESTRADOR, não gate do QA

Ciclo **Crítico → modo MERGE-HUMANO**. Conforme o recorte deste ciclo (mesmo padrão do BLK-RELPON-13),
o Passo 6.0 é executado pelo **orquestrador no fechamento**, depois deste veredito. Estado medido agora:

```
$ python scripts/housekeeping_move_block.py BLK-GRAPH-02 --check
FALHA --check: stub ausente no backlog para BLK-GRAPH-02      EXIT=1
$ python scripts/housekeeping_move_block.py BLK-GRAPH-02 --is-done
BLK-GRAPH-02: ABERTO em tasks/completed.md                    EXIT=1
```

**Não reprovei por isso.** O que o orquestrador deve rodar para fechar:
1. `python scripts/housekeeping_move_block.py BLK-GRAPH-02` (move o bloco, **não** editar à mão).
2. `python scripts/housekeeping_move_block.py BLK-GRAPH-02 --check` → tem de sair **OK**.
3. Byte-identidade do bloco movido contra `git show HEAD:tasks/backlog.md`.
4. `python -m pytest -q tests/unit/test_housekeeping_helper.py` verde
   (**usar serial ou `-n 2`** — `-n auto` está quebrado, ver `BLK-QA-XDIST-01`).
5. Commitar **por path**, com `git checkout -- graphify-out/` antes.

**Atenção:** eu acrescentei o bloco **`BLK-QA-XDIST-01`** ao `tasks/backlog.md` (arquivo fica *dirty*);
ele precisa entrar no commit de fechamento junto com o housekeeping.

## Decisão recomendada

**Fechar o ciclo** (Passo 6, modo MERGE-HUMANO) — sem bloco de correção para o BLK-GRAPH-02 em si.
Blocos de follow-up registrados: **`BLK-QA-XDIST-01`** (novo, criado por mim) e **`BLK-FIX-LTV-01`**
(já existia).

**Checklist para o humano antes do merge:**
1. **Ratificar no corpo do PR** o desvio consciente do critério de aceite nº 8 (a string
   `python -m graphify . --update` permanece no `CLAUDE.md`, **como advertência**) — é o único item que
   pede decisão humana explícita.
2. **Labels cumulativas:** `criticidade:critica` (autor) + `critica-aprovada` (`@Kastaldy` **ou**
   `@VinhoAbencoado`) + `aprovado-humano` (humano **≠ autor**; o autor dos 4 commits é `VinhoAbencoado`,
   então este tem de vir de outra pessoa) + review nativo de **CODEOWNER** para `/CLAUDE.md` e
   `/pyproject.toml`.
3. **Declarar R-PUBLISH no corpo do PR:** o merge republica a imagem da API/bot no GHCR (`ci.yml:236`).
   **Não é deploy** — deploy segue manual por digest (§6).
4. **Declarar R-REVIEW/D9:** a divergência com o `constraints.txt` é intencional e medida.
5. **Após o merge, nesta estação:** `git config core.hooksPath .githooks` (sem isso o hook versionado
   não entra em vigor e o `post-checkout` perigoso continua ativo) e `/graphify . --update` numa sessão
   Claude (este ciclo mexeu em `CLAUDE.md`, `docs/`, `tasks/` e numa DEC — a camada semântica está
   desatualizada).
