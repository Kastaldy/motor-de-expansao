# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-PERF-01a — Shared transformer no render censitário + pré-filtro do agregar_municipio

## O que foi feito

### Fix 1 — Shared transformer em `censo_map.py`
Dentro de `render_mapas_censitarios_combinados`, criado um `Transformer` compartilhado uma única vez antes do loop de setores, usando `_local_metric_crs` e `_transformer` (já importados de `censo_point`):
```python
_crs_local = _local_metric_crs(lat, lng)
_to_3857 = _transformer(_crs_local, CRS_WEB_MERCATOR)
```
Substituídas as 3 chamadas pré-loop (`_to_mercator`/`_point_to_mercator`) por `_project_geometry(geom, _to_3857)` e `_to_3857.transform(0.0, 0.0)`, e a chamada dentro do loop por `_project_geometry(geom, _to_3857)`. As funções `_to_mercator` e `_point_to_mercator` permanecem no módulo sem alteração (usadas por `_render_camada` para pins).

### Fix 2 — Parâmetro opcional `df_pre_filtrado` em `agregar_municipio`
- `relatorio_municipal.py`: adicionado `df_pre_filtrado: pd.DataFrame | None = None` à assinatura. Quando fornecido, substitui o full-scan de 1,5 M hexes por `.copy()` direto; caminho sem parâmetro (default `None`) é idêntico ao anterior.
- `pages.py`: importado `_municipio_mask`; 3 callers atualizados — pré-filtro calculado uma vez antes de `agregar_municipio`, passado via `df_pre_filtrado=df_muni`; bloco de re-filtragem duplicado pós-chamada removido; `df_muni` reutilizado em `render_mapas_municipio`.
- `api/service.py`: 1 caller atualizado com `df_pre_filtrado=df_muni` (já disponível na linha acima).

### Testes
- `tests/unit/test_relatorio_pontual_censitario_mapa.py`: adicionado `test_shared_transformer_bytes_identicos` — 2 chamadas consecutivas com os mesmos inputs produzem PNGs byte-idênticos.
- `tests/unit/test_relatorio_municipal.py`: adicionado `test_agregar_municipio_prefiltrado_identico_ao_nacional` — df_pre_filtrado com 4 linhas SP + 100 linhas Manaus produz resultado idêntico ao full-scan em 6 métricas chave.

## Arquivos alterados
- `src/motor_expansao/dashboard/censo_map.py` — Fix 1: transformer compartilhado (5 linhas substituídas/inseridas no bloco pré-loop + loop)
- `src/motor_expansao/dashboard/relatorio_municipal.py` — Fix 2: nova assinatura + branch `df_pre_filtrado`
- `src/motor_expansao/dashboard/pages.py` — Fix 2: import `_municipio_mask` + 3 callers atualizados (pré-filtro + remoção re-filtragem duplicada)
- `src/motor_expansao/api/service.py` — Fix 2: 1 caller com `df_pre_filtrado=df_muni`
- `tests/unit/test_relatorio_pontual_censitario_mapa.py` — novo teste Fix 1
- `tests/unit/test_relatorio_municipal.py` — novo teste Fix 2

## Validações executadas
- pytest subconjunto impactado: **295 passed, 0 failed, 0 skipped** (`tests/unit/test_relatorio_pontual_censitario_mapa.py tests/unit/test_relatorio_municipal.py tests/integration/test_streamlit_app.py`)
- import smoke: **ok** (`import streamlit_app`)
- ruff check 4 arquivos modificados: **All checks passed!**
- git diff --stat HEAD: 7 entradas (6 alvo + config.py CRLF warning pré-existente, não tocado pelo Builder)

```
 src/motor_expansao/api/service.py                         |   1 +
 src/motor_expansao/dashboard/censo_map.py                 |  13 +-
 src/motor_expansao/dashboard/pages.py                     |  40 +---
 src/motor_expansao/dashboard/relatorio_municipal.py       |  13 +-
 src/motor_expansao/dimensionamento/config.py              | 212 (CRLF only)
 tests/unit/test_relatorio_municipal.py                    |  36 ++++
 tests/unit/test_relatorio_pontual_censitario_mapa.py      |  16 ++
 7 files changed, 189 insertions(+), 142 deletions(-)
```

Nota: `config.py` aparece no diff apenas com substituição CRLF→LF (warning pré-existente do git); sem mudança de conteúdo lógico.

## Problemas encontrados
Nenhum.

## Pendências
- Suíte completa: responsabilidade do QA (gate único por ciclo).
- `ruff check` e `mypy` sobre todo o src: responsabilidade do QA.
- Harness de perf B6 (`scripts/perf_baseline_app.py`): responsabilidade do QA / ciclo posterior.

## Riscos remanescentes
Nenhum identificado. Fix 1 é matematicamente idêntico (mesmo transformer, mesmos parâmetros). Fix 2 é zero-diff semântico quando `df_pre_filtrado` é fornecido — validado pelo teste de resultado idêntico.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY sobre M1)
- Artefatos M1 preservados: sim (nenhum parquet de staging/output tocado)
- Dashboard offline mantido: sim (nenhuma dependência de rede nova; DEC-004 intacta)
