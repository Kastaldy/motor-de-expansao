# Handoff — Builder

## Skill que gerou este handoff
Builder

## O que foi implementado
Bloco **BLK-SCORE-01** — dataset rotulado de validacao (Ultra + Skyfit + Engenharia do
Corpo), READ-ONLY sobre o M1. Implementado estritamente conforme o plano do Planner (rev.2)
e a secao autoritativa `REVISAO 2 — Skyfit FINAL (SUPERSEDE)`.

- `analysis/build_validation_dataset.py` (NOVO) + `analysis/__init__.py`: funcoes puras tipadas.
  - `load_ultra`: parquet de performance Ultra (`unidades_ultra_performance_hex.parquet`); score
    M1 `score_priorizacao` embutido; demais scores e `cod_municipio`/`nome_municipio` via join por `hex_id`.
  - `load_skyfit`: desfecho `Sky Fit dados.xlsx` (sheet `Sell Out`, `header=3`, 326 linhas) +
    coords `concorrentes/unidades_skyfit.csv` (sep=";" utf-8-sig, 482 linhas). Match por NOME em
    cascata `nome_exato -> nome_fuzzy (difflib cutoff 0.84) -> cidade_centroide`; hex via
    `latlng_to_h3`. NAO usa os arquivos geocodificados imprecisos; `parse_skyfit_coord` dispensada
    (REVISAO 2; coords ja limpas).
  - `load_engcorpo`: planilha `Academias` (61 linhas) + staging `concorrentes_mapeados.parquet`
    (`rede==engenharia_do_corpo` & `status_registro==valido`, 64 linhas); hex por join de nome
    normalizado; nao-casados marcados (`rotulo_casado=False`), nunca descartados.
  - `resolve_hex`, `latlng_to_h3` (espelha `_latlng_to_h3`: h3 v4 `latlng_to_cell`/fallback v3,
    res=7; degrada para None se h3 ausente ou coord fora da faixa Brasil).
  - `join_scores`: left join dos 4 scores por `hex_id` + flags `score_*_disponivel`;
    `cod_municipio` preservado como string; sem recalculo de score.
  - `normalize_name`, `normalize_name_skyfit`, `match_skyfit_coords`, `join_label_by_name`,
    `unify_wellhub`, `canonical_students`, `add_quality_flags`, `audit_labels`,
    `render_audit_report`, `build`, `main(argv)`.
  - Esquema final = 31 colunas: as do esquema do plano + as NOVAS `hex_origem`/`hex_precisao`
    (REVISAO 2), preenchidas para todas as redes.
- `tests/unit/test_validation_dataset.py` (NOVO): 23 testes, somente fixtures sinteticas
  (sem dados reais), cobrindo os itens 1-10 + 11'/12'/13' do plano de teste.
- `.gitignore` (ALTERADO): adicionadas regras `data/analysis/` + `data/analysis/**` (a regra de
  diretorio sozinha nao fazia `git check-ignore` casar o `.md`; o glob `**` garante; relatorio `.md`
  nao vaza PII). `git check-ignore` confirma .parquet (via `*.parquet`) e .md (via `data/analysis/**`)
  ignorados; `git add --dry-run data/analysis/` nao adiciona nada.
- Artefatos gerados com os dados reais locais: `data/analysis/dataset_validacao.parquet`
  (441 linhas, 31 colunas, 0 unidade_id dup) + `data/analysis/relatorio_auditoria_rotulo.md` (sem PII).

## Correcao vs plano (caminho do dominio)
O plano apontava `data/staging/plano_expansao_dominio.parquet`. Em disco o arquivo real fica em
`data/outputs/plano_expansao_dominio.parquet` (500 hexes; `hex_id` + `score_dominio_hibrido`).
Ajustei a constante `DOMINIO_PARQUET` para o caminho real; o `main` aceita `--dominio` para
override e trata a fonte como OPCIONAL (se faltar, degrada para nulo + flag, sem recalcular
score). Com o caminho corrigido, o dominio junta normalmente (cobertura parcial esperada: so 500
hexes no plano).

## Arquivos alterados
- analysis/__init__.py (NOVO)
- analysis/build_validation_dataset.py (NOVO)
- tests/unit/test_validation_dataset.py (NOVO)
- .gitignore (ALTERADO: regra data/analysis/)
- data/analysis/dataset_validacao.parquet (GERADO, gitignored)
- data/analysis/relatorio_auditoria_rotulo.md (GERADO, gitignored)
- context/handoff.md (este arquivo)
- context/handoff/20260531-005210-builder.md (snapshot append-only, autoritativo)
- tasks/current_task.md (Skill atual: Builder; Proxima: QA)

## Como rodar/validar
- `python -m pytest -q tests/unit/test_validation_dataset.py`  -> 23 passed
- `python -m pytest -q`                                         -> 591 passed, 1 skipped
- `python -m analysis.build_validation_dataset`                 -> grava os 2 artefatos
- `git check-ignore data/analysis/dataset_validacao.parquet data/analysis/relatorio_auditoria_rotulo.md`

## Resultado dos testes
- `pytest -q tests/unit/test_validation_dataset.py`: **23 passed**. Verificado tambem com
  `-W error::FutureWarning` (23 passed) -> o codigo do build NAO emite FutureWarning (o concat
  e blindado por `warnings.catch_warnings` apos alinhamento de colunas).
- `pytest -q` (suite completa): **591 passed, 1 skipped, 11 warnings**
  (baseline informado 532 passed + 1 skipped; +23 novos testes; restante ja existente; sem regressao).
- `python -m analysis.build_validation_dataset` roda fim a fim (inclusive sob `-W error::FutureWarning`)
  e grava os 2 artefatos. dataset_validacao.parquet = 441 linhas, 31 colunas, 0 unidade_id dup.

## Cobertura/auditoria (agregado, sem PII)
- Unidades por rede (entrada == linhas no dataset, sem duplicacao):
  ultra=54, skyfit=326, engcorpo=61 -> total 441 (0 duplicacao).
- Hex resolvido: ultra 53/54 (98.1%); skyfit 301/326 (92.3%); engcorpo 31/61 (50.8%).
- Skyfit por `hex_origem`: nome_exato=175, nome_fuzzy=16, cidade_centroide=110, nao_resolvido=25.
  Por `hex_precisao`: unidade=191, cidade=110, indisponivel=25.
- Alunos recorrentes nulos: ultra=0, skyfit=28, engcorpo=3. Origem/confiabilidade:
  ultra=alunos_total (fallback ativos_pag), medido; skyfit=Alunos EVO, medido;
  engcorpo=Alunos Totais, estimado.
- Scores disponiveis (priorizacao / setor / residual / dominio):
  ultra 53/49/53/0; skyfit 275/296/300/39; engcorpo 30/31/31/3. Dominio total=42
  (so 500 hexes no plano -> cobertura parcial esperada, flag `score_dominio_disponivel`).
- Rotulos nao casados: engcorpo=30 (planilha 61 vs staging por nome normalizado; nao-casados
  marcados + flag, nunca descartados); ultra/skyfit=0 (desfecho na propria fonte).

## Pendencias ou observacoes
- EngCorpo: cobertura de hex/score baixa (31/61). O match por nome normalizado exato e
  conservador (a planilha usa rotulos tipo "EC - MATRIZ, RS" enquanto o staging usa
  "Vacaria, RS" etc.); 30 nao casaram. Por design do plano nao ha fuzzy para EngCorpo (CI
  deterministico). Se o backtest precisar de mais cobertura EngCorpo, recomendar a QA/usuario
  um ciclo de melhoria de match (fora do escopo deste bloco).
- Skyfit: 28 alunos nulos sao linhas do desfecho com `Alunos EVO` vazio na propria planilha;
  preservadas como linha (nao descartadas). `cidade_centroide` (110) e coarse por design.
- h3 instalado e v4 (`latlng_to_cell`; nao ha `geo_to_h3`) — o helper cobre ambos.
- Nota tecnica: durante a edicao, o cabecalho de imports do script foi reescrito para remover
  linhas duplicadas (`import pandas as pd`) acidentais e adicionar `import warnings`; AST e
  import do modulo validados, sem alterar comportamento.
- Nenhum artefato/codigo M1 oficial alterado. `git status --porcelain`:
  `M .gitignore`, `M context/handoff.md`, `M tasks/current_task.md`,
  `?? analysis/`, `?? context/handoff/20260531-005210-builder.md`, `?? tests/unit/test_validation_dataset.py`.
  `M CLAUDE.md` e modificacao pre-existente do usuario (fora deste ciclo). `data/analysis/`
  nao aparece (gitignored).

## Proxima Skill recomendada
QA
