# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (housekeeping move BLK-FIX-12 + merge pelo orquestrador)

## VEREDITO
APROVADO

## Justificativa
O fix corrige os 3 pontos de falha (default `None` em `competitors_logos_dir`, guard None-safe em
`service.py`, env+volume `concorrentes` no serviço `api` do compose) com testes legítimos; todas as 6
validações obrigatórias re-executadas por conta própria passam (suíte FULL `733 passed, 4 skipped` via
`-n auto`), escopo respeitado e zero toque em artefatos/score do M1.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- O teste `test_service_guard_logos_dir_none_resulta_em_none` REPLICA o guard de `service.py` em vez de
  invocar `service.gerar_pdf_ponto` diretamente. É evidência válida (prova que o `is not None` evita o
  `TypeError` de `Path(None).is_dir()`), mas é "leve": se o guard em `service.py` mudar, o teste continua
  verde por não estar acoplado à função real. Um teste futuro poderia mockar o motor (`_resolver_e_carregar`
  + `analisar_ponto_censitario_setores`) e chamar `gerar_pdf_ponto` para amarrar o comportamento à função
  de produção. NÃO-bloqueador.

## Testes faltantes
- nenhum (cobertura suficiente para um fix de configuração/guard: default None, env→Path, preload popula
  `_ICON_CACHE`, guard None→None). Ver melhoria opcional acima.

## Riscos remanescentes
- O efeito útil (logos aparecerem no PDF) depende de o host ter `/opt/motor-expansao/concorrentes` com os
  `logo_<rede>.png` reais — passo operacional VPS exclusivo do humano (§6). Se ausente em runtime, o guard
  retorna `logos_dir=None` graciosamente (fallback de sigla); sem regressão.
- `_ICON_CACHE` é global de módulo; os testes limpam a chave em `finally` — sem vazamento de estado entre
  testes (verificado no código do teste).

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte FULL (gate único) — `python -m pytest -n auto -q`
```
........................................................................ [ 19%]
........................................................................ [ 29%]
........................................................................ [ 39%]
.........................................s.s............................ [ 48%]
........................................................................ [ 58%]
........................................................................ [ 68%]
........................................................................ [ 78%]
........................................................................ [ 88%]
........................................................................ [ 97%]
....s...........                                                         [100%]
733 passed, 4 skipped, 153206 warnings in 103.07s (0:01:43)
```
xdist (`-n auto`) rodou sem problema neste ambiente (Python 3.14/Windows) — NÃO foi necessário cair para
serial. 0 failed. (Os 4 skipped são `pytest.importorskip("httpx")` e afins; httpx não instalado localmente
— esperado/documentado.) A contagem absoluta diverge das baselines históricas do CLAUDE.md por ambiente
(Python 3.14 local), não por defeito.

### 2. `python -m pytest -q tests/unit/test_api_skeleton.py tests/unit/test_ultra_pins.py`
```
33 passed, 2 skipped, 1762 warnings in 1.57s
```
2 skipped = `pytest.importorskip("httpx")` (esperado).

### 3. `python -c "import streamlit_app; print('import ok')"`
```
2026-06-12 20:22:58.762 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
(...repetições do mesmo WARNING de runtime ausente — esperado fora do `streamlit run`...)
import ok
```

### 4. `python -c "from motor_expansao.api.settings import Settings; s=Settings(); assert s.competitors_logos_dir is None; print('OK None')"`
```
OK None
```

### 5. `python -m ruff check src/motor_expansao/api/settings.py src/motor_expansao/api/service.py tests/unit/test_api_skeleton.py`
```
All checks passed!
```

### 6. `python -m mypy src/motor_expansao/api/settings.py src/motor_expansao/api/service.py`
```
Success: no issues found in 2 source files
```

### Housekeeping (Passo 6.0) — FALHA ESPERADA pré-fechamento
`python scripts/housekeeping_move_block.py BLK-FIX-12 --check`
```
FALHA --check: stub ausente no backlog para BLK-FIX-12
REAL_EXIT=1
```
FALHA é ESPERADA: o bloco ainda está em `tasks/backlog.md` (heading `### BLK-FIX-12` presente na linha 292)
e o move via helper é executado pelo ORQUESTRADOR no fechamento, APÓS este veredito. O exit code real é 1
(falha legítima, não erro de tooling).

`python -m pytest -q tests/unit/test_housekeeping_helper.py`
```
10 passed, 491 warnings in 0.13s
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho
crítico. Os 4 novos testes usam fixtures legítimas (`tmp_path`, PNG mínimo 1×1 válido) para isolar lógica —
prática normal e fora do escopo da regra de bypass (que mira tooling dependente de config/artefatos reais de
produção). O teste (c) `test_preload_logos_com_dir_valido_popula_icon_cache` exercita a FUNÇÃO REAL
`competitors.preload_logos` com o filename real (`COMPETITOR_LOGO_FILES["smart_fit"] = "logo_smart_fit.png"`,
verificado no código), não um stub. Smokes 3/4 rodam contra o código real importado. Suíte FULL roda contra o
código real. Sem bypass.

## Conferência cruzada com log do Builder
Bate. O Builder reportou: subconjunto `33 passed, 2 skipped` (idêntico ao meu re-run #2); `import ok`;
`OK None`; ruff `All checks passed!`; mypy `Success: no issues found in 2 source files`. Todos reproduzidos
literalmente. O `git diff` dos 3 arquivos de código confere com o descrito no handoff do Builder (default
`Path | None = None`; guard `is not None and ...is_dir()`; env `API_COMPETITORS_LOGOS_DIR: "/app/concorrentes"`
+ volume `/opt/motor-expansao/concorrentes:/app/concorrentes:ro` espelhando o `streamlit`). Doc atualizada
(§3 tabela de volumes + §4 default `None`). Divergência única e benigna: o Builder rodou a FULL serial; o QA
(gate) rodou `-n auto` e xdist funcionou aqui — contagem `733 passed, 4 skipped` (o subconjunto do Builder não
é a suíte FULL, então as contagens não são comparáveis entre si; ambas com 0 failed).

## Guardrails verificados
- score_priorizacao não alterado: sim (não aplicável — fix de configuração/visualização; nenhum pipeline M1,
  parquet ou fórmula tocado; `git diff --name-only` só lista os 5 arquivos do ciclo + process files)
- Artefatos M1 preservados: sim (nenhum artefato oficial no diff)
- Parâmetros canônicos preservados: sim (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, pesos
  renda=0.40/pop=0.60 — nenhum parâmetro de score tocado; só `competitors_logos_dir` na camada de config da API)
- Validações re-executadas pelo QA (sem bypass):
  - suíte FULL `-n auto` → 733 passed, 4 skipped, 0 failed
  - subconjunto api_skeleton+ultra_pins → 33 passed, 2 skipped
  - import streamlit_app → import ok
  - settings default None → OK None
  - ruff (3 arquivos) → All checks passed!
  - mypy (2 arquivos) → Success: no issues found
  - housekeeping --check → FALHA esperada (exit 1; stub ausente pré-fechamento)
  - test_housekeeping_helper → 10 passed
- Escopo respeitado: sim. `git diff --name-only` lista exatamente os 5 arquivos do ciclo
  (`src/motor_expansao/api/settings.py`, `src/motor_expansao/api/service.py`, `docker-compose.prod.yml`,
  `tests/unit/test_api_skeleton.py`, `docs/api_geoespacial_deploy.md`) + arquivos de processo
  (`context/handoff.md`, `tasks/current_task.md`) + os 2 dirty PRÉ-EXISTENTES e NÃO relacionados
  (`data/raw/ibge/malha_brasil.geojson`, `data/raw/ibge/malha_uf_brasil.geojson`, ambos `D`) que NÃO entram no
  escopo do ciclo. Dashboard (`competitors.py`/`censo_map.py`/`pages.py`/`streamlit_app.py`) e pipelines M1
  INTOCADOS (não aparecem no diff). §6 VPS: nenhum comando executado no servidor; orientações de deploy
  deixadas para o humano. §2 assets gitignored: nenhum PNG de logo versionado.

## Decisão recomendada
Fechar ciclo: o orquestrador executa o housekeeping move (`scripts/housekeeping_move_block.py BLK-FIX-12`),
faz merge e (passo operacional do humano, §6) garante `/opt/motor-expansao/concorrentes` no host antes do
redeploy da imagem `api`.
