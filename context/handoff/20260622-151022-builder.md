# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-DIM-20 — UI: parâmetros de fluxo de caixa editáveis (capex parcelado)

## O que foi feito

1. **`simulador.py`**: Adicionados 3 parâmetros keyword-only na assinatura de `viabilidade()` imediatamente após `coef_capex_m2`:
   - `capex_financiado_pct: float = 0.0`
   - `prazo_financiamento_meses: int = 36`
   - `juros_financiamento_am: float = 0.018`
   Docstring da função atualizada com descrição dos 3 novos parâmetros.
   Docstring de `ViabilidadeResult.payback_meses` atualizada para mencionar a PMT.

2. **`simulador.py`**: Bloco PMT calculado UMA VEZ antes do `for t in range(1, 61):` (seção 11a):
   - `_C = capex_efetivo * capex_financiado_pct`
   - Se `_C > 0` e `juros > 0`: fórmula de anuidade `_pmt = _C * r * (1+r)^n / ((1+r)^n - 1)`
   - Se `_C > 0` e `juros == 0`: parcela simples `_pmt = _C / max(n, 1)`
   - Caso contrário: `_pmt = 0.0`

3. **`simulador.py`**: Dentro do loop, após `fcf_t = eb_t - ir_t` e antes de `fcf_acum += fcf_t`:
   ```python
   if _pmt > 0 and t <= prazo_financiamento_meses:
       fcf_t -= _pmt
   ```
   EBITDA (seções 7-8) intocado — PMT afeta somente FCF no loop.

4. **`pages.py`**: Adicionado `SIM_CAPEX_DEFAULT` ao import de `motor_expansao.dimensionamento.config` (linha 96; ruff --fix aplicado para ordenação de imports).

5. **`pages.py`**: Dentro do `with st.expander("Parametros avancados", ...)`, após `margem_alvo_pct` e antes do `st.form_submit_button`:
   - `st.markdown("**Capex e financiamento**")`
   - `capex_total = st.number_input("Capex total (R$)", value=float(SIM_CAPEX_DEFAULT), ...)`
   - `pct_financiado = st.slider("% do capex financiado", ...)`
   - Se `pct_financiado > 0`: campos condicionais `prazo_financiamento_meses` (number_input) e `juros_am_pct` (number_input) + caption
   - Branch `else`: defaults `prazo_financiamento_meses=36`, `juros_am_pct=1.8`

6. **`pages.py`**: Na chamada `analisar_viabilidade_ponto(...)` adicionados kwargs:
   - `capex=float(capex_total)`
   - `capex_financiado_pct=float(pct_financiado) / 100.0`
   - `prazo_financiamento_meses=int(prazo_financiamento_meses)`
   - `juros_financiamento_am=float(juros_am_pct) / 100.0`
   `viabilidade_ponto.py` não alterado — já propaga `**kwargs`.

7. **`test_simulador.py`**: Adicionados 4 testes CA-10a..d ao final do arquivo:
   - CA-10a (`test_financiamento_zero_resultado_identico`): regressão zero — `capex_financiado_pct=0.0` dá resultado idêntico.
   - CA-10b (`test_financiamento_aumenta_payback`): financiamento 100% => payback estritamente maior.
   - CA-10c (`test_financiamento_nao_altera_ebitda`): `margem_ebitda_pct` e `ebitda_mensal` idênticos com e sem financiamento.
   - CA-10d (`test_pmt_formula_correta`): PMT de referência para C=600k, r=0.018, n=36 cai em [22k, 24k].

## Arquivos alterados
- `/repo/src/motor_expansao/dimensionamento/simulador.py` — assinatura com 3 novos params, docstrings, bloco PMT (seção 11a) antes do loop, subtração `fcf_t -= _pmt` no loop
- `/repo/src/motor_expansao/dashboard/pages.py` — import `SIM_CAPEX_DEFAULT`, novos widgets no expander, kwargs na chamada de `analisar_viabilidade_ponto`
- `/repo/tests/unit/dimensionamento/test_simulador.py` — CA-10a..d adicionados ao final

## Validações executadas
- `pytest tests/unit/dimensionamento/test_simulador.py -q`: **35 passed** (31 originais + 4 novos CA-10a..d)
- `pytest tests/unit/dimensionamento/test_simulador.py tests/integration/test_streamlit_app.py -q`: **244 passed**
- `ruff check` (3 arquivos alterados): **All checks passed** (1 issue de import sort corrigida com `--fix`)
- `mypy src/motor_expansao/dimensionamento/simulador.py src/motor_expansao/dashboard/pages.py --ignore-missing-imports`: **Success: no issues found in 2 source files**
- `python -c "import streamlit_app; print('import ok')"`: **import ok**

## Problemas encontrados
- Ruff reportou I001 (import block unsorted) em `pages.py` após adicionar `SIM_CAPEX_DEFAULT`. Corrigido com `ruff check --fix`. Nenhum problema funcional.

## Pendências
- Suite completa (`pytest -q`) fica a cargo do QA (gate único por ciclo).
- Regeneração de parquets de staging: não aplicável (bloco READ-ONLY sobre M1, sem toque em artefatos oficiais).

## Riscos remanescentes
- Nenhum. CA-10a valida regressão zero. CA-10c valida que EBITDA é imune à PMT.

## Guardrails verificados
- score_priorizacao não alterado: não aplicável (bloco READ-ONLY sobre M1)
- Artefatos M1 preservados: sim — nenhum parquet ou artefato oficial tocado
- Dashboard offline mantido: sim — novos campos são widgets locais dentro do formulário; nenhuma chamada de rede adicionada
- `config.py` do M1 intocado: sim
- `viabilidade_ponto.py` intocado: sim (propaga `**kwargs` automaticamente)
- Loop guard (`scripts/loop_guard.py`): não ativa — diff não toca `config.py`/`pipelines/m1`/`*scoring*`/artefatos M1/deploy/Dockerfiles
