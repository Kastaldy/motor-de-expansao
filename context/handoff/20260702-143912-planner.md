# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
REVISÃO HUMANA (gate) — anti-PII + estratégia de DEDUP. Aprovação humana OBRIGATÓRIA antes do Builder.

## Entendimento da tarefa
Ingerir `NAO_ABRA/03_Competidores.xlsx` (24.045 academias menores WellHub/TotalPass) como camada de OFERTA agregada por `hex_id` (H3 res-7), descartando toda PII (`Latitude`/`Longitude`/`Nome_Academia`) na fronteira de entrada, materializando `data/staging/oferta_academias_menores_h3.parquet` (gitignored, NÃO oficial) + relatório de qualidade/DEDUP vs `concorrentes_mapeados.parquet` — sem tocar M1 nem recompor o residual (DEC-013 parte 3, 1º passo).

## Inspeção de confirmação (schema/agregados; nenhuma linha/PII impressa)
- `03_Competidores.xlsx`: 1 aba, 24.045 × 9 colunas. Confirmado o schema do BO. `Cluster_ID` 100% nula. `Plano` = 11 categorias (`tp1_plus` 6.129, `tp2` 4.476, `tp1` 3.886, `tp3` 3.618, `tp4` 2.154, `tp2_plus` 1.406, `tp0` 1.273, `tp5` 712, `tp5_plus` 152, `tp6` 129, `tp7` 110). `Alunos_Academia`: min 0, mediana 19, média 79,9, máx 5.276; **4.090 zeros** (17%). Coords: **mediana e máx de casas decimais = 2** (~1,1 km, caveat confirmado). Cabeçalho `Munic�pio` corrompido (encoding).
- **DEDUP medido (agregado, coords derivadas na fronteira e descartadas):** academias menores caem em **6.785 hexes res-7 únicos**; `concorrentes_mapeados.parquet` tem **1.659 hexes** únicos; **sobreposição = 1.425 hexes**. **9.482 academias menores (39%) caem em hex já coberto por concorrente mapeado**, somando **1.203.919 alunos** dos 1.920.955 totais (63% do total de alunos) — dupla-contagem de oferta é risco central e alto.
- `openpyxl>=3.1.0` **JÁ é dep base** do `pyproject.toml` (v3.1.5 instalada) → **NÃO é dependência nova**. `h3`/`pandas`/`pyarrow` também na base.
- 4 artefatos oficiais M1: `brasil_estrutural.parquet`, `brasil_priorizados.parquet`, `hexagonos_brasil_oportunidades.parquet` em `data/staging/`; `hexagonos_brasil_dashboard.parquet` em **`data/outputs/`** (não em staging). Mtime de todos deve permanecer inalterado.
- `tests/unit/demanda_revelada/` já existe como pacote (com `__init__.py` vazio).

## Plano técnico
1. **Contrato novo** em `src/motor_expansao/demanda_revelada/oferta_academias_menores.py` (módulo único contrato+ingestão, análogo ao par `contrato.py`/`ingestao.py`, mas autocontido). Constantes puras no topo:
   - `VERSAO_CONTRATO_OFERTA_MENORES = "oferta_academias_menores_v1"`.
   - `H3_RES_CONTRATO = 7` (reusar/importar de `.contrato`).
   - `CONTRATO_COLUNAS_OFERTA_MENORES: dict[str, str]` = ordem/dtypes canônicos (lista exata em D3, recomendação abaixo).
   - Reusar `COLUNAS_PII_PROIBIDAS` de `.contrato` e ESTENDER localmente com `frozenset({"nome_academia", "total_alunos_cluster"})` — `Total_Alunos_Cluster` entra na lista de proibidas para blindar contra dupla-contagem por cluster (nunca somar). Definir `_COLUNAS_PII_LOCAIS = COLUNAS_PII_PROIBIDAS | {...}` e validar contra ela.
2. **Leitura anti-PII na fronteira** — função pura `_ler_e_derivar_hex(fonte: Path) -> pd.DataFrame`:
   - `pd.read_excel(fonte, sheet_name=0)`; renomear coluna corrompida `Munic�pio` por índice/normalização (não é chave crítica — a chave é coord → hex).
   - Derivar `hex_id` via helper `_to_cell(lat, lng)` (copiar o padrão de `ingestao._to_cell`: `h3.latlng_to_cell(float, float, 7)`, `try/except (ValueError, TypeError) -> None`).
   - **IMEDIATAMENTE** após derivar `hex_id`: `df = df.drop(columns=["Latitude","Longitude","Nome_Academia","Cluster_ID"])` (dropar PII e a coluna 100% nula). Nunca persistir/logar Lat/Lng/Nome.
   - `dropna(subset=["hex_id"])`.
   - Retorna frame SEM PII com `hex_id`, `Total_Academias`, `Total_Alunos_Cluster` (só p/ auditoria em memória, NÃO agregada), `Plano`, `Alunos_Academia`, `municipio` (normalizado).
3. **Agregação por hex** — função pura `_agregar_h3(df: pd.DataFrame) -> pd.DataFrame`:
   - `g = df.groupby("hex_id")`.
   - `n_academias_menores = g.size()`.
   - `alunos_academias_menores = g["Alunos_Academia"].sum()` — **SÓ** `Alunos_Academia`; `Total_Alunos_Cluster` **NUNCA** somado (proibido; é soma por cluster).
   - Distribuição por `Plano` (D3): pivô `g["Plano"].value_counts()` → colunas `n_plano_tp0..tp7` + `_plus` conforme recomendação D3 (ver abaixo). Preencher 0, int64.
   - Coerção de dtypes conforme `CONTRATO_COLUNAS_OFERTA_MENORES` (int64 sem NaN; `hex_id`/`versao_contrato` string).
   - `versao_contrato = VERSAO_CONTRATO_OFERTA_MENORES`.
   - Ordenar por `hex_id`, `reset_index(drop=True)`, reordenar para a ordem canônica do contrato (determinismo/reprodutibilidade).
4. **Redes de segurança** — reusar o padrão do `ingestao.py`:
   - `_assert_sem_pii(df)`: falha se houver coluna fora do contrato OU coluna em `_COLUNAS_PII_LOCAIS` (inclui `total_alunos_cluster`/`nome_academia`).
   - `_assert_res7(df)`: `h3.is_valid_cell` + `get_resolution == 7` para todo `hex_id`.
5. **Função pública** `ingerir_oferta_academias_menores(fonte: Path = FONTE_DEFAULT, destino: Path = DESTINO_DEFAULT, *, escrever: bool = True) -> pd.DataFrame`:
   - `FONTE_DEFAULT = Path("NAO_ABRA/03_Competidores.xlsx")`; `DESTINO_DEFAULT = Path("data/staging/oferta_academias_menores_h3.parquet")`.
   - Fluxo: `_ler_e_derivar_hex` → `_agregar_h3` → `_assert_sem_pii` → `_assert_res7` → (se `escrever`) `to_parquet(index=False)`.
   - Parametrizável por `fonte` (testes usam fixture sintética, nunca o dump real).
6. **Relatório de qualidade + DEDUP** — função pura `gerar_relatorio_qualidade(df_oferta, concorrentes_path, universo_path, destino_md, *, escrever=True) -> str` no mesmo módulo (ou submódulo `oferta_academias_menores_relatorio.py`):
   - Entradas: o frame agregado + `data/staging/concorrentes_mapeados.parquet` (chave `hex_id_res7`) + universo do Motor `data/outputs/hexagonos_brasil_dashboard.parquet` (coluna `hex_id`).
   - Conteúdo (só agregados, ZERO PII): nº academias/alunos agregados; nº hexes; % que casa com o universo do Motor; **sobreposição com `concorrentes_mapeados`** (nº hexes com ambos; nº academias menores e Σ alunos em hex já coberto — os números medidos: 6.785 hexes, 1.425 overlap, 9.482 academias / 1,20 M alunos em hex coberto); caveat de coords ~1 km; distribuição por `Plano`; **estratégia de DEDUP candidata registrada** (D1). Nunca ler/gravar `Nome_Academia`.
   - Destino: `data/reports/scratch/oferta_academias_menores_qualidade.md` (gitignored). Retorna a string do markdown.
7. **Fixture sintética** `tests/fixtures/oferta_academias_menores_fake.xlsx`: um `.xlsx` sintético (gerar com `openpyxl`/pandas num script utilitário ou commitar direto) com ~6–10 linhas SEM PII real (coords públicas conhecidas p/ hexes previsíveis, nomes fictícios tipo `"FAKE GYM A"`, valores de `Alunos_Academia`/`Plano`/`Total_Alunos_Cluster` controlados p/ asserts determinísticos). Colunas idênticas ao schema real (incluindo cabeçalho corrompido `Munic�pio` p/ testar a normalização).
8. **Testes** `tests/unit/demanda_revelada/test_ingestao_oferta_menores.py`:
   - `test_parquet_existe_e_legivel`; `test_contrato_colunas_e_dtypes`; `test_zero_pii` (nenhuma coluna em `_COLUNAS_PII_LOCAIS`: `Latitude`/`Longitude`/`Nome_Academia` normalizadas + `total_alunos_cluster`); `test_hex_res7`; `test_agregacao_correta` (`Alunos_Academia` somado; assert explícito de que `Total_Alunos_Cluster` NÃO vira coluna nem é somado); `test_reprodutibilidade` (frames idênticos, ordenado por hex_id); `test_isolamento_import` (o módulo não importa `pipelines.m1`/`dashboard`/`censo`/`api` — ler o source e/ou checar `sys.modules`); `test_relatorio_sem_pii` (a string do relatório não contém tokens PII/nomes).
9. **Exports** em `src/motor_expansao/demanda_revelada/__init__.py`: adicionar `ingerir_oferta_academias_menores`, `gerar_relatorio_qualidade`, `CONTRATO_COLUNAS_OFERTA_MENORES`, `VERSAO_CONTRATO_OFERTA_MENORES` ao import e `__all__`.
10. **Doc (opcional, curto)** em `docs/modelo_mercado_hexagonos.md` (§7): nota de 2–3 linhas de que a oferta de academias menores foi STAGED como camada separada `oferta_academias_menores_h3.parquet`, ainda **NÃO** integrada ao residual (follow-up BLK-TP-09).
11. **Bookkeeping**: `tasks/backlog.md` (bloco BLK-TP-08 → concluído no fechamento pelo QA), `tasks/current_task.md`, `tasks/completed.md`. Commit por path (só os caminhos abaixo). NÃO commitar `NAO_ABRA/`, parquet de staging nem o relatório (todos gitignored).

## Arquivos afetados
- `src/motor_expansao/demanda_revelada/oferta_academias_menores.py` (novo — contrato + ingestão + relatório; ou par `_relatorio.py` a critério do Builder)
- `src/motor_expansao/demanda_revelada/__init__.py` (adicionar exports)
- `tests/unit/demanda_revelada/test_ingestao_oferta_menores.py` (novo)
- `tests/fixtures/oferta_academias_menores_fake.xlsx` (novo — fixture sintética SEM PII)
- `docs/modelo_mercado_hexagonos.md` (opcional, nota curta §7)
- Gerados em runtime (NÃO commitáveis, gitignored): `data/staging/oferta_academias_menores_h3.parquet`, `data/reports/scratch/oferta_academias_menores_qualidade.md`
- Bookkeeping: `tasks/backlog.md`, `tasks/current_task.md`, `tasks/completed.md`, `context/handoff.md`, `context/handoff/`

## Dependências
- `openpyxl>=3.1.0` — **JÁ é dep base** (v3.1.5), engine de `.xlsx` do `pandas.read_excel`. **NENHUMA dependência nova.**
- `h3`, `pandas`, `pyarrow` — deps base. `re`/`json`/`pathlib` — stdlib.
- Leitura (read-only) de `data/staging/concorrentes_mapeados.parquet` (chave `hex_id_res7`) e `data/outputs/hexagonos_brasil_dashboard.parquet` (coluna `hex_id`) só no relatório.

## Riscos técnicos
- **PII na origem** (`Latitude`/`Longitude`/`Nome_Academia`): mitigado por DEC-012 — drop na fronteira logo após derivar `hex_id`; `_assert_sem_pii` + `test_zero_pii`; fonte real gitignored (`NAO_ABRA/`); fixtures sintéticas.
- **Dupla-contagem de oferta (risco central):** 39% das academias menores (63% dos alunos) caem em hex já coberto por concorrente mapeado. Neste bloco é só QUANTIFICADO no relatório — NÃO subtrair oferta (follow-up). `Total_Alunos_Cluster` proibido de somar (blindado na lista PII local).
- **Coords ~2 casas (~1 km):** ruído no join res-7 (academias distintas colapsam no mesmo hex; borda incerta). Documentar no relatório; camada é refino, não verdade fina.
- **`Alunos_Academia` com 4.090 zeros (17%) e outliers (máx 5.276):** decidir no gate se filtra/clipa (recomendação: NÃO filtrar neste bloco — só somar e documentar a distribuição; filtro é decisão de integração/follow-up).
- **Cabeçalho `Munic�pio` corrompido:** normalizar por índice/rename; não é chave (coord → hex é a chave).
- **Universo do Motor para o % de cobertura:** usar `data/outputs/hexagonos_brasil_dashboard.parquet` (coluna `hex_id`); se ausente em runtime, degradar graciosamente no relatório (reportar "universo indisponível").
- **Tentação de recompor o residual:** proibido neste bloco (follow-up com gate próprio).

## Critérios de aceite finais
- Módulo novo isolado: `grep` de import NÃO acusa `pipelines.m1`, `dashboard`, `censo`, `api` (teste `test_isolamento_import`).
- `data/staging/oferta_academias_menores_h3.parquet` gerado, agregado por `hex_id` res-7 válido (`h3.is_valid_cell` + `get_resolution==7`), com `versao_contrato="oferta_academias_menores_v1"`.
- **Zero PII** no parquet/relatório/teste: nenhuma coluna de `_COLUNAS_PII_LOCAIS` (Lat/Lng/Nome + `total_alunos_cluster`); `test_zero_pii` falha se qualquer PII aparecer.
- **Relatório de qualidade + DEDUP** presente e legível: nº academias/alunos, nº hexes, % casado com o universo, caveat coords ~1 km, medição de sobreposição vs `concorrentes_mapeados` (hexes/alunos em hex coberto) + estratégia de DEDUP candidata registrada.
- Fonte real (`NAO_ABRA/03_Competidores.xlsx`) NÃO versionada; testes usam fixture sintética (a suíte não lê o dump real).
- **mtime dos 4 artefatos oficiais M1 inalterado** (`brasil_estrutural`, `brasil_priorizados`, `hexagonos_brasil_oportunidades` em `data/staging/`; `hexagonos_brasil_dashboard` em `data/outputs/`).
- `score_oportunidade_residual` e parquets de mercado NÃO regenerados/alterados.
- Suíte verde (`pytest -n auto`); `import streamlit_app` ok; ruff + mypy limpos.

## Validações obrigatórias
- Comando: `python -m pytest -n auto -q tests/unit/demanda_revelada/ tests/integration/test_streamlit_app.py` — Critério: 0 falhas.
- Comando: `python -c "import streamlit_app"` — Critério: 0 erro de import.
- Comando: isolamento — `python -c "import ast,sys; src=open('src/motor_expansao/demanda_revelada/oferta_academias_menores.py').read(); assert not any(k in src for k in ('pipelines.m1','dashboard','censo','motor_expansao.api'))"` (ou o teste `test_isolamento_import`) — Critério: sem import proibido.
- Comando: mtime — capturar `stat` dos 4 oficiais ANTES do Builder e comparar DEPOIS (`data/staging/brasil_estrutural.parquet`, `brasil_priorizados.parquet`, `hexagonos_brasil_oportunidades.parquet`; `data/outputs/hexagonos_brasil_dashboard.parquet`) — Critério: mtime inalterado.
- Comando: `python -m ruff check src/motor_expansao/demanda_revelada/ tests/unit/demanda_revelada/` e `python -m mypy src/motor_expansao/demanda_revelada/oferta_academias_menores.py` — Critério: limpos.
- Comando (gate de dep — confirmação): `python -c "import openpyxl; print(openpyxl.__version__)"` — Critério: já disponível (nenhuma dep nova).

## DECISÕES PARA O GATE HUMANO
### D1 — Estratégia de DEDUP
Opções: (a) só por `hex_id` (overlap) no relatório; (b) `hex_id` + token de nome normalizado EM MEMÓRIA como reforço (sem persistir nome); (c) marcar flag de sobreposição no parquet.
**Recomendação: (a) sobreposição por `hex_id` como cross-check PRIMÁRIO, quantificada SÓ no relatório, complementada por (b) contagem de tokens de rede em memória (sem persistir nome) apenas como sinal auxiliar no relatório.** NÃO marcar flag no parquet (c) nem subtrair oferta — a subtração/integração fina é follow-up (BLK-TP-09, sob gate). Motivo: o DEDUP aqui é de RELATÓRIO (medir o risco: 1.425 hexes em comum, 9.482 academias / 1,20 M alunos já cobertos), não de correção de oferta; manter o parquet limpo de julgamento de dedup preserva a fronteira anti-PII e evita acoplar decisão de integração ao artefato bruto.

### D2 — Caveat de coords ~1 km
Opções: aceitar o ruído (como BLK-TP-01) e documentar; ou arredondamento explícito adicional.
**Recomendação: aceitar o ruído e DOCUMENTAR no relatório (mesmo caveat do BLK-TP-01).** Sem arredondamento extra — a derivação de `hex_id` res-7 já absorve as ~2 casas; arredondar mais só perderia informação. Registrar que a camada é refino, não verdade fina.

### D3 — Contrato de colunas agregadas (lista exata)
**Recomendação:** parquet com **`hex_id` (string), `n_academias_menores` (int64), `alunos_academias_menores` (int64, Σ `Alunos_Academia`), distribuição por `Plano` como 11 colunas `n_plano_<tp>` (int64: `n_plano_tp0`, `n_plano_tp1`, `n_plano_tp1_plus`, `n_plano_tp2`, `n_plano_tp2_plus`, `n_plano_tp3`, `n_plano_tp4`, `n_plano_tp5`, `n_plano_tp5_plus`, `n_plano_tp6`, `n_plano_tp7`), `versao_contrato` (string)`.
- **Incluir distribuição por `Plano`?** SIM — é não-PII, barato, e é o único proxy de tipo/capacidade (útil ao follow-up de capacidade por tipo da DEC-013). 
- **Incluir flag de sobreposição com concorrente mapeado no parquet?** NÃO (coerente com D1 — fica só no relatório). Alternativa mínima, se o gate preferir: manter só `hex_id`/`n_academias_menores`/`alunos_academias_menores`/`versao_contrato` (4 colunas) e mover a distribuição de `Plano` para o relatório. Recomendo a versão COM `Plano` no parquet.
- **`Total_Alunos_Cluster`:** NUNCA como coluna nem soma (proibido — dupla-contagem por cluster).

### D4 — Escopo (não integrar ao residual)
**Recomendação: MANTER — este bloco só INGERE + gera relatório; NÃO recompõe `score_oportunidade_residual`/`oferta_efetiva_disponivel`, NÃO regenera parquets de mercado.** A integração (dedup fino + subtração de oferta + capacidade por tipo/Huff) é epic futura (BLK-TP-09 / DEC-013 parte 3), sob gate próprio.

## Fora de escopo
- Recompor/alterar `score_oportunidade_residual`, `oferta_efetiva_disponivel`, `oferta_consumida_*` ou qualquer coluna de `hexagonos_mercado_mapeado.parquet` / `calcular_colunas_mercado.py`.
- Regenerar qualquer parquet de mercado/residual/carteira/plano ou os 4 artefatos oficiais do M1.
- Escrita/recálculo em `score_priorizacao`, `hex_score_estrutural`, pesos (renda 0.40/pop 0.60).
- Modelar capacidade real por tipo/plano (Huff/dedup fino) — epic futura; aqui só se DOCUMENTA a variabilidade no relatório.
- Import de `pipelines/m1`, `dashboard`, `censo_*` ou `api` no módulo novo.
- Versionar a fonte real ou qualquer parquet/relatório com PII; persistir/logar Lat/Lng/Nome individuais.
- Deploy, VPS, alteração do contrato `demanda_revelada_v1` existente.

## Alerta de aprovação
**ATENÇÃO: ESTA TAREFA É DE CRITICIDADE ALTA E INGERE UMA FONTE EXTERNA COM PII NA ORIGEM (`Latitude`/`Longitude`/`Nome_Academia`) + EXIGE JULGAMENTO DE DEDUP. REVISÃO HUMANA OBRIGATÓRIA (GATE ANTI-PII + DEDUP) ANTES DO BUILDER. O USUÁRIO DEVE LER ESTE HANDOFF E CONFIRMAR AS DECISÕES D1–D4 (estratégia de DEDUP, caveat de coords, contrato de colunas agregadas, escopo sem integração ao residual). É READ-ONLY SOBRE O M1 — NÃO ALTERA SCORE/PESOS/CARTEIRA/PLANO/ARTEFATOS OFICIAIS.**

## Guardrails ativos
- **§5 (READ-ONLY sobre o M1):** zero escrita/recálculo em `score_priorizacao`, `hex_score_estrutural`, carteira, plano, artefatos oficiais; NÃO recompor o residual; mtime dos 4 oficiais inalterado.
- **DEC-012 (anti-PII por construção):** consumir só o agregado por hex; drop Lat/Lng/Nome na fronteira; zero PII em artefato/log/teste (`_COLUNAS_PII_LOCAIS` + `test_zero_pii`); fonte real em `NAO_ABRA/` (gitignored, nunca versionada); fixtures sintéticas.
- **DEC-013 (parte 3):** agregadores WellHub/TotalPass coletados/armazenados com DEDUP + capacidade por tipo ANTES de qualquer integração ao residual. Este bloco só INGERE + relatório de DEDUP; integração é epic futura sob gate.
- **DEC-009 (intacta):** oferta observada entra como insumo, NUNCA como preditor geográfico de magnitude.
- **Isolamento de pacote:** o módulo novo NÃO importa de `pipelines/m1`, `dashboard`, `censo_*`, `api`.
