# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Approver (aprovação humana obrigatória antes do Builder — criticidade Alta; toca `streamlit_app.py` de produção)

## Entendimento da tarefa
Inserir um bloco de fallback em `load_base_calibracao()` (streamlit_app.py, linhas 353–354) para ler `unidades_ultra_performance_hex.parquet` quando `base_calibracao_multirede.parquet` não existe, garantindo que a faixa p10/p50/p90 de alunos/m² apareça no dashboard em produção sem o multirede.

---

## Decisão técnica: abordagem para `alunos_por_m2` no fallback

**Decisão: ABORDAGEM (a) — re-derivar via `alunos_total / metragem` com o mesmo guard.**

Justificativa: mantém consistência com o bloco principal da função (linhas 356–358) e com `calcular_penetracao_ultra_hex.py:174`; evita dependência silenciosa de uma coluna pré-computada que poderia mudar de semântica em versões futuras do parquet. A coluna `alunos_por_m2` pré-existente no parquet é IGNORADA; o fallback deriva do zero usando `alunos_total` (campo equivalente ao `alunos_reais` do multirede; `alunos_reais` NÃO existe neste parquet).

---

## Plano técnico

### Passo 1 — Definir a constante do path de fallback (streamlit_app.py, após linha 209)

Adicionar imediatamente após a definição de `BASE_CALIBRACAO_PATH` (linha 207–209) uma nova constante:

```python
UNIDADES_ULTRA_PERF_PATH = (
    BASE_CALIBRACAO_PATH.parent / "unidades_ultra_performance_hex.parquet"
)
```

Derivar de `BASE_CALIBRACAO_PATH.parent` (= `data/staging/`) porque não existe `STAGING_DIR` nomeado no módulo. Esta constante serve também como alvo de monkeypatch no teste.

### Passo 2 — Inserir bloco de fallback em `load_base_calibracao()` (streamlit_app.py, linhas 353–354)

Substituir o `return pd.DataFrame()` atual pelo seguinte bloco (mantendo o `@st.cache_data` e toda a lógica do caminho principal intactos):

```python
    if not BASE_CALIBRACAO_PATH.exists():
        # Fallback: parquet de unidades Ultra (BLK-DIM-18)
        if not UNIDADES_ULTRA_PERF_PATH.exists():
            return pd.DataFrame()
        df_fb = pd.read_parquet(UNIDADES_ULTRA_PERF_PATH)
        alunos = pd.to_numeric(df_fb.get("alunos_total"), errors="coerce")
        metragem = pd.to_numeric(df_fb.get("metragem"), errors="coerce")
        df_fb["alunos_por_m2"] = (alunos / metragem).where(
            (alunos > 0) & (metragem > 0)
        )
        return df_fb
```

Pontos críticos para o Builder:
- Usar `alunos_total`, NÃO `alunos_reais` (este campo não existe no parquet de fallback).
- Usar variável local `df_fb` (não `df`) para não colidir com o `df` do bloco principal.
- O `return pd.DataFrame()` final do bloco `if not BASE_CALIBRACAO_PATH.exists()` passa a ser o segundo `return` (quando NENHUM dos dois parquets existe).
- O bloco do caminho principal (linhas 355–359 atuais) permanece 100% intocado.
- O decorator `@st.cache_data(show_spinner=False)` permanece intocado.

### Passo 3 — Atualizar o docstring da função (streamlit_app.py, linhas 346–352)

Adicionar ao docstring existente uma linha documentando o fallback:

```
    Fallback: se o multirede nao existir, usa `unidades_ultra_performance_hex.parquet`
    derivando `alunos_por_m2 = alunos_total / metragem` com o mesmo guard.
```

### Passo 4 — Adicionar teste de fallback em tests/integration/test_streamlit_app.py

Inserir o teste **imediatamente após** `test_load_base_calibracao_deriva_alunos_por_m2` (linha 5350), antes de `test_render_viabilidade_ponto_smoke_sem_pino` (linha 5353):

```python
def test_load_base_calibracao_fallback_unidades_ultra(tmp_path, monkeypatch):
    """Fallback usa unidades_ultra_performance_hex.parquet quando multirede ausente (BLK-DIM-18).

    - BASE_CALIBRACAO_PATH aponta para path inexistente.
    - UNIDADES_ULTRA_PERF_PATH aponta para parquet sintetico com alunos_total/metragem.
    - Retorno deve ter alunos_por_m2 sem NaN nos registros validos.
    - Registro invalido (alunos_total=0) deve ter alunos_por_m2 NaN.
    - O teste existente test_load_base_calibracao_deriva_alunos_por_m2 nao e alterado.
    """
    # Fixture sintetica — nunca usa o parquet real
    fallback_path = tmp_path / "unidades_ultra_performance_hex.parquet"
    pd.DataFrame([
        {"alunos_total": 3000, "metragem": 1500},        # valido -> 2.0
        {"alunos_total": 0,    "metragem": 1000},         # invalido -> NaN
        {"alunos_total": 2000, "metragem": 0},            # invalido -> NaN
        {"alunos_total": 2500, "metragem": 1000},         # valido -> 2.5
    ]).to_parquet(fallback_path, index=False)

    # Multirede inexistente
    monkeypatch.setattr(streamlit_app, "BASE_CALIBRACAO_PATH", tmp_path / "nao_existe.parquet")
    # Fallback aponta para a fixture sintetica
    monkeypatch.setattr(streamlit_app, "UNIDADES_ULTRA_PERF_PATH", fallback_path)
    streamlit_app.load_base_calibracao.clear()

    out = streamlit_app.load_base_calibracao()

    assert not out.empty, "Fallback deve retornar DataFrame nao-vazio"
    assert "alunos_por_m2" in out.columns
    assert out.loc[0, "alunos_por_m2"] == pytest.approx(2.0)
    assert pd.isna(out.loc[1, "alunos_por_m2"])
    assert pd.isna(out.loc[2, "alunos_por_m2"])
    assert out.loc[3, "alunos_por_m2"] == pytest.approx(2.5)

    streamlit_app.load_base_calibracao.clear()


def test_load_base_calibracao_fallback_ambos_ausentes(tmp_path, monkeypatch):
    """Quando nenhum dos dois parquets existe, retorna DataFrame vazio (sem excecao)."""
    monkeypatch.setattr(streamlit_app, "BASE_CALIBRACAO_PATH", tmp_path / "nao_existe.parquet")
    monkeypatch.setattr(streamlit_app, "UNIDADES_ULTRA_PERF_PATH", tmp_path / "nao_existe2.parquet")
    streamlit_app.load_base_calibracao.clear()

    out = streamlit_app.load_base_calibracao()
    assert isinstance(out, pd.DataFrame)
    assert out.empty

    streamlit_app.load_base_calibracao.clear()
```

Nota: o import `import pytest` e `import pandas as pd` já existem no arquivo de teste — não adicionar duplicatas.

### Passo 5 — Verificar se mypy cobre streamlit_app.py

`streamlit_app.py` está na raiz do projeto, fora de `src/`. O comando canônico `mypy src/` NÃO o cobre. O Builder deve executar adicionalmente:

```bash
mypy streamlit_app.py --ignore-missing-imports
```

(O `--ignore-missing-imports` é necessário porque `streamlit` não tem stubs completos e o restante do CI já o usa implicitamente via config.)

---

## Arquivos afetados
- `C:\Users\Felipe Silva\Downloads\motor-de-expansao\motor-de-expansao\streamlit_app.py` (apenas `load_base_calibracao` + nova constante `UNIDADES_ULTRA_PERF_PATH`)
- `C:\Users\Felipe Silva\Downloads\motor-de-expansao\motor-de-expansao\tests\integration\test_streamlit_app.py` (2 novos testes adicionados após linha 5350)

## Dependências
- `unidades_ultra_performance_hex.parquet` existe em `data/staging/` em produção (confirmado por inspeção do BO).
- O engine `faixa_alunos_por_densidade` (viabilidade_ponto.py) só exige `alunos_por_m2` — nenhuma alteração lá.

## Riscos técnicos
- **Risco principal (copy-paste cego):** o Builder pode copiar `df.get("alunos_reais")` do bloco principal. `alunos_reais` NÃO existe no parquet de fallback → `alunos_por_m2` ficaria todo NaN → faixa continuaria "n/d". O plano usa `alunos_total` explicitamente (Passo 2).
- **Cache Streamlit:** `@st.cache_data` pode entregar o DataFrame vazio antigo após deploy; operador deve restartar o Streamlit ou aguardar expiração natural do cache. Nenhuma ação de código necessária.
- **mypy fora de `src/`:** `mypy src/` não valida `streamlit_app.py`; o Passo 5 registra o comando adicional obrigatório.
- **`pytest -q` em CI:** o CI roda com `.[dev]`; `streamlit_app.py` importa `load_base_calibracao` via `import streamlit_app`, que está na raiz — confirmar que a suite de integração já adiciona a raiz ao `PYTHONPATH` (o teste existente `test_load_base_calibracao_deriva_alunos_por_m2` já funciona dessa forma, portanto o ambiente já está configurado).

## Critérios de aceite finais
1. `load_base_calibracao()` com apenas `unidades_ultra_performance_hex.parquet` presente retorna DataFrame não-vazio com `alunos_por_m2` calculada (sem NaN nos registros onde `alunos_total > 0` e `metragem > 0`).
2. `load_base_calibracao()` com nenhum dos dois parquets presente retorna `pd.DataFrame()` vazio sem exceção.
3. O teste existente `test_load_base_calibracao_deriva_alunos_por_m2` passa sem modificação.
4. 2 novos testes passam: `test_load_base_calibracao_fallback_unidades_ultra` e `test_load_base_calibracao_fallback_ambos_ausentes`.
5. Dashboard exibe faixa p10/p50/p90 quando apenas o parquet de unidades está presente.
6. Nenhuma coluna M1 (`score_priorizacao`, `hex_score_estrutural`, carteira, plano, artefatos oficiais) foi alterada.

## Validações obrigatórias
- `python -m pytest -q tests/integration/test_streamlit_app.py` → 0 falhas, 0 erros de coleta
- `python -m pytest -q` (suite completa) → 0 falhas
- `ruff check streamlit_app.py tests/integration/test_streamlit_app.py` → 0 erros
- `mypy src/` → limpo (sem regressão)
- `mypy streamlit_app.py --ignore-missing-imports` → 0 erros no código novo
- `python -c "import streamlit_app"` → sem exceção (smoke import)

## Fora de escopo
- Regenerar `base_calibracao_multirede.parquet` (pipeline `base_multirede.py` — outro ciclo).
- Alterar `viabilidade_ponto.py`, `simulador.py` ou qualquer engine de dimensionamento.
- Alterar qualquer campo M1 (`score_priorizacao`, `hex_score_estrutural`, carteira, plano, artefatos oficiais).
- Expandir para outros arquivos além de `streamlit_app.py` e `tests/integration/test_streamlit_app.py`.
- Integrar WellHub/TotalPass ou outros parquets de concorrentes ao fallback.

## Alerta de aprovação
ATENÇÃO: esta tarefa toca `streamlit_app.py` de produção (criticidade Alta). Aprovação humana obrigatória antes do Builder. O usuário deve ler este handoff e confirmar antes de acionar o Builder.

## Guardrails ativos
- §5 (READ-ONLY M1): visualizações, análise radial e interações de mapa não podem recalcular ou alterar `score_priorizacao`, `hex_score_estrutural`, carteira, plano curto prazo, plano de domínio ou artefatos oficiais do M1 sem aprovação explícita. Este fallback é READ-ONLY.
- DEC-009: a demanda entra como premissa explícita (input do operador), NUNCA prevista pela geografia. O fallback não muda essa semântica.
- DEC-001: pesos `renda=0.40`/`pop=0.60` e fórmula do score INALTERADOS.
