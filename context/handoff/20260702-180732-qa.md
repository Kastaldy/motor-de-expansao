# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador: resumo → completed.md via Passo 6.2 [helper no-op, bloco ad-hoc] + commit por path; atualizar "Depende de" do BLK-TP-09 no backlog citando este FU1 concluído). NÃO rodar `housekeeping_move_block` (ad-hoc puro, sem heading no backlog).

## VEREDITO
APROVADO

> **Leitura correta do veredito:** APROVADO significa que o BLOCO fez a medição corretamente e ENTREGOU um veredito honesto e reproduzível. O **Candidato A é NO-GO (NAO_APLICAR)** — ele NÃO deve ser aplicado ao BLK-TP-09. NO-GO é resultado VÁLIDO (DEC-008). O bloco é aprovado por medir e recomendar corretamente, não por produzir um GO.

## Justificativa
Reproduzi o NO-GO por conta própria contra os parquets reais e os números batem EXATAMENTE com o Builder (baseline R²_oof +0,3119; Candidato A +0,2692; Δ pareado completo −0,0427 IC95[−0,0477,−0,0379]; fora −0,0193 IC95[−0,0232,−0,0157]; metropolitano −0,0613; vence=False nos 3 recortes). O NO-GO vem do out-of-fold honesto (mesmo harness/seed de `calibracao_residual.py`, k-fold 5×5, IC95 bootstrap Δ PAREADO, R² in-sample nem computado no fluxo). Suíte completa 1290 passed / 1 skipped / 0 falhas; READ-ONLY sobre o M1 confirmado (mtimes intactos, nenhum parquet de produção tocado).

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- O handoff do Builder afirma "mtime dos 4 oficiais M1 inalterado", mas `hexagonos_brasil_dashboard.parquet` **não existe neste checkout local** (só `hexagonos_brasil_dashboard_base.parquet`). Isso é ausência de dado no ambiente, NÃO regressão do ciclo — os 3 oficiais presentes + `hexagonos_mercado_mapeado.parquet` têm mtime inalterado e a garantia READ-ONLY é robusta (nada foi escrito/regenerado). Sugestão: o Builder afirmar "os oficiais M1 PRESENTES + hexagonos_mercado_mapeado" para não implicar existência do artefato ausente.

## Testes faltantes
- nenhum bloqueador. Os 18 testes cobrem os 4 casos do dedup fino (rede mapeada não soma / independente sempre soma / rede não mapeada soma / independente soma com concorrente de outra rede no hex), residual A cai/[0,100], baseline == parquet, Δ pareado determinístico e cand==base⇒Δ≈0 (mesmos índices), R² in-sample banido, isolamento AST, anti-PII, NO-GO válido, CAP_REF=2500.

## Riscos remanescentes
- nenhum sobre produção (READ-ONLY; zero escrita em artefato M1/mercado). O único efeito de disco é o relatório gitignored `data/analysis/revalidacao_residual_candidatos.md` (reescrito pela minha reprodução do `__main__`; confirmado por `git check-ignore`). BLK-TP-09 NÃO deve aplicar o Candidato A; a reabertura do Candidato C (capacidade de clube de `data/validacao/`) fica para gate futuro.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte COMPLETA (gate único) — `python -m pytest -n auto -q`
```
1290 passed, 1 skipped, 359150 warnings in 231.29s (0:03:51)
```
0 falhas / 0 erros de coleta. (Python 3.14 local; warnings de deprecação/CRS pré-existentes, não-defeitos.) Baseline ~1264 + 18 novos + demais ≈ consistente. `tests/unit/test_housekeeping_helper.py` incluído e verde (28 passed no subset dedicado abaixo).

### 2. Smoke — `python -c "import streamlit_app; print('import ok')"`
```
import ok
```

### 3. Isolamento (AST) — módulo novo NÃO importa m1/dashboard/censo/api/config/pipelines pesados
```
BAD IMPORTS: NONE -> isolated OK
```

### 4. Reprodução INDEPENDENTE do NO-GO — `PYTHONPATH=src python -m motor_expansao.demanda_revelada.revalidacao_residual_candidatos`
```
RevalidacaoResidual FU1: baseline_r2=0.3119 cand_A_r2=0.2692 delta_completo=-0.0427 ic=(-0.0477,-0.0379) vence_completo=False vence_fora=False veredito=NAO_APLICAR
  Baseline: R2_oof_log=0.3119 IC95=[0.2977, 0.3250] | rho_oof=0.4615 | n=16411
  Candidato A: R2_oof_log=0.2692 IC95=[0.2553, 0.2819] | rho_oof=0.4327
  Delta pareado (A-baseline) COMPLETO = -0.0427 IC95=[-0.0477, -0.0379] -> vence=False
  Delta pareado (A-baseline) FORA (nao-metro) = -0.0193 IC95=[-0.0232, -0.0157] -> vence=False
  Dedup fino: +1748710 alunos somados / -157840 dedupados no join.
```
BATE EXATO o Builder e o .md (metropolitano −0,0613 confirmado no relatório). Determinístico (seed=42).

### 5. mtime M1 + mercado — INALTERADO (antes/depois da reprodução)
```
1780501621 data/staging/brasil_estrutural.parquet
1780501631 data/staging/brasil_priorizados.parquet
1780501655 data/staging/hexagonos_brasil_oportunidades.parquet
1781205056 data/staging/hexagonos_mercado_mapeado.parquet
```
(idêntico antes e depois de rodar o `__main__`.) `hexagonos_brasil_dashboard.parquet` ausente neste checkout (ambiente). `git status --porcelain -- 'data/staging/*.parquet'` = vazio (nenhum parquet de produção modificado).

### 6. git status / gitignore / diff-stat
```
 M context/handoff.md
 M tasks/current_task.md
?? context/handoff/20260702-170214-planner.md
?? context/handoff/20260702-172640-builder.md
?? src/motor_expansao/demanda_revelada/revalidacao_residual_candidatos.py
?? tests/unit/demanda_revelada/test_revalidacao_residual_candidatos.py
--- check-ignore ---
data/analysis/revalidacao_residual_candidatos.md   (IGNORADO)
```
(As entradas `D data/raw/ibge/malha_*.geojson` e `?? scripts/backtest_smartfit_scores.py` pré-existem ao ciclo — estavam no git status inicial da sessão; fora do escopo deste bloco.) `git diff --stat` toca só bookkeeping (handoff/current_task) + os 2 geojson pré-existentes; NENHUM arquivo de produção do plano além dos 2 novos (módulo + teste).

### 7. Ruff — `ruff check` nos 2 arquivos novos
```
All checks passed!
```

### 8. Mypy — `mypy` nos 2 arquivos novos
```
Success: no issues found in 2 source files
```

### 9. Housekeeping (ad-hoc) — `python scripts/housekeeping_move_block.py BLK-TP-06-FU1 --check`
```
FALHA --check: stub ausente no backlog para BLK-TP-06-FU1
grep "### BLK-TP-06-FU1" tasks/backlog.md -> NO heading -> ad-hoc puro
```
Esperado: bloco AD-HOC (não veio do backlog). `--check` reporta "stub ausente" = comportamento correto para ad-hoc; o move é no-op. Fechamento vai a completed.md via Passo 6.2. NÃO rodar move.

### 10. Subset dedicado (housekeeping helper + 18 novos)
```
28 passed, 2982 warnings in 30.62s
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico. A regra de no-bypass mira TOOLING contra config de produção (SOPS); aqui a validação de mérito foi rodada contra os PARQUETS REAIS de `data/staging/` (`demanda_revelada_h3`, `hexagonos_mercado_mapeado`, `oferta_academias_menores_rede_h3`, `concorrentes_mapeados`) — não fixture — e reproduziu o NO-GO byte-a-byte. As fixtures sintéticas dos 18 testes unitários são prática legítima (isolam a lógica de dedup/harness), NÃO bypass. A suíte completa rodou 1× aqui (gate único), com `-n auto`, 0 falhas.

## Conferência cruzada com log do Builder
BATE. Baseline +0,3119 / rho +0,4615 / n=16.411; Candidato A +0,2692 / rho +0,4327; Δ pareado completo −0,0427 IC95[−0,0477,−0,0379]; fora −0,0193 IC95[−0,0232,−0,0157]; metropolitano −0,0613; dedup +1.748.710 somados / −157.840 dedupados (8,3%). Ruff/mypy/isolamento/mtime/git status/import: todos batem. Divergência de contagem de suíte (Builder rodou subset 278; QA rodou full 1290) é esperada (o QA é o gate único da full).

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; nunca tocado; DEC-001 intacta — renda=0.40/pop=0.60)
- Artefatos M1 preservados: sim (mtime dos 3 oficiais presentes + `hexagonos_mercado_mapeado.parquet` inalterado; `hexagonos_brasil_dashboard.parquet` ausente no ambiente, não regressão; nenhum parquet de mercado regenerado; `calcular_colunas_mercado.py` intocado — não está no diff)
- DEC-008 (NO-GO honesto): out-of-fold vs baseline da média; R² in-sample BANIDO (nem computado no fluxo do veredito FU1); k-fold 5×5 seed=42; IC95 bootstrap; Δ PAREADO (mesmos folds nos 2 vetores oof, não ICs independentes); NO-GO é resultado válido — sim
- Baseline == score de produção: sim (`residual_baseline = score_oportunidade_residual` do parquet; teste `test_baseline_igual_score_do_parquet`; baseline reproduz o +0,3119 do TP-06)
- Dedup fino correto: sim (soma independentes sempre + rede conhecida NÃO já mapeada; deduplica só o par (hex,rede) já em `concorrentes_mapeados`; 4 casos cobertos por teste sintético)
- DEC-009 (anti-PII/demanda): `membros` usado SÓ como alvo `y=log1p(membros)` (linhas 260-261), nunca preditor; fixtures sintéticas; sem PII no .md (`test_anti_pii_relatorio`); `data/validacao/` e `NAO_ABRA/` NÃO lidos (só citados no texto ADIADO)
- DEC-012 (isolamento): pacote `demanda_revelada/` disjunto; AST limpo — sim
- Relatório honesto: o .md declara NAO_APLICAR explícito, "NÃO recomendar ao BLK-TP-09", seção "6. Candidato C — ADIADO" (fonte futura `data/validacao/`) + confounds; sem vender GO ilusório — sim
- Escopo respeitado: sim (só os 2 arquivos do plano + relatório gitignored + bookkeeping)

## Decisão recomendada
Fechar ciclo. Bloco APROVADO (mediu corretamente e entregou NO-GO honesto e reproduzível). Fechamento manual: resumo → completed.md via Passo 6.2 (helper no-op, ad-hoc); commit por path; atualizar "Depende de" do BLK-TP-09 no backlog citando este FU1 (Candidato A = NAO_APLICAR; Candidato C adiado para fonte `data/validacao/`). NÃO criar bloco de correção.
