# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-CENSO-03 — Relatório censitário: refino visual do mapa (base CLARA Voyager + camada só-concorrentes + overlay de ruas escuras + tema claro)

## O que foi feito
Executados os Blocos A–E do plano do Planner (18 passos), com mudanças mínimas e READ-ONLY sobre o M1.

- **Bloco A (censo_map.py — tema claro):**
  - `_BASEMAP_PROVIDER_ATTR`: `"DarkMatter"` → `"Voyager"` (base CLARA estilo GeoFusion).
  - `_BASEMAP_CONTRAST`: `1.35` → `1.15`.
  - `_CHOROPLETH_ALPHA`: `95` → `140`.
  - Overlay de ruas INVERTIDO: removidas `_STREET_FLOOR=52`/`_STREET_GAIN=2.6`/`_STREET_CAP=230`; adicionadas `_STREET_CEIL=160`/`_STREET_GAIN=2.2`/`_STREET_CAP=210`. A máscara passou de `lum > floor` (pixels claros do Dark Matter) para `lum < ceil` (pixels ESCUROS do Voyager). NUNCA edge-detection.
  - `_DARK_MAP_INK`: `(226,232,240)` → `(31,41,55)` (tinta escura p/ escala/labels sobre fundo claro).
  - `_CIRCLE_RGBA`: mantido `(255,176,59,235)` (laranja, decisão do gate); comentário atualizado.
  - Fallback offline: fill do canvas e do crop `(34,38,49)` → `(245,245,245)`; interior de polígono `(34,38,49,120)` → `(200,200,200,80)`.
  - Footer: "CartoDB Dark Matter" → "CartoDB Voyager. (c) OpenStreetMap, (c) CARTO."
  - Comentários de cabeçalho/scale bar atualizados para tema claro.

- **Bloco B (camada Concorrentes só-pins):**
  - Adicionado parâmetro `pins_only: bool = False` a `_render_camada`. Quando `True`: pula o loop de choropleth E o overlay de ruas de pixel; mantém basemap + círculo laranja + ponto central + pins + escala + footer + legenda só-pins. Também guarda a mensagem "Sem setores intersectados" para não aparecer em pins-pura.
  - Chamada da camada concorrentes em `render_mapas_censitarios_combinados` passou `pins_only=True`, `titulo="Relatorio Pontual Censitario - Concorrentes e Ultra"`, `legenda_titulo="Pins: Ultra e concorrentes"`, `legenda_entries=[]`.

- **Bloco C (censo_report.py):**
  - `MAP_LAYER_TITLES[2]`: `("concorrentes", "Score censitario de contexto")` → `("concorrentes", "Concorrentes e Ultra")`.
  - Título hardcoded em `gerar_pdf_relatorio_pontual_censitario` (`_map_page` da camada concorrentes): `"Score censitario de contexto"` → `"Concorrentes e Ultra"`.
  - Docstring de 7 páginas atualizada. `_ATRIBUICAO_TILES` inalterado (genérico, válido p/ Voyager).

- **Bloco D (docs):**
  - `docs/relatorio_pontual_censitario.md` §6/§7/§8: provedor Voyager COM labels, overlay de pixels escuros, camada Concorrentes e Ultra só-pins, canvas claro no fallback, página 4 do PDF renomeada.
  - `CLAUDE.md` §4 (parágrafo Motor/UI censitario): Dark Matter→Voyager COM labels, pixels CLAROS→ESCUROS, `_STREET_FLOOR`/`lum>`→`_STREET_CEIL`/`lum<`, canvas escuro→canvas CLARO, alpha 95→140, contraste 1.35→1.15, BLK-CENSO-03-FU3→BLK-CENSO-03, FIND_EDGES (FU1/FU2) descartado; camada Concorrentes e Ultra = SÓ-pins; lista de 7 páginas atualizada.
  - `CLAUDE.md` DEC-004: provedor "Voyager No-Labels"(orig)→Dark Matter(FU3)→Voyager COM labels(BLK-CENSO-03), contraste 1.15.

- **Bloco E (testes):**
  - `test_relatorio_pontual_censitario_export.py`: `_count_layer_titles` agora usa `b"Concorrentes e Ultra"`; adicionado `test_pdf_footer_cita_voyager`.
  - `test_relatorio_pontual_censitario_mapa.py`: comentário do pin vermelho atualizado; adicionados 5 testes — `test_basemap_provider_attr_e_voyager`, `test_overlay_usa_pixels_escuros_nao_claros`, `test_dark_map_ink_e_escuro`, `test_camada_concorrentes_pins_pura_sem_choropleth`, `test_fallback_offline_canvas_claro`.

- **.gitignore:** adicionado `data/reports/scratch/` (PNGs de validação visual nunca versionados).

## Valores finais dos knobs (fixados após validação visual real)
- `_STREET_CEIL = 160`
- `_STREET_GAIN = 2.2`
- `_STREET_CAP = 210`
- `_CHOROPLETH_ALPHA = 140`
- `_BASEMAP_CONTRAST = 1.15`
- `_DARK_MAP_INK = (31, 41, 55)`
- `_CIRCLE_RGBA = (255, 176, 59, 235)` (inalterado)
- Pontos de partida do Planner NÃO precisaram de retune: produziram resultado aprovado na primeira passada.

## Validação visual REAL (Passo 12 — OBRIGATÓRIA) — APROVADA
- Ambiente COM internet + contextily 1.7.0. Script: `data/reports/scratch/blk_censo_03_visual.py` (gitignored).
- Ponto RJ `-22.87650,-43.34582`, `basemap=True`, 1000x760. PNGs gerados (~455–464 KB cada = tiles reais, não fallback):
  - `data/reports/scratch/blk_censo_03_rj_densidade.png`
  - `data/reports/scratch/blk_censo_03_rj_renda.png`
  - `data/reports/scratch/blk_censo_03_rj_concorrentes.png`
- Inspeção visual (via Read das imagens, incl. crops com zoom NEAREST):
  - (a) Fundo CLARO Voyager visível: **SIM**.
  - (b) Ruas/nomes ESCUROS nítidos por cima do choropleth: **SIM** (claramente legíveis no zoom sobre a faixa salmão/vermelha).
  - (c) Camada concorrentes = SÓ PINS (sem choropleth de cor): **SIM** (fundo uniforme, zero tint; pins Ultra/concorrente + pin central).
  - (d) Círculo LARANJA presente: **SIM**.
  - (e) Barra de escala em tinta ESCURA: **SIM** (`_DARK_MAP_INK` escuro, lum<100 verificado por teste).
- Veredito: **APROVADO** — sem reprovação, sem necessidade de retune.

## Validações executadas (saída literal)
Subconjunto (Builder; suíte full é do QA):
```
python -m pytest -n auto -q tests/unit/test_relatorio_pontual_censitario_mapa.py tests/unit/test_relatorio_pontual_censitario_export.py tests/integration/test_streamlit_app.py
-> 198 passed, 18434 warnings in 22.98s

python -m ruff check src/motor_expansao/dashboard/censo_map.py src/motor_expansao/dashboard/censo_report.py
-> All checks passed!

python -m mypy src/motor_expansao/dashboard/censo_map.py src/motor_expansao/dashboard/censo_report.py
-> Success: no issues found in 2 source files

python -c "import streamlit_app; print('import ok')"
-> import ok
```
Por quê esse subconjunto: cobre os dois módulos tocados (censo_map, censo_report) e o smoke de integração obrigatório (test_streamlit_app). Os warnings são DeprecationWarning de `pytest_asyncio` no Python 3.14 do ambiente (não relacionados ao escopo).

- pytest: **198 passed, 0 failed, 0 skipped** (no subconjunto)
- import: **ok**
- ruff: **limpo**; mypy: **limpo**

## Arquivos alterados
- `src/motor_expansao/dashboard/censo_map.py` — provedor Voyager, contraste/alpha, overlay de pixels escuros (`_STREET_CEIL`), tinta escura, fallback claro, `pins_only` em `_render_camada` + chamada da camada concorrentes só-pins, footer Voyager.
- `src/motor_expansao/dashboard/censo_report.py` — `MAP_LAYER_TITLES` + título da página de mapa concorrentes = "Concorrentes e Ultra"; docstring 7 páginas.
- `tests/unit/test_relatorio_pontual_censitario_mapa.py` — comentário atualizado + 5 testes novos (provider, overlay escuro, ink escuro, pins-pura sem choropleth, fallback claro).
- `tests/unit/test_relatorio_pontual_censitario_export.py` — `_count_layer_titles` + `test_pdf_footer_cita_voyager`.
- `docs/relatorio_pontual_censitario.md` — §6/§7/§8 (Voyager COM labels, ruas escuras, camada pins-pura, canvas claro, página 4).
- `CLAUDE.md` — §4 (parágrafo Motor/UI censitario) + DEC-004 (provedor).
- `.gitignore` — `data/reports/scratch/`.

## Problemas encontrados
- `_score_legend_entries()` (censo_map.py) ficou sem uso após a camada concorrentes virar pins-pura. Mantido no módulo (função de nível de módulo, não sinalizada pelo ruff; remoção seria escopo extra). `score_band_to_color` segue importado/usado por `_color_for_score`. Sinalizado ao QA como dead code menor opcional para limpeza futura.

## Pendências
- Nenhuma de código. Deploy (rebuild de imagem Docker + redeploy por digest na VPS) exige aprovação explícita de Felipe (§6 / DEC-004 FU1) — NÃO executado.

## Riscos remanescentes
- Conflito visual verde do Voyager × rampa de renda/score (risco já documentado pelo gate): MANTIDAS as rampas atuais por decisão do gate. Na validação visual real (renda com bandas laranja+verde) as faixas ficaram legíveis; se o QA reprovar, vira follow-up sem trocar paleta neste ciclo.
- Validação visual feita com SP/RJ sintéticos sobre tiles reais do RJ — representa o caminho de render fielmente.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (READ-ONLY; nenhuma escrita em score/pesos/carteira/plano/artefatos M1).
- Artefatos M1 preservados: **sim** (nenhum parquet/pipeline tocado).
- Dashboard offline mantido: **sim** (tiles só na geração; fallback offline = canvas CLARO, coberto por teste; import contextily lazy).
- `setor_censitario_intersecao_area_1p5km` e raio 1.5 km: **INTOCADOS**.
- Sem nova dependência: **sim** (contextily já no extra `[basemap]`).
- Sem edge-detection (FIND_EDGES): **confirmado** (overlay usa pixels nativos do tile; teste garante `_STREET_FLOOR` removido).

## Resumo de 1 linha
Trocada a base do Relatório Censitário de Dark Matter para Voyager CLARA com ruas escuras recolocadas por cima do choropleth e camada Concorrentes-só-pins; validação visual real APROVADA; subconjunto 198 passed / ruff+mypy limpos / import ok.
