# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
**BLK-MA-21 — Cron SEMANAL dos agregadores (ciclo v2)**, na branch `ciclo/BLK-MA-21-v2`.
Os **15 passos** do handoff do Planner (`context/handoff/20260826-105726-planner.md`), executados na
ordem, sobre as **4 decisões aprovadas por Felipe em 2026-08-26** ("aprovar"): D1 (cron terça sem
guarda de dia do mês), D4 (frescor `7` -> `3` + régua `max` -> `min` com descarte de data futura),
D5 (`RETENCAO_SEMANAS` `78` -> `26`) e a decisão NOVA do healthcheck (`45` -> `9` dias, check na
quinta). Nenhuma decisão foi reaberta.

## O que foi feito

### Por decisão

- **D1 — grade.** Crontab do cabeçalho do wrapper e do runbook passa a `0 2 * * 2` sem
  `[ "$(date +\%d)" -le 07 ]`; cai o `%` escapado. O parágrafo "dia fixo do mês cairia em domingo ~1
  vez a cada 7 meses" foi substituído pelas folgas medidas (39h depois do runner de domingo, ~102h
  antes do próximo) e pelo argumento da retentativa dentro da mesma semana ISO.
- **D4 — frescor.** `MAX_IDADE_DIAS_DEFAULT` `7.0` -> **`3.0`**; `MAX_IDADE_DIAS="${MAX_IDADE_DIAS:-3}"`
  no wrapper. `_data_coleta_mais_nova` -> **`_data_coleta_mais_antiga(arquivos, hoje)`**: agrega por
  **mínimo** e **descarta** `valor > hoje`. `idade_do_feed` repassa `agora.date()`. Rótulo
  `REGUA_DATA_COLETA` passa de `"data_coleta"` para **`"data_coleta_min"`** (procedência não pode
  ficar ambígua entre versões da imagem). Direção de falha declarada: **fail-closed**.
- **D5 — retenção.** `RETENCAO_SEMANAS = 78` -> **`26`**. O bloco de comentário inteiro
  (`contrato.py`) foi REESCRITO: cai a tabela mensal (5,98/12,2/17,9), o divisor 4,345, a frase do
  "relógio que marca a hora errada" e o "~310 MB"; entra a aritmética uniforme, o **piso medido 13**
  (com a tabela `semanas_sem_mudanca`/`v4`), o `26 = 2× o piso`, os **160,0 MB** e o custo de
  **leitura** (~70,5 MB de pico de RSS por semana retida).
- **NOVA — healthcheck.** `MONITOR_AGREGADOR_MAX_DIAS` `45` -> **`9`**; check `0 12 * * 1` ->
  **`0 12 * * 4`** (runbook, em dois lugares). A justificativa foi escrita como o **sintetizador
  arbitrou**: a faixa que separa é `[3, 9]` INTEIRA e `9` é o **teto** dela — a afirmação de
  unicidade é falsa e não foi para a DEC.

### Os pontos de atenção do plano, todos tratados

1. **Build-breaker.** `tests/unit/test_wrappers_cron_agregadores.py:224` (`...:-45}"`) foi para
   `:-9` no mesmo commit lógico. Sem isso a suíte cairia no passo 4.
2. **As DUAS inversões de sentido** (risco operacional #1) corrigidas juntas:
   `docs/infra_producao.md` (linha do checklist `DRY_RUN`) e `run_snapshot_agregadores.sh:94-95`
   agora mandam exigir `26` (e dizem que `78` denuncia imagem antiga). **Trava nova de teste**:
   `test_wrapper_e_runbook_citam_a_mesma_retencao` lê `contrato.RETENCAO_SEMANAS` e exige que o
   wrapper e o runbook citem o MESMO número — e que nenhum dos dois volte a chamar `26` de "valor
   antigo".
3. **A maturação do epic** foi corrigida em todos os 7 arquivos de código: `churn_staleness.py`
   (o "~8 MESES" da L28, que é a origem de toda a linha do tempo), `score.py:29`, `snapshots.py`
   (5 pontos), `presenca_agregador.py:75` e **`alvos_ma.py:583` — a STRING DE AUDITORIA do
   artefato** ("primeiro mes" -> "primeira semana"), que é lida por consumidor.
4. **O `7` inerte.** `test_feed_no_limite_ainda_e_fresco` foi **revisto e MANTIDO como está**: ele
   trava a semântica da borda inclusiva com limiar **injetado** (`1.0`/`0.5`), não o default, então
   continua correto. A semântica que estava travada errado era outra —
   `test_feed_velho_nao_e_publicado`, que envelhecia **30 dias** (defeito da cadência mensal) e
   passava `max_idade_dias=7` explícito: com o 7 explícito ele ficaria verde pelo motivo errado
   (`7 <= 7`). Passou a envelhecer **7 dias** e a usar o **default**.
5. **BLK-MA-21-FU2.** **Não existe no backlog desta branch** (ficou na branch do PR #263, fechado).
   Não havia o que remover. Registro aqui que o defeito que ele descrevia — a régua não travava
   `data_coleta` no futuro — foi **absorvido pela troca `max` -> `min` com descarte**, e está
   travado por `test_data_coleta_no_futuro_nao_zera_a_idade` + `test_feed_inteiro_no_futuro_cai_no_fallback_de_mtime`.

### Extra que o plano pedia e não estava numerado

- **Log ANTES do lock** (`run_snapshot_agregadores.sh`): o `mkdir`/`LOG`/`exec > >(tee -a "$LOG")`
  subiu para antes do `flock -n 9`. Sem isso, uma rodada TRAVADA segura o lock e toda semana
  seguinte sai `exit 0` **sem deixar arquivo em `/var/log/motor-snapshots/`**. Travado por
  `test_wrapper_agregadores_loga_antes_do_flock`.

## Arquivos alterados

**Código (7)**
- `src/motor_expansao/vulnerabilidade/contrato.py` — `RETENCAO_SEMANAS` `78`->`26`; bloco de
  comentário da constante reescrito (aritmética uniforme + piso 13 + custo de RAM); comentário do
  `COLUNAS_PARTICAO` deixa de falar em "execução mensal".
- `src/motor_expansao/vulnerabilidade/curadoria_agregadores.py` — `MAX_IDADE_DIAS_DEFAULT`
  `7.0`->`3.0` com o porquê medido; `_data_coleta_mais_nova`->`_data_coleta_mais_antiga` (min +
  descarte de data futura, recebe `hoje`); `idade_do_feed` reescrito (régua = linha mais VELHA;
  fail-closed declarado); `REGUA_DATA_COLETA` -> `"data_coleta_min"`; 6 docstrings de "mensal".
- `src/motor_expansao/vulnerabilidade/snapshots.py` — 5 passagens de cadência + docstring de
  `podar_snapshots` (razão da poda por fonte reescrita: semana de calendário × semana observada,
  com a medição das 20 semanas/50% de falha).
- `src/motor_expansao/vulnerabilidade/churn_staleness.py` — L10/L28/L163 (a linha do tempo do epic).
- `src/motor_expansao/vulnerabilidade/score.py` — L29 (`~8-12 meses` -> `~8-12 SEMANAS`).
- `src/motor_expansao/vulnerabilidade/alvos_ma.py` — L583, **string de auditoria do artefato**.
- `src/motor_expansao/vulnerabilidade/presenca_agregador.py` — L75.

**Shell (3)**
- `scripts/cron/run_snapshot_agregadores.sh` — título SEMANAL; crontab sem guarda; **inversão de
  sentido da retenção corrigida**; `MAX_IDADE_DIAS:-3`; **log antes do flock**; `retencao_semanas=26`
  no echo do DRY-RUN; rótulo `data_coleta_min` no checklist; "45 dias depois" -> "9 dias depois";
  as duas strings de log que o operador lê em `/var/log` (MENSAL -> SEMANAL); prosa de "mes".
- `scripts/cron/run_snapshot_concorrentes.sh` — cabeçalho de cadência corrigido (o cron dos
  agregadores **passou a existir**) + **registro escrito** de que os dois wrappers nunca se excluem
  e de que quem evita o estrago é o particionamento de 2 chaves, com o `flock` remetido ao
  `BLK-MA-21-FU3`. Preservados os asserts de teste (`NAO** PASSA POR AQUI`, `snapshots_concorrentes_v4`).
- `scripts/healthcheck_vps.sh` — limiar `45`->`9` com a justificativa correta; `0 12 * * 1` sai do
  runbook; **a MENSAGEM DE FAIL que o operador lê** ("O cron mensal foi agendado?" -> "O cron
  semanal dos agregadores foi agendado?"); L12/L31/L197.

**Testes (5)**
- `tests/unit/test_wrappers_cron_agregadores.py` — build-breaker do `:-45` corrigido; teste
  renomeado (`..._a_cadencia_dos_agregadores_por_ele`); 6 docstrings; **5 testes NOVOS**:
  `test_crontab_do_cabecalho_e_semanal_sem_guarda_de_dia_do_mes`,
  `test_wrapper_agregadores_loga_antes_do_flock`, `test_wrapper_e_runbook_citam_a_mesma_retencao`,
  `test_healthcheck_limiar_separa_rodada_da_semana_de_uma_rodada_perdida`,
  `test_healthcheck_dos_agregadores_e_agendado_na_quinta`.
- `tests/unit/vulnerabilidade/test_snapshots.py` — **T9 REESCRITO** (`test_retencao_serve_as_duas_cadencias`
  -> `test_retencao_satura_o_v4_na_cadencia_semanal`), removendo o
  `assert 26 / semanas_por_mes < c.MIN_SEMANAS`, que era uma **rejeição hard-coded do valor
  aprovado**; helper `_serie_hash_constante`; **1 teste NOVO**
  (`test_retencao_tolera_metade_das_semanas_perdidas`); 4 docstrings.
- `tests/unit/vulnerabilidade/test_churn_staleness.py` — `assert c.RETENCAO_SEMANAS == 26`
  (+ `>= 13`) com a razão nova; docstring L307.
- `tests/unit/vulnerabilidade/test_curadoria_agregadores.py` — `test_feed_velho_nao_e_publicado`
  corrigido (7 dias + default); `test_data_coleta_lida_com_parser_csv...` retunado de `3.0` para
  `5.0` (a régua passou a ser o MÍNIMO); `test_regua_usada_vai_ao_relatorio` trava o rótulo novo;
  **4 testes NOVOS**: `test_feed_de_7_dias_e_recusado_e_o_de_1_dia_publica`,
  `test_idade_sai_da_linha_mais_velha_do_feed`, `test_data_coleta_no_futuro_nao_zera_a_idade`,
  `test_feed_inteiro_no_futuro_cai_no_fallback_de_mtime`; 3 docstrings.
- `tests/unit/vulnerabilidade/test_alvos_ma.py` — docstring L606-607.

**Documentos (4)**
- `docs/decisions/DEC-039.md` — título (MENSAL/`78` -> SEMANAL/`26`), tabela das nove decisões
  (D1/D4/D5/D9), seções D1, D4, D5 (inteira) e D9, consequências 1/6/7 + nova 8, e uma **Emenda 2
  completa** (~90 linhas) com o que muda item a item, o porquê medido, as duas inversões de sentido,
  a correção da justificativa falsa do `9`, riscos aceitos e o que fica fora.
- `docs/infra_producao.md` — **a inversão de sentido do checklist**, título da seção, crontab,
  healthcheck (dia + limiar, nos DOIS lugares), guarda de frescor (default 3 + régua `min`), seção
  de retenção inteira, o `.bak` (`~200 KB/mês` -> **14,33 MiB por rotação, ~745 MiB/ano**), e ~20
  passagens de cadência.
- `docs/vulnerabilidade_ma_contrato.md` — §6 (retenção 26 + piso 13), a tabela do S1, os dois
  relógios do §12, a maturação (`~8 MESES` -> `~8 SEMANAS`, "cerca de um ano" -> "~3 meses"), D8 e
  a nota "qual cron liga qual relógio" (~18 pontos).
- `tasks/backlog.md` — bullet D5 com o RESULTADO; a afirmação **FALSA** dos "~7 dias por construção"
  marcada e corrigida (numa coleta boa são 0-1 dia; os ~7 são a idade quando a coleta FALHA); seção
  de retenção reescrita com os números certos (os três de disco estavam errados por ~3×); escopo
  item 4 e critério de aceite 3; **item 7 NOVO** (healthcheck), que não estava em escopo nenhum;
  **2 blocos de follow-up NOVOS** (ver Pendências); e 5 afirmações vizinhas (BLK-MA-06/BLK-MA-20)
  que passaram a mentir.

## Validações executadas

- **pytest (SERIAL, sem `-n auto`)** — `PYTHONPATH=$PWD/src python -m pytest <subconjunto> -q`:
  **1072 passed, 5 skipped, 0 failed** (70,8 s). Subconjunto e por quê:
  - `tests/unit/test_wrappers_cron_agregadores.py` (36) — contrato textual dos 3 shells alterados;
  - `tests/unit/vulnerabilidade/` (595 no diretório inteiro) — os 7 módulos de código tocados;
  - `tests/unit/test_garimpeiro_select_block.py`, `test_housekeeping_helper.py`,
    `test_loop_guard.py`, `test_loop_guard_paths.py` — únicos fora da camada que leem
    `tasks/backlog.md` / caminhos de guard (achados por `grep -rln` em `tests/`);
  - `tests/contracts/` — parâmetros canônicos, para provar que o M1 não se moveu.
  - **Baseline ANTES de qualquer edição** (mesmos 6 arquivos do passo 15 do plano): **325 passed**.
- **Quebrou de propósito e foi ajustado:** o build-breaker do `:-45` e os 2 testes novos que
  dependem do runbook falharam na ordem prevista (antes do passo 10) e passaram depois. Nenhuma
  falha inesperada.
- **`python -m ruff check .`** — `All checks passed!`.
- **`ruff format`** — aplicado só nos arquivos tocados. **Revertido em
  `src/.../presenca_agregador.py` e `src/.../score.py`**: o formatador reflowava ~14 linhas que eu
  não escrevi (o CI roda só `ruff check`, não `format --check`), e isso seria refatoração fora de
  escopo. Mesma reversão em 3 hunks de `test_churn_staleness.py`.
- **`bash -n`** nos 3 scripts: OK. **CRLF = 0** em TODOS os 19 arquivos alterados (medido com
  Python; `grep -c $'\r'` dentro de laço dá falso positivo nesta estação).
- **`python -c "import motor_expansao.vulnerabilidade; print('ok')"`** -> `ok`. **Substituição
  declarada:** `prompts/builder.md:51` manda `import streamlit_app`, módulo **REMOVIDO pela
  DEC-022**; o prompt está stale.
- **`python scripts/loop_guard.py --base origin/main --json`** -> **nenhum CRÍTICO**; só classe
  `governanca` (`tasks/backlog.md` + o pacote `vulnerabilidade/`, esperado para este bloco).
- **Smokes `--dry-run`, com a árvore de staging provada intacta por SHA-256:**
  - hash da árvore `data/staging/snapshots_concorrentes/` ANTES e DEPOIS:
    `08da079f9a34195eaa39e1e354ca8fab79b33fb80f50c4779b15202fec145b73` (2 itens) — **idêntico**.
  - `curadoria_agregadores --dry-run` sobre clone sintético: `regua_idade=data_coleta_min`,
    `limite 3.0`, TotalPass 86 d e WellHub 21 d **recusados** (`fontes_publicadas=` vazio) — a régua
    nova funciona ponta a ponta.
  - `snapshots --fontes wellhub --dry-run`: `retencao_semanas: 26`,
    `versao_contrato: snapshots_concorrentes_v4`, 22.173 linhas.
  - `snapshots --migrar-layout --dry-run`: `{'migrar_layout': ['2026-33'], 'ambiguas': []}`.
  - **Nada foi regerado** em `data/staging/snapshots_concorrentes/semana=2026-33/`.
- **Varredura final obrigatória** (`grep -rn` por `mensal|MENSAL|mês|meses|78` nos 19 arquivos do
  bloco): as 18 ocorrências do plano eram o piso. **Achei e corrigi 8 a mais:**
  `test_snapshots.py:549` ("a execucao mensal dos"), `:1433` ("meses depois"), `:1487` ("o MENSAL
  grava"), e 5 no `tasks/backlog.md` fora do bloco (BLK-MA-06 L1583/L1589/L1625-1626 e BLK-MA-20
  L1808/L1832-1834) que afirmavam "o cron mensal não existe" / "26 partições = 26 meses". O que
  sobrou de "mensal" é **deliberado**: explica a premissa morta e está marcado como tal.

## Problemas encontrados

1. **`test_snapshots.py` tem mojibake PRÉ-EXISTENTE** (`sÃ©rie`, `VISÃVEL`, `nÃ£o` — UTF-8 duplo,
   confirmado em `git show HEAD:`). Eu **não** o introduzi e **não** o corrigi (seria churn fora de
   escopo); apenas casei os bytes existentes ao editar. Fica como observação para o QA.
2. **A branch está 1 commit atrás de `origin/main`** (`d5d3464`, "feat(web): tema claro…" #261). Não
   rebasei — não estava no escopo e mudaria a base do PR. **O QA precisa saber**: enquanto a base
   estiver desatualizada, `loop_guard --base origin/main` lista arquivos de `web/` e `CLAUDE.md` que
   este ciclo **não tocou** (são o diff reverso da main). Contra o merge-base real, só `governanca`.
3. **O clone local `../GymScraping` está no estado AMBÍGUO de D3** (`csvs_musculacao/` de 07/08 mais
   antigo que `csvs/` de 18/08), então a curadoria em modo seco **levanta** contra ele. Isso é o
   comportamento correto e já está documentado no runbook como ação humana pendente; o smoke rodou
   sobre um clone sintético que resolve a ambiguidade.

## Pendências

- **Follow-ups REGISTRADOS no backlog** (fora de escopo por decisão do sintetizador, com a
  justificativa medida escrita em cada um):
  - **`BLK-MA-21-FU3`** — `flock` no `run_snapshot_concorrentes.sh`. Média. O `grep` prova que a
    única ocorrência de `flock` ali é um comentário sobre o *outro* script, e `podar_snapshots` faz
    `shutil.rmtree` e é chamada pelas DUAS cadências: duas podas concorrentes não têm defesa no
    código. Ficou fora porque mexe num wrapper **já aplicado na VPS**.
  - **`BLK-MA-21-FU4`** — poda POR FONTE. Alta/prioridade **Baixa**. A justificativa antiga (duas
    cadências) morreu; a que sobrevive é a assimetria semana de calendário × semana observada,
    com a medição (20 semanas, 50% de falha, poda N=13 -> `wellhub` com 7 < `MIN_SEMANAS = 8`).
- **NÃO commitei** (é passo do orquestrador). **Não toquei** `graphify-out/*` nem `PRD.md` — os
  `graphify-out/*` já estavam modificados no início da sessão.
- **`context/handoff/20260826-105726-planner.md`** segue **untracked**.
- **Camada semântica do grafo desatualizada**: mexi em `docs/`, `tasks/` e nas DECs. Alguém precisa
  rodar `/graphify . --update` numa sessão Claude (§7 do CLAUDE.md) — não é o `python -m`.
- **Aplicação na VPS continua manual, comando a comando (§6).** Nenhum comando remoto foi executado
  por mim, por nenhuma via.

## Riscos remanescentes

- **A régua `min` é fail-closed:** uma linha com `data_coleta` corrompida no passado passa a recusar
  o feed inteiro e o wrapper sai com `exit 3`. É a direção segura e foi decidida assim. **Custo hoje
  = zero, remedido por mim em 2026-08-26**: spread de `data_coleta` é **0** nos três diretórios do
  clone (`TotalPass/csvs` 2026-06-01; `Wellhub/csvs` e `csvs_musculacao` 2026-08-05).
- **`26` é 2× o piso, não 6×.** Se a taxa real de falha semanal por fonte na VPS passar de ~50%, as
  13 observações não chegam. Não é medível daqui; o detector é o próprio check por fonte.
- **A duração do WellHub segue não desempatada** (20h03 × 31h08). O `3` foi escolhido justamente por
  sobreviver às duas.
- **Baixar a retenção é destrutivo e irreversível na VPS.** Hoje é inócuo (Felipe: BLK-MA-06
  aplicado há 1-2 dias, **nenhuma partição acumulada**), e a janela fecha com o tempo. O `--dry-run`
  **não** mostra o que a poda apagaria.
- **A imagem em produção não é a `v4`** (Felipe). A ordem de aplicação do runbook (imagem primeiro)
  continua sendo a defesa contra o modo destrutivo.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (nenhum arquivo de `pipelines/m1/`, `config.py`,
  `normalizar_concorrentes.py` ou `calcular_colunas_mercado.py` foi tocado; `tests/contracts/`
  verde).
- Artefatos M1 preservados: **sim** — camada READ-ONLY; a árvore de `data/staging/snapshots_concorrentes/`
  foi provada byte a byte idêntica por SHA-256 antes e depois dos smokes.
- Dashboard offline mantido: **sim** (não aplicável — nada em `web/`, `dashboard/` ou `api`).
- `../GymScraping` READ-ONLY: **sim** (só leitura, para conferir `data_coleta` e o layout dos feeds).
- Acentuação §2: prosa de operador acentuada; **nenhum identificador acentuado**. Os 3 shells
  seguem a convenção ASCII do próprio arquivo (log em `/var/log`). O único valor bruto alterado é
  `REGUA_DATA_COLETA = "data_coleta_min"` — ASCII, e é um rótulo de payload, não um label de tela.
- EOL (DEC-017): **LF em 100% dos 19 arquivos alterados**, `docs/` e `tasks/` inclusive (todas as
  escritas usaram `newline="\n"`; verificado no fim, CR total = 0).
