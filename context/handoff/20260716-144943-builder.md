# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-REV-08 — Spike técnico: mapa client-side deck.gl `H3HexagonLayer` no cap real (18–35k hexes) + harness Playwright + runbook de medição. Gate [REVISÃO HUMANA — visual/perf] APROVADO por Vinicius em 2026-07-16 (5 pontos confirmados como propostos).

## O que foi feito
Implementados os 7 passos do plano fechado (D1–D6), READ-ONLY sobre o M1, isolado e opt-in, sem dep nova.

1. **`src/motor_expansao/dashboard/ui_spike_deckgl.py`** (NOVO, SPIKE DESCARTÁVEL, docstring marcando "não importar em produção"). Funções: `gerar_recorte_spike_json(uf, enriquecido_dir=None, *, limit=MAP_POINT_LIMIT, mode_stress=False)`, `_build_deckgl_html(data, height=560)`, `render_mapa_deckgl`, `is_spike_enabled` (env `ULTRA_SPIKE_DECKGL` + `session_state["show_ui_spike_deckgl"]`), `render_spike_page`. Análogo estrutural a `ui_proto.py`.
2. **Payload/cap/bandas (D3):** fonte = partição enriquecida por UF (`data/outputs/hexagonos_dashboard_enriquecido/uf=<UF>/parte-0.parquet`, o mesmo derivado que a produção lazy-carrega). Cap espelhando `components.py:1552`: `MAP_POINT_LIMIT_LARGE (18000)` se `len(df) > MAP_POINT_LIMIT (35000)`, senão `limit` (default 35000); ordena por `score_priorizacao` desc estável antes do `.head`; `mode_stress=True` desliga o cap. Chaves curtas `h/bm1/br/s/sr/p/o`, SEM geometria, SEM lat/lng por hex, SEM PII. `view` (bbox) pré-computada uma vez em Python. Bandas 0–9 vetorizadas sobre `RESIDUAL_SCORE_BANDS` via `searchsorted`. JSON determinístico (`sort_keys`, separators compactos, `ensure_ascii=False`); `version="ui_spike_deckgl_v1"`. Cache isolado `data/cache/ui_spike_deckgl/<UF>.json` (gitignored, NÃO reusa `ui_proto`).
3. **HTML deck.gl:** bundle UMD `deck.gl@latest` por CDN; `H3HexagonLayer` com `getHexagon: d=>d.h` + `getFillColor` por banda + `pickable`/`onClick`→painel de detalhe; basemap raster CARTO dark via `TileLayer`+`BitmapLayer`; toggle **M1↔Residual** (recolor via `updateTriggers.getFillColor`, sem re-serializar) e **cenário múltiplo** (add/remove client-side + sumário local). Marcadores `window.__spikeFirstPaintMs` (deck `onAfterRender` do 1º frame com dados), `window.__spikeRecolorMs`, `window.__spikeAddHexMs` (todos `performance.now()`). Hooks programáticos `window.__spikeToggleColor()` / `__spikeAddHex(hexId?)` / `__spikeClearScenario()` (robustez do harness, também acionados pelos botões).
4. **`scripts/rev08_spike_playwright.py`** (NOVO): CLI `--url` (default `http://localhost:8501`), `--uf` (SP), `--flow {render,recolor,scenario,pdf,all}` (all), `--runs` (5), `--out`, `--target {spike,production}` (spike), `--i-confirm-production`, `--headed`, `--timeout-ms`. 4 fluxos de dor (render/recolor/scenario lêem os `window.__spike*` no iframe do componente; `pdf` é EXCLUSIVO de `--target production`, no spike é PULADO com aviso). Agrega mediana/p95/min/max. **Guarda anti-produção:** `check_production_guard` aborta (exit 2) se `--url` contém `ultra-expansao.tech` sem `--i-confirm-production`. Import de `playwright` é LAZY (`--help` funciona sem o pacote).
5. **`docs/rev08_spike_perf_runbook.md`** (NOVO): 3 sub-entregáveis (i DevTools contra produção; ii Playwright contra produção; iii A/B via Caddy VPS), tabelas EM BRANCO para o humano preencher, cada comando de VPS com aviso `⚠️ §6 — exige confirmação humana individual por comando; não encadear`. Texto ao usuário com acentuação correta.
6. **`tests/unit/test_ui_spike_deckgl.py`** (NOVO): os 7 casos do D6 (schema; `h` não-vazio e `bm1`/`br` 0..9; cap respeitado + `mode_stress` não corta + cap LARGE por patch de limiar; ZERO PII/geometria/lat-lng; determinismo hexagons+view; `_build_deckgl_html` com CDN/deck.gl/`H3HexagonLayer`/`updateTriggers`/dados e sem PII; `is_spike_enabled` env+session) + smoke de import e da guarda anti-produção do harness (função pura, sem browser/rede).

**Correção pontual (dentro do meu módulo):** `_REPO_ROOT` usa `parents[3]` (repo root correto no dev E na produção `/app`), não `parents[4]`. O `ui_proto.py` usa `parents[4]` — bug latente lá que só não quebra porque seus testes sempre passam `enriquecido_dir`; sem a correção, `gerar_recorte_spike_json("SP")` resolvia a raiz um nível acima e não achava os dados. Não toquei `ui_proto.py`.

## Arquivos alterados
- **Criado:** `src/motor_expansao/dashboard/ui_spike_deckgl.py` — módulo do spike (recorte/HTML/render/opt-in/página).
- **Criado:** `scripts/rev08_spike_playwright.py` — harness de medição parametrizável, guarda anti-produção.
- **Criado:** `docs/rev08_spike_perf_runbook.md` — runbook de medição humana (3 sub-entregáveis, tabelas em branco, avisos §6).
- **Criado:** `tests/unit/test_ui_spike_deckgl.py` — 25 testes determinísticos, sem browser/rede.
- **Runtime (gitignored, não versionado):** `data/cache/ui_spike_deckgl/SP.json` gerado na validação (sob `data/cache/`).
- **Bookkeeping:** `tasks/current_task.md`, `context/handoff.md`, `context/handoff/20260716-144943-builder.md`.

## Validações executadas (saída real)
- **pytest spike (serial):** `python -m pytest -q tests/unit/test_ui_spike_deckgl.py` → **25 passed** (0 failed, 0 skipped).
- **pytest subconjunto impactado (serial):** `tests/unit/test_ui_spike_deckgl.py tests/unit/test_ui_proto.py tests/integration/test_streamlit_app.py` → **316 passed, 0 failed**. (Nota: `-n auto`/xdist bateu num INTERNALERROR de `execnet` — flakiness de gateway no Python 3.14 local, NÃO falha de teste, como CLAUDE.md §5 registra p/ o ambiente; a execução SERIAL é o resultado limpo.)
- **ruff:** `python -m ruff check` nos 3 .py tocados → **All checks passed!**
- **mypy:** `python -m mypy src/motor_expansao/dashboard/ui_spike_deckgl.py` → **Success: no issues found in 1 source file**.
- **import spike:** `python -c "import motor_expansao.dashboard.ui_spike_deckgl"` → ok.
- **import app:** `import streamlit_app` (módulo top-level da raiz; o `motor_expansao.dashboard.streamlit_app` do handoff estava impreciso) → **import ok**.
- **grep pages.py:** `grep -L "ui_spike_deckgl" src/motor_expansao/dashboard/pages.py` → imprime o caminho = **NÃO importa o spike**. Confirmado.
- **gerar_recorte_spike_json("SP") real:** `len(hexagons)=18000` (SP 47.389 > 35k → cap LARGE 18k, exatamente o cenário do plano); `view={longitude:-48.60185, latitude:-22.36087, zoom:4.55}`; chaves por hex = `['bm1','br','h','o','p','s','sr']`; **violações PII/geo/lat-lng = [] (zero)**; `bm1` range 7–9, `br` range 0–9; cache SP.json escrito.
- **harness `--help`:** lista `--url/--uf/--flow/--target/--i-confirm-production/--headed/--timeout-ms`. **Guarda anti-produção:** `--url https://dashboard.ultra-expansao.tech` sem `--i-confirm-production` → mensagem "ABORTADO…" + **exit 2**.
- **READ-ONLY M1:** mtime inalterado dos 4 oficiais — `brasil_estrutural.parquet` e `brasil_priorizados.parquet` (2026-06-10 09:50), `hexagonos_brasil_oportunidades.parquet` (2026-06-10 09:50), `data/outputs/hexagonos_brasil_dashboard.parquet` (2026-06-10 10:24). Nenhum recomputado.
- **git status:** untracked = apenas os 4 arquivos novos + 2 snapshots de handoff (BO/Planner, pré-existentes) + este handoff. Zero modificação em `pages.py`/`config.py`/`pipelines/m1/`/`pyproject.toml`/CI/Dockerfiles.

## Problemas encontrados
- `_REPO_ROOT` copiado de `ui_proto.py` (`parents[4]`) resolvia a raiz um nível acima → corrigido para `parents[3]` (correto no dev e prod). Ver "O que foi feito". Sem impacto fora do módulo.
- Baseline de contagem: ambiente local roda Python 3.14; `-n auto` é instável (execnet). Rodei serial. O gate único da suíte FULL é do QA.

## Pendências
- **QA:** rodar a suíte FULL como gate único (`pytest -q`, serial recomendado neste ambiente por causa do xdist/execnet no Python 3.14) e confirmar sem regressão vs baseline.
- **Passo HUMANO pós-Builder (não é do QA):** a MEDIÇÃO empírica (FPS, clique→paint, frames WS, A/B VPS) segue o `docs/rev08_spike_perf_runbook.md` — números reais não foram inventados; tabelas ficam em branco. `python -m playwright install chromium` é passo de ambiente do humano.

## Riscos remanescentes
- **CDN em runtime:** o HTML deck.gl busca `deck.gl@latest` + tiles CARTO por CDN — só quando o spike é aberto no browser (opt-in). Os testes NÃO abrem browser/CDN (cobrem só as funções Python/string) → sem flakiness de rede na suíte. O spike NÃO é caminho de produção (não entra em `pages.py`).
- **Payload inline grande:** 18k × 7 chaves ≈ payload de MB embutido no HTML — é justamente o custo que o spike quer expor (documentado no runbook para medição humana), não otimizado prematuramente.
- **Seletores do harness:** os seletores Playwright (`Gerar recorte`, `Gerar relatório`) são aproximados; o runbook nota que o humano ajusta ao layout real. O harness não roda na esteira.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (READ-ONLY; só leitura do enriquecido derivado; `score_priorizacao` LIDO, nunca escrito).
- Artefatos M1 preservados: **sim** (mtime dos 4 oficiais inalterado; nada em `pipelines/m1/`/`config.py`).
- Dashboard offline mantido: **sim** (o spike é opt-in e isolado; a carga/interatividade do dashboard de produção não muda; `pages.py` não importa o spike). Rede só no browser do spike sob opt-in — não é dependência de runtime do dashboard de produção.
- §6 (VPS): **respeitado** — nenhum comando executado contra VPS/produção; entregues apenas script + runbook, com guarda anti-produção e avisos §6 por comando.
- Sem dep nova: **sim** (deck.gl por CDN; `playwright` já em `[scraping]`; `pyproject.toml` intocado).
