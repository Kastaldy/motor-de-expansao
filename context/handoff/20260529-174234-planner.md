# Handoff — Planner

## Snapshot / revisão
Snapshot append-only. Substitui (não apaga) o snapshot `20260529-173052-planner.md` após revisão humana do plano BLK-OPS-02. Incorpora 3 ajustes pedidos pelo humano:
1. PR de ciclo obrigatório (nova Fase E): Builder abre PR `ciclo/BLK-OPS-02` → `main` via `gh pr create` (PR only, SEM merge); merge segue passo humano no fechamento. Justificativa: `ci.yml` só dispara em `push: main`/`push: codex-dashboard-m1-streamlit`/`pull_request: main` — não dispara em push da branch do ciclo, e o `workflow_dispatch` da Fase B ainda não existe no momento da verificação; o PR faz o `pull_request: main` disparar o CI para o QA verificar os checks.
2. Negação do `.gitignore` para fixtures: INCLUIR neste bloco (passo 1 mantido como estava — confirmado inofensivo e destrava o BLK-OPS-02a).
3. ruff/mypy com escopo de correção LIMITADO: só o trivial (`ruff check . --fix` + ajustes pontuais); refatoração maior → bloco separado BLK-OPS-02b; supressão apenas pontual e documentada (`# noqa`/`# type: ignore` com motivo), nunca em massa.

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
APROVAÇÃO HUMANA (criticidade Alta) → depois Builder (commita por path → abre PR de ciclo, SEM merge) → QA (verifica checks do PR) → veredito → merge humano no fechamento

## Esteira / fluxo do ciclo
Builder commita por path na branch `ciclo/BLK-OPS-02` → **Builder abre PR `ciclo/BLK-OPS-02` → `main` via `gh pr create` (apenas abre; NÃO faz merge)** → o `pull_request: main` do `ci.yml` dispara o CI no PR → QA verifica os **checks do PR** (CI completo verde + Docker Publish verde) na aba Actions/GitHub → veredito do QA → **merge é passo HUMANO no fechamento** (não automático).

## Entendimento da tarefa
Fazer o CI rodar a suíte pytest completa e ficar verde no runner limpo (via skip-guard documentado nos ~6 testes acoplados a dados reais), adicionar ruff/mypy ao gate, e criar um workflow que builda a imagem Streamlit e a publica no GHCR com tag por commit (SHA), além de um runbook `docs/deploy.md` em modo `pull` + `up -d` — sem tocar em scoring, artefatos M1 nem no VPS.

## Plano técnico

> Ordem por dependência. Cada passo é reversível (git revert do arquivo). NÃO rodar deploy no VPS. NÃO rodar pytest aqui no Planner; os comandos abaixo são para o Builder/QA.

### Fase A — Destravar a suíte completa (skip-guard)

1. **`.gitignore` (raiz)** — `*.parquet`/`*.csv` são ignorados globalmente (linhas 26-27). Para qualquer fixture sintética futura ser commitável é preciso negação explícita. NESTE bloco (skip-guard, sem fixtures de dados) NÃO é estritamente necessário, mas adicionar a negação já deixa BLK-OPS-02a destravado. **Decisão: adicionar neste bloco** o bloco de negação logo após a linha `*.csv`:
   ```
   # Fixtures de teste versionadas (sintéticas, sem dado real) — exceção ao ignore de parquet/csv
   !tests/fixtures/
   !tests/fixtures/**
   ```
   Validar com `git check-ignore tests/fixtures/sample.parquet` → deve retornar vazio (exit 1) após a mudança. Reversível: remover as 3 linhas.

2. **`tests/integration/test_normalizar_concorrentes.py`** — hoje `assert PARQUET.exists()` é hard-fail (linha 24) dentro do fixture `df`. Trocar por skip-guard no padrão de `test_expansao_dominio.py`:
   - Adicionar, após `PARQUET = ...` (linha 11):
     ```python
     pytestmark = pytest.mark.skipif(
         not PARQUET.exists(),
         reason="data/staging/concorrentes_mapeados.parquet ausente (dados reais)",
     )
     ```
   - No fixture `df` (linha 22-25): remover o `assert PARQUET.exists()` (o skipif já cobre); manter `return pd.read_parquet(PARQUET)`.
   - NÃO alterar nenhum `assert` de schema/contagem (linhas 28-72). Reversível.

3. **`tests/integration/test_modelo_mercado_hexagonos.py`** — é misto: tem testes puramente sintéticos que DEVEM continuar rodando e testes acoplados a dados reais. NÃO usar `pytestmark` global (mataria os sintéticos). Aplicar skip granular:
   - Os testes unitários sintéticos `test_calcular_bloqueia_sam_quando_ha_canibalizacao`, `test_calcular_tam_sam_residual_absoluto_exemplo_manual`, `test_anexar_oferta_ultra_real_soma_alunos_por_hex`, `test_anexar_colunas_censo_preserva_camada_censitaria_do_hibrido`, `test_bloco8_pop_hex_base_usa_censo_independente_de_flag_censo_elegivel` (linhas 151-320) NÃO dependem de arquivo → **deixar como estão** (devem PASSAR).
   - Definir constantes de presença no topo (após as `*_PATH`, linha 24):
     ```python
     CONCORRENTES_CSVS_OK = all(p.exists() for p in CSV_SOURCES)
     ```
   - `test_csvs_concorrentes_legiveis` (linha 129): decorar com
     `@pytest.mark.skipif(not csv_path.exists(), ...)` — porém é parametrizado por `csv_path`; mais simples trocar o `assert csv_path.exists()` (linha 131) por `if not csv_path.exists(): pytest.skip("CSV concorrente real ausente")`. Reversível.
   - `test_ultra_loader_lida_com_metadado_e_encoding_legacy` (linha 137): trocar `assert ULTRA_RAW_PATH.exists()` (linha 138) por `if not ULTRA_RAW_PATH.exists(): pytest.skip("data/ultra/Ultra.csv ausente")`.
   - Fixtures `mercado_guardrails_df`, `mercado_score_df`, `hibrido_score_df` (linhas 111-126): trocar `assert ...exists()` por `pytest.skip(...)` dentro do fixture (skip propaga para os testes que os consomem: `test_parquet_final_tem_schema_minimo`, `test_parquet_final_respeita_guardrails_do_piloto`, `test_piloto_preserva_score_oficial_do_mercado_e_cobertura_hibrida`).
   - `test_artefatos_oficiais_m1_mantem_invariantes` (parametrizado, linha 370): trocar `assert artifact_path.exists()` (linha 375) por `if not artifact_path.exists(): pytest.skip("artefato M1 real ausente")`.
   - **GUARDRAIL M1:** NÃO tocar em nenhum `assert` de `score_oficial == score_priorizacao`, `score_oficial_nome`, `osm_status`, `hex_score_estrutural` (linhas 78-108, 327-384). Só converter `assert exists()` → `pytest.skip`.

4. **`tests/integration/test_calcular_penetracao_ultra_hex.py`** — fixture `df` chama `modulo.gerar_base()` (linha 37) que lê dados reais; não há guarda de path. Adicionar skip-guard baseado no parquet que ele consome:
   - Após `UNIDADES_PATH = ...` (linha 14):
     ```python
     pytestmark = pytest.mark.skipif(
         not UNIDADES_PATH.exists(),
         reason="data/staging/unidades_ultra_performance.parquet ausente (dados reais)",
     )
     ```
   - O teste puramente sintético `test_divisoes_seguras_com_populacao_e_metragem_invalidas` (linha 77) NÃO usa `df`/`gerar_base` — mas o `pytestmark` de módulo o pularia também. **Decisão:** para preservar a cobertura sintética, NÃO usar `pytestmark` global aqui; em vez disso guardar só o fixture `df`:
     ```python
     @pytest.fixture(scope="module")
     def df() -> pd.DataFrame:
         if not UNIDADES_PATH.exists():
             pytest.skip("unidades_ultra_performance.parquet ausente (dados reais)")
         return modulo.gerar_base()
     ```
     Isso pula apenas os 4 testes que recebem `df` (`test_schema_minimo`, `test_total_linhas_preserva_base_de_unidades`, `test_fontes_de_populacao_e_formulas_reais`, `test_ranking_de_penetracao`, `test_validar_base_nao_falha`) e mantém o sintético passando. NÃO alterar `len == 54` (linha 48).

5. **`tests/integration/test_comparar_geofusion_vs_hex.py`** — fixture `df` = `modulo.gerar_comparacao()` (linha 37, dados reais). Há vários testes sintéticos puros: `test_calcular_comparacao_unit` (linha 97) e `test_relatorio_gerado`/`test_validar_nao_falha` recebem `df`. Aplicar o mesmo padrão de guarda-no-fixture (sem `pytestmark` global), usando o caminho real que `gerar_comparacao` precisa. Como o teste não declara explicitamente um único PATH, guardar pelo mesmo `data/staging/unidades_ultra_performance.parquet` (base de unidades que `gerar_comparacao` consome — Builder confirma a dependência lendo `jobs/pipelines/comparar_geofusion_vs_hex.py`):
   ```python
   UNIDADES_PATH = ROOT / "data" / "staging" / "unidades_ultra_performance.parquet"

   @pytest.fixture(scope="module")
   def df() -> pd.DataFrame:
       if not UNIDADES_PATH.exists():
           pytest.skip("base de unidades real ausente — comparacao GeoFusion")
       return modulo.gerar_comparacao()
   ```
   `test_calcular_comparacao_unit` continua passando. NÃO alterar `len == 54` (linha 45).

6. **`tests/integration/test_validar_penetracao_ultra_hex.py`** — todos os 6 testes chamam `modulo.gerar_analise()` (dados reais), nenhum é sintético. Aplicar `pytestmark` de módulo:
   - Após os imports/ROOT (não há PATH declarado): adicionar
     ```python
     ROOT = Path(__file__).resolve().parents[2]
     UNIDADES_PATH = ROOT / "data" / "staging" / "unidades_ultra_performance.parquet"
     pytestmark = pytest.mark.skipif(
         not UNIDADES_PATH.exists(),
         reason="base de unidades real ausente — validar penetracao",
     )
     ```
   - Builder confirma em `jobs/pipelines/validar_penetracao_ultra_hex.py` qual parquet `gerar_analise()` lê e ajusta o PATH se for outro. NÃO alterar `len == 54` (linha 14).

7. **`tests/integration/test_normalizar_unidades_ultra.py`** — fixture `df` = `modulo.gerar_base()` (linha 36) + `test_loader_ultra_lida_com_metadado_encoding_e_uf_corrompida` lê `ULTRA_RAW_PATH` (linha 83). Guardar pelo `data/ultra/Ultra.csv` que é a fonte:
   - Guardar o fixture:
     ```python
     @pytest.fixture(scope="module")
     def df() -> pd.DataFrame:
         if not ULTRA_RAW_PATH.exists():
             pytest.skip("data/ultra/Ultra.csv ausente (dado real legado)")
         return modulo.gerar_base()
     ```
   - Em `test_loader_...` (linha 84): trocar `assert ULTRA_RAW_PATH.exists()` por `if not ULTRA_RAW_PATH.exists(): pytest.skip("data/ultra/Ultra.csv ausente")`. NÃO alterar `len == 54` (linha 47) nem `len(raw) == 150`.

8. **`tests/unit/test_pop_censo_v0001.py`** — JÁ tem skip correto: `test_v0001_e_populacao_total_nao_domicilios` faz `pytest.skip` se o parquet não existir (linhas 194-195); os demais testes do arquivo são 100% sintéticos (h3/shapely em memória) e PASSAM. **NÃO precisa de alteração.** Listado aqui só para confirmar.

9. **`tests/integration/test_expansao_dominio.py`** — JÁ tem `pytestmark = pytest.mark.skipif(not MERCADO_PATH.exists())` (linhas 19-22). **NÃO precisa de alteração.** É o modelo de referência.

### Fase B — Atualizar o gate CI

10. **`.github/workflows/ci.yml`** — substituir o passo "Testes rapidos" pela suíte completa e adicionar qualidade. Manter gatilhos atuais (`push: main, codex-dashboard-m1-streamlit` + `pull_request: main`) e adicionar `workflow_dispatch` para QA manual em branch de teste. Estrutura final do job `test`:
    - `actions/checkout@v4`
    - `actions/setup-python@v5` (python 3.11) com `cache: pip` (acelera; opcional `cache-dependency-path: pyproject.toml`).
    - `pip install -e ".[dev]"` (inalterado).
    - **Novo step "Lint (ruff)":** `ruff check .` — meta: 0 erros.
    - **Novo step "Types (mypy)":** `mypy src/` — meta: 0 erros (escopo `src/` conforme critério do bloco; `strict=false` já no pyproject).
    - **Step "Testes (suíte completa)":** `python -m pytest -q` (remove a lista de 2 arquivos). Meta: ver "Meta do CI" abaixo.
    - **Step "Smoke import":** `python -c "import streamlit_app; print('ok')"` (inalterado).
    - Reversível: restaurar o arquivo original (3 steps).
    - **Atenção `conftest.py`:** ele reescreve `tmp_path` para `tmp_codex_runtime/manual_pytest/...` (relativo ao checkout). Funciona no runner; `tmp_codex_runtime/` já está no `.gitignore` (linha 54) → não gera lixo versionado. Não alterar conftest.

### Fase C — Workflow de build + push GHCR

11. **`.github/workflows/docker-publish.yml`** (NOVO) — builda e publica a imagem no GHCR por commit. Estrutura:
    - `name: Docker Publish (GHCR)`
    - Gatilho: `on: { push: { branches: [main] }, workflow_dispatch: {} }`. **Decisão:** publicar em push na `main` (deploy é manual depois) + dispatch para QA em branch de teste. Não usar tag-trigger neste bloco (mantém simples; tag por SHA já dá rastreabilidade).
    - `permissions: { contents: read, packages: write }` (usa `GITHUB_TOKEN` nativo — SEM segredo manual).
    - Job `build-push` (`runs-on: ubuntu-latest`):
      - `actions/checkout@v4`
      - `docker/login-action@v3` com `registry: ghcr.io`, `username: ${{ github.actor }}`, `password: ${{ secrets.GITHUB_TOKEN }}`.
      - `docker/metadata-action@v5` para `images: ghcr.io/${{ github.repository }}/motor-expansao-streamlit`, tags: `type=sha` (SHA por commit) + `type=raw,value=latest,enable={{is_default_branch}}`.
      - `docker/build-push-action@v6` com `context: .`, `file: Dockerfile.streamlit`, `push: true`, `tags`/`labels` do metadata, `cache-from/to: type=gha`.
    - **Imagem NÃO embute segredos:** o `.dockerignore`/Dockerfile copiam o repo; `.env`/`Caddyfile`/`authelia/*` já são gitignored e não estão no contexto de build versionado. Builder deve confirmar que `.env` não entra no `COPY . .` (recomendação: criar/checar `.dockerignore` com `.env`, `data/`, `concorrentes/`, `secrets/`, `.git/` — **sub-passo opcional**, ver Riscos). NÃO há segredo de runtime na imagem (os dados entram por volume read-only no compose).
    - Reversível: deletar o arquivo.

12. **`docker-compose.prod.yml`** — passar de `build` para `pull` SEM quebrar build local. **Decisão:** comentar (não apagar) o bloco `build:` e apontar `image:` para a tag remota com variável de override:
    - Trocar `image: motor-expansao-streamlit:latest` por
      `image: ${STREAMLIT_IMAGE:-ghcr.io/<owner>/<repo>/motor-expansao-streamlit:latest}` (Builder resolve `<owner>/<repo>` = `github.repository`).
    - Remover/comentar o bloco `build:` (linhas 18-20). Quem quiser build local usa um arquivo de override (`docker-compose.override.yml` com `build:`) OU o doc instrui `docker build -f Dockerfile.streamlit`. Como o critério de aceite exige que `docker build -f Dockerfile.streamlit -t test:ci .` continue funcionando (e ele NÃO depende do compose), remover `build:` do compose de prod é seguro.
    - Reversível: descomentar `build:` e reverter `image:`.

### Fase D — Runbook de deploy

13. **`docs/deploy.md`** (NOVO; não existe — confirmado) — runbook curto do modo `pull`. Conteúdo mínimo:
    - Pré-condições: imagem publicada no GHCR pelo workflow (tag SHA ou `latest`); `.env`, `Caddyfile`, `authelia/*` já presentes no VPS; `/opt/motor-expansao/data/outputs` com parquets.
    - Passos no VPS (documentar, NÃO executar): `docker login ghcr.io` (PAT read:packages se o pacote for privado — nota), `export STREAMLIT_IMAGE=ghcr.io/<owner>/<repo>/motor-expansao-streamlit:<sha|latest>`, `docker compose -f docker-compose.prod.yml pull`, `docker compose -f docker-compose.prod.yml up -d` (SEM `--build`), `docker compose ... ps`/`logs`.
    - Rollback: `export STREAMLIT_IMAGE=...:<sha_anterior> && pull && up -d`.
    - Reforçar guardrail §6: nenhum comando no VPS sem confirmação humana.
    - Referência cruzada: atualizar a seção "Atualizacao" de `docs/deploy_vps_streamlit.md` (linhas 85-89, hoje `git pull` + `build`) para apontar ao novo modo `pull` (ou nota "ver docs/deploy.md"). **Decisão: apenas adicionar nota de cruzamento**, não reescrever, para não inflar o diff.

### Fase E — Abertura do PR de ciclo (PR only, sem merge)

14. **Abrir PR `ciclo/BLK-OPS-02` → `main` (NÃO fazer merge).** APÓS commitar todos os paths na branch `ciclo/BLK-OPS-02` e dar `git push -u origin ciclo/BLK-OPS-02`, o Builder abre o PR via `gh` CLI (disponível). O merge continua sendo passo HUMANO no fechamento — o Builder NÃO faz merge.
    - **Por que o PR é obrigatório (justificativa registrada):** o `ci.yml` só dispara em `push: main`, `push: codex-dashboard-m1-streamlit` e `pull_request: main` — **NÃO dispara em push da branch `ciclo/BLK-OPS-02`**, e o `workflow_dispatch` adicionado na Fase B ainda não existe no `main`/no momento da verificação (só passa a existir após o merge). Logo, **sem o PR o QA não consegue verificar autonomamente a aceitação central do bloco (CI completo verde + Docker Publish verde) via os checks do PR.** Abrir o PR faz o `pull_request: main` disparar o CI imediatamente nos commits da branch do ciclo.
    - Comando:
      ```bash
      gh pr create \
        --base main \
        --head ciclo/BLK-OPS-02 \
        --title "BLK-OPS-02 — CI suíte completa + ruff/mypy + Docker Publish GHCR + runbook deploy pull" \
        --body "Fecha a aceitação central do BLK-OPS-02: suíte pytest completa verde no runner limpo via skip-guards (Fase A), gate ruff+mypy (Fase B), workflow Docker Publish (GHCR) por SHA (Fase C) e runbook docs/deploy.md modo pull (Fase D). PR aberto para o QA verificar os checks (CI + Docker Publish) na aba Actions. NÃO mergear — merge é passo humano no fechamento após veredito do QA. Não altera scoring/artefatos M1; não toca no VPS."
      ```
    - **Registrar nº/URL do PR no handoff** (este arquivo e no snapshot de Builder→QA) para o QA localizar os checks. `gh pr create` imprime a URL no stdout; capturar e colar.
    - **Atenção (Docker Publish):** o `docker-publish.yml` (Fase C) tem gatilho `push: main` + `workflow_dispatch` — ele **NÃO** dispara no `pull_request`. Para o QA ver o Docker Publish verde ANTES do merge, o Builder deve disparar via `workflow_dispatch` na branch do ciclo: `gh workflow run "docker-publish.yml" --ref ciclo/BLK-OPS-02` e registrar a URL do run. (Alternativa documentada: aceitar que o Docker Publish só rode no push pós-merge à `main` — mas para fechar a verificação ANTES do merge, o dispatch na branch é o caminho.) Registrar a URL do run de Docker Publish no handoff junto com a do PR.
    - Reversível: fechar o PR (`gh pr close`) sem merge; nada vai para `main`.

## Decisões de plano (com justificativa)

- **Estratégia testes acoplados (HÍBRIDA, skip-guard agora):** usar `pytest.mark.skipif`/`pytest.skip` no padrão JÁ validado em `test_expansao_dominio.py`. Justificativa: (1) é determinístico e deixa o CI verde num runner limpo HOJE, sem depender de gerar fixtures com schema fiel de ~40 colunas de mercado + invariantes M1; (2) zero risco de fixture conter resíduo de dado real (Ultra/Skyfit/Wellhub) — guardrail explícito; (3) preserva 100% dos asserts de schema/contagem/scoring (só converte `assert exists()` em skip), então quando os dados reais existirem localmente os testes voltam a validar tudo. Fixtures sintéticas com schema fiel ficam para o sub-bloco BLK-OPS-02a (abaixo), entregue depois e independente.
- **Meta do CI (passed/skipped esperados):** "completo com N skips justificados e contabilizados", NÃO full-green-com-fixtures. Baseline local de referência: `532 passed, 1 skipped`. No runner limpo (sem `data/`), a meta é: **suíte completa roda sem erro/fail**, com os testes acoplados a dados reais em `skipped` (estimativa: ~25-35 skips somando os módulos `test_normalizar_concorrentes`, partes de `test_modelo_mercado_hexagonos`, `test_calcular_penetracao_ultra_hex`, `test_comparar_geofusion_vs_hex`, `test_validar_penetracao_ultra_hex`, `test_normalizar_unidades_ultra`, `test_expansao_dominio` + o 1 skip pré-existente). **O número exato de skips o Builder contabiliza ao rodar `pytest -q` no PR e registra no handoff de QA; o critério de aceite é "0 failed, 0 errors; todos os não-rodados são skips justificados por path ausente".** Localmente (com dados reais) a suíte deve continuar `532 passed, 1 skipped` — garantia de que o skip-guard não mascara regressão.
- **Sub-bloco BLK-OPS-02a (RECOMENDADO, separado):** "Fixtures sintéticas versionadas para a suíte de integração acoplada a dados reais". Conteúdo: gerar Parquets/CSVs sintéticos pequenos com **schema fiel** (concorrentes_mapeados, hexagonos_mercado_mapeado com as ~40 colunas, unidades_ultra_performance com 54 linhas FICTÍCIAS, Ultra.csv sintético 150 linhas, artefatos M1 mínimos com `score_priorizacao/score_oficial/hex_score_estrutural/osm_status`), versionados sob `tests/fixtures/` (exige a negação do .gitignore do passo 1), e refatorar os testes para apontar para essas fixtures via fixture/monkeypatch de PATH quando os reais não existirem. Objetivo: trocar skips por PASSES reais no runner, validando schema+invariantes M1 sem dado real. **ALERTA M1:** as fixtures DEVEM preservar o contrato de colunas oficiais (`score_oficial == score_priorizacao`, `score_oficial_nome=="score_priorizacao"`, `osm_status=="nao_aplicado_mvp_nacional"`) e NÃO podem mascarar regressão de scoring → BLK-OPS-02a é criticidade Alta e exige aprovação humana própria. BLK-OPS-02 NÃO depende de 02a (entrega CI verde por si só).
- **Workflow build/push GHCR:** novo `.github/workflows/docker-publish.yml`, gatilho `push: main` + `workflow_dispatch`, login com `GITHUB_TOKEN` nativo, `permissions: packages: write`, tag `type=sha` (commit) + `latest` no default branch, via `docker/build-push-action@v6` com cache GHA. SEM segredo manual. Deploy efetivo permanece passo humano.
- **docker-compose.prod.yml modo pull:** remover/comentar `build:`, trocar `image:` para `${STREAMLIT_IMAGE:-ghcr.io/<owner>/<repo>/motor-expansao-streamlit:latest}`. Build local de sanity continua via `docker build -f Dockerfile.streamlit -t test:ci .` (independe do compose), então o critério de aceite de build local não quebra.
- **ruff/mypy no CI:** SIM, adicionar como dois steps no `ci.yml` (`ruff check .`, `mypy src/`). Já configurados em `pyproject.toml` e presentes no extra `[dev]`. Meta de cada um: 0 erros. **Risco:** podem já existir violações latentes no repo (nunca rodaram no CI) — ver Riscos.
- **ruff/mypy — ESCOPO DE CORREÇÃO LIMITADO (regra do humano):** corrigir APENAS o necessário para o CI ficar verde. Permitido: `ruff check . --fix` no trivial (imports não usados, formatação, ordenação) e ajustes pontuais de tipo. **PROIBIDO neste bloco:** qualquer violação que exija **refatoração maior** de código de produção — NÃO corrigir aqui. Para essas, o Builder deve **registrar um bloco separado BLK-OPS-02b — "saneamento ruff/mypy"** no handoff e no `tasks/backlog.md`, listando as violações que ficaram de fora e o motivo. Se for necessário destravar o gate sem refatorar, usar **supressão pontual e DOCUMENTADA** (`# noqa: <code>` / `# type: ignore[<code>]` SEMPRE com comentário explicando o porquê e referenciando BLK-OPS-02b) — **NUNCA supressão em massa** (sem `# noqa` solto/genérico, sem `ignore_errors` global no mypy, sem desabilitar regra no `pyproject.toml`). NÃO expandir o escopo de BLK-OPS-02 com refatoração de código de produção.
- **Bloco separado BLK-OPS-02b (condicional):** "Saneamento ruff/mypy — violações que exigem refatoração". Só é criado se o Builder encontrar violações não-triviais ao rodar `ruff check .` / `mypy src/`. Conteúdo: lista das violações suprimidas/adiadas com `# noqa`/`# type: ignore` documentado em BLK-OPS-02, e o plano de refatoração. Independente de BLK-OPS-02 (que fecha com o gate verde, mesmo com supressões pontuais documentadas).

## Arquivos afetados
- `C:\Users\Felipe Silva\Downloads\motor-de-expansao\motor-de-expansao\.gitignore` (negação para `tests/fixtures/`)
- `C:\Users\Felipe Silva\Downloads\motor-de-expansao\motor-de-expansao\.github\workflows\ci.yml` (suíte completa + ruff + mypy + cache pip + workflow_dispatch)
- `C:\Users\Felipe Silva\Downloads\motor-de-expansao\motor-de-expansao\.github\workflows\docker-publish.yml` (NOVO)
- `C:\Users\Felipe Silva\Downloads\motor-de-expansao\motor-de-expansao\docker-compose.prod.yml` (build → pull)
- `C:\Users\Felipe Silva\Downloads\motor-de-expansao\motor-de-expansao\docs\deploy.md` (NOVO runbook pull)
- `C:\Users\Felipe Silva\Downloads\motor-de-expansao\motor-de-expansao\docs\deploy_vps_streamlit.md` (nota de cruzamento — opcional, diff mínimo)
- `C:\Users\Felipe Silva\Downloads\motor-de-expansao\motor-de-expansao\tests\integration\test_normalizar_concorrentes.py` (skip-guard)
- `C:\Users\Felipe Silva\Downloads\motor-de-expansao\motor-de-expansao\tests\integration\test_modelo_mercado_hexagonos.py` (skip granular — NÃO mexer nos asserts M1)
- `C:\Users\Felipe Silva\Downloads\motor-de-expansao\motor-de-expansao\tests\integration\test_calcular_penetracao_ultra_hex.py` (guard no fixture)
- `C:\Users\Felipe Silva\Downloads\motor-de-expansao\motor-de-expansao\tests\integration\test_comparar_geofusion_vs_hex.py` (guard no fixture)
- `C:\Users\Felipe Silva\Downloads\motor-de-expansao\motor-de-expansao\tests\integration\test_validar_penetracao_ultra_hex.py` (pytestmark skip)
- `C:\Users\Felipe Silva\Downloads\motor-de-expansao\motor-de-expansao\tests\integration\test_normalizar_unidades_ultra.py` (guard no fixture)
- (NÃO alterar: `tests\unit\test_pop_censo_v0001.py` e `tests\integration\test_expansao_dominio.py` — já têm skip)
- `C:\Users\Felipe Silva\Downloads\motor-de-expansao\motor-de-expansao\tasks\backlog.md` (registrar BLK-OPS-02b se houver violações ruff/mypy que exijam refatoração — condicional)
- (Ação GitHub, não arquivo) PR `ciclo/BLK-OPS-02` → `main` aberto via `gh pr create` na Fase E (PR only, sem merge)

## Dependências
- Fase A (skip-guards) deve ser concluída ANTES de habilitar a suíte completa no `ci.yml` (Fase B), senão o gate quebra no primeiro push.
- Passo 1 (.gitignore) só é pré-requisito real de BLK-OPS-02a (fixtures), não dos skip-guards; pode entrar junto sem efeito colateral.
- Fase C (docker-publish.yml) é independente da Fase A/B mas o critério de QA pede ambos os workflows verdes no mesmo push de teste.
- Fase D (deploy.md) depende conceitualmente de Fase C (nome/tag da imagem no GHCR) mas é só documentação.
- Builder deve LER `jobs/pipelines/comparar_geofusion_vs_hex.py` e `jobs/pipelines/validar_penetracao_ultra_hex.py` para confirmar o PATH exato de dados reais que cada `gerar_*()` consome (passos 5 e 6).
- Fase E (PR de ciclo, passo 14) depende de TODAS as fases A-D commitadas e da branch `ciclo/BLK-OPS-02` pushada. O PR é o que dispara o `pull_request: main` do CI — pré-requisito da verificação externa do QA. Builder NÃO faz merge; merge é humano no fechamento.

## Riscos técnicos
- **ruff/mypy nunca rodaram no CI:** podem expor violações latentes no repo inteiro. Se houver muitas, o gate fica vermelho. Mitigação (escopo limitado, regra do humano): Builder roda `ruff check .` e `mypy src/` localmente ANTES de commitar; corrige SÓ o trivial via `ruff check . --fix` + ajustes pontuais. Violações que exijam **refatoração maior** NÃO são corrigidas aqui → registrar **BLK-OPS-02b** (handoff + backlog) e, se preciso para destravar o gate, usar `# noqa: <code>`/`# type: ignore[<code>]` pontual e DOCUMENTADO (comentário do porquê + ref. a BLK-OPS-02b). NUNCA supressão em massa nem desabilitar regra no `pyproject.toml`. NÃO expandir BLK-OPS-02 com refatoração de produção.
- **Tempo/tamanho da suíte:** geoprocessamento (geopandas/shapely/pyproj/h3) pode deixar a suíte lenta no runner; cache de pip ajuda no install, não no teste. Aceitável para um gate; medir o tempo no primeiro run.
- **`.env` no contexto de build:** `COPY . .` no Dockerfile pode arrastar arquivos locais. `.env`/`Caddyfile`/`authelia/*`/`secrets/*.dec` já são gitignored (não estão no checkout do runner), mas um `.dockerignore` explícito é defesa em profundidade. Builder deve verificar/criar `.dockerignore` (`.git`, `.env`, `data/`, `concorrentes/`, `secrets/`, `tests/`, `tmp_codex_runtime/`).
- **Pacote GHCR privado:** o `pull` no VPS exige `docker login ghcr.io` com PAT `read:packages`. Documentar em `deploy.md`; não é segredo de CI.
- **`scope="module"` nos fixtures:** o skip dentro de fixture module-scoped propaga corretamente para todos os testes consumidores (comportamento padrão do pytest) — confirmado pelo padrão de `test_expansao_dominio.py`.
- **Skip granular em `test_modelo_mercado_hexagonos.py`:** risco de pular acidentalmente um teste sintético. Mitigação: aplicar skip SÓ nos fixtures e nos 3 testes com `assert exists()`; os 5 testes sintéticos (151-320) ficam intocados.

## Critérios de aceite finais
- `pytest -q` roda a **suíte completa** no runner limpo com **0 failed / 0 errors**; testes acoplados a dados reais aparecem como **skipped** com `reason` explícito (count registrado pelo Builder/QA).
- Localmente (com `data/` real) a suíte mantém `532 passed, 1 skipped` — prova de que o skip-guard não mascara regressão.
- `ruff check .` → 0 erros; `mypy src/` → 0 erros, ambos como steps verdes no `ci.yml`.
- Workflow `docker-publish.yml` builda e publica `ghcr.io/<owner>/<repo>/motor-expansao-streamlit` com tag por SHA (run verde na aba Actions — disparado via `workflow_dispatch` na branch do ciclo para a verificação ANTES do merge).
- **PR de ciclo aberto (Fase E):** existe um PR `ciclo/BLK-OPS-02` → `main` aberto (NÃO mergeado) pelo Builder, com nº/URL registrados no handoff. **A aceitação central (CI completo verde + Docker Publish verde) é verificada pelo QA via os CHECKS DO PR**, porque o `ci.yml` só dispara em `pull_request: main` (e não em push da branch do ciclo). O merge é passo humano no fechamento.
- `docker-compose.prod.yml` referencia a imagem remota via `${STREAMLIT_IMAGE:-...ghcr...}` e NÃO contém mais `build:` ativo.
- `docs/deploy.md` documenta `pull` + `up -d` sem `--build`, com rollback por SHA e guardrail VPS.
- Nenhum arquivo de scoring, artefato M1, carteira ou plano alterado; nenhum comando executado no VPS.

## Validações obrigatórias
- Comando: `git check-ignore tests/fixtures/sample.parquet` (após passo 1) | Critério: retorno vazio / exit 1 (não mais ignorado).
- Comando: `python -m pytest -q` (runner/local limpo) | Critério: 0 failed, 0 errors; só skips justificados.
- Comando: `python -m pytest -q` (local com dados reais) | Critério: `532 passed, 1 skipped` (sem regressão).
- Comando: `ruff check .` | Critério: 0 erros.
- Comando: `mypy src/` | Critério: 0 erros.
- Comando: `docker build -f Dockerfile.streamlit -t test:ci .` | Critério: build conclui sem erro (sanity local).
- Comando: `python -c "import streamlit_app; print('ok')"` | Critério: imprime `ok`.
- **Verificação externa (QA, via CHECKS DO PR):** o QA usa o **PR `ciclo/BLK-OPS-02` → `main` aberto pelo Builder na Fase E** (nº/URL registrado no handoff). Verificar nos **checks do PR** / aba Actions: (a) check **CI** verde com a suíte completa + ruff + mypy (o `pull_request: main` dispara o CI nos commits da branch do ciclo — é por isso que o PR é obrigatório; a branch do ciclo sozinha NÃO dispara o `ci.yml`); (b) **Docker Publish (GHCR)** verde — disparado pelo Builder via `gh workflow run "docker-publish.yml" --ref ciclo/BLK-OPS-02` (URL do run registrada no handoff), com imagem publicada e tag SHA visível em Packages. Confirmar contagem de `passed/skipped` no log do step de testes. **O `workflow_dispatch` adicionado na Fase B continua no `ci.yml` (útil futuramente), mas a verificação DESTE ciclo é via os checks do PR, não via dispatch em branch.** QA verifica os checks ANTES do veredito; **merge é passo humano no fechamento** (QA NÃO mergeia).

## Fora de escopo
- Alterar lógica de scoring (`score_priorizacao`, `hex_score_estrutural`, `ajuste_executivo`) ou qualquer artefato M1/carteira/plano.
- Gerar as fixtures sintéticas com schema fiel (→ sub-bloco BLK-OPS-02a, aprovação própria).
- **Refatoração maior de código de produção para ruff/mypy** (→ bloco separado BLK-OPS-02b; aqui só correção trivial + supressão pontual documentada).
- **Fazer merge do PR de ciclo** — o Builder/QA apenas abrem/verificam o PR; o merge `ciclo/BLK-OPS-02` → `main` é passo HUMANO no fechamento.
- Executar deploy efetivo no VPS / qualquer comando SSH/MCP no servidor.
- Reescrever `docs/deploy_vps_streamlit.md` / `docs/deploy_plan.md` (só nota de cruzamento).
- Resolver outros blocos do backlog.

## Alerta de aprovação
ESTA TAREFA É CRITICIDADE ALTA. APROVAÇÃO HUMANA OBRIGATÓRIA ANTES DO BUILDER.
Nenhum passo deste plano recalcula ou altera `score_priorizacao`, `hex_score_estrutural` ou artefatos oficiais M1 — apenas converte `assert PATH.exists()` em skip-guard, preservando integralmente os asserts de contrato/scoring. SE durante a execução o Builder precisar tocar nos asserts de colunas oficiais M1 (`score_oficial`, `score_oficial_nome`, `osm_status`, `hex_score_estrutural`) ou introduzir fixtures que materializem essas colunas, ISSO EXIGE NOVA APROVAÇÃO HUMANA EXPLÍCITA E DEVE SER SINALIZADO EM MAIÚSCULAS — é o gatilho que empurra o trabalho para o sub-bloco BLK-OPS-02a.

## Guardrails ativos
- CLAUDE.md §5 (permanente): CI/visualizações não podem recalcular ou alterar score/carteira/plano/artefatos M1 sem aprovação explícita.
- CLAUDE.md §2: toda mudança relevante entra com teste; nenhum PR sobe com CI quebrado; `config.py`/`CLAUDE.md`/`PRD.md` são canônicos.
- CLAUDE.md §6: GUARDRAIL ABSOLUTO — nenhum comando no VPS via MCP/SSH sem confirmação humana por comando. Este bloco NÃO toca no servidor.
- Spec do bloco: fixtures de teste NÃO contêm dados reais de Ultra/Skyfit/Wellhub; `.env` de produção fica fora do build/imagem (BLK-OPS-01).
- Parâmetros canônicos imutáveis sem DEC: H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, pesos renda=0.40/pop=0.60.
- Staging em Parquet; CSVs locais `sep=";"`, `encoding="utf-8-sig"`; legado `data/ultra/Ultra.csv` `sep=";"`, `encoding="latin-1"`, 1 linha de metadado.
