# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
**REVISÃO HUMANA (gate)** — modelagem Alta; aprovação humana obrigatória antes do Builder.

## Entendimento da tarefa
Medir, out-of-fold (DEC-008: k-fold repetido vs baseline da média, IC95 bootstrap seed-fixa, R² in-sample banido), quanto o `score_oportunidade_residual` (camada PARALELA de mercado/residual) prevê a **demanda OBSERVADA** (`membros`, Demanda Revelada) casada por `hex_id`, quantificando honestamente o +0,52 in-sample da DEC-012 e emitindo veredito GO/NO-GO; se GO, **propor por escrito** (sem aplicar) uma recalibração dos componentes do residual — tudo READ-ONLY sobre o M1.

## Fatos confirmados na inspeção (só agregados, anti-PII)
- Join inner `demanda × mercado` por `hex_id` = **16.411 hexes** (99,0% dos 16.575 da demanda; ≈ **1,06%** dos 1.542.531 do Motor). `residual notna` nos 16.411; `residual > 0` em **7.646**.
- `membros` presente em todos os 16.575 hexes (soma 1.960.742), **cauda longa** (mediana 7, p90 243, p99 2.257) → transformação `log1p` justificada.
- **Spearman in-sample `score_oportunidade_residual × membros` = +0,517** (no join) — é exatamente o +0,52 da DEC-012; ESTE número in-sample é o que o bloco deve refazer OUT-OF-FOLD como veredito (não repeti-lo como prova).
- Concentração do join: SP 29,1%, MG 12,7%, RJ 9,2% (top-3 ≈ 51%) — enviesado para metrópoles do Sudeste (reportar valores reais no relatório; não afirmar "66% SP").
- `alunos_parceiras > 0` só em 5.341 hexes (OFERTA instalada) — denominador diferente de `membros`; usar só como covariável/cross-check.
- Precedente direto do harness: `demanda_revelada/backtest_tp05.py` (Ridge + `RepeatedKFold(5×5, seed=42)` + IC95 bootstrap dos pares `(y, y_pred_oof)` + `flag_extrapolacao` por envelope min-max + `nota_honesta`), reusando `dimensionamento/aderencia.py` (`ALPHA_GRID`, `LIMIAR_R2_GO`, `_r2_loo_para_alpha`) e `dimensionamento/backtest_dim.py` (`_r2`, `_rmse`). **Sem dep nova.**

## Plano técnico
1. **Travar alvo e preditor (semântica dos 2 tipos):**
   - **ALVO** `y = log1p(membros)` (demanda paga OBSERVADA; 16.575 hexes, cauda longa). `log1p` decidido (justificativa no §DECISÕES; guardar espelho `y_alunos = expm1(y)` só para auditoria de back-transform).
   - **PREDITOR principal** `score_oportunidade_residual`. Secundário/auditável: `oferta_efetiva_disponivel` (componente-fonte do residual; `score = clip(100*oferta/2500,0,100)`), reportado em modelo separado — NÃO somado ao principal sem justificativa.
   - `alunos_parceiras`/`n_acad_parceiras` = OFERTA instalada → entram SÓ como covariável de contexto / cross-check (nunca como alvo). Documentar no relatório a distinção `membros` (demanda) × `alunos_parceiras` (oferta) — R1.
   - Registrar o guardrail DEC-009: aqui a demanda é ALVO OBSERVADO e o `score` é o PREDITOR; medir se o score alinha com a demanda observada é PERMITIDO (precedente BLK-TP-05); PROIBIDO usar `membros`/qualquer coluna da demanda como preditor geográfico de magnitude ou ajuste de score.

2. **Módulo novo — `src/motor_expansao/demanda_revelada/calibracao_residual.py`** (funções PURAS, sem I/O de rede; pacote DISJUNTO — NÃO importa de `pipelines/m1`, `dashboard`, `censo_*`, `api`, `config.py` raiz; PODE importar de `dimensionamento/` e de `.contrato`). Assinatura:
   - `@dataclass CalibracaoResidualResult` com campos (espelhando `BacktestTP05Result`):
     `alpha_selecionado: float`, `coefs: dict[str,float]`, `intercepto: float`,
     `r2_oof_log: float` (ANCORA do gate), `r2_oof_membros: float` (auditoria expm1),
     `rmse_oof_log/rmse_oof_membros/rmse_oof_baseline_log/rmse_oof_baseline_membros: float`,
     `r2_oof_baseline: float` (=0.0), `ic95_r2_oof: tuple[float,float]`,
     `rho_oof: float`, `ic95_rho_oof: tuple[float,float]` (**Spearman out-of-fold + IC95 bootstrap** — métrica adicional exigida pelo gate),
     `r2_insample: float` (rótulo literal "apenas auditoria — NAO usar como desempenho"),
     `n_treinamento: int`, `n_join: int`, `pct_cobertura_universo: float` (~1,06),
     `n_descartado_invalidos: int`, `range_membros: tuple[float,float]`,
     `spearman_bruta: dict[str,tuple[float,float]]` (bivariada residual/oferta/covariáveis vs membros, ANTES do modelo),
     `pct_extrapolacao: float`, `flag_extrapolacao_padrao_global: bool`, `metodo_validacao: str`,
     `concentracao_uf: dict[str,float]` (top-3 UF % do join — caveat de viés),
     `veredito: str`, `nota_honesta: str` + `@property go`.
   - `preparar_dados(df_join) -> (X, y, meta)`: `X` = coluna(s) do preditor principal (`score_oportunidade_residual`; opção de incluir `log1p(oferta_efetiva_disponivel)` no modelo secundário), `y = log1p(membros)`; limpa NaN/inf; `meta` com `n_join`, descartados, `range_membros`, `features`, `concentracao_uf`.
   - `correlacoes_bivariadas(df_join) -> dict[str,(rho,p)]`: Spearman de `score_oportunidade_residual`, `oferta_efetiva_disponivel`, `alunos_parceiras`, `n_acad_parceiras` vs `membros` (ANTES do modelo; reporta colinearidade/contexto).
   - `_kfold_repetido_oof`, `_ic_bootstrap_r2`, `_ic_bootstrap_rho` (novo, análogo ao de R²), `_pct_extrapolacao`, `_selecionar_alpha_e_oof`: reusar/portar de `backtest_tp05.py` (mesma seed 42, `N_REPEATS=5`, `N_PISO_KFOLD=200`, `N_PISO_LOO=30`, `N_BOOTSTRAP≥2000`). **Nota:** subir `N_BOOTSTRAP` para **2000** (o pedido do bloco cita 2000; TP-05 usava 500) — decisão registrada, seed fixa preserva determinismo.
   - `calibrar_residual_demanda(df_join, limiar_r2=LIMIAR_R2_GO) -> CalibracaoResidualResult`: orquestra Spearman bruta → prepara → escolhe alpha por menor RMSE oof → oof (Ridge) → IC95 R² e IC95 rho (bootstrap seed 42) → modelo final (coefs + R² in-sample auditoria) → `pct_extrapolacao`/flag → veredito → `nota_honesta`.
   - `relatorio_calibracao(result) -> str` (markdown PT sem PII) + `escrever_relatorio(result, path)`; `_assert_sem_pii_no_relatorio(texto)` (rede de segurança contra `COLUNAS_PII_PROIBIDAS`).
   - `_join_demanda_residual(dem_df, mkt_df) -> pd.DataFrame`: inner join por `hex_id` das colunas agregadas necessárias (nunca PII). Bloco `if __name__ == "__main__"` lê os dois parquets, roda e escreve o relatório (não coberto por teste).

3. **Protocolo de validação DEC-008 (concreto):**
   - `RepeatedKFold(n_splits=5, n_repeats=5, random_state=42)` vs **baseline da média** (fallback `k=10` se `n<200`, LOO se `n<30` — aqui `n≈16.411` → 5×5).
   - Métricas OUT-OF-FOLD: `R²_oof` (âncora) **e** `rho_oof` (Spearman das predições oof concatenadas vs alvo), cada uma com **IC95 bootstrap (seed=42, 2000 reamostras)**.
   - `R²_insample` computado SÓ com rótulo literal de auditoria; nunca no veredito.
   - Predições com intervalo (±1 RMSE_oof_log back-transformado) + `flag_extrapolacao` por envelope min-max das features.
   - **Alvo `log1p(membros)`** (contagem cauda longa; back-transform `expm1` só auditoria).
   - **Critério GO/NO-GO numérico:** GO se (`R²_oof_log > LIMIAR_R2_GO` (0,05) **E** `IC95_inferior(R²_oof) > 0`) **OU** (`rho_oof ≥ 0,30` **E** `IC95_inferior(rho_oof) > 0`); documentar qual condição disparou. Recomendação de gate = **exigir R² como âncora e reportar rho como suporte** (ver DECISÕES). NO-GO é resultado VÁLIDO (DEC-008) — não forçar GO. "O residual prevê demanda observada" só se sustenta se a métrica-âncora superar o baseline com IC não cruzando zero, **restrito ao recorte metropolitano do join (~1%)**.

4. **Escopo R4 (academias menores WellHub/TotalPass) — DECISÃO PARA O GATE (recomendação (A)):** validar SÓ com o que já está ingerido (`demanda_revelada_h3.parquet`: `membros` alvo; `alunos_parceiras`/`n_acad_parceiras` como contexto). A ingestão de `03_Competidores.xlsx` (24.045 academias, `Alunos_Academia`/`Plano`/`Cluster`/`Município`) exigiria NOVA ingestão anti-PII (drop Lat/Lng/Nome na fronteira, agregação por hex) → vira sucessor **BLK-TP-08**. Justificativa e trade-off no §DECISÕES.

5. **"Calibrar melhor" sem violar §5:** este bloco **VALIDA + PROPÕE (documenta)** — se GO, a seção "Proposta de recalibração" do relatório descreve como os componentes do residual (ex.: capacidade default 2.500, peso de `oferta_efetiva_disponivel`, faixa de corte) poderiam ser ajustados para melhorar o alinhamento com a demanda observada, **sem alterar nenhuma linha da fórmula em produção** (`calcular_colunas_mercado.py`) nem regenerar `hexagonos_mercado_mapeado.parquet`/derivados. Aplicar = FOLLOW-UP com gate próprio. Deixar explícito no relatório: "análise/recomendação" vs "mudança de produção".

6. **Relatório `data/analysis/calibracao_residual_demanda.md`** (gitignored), seções: (1) Amostra + cobertura/viés (16.411 hexes, ~1,06%, top-3 UF); (2) Distinção dos 2 tipos de aluno/ativo (`membros` demanda × `alunos_parceiras` oferta) + alvo travado; (3) Correlações bivariadas (Spearman ANTES do modelo); (4) Métricas honestas out-of-fold (R²_oof + IC95, rho_oof + IC95, RMSE vs baseline; R²_insample rotulado auditoria); (5) Veredito GO/NO-GO com o critério numérico; (6) Se GO — Proposta de recalibração (não aplicada); (7) Limitações/confounds (cobertura ~1%, viés metropolitano, 2 tipos de aluno, ruído de coords ~1 km R5, viés de seleção da plataforma, `extrato ≠ universo` R7).

7. **Testes `tests/unit/demanda_revelada/test_calibracao_residual.py`** (fixtures SINTÉTICAS, seed fixa, sem rede/sem fonte real):
   - anti-PII: nenhuma coluna de `COLUNAS_PII_PROIBIDAS` no result/relatório; `_assert_sem_pii_no_relatorio` cobre.
   - LOO/k-fold honesto: `r2_insample` presente mas com rótulo literal de auditoria; não usado no veredito.
   - intervalos + `flag_extrapolacao` presentes; `pct_extrapolacao` finito.
   - **alvo = `membros` / preditor = `score_oportunidade_residual`** (teste que garante que `membros` NÃO é feature e o preditor NÃO é `membros` — não inverter os 2 tipos); `alunos_parceiras` nunca alvo.
   - demanda nunca usada como preditor geográfico (asserção de que `membros`/colunas da demanda não aparecem em `FEATURES`).
   - isolamento de import: `ast`/`inspect` garante que o módulo não importa `pipelines.m1`/`dashboard`/`censo`/`api`/`config`.
   - IC determinístico: rodar 2× com mesma seed → mesmo IC (R² e rho).
   - GO/NO-GO: fixture com sinal forte → GO; fixture com ruído → NO-GO (IC cruza zero).

## Arquivos afetados
- `src/motor_expansao/demanda_revelada/calibracao_residual.py` (NOVO)
- `tests/unit/demanda_revelada/test_calibracao_residual.py` (NOVO; fixtures sintéticas)
- `data/analysis/calibracao_residual_demanda.md` (NOVO relatório, gitignored — materializado pelo `__main__`, não pelo teste)
- `tasks/backlog.md` (bloco BLK-TP-06 → concluído), `tasks/current_task.md`, `tasks/completed.md`
- `context/handoff.md`, `context/handoff/20260702-133845-planner.md`
- (opcional) `docs/modelo_mercado_hexagonos.md` — NÃO alterar a fórmula; se necessário, só nota de "validação externa" (preferir manter no relatório de `data/analysis/`).

## Dependências
- `data/staging/demanda_revelada_h3.parquet` (16.575 × 9) e `data/staging/hexagonos_mercado_mapeado.parquet` (colunas `hex_id`, `score_oportunidade_residual`, `oferta_efetiva_disponivel`, `uf`) — LIDOS READ-ONLY.
- `motor_expansao.dimensionamento.aderencia` (`ALPHA_GRID`, `LIMIAR_R2_GO`, `_r2_loo_para_alpha`) e `dimensionamento.backtest_dim` (`_r2`, `_rmse`) — reuso, sem dep nova.
- `numpy`, `pandas`, `scipy.stats.spearmanr`, `sklearn.linear_model.Ridge`, `sklearn.model_selection.RepeatedKFold` (já na base; mesmos de `backtest_tp05.py`).

## Riscos técnicos
- **R1 (semântico, central):** confundir `membros` (demanda) com `alunos_parceiras`/`Alunos_Academia` (oferta). Mitigar: alvo travado = `membros`; teste garante não-inversão; distinção escrita no relatório.
- **R2 (cobertura/viés):** join casa ~1,06% do universo, enviesado p/ Sudeste (SP 29%, top-3 51%). Veredito vale p/ metrópoles, não é validação nacional — caveat forte no relatório e em `nota_honesta`.
- **R3 (metodologia):** tentação de reportar o +0,517 in-sample como veredito. DEC-008 exige out-of-fold + IC95 seed-fixa + R² in-sample banido. Sem isso o bloco falha o gate.
- **R4 (escopo/PII):** incorporar `03_Competidores.xlsx` exige nova ingestão anti-PII (potencialmente grande) → recomendação (A): adiar p/ BLK-TP-08.
- **R5 (ruído de join):** coords das células ~1 km → ruído no join res-7 (~5,16 km²); declarar no relatório.
- **R6 (guardrail):** proposta de calibração NÃO pode virar aplicação em produção nem tocar a fórmula/artefatos — só relatório.
- **R7 (extrato ≠ universo):** `02_Oportunidades_Expansão.xlsx` (15 linhas) e o "89% a >5km" são o extrato das sugestões, não o universo — não usar como base.

## Critérios de aceite finais
- Módulo READ-ONLY na camada paralela: **não importa** de `pipelines/m1`, `dashboard`, `censo_*`, `api`, `config` (teste de isolamento).
- Validação por k-fold 5×5 repetido (seed 42) vs baseline da média; **R² in-sample banido** do veredito (só rótulo auditoria); predições com intervalo + `flag_extrapolacao`; IC95 bootstrap (seed 42, 2000 reamostras) para R²_oof **e** rho_oof.
- Join por `hex_id` com **caveat de cobertura** (16.411 ≈ 1,06%; concentração Sudeste) reportado.
- **Alvo travado e documentado** (`membros` = demanda, NÃO `alunos_parceiras`); distinção dos 2 tipos escrita.
- Veredito **GO/NO-GO** em `data/analysis/calibracao_residual_demanda.md`; se GO, **proposta** de calibração (NÃO aplicada). ZERO escrita em `hexagonos_mercado_mapeado.parquet` ou derivados.
- Anti-PII: só agregados; **fixtures sintéticas**; zero coluna de `COLUNAS_PII_PROIBIDAS` em artefato/log/teste.
- **mtime dos 4 artefatos oficiais do M1 inalterado**; suíte verde; `import streamlit_app` ok.

## Validações obrigatórias
- Comando: `python -m pytest -n auto -q tests/unit/demanda_revelada/ tests/integration/test_streamlit_app.py` — Critério: **0 falhas**.
- Comando: `python -c "import streamlit_app"` — Critério: **0 erro**.
- Comando (isolamento): `python -c "import ast,sys; t=ast.parse(open('src/motor_expansao/demanda_revelada/calibracao_residual.py',encoding='utf-8').read()); mods=[a.module or '' for n in ast.walk(t) if isinstance(n,ast.ImportFrom) for a in [n]]; bad=[m for m in mods if any(k in (m or '') for k in ('pipelines.m1','dashboard','censo','api','config'))]; sys.exit(1 if bad else 0)"` — Critério: **sem imports proibidos**.
- Comando (mtime M1): registrar `stat -c %Y` (ou equivalente) de `data/staging/{brasil_estrutural,brasil_priorizados,hexagonos_brasil_oportunidades,hexagonos_brasil_dashboard}.parquet` antes/depois — Critério: **inalterado**.

## DECISÕES PARA O GATE HUMANO
1. **Escopo R4 — academias menores WellHub/TotalPass:**
   - (A) Validar SÓ com o já ingerido (`membros` alvo; `alunos_parceiras`/`n_acad_parceiras` contexto); ingestão de `03_Competidores.xlsx` vira **BLK-TP-08**.
   - (B) Ingerir `03_Competidores.xlsx` NESTE bloco (nova ingestão anti-PII, drop Lat/Lng/Nome na fronteira) — maior escopo/risco.
   - (C) Ingerir só uma agregação mínima por hex de `Alunos_Academia` como covariável extra, sem contrato completo — meio-termo, ainda exige fronteira anti-PII nova.
   - **RECOMENDAÇÃO: (A).** O bloco é de VALIDAÇÃO honesta do sinal existente; `03_Competidores` é OFERTA (não muda o ALVO `membros`), a nova ingestão infla escopo e risco PII, e o valor incremental é cross-check — não veredito. Adiar p/ BLK-TP-08 mantém o gate focado e loop-safe.

2. **Transformação do alvo — `log1p(membros)` vs cru:**
   - `membros` é contagem com cauda longa (mediana 7, p90 243, p99 2.257) → Ridge no espaço cru seria dominado pelos hexes gigantes e violaria homoscedasticidade.
   - **RECOMENDAÇÃO: `log1p(membros)`** (mesma escolha do precedente BLK-TP-05 para `alunos_parceiras`); R²/rho reportados no espaço log (âncora) + `expm1` para auditoria/interpretabilidade.

3. **Critério GO/NO-GO:**
   - GO se (`R²_oof_log > 0,05` E `IC95_inf(R²_oof) > 0`) **OU** (`rho_oof ≥ 0,30` E `IC95_inf(rho_oof) > 0`); reportar qual disparou; NO-GO é resultado válido (DEC-008).
   - **RECOMENDAÇÃO:** usar **R²_oof_log como âncora** (herdado de aderencia.py/TP-05) e **rho_oof como suporte** — Spearman é robusto a outliers e casa com o +0,52 histórico, mas R² garante que o modelo bate o baseline da média fora da amostra. Exigir a âncora R² para o GO "forte"; um GO só por rho deve ser rotulado "alinhamento monotônico sem ganho de erro out-of-fold".

4. **Tratamento dos 2 tipos de aluno/ativo:**
   - **ALVO = `membros`** (demanda paga observada; 16.575 hexes). `alunos_parceiras`/`n_acad_parceiras`/`Alunos_Academia` = OFERTA instalada → SÓ covariável/cross-check, NUNCA alvo.
   - **RECOMENDAÇÃO:** travar `membros` como alvo único; documentar a distinção no relatório e blindar por teste (não-inversão). Inverter os dois validaria o residual contra oferta instalada, não contra demanda — erro semântico que anularia o bloco (R1).

## Fora de escopo
- QUALQUER recálculo/alteração de `score_priorizacao`, `hex_score_estrutural`, pesos (renda 0.40/pop 0.60), carteira, plano, plano de domínio, artefatos oficiais do M1.
- **Aplicar** a recalibração da fórmula de `score_oportunidade_residual` / regenerar `hexagonos_mercado_mapeado.parquet` ou derivados (follow-up com gate próprio).
- Usar a demanda revelada como **preditor geográfico de magnitude** ou ajuste do `score_priorizacao`/residual (DEC-009).
- R² in-sample, `fit(X,y)→predict(X)` ou reproduzir o +0,52 sem out-of-fold como veredito.
- Ingerir/persistir/logar PII; nova ingestão de `03_Competidores.xlsx` (recomendado adiar p/ BLK-TP-08).
- Tocar `dashboard/`, `censo_*`, `api/`, `pipelines/m1/`.

## Alerta de aprovação
**ATENÇÃO: ESTA TAREFA É MODELAGEM DE CRITICIDADE ALTA COM GATE HUMANO OBRIGATÓRIO.** Embora READ-ONLY sobre o M1 (não toca `score_priorizacao`/`hex_score_estrutural`/pesos/artefatos oficiais), ela VALIDA e PROPÕE calibração de um campo ATIVO da camada paralela de mercado/residual (`score_oportunidade_residual`) e envolve dado com PII na origem (DEC-012). **O usuário DEVE ler este handoff e as DECISÕES PARA O GATE HUMANO (escopo R4, transformação do alvo, critério GO/NO-GO, tratamento dos 2 tipos de aluno) e confirmar ANTES do Builder.** NENHUMA recalibração da fórmula em produção pode ser aplicada neste bloco.

## Guardrails ativos
- **§5 (READ-ONLY M1):** camadas paralelas não podem recalcular/alterar `score_priorizacao`, `hex_score_estrutural`, carteira, plano, plano de domínio ou artefatos oficiais sem aprovação explícita.
- **DEC-008:** k-fold/LOO repetido SEMPRE vs baseline da média; R² in-sample BANIDO; começar simples (Ridge/GLM); toda saída com intervalo + flag de extrapolação; NO-GO é resultado válido.
- **DEC-009:** demanda é ALVO/insumo OBSERVADO, NUNCA preditor geográfico de magnitude; proibido reintroduzir como ajuste do score.
- **DEC-012 (anti-PII):** só camada agregada; `employee_id`/`company_id`/coordenadas residenciais nunca lidos/persistidos/logados; zero PII em artefato/log/teste (`COLUNAS_PII_PROIBIDAS`); fonte real (`NAO_ABRA/`) nunca versionada; fixtures sintéticas.
- **§1:** Ultra é low-cost/massa; Smart Fit (`concorrente_lc`/SF) é o concorrente low-cost de referência.
- **Parâmetros canônicos imutáveis sem DEC:** H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, pesos renda=0.40/pop=0.60.
