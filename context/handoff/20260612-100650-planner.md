# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
REVISÃO HUMANA do plano (gate obrigatório — esteira Alta) → depois Builder → QA

## Entendimento da tarefa
Recorte BLK-UI-01: tornar a sidebar mais aparente, adicionar spinners de carregamento nos 3 pontos de maior latência e remover poluição visual técnica do dashboard — tudo puramente visual, READ-ONLY sobre o M1, sem tocar carga lazy/render lazy/mapa enxuto.

## Confirmação de file:line REAIS (validados no código atual)
- `streamlit_app.py:180` → `initial_sidebar_state="collapsed"` CONFIRMADO (dentro de `st.set_page_config`, linhas 177-181).
- `streamlit_app.py:441-445` → `try: df = load_uf_slice(selected_uf)` dentro de `main()`; a chamada está na **linha 442**, envolta por `try/except (FileNotFoundError, ValueError)`. CONFIRMADO.
- `streamlit_app.py:485-489` → 1º `st.caption()` = "Recorte atual: N hexagonos | …" (ÚTIL — MANTER).
- `streamlit_app.py:490-493` → 2º `st.caption()` = "Base oficial preservada: `data/outputs/hexagonos_brasil_dashboard.parquet` …" (poluição técnica — alvo de remoção/expander).
- `pages.py:129` → `inject_styles()`; bloco `[data-testid="stSidebar"]` nas linhas **146-154** (gradient + `border-right: 1px solid`). CONFIRMADO.
- `pages.py:363-379` → `render_header()`; 5 pills `.pill` nas linhas **370-374** (`score_priorizacao`, `score_setor_2022_calibrado`, `score_expansao_hibrido`, "UFs censo: DF GO MG RJ RS SP", "Filtros na sidebar"). CONFIRMADO.
- `pages.py:382-395` → `render_uf_selectbox()`; PRIMEIRO elemento da sidebar = `st.sidebar.markdown("### Filtros globais")` (linha 388) + caption (389) + `st.sidebar.selectbox("UF", …)` (390-395). O selectbox segue sendo o gate da carga lazy. CONFIRMADO.
- `pages.py:1028-1033` → dois `st.caption()` em `render_modelo_hibrido_v2` (M1 decide município / sinal editorial de cautela). CONFIRMADO.
- `pages.py:2026-2030` → caption de limitações do `st.pydeck_chart` no ESTADO VAZIO da Análise Pontual (antes de qualquer clique). CONFIRMADO.
- `pages.py:2049-2052` → caption de centroide H3 (estado COM ponto). É GUARDRAIL VISUAL — **NÃO remover** (instrução do BO).
- `pages.py:2550-2558` → chamada `render_mapas_censitarios_combinados(...)` (multi-linha; fetch de tiles DEC-004). CONFIRMADO.
- `pages.py:2800-2812` → chamada `deck, n_points = build_unified_map_figure(...)` (multi-linha) dentro de `render_mapa_territorial` (def na 2691). CONFIRMADO.
- `constants.py:270-287` → `COLORS` tem `brand_alt="#19B7FF"`, `border`, `accent`, `text`, `muted` — suficiente para o CSS da sidebar sem nova constante.
- Não há nenhum `st.spinner` no código de dashboard hoje (primeiro uso).
- Testes: `test_streamlit_app.py` mocka `render_header`, `render_uf_selectbox`, `pydeck_chart`, `st.caption`, `st.markdown` no teste de `main()` (linha 4253-4294); NÃO mocka `st.spinner`. As referências a `score_priorizacao` no teste são dados de fixture, não asserções sobre pills do header.

## Plano técnico

> Convenção: cada passo cita arquivo:linha-âncora ATUAL. As linhas deslocam após edições anteriores no mesmo arquivo — o Builder deve ancorar pelo TEXTO citado, não pelo número absoluto.

### Bloco A — Melhoria 1: Sidebar mais aparente

**A1. `streamlit_app.py:180` — abrir a sidebar por padrão.**
- Trocar `initial_sidebar_state="collapsed"` por `initial_sidebar_state="expanded"`.
- Decisão de produto: ver **D1**.

**A2. `pages.py` `inject_styles()` (bloco `[data-testid="stSidebar"]`, linhas 146-151) — reforço visual da sidebar.**
- Aumentar o contraste da borda lateral: trocar `border-right: 1px solid {COLORS["border"]}` por uma borda mais destacada usando `COLORS["brand_alt"]` (ex.: `border-right: 2px solid rgba(25, 183, 255, 0.45)`) e/ou adicionar `box-shadow: 4px 0 24px rgba(0,0,0,0.35)` para separar a sidebar do conteúdo.
- Sem `!important`; especificidade por `data-testid` (padrão já vigente no arquivo).
- Decisão de produto sobre a intensidade do destaque: ver **D2**.

**A3. `pages.py` `render_uf_selectbox()` (linhas 388-389) — título mais forte no topo da sidebar.**
- Substituir o `st.sidebar.markdown("### Filtros globais")` por um cabeçalho com identidade visual mais forte (um pequeno bloco HTML estilizado via `st.sidebar.markdown(..., unsafe_allow_html=True)` reutilizando a paleta `COLORS`), mantendo o caption explicativo (linha 389) logo abaixo.
- INVARIANTE CRÍTICA: o `st.sidebar.selectbox("UF", …)` (linhas 390-395) deve permanecer o PRIMEIRO widget de input da sidebar e o valor retornado segue sendo o gate da carga lazy. O cabeçalho é markdown decorativo ANTES do selectbox (já é o caso hoje) — não introduzir nenhum widget que leia estado antes dele.
- Texto/forma exata do cabeçalho: ver **D3**.

### Bloco B — Melhoria 2: Indicadores de carregamento (spinners decorativos)

**B1. `streamlit_app.py:442` — spinner ao trocar de UF.**
- Envolver a chamada `df = load_uf_slice(selected_uf)` em `with st.spinner("Carregando dados da UF..."):`, mantendo a chamada DENTRO do `try` existente (linhas 441-445), de modo que o `except (FileNotFoundError, ValueError)` continue capturando. Estrutura:
  ```python
  try:
      with st.spinner("Carregando dados da UF..."):
          df = load_uf_slice(selected_uf)
  except (FileNotFoundError, ValueError) as exc:
      st.error(str(exc))
      st.stop()
  ```
- `@st.cache_resource` entrega cache quente em ms → spinner aparece só na 1ª carga por UF (correto). Texto: ver **D4**.

**B2. `pages.py:2800` — spinner ao construir o mapa territorial.**
- Envolver a chamada `deck, n_points = build_unified_map_figure(...)` (linhas 2800-2812) em `with st.spinner("Construindo mapa..."):`. O resultado (`deck`, `n_points`) é consumido DEPOIS do bloco — o `with` deve englobar só a atribuição; o `if deck is None:` e o render permanecem fora do `with`. Texto: ver **D4**.

**B3. `pages.py:2550` — spinner ao gerar os mapas censitários.**
- Envolver a chamada `mapas = render_mapas_censitarios_combinados(...)` (linhas 2550-2558) em `with st.spinner("Gerando mapas censitarios..."):`. Inclui fetch de tiles DEC-004 — é o ponto de maior latência percebida no Relatório Pontual. Texto: ver **D4**.

> Guardrail B: spinners são context managers decorativos. NÃO mexer no corpo de `load_uf_slice`, `build_unified_map_figure` nem `render_mapas_censitarios_combinados`. Sem efeito em cache, carga lazy (Bloco 4), render lazy (Bloco 5) ou mapa enxuto (Bloco 6).

### Bloco C — Melhoria 3: Limpeza de poluição visual

**C1. `streamlit_app.py:490-493` — caption técnico de proveniência.**
- Remover o 2º `st.caption()` ("Base oficial preservada: …"). O `render_manifest_footer(MANIFEST_PATH)` (linha 565) já cobre proveniência no rodapé. Manter intacto o 1º caption "Recorte atual: …" (485-489). Decisão remover-vs-mover-para-expander: ver **D5**.

**C2. `pages.py:370-374` (`render_header`) — pills técnicas.**
- Simplificar as 5 pills para 2-3 com linguagem de usuário, removendo os nomes internos de coluna (`score_priorizacao`, `score_setor_2022_calibrado`, `score_expansao_hibrido`) e a lista de UFs do header. Conteúdo exato das pills mantidas: ver **D6**.

**C3. `pages.py:2026-2030` — caption de limitações do pydeck no estado vazio.**
- Remover este `st.caption()` do estado vazio da Análise Pontual (aparece antes de qualquer interação). O `st.info(...)` imediatamente acima (2022-2025) já orienta o usuário. NÃO tocar no caption de centroide H3 (2049-2052), que é guardrail de produto. Decisão remover-vs-mover-para-expander: ver **D7**.

**C4. `pages.py:1028-1033` (`render_modelo_hibrido_v2`) — dois captions repetitivos.**
- Consolidar os dois `st.caption()` em UM caption curto (a orientação "M1 decide município, Censitário é leitura local" já está nos model-cards logo acima, linhas 1004-1011). Decisão consolidar-vs-remover e texto final: ver **D8**.

**C5. (Opcional/avaliação) Legendas de score atrás de expander — NÃO incluído neste plano.**
- O BO listou esconder `render_geographic_source_legend`/legendas atrás de expander como "a avaliar pelo Planner". DECISÃO DO PLANNER: **deixar FORA deste recorte** — as legendas são apoio de leitura imediato do mapa; escondê-las atrás de expander prejudica usabilidade mais do que reduz poluição, e mexer nelas amplia a superfície de risco/teste. Registrado como Fora de Escopo. Caso o humano discorde: ver **D9**.

### Bloco D — Testes

**D-test. `tests/integration/test_streamlit_app.py`.**
- Adicionar `mock.patch("streamlit.spinner")` ao teste `test_main_renderiza_apenas_a_aba_ativa` (contexto da linha 4263-4285), por simetria com os outros mocks de UI e para blindar contra qualquer dependência de contexto de `st.spinner` no `main()`. (O `st.spinner` é um context manager seguro fora de script-run, mas o mock evita acoplamento.)
- Se algum teste existente asaltar o TEXTO de captions/pills removidos, atualizá-lo. (Validado: hoje NÃO há asserção sobre o texto das pills nem sobre os captions-alvo; os usos de `score_priorizacao` nos testes são fixtures de dados.)
- Smoke import (`import streamlit_app`) deve seguir verde — nenhuma mudança de assinatura pública.

## Decisões para o GATE HUMANO (D1..D9) — confirmar antes do Builder

- **D1 — Sidebar expandida por padrão.** (A) `initial_sidebar_state="expanded"` global (recomendado pelo BO e por mim — atende diretamente "menu mais aparente"); risco: em telas estreitas consome espaço horizontal. (B) Manter "collapsed" e investir só no CSS de destaque (A2) — menos intrusivo, mas o usuário ainda precisa abrir a sidebar. **Recomendação: A.** (Streamlit não tem media-query nativa confiável para sidebar inicial; manter simples.)
- **D2 — Intensidade do realce CSS da sidebar.** (A) Só borda mais forte (2px ciano). (B) Borda + sombra de separação (`box-shadow 4px 0 …`). (C) Borda + sombra + leve realce do cabeçalho. **Recomendação: B** (claro sem exagero).
- **D3 — Cabeçalho do topo da sidebar.** (A) Manter "Filtros globais" como `### ` (markdown simples). (B) Bloco estilizado curto, ex.: título "Filtros globais" + sublinha "Recorte executivo M1 + Híbrido" com cor de marca. **Recomendação: B** (reforça identidade; mantém o caption atual).
- **D4 — Textos dos spinners.** Propostos: B1 "Carregando dados da UF...", B2 "Construindo mapa...", B3 "Gerando mapas censitarios...". Confirmar ou ajustar. **Recomendação: usar os 3 textos propostos.**
- **D5 — Caption de proveniência (streamlit_app 490-493).** (A) Remover (manifesto no rodapé já cobre). (B) Mover para um `st.expander("Sobre os dados")` colapsado. **Recomendação: A** (rodapé já cumpre a rastreabilidade).
- **D6 — Pills do header.** (A) Manter 3 pills funcionais com linguagem de usuário: "Onde expandir (M1)", "Qual bairro (Censitário)", "Fila operacional (Híbrido)" e remover a pill de UFs e os nomes de coluna. (B) Manter 2 pills + 1 pill "Filtros na sidebar". (C) Remover todas as pills, deixar só título+subtítulo do hero. **Recomendação: A** (preserva a narrativa das 3 camadas sem jargão de coluna).
- **D7 — Caption pydeck no estado vazio (2026-2030).** (A) Remover. (B) Mover para `st.expander("Dica de uso")` colapsado. **Recomendação: A** (o `st.info` acima já orienta; detalhe técnico de "botão direito" é ruído).
- **D8 — Captions do híbrido (1028-1033).** (A) Consolidar em 1 caption curto: "M1 decide o município; o Censitário é leitura local e o Híbrido organiza a fila operacional." (B) Remover ambos (já há model-cards acima). **Recomendação: A** (mantém o aviso de cautela editorial de forma enxuta).
- **D9 — Legendas atrás de expander.** (A) NÃO fazer agora (recomendação do Planner — fora de escopo). (B) Esconder `render_geographic_source_legend` (modo M1) atrás de `st.expander("Legenda")`. **Recomendação: A.**

## Arquivos afetados
- `streamlit_app.py` (A1, B1, C1)
- `src/motor_expansao/dashboard/pages.py` (A2, A3, B2, B3, C2, C3, C4)
- `tests/integration/test_streamlit_app.py` (D-test)
- `src/motor_expansao/dashboard/constants.py` — NÃO necessário (a paleta `COLORS` já basta); só tocar se D2/D3 exigirem uma constante nova (baixo risco, opcional).

## Dependências
- Nenhuma dependência nova de pacote. `st.spinner` é nativo do Streamlit (≥1.26, já no projeto).
- Ordem entre passos do MESMO arquivo importa só para o casamento de linhas — Builder ancora por texto.

## Riscos técnicos
- **Deslocamento de linhas** ao editar `pages.py`/`streamlit_app.py` várias vezes: ancorar por texto citado, não por número.
- **Sidebar expandida (D1=A)** em telas pequenas: aceito como trade-off de produto (usuário pode colapsar manualmente).
- **Spinner fora do `try` (B1)**: manter a chamada `load_uf_slice` DENTRO do `try` e o `with st.spinner` DENTRO do `try` também — não deslocar o tratamento de exceção.
- **Spinner B2**: englobar só a atribuição `deck, n_points = ...`; o `if deck is None` e o render ficam FORA do `with` (não precisam do spinner e não devem mudar de indentação além do necessário).
- **Remoção de captions/pills**: validado que nenhum teste assere o texto removido; ainda assim o Builder deve rodar a suíte para confirmar.

## Critérios de aceite finais
- `initial_sidebar_state="expanded"` (se D1=A) e sidebar visivelmente mais destacada (borda/sombra).
- `render_uf_selectbox` continua sendo o PRIMEIRO widget da sidebar e o gate da carga lazy intacto.
- Spinner visível na 1ª troca de UF, na construção do Mapa Territorial e na geração dos mapas censitários.
- Caption técnico de proveniência (streamlit_app 490-493) removido (ou em expander, conforme D5); 1º caption "Recorte atual" preservado.
- Pills do header simplificadas (sem nomes internos de coluna), conforme D6.
- Caption de limitação do pydeck no estado vazio (2026-2030) removido/movido (D7); caption de centroide H3 (2049-2052) PRESERVADO.
- Captions do híbrido (1028-1033) consolidados (D8).
- M1 intocado: `score_priorizacao`, `hex_score_estrutural`, `score_setor_2022_calibrado`, `score_expansao_hibrido`, `score_oportunidade_residual`, carteira, plano, SAM e artefatos oficiais — ZERO alteração (mudanças são CSS/markdown/spinner/strings).
- Contratos de performance intactos: `load_uf_slice`/`read_enriched_uf_partition`/`load_uf_catalog` (Bloco 4), `render_tab_selector`/`st.segmented_control` (Bloco 5), `_downsample_map_index`/`MAP_SOURCE_COLUMNS_*` (Bloco 6) — corpo NÃO alterado.

## Validações obrigatórias
- Comando (alvo): `python -m pytest -q tests/integration/test_streamlit_app.py`
- Comando (suíte impactada, opcional acelerado): `python -m pytest -q -n auto tests/integration/test_streamlit_app.py`
- Suíte FULL (gate único no QA): `python -m pytest -q` — baseline `532 passed, 1 skipped` (não regredir).
- Smoke import: `python -c "import streamlit_app"` sem erro.
- Lint/type: `ruff check` e `mypy` limpos no escopo tocado (`streamlit_app.py`, `pages.py`, teste).
- Critério mínimo: 0 falhas, 0 erro de import, ruff/mypy limpos.

## Fora de escopo
- C5/D9: esconder legendas de score atrás de expander (Planner decidiu NÃO fazer agora).
- Refatoração ampla das 4 abas; criação/reorganização de abas.
- Qualquer alteração em score/pesos/carteira/plano/SAM/artefatos M1.
- Tocar nos builders de mapa, em `render_tab_selector`, `load_uf_slice`, `read_enriched_uf_partition`, `load_uf_catalog`, `_downsample_map_index`, `MAP_SOURCE_COLUMNS_*` além de envolver chamadas com `st.spinner`.
- Caption de centroide H3 (`pages.py:2049-2052`) e o caption de centroide do `render_mapa_pydeck_fragment` — guardrails de produto, PRESERVAR.
- Reintroduzir dependência de API ao vivo no dashboard de produção.

## Alerta de aprovação
ATENÇÃO: esta é uma esteira **ALTA com GATE HUMANO**. Embora o recorte seja puramente VISUAL e READ-ONLY sobre o M1 (não toca score, pesos nem artefatos oficiais), o plano contém **9 decisões de produto (D1..D9)** que exigem **REVISÃO HUMANA antes do Builder**. O usuário (Felipe/Vini) deve ler este handoff, confirmar/ajustar D1..D9 e só então liberar o Builder. NÃO acionar o Builder sem essa confirmação.

## Guardrails ativos
- §5 CLAUDE.md: visualizações e interações de mapa NÃO podem recalcular/alterar `score_priorizacao`, `hex_score_estrutural`, carteira, plano curto prazo, plano domínio ou artefatos oficiais do M1 sem aprovação explícita. Este recorte é CSS/markdown/spinner/strings — não recalcula nada.
- §2 CLAUDE.md: não criar dependência de API ao vivo no dashboard de produção (spinners não fazem fetch; DEC-004 do basemap permanece restrita ao caminho de geração do relatório).
- Preservar carga lazy por UF (Bloco 4), render lazy de abas (Bloco 5) e fonte de mapa enxuta (Bloco 6) — corpo intocado, só wrapper de spinner por fora.
- `render_uf_selectbox` permanece o PRIMEIRO elemento/gate da sidebar.
- Toda mudança relevante entra com teste; nenhum PR sobe com CI quebrado.

## Paths pré-sujos (NÃO commitar — alheios ao ciclo)
- data/outputs/setores_censitarios_2022_geo/_metadata.json
- data/reports/relatorio_pontual_censitario_base_geo.md
