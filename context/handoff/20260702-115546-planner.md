# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Approver / **REVISÃO HUMANA (gate Felipe — modelagem)**

## Entendimento da tarefa
Criar um módulo READ-ONLY em `src/motor_expansao/demanda_revelada/calibracao_curva.py` que VALIDA (não altera em produção) a curva tamanho→densidade da `faixa_alunos_por_densidade` sobre a base com metragem real (N=112, Ultra+Engenharia) sob a disciplina DEC-008 (LOO/k-fold vs baseline, sem R² in-sample, intervalos p10/p50/p90 + flag de extrapolação), usando `alunos_parceiras` da Demanda Revelada apenas como sanity-check externo — porque, conforme evidência abaixo, `alunos_parceiras` NÃO tem metragem e representa micro-academias parceiras (mediana ~16 alunos/unidade) incomparáveis unidade-a-unidade com clubes Ultra/Engenharia (~2.700).

## EVIDÊNCIA MEDIDA (agregada, anti-PII — insumo do gate)
- `demanda_revelada_h3.parquet`: N=16.575 hexes; `alunos_parceiras>0` em 5.341; `n_acad_parceiras` mediana 3, máx 123. **SEM metragem.**
- `alunos_por_unidade = alunos_parceiras/n_acad_parceiras` (n_acad em [1,3], N=3.035): p10/p50/p90 = **2 / 16 / 88** alunos/unidade → são **micro-academias de bairro do TotalPass** (o "aluno" é o beneficiário registrado por academia parceira), NÃO enrolamento total de clube.
- `base_calibracao_multirede.parquet`: 426 linhas (SkyFit 311 SEM metragem, Engenharia 61, Ultra 54). **Com metragem+alunos: N=112** (Engenharia 58 + Ultra 54). `alunos_reais` p10/p50/p90 = **1.620 / 2.678 / 4.039**.
- **Conclusão dura:** as duas populações de "alunos/unidade" estão a **~2 ordens de grandeza** de distância → `alunos_parceiras` **não pode** ser injetado na base de calibração da curva `m²→alunos/m²` nem usado como validação cruzada unidade-a-unidade. Serve só como sanity-check de ordem de grandeza (com disclaimer).
- Heterogeneidade de rede confirmada: `alunos_por_m2` mediana **Engenharia 2,31 vs Ultra 1,57** (base 112). `metragem` p50=1.400 (min 636, máx 5.863). Da fatia com metragem+alunos, Engenharia tem 36 `fuzzy` + **22 `nao_casado`** (sem coord, mas com m²+alunos do xlsx); Ultra 54 `direto`.
- Reafirma `densidade_contexto.md` (DIM-07): curva metragem→densidade é o ÚNICO sinal real (corr −0,37; R²_LOO metragem +0,096; geografia NO-GO).
- **Precedente de dependência/isolamento (BLK-TP-05):** `demanda_revelada/backtest_tp05.py` JÁ importa `sklearn.linear_model.Ridge`, `RepeatedKFold`, `scipy.stats.spearmanr`, e `_r2_loo_para_alpha`/`_r2`/`_rmse` de `dimensionamento/`. Logo: (a) sklearn/scipy SÃO deps base (`pyproject.toml` linhas 34-35), sem dep nova; (b) importar de `dimensionamento/` é PERMITIDO (a regra de isolamento veda só `pipelines/m1/`, `dashboard/`, `censo_*`, `api`).

## DECISÕES PARA O GATE HUMANO (Felipe aprova/ajusta antes do Builder)

### D1 — Papel de `alunos_parceiras` (sem m²) na calibração da curva  ⟵ decisão central
- **(A) [RECOMENDADA] `alunos_parceiras` = sanity-check externo qualitativo (ordem de grandeza), NÃO validação cruzada da curva.** Calibrar/validar a curva SÓ na base com m² (N=112). O módulo computa e reporta a distribuição de `alunos_por_unidade` das parceiras como contexto ("micro-academias, ~16 alunos/unid, incomparáveis com clubes"), com disclaimer explícito de que NÃO entra na curva. Trade-off: honesto (não fabrica m²), mas o insumo TP-04 acrescenta pouco à curva em si — o valor é confirmar que a curva não tem uso ingênuo cruzado.
- (B) `alunos_parceiras` = validação cruzada indireta de FAIXA: checar se a faixa p10/p50/p90 que a curva Ultra+Eng prevê para um m² típico "contém" a distribuição das parceiras. **REJEITADA como default**: as escalas não se sobrepõem (16 vs 2.700 alunos/unid); a checagem sempre falharia por construção e seria enganosa.
- (C) Injetar `alunos_parceiras` na base com m² NULO. **PROIBIDA** (BO §Fora de escopo: inferir m² = fabricar dado).
- **Recomendação: A.**

### D2 — Fonte da base com m² para calibrar/validar a curva
- **(A) [RECOMENDADA] `base_calibracao_multirede.parquet`, fatia com metragem+alunos (N=112, Ultra+Engenharia)** — maior N com m² real; usa `marca` como estratificação/covariável (D4).
- (B) Só Ultra (N=54, `unidades_ultra_performance_hex.parquet`) — é a fonte da curva EM PRODUÇÃO hoje, mais homogênea, mas N menor e monomarca. Reportar como **coorte de referência secundária** no relatório.
- **Recomendação: A como principal + B como coorte de robustez (reportar ambos).**

### D3 — Filtro `flag_qualidade_match` na base multirede
- **(A) [RECOMENDADA] NÃO filtrar por coord.** A curva usa só `metragem`+`alunos_reais` (não usa lat/lng); as 22 linhas Engenharia `nao_casado` têm m²+alunos válidos do xlsx e são amostra legítima da curva. Manter todas as 112.
- (B) Filtrar só `direto`+`fuzzy` (N≈90). Trade-off: perde 22 linhas Eng por um critério (coord) irrelevante à curva.
- **Recomendação: A** (registrar `flag_qualidade_match` no output para auditoria, sem usá-lo como filtro).

### D4 — Heterogeneidade de rede (`marca`)
- **(A) [RECOMENDADA] Estratificar/reportar por marca + rodar validação (i) pooled e (ii) `marca` como dummy covariável.** Reportar o R²_oof nos dois cenários e a densidade mediana por marca (Eng 2,31 vs Ultra 1,57). NÃO recalibrar coeficientes.
- (B) Ignorar marca (pooled só). **REJEITADA** — mascara confound conhecido.
- **Recomendação: A.**

### D5 — Corte de hexes com `n_acad_parceiras` alto (ruído) — só afeta o sanity-check (D1-A)
- **(A) [RECOMENDADA] Reportar `alunos_por_unidade` das parceiras em faixas de `n_acad` (1, 2-3, 4-10, 10+) e usar como referência PRINCIPAL a faixa `n_acad ≤ 3` (N=3.035)** — hex denso com 10+ academias mistura tiers e é ruidoso.
- (B) Sem corte (usar todos os 5.341). Reportar como sensibilidade.
- **Recomendação: A** (faixa ≤3 como referência; faixas maiores como sensibilidade no relatório).

### D6 — Filtrar parceiras por tipo de rede low-cost comparável
- **(A) [RECOMENDADA] NÃO filtrar por tipo** — o dump agregado NÃO carrega tipo/rede por academia parceira (só `n_acad_parceiras`/`alunos_parceiras`); filtrar exigiria dado que não existe. Registrar como limitação e tratar a distribuição como heterogênea (disclaimer).
- **Recomendação: A** (impossível filtrar; documentar heterogeneidade).

### D7 — Critério GO/NO-GO numérico da validação da curva (base com m²)
- **[RECOMENDADO] "curva VALIDA"** ⟺ no melhor modelo out-of-fold `densidade(alunos/m²) ~ f(log metragem)`: **R²_oof > 0 com IC95 bootstrap (seed=42) NÃO cruzando zero**. Alinha com `LIMIAR_R2_GO=0.05` de `aderencia.py`; nota: DIM-07 mediu R²_LOO≈+0,096 → margem estreita, IC provavelmente próximo de zero (resultado honesto esperado, possivelmente NO-GO ou GO fraco). NO-GO é resultado VÁLIDO (DEC-008).
- **Recomendação: adotar o limiar acima.**

## Plano técnico
1. **Novo módulo `src/motor_expansao/demanda_revelada/calibracao_curva.py`** — funções PURAS (entrada = DataFrames/paths; saída = dataclass/dict; sem I/O de rede; NÃO importa de `pipelines/m1/`, `dashboard/`, `censo_*`, `api`; PODE importar de `dimensionamento/` — precedente BLK-TP-05). Cabeçalho com guardrails (READ-ONLY M1, DEC-008/009/012, curva prediz de m² SOMENTE).
   - Constantes locais: `SEED_BOOTSTRAP=42`, `N_BOOT=2000`, `K_FOLD=5`, `N_REPEATS=5`, `R2_GO_LIMIAR=0.0` (IC não cruza zero), `N_ACAD_REF_MAX=3` (D5), `ALPHA_GRID` (reusar de `aderencia`).
2. **`carregar_base_curva(path_multirede=..., path_ultra=...) -> pd.DataFrame`**: lê `base_calibracao_multirede.parquet`; filtra `metragem>0 & alunos_reais>0` (N≈112); deriva `alunos_por_m2 = alunos_reais/metragem`; mantém `marca`, `metragem`, `flag_qualidade_match`. Sem filtro por coord (D3-A). Coorte Ultra (N=54) opcional via `path_ultra` para robustez (D2-B).
3. **`validar_curva_densidade(base_df, *, estratificar_marca=True) -> CurvaValidacaoResult`** — coração da validação DEC-008:
   - alvo `y = alunos_por_m2`; feature `X = log(metragem)` (D7). Variante com dummy `marca` (D4-A).
   - **k-fold 5×5 repetido (`RepeatedKFold(n_splits=5, n_repeats=5, random_state=42)`) vs baseline da média** (reusar `_r2`/`_rmse` de `dimensionamento.backtest_dim` e/ou `_r2_loo_para_alpha` de `dimensionamento.aderencia`); `alpha` por menor rmse_oof; se N pequeno estourar folds, fallback LOO documentado.
   - Métricas out-of-fold: `r2_oof`, `rho_oof` (Spearman, `scipy.stats.spearmanr`); **IC95 por bootstrap (2000 reamostras, seed=42)** sobre o R²_oof. **R² in-sample NUNCA no output** (pode existir só como variável interna descartada — NÃO expor).
   - **Intervalos de predição p10/p50/p90 de `alunos_por_m2`** por janela de m² (espelhar a lógica de janela ±20%/±50%/base-inteira da `faixa_alunos_por_densidade` — SEM importar/alterar `viabilidade_ponto.py`; reimplementar a janela localmente ou chamar a função como caixa-preta read-only passando `base_df` renomeada).
   - **`flag_extrapolacao`**: `True` para m² fora do envelope `[p05, p95]` (ou min/max) da metragem da base — helper `flag_extrapolacao_m2(m2, base_df) -> bool`.
   - Retorna `CurvaValidacaoResult(dataclass)` com: `n`, `alpha_sel`, `r2_oof`, `r2_oof_ic95` (tupla), `rho_oof`, `rho_oof_ic95`, `r2_baseline`, `go` (bool via D7), `p10/p50/p90` por faixa de m² de referência, `densidade_por_marca` (dict), `veredito` (str).
4. **`sanity_check_parceiras(demanda_df, *, n_acad_max=3) -> dict`** (D1-A/D5-A/D6-A): computa `alunos_por_unidade` das parceiras por faixa de `n_acad` (1, 2-3, 4-10, 10+); reporta N e p10/p50/p90 por faixa; **marca explicitamente que NÃO entra na curva** (`usado_na_curva=False`, `motivo="micro-academias parceiras; sem m²; escala incomparável"`). NUNCA usa `alunos_parceiras` como preditor (só descritivo).
5. **`escrever_relatorio(result, sanity, path=Path("data/analysis/calibracao_curva_densidade.md"))`** — materializa o .md (seções em §Relatório abaixo). Gitignored (padrão `data/analysis/`).
6. **`__main__` guard** (`# pragma: no cover`): monta base → valida → sanity → escreve relatório. NÃO grava parquet novo (bloco é análise; não há artefato de staging a versionar). NÃO chama nada que toque M1.
7. **`__all__`** exportando as funções públicas + a dataclass.
8. **Testes `tests/unit/demanda_revelada/test_calibracao_curva.py`** (criar `__init__.py` se o dir não existir) — fixtures SINTÉTICAS (nunca a fonte real):
   - `test_anti_pii`: nenhuma coluna de `COLUNAS_PII_PROIBIDAS` (contrato.py) nos DataFrames de saída/relatório.
   - `test_loo_honesto_sem_r2_insample`: o result e o relatório NÃO expõem `r2_insample`/`r2_in_sample` (assert ausência da chave e da substring no .md).
   - `test_intervalos_presentes`: p10/p50/p90 presentes e ordenados; `flag_extrapolacao` numérico/bool presente.
   - `test_alunos_parceiras_nunca_preditor`: `sanity_check_parceiras` retorna `usado_na_curva=False`; a validação da curva não recebe `alunos_parceiras` como coluna de X (assert que o X só contém metragem/marca).
   - `test_isolamento_import`: `import motor_expansao.demanda_revelada.calibracao_curva` OK e o módulo não importa `pipelines.m1`/`dashboard`/`censo`/`api` (grep do source ou checagem de `sys.modules`).
   - `test_ic_bootstrap_deterministico`: mesma seed → mesmo IC (reprodutibilidade).
   - `test_go_nogo_criterio`: fixture com sinal forte → `go=True`; fixture ruído → `go=False` (IC cruza zero).
   - `test_relatorio_cria_arquivo`: `escrever_relatorio` grava .md não-vazio em `tmp_path`.
9. **`data/analysis/calibracao_curva_densidade.md`** — relatório gitignored (§Relatório).
10. **Backlog/completed/current_task**: mover BLK-TP-04 para concluído (`tasks/backlog.md` + `tasks/completed.md`) após QA; `tasks/current_task.md` atualizado pela esteira.

## Estrutura do relatório `data/analysis/calibracao_curva_densidade.md`
1. Cabeçalho: READ-ONLY M1 (DEC-001/008/009/012); sem PII (só agregados).
2. **Dados**: base com m² (N, por marca, envelope de metragem, densidade mediana por marca).
3. **Papel de `alunos_parceiras`** (D1-A): distribuição alunos/unidade por faixa de n_acad; disclaimer de escala incomparável (~16 vs ~2.700); **NÃO entra na curva**.
4. **Validação da curva (DEC-008)**: tabela k-fold 5×5 vs baseline — R²_oof (IC95), rho_oof (IC95), alpha; pooled vs marca-dummy; coorte Ultra (N=54) como robustez.
5. **Intervalos de predição** p10/p50/p90 por faixa de m² + regra de `flag_extrapolacao`.
6. **Veredito GO/NO-GO explícito** (D7) e leitura honesta (esperado GO fraco/NO-GO dado DIM-07 ≈ +0,096).
7. **Limitações**: N=112 pequeno, heterogeneidade Eng×Ultra, SkyFit sem m², parceiras sem m²/tipo, ~90% da variância de densidade fora do que temos (DIM-07).
8. **Recomendação**: se validar → recalibração dos coeficientes é FOLLOW-UP com gate próprio (NÃO aplicar aqui).

## Arquivos afetados
- `src/motor_expansao/demanda_revelada/calibracao_curva.py` — NOVO.
- `tests/unit/demanda_revelada/test_calibracao_curva.py` — NOVO (+ `tests/unit/demanda_revelada/__init__.py` se ausente).
- `data/analysis/calibracao_curva_densidade.md` — NOVO (gitignored).
- `tasks/backlog.md`, `tasks/completed.md`, `tasks/current_task.md` — bookkeeping.
- `context/handoff.md` + `context/handoff/20260702-115546-planner.md`.

## Dependências
- `numpy`, `pandas`, `pyarrow`, `h3`, `scipy`, `scikit-learn` — TODAS já base (`pyproject.toml`). **Nenhuma dep nova.**
- Reuso permitido: `dimensionamento.aderencia._r2_loo_para_alpha`, `dimensionamento.backtest_dim._r2`/`_rmse`, `dimensionamento.aderencia.ALPHA_GRID` (precedente BLK-TP-05).
- `demanda_revelada.contrato.COLUNAS_PII_PROIBIDAS` para o teste anti-PII.
- Leitura read-only: os 3 parquets de staging (nenhum é reescrito).

## Riscos técnicos
1. **N=112 pequeno** → k-fold 5×5 pode ter folds instáveis; IC largo. Mitigação: `RepeatedKFold` (25 folds efetivos) + bootstrap; fallback LOO documentado; NO-GO é resultado válido.
2. **Escala incomparável parceiras×clubes** (evidência) → risco de leitura enganosa se D1 escorregar para B/C. Mitigação: D1-A trava parceiras como descritivo com disclaimer.
3. **Confound de marca** (Eng 2,31 vs Ultra 1,57) → risco de curva pooled enviesada. Mitigação: D4-A (estratificar + dummy + reportar ambos).
4. **Tentação de recalibrar coeficientes** → PROIBIDO neste bloco (guardrail explícito §Alerta). Só documenta/recomenda; recalibração = follow-up com gate.
5. **Vazamento de escala/leak em CV** → usar features log cruas sem scaler dependente de fold (padrão de `_r2_loo_para_alpha`).
6. **R² in-sample vazar no output** → teste dedicado `test_loo_honesto_sem_r2_insample` (assert ausência no result e no .md).

## Critérios de aceite finais
1. `calibracao_curva.py` criado; funções puras; valida a curva na base com m² (N≈112) por k-fold 5×5 vs baseline com IC95 bootstrap (seed=42), intervalos p10/p50/p90 e `flag_extrapolacao`; `alunos_parceiras` só como sanity-check (`usado_na_curva=False`).
2. Relatório `calibracao_curva_densidade.md` com veredito GO/NO-GO explícito (D7) e limitações.
3. Testes cobrindo: anti-PII, LOO honesto sem R² in-sample, intervalos presentes, `alunos_parceiras` nunca preditor, isolamento de import, IC determinístico, GO/NO-GO.
4. Suíte completa verde (0 falhas, 0 erros de coleta).
5. **mtime dos 4 artefatos oficiais M1 INALTERADO** (`brasil_estrutural`, `brasil_priorizados`, `hexagonos_brasil_oportunidades`, `hexagonos_brasil_dashboard`).
6. `import streamlit_app` OK; nenhuma dep nova além de numpy/scipy/pandas/pyarrow/h3/sklearn (base).
7. `viabilidade_ponto.py` / `faixa_alunos_por_densidade` INALTERADO.

## Validações obrigatórias
- Comando: `python -m pytest -q tests/unit/demanda_revelada/` — Critério: 0 falhas.
- Comando: `python -m pytest -q` (suíte completa) — Critério: 0 falhas, 0 erros de coleta.
- Comando: `python -c "import streamlit_app"` — Critério: 0 erro (smoke).
- Comando (mtime M1): `python -c "import os; [print(f, os.path.getmtime('data/staging/'+f)) for f in ['brasil_estrutural.parquet','brasil_priorizados.parquet','hexagonos_brasil_oportunidades.parquet','hexagonos_brasil_dashboard.parquet']]"` antes/depois — Critério: mtimes idênticos (nenhum artefato M1 regenerado).
- Comando (isolamento): `python -c "import motor_expansao.demanda_revelada.calibracao_curva as m; import inspect; src=inspect.getsource(m); assert not any(k in src for k in ['pipelines.m1','dashboard','censo_','motor_expansao.api'])"` — Critério: sem import proibido.

## Fora de escopo
- Alterar `score_priorizacao`, `hex_score_estrutural`, pesos (renda=0.40/pop=0.60), carteira, plano, artefatos oficiais do M1.
- **Alterar `viabilidade_ponto.py` / `faixa_alunos_por_densidade` ou seus coeficientes em produção** — recalibração é FOLLOW-UP com gate próprio.
- Injetar `alunos_parceiras` (m² nulo) na base de calibração / inferir m² das parceiras (fabricar dado).
- Reintroduzir regressão geográfica de demanda (pop/renda/concorrência/lat/lng) como preditor da curva (DEC-009).
- Novas deps pesadas; UI/dashboard; método de interseção setorial / raio 1,5 km / parâmetros M1 §3.
- Escrever novo parquet de staging (bloco é análise; sem artefato de dados novo).

## Alerta de aprovação
**ATENÇÃO: ESTA TAREFA É DE MODELAGEM (Alta criticidade) E EXIGE APROVAÇÃO HUMANA DE FELIPE ANTES DO BUILDER.** É READ-ONLY sobre o M1 (não toca `score_priorizacao`, pesos, carteira, plano nem artefatos oficiais), MAS envolve DECISÕES DE MÉTODO (D1–D7 acima, em especial D1 = papel de `alunos_parceiras` sem m², e D7 = critério GO/NO-GO). **FELIPE DEVE LER E CONFIRMAR/AJUSTAR AS DECISÕES D1–D7 ANTES DE O BUILDER IMPLEMENTAR.** O Builder NÃO pode alterar os coeficientes da curva em produção — se a validação sugerir recalibração, isso vira follow-up com novo gate.

## Guardrails ativos
- **§5 CLAUDE.md (READ-ONLY M1):** `score_priorizacao`, `hex_score_estrutural`, pesos, carteira, plano e artefatos oficiais INTOCÁVEIS; nenhum mtime M1 pode mudar.
- **DEC-008:** LOO/k-fold vs baseline da média; R² in-sample BANIDO dos outputs; intervalos de predição + flag de extrapolação obrigatórios; IC95 bootstrap seed fixa=42.
- **DEC-009:** demanda é insumo OBSERVADO, NUNCA preditor geográfico de magnitude; a curva prediz alunos a partir de `m²` SOMENTE (proibido lat/lng/pop/renda como preditor).
- **DEC-012 (anti-PII):** consumir só a camada agregada (`alunos_parceiras`/`n_acad_parceiras`); `COLUNAS_PII_PROIBIDAS` ausentes; testes com fixture sintética (nunca a fonte real `NAO_ABRA/`).
- **§2:** sem API ao vivo / dependência de rede no caminho de calibração.
- **§6 (VPS):** zero comandos na VPS sem confirmação explícita do usuário.
- **Parâmetros canônicos §3 imutáveis** sem DEC: H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, renda=0.40/pop=0.60.
