# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Builder   (criticidade Média ⇒ sem gate humano)

## Entendimento da tarefa
Eliminar o `MessageSizeError` em UFs grandes elevando `server.maxMessageSize` para 500 no
`.streamlit/config.toml` e enxugando o DataFrame serializado ao `H3HexagonLayer` (pydeck) para
conter SOMENTE as colunas que o render e o tooltip consomem, sem tocar score/carteira/plano/M1.

## Evidências confirmadas no código (arquivo:linha)
- `H3HexagonLayer` consome APENAS 3 colunas via accessors string: `get_hexagon="hex_id"`,
  `get_fill_color="fill_color"`, `get_line_color="line_color"`:
  - M1: components.py:1092-1106 (`data=map_df`)
  - Híbrido: components.py:1358-1372 (`data=map_df`)
  - Residual: components.py:1475-1489 (`data=map_df`)
- Tooltip HTML referencia APENAS `{tooltip_title}` e `{tooltip_line_1..14}`:
  - `_shared_map_tooltip` (components.py:772-798) usa `tooltip_title` + `tooltip_line_1..14`.
  - `_hybrid_compact_tooltip` (components.py:808-829) usa `tooltip_title` + `tooltip_line_1..8`.
  - M1 sempre usa `_shared_map_tooltip` (14 linhas) → manter `tooltip_line_1..14`.
  - Híbrido/Residual: `_HYBRID_TOOLTIP_SHOW_DETAIL=False` (components.py:805) ⇒ usa o compacto
    (8 linhas), mas `_apply_hex_tooltip_fields(mode="hybrid")` só cria `tooltip_line_1..8` nesse
    caso (components.py:454-458). Para ser robusto a flip da flag, o keep-set deriva as colunas
    `tooltip_*` REALMENTE PRESENTES no DataFrame (ver passo 3), não uma lista fixa.
- O conjunto `tooltip_title` + `tooltip_line_*` é a ÚNICA saída textual consumida; todas as
  auxiliares (`*_fmt`, `*_label`, `tooltip_residual_1..3`, `score_*_fmt`, `confianca_geografica`,
  e todas as `MAP_SOURCE_COLUMNS_*` ~30/~33) são insumo intermediário e NÃO são lidas pelo deck.
- Layers de pins (competidores/Ultra) e o layer de busca JÁ são enxutos e estão FORA do escopo:
  - `_build_competitor_icon_layer` (components.py:688+) e `_build_ultra_icon_layer` constroem
    DataFrames próprios, filtrados ao viewport, com poucas colunas (icon/tooltip). Não regredir.
  - `_search_hex_payload` (components.py:900-915) já projeta para `["hex_id", tooltip_title,
    tooltip_line_1..14]` antes do `_build_search_hex_layer` (918-932). Já enxuto. Não tocar.
- O cap de 35k (`_downsample_map_index`, components.py:960-983; `MAP_POINT_LIMIT`,
  constants.py:98) atua ANTES da materialização — o estouro é LARGURA (colunas), não altura.
- ATENÇÃO regressão de teste: `test_build_map_figure_mostra_todos_os_hexes_validos_da_uf_sem_cap_editorial`
  (test_streamlit_app.py:493) hoje afirma `rendered["confianca_geografica"]` em `deck.layers[0].data`.
  Como `confianca_geografica` NÃO é consumida pelo deck, o enxugamento a removeria e quebraria esse
  assert. O Builder DEVE atualizar esse assert (ver passo 6) — é a única regressão de teste prevista.

## Plano técnico
1. **config.toml** — `.streamlit/config.toml`: adicionar `maxMessageSize = 500` dentro do bloco
   `[server]` existente (após a linha `enableXsrfProtection = true`, linha 6). Não criar bloco novo,
   não mexer em browser/client.

2. **Helper de keep-set determinístico** — em `components.py`, criar uma função pura, perto de
   `_downsample_map_index` (≈ após a linha 983), por ex.:
   ```python
   # Colunas que o H3HexagonLayer/tooltip do pydeck realmente consomem. As demais
   # (auxiliares *_fmt/*_label/tooltip_residual_*, colunas-fonte) sao insumo intermediario
   # e NAO entram no payload serializado ao frontend (corrige MessageSizeError em UFs grandes).
   _DECK_RENDER_COLUMNS = ("hex_id", "fill_color", "line_color")

   def _deck_layer_frame(map_df: pd.DataFrame) -> pd.DataFrame:
       """Projeta map_df para SOMENTE as colunas consumidas pelo H3HexagonLayer
       (hex_id/fill_color/line_color) + as colunas de tooltip (tooltip_title,
       tooltip_line_*) realmente presentes. Nao muta map_df (o df completo segue
       alimentando a busca por hex fora do recorte). Nao recalcula score."""
       tooltip_cols = [
           c for c in map_df.columns
           if c == "tooltip_title" or c.startswith("tooltip_line_")
       ]
       keep = [c for c in (*_DECK_RENDER_COLUMNS, *tooltip_cols) if c in map_df.columns]
       # dedup preservando ordem
       seen: set[str] = set()
       ordered = [c for c in keep if not (c in seen or seen.add(c))]
       return map_df.loc[:, ordered].copy()
   ```
   Justificativa de derivar (em vez de listar em constants.py): o conjunto de `tooltip_line_*`
   muda conforme `_HYBRID_TOOLTIP_SHOW_DETAIL` e conforme o modo (m1=14, híbrido compacto=8).
   Derivar das colunas presentes garante que o tooltip nunca quebre se a flag/linhas mudarem.

3. **Aplicar em `build_map_figure`** — components.py:1092-1094. Antes de instanciar o
   `pdk.Layer("H3HexagonLayer", ...)`, calcular `layer_df = _deck_layer_frame(map_df)` e passar
   `data=layer_df` (não `data=map_df`). NÃO mutar `map_df` (ainda usado por
   `_build_competitor_icon_layer(... map_df)`, `_build_ultra_icon_layer(... map_df)`,
   `resolve_map_view(map_df, ...)` e centro/zoom — todos precisam das colunas lat/lng/uf etc.).
   Apenas o argumento `data=` do `H3HexagonLayer` muda.

4. **Aplicar em `build_hybrid_map_figure`** — components.py:1358-1360. Mesmo padrão:
   `layer_df = _deck_layer_frame(map_df)`; `data=layer_df` no `H3HexagonLayer`. `map_df` segue
   intacto para competitor/ultra/resolve_map_view.

5. **Aplicar em `build_residual_heatmap_figure`** — components.py:1475-1477. Mesmo padrão
   (o handoff do Block Orchestrator autoriza "residual/domínio se aplicável"; este builder usa o
   mesmo `H3HexagonLayer` com `data=map_df`, então entra no fix por consistência). Não há builder
   de domínio com `H3HexagonLayer` no escopo lido — confirmar via grep antes; se existir e usar o
   mesmo padrão, aplicar igualmente.

6. **Corrigir o assert que lê coluna não-render** — test_streamlit_app.py:493:
   `assert rendered["confianca_geografica"].tolist() == ["granular", "granular", "granular"]`.
   Como `confianca_geografica` deixa de ser serializada ao layer, trocar esse assert por uma
   verificação equivalente que NÃO dependa de coluna-fonte no payload — opção recomendada: validar
   a contagem/identidade dos hexes (já feito nas linhas 491-492) e mover a checagem de
   `confianca_geografica` para o frame completo se necessário, OU simplesmente remover essa linha
   (491-492 já garantem o recorte correto de 3 hexes granulares). Builder escolhe a forma mínima
   que mantém a intenção do teste sem reintroduzir a coluna no payload.

7. **Novo teste** — adicionar em test_streamlit_app.py (perto dos outros `build_map_figure`,
   ≈ após a linha 628), exercitando o builder de verdade (NO-BYPASS):
   - `test_build_map_figure_payload_do_layer_so_tem_colunas_de_render_e_tooltip`:
     monta um `df` (reusar `_hex_row`), chama `streamlit_app.build_map_figure(df, selected_ufs=[...])`,
     pega `rendered = pd.DataFrame(deck.layers[0].data)` e assere:
       - `set(rendered.columns) <= {"hex_id","fill_color","line_color","tooltip_title",
         *[f"tooltip_line_{i}" for i in range(1,15)]}` (subconjunto do keep-set);
       - colunas auxiliares AUSENTES: `"score_priorizacao_fmt" not in rendered.columns`,
         `"confianca_geografica" not in rendered.columns`, `"tooltip_residual_1" not in
         rendered.columns`, `"faixa_label" not in rendered.columns`,
         `"renda_per_capita" not in rendered.columns`;
       - colunas de render PRESENTES: `{"hex_id","fill_color","line_color","tooltip_title"}
         <= set(rendered.columns)`;
       - tooltip preservado: `rendered.loc[0, "tooltip_title"]` é string não vazia.
   - Análogo para o híbrido:
     `test_build_hybrid_map_figure_payload_do_layer_enxuto` (reusar o `hybrid_row` do teste
     existente :1031), assertando o mesmo subconjunto e ausência de `score_censo_fmt`,
     `densidade_pop_setor_hab_km2`, `tooltip_residual_2`.
   - (Opcional, se baixo custo) idem residual reusando `residual_row` (:1109).

## Arquivos afetados
- `.streamlit/config.toml`
- `src/motor_expansao/dashboard/components.py` (novo helper + 2-3 trocas `data=`)
- `tests/integration/test_streamlit_app.py` (1 assert ajustado + 2-3 testes novos)
- `tasks/current_task.md` · `tasks/backlog.md` · `tasks/completed.md`
- `context/handoff.md` · `context/handoff/`

## Dependências
- Nenhuma nova dependência. Usa pandas/pydeck já presentes.
- O helper deve ficar ANTES dos builders no arquivo (Python resolve em runtime, mas manter coeso).

## Riscos técnicos
- REGRESSÃO PREVISTA (única): assert de `confianca_geografica` em test_streamlit_app.py:493 —
  tratada no passo 6. Sem isso, a suite quebra.
- Mutar `map_df` por engano (ex.: `map_df = map_df[keep]`) quebraria competitor/ultra/resolve_view
  que precisam de lat/lng/uf — usar variável nova `layer_df`, nunca reatribuir `map_df`.
- Tooltip quebrar se a derivação não pegar todas as `tooltip_line_*` — por isso derivar por
  `startswith("tooltip_line_")` em vez de range fixo.
- Layers de pins/busca: NÃO tocar; já enxutos. Não passar `layer_df` a eles.

## Critérios de aceite finais
- `.streamlit/config.toml` tem `maxMessageSize = 500` dentro de `[server]`.
- `pd.DataFrame(deck.layers[0].data)` (M1, híbrido, residual) contém SOMENTE
  `hex_id/fill_color/line_color/tooltip_title/tooltip_line_*` — sem auxiliares nem colunas-fonte.
- Cap `MAP_POINT_LIMIT=35000` e a chave de ordenação/dedup do `_downsample_map_index` inalterados.
- Conteúdo dos tooltips visíveis idêntico (mesmos `tooltip_line_*`).
- `map_df` não é mutado; busca por hex fora do recorte e pins de competidor/Ultra intactos.
- Nenhum valor de score/carteira/plano/artefato M1 alterado.

## Validações obrigatórias
- Comando: `pytest -q tests/integration/test_streamlit_app.py`
  - Critério mínimo: verde, incluindo os 2-3 testes novos e o assert ajustado.
- Comando: `pytest -q`
  - Critério mínimo: sem regressões vs. baseline (`532 passed, 1 skipped`; se o ambiente
    reproduzir 567, usar esse número — não pode haver testes a MENOS passando nem novos falhando).
- Comando: `python -c "import streamlit_app"`
  - Critério mínimo: import sem erro (app carrega).

## Fora de escopo
- Recalcular/alterar `score_priorizacao`, `hex_score_estrutural`, `score_expansao_hibrido`,
  carteira, plano curto prazo, plano de domínio ou artefatos oficiais do M1.
- Alterar `MAP_POINT_LIMIT`, `MAP_SORT_COLUMNS`/ascending, ou a lógica de `_downsample_map_index`.
- Alterar conteúdo de tooltips visíveis ou layers de pins (competidor/Ultra) e de busca.
- Mexer em pipelines M1, geometria censitária, geração de Parquets.
- Commitar `CLAUDE.md` (M) ou `PROMPT-portar-run-cycle.md` (??). Commit só por path.

## Alerta de aprovação
Sem aprovação humana necessária (Média). Builder pode prosseguir.
O plano NÃO toca colunas de score nem recalcula nada: apenas PROJETA (seleção de colunas) o frame
já calculado antes de serializar ao pydeck. Se durante a implementação o Builder perceber que
enxugar exige tocar/recalcular QUALQUER coluna de score (`score_priorizacao`,
`hex_score_estrutural`, `score_expansao_hibrido`) ou artefato M1, **ESCALAR PARA CRÍTICA E PARAR**.

## Guardrails ativos
- CLAUDE.md §5 (Guardrail permanente): visualizações, análise radial e interações de mapa NÃO
  podem recalcular ou alterar `score_priorizacao`, `hex_score_estrutural`, carteira, plano curto
  prazo, plano domínio ou artefatos oficiais do M1 sem aprovação explícita.
- CLAUDE.md §5 (Bloco 6 — Fonte de mapa enxuta): o cap (`MAP_POINT_LIMIT`) e a chave de
  ordenação/dedup do downsample atuais devem ser preservados (mesmos top-N por prioridade).
- CLAUDE.md §2: toda mudança relevante entra com teste; nenhum PR sobe com CI quebrado.
- Commit SÓ por path; NUNCA `git add -A`. `CLAUDE.md` (M) e `PROMPT-portar-run-cycle.md` (??) do
  worktree pré-sujo NÃO entram neste ciclo.
