# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Campos de controle
- Skill atual: Builder
- Próxima Skill: QA
- Bloco: BLK-ARCH-01b — Tipar os 14 módulos migrados e remover o override de mypy
- Criticidade: Média
- Branch ativo: `ciclo/BLK-ARCH-01b`
- Status: aguardando QA

## Bloco executado
BLK-ARCH-01b — Corrigir os 50 erros de tipo dos 14 módulos de `src/motor_expansao/pipelines/`
hoje silenciados pelo `[[tool.mypy.overrides]]` (`ignore_errors = true`) e REMOVER esse override,
sem alterar comportamento/valores/score/artefatos M1.

## O que foi feito (por lote/módulo)

### Passo 0 — baseline (PRÉ)
- Branch confirmado `ciclo/BLK-ARCH-01b`; HEAD `e207d30`; worktree limpo (só arquivos de controle do ciclo).
- sha256 PRÉ dos 4 artefatos M1 capturado (ver prova de hash) — bate com o esperado no plano.
- Override removido TEMPORARIAMENTE + `mypy src/` → confirmou EXATAMENTE `Found 50 errors in 12 files (checked 44 source files)`, idêntico à enumeração do Planner.
- Baseline pytest: a 1ª execução acusou 1 falha FLAKY em `test_artefatos_oficiais_m1_mantem_invariantes[artifact_path3]` (artefato de isolamento/ordenação — passou isolada `5 passed` e na 2ª/3ª execução completa). Baseline REAL confirmado no estado entregue (override restaurado via stash): **541 passed, 1 skipped, 9 warnings**.
- Paridade CI/local: `.github/workflows/ci.yml` usa Python 3.11 + `pip install -e .[dev]` (sem pandas-stubs) e roda `mypy src/`. Ambiente local: mypy 2.1.0 / pandas 2.3.3 / types-requests, SEM pandas-stubs — MESMA config do CI. Os 50 erros são stub-independentes (object/float|None/numpy). Nenhum stub adicionado.

### Política aplicada (resumo)
- Grupo A: `no-redef` de fallback `try/except import` → `# type: ignore[no-redef]` no import do ramo `except` (sem tocar o `try`/fluxo); `[assignment]` de `var = None` (dict literal) → anotação `dict[str, Path | None]`; `[var-annotated]` de dicts acumuladores → `dict[str, Any]` / `dict[str, dict[str, Any]]` (com `from typing import Any` onde faltava); `np.where(cond, arr, None)` → `# type: ignore[call-overload]` pontual (ramo `None` intencional, NÃO trocado).
- Grupo B: coerção `float/int(object)` viva → `# type: ignore[CÓDIGO]` PONTUAL no site exato (códigos: arg-type, call-overload, str-format, attr-defined, index, misc, return-value), seguindo a convenção já existente nos módulos. Sem reescrever lógica; o único reformat foi quebrar `_coord_valida` em multi-linha para anexar os 2 ignores (mesma expressão lógica).
- Grupo C: `validar_fase_a_censo2022:982` join(object) → `# type: ignore[arg-type]`. `gerar_carteira_acionavel:157` `default` indefinido NÃO tocado (bug de runtime latente, fora de escopo).

### Lote 1 — Trio Fase A + validar_fase_a
- `fase_a_censo2022_setores.py`: `+from typing import Any`; no-redef :36 (ignore no except import); `resultado = {...}` → `resultado: dict[str, Path | None] = {...}` (cobre :720/723/730/738); `info_processamento = {"por_uf": {}}` → `: dict[str, Any]` (×2: :778/:1192).
- `validar_fase_a_censo2022.py`: no-redef :34 (×11) → `# type: ignore[no-redef]` no `from fase_a_censo2022_setores import (` do except; :982 join → `# type: ignore[arg-type]`.
- `fase_a_piloto_expandido.py`: :142/157/159 `int(object)` → `[call-overload]`; :570 → `[str-format]`; :610/803 → `[attr-defined]`; :805 → `[index]`.
- `fase_a_nacional_completo.py`: :59 → `[return-value]`; :185 → `[misc]`; :193 → `[arg-type, return-value]`; :195 → `[str-format]`; :207/208 → `[arg-type]`; :403 → `[misc, attr-defined]`; :404 → `[arg-type]`.

### Lote 2 — Geo censitário
- `materializar_setores_censitarios_geo.py`: no-redef :29 (×2) → except import reescrito em forma parentizada com `# type: ignore[no-redef]` (sem tocar `try`/fluxo). mypy isolado do módulo: Success.

### Lote 3 — Dupla penetração
- `validar_penetracao_ultra_hex.py`: :110/116/123 `float(object)` → `# type: ignore[arg-type]`.
- `calcular_penetracao_ultra_hex.py`: já limpo (não estava nos 50) — só saiu do override (removido por inteiro).

### Lote 4 — Mercado/enriquecimento/geofusion
- `enriquecimento_espacial_hexagonos.py`: :134 `np.where(cond, redes_abc[argmax], None)` → `# type: ignore[call-overload]` (ramo `None` preservado).
- `comparar_geofusion_vs_hex.py`: :123 `float(object)` → `# type: ignore[arg-type]`.
- `calcular_colunas_mercado.py`: já limpo — só saiu do override.

### Lote 5 — Ultra/carteira/híbrido/validação
- `normalizar_unidades_ultra.py`: :158 `_coord_valida` reescrito em multi-linha (mesma lógica) com `# type: ignore[arg-type]` nos dois `float(...)`.
- `gerar_carteira_acionavel.py`: :146 `int(float(value))` e :155 `float(value)` → `# type: ignore[arg-type]`. (:157 `default` latente NÃO consertado.)
- `modelo_hibrido_expansao.py`: :102/110 `float(float|None)` → `# type: ignore[arg-type]`.
- `validar_modelo_ultra.py`: `+from typing import Any`; :265 `resultados: dict[str, dict[str, Any]] = {}`; :286 `resultado: dict[str, Any] = {}`.

### Passo final
- Bloco `[[tool.mypy.overrides]]` INTEIRO removido do `pyproject.toml` (+ módulos + `ignore_errors=true` + comentário-cabeçalho órfão das linhas ~102-106). `[tool.mypy]` global INTACTO (`strict=false`, `ignore_missing_imports=true` preservados — NÃO relaxados).

## Arquivos alterados
- `pyproject.toml` — removido o bloco `[[tool.mypy.overrides]]` (24 linhas a menos no diff). Resto inalterado.
- `src/motor_expansao/pipelines/fase_a_censo2022_setores.py` — `+from typing import Any`; ignore no-redef no except import; `dict[str, Path | None]`; `dict[str, Any]` (×2).
- `src/motor_expansao/pipelines/validar_fase_a_censo2022.py` — ignore no-redef no except import (cobre 11 nomes); ignore arg-type no join.
- `src/motor_expansao/pipelines/fase_a_piloto_expandido.py` — 7 ignores pontuais (call-overload/str-format/attr-defined/index).
- `src/motor_expansao/pipelines/fase_a_nacional_completo.py` — 8 ignores pontuais (return-value/misc/arg-type/str-format/attr-defined).
- `src/motor_expansao/pipelines/materializar_setores_censitarios_geo.py` — except import parentizado com ignore no-redef.
- `src/motor_expansao/pipelines/validar_penetracao_ultra_hex.py` — 3 ignores arg-type.
- `src/motor_expansao/pipelines/enriquecimento_espacial_hexagonos.py` — 1 ignore call-overload no `np.where`.
- `src/motor_expansao/pipelines/comparar_geofusion_vs_hex.py` — 1 ignore arg-type.
- `src/motor_expansao/pipelines/normalizar_unidades_ultra.py` — `_coord_valida` multi-linha com 2 ignores arg-type (mesma lógica).
- `src/motor_expansao/pipelines/gerar_carteira_acionavel.py` — 2 ignores arg-type.
- `src/motor_expansao/pipelines/modelo_hibrido_expansao.py` — 2 ignores arg-type.
- `src/motor_expansao/pipelines/validar_modelo_ultra.py` — `+from typing import Any`; 2 anotações de dict.
- `tasks/current_task.md` — Skill atual → Builder; status aguardando QA.
- `context/handoff.md` + snapshot `context/handoff/20260530-022134-builder.md`.

`calcular_colunas_mercado.py` e `calcular_penetracao_ultra_hex.py`: NÃO alterados (já limpos); só deixaram de existir no override.

## Validações executadas (saída literal)
1. `python -m mypy src/`  (override REMOVIDO):
   ```
   Success: no issues found in 44 source files
   ```
   `grep -c "tool.mypy.overrides" pyproject.toml` → `0` (override NÃO existe mais).
2. `python -m pytest -q`:
   ```
   541 passed, 1 skipped, 9 warnings in 105.78s (0:01:42)
   ```
   (idêntico ao baseline)
3. `ruff check .`:
   ```
   All checks passed!
   ```
4. `python -c "import streamlit_app; print('STREAMLIT_IMPORT_OK')"`:
   ```
   STREAMLIT_IMPORT_OK
   ```
5. sha256 dos 4 artefatos M1 — PRÉ vs PÓS (4/4 IDÊNTICOS, batem com o esperado no plano):
   - `data/staging/brasil_priorizados.parquet`            = `c226954945ad0757a0429c84c43f410492c0ea7d15ca1d9b6a15f68727806567` (PRÉ=PÓS ✅)
   - `data/staging/brasil_estrutural.parquet`             = `7baa07a2cbc0b7d8f2a8878932cae0ebf9400fce00e9c48c366c9903215f131b` (PRÉ=PÓS ✅)
   - `data/staging/hexagonos_brasil_oportunidades.parquet`= `805c65e28adfd800c7d5524e73fa9b0a044b992f17d617d0f2c6e40f9bbf61ca` (PRÉ=PÓS ✅)
   - `data/outputs/hexagonos_brasil_dashboard.parquet`    = `0cfb1015fc9df8b63776eb07ae3e666766905f768a4a78a406ae4d6b7cb6f618` (PRÉ=PÓS ✅)
6. `git diff --stat`:
   ```
   pyproject.toml                                       | 24 ---
   src/motor_expansao/pipelines/comparar_geofusion_vs_hex.py            |  2 +-
   src/motor_expansao/pipelines/enriquecimento_espacial_hexagonos.py    |  2 +-
   src/motor_expansao/pipelines/fase_a_censo2022_setores.py             |  8 +-
   src/motor_expansao/pipelines/fase_a_nacional_completo.py             | 16 +-
   src/motor_expansao/pipelines/fase_a_piloto_expandido.py              | 14 +-
   src/motor_expansao/pipelines/gerar_carteira_acionavel.py             |  4 +-
   src/motor_expansao/pipelines/materializar_setores_censitarios_geo.py |  6 +-
   src/motor_expansao/pipelines/modelo_hibrido_expansao.py              |  4 +-
   src/motor_expansao/pipelines/normalizar_unidades_ultra.py            |  8 +-
   src/motor_expansao/pipelines/validar_fase_a_censo2022.py             |  4 +-
   src/motor_expansao/pipelines/validar_modelo_ultra.py                 |  8 +-
   src/motor_expansao/pipelines/validar_penetracao_ultra_hex.py         |  6 +-
   (+ context/handoff.md, tasks/current_task.md, context/handoff/<snapshots>)
   ```
   Só os 12 módulos com erro + pyproject.toml + controle. Nenhum teste alterado. Nada fora de escopo.

## Confirmação do override
REMOVIDO por inteiro. `grep -c "tool.mypy.overrides" pyproject.toml` → `0`. Config `[tool.mypy]` global intacta (strict=false, ignore_missing_imports=true).

## Problemas encontrados
- A 1ª execução de `pytest -q` (logo após o Passo 0) reportou 1 falha em `test_artefatos_oficiais_m1_mantem_invariantes[artifact_path3]`. Investigado: FLAKY/ordem-dependente — passa isolada (`5 passed`) e nas execuções completas seguintes (541 passed). NÃO relacionado às mudanças deste ciclo. Registrado para o QA.

## Pendências
- Nenhuma para o escopo deste ciclo.

## Riscos remanescentes
- Paridade de stubs: ambiente local replica a config do CI (sem pandas-stubs). Se o CI eventualmente instalar pandas-stubs, podem surgir erros ADICIONAIS de Series não cobertos aqui — improvável (CI atual não instala). Risco baixo.
- `gerar_carteira_acionavel.py:157` — `default` indefinido no `except` de `_safe_float` é bug de runtime LATENTE (NameError se a exceção for atingida). Deixado INTACTO por escopo (Grupo C). Não é regressão deste ciclo.

## Guardrails verificados
- score_priorizacao não alterado: sim (não tocado; hashes M1 idênticos)
- Artefatos M1 preservados: sim (4/4 sha256 byte-idênticos pré/pós)
- Dashboard offline mantido: sim (import streamlit_app ok; nenhuma mudança de runtime)
- Correção mecânica, sem mudar comportamento/valores/dtype/ordenação: sim
- `# type: ignore` apenas pontual e com código específico; ZERO blanket ignore; config global de mypy inalterada: sim
- Commit por path: pendente (responsabilidade do orquestrador no fechamento; Builder NÃO commitou)
- Parâmetros canônicos imutáveis (H3=7, DIST=1.0, RENDA=4500, pesos 0.40/0.60): não tocados
