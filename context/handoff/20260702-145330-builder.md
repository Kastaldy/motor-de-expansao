# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Gate humano — registro obrigatório
Usuário APROVOU em 2026-07-02 (D1–D4 = recomendações do Planner):
- **D1** DEDUP por `hex_id` (overlap) SÓ no relatório — não subtrai oferta, não marca flag no parquet.
- **D2** aceitar ruído de coords ~1 km e documentar (mesmo caveat do BLK-TP-01; sem arredondamento extra).
- **D3** contrato COM distribuição por `Plano` (11 colunas `n_plano_<tp>`); `Total_Alunos_Cluster` PROIBIDO de somar.
- **D4** só INGERE + relatório; NÃO integra ao `score_oportunidade_residual`/`oferta_efetiva_disponivel`.
READ-ONLY sobre o M1.

## Bloco executado
BLK-TP-08 — Ingestão anti-PII das academias menores (WellHub/TotalPass) na camada de oferta

## O que foi feito
- Módulo novo isolado `src/motor_expansao/demanda_revelada/oferta_academias_menores.py` (pacote paralelo; NÃO importa de `pipelines/m1`, `dashboard`, `censo_*`, `api`):
  - `ingerir_oferta_academias_menores(fonte, destino, *, escrever=True)`: lê `03_Competidores.xlsx` (openpyxl, dep base) → deriva `hex_id` res-7 de `Latitude`/`Longitude` na FRONTEIRA e IMEDIATAMENTE dropa `Latitude`/`Longitude`/`Nome_Academia`/`Cluster_ID` → agrega por `hex_id` (soma `Alunos_Academia`; `Total_Alunos_Cluster` NUNCA somado, nunca vira coluna) → distribuição por `Plano` (11 colunas) → `_assert_sem_pii` + `_assert_res7` → grava parquet.
  - `gerar_relatorio_qualidade(...)`: markdown de cobertura + DEDUP por `hex_id` (overlap) vs `concorrentes_mapeados` + % casado com o universo do Motor + caveat coords ~1 km + distribuição por `Plano`. Só agregados, ZERO PII.
  - Constantes: `VERSAO_CONTRATO_OFERTA_MENORES="oferta_menores_v1"`, `CONTRATO_COLUNAS_OFERTA_MENORES` (15 colunas), `_COLUNAS_PII_LOCAIS` (estende `COLUNAS_PII_PROIBIDAS` do contrato base com `latitude`/`longitude`/`nome_academia`/`cluster_id`/`total_alunos_cluster`).
- Exports adicionados em `src/motor_expansao/demanda_revelada/__init__.py`.
- Fixture SINTÉTICA sem PII `tests/fixtures/oferta_academias_menores_fake.xlsx` (5 linhas: 2 no mesmo hex SP, 1 SP-B, 1 RJ, 1 sem coord → dropada; cabeçalho corrompido `Munic�pio` como no real; `Total_Alunos_Cluster=500` p/ testar que NUNCA é somado).
- Testes `tests/unit/demanda_revelada/test_ingestao_oferta_menores.py` (10 testes).
- Nota curta em `docs/modelo_mercado_hexagonos.md` §7 (camada STAGED separada, NÃO integrada ao residual; DEDUP só quantificado).
- Gerado o parquet REAL `data/staging/oferta_academias_menores_h3.parquet` (gitignored) e o relatório `data/reports/scratch/oferta_academias_menores_qualidade.md` (gitignored) sobre o xlsx real.

## Contrato do parquet (prova anti-PII — colunas reais do artefato gerado)
`['hex_id', 'n_academias_menores', 'alunos_academias_menores', 'n_plano_tp0', 'n_plano_tp1', 'n_plano_tp1_plus', 'n_plano_tp2', 'n_plano_tp2_plus', 'n_plano_tp3', 'n_plano_tp4', 'n_plano_tp5', 'n_plano_tp5_plus', 'n_plano_tp6', 'n_plano_tp7', 'versao_contrato']`
- **PII presente: {} (conjunto VAZIO)** — nenhuma de `latitude`/`longitude`/`nome_academia`/`cluster_id`/`total_alunos_cluster`. `versao_contrato="oferta_menores_v1"`.

## Números reais de cobertura / DEDUP (do relatório gerado sobre o xlsx real)
- Academias menores agregadas: **24.045**; alunos (Σ `Alunos_Academia`): **1.920.955**; hexes res-7 distintos: **6.785**.
- Casam com o universo do Motor (`hexagonos_brasil_dashboard.parquet`): **6.731 / 6.785 (99,2%)**.
- Hexes de concorrentes mapeados (únicos): **1.659**; sobreposição: **1.425 hexes**.
- Academias menores em hex já coberto: **9.482 (39,4%)**; alunos em hex já coberto: **1.203.919 (62,7%)**.
- (Números batem exatamente com os medidos pelo BO/Planner.)

## Arquivos alterados
- `src/motor_expansao/demanda_revelada/oferta_academias_menores.py` (NOVO — contrato + ingestão anti-PII + relatório de qualidade/DEDUP).
- `src/motor_expansao/demanda_revelada/__init__.py` (adicionados exports `ingerir_oferta_academias_menores`, `gerar_relatorio_qualidade`, `CONTRATO_COLUNAS_OFERTA_MENORES`, `VERSAO_CONTRATO_OFERTA_MENORES`).
- `tests/unit/demanda_revelada/test_ingestao_oferta_menores.py` (NOVO — 10 testes).
- `tests/fixtures/oferta_academias_menores_fake.xlsx` (NOVO — fixture sintética SEM PII).
- `docs/modelo_mercado_hexagonos.md` (§7 — nota curta da camada STAGED não integrada ao residual).
- Gerados em runtime (gitignored, NÃO commitáveis): `data/staging/oferta_academias_menores_h3.parquet`, `data/reports/scratch/oferta_academias_menores_qualidade.md`.
- Bookkeeping: `context/handoff.md`, `context/handoff/20260702-145330-builder.md`, `tasks/current_task.md`.

## Validações executadas
- `python -m pytest -n auto -q tests/unit/demanda_revelada/ tests/integration/test_streamlit_app.py` → **241 passed, 0 failed, 0 skipped** (inclui os 10 novos + o pacote demanda_revelada existente + integração streamlit). Escolhido este subconjunto porque toca só o pacote `demanda_revelada` + o smoke obrigatório do dashboard.
- `python -c "import streamlit_app"` → **import ok**.
- `test_isolamento_import` (AST) → **1 passed** (o módulo não importa `pipelines.m1`/`dashboard`/`censo`/`motor_expansao.api`). Obs.: a verificação por substring bruto do handoff acusa falso-positivo porque a DOCSTRING de guardrail contém as palavras "dashboard"/"censo"; a verificação correta é por nós de import (AST), que passa.
- `ruff check` (módulo novo + teste) → **All checks passed**.
- `mypy` (módulo novo) → **Success: no issues found**.
- **mtime dos 4 artefatos M1 INALTERADO** (capturado ANTES e DEPOIS de rodar a ingestão real): `brasil_estrutural.parquet`, `brasil_priorizados.parquet`, `hexagonos_brasil_oportunidades.parquet` (staging) e `hexagonos_brasil_dashboard.parquet` (outputs) — todos idênticos (True/True/True/True).
- `git status --porcelain`: NÃO mostra nenhum artefato M1 nem `hexagonos_mercado_mapeado.parquet` modificado; o parquet novo e o relatório NÃO aparecem (gitignored, confirmado por `git check-ignore`).

## Problemas encontrados
Nenhum bloqueante. Único ponto de nota: a checagem de isolamento por substring do handoff é falso-positiva pela docstring; usei/validei o teste AST `test_isolamento_import` (recomendação explícita do Planner "ou o teste").

## Pendências
- Fechamento de `tasks/backlog.md` (BLK-TP-08 → concluído) e `tasks/completed.md` no encerramento pelo QA/orquestrador.
- Integração ao residual (subtração de oferta + DEDUP fino + capacidade por tipo/Huff) = follow-up BLK-TP-09 sob gate próprio (fora deste bloco por D4).

## Riscos remanescentes
- Dupla-contagem de oferta (39% acad / 63% alunos em hex já coberto) é conhecida e apenas QUANTIFICADA no relatório — não corrigida aqui (D1/D4).
- Coords ~1 km (~2 casas) geram ruído no join res-7 — documentado; camada é refino, não verdade fina.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (READ-ONLY; módulo isolado não toca M1).
- Artefatos M1 preservados: **sim** (mtime dos 4 oficiais INALTERADO, verificado antes/depois).
- `oferta_efetiva_disponivel`/`score_oportunidade_residual` NÃO recompostos: **sim** (parquets de mercado intocados).
- Dashboard offline mantido: **sim** (`import streamlit_app` ok; sem dependência de rede; sem dep nova — openpyxl já base).
- Anti-PII (DEC-012): **sim** — PII dropada na fronteira; parquet real com ZERO colunas PII; fonte real (`NAO_ABRA/`) e artefatos gerados gitignored; testes com fixture sintética.
- Isolamento de pacote: **sim** (AST test verde).
