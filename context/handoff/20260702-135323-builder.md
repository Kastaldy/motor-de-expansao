# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-TP-06 — Calibração/validação do score residual (`score_oportunidade_residual`) com a demanda revelada OBSERVADA (`membros`), out-of-fold (DEC-008). READ-ONLY sobre o M1.

## Gate humano (registro obrigatório)
APROVADO pelo usuário (Felipe Silva) em 2026-07-02, com as recomendações do Planner:
- **R4=(A):** validar SÓ com o já ingerido (`demanda_revelada_h3.parquet`); `03_Competidores.xlsx` adiado p/ BLK-TP-08 — NÃO ingerido neste bloco.
- **Alvo = `log1p(membros)`** (demanda paga OBSERVADA, cauda longa).
- **GO/NO-GO** = `R²_oof_log` como ÂNCORA (> 0,05 e IC95_inf > 0) + `rho_oof` como SUPORTE (≥ 0,30 e IC95_inf > 0). GO forte = âncora R²; GO só por rho é rotulado "alinhamento monotônico sem ganho de erro out-of-fold". NO-GO é resultado VÁLIDO.
- **`membros` = alvo único;** `alunos_parceiras`/`n_acad_parceiras` = OFERTA instalada → só covariável/cross-check, NUNCA alvo.

## O que foi feito
- Novo módulo PURO `calibracao_residual.py` (pacote disjunto `demanda_revelada/`): dataclass `CalibracaoResidualResult` + `_join_demanda_residual`, `correlacoes_bivariadas` (Spearman ANTES do modelo), `preparar_dados` (alvo `y=log1p(membros)`; preditor principal `score_oportunidade_residual`; modelo secundário auditável `log1p(oferta_efetiva_disponivel)` reportado SEPARADO), helpers OOF portados de `backtest_tp05.py` (`_kfold_repetido_oof`/`_ic_bootstrap_r2`/`_pct_extrapolacao`/`_selecionar_alpha_e_oof`) + NOVO `_ic_bootstrap_rho`/`_rho_oof` (Spearman out-of-fold + IC95), `calibrar_residual_demanda(...)`, `relatorio_calibracao`/`escrever_relatorio` + `_assert_sem_pii_no_relatorio`. Seed=42, N_REPEATS=5, **N_BOOTSTRAP=2000** (subido de 500; determinismo preservado pela seed). `R²_insample` só com rótulo literal de auditoria — NUNCA no veredito. Critério GO/NO-GO = âncora R² OU suporte rho, exatamente como aprovado. `__main__` guard (`# pragma: no cover`) lê os 2 parquets, roda e escreve o relatório.
- Reuso sem dep nova: `dimensionamento.aderencia` (`ALPHA_GRID`, `LIMIAR_R2_GO`, `_r2_loo_para_alpha`), `dimensionamento.backtest_dim` (`_r2`, `_rmse`), `contrato.COLUNAS_PII_PROIBIDAS`. Isolamento: NÃO importa de `pipelines.m1`/`dashboard`/`censo`/`api`/`config`.
- Testes sintéticos (seed fixa; sem rede/sem fonte real): anti-PII, r2_insample rotulado auditoria fora do veredito, intervalos+flag, alvo=`membros`/preditor=`score` (anti-inversão; `alunos_parceiras` nunca alvo/feature), demanda nunca preditor geográfico, isolamento de import (ast + linhas de import textuais), IC determinístico (R² e rho), GO/NO-GO (sinal forte→GO / ruído→NO-GO), join inner por hex_id.
- Relatório real materializado uma vez pelo `__main__` sobre os DADOS REAIS (gitignored): `data/analysis/calibracao_residual_demanda.md` (7 seções + veredito + Proposta de recalibração documentada NÃO aplicada + confounds).

## VEREDITO GO/NO-GO (real, dados reais)
**GO (âncora R²).** Dispara pela âncora R²; o suporte rho também qualifica.
- `R²_oof_log` (âncora) = **+0,3119** | IC95 = **[+0,2977, +0,3250]** (não cruza zero; > 0,05).
- `rho_oof` (suporte, Spearman OOF) = **+0,4615** | IC95 = **[+0,4477, +0,4739]** (≥ 0,30; não cruza zero).
- `R²_insample` (auditoria, FORA do veredito) = +0,3121; Spearman in-sample bivariado `residual × membros` = **+0,517** (reproduz o +0,52 da DEC-012 — in-sample, não é o veredito).
- `R²_oof_membros` (auditoria expm1) = −0,0110 (negativo: modelo ancorado em log-space; espaço cru é dominado por hexes gigantes — reportado como auditoria, não decide gate).
- Secundário auditável `log1p(oferta_efetiva_disponivel)` (SEPARADO) R²_oof = +0,2610 | IC95 [+0,2479, +0,2731].
- `n_join` = **16.411** | `n_treinamento` = 16.411 | inválidos = 0 | **cobertura ~1,06%** do universo (1.542.531) | método = `kfold_5x5` | alpha = 100 | pct_extrapolação = 0,0%.
- Concentração top-3 UF: SP 29,1%, MG 12,7%, RJ 9,2% (viés metropolitano do Sudeste — caveat forte no relatório e na `nota_honesta`).
- Leitura honesta: o `score_oportunidade_residual` PREVÊ a demanda observada fora da amostra (bate o baseline da média) e a ORDENA (rho), RESTRITO ao recorte metropolitano (~1%). RECALIBRAR a fórmula em produção é FOLLOW-UP com gate próprio (BLK-TP-07) — NÃO aplicado aqui.

## Arquivos alterados
- `src/motor_expansao/demanda_revelada/calibracao_residual.py` (NOVO — módulo de validação/calibração READ-ONLY).
- `tests/unit/demanda_revelada/test_calibracao_residual.py` (NOVO — 10 testes, fixtures sintéticas).
- `data/analysis/calibracao_residual_demanda.md` (NOVO relatório, gitignored — via `__main__`, não pelo teste).
- `context/handoff.md` (este), `context/handoff/20260702-135323-builder.md` (cópia append-only), `tasks/current_task.md` (status aguardando QA).

## Validações executadas
- pytest (subconjunto impactado, xdist): `python -m pytest -n auto -q tests/unit/demanda_revelada/ tests/integration/test_streamlit_app.py` → **231 passed, 0 failed, 0 skipped**. (Inclui os 10 novos de `test_calibracao_residual.py` + os demais de `demanda_revelada/` + o smoke de dashboard `test_streamlit_app.py`, exigido em todo ciclo.)
- import: `python -c "import streamlit_app"` → **ok**.
- isolamento (comando ast do handoff do Planner): **sem imports proibidos** (`bad imports: []`).
- ruff + mypy nos 2 arquivos novos: **All checks passed! / Success: no issues found** (rede de cortesia; QA roda o gate full).
- **mtime dos artefatos M1 INALTERADO** (BEFORE == AFTER):
  - `brasil_estrutural.parquet` 1780501621 (antes == depois)
  - `brasil_priorizados.parquet` 1780501631 (antes == depois)
  - `hexagonos_brasil_oportunidades.parquet` 1780501655 (antes == depois)
  - `hexagonos_brasil_dashboard.parquet` **AUSENTE neste checkout** (artefato derivado grande não presente localmente) → trivialmente inalterado; não foi gerado nem tocado.
- `git status --porcelain`: NENHUM `*.parquet` nem `hexagonos_mercado_mapeado.parquet` modificado; só arquivos do ciclo aparecem (untracked: módulo, teste; o relatório é gitignored e não aparece).

## Problemas encontrados
- Nenhum de bloqueio. Ajuste durante o desenvolvimento: o teste de isolamento por substring textual casava a palavra "dashboard" na docstring de guardrails do módulo; refinado para checar só LINHAS de import textuais (a checagem AST — o guard real — já estava limpa). Sem impacto no código de produção.

## Pendências
- FOLLOW-UP (fora deste bloco, exige DEC + gate humano — **BLK-TP-07**): APLICAR a recalibração da fórmula do `score_oportunidade_residual` em produção (capacidade default 2.500, peso de `oferta_efetiva_disponivel`, faixa de corte 0-100) e regenerar `hexagonos_mercado_mapeado.parquet`/derivados. Este bloco só VALIDOU + PROPÔS (documentado no relatório).
- BLK-TP-08 (fora deste bloco): ingestão anti-PII de `03_Competidores.xlsx` (academias menores WellHub/TotalPass) como cross-check.
- Bookkeeping de backlog (`tasks/backlog.md` → BLK-TP-06 concluído) e `tasks/completed.md`: a cargo do orquestrador/próximo passo se aplicável (não commitado por mim).

## Riscos remanescentes
- **R2 (cobertura/viés):** o GO vale para o recorte metropolitano do join (~1,06%, SP/MG/RJ ≈ 51%), NÃO é validação nacional — declarado no relatório e na `nota_honesta`.
- **R6 (guardrail):** a "Proposta de recalibração" é análise/recomendação; NÃO pode virar aplicação em produção sem BLK-TP-07 + DEC + gate.
- `R²_oof_membros` negativo (espaço cru) é esperado e honesto (modelo ancorado em log); não é defeito — documentado.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (READ-ONLY; fórmula/pesos renda=0.40/pop=0.60 intocados).
- Artefatos M1 preservados: **sim** (mtime idêntico dos 3 presentes; 4º ausente no checkout; nenhum parquet/mercado modificado no git).
- Dashboard offline mantido: **sim** (nenhuma dependência de rede; módulo não toca dashboard/censo/api).
- DEC-008: **sim** (k-fold 5×5 vs baseline da média; R² in-sample banido do veredito, só auditoria rotulada; IC95 bootstrap seed=42, 2000 reamostras, R² e rho; flag de extrapolação; NO-GO seria válido).
- DEC-009: **sim** (`membros` = ALVO observado; NÃO usado como preditor geográfico; teste anti-inversão + asserção de FEATURES).
- DEC-012 (anti-PII): **sim** (só camada agregada; `COLUNAS_PII_PROIBIDAS` ausentes; fixtures sintéticas; fonte real não versionada; relatório gitignored e passa `_assert_sem_pii_no_relatorio`).
- Sem dep nova: **sim** (numpy/pandas/scipy/sklearn já na base).
