# Handoff — Builder (BLK-FIX-06, Fase B concluída)

## Skill que gerou este handoff
Builder (Fase B — APLICAÇÃO: regeneração dos artefatos oficiais do M1)

## Próxima Skill recomendada
**QA/Quality Analyzer**

## DEC-002 APROVADA (autorização que liberou esta Fase B)
**DEC-002 APROVADA POR Felipe Silva EM 2026-06-03.** Critério geométrico HÍBRIDO em
`base_h3_brasil.py` ("centróide-dentro OU fracao_terra >= M1_HEX_LAND_FRACTION_MIN")
com **limiar final = 0.20**, e REGENERAÇÃO dos artefatos oficiais do M1. Registrada em
`CLAUDE.md §8` (logo após a DEC-001). O limiar 0.20 já era o default em `config.py`
(definido na Fase A) — confirmado e NÃO alterado.

## O que foi feito (Fase B)
1. **Confirmação da Fase A**: `M1_HEX_LAND_FRACTION_MIN = 0.20` presente nas 2 classes
   `Settings` (pydantic + fallback); critério híbrido implementado em `base_h3_brasil.py`;
   testes em `tests/integration/test_base_h3_brasil.py`. Universo oficial atual medido:
   **1.537.950** hexes (consistente com a Fase A).
2. **DEC-002 registrada em `CLAUDE.md §8`** (após a DEC-001), no estilo da DEC-001, com os
   números reais de `data/reports/base_h3_litoral_impacto.md` (474 recuperados; impacto ~nulo
   nos existentes; top-20%/UF entram 136/saem 41; repro Praia Grande/RJ; comparação 0.20 vs
   0.30). Parâmetros canônicos do §3 e DEC-001 NÃO tocados.
3. **Docstring de `base_h3_brasil.py` atualizada** (item 3 do topo): cita o critério HÍBRIDO
   real (centróide-dentro OU fracao_terra >= limiar), a DEC-002 (limiar 0.20), os hexes de
   repro (`87a810c02ffffff`, `87a8a078cffffff`) e que substitui o filtro de centróide puro
   que recortava o litoral. (O comentário enganoso "costeiros já preservados" já fora removido
   na Fase A; nenhum remanescente — só texto, sem mudança de lógica.)
4. **REGENERAÇÃO dos artefatos oficiais** (offline/determinística, caminhos canônicos,
   `refresh_malha=False`/`refresh_demografia=False`; caches locais: demografia parquet + 27
   geojson municipais + malha UF/Brasil; `demografia_municipios_cache_hit` em todas as UFs):
   - (a) `executar_base_h3_brasil()` → `data/staging/brasil/uf=XX/hexagonos.parquet` +
     `data/reports/base_h3_brasil.md`. Resultado: **total=1.538.424, recuperados_costeiros=474,
     removidos=218, land_min=0.2** (bate EXATAMENTE com a Fase A; +474 vs 1.537.950).
   - (b) `rodar_pipeline_brasil()` (= `rodar_pipeline_fase1_brasil`) → `brasil_estrutural.parquet`,
     `brasil_priorizados.parquet` (307.674), `hexagonos_brasil_oportunidades.parquet` (1.538.424)
     + `generate_fase1_bi_artifacts` (`hexagonos_brasil_dashboard.parquet`,
     `hexagonos_mapa_sample.parquet`, `top_oportunidades_resumo.csv`, `resumo_por_uf.csv`,
     `resumo_executivo_fase1.md`).
   - (c) `materialize_enriched_dashboard()` → `data/outputs/hexagonos_dashboard_enriquecido/uf=XX/`
     (artefato derivado, NÃO oficial): **1.538.424 linhas, 27 UFs**.
5. **Verificação pós-run**: base oficial=1.538.424 (+474); Praia Grande `87a810c02ffffff` e
   litoral RJ `87a8a078cffffff` presentes na base, em `hexagonos_brasil_oportunidades` E em
   `hexagonos_brasil_dashboard`; 45 colunas em oportunidades, TODOS os campos mínimos do score
   oficial presentes (`renda_pct_nacional`, `pop_pct_nacional`, `hex_score_estrutural`,
   `ajuste_executivo`, `score_priorizacao`, `score_oficial`, `score_oficial_nome`,
   `score_percentil_nacional`); `PESOS_HEX_SCORE_ESTRUTURAL = {renda:0.4, pop:0.6}` (inalterado).
6. **`docs/m1_outputs_oficiais.md`**: NÃO cita contagem fixa de hexes → deixado como está
   (per instrução).
7. **Limpeza de scratch**: removidos `data/staging/brasil_litoral_tmp*`, `brasil_estrutural_tmp*`,
   todos os `*.0NN.tmp.parquet`/`*.0NN.tmp.csv`, `data/reports/camada_oportunidade_fase1.0NN.tmp.md`
   e o log/driver de orquestração da Fase B. MANTIDOS (evidência/reprodutibilidade):
   `data/reports/base_h3_litoral_impacto.md`, `data/reports/base_h3_litoral_leque.csv`,
   `scripts/medir_impacto_litoral_blk_fix_06.py`.

## Arquivos alterados/criados nesta Fase B
- `CLAUDE.md` — §8: NOVA DEC-002 (após DEC-001). §3 e DEC-001 inalterados.
- `src/motor_expansao/pipelines/m1/base_h3_brasil.py` — docstring do topo (item 3) cita
  critério híbrido real + DEC-002 + repro. Sem mudança de lógica.
- Artefatos oficiais REGENERADOS (linhas adicionadas, colunas preservadas):
  `data/staging/brasil/uf=XX/hexagonos.parquet`, `data/reports/base_h3_brasil.md`,
  `data/staging/brasil_estrutural.parquet`, `data/staging/brasil_priorizados.parquet`,
  `data/staging/hexagonos_brasil_oportunidades.parquet`,
  `data/outputs/hexagonos_brasil_dashboard.parquet`, `data/outputs/hexagonos_mapa_sample.parquet`,
  `data/outputs/top_oportunidades_resumo.csv`, `data/outputs/resumo_por_uf.csv`,
  `data/reports/resumo_executivo_fase1.md`, `data/reports/camada_oportunidade_fase1.md`,
  `data/outputs/hexagonos_dashboard_enriquecido/uf=XX/` (derivado).
- `context/handoff.md` (este) + snapshot `context/handoff/20260603-100900-builder.md`.
- `tasks/current_task.md` (Skill atual/Próxima/Status).

## Números da regeneração (antes → depois)
- **Universo M1: 1.537.950 → 1.538.424 hexes (+474 costeiros recuperados; removidos=218; limiar 0.20).**
- `brasil_priorizados` (top-20%/UF): 307.579 → 307.674 (floor(20%) cresce com a UF; coerente
  com a Fase A `top20_novo`).
- Enriquecido particionado: 1.538.424 linhas, 27 UFs.
- Repro do litoral CONFIRMADA nos OFICIAIS: `87a810c02ffffff` (Praia Grande/SP) e
  `87a8a078cffffff` (litoral RJ) presentes em base + oportunidades + dashboard.

## Validações (saída literal)
- `python -m pytest -n auto -q tests/integration/test_base_h3_brasil.py tests/integration/test_streamlit_app.py tests/integration/test_modelo_mercado_hexagonos.py tests/integration/test_fase1_bi_exports.py tests/integration/test_hex_enrichment_brasil.py tests/unit/test_config_fallback.py`
  → **`217 passed, 19433 warnings in 24.35s`** (0 failed, 0 skipped). Subconjunto escolhido:
  base_h3 (alvo do critério), streamlit_app (smoke do app sobre artefatos novos),
  modelo_mercado_hexagonos (GUARDRAIL: valida colunas oficiais `score_priorizacao`/
  `hex_score_estrutural`/`score_oficial` nos parquets REGENERADOS), fase1_bi_exports +
  hex_enrichment_brasil (pipeline de geração), config_fallback (blindagem do novo parâmetro).
  Nenhum teste hardcoda a contagem de hexes — o +474 não quebrou baseline algum.
- `python -c "import streamlit_app; print('import ok')"` → `import ok` (warnings esperados de bare mode).
- Regeneração: exit 0; `(a) base H3 total=1538424 recuperados=474 removidos=218`;
  `(b) oportunidades=1538424 priorizados=307674`; `(c) enriquecido=1538424`.

## Guardrails confirmados
- Pesos/fórmula do score: **INALTERADOS** — `renda=0.40`/`pop=0.60`
  (`PESOS_HEX_SCORE_ESTRUTURAL = {renda:0.4, pop:0.6}` lido do código pós-run);
  `score_priorizacao`/`hex_score_estrutural` sem mudança de fórmula (código de score não tocado).
- Parâmetros canônicos do §3: **INALTERADOS** — apenas ADIÇÃO de `M1_HEX_LAND_FRACTION_MIN`
  (já na Fase A); a regeneração ADICIONA linhas (costeiros), NÃO remove colunas (45 cols em
  oportunidades, todos os campos mínimos presentes).
- Camadas paralelas (censitário/híbrido/mercado): NÃO tocadas. (O `materialize_enriched_dashboard`
  é artefato derivado documentado, não recalcula score nem altera oficiais.)
- Nada commitado/branch/merge; PRD.md e arquivos não relacionados intactos (orquestrador
  cuida do git por path e do housekeeping de backlog/completed).

## Para o QA
- Rodar a SUÍTE FULL (gate único) — `pytest -n auto`. Esperado verde; não há baseline de
  contagem de hexes em teste a ajustar.
- Sanidade dos oficiais: total 1.538.424; repro litoral presente; campos mínimos do score; pesos
  0.40/0.60; colunas oficiais preservadas (já checado, mas confirmar de forma independente).
