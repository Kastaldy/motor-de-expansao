# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-MAP-01 — Filtro individual de concorrentes nos overlays do Mapa Territorial

## O que foi feito

Implementado o filtro visual de redes de concorrentes em `render_mapa_territorial` (`pages.py`), seguindo exatamente os passos 1–5 do plano do Planner e as decisões D1=A/D2=A/D3=A aprovadas pelo gate humano.

**Passos executados:**

1. **Import de `COMPETITOR_BRANDS`** adicionado em `pages.py` (posição alfabética correta no bloco de imports locais: após `censo_report`, antes de `components`).

2. **Passo 1 — Flag e multiselect de redes** (`pages.py`, após o bloco `if selected_mode == "dominio" ... return`):
   - Flag `_show_rede_filter`: True quando overlay "concorrentes" está ativo, `competitors_df` não é None/vazio e tem coluna "rede".
   - `selected_redes: list[str] = []` por default.
   - Quando `_show_rede_filter=True`: constrói `_all_redes` ordenado por posição em `COMPETITOR_BRANDS` (fallback ao final para redes desconhecidas), exibe `st.multiselect` com `default=_all_redes` e `format_func` usando `COMPETITOR_BRANDS.get(r, {}).get("label", r)` (chave "label" confirmada na estrutura real do dict).

3. **Passo 2 — Ponto único de filtragem** (imediatamente antes de `_render_unified_legend`):
   - D2=A: `_show_rede_filter and not selected_redes` → `competitors_df_filtered = None` (esconde todos).
   - `_show_rede_filter and selected_redes` → `competitors_df_filtered = competitors_df[competitors_df["rede"].isin(selected_redes)]`.
   - `not _show_rede_filter` → `competitors_df_filtered = competitors_df` (retrocompatibilidade byte-a-byte).

4. **Passo 3 — `_render_unified_legend`**: substituído `competitors_df=competitors_df` por `competitors_df=competitors_df_filtered`.

5. **Passo 4 — `build_unified_map_figure`**: substituído `competitors_df=competitors_df` por `competitors_df=competitors_df_filtered`.

6. **Passo 5 — bloco de caption**: substituído `_comp_for_caption = competitors_df if "concorrentes" in _enabled else None` por `competitors_df_filtered`.

7. **`components.py` NÃO alterado**: guard existente em `render_competitor_legend` (linha 197: `if competitors_df is None or competitors_df.empty or "rede" not in competitors_df.columns: return`) é suficiente. Dispatcher `build_unified_map_figure` já trata `competitors_df=None` via `_comp = competitors_df if "concorrentes" in enabled_overlays else None` (linha 3031). Nenhuma mudança necessária.

8. **Passo 7 — 3 testes novos** em `tests/integration/test_streamlit_app.py`, bloco `# ── BLK-MAP-01: filtro individual de redes ──`:
   - `test_filtro_rede_uma_rede_so_ela_renderiza` (Cenário A): filtra para smart_fit → só ela na camada de pins.
   - `test_filtro_rede_vazio_esconde_concorrentes` (Cenário B): `competitors_df=None` (D2=A) → `len(deck.layers) == 1`.
   - `test_filtro_rede_todas_comportamento_atual` (Cenário C): todas as redes → retrocompatibilidade, ambas as redes renderizadas.

**Ajustes durante execução:**
- Import posicionado inicialmente no fim do bloco de imports (ruff I001); corrigido para posição alfabética correta (`competitors` após `censo_report`, antes de `components`).
- `type: ignore` corrigido de `[union-attr]` para `[index]` (código de erro real reportado pelo mypy para acesso a `competitors_df["rede"]` dentro do bloco `if _show_rede_filter`).

## Arquivos alterados

- `src/motor_expansao/dashboard/pages.py` — adicionado import de `COMPETITOR_BRANDS`; implementados passos 1–5 (multiselect de redes + ponto único de filtragem + propagação de `competitors_df_filtered` para legend/builder/caption).
- `tests/integration/test_streamlit_app.py` — adicionados 3 novos testes BLK-MAP-01 (cenários A/B/C) + fixture auxiliar `_make_competitors_two_redes`.

## Validações executadas

- **pytest subconjunto** (`tests/integration/test_streamlit_app.py` — subconjunto impactado):
  `182 passed, 0 failed, 0 skipped` (inclui os 3 novos testes BLK-MAP-01).
  Executor: serial `pytest -q` (Python 3.14 local; xdist com `-n auto` não testado separadamente pois suite serial passou integralmente).
- **import smoke**: `python -c "import streamlit_app; print('import ok')"` → `import ok` (ok).
- **ruff check** (`src/motor_expansao/dashboard/pages.py tests/integration/test_streamlit_app.py`): `All checks passed!` (0 erros).
- **mypy** (`src/motor_expansao/dashboard/pages.py`): `Success: no issues found in 1 source file` (0 erros).

## Problemas encontrados

1. **Ordem de imports** (ruff I001): import de `COMPETITOR_BRANDS` posicionado inicialmente após todos os outros imports locais → corrigido para posição alfabética correta (após `censo_report`, antes de `components`).
2. **Código de erro mypy**: `# type: ignore[union-attr]` não cobria o erro real `[index]` → substituído por `# type: ignore[index]`.

Ambos resolvidos antes das validações finais.

## Pendências

Nenhuma — todas do escopo BLK-MAP-01 implementadas e validadas.

## Riscos remanescentes

Nenhum identificado. O filtro é puramente visual (`.isin()` sobre DF em memória); não afeta score, carteira, plano nem artefatos M1. A retrocompatibilidade está garantida pelo caminho `else: competitors_df_filtered = competitors_df`.

## Guardrails verificados

- score_priorizacao não alterado: sim (filtro puramente visual; READ-ONLY M1 absoluto)
- Artefatos M1 preservados: sim (zero escrita em parquets ou artefatos oficiais)
- Dashboard offline mantido: sim (nenhuma dependência de API ao vivo adicionada; `st.multiselect` é widget local)
- BLK-FIX-11 intocado: sim (overlays `ultra`, `ancoras_dominio`, `descartados_5k` não alterados)
- Performance preservada: sim (carga lazy por UF, cap `COMPETITOR_PIN_LIMIT`, fonte de mapa enxuta não tocados)
- `components.py` não alterado: sim (guard existente é suficiente; verificado)
