# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador: housekeeping se no backlog + commit por path)

## VEREDITO
APROVADO

## Justificativa
A entrega do BLK-TP-08-FU está correta, isolada e READ-ONLY sobre o M1: a suíte completa passa (1264 passed, 1 skipped, exit 0), os 2 artefatos REAIS gerados por conta própria têm ZERO PII, a anti-reidentificação (N<3→`independente`) se sustenta nos dados reais (mínimo N=5 entre as redes sobreviventes), e o número-chave do bloco — dedup fino 62,7%→8,3% — foi reproduzido exatamente. mtime dos 4 oficiais M1 + `hexagonos_mercado_mapeado.parquet` INALTERADO.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- Discrepância documental leve: o handoff do Builder e o docstring do teste dizem "14 testes"; o arquivo tem **13** funções de teste (pytest confirma `13 passed`). Todos verdes — apenas ajustar a contagem no texto. Não afeta cobertura (o teste de falso-positivo, isolamento ast, zero-PII, colapso N<3, contrato/dtypes, agregação, capacidade e determinismo estão todos presentes).

## Testes faltantes
- nenhum crítico. Cobertura sólida: token word-boundary + falso-positivo evitado (`Studio Live Well`/`Terrace Fit Club`/`PHD Consultoria` → `independente`), normalização de acento, colapso anti-reid N<3 (limiar 3 e 2), ZERO PII nos 2 parquets + relatório, contrato/dtypes, agregação hex×rede (com `Total_Alunos_Cluster` nunca somado), res-7, capacidade média/mediana/`flag_confiavel`, isolamento (ast) e determinismo.

## Riscos remanescentes
- Cobertura ~2,2% classificada / ~97,8% `independente` (esperado e documentado): a capacidade por rede só é útil para as 10 redes N≥10; a oferta menor é majoritariamente `independente`. É caveat de produto, não defeito.
- Coords ~1 km (mesmo caveat do TP-08/TP-01): ruído no join res-7 — camada de refino, não verdade fina. Documentado.
- Housekeeping (Passo 6.0): o bloco é AD-HOC mas o Builder o adicionou ao backlog COM heading `### BLK-TP-08-FU` (linha 964) + stub (linha 962) + resumo já em `completed.md` (linha 6484). Estado ESPERADO de pré-move: `housekeeping_move_block.py --check` reporta "ainda tem heading no backlog" — o move é passo do ORQUESTRADOR no fechamento (não rodei; instrução explícita). `tests/unit/test_housekeeping_helper.py` verde na full (10 passed).

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte COMPLETA (gate único) — `python -m pytest -n auto -q`
```
1264 passed, 1 skipped, 346518 warnings in 209.25s (0:03:29)
```
Exit code 0. 0 falhas / 0 erros de coleta. Baseline ~1251 (BLK-TP-08) + 13 novos = 1264. Sem flakiness de paralelismo observada.

### 2. Smoke — `python -c "import streamlit_app; print('import ok')"`
```
2026-07-02 16:34:16 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
...(warnings de runtime esperados sem sessão streamlit)...
import ok
```

### 3. Isolamento (ast) de `classificacao_rede_menor.py`
```
IMPORTS: ['from __future__', 're', 'unicodedata', 'from pathlib', 'h3', 'pandas', 'from contrato', 'from oferta_academias_menores']
VIOLATIONS: []
ISOLATION OK
```
NÃO importa `pipelines.m1` / `dashboard` / `censo` / `motor_expansao.api`. Só stdlib + h3/pandas + o próprio pacote `demanda_revelada`.

### 4. mtime dos 4 artefatos M1 + `hexagonos_mercado_mapeado.parquet` (antes → depois de gerar os 2 parquets reais + rodar a full)
```
ANTES:  1781271963 hexagonos_brasil_dashboard.parquet
        1781271963 top_oportunidades_resumo.csv
        1781271963 resumo_por_uf.csv
        1780501631 brasil_priorizados.parquet
        1781205056 hexagonos_mercado_mapeado.parquet
DEPOIS: 1781271963 hexagonos_brasil_dashboard.parquet
        1781271963 top_oportunidades_resumo.csv
        1781271963 resumo_por_uf.csv
        1780501631 brasil_priorizados.parquet
        1781205056 hexagonos_mercado_mapeado.parquet
```
IDÊNTICO. `git status --porcelain` NÃO lista nenhum artefato M1/mercado modificado. Os 2 parquets novos + relatório existem em disco e são **gitignored** (`git check-ignore` confirma; não aparecem no git status). `calcular_colunas_mercado.py` INTOCADO.

### 5. Lint/type
```
ruff check ...classificacao_rede_menor.py __init__.py test_classificacao_rede_menor.py
-> All checks passed!

mypy src/.../classificacao_rede_menor.py tests/.../test_classificacao_rede_menor.py
-> Success: no issues found in 2 source files
```

### Prova anti-PII dos 2 artefatos REAIS (lidos de disco, gerados pelo QA sobre `NAO_ABRA/03_Competidores.xlsx`)
```
OFERTA colunas (disco): ['hex_id', 'rede_menor', 'n_academias_menores', 'alunos_academias_menores', 'versao_contrato']
  dtypes: hex_id=string, rede_menor=string, n_academias_menores=int64, alunos_academias_menores=int64, versao_contrato=string
CAP    colunas (disco): ['rede_menor', 'media_alunos', 'mediana_alunos', 'n_filiais', 'flag_confiavel', 'versao_contrato']
[oferta] PII columns present: NONE
[cap]    PII columns present: NONE   (checado: nome_academia/latitude/longitude/cluster_id/total_alunos_cluster)
rede_menor dtype (oferta): string  (é CATEGORIA, não o nome cru)
independente in cap table?: False  (excluída da tabela de capacidade)
```
Confirmado no código: nome/coords/cluster dropados na FRONTEIRA (`_ler_e_derivar_hex_rede` → `drop(_COLUNAS_DROP_FRONTEIRA)`); o nome cru só transita em memória em `classificar_rede`, nunca persistido/logado; `Total_Alunos_Cluster` nunca somado.

### Anti-reidentificação (gate B) — dados reais
```
Limiar anti-reid: 3
Known categories surviving: 13
Surviving categories with N in [1,2]: []  -> NONE (OK)
min N among surviving known: 5
```
Nenhuma categoria de cardinalidade 1–2 sobrevive no artefato.

### Reprodução de números
```
n_total classificadas (hex válido): 24,045
  conhecida:    528 (2.20%)
  independente: 23,517 (97.80%)
Redes na tabela cap (pós-colapso): 13 | confiáveis N>=10: 10
DEDUP GROSSEIRO: 1,203,919 alunos (62.7%)
DEDUP FINO:      273 pares hex×rede, 158,532 alunos (8.3%)
=> 62.7% -> 8.3%   (reproduzido exatamente)
```

### Fixture sintética `tests/fixtures/rede_menor_fake.xlsx`
```
10 linhas, nomes FABRICADOS: 'Academia Smart Fit Centro','SMARTFIT Unidade 2','Studio Live Well',
'Panobianco Vila X','PANOBIANCO Norte','panobianco sul','Terrace Fit Club','Blue Fit Copacabana',
'Academia do Joao','Smart Fit Fantasma' (coords de landmark SP/RJ; última linha NaN → dropada).
```
Sem PII de estabelecimento real. Testes NUNCA leem `NAO_ABRA/`.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock do caminho crítico. A suíte full rodou 1× (gate único) sem `-p no:xdist`. A prova anti-PII e a reprodução dos números foram feitas contra a FONTE REAL de produção (`NAO_ABRA/03_Competidores.xlsx`, 24.045 academias) e o `concorrentes_mapeados.parquet` real — não sobre fixture. As fixtures sintéticas são uso legítimo de teste unitário (fora do escopo da regra de bypass de tooling).

## Conferência cruzada com log do Builder
Bate na substância. Divergências mínimas (não bloqueantes):
- Builder diz "14 testes"; o arquivo tem **13** funções (pytest: `13 passed`). Ajuste de contagem no texto.
- Todos os números do Builder confirmados por evidência própria: cobertura 2,2%/97,8%; 13 redes/10 confiáveis; dedup fino 62,7%→8,3%; colunas dos 2 parquets idênticas; mtime M1 intacto; isolamento; ruff/mypy limpos; `import ok`.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; nenhum toque em score/pesos/fórmula; pesos renda=0.40/pop=0.60 e H3_RESOLUTION=7 canônicos intocados)
- Artefatos M1 preservados: sim (mtime dos 4 oficiais + `hexagonos_mercado_mapeado.parquet` IDÊNTICO antes/depois; git status sem artefato M1/mercado modificado)
- mercado/residual não recomposto: sim (`calcular_colunas_mercado.py` intocado; nenhum parquet de mercado regenerado)
- anti-PII (DEC-012): sim (2 parquets reais ZERO PII lidos de disco; nome/coords/cluster dropados na fronteira; `rede_menor` é categoria; `Total_Alunos_Cluster` nunca somado; fonte real gitignored; fixtures sintéticas)
- anti-reidentificação (gate B): sim (rede N<3→`independente`; nenhuma categoria de cardinalidade 1–2 sobrevive; min N=5 nos dados reais)
- matching word-boundary (gate A): sim (`re.compile(r"\b…\b")`; teste de falso-positivo verde: "live"/"race"/"phd" não viram rede)
- Isolamento: sim (ast: sem `pipelines.m1`/`dashboard`/`censo`/`api`)
- Escopo respeitado: sim (conjunto tocado = só o do plano: novo módulo + `__init__.py` aditivo + teste novo + fixture + doc aditivo + bookkeeping/handoff; os 2 parquets + relatório gitignored)
- Validações re-executadas pelo QA (sem bypass): full `1264 passed, 1 skipped` → OK; `import streamlit_app` → ok; ast isolamento → OK; mtime M1 → IDÊNTICO; ruff → passed; mypy → Success; anti-PII real → NONE; anti-reid real → NONE em [1,2]; dedup fino → 62,7%→8,3%; helper housekeeping test → 10 passed

## Decisão recomendada
Fechar ciclo. Fechamento manual pelo orquestrador: como existe heading `### BLK-TP-08-FU` em `backlog.md`, rodar `scripts/housekeeping_move_block.py BLK-TP-08-FU` (remove o heading do backlog; bloco já em `completed.md`) e commit por path. Desbloquear/retomar o BLK-TP-06-FU1 (dado fechado) como follow-up sob gate próprio.
