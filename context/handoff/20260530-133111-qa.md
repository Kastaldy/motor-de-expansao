# Handoff — QA / Quality Analyzer

## VEREDITO: APROVADO

## Skill que gerou este handoff
QA / Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador) — Passo 6.0 housekeeping (move BLK-OPS-03 com `--check` verde) + commit por path.

## Bloco auditado
BLK-OPS-03 — Manifesto de proveniência nos outputs

## Justificativa
A entrega do Builder é estritamente ADITIVA e NÃO-MUTANTE. Re-executei por conta própria,
sem bypass, TODAS as validações obrigatórias e a prova de não-mutação por SHA256 contra os
artefatos M1 reais de produção. Resultado: todas verdes; os 5 hashes M1 são byte-idênticos
pré/pós a geração do `_manifest.json` real; o JSON contém as 9 chaves com valores canônicos
corretos lidos de `settings`/`PESOS_HEX_SCORE_ESTRUTURAL` (sem hardcode); escopo respeitado
(diff de `fase1_bi_exports.py` = 1 import + 2 linhas no FIM de `main()`, fora de score/write_outputs);
JSON UTF-8 sem BOM. Nenhum critério de aprovação ficou em aberto.

## Problemas críticos
- Nenhum.

## Problemas médios
- Nenhum.

## Problemas leves
- O `_manifest.json` real gravado em Windows ficou com EOL `CRLF` (`\r\n`) apesar de `write_text`
  usar `+ "\n"` — é tradução de newline do Windows, NÃO um BOM. O guardrail canônico (proibir BOM
  utf-8-sig) está respeitado: primeiros 3 bytes = `b"{\r\n"`, não `b"\xef\xbb\xbf"`. Sem impacto
  (arquivo é artefato gerado, não versionado). Apenas registrado.
- `data/outputs/` NÃO está explicitamente no `.gitignore` (só `data/raw/`, `data/staging/`,
  `data/ultra/`, etc.). Os parquets/CSVs de output ali são UNTRACKED mas não ignorados. Consequência:
  `_manifest.json` aparece como `??` (untracked) no `git status`. NÃO deve ser commitado — é artefato
  gerado, como os parquets vizinhos. Confirmado: `git check-ignore data/outputs/_manifest.json` → rc=1
  (não casado por regra), `git status --short` → `?? data/outputs/_manifest.json`. RECOMENDAÇÃO ao
  fechamento: NÃO adicionar `_manifest.json` ao commit (commit só por path dos arquivos de código/teste/processo).

## Testes faltantes
- Nenhum bloqueante. Os 6 testes cobrem chaves/tipos, valores canônicos (trava 0.40/0.60 e mapeamento
  renda/pop não-invertido), sha256 de bytes brutos presente/ausente, campos voláteis (timestamp UTC +
  formato de commit) e gravação UTF-8 sem BOM. Cobertura adequada para o escopo aditivo.
- Observação não-bloqueante: não há teste automatizado afirmando que `main()` chama `write_manifest`
  como ÚLTIMO passo; isso foi verificado manualmente por leitura do diff (passo isolado após o bloco
  HYBRID, fora de `generate_fase1_bi_artifacts`/`write_outputs`). Aceitável para este bloco.

## Riscos remanescentes
- Nenhum risco de mutação: módulo stdlib-only, `write_manifest` só escreve `_manifest.json`; provado
  por hash. Render do dashboard é read-only e tem guarda de ausência do JSON.

## Saída literal das validações re-executadas

### 1. `python -m pytest -q tests/unit/test_manifest.py`
```
......                                                                   [100%]
6 passed in 0.74s
```

### 2. `python -m pytest -q tests/integration/test_streamlit_app.py`
```
........................................................................ [ 48%]
........................................................................ [ 97%]
...                                                                      [100%]
147 passed in 8.69s
```

### 3. `python -c "import streamlit_app; print('import ok')"`
```
2026-05-30 13:33:20.860 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
... (warnings de cache sem runtime, esperados fora do Streamlit) ...
import ok
```

### 4. `python -m pytest -q` (suíte completa)
```
547 passed, 1 skipped, 9 warnings in 75.30s (0:01:15)
```
Baseline anterior era 532 passed / 1 skipped; subiu para 547 passed (inclui os 6 novos testes do
manifesto + contagens já presentes no momento desta medição), 1 skipped preservado. Nada quebrou.

### 5. `python -m mypy src/motor_expansao/pipelines/m1/provenance.py`
```
Success: no issues found in 1 source file
```

### 6. `python -m ruff check src/.../provenance.py tests/unit/test_manifest.py streamlit_app.py src/.../components.py`
```
All checks passed!
```

## PROVA DE NÃO-MUTAÇÃO POR SHA256 (passo isolado contra caminho REAL de produção)

Executado o passo ISOLADO (sem rodar o pipeline pesado):
`python -c "from motor_expansao.pipelines.m1.provenance import write_manifest; p = write_manifest(); print('manifest:', p)"`
→ saída: `manifest: data\outputs\_manifest.json` (criou o caminho REAL `data/outputs/_manifest.json`,
que NÃO existia antes — confirmado por `ls` retornando "No such file or directory" no pré).

Os 5 artefatos M1 EXISTEM localmente. Hashes (sha256sum) PRÉ e PÓS:

| Artefato | PRÉ | PÓS | Igual? |
|---|---|---|---|
| data/outputs/hexagonos_brasil_dashboard.parquet | `0cfb1015fc9df8b63776eb07ae3e666766905f768a4a78a406ae4d6b7cb6f618` | `0cfb1015fc9df8b63776eb07ae3e666766905f768a4a78a406ae4d6b7cb6f618` | SIM |
| data/outputs/hexagonos_mapa_sample.parquet | `f8166329afee6e38d52e9b46aba9e505b99f7e372ab0591dff9c8de4b6f2e437` | `f8166329afee6e38d52e9b46aba9e505b99f7e372ab0591dff9c8de4b6f2e437` | SIM |
| data/staging/brasil_estrutural.parquet | `7baa07a2cbc0b7d8f2a8878932cae0ebf9400fce00e9c48c366c9903215f131b` | `7baa07a2cbc0b7d8f2a8878932cae0ebf9400fce00e9c48c366c9903215f131b` | SIM |
| data/staging/brasil_priorizados.parquet | `c226954945ad0757a0429c84c43f410492c0ea7d15ca1d9b6a15f68727806567` | `c226954945ad0757a0429c84c43f410492c0ea7d15ca1d9b6a15f68727806567` | SIM |
| data/staging/hexagonos_brasil_oportunidades.parquet | `805c65e28adfd800c7d5524e73fa9b0a044b992f17d617d0f2c6e40f9bbf61ca` | `805c65e28adfd800c7d5524e73fa9b0a044b992f17d617d0f2c6e40f9bbf61ca` | SIM |

TODOS byte-idênticos pré/pós → guardrail de não-mutação CUMPRIDO.

## Conteúdo do `_manifest.json` REAL gerado e validação de schema
```json
{
  "schema_version": 1,
  "ibge_vintage": "censo_2022",
  "ultra_csv_sha256": "685ba21f74894ccdee1383a29cb3075c21cfd1e4948b051d1b833073ce469c66",
  "code_commit": "b89123ce777f19d461c49f078dbf2431eadc5dfc",
  "generated_at": "2026-05-30T16:35:10.651985+00:00",
  "h3_resolution": 7,
  "pesos": {
    "renda": 0.4,
    "pop": 0.6
  },
  "dist_min_ultra_km": 1.0,
  "renda_min": 4500.0
}
```
Validação de cada chave:
- 9 chaves presentes: OK.
- `schema_version == 1`: OK.
- `ibge_vintage == "censo_2022"`: OK.
- `h3_resolution == 7`: OK (== settings.H3_RESOLUTION).
- `pesos == {"renda": 0.4, "pop": 0.6}`: OK (lidos de `PESOS_HEX_SCORE_ESTRUTURAL["renda_per_capita"]`/`["populacao_proxy"]`; mapeamento NÃO invertido).
- `dist_min_ultra_km == 1.0`: OK (== settings.DIST_MIN_ULTRA_KM).
- `renda_min == 4500.0`: OK (== settings.RENDA_MIN).
- `generated_at` ISO 8601 com offset UTC `+00:00`: OK.
- `code_commit` = hash de 40 hex: OK e BATE com `git rev-parse HEAD` = `b89123ce777f19d461c49f078dbf2431eadc5dfc`.
- `ultra_csv_sha256` = hash de 64 hex: OK e BATE com `sha256sum data/ultra/Ultra.csv` (bytes brutos) = `685ba21f...ce469c66`.
- UTF-8 SEM BOM: OK (primeiros 3 bytes `b"{\r\n"`, não `\xef\xbb\xbf`).

## Conferência de no-bypass
- Re-executei pessoalmente as 6 validações + a prova de hash; nenhuma saída foi reaproveitada do Builder.
- O uso de `tmp_path` nos testes unitários é legítimo (NÃO é bypass): isola o I/O de hash/JSON.
- A exigência de "caminho real" foi cumprida com a geração REAL de `data/outputs/_manifest.json`
  (não tmp), provando que gerar o manifesto real não toca os parquets vizinhos.

## Conferência cruzada com o log do Builder
- Builder reportou `pytest -q` = `547 passed, 1 skipped` → confirmado (idêntico).
- Builder reportou `test_manifest.py` 6 passed, `test_streamlit_app.py` 147 passed, mypy/ruff limpos,
  import ok → todos confirmados por re-execução própria.
- Builder NÃO rodou `write_manifest()` no caminho real (só tmp); EU rodei no caminho real e provei
  a não-mutação por hash. Fechado o gap apontado pelo próprio Builder.
- Desvio menor do plano literal: `datetime.now(UTC)` em vez de `datetime.now(timezone.utc)` (por ruff
  UP017). CONFIRMADO funcionalmente idêntico: o `generated_at` gerado tem offset `+00:00`
  (`utcoffset().total_seconds() == 0` no teste), e `datetime.UTC` é alias de `timezone.utc` desde
  Python 3.11. Aceitável.

## Guardrails verificados
- score_priorizacao NÃO alterado: SIM (diff de `fase1_bi_exports.py` não toca nenhuma função de score/write_outputs; só import + 2 linhas no fim de `main()`).
- Artefatos M1 preservados (hashes idênticos pré/pós): SIM — ver tabela acima; os 5 hashes batem exatamente.
- Pesos lidos de `PESOS_HEX_SCORE_ESTRUTURAL` (renda←renda_per_capita=0.40, pop←populacao_proxy=0.60), NÃO hardcoded: SIM (confirmado no código de `provenance.py` linhas 84-87 + `constants.py` linhas 31-34).
- Parâmetros canônicos preservados (só lidos): H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0 — SIM (manifesto apenas LÊ de `settings`).
- JSON UTF-8 SEM BOM; `ultra_csv_sha256` hasheia BYTES BRUTOS: SIM (sha256 do binário bate com sha256sum independente).
- Dashboard footer read-only e não quebra se o JSON falta: SIM (`render_manifest_footer` retorna cedo se `not manifest_path.exists()`, com `try/except (OSError, ValueError)`).
- Manifesto AO LADO, nunca DENTRO do conteúdo de scoring: SIM (`data/outputs/_manifest.json` separado).
- `_manifest.json` rastreado pelo git? NÃO (untracked `??`, não-ignorado). NÃO commitar — é artefato gerado.

## Housekeeping (Passo 6.0)
- `python scripts/housekeeping_move_block.py BLK-OPS-03 --check` → saída:
  `FALHA --check: stub ausente no backlog para BLK-OPS-03` (exit 0).
  Estado ATUAL e ESPERADO: BLK-OPS-03 ainda é uma entrada COMPLETA no `tasks/backlog.md`
  (linhas 25-44, Status: Pendente), ainda NÃO virou stub. O `--check` valida o invariante
  PÓS-move (stub no backlog + bloco em completed.md), que ainda não ocorreu. Correto neste ponto.
- `tests/unit/test_housekeeping_helper.py` → `10 passed in 0.03s` (verde; também coberto pelo `pytest -q`).
- Helper disponível e `--check` executado; o move efetivo BLK-OPS-03 ocorrerá no Passo 6.0 do
  fechamento pelo orquestrador, com `--check` verde exigido antes do commit. Pré-condição satisfeita
  do ponto de vista do QA (helper presente + suíte do helper verde); a confirmação byte-idêntica
  final do move é no fechamento.

## Decisão recomendada
APROVADO. Prosseguir ao fechamento manual:
1. Passo 6.0: rodar o helper de housekeeping para mover BLK-OPS-03 (backlog→stub + completed.md),
   exigindo `--check` verde antes do commit.
2. Commit POR PATH (nunca `git add -A`): código (`provenance.py`, `fase1_bi_exports.py`,
   `components.py`, `streamlit_app.py`), teste (`test_manifest.py`), processo
   (`tasks/*`, `context/handoff*`). NÃO incluir `data/outputs/_manifest.json` (artefato gerado, untracked).
