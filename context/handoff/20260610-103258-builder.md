# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-API-01 — Definir arquitetura e contrato da API (G1)

## O que foi feito
Bloco de DOCS/design (ZERO código de produção, READ-ONLY M1). Gate humano das 6 decisões-chave
resolvido por Felipe Silva em 2026-06-10; redigido o contrato/OpenAPI/ADR com as escolhas confirmadas:
1=(c) JSON+?formato=pdf, 2=(b) token por consumidor, 3=(a) MVP mínimo (/health + /analisar),
4=(b) {lat,lng} E link Google Maps, 5=(a) raio fixo 1.5 km, 6=Incluir carimbo de versão.

Entregáveis:
- `docs/api_geoespacial_contrato.md` (novo): contrato técnico completo (15 seções) — cabeçalho/escopo
  (ClickUp 86e1rtfe3, pai 86e1rtfcy, G1–G4); premissas canônicas inegociáveis (on-demand sem PostGIS,
  importa-não-edita censo_*, pasta api/ disjunta, extra [api], READ-ONLY M1, parâmetros canônicos);
  layout descritivo de `src/motor_expansao/api/` (a criar em G2, nenhum arquivo criado agora); fronteira
  importa-não-edita com as 4 assinaturas REAIS confirmadas no código; resolução coord→partição; endpoints
  do MVP (GET /health + POST /analisar; lookup M1/mercado fora do MVP→BLK-API-05); schemas
  request/response com campos das decisões 1/4/6; auth por token→consumidor; tabela de erros
  400/401/403/404/422/500; versionamento /api/v1 + carimbo; deps subset MVP do extra [api]
  (fastapi/uvicorn[standard]/pydantic/pydantic-settings) e o legado PostGIS a descartar; o que se aproveita
  do scaffold legado; decomposição G2+; guardrails. Cita "ver DEC-005 (§8)".
- `docs/api_geoespacial_openapi.yaml` (novo): OpenAPI 3.1.0 do MVP — GET /health, POST /analisar (request
  {lat,lng} OU maps_url + formato?; responses application/json com KPIs+carimbo e application/pdf binário;
  securityScheme bearer token por consumidor; ErrorResponse com códigos enum). YAML válido, coerente com o contrato.
- `CLAUDE.md` §8: nova DEC-005 (Estratégica; APROVADA por Felipe 2026-06-10) no estilo das DEC-001..004,
  registrando as 6 decisões + premissas fixadas + decomposição + "Pesos/fórmula/artefatos do M1: INALTERADOS".
  DEC-001..004 intocadas; só append.
- `tasks/backlog.md`: adicionados BLK-API-02..07 por inserção após o bloco BLK-API-01 (esqueleto+/health+
  settings+auth; POST /analisar JSON; saída PDF; M1/mercado CONDICIONAL/roadmap; integração G3; G4
  Telegram/WhatsApp), cada um com tabela de campos, escopo e critérios. Nenhum bloco não relacionado movido.
  Housekeeping do BLK-API-01 (mover p/ completed) NÃO feito — é do orquestrador no fechamento.
- `README.md` e `PRD.md`: ponteiro de 1 linha ao contrato + DEC-005 no índice/mapa de docs.
- `tasks/current_task.md`: status → "aguardando QA"; próxima Skill = QA.

Pasta `src/motor_expansao/api/` NÃO criada (Planner recomendou; Felipe não pediu no gate). ZERO arquivos em src/.

## Arquivos alterados
- `docs/api_geoespacial_contrato.md` (novo) — contrato técnico completo da API GeoEspacial (G1).
- `docs/api_geoespacial_openapi.yaml` (novo) — esboço OpenAPI 3.1.0 dos endpoints do MVP.
- `CLAUDE.md` — §8: nova DEC-005 (append; DEC-001..004 intocadas).
- `tasks/backlog.md` — BLK-API-02..07 inseridos após BLK-API-01 (+ nota de decomposição).
- `README.md` — ponteiro ao contrato no "Mapa de docs".
- `PRD.md` — ponteiro ao contrato/DEC-005 no índice de docs.
- `tasks/current_task.md` — status "aguardando QA"; próxima Skill = QA.
- `context/handoff.md` + `context/handoff/20260610-HHMMSS-builder.md` — este handoff.

## Validações executadas
- pytest (subconjunto): `python -m pytest -n auto -q tests/integration/test_streamlit_app.py` → **176 passed, 0 failed, 0 skipped** (15138 warnings de DeprecationWarning de ambiente Python 3.14/pytest-asyncio, não defeito). Rodado por ser a rede de segurança do dashboard (único subconjunto pertinente — bloco é só docs, não toca código).
- import: `python -c "import streamlit_app"` → **import ok**.
- ruff: `ruff check src/ tests/` → **All checks passed!** (confirma que nada vazou para src/).
- mypy: `mypy src/` → **Success: no issues found in 46 source files** (3 notes informativos de annotation-unchecked, pré-existentes, não erros).
- git scope: `git status --porcelain` / `git diff --name-only` → mudanças SÓ em CLAUDE.md, PRD.md, README.md, tasks/backlog.md, tasks/current_task.md, context/ e docs/ (2 novos). ZERO mudança em src/motor_expansao/ (pipelines/scoring.py/config.py/dashboard/censo_*). As deleções `data/raw/ibge/*.geojson` são o worktree pré-sujo (não tocadas).

## Problemas encontrados
nenhum

## Pendências
- Housekeeping do BLK-API-01 (mover para tasks/completed.md) é do orquestrador no fechamento — não feito pelo Builder por instrução.
- A suíte FULL é responsabilidade do QA (gate único). Subconjunto + smoke rodados aqui conforme prompts/builder.md.

## Riscos remanescentes
- Ambiente local Python 3.14 difere do CI (3.11): contagem absoluta de testes pode variar; critério do QA é 0 falhas/0 erros de coleta, não o passed absoluto.

## Guardrails verificados
- score_priorizacao não alterado: sim (bloco só docs; ZERO escrita em src/scoring/config/pipelines).
- Artefatos M1 preservados: sim (nenhum artefato tocado; READ-ONLY M1 comprovado por git scope).
- Dashboard offline mantido: sim (nenhuma dependência de API ao vivo introduzida no dashboard; API é camada paralela documentada, sem código).
