# Handoff — QA (BLK-OPS-02 — CI completo + build via registry)

## Skill que gerou este handoff
QA / Quality Analyzer

## VEREDITO
**APROVADO COM RESSALVAS**

Resumo 1 linha: BLK-OPS-02 atinge a aceitação central (CI completo verde no runner limpo — 460 passed/73 skipped, 0 failed/0 errors; baseline local intacto 532 passed/1 skipped; workflow GHCR + compose pull + runbook deploy.md corretos; M1/score/artefatos NÃO tocados), com 2 ressalvas conhecidas e aprovadas pelo humano (ruff/mypy informativos → BLK-OPS-02b; Docker Publish verificado-na-fusão, dispara só no push à main).

## Justificativa
Re-executei por conta própria todas as validações (saída literal abaixo). Os critérios de aceite centrais do Planner/Orquestrador foram atingidos:
- (a) CI roda a suíte COMPLETA e fica verde no runner limpo: SIM — run `26664015146` (gatilho `pull_request`, commit `4af99de`) com job `test` = **pass**, `460 passed, 73 skipped, 8 warnings` (0 failed/0 errors). As 73 skips = testes acoplados a dado real ausente no runner, pulados por skip-guard justificado.
- Prova de não-mascaramento de regressão M1: LOCALMENTE, com o dado real presente nesta máquina, a suíte completa segue **532 passed, 1 skipped** — confirma que os skip-guards só pulam por ausência de path e voltam a validar 100% dos asserts (schema/contagem/scoring) quando o dado existe.
- (b) build publica no registry com tag por SHA: workflow `docker-publish.yml` existe e está correto (GHCR, `type=sha` + `latest` no default branch, `GITHUB_TOKEN`, `build-push@v6`, cache gha) — RESSALVA: só dispara em `push: main` (workflow_dispatch falha pré-merge por 404 — limitação documentada). Verificado-na-fusão: aceitável.
- (c) runbook `docs/deploy.md` modo pull: SIM — pull + up -d sem --build, rollback por SHA, guardrail §6, login GHCR, troubleshooting. Completo e correto.
- (d) ruff/mypy verdes: NÃO atingido (286 ruff + 23 mypy). RESSALVA aprovada pelo humano (Felipe Silva, 2026-05-29): rebaixados a `continue-on-error: true` (informativos) + saneamento em BLK-OPS-02b. Por decisão humana NÃO são bloqueadores deste veredito.

Escopo, guardrails e commit-por-path foram respeitados integralmente. Por isso o veredito é APROVADO COM RESSALVAS (as ressalvas são pré-aprovadas e rastreadas), não APROVADO pleno.

## Problemas críticos
Nenhum.

## Problemas médios
Nenhum bloqueante. Itens rastreados (não-bloqueantes, já aprovados):
- Dívida ruff/mypy (286+23) movida para **BLK-OPS-02b** — gate informativo até lá. Decisão humana registrada.
- Docker Publish não pôde ser disparado pré-merge (workflow_dispatch 404 "not found on default branch" — o workflow ainda não está na `main`; e não tem trigger `pull_request`). Dispara no `push: main` ao mergear. Tratado como "verificado-na-fusão" — aceitável e documentado no próprio workflow/Planner.

## Problemas leves
- Anotações do CI `X Process completed with exit code 1` (test:.github#2 e #43) referem-se EXCLUSIVAMENTE aos steps ruff/mypy `continue-on-error: true` — logam anotação de falha mas NÃO reprovam o job (job = pass). Comportamento esperado/desenhado; pode confundir leitura rápida da aba Actions até BLK-OPS-02b tornar bloqueante.
- Deprecação Node.js 20 em `actions/checkout@v4`/`setup-python@v5` (aviso do runner GitHub, não do código). Sem ação neste bloco.

## Testes faltantes
- `docker build -f Dockerfile.streamlit -t test:ci .` não re-executado: Docker indisponível nesta máquina. Sanity de build coberta pelo CI/merge (push à main dispara docker-publish que faz o build real). Não-bloqueante, conforme plano.
- Validação real do build/push GHCR só ocorrerá na fusão (ressalva b).

## Riscos
- Ao mergear, o `docker-publish.yml` roda pela 1ª vez no `push: main` — se houver problema de Dockerfile/contexto, aparece só lá. Mitigado: o build local de sanity é independente do compose e o `.dockerignore` exclui `.env`/`data`/`concorrentes`/`secrets` (defesa em profundidade). Recomenda-se acompanhar o run de docker-publish pós-merge.
- A dívida ruff/mypy permanece latente em produção/M1 (hex_enrichment.py, base_h3_brasil.py); enquanto informativa, regressões de lint/tipo não reprovam o gate. Endereçado em BLK-OPS-02b.

## Saída literal de cada validação re-executada
1. `git check-ignore tests/fixtures/sample.parquet ; echo EXITCODE=$?`
   ```
   (saída vazia)
   EXITCODE=1
   ```
   → Critério OK: NÃO ignorado (exit 1, saída vazia). Negação `!tests/fixtures/` no .gitignore funciona.

2. `python -m pytest -q` (LOCAL, dados reais nesta máquina) — linha final:
   ```
   532 passed, 1 skipped, 9 warnings in 146.41s (0:02:26)
   ```
   → Critério OK: `532 passed, 1 skipped`, sem regressão. Skip-guard NÃO mascara regressão de M1 quando o dado existe.

3. `python -c "import streamlit_app; print('import ok')"`
   ```
   (warnings de ScriptRunContext/MemoryCacheStorageManager do Streamlit em bare mode — esperados)
   import ok
   ```
   → Critério OK: imprime `import ok`.

4. `python -m pytest -q tests/integration/test_streamlit_app.py` — linha final:
   ```
   147 passed in 17.60s
   ```
   → Critério OK: 0 failed.

5. Lint/tipos (INFORMATIVO/não-bloqueador por decisão humana):
   `ruff check .`
   ```
   Found 286 errors.
   [*] 219 fixable with the `--fix` option (46 hidden fixes can be enabled with the `--unsafe-fixes` option).
   ```
   `mypy src/`
   ```
   Found 23 errors in 6 files (checked 18 source files)
   ```
   → Registrado. Não-bloqueador (BLK-OPS-02b).

6. Verificação externa (PR checks):
   `gh pr checks 1 --repo Kastaldy/motor-de-expansao`
   ```
   test	pass	2m22s	https://github.com/Kastaldy/motor-de-expansao/actions/runs/26664015146/job/78593165759
   ```
   `gh run view 26664015146 --repo Kastaldy/motor-de-expansao`
   ```
   ✓ ciclo/BLK-OPS-02 CI Kastaldy/motor-de-expansao#1 · 26664015146
   Triggered via pull_request about 10 minutes ago
   JOBS
   ✓ test in 2m22s (ID 78593165759)
   ANNOTATIONS
   ! Node.js 20 actions are deprecated. [...]
   X Process completed with exit code 1.  (test: .github#2)   ← step ruff (continue-on-error)
   X Process completed with exit code 1.  (test: .github#43)  ← step mypy (continue-on-error)
   ```
   `gh run view 26664015146 --log | grep passed`
   ```
   test	Testes (suite completa)	2026-05-29T21:52:40Z 460 passed, 73 skipped, 8 warnings in 71.42s (0:01:11)
   ```
   → Critério OK: CI **success** no commit `4af99de`; suíte completa `460 passed, 73 skipped`, 0 failed/0 errors. Os `X exit code 1` são os steps ruff/mypy não-bloqueantes (não reprovam o job).

7. `docker build` — Docker indisponível nesta máquina. Sanity de build coberta pelo CI/merge (docker-publish no push à main). Não-bloqueador, conforme plano.

## Conferência de NO-BYPASS (escopo da regra)
- A regra anti-bypass mira TOOLING acoplado a config/artefatos reais de produção (ex.: SOPS). Os skip-guards do pytest **NÃO são bypass**: são prática legítima de pular testes quando o dado real (gitignored, ausente no runner) não está presente. PROVA de não-mascaramento: localmente, com o dado real, a suíte completa segue **532 passed, 1 skipped** (validação 2). CONFIRMADO.
- Inspeção do diff de testes (`git diff 092a43b..4af99de -- tests/`): o Builder SÓ converteu `assert PATH.exists()` em `pytest.skip`/`skipif`. Nenhum assert de schema/contagem/scoring tocado. Verifiquei via Grep em `test_modelo_mercado_hexagonos.py` que permanecem INTACTOS: `score_oficial == score_priorizacao` (linhas 370, 386), `score_oficial_nome == {"score_priorizacao"}` (388), `osm_status == {"nao_aplicado_mvp_nacional"}` (390), row-counts `1000/223/472` (28-30), `len == 150` (146). Os `len == 54` dos demais arquivos não aparecem no diff (não tocados). CONFIRMADO — nenhum assert M1 alterado.

## Conferência cruzada com log do Builder
- Builder (handoff corrente + snapshot 184859) afirmou: psutil declarado em `dependencies` core; baseline local 532/1; CI re-disparado passa da coleta; ruff/mypy informativos. Re-verifiquei tudo de forma independente:
  - `pyproject.toml`: o diff do ciclo adiciona EXATAMENTE `"psutil>=5.9.0"` na seção `dependencies` (após `click>=8.1.0`). CONFERE.
  - Baseline local 532/1: CONFERE (validação 2).
  - CI verde no runner limpo (460/73): CONFERE (validação 6) — coleta não aborta mais (psutil presente).
  - scipy avaliado e descartado pelo Builder (não está no grafo de coleta da suíte): aceito — a suíte verde no runner limpo (sem scipy adicionado) é a prova empírica.
- Builder ff63a54 afirmou skip-guards só convertendo `assert exists()`: CONFERE (diff de testes inspecionado hunk-a-hunk).
- Nenhuma divergência entre o log do Builder e minha evidência própria.

## Guardrails verificados
- **Score/M1 NÃO alterados:** `git diff 092a43b..4af99de --name-only | grep -iE "src/|config.py|scoring|data/outputs|\.parquet"` → VAZIO. Nenhum arquivo de scoring/config/pipeline de score/parquet oficial no diff. CONFIRMADO.
- **Parâmetros canônicos intactos:** H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, pesos 0.40/0.60 NÃO aparecem no diff. CONFIRMADO.
- **Escopo respeitado:** diff stat = só CI/infra/docs/testes(skip)/controle/handoff/pyproject(psutil). `PRD.md` NÃO tocado (grep vazio). CONFIRMADO.
- **Edição não-relacionada BLK-SCORE-01** (`tasks/backlog.md` — "Engenharia do Corpo"/`alunos_totais`): permanece **NÃO-commitada** (`git status` = só ` M tasks/backlog.md`). NÃO foi arrastada para os commits do ciclo. CONFIRMADO.
- **Commit isolado por path:** ff63a54 e 4af99de contêm só paths do ciclo (CI/workflows/compose/.dockerignore/.gitignore/docs/tests-skip/controle/handoff/pyproject). CONFIRMADO.
- **VPS (§6):** nenhum comando executado no servidor. CONFIRMADO.

## Decisão recomendada
**Liberar para MERGE humano** (`ciclo/BLK-OPS-02` → `main`). A aceitação central está provada com evidência própria; as 2 ressalvas (ruff/mypy informativo → BLK-OPS-02b; Docker Publish verificado-na-fusão) são conhecidas e aprovadas pelo humano. Pós-merge: acompanhar o 1º run de `docker-publish.yml` no push à main para confirmar a publicação real da imagem no GHCR.
