# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
**Builder** (bloco de PESQUISA/ANÁLISE, READ-ONLY sobre o M1; embora a criticidade seja
**Estratégica**, NÃO toca `score_priorizacao`/`hex_score_estrutural`/pesos/artefatos M1 — é
relatório que só LÊ; esteira do loop = BO → Planner → Builder → QA, sem gate humano extra).
A DECISÃO real de rebuild vs refactor NÃO é deste bloco — é do **BLK-REV-12** (gate humano + DEC).

## Entendimento da tarefa
Escrever UM relatório Markdown `data/analysis/avaliacao_stack.md` que consolida os achados
dos REV-01..06 numa **matriz de decisão (4 opções de stack × 7 critérios) + recomendação
PRELIMINAR**, partindo da topologia multi-container REAL de produção, sem decidir nada (a
decisão é do REV-12) e sem tocar código/M1.

## Plano técnico

O Builder deve CRIAR o arquivo `data/analysis/avaliacao_stack.md` com EXATAMENTE as 8 seções
abaixo, nesta ordem. Não implementar código, não medir nada de novo — só citar os números já
produzidos nos REV-01..06. Cada número citado deve vir com a âncora da fonte (ex.: "REV-03").

### Passo 1 — Cabeçalho + metadados (topo do arquivo)
Bloco de citação (`>`) com: ID do bloco (BLK-REV-07); data (2026-07-10); que é READ-ONLY
sobre o M1; que CONSOLIDA os REV-01..06 (citar, não re-medir); que a DECISÃO final é do
BLK-REV-12; e as fontes lidas (os 6 relatórios REV-01..06 + `docs/arquitetura_app_atual.md`
+ `docker-compose.prod.yml`).

### Passo 2 — Seção 1: Sumário executivo (≤ 8 linhas)
- Afirmar a **premissa da topologia real** (ponto de partida obrigatório): produção já é
  **multi-container** (`streamlit` + `api` FastAPI + `telegram-bot` + `caddy` + `authelia`),
  com dados em **volumes `:ro`** (`/opt/motor-expansao/data/outputs`, `~1,6 GB`) que
  `streamlit` E `api` **já compartilham**. Logo `api`/Caddy/volumes JÁ existem → o custo de
  (b)/(d) é "trocar o container `streamlit` por um frontend estático servido pelo Caddy que já
  existe, reusando a `api`", **não** reescrever do zero.
- Afirmar que **"offline §2" = "sem serviço EXTERNO ao vivo"**, NÃO "sem backend"; a `api` na
  VPS lendo os volumes locais NÃO viola o §2 (tiles/geocoding são exceções DEC-004/010/011).
  **NÃO re-litigar** isso — partir daqui como dado.
- Encerrar com a **recomendação preliminar em 1 frase** (ver Passo 6) + a ressalva de que a
  decisão é do REV-12.

### Passo 3 — Seção 2: As 4 opções em uma frase cada
Listar as 4 opções EXATAMENTE como no handoff do BO (sem adicionar/remover):
- **(a) Manter Streamlit + otimizar** — `@st.cache_data`/`@st.cache_resource` nos builders,
  `@st.fragment` (já parcial), tooltip enxuto, cap, shared transformer no PDF.
- **(b) Frontend React/Next.js (SPA estático servido pelo Caddy) + a `api` FastAPI existente.**
- **(c) Dash / Panel.**
- **(d) Frontend custom com deck.gl/MapLibre client-side + a `api`.**

### Passo 4 — Seção 3: A MATRIZ DE DECISÃO (o núcleo — obrigatória)
Uma tabela Markdown com **4 linhas (a,b,c,d) × 7 colunas de critério** + 1 coluna final de
"posição no espectro". Cada célula = veredito curto (ex.: Alto/Médio/Baixo ou +/0/−) **com a
âncora numérica** entre parênteses quando houver evidência REV. As 7 colunas (na ordem do BO):

1. **Performance (mapa + troca de cor/seleção sob rerun)** — ancorar nos números:
   - REV-01/03: render `build_map_figure` 0,65–0,97 s Python-side; `deck.to_json()` 0,6–1,5 s;
     payload **21–24 MB**; **~1,21 KB/hex**, linear em N; **~65% do payload = as 14 strings de
     tooltip** (15,7 de 24 MB); geometria só ~8,5%.
   - REV-04: troca de cor recompõe o builder inteiro (0,7–3,3 s/rerun); só `fill_color` (~8,5%
     do payload) muda entre modos; sem cache nenhum nos builders.
   - REV-05: add/remove hex reconstrói o mapa (700–900 ms) por rerun completo.
   - REV-06: PDF Pontual gargalo = transformer pyproj por setor (77% do tempo; fix O1 = shared
     transformer, **86×**, 4,5 s → ~0,7 s); independente da escolha de stack.
   - Vereditos esperados (o Builder fundamenta): (a) teto REAL no rerun+`to_json` mesmo com
     cache/tooltip enxuto (o payload continua sendo montado em Python e trafegando no WebSocket);
     (d) **maior teto** (MVT/deck.gl client-side: payload ~constante, não ∝ N; browser busca
     tiles) mas o ganho é EMPÍRICO a confirmar (não medimos browser/WebGL — ver Seção 7 e REV-08);
     (b) igual a (d) SE usar deck.gl/MapLibre no SPA, senão herda o teto de render client-side
     conforme a lib; (c) Dash/Panel ainda serializa server-side como Streamlit → teto similar a (a).
2. **Controle de UX (progressive disclosure para leigos — dor #5)** — (a)/(c) limitados ao
   modelo de widgets Python top-down; (b)/(d) dão controle total de UX (React), ao custo de
   frontend próprio.
3. **Preservação do offline §2** — TODAS conseguem (dado local na VPS); comparar COMO: (a)/(c)
   servem tudo do processo Python; (b)/(d) = SPA estático servido pelo Caddy + `api` lendo os
   volumes `:ro` (mesmo modelo do container atual → offline preservado 100%). Tiles/geocoding
   seguem como exceção pontual (DEC-004/010/011), igual hoje.
4. **Reuso da `api`/Caddy/volumes já existentes** — (a)/(c) reusam pouco da `api` (o dashboard
   não a consome hoje) mas mantêm todo o resto; (b)/(d) **reusam Caddy + volumes + a `api`
   FastAPI já pronta** (que já expõe `POST /analisar`, `/analisar-municipio`, `/health` lendo os
   mesmos volumes) → maior aproveitamento de infra pronta; precisariam ESTENDER a `api` com
   endpoints de dados do mapa (hexes por UF/filtro).
5. **Velocidade/custo de dev e manutenção por perfil de time** — (a)/(c) **Python-only**
   (menor atrito para time Python; (a) reusa 100% do código atual); (b)/(d) exigem stack
   **JS/TS** (React/deck.gl) = novo perfil de time, dois runtimes p/ manter.
6. **Risco de migração** — (a) **baixo** (só otimizações incrementais no código existente);
   (c) **alto** (reescrever a UI inteira noutro framework Python, sem ganho de teto claro →
   pior custo/benefício); (b)/(d) **médio-alto** (frontend novo, mas backend/proxy/volumes
   já existem → menor do que "rebuild do zero"; (d) concentra o risco no componente de mapa).
7. **Espectro incremental→cirúrgico→rebuild (posição)** — ver Passo 5 (coluna final da matriz).

Cada célula deve ser DEFENSÁVEL pelos números; proibido "achismo" sem âncora onde há evidência.

### Passo 5 — Seção 4: Mapeamento do espectro incremental → cirúrgico → rebuild
Texto + a coluna final da matriz situando cada opção:
- **Incremental** (menor risco, teto limitado): (a) cache+`@st.fragment`+tooltip enxuto+cap+
  shared transformer no PDF — tudo já diagnosticado como fix nº1 nos REV-03/04/05/06.
- **Cirúrgico** (trocar SÓ o componente de mapa por client-side, mantendo o resto Streamlit):
  ponto intermediário — ataca o teto do `to_json`/payload (REV-03 opção 4/MVT) sem reescrever
  a app inteira; alto risco localizado no mapa (componente customizado, descartado no ciclo
  anterior por §5, mas é a fronteira que o REV-08 testaria empiricamente).
- **Rebuild** (maior teto, maior custo/risco): (b)/(d) — frontend novo sobre a `api`/Caddy
  existentes. (b) = SPA genérico; (d) = SPA focado no mapa client-side (deck.gl/MapLibre).
Deixar explícito que (a)=incremental, (c)=rebuild-lateral (reescreve UI sem mudar o teto),
(b)/(d)=rebuild sobre infra existente, e o "cirúrgico" é a via do REV-08.

### Passo 6 — Seção 5: Recomendação PRELIMINAR (fundamentada, NÃO veredito)
- Recomendar como PRIMEIRO passo de baixo risco/alto ROI a trilha **incremental (a)**: aplicar
  os fixes já diagnosticados (`@st.cache_data`/`@st.cache_resource` nos builders — REV-04 H2;
  `@st.fragment` no painel multi-hex — REV-05; tooltip enxuto + cap — REV-03; shared transformer
  no PDF — REV-06 O1), porque atacam os gargalos MEDIDOS com risco baixo e sem trocar de stack.
- Registrar HONESTAMENTE que (a) tem **teto real**: mesmo com cache e tooltip enxuto, o modelo
  rerun+`to_json`+WebSocket continua montando e trafegando o payload em Python; se o teto de (a)
  não bastar para a escala desejada, a evolução natural é **(d) o mapa client-side** (deck.gl/
  MapLibre + MVT sobre a `api`), cujo teto superior é PLAUSÍVEL mas **ainda não comprovado
  empiricamente** — é o que o **BLK-REV-08 (spike)** confirmaria/refutaria.
- Registrar que **(c) Dash/Panel** tem o PIOR custo/benefício (reescrever a UI sem ganhar teto)
  e que **(b)** só se justifica se houver requisito de UX além do mapa que Streamlit não cobre.
- **Ser honesto nos DOIS sentidos** (anti-viés): (a) tem teto; (d)/(b) carregam risco de
  migração + custo de perfil de time JS/TS num time hoje Python-only. É PRELIMINAR, não veredito.

### Passo 7 — Seção 6: O que fica EXPLICITAMENTE para o BLK-REV-12 decidir
Frase clara: a decisão rebuild vs refactor é do **BLK-REV-12** (gate humano + DEC registrada);
este relatório só embasa. Listar as perguntas em aberto que o REV-12 precisa fechar (ex.: a
escala-alvo de hexes/usuários simultâneos; se há apetite por stack JS/TS no time; se o teto de
(d) medido pelo REV-08 justifica o custo). Este bloco **NÃO** escolhe.

### Passo 8 — Seção 7: O que NÃO foi medido (herdar caveats dos REV-01..06)
Listar, herdando as caveats: (i) **render WebGL/paint no browser** não medido (headless);
(ii) **transferência WebSocket + `JSON.parse`/hidratação no deck.gl** não cronometrados (só o
tamanho 21–24 MB como proxy); (iii) **latência real de rede** VPS↔cliente; (iv) **fetch real de
tiles** (contextily ausente no env de medição); (v) o **teto empírico de (d)** só o **BLK-REV-08
(spike deck.gl/MapLibre, manual/NÃO loop-safe)** confirmaria; (vi) o loop **não enxerga a UI** —
este bloco mede o lado Python/arquitetural. Encerrar com nota de guardrails READ-ONLY M1.

### Passo 9 — Validações (o Builder roda no fecho)
- `git check-ignore data/analysis/avaliacao_stack.md` → deve imprimir o caminho (gitignored). [já
  confirmado pelo Planner: retorna o caminho, exit 0].
- `python scripts/loop_guard.py --base HEAD` → **GUARD OK** (nenhum caminho proibido tocado).
- `git diff HEAD -- src/motor_expansao/pipelines/m1/ src/motor_expansao/config.py` → **vazio**.
- `git status --porcelain data/analysis/avaliacao_stack.md` → arquivo é novo/untracked, mas
  gitignored (não deve aparecer em `git status` sem `--ignored`) — confirma que não vaza para o commit.

## Arquivos afetados
- **CRIAR:** `/repo/data/analysis/avaliacao_stack.md` (único entregável; gitignored via `data/analysis/`).
- `/repo/context/handoff.md` + snapshot em `/repo/context/handoff/` (fluxo da esteira).
- `/repo/tasks/current_task.md` (housekeeping — Próxima Skill: Builder).
- NENHUM arquivo em `src/`, `config.py`, `pipelines/`, `data/outputs|staging` ou artefato M1.

## Dependências
- Nenhuma dependência de execução/código. Só depende dos 6 relatórios REV-01..06 já produzidos
  (existem em `data/analysis/` e `docs/`) e do `docker-compose.prod.yml` (existe).
- Sem instalação de pacotes; não roda pipeline; não lê dados pesados.

## Riscos técnicos
- **Viés pró-status-quo OU pró-rebuild:** a matriz deve ser honesta nos dois sentidos — (a) tem
  teto real (rerun/`to_json`); (d)/(b) têm risco de migração + custo de perfil de time. É
  recomendação PRELIMINAR, não veredito.
- **Tratar a produção como monólito** (erro clássico): partir da topologia multi-container real;
  subestimar a `api`/Caddy/volumes já prontos inflaria artificialmente o custo de (b)/(d).
- **Re-litigar o offline §2** (proibido) ou re-medir performance (desnecessário — citar REV-01..06).
- **Confundir o inventário REV-02:** ele está em `docs/arquitetura_app_atual.md`, **NÃO** em
  `data/analysis/inventario_*` (o nome no prompt do orquestrador está errado).
- **Inventar opções** além das 4 (a,b,c,d) — proibido adicionar/remover.

## Critérios de aceite finais
- `data/analysis/avaliacao_stack.md` existe e está gitignored (`git check-ignore` confirma o caminho).
- Contém a **matriz completa: 4 opções (a,b,c,d) × 7 critérios**, preenchida, + coluna de espectro.
- Cada célula/opção **ancora os números nas evidências REV-01..06** (payload 21–24 MB, tooltip
  65%, `to_json` 0,6–1,5 s, rerun 700–900 ms, PDF transformer 86×, ~1,21 KB/hex), não em achismo.
- Parte explicitamente da **topologia multi-container real** e afirma que `api`/Caddy/volumes já
  existem, reduzindo o custo de (b)/(d).
- Mapeia o **espectro incremental→cirúrgico→rebuild** e situa cada opção nele.
- Entrega **recomendação PRELIMINAR** clara e diz **explicitamente que a decisão é do REV-12**.
- Tem seção "o que NÃO foi medido" herdando caveats de browser/WebGL/rede/tiles dos REV-01..06.
- READ-ONLY M1 verificável (loop_guard OK; diff M1 vazio).

## Validações obrigatórias
- Comando: `git check-ignore data/analysis/avaliacao_stack.md`
  Critério mínimo: imprime o caminho (exit 0) → artefato gitignored.
- Comando: `python scripts/loop_guard.py --base HEAD`
  Critério mínimo: **GUARD OK** (nenhum caminho proibido no diff).
- Comando: `git diff HEAD -- src/motor_expansao/pipelines/m1/ src/motor_expansao/config.py`
  Critério mínimo: saída **vazia** (nenhuma alteração no M1/config).

## Fora de escopo
- **NÃO DECIDIR** rebuild vs refactor — é do BLK-REV-12 (gate humano + DEC).
- **NÃO implementar** otimização nem protótipo (o spike de mapa client-side é o BLK-REV-08).
- **NÃO re-litigar** o requisito offline §2 nem re-medir performance (usar REV-01..06).
- **NÃO adicionar/remover** opções além das 4 (a,b,c,d).
- **NÃO tocar** `src/`, `config.py`, `pipelines/m1/`, artefatos oficiais do M1, VPS/deploy/rede.
- Não expandir para outros blocos da epic REV (08..12).

## Alerta de aprovação
Sem alerta de aprovação humana adicional. Apesar da criticidade **Estratégica** (embasa
rebuild vs refactor), o bloco é **READ-ONLY sobre o M1** — NÃO toca `score_priorizacao`,
`hex_score_estrutural`, carteira, plano nem artefatos oficiais; é pesquisa/relatório que só LÊ,
marcado **loop-safe**. A DECISÃO real fica no BLK-REV-12 (esse sim com gate humano + DEC).

## Guardrails ativos (de CLAUDE.md)
- **§5 guardrail permanente:** visualizações, análises e relatórios são **READ-ONLY sobre o M1**
  — NÃO recalculam `score_priorizacao`, `hex_score_estrutural`, carteira, plano nem artefatos
  oficiais. Pesos `renda=0.40`/`pop=0.60` e fórmula INALTERADOS.
- **§2:** dashboard/produção sem dependência de API ao vivo (offline sobre Parquets locais); a
  `api` lendo volumes locais NÃO viola isso (esclarecido no ponto de partida do BO).
- **§6.1 loop-safe:** READ-ONLY M1; NÃO toca VPS/deploy/segredos; NÃO persiste PII; consome
  `data/staging`/`data/outputs` sem ingestão ao vivo; relatório em `data/analysis` (gitignored).
- **§6:** proibido executar comandos na VPS sem confirmação (não aplicável — bloco não toca VPS).
