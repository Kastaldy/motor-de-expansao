# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador): housekeeping move BLK-OPS-11 backlog→completed + commit por path
+ confirmação do CI real (`gh run watch`, Python 3.11) antes do merge de `ciclo/BLK-OPS-11`.

## VEREDITO
APROVADO COM RESSALVAS

## Justificativa
A CURA está provada por conta própria e é independente de versão: o guard `if isinstance(default,
property): continue` no `__init__` do ramo fallback elimina o `AttributeError: property 'CORES' ...
has no setter`; reproduzi o estado exato do CI (`pydantic_settings` ausente) e confirmei 627 testes
coletados com 0 erros de collection sob fallback forçado (vs. 12 erros pré-fix), além de provar que
o MESMO cenário falhava antes da correção. Escopo respeitado e guardrails M1 intactos. Ressalva
única e NÃO bloqueante no plano local: as versões reais resolvidas pelo CI 3.11 dentro das faixas
pinadas e o run verde de ponta a ponta no GitHub Actions só podem ser confirmados pelo
orquestrador/humano no fechamento (Python local = 3.14 ≠ CI 3.11) — a cura, porém, não depende disso.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- `@property CORES` está duplicada nos dois ramos (pydantic e fallback) de `config.py` — dedup fora
  de escopo deste bloco; candidato a follow-up de higiene.
- Tetos amplos (`pandas<3`, `numpy<2.4`, `pyarrow<24`, `streamlit<2`) adiam (não eliminam) o risco de
  incompat futura — dívida controlada e explicitada pelo Builder.

## Testes faltantes
- nenhum para o escopo. O novo `tests/unit/test_config_fallback.py` cobre exatamente a regressão
  (instancia sob fallback + CORES + env-override). O ramo pydantic já é exercitado pela suíte normal.

## Riscos remanescentes
- **Paridade Python (não bloqueante p/ veredito local):** as versões que o CI 3.11 resolve dentro
  das faixas (`ruff>=0.15,<0.16`, `mypy>=1.10.0,<2`, etc.) NÃO foram verificadas localmente; o
  critério "CI verde de ponta a ponta" será confirmado pelo orquestrador via `gh run watch`. Se o
  `pip install -e ".[dev]"` no 3.11 não encontrar versão na faixa do floor, o gate reprova — confirmar
  no run real.
- **mypy / stub `types-requests` (observação, NÃO falso-verde do gate):** `requests` é importado em 3
  módulos `src/` e `types-requests` NÃO está declarado no extra `dev` do `pyproject.toml` (só está no
  meu env local). Porém `[tool.mypy] ignore_missing_imports = true` faz o `mypy src/` ignorar o stub
  ausente — logo o step NÃO erra no CI por falta do stub (sem falso-verde de gate; o gate é
  consistente local↔CI). É config pré-existente, fora do escopo deste bloco; registrado como
  observação para não confundir com a lição de paridade mypy do BLK-OPS-02b-FU1.

## Saída literal das validações (re-executadas pelo QA)

### `ruff check . --no-cache`
```
All checks passed!
```

### `mypy src/`
```
src\motor_expansao\pipelines\m1\ibge_censo.py:158: note: By default the bodies of untyped functions are not checked, consider using --check-untyped-defs  [annotation-unchecked]
src\motor_expansao\pipelines\m1\ibge_censo.py:159: note: By default the bodies of untyped functions are not checked, consider using --check-untyped-defs  [annotation-unchecked]
src\motor_expansao\pipelines\m1\poi_enrichment.py:51: note: By default the bodies of untyped functions are not checked, consider using --check-untyped-defs  [annotation-unchecked]
Success: no issues found in 46 source files
```
(Apenas notes informativos `annotation-unchecked`; 0 erros. `types-requests==2.33.0.20260518`
instalado no env local; ver Riscos para a análise do CI — `ignore_missing_imports=true` evita
falso-verde de gate.)

### `python -m pytest -q tests/unit/test_config_fallback.py`
```
..                                                                       [100%]
2 passed in 0.23s
```

### `python -m pytest -q` (suíte completa)
```
626 passed, 1 skipped, 9 warnings in 84.46s (0:01:24)
```
0 falhas, 0 erros de collection. Bate EXATAMENTE com o Builder (626 passed, 1 skipped).

### `python -m pytest -q tests/integration/test_streamlit_app.py`
```
150 passed in 6.22s
```

### `python -c "import streamlit_app; print('import ok')"`
```
import ok
```
(Precedido por warnings benignos de `ScriptRunContext`/`MemoryCacheStorageManager` — esperados ao
importar Streamlit em modo bare.)

### PROVA DA CURA — fallback forçado APÓS a correção (reproduzido pelo QA, sem usar o teste do Builder)
```
BaseSettings is None: True
CORES len: 9 purple: #6B21A8
constants reload ok under fallback; CORES via constants? n/a
H3_RESOLUTION: 7
```

### PROVA DA CURA — `pytest --collect-only` sob fallback forçado (plugin QA temporário que torna `pydantic_settings` inimportável)
```
627 tests collected in 3.95s
```
Filtro de erros (`grep -iE "error|interrupted|collected"`):
```
627 tests collected in 1.96s
```
0 erros de collection. Sanidade — sob o mesmo plugin, `BaseSettings is None: True` (fallback de fato
ativo) e `motor_expansao.core.constants` (um dos 12 módulos antes afetados) importa OK. Plugin
temporário removido após a prova; `git status` não mostra resíduo.

### PROVA DO ESTADO ANTERIOR — pré-fix falhava no MESMO cenário
`git stash` do `config.py` (volta ao HEAD, sem o guard) + fallback forçado:
```
=== config.py reverted to pre-fix (HEAD) ===
EXPECTED FAILURE: AttributeError property 'CORES' of 'Settings' object has no setter
```
Restaurado o fix em seguida (`git diff --stat -- src/motor_expansao/config.py` = `2 insertions`;
guard presente na linha 149). Confirma que o guard é a ÚNICA mudança de comportamento que cura a
collection.

### Housekeeping (estado PRÉ-move — o move é do orquestrador no fechamento)
`python scripts/housekeeping_move_block.py BLK-OPS-11 --check`:
```
FALHA --check: stub ausente no backlog para BLK-OPS-11
exit=1
```
FALHA ESPERADA pré-move (stub ainda ausente; heading `### BLK-OPS-11` ainda presente como bloco
completo na linha 135 do `backlog.md`, NÃO stub). Builder NÃO moveu o bloco — correto.

`python -m pytest -q tests/unit/test_housekeeping_helper.py`:
```
..........                                                               [100%]
10 passed in 0.08s
```

## Conferência de no-bypass
- Confirmo que NENHUMA validação usou `--config /dev/null`, fixture fora do caminho real ou mock do
  caminho crítico para forjar verde.
- O novo `test_config_fallback.py` exercita o RAMO FALLBACK REAL: o shim de `builtins.__import__`
  levanta `ModuleNotFoundError` para `pydantic_settings` e o teste asserta `cfg.BaseSettings is None`
  ANTES de instanciar `Settings()` — ou seja, prova que `Settings` é a classe do `else` (não a
  pydantic) e que `CORES` (a `@property` que quebrava) resolve. NÃO é mock do caminho crítico; é a
  reprodução fiel do ambiente do CI. O `_coerce_env_value` é exercitado de verdade no
  `test_fallback_env_override` (`H3_RESOLUTION=9` → `int` 9).
- A "prova da cura" do QA foi feita por conta própria, independente da fixture do Builder (reload
  manual + plugin de collection), e bate com o resultado dele.
- Nenhum teste silenciado: `git diff` não adiciona `skip`/`xfail` em arquivos de teste (as
  ocorrências de "skip/xfail" no diff são apenas texto de prosa em handoff/backlog). As 6 marcas
  skip/xfail existentes em `tests/` são pré-existentes, em outros arquivos, fora deste diff.

## Conferência cruzada com log do Builder
- BATE. `ruff` (All checks passed), `mypy src/` (Success, 0 erros, só notes), `test_config_fallback`
  (2 passed), suíte completa (626 passed, 1 skipped — idêntico), streamlit (150 passed), smoke
  import (ok) e a prova da cura (BaseSettings None, CORES len 9, 12/12 módulos sob fallback) batem
  com o handoff do Builder. Diferença de tempo da suíte (84s QA vs 207s Builder) é ambiente, não
  resultado.

## Guardrails verificados
- **score_priorizacao não alterado:** sim. `git diff -- src/motor_expansao/scoring.py` e
  `core/scoring.py` vazios; `core/constants.py` vazio. Pesos `renda=0.40`/`pop=0.60`
  (`PESOS_HEX_SCORE_ESTRUTURAL`) intactos.
- **Artefatos M1 preservados:** sim. `git diff --stat -- src/` mostra SÓ `config.py` (2 insertions);
  nenhum artefato M1 regerado; `git status --porcelain` não mostra `PRD.md` nem artefatos não
  relacionados.
- **Parâmetros canônicos:** `H3_RESOLUTION=7`, `DIST_MIN_ULTRA_KM=1.0`, `RENDA_MIN=4500.0`
  confirmados em `config.py` (ambos os ramos).
- **Escopo do `config.py`:** mudou SÓ o `__init__` do ramo fallback (1 guard de property). Ramo
  pydantic, dict/`@property CORES` e `_coerce_env_value` INTOCADOS (confirmado por `git diff`).
- **Escopo do `pyproject.toml`:** aplicou exatamente os tetos/faixas do plano (streamlit<2, pandas<3,
  pyarrow<24, numpy<2.4, scikit-learn<1.8; ruff>=0.15,<0.16, mypy>=1.10.0,<2 — NÃO `==2.1.0`,
  pytest>=8.2.0,<10, pytest-asyncio>=0.23.0,<0.25). Extra `api` NÃO tocado (pydantic/pydantic-settings
  com floors originais).
- **Escopo do `ci.yml`:** só trocou Lint para `ruff check . --no-cache`. NÃO adicionou o extra `api`;
  `pip install -e ".[dev]"` mantido (fallback é o caminho suportado/testado).
- **Validações re-executadas pelo QA (sem bypass):**
  - `ruff check . --no-cache` → 0 erros
  - `mypy src/` → 0 erros (com `ignore_missing_imports=true`; sem falso-verde de gate)
  - `pytest tests/unit/test_config_fallback.py` → 2 passed
  - `pytest -q` → 626 passed, 1 skipped, 0 erros de collection
  - `pytest tests/integration/test_streamlit_app.py` → 150 passed
  - `import streamlit_app` → ok
  - Prova da cura (fallback forçado) → 627 collected, 0 erros; pré-fix falhava (AttributeError)
  - housekeeping `--check` → FALHA esperada pré-move; `test_housekeeping_helper.py` → 10 passed
- **Escopo respeitado:** sim.

## Decisão recomendada
Fechar ciclo (orquestrador/humano): (1) housekeeping move BLK-OPS-11 backlog→completed via
`scripts/housekeeping_move_block.py`; (2) commit por path (NUNCA `git add -A`; NÃO arrastar
`PRD.md`); (3) confirmar o run verde de ponta a ponta no GitHub Actions (Python 3.11) via
`gh run watch` ANTES do merge de `ciclo/BLK-OPS-11` — essa é a única pendência que sustenta a
ressalva. Nenhum bloco de correção necessário.
