# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-RELMUN-03 — Validar hexágono só por Residual Fitness (remover o filtro de SAM Fitness).
APROVADO POR Vinicius EM 2026-07-02 (produto D-PROD-1 = REMOVER a constante `SAM_DESTAQUE_MIN`;
emenda à DEC-011).

## O que foi feito
Removido o termo de SAM Fitness (`sam_fitness_potencial >= 3000`) do critério de "hexágono
destacado/válido" do Relatório Municipal. `_hex_destacado_mask` agora retorna SOMENTE
`oferta_efetiva_disponivel >= OFERTA_DESTAQUE_MIN (2000)` (Residual Fitness). Fonte única do
critério — consumidores (`agregar_municipio`, `_zonas_geometricas`, mapa/cobertura) herdam a
mudança automaticamente. Textos/docstrings/notas de PDF propagados. Constante `SAM_DESTAQUE_MIN`
REMOVIDA (D-PROD-1). Emenda formal à DEC-011 registrada em CLAUDE.md §8. 2 testes de guard de
regressão do SAM adicionados. READ-ONLY sobre o M1.

Detalhamento por passo do plano do Planner:
- Passo 1: corpo de `_hex_destacado_mask` → `oferta = pd.to_numeric(...); return oferta >= OFERTA_DESTAQUE_MIN`.
  Guard `if df_muni.empty` preservado. Semântica NaN-safe preservada (`NaN >= 2000 == False`).
- Passo 2: docstring de `_hex_destacado_mask` → só Residual Fitness.
- Passo 3 (D-PROD-1 = REMOVER): apagada a linha `SAM_DESTAQUE_MIN = 3000.0`; adicionada a linha
  explicativa ao comentário do bloco de constantes. Confirmado por grep que nenhum outro
  módulo/teste importa `SAM_DESTAQUE_MIN` (só aparecia em relatorio_municipal.py + docs/handoffs/backlog).
- Passo 4: propagados os 5 textos — docstring do módulo (D1), docstring de `_zonas_geometricas`,
  nota "Potencial de Entrada", comentário + nota "Resumo da Região".
- Passo 5: testes — asserção do PDF (`assert b"SAM Fitness" not in pdf_bytes` + mantém
  `b"Residual Fitness >= 2.000"`), wording de 2 docstrings de fixture (só texto, valores numéricos
  intocados), import de `_hex_destacado_mask`, e os 2 testes novos (5.3 e 5.4) LITERAIS do plano.
- Passo 6: emenda à DEC-011 inserida em CLAUDE.md §8 (placeholder da constante preenchido com
  "REMOVIDA").

## Desvio necessário do literal do plano (registrado)
O literal do Passo 4a (docstring do módulo, D1) do Planner terminava com "termo de SAM Fitness
REMOVIDO", que contém a bigrama "SAM Fitness" e faria o grep de ausência obrigatório
(`SAM Fitness|SAM_DESTAQUE_MIN|sam_fitness_potencial` no módulo) sair NÃO-vazio, violando o
critério de aceite. Reconciliei escrevendo "termo de SAM removido" (mesma redação já usada nas
outras três docstrings/comentários editados: linhas ~260/464/1666). Intenção preservada; grep
de ausência agora VAZIO. Nenhuma outra divergência do plano.

## Arquivos alterados
- `src/motor_expansao/dashboard/relatorio_municipal.py`
  - Removida a constante `SAM_DESTAQUE_MIN = 3000.0` (+ comentário BLK-RELMUN-03 no bloco de constantes).
  - `_hex_destacado_mask`: corpo e docstring passam a usar só `oferta_efetiva_disponivel >= OFERTA_DESTAQUE_MIN`.
  - Docstring do módulo (D1), docstring de `_zonas_geometricas`, nota "Potencial de Entrada",
    comentário + nota "Resumo da Região": redação sem SAM.
- `tests/unit/test_relatorio_municipal.py`
  - Import de `_hex_destacado_mask`.
  - Asserção do PDF: `assert b"SAM Fitness" not in pdf_bytes` (guard de ausência) + mantém `b"Residual Fitness >= 2.000"`.
  - Wording de 2 docstrings de fixture (`_sample_df`, `_df_3_aprovados`) — só texto, valores intocados.
  - 2 testes novos: `test_hex_destacado_criterio_so_residual_sem_sam`,
    `test_agregar_municipio_conta_hex_sam_baixo_oferta_alta`.
- `CLAUDE.md` (§8): emenda 2026-07-02 à DEC-011 (aprovada por Vinicius; `SAM_DESTAQUE_MIN` = REMOVIDA).
- `tasks/current_task.md`: status → aguardando QA.
- `context/handoff.md` + `context/handoff/20260702-122035-builder.md` (este handoff).

## Validações executadas
- pytest (subconjunto impactado): `python -m pytest -q tests/unit/test_relatorio_municipal.py tests/integration/test_streamlit_app.py`
  → **250 passed, 0 failed, 0 skipped** (12944 warnings, só DeprecationWarning de terceiros).
  Nota: `-n auto` (xdist) abortou com `INTERNALERROR EOFError` no bootstrap dos workers — falha de
  ambiente do execnet/xdist no Python 3.14 local, NÃO relacionada às mudanças; rodei serial (sem
  `-n auto`), verde. QA roda a suíte full 1× (gate único).
- Por que essas suites: `test_relatorio_municipal.py` é o alvo direto; `test_streamlit_app.py` é o
  smoke de integração obrigatório do dashboard.
- import: `python -c "import streamlit_app"` → **ok**.
- ruff: `python -m ruff check src/.../relatorio_municipal.py tests/.../test_relatorio_municipal.py`
  → **All checks passed!** (`ruff` não está no PATH direto; usei `python -m ruff`).
- mypy: `python -m mypy src/motor_expansao/dashboard/relatorio_municipal.py`
  → **Success: no issues found in 1 source file** (sem erros novos nem pré-existentes).
- grep ausência (módulo): `SAM Fitness|SAM_DESTAQUE_MIN|sam_fitness_potencial` em
  `relatorio_municipal.py` → **VAZIO** (No matches found).
- grep ausência (testes municipal): `SAM Fitness >= 3.000|SAM_DESTAQUE_MIN` em
  `test_relatorio_municipal.py` → **VAZIO** (No matches found).

## Problemas encontrados
- `-n auto` (pytest-xdist) crasha no bootstrap dos workers (execnet EOFError) no ambiente local
  Python 3.14 — problema de ambiente, contornado rodando serial. Sinalizar ao QA para usar serial
  se reproduzir.
- Conflito entre o literal do Passo 4a e o grep de ausência obrigatório — reconciliado (ver
  "Desvio necessário" acima).

## Pendências
- QA: rodar a suíte completa (gate único) e confirmar ausência de regressões.
- Revisão visual humana do PDF do Relatório Municipal (Vinicius) antes do merge (critério de aceite
  do Planner): confirmar que mais hexágonos "amarelos" / "Espaço para academias" maior é o
  comportamento esperado.
- Housekeeping de `tasks/backlog.md` / `tasks/completed.md` no fechamento do ciclo (não feito aqui,
  por instrução do orquestrador).

## Riscos remanescentes
- Consequência de produto esperada (não bug): contagem de "Aprovados"/hexágonos destacados e
  "Espaço para academias" AUMENTAM em relatórios já gerados/futuros — documentado na emenda DEC-011.
- Nenhum risco técnico: `_hex_destacado_mask` é fonte única do critério (grep confirmou zero
  segunda implementação de `sam_fitness_potencial >= 3000`).

## Guardrails verificados
- score_priorizacao não alterado: sim (não aplicável — só display do relatório municipal).
- Artefatos M1 preservados: sim (nenhum artefato/pipeline oficial tocado; mudança local a
  `relatorio_municipal.py`).
- `flag_sam`/DEC-006/DEC-007 (pipeline de mercado, `calcular_colunas_mercado.py`): INTOCADOS.
- Relatório Pontual Censitário (`censo_report.py`, `test_relatorio_pontual_censitario_export.py:156`
  `b"SAM Fitness"`): INTOCADOS.
- Estrutura/contagem de páginas do PDF (9 páginas, `/Count 9`): INALTERADA (não forcei "8 páginas").
- Dashboard offline mantido: sim (nenhuma dependência de rede nova).
