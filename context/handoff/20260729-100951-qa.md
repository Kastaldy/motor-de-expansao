# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer (BLK-RELPON-14 · 2026-07-29 10:09:51)

## Próxima Skill recomendada
**Fechamento manual** (Passo 6 do orquestrador, modo MERGE-HUMANO) — o critério de aceite 7 é gate
visual do Vinicius e o check `guard` do CI exige a label `aprovado-humano` para este diff.

## VEREDITO
**APROVADO**

## Justificativa
Reexecutei por conta própria todas as validações obrigatórias e, além delas, montei uma auditoria A/B
independente que carrega o `censo_map.py` de `main` como módulo paralelo e roda o MESMO caminho de
produção nos dois: o círculo azul caiu de 5.000,000 m para 1.500,000 m nos DOIS painéis, o
enquadramento continua sendo pedido a 5 km, e **os 38 hexágonos renderizados, os polígonos (WKB) e os
valores-fonte são idênticos antes e depois** — nenhum recorte de dado mudou. Os 6 PNGs que não são de
hexágono saem byte-idênticos e o `/Count 8` do PDF é o mesmo nas duas variantes. Nenhum teste foi
afrouxado: o arquivo foi de 48 para 52 testes e de 153 para 173 asserts, e o único teste "removido" é
o renomeado.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- **`test_circulo_em_pixel_encolheu_nos_dois_paineis_de_hexagono` é tautológico sob mutação da própria
  constante.** Rodei mutação (`censo_map.RAIO_CIRCULO_DISPLAY_KM = 5.0`) e ele **SOBREVIVEU**, porque o
  valor esperado é derivado da mesma constante (`inner_h * RAIO_CIRCULO_DISPLAY_KM / ...`). Isso é a
  escolha de design declarada pelo Builder ("derivado das constantes, nunca hardcoded") e **não é
  defeito**: a mesma mutação MATOU `test_circulo_desenhado_tem_1_5_km_com_frame_de_5_km` (pelo assert
  anti-vácuo `raios.max() < 0.5 * 5.000 m`) e `test_raio_do_circulo_e_constante_de_render_propria`
  (`== 1.5`). Registro só para o leitor futuro não atribuir a esse teste um poder que ele não tem: ele
  prova "o desenho casa com a constante", não "a constante vale 1,5 km".
- O comentário das linhas 1145-1147 de `censo_map.py` diz que "com `rotulo_escala` setado, `raio_km`
  deixa de alimentar o rodapé". É verdade, mas `raio_km` continua sendo um parâmetro vivo e obrigatório
  de `_render_camada`. Nada a fazer agora; só não deixar virar dívida se um dia alguém quiser removê-lo.

## Testes faltantes
- nenhum bloqueante. O conjunto cobre: geometria em metros (tol. 1,0 m), geometria em pixel nos 2
  painéis, o frame pedido a 5 km, o texto exato do rodapé/títulos, o overflow de largura, ASCII puro e
  a proibição negativa de `"Raio 5,0 km"`. A única coisa que NENHUM teste do repo cobre — e que eu
  cobri fora da suíte — é a **paridade de recorte de dado contra o código de `main`**. Se o time quiser
  isso permanente, seria um teste de invariante ("o nº de hexes renderizados não depende do círculo"),
  mas é opcional: o clip é estruturalmente por `frame_3857`.

## Riscos remanescentes
- **Gate visual humano (critério de aceite 7) PENDENTE** — é do Vinicius, antes do merge. Não é meu
  para aprovar nem reprovar.
- **Folga de 23 px** no título mais longo (`Residual Fitness - escala 5 km`, 586 px contra budget de
  609 px @1000). Conferi que o budget do teste é o correto e CONSERVADOR: o título é desenhado em x=28
  e a coluna da legenda começa em `width - _LEGEND_COL_W` = 670 (folga real 642 px); o teste usa a
  borda direita do `map_box` (`width - (_LEGEND_COL_W + 33)` = 637 → 609 px). Travado por teste.
- **O check `guard` do CI vai REPROVAR este diff sem label.** Rodei `scripts/loop_guard.py` contra os
  paths do ciclo: `src/motor_expansao/dashboard/censo_map.py` é classe **`governanca`** ("camada que
  calcula/exibe os números do dashboard"). Pela DEC-016 isso exige a label **`aprovado-humano`**
  aplicada por um humano com permissão de escrita que não seja o autor do PR. Se `tasks/backlog.md`
  entrar no mesmo PR, ele soma uma 2ª violação de governança (esperado — o bloco é do ciclo).
  Consequência prática: **este ciclo é MERGE-HUMANO**, não auto-merge, o que já bate com o bloco não
  ser `loop-safe`.

## Saída literal das validações (re-executadas pelo QA)

### 1. `python -m pytest -q tests/unit/test_relatorio_pontual_censitario_mapa.py` (SERIAL)
```
52 passed, 2653 warnings in 100.98s (0:01:40)
```

### 2. Conjunto do plano (§6 comando 2, SERIAL)
```
$ python -m pytest -q tests/unit/test_relatorio_pontual_censitario_export.py \
    tests/unit/test_relatorio_pontual_orquestracao.py \
    tests/unit/test_relatorio_pontual_viabilidade.py tests/unit/test_api_skeleton.py
78 passed, 4122 warnings in 181.76s (0:03:01)
```

### 3. Suíte COMPLETA (SERIAL — `-n auto` está quebrado nesta máquina, BLK-QA-XDIST-01)
```
$ python -m pytest -q
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 2032 passed, 2 skipped, 338321 warnings in 3026.98s (0:50:26)
```
**A única falha é a PRÉ-EXISTENTE conhecida, confirmada por mim como a mesma:**
```
E  FileNotFoundError: [Errno 2] No such file or directory:
   '...\motor-de-expansao\data\staging\unidade_territorio_retencao.parquet'

$ ls data/staging/unidade_territorio_retencao.parquet
ls: cannot access ...: No such file or directory
```
Catalogada como **BLK-FIX-LTV-01** (`tasks/backlog.md:1625` — "Guarda de skip faltante em
`test_run_readonly_m1_por_mtime` (só teste)"); em CI o teste pula.
**Aritmética de baseline que fecha exatamente:** o backlog (`tasks/backlog.md:1694`) registra a
baseline serial vigente como `2028 passed, 2 skipped` + o FAILED conhecido. Medi **2032 passed, 2
skipped** + o mesmo FAILED → **+4 = exatamente os 4 testes novos do bloco**. Nenhum teste sumiu,
nenhum passou a `skip`, nenhuma regressão.

### 4. `python -m ruff check . --no-cache`
```
All checks passed!
```

### 5. `python -m mypy src/`
```
src\motor_expansao\pipelines\m1\ibge_censo.py:24: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\ibge_censo.py:158: note: By default the bodies of untyped functions are not checked, consider using --check-untyped-defs  [annotation-unchecked]
src\motor_expansao\pipelines\m1\ibge_censo.py:159: note: By default the bodies of untyped functions are not checked, consider using --check-untyped-defs  [annotation-unchecked]
src\motor_expansao\pipelines\m1\poi_enrichment.py:15: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\poi_enrichment.py:51: note: By default the bodies of untyped functions are not checked, consider using --check-untyped-defs  [annotation-unchecked]
src\motor_expansao\pipelines\m1\base_h3_brasil.py:31: error: Library stubs not installed for "requests"  [import-untyped]
Found 3 errors in 3 files (checked 113 source files)
```
**Classificação: PRÉ-EXISTENTES, não regressão.** Os 3 erros são `types-requests` ausente nesta
máquina, todos em `src/motor_expansao/pipelines/m1/` — arquivos que o `git diff` deste ciclo **não
lista**. `censo_map.py` sai limpo. Bate com o relato do Builder.

### 6. `python -c "import streamlit_app"`
```
2026-07-29 09:32:21.921 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
2026-07-29 09:32:21.922 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
import ok
```

---

### 7. AUDITORIA INDEPENDENTE A/B (QA, além do que o Builder rodou)

Método: `git show main:src/.../censo_map.py` é gravado num arquivo temporário e carregado como módulo
`censo_map_main` (os imports do arquivo são todos absolutos, então ele roda íntegro em paralelo ao
módulo novo). Espiãs **observam** `_frame_box_metric`/`_render_camada` reais (delegam à função real,
não substituem comportamento) e as fixtures são as **do próprio teste do repo**
(`tmod._hexes_sinteticos()` / `tmod._setores_um_quadrado()`), não fixtures inventadas por mim.

```
==============================================================================
A) MODULO DE `main` CARREGADO EM PARALELO (A/B real, sem mock)
==============================================================================
  main.RAIO_RESIDUAL_DISPLAY_KM = 5.0
  main tem RAIO_CIRCULO_DISPLAY_KM? False
  novo.RAIO_RESIDUAL_DISPLAY_KM = 5.0
  novo.RAIO_CIRCULO_DISPLAY_KM  = 1.5
  novo._RESIDUAL_GRID_DISK_K    = 5  (main: 5)
  novo._HEX_ROTULO_ESCALA       = 'Raio 1,5 km - escala 5 km'
  fixtures do proprio teste: 91 hexes, 1 setor(es)

==============================================================================
B) A/B PELO CAMINHO PUBLICO `render_mapas_censitarios_combinados`
==============================================================================

  [main] chaves geradas: ['concorrentes', 'densidade', 'entorno', 'renda', 'renda_domiciliar', 'residual', 'score', 'socioeconomia']
  [main] raios pedidos a _frame_box_metric: [1.5, 0.14, 5.0, 5.0]

  [novo] chaves geradas: ['concorrentes', 'densidade', 'entorno', 'renda', 'renda_domiciliar', 'residual', 'score', 'socioeconomia']
  [novo] raios pedidos a _frame_box_metric: [1.5, 0.14, 5.0, 5.0]

  B1) ENQUADRAMENTO
    raios de frame main == novo ? True
    paineis com raio_km=5.0: main=2 novo=2

  B2) RECORTE DE DADO (hexes renderizados) - main vs novo
    - 'Socioeconomia - raio 5 km' -> 'Socioeconomia - escala 5 km'
        n_hexes: main=38 novo=38  (igual=True)
        bounds identicos: True
        poligonos (WKB) identicos: True
        valores/cores-fonte identicos: True
    - 'Residual Fitness - raio 5 km' -> 'Residual Fitness - escala 5 km'
        n_hexes: main=38 novo=38  (igual=True)
        bounds identicos: True
        poligonos (WKB) identicos: True
        valores/cores-fonte identicos: True
    ==> NENHUM RECORTE DE DADO MUDOU: True

  B3) CIRCULO DESENHADO (medido em metros, desprojetando 3857 -> CRS metrico local)
    - 'Socioeconomia - escala 5 km'
        main: r_min=5000.000 m  r_max=5000.000 m
        novo: r_min=1500.000 m  r_max=1500.000 m
    - 'Residual Fitness - escala 5 km'
        main: r_min=5000.000 m  r_max=5000.000 m
        novo: r_min=1500.000 m  r_max=1500.000 m
    ==> circulo 5000 m -> 1500 m nos DOIS paineis (tol. 1,0 m)

  B4) BYTE-IDENTIDADE das camadas que NAO sao de hexagono
    - concorrentes       IDENTICO  (esperado IDENTICO)
    - densidade          IDENTICO  (esperado IDENTICO)
    - entorno            IDENTICO  (esperado IDENTICO)
    - renda              IDENTICO  (esperado IDENTICO)
    - renda_domiciliar   IDENTICO  (esperado IDENTICO)
    - residual           DIFERENTE (esperado DIFERENTE)
    - score              IDENTICO  (esperado IDENTICO)
    - socioeconomia      DIFERENTE (esperado DIFERENTE)

==============================================================================
C) TEXTOS DESENHADOS (D2/D3) - capturados do `_draw_text` REAL
==============================================================================
    presente 'Socioeconomia - escala 5 km': True
    presente 'Residual Fitness - escala 5 km': True
    presente 'Raio 1,5 km - escala 5 km - EPSG:3857 - fundo de ruas offline': True
    presente 'Raio 1,5 km - EPSG:3857 - fundo de ruas offline': True
    textos iniciados por 'Raio 5,0 km': [] (deve ser [])
    caracteres NAO-ASCII em TODO texto desenhado: [] (deve ser [])

    budget de titulo = 609 px  (_LEGEND_COL_W=330)
      'Socioeconomia - escala 5 km'            =  573 px  folga=+36
      'Residual Fitness - escala 5 km'         =  586 px  folga=+23
      'Socioeconomia - raio 5 km'              =  520 px  folga=+89
      'Residual Fitness - raio 5 km'           =  533 px  folga=+76
      rodape (61 ch) =  614 px  (canvas util 964)  folga=+350
      rodape (68 ch) =  703 px  (canvas util 964)  folga=+261

==============================================================================
D) MOTOR CENSITARIO / CONFIG INTOCADOS
==============================================================================
    RAIO_CENSITARIO_DEFAULT_KM = 1.5
    METODO_RELATORIO_PONTUAL_CENSITARIO = 'setor_censitario_intersecao_area_1p5km'
    config tem RAIO_CIRCULO_DISPLAY_KM? False (deve ser False)
    config tem RAIO_RESIDUAL_DISPLAY_KM? False (deve ser False)
    settings.H3_RESOLUTION=7 DIST_MIN_ULTRA_KM=1.0 RENDA_MIN=4500.0
    settings.M1_SCORE_OFICIAL=score_priorizacao M1_HEX_LAND_FRACTION_MIN=0.05
    core.constants PESOS_HEX_SCORE_ESTRUTURAL={'renda_per_capita': 0.4, 'populacao_proxy': 0.6}

==============================================================================
RESULTADO: TODAS AS ASSERCOES DA AUDITORIA INDEPENDENTE PASSARAM
==============================================================================
```

### 8. TESTE DE MUTAÇÃO — o assert negativo `"Raio 5,0 km"` pega de fato?

```
  E0) BASELINE (codigo como entregue):
    PASSOU    test_rodape_1p5_por_raio_km_nas_camadas_de_setor_e_rotulo_escala_nas_de_hexagono
    PASSOU    test_socioeconomia_e_hexagono_nao_setor_a_5km
    PASSOU    test_circulo_desenhado_tem_1_5_km_com_frame_de_5_km
    PASSOU    test_circulo_em_pixel_encolheu_nos_dois_paineis_de_hexagono
    PASSOU    test_raio_do_circulo_e_constante_de_render_propria

  E1a) MUTACAO 'reverte D2 nas duas': _HEX_ROTULO_ESCALA = None
    MATOU        test_rodape_1p5_por_raio_km_nas_camadas_de_setor_e_rotulo_escala_nas_de_hexagono
    MATOU        test_socioeconomia_e_hexagono_nao_setor_a_5km

  E1b) MUTACAO PARCIAL (isola o assert NEGATIVO): so a camada `residual` volta a 'Raio 5,0 km';
       os asserts POSITIVOS continuam satisfeitos.
    MATOU        test_rodape_1p5_por_raio_km_nas_camadas_de_setor_e_rotulo_escala_nas_de_hexagono
                 -> nenhuma camada pode voltar a anunciar 'Raio 5,0 km': o circulo tem 1,5 km (BLK-RELPON-14)

  E2) MUTACAO 'reverte D1': censo_map.RAIO_CIRCULO_DISPLAY_KM = 5.0
    MATOU        test_circulo_desenhado_tem_1_5_km_com_frame_de_5_km
    PASSOU (!!)  test_circulo_em_pixel_encolheu_nos_dois_paineis_de_hexagono   <- ver "Melhorias opcionais"
    MATOU        test_raio_do_circulo_e_constante_de_render_propria

  E3) MUTACAO 'reverte D3': titulos voltam a '... - raio 5 km'
    MATOU        test_rodape_1p5_por_raio_km_nas_camadas_de_setor_e_rotulo_escala_nas_de_hexagono
    MATOU        test_socioeconomia_e_hexagono_nao_setor_a_5km
```
**Conclusão:** o assert negativo prometido pelo plano EXISTE (`test_..._mapa.py:954-956`) e **PEGA** —
provei isolando-o com uma regressão PARCIAL (só o painel `residual` volta ao rodapé antigo), cenário
em que todos os asserts positivos continuam satisfeitos e ele é a única trava que reprova.

### 9. `/Count` do PDF (caminho real de geração), `main` vs atual
```
  [main ] chaves de mapa: 8  recente /Count=['8']  classico /Count=['8']
  [atual] chaves de mapa: 8  recente /Count=['8']  classico /Count=['8']
  ==> /Count 8 nas duas variantes, ANTES e DEPOIS. Inalterado.
```
(`/Count 12` do PDF de viabilidade fica travado por `test_relatorio_pontual_orquestracao.py:104`,
`test_relatorio_pontual_viabilidade.py:121` e `test_relatorio_pontual_ui_relviab06.py:67` — todos
verdes na validação 2 e na suíte completa.)

### 10. Escopo / M1 / artefatos
```
$ git log --oneline main..HEAD          # branch ciclo/BLK-RELPON-14
(vazio — o trabalho do ciclo está TODO no worktree, ainda não commitado)

$ git diff --name-only
docs/relatorio_pontual_censitario.md
graphify-out/.graphify_labels.json
graphify-out/GRAPH_REPORT.md
graphify-out/graph.json
src/motor_expansao/dashboard/censo_map.py
tasks/backlog.md
tests/unit/test_relatorio_pontual_censitario_mapa.py

$ git diff main...HEAD -- config.py src/motor_expansao/pipelines/ data/
(vazio)
$ git diff        -- config.py src/motor_expansao/pipelines/ data/
(vazio)

$ git diff --stat  (paths do ciclo)
 docs/relatorio_pontual_censitario.md                 |  14 +-
 src/motor_expansao/dashboard/censo_map.py            |  56 +-
 tests/unit/test_relatorio_pontual_censitario_mapa.py | 157 +-

mtime dos artefatos oficiais do M1 (hoje é 2026-07-29):
2026-06-10 09:50:32  data/staging/brasil_estrutural.parquet
2026-06-10 09:50:32  data/staging/brasil_priorizados.parquet
2026-06-10 09:50:34  data/staging/hexagonos_brasil_oportunidades.parquet
2026-06-10 10:24:10  data/outputs/hexagonos_brasil_dashboard.parquet
2026-06-10 10:24:11  data/outputs/hexagonos_mapa_sample.parquet
2026-06-10 10:24:10  data/outputs/top_oportunidades_resumo.csv
2026-06-10 10:24:10  data/outputs/resumo_por_uf.csv
```
Obs.: **não existe `config.py` na raiz** do repo — o canônico é `src/motor_expansao/config.py`, já
coberto pelo escopo `src/motor_expansao/` do `git diff --name-only` acima (que lista só `censo_map.py`).

### 11. Autoria do worktree sujo (o Builder tocou `graphify-out`/`backlog`?) — NÃO
```
mtime dos arquivos sujos:
2026-07-28 15:25:32  tasks/backlog.md            <- ANTES do handoff do BO (15:31)
2026-07-28 15:26:30  graphify-out/graph.json     <- hook post-checkout, ANTES do BO
2026-07-28 15:26:32  graphify-out/GRAPH_REPORT.md
2026-07-28 15:26:32  graphify-out/.graphify_labels.json
2026-07-28 15:51:47  src/motor_expansao/dashboard/censo_map.py     <- janela do Builder
2026-07-28 15:53:16  tests/unit/test_relatorio_pontual_censitario_mapa.py
2026-07-28 15:58:03  docs/relatorio_pontual_censitario.md
2026-07-28 16:00:54  context/handoff/20260728-155919-builder.md
```
`tasks/backlog.md` e `graphify-out/*` foram tocados **antes** de o Builder existir (o Planner só
terminou às 15:45). Confirmado: não foram alterados por ele.

### 12. Estrictude dos testes: `main` vs worktree
```
testes:  48 -> 52     (+4)
asserts: 153 -> 173   (+20)

removidos (nomes):  test_rodape_do_png_deriva_do_raio_km_1p5_identico_e_5p0_novo   <- RENOMEADO, não deletado
adicionados:        test_circulo_desenhado_tem_1_5_km_com_frame_de_5_km
                    test_circulo_em_pixel_encolheu_nos_dois_paineis_de_hexagono
                    test_raio_do_circulo_e_constante_de_render_propria
                    test_rodape_1p5_por_raio_km_nas_camadas_de_setor_e_rotulo_escala_nas_de_hexagono
                    test_titulos_das_camadas_de_hexagono_cabem_sem_invadir_a_legenda

linhas de assert que SUMIRAM (3, todas de TEXTO DE PRODUTO substituído por D2/D3):
   - assert "Raio 5,0 km - EPSG:3857 - fundo de ruas offline" in textos
   - assert "Residual Fitness - raio 5 km" in textos
   - assert "Socioeconomia - raio 5 km" in textos

testes-invariante conferidos como BYTE-IDÊNTICOS a `main`:
   test_frame_box_metric_puro_reproduz_o_calculo_do_caller ............ BYTE-IDENTICO
   test_raio_de_exibicao_nao_toca_o_raio_do_motor .................... BYTE-IDENTICO
   test_camada_residual_presente_com_hexes_df_e_png_valido ........... BYTE-IDENTICO
   test_camada_residual_nao_desenha_pins_blk_relpon_10_fu1 ........... BYTE-IDENTICO
   test_camadas_censitarias_declara_as_8_chaves ..................... BYTE-IDENTICO
   test_camadas_existentes_ficam_byte_identicas_com_os_defaults_novos . BYTE-IDENTICO
       (diff aparente = só a quebra de linha final do arquivo)
```
**Veredito de estrictude: o conjunto ficou MAIS ESTRITO.** Nenhuma asserção foi relaxada nem removida
sem substituição; as 3 que sumiram eram expectativas do texto antigo, trocadas por igualdade exata do
texto novo **mais** um assert negativo que antes não existia.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, `--deselect`,
`-k` para esconder falha, nem mock do caminho crítico.
- A suíte completa e os 2 comandos alvo rodaram **inteiros e SERIAIS**, sem seleção.
- Na auditoria A/B eu uso espiãs, e declaro explicitamente o que elas fazem: `_spy_frame` e
  `_spy_camada` **registram os argumentos e delegam à função REAL** (`real_frame(...)` /
  `real_camada(**kwargs)`), então o PNG produzido é o de produção — o espião observa, não substitui. O
  caminho exercitado é o público `render_mapas_censitarios_combinados`, o mesmo que a API/bot e o
  dashboard chamam.
- As fixtures da auditoria são importadas do **próprio arquivo de teste do repo**, não escritas por mim.
- O único ponto em que eu **substituo** comportamento é o teste de MUTAÇÃO (§8), cujo propósito é
  justamente introduzir a regressão e verificar que o teste reprova; nenhum verde do veredito se apoia
  nele.
- `pytest -n auto` NÃO foi usado (quebrado nesta máquina, BLK-QA-XDIST-01); a suíte rodou serial.

## Conferência cruzada com log do Builder
**BATE.** Item a item:

| Alegação do Builder | Minha medição | Status |
|---|---|---|
| 52 passed no arquivo primário | 52 passed | bate |
| 78 passed no conjunto do plano | 78 passed | bate |
| ruff limpo | `All checks passed!` | bate |
| mypy: 3 erros pré-existentes de `types-requests` | 3 erros, mesmos arquivos, nenhum do ciclo | bate |
| `import streamlit_app` OK | OK | bate |
| bbox azul 153 px (era 509 px) | círculo 1.500,000 m (era 5.000,000 m); 6 outras camadas byte-idênticas | bate (medi em metros, mais forte) |
| `_frame_box_metric` recebe 5 km, inalterado | `[1.5, 0.14, 5.0, 5.0]` idêntico em `main` e no novo | bate |
| Grid 2x2 / satélite / concorrentes byte-idênticas | 6 chaves byte-idênticas; nenhum hunk do diff depois da linha 1569 (o bloco de satélite começa na 1641) | bate |
| `/Count 8` inalterado | `/Count 8` nas 2 variantes, antes e depois | bate |
| não afrouxou testes | 48→52 testes, 153→173 asserts, invariantes byte-idênticos | bate |
| não tocou `graphify-out`/`backlog.md` | mtimes anteriores ao próprio BO | bate |
| títulos 573/586 px vs budget 609 | 573/586 px vs 609 px | bate |
| suíte: "meça a baseline no momento" | 2032 passed / 2 skipped / 1 failed conhecido = baseline 2028 + os 4 testes novos | bate |

**Divergências:** nenhuma de substância. Duas notas menores:
1. O Builder escreveu que o teste de pixel prova que "o círculo encolheu"; a mutação mostra que ele
   prova "o círculo casa com a constante" (ver Melhorias opcionais). Não muda o veredito — a regressão
   é pega por outros 2 testes.
2. O Builder listou 2 desvios cosméticos do plano (omitir números de linha absolutos nos comentários e
   agrupar os 4 testes numa seção ao fim do arquivo). Confirmei ambos no diff e concordo: são
   melhorias, não desvios de escopo — o comentário com `L1047` já nasceria stale, porque a própria
   inserção da constante deslocou aquela linha para 1078.

## Cobertura do critério de aceite do backlog (item a item)
1. **Círculo 1,5 km nos dois painéis + enquadramento 5 km, por teste geométrico:** ATENDIDO.
   1.500,000 m medidos nas 2 chaves (A/B contra `main`, que dá 5.000,000 m); frame pedido a 5 km.
2. **`RAIO_RESIDUAL_DISPLAY_KM == 5.0` e `_frame_box_metric` inalterado:** ATENDIDO. A linha
   `frame_metric = _frame_box_metric(RAIO_RESIDUAL_DISPLAY_KM, width, height)` é byte-idêntica à de
   `main` (mudou só de número de linha, 1047 → 1078, pela inserção da constante acima). Os dois testes
   que o backlog citava (`...:1004,1016` em `main`) estão byte-idênticos e verdes.
3. **Teste novo amarrando o círculo a 1,5 km:** ATENDIDO
   (`test_circulo_desenhado_tem_1_5_km_com_frame_de_5_km` + `test_raio_do_circulo_e_constante_de_render_propria`;
   ambos matam a mutação de reversão).
4. **Rótulo do rodapé coerente com o desenho (D2):** ATENDIDO. Sai exatamente
   `Raio 1,5 km - escala 5 km - EPSG:3857 - fundo de ruas offline`, ASCII puro, 614 px num canvas útil
   de 964 px.
5. **`/Count` e demais páginas inalterados; nenhuma constante de motor tocada:** ATENDIDO.
   `/Count 8` idêntico; 6 das 8 camadas byte-idênticas; `not hasattr(config, "RAIO_CIRCULO_DISPLAY_KM")`
   travado por teste e reconferido por mim.
6. **`ruff`/`mypy` limpos; suíte verde:** ATENDIDO (mypy com os 3 erros pré-existentes fora do escopo;
   suíte com só a falha pré-existente do BLK-FIX-LTV-01).
7. **Gate visual humano de Vinicius:** **PENDENTE DO HUMANO** — fora do meu alcance; não reprovo por ele.

## Guardrails verificados
- `score_priorizacao` não alterado: **sim** — `git diff` (worktree e `main...HEAD`) vazio para
  `config.py`, `src/motor_expansao/pipelines/` e `data/`; nenhum arquivo de scoring no diff.
- Artefatos M1 preservados: **sim** — mtimes de 2026-06-10/11, intocados hoje (2026-07-29).
- Parâmetros canônicos: **sim** — `H3_RESOLUTION=7`, `DIST_MIN_ULTRA_KM=1.0`, `RENDA_MIN=4500.0`,
  `M1_SCORE_OFICIAL=score_priorizacao`, `M1_HEX_LAND_FRACTION_MIN=0.05`, pesos
  `{renda_per_capita: 0.40, populacao_proxy: 0.60}`.
- Motor censitário INTOCADO: **sim** — `RAIO_CENSITARIO_DEFAULT_KM == 1.5`,
  `METODO_RELATORIO_PONTUAL_CENSITARIO == "setor_censitario_intersecao_area_1p5km"`; `censo_point.py`
  fora do diff.
- `_RESIDUAL_GRID_DISK_K` (5) e o recorte de dado: **intocados** — provado por A/B, não presumido.
- Acentuação (§2): **sim** — exceção de RENDER aplicada; ZERO caracteres não-ASCII em TODO texto
  desenhado nos 8 PNGs; docs e este handoff em português acentuado.
- Dashboard offline: **sim** — todo o caminho de teste com `basemap=False`, nenhuma dependência nova.
- Alcance colateral: **nenhum** — `RAIO_RESIDUAL_DISPLAY_KM`/`RAIO_CIRCULO_DISPLAY_KM`/`_HEX_ROTULO_ESCALA`
  só são referenciados por `censo_map.py` e pelo seu arquivo de teste; `MAP_LAYER_TITLES` de
  `censo_report.py` usa `"Socioeconomia"`/`"Residual Fitness"` (sem raio).
- Validações re-executadas pelo QA (sem bypass): pytest arquivo primário → 52 passed · pytest conjunto
  do plano → 78 passed · **pytest suíte completa → 2032 passed, 2 skipped, 1 failed pré-existente** ·
  ruff → limpo · mypy → 3 pré-existentes · `import streamlit_app` → ok · auditoria A/B → todas as
  asserções passaram · mutação → assert negativo confirmado · `/Count` → 8 antes e depois.
- Escopo respeitado: **sim** — 3 arquivos do ciclo, exatamente os previstos. `graphify-out/*` e
  `tasks/backlog.md` estavam sujos ANTES do Builder (mtimes provam). Nenhum commit/branch/`git add` foi
  feito por mim.

## Housekeeping (Passo 6.0 — do ORQUESTRADOR, medido por mim, não executado)
```
$ python scripts/housekeeping_move_block.py BLK-RELPON-14 --check
FALHA --check: stub ausente no backlog para BLK-RELPON-14      (exit 1)

$ python scripts/housekeeping_move_block.py BLK-RELPON-14 --is-done
BLK-RELPON-14: ABERTO em tasks/completed.md                     (exit 1)
```
**Esperado nesta altura** — o Passo 6.0 ainda não rodou. Como este ciclo é **MERGE-HUMANO** (gate
visual + `guard` exigindo `aprovado-humano`), vale o modo "MERGE-HUMANO" do meu prompt: o helper deve
mover o bloco **no MESMO PR** e, depois disso, `--check` tem de sair OK e o bloco em `completed.md`
tem de ser byte-idêntico ao original de `backlog.md`. **Não executei o move.**

## Pendências para o Passo 6
1. **Gate visual do Vinicius** (critério 7) — comparar o PDF antes/depois no mesmo ponto.
2. **Housekeeping** via `python scripts/housekeeping_move_block.py BLK-RELPON-14` + `--check` OK.
3. **Label `aprovado-humano`** no PR (o `guard` classifica `censo_map.py` como `governanca`), aplicada
   por humano com write que não seja o autor do PR.
4. **Commits por path** conforme o Builder recomendou: (1) `censo_map.py` + o teste, (2) `docs/...md`,
   (3) `tasks/backlog.md`. **NÃO commitar** `graphify-out/*`, `PRD.md`, `context/handoff.md`,
   `tasks/current_task.md`.
5. **`/graphify . --update`** numa sessão Claude depois de mexer em `docs/`/`tasks/` (a camada semântica
   do grafo é MANUAL — CLAUDE.md §7).

## Arquivos que EU deixei dirty
- `context/handoff.md` (corrente) e `context/handoff/20260729-100951-qa.md` (snapshot) — saída
  obrigatória da Skill.
- `tasks/current_task.md` — só o bookkeeping do ciclo (`Skill atual`, `Status`, `Esteira executada`),
  como manda o meu prompt de Skill. Está na lista de NÃO-commitar.
- **Nada mais.** Não editei código de produção, testes, docs nem `tasks/backlog.md`.

## Decisão recomendada
**Fechar ciclo** (Passo 6, modo MERGE-HUMANO), condicionado ao gate visual do Vinicius. Nenhum bloco de
correção necessário.
