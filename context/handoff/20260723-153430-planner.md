# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
**Gate humano/visual — Vinicius** (criticidade Alta). SÓ APÓS a aprovação humana do gate visual é que o **Builder** pode rodar. Não pular direto para o Builder.

## Entendimento da tarefa
Religar o painel `socioeconomia` do slide-hero para desenhar `score_setor_2022_calibrado` por hexágono H3 res-7 num disco `grid_disk(k=5)` a `RAIO_RESIDUAL_DISPLAY_KM=5.0` km (padrão do Residual Fitness, mesma métrica/paleta de hoje), sem pins, com chave condicional/fallback textual, reduzindo um pouco as 2 imagens da página, espelhando a variante clássica — mantendo `/Count 8`, as 8 chaves de `CAMADAS_CENSITARIAS` e as outras 7 camadas byte-idênticas. Render puro, READ-ONLY sobre o M1.

## Sub-decisões técnicas resolvidas (para o Builder não redecidir)
- **(A) GENERALIZAR (não copiar).** Parametrizar os 3 helpers hex com parâmetros keyword-only DEFAULT-PRESERVING (os defaults reproduzem o residual byte-a-byte) e reusá-los pela socioeconomia — evita duplicar a lógica de frame/basemap/clip de 5 km e mantém UM único caminho para o residual (menor risco de regressão do que uma cópia que pode divergir). Assinaturas exatas nos passos 1-3.
- **(B) CHAVE CONDICIONAL (opção a), à risca do residual.** `socioeconomia` só entra no dict quando o render hex devolve bytes (há `hexes_df` + coluna `score_setor_2022_calibrado` + hex desenhável); caso contrário é AUSENTE e o PDF cai no fallback textual da célula. Consequência de teste: `_CAMADAS_SEM_HEXES` perde `socioeconomia` (passa a 6 chaves) e `_CAMADAS` vira `_CAMADAS_SEM_HEXES | {"socioeconomia", "residual"}` (8 chaves); o assert que muda é `test_slide_hero_offline_safe_sem_camada_residual` (export.py:837): `assert "socioeconomia" in mapas` → `assert "socioeconomia" not in mapas`.
- **(C) Valor central da faixa (`valor_ponto`).** Análogo ao residual: score do hex H3 res-7 que CONTÉM o ponto, via `_residual_hex_central(..., value_col="score_setor_2022_calibrado")`, formatado por `_format_valor_ponto_score` (inteiro 0-100, "n/d" quando ausente), rótulo `"Score"` → renderiza `"Score no hexagono: NN"` (ASCII, reusa `_legenda_valor_hex`).
- **(D) Redução das 2 imagens = fator de escala uniforme no ramo `pack=True`.** Novo `scale: float = 1.0` em `_map_grid_cells_packed` (aplicado a `cell_w`/`cell_h` DEPOIS do clamp de `max_total_w`, ANTES de recentrar) e `packed_scale: float = 1.0` propagado por `_draw_maps_grid`/`_classico_draw_maps_grid`. Só as duas páginas socioeconomia+residual passam `packed_scale=_HERO_MAP_SCALE`. **Default de partida `_HERO_MAP_SCALE = 0.92` (≈92%), CALIBRÁVEL no gate visual de Vinicius** — é UM número, uma constante de módulo. `scale=1.0`/`packed_scale=1.0` preserva byte-a-byte o grid 2x2 (Mapas de calor), o 1x1 (Entorno) e o 2x1 atual, porque não alteram nenhuma célula com o default.

## Plano técnico
### censo_map.py (`src/motor_expansao/dashboard/censo_map.py`)
1. **`_hex_polygons_3857` (905-967) — parametrizar `value_col` + guarda de coluna.**
   - Assinatura: `def _hex_polygons_3857(lat, lng, hexes_df, frame_3857, to_3857, *, value_col: str = "oferta_efetiva_disponivel")`.
   - Logo após a guarda atual de `hex_id` (linha 928), adicionar guarda de coluna: se `value_col not in hexes_df.columns` → `return empty`. (Isso faz a socioeconomia SEM a coluna cair no fallback textual em vez de renderizar hexes todos-NaN; preserva o residual, que sempre tem `oferta_efetiva_disponivel`.)
   - Trocar o literal da L963 `row.get("oferta_efetiva_disponivel")` → `row.get(value_col)`.
   - Atualizar o docstring (a `Serie posicional` passa a conter `value_col`, não fixo `oferta_efetiva_disponivel`).
2. **`_residual_hex_central` (970-992) — parametrizar `value_col`.**
   - Assinatura: `def _residual_hex_central(lat, lng, hexes_df, *, value_col: str = "oferta_efetiva_disponivel")`.
   - Trocar o subset `{"hex_id", "oferta_efetiva_disponivel"}` (L978) por `{"hex_id", value_col}` e a leitura `linha["oferta_efetiva_disponivel"]` (L989) por `linha[value_col]`. Default preserva o residual byte-a-byte.
3. **`_render_camada_residual_hex` (995-1085) — parametrizar título/legenda/cor/valor central, DEFAULT-PRESERVING.**
   - Adicionar parâmetros keyword-only ao final da assinatura (após `choropleth_alpha`), todos com default que reproduz o residual atual:
     - `value_col: str = "oferta_efetiva_disponivel"`
     - `titulo: str = "Residual Fitness - raio 5 km"`
     - `legenda_titulo: str = "Residual disponivel (alunos)"`
     - `legenda_entries: list[tuple[str, tuple[int, int, int, int]]] | None = None` (None → `_bands_legend_entries(OFERTA_DISPONIVEL_ALUNOS_BANDS)`)
     - `color_fn: Callable[[float], tuple[int, int, int, int]] | None = None` (None → o `_residual_fn` inline atual, construído com `eff_alpha`/`sem_dado`)
     - `valor_central_rotulo: str = "Residual"`
     - `valor_central_fmt: Callable[[float | None], str] = _format_valor_residual`
   - No corpo: passar `value_col=value_col` para `_hex_polygons_3857(...)` (L1036); resolver `_legenda_entries_eff = legenda_entries if legenda_entries is not None else _bands_legend_entries(OFERTA_DISPONIVEL_ALUNOS_BANDS)`; resolver `_color_fn_eff = color_fn if color_fn is not None else _residual_fn` (o `_residual_fn` inline permanece, para o default). Na chamada de `_render_camada`: `titulo=titulo`, `legenda_titulo=legenda_titulo`, `legenda_entries=_legenda_entries_eff`, `color_fn=_color_fn_eff`, e `valor_ponto=_legenda_valor_hex(valor_central_rotulo, valor_central_fmt(_residual_hex_central(lat, lng, hexes_df, value_col=value_col)))`.
   - **Invariante:** com TODOS os defaults, o corpo executa a lógica idêntica de hoje → residual byte-a-byte igual (travado por `test_camada_residual_*` e `com["residual"] == sem["residual"]`).
4. **`render_mapas_censitarios_combinados` — religar `socioeconomia` ao render hex, chave CONDICIONAL.**
   - REMOVER o bloco atual `socioeconomia_png = _render_camada(...)` (1445-1453) e a chave `"socioeconomia": socioeconomia_png` do dict literal (1479).
   - Adicionar, junto ao bloco do `residual` (perto de 1486-1499) — DEPOIS de `_score_fn`/`score_series`/`_score_legend_entries` já existirem — uma chamada condicional:
     ```
     socioeconomia_png = _render_camada_residual_hex(
         lat, lng, hexes_df,
         basemap=basemap, width=width, height=height,
         street_ceil=street_ceil, street_gain=street_gain, street_cap=street_cap,
         choropleth_alpha=choropleth_alpha,
         value_col="score_setor_2022_calibrado",
         titulo="Socioeconomia - raio 5 km",                 # ASCII (exceção de RENDER §2)
         legenda_titulo="Score censitario (0-100)",
         legenda_entries=_score_legend_entries(),
         color_fn=_score_fn,
         valor_central_rotulo="Score",
         valor_central_fmt=_format_valor_ponto_score,
     )
     if socioeconomia_png is not None:
         mapas["socioeconomia"] = socioeconomia_png
     ```
   - Atualizar o docstring da função (o item `socioeconomia` do BLK-RELPON-10 deixa de ser "MESMO choropleth do `score` no raio de 1,5 km, SEMPRE produzida" e passa a "hexágono H3 res-7, 5 km, mesma métrica/paleta do `score`, CONDICIONAL ao `hexes_df` como o `residual`; o que a distingue do `score` do grid é a escala/geometria").
   - **Título ASCII:** `"Socioeconomia - raio 5 km"` (sem vírgula, espelhando `"Residual Fitness - raio 5 km"`). O rodapé "Raio 5,0 km - ..." vem sozinho de `raio_km=RAIO_RESIDUAL_DISPLAY_KM` dentro de `_render_camada_residual_hex`.
   - **Nota de custo (aceitável, não refatorar):** a socioeconomia hero faz o próprio fetch de basemap 5 km (igual ao residual faz o seu). Mesma bbox do residual, mas o cache de tiles em disco dedup. Compartilhar basemap entre os dois exigiria refactor fora do escopo (risco > ganho); manter cada um com o seu fetch.

### censo_report.py (`src/motor_expansao/dashboard/censo_report.py`)
5. **Fator de escala (sub-decisão D).**
   - Adicionar constante de módulo perto de `_SOCIOECONOMIA_RESIDUAL_TITULO` (512): `_HERO_MAP_SCALE = 0.92  # CALIBRÁVEL no gate visual de Vinicius; 1.0 = tamanho atual`.
   - `_map_grid_cells_packed` (421-451): adicionar keyword-only `scale: float = 1.0`; após o clamp de `max_total_w` (linha 442) e ANTES de `total_w`/`total_h`, inserir `cell_w *= scale; cell_h *= scale`. Recentragem (x0/y0) já usa `total_w`/`total_h`, então as células menores ficam centradas. Default 1.0 = geometria idêntica.
   - `_draw_maps_grid` (454-509): adicionar keyword-only `packed_scale: float = 1.0`; repassar `scale=packed_scale` na chamada de `_map_grid_cells_packed` (482-484). O ramo `pack=False`/`_map_grid_cells` ignora o parâmetro.
   - `_classico_draw_maps_grid` (1794-1821): adicionar keyword-only `packed_scale: float = 1.0`; repassar `packed_scale=packed_scale` na chamada de `_draw_maps_grid` (1811).
6. **Aplicar a escala só nas 2 páginas do slide-hero + atualizar docstrings.**
   - `_socioeconomia_residual_page` (549-579): na chamada de `_draw_maps_grid` (567-577), adicionar `packed_scale=_HERO_MAP_SCALE`.
   - `_classico_socioeconomia_residual_page` (1851-1874): na chamada de `_classico_draw_maps_grid` (1867-1872), adicionar `packed_scale=_HERO_MAP_SCALE`.
   - Atualizar os docstrings das duas funções: `Socioeconomia = score_setor_2022_calibrado por hexágono H3 res-7 no raio de 5 km` (não "por setor censitário no raio de 1,5 km"). NÃO tocar `_entorno_page`/`_mapas_calor_page`/`_classico_*` correspondentes (default `packed_scale=1.0` preserva byte-identidade).

### Testes
7. **tests/unit/test_relatorio_pontual_censitario_mapa.py**
   - **Reestruturar as constantes (30-39):** `_CAMADAS_SEM_HEXES = {"densidade","renda","score","renda_domiciliar","concorrentes","entorno"}` (remove `socioeconomia`, 6 chaves); `_CAMADAS = _CAMADAS_SEM_HEXES | {"socioeconomia", "residual"}` (8 chaves). Isso mantém `test_camadas_censitarias_declara_as_8_chaves` (811-812: `set(CAMADAS_CENSITARIAS) == _CAMADAS`, `len==8`) e faz os `set(mapas) == _CAMADAS_SEM_HEXES` sem hexes (111, 131, 307) baterem com 6 chaves.
   - **`_hexes_sinteticos` (703-711):** adicionar a coluna `score_setor_2022_calibrado` ao DataFrame, com valores cruzando faixas 0-100 (ex.: `[float((i % 10) * 11) for i in range(len(celulas))]`), para a socioeconomia hex RENDERIZAR quando hexes são passados (`set(mapas) == _CAMADAS` = 8 em 731 e 1155).
   - **Título (839):** `assert "Socioeconomia - raio 1,5 km" in textos` → `assert "Socioeconomia - raio 5 km" in textos`. (Linhas 836-837, os rodapés "Raio 1,5 km" e "Raio 5,0 km", permanecem válidas — as 4 camadas de 1,5 km ainda emitem "Raio 1,5 km" e socioeconomia+residual emitem "Raio 5,0 km".)
   - **Trava anti-vácuo #1 (777-781), no teste que PASSA `hexes_df`:** socioeconomia agora é hex/5 km SEM pins → imune aos pontos. Trocar `assert com["socioeconomia"] != sem["socioeconomia"]` por `assert com["socioeconomia"] == sem["socioeconomia"]` (mesma prova do residual, agora também para a socioeconomia) e mover a sentinela anti-vácuo para uma camada de 1,5 km que MANTÉM pins: `assert com["score"] != sem["score"]`. Atualizar comentário/docstring (750-751, 777-781).
   - **Trava anti-vácuo #2 (998-1001), no teste que NÃO passa `hexes_df`:** socioeconomia estaria AUSENTE (KeyError). Trocar a sentinela para `assert com["score"] != sem["score"]` (camada presente sem hexes e com pins). Atualizar comentário (975-977, 998).
   - **Novos testes** (comportamento novo entra com teste):
     - `test_socioeconomia_e_hexagono_nao_setor_a_5km`: com `hexes_df=_hexes_sinteticos()`, a chave `socioeconomia` existe, é PNG válido 1000x760 e o título "Socioeconomia - raio 5 km" + rodapé "Raio 5,0 km" aparecem (spy em `_draw_text` como em 815-841). Prova indireta de hex/5 km (mesma bbox do residual).
     - `test_socioeconomia_ausente_sem_hexes_df_ou_sem_coluna_score`: (a) sem `hexes_df` → `"socioeconomia" not in mapas`; (b) `hexes_df` sem a coluna `score_setor_2022_calibrado` (só `hex_id`+`oferta`) → `"socioeconomia" not in mapas`, sem exceção; (c) `hexes_df` de outra região (hex fora do disco) → ausente.
     - `test_socioeconomia_reage_ao_score_setor_2022_calibrado`: dois `hexes_df` com `score` diferentes por hex ⇒ PNGs de socioeconomia diferentes (a cor depende do dado).
8. **tests/unit/test_relatorio_pontual_censitario_export.py**
   - **`test_slide_hero_offline_safe_sem_camada_residual` (832-843):** linha 837 `assert "socioeconomia" in mapas` → `assert "socioeconomia" not in mapas`; atualizar o docstring (sem `hexes_df`, AMBAS `socioeconomia` e `residual` caem no fallback textual; as duas células do slide-hero mostram "Mapa indisponível"). As demais asserts (836 `"residual" not in mapas`, 841-843 `%PDF-1.4`/`/Count 8`/"Mapa indisponível") permanecem.
   - **Contagens de imagem — NENHUMA muda, mas VERIFICAR:** todas as asserts são `>=` (5×`>=5`, 1×`>=6` em 908). Com `_sample_result` (sem hexes) e socioeconomia agora AUSENTE, o mínimo cai de 7 para 6 imagens de mapa (4 do grid 2x2 + 1 Concorrentes + 1 Entorno). `>=6` (908) passa exatamente (era folgado); todos os `>=5` passam. O Builder deve confirmar isso ao rodar a suíte, e NÃO relaxar nenhuma dessas asserts.

### Docs (RECOMENDADO — atualizar o contrato do painel)
9. **docs/relatorio_pontual_censitario.md:** atualizar (a) o item 3 "Socioeconomia e Residual Fitness" (linha 215): `socioeconomia` = `score_setor_2022_calibrado` por **hexágono H3 res-7** num raio de EXIBIÇÃO de 5 km (disco `grid_disk` k=5), **mesma paleta/escala do `score`** (o que muda é a geometria/escala, não a métrica), **CONDICIONAL** ao `hexes_df` como o `residual`; (b) a menção da faixa superior de socioeconomia (linha 88): passa a usar rótulo "no hexagono" (`_legenda_valor_hex`), não "no raio". Sem DEC nova (mesma família visual do BLK-RELPON-10 já aprovado).

## Arquivos afetados
- `src/motor_expansao/dashboard/censo_map.py` (passos 1-4)
- `src/motor_expansao/dashboard/censo_report.py` (passos 5-6)
- `tests/unit/test_relatorio_pontual_censitario_mapa.py` (passo 7)
- `tests/unit/test_relatorio_pontual_censitario_export.py` (passo 8)
- `docs/relatorio_pontual_censitario.md` (passo 9, recomendado)
- `tasks/completed.md` (append — só na Skill de fechamento, NÃO agora)

## Dependências
- `h3` (import lazy já usado por `_hex_polygons_3857`/`_residual_hex_central`).
- `score_band_to_color` (já importado em `censo_map.py:36`) — trata NaN (retorna cinza fixo `[120,120,140,70]`), então `_score_fn` colore hexes com score NaN sem crashar.
- Coluna `score_setor_2022_calibrado` no `hexes_df` servido: é `optional_cols` no modo híbrido (`constants.py:591`) e `required_cols` no modo censitário (596). Quando ausente na fatia da UF, a guarda do passo 1 leva ao fallback textual.

## Riscos técnicos
- **Byte-identidade do residual:** qualquer default de `_render_camada_residual_hex`/`_hex_polygons_3857`/`_residual_hex_central` que não reproduza exatamente o código atual quebra `com["residual"] == sem["residual"]` e `test_camada_residual_*`. Os defaults do passo 3 são o guardrail — não alterar a ordem de operações do corpo residual.
- **Ripple das constantes de teste:** a mudança de `_CAMADAS_SEM_HEXES`/`_CAMADAS` + `_hexes_sinteticos` afeta 6 asserts de conjunto (mapa 111/131/307/731/811/1155). O Builder deve alterar as 3 constantes/fixtures de uma vez e rodar o arquivo inteiro.
- **`>=6` de imagens (export 908) fica exato (6):** ainda passa, mas sem folga; se alguma das 6 imagens não embutir, cai para 5. É pré-existente; não relaxar a assert.
- **Densidade a 5 km:** ~91 hexes (k=5). Sem a coluna `score` na fatia da UF → fallback textual (não crash), garantido pela guarda do passo 1.
- **Escala das imagens é subjetiva:** `_HERO_MAP_SCALE=0.92` é ponto de partida; o valor FINAL sai do gate visual de Vinicius. Não travar nenhum teste no valor 0.92 (testar a MECÂNICA: `scale<1.0` ⇒ células menores e centradas; não o número).

## Critérios de aceite finais
- Painel `socioeconomia` do hero = `score_setor_2022_calibrado` colorido por `score_band_to_color`, por hexágono H3 res-7 (disco `grid_disk(k=5)`), raio de 5 km; título ASCII "Socioeconomia - raio 5 km"; rodapé "Raio 5,0 km - ...".
- SEM pins na camada socioeconomia; chave CONDICIONAL + fallback textual quando faltar `hexes_df`/coluna/hex (à risca do residual).
- As 2 imagens da página um pouco menores (fator `_HERO_MAP_SCALE`, aprovado no gate visual).
- Variante clássica espelhada (mesma geometria/comportamento).
- `/Count 8` preservado nas duas variantes; `CAMADAS_CENSITARIAS` com as mesmas 8 chaves; densidade/renda/score/renda_domiciliar/concorrentes/entorno/residual byte-idênticas.
- ASCII em todos os labels do PNG.
- READ-ONLY M1 confirmado por mtime dos artefatos oficiais (nenhum reescrito).
- Testes atualizados + novos; suíte verde.

## Validações obrigatórias
- Comando: `python -m pytest -q tests/unit/test_relatorio_pontual_censitario_mapa.py tests/unit/test_relatorio_pontual_censitario_export.py`
  - Critério mínimo: 0 falhas; `test_camadas_existentes_ficam_byte_identicas_com_os_defaults_novos` e `test_camada_residual_*` verdes (byte-identidade residual + defaults de `_render_camada`).
- Comando: `python -m pytest -q` (suíte completa)
  - Critério mínimo: 0 falhas (baseline `532 passed, 1 skipped` + os novos testes; nenhuma regressão).
- Comando: `python -c "import streamlit_app"`
  - Critério mínimo: import ok, sem erro.
- Comando: `ruff check src/motor_expansao/dashboard/censo_map.py src/motor_expansao/dashboard/censo_report.py tests/unit/test_relatorio_pontual_censitario_mapa.py tests/unit/test_relatorio_pontual_censitario_export.py` e `mypy` no mesmo diff
  - Critério mínimo: limpos.
- Comando (READ-ONLY M1 por mtime, antes×depois): `python - <<'PY'` listando mtime de `data/staging/brasil_estrutural.parquet`, `data/staging/brasil_priorizados.parquet`, `data/staging/hexagonos_brasil_oportunidades.parquet`, `data/staging/hexagonos_brasil_dashboard.parquet`, `data/staging/hexagonos_mapa_sample.parquet` — ou `ls -l --time-style=full-iso` desses artefatos — e comparar; ou no PowerShell `Get-Item <artefato> | Select-Object FullName,LastWriteTime`.
  - Critério mínimo: mtime INALTERADO em todos (nenhum artefato oficial reescrito).

## Fora de escopo
- Escrever/recalcular `score_priorizacao`, `hex_score_estrutural`, scores censitários, `oferta_efetiva_disponivel`, carteira, plano ou artefatos oficiais do M1.
- Tocar o motor censitário: `setor_censitario_intersecao_area_1p5km`, `RAIO_CENSITARIO_DEFAULT_KM=1,5` (o grid 2x2 e o motor seguem em 1,5 km; muda só a EXIBIÇÃO do painel hero).
- Alterar a métrica (continua `score_setor_2022_calibrado`) ou a paleta (continua `score_band_to_color`).
- Mexer na camada `score` do grid 2x2 ou em qualquer outra camada — byte-idênticas.
- Alterar `/Count 8`, o número de chaves de `CAMADAS_CENSITARIAS`, ou compartilhar/otimizar o fetch de basemap entre socioeconomia e residual.
- Dependência de rede ao vivo / provedor novo / DEC nova.
- Commitar `context/handoff.md`, `tasks/current_task.md`, `PRD.md`; colar corpo em `CLAUDE.md`/`PRD.md`.

## Alerta de aprovação
**ATENÇÃO: esta tarefa é de criticidade ALTA (mudança visual no PDF do Relatório Pontual, mesma natureza do BLK-RELPON-10). Exige GATE VISUAL HUMANO de Vinicius ANTES do Builder** — para aprovar (1) a aparência final do hex a 5 km na socioeconomia e (2) o tamanho das 2 imagens (`_HERO_MAP_SCALE`, partida 0.92, calibrável). O Builder SÓ pode rodar após a confirmação humana. A tarefa é render puro READ-ONLY sobre o M1 (nenhum passo escreve em `score_priorizacao`/`hex_score_estrutural`/pesos/artefatos oficiais — `score_setor_2022_calibrado` é apenas LIDO do `hexes_df` para colorir); se, na implementação, aparecer QUALQUER escrita nesses campos, ELEVAR para Crítica + DEC e alertar.

## Guardrails ativos
- **READ-ONLY M1 (§5 CLAUDE.md):** visualizações/render não recalculam nem alteram `score_priorizacao`, `hex_score_estrutural`, scores censitários, `oferta_efetiva_disponivel`, carteira, plano ou artefatos oficiais. Confirmado por mtime no QA.
- **Motor censitário INTOCADO:** `setor_censitario_intersecao_area_1p5km`, `RAIO_CENSITARIO_DEFAULT_KM=1,5`.
- **Acentuação (§2):** texto de usuário com acentuação correta; EXCEÇÃO DE RENDER — labels do PNG (fonte sem glifo acentuado) em ASCII: título "Socioeconomia - raio 5 km", "Score censitario (0-100)", "Score no hexagono: ...".
- **Estrutura do PDF:** `/Count 8` e as 8 chaves de `CAMADAS_CENSITARIAS` inalteradas; demais camadas byte-idênticas.
- **Sem dependência de API ao vivo** no dashboard de produção; sem provedor/DEC novos.
- **Toda mudança relevante entra com teste; nenhum PR sobe com CI quebrado** (§2).
