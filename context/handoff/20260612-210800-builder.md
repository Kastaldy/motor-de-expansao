# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-API-08 — Documentação ponta-a-ponta da API GeoEspacial

## O que foi feito

Criado `docs/api_geoespacial_uso.md` como fonte única de uso ponta-a-ponta da API GeoEspacial.
O documento cobre 11 seções conforme o plano técnico do Planner:

1. **Visão geral e arquitetura** — 2 containers (API + bot), diagrama ASCII, versão do contrato,
   dados lidos (READ-ONLY), docs interativas só fora de produção, raio fixo 1,5 km.
2. **Autenticação** — header Bearer, mapa token→consumidor, 401, padrão dev, gotchas de produção,
   como criar/rotacionar tokens.
3. **Endpoints** — `GET /health` (sem auth, alias `/api/v1/health`) + `POST /api/v1/analisar`
   (schema completo de request/response JSON com todos os 17 campos, response PDF,
   negociação de conteúdo com as 3 formas).
4. **Exemplos curl** — health, JSON (lat/lng), JSON (maps_url), PDF (3 formas equivalentes).
5. **Formatos de localização** — 4 padrões de URL por prioridade, links compactados,
   bounding box de validação.
6. **Bot Telegram "Paulo"** — fluxo de 5 etapas, 3 formas de localização, persistência de
   sessões, comandos, como rodar.
7. **Tabela de erros** — 4 slugs confirmados (coordenada_invalida/nao_autenticado/
   base_geo_ausente/erro_interno) + nota sobre 422.
8. **Variáveis de ambiente `API_*`** — tabela com todas as 15 variáveis, gotchas de produção
   para `API_CORS_ORIGINS` e `API_TOKENS`.
9. **Rodar localmente** — 3 comandos + notas sobre extras de dependência.
10. **Ponteiros para 4 docs relacionados** — sem duplicar conteúdo.
11. **Limitações e roadmap** — 7 itens (raio fixo, PostGIS, docs em produção, geocoder,
    SAM/residual, autenticação MVP, performance cold load).

**Verificações de veracidade técnica realizadas:**
- `__version__` confirmado como `"api-geoespacial/v1"` em `src/motor_expansao/api/__init__.py`.
- Rotas `/health`, `/api/v1/health` e `POST /api/v1/analisar` confirmadas em `main.py`.
- Todas as 15 variáveis `API_*` com defaults exatos confirmadas em `settings.py`.
- Bearer/401/`nao_autenticado` confirmados em `auth.py`.
- Negociação JSON/PDF (query param + body + Accept header) confirmada em `routes/analisar.py`.
- Campos de `AnalisarRequest` e `AnalisarResponseJSON` confirmados em `schemas/__init__.py`.
- Slugs `coordenada_invalida` (service.py linha ~89 + routes/analisar.py), `base_geo_ausente`
  (service.py linha ~94-98), `nao_autenticado` (auth.py + errors.py), `erro_interno` (errors.py)
  confirmados nos fontes.
- 4 padrões de URL + bounding box confirmados em `coord.py`.
- Cadeia de geocoding (Selenium/Chrome → Google Geocoding API → Nominatim) confirmada em `geo.py`.
- Padrões EMBED_PIN/DETAIL_PIN/REVERSED_PIN/CAMERA_AT confirmados em `maps_geocoder.py`.
- Fluxo do bot (estados saudou/autorizado/etapa="login", mensagens _SAUDACAO/_PEDIR_LOGIN/
  _PEDIR_LOCAL/_GERANDO/_AJUDA, 3 formas de localização, persistência `bot_sessoes_path`,
  timeout 240s) confirmados em `telegram_bot.py`.

**Divergências encontradas (código é a verdade):**
- Nenhuma divergência material encontrada. O plano do Planner estava correto em todos os fatos.
  Detalhe: o estado `aguardando_local` mencionado como "risco residual" no plano não existe
  como variável explícita (confirmado) — o fluxo real usa `etapa="login"` para o passo de nome,
  e após isso qualquer mensagem não-comando é tratada como localização. Documentado corretamente.

## Arquivos alterados

- `docs/api_geoespacial_uso.md` — **criado** (novo; entrega principal do BLK-API-08)
- `tasks/current_task.md` — status atualizado de "em execução / Planner" para "aguardando QA / Builder"
- `context/handoff.md` — handoff corrente atualizado
- `context/handoff/20260612-210800-builder.md` — este snapshot append-only

**Arquivos NÃO tocados:** nenhum arquivo de código (`src/`, `tests/`, `streamlit_app.py`,
`config.py`, etc.) foi modificado.

## Validações executadas

- **pytest:** NÃO executado (bloco puramente documental; nenhum arquivo de código foi tocado;
  suíte completa é responsabilidade do QA). Critério: 0 falhas, 0 erros de coleção esperados.
- **smoke import:** `python -c "import streamlit_app; print('import ok')"` → **ok**
  (warnings de ScriptRunContext são esperados fora do runtime Streamlit; não indicam erro)
- **git diff --name-only:** `context/handoff.md`, `tasks/current_task.md` (modificados);
  `docs/api_geoespacial_uso.md` (novo/untracked); os 2 geojson deletados (`data/raw/ibge/`)
  são pré-existentes no worktree, não foram tocados pelo Builder.
- **Checklist de seções** (Passo 12 do plano): TODAS as 12 seções cobertas:
  - [x] Visão geral e arquitetura (2 containers)
  - [x] Autenticação (header, mapa token→consumidor, 401, gotchas)
  - [x] `GET /health` (sem auth, resposta)
  - [x] `POST /api/v1/analisar` (request schema, response JSON completo, response PDF, negociação)
  - [x] Exemplos curl (health, JSON, PDF — 3 formas)
  - [x] Formatos de localização (4 padrões URL + links compactados + bounding box)
  - [x] Fluxo bot Telegram passo a passo (5 etapas, 3 formas de localização)
  - [x] Tabela de erros (4 códigos confirmados + nota 422)
  - [x] Tabela de vars `API_*` (15 variáveis completas com defaults + gotchas)
  - [x] Rodar local (3 comandos + notas)
  - [x] Ponteiros para os 4 docs existentes (sem duplicar)
  - [x] Limitações e roadmap (7 itens)

## Problemas encontrados

Nenhum. Todos os fatos técnicos foram confirmados nos fontes; o plano do Planner estava correto.

## Pendências

Nenhuma pendência para o QA além da suíte de testes completa (`pytest -n auto`).

## Riscos remanescentes

- **Timeout do PDF no bot (cold load):** mencionado no doc (seção 3b e 11) como comportamento
  normal esperado (~10-30 s; bot usa timeout 240 s). Não é risco de implementação.
- **Chrome/Selenium:** mencionado no doc como requisito do extra `[geocoder]`; sem Chrome,
  geocoding de endereço+CEP cai em Nominatim silenciosamente. Comportamento já documentado.

## Guardrails verificados

- score_priorizacao não alterado: **não aplicável** (bloco documental puro)
- Artefatos M1 preservados: **não aplicável** (nenhum arquivo de dados ou código tocado)
- Dashboard offline mantido: **não aplicável** (nenhum código de dashboard tocado)
- READ-ONLY sobre M1: **sim** — nenhum artefato oficial, score, peso ou parâmetro canônico alterado
- GUARDRAIL VPS §6: **respeitado** — nenhum comando no servidor executado
