# Handoff — QA

## Skill que gerou este handoff
QA

## Próxima Skill recomendada
Nenhuma (ciclo BLK-ATR-03 concluído; pronto para revisão humana + merge).

## Bloco validado
BLK-ATR-03 — Testar a estrutura de leitura: matriz de eixos vs score composto (GO/NO-GO).

## Veredito
**APROVADO.**

## Validações executadas de forma INDEPENDENTE

### 1. Suíte FULL (`python -m pytest -q`)
- Resultado: **1344 passed, 4 skipped, 4 failed** (454.61s).
- As **4 falhas são PRÉ-EXISTENTES** e do `openlocationcode` (módulo não instalado →
  `resolve_plus_code` retorna `None`): `test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline`,
  `::test_resolve_plus_code_curto_usa_localidade_mockada`,
  `test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline`,
  `::test_render_coord_search_plus_code_curto_usa_localidade_mock`. Todas fora do escopo BLK-ATR-03.
  Confirmado `ModuleNotFoundError: No module named 'openlocationcode'`.
- **Regressões novas = 0.** Os 15 testes novos de `test_estrutura_funil.py` passaram dentro da full.

### 2. ruff
- `ruff check estrutura_funil.py test_estrutura_funil.py` → **All checks passed!**

### 3. Guardrails
- Isolamento (DEC-012): `grep -nE "from.*pipelines|from.*dashboard|from.*censo_|from.*api|import config"` → **VAZIO**.
  Imports só de `motor_expansao.dimensionamento.{aderencia,backtest_dim}` (camada paralela irmã; precedente
  `backtest_tp05.py`/`calibracao_residual.py`) + `.contrato` (COLUNAS_PII_PROIBIDAS) + stdlib/numpy/pandas/scipy/sklearn.
- `python scripts/loop_guard.py --base ciclo/loop-20260706-152137` → **GUARD OK** (21 caminhos, nenhum proibido).
- READ-ONLY M1 (§5): mtime dos 4 oficiais INALTERADO (estrutural/priorizados/oportunidades 2026-06-03;
  dashboard 2026-06-12); `git status` limpo neles.
- `python -c "import streamlit_app"` → **import ok**.

### 4. Corretude (leitura do código)
- **Gate ATR-02 replicado inline**: constantes locais `POP_MIN_GATE_ATR=5000` / `RENDA_PC_MIN_GATE_ATR=1500.0`;
  aplicadas em `aplicar_gate_atratividade` (linha 249: `pop_corte >= POP_MIN_GATE_ATR & renda_pc >= RENDA_PC_MIN_GATE_ATR`).
  Réguas de `pop_corte.py` COPIADAS como privadas (`_derive_populacao_corte` etc.), não importadas.
- **R² in-sample BANIDO do veredito**: `_decidir_veredito` (linhas 587–646) não referencia `insample`; o campo
  `r2_insample` existe só como auditoria rotulada com `_ROTULO_INSAMPLE`.
- **`membros` só ALVO (DEC-009)**: `_preparar_alvo` → `y = log1p(membros)`; `membros` nunca em `EIXOS`/`FEATURES_COMPOSTO`;
  teste `test_membros_nunca_feature` blinda.
- **Degradação graciosa em share=1.0**: `flag_huff_disponivel = (share < 1.0)`; disputa = percentil de `1 - share`
  (percentil baixo no monopólio, nenhuma linha perdida); sub-análise dos competitivos separada. Fixture
  `_df_redundante` (share=1.0 total) exercita.
- Veredito honesto: GO-composto só se `R²_oof > 0.05` E `IC95_inf > 0` E `ganho > 0.01` E não-redundante
  (`pearson pred < 0.95`); caso contrário → MATRIZ (default; NO-GO válido, DEC-008).

### 5. Housekeeping
- `housekeeping_move_block.py BLK-ATR-03 --date 2026-07-06` → movido (2466 bytes) para completed + stub no backlog.
- `housekeeping_move_block.py BLK-ATR-03 --check` → **OK**.

## Guardrails honrados
- §5 READ-ONLY M1: mtime dos 4 oficiais inalterado; nenhuma escrita em score/pesos/artefatos.
- DEC-008: out-of-fold vs baseline; R² in-sample fora do veredito; IC95 seed=42; NO-GO/matriz é resultado válido.
- DEC-009: `membros` só como ALVO.
- DEC-012: pacote disjunto; sem PII; fixtures sintéticas.

## Nota operacional
O veredito REAL do funil (`data/analysis/estrutura_funil.md`) NÃO foi materializado — é passo operacional
pós-merge (`python -m motor_expansao.demanda_revelada.estrutura_funil`, caminho `# pragma: no cover`),
não gate de teste.

## Arquivos afetados neste passo QA
- `tasks/completed.md` (bloco BLK-ATR-03 movido + nota de conclusão/QA)
- `tasks/backlog.md` (stub de 1 linha)
- `tasks/current_task.md` (status → concluído)
- `context/handoff.md` + `context/handoff/20260706-202245-qa.md`
