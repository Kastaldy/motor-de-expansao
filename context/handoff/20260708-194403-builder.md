# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA

## Bloco
BLK-VIAB-07

## Status
IMPLEMENTADO

## Veredito do script de validação
GO

Delta MAPE = 9.13 p.p. (sem filtro 36.44% -> com filtro low_cost_massa 27.31%),
IC95 do delta = [6.60, 11.77] (não cruza zero), baseline da média = 45.19%
(cenário com filtro bate o baseline). Os 3 critérios de GO satisfeitos:
ganho >= 1 p.p. E IC não cruza zero E bate baseline. Como foi GO, o parâmetro
`formato` foi propagado ao orquestrador `analisar_viabilidade_ponto` (A3) e o
teste de propagação foi adicionado.

## Arquivos alterados
- `src/motor_expansao/dimensionamento/viabilidade_ponto.py` (modificado)
  - Constantes `FORMATO_LOW_COST_MASSA`, `FORMATO_BOUTIQUE_PREMIUM`, `FORMATO_POR_MARCA` (após `DEMANDA_FONTE_PREMISSA`).
  - `faixa_alunos_por_densidade`: novo kwarg-only `formato: str | None = None` + bloco de filtro gracioso (deriva de coluna `formato` ou de `marca` via `FORMATO_POR_MARCA`; fallback para base completa se < n_min).
  - `analisar_viabilidade_ponto`: novo kwarg-only `formato: str | None = None` propagado a `faixa_alunos_por_densidade` (GO).
  - `__all__`: adicionadas as 3 constantes novas.
  - Docstrings de ambas as funções atualizadas.
- `scripts/validar_formato_viabilidade.py` (criado) — validação k-fold 5×5 seed=42, alvo `alunos_reais`, IC95 bootstrap, veredito GO/NO-GO, grava relatório gitignored.
- `tests/unit/dimensionamento/test_viabilidade_ponto.py` (adicionados 7 casos BLK-VIAB-07):
  `test_formato_none_byte_identico`, `test_formato_filtra_comparaveis`,
  `test_formato_deriva_de_marca_sem_coluna_formato`, `test_formato_fallback_n_min`,
  `test_mapeamento_formato_por_marca`, `test_formato_exportado_em_all`,
  `test_analisar_viabilidade_ponto_propaga_formato` (só por GO).
- `data/analysis/relatorio_formato_densidade.md` (gerado pelo script; GITIGNORED — NÃO commitado).

## Saída literal das validações

### Script validar_formato_viabilidade.py
```
[BLK-VIAB-07] veredito=GO MAPE sem=36.44% com=27.31% delta=9.13pp IC_delta=[6.60,11.77] baseline=45.19%
Relatorio: /repo/data/analysis/relatorio_formato_densidade.md
```

### pytest
```
============================= test session starts ==============================
platform linux -- Python 3.12.13, pytest-8.4.2, pluggy-1.6.0
rootdir: /repo
configfile: pyproject.toml
plugins: xdist-3.8.0, mock-3.15.1, asyncio-0.24.0, cov-7.1.0, anyio-4.13.0
asyncio: mode=Mode.AUTO, default_loop_scope=None
collected 34 items

tests/unit/dimensionamento/test_viabilidade_ponto.py ................... [ 55%]
...............                                                          [100%]

============================== 34 passed in 2.96s ==============================
```

### ruff
```
All checks passed!
```
(1ª execução apontou UP031 no script — percent-format `% len(df_ultra)` convertido para f-string; reexecução limpa.)

### loop_guard
```
GUARD OK: 3 caminho(s) alterado(s), nenhum proibido.
```
(Executado com os 3 paths do bloco staged; `--base HEAD` só enxerga diff commitado/staged.)

## Guardrails verificados
- §5 READ-ONLY M1: ZERO toque em `config.py` (canônico M1), `pipelines/m1/`, `score_priorizacao`, pesos, artefatos oficiais. Só `dimensionamento/viabilidade_ponto.py` + testes + script novo.
- §6.1 loop-safe: sem VPS, sem rede, sem PII. Script lê só `data/staging/base_calibracao_multirede.parquet`.
- DEC-008: veredito por k-fold out-of-fold 5×5 vs baseline da média; R² in-sample BANIDO (não aparece no script); IC95 bootstrap; NO-GO seria resultado válido (aqui deu GO honesto).
- DEC-009: alvo = `alunos_reais` (reais), nunca `membros`/agregador. A curva produz FAIXA de referência por m2, não predição geográfica.
- Default `formato=None` => `faixa_alunos_por_densidade` byte-idêntica (travado por `test_formato_none_byte_identico`); dashboard não passa `formato` => sem regressão.
- `FORMATO_POR_MARCA` no módulo `dimensionamento/`, NÃO em `config.py`.
- `data/analysis/relatorio_formato_densidade.md` é gitignored — não commitado.
- Sem `git add -A`/`git add .`; só os 3 paths do bloco staged. Sem merge/push/deploy.

## Notas para o QA
- Base útil real: Ultra N=54 (low_cost_massa), EngCorpo N=58 (boutique_premium), skyfit 0 (metragem NaN). O ganho medido (9.13 p.p.) confirma a expectativa do BLK-VIAB-04-FU (Ultra-only ~26.8% vs misto ~35.3%).
- O parquet `base_calibracao_multirede.parquet` NÃO tem coluna `alunos_por_m2` nem `formato`; o script deriva `alunos_por_m2 = alunos_reais/metragem` e `formato` via `FORMATO_POR_MARCA`. Já o dashboard passa uma base só-Ultra COM `alunos_por_m2` e sem `formato` — o default `None` preserva o caminho atual.
- O filtro por formato acontece ANTES da limpeza de `__apm` e da janela de metragem; fallback silencioso para base completa quando < n_min pós-filtro (coberto por `test_formato_fallback_n_min`).
- QA deve rodar a suíte full como gate único (`pytest -n auto`). Sugestão: também `ruff`/`mypy` no módulo.
