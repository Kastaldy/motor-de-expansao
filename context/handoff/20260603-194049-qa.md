# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador executa o Passo 6.0 housekeeping move + commit; documentação opcional)

## VEREDITO
APROVADO

## Justificativa
O Builder implementou EXATAMENTE as 4 decisões de produto aprovadas no gate (Felipe, 2026-06-03), em
escopo restrito (só `components.py` + teste), sem tocar score/artefatos/pesos; todas as validações
re-executadas pelo QU passaram (full suite `660 passed, 1 skipped, 0 failed`; ruff/mypy limpos; smoke ok),
serial == `-n auto` (185), e o housekeeping `--check` está no estado pré-move esperado.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- Follow-up de PRODUTO (já registrado pelo Builder, fora deste ciclo): orla de baixa pop residencial
  ainda cai no corte de display <5k e renderiza com a cor de "descartado <5k" (agora VISÍVEL, alpha 150);
  e a régua de 5k nas TABELAS de carteira/domínio (`pages.py:1279`/`:1666`) segue intocada (decisão iii —
  eventual toggle em ciclo separado). Nenhuma ação neste ciclo.

## Testes faltantes
- nenhum. A cobertura é suficiente: (a) M1 orla pop<5k visível no layer, alpha≥140, ≠ cinza antigo
  (`test_build_map_figure_orla_baixa_pop_renderiza_visivel`); (b) Híbrido e Residual com score NaN
  presentes no layer com `_NAN_SCORE_FILL` (2 testes novos); (c) `_apply_pop_cut_colors` usa alpha
  visível (`test_apply_pop_cut_colors_usa_alpha_visivel`); (d) o teste de cor existente foi atualizado
  para a nova `_DISCARDED_FILL`; (e) o caso "sem dados no mapa" foi reancorado em geometria NaN — caso
  honesto que ainda exercita o ramo `st.info` (None-return), sem mascarar regressão (o path score-NaN
  agora é coberto por asserções POSITIVAS mais fortes nos 2 testes novos).

## Riscos remanescentes
- Scope relaxado (Híbrido/Residual) pode aumentar o nº de hexes em UFs grandes e encostar no cap
  `MAP_POINT_LIMIT_LARGE`. Mitigação ativa e verificada: cap/downsample INALTERADOS; testes de payload
  enxuto seguem verdes na suíte full. Baixo risco.

## Saída literal das validações (re-executadas pelo QA)

### git diff --name-only HEAD (escopo)
```
context/handoff.md
src/motor_expansao/dashboard/components.py
tasks/current_task.md
tests/integration/test_streamlit_app.py
```
(Só `components.py` + `test_streamlit_app.py` são código; `handoff.md`/`current_task.md` são tracking.
NENHUM constants/config/pipelines/score/data.py/pages.py/utils.py no diff.)

### git diff --name-only HEAD | grep -iE "constants|config|pipelines|scor"
```
VAZIO_GUARDRAIL_OK
```

### Pesos M1 intactos (src/motor_expansao/core/constants.py — NÃO no diff)
```
PESOS_HEX_SCORE_ESTRUTURAL = {
    "renda_per_capita": 0.40,
    "populacao_proxy": 0.60,
}
```

### python -m pytest -q tests/integration/test_streamlit_app.py tests/unit/test_pop_cut.py (SERIAL)
```
185 passed, 7976 warnings in 10.49s
EXIT_CODE=0
```

### python -m pytest -n auto -q tests/integration/test_streamlit_app.py tests/unit/test_pop_cut.py (PARALELO)
```
185 passed, 15952 warnings in 23.50s
EXIT_CODE=0
```
(Serial == -n auto: 185 == 185, sem flakiness por paralelismo.)

### python -m pytest -n auto -q (SUÍTE FULL — gate único do ciclo)
```
660 passed, 1 skipped, 125507 warnings in 89.84s (0:01:29)
PYTEST_EXIT=0
```
(0 failed / 0 erros de coleção. Acima da baseline histórica 532/2026-05-28 — divergência de ambiente
Python 3.14, não defeito; critério é 0 falhas, conforme BLK-ORQ-01/BLK-FIX-06. Os 125507 warnings são
DeprecationWarning de pytest-asyncio do ambiente, pré-existentes.)

### python -c "import streamlit_app; print('import ok')" (SMOKE)
```
import ok
EXIT_CODE=0
```

### python -m ruff check src tests
```
All checks passed!
RUFF_EXIT=0
```

### python -m mypy src (types-requests 2.33.0.20260518 instalado)
```
Success: no issues found in 46 source files
MYPY_EXIT=0
```
(As linhas "note: ... [annotation-unchecked]" são informativas, não erros.)

### python scripts/housekeeping_move_block.py BLK-FIX-06-C --check (PRÉ-MOVE — estado esperado)
```
FALHA --check: stub ausente no backlog para BLK-FIX-06-C
HK_EXIT=1
```
(EXIT≠0 com "stub ausente no backlog" CONFIRMA que o helper RECONHECE o bloco — NÃO é EXIT_AD_HOC=3 /
BlockNotFound. Estado pronto para o MOVE do orquestrador. QA NÃO executou o move.)

### python -m pytest -q tests/unit/test_housekeeping_helper.py
```
10 passed, 421 warnings in 0.08s
HK_TEST_EXIT=0
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock do caminho
crítico. Este ciclo NÃO tem tooling de produção (SOPS/segredos/artefatos reais) — a regra anti-bypass está
SEM ALVO aqui. Os testes pytest montam DataFrames sintéticos em memória e chamam os builders reais
(`build_map_figure`/`build_hybrid_map_figure`/`build_residual_heatmap_figure`/`_apply_pop_cut_colors`),
inspecionando `fill_color`/membros do layer — fixture legítima de teste unitário, explicitamente NÃO-bypass
conforme `prompts/qa_analyzer.md` §(b). Os builders só LEEM score; nenhuma escrita.

## Conferência cruzada com log do Builder
BATE em tudo. Builder reportou 185 passed no subset → QA confirmou 185 (serial e `-n auto`). Builder
reportou ruff "All checks passed!" e mypy "Success: no issues found in 46 source files" → idênticos no QA.
Builder reportou import ok → idêntico. Valores finais das constantes conferidos por leitura do arquivo:
`_DISCARDED_FILL=[150,150,170,150]`, `_DISCARDED_LINE=[170,170,190,200]`, `_NAN_SCORE_FILL=[110,116,140,150]`.
Precedência verificada por leitura: o fallback de NaN roda ANTES de `_apply_pop_cut_colors` no Híbrido
(fallback `:1635-1640` → pop-cut `:1684`) e no Residual (fallback `:1807-1817` → pop-cut `:1826`), de modo
que hex NaN+pop<5k termina com `_DISCARDED_FILL`. M1 (`build_map_figure`) NÃO recebeu fallback de NaN
(`:1412` mapeia `score_priorizacao`, que na orla é válido) — correto, só herda a nova `_DISCARDED_FILL`.
Guards de COLUNA ausente preservados (`:1581-1582` e `:1767-1768`). Builder marcou a única divergência
(suite full 660 vs 185 do subset) como esperada — o subset é só o impactado; a full é o gate do QA.

## Decisões de produto aprovadas — verificação 1:1 (leitura do arquivo)
- (i) `_DISCARDED_FILL = [150, 150, 170, 150]` (alpha 150, visível): CONFIRMADO `components.py:1109`.
- (ii) `_NAN_SCORE_FILL = [110, 116, 140, 150]` novo, VISÍVEL (alpha 150 ≥ 140) e DISTINTO de
  `_DISCARDED_FILL` (RGB [150,150,170] ≠ [110,116,140]) e das 10 faixas `RESIDUAL_SCORE_BANDS` (todas
  saturadas red/orange/yellow/green, nenhuma cinza-azulada): CONFIRMADO `components.py:1113`.
  Scope Híbrido/Residual relaxado de `score_setor_2022_calibrado.notna()` para `hex_id/lat/lng notna()`,
  com guard de COLUNA ausente preservado: CONFIRMADO.
- (iii) Tabelas (`pages.py`) NÃO mexidas: CONFIRMADO — `pages.py` NÃO está no diff.
- (iv) Duas cores de exceção DISTINTAS (descartado-<5k ≠ sem-score-NaN): CONFIRMADO.
- Precedência de cor (NaN antes de pop-cut → hex NaN+pop<5k vira "descartado <5k"): CONFIRMADO por leitura.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (builders só LEEM; `score_band_to_color`/`utils.py` intocado;
  `test_pop_cut.py::test_nao_altera_score_priorizacao` verde na full).
- Artefatos M1 preservados: **sim** (nenhum parquet/pipeline/`config.py`/`core/constants.py` no diff).
- Pesos `renda=0.40`/`pop=0.60` (DEC-001): **intactos** (`core/constants.py`, fora do diff).
- Parâmetros canônicos §3 (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, M1_HEX_LAND_FRACTION_MIN):
  **inalterados** (`config.py` fora do diff).
- Validações re-executadas pelo QA (sem bypass):
  - escopo (git diff) → só components.py + test (+ tracking)
  - subset serial → 185 passed
  - subset -n auto → 185 passed (== serial)
  - full -n auto → 660 passed, 1 skipped, 0 failed
  - import streamlit_app → ok
  - ruff → All checks passed!
  - mypy → Success: no issues found in 46 source files
  - housekeeping --check → exit 1 "stub ausente no backlog" (pré-move esperado)
  - test_housekeeping_helper → 10 passed
- Escopo respeitado: **sim** — nenhum arquivo fora de `components.py`/`test_streamlit_app.py` (+ tracking).

## Decisão recomendada
Fechar ciclo. O orquestrador executa o Passo 6.0 (move via `scripts/housekeeping_move_block.py
BLK-FIX-06-C`) e o commit. QA NÃO commitou, NÃO criou branch, NÃO fez merge, NÃO rodou o move, NÃO tocou
PRD.md.
