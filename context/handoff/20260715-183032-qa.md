# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (modo MERGE-HUMANO) — gate de REVISÃO VISUAL do PDF por humano antes do merge.

## VEREDITO
APROVADO

## Justificativa
A entrega implementa EXATAMENTE o escopo do BLK-RELPON-08 (D1/D2/D3, Q1-Q4), READ-ONLY sobre o
M1, em exatamente os 5 arquivos previstos; todos os 300 testes do escopo (motor + export/PDF +
integração) passam, ruff/mypy limpos, import ok. As únicas 4 falhas da suíte full são
pré-existentes de ambiente (Python 3.14 local) — comprovadas idênticas na BASE via `git stash` —
e não têm relação alguma com os arquivos tocados.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- **4 falhas pré-existentes de ambiente na suíte full** (NÃO introduzidas por este ciclo):
  `tests/unit/dimensionamento/test_batch_viabilidade.py::test_rodar_candidato_grade_serializa`,
  `tests/unit/test_score_retencao_territorial.py::test_schema_exato_do_output`,
  `tests/unit/test_validation_dataset.py::test_resolve_hex_valido_e_nulo`,
  `tests/unit/test_validation_dataset.py::test_join_label_by_name_casado_e_nao_casado`.
  São de erro de tipo tarfile/lzma e `assert nan is None` — divergências de Python 3.14 local
  (CLAUDE.md §5: "divergência vs baseline é de ambiente, não defeito"). Provei serem
  pré-existentes rodando-as na BASE com as 5 mudanças em `git stash` → falham IDENTICAMENTE.
  Fora do escopo dos arquivos tocados; o gate real de merge é o CI em Python 3.11.

## Melhorias opcionais
- nenhuma

## Testes faltantes
- nenhum. Cobertura nova adequada: agregação `domicilios_total_raio` (setor totalmente dentro =
  peso 1.0, setor parcial = peso de área conhecido, "n/d" por coluna ausente, "n/d" sem
  candidatos), ordem da linha 1, helper `_cor_por_meta` (verde/vermelho/neutro, meta inclusiva,
  None/NaN), helper assimétrico `_cor_consumo_concorrentes` (mercado consumido, n/d isolado),
  remoção do card "Score censitário máximo" do PDF, rótulo "Número de domicílios".

## Riscos remanescentes
- **Revisão VISUAL humana do PDF é gate obrigatório** antes do merge (contraste dos tons pastel
  `_CARD_VERDE_RGB`/`_CARD_VERMELHO_RGB`/`_CARD_NEUTRO_RGB` sobre texto cinza-escuro — hipótese de
  partida do Planner, ajuste fino fica para o gate visual). Não é bloqueio de código.
- Metas `_META_*` são thresholds de DISPLAY locais ao PDF; não alteram `flag_sam` (DEC-006/007)
  nem `sam_fitness_potencial`/`oferta_efetiva_disponivel` (só LIDOS).

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte FULL (gate único do ciclo) — `python -m pytest -n auto -q`
```
=========================== short test summary info ===========================
FAILED tests/unit/dimensionamento/test_batch_viabilidade.py::test_rodar_candidato_grade_serializa
FAILED tests/unit/test_score_retencao_territorial.py::test_schema_exato_do_output
FAILED tests/unit/test_validation_dataset.py::test_resolve_hex_valido_e_nulo
FAILED tests/unit/test_validation_dataset.py::test_join_label_by_name_casado_e_nao_casado
4 failed, 1726 passed, 85 skipped, 548484 warnings in 1777.42s (0:29:37)
```
As 4 falhas NÃO são flakiness de paralelismo (determinísticas) nem do escopo. Prova de
pré-existência — as mesmas 4 rodadas na BASE com as 5 mudanças em `git stash`:
```
=== STASHED. Rodando as 4 na BASE (sem mudancas do branch) ===
FAILED tests/unit/dimensionamento/test_batch_viabilidade.py::test_rodar_candidato_grade_serializa
FAILED tests/unit/test_score_retencao_territorial.py::test_schema_exato_do_output
FAILED tests/unit/test_validation_dataset.py::test_resolve_hex_valido_e_nulo
FAILED tests/unit/test_validation_dataset.py::test_join_label_by_name_casado_e_nao_casado
4 failed, 3105 warnings in 2.16s
```
Ambiente local = `Python314` (Python 3.14); erros do tipo `assert nan is None` e
`Argument "fileobj" to "_BytesTarFile" has incompatible type` / `LZMAFile`.

### 1b. Testes do escopo (relatório pontual motor + export/PDF + integração)
```
python -m pytest -q tests/unit/test_relatorio_pontual_censitario_motor.py \
  tests/unit/test_relatorio_pontual_censitario_export.py \
  tests/integration/test_streamlit_app.py
300 passed, 16312 warnings in 50.48s
```

### 2. Smoke import — `python -c "import streamlit_app; print('import ok')"`
```
(warnings de MemoryCacheStorageManager/ScriptRunContext — esperados fora do runtime Streamlit)
import ok
```

### 3. Lint/tipos nos arquivos tocados
```
=== RUFF ===  (censo_point.py, censo_report.py, test_..._motor.py, test_..._export.py)
All checks passed!
=== MYPY ===  (censo_point.py, censo_report.py)
Success: no issues found in 2 source files
```

### 4. Guardrail READ-ONLY M1 — `git diff --name-only HEAD | grep -E 'pipelines/m1|config.py|score|hex_score|artefato'`
```
NENHUM arquivo M1/config/score tocado
```

### 5. Acentuação (§2) — scan de codepoints > U+00FF em `censo_report.py`
Todos os `U+2014` (travessão) encontrados estão em COMENTÁRIOS/DOCSTRINGS (não renderizados no
PDF) e são PRÉ-EXISTENTES (nenhum em linha adicionada pelo diff). As strings novas RENDERIZADAS
("Número de domicílios" e a nota de fonte do semáforo) são latin-1 safe — confirmado pelos
testes de export que asseguram os rótulos via `.encode("latin-1")` e passam.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, nem mock
do caminho crítico. A suíte usa fixtures/sintéticos legítimos de teste unitário (setores
sintéticos com peso de área conhecido; injeção de `domicilios_total_raio` no `result` no teste de
PDF) — prática normal, fora do escopo da regra anti-bypass (que mira tooling de produção como
SOPS). Suíte full re-executada 1× por conta própria com `-n auto`.

## Conferência cruzada com log do Builder
Bate. Builder reportou subconjunto impactado 300 passed / import ok / ruff All checks passed /
mypy Success — reproduzido idêntico. Builder reportou "contagem de páginas 6 / `PDF_SECTION_HEADERS`
inalterado" — coerente com os testes de export verdes. O Builder rodou apenas o subconjunto (por
design); o QA rodou a suíte FULL (gate único) e verificou que as 4 falhas são de ambiente e
pré-existentes, não do diff.

## Guardrails verificados
- score_priorizacao não alterado: **sim** (nenhuma escrita em score/pesos/`hex_score_estrutural`;
  `config.py`/`pipelines/m1/` intocados — confirmado por `git diff --name-only`).
- Artefatos M1 preservados: **sim** (nenhum artefato oficial tocado; só leitura/agregação
  censitária e render do PDF).
- Parâmetros canônicos: **preservados** (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0,
  pesos renda=0.40/pop=0.60 — nenhum tocado; método de interseção
  `setor_censitario_intersecao_area_1p5km`, raio 1,5 km e marca d'água INTOCADOS).
- `domicilios_total_raio` segue EXATAMENTE o padrão de peso de área de `pop_total_raio`
  (`domicilios_particulares_ocupados_setor_2022 × peso_area_setor`, soma; guard "n/d" gracioso;
  chave `None` no dict default garantindo o campo em todos os early-returns). Verificado no código
  e coberto por teste dedicado com peso de área conhecido + caso "n/d".
- `_big_numbers_page`: L1 = [População total no raio, Renda per capita média, Número de
  domicílios, Score censitário médio]; "Score censitário máximo" removido da grid (`score_setor_max`
  permanece em `result`/CSV); L2 inalterada; `_cor_por_meta` trata None/"n/d" → NEUTRO ANTES da
  comparação; regra assimétrica do Consumo (SAM≥2000 E Residual<2000 → vermelho); Concorrentes
  espelha Consumo; 6 metas são constantes NOMEADAS. Tudo confirmado.
- Validações re-executadas pelo QA (sem bypass):
  - `pytest -n auto` (full) → 4 failed (pré-existentes de ambiente, provados) / 1726 passed / 85 skipped
  - `pytest` (escopo relatório pontual + export + integração) → 300 passed
  - `import streamlit_app` → import ok
  - `ruff check` (4 arquivos) → All checks passed
  - `mypy` (censo_point.py, censo_report.py) → Success: no issues found
- Escopo respeitado: **sim** — exatamente os 5 arquivos do handoff/current_task
  (`censo_point.py`, `censo_report.py`, 2 arquivos de teste, `docs/relatorio_pontual_censitario.md`).
  Nenhum excesso.

## Housekeeping
A executar no fechamento pelo orquestrador (modo MERGE-HUMANO, Passo 6.0): o bloco BLK-RELPON-08 é
**manual (NÃO loop-safe)** com gate de revisão visual do PDF. O QA NÃO moveu o bloco. QA validou
apenas código + suíte.

## Decisão recomendada
Fechar ciclo após o gate de REVISÃO VISUAL humana do PDF (obrigatório para bloco manual). Merge
por humano (modo MERGE-HUMANO). O gate real de CI (Python 3.11) deve confirmar suíte verde — as 4
falhas locais são de ambiente 3.14 e não devem aparecer no CI.
