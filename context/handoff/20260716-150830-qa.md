# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (bloco Média, READ-ONLY M1; gate humano [REVISÃO HUMANA — visual/perf] já ocorrido entre Planner e Builder). A MEDIÇÃO empírica (FPS/latência/A-B) é passo HUMANO pós-QA, fora do escopo de código deste bloco.

## VEREDITO
APROVADO

## Justificativa
O spike deck.gl `H3HexagonLayer` está implementado conforme o plano fechado (D1–D6): isolado, opt-in, descartável, READ-ONLY sobre o M1 e sem dep nova. Todas as validações obrigatórias re-executadas por mim passaram; a única falha da suíte FULL é o teste pré-existente `test_score_retencao_territorial::test_run_readonly_m1_por_mtime` (parquet de staging gitignored ausente no ambiente local, pacote `lifetime/`), sem qualquer relação com o spike — não é regressão.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- A "Validações obrigatórias" do handoff do Planner lista `python -c "import motor_expansao.dashboard.streamlit_app"`, mas esse módulo NÃO existe — o app real é o top-level `streamlit_app`. O Builder tratou isso corretamente (usou `import streamlit_app`, ok). É imprecisão de documentação do Planner, não defeito do Builder.
- Baseline local NÃO fica 100% verde por causa do parquet `data/staging/unidade_territorio_retencao.parquet` gitignored ausente (falha ambiental pré-existente, alheia a este bloco). Nada a corrigir aqui; registrado para não mascarar.

## Testes faltantes
- nenhum. Os 7 casos do D6 estão cobertos (25 testes determinísticos, sem browser/rede). O harness Playwright é corretamente testado só na parte pura (guarda anti-produção + parser), sem browser.

## Riscos remanescentes
- CDN em runtime (deck.gl@latest + tiles CARTO): só quando o spike é aberto no browser sob opt-in; não é caminho de produção (`pages.py` não importa) e os testes não batem em rede. Aceitável para spike.
- Payload inline ~18k×7 chaves: é justamente o custo que o spike quer expor (documentado no runbook para medição humana). Não otimizado — correto para o objetivo.

## Saída literal das validações (re-executadas pelo QA)

### Suíte FULL (gate único) — SERIAL (`-p no:xdist`; xdist/execnet instável no Python 3.14 local, conforme CLAUDE.md §5)
```
$ python -m pytest -q -p no:xdist
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 1843 passed, 2 skipped, 278932 warnings in 871.88s (0:14:31)
```
Causa da única falha (reproduzida isolada):
```
$ python -m pytest -q -p no:xdist "tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime"
E   FileNotFoundError: [Errno 2] No such file or directory:
    '...\data\staging\unidade_territorio_retencao.parquet'
1 failed, 625 warnings in 1.74s
```
→ Pré-existente e ambiental (parquet de staging gitignored ausente), pacote `lifetime/` (BLK-LTV), sem relação com `ui_spike_deckgl`. `git diff --name-only HEAD` vazio ⇒ o spike não tocou nada tracked ⇒ falharia idêntico no HEAD. NÃO é regressão do BLK-REV-08.

### Testes do spike
```
$ python -m pytest -q -p no:xdist tests/unit/test_ui_spike_deckgl.py
25 passed, 1315 warnings in 0.97s
```

### ruff
```
$ python -m ruff check src/motor_expansao/dashboard/ui_spike_deckgl.py scripts/rev08_spike_playwright.py tests/unit/test_ui_spike_deckgl.py
All checks passed!
```

### mypy
```
$ python -m mypy src/motor_expansao/dashboard/ui_spike_deckgl.py
Success: no issues found in 1 source file
```

### Imports
```
$ python -c "import motor_expansao.dashboard.ui_spike_deckgl"        -> import spike ok
$ python -c "import streamlit_app"                                    -> import streamlit_app ok
$ python -c "import motor_expansao.dashboard.streamlit_app"           -> ModuleNotFoundError (módulo inexistente;
                                                                        o app real é top-level `streamlit_app`)
```

### Isolamento pages.py
```
$ grep -L "ui_spike_deckgl" src/motor_expansao/dashboard/pages.py
src/motor_expansao/dashboard/pages.py         # imprime o path = NÃO contém a string = spike NÃO importado
$ grep -c "ui_spike_deckgl" src/motor_expansao/dashboard/pages.py
0
```

### Cap espelha produção (`components.py:1552`)
```
components.py:1552: effective_limit = MAP_POINT_LIMIT_LARGE if len(key) > MAP_POINT_LIMIT else MAP_POINT_LIMIT
ui_spike_deckgl.py: effective_limit = MAP_POINT_LIMIT_LARGE if len(df)  > MAP_POINT_LIMIT else limit
(limit default = MAP_POINT_LIMIT ⇒ idêntico ao caminho de produção; mode_stress desliga o cap.)
```

### Recorte real SP (partição de produção existente)
```
len(hexagons): 18000            # SP 47.389 > 35k → cap LARGE 18k (cenário do plano)
view: {'longitude': -48.60185, 'latitude': -22.36087, 'zoom': 4.55}
version: ui_spike_deckgl_v1
keys por hex (union): ['bm1', 'br', 'h', 'o', 'p', 's', 'sr']
violacoes PII/geo/lat-lng: []
ordenado desc: True
bm1 range: 7 9 | br range: 0 9
NaN no blob: False | Infinity: False
determinismo hexagons byte-identico: True | view identico: True
```

### HTML deck.gl (`_build_deckgl_html`)
```
OK unpkg.com/deck.gl | OK H3HexagonLayer | OK getHexagon | OK updateTriggers
OK window.__spikeFirstPaintMs | OK window.__spikeRecolorMs | OK window.__spikeAddHexMs
OK __spikeToggleColor | OK __spikeAddHex | OK 87a8a07c4ffffff (dados embutidos)
PII vaza: False | HTML usa CDN: True
```

### Harness Playwright
```
$ python scripts/rev08_spike_playwright.py --help
usage: rev08_spike_playwright.py ... [--url] [--uf] [--flow {render,recolor,scenario,pdf,all}]
       [--runs] [--out] [--target {spike,production}] [--i-confirm-production] [--headed] [--timeout-ms]
(--help funciona sem playwright instalado ⇒ import lazy OK)

$ python scripts/rev08_spike_playwright.py --url https://dashboard.ultra-expansao.tech
ABORTADO: a URL '...' aponta para produção (ultra-expansao.tech). ...repita com --i-confirm-production.
exit=2
```

### Runbook
```
3 sub-entregáveis presentes (i DevTools / ii Playwright prod / iii A-B Caddy).
$ grep -c "§6" docs/rev08_spike_perf_runbook.md                              -> 8
$ grep -c "exige confirmação humana individual" docs/rev08_spike_perf_runbook.md -> 6 (1 por comando VPS)
Tabelas EM BRANCO (métricas não inventadas). Acentuação PT correta.
```

### READ-ONLY M1 / escopo
```
$ git diff --name-only HEAD            -> (vazio; nada tracked modificado)
$ git status --short
?? context/handoff/20260716-141859-block-orchestrator.md
?? context/handoff/20260716-142730-planner.md
?? context/handoff/20260716-144943-builder.md
?? docs/rev08_spike_perf_runbook.md
?? scripts/rev08_spike_playwright.py
?? src/motor_expansao/dashboard/ui_spike_deckgl.py
?? tests/unit/test_ui_spike_deckgl.py
(handoff.md/current_task.md são gitignored — daí não aparecem; bookkeeping esperado)

mtime dos 4 oficiais (todos 2026-06-10, inalterados):
2026-06-10 09:50  data/staging/brasil_estrutural.parquet
2026-06-10 09:50  data/staging/brasil_priorizados.parquet
2026-06-10 09:50  data/staging/hexagonos_brasil_oportunidades.parquet
2026-06-10 10:24  data/outputs/hexagonos_brasil_dashboard.parquet
```

### Dependências
```
pyproject.toml intocado (git diff vazio). playwright>=1.44.0 em [scraping] (linha 99).
deck.gl NÃO consta em pyproject (por CDN). pydeck>=0.8.1 é dep base pré-existente, não deste bloco.
```

### Ressalva do Builder conferida (`parents[3]`)
```
parents[3] = <repo root> (tem CLAUDE.md: True)
parents[4] = <repo root>/.. (tem CLAUDE.md: False)
⇒ parents[3] correto. ui_proto.py NÃO foi tocado (não consta no git status).
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock do caminho crítico. A suíte FULL rodou de verdade (871s). O recorte foi gerado contra a partição REAL de produção `data/outputs/hexagonos_dashboard_enriquecido/uf=SP` (não fixture). Os testes unitários usam DataFrame sintético para isolar lógica — prática legítima e explicitamente fora do escopo da regra de no-bypass (que mira tooling contra config/artefatos de produção). O SERIAL foi usado por instabilidade documentada do xdist/execnet no Python 3.14 local (CLAUDE.md §5), não para mascarar flakiness de teste mal-isolado — os 25 testes do spike e o subconjunto impactado passam serial de forma limpa.

## Conferência cruzada com log do Builder
Bate. Builder: 25 passed no spike; 316 no subconjunto; ruff/mypy limpos; imports ok; pages.py não importa; SP=18000 sem PII/geo; guarda exit 2; mtime dos 4 inalterado. Tudo reproduzido por mim com saída idêntica. Divergência apenas numérica esperada na suíte FULL: Builder citou baseline ~1817; eu obtive 1843 passed (deriva natural de baseline por acúmulo de testes) + o 1 pré-existente falho que o Builder não rodou (gate era do QA).

## Guardrails verificados
- score_priorizacao não alterado: sim (só LIDO do parquet derivado; nunca escrito).
- Artefatos M1 preservados: sim (mtime dos 4 oficiais inalterado; nada em `pipelines/m1/`/`config.py`).
- Parâmetros canônicos preservados: sim (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, pesos renda=0.40/pop=0.60 — nenhum tocado; bloco não mexe em score/config).
- Validações re-executadas pelo QA (sem bypass): suíte FULL → 1 pré-existente falho + 1843 passed/2 skipped; spike tests → 25 passed; ruff → limpo; mypy → limpo; imports → ok; grep pages → não importa; recorte SP real → 18000 sem PII/geo; HTML → deck.gl/H3HexagonLayer/marcadores; harness → --help ok + abort exit 2; runbook → 3 sub-entregáveis + §6.
- §6 (VPS): respeitado — nenhum comando na VPS executado; só script + runbook com guarda e avisos.
- Escopo respeitado: sim (4 arquivos novos + bookkeeping gitignored; `pages.py`/`config.py`/`pipelines/m1/`/`pyproject.toml`/CI/Dockerfiles intocados; deck.gl por CDN).

## Decisão recomendada
Fechar ciclo. Bloco APROVADO. Próximo passo real (fora da esteira de código): medição humana empírica seguindo `docs/rev08_spike_perf_runbook.md` para embasar o BLK-REV-12.
