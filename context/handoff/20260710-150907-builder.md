# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-PERF-01b — Cache dos builders de mapa (@st.cache_data) + @st.fragment no painel multi-hex
+ seletor de modo de cor movido para dentro do fragment do mapa.

## O que foi feito (por passo do plano de 13 passos)

### A) components.py — cache (passos 1-2)
- **Passo 1:** `import hashlib`; constante `MAP_FIGURE_CACHE_MAX_ENTRIES = 24` (bound de memória,
  NÃO TTL); helper puro `_map_frame_token(df, cols) -> str` (None→`"none"`, vazio→`"n0"`,
  senão `f"n{len}:{sha1(hash_pandas_object(cols presentes))[:16]}"`; determinístico, cols-presentes-only).
- **Passo 2:** `build_unified_map_figure_cached(...)` decorado com
  `@st.cache_data(show_spinner=False, max_entries=MAP_FIGURE_CACHE_MAX_ENTRIES)` — **SEM `ttl=`** (D2).
  DataFrames entram como args underscore (`_df`/`_competitors_df`/`_ultra_df`/`_dominio_df`,
  ignorados no hash); identidade do recorte vem de `df_token`/`competitors_token`/`ultra_token`/
  `dominio_token` + `color_mode` + `enabled_overlays` (tupla ordenada) + ufs/cities/faixas +
  `search_pin`/`search_hex_id`. `multihex_cenario` FORA da chave. O corpo chama
  `build_unified_map_figure(...)` pelo nome de módulo (monkeypatch-friendly). Os 4 builders puros
  e o dispatcher permanecem INALTERADOS.

### B) pages.py — fragment do mapa (passos 3-4)
- **Passo 3:** novo `@st.fragment _render_mapa_fragment(df, *, competitors_df, ultra_df, dominio_df,
  selected_ufs, selected_cities, selected_faixas, search_pin, search_hex_id)` — absorve o seletor de
  modo de cor (`key="mapa_territorial_color_mode"`), o multiselect de overlays
  (`key="mapa_territorial_overlays"`), os guards de modo indisponível/domínio (agora `st.warning`/
  `st.info` + `return` DO FRAGMENT), o filtro de redes + `competitors_df_filtered`, o expander de
  Legenda, o build do deck via **`build_unified_map_figure_cached`** (tokens via `_map_frame_token`,
  listas→tuplas, `enabled_overlays`→`tuple(sorted(...))`), o guard `deck is None`, o append da layer
  de destaque multi-hex (pós-cache; cópia por chamada do cache_data), a chamada a
  `render_mapa_pydeck_fragment` (INALTERADA) e os captions de escopo/pins/cluster.
- **Passo 4:** `render_mapa_territorial` mantém init do estado multi-hex + headers, chama
  `_render_mapa_fragment(...)` uma vez, e segue com click_coord/active_hex_id/painel multi-hex/
  Detalhamento. `competitors_df_filtered` deixou de existir no escopo do parent.

### C) pages.py — fragment do painel multi-hex + botão D1 (passo 5)
- Novo `@st.fragment _render_multihex_panel(df, active_hex_id, multihex_ids)`: chama
  `_render_multihex_controls` (INALTERADO), relê `multihex_cenario` e renderiza
  `_render_multihex_kpis` (feedback imediato), e o **botão D1 "Atualizar mapa"**
  (`key="btn_multihex_refresh_map"` → `st.rerun(scope="app")`) + `st.caption` acentuado explicando
  que os KPIs já refletem o cenário e o destaque no mapa atualiza ao clicar/trocar de aba/buscar/
  clicar no mapa. A chamada `_render_multihex_controls(...)` no parent foi substituída por
  `_render_multihex_panel(df, active_hex_id, multihex_ids)`.

### D) Testes (passos 6-11) — tests/integration/test_streamlit_app.py
- **Passo 6 (invalidação):** `test_map_cache_invalidacao_por_parametro` — MISS na 1ª chamada, HIT ao
  repetir, MISS por troca de UF+df_token / cidade / modo (novo modo) / overlays / busca / rede;
  HIT ao revisitar modo já visto.
- **Passo 7 (HIT multihex + cópia segura):** `test_map_cache_multihex_fora_da_chave_e_copia_segura`
  — multihex fora da chave → HIT; anexar layer ao deck retornado NÃO vaza para o 2º (cópia por chamada).
- **Passo 8 (anti-vazamento A→B→A):** `test_map_cache_anti_vazamento_A_B_A` — hex_id do deck pertencem
  exclusivamente ao recorte corrente; 3ª chamada (A) é HIT.
- **Passo 9 (D1):** `test_render_multihex_panel_botao_atualizar_mapa_chama_rerun_scope_app`
  (assert `st.rerun` chamado com `scope="app"`) e
  `test_render_multihex_panel_sem_clique_nao_chama_rerun_renderiza_kpis` (sem clique → sem rerun,
  KPIs via `st.metric`).
- **Passo 10 (D2):** `test_map_cache_wrapper_decorator_sem_ttl` — lê o fonte, valida que o decorator
  contém `st.cache_data` e NÃO contém `ttl=`.
- **Passo 11 (picklability):** `test_deck_picklavel_preserva_atributos_cap` — pickle roundtrip
  reproduz `deck.to_json()` e preserva `_ultra_capped`/`_ultra_effective_cap`.
- Extra: `test_map_frame_token_none_vazio_e_sensivel_ao_conteudo`,
  `test_map_fragment_e_painel_multihex_sao_fragments`.
- **Ajuste de 4 testes existentes** (`test_render_mapa_territorial_modo_m1/indisponivel/dominio/
  sem_dados`): o build/render do mapa migrou para `_render_mapa_fragment` (um `@st.fragment`, cujo
  corpo NÃO roda em bare mode), então passaram a exercitar `_render_mapa_fragment.__wrapped__` via o
  helper `_call_mapa_fragment` — mesmo padrão já usado em `tests/unit/test_mapa_fragment.py`. A
  intenção original (m1 renderiza / hibrido indisponível avisa / domínio sem dados informa / sem
  dados informa) foi preservada.

## Medição B3/B4 (antes/depois) + MISS/HIT do cache

### B3/B4 — builders PUROS (via `python scripts/perf_baseline_app.py`, este run = "depois")
Os builders puros NÃO foram tocados neste bloco, logo **antes ≡ depois** (confirmação de
não-regressão; o custo de MISS é o mesmo custo do builder):
- **B3 (build_map_figure):** RO frio 1,739s / SC frio 2,119s / **AM frio 2,325s** (cap 18.000).
- **B4 (troca de modo, build_hybrid_map_figure):** AM ~1,86–1,94s por modo
  (score_expansao_hibrido 1,935s / setor_2022 1,929s / residual 1,917s / priorizacao 1,861s).

### MISS vs HIT do wrapper cacheado (snippet efêmero, NÃO commitado; df sintético Manaus ~18,5k hexes)
- **MISS (1ª build):** 1,972s
- **HIT (2ª chamada, cache):** 0,137s   |   **HIT (3ª):** 0,140s
- **Speedup MISS/HIT ≈ 14×.** O custo residual do HIT (~0,14s) é a **cópia-por-chamada** do
  `cache_data` (unpickle do deck de ~18k linhas de tooltip) — tradeoff deliberado (Planner) que
  garante o isolamento de mutação do `deck.layers.append` do destaque multi-hex; NÃO é rebuild.
  Interpretação operacional: troca de cor / add-remove hex que caem em HIT deixam de pagar o rebuild
  de ~2s (passam a ~0,14s de cópia). Add/remove hex não reconstrói o builder (multihex fora da chave).

## Arquivos alterados
- `src/motor_expansao/dashboard/components.py` — `import hashlib`; `MAP_FIGURE_CACHE_MAX_ENTRIES`;
  `_map_frame_token`; `build_unified_map_figure_cached` (@st.cache_data sem ttl). Builders e
  dispatcher INALTERADOS.
- `src/motor_expansao/dashboard/pages.py` — imports de `_map_frame_token` e
  `build_unified_map_figure_cached`; novos `@st.fragment` `_render_mapa_fragment` e
  `_render_multihex_panel` (botão D1); refactor de `render_mapa_territorial` (delega o mapa ao
  fragment e o painel multi-hex ao novo fragment); `build_unified_map_figure` marcado `# noqa: F401`
  (re-export para testes). `render_mapa_pydeck_fragment`/`_render_multihex_controls`/
  `_render_multihex_kpis`/`_render_analise_pontual_multihex` INALTERADOS.
- `tests/integration/test_streamlit_app.py` — 9 testes novos (passos 6-11 + extras) e 4 testes
  existentes ajustados para o corpo do fragment via `__wrapped__`.

## Validações executadas
- **pytest (subset impactado):** `tests/integration/test_streamlit_app.py` +
  `tests/unit/test_mapa_fragment.py` com `-n auto` → **247 passed, 0 failed, 0 skipped**.
  (Rodei este subset por cobrir 100% dos arquivos tocados: os builders/dispatcher/cache em
  components, os fragments/refactor em pages, e os fragments legados do mapa.)
- **pytest (novos isolados):** `-k "map_cache or map_frame_token or multihex_panel or
  deck_picklavel or map_fragment_e_painel"` → **9 passed**.
- **import:** `python -c "import streamlit_app"` → ok.
- **ruff:** `ruff check components.py pages.py test_streamlit_app.py` → All checks passed.
- **mypy:** `mypy components.py pages.py` → Success: no issues found in 2 source files.
- **loop_guard:** `python scripts/loop_guard.py --base HEAD` → GUARD OK (nenhum path proibido).
- **git diff (guardrail):** vazio em `src/motor_expansao/pipelines/m1/`, `src/motor_expansao/config.py`
  e `src/motor_expansao/dashboard/constants.py`. Diff restrito aos paths do ciclo
  (components.py, pages.py, tests/).
- **grep `ttl=`:** nenhuma ocorrência em `components.py` (nem no decorator nem em comentário).
- **Suíte FULL:** NÃO rodada aqui (é o gate único do QA, conforme prompts/builder.md).

## Problemas encontrados
- Mover o build/render do mapa para um `@st.fragment` fez 4 testes existentes falharem porque o
  corpo de um `@st.fragment` NÃO executa em bare mode (sem ScriptRunContext) — mesmo motivo pelo qual
  `tests/unit/test_mapa_fragment.py` já testa `render_mapa_pydeck_fragment` via `__wrapped__`.
  Resolvido ajustando os 4 testes para exercitar `_render_mapa_fragment.__wrapped__`. Nenhum impacto
  em produção (há runtime real).

## Pendências
- **Validação VISUAL humana PÓS-QA (Felipe)** — não bloqueante do gate do QA: cache + fragment do
  mapa + fragment do painel + destaque laranja + botão "Atualizar mapa" no app real.

## Riscos remanescentes
- **Interação de reruns entre fragments aninhados/irmãos** validada só headless (corpos via
  `__wrapped__`); comportamento visual (clique no mapa, troca de modo, add/remove hex, "Atualizar
  mapa") depende da validação visual pós-QA.
- **HIT ~0,14s (não 0)** por cópia-por-chamada do `cache_data` — inerente e desejado (isolamento de
  mutação); ganho real ~14× sobre o rebuild.
- **Memória sem TTL:** `max_entries=24` limita o teto (~360 MB); tunável se a medição de produção pedir.

## Guardrails verificados
- score_priorizacao não alterado: sim (bloco 100% display/cache/interação; builders/dispatcher e
  fórmula intocados).
- Artefatos M1 preservados: sim (diff vazio em pipelines/m1 e config.py; loop_guard OK).
- Dashboard offline mantido: sim (nenhuma dependência de rede introduzida; cache é in-process).
- `MAP_POINT_LIMIT*` e `_downsample_map_index`: INTOCADOS (constants.py sem diff).
- Tooltip fields (01c): INTOCADOS.
- Acentuação: strings novas de UI (caption do botão "Atualizar mapa") acentuadas; identificadores/keys
  (`btn_multihex_refresh_map`, `mapa_territorial_color_mode`) sem acento.
