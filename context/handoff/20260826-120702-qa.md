# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (merge humano — bloco de criticidade **Alta**, exige label `aprovado-humano`)

## VEREDITO
**APROVADO COM RESSALVAS**

## Justificativa
As **quatro decisões aprovadas por Felipe foram implementadas exatamente como decididas** — verifiquei
cada uma no código, reproduzi o piso `13` por conta própria e provei a régua `min` nos onze limites,
com a suíte completa verde (**3768 passed, 34 skipped, 0 failed**) e a partição viva byte-idêntica.
As ressalvas são cinco defeitos **médios** e nenhum crítico: o mais relevante é que o **índice
canônico do `CLAUDE.md` (§8) ainda descreve a DEC-039 pela premissa MENSAL morta**, e a trava nova
contra a inversão de sentido **não pega** a regressão do runbook (provado por mutação).

## Problemas críticos (bloqueadores)
- **nenhum.**

## Problemas médios (não bloqueadores)

1. **`CLAUDE.md:209` — o índice canônico §8 ainda descreve a DEC-039 pela premissa MENSAL.** É o
   arquivo que o próprio `CLAUDE.md` manda ler antes de qualquer tarefa, e a linha afirma hoje
   **seis** coisas falsas: `**Cron MENSAL dos agregadores**`; `**(D5)** RETENCAO_SEMANAS 26 -> **78**`;
   `**(D1)** 1a terca do mes, 02:00 UTC`; `RECUSA feed velho (>7 dias)`; `desde o 1o mes (o cronometro
   de 8 meses)`; e `26 dava **5,98** observacoes a um feed mensal`. O corpo da DEC está **correto**
   (`grep -c "Cron MENSAL" docs/decisions/DEC-039.md` → `0`), e o marcador `APROVADA (emendada)` da
   linha se refere à **Emenda 1**, não à Emenda 2. **Não é do Builder** — entrou no commit-base do
   ciclo (`69874b7`); o commit do Builder (`2f1e415`) não tocou `CLAUDE.md`.
   **Conserto exato:** reescrever a linha 209 para "Cron **SEMANAL** dos agregadores", `D5` `78` ->
   `26` (piso 13), `D1` "toda terça, 02:00 UTC", `D4` ">3 dias pela régua `min`", `D9` "desde a 1ª
   semana (o cronômetro de 8 semanas)", e marcar `APROVADA (emendada 2x)`.

2. **A trava `test_wrapper_e_runbook_citam_a_mesma_retencao` NÃO pega a regressão do runbook.**
   Testei por **mutação**, como pedido, nas quatro frentes. Ela pega 3 de 4 — e falha exatamente na
   que o próprio docstring promete ("o checklist mandava NÃO agendar se o DRY_RUN desse 26"):

   | mutação | resultado |
   |---|---|
   | `docs/infra_producao.md:515` `26` -> `78` (o risco operacional #1) | **PASSOU (verde)** — não pegou |
   | `run_snapshot_agregadores.sh` `tem de ser 26` -> `78` | falhou (pegou) |
   | `run_snapshot_agregadores.sh` `retencao_semanas=26` -> `78` | falhou (pegou) |
   | `contrato.py` `RETENCAO_SEMANAS = 26` -> `30` | falhou (pegou) |

   **Causa:** o assert do runbook é `f"\`{esperado}\`" in runbook`, uma substring genérica já
   satisfeita por duas ocorrências de **prosa** (`docs/infra_producao.md:436` e `:449`), então a
   linha do checklist pode regredir sozinha em silêncio.
   **Conserto exato:** trocar por
   `assert f"| \`retencao_semanas\` | tem de ser \`{esperado}\`" in runbook` (ancorar na LINHA da
   tabela, não no número solto).

3. **O fallback de `mtime` continua fail-OPEN contra timestamp no futuro** — assimetria com a régua
   primária, que a D4 acabou de blindar. Medido por mim: feed com `data_coleta` 100% no futuro
   (descartada corretamente) **mais** `mtime` no futuro (o cenário realista: relógio torto na origem
   produz os dois juntos) devolve `idade = -3414,5` pela régua `mtime`, e `feed_esta_fresco(limite=3.0)`
   → `(True, -3414.5)`: **publica**. A D4 fechou a porta da frente e deixou a dos fundos aberta.
   **Conserto exato:** em `idade_do_feed`, após `mtime = _mtime_mais_novo(...)`, recusar o futuro —
   `if mtime > agora.timestamp(): return None, REGUA_INDISPONIVEL` (ou clampar a idade em `0.0`),
   com teste no molde de `test_feed_inteiro_no_futuro_cai_no_fallback_de_mtime`.

4. **A mensagem de recusa não diz ao operador QUAL ARQUIVO nem QUAL DATA.** Isto era o item 1 da
   auditoria e agora pesa mais, porque a troca `max` -> `min` mudou a direção de falha: **uma linha
   corrompida entre 27 CSVs recusa o feed inteiro**. Medido: com 28 arquivos, 27 frescos e uma linha
   `1970-01-01` no `uf13.csv`, a recusa sai como
   `feed velho: idade 20691.0 dia(s) pela regua \`data_coleta_min\`, limite 3.0` + `diretorio_origem`.
   O diretório e a régua estão lá (e a data é derivável por `hoje - idade`), mas o operador tem de
   varrer 27 arquivos à mão para achar a linha. **Conserto exato:** `_data_coleta_mais_antiga`
   devolver `(date, Path)` em vez de `date`, e a string de recusa citar o arquivo e a data literal.

5. **`tasks/backlog.md:1793` — resíduo da cadência morta, apresentado como correção vigente.** O
   texto diz `então ela é **certa, todo mês**` sob o rótulo "**Correção de uma afirmação deste
   bloco:**", e **contradiz o próprio arquivo** em `tasks/backlog.md:1943`
   (`é CERTA, toda semana, para sempre`). Sob cadência semanal a colisão piorou (três fontes na mesma
   partição), não ficou igual. **Conserto exato:** `todo mês` -> `toda semana`.

## Melhorias opcionais
- `docs/infra_producao.md:317-318` mede a maturação em `~2 meses` enquanto `churn_staleness.py:29` e
  `docs/vulnerabilidade_ma_contrato.md:354` medem a **mesma** grandeza em `~8 semanas`. Não é falso
  (e o `~8 meses` ali está explicitamente datado), mas é a mistura de unidades que a Emenda 2 aponta
  como fonte de confusão. Padronizar em semanas.
- `context/handoff/20260826-113105-builder.md` introduziu **2** sequências de mojibake real (assinatura
  `C3 83 C2`). É snapshot de sessão, não texto de usuário — cosmético.

## Testes faltantes
- Teste travando o **fallback de `mtime` contra timestamp no futuro** (problema médio 3). Hoje não
  existe: `test_feed_inteiro_no_futuro_cai_no_fallback_de_mtime` prova que a régua primária descarta
  o futuro, mas **não** que o fallback o recusa.
- Teste ancorando a **linha do checklist** do runbook (problema médio 2). A trava atual passa verde
  com a linha regredida.

## Riscos remanescentes
- **A régua `min` é fail-closed por decisão** — correto, e o custo hoje é **zero**: remedi o spread
  real por conta própria nos três diretórios de `../GymScraping` e ele é **0 dia** em 83.685 linhas
  (`data_coleta` é carimbada uma vez, antes do `ThreadPoolExecutor`). **A premissa de "~22h de
  spread" da auditoria não se sustenta no dado**: a régua tem granularidade de DIA e o feed carrega
  uma data única por coleta. `3` dias comporta o spread real com folga de 3 dias.
- `26 = 2× o piso 13`, não 6×. Se a taxa de falha semanal por fonte na VPS passar de ~50%, as 13
  observações não chegam. Não é medível daqui.
- Baixar a retenção é destrutivo e irreversível na VPS, e o `--dry-run` **não** mostra o que a poda
  apagaria. Hoje é inócuo (Felipe: BLK-MA-06 aplicado há 1-2 dias, nenhuma partição acumulada) —
  confirmei que **nada no código depende de partições acumuladas**: com 13 semanas em disco,
  `podar_snapshots(retencao_semanas=26)` remove `0`.
- A imagem em produção não é a `v4`; a ordem do runbook (imagem primeiro) segue sendo a defesa.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte COMPLETA, SERIAL — gate único do ciclo
```
$ python -m pytest -q
3768 passed, 34 skipped, 856551 warnings in 1579.18s (0:26:19)
EXIT=0 2026-08-26T12:03:42-03:00
```
**Decomposição da diferença** (pedida pelo orquestrador). Coletados agora = `3768 + 34` = **3802**;
o ciclo mensal fechou em `3704 + 34` = **3738**. Delta **+64**:
- **+51 líquidos do PR #264** (`8e24f08`, Relatório Municipal por bairro): `+52 def test_` / `-1`,
  em `test_api_coord.py`, `test_api_geo.py`, `test_api_municipio.py`, `test_relatorio_municipal.py`,
  `test_relatorio_municipal_motor.py`;
- **+10 líquidos deste ciclo** (`2f1e415`): `+12 def test_` / `-2` (os dois removidos são os
  RENOMEADOS `..._a_cadencia_mensal_por_ele` e `test_retencao_serve_as_duas_cadencias`);
- **+3 residuais** de expansão de `parametrize` (um `def test_` rende mais de um caso coletado);
- **+0 do PR #261** (`d5d3464`, tema claro): os 3 testes que ele traz estão em
  `web/src/lib/tema.test.ts`, que é **vitest**, não pytest.

`-n auto` não foi usado (quebra nesta estação, por infraestrutura) e **`-p no:xdist` não foi usado**
para mascarar nada — a suíte rodou serial, íntegra, do começo ao fim.

### 2. `python -m ruff check .`
```
All checks passed!
```

### 3. `bash -n` nos 3 shells
```
OK  scripts/cron/run_snapshot_agregadores.sh
OK  scripts/cron/run_snapshot_concorrentes.sh
OK  scripts/healthcheck_vps.sh
```

### 4. CRLF no diff inteiro (DEC-017)
Medi no **conteúdo versionado** (o `index`), que é o que a DEC-017 rege — não no working tree, onde
o `core.autocrlf` do Windows converte na saída:
```
$ git diff --name-only origin/main...HEAD | xargs git ls-files --eol | grep -c "i/crlf"
0
```
**CRLF = 0 nos 33 arquivos do diff**, `tasks/` e `docs/` inclusive. (No working tree, 8 arquivos
aparecem como `w/crlf` — todos com `i/lf`; é artefato de checkout desta estação, não do repositório.)

### 5. `python scripts/loop_guard.py --base origin/main --json`
```json
{"limpo": false, "violacoes": [
 {"path": "CLAUDE.md", "classe": "governanca", "motivo": "fonte canonica de regras e DECs"},
 {"path": "src/motor_expansao/vulnerabilidade/alvos_ma.py", "classe": "governanca", "motivo": "camadas paralelas com insumo de PII na origem (DEC-012)"},
 {"path": "src/motor_expansao/vulnerabilidade/churn_staleness.py", "classe": "governanca", "motivo": "..."},
 {"path": "src/motor_expansao/vulnerabilidade/contrato.py", "classe": "governanca", "motivo": "..."},
 {"path": "src/motor_expansao/vulnerabilidade/curadoria_agregadores.py", "classe": "governanca", "motivo": "..."},
 {"path": "src/motor_expansao/vulnerabilidade/presenca_agregador.py", "classe": "governanca", "motivo": "..."},
 {"path": "src/motor_expansao/vulnerabilidade/score.py", "classe": "governanca", "motivo": "..."},
 {"path": "src/motor_expansao/vulnerabilidade/snapshots.py", "classe": "governanca", "motivo": "..."},
 {"path": "tasks/backlog.md", "classe": "governanca", "motivo": "backlog - marcador de autonomia/criticidade dos blocos"}]}
```
**Nenhum CRÍTICO.** Só `governanca`, exatamente o esperado para este bloco (regime Alta + label
`aprovado-humano`).

### 6. Smokes `--dry-run`, com SHA-256 da árvore de staging antes e depois

Hash da árvore `data/staging/snapshots_concorrentes/` **ANTES** de toda a bateria:
```
semana=2026-33/parte-0.parquet  3454901 bytes
ITENS: 1
TREE_SHA256: 878d287f1fb7d53fdc2c969aa2cbcd04ecd5e7dd54aca95994e72042a0729fe8
```

`snapshots --fontes wellhub --dry-run`:
```
INFO:__main__:dry-run: nada gravado, nenhuma semana podada
{'semana': '2026-35', 'fontes_lidas': 'wellhub', 'linhas_lidas': 22173, 'linhas_mantidas': 22173,
 'linhas_snapshot': 22173, 'chaves_colapsadas': 0, 'rating_ilegivel': 0,
 'retencao_semanas': 26, 'versao_contrato': 'snapshots_concorrentes_v4',
 'dry_run': True, 'semanas_removidas': 0}
```

`snapshots --migrar-layout --dry-run`:
```
{'migrar_layout': ['2026-33'], 'ambiguas': [], 'dry_run': True}
```

`curadoria_agregadores --dry-run` contra o **clone REAL** `../GymScraping` (caminho de produção):
```
ValueError: WellHub em estado ambiguo: `csvs_musculacao/` existe mas e' MAIS ANTIGO que `csvs/` ...
..\GymScraping\Wellhub\csvs_musculacao (mais novo: 2026-08-07T19:06:09+00:00) x
..\GymScraping\Wellhub\csvs (mais novo: 2026-08-18T18:29:19+00:00).
Confira de que modo o coletor rodou e apague o diretorio obsoleto a mao.
```
Este **é** o comportamento correto (abort de ambiguidade D3, já documentado no runbook como ação
humana pendente), e a mensagem é acionável: nomeia os dois diretórios e os dois mtimes.

`curadoria_agregadores --dry-run` sobre clone montado com os **bytes reais** dos feeds (só a
ambiguidade D3 resolvida, por ausência de `Wellhub/csvs`):
```
INFO: idade do feed em ...\clone\TotalPass\csvs: 86.00 dia(s) pela regua `data_coleta_min` (limite 3.0)
INFO: idade do feed em ...\clone\Wellhub\csvs_musculacao: 21.00 dia(s) pela regua `data_coleta_min` (limite 3.0)
WARNING: agregador totalpass NAO publicado: feed velho: idade 86.0 dia(s) pela regua `data_coleta_min`, limite 3.0.
WARNING: agregador wellhub  NAO publicado: feed velho: idade 21.0 dia(s) pela regua `data_coleta_min`, limite 3.0.
fontes_publicadas=
```

Hash da árvore **DEPOIS** de toda a bateria (suíte completa + smokes + mutações):
```
TREE_SHA256: 878d287f1fb7d53fdc2c969aa2cbcd04ecd5e7dd54aca95994e72042a0729fe8
```
**IDÊNTICO.** A partição viva `semana=2026-33/` (22.173 linhas, `v3`, insumo de produção) segue
byte-a-byte intacta e legível.

### 7. Auditoria dura #1 — a régua `min` nos limites (bateria própria)
```
1 spread 3 arquivos (min=08-19 -> 7d)          -> (7.0, 'data_coleta_min')
2 futuro descartado (resta 08-24 -> 2d)        -> (2.0, 'data_coleta_min')
3 feed inteiro no futuro -> fallback           -> (-0.110, 'mtime')
4 celula vazia -> fallback                     -> (-0.110, 'mtime')
5 coluna ausente -> fallback                   -> (-0.110, 'mtime')
6 so cabecalho -> fallback                     -> (-0.110, 'mtime')
7 arquivo 0 byte -> fallback                   -> (-0.110, 'mtime')
8 BR ignorado + ISO-datetime + 08-23           -> (3.0, 'data_coleta_min')
9 1 linha corrompida em 28 arquivos            -> (20691.0, 'data_coleta_min')
10 sem CSV nenhum                              -> (None, 'indisponivel')
11 ';' dentro de aspas (desalinho)             -> (4.0, 'data_coleta_min')

feed_esta_fresco, default MAX_IDADE_DIAS_DEFAULT = 3.0:
  c1: (False, 7.0)   c2: (True, 2.0)   c9: (False, 20691.0)
REGUA_DATA_COLETA = 'data_coleta_min'
```
Formato divergente (`25/08/2026`) e ISO-datetime com fuso são ignorados em silêncio e a régua cai no
`mtime` **dizendo que caiu** (`regua_idade` no relatório) — tolerância documentada, aceitável.

**Spread real medido em `../GymScraping` (READ-ONLY, só leitura):**
```
TotalPass/csvs        (27 CSVs, 15986 linhas)  data_coleta=2026-06-01  SPREAD_DIAS=0
Wellhub/csvs          (27 CSVs, 45526 linhas)  data_coleta=2026-08-05  SPREAD_DIAS=0
Wellhub/csvs_musculacao (27 CSVs, 22173 linhas) data_coleta=2026-08-05 SPREAD_DIAS=0
```

### 8. Auditoria dura #2 — piso `13` reproduzido por conta própria
```
RETENCAO_SEMANAS=26 MIN_SEMANAS=8 STALE_SEMANAS=12
N=  8  sem_mudanca=  7  imatura=False stale_interp=False v4=0.5833
N= 11  sem_mudanca= 10  imatura=False stale_interp=False v4=0.8333
N= 12  sem_mudanca= 11  imatura=False stale_interp=True  v4=0.9167
N= 13  sem_mudanca= 12  imatura=False stale_interp=True  v4=1.0000
N= 26  sem_mudanca= 25  imatura=False stale_interp=True  v4=1.0000
26 semanas com 50% de falha -> obs=13 sem_mudanca=12 v4=1.0000  (exatamente no piso)
```
**Confirma o plano:** `N=12 -> 0,9167` e `N=13 -> 1,0000`. Piso **13**, e `26 = 2× o piso`.

**`podar_snapshots` com layout de DUAS chaves** (árvore em TEMP, fora do repo, 20 semanas × 3 fontes):
```
ANTES: 20 semanas, 60 arquivos
  folhas na semana 01: ['fonte=totalpass', 'fonte=unidades', 'fonte=wellhub']
podar_snapshots(retencao_semanas=13) -> removeu 7: ['2026-01'...'2026-07']
DEPOIS: 13 semanas, 39 arquivos    semana=2026-01 ainda existe? False
podar_snapshots(retencao_semanas=26) sobre 13 semanas -> removeu 0
```
**Apaga a semana INTEIRA, com as três folhas `fonte=` juntas** — é o pretendido, e é exatamente a
assimetria que justifica o `BLK-MA-21-FU4`.

### 9. Auditoria dura #5 — trava de AST contra arquivo-isca FORA do repo
```
diretorio TEMP (FORA do repo): C:\Users\...\Temp\qa_isca_c3lg9nw4
COLUNAS_PARTICAO = ('semana', 'fonte')
chamadas vistas: 4
violacoes (3):
    escritor_ruim.py:3 (`write_dataset`)
    leitor_ruim.py:3 (`dataset`)
    sem_kwarg.py:3 (`dataset`)
CONTROLE 'bom.py' (duas chaves) marcado por engano? nao
RESULTADO: TRAVA FUNCIONA
```

### 10. Guardrails do M1
```
$ git diff --name-only origin/main...HEAD | grep -Ei "config\.py|pipelines/m1|scoring|normalizar_concorrentes|calcular_colunas_mercado"
  NENHUM (M1 intocado)

$ python -m pytest tests/contracts/test_parametros_canonicos.py -q
4 passed in 0.87s
```

## Conferência de no-bypass
Confirmo que **nenhuma** validação usou `--config /dev/null`, mock do caminho crítico, ou fixture que
contorne a config real. Três pontos merecem registro explícito:
1. A suíte completa rodou **serial e íntegra**, sem `-p no:xdist` e sem deselecionar nada.
2. O tooling de curadoria foi exercitado **contra o caminho REAL de produção** (`../GymScraping`), e o
   resultado — o abort de ambiguidade D3 — é o comportamento correto e documentado, não um contorno.
   A execução ponta a ponta da régua nova usou um clone montado com os **bytes reais dos feeds**
   (`cp` dos 54 CSVs), com **apenas** a ambiguidade D3 resolvida por ausência de um diretório; não é
   fixture sintética.
3. As mutações de trava foram aplicadas e **revertidas com `git checkout --`**; conferi que a árvore
   voltou ao estado original (`git status --porcelain` só lista `graphify-out/*`, pré-existente).

## Conferência cruzada com log do Builder
**Bate**, com dois ajustes de medição e uma atribuição:
- `retencao_semanas=26`, `versao_contrato=snapshots_concorrentes_v4`, `22.173` linhas,
  `{'migrar_layout': ['2026-33'], 'ambiguas': []}`, TotalPass `86 d` / WellHub `21 d` recusados,
  `fontes_publicadas=` vazio, `ruff check` limpo, `bash -n` OK, `loop_guard` sem CRÍTICO: **idênticos**.
- **Hash da árvore de staging: diverge no VALOR, não no fato.** O Builder reporta
  `08da079f...` com "2 itens"; eu meço `878d287f...` com **1 item** (`semana=2026-33/parte-0.parquet`,
  3.454.901 bytes). São métodos de hash diferentes (ele parece contar o diretório); o que importa —
  **ANTES == DEPOIS** — vale nos dois.
- **Aviso 1 do Builder (mojibake) CONFIRMADO, e melhor do que ele diz.** Medi pela assinatura de
  bytes `C3 83 C2` (a `C3 83` sozinha pega o "Ã" legítimo de NÃO/SÃO e dá 5.936 falsos positivos no
  repo): `tests/unit/vulnerabilidade/test_snapshots.py` tem **295 em `origin/main` e 290 em `HEAD`** —
  pré-existente e **diminuiu** 5. Não afeta string lida por usuário (são docstrings e comentários de
  teste). Único mojibake **novo** do ciclo: 2 ocorrências no snapshot de handoff do próprio Builder.
- **Aviso 2 do Builder (reversão do `ruff format`) — decisão CORRETA.** Verifiquei que os três
  arquivos **já estavam fora do `ruff format` em `origin/main`**, que o repo inteiro tem **269
  arquivos** nessa condição, e que o CI roda **só** `ruff check . --no-cache`
  (`.github/workflows/ci.yml:54`). Aplicar `format` teria criado churn fora de escopo contra uma
  convenção que o repositório não adota. Nenhuma inconsistência introduzida.
- **Aviso 3 (FU3/FU4) CONFIRMADO.** Os dois blocos estão bem escritos, com a medição embutida,
  `Depende de`, e — o que mais importa — `| **Autonomia** | **manual (NÃO loop-safe)** |` nos dois.
  **Não comeram bloco vizinho**: o bloco anterior termina íntegro no seu "Critério de aceite" e
  `BLK-MA-07` segue logo abaixo com a tabela completa. Confirmei também que o seletor do loop
  **não** os enxerga: `python scripts/garimpeiro_select_block.py` → `BLK-EXEC-COORD-01`.
- **Aviso 4 (FU2) CONFIRMADO.** O `BLK-MA-21-FU2` não existe nesta branch, e o defeito que ele
  descrevia está travado por `test_data_coleta_no_futuro_nao_zera_a_idade` e
  `test_feed_inteiro_no_futuro_cai_no_fallback_de_mtime` — rodei os 8 testes da família: `8 passed`.
  **Ressalva:** a trava cobre a régua **primária**; o fallback de `mtime` segue fail-open (médio 3).
- **Escopo:** o commit do Builder (`2f1e415`) tocou **21 arquivos** = os 19 declarados + os 2
  snapshots de handoff. Os demais arquivos que aparecem no diff contra `origin/main`
  (`CLAUDE.md`, `docs/decisions/README.md`, `scripts/check_artifacts.py`, `test_contrato.py`,
  `test_presenca_agregador.py`, `test_score.py`) vieram do commit-base do ciclo `69874b7`. **Escopo do
  Builder respeitado.**
- **Varredura de "mensal" independente:** confirmei `src/motor_expansao/vulnerabilidade/` com **zero**
  ocorrências de `mensal|mês|meses` e zero de `78`/`4,345`/`310 MB`/`5,98`. Todas as strings lidas por
  operador/consumidor estão corretas — incluindo `alvos_ma.py:583` ("primeira semana"),
  a mensagem de FAIL do healthcheck ("O cron **semanal** dos agregadores foi agendado?") e as duas
  strings de log em `/var/log`. **Achei 2 resíduos que o Builder não pegou**: `CLAUDE.md:209`
  (médio 1) e `tasks/backlog.md:1793` (médio 5).

## Guardrails verificados
- **score_priorizacao não alterado:** **sim** — nenhum arquivo de `config.py`, `pipelines/m1/`,
  `*scoring*`, `normalizar_concorrentes.py` ou `calcular_colunas_mercado.py` no diff.
- **Artefatos M1 preservados:** **sim** — camada READ-ONLY; `tests/contracts/test_parametros_canonicos.py`
  → `4 passed`; árvore de staging byte-idêntica por SHA-256 antes e depois.
- **Parâmetros canônicos:** preservados (travados pelo contrato acima).
- **Nenhum comando na VPS:** **confirmado** — não executei nada remoto, por nenhuma via (MCP, ssh ou
  scp). Só leitura local.
- **`../GymScraping` READ-ONLY:** **sim** — só `cat`/`stat`/`cp` para fora; nada escrito lá.
- **Acentuação §2:** correta. Prosa de operador acentuada; **nenhum identificador acentuado**. O único
  valor bruto novo é `REGUA_DATA_COLETA = "data_coleta_min"` (ASCII, rótulo de payload). Os dois
  wrappers de cron são **100% ASCII**; `healthcheck_vps.sh` tem acentos apenas em comentários e em
  mensagens de Telegram (texto de operador — correto por §2).
- **EOL (DEC-017):** LF em 100% do conteúdo versionado do diff (`i/crlf` = 0 em 33 arquivos).
- **Escopo respeitado:** **sim** (ver conferência cruzada).
- **Validações re-executadas pelo QA (sem bypass):**
  `pytest -q` serial → **3768 passed / 34 skipped / 0 failed** ·
  `ruff check .` → limpo · `bash -n` ×3 → OK · CRLF no index → 0 ·
  `loop_guard --base origin/main --json` → sem CRÍTICO ·
  3 smokes `--dry-run` + 1 contra o clone real → OK · SHA-256 antes/depois → idêntico ·
  bateria de 11 limites da régua `min` → OK · piso 13 reproduzido → OK ·
  `podar_snapshots` 2 chaves → OK · trava de AST contra isca fora do repo → OK ·
  4 mutações da trava de retenção → 3 pegas, 1 escapou (médio 2) ·
  `test_parametros_canonicos.py` → 4 passed.
- **Substituição declarada (problema LEVE, prompt stale):** `prompts/qa_analyzer.md` e
  `prompts/builder.md` exigem o smoke `import streamlit_app`, módulo **removido pela DEC-022**. Rodei
  no lugar `python -c "import motor_expansao.vulnerabilidade"` → `ok`. **Registrado como problema
  leve, não como validação pulada** — a substituição do Builder é legítima.

## Housekeeping de concluídos
**N/A nesta etapa, por instrução do orquestrador.** O bloco segue **aberto de propósito** no backlog;
`python scripts/housekeeping_move_block.py BLK-MA-21 --check` falha **e isso é esperado**. O
housekeeping é passo do orquestrador, posterior a este veredito. Modo de merge: **MERGE-HUMANO**
(criticidade **Alta** → exige a label `aprovado-humano`; DEC-016).

## Decisão recomendada
**Fechar o ciclo**, com **um conserto obrigatório antes do merge**: o **médio 1** (`CLAUDE.md:209`),
porque é o índice canônico que todo agente lê antes de qualquer tarefa e ele descreve a DEC-039 pela
premissa que este próprio ciclo derrubou. Os médios **2** e **3** (trava fraca do runbook e fallback
de `mtime` fail-open) são pequenos, cirúrgicos e têm o conserto exato descrito — cabem no mesmo PR ou
num `BLK-MA-21-FU5`, a critério do orquestrador. Os médios **4** e **5** podem ir para follow-up.
