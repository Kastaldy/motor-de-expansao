# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador: Passo 6.0 housekeeping move BLK-UI-09 → completed.md, então merge humano)

## VEREDITO
APROVADO

## Justificativa
O Builder implementou exatamente a Opção B autorizada pela emenda à DEC-010 (2026-06-19): cascata
numérico (INTOCADO) → link do Maps (NOVO, URL longa offline / link curto via `resolve_short_link`)
→ endereço (INTOCADO), com validação bbox Brasil e fallback gracioso. Todas as validações
re-executadas por conta própria passam (suíte full 1030 passed / 1 skipped / 0 failed), nenhum teste
bate na rede real (urllib mockado), e o escopo é READ-ONLY sobre o M1 (só `pages.py` + `maps_geocoder.py`).

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- `_e_link_curto_maps` cobre `maps.app.goo.gl` e `goo.gl/maps`; encurtadores alternativos
  (ex.: `g.co/maps`) cairiam no ramo de URL longa offline e, sem coordenada na string, retornariam
  `st.warning` claro — comportamento gracioso, mas poderia ser estendido no futuro se aparecerem.

## Testes faltantes
- nenhum essencial. Cobertura adicionada é sólida: `extract_any_coord` (URL longa `!3d/!4d`,
  `@lat,lng`, fora do Brasil rejeitada por bbox, sem coordenada, link curto offline);
  `_parece_link`/`_e_link_curto_maps` parametrizados; `resolve_short_link` com urllib mockado
  (sucesso, falha de rede → None, URL vazia → None).

## Riscos remanescentes
- `resolve_short_link` faz 1 requisição HTTP ao seguir o redirect de link curto (rede). Coberto pela
  emenda à DEC-010 (Opção B): escopo restrito ao sub-caminho de link curto, import lazy, timeout 6 s,
  `try/except` amplo → None, fallback gracioso, nada persistido além de lat,lng. Suíte NUNCA bate na
  rede real. Dashboard offline e Blocos 4–6/basemap seguem sem rede.
- `-n auto` (xdist) aborta com INTERNALERROR de execnet (`EOFError: couldn't load message header`) no
  Python 3.14 local — limitação de AMBIENTE documentada (BLK-UI-08, BLK-ORQ-01), NÃO defeito. Contorno
  autorizado: suíte full executada SERIAL (`python -m pytest -q`), NÃO é bypass de validação.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte COMPLETA — `python -m pytest -n auto` (gate único) → ABORTOU (ambiente Py3.14/execnet)
```
INTERNALERROR>   File ".../xdist/workermanage.py", line 331, in setup
INTERNALERROR>     self.gateway._rinfo()
INTERNALERROR>   File ".../execnet/gateway.py", line 89, in _rinfo
INTERNALERROR>     ch.waitclose()
INTERNALERROR>   File ".../execnet/gateway_base.py", line 571, in from_io
INTERNALERROR>     raise EOFError("couldn't load message header, " + e.args[0]) from None
INTERNALERROR> EOFError: couldn't load message header, expected 9 bytes, got 0
```
CONTORNO DE AMBIENTE (Python 3.14 + execnet), NÃO bypass de validação — precedente documentado
(BLK-UI-08, BLK-ORQ-01). Refeito SERIAL abaixo.

### 1b. Suíte COMPLETA SERIAL — `python -m pytest -q`
```
1030 passed, 1 skipped, 121649 warnings in 227.31s (0:03:47)
```
0 failed. (warnings = DeprecationWarning de pytest_asyncio/Python 3.14 + FutureWarning/UserWarning
de pandas/geopandas, todos pré-existentes, alheios ao ciclo.)

### 2. Subconjunto impactado — `python -m pytest -q tests/unit/test_coord_search.py tests/integration/test_streamlit_app.py`
```
252 passed, 12144 warnings in 19.54s
```
0 failed. Bate com o log do Builder (252 passed).

### 3. Smoke import — `python -c "import streamlit_app; print('import ok')"`
```
2026-06-19 12:37:03.016 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
2026-06-19 12:37:03.017 WARNING streamlit.runtime.caching.cache_data_api: No runtime found, using MemoryCacheStorageManager
import ok
```

### 4. Ruff — `python -m ruff check src/motor_expansao/dashboard/pages.py src/motor_expansao/api/maps_geocoder.py tests/unit/test_coord_search.py`
```
All checks passed!
```

### 5. Mypy — `python -m mypy src/motor_expansao/dashboard/pages.py src/motor_expansao/api/maps_geocoder.py`
```
Success: no issues found in 2 source files
```

### 6. Housekeeping (estado pré-move esperado) — `python scripts/housekeeping_move_block.py BLK-UI-09 --check`
```
FALHA --check: stub ausente no backlog para BLK-UI-09
```
Exit real = 1 (estado PRÉ-move CORRETO; o move é responsabilidade do ORQUESTRADOR no Passo 6.0, não do QA).

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do
caminho crítico de produção. Especificamente para este bloco (NO-BYPASS de rede):
- Todos os 6 `monkeypatch.setattr(maps_geocoder.urllib.request, "urlopen", ...)` no
  `tests/unit/test_coord_search.py` (linhas 139, 155, 164, 176, 199, 320, 338) mockam o `urlopen` →
  NENHUM teste bate na rede real. `resolve_short_link` testado sempre com urllib mockado;
  `extract_any_coord` é regex pura (sem I/O).
- O contorno serial da suíte full é por limitação de ambiente (Py3.14/execnet), NÃO bypass de
  validação: a suíte INTEIRA rodou (1030 testes), só sem paralelismo.

## Conferência cruzada com log do Builder
Bate. Builder reportou subset 252 passed / import ok / ruff OK / mypy Success / xdist abortou no
Py3.14 → serial; QA reproduziu idêntico (252 passed; full serial 1030 passed/1 skipped/0 failed;
import ok; ruff All checks passed; mypy Success). Sem divergências.

## Guardrails verificados
- score_priorizacao não alterado: sim. `git diff --name-only HEAD -- src/` = apenas
  `api/maps_geocoder.py` e `dashboard/pages.py`. `git diff HEAD -- config.py src/motor_expansao/config.py`
  vazio → parâmetros canônicos INTOCADOS (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0,
  renda=0.40/pop=0.60).
- Artefatos M1 preservados: sim. Nenhuma escrita em `pipelines/m1`, `*scoring*`, carteira, plano,
  parquets oficiais.
- Caminhos preservados byte-a-byte: sim. `parse_coordinate_input` (numérico) intocado; ramo de
  endereço (`resolve_endereco_http` + `_render_endereco_fallback_link`) intocado na lógica — o diff só
  ADICIONA o ramo de link e renumera o comentário `# 2)` → `# 3)` do passo de endereço.
- Ordem da cascata: link (passo 2) tentado ANTES do endereço (passo 3); `_parece_link` True →
  ramo de link com `return`, link nunca cai no Nominatim como endereço literal. Validado por
  inspeção + testes.
- Validação bbox Brasil aplicada ao resultado de link (longo E curto): sim — `_validate_brazil_bbox`
  chamado sobre `extract_any_coord(target_url)` para os dois sub-caminhos.
- DEC-010 emenda 2026-06-19 autoriza exatamente o que o Builder fez: sim — emenda registra Opção B
  (seguir redirect de links curtos via rede), helper `resolve_short_link` urllib puro, import lazy,
  timeout 6 s, fallback gracioso, bbox Brasil. CLAUDE.md editado pelo ORQUESTRADOR (emenda), não pelo
  Builder — esperado.
- Blocos 4–6 (carga lazy por UF, render lazy de abas, mapa enxuto) e basemap: intocados.
- Escopo respeitado: sim — só os 3 arquivos do escopo + handoffs. Os 2 paths pré-sujos
  (`data/outputs/setores_censitarios_2022_geo/_metadata.json`,
  `data/reports/relatorio_pontual_censitario_base_geo.md`) são ruído de working-tree alheio ao ciclo
  (NÃO commitados; documentados em current_task como "paths pré-sujos — NÃO commitar"). Builder não
  fez commits (correto). Único commit no branch vs main = housekeeping BLK-UI-07 (pré-existente).

## Decisão recomendada
Fechar ciclo. Orquestrador executa o housekeeping (Passo 6.0: `housekeeping_move_block.py BLK-UI-09`)
movendo o bloco para `completed.md`, depois merge humano da branch `ciclo/BLK-UI-09`.
