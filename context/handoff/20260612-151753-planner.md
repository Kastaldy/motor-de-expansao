# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Builder (sonnet — Média, sem gate)

## Entendimento da tarefa
Adicionar os seletores reais do DOM do Streamlit 1.58 (`stBaseButton-segmented_controlActive` / `stBaseButton-segmented_control`) com `!important` ao bloco CSS de `inject_styles()` em `pages.py`, e um assert de regressão no teste existente, de modo que a aba ativa fique ciano sólido e o gap apareça no navegador.

---

## Plano técnico

### Passo 1 — Confirmar âncoras de leitura antes de editar

Leia via Read (para confirmar posição exata antes de editar):
- `src/motor_expansao/dashboard/pages.py` linhas 295–355 (bloco CSS do seletor)
- `tests/integration/test_streamlit_app.py` linhas 4466–4483 (teste existente)

As âncoras confirmadas pelo Planner (texto ancora, não número de linha):

| Trecho | Texto âncora |
|---|---|
| GAP | `div[data-testid="stSegmentedControl"],` |
| Botões INATIVOS | `[data-testid="stSegmentedControl"] button,` |
| HOVER | `[data-testid="stSegmentedControl"] button:hover,` |
| ATIVO (defeituoso) | `[data-testid="stSegmentedControl"] button[aria-checked="true"],` |

---

### Passo 2 — `pages.py`: modificar regra GAP (linhas ~304-308)

**Arquivo:** `src/motor_expansao/dashboard/pages.py`

**Localizar** este bloco (texto âncora exato):
```
            div[data-testid="stSegmentedControl"],
            div[data-testid="stSegmentedControl"] > div {{
                display: flex;
                gap: 8px;
            }}
```

**Substituir por** (adiciona `!important` ao gap; mantém `display:flex`):
```
            div[data-testid="stSegmentedControl"],
            div[data-testid="stSegmentedControl"] > div {{
                display: flex;
                gap: 8px !important;
            }}
```

---

### Passo 3 — `pages.py`: adicionar seletor real ao bloco de botões INATIVOS (linhas ~309-319)

**Arquivo:** `src/motor_expansao/dashboard/pages.py`

**Localizar** este bloco (texto âncora exato):
```
            [data-testid="stSegmentedControl"] button,
            div[role="radiogroup"] [data-baseweb="button"],
            [data-baseweb="button-group"] button {{
                background: rgba(30, 38, 65, 0.88);
                color: {COLORS["muted"]};
                border: 1px solid rgba(25, 183, 255, 0.30);
                border-radius: 10px;
                font-size: 1rem;
                font-weight: 600;
                padding: 0.5rem 1.15rem;
            }}
```

**Substituir por** (adiciona `[data-testid="stBaseButton-segmented_control"]` como seletor adicional e `!important` nas props visuais; font-size/font-weight/padding mantidos sem `!important` pois não têm conflito de especificidade):
```
            [data-testid="stSegmentedControl"] button,
            [data-testid="stBaseButton-segmented_control"],
            div[role="radiogroup"] [data-baseweb="button"],
            [data-baseweb="button-group"] button {{
                background: rgba(30, 38, 65, 0.88) !important;
                color: {COLORS["muted"]} !important;
                border: 1px solid rgba(25, 183, 255, 0.30) !important;
                border-radius: 10px !important;
                font-size: 1rem;
                font-weight: 600;
                padding: 0.5rem 1.15rem;
            }}
```

> **Decisão de precedência (Builder deve seguir):** `[data-testid="stBaseButton-segmented_control"]` é o seletor do INATIVO; `[data-testid="stBaseButton-segmented_controlActive"]` é o seletor do ATIVO. São testids DISTINTOS — não há sobreposição. O ativo NÃO corresponde ao inativo porque o testid tem sufixo diferente (`Active`). Portanto não há risco de o inativo sobrescrever o ativo, mesmo com `!important`, porque os seletores simplesmente não casam o mesmo elemento.

---

### Passo 4 — `pages.py`: adicionar regra nova do ATIVO APÓS o bloco aria-checked (linhas ~325-333)

**Arquivo:** `src/motor_expansao/dashboard/pages.py`

**Localizar** este bloco (texto âncora exato — regra ATIVA defeituosa):
```
            [data-testid="stSegmentedControl"] button[aria-checked="true"],
            [data-testid="stSegmentedControl"] button[aria-selected="true"],
            [data-baseweb="button-group"] button[aria-checked="true"] {{
                background: #19B7FF;
                color: #0A0C18;
                border-color: rgba(25, 183, 255, 0.9);
                box-shadow: 0 0 8px rgba(25, 183, 255, 0.35);
                font-weight: 700;
            }}
```

**Substituir por** (mantém lista `aria-*` existente INTACTA + AGREGA seletor real como item adicional da lista; todas as props do ativo ganham `!important`; regra única, sem separação):
```
            [data-testid="stSegmentedControl"] button[aria-checked="true"],
            [data-testid="stSegmentedControl"] button[aria-selected="true"],
            [data-baseweb="button-group"] button[aria-checked="true"],
            [data-testid="stBaseButton-segmented_controlActive"] {{
                background: #19B7FF !important;
                color: #0A0C18 !important;
                border-color: #19B7FF !important;
                box-shadow: 0 0 8px rgba(25, 183, 255, 0.35) !important;
                font-weight: 700 !important;
            }}
```

> **Decisão de design (Planner):** seletores `aria-checked`/`aria-selected` AGREGADOS (não substituídos) na mesma lista de seletores da regra. Isso: (a) mantém o teste `test_inject_styles_cobre_componentes_baseweb` passando sem alteração (ele verifica `aria-checked="true"` e `aria-selected="true"`); (b) adiciona o seletor real que casa o DOM do Streamlit 1.58.

> **Ordem/precedência ativo vs inativo:** a regra do ativo vem DEPOIS da regra do inativo no CSS (Passo 3 antes do Passo 4). Como os testids são distintos (`stBaseButton-segmented_control` vs `stBaseButton-segmented_controlActive`), não há cascata a resolver — mas mesmo que houvesse, a regra posterior com mesmo `!important` venceria pela ordem de aparição. O Builder deve preservar a ordem: regra inativo → regra hover → regra ativo.

---

### Passo 5 — `test_streamlit_app.py`: adicionar assert de regressão

**Arquivo:** `tests/integration/test_streamlit_app.py`

**Localizar** o fim do teste `test_inject_styles_cobre_componentes_baseweb` (texto âncora):
```
    assert "#19B7FF" in css  # aba ativa ciano sólido (BLK-UI-04)
```

**Substituir por** (adiciona assert de regressão imediatamente após):
```
    assert "#19B7FF" in css  # aba ativa ciano sólido (BLK-UI-04)
    assert "stBaseButton-segmented_controlActive" in css  # seletor real Streamlit 1.58 (BLK-UI-05)
    assert "stBaseButton-segmented_control" in css  # seletor inativo real Streamlit 1.58 (BLK-UI-05)
```

> Os dois asserts novos verificam apenas presença da string no CSS gerado — não verificam render no navegador (limite documentado). O assert do `aria-checked`/`aria-selected` existente continua passando porque os seletores não são removidos.

---

### Passo 6 — Validação

Executar em sequência (serial, sem flags especiais):

```
python -m pytest -q tests/integration/test_streamlit_app.py
```
Critério: zero novas falhas (baseline: `696 passed, 1 skipped, 3 failed PRÉ-EXISTENTES`). Os 3 pre-existentes devem permanecer como estavam.

```
python -c "import streamlit_app"
```
Critério: sem exceção.

```
ruff check src/motor_expansao/dashboard/pages.py tests/integration/test_streamlit_app.py
```
Critério: sem erros novos.

```
mypy src/motor_expansao/dashboard/pages.py
```
Critério: sem erros novos introduzidos por este diff.

---

## Arquivos afetados
- `src/motor_expansao/dashboard/pages.py` — SÓ o bloco CSS do `stSegmentedControl` dentro de `inject_styles()` (~linhas 304-333)
- `tests/integration/test_streamlit_app.py` — adicionar 2 asserts ao final de `test_inject_styles_cobre_componentes_baseweb`

## Dependências
- `COLORS["muted"]` = `"#A7B3D1"` (confirmado em `constants.py:278`); o Builder usa `{COLORS["muted"]}` (f-string template) na prop `color`, não o valor literal — manter como está no template.
- Branch correta: `ciclo/BLK-UI-05` (stack sobre `ciclo/BLK-UI-04`).

## Riscos técnicos
- **Render visual não verificável por pytest:** o assert de regressão garante a presença da string no CSS gerado, mas a confirmação de que o browser aplica a regra corretamente é responsabilidade do usuário. Registrar nota no commit.
- **Testids podem mudar em versões futuras do Streamlit:** os seletores `aria-checked`/`aria-selected` legados ficam como fallback documentado. Não remover.
- **Branch stack:** o Builder deve estar em `ciclo/BLK-UI-05` e não deve tocar `ciclo/BLK-UI-04` ou branches anteriores.
- **Paths pré-sujos:** `data/outputs/setores_censitarios_2022_geo/_metadata.json` e `data/reports/relatorio_pontual_censitario_base_geo.md` NÃO devem ser staged/commitados.
- **Risco de colisão de `!important`:** como `stBaseButton-segmented_control` e `stBaseButton-segmented_controlActive` são testids distintos (um elemento nunca terá os dois ao mesmo tempo), as regras com `!important` não competem — ativo sempre vence no elemento ativo, inativo sempre vence no elemento inativo.

## Critérios de aceite
- O CSS gerado por `inject_styles()` contém `stBaseButton-segmented_controlActive` com `#19B7FF !important`.
- O CSS gerado contém `stBaseButton-segmented_control` (inativo) com `rgba(30, 38, 65, 0.88) !important`.
- Os seletores `aria-checked="true"` e `aria-selected="true"` PERMANECEM no CSS (não foram removidos).
- O seletor `stSegmentedControl` PERMANECE no CSS.
- O gap tem `8px !important` no container.
- O teste `test_inject_styles_cobre_componentes_baseweb` continua passando (asserts existentes intactos).
- Novos asserts `"stBaseButton-segmented_controlActive" in css` e `"stBaseButton-segmented_control" in css` adicionados e passando.
- `render_tab_selector()` (`pages.py` a partir de linha ~440) NÃO foi alterada — confirmar via diff.
- Suite `pytest -q tests/integration/test_streamlit_app.py`: zero novas falhas além dos 3 pre-existentes.
- `ruff check` e `mypy` limpos sobre os arquivos alterados.
- Nenhum artefato M1 tocado.

## Validações obrigatórias
- Comando: `python -m pytest -q tests/integration/test_streamlit_app.py`
- Critério mínimo: `696 passed, 1 skipped, 3 failed` (pre-existentes) — ou melhor; ZERO novas falhas.
- Comando: `python -c "import streamlit_app"`
- Critério: sem exceção.
- Comando: `ruff check src/motor_expansao/dashboard/pages.py tests/integration/test_streamlit_app.py`
- Critério: sem erros novos.
- Comando: `mypy src/motor_expansao/dashboard/pages.py`
- Critério: sem erros novos.

## Fora de escopo
- Recalcular qualquer score ou artefato M1.
- Tocar `render_tab_selector()` ou qualquer lógica de render lazy (Bloco 5).
- Alterar CSS de outros componentes além do bloco `stSegmentedControl` (~linhas 304-333).
- Recolocar dependência de API ao vivo.
- Mergear branches ou tocar outros arquivos além dos dois listados.
- Alterar `PRD.md`, `CLAUDE.md`, `data/outputs/setores_censitarios_2022_geo/_metadata.json`, `data/reports/relatorio_pontual_censitario_base_geo.md`.

## Alerta de aprovação
Não aplicável — criticidade Média; sem gate humano antes do Builder.

## Guardrails ativos
- Guardrail permanente (CLAUDE.md §5): visualizações e interações de mapa não podem recalcular ou alterar `score_priorizacao`, `hex_score_estrutural`, carteira, plano curto prazo, plano de domínio ou artefatos oficiais do M1 sem aprovação explícita.
- Bloco 5 INTOCÁVEL: `render_tab_selector` e a lógica de `st.segmented_control`/`session_state` não podem ser alteradas (CLAUDE.md §4).
- CLAUDE.md §6: NUNCA executar comandos no VPS sem confirmação explícita do usuário para cada comando individual.
- READ-ONLY sobre o M1: este bloco toca SOMENTE CSS em `inject_styles()` — zero escrita em artefatos M1.
