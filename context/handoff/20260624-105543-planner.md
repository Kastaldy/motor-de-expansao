# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
**Approver (gate humano)** — esteira Alta. O gate deve **ESCOLHER a direção (A1 / A2 / B / C)** ANTES do Builder.

## Entendimento da tarefa
Dar à página 7/9 "Bairros por Zona" do Relatório Municipal a lista de bairros REAIS agrupados pelas 3 zonas geométricas (Âncora central / Flancos laterais / Cerco), com fallback gracioso, READ-ONLY sobre o M1 e sem regredir o Relatório Pontual Censitário.

---

## DESCOBERTA QUE MUDA O PLANO (verificação de realidade do repo)

O Block Orchestrator assumiu que o shapefile de setores IBGE 2022 **não tem** `NM_BAIRRO`. **Isso está incorreto.** Eu li o cabeçalho do DBF real
`data/raw/CENSO 2022/BR_setores_CD2022/BR_setores_CD2022.dbf` (468.099 registros) e ele JÁ CONTÉM, por setor:

```
CD_SETOR, SITUACAO, CD_SIT, CD_TIPO, AREA_KM2, CD_REGIAO, NM_REGIAO, CD_UF, NM_UF,
CD_MUN, NM_MUN, CD_DIST, NM_DIST, CD_SUBDIST, NM_SUBDIST,
CD_BAIRRO (13), NM_BAIRRO (254),  <-- JÁ EXISTEM
CD_NU, NM_NU, CD_FCU, NM_FCU, CD_AGLOM, NM_AGLOM, CD_RGINT, NM_RGINT, CD_RGI, NM_RGI,
CD_CONCURB, NM_CONCURB
```

O `.cpg` do shapefile é **UTF-8**. A coluna `NM_BAIRRO` simplesmente NÃO é selecionada hoje em `carregar_malha_uf`
(`materializar_setores_censitarios_geo.py:225`: `cols = ["CD_SETOR","CD_UF","CD_MUN","NM_MUN","SITUACAO","AREA_KM2","geometry"]`).

**Consequência:** a Direção A (re-materializar incluindo bairro) NÃO precisa de aquisição de dado externo, NÃO precisa de join espacial e é 100% OFFLINE — vira uma ADIÇÃO DE COLUNA. Direções B (OSM) e C (Nominatim) tornam-se inferiores em todos os eixos.

### Cobertura real de `NM_BAIRRO` (amostra medida via geopandas, com `where=CD_UF=...`)
| UF | setores | com bairro | % | municípios | municípios com bairro |
|---|---:|---:|---:|---:|---:|
| RJ | 41.700 | 30.535 | **73,2%** | 92 | 31 |
| AC | 2.215 | 221 | **10,0%** | 22 | 8 |
| DF | 5.418 | 0 | **0,0%** | 1 | 0 |

**Cobertura é HETEROGÊNEA** (capitais/grandes têm bairro; municípios pequenos e o DF não têm). Logo o **fallback gracioso é MANDATÓRIO em qualquer direção**, exatamente como o BO alertou — mas a fonte é interna e offline. Amostra de nomes reais lidos: "Centro", "Cascata", "Bela Vista", "Cidade Nova" (AC); "Gramacho", "Taquara", "Campo Grande", "Anchieta" (RJ).

### Demais fatos verificados
- Deps: `geopandas>=0.14.4`, `shapely>=2.0.4`, `pyproj>=3.6.1` na BASE; `contextily>=1.5.0` em `[basemap]`. **NÃO há `osmnx` nem `fiona`** declarados → Direção B exigiria nova dependência pesada.
- **Nenhum** shapefile/gpkg/pbf de bairro/OSM em `data/` (só `data/ibge/municipios_*.geojson`, que têm apenas `codarea`, sem bairro). Direção B precisaria adquirir e versionar/gitignorar um novo dataset externo.
- Base geo materializada: `data/outputs/setores_censitarios_2022_geo/` existe, **27 UFs**, ~1,17 GB, 468.099 setores.
- O `df` que alimenta `agregar_municipio` (em `pages.py:3067` e `3916`) é o slice enriquecido por UF (hex-level); **NÃO carrega bairro hoje**. O trace censitário (`censo_df`, mesclado em `enrich_dashboard_data` via `censo_extra_cols`, `data.py:391-409`) é H3-agregado por `hex_id` e **também não tem bairro**.
- Testes: `tests/unit/test_relatorio_municipal.py` cobre `_bairros_page` indiretamente (asserts `b"Bairros indisponiveis na base atual"`, `/Count 9`). Esses asserts MUDAM neste bloco.

---

## QUESTÕES ABERTAS — O GATE HUMANO DEVE ESCOLHER 1 DIREÇÃO

A escolha final é do humano. Eu detalho cada direção (mudança, esforço, prós, contras, riscos, conformidade) e RECOMENDO uma, mas NÃO pré-fixo.

### Direção A — re-materializar a malha geo IBGE incluindo `nome_bairro` (fonte INTERNA, offline)
A coluna já está no DBF; basta selecioná-la, materializá-la e expô-la ao relatório. Há duas sub-variantes pelo CAMINHO de propagação ao relatório:

#### A1 — propagar `nome_bairro` ATÉ O HEX (via `censo_extra_cols` / `enrich_dashboard_data`)
- **O que muda em código/dados:**
  1. `materializar_setores_censitarios_geo.py`: incluir `NM_BAIRRO`/`CD_BAIRRO` (e opcional `NM_SUBDIST`/`NM_DIST` p/ fallback) em `carregar_malha_uf` (linha 225) e mapear `nome_bairro`/`cod_bairro` em `montar_base_setorial_uf`; adicionar `nome_bairro` a `COLUNAS_ARTEFATO` (linha 84).
  2. Re-materializar os parquets da malha geo (27 UFs, ~1,17 GB).
  3. Re-agregar `nome_bairro` no TRACE CENSITÁRIO H3 (`censo2022_setores_*` / o `censo_df` mesclado em `enrich_dashboard_data`): bairro DOMINANTE por hex (moda por `hex_id`).
  4. `data.py`: adicionar `nome_bairro` a `censo_extra_cols` (linha 391).
  5. Re-gerar o enriquecido por UF + o dashboard parquet (a cadeia `enrich_dashboard_data`/`fase1_bi_exports`).
  6. `relatorio_municipal.py`: `_bairros_page` lê `nome_bairro` por hex do `df_muni`, agrupa pelos hexes de cada zona (`hex_zona_geo` de `_zonas_geometricas`) e lista bairros distintos por zona.
- **Esforço:** ALTO. Re-materialização global + re-agregação do trace H3 + regeneração do enriquecido + alteração de `_bairros_page` + testes. É o caminho mais "completo" mas o mais caro e o que mais toca a cadeia de dados do dashboard.
- **Prós:** bairro disponível para QUALQUER consumidor do hex (não só o relatório); offline; fonte oficial IBGE; consistente com o resto do enriquecido.
- **Contras/Riscos:** re-materialização global pesada; mexe na cadeia de enriquecimento (mais superfície de regressão / contratos dos Blocos 4–6); a agregação "bairro dominante por hex" perde resolução intra-hex (um hex res-7 ≈ 5 km² pode cobrir vários bairros). Exige regenerar artefatos derivados de dashboard.
- **Conformidade:** READ-ONLY M1 (não toca score/pesos/artefatos oficiais; o trace censitário é camada paralela). Offline §2 OK. Performance Blocos 4–6: precisa garantir que a coluna nova não estoure a carga lazy (1 coluna `str` a mais por linha — impacto pequeno, mas a regeneração precisa rodar).

#### A2 — re-materializar a malha geo e o relatório LÊ a partição geo on-demand (RECOMENDADA)
- **O que muda em código/dados:**
  1. `materializar_setores_censitarios_geo.py`: mesma adição da A1 (passos 1–2): selecionar `NM_BAIRRO`/`CD_BAIRRO` em `carregar_malha_uf`, mapear `nome_bairro` em `montar_base_setorial_uf`, adicionar `nome_bairro` a `COLUNAS_ARTEFATO`. Re-materializar a malha geo (~1,17 GB).
  2. **NÃO** toca `enrich_dashboard_data`/`censo_extra_cols`/dashboard parquet (cadeia hot do dashboard intocada).
  3. `relatorio_municipal.py`: `agregar_municipio` ganha parâmetro OPCIONAL `bairros_por_hex: dict[str,str] | None = None` (ou `censo_geo_dir: Path | None = None`). Um helper novo `_bairros_por_zona(df_muni, hex_zona_geo, bairros_por_hex)` resolve, para cada hex de zona, o bairro (lookup direto) e lista os distintos por zona.
  4. Helper de lookup setor→hex→bairro: para o município pedido, ler a partição `uf=XX/cod_municipio=NNNNNNN/part-000.parquet` (já existe `ler_particao_setores`), pegar `cod_setor`+`nome_bairro`+centróide do setor, mapear o centróide do setor a `hex_id` res-7 (h3) e construir `bairros_por_hex` (bairro dominante por hex por população de setor). Como é UM município por geração, o custo é trivial.
  5. Em `pages.py` (`render_relatorio_municipal_download_topo` ~3067 e `render_relatorio_municipal_expander` ~3916): resolver o `cod_municipio` do município selecionado (reusa o padrão `resolve_cod_municipio_from_geo_dir`) e passar `censo_geo_dir`/`bairros_por_hex` a `agregar_municipio`. Fallback gracioso: sem partição → `None` → página simplificada como hoje, sem exceção.
- **Esforço:** MÉDIO. Adição de coluna + re-materialização da malha geo (mesmo job da A1) + 1 helper de lookup no relatório + fiação no `pages.py` + testes. **Não** re-gera o enriquecido nem o dashboard parquet (menos superfície de regressão).
- **Prós:** mantém a cadeia hot do dashboard (Blocos 4–6) BYTE-A-BYTE intocada; mantém a resolução do SETOR (bairro por setor, não por hex inteiro); lê só 1 partição por geração (rápido); offline; é o mesmo padrão de `censo_geo_dir` já usado pelo Relatório Pontual/Mapa Territorial. O re-materializar continua sendo um passo de DADOS, mas sem regenerar a cadeia de dashboard.
- **Contras/Riscos:** re-materialização global da malha geo (~1,17 GB) ainda é necessária para que TODOS os municípios tenham `nome_bairro` na partição — mas pode ser faseada por UF (ver "Estratégia de re-materialização"); mapeamento setor→hex→zona adiciona uma camada de lógica (testável e determinística, sem rede).
- **Conformidade:** READ-ONLY M1 (idêntico A1). Offline §2 OK. Performance Blocos 4–6: **não mexe** em `load_uf_slice`/`read_enriched_uf_partition`/`build_dashboard_dataset` → zero risco de regressão de carga.

### Direção B — cruzar com camada externa de bairros (OSM `admin_level=10` / arquivo local)
- **O que muda:** novo dataset externo (não existe no repo) + nova dependência (`osmnx` ou `fiona` não declaradas) + join espacial setor×bairro on-demand no relatório.
- **Esforço:** ALTO (aquisição + versionamento gitignored + nova dep + join espacial + cobertura irregular).
- **Prós:** poderia cobrir municípios sem bairro IBGE (alguns têm bairro no OSM).
- **Contras/Riscos:** **conflita com §2** se online na carga; OSM `admin_level=10` tem cobertura irregular e qualidade variável no Brasil; nova dependência pesada; aquisição de dado não reprodutível. **Pior em todos os eixos vs. A**, dado que a fonte IBGE já está no repo.
- **Conformidade:** offline só se com arquivo local pré-baixado (novo artefato); READ-ONLY M1 OK.

### Direção C — reverse-geocode Nominatim do centróide dos hexes de zona
- **O que muda:** chamar `resolve_endereco_http`/Nominatim por hex e extrair `suburb`/`neighbourhood`.
- **Esforço:** baixo em código, INVIÁVEL em operação.
- **Contras/Riscos:** **viola DEC-010** (máx. 1 req/s, sem uso em massa) e §2 (rede na geração em lote); lento e não-determinístico. **Não pode ser o mecanismo primário.** Admissível só como fallback PONTUAL para municípios minúsculos (<5 hexes de zona) e SE o gate aprovar explicitamente — não recomendado.
- **Conformidade:** fere §2/DEC-010 em lote.

### RECOMENDAÇÃO DO PLANNER (sujeita ao gate): **Direção A2**
Justificativa técnica: (1) a fonte de bairro é INTERNA e OFICIAL (DBF IBGE já no repo) → offline, reprodutível, zero aquisição; (2) A2 mantém a cadeia hot do dashboard (Blocos 4–6) intocada, minimizando superfície de regressão vs. A1; (3) preserva a resolução do SETOR (bairro por setor, melhor fidelidade ao template que "bairro dominante do hex inteiro" da A1); (4) reusa o padrão `censo_geo_dir`/`ler_particao_setores` já existente; (5) custo de geração trivial (1 partição/município). A1 é aceitável se o humano quiser bairro disponível para outros consumidores do hex; B e C ficam para trás (B: nova dep + dado externo; C: fere DEC-010).

---

## Plano técnico

### PARTE COMUM A QUALQUER DIREÇÃO (executar em todas)
1. **`_bairros_page` deixa de ser simplificada** (`relatorio_municipal.py:1608-1659`):
   - Trocar a nota fixa "Bairros indisponiveis na base atual; ... nao inclui NM_BAIRRO." pela listagem de bairros REAIS por zona quando houver bairros; manter as 3 zonas geométricas (`result["zonas_geo"]`) como agrupador (Zona 1 Âncora central / 2 Flancos laterais / 3 Cerco).
   - Para cada zona: cabeçalho colorido "Zona N - {rotulo}" (mantido) + lista de bairros distintos daquela zona (ex.: "Centro, Cascata, Bela Vista, ..."), com cap de N bairros por zona (sugiro 8–12, igual ao template) e "... e mais N" ao truncar.
2. **`agregar_municipio`** passa a expor `bairros_por_zona` no `result` (lista alinhada a `zonas_geo`): `result["bairros_por_zona"] = [{"zona_n":1,"rotulo":"Ancora central","bairros":[...]}, ...]`. Quando não houver fonte de bairro → listas vazias (fallback).
3. **Fallback gracioso (mandatório):** município sem bairro mapeado (DF, pequenos) → `_bairros_page` renderiza as zonas geométricas + tese SEM exceção e SEM a nota de "indisponível" como texto PRINCIPAL (pode haver nota técnica discreta no rodapé). Nenhum `raise`. (CA 2.)
4. **Helper de agrupamento** `_bairros_por_zona(hex_zona_geo, bairros_por_hex) -> list[dict]`: para cada hex de `hex_zona_geo` (mapa `hex_id -> 0|1|2`), olha `bairros_por_hex[hex_id]`, agrupa nomes distintos por zona, ordena por frequência (bairro mais recorrente primeiro). PURA, sem rede, testável.
5. **`PDF_SECTION_HEADERS`** e contagem de páginas: **9 páginas, ordem inalterada** ("Bairros por Zona" segue como header). Não mudar `/Count 9`.
6. **`docs/relatorio_municipal_template.md`:** atualizar D9 (linha ~124) de "SIMPLIFICADA" para "IMPLEMENTADA": Página 6/7 lista bairros reais por zona (fonte IBGE `NM_BAIRRO` do setor, agregado por zona geométrica), com fallback gracioso para municípios sem bairro mapeado. Registrar a fonte e a heterogeneidade de cobertura.
7. **Testes** (`tests/unit/test_relatorio_municipal.py`):
   - Atualizar `test_pdf_municipal_fallback_sem_dominio_pagina_6` e `test_pdf_municipal_pagina_6_nota_bairro_sempre_presente`: o assert `b"Bairros indisponiveis na base atual"` MUDA — quando NÃO há fonte de bairro a página renderiza as zonas geométricas sem essa string como texto principal; ajustar para a nova mensagem de fallback.
   - Novo teste COM bairros: passar `bairros_por_hex` (ou `bairros_por_zona`) com nomes reais → assert que os nomes aparecem nos bytes do PDF agrupados por zona, `/Count 9` mantido.
   - Novo teste SEM bairros (fallback): sem fonte de bairro → página renderiza, `/Count 9` mantido, sem exceção, sem PII.
   - Manter `test_coexistencia_relatorio_pontual_intocado` verde (Pontual byte-a-byte).

### SE A ESCOLHA FOR A1 (propagar ao hex)
- **A1.1** `materializar_setores_censitarios_geo.py`: em `carregar_malha_uf` (linha 225), adicionar `"NM_BAIRRO","CD_BAIRRO"` (e opc. `"NM_SUBDIST","NM_DIST"`) à lista `cols`; mapear `gdf["nome_bairro"] = gdf["NM_BAIRRO"].astype(str)` (normalizar vazio/"nan"→NA). Em `montar_base_setorial_uf`, propagar `nome_bairro`. Adicionar `"nome_bairro"` a `COLUNAS_ARTEFATO` (linha 84).
- **A1.2** Re-materializar a malha geo (script existente `materializar_base_geo`, todas as 27 UFs). Estimar tempo (ver "Estratégia de re-materialização").
- **A1.3** Re-agregar `nome_bairro` no trace censitário H3 (`censo_df`): bairro DOMINANTE por `hex_id` (moda ponderada por `pop_total_setor_2022`). Localizar o pipeline que gera `censo2022_setores_*`/o trace mesclado em `enrich_dashboard_data` e adicionar a coluna (Builder deve localizar o gerador exato; é a única parte que pode exigir descoberta adicional).
- **A1.4** `data.py:391` — adicionar `"nome_bairro"` a `censo_extra_cols`.
- **A1.5** Re-gerar enriquecido por UF + dashboard parquet (cadeia `enrich_dashboard_data`/`fase1_bi_exports`).
- **A1.6** `_bairros_page` lê `df_muni["nome_bairro"]` por hex; `agregar_municipio` constrói `bairros_por_hex` a partir do próprio `df_muni`.

### SE A ESCOLHA FOR A2 (relatório lê a partição geo on-demand) — RECOMENDADA
- **A2.1** `materializar_setores_censitarios_geo.py`: IDÊNTICO a A1.1 (selecionar `NM_BAIRRO`/`CD_BAIRRO`, mapear `nome_bairro`, adicionar a `COLUNAS_ARTEFATO`).
- **A2.2** Re-materializar a malha geo (mesmo job da A1.2). **NÃO** mexe em `enrich_dashboard_data`/`censo_extra_cols`/dashboard parquet.
- **A2.3** Novo helper em `relatorio_municipal.py` `_carregar_bairros_por_hex(uf, cod_municipio, censo_geo_dir) -> dict[str,str]`: usa `ler_particao_setores(root=censo_geo_dir, uf=uf, cod_municipio=cod_municipio, columns=["cod_setor","nome_bairro","bbox_minx","bbox_miny","bbox_maxx","bbox_maxy","pop_total_setor_2022"])`; deriva o centróide do setor (do bbox), mapeia a `hex_id` res-7 via h3, e resolve o bairro dominante por hex (moda ponderada por pop). Import lazy de h3. Fallback gracioso: partição ausente/sem `nome_bairro` → `{}`.
- **A2.4** `agregar_municipio` ganha `bairros_por_hex: dict[str,str] | None = None` (default `None` = comportamento idêntico ao atual; preserva todos os testes que não passam o param). Quando dado, popula `result["bairros_por_zona"]` via `_bairros_por_zona(hex_zona_geo, bairros_por_hex)`.
- **A2.5** `pages.py` (`render_relatorio_municipal_download_topo` ~3067 e `render_relatorio_municipal_expander` ~3916): resolver `cod_municipio` (reusar `resolve_cod_municipio_from_geo_dir`/padrão existente) e construir `bairros_por_hex` via A2.3, passando a `agregar_municipio`. Fallback: sem `censo_geo_dir`/partição → `None` → página simplificada (sem exceção).

### SE A ESCOLHA FOR B ou C
- Não recomendadas; se o gate insistir, abrir SUB-bloco de aquisição de dado (B) ou registrar DEC explícita de exceção pontual de rede (C, só fallback <5 hexes). O Planner deve ser reacionado para detalhar — NÃO implementar nesta passada.

---

## Estratégia de re-materialização (A1/A2)
- O job re-materializa a malha geo. Para REDUZIR risco/tempo: re-materializar **incrementalmente por UF** com `materializar_base_geo(ufs=[...])` (o pipeline já aceita `--uf`/`ufs`), validando 1–2 UFs (ex.: RJ com 73% de cobertura + DF com 0%) ANTES do batch completo. A coluna nova só adiciona ~1 `str`/setor (impacto de tamanho pequeno). Re-materialização é PASSO OPERACIONAL de dados (como nas DEC-006/007): pode ocorrer pós-merge; o fechamento de testes NÃO depende dele (testes usam DataFrames sintéticos com `nome_bairro`). Documentar a ordem e a verificação de reprodutibilidade (contagem de setores inalterada: 468.099).

## Arquivos afetados
- `src/motor_expansao/dashboard/relatorio_municipal.py` — `_bairros_page` (1608-1659), `agregar_municipio` (419-521), novo `_bairros_por_zona`; (A2) novo `_carregar_bairros_por_hex`.
- **(A1/A2)** `src/motor_expansao/pipelines/materializar_setores_censitarios_geo.py` — `carregar_malha_uf` (216-234), `montar_base_setorial_uf` (307-373), `COLUNAS_ARTEFATO` (84-118); re-materialização dos parquets.
- **(A1 apenas)** `src/motor_expansao/dashboard/data.py` — `censo_extra_cols` (391-409); + gerador do trace censitário H3 (a localizar) + regeneração do enriquecido.
- **(A2)** `src/motor_expansao/dashboard/pages.py` — fiação de `bairros_por_hex`/`censo_geo_dir` nas 2 invocações (~3067, ~3916).
- `docs/relatorio_municipal_template.md` — D9 (~124) "simplificada" → "implementada".
- `tests/unit/test_relatorio_municipal.py` — atualizar asserts da Página 6 + 2 testes novos (com/sem bairro).

## Dependências
- Técnica: `geopandas`/`shapely`/`pyproj` (base) e `h3` já presentes — suficientes para A1/A2 sem nova dependência. B exigiria `osmnx`/`fiona` (ausentes). C reusa `maps_geocoder` (rede — inviável em lote).
- De dados: A1/A2 dependem só do DBF IBGE já no repo. B depende de dado externo inexistente.

## Riscos técnicos
- **Cobertura heterogênea de bairro (medida):** RJ 73%, AC 10%, DF 0%. Fallback gracioso é MANDATÓRIO (parte comum, passo 3) — não opcional.
- **Encoding do shapefile:** `.cpg` diz UTF-8, mas a leitura de amostra mostrou mojibake ("Pl�cido", "Cabu�u"). O Builder deve confirmar/forçar `encoding="utf-8"` em `gpd.read_file` (kwarg do driver pyogrio/fiona) e normalizar p/ ASCII/latin-1 no PDF (já há `_ascii`). Validar acentos em pelo menos 1 UF.
- **Re-materialização global (~1,17 GB):** tempo alto; mitigar com execução incremental por UF e fechamento de testes independente dos parquets reais (passo "Estratégia de re-materialização").
- **(A1) superfície de regressão maior:** mexe em `censo_extra_cols`/enriquecido → risco para Blocos 4–6 e contratos do dashboard; A2 evita isso.
- **Resolução intra-hex:** um hex res-7 cobre vários bairros. A1 (bairro dominante por hex) perde resolução; A2 (bairro por setor) preserva melhor. Documentar a escolha.
- **Testes existentes da Página 6 quebram de propósito** (`b"Bairros indisponiveis..."`): atualizar conscientemente, sem bypass.
- **Coexistência:** `_bairros_page` é do módulo NOVO; o Relatório Pontual NÃO é tocado — manter `test_coexistencia_relatorio_pontual_intocado` verde.

## Critérios de aceite finais
1. A Página 7/9 "Bairros por Zona" lista bairros REAIS agrupados por zona (Âncora central / Flancos laterais / Cerco) para ≥1 município coberto (ex.: um do RJ).
2. Fallback gracioso verificável: município sem bairro (ex.: DF) renderiza a página sem exceção, sem a nota de "indisponível" como texto PRINCIPAL.
3. Relatório Pontual Censitário (raio 1,5 km) BYTE-A-BYTE intocado: suíte censitária verde; `setor_censitario_intersecao_area_1p5km` e `render_mapas_censitarios_combinados` inalterados.
4. M1 intocado: `score_priorizacao`, pesos `renda=0.40`/`pop=0.60`, `hex_score_estrutural`, artefatos oficiais, `config.py` inalterados.
5. Suíte completa verde (ruff + mypy + pytest), sem bypass de CI.
6. PDF continua com **9 páginas** na MESMA ordem (`/Count 9`).
7. Performance Blocos 4–6 não regredida (`load_uf_slice`, `read_enriched_uf_partition`, `build_dashboard_dataset`, `render_tab_selector` intocados — A2 não os toca; A1 só ADICIONA 1 coluna ao trace, sem alterar a API de carga).

## Validações obrigatórias
- Comando: `python -m pytest -q tests/unit/test_relatorio_municipal.py` — Critério: 0 falhas; novos testes com/sem bairro verdes; `/Count 9`.
- Comando: `python -m pytest -q tests/ -k "censo or censitario or streamlit_app or relatorio"` — Critério: 0 falhas (coexistência do Pontual + dashboard).
- Comando: `python -m pytest -q -n auto` (suíte completa no QA) — Critério: 0 falhas, 0 erros de import.
- Comando: `ruff check src tests` e `ruff format --check src tests` — Critério: limpo.
- Comando: `mypy src/motor_expansao/dashboard/relatorio_municipal.py src/motor_expansao/pipelines/materializar_setores_censitarios_geo.py` — Critério: 0 erros.
- (A1/A2, pós-merge operacional) `python -m motor_expansao.pipelines.materializar_setores_censitarios_geo --uf RJ --uf DF` em scratch + verificar `nome_bairro` populado em RJ e vazio em DF, contagem de setores inalterada.

## Fora de escopo
- Alterar `score_priorizacao`, `hex_score_estrutural`, carteira, plano, plano de domínio ou artefatos oficiais M1.
- Alterar o Relatório Pontual Censitário (`censo_report.py`, `censo_map.py`, `analisar_ponto_censitario_setores`, `setor_censitario_intersecao_area_1p5km`).
- Quebrar contratos de performance dos Blocos 4–6 (carga lazy por UF, render lazy de abas, fonte de mapa enxuta).
- Reverse-geocode em lote na carga do dashboard (Nominatim só fallback pontual se o gate aprovar; não recomendado).
- Mudar o número de páginas do PDF (segue 9) ou a ordem das seções.
- Alterar `config.py`, `pipelines/m1/` ou parâmetros canônicos do §3.

## Alerta de aprovação
**ATENÇÃO: esta tarefa é de criticidade ALTA e exige GATE HUMANO antes do Builder.** O humano deve ler este handoff e (1) APROVAR a execução e (2) **ESCOLHER UMA DIREÇÃO entre A1, A2, B e C** — as direções são mutuamente exclusivas e têm impactos de dados/regressão muito diferentes. **Recomendação do Planner: Direção A2** (fonte de bairro INTERNA/OFFLINE já no DBF IBGE, sem nova dependência, sem tocar a cadeia hot do dashboard). O Builder NÃO deve iniciar nem escolher a direção sem essa decisão. NÃO toca artefatos M1 (READ-ONLY), por isso é Alta e não Crítica.

## Guardrails ativos
- §5 CLAUDE.md: visualizações/relatório não podem recalcular/alterar `score_priorizacao`, `hex_score_estrutural`, carteira, plano, plano de domínio ou artefatos oficiais do M1 sem aprovação explícita.
- §2 CLAUDE.md: não criar dependência de API ao vivo no dashboard de produção (Direção A é 100% offline; B/C só sob DEC).
- DEC-011 (D9): simplificação temporária da Página de bairros — RESOLVIDA por este bloco.
- DEC-010: Nominatim máx. 1 req/s, sem uso em massa — proíbe reverse-geocode em lote (Direção C).
- DEC-004/DEC-011 (D3): tiles online só no caminho de GERAÇÃO do relatório, com fallback offline — inalterado por este bloco.
- §6.1: este bloco NÃO é `loop-safe` (toca base geo derivada + página do relatório; exige gate humano).
