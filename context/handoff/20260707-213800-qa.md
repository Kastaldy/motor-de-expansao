# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Block Orchestrator (fechamento manual do ciclo)

## VEREDITO
APROVADO

## Justificativa
O módulo `batch_viabilidade.py` implementa o batch coordless corretamente, respeitando DEC-009
(demanda SÓ como premissa por tier; `setores_df=None` em todas as chamadas; lat/lng são placeholders
`0.0`) e READ-ONLY M1 (viabilidade_ponto.py com diff vazio, mtimes dos artefatos oficiais inalterados).
Todas as validações obrigatórias re-executadas pelo QA passaram; as 5 falhas/erro da suíte FULL são
PRÉ-EXISTENTES e puramente ambientais (deps opcionais `openlocationcode`/`matplotlib` ausentes),
confirmadas idênticas no commit-base ANTES do VIAB-03.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- `test_rodar_candidato_grade_serializa` usa `pd.read_json(<string literal>)`, que emite `FutureWarning`
  (deprecação); envolver em `io.StringIO` silencia o aviso. Cosmético, não bloqueante.

## Testes faltantes
- nenhum. Cobertura adequada: limpeza da base de calibração (3 casos), mapeamento de tier/faixa
  (3 casos), rodar_candidato/flags/coordless/serialização (3 casos), batch/ranking/sem-PII/determinismo
  (5 casos), materialização (1 caso). Total 14, verdes.

## Riscos remanescentes
- Tier `>=3000` tem N=2 (`flag_extrapolacao=True`): percentis p50/p90 instáveis; o relatório `.md`
  avisa explicitamente. Aceito e documentado (não é defeito).
- Ambiente de CI/loop sem `openlocationcode` e `matplotlib` — 5 testes de outras trilhas
  (coord_search Plus Code + demanda_revelada viz) ficam vermelhos por ImportError. Fora do escopo
  deste bloco; recomenda-se instalar as deps opcionais ou marcar `skipif` num bloco de manutenção.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suite FULL (gate único) — `python -m pytest -n auto -q`
```
=========================== short test summary info ============================
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_curto_usa_localidade_mock
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_curto_usa_localidade_mockada
ERROR tests/unit/demanda_revelada/test_viz_atratividade.py
4 failed, 1478 passed, 4 skipped, 19 warnings, 1 error in 433.56s (0:07:13)
```
As 5 falhas/erro são PRÉ-EXISTENTES e AMBIENTAIS (verificado no commit-base 36aedd7^, ANTES do VIAB-03):
- 2 acknowledged no critério de aceite: `test_render_coord_search_plus_code_completo_offline`,
  `test_render_coord_search_plus_code_curto_usa_localidade_mock` (ImportError `openlocationcode`).
- +2 mesma causa-raiz `openlocationcode`: `test_resolve_plus_code_completo_decodifica_offline`,
  `test_resolve_plus_code_curto_usa_localidade_mockada`.
- 1 ERROR de coleta: `test_viz_atratividade.py` → `ModuleNotFoundError: No module named 'matplotlib'`.
- `python -c "import openlocationcode"` → `ModuleNotFoundError`; `python -c "import matplotlib"` → `ModuleNotFoundError`.
- Único arquivo de teste alterado no ciclo (36aedd7^..HEAD): `tests/unit/dimensionamento/test_batch_viabilidade.py`.
- Re-executados no base 36aedd7^: os 5 falham/erram IDENTICAMENTE (matplotlib ERROR + 2 plus_code FAILED confirmados).
- ZERO falhas novas atribuíveis ao VIAB-03.

### 2. ruff — `ruff check batch_viabilidade.py test_batch_viabilidade.py`
```
All checks passed!
```

### 3. mypy — `mypy src/motor_expansao/dimensionamento/batch_viabilidade.py`
```
Success: no issues found in 1 source file
```

### 4. Smoke import — `python -c "from motor_expansao.dimensionamento.batch_viabilidade import run; print('ok')"`
```
ok
```

### 5. viabilidade_ponto.py intocado — `git diff HEAD -- .../viabilidade_ponto.py`
```
(vazio — 0 linhas; exit 0)
```

### 6. Guardrails M1 (mtimes) — `stat -c "%Y %n" brasil_estrutural.parquet brasil_priorizados.parquet`
```
1780501621 data/staging/brasil_estrutural.parquet
1780501631 data/staging/brasil_priorizados.parquet
```
Iguais ao baseline (1780501621 / 1780501631) — INALTERADOS.

### 7. loop_guard — `python scripts/loop_guard.py --base 36aedd7^`
```
GUARD OK: 5 caminho(s) alterado(s), nenhum proibido.
```
Nota: o comando do handoff sem `--base` falha (arg obrigatório); usei `--base 36aedd7^` (pai do commit
VIAB-03 = início efetivo do ciclo). config.py de `dimensionamento/` NÃO é caminho proibido no `_DENY`
(só o `config.py` raiz do M1) e sua churn de CRLF é unstaged (fora do diff com-intenção-de-merge).

### 8. DEC-009 (demanda como premissa) — confirmado em código (leitura de batch_viabilidade.py)
- lat/lng são placeholders coordless: `LAT_COORDLESS=0.0`/`LNG_COORDLESS=0.0` (l.40-41), passados
  como `lat=LAT_COORDLESS, lng=LNG_COORDLESS` (l.172-173) — NUNCA preditor de demanda.
- `demanda_premissa` vem SEMPRE do tier: `assign_tier_e_faixa(m2, df_tiers)` → p10/p50/p90 (l.168);
  `_analisar(p10)`, `_analisar(p50)`, `_analisar(p90)` (l.181-183). Nunca de lat/lng.
- `setores_df=None` em TODAS as chamadas: único helper `_analisar` com `setores_df=None` (l.178);
  teste `test_rodar_candidato_coordless_e_flags` afirma `all(c["setores_df"] is None ...)` nas 3 chamadas.
- flag `demanda_fonte="premissa_explicita"`: `"demanda_fonte": str(res.demanda_fonte)` (l.227);
  teste `test_demanda_fonte_premissa_em_todas_linhas` afirma para todas as linhas.

### 9. housekeeping --check — `python scripts/housekeeping_move_block.py BLK-VIAB-03 --check`
```
OK: BLK-VIAB-03 movido (stub no backlog + bloco em completed.md).
(exit 0)
```
Move feito via helper versionado (`--date 2026-07-07`, "OK: movido BLK-VIAB-03 (2395 bytes)"); stub de
1 linha em `backlog.md:902` (`- BLK-VIAB-03 (concluído 2026-07-07) — ver tasks/completed.md`); bloco
íntegro em `completed.md`. Byte-identity garantida pelo helper (10 testes em test_housekeeping_helper.py,
verdes na suíte). NÃO houve edição manual.

### 10. Testes dos novos módulos VIAB — `pytest tests/unit/dimensionamento/ -v`
```
286 passed, 1 warning in 50.87s
```
Inclui os 14 testes de `test_batch_viabilidade.py` (todos PASSED) + os testes pré-existentes de
simulador/viabilidade_ponto/demanda_premissa. Subset isolado do novo módulo: `14 passed, 1 warning`.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock do
caminho crítico. A suíte usa fixtures sintéticas legítimas para isolar lógica (prática normal, não
bypass — §26 do prompt). O housekeeping foi validado contra `backlog.md`/`completed.md` reais via o
helper versionado. loop_guard rodou contra o diff real base..HEAD.

## Conferência cruzada com log do Builder
Bate. ruff/mypy/import/14-testes/diff-vazio-viabilidade_ponto/mtimes-M1/DEC-009/gitignore: idênticos
ao reportado pelo Builder. As 2 falhas Plus Code que o Builder citou como pré-existentes foram
confirmadas — e o QA identificou +2 falhas Plus Code (mesma causa `openlocationcode`) + 1 ERROR
matplotlib que só aparecem na suíte FULL (o Builder rodou subset), TODAS pré-existentes/ambientais.

## Guardrails verificados
- score_priorizacao não alterado: sim (não aplicável — READ-ONLY M1; DEC-001/DEC-008/DEC-009).
- Artefatos M1 preservados: sim — mtimes `brasil_estrutural.parquet`=1780501621 e
  `brasil_priorizados.parquet`=1780501631 INALTERADOS.
- Validações re-executadas pelo QA (sem bypass):
  - pytest -n auto (FULL) → 4 failed + 1 error (todos pré-existentes ambientais) / 1478 passed
  - ruff → All checks passed
  - mypy → Success
  - import run → ok
  - git diff viabilidade_ponto.py → vazio
  - loop_guard --base 36aedd7^ → GUARD OK
  - housekeeping --check → OK (exit 0)
  - pytest dimensionamento -v → 286 passed
- Escopo respeitado: sim — só `batch_viabilidade.py` (novo) + `test_batch_viabilidade.py` (novo) +
  fechamento (tasks/handoffs). Nenhuma dependência arrastada; saídas gitignored.
- Parâmetros canônicos preservados: H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0,
  pesos renda=0.40/pop=0.60 — não tocados (camada paralela de dimensionamento).

## Decisão recomendada
Fechar ciclo. Housekeeping via helper já aplicado (stub + completed byte-idêntico, --check OK).
