# Handoff — QA/Quality Analyzer

## VEREDITO: APROVADO

BLK-SCORE-01a entregue conforme escopo. Todas as 4 validações obrigatórias
re-executadas pelo QA passam; o arquivo novo está 100% verde (32 passed = 21
originais + 11 novos), a suíte completa não regride (602 passed, 1 skipped), o
build roda exit 0 e regenera os 2 artefatos com cobertura EngCorpo **37/61
(60.7%)** — materialmente acima do baseline 31/61 (+6 unidades, +19%). Guardrails
READ-ONLY sobre o M1 confirmados; helpers compartilhados Ultra/Skyfit intocados;
parâmetros canônicos preservados. Nenhum problema crítico ou médio. 1 observação
leve (semântica do housekeeping `--check`, esperada — não é defeito).

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Orquestrador (fechamento do ciclo: commit por path + move do bloco no backlog)

## Bloco auditado
BLK-SCORE-01a — Melhorar match de nome do Engenharia do Corpo

## Saída literal das validações (re-executadas pelo QA)

### `python -m pytest -q tests/unit/test_validation_dataset.py`
```
................................                                         [100%]
32 passed in 1.03s
```

### `python -m pytest --collect-only -q tests/unit/test_validation_dataset.py` (conferência de contagem)
```
tests/unit/test_validation_dataset.py::test_audit_report_sem_pii
tests/unit/test_validation_dataset.py::test_load_engcorpo_fallback_staging
tests/unit/test_validation_dataset.py::test_load_engcorpo_nao_casado_marcado_nunca_descartado

32 tests collected in 0.04s
```
32 confirmado (21 originais + 11 novos EngCorpo). A afirmação do Builder bate; a
imprecisão de contagem que ocorreu no BLK-SCORE-01 NÃO se repetiu aqui.

### `python -m pytest -q` (suíte completa)
```
602 passed, 1 skipped, 9 warnings in 91.38s (0:01:31)
```
Reconciliação: baseline real 591 passed/1 skipped + 11 novos EngCorpo = 602. Sem
regressão. (Os 9 warnings são pré-existentes — CRS geográfico em
`fase_a_censo2022_setores.py`, alheios a este bloco.)

### `python -m analysis.build_validation_dataset`
```
[build_validation_dataset] dataset: 441 linhas -> ...\data\analysis\dataset_validacao.parquet
[build_validation_dataset] score_dominio: presente (500 hexes)
  ultra: entrada=54 linhas=54 hex_ok=53 (98.1%) alunos_nulos=0
  skyfit: entrada=326 linhas=326 hex_ok=301 (92.3%) alunos_nulos=28
  engcorpo: entrada=61 linhas=61 hex_ok=37 (60.7%) alunos_nulos=3
[build_validation_dataset] relatorio -> ...\data\analysis\relatorio_auditoria_rotulo.md
EXIT=0
```
Exit 0; regenerou os 2 artefatos. Cobertura EngCorpo **37/61 (60.7%)** — bate com
o Builder. Por hex_origem (do relatório regenerado): nome_exato=34,
cidade_centroide=3, nao_resolvido=24, **0 fuzzy** (nenhum falso-positivo nos dados
reais). Scores disponíveis EngCorpo (prio/setor/residual/dominio): 36/37/37/4.

### `python -c "import streamlit_app; print('import ok')"`
```
... (warnings esperados de "No runtime found" / "missing ScriptRunContext" em bare mode) ...
import ok
EXIT=0
```

### Housekeeping — `python scripts/housekeeping_move_block.py BLK-SCORE-01a --check`
```
FALHA --check: stub ausente no backlog para BLK-SCORE-01a
EXIT=1
```
INTERPRETAÇÃO (não é defeito): o modo `--check` chama `verify_moved`, que valida o
estado **PÓS-move** (exige um STUB de 1 linha no backlog + o bloco já em
completed.md). Como o move só é feito pelo ORQUESTRADOR no fechamento, ANTES do
move o bloco completo ainda está no backlog (heading `### BLK-SCORE-01a`, linhas
48-111), então `--check` corretamente retorna FALHA/exit 1. Para comparação,
`--check BLK-SCORE-01` (já mergeado/movido) retorna `OK ... exit 0` — confirmando
que a semântica do helper é "verificador de pós-move", não "reconhecedor de
pré-move". O `--check` exit 0 citado no enunciado para o BLK-SCORE-01 refere-se a
um bloco que JÁ tinha sido movido; o equivalente aqui só ocorrerá após o move do
orquestrador. **Dry-run em memória confirmou** que `move_block('BLK-SCORE-01a')`
encontra o bloco (4323 bytes), remove o heading do backlog e o insere em
completed.md — ou seja, o move futuro funcionará.

### `python -m pytest -q tests/unit/test_housekeeping_helper.py`
```
..........                                                               [100%]
10 passed in 0.04s
```
`pytest -q` (suíte completa, acima) inclui esse arquivo — 10 testes verdes.

## Conferência de no-bypass
- A tarefa é uma suíte pytest com fixtures sintéticas (xlsx/csv/parquet escritos
  em `tmp_path`) + um caminho real (`python -m analysis.build_validation_dataset`
  contra as fontes locais reais gitignored). Re-executei AMBOS de verdade: a suíte
  E o build real. Não houve bypass: as fixtures são prática legítima de teste (o CI
  não tem as fontes reais), e o caminho real foi exercido de fato contra
  `academias_engenharia_do_corpo.xlsx` + `unidades_engenharia_do_corpo.csv` +
  staging, produzindo 441 linhas / 61 EngCorpo / 37 resolvidos.
- "Rodou sem erro" NÃO foi aceito como prova: validei números, distribuições e
  invariantes lendo o parquet e o relatório regenerados.

## Conferência cruzada com o log do Builder
| Afirmação do Builder | Verificado pelo QA | Resultado |
|---|---|---|
| arquivo novo 32 passed (21+11) | re-executado + collect-only | OK (32) |
| suíte 602 passed, 1 skipped (591+11) | re-executado | OK (602/1) |
| build exit 0, 2 artefatos | re-executado | OK |
| EngCorpo 37/61 (60.7%) | stdout + relatório + parquet | OK |
| nome_exato=34, cidade_centroide=3, nao_resolvido=24, 0 fuzzy | relatório + parquet | OK |
| 61 entrada == 61 saída | parquet (len ec==61) | OK |
| não-casados marcados, nunca descartados | parquet (24 nao_resolvido → casado=False E hex_resolvido=False) | OK |
| Ultra 53/54, Skyfit 301/326 (anti-regressão) | stdout + parquet | OK (idêntico ao baseline) |
| import streamlit_app ok | re-executado | OK |
Nenhuma divergência entre o log do Builder e a re-execução do QA.

## Pontos de auditoria específicos
1. **"materialmente acima de 31/61":** 37/61 = +6 unidades, +19% sobre o baseline.
   O critério do backlog é "materialmente acima do baseline (31/61)", não "≥38".
   ATENDE. (O ~38 do backlog era referência aproximada; ficou a 1 unidade dela, e
   só não chega lá porque os 24 restantes são bairros sem mapeamento bairro→cidade
   — fora de escopo. Severidade: nenhuma.)
2. **0 fuzzy acionado:** confirmado no relatório E no parquet. Os 24 não-resolvidos
   são bairros (ex.: `EC - ESPLANADA, RS`) cuja cidade real só existe via
   bairro→cidade (fora de escopo). NÃO é bug: os 24 estão MARCADOS
   (`rotulo_casado=False` E `hex_resolvido=False`, ambos all-False confirmado por
   leitura do parquet), nunca descartados (61 entrada == 61 saída). Conservador por
   desenho anti-falso-positivo.
3. **Anti-falso-positivo (Tier 2 exige cidade+UF):** confirmado por leitura de
   código (linhas 477-481: aceita o candidato fuzzy só se
   `_cidade_norm` E `_uf` baterem E `_cidade_norm` não-vazio; senão cai ao Tier 3)
   E por teste: `test_match_engcorpo_coords_fuzzy_rejeitado_quando_cidade_ou_uf_divergem`
   (linhas 189-202) usa desf RS / coords SC com nomes quase iguais e asserta
   `hex_origem != "nome_fuzzy"` E `== "nao_resolvido"`. Existe e testa de fato.
4. **Anti-regressão (helpers compartilhados):** `git diff` mostra APENAS `+def`
   aditivos (`normalize_name_engcorpo`, `_city_uf_from_engcorpo_name`,
   `match_engcorpo_coords`); ZERO `-def`. `normalize_name`/`normalize_name_skyfit`/
   `match_skyfit_coords`/`_city_uf_from_coord_name` intocados. Hunks confinados a:
   constantes (l.51), novos helpers, `match_engcorpo_coords`, reescrita de
   `load_engcorpo`, e args EngCorpo no `main`. `load_ultra`/`load_skyfit`/
   `join_scores` não tocados. Ultra 53/54 e Skyfit 301/326 idênticos ao baseline.
5. **Determinismo:** `test_match_engcorpo_coords_determinismo` roda o match 2x e
   compara hex_origem + lat. Lookups por dict na ordem do arquivo (`keep="first"`,
   `if nm not in by_name`), sem seed, sem ordenação ambígua. OK.
6. **READ-ONLY M1:** `git status --short` →
   `M CLAUDE.md` (pré-sujo, intocado), `M analysis/build_validation_dataset.py`,
   `M context/handoff.md`, `M tasks/current_task.md`,
   `M tests/unit/test_validation_dataset.py`, e 3 `?? context/handoff/*.md` de
   fluxo. NENHUM path M1 (`data/outputs/*`, `src/`, `constants.py`, `scoring.py`,
   `brasil_*`, `hexagonos_*`, `carteira_*`, `plano_*`). `git check-ignore`
   confirma `data/analysis/dataset_validacao.parquet` e
   `data/analysis/relatorio_auditoria_rotulo.md` gitignored (exit 0).
7. **PII:** `relatorio_auditoria_rotulo.md` só contém agregados (counts por
   hex_origem/hex_precisao, min/mediana/max de alunos) — nenhum nome de unidade.
   Teste `test_audit_report_sem_pii` reforça. OK.

## Guardrails verificados
- `score_priorizacao`/`hex_score_estrutural` NÃO alterados: sim (nenhum recálculo;
  só leitura/join por hex_id; `join_scores` intocado).
- Artefatos oficiais do M1 preservados: sim (git status sem nenhum path M1).
- Parâmetros canônicos preservados: `H3_RESOLUTION=7` e `DEFAULT_FUZZY_CUTOFF=0.84`
  inalterados (diff não os toca); demais canônicos (DIST_MIN_ULTRA_KM, RENDA_MIN,
  pesos 0.40/0.60) fora do escopo deste módulo e intocados.
- Saída só em `data/analysis/` (nunca `data/outputs/`), ambos gitignored: sim.
- CSV lido com `sep=";"`, `encoding="utf-8-sig"`: sim (l.635).
- Escopo NÃO excedido: só EngCorpo; nenhuma expansão a BLK-SCORE-02/03; `CLAUDE.md`
  não tocado pelo Builder (continua o `M` pré-sujo).

## Problemas por severidade
- Crítico (bloqueador): nenhum.
- Médio: nenhum.
- Leve: o housekeeping `--check` retorna exit 1 AGORA — esperado/correto, pois é
  verificador de pós-move e o move é do orquestrador. Documentado acima; nenhuma
  ação necessária do Builder.

## Decisão recomendada
**FECHAR O CICLO.** Orquestrador procede com commit por path (NUNCA `git add -A`;
`CLAUDE.md` NÃO entra) e executa o move real
`python scripts/housekeeping_move_block.py BLK-SCORE-01a --date 2026-05-31`
(dry-run em memória já validado pelo QA). Sem bloco de correção; sem reabertura.
