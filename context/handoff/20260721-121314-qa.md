# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (orquestrador: housekeeping 6.0 + commit por path) → **GATE VISUAL do Vinicius**
sobre os PDFs de `data/reports/scratch/blk_relpon_09/`.

## Bloco auditado
BLK-RELPON-09 — Indicador de concorrente = logo quadrada nos PDFs (Pontual + Municipal).
Criticidade Média. Branch `ciclo/BLK-RELPON-09`, baseline do ciclo `f7bd08b`.

## VEREDITO
**APROVADO**

## Justificativa
Todas as validações foram re-executadas por evidência própria e passam: a suíte FULL fecha em
**1935 passed / 2 skipped** com **1 única falha, PROVADA pré-existente no baseline `f7bd08b`** (parquet
de staging gitignored ausente, módulo `lifetime/`, sem relação com o bloco). As duas restrições duras
foram verificadas mecanicamente: `_render_pin_tile`/`build_icon_atlas` têm **hash de fonte idêntico**
ao esperado e o `git diff -U0` de `competitors.py` é um **hunk único de APPEND** (`@@ -617,0 +618,124 @@`).
Sobre o NO-BYPASS, fui além da leitura: **testei por mutação** o teste reescrito e ele falha nas 3
mutações (âncora na base, marcador retangular, marcador ausente), enquanto **o teste ANTIGO passava
com ZERO marcadores desenhados** — a reescrita é estritamente mais forte, não enfraquecida.

## Problemas críticos (bloqueadores)
- **nenhum.**

## Problemas médios (não bloqueadores)
- **`docs/arquitetura_app_atual.md:269` ficou factualmente desatualizado**: diz
  `- Deps: competitors (_render_pin_tile), utils (score_band_to_color)` na seção do **Relatório
  Municipal**, que agora depende de `_render_square_logo_tile`. O Builder detectou e **corretamente
  NÃO ampliou o escopo** (o Planner autorizou apenas `docs/relatorio_pontual_censitario.md:176`).
  Correção trivial de 1 linha — sugerido para o fechamento do ciclo ou bloco de doc próprio.

## Melhorias opcionais
- `docs/refatoracao/review.md:925` e `docs/refatoracao/findings.json:789` também citam o import de
  `_render_pin_tile` por `relatorio_municipal.py:44`/`censo_map.py:24`. **Recomendo NÃO alterar**:
  são artefatos CONGELADOS de uma revisão passada (evidência histórica datada), não docs vivos.
- Drift de docstring **pré-existente** (não deste bloco): `tests/unit/test_relatorio_municipal.py:4`
  e o comentário da linha 390 dizem "8 páginas / `/Count 8`", mas as asserções reais (linhas 402,
  451, 517, 715, 729, 774) exigem `/Count 9`. Os PDFs de amostra têm de fato 9 páginas
  (Municipal) e 6 (Pontual). Nada foi tocado aqui pelo bloco; vale um ajuste de texto algum dia.

## Testes faltantes
- **nenhum.** A cobertura nova é adequada e, no ponto mais sensível, superior ao pedido: além dos
  5 testes do plano em `test_ultra_pins.py` (incluindo o **guarda permanente** de `_render_pin_tile`
  128x128), o teste do Pontual virou diferencial estrutural e o do Municipal trava o footprint
  quadrado. Verifiquei por mutação que ambos são não-vacuosos.

## Riscos remanescentes
1. **Gate visual do Vinicius (pendente, é o gate deste bloco).** Pontos a olhar com atenção — ver
   seção dedicada abaixo.
2. **Colisão de marcadores no Municipal de cidades densas** (São Paulo). Já existia com o balão de
   34 px; o quadrado de 26 px reduz a área do marcador. Reversível em 1 linha (`_PIN_LOGO_PX`).
3. **Sombra `(0,0,0,60)` some sobre basemap escuro.** Aceito no plano; a defesa primária de contraste
   é a borda branca de 2 px e o basemap de produção é o Voyager CLARO (DEC-004).
4. **Housekeeping do bloco ainda NÃO foi feito** (`--check` falha por stub ausente) — é o Passo 6.0
   do orquestrador, posterior ao QA, e o Planner marcou `tasks/backlog.md`/`completed.md` como
   "NÃO tocar" pelo Builder. Não é defeito; é ordem de esteira.

---

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte FULL — gate único do ciclo (SERIAL, `python -m pytest -q`)

Comando usado: **`python -m pytest -q`** (serial). Não usei `-n auto`: o `pytest-xdist` aborta neste
ambiente (Windows / Python 3.14) com `INTERNALERROR/execnet`, defeito de infraestrutura já
documentado no repo e antecipado pelo Planner (risco 5).

```
=========================== short test summary info ===========================
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 1935 passed, 2 skipped, 311752 warnings in 1839.00s (0:30:39)
```

### 1-bis. A única falha é PRÉ-EXISTENTE (regressão descartada por evidência própria)

Causa raiz (teste isolado):

```
$ python -m pytest -q tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
E   FileNotFoundError: [Errno 2] No such file or directory:
    'C:\...\motor-de-expansao\data\staging\unidade_territorio_retencao.parquet'
1 failed, 651 warnings in 5.23s
```

Insumos gitignored ausentes localmente (confirmado):

```
$ ls -la data/staging/score_retencao_territorial.parquet
ls: cannot access 'data/staging/score_retencao_territorial.parquet': No such file or directory
$ ls -la data/staging/growth_api_historico.parquet
ls: cannot access 'data/staging/growth_api_historico.parquet': No such file or directory
```

**Prova de que já falhava no baseline `f7bd08b`** (stash dos 7 arquivos → árvore idêntica ao baseline
→ mesmo teste → mesma falha → stash pop):

```
$ git stash push -m "QA-baseline-check-BLK-RELPON-09" -- <os 7 arquivos do ciclo>
Saved working directory and index state On ciclo/BLK-RELPON-09: QA-baseline-check-BLK-RELPON-09

$ git diff --stat f7bd08b
(vazio = árvore identica ao baseline)

$ python -m pytest -q tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 651 warnings in 3.54s

$ git stash pop
Dropped refs/stash@{0} (17183dd5c0f8a5909ed03fbfe774699d77f46460)
```

Árvore restaurada e hashes protegidos re-conferidos APÓS o `pop` (nada se perdeu no roundtrip):

```
$ git diff --stat f7bd08b
 docs/relatorio_pontual_censitario.md               |   2 +-
 src/motor_expansao/dashboard/censo_map.py          |  23 ++--
 src/motor_expansao/dashboard/competitors.py        | 124 +++++++++++++++++++++
 .../dashboard/relatorio_municipal.py               |  34 ++++--
 tests/unit/test_relatorio_municipal.py             |  36 ++++++
 .../unit/test_relatorio_pontual_censitario_mapa.py | 113 ++++++++++++++-----
 tests/unit/test_ultra_pins.py                      |  86 ++++++++++++++
 7 files changed, 372 insertions(+), 46 deletions(-)

_render_pin_tile 3fe49e2618b25c15768e035493e49539484fd3840c0bbc938346c39bcf50ece2
build_icon_atlas e0fb871bc5fdb91ff61f4fef060fc62929f75c4bfaf988bff7cfe06cb025ea8f
```

**Conclusão: 0 regressões do ciclo.** A falha é ambiental (dado de staging gitignored ausente na
máquina local), no pacote `lifetime/`, que este bloco não toca.

### 2. Restrição dura — `_render_pin_tile` e `build_icon_atlas` INTOCADOS

**(a) Hash de fonte** — idêntico ao esperado pelo Planner (`3fe49e26…ece2` / `e0fb871b…ea8f`):

```
$ python -c "import hashlib,inspect,sys; sys.path.insert(0,'src'); from motor_expansao.dashboard import competitors as c; [print(f.__name__, hashlib.sha256(inspect.getsource(f).encode('utf-8')).hexdigest()) for f in (c._render_pin_tile, c.build_icon_atlas)]"
_render_pin_tile 3fe49e2618b25c15768e035493e49539484fd3840c0bbc938346c39bcf50ece2
build_icon_atlas e0fb871bc5fdb91ff61f4fef060fc62929f75c4bfaf988bff7cfe06cb025ea8f
```

**(b) `git diff -U0` de `competitors.py` — hunk ÚNICO, nenhum dentro de 504-618:**

```
$ git diff -U0 -- src/motor_expansao/dashboard/competitors.py | grep "^@@"
@@ -617,0 +618,124 @@ def build_icon_atlas(

$ git show f7bd08b:src/motor_expansao/dashboard/competitors.py | wc -l
617
$ wc -l < src/motor_expansao/dashboard/competitors.py
741
```

`-617,0` = **zero** linhas removidas ou alteradas; a inserção começa em 618, isto é, **após** o fim do
arquivo original (617 linhas). É APPEND puro. Requisito **CUMPRIDO**.

**(c) Testes de atlas em `test_ultra_pins.py` sem NENHUMA asserção alterada** — o arquivo tem
exatamente 2 hunks (imports + append no fim), logo tudo entre a linha 14 e a 326 está intacto:

```
$ git diff -U0 -- tests/unit/test_ultra_pins.py | grep "^@@"
@@ -13,0 +14,2 @@ from motor_expansao.dashboard.competitors import (
@@ -326,0 +329,84 @@ def test_build_ultra_icon_layer_usa_atlas():

$ grep -n "== 128\|anchorY" tests/unit/test_ultra_pins.py
121:    assert "anchorY" in icon
269:    retorna atlas data-URI PNG nao-vazio e mapping com x/y/width/height/anchorY."""
282:    for key in ("x", "y", "width", "height", "anchorY"):
284:    assert tile["width"] == 128
285:    assert tile["height"] == 128
286:    assert tile["anchorY"] == 122
302:    assert mapping["__ultra__"]["anchorY"] == 122
```

`width==128`, `height==128`, `anchorY==122` presentes e **fora de qualquer hunk**. Verdes na suíte FULL.

### 3. NO-BYPASS — o teste reescrito é ESTRITAMENTE MAIS FORTE (verificado por mutação)

**(a) Nenhum enfraquecimento sintático:**

```
$ git diff | grep "^+.*\(skip\|xfail\|pytest\.mark\)"
(nenhum skip/xfail adicionado)

$ git diff | grep -c "^-.*assert"     # asserts removidos
1
$ git diff | grep -c "^+.*assert"     # asserts adicionados
23
```

O único `assert` removido é justamente o fraco (`assert reds > 0`); entram 23.

**(b) O teste ANTIGO era quase VÁCUO** — renderizei a camada `concorrentes` **sem nenhum concorrente
e sem nenhuma Ultra** e apliquei o critério antigo:

```
Criterio do teste ANTIGO (reds>0) numa imagem SEM NENHUM marcador de concorrente/Ultra:
  reds = 1 -> PASSARIA (teste era quase vacuo)
```

O vermelho vinha do **pin central** `(220,38,38)`, que satisfaz `r>150 and g<80 and b<80`. Ou seja:
o teste antigo **passaria mesmo que os marcadores nunca fossem desenhados**.

**(c) O teste NOVO falha em 3 mutações independentes** (mutação em runtime, sem tocar o repo):

```
CONTROLE (codigo entregue, esperado PASSAR)   : PASSOU
M1 ancora na BASE (esperado FALHAR)           : FALHOU (AssertionError) na linha 251: assert abs(((ys.min() + ys.max()) / 2) - ponta_cy) <= 3
M2 marcador RETANGULAR 30x18 (esperado FALHAR): FALHOU (AssertionError) na linha 227: assert 1200 <= int(mask.sum()) <= 2600
M3 sem marcador nenhum (esperado FALHAR)      : FALHOU (AssertionError) na linha 227: assert 1200 <= int(mask.sum()) <= 2600
```

M1 quebra exatamente a asserção que trava a decisão **S2b** (âncora centrada). O teste do Municipal
recebeu o mesmo tratamento, mutando para o balão 34x34 antigo:

```
CONTROLE (codigo entregue, esperado PASSAR)    : PASSOU
MUT balao 34x34 antigo (esperado FALHAR)       : FALHOU (AssertionError) na linha 382: assert 250 <= int(mask.sum()) <= 700
```

**Julgamento: o teste foi REESCRITO com critério muito mais forte, não enfraquecido.** Prova, de forma
determinística, (a) que os 2 marcadores existem, (b) que são 2 clusters separados, (c) footprint
quadrado 26-34 px, (d) âncora centrada e (e) identidade de marca por lado.

### 4. READ-ONLY sobre o M1 (§5)

**mtime dos artefatos oficiais, medido pelo QA APÓS a suíte FULL e as mutações:**

```
2026-06-10 09:50:32  data/staging/brasil_estrutural.parquet
2026-06-10 09:50:32  data/staging/brasil_priorizados.parquet
2026-06-10 09:50:34  data/staging/hexagonos_brasil_oportunidades.parquet
2026-06-10 10:24:10  data/outputs/hexagonos_brasil_dashboard.parquet
2026-06-10 10:24:11  data/outputs/hexagonos_mapa_sample.parquet
2026-06-10 10:24:10  data/outputs/top_oportunidades_resumo.csv
2026-06-10 10:24:10  data/outputs/resumo_por_uf.csv
HOJE: 2026-07-21 12:12:58
```

Todos de **2026-06-10** — nenhuma escrita hoje, nem pelo Builder nem pela minha re-execução.

```
$ git status --porcelain -- data/ config.py src/motor_expansao/pipelines/
(NENHUMA -- limpo)
```

Scratch do gate visual devidamente ignorado:

```
$ git check-ignore -v data/reports/scratch/blk_relpon_09/relatorio_pontual_amostra.pdf
.gitignore:76:data/reports/scratch/	data/reports/scratch/blk_relpon_09/relatorio_pontual_amostra.pdf
```

### 5. Lint e Tipos

```
$ python -m ruff check src/motor_expansao/dashboard/competitors.py src/motor_expansao/dashboard/censo_map.py src/motor_expansao/dashboard/relatorio_municipal.py tests/unit/test_ultra_pins.py tests/unit/test_relatorio_pontual_censitario_mapa.py tests/unit/test_relatorio_municipal.py
All checks passed!
exit=0

$ python -m mypy src/motor_expansao/dashboard/competitors.py src/motor_expansao/dashboard/censo_map.py src/motor_expansao/dashboard/relatorio_municipal.py
Success: no issues found in 3 source files
exit=0
```

### 6. Smoke de import

```
$ python -c "import sys; sys.path.insert(0,'src'); import streamlit_app; from motor_expansao.dashboard import censo_map, relatorio_municipal, competitors; print('ok')"
ok
```

(Só os WARNINGs esperados de `streamlit.runtime` fora de runtime.)

### 7. Escopo — exatamente os paths do ciclo, nada arrastado

```
$ git status --porcelain
 M docs/relatorio_pontual_censitario.md
 M src/motor_expansao/dashboard/censo_map.py
 M src/motor_expansao/dashboard/competitors.py
 M src/motor_expansao/dashboard/relatorio_municipal.py
 M tests/unit/test_relatorio_municipal.py
 M tests/unit/test_relatorio_pontual_censitario_mapa.py
 M tests/unit/test_ultra_pins.py
?? context/handoff/20260721-103320-block-orchestrator.md
?? context/handoff/20260721-104742-planner.md
?? context/handoff/20260721-113812-builder.md

$ git status --porcelain | grep -vE "<os 7 paths do ciclo>|context/handoff"
(NENHUM -- escopo respeitado)
```

**A mudança em `docs/relatorio_pontual_censitario.md` é de fato SÓ a linha 176**, como autorizado
pelo Planner:

```
$ git diff --numstat -- docs/relatorio_pontual_censitario.md
1	1	docs/relatorio_pontual_censitario.md

$ git diff -U0 -- docs/relatorio_pontual_censitario.md | grep "^@@"
@@ -176 +176 @@ Regras implementadas:
```

Sem resíduo do crop acoplado à geometria do balão:

```
$ grep -rn "37, 20, 91, 74" --include=*.py .
(nenhum)
```

### 8. Acentuação (CLAUDE.md §2)

```
$ git diff -- src/ | grep "^+" | grep -iE "st\.(caption|markdown|info|warning|success|metric|write)|help=|label=|title=|_ascii|pdf\.(cell|text|multi_cell)"
(nenhuma string nova voltada ao usuario -- o bloco nao adiciona texto ao PDF nem ao dashboard)
```

Único caractere fora de latin-1 nas linhas adicionadas de `src/` é o box-drawing `─` do **comentário
de seção** `# ── logo quadrada para relatorios (BLK-RELPON-09) ──`. **Não é violação:** (i) é
comentário Python, nunca vira string e nunca chega ao `fpdf2`; (ii) é a convenção JÁ VIGENTE no
arquivo e no repo:

```
$ git show f7bd08b:src/motor_expansao/dashboard/competitors.py | grep -c "# ──"
6      # ja existiam 6 separadores identicos no baseline, no MESMO arquivo
```

As docstrings novas usam `--` ASCII em vez de travessão, como manda a regra. **Identificadores todos
sem acento** (`_SQUARE_LOGO_RADIUS`, `_SQUARE_LOGO_BORDER_PX`, `_SQUARE_LOGO_SHADOW_PX`,
`_SQUARE_LOGO_SHADOW_RGBA`, `_PIN_LOGO_PX`, `_REDE_LOGO_RASTER_PX`, `_render_square_logo_tile`).
Texto de doc (`docs/relatorio_pontual_censitario.md`) segue o padrão do arquivo.

### 9. Julgamento dos 3 desvios registrados pelo Builder — nenhum enfraquece asserção

**(a) Pixel-sonda da sombra movido de `(29,29)` para `(29,15)` — LEGÍTIMO, medido por mim:**

```
DESVIO 1 -- pixel-sonda da sombra:
  (29,29) = (0, 0, 0, 0)      <- vertice arredondado pelo radius=3 (o plano estava errado)
  (29,15) = (0, 0, 0, 60)     <- meio do lado direito (usado pelo Builder)
  (15,29) = (0, 0, 0, 60)     <- meio do lado inferior (simetrico, confirma)
  (15,15) = (255, 255, 255, 255)
```

O pixel do plano é **transparente** porque `radius=3` arredonda o canto — a asserção do plano
falharia por erro do plano, não do código. O pixel escolhido devolve exatamente `(0,0,0,60)` e a
asserção `0 < alpha < 255` continua provando **o mesmo fato** (sombra presente e translúcida), num
pixel que existe. Correção de plano, não enfraquecimento.

**(b) `_MINIMAL_PNG` é stream quebrado → helper `_valid_png()` — LEGÍTIMO e tornou o teste MAIS
FORTE. Verificado por mim:**

```
DESVIO 2 -- _MINIMAL_PNG do teste e decodificavel pelo Pillow?
  FALHA: OSError : broken data stream when reading image file

E o helper novo _valid_png()?
  DECODIFICA OK, size= (8, 8) cor= (0, 128, 255, 255)
```

Com `_MINIMAL_PNG` a função cairia no `except` → fallback de sigla → a asserção "cor de marca
ausente" falharia. O helper novo permitiu **acrescentar** uma asserção que o plano não tinha: além de
`(200,0,30,255)` ausente e branco presente, exige a **cor da própria logo** `(0,128,255,255)` no
tile. `_MINIMAL_PNG` e os testes que o usam ficaram **intocados** (só há adição de `_valid_png`).

**(c) `zip(..., strict=True)` (B905) — LEGÍTIMO.** É exigência do `ruff` e, se algo, **fortalece**:
`strict=True` levanta em vez de truncar silenciosamente comprimentos díspares.

## Conferência de no-bypass
**Confirmo:** nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, nem mock do
caminho crítico. A suíte rodou completa e serial contra o código entregue. As fixtures usadas nos
testes unitários (`_valid_png`, `_sample_df`, `_ICON_CACHE.pop`) são isolamento legítimo de teste
unitário — explicitamente **fora** do escopo da regra de bypass (que mira tooling contra config real
de produção), e o próprio caminho de produção foi exercitado de ponta a ponta pelo Builder no Passo 7
(PDFs reais, dado real, logos reais, basemap ONLINE). Reforcei o rigor com **testes de mutação**,
que provam que o verde não vem de asserções vazias.

## Conferência cruzada com log do Builder
**Bate integralmente.** Reproduzi por conta própria: os 2 hashes, o hunk único `@@ -617,0 +618,124 @@`,
`ruff` `All checks passed!`, `mypy` `Success: no issues found in 3 source files`, o smoke `ok`, os
mtimes de 2026-06-10 e os 3 desvios (todos confirmados empiricamente). A contagem do subconjunto do
Builder (137 passed) é consistente com a suíte FULL. A única divergência de **rótulo** (não de fato):
o Builder listou 2 menções desatualizadas a `_render_pin_tile` em docs fora de escopo; são **3**
(`docs/arquitetura_app_atual.md:269`, `docs/refatoracao/review.md:925` e
`docs/refatoracao/findings.json:789`) — e destas só a primeira é doc vivo.

## Guardrails verificados
- `score_priorizacao` não alterado: **sim** (bloco 100% de render; nenhum arquivo de score/pipeline
  tocado; mtime dos oficiais em 2026-06-10).
- `hex_score_estrutural`, pesos `renda=0.40`/`pop=0.60`: **inalterados**.
- Parâmetros canônicos §3 (`H3_RESOLUTION=7`, `DIST_MIN_ULTRA_KM=1.0`, `RENDA_MIN=4500.0`):
  **inalterados** (`config.py` sequer aparece no `git status`).
- Artefatos M1 preservados: **sim** (evidência de mtime própria, pós-suíte).
- `flag_sam`, scores censitários, carteira, plano, plano de domínio: **não recalculados**.
- `_render_pin_tile` / `build_icon_atlas` intocados: **sim** (hash + append puro + testes de atlas
  fora de qualquer hunk + teste-guarda novo).
- Dashboard offline mantido: **sim** — nenhuma dependência de rede nova; o fetch de tiles continua
  restrito à GERAÇÃO de relatório (DEC-004/DEC-011), lazy, com fallback offline intacto.
- Acentuação (§2): **conforme** (nenhuma string nova ao usuário; identificadores sem acento;
  box-drawing só em comentário, convenção pré-existente).
- Validações re-executadas pelo QA (sem bypass):
  - `python -m pytest -q` (FULL, serial) → **1 failed (pré-existente), 1935 passed, 2 skipped**
  - hash das 2 funções protegidas → **idêntico ao esperado**
  - `git diff -U0 competitors.py` → **hunk único `@@ -617,0 +618,124 @@`**
  - `python -m ruff check <6 arquivos>` → **All checks passed!**
  - `python -m mypy <3 arquivos>` → **Success: no issues found in 3 source files**
  - smoke `import streamlit_app` + 3 módulos → **ok**
  - mutação do teste do Pontual (3 mutações) → **3/3 falham como esperado**
  - mutação do teste do Municipal (balão 34x34) → **falha como esperado**
  - baseline `f7bd08b` via stash → **a única falha da suíte já existia**
- Escopo respeitado: **sim** — 7 arquivos, exatamente os paths de `tasks/current_task.md`; doc
  alterado em 1 linha (176), como autorizado.

## Housekeeping de concluídos
**Modo MERGE-HUMANO. PENDENTE — e isso é esperado nesta etapa, não é defeito.**

```
$ python scripts/housekeeping_move_block.py BLK-RELPON-09 --check
FALHA --check: stub ausente no backlog para BLK-RELPON-09
```

O Planner marcou `tasks/backlog.md`/`tasks/completed.md` como **"NÃO tocar"** pelo Builder, e o
Passo 6.0 é do orquestrador, **posterior ao QA**. Registro aqui para o fechamento: o orquestrador
precisa rodar o helper e obter `--check` OK **antes do merge**, com byte-identidade do bloco movido
contra `git show HEAD:tasks/backlog.md`.

---

## Para o GATE VISUAL do Vinicius — o que olhar com atenção (o QA NÃO substitui este gate)

Amostras válidas e íntegras (conferi header, tamanho e páginas):

```
relatorio_pontual_amostra.pdf:            1.625.872 bytes | %PDF-1.4 | 6 paginas
relatorio_municipal_amostra.pdf:            617.545 bytes | %PDF-1.4 | 9 paginas
relatorio_municipal_amostra_campinas.pdf:   534.299 bytes | %PDF-1.4 | 9 paginas
pontual_concorrentes.png (1280,760) + 4 choropleths (1280,760)
municipal_resumo.png / _campinas.png (1000,704) | _crop_rede_logos_64.png (320,64)
```

Pontos que **só o olho humano decide** e que eu deliberadamente não julguei:

1. **Legibilidade da logo a 30 px (Pontual) / 26 px (Municipal)** sobre o choropleth translúcido.
   É o critério nº 1 e o motivo de existir do bloco.
2. **Colisão de marcadores no Municipal de São Paulo** (296 hexes, pior caso). Comparar com
   `municipal_resumo_campinas.png`, que é o caso típico. Reversão custa 1 linha (`_PIN_LOGO_PX`).
3. **Âncora centrada (S2b)** frente ao pin vermelho central em `_crop_pins.png`. Se preferir a
   âncora na base, a reversão é `- size // 2` → `- size` nos 2 call-sites **+ ajustar a banda
   vertical do teste** (linha 251 de `test_relatorio_pontual_censitario_mapa.py`) — o teste está
   travado nessa decisão de propósito, então ele vai falhar até o ajuste. Isso é feature, não bug.
4. **Página "Concorrentes por rede" do Municipal** (`_crop_rede_logos_64.png`): logo a 64 px **sem
   keyline e sem sombra** sobre página branca; conferir se o fallback de sigla (card na cor da
   marca) fica aceitável para redes sem logo.
5. **Placa BRANCA atrás da logo**: decisão S2c.2. Confirme que não destoa das logos que já têm fundo
   próprio.

## Decisão recomendada
**Fechar o ciclo.** Ordem sugerida ao orquestrador: (1) housekeeping 6.0 (`--check` OK) →
(2) commit por path → (3) **gate visual do Vinicius** sobre os PDFs → (4) merge.
Follow-up trivial e opcional, a critério do orquestrador: corrigir
`docs/arquitetura_app_atual.md:269` (`_render_pin_tile` → `_render_square_logo_tile` na linha de
Deps do Relatório Municipal). **Nenhum bloco de correção precisa ser criado.**
