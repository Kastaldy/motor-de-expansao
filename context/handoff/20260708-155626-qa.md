# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (housekeeping move BLK-RELMUN-05 + commit por path + merge ciclo/BLK-RELMUN-05 → integracao/map02-relmun05-06)

## VEREDITO
APROVADO (final) — suíte completa autoritativa do orquestrador confirmada: `1 failed, 1536 passed, 2 skipped`. A ÚNICA falha é pré-existente/ambiental do M2 (`test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime`), alheia a este bloco: NÃO está no diff do ciclo e falha por `FileNotFoundError` num parquet gitignored ausente (`data/staging/unidade_territorio_retencao.parquet`). O +1 vs. baseline (1535→1536 passed) é exatamente o novo `test_cores_aprovados_verdes_blk_relmun_05`.

## Justificativa
As mudanças são estritamente de DISPLAY no Relatório Municipal (2 constantes de cor → verde, 2 strings visíveis "amarelos"→"destacados", comentários/docstrings de coerência, emenda de doc), 100% READ-ONLY sobre o M1; identificadores/critério/score/artefatos intactos; ruff/mypy/import/subconjunto de testes verdes por conta própria; suíte completa verde exceto a falha ambiental do M2, comprovadamente não relacionada a este bloco.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- Revisão VISUAL do PDF (não automatizável): confirmar que os 2 tons de verde (20,170,80 vs 90,190,120) são distinguíveis entre si e do cinza (150,156,170) sobre o basemap claro. A distância de luminância entre os 2 verdes é moderada; a diferença de matiz/saturação garante distinção, mas convém a conferência humana registrada no gate D1.

## Testes faltantes
- nenhum (novo `test_cores_aprovados_verdes_blk_relmun_05` trava os 3 RGB + inequalidade; 2 asserts de wording no PDF gerado — significativos, não tautológicos: o assert `b"amarelo" not in pdf_bytes.lower()` prova ausência do texto no PDF descomprimido e `"hexágonos destacados".encode("latin-1") in pdf_bytes` prova a presença do novo wording).

## Riscos remanescentes
- Distinção visual dos 2 verdes — mitigado por constantes distintas + assert de inequalidade + revisão visual do gate D1 (pré-aprovado por Vinicius).

## Saída literal das validações (re-executadas pelo QA)

### `python -m ruff check .`
```
All checks passed!
```

### `python -m mypy src`
```
src\motor_expansao\dimensionamento\growth_api_client.py:28: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\ibge_censo.py:24: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\poi_enrichment.py:15: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\base_h3_brasil.py:31: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\geo.py:21: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\telegram_bot.py:23: error: Library stubs not installed for "requests"  [import-untyped]
Found 6 errors in 6 files (checked 108 source files)
```
→ 6 erros PRÉ-EXISTENTES de `types-requests` (import-untyped), TODOS em arquivos NÃO tocados por este ciclo. ZERO erro novo; ZERO erro em `relatorio_municipal.py`.

### `python -c "import streamlit_app; print('import ok')"`
```
import ok
```
(warnings de ScriptRunContext/MemoryCacheStorageManager pré-existentes do Streamlit em bare mode — inócuos)

### `python -m pytest -q tests/unit/test_relatorio_municipal.py tests/unit/test_relatorio_acentuacao_regressao.py`
```
41 passed, 1861 warnings in 15.08s
```
(warnings = DeprecationWarning pré-existentes de pytest_asyncio no Python 3.14; o 2º arquivo gera o PDF municipal e valida ausência de "?" fora de latin-1 — verde)

### `python scripts/housekeeping_move_block.py BLK-RELMUN-05 --check`
```
FALHA --check: stub ausente no backlog para BLK-RELMUN-05
```
→ ESPERADO no estado PRÉ-move (o QA não faz housekeeping; o move ocorre no fechamento). O helper reconhece o bloco.

### `git diff --stat`
```
 CLAUDE.md                                          |   1 +
 context/handoff.md                                 | 201 ++++++---------------
 src/.../dashboard/relatorio_municipal.py           |  27 +--
 tasks/current_task.md                              |  59 +++---
 tests/unit/test_relatorio_municipal.py             |  14 ++
```
→ Só código-alvo (`relatorio_municipal.py`, `test_relatorio_municipal.py`), doc (`CLAUDE.md` — só a emenda BLK-RELMUN-05 na DEC-011) e bookkeeping (`handoff.md`, `current_task.md`). ZERO parquet; ZERO alteração em `pipelines/m1/`, `config.py`, `censo_point/map/report`, artefatos oficiais.

### `python -m pytest -q` (suíte completa, SERIAL — gate único, executada pelo orquestrador)
```
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 1536 passed, 2 skipped, 241552 warnings in 939.08s
```
→ 1536 passed (o +1 vs. 1535 anteriores = novo `test_cores_aprovados_verdes_blk_relmun_05`). A ÚNICA falha é ALHEIA a este bloco e AMBIENTAL: `git diff --name-only` NÃO inclui `test_score_retencao_territorial.py`; a re-execução isolada pelo QA confirma `FileNotFoundError: ...data\staging\unidade_territorio_retencao.parquet` (parquet gitignored do M2/lifetime, DEC-014, ausente localmente). Não é regressão introduzida pelo BLK-RELMUN-05.

## Conferência dos focos da auditoria
1. **Cores:** confirmado no diff `_COR_APROVADO_PROPRIO = (20,170,80)`, `_COR_APROVADO_MUNICIPAL = (90,190,120)`, `_COR_REPROVADO = (150,156,170)` INALTERADO. Derivados (`_HEX_DESTAQUE_RGBA`/`_HEX_APROVADO_RGBA`/legendas) NÃO aparecem no diff → herdam por referência às constantes-fonte, sem edição manual redundante. OK.
2. **Texto:** exatamente 2 strings visíveis de PDF viraram "destacados" (`_resumo_page` :1657 e `_espaco_academias_page` :2071). `grep -n "amarel"` restante = SÓ identificadores (`soma_oferta_amarelos`/`n_hex_amarelos`/`parcelas_amarelos` :617-676, :1658-1662) + docstrings não user-facing (:10 histórico, :883 dominio-fallback deixado de propósito pois usa cinza-neutro). NENHUMA string de PDF visível "amarelo" remanescente. Identificadores e chaves de `result` INTACTOS.
3. **DEC-011 emenda:** presente no CLAUDE.md (após emenda BLK-RELMUN-03) deixando explícito que é DISPLAY: cor ≠ critério; critério `oferta_efetiva_disponivel >= OFERTA_DESTAQUE_MIN=2000`, identificadores, `flag_sam`/DEC-006/DEC-007, score/pesos/`hex_score_estrutural`/artefatos M1 INTOCADOS.
4. **Escopo READ-ONLY M1:** `git diff --stat` só em relatorio_municipal.py + test + CLAUDE.md + bookkeeping; zero em pipelines/m1, config.py, censo_*, artefatos; nenhum parquet. Cores de ZONA (`_ZONA_CORES_*`, :155-161) NÃO tocadas (não aparecem no diff; única linha "zona" no diff é contexto de docstring/`get("zonas")`).
5. **Testes novos:** `test_cores_aprovados_verdes_blk_relmun_05` trava os 3 RGB exatos + inequalidade (não tautológico); 2 asserts de wording sobre o PDF DESCOMPRIMIDO gerado (significativos).

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico. Testes unitários com fixtures de DataFrame são isolamento legítimo (não é o tooling-em-escopo da regra de no-bypass). A suíte completa é o gate único e roda no orquestrador (não duplicada aqui, para não criar processo órfão).

## Conferência cruzada com log do Builder
Bate. ruff "All checks passed!", mypy 6 pré-existentes / 0 novo, import ok, 41 passed no subconjunto — idênticos ao relatado pelo Builder. Diff restrito aos alvos, sem parquet.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY M1; só cores/texto/doc de display)
- hex_score_estrutural não alterado: sim
- Artefatos M1 preservados: sim (nenhum parquet/artefato tocado)
- Parâmetros canônicos (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, renda=0.40/pop=0.60): intocados (bloco de display)
- Critério de destaque (`oferta_efetiva_disponivel >= 2000`) / `_hex_destacado_mask` / `flag_sam`: intocados
- Cores de ZONA (`_ZONA_CORES_*`): intocadas
- Identificadores com "amarelo": intocados (renomeação zero — grep confirma)
- Validações re-executadas pelo QA (sem bypass): ruff→pass; mypy→6 pré-existentes/0 novo; import→ok; pytest subset→41 passed; housekeeping --check→FALHA esperada pré-move; git diff --stat→escopo ok
- Escopo respeitado: sim

## Decisão recomendada
Fechar ciclo: housekeeping move de BLK-RELMUN-05 (via `scripts/housekeeping_move_block.py`) + commit por path + merge ciclo/BLK-RELMUN-05 → integracao/map02-relmun05-06. Suíte completa confirmada verde (única falha ambiental/M2 alheia). VEREDITO FINAL = APROVADO.
