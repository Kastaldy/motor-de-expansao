# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (Passo 6.0: housekeeping move de BLK-DIM-12 via `scripts/housekeeping_move_block.py`, feito pelo orquestrador APÓS este veredito).

## VEREDITO
APROVADO

## Justificativa
A 5ª aba "Viabilidade do Imovel" (Opção B) reusa o engine BLK-DIM-11 INTOCADO, respeita o guardrail anti-geográfico da DEC-009 (demanda SEMPRE premissa do operador, verificado por código E por teste), é READ-ONLY sobre o M1 (só os 3 arquivos esperados mudaram em 871d3db; zero toque em score/pesos/artefatos/engine/config), é 100% offline (parser de link puro, sem geocoding ao vivo) e preserva a performance (cache 1× + engine só on-submit + render lazy). Todas as validações re-executadas pelo QA passaram: ruff/mypy limpos, import ok, suíte full `955 passed, 1 skipped` (exit 0), 7 novos testes verdes.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- O teste `test_render_viabilidade_ponto_smoke_sem_pino` não mocka `st.form`/`st.number_input` porque o `return` precoce (sem ponto) acontece antes do form; correto. Opcional: um teste do caminho de zona morta True (`flag_zona_morta=True` → `st.warning`) e do modo degradado de catchment (`setores_df=None` → caption) reforçaria cobertura visual, mas o engine já cobre esses ramos no seu próprio suite e a UI só os repassa.

## Testes faltantes
- nenhum bloqueador. Os 7 novos cobrem: guardrail anti-geográfico (DEC-009), toggle p50 não-silencioso, faixa "n/d" sem comparáveis, derivação de `alunos_por_m2` no loader, smoke sem pino (engine não chamado), parse de link Google Maps offline, e registro da aba em `DASHBOARD_TAB_LABELS`.

## Riscos remanescentes
- Baixo — testes de UI mockam `st.form`/`st.expander`/`st.spinner` como context managers; o comportamento real "form só submete no botão" é de runtime, não exercido pelos testes. Mitigado: o teste de guardrail captura o arg real (`demanda`) passado ao engine, provando que o valor da caixa (não da geografia) é o que chega ao motor.
- Baixo — `load_base_calibracao` depende de `data/staging/base_calibracao_multirede.parquet`; ausência → `pd.DataFrame()` vazio → faixa "n/d" sem travar (modo degradado coberto por `test_viabilidade_ponto_faixa_nd_sem_alunos_por_m2`).

## Saída literal das validações (re-executadas pelo QA)

### 1. ruff
```
$ python -m ruff check src/motor_expansao/dashboard/pages.py streamlit_app.py tests/integration/test_streamlit_app.py
All checks passed!
```

### 2. mypy
```
$ python -m mypy src/motor_expansao/dashboard/pages.py streamlit_app.py
Success: no issues found in 2 source files
```

### 3. import smoke
```
$ python -c "import streamlit_app; print('import ok')"
[WARNING streamlit ... missing ScriptRunContext / No runtime found, using MemoryCacheStorageManager  -- ruído esperado em bare mode]
import ok
```

### 4. GATE ÚNICO — suíte completa (-n auto)
```
$ python -m pytest -n auto -q
955 passed, 1 skipped, 222702 warnings in 177.71s (0:02:57)
exit code 0
```
Baseline ≥ `532 passed, 1 skipped` superado. 0 falhas, 0 erros de coleta. Warnings são DeprecationWarning de bibliotecas (pytest_asyncio, fastapi/starlette) e CRS geográfico de pipelines pré-existentes — não relacionados ao ciclo.

### 4b. Isolamento dos novos testes BLK-DIM-12
```
$ python -m pytest -q -k "viabilidade or base_calibracao"
21 passed, 935 deselected, ... in 33.89s
```
Os 7 novos do bloco (confirmados por coleta):
- test_viabilidade_ponto_demanda_e_sempre_premissa_do_operador
- test_viabilidade_ponto_toggle_p50_preenche_campo_editavel
- test_viabilidade_ponto_faixa_nd_sem_alunos_por_m2
- test_load_base_calibracao_deriva_alunos_por_m2
- test_render_viabilidade_ponto_smoke_sem_pino
- test_viabilidade_ponto_parse_link_google_maps
- test_render_viabilidade_ponto_aba_no_dashboard_tab_labels
(os demais "viabilidade" pertencem ao suite do engine BLK-DIM-11, já existente.)

### 5. Housekeeping (Passo 6.0) — PRÉ-move, esperado FALHAR
```
$ python scripts/housekeeping_move_block.py BLK-DIM-12 --check
FALHA --check: stub ausente no backlog para BLK-DIM-12
EXIT=1
```
Estado CORRETO antes do fechamento (stub ainda ausente, heading `### BLK-DIM-12` ainda presente em `tasks/backlog.md` linha 462). O move será feito pelo orquestrador no fechamento. NÃO bloqueia o veredito (instrução explícita do ciclo).
```
$ python -m pytest tests/unit/test_housekeeping_helper.py -q
10 passed, 491 warnings in 0.23s
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico. A suíte rodou contra o código de produção real em HEAD; os testes de UI usam mocks de Streamlit (prática normal e legítima para testar render sem runtime) e o teste de guardrail captura o argumento REAL passado ao engine. `load_base_calibracao` foi testado com um parquet real gravado em `tmp_path` casando o schema de produção (`alunos_reais`/`metragem`). Nenhum tooling de produção em escopo de bypass (não há SOPS/segredos neste ciclo).

## Conferência cruzada com log do Builder
Bate. Builder reportou ruff OK / mypy OK / import ok / `test_streamlit_app.py` 190 passed (-n auto) / 7 novos verdes. QA re-executou e confirmou os mesmos resultados, ampliando para a suíte FULL (`955 passed, 1 skipped`). Divergência apenas na contagem absoluta da suíte full (responsabilidade do QA, não rodada pelo Builder) — esperado. A contagem 955 reflete o ambiente Python 3.14 local (a baseline 532 do CLAUDE.md é histórica de 2026-05-28; o crescimento acumulado de testes do projeto explica a diferença, não defeito).

## Guardrails verificados
- score_priorizacao não alterado: sim — `git diff-tree -r HEAD` mostra só `pages.py`, `streamlit_app.py`, `tests/integration/test_streamlit_app.py` (+ handoffs/current_task). Grep por `viabilidade_ponto|simulador|catchment_batch|dimensionamento/config|pipelines/m1|^config.py|scoring|brasil_|hexagonos_` no diff: 0 matches.
- Artefatos M1 preservados: sim — zero escrita; a aba só lê parquet de comparáveis e calcula em memória. Nenhum `data/staging/brasil_*` nem `data/outputs/hexagonos_*` no commit.
- Engine INTOCADO: sim — `src/motor_expansao/dimensionamento/viabilidade_ponto.py` (e simulador/catchment_batch/config) NÃO estão no diff-tree de 871d3db.
- DEC-009 (anti-geográfico): sim — `render_viabilidade_ponto` passa ao engine SEMPRE `float(demanda_premissa)` lido do `number_input`; o toggle p50 só preenche `value=`; `demanda_fonte="premissa_explicita"` é exibido. Verificado por código E pelo teste `test_viabilidade_ponto_demanda_e_sempre_premissa_do_operador` (captura demanda=777.0, valor da caixa) e `test_viabilidade_ponto_toggle_p50_preenche_campo_editavel` (p50 sugere 3000, engine recebe 900 editado). A faixa por densidade depende SÓ de `m2`+comparáveis, nunca de lat/lng.
- Offline / sem API ao vivo: sim — `_resolve_viab_ponto` usa `parse_coordinate_input` (string pura + bbox Brasil) e `extract_any_coord` (regex/unquote, import lazy; Selenium só em `MapsGeocoder.__init__`, nunca no caminho da aba). Sem geocoding ao vivo.
- Performance (Blocos 4-6): sim — `load_base_calibracao` com `@st.cache_data` (1×); engine roda só on form-submit; render lazy de abas preservado (5ª aba só executa quando ativa via `elif active_tab`).
- Parâmetros canônicos: H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, pesos renda=0.40/pop=0.60 — INALTERADOS (`config.py` raiz não tocado).
- Validações re-executadas pelo QA (sem bypass): ruff → All checks passed; mypy → no issues; import → ok; pytest full -n auto → 955 passed, 1 skipped (exit 0); subset BLK-DIM-12 → 21 passed; helper housekeeping → 10 passed; housekeeping --check → FALHA esperada pré-move.
- Escopo respeitado: sim — exatamente os 3 arquivos planejados (+ handoffs/current_task). As 2 deleções de geojson no working tree são pré-sujas, alheias ao ciclo e NÃO commitadas (confirmado: não estão no diff-tree de HEAD).

## Decisão recomendada
Fechar ciclo — orquestrador executa o Passo 6.0 (housekeeping move de BLK-DIM-12 via helper versionado) e merge da branch `ciclo/BLK-DIM-12`.
