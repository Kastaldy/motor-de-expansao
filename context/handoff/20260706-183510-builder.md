# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA

## Bloco
BLK-ATR-02 — Gate de viabilidade absoluto (população ≥ 5.000 E renda per capita ≥ 1.500)
na camada de mercado. Coluna PARALELA `flag_gate_atratividade`. READ-ONLY sobre o M1.

## O que foi feito (conforme plano do Planner)

1. **Constante local** `RENDA_PER_CAPITA_MIN_ATR = 1_500` adicionada em
   `src/motor_expansao/pipelines/calcular_colunas_mercado.py`, logo após `POP_MIN_SAM_GATE`
   (linha 42), antes de `SOURCE_REQUIRED_COLS`. NÃO adicionada em `config.py` nem em nenhum
   outro arquivo (grep confirma: só casa em `calcular_colunas_mercado.py`).

2. **Coluna `flag_gate_atratividade`** inserida em `calcular()`, LOGO APÓS
   `df = derive_pop_cut_columns(df, pop_min=POP_MIN_SAM_GATE)` e antes de `flag_canibal = ...`:
   ```python
   _renda_pc = pd.to_numeric(df["renda_per_capita"], errors="coerce")
   df["flag_gate_atratividade"] = (
       df["flag_pop_min_5k"].fillna(False).astype(bool)
       & _renda_pc.ge(RENDA_PER_CAPITA_MIN_ATR).fillna(False)
   )
   ```
   Reutiliza `flag_pop_min_5k` (régua de população DEC-006/DEC-007, não recalculada) e lê
   `renda_per_capita` direto do M1. NaN/`0.0` → `False` (conservador). Tipo bool puro, sem NaN.
   NÃO altera `flag_sam`, `tese_entrada`, `prioridade_mercado_mapeado` (coluna paralela).

3. **`validar()`**: `"flag_gate_atratividade"` adicionado ao conjunto `required`; bloco de
   diagnóstico impresso (contagem + % aprovados + distribuição por UF sob guard `if "uf" in df.columns`),
   inserido entre o print de `flag_sam` e o de `oferta_efetiva_disponivel`.

4. **Testes** — novo arquivo `tests/unit/test_calcular_colunas_mercado_gate.py` com 7 testes
   (6 obrigatórios + `test_constante_do_gate_de_renda`). Fixture `_df` monta 1 linha completa e
   chama `calcular()`. Casos: acima dos 2 pisos → True (+ dtype bool + zero NaN); pop<5000 → False;
   renda<1500 → False; renda NaN → False; renda==1500 → True (inclusivo); score_priorizacao=88.0 e
   hex_score_estrutural=82.0 inalterados após `calcular()`.

5. **Documentação** — `docs/modelo_mercado_hexagonos.md` §5.4: nova tabela + nota explicando
   regra, escopo paralelo (não substitui `flag_viavel`/`RENDA_MIN=4500` nem `flag_sam`), READ-ONLY M1.

## Arquivos afetados
- `src/motor_expansao/pipelines/calcular_colunas_mercado.py` (constante + coluna + validação)
- `tests/unit/test_calcular_colunas_mercado_gate.py` (novo, 7 testes)
- `docs/modelo_mercado_hexagonos.md` (§5.4)

## Resultado das validações

- `ruff check src/motor_expansao/pipelines/calcular_colunas_mercado.py tests/unit/test_calcular_colunas_mercado_gate.py`
  → **All checks passed!**
- `python -m pytest -n auto -q tests/unit/test_calcular_colunas_mercado_gate.py tests/unit/test_pop_cut.py`
  → **17 passed in 7.40s** (7 gate + 10 pop_cut; 0 falhas, sem erro de import).
- `python -c "import streamlit_app"` → **import ok** (só warnings esperados de bare mode do Streamlit).
- `python scripts/loop_guard.py --base ciclo/loop-20260706-152137` → **GUARD OK** (nenhum path proibido).
- `grep -rn "RENDA_PER_CAPITA_MIN_ATR" src/` → casa SÓ em `calcular_colunas_mercado.py` (linhas 42 e 295).

## Guardrails verificados
- `flag_viavel`, `RENDA_MIN` (4500), `renda_target_proxy`, `pipelines/m1/`: INTOCADOS.
- Constante SÓ em `calcular_colunas_mercado.py` (não em `config.py`).
- `flag_gate_atratividade` é coluna paralela — `np.select` de `tese_entrada`/`prioridade_mercado_mapeado`
  NÃO referencia a nova flag.
- Nenhuma escrita de parquet neste bloco → mtime dos 4 oficiais M1 inalterado.
- DEC-001 e DEC-007 intactas (pisos do funil ≠ pesos do M1; gate `flag_sam` inalterado).

## Nota para o QA
- O helper `pop_corte.py` NÃO foi alterado (reutilizado como está).
- O `git diff --stat` de `docs/modelo_mercado_hexagonos.md` mostra ~810 linhas de churn — isso é
  conversão CRLF→LF do arquivo inteiro (pré-existente no working tree), NÃO conteúdo. A mudança
  real de conteúdo é a nova tabela/nota da §5.4.
- Suíte completa (`pytest -n auto -q`) é o gate único do QA; não a rodei aqui (rodei só o subconjunto
  impactado + smoke import, conforme BLK-ORQ-01).
