# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (Passo 6.0 do orquestrador: housekeeping backlog→completed + merge). Abrir bloco de correção separado BLK-FIX-07 para a data-drift dos CSVs de concorrentes (não bloqueia BLK-EST-03).

## VEREDITO
APROVADO COM RESSALVAS

## Justificativa
O wiring `consumidor→solicitante` está correto, mínimo (1 linha funcional em `service.py`), READ-ONLY sobre o M1, anti-PII, e PROVADO por 2 testes novos que PASSAM (não skipped). As 2 únicas falhas da suíte full (`test_csvs_concorrentes_legiveis`) são PRÉ-EXISTENTES e independentes deste bloco — provado por `git stash` (falham idênticas no baseline sem as mudanças): drift de linhas dos CSVs reais de concorrentes (226≠223, 455≠472), trilha de mercado, sem nenhuma relação com a API.

## Problemas críticos (bloqueadores)
- nenhum (relativo ao escopo BLK-EST-03).

## Problemas médios (não bloqueadores)
- Suíte full NÃO está 100% verde neste ambiente: 2 falhas em `tests/integration/test_modelo_mercado_hexagonos.py::test_csvs_concorrentes_legiveis[csv_path1-223]` e `[csv_path2-472]`. São PRÉ-EXISTENTES (reproduzidas com as mudanças do bloco em stash → falham igual no baseline) e de DATA-DRIFT: os CSVs reais de concorrentes em disco têm 226 e 455 linhas, mas o teste fixa `expected_rows` 223 e 472. NÃO atribuível a BLK-EST-03; recomendo bloco de correção separado (BLK-FIX-07) para realinhar `CSV_SOURCES` à base re-coletada (data_coleta 2026-04-22).
- `pytest -n auto` (xdist) é INSTÁVEL neste ambiente Python 3.14: o run paralelo travou em ~96% (worker preso, provável fetch de tile de basemap), confirmando o relato do Builder. Gate executado SERIALMENTE (`python -m pytest -q`), resultado determinístico. Divergência de ambiente, não de produto.
- Contagem de coletados diverge do baseline 2026-06-13 (816 passed serial vs 780 do BLK-DIM-00). Diferença de ambiente/coleta local (Python 3.14), não regressão — o delta do bloco é exatamente +2 testes novos, ambos passando.

## Melhorias opcionais
- Tornar `test_csvs_concorrentes_legiveis` robusto a data-drift (ex.: asserir `>= N` ou ler a contagem esperada de um manifesto versionado) em vez de número fixo hardcoded.
- Instalar `types-requests` em tooling separado para zerar o único erro de mypy (stub ausente, pré-existente, service.py:222).

## Testes faltantes
- nenhum para o escopo do bloco. O wiring está coberto pelos 2 ângulos (rota com `formato=pdf` capturando o consumidor real; chamada direta com `None` capturando o fallback).

## Riscos remanescentes
- A marca d'água do PDF passa a carregar a identidade do consumidor (token→consumidor) — esperado e aprovado no gate humano (Vinicius, 2026-06-15). Fixtures usam nome FICTÍCIO `dev-local`; nenhum PDF/PII real escrito ou versionado.
- As 2 falhas de data-drift continuam vermelhas até o bloco de correção; não afetam M1, API, nem o objetivo deste ciclo.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte FULL — gate único (serial; `-n auto`/xdist travou no ambiente, ver ressalva)
```
$ python -m pytest -q (serial)
SKIPPED [1] tests\integration\test_expansao_dominio.py:152: plano_sample nao carregou score_priorizacao
FAILED tests/integration/test_modelo_mercado_hexagonos.py::test_csvs_concorrentes_legiveis[csv_path1-223]
FAILED tests/integration/test_modelo_mercado_hexagonos.py::test_csvs_concorrentes_legiveis[csv_path2-472]
2 failed, 816 passed, 1 skipped, 87394 warnings in 122.53s (0:02:02)
```
Tentativa `-n auto` (xdist): travou em ~96% (worker preso em fetch de tile), abortada via TaskStop. As 2 falhas (`FF`) já apareciam idênticas no início do run paralelo → mesmas falhas, mesma causa de ambiente.

### 1b. Prova de PRÉ-EXISTÊNCIA das 2 falhas (mudanças do bloco em `git stash`)
```
$ git stash push -- src/motor_expansao/api/service.py tests/integration/test_api_analisar.py
$ python -m pytest "tests/integration/test_modelo_mercado_hexagonos.py::test_csvs_concorrentes_legiveis" -q
FAILED tests/integration/test_modelo_mercado_hexagonos.py::test_csvs_concorrentes_legiveis[csv_path1-223]
FAILED tests/integration/test_modelo_mercado_hexagonos.py::test_csvs_concorrentes_legiveis[csv_path2-472]
2 failed, 1 passed, 843 warnings in 0.74s
$ git stash pop   # restaurado limpo
```
Detalhe da asserção (linha 153): `assert len(df) == expected_rows` → `226 == 223` e `455 == 472`. CSVs reais de concorrentes (`data_coleta 2026-04-22`) com contagem diferente do hardcoded. Sem relação com `service.py`/API.

### 2. import streamlit_app
```
$ python -c "import streamlit_app; print('import ok')"
import ok
```

### 3. ruff (arquivos alterados)
```
$ python -m ruff check src/motor_expansao/api/service.py tests/integration/test_api_analisar.py
All checks passed!
```

### 4. mypy (service.py)
```
$ python -m mypy src/motor_expansao/api/service.py
src\motor_expansao\api\service.py:222: error: Library stubs not installed for "requests"  [import-untyped]
Found 1 error in 1 file (checked 1 source file)
```
PRÉ-EXISTENTE: o erro está na linha 222 (`import requests` em `_reuse_contextily_session`), intocada por este bloco; a mudança está nas linhas 306-309. `consumidor: str | None` é compatível com `solicitante: str | None` → ZERO erro novo.

### 5. Testes do bloco com nomes por-teste (provam NÃO-skip do wiring)
```
$ python -m pytest tests/integration/test_api_analisar.py -v -o console_output_style=classic
tests/integration/test_api_analisar.py::test_analisar_pdf_carimba_consumidor PASSED
tests/integration/test_api_analisar.py::test_analisar_pdf_solicitante_none_fallback PASSED
... (10 passed, 0 skipped, 0 failed em 18.07s)
```
Os 2 testes do wiring `consumidor→solicitante` PASSARAM (não ficaram skipped por `_exige_base_geo` — base geo materializada localmente). Objetivo do bloco PROVADO.

### 6. Housekeeping pré-move (esperado FALHAR antes do Passo 6.0)
```
$ python scripts/housekeeping_move_block.py BLK-EST-03 --check
FALHA --check: stub ausente no backlog para BLK-EST-03   (exit 1)
```
FALHA pré-move ESPERADA: o move backlog→completed é do orquestrador no fechamento (Passo 6.0), ainda não executado. NÃO é bloqueador.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico para mascarar defeito. Os 2 testes novos PATCHAM `motor_expansao.dashboard.censo_report.gerar_pdf_relatorio_pontual_censitario` (a função de ORIGEM, alvo correto do import local-por-nome em `gerar_pdf_ponto`) — isso é ISOLAMENTE LEGÍTIMO de teste: a própria `gerar_pdf_ponto` roda REAL (resolve município, renderiza mapas com a base geo real, monta o residual) e só o render FINAL do PDF é curto-circuitado pelo spy para capturar e asserir o kwarg `solicitante` repassado. Não é bypass do caminho crítico; é exatamente o que prova o wiring. Spy retorna `b"%PDF-1.4 fake"` (bytes sintéticos, nenhum PDF real em disco).

## Conferência cruzada com log do Builder
BATE: ruff (All checks passed), import ok, mypy (1 erro pré-existente de stub `requests`), e os 2 testes novos passam — tudo idêntico ao relatado. DIVERGE em um ponto que FAVORECE auditoria adicional: o Builder rodou apenas um subconjunto serial (208 passed) e não a suíte full; eu rodei a full serial e encontrei 2 falhas pré-existentes que o subconjunto do Builder não cobria (`test_modelo_mercado_hexagonos`). São de data-drift, não introduzidas pelo bloco (provado por stash) — registradas como ressalva, não como regressão do Builder.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY M1; nada de `pipelines/m1/`, `config.py` raiz, `brasil_*`/`hexagonos_*` tocado)
- Artefatos M1 preservados: sim (`git status --porcelain` mostra só `service.py`, `test_api_analisar.py`, `handoff.md`, `current_task.md` do ciclo; pesos `renda=0.40`/`pop=0.60` inalterados; H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0 intocados)
- Pré-sujos alheios ao ciclo (registrados, NÃO violação, NÃO commitar): `data/outputs/setores_censitarios_2022_geo/_metadata.json` e `data/reports/relatorio_pontual_censitario_base_geo.md`
- Anti-PII: sim (fixtures com nome fictício `dev-local`; spy retorna bytes sintéticos; nenhum PDF/PII real escrito ou versionado)
- Escopo respeitado: sim — `censo_report.py` NÃO alterado (git name-only confirma); `pages.py` NÃO tocado; `AnalisarResponseJSON.consumidor` inalterado; assinatura `gerar_pdf_ponto(consumidor: str | None)` NÃO estreitada; nenhum logging/logger/print novo (premissa R3 mantida)
- Validações re-executadas pelo QA (sem bypass): pytest full serial → 2 failed (pré-existentes, provados por stash) / 816 passed / 1 skipped; import → ok; ruff → clean; mypy → 1 erro pré-existente de stub; testes do bloco → 10 passed (2 novos não-skipped); housekeeping --check → FALHA pré-move (esperada)

## Decisão recomendada
Fechar o ciclo BLK-EST-03 (o objetivo do bloco está provado e limpo; READ-ONLY M1, anti-PII, escopo mínimo). EM PARALELO, criar bloco de correção BLK-FIX-07 para a data-drift dos CSVs de concorrentes em `test_csvs_concorrentes_legiveis` (realinhar `CSV_SOURCES` à base re-coletada ou tornar a asserção robusta). Antes do merge, o orquestrador executa o housekeeping (Passo 6.0) via `scripts/housekeeping_move_block.py` e NÃO deve commitar os 2 paths pré-sujos alheios.
