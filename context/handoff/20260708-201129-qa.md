# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Block Orchestrator (fechamento)

## VEREDITO
APROVADO

## Justificativa
BLK-VIAB-07 re-validado de forma INDEPENDENTE (não confiei nos números do Builder).
Todas as validações obrigatórias passam: 34/34 testes do bloco verdes, ruff limpo,
mypy limpo, loop_guard OK, M1 mtimes inalterados, diff M1/config vazio, `formato=None`
byte-idêntico, R² in-sample ausente do veredito (DEC-008 honrada), alvo = `alunos_reais`
(DEC-009 honrada). O script de validação reproduz EXATAMENTE os números do Builder
(GO, delta 9.13 p.p., IC [6.60, 11.77], baseline 45.19%). As 4 falhas na suite full +
1 erro de coleta são PRÉ-EXISTENTES por dependências opcionais ausentes no ambiente
(`openlocationcode` e `matplotlib`), em camadas (`api/`, `demanda_revelada/`) sem nenhuma
relação com o bloco — confirmado falhando idêntico em HEAD com as mudanças do bloco
stashed. Zero novas falhas introduzidas pelo bloco. Housekeeping executado (bloco movido
para completed.md + stub no backlog; --check EXIT=0; teste do helper verde).

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- Ambiente de teste sem `matplotlib` (1 erro de coleta em `tests/unit/demanda_revelada/test_viz_atratividade.py`, do BLK-ATR-04) e sem `openlocationcode` (4 falhas de Plus Code em `test_coord_search.py`/`test_streamlit_app.py`, camada `api/`). Ambas são dependências declaradas em `pyproject.toml` porém não instaladas neste ambiente; PRÉ-EXISTENTES, não introduzidas por BLK-VIAB-07. Recomenda-se instalar os extras/deps no ambiente do loop para eliminar o ruído nas próximas rodadas.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suite FULL
Suite full com `--ignore=tests/unit/demanda_revelada/test_viz_atratividade.py`
(módulo não-coletável por falta de matplotlib — dep opcional ausente, pré-existente):
```
=========================== short test summary info ============================
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_curto_usa_localidade_mock
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_curto_usa_localidade_mockada
4 failed, 1524 passed, 4 skipped, 19 warnings in 1034.83s (0:17:14)
```
As 4 falhas são PRÉ-EXISTENTES (falta `openlocationcode`) — reproduzidas idênticas em
HEAD com o bloco stashed. Bloco em si: 34/34 verdes.
```
tests/unit/dimensionamento/test_viabilidade_ponto.py
..................................                                       [100%]
34 passed in 4.12s
```

### 2. ruff
```
All checks passed!
```
(mypy no módulo, sugestão do Builder: `Success: no issues found in 1 source file`)

### 3. loop_guard
```
GUARD OK: 1 caminho(s) alterado(s), nenhum proibido.
EXIT=0
```
(`--base HEAD` enxerga só o diff commitado/staged; a varredura manual de forbidden-paths
sobre TODO o working tree confirma que os únicos códigos alterados são os 3 arquivos do
bloco; `dimensionamento/config.py` aparece só por normalização CRLF→LF, diff `-w` VAZIO;
não é o `config.py` canônico do M1.)

### 4. M1 mtimes
```
1780501621 /repo/data/staging/brasil_estrutural.parquet
1780501631 /repo/data/staging/brasil_priorizados.parquet
```

### 5. git diff M1/config
```
(vazio)
```
`git diff HEAD -- src/motor_expansao/pipelines/m1/ src/motor_expansao/config.py` → EXIT=0, sem saída.

### 6. Validação semântica
- `formato=None` byte-idêntico: `faixa_alunos_por_densidade(1500., base) == faixa_alunos_por_densidade(1500., base, formato=None)` → `True` (dict idêntico, `n_comparaveis=8` na base mista). Com filtro `low_cost_massa`, resultado difere e p50_com < p50_sem → filtro atua só quando pedido.
- MAPE com filtro (27.31%) < sem filtro (36.44%) → coerente com homogeneidade de formato. Script reproduz: `veredito=GO MAPE sem=36.44% com=27.31% delta=9.13pp IC_delta=[6.60,11.77] baseline=45.19%` — IDÊNTICO ao Builder.
- R² in-sample AUSENTE do veredito: `grep -niE "r2|in-sample|fit.*predict"` só encontra as strings "R2 in-sample BANIDO" (comentário/relatório). O veredito usa MAPE out-of-fold + delta IC bootstrap + baseline da média. DEC-008 honrada.
- Alvo = `alunos_reais`: presente em todas as etapas do script; `membros` NÃO aparece em lugar nenhum. DEC-009 honrada. A curva produz FAIXA de referência por m², não predição geográfica.

### 7. Housekeeping
`--check` inicial retornou EXIT=1 (stub ausente). Executado o move:
```
OK: movido BLK-VIAB-07 (1887 bytes) -> tasks/completed.md; stub em tasks/backlog.md.
EXIT=0
```
Re-check:
```
OK: BLK-VIAB-07 movido (stub no backlog + bloco em completed.md).
EXIT=0
```
Teste do helper:
```
10 passed in 1.76s
```

## Conferência de no-bypass
A suíte full foi RE-EXECUTADA pelo QA como gate único (não confiei no subset do Builder).
Todas as validações foram re-executadas de forma independente; os números do script de
validação foram reproduzidos do zero e batem exatamente. As falhas foram investigadas até
a causa-raiz (deps opcionais ausentes) e provadas pré-existentes por stash/re-run em HEAD.
Nenhum teste foi pulado, silenciado ou marcado como xfail para passar o gate.

## Guardrails verificados
- §5 READ-ONLY M1: diff de `src/motor_expansao/pipelines/m1/` e `src/motor_expansao/config.py` (M1 canônico) VAZIO; `score_priorizacao`/pesos/`hex_score_estrutural`/carteira/plano/artefatos oficiais intocados; mtimes dos 2 parquets M1 inalterados.
- §6.1 loop-safe: só `dimensionamento/viabilidade_ponto.py` + teste + script novo; sem VPS/rede/PII; script lê só `data/staging/base_calibracao_multirede.parquet`.
- DEC-008: veredito por k-fold out-of-fold 5×5 (seed=42) vs baseline da média; R² in-sample banido do veredito; IC95 bootstrap; NO-GO seria válido (deu GO honesto, reproduzido).
- DEC-009: alvo = `alunos_reais` (real), nunca `membros`/agregador; curva = faixa de referência por m², não preditor geográfico.
- Default `formato=None` byte-idêntico (verificado empiricamente + travado por `test_formato_none_byte_identico`); dashboard não passa `formato` → sem regressão.
- `FORMATO_POR_MARCA` no módulo `dimensionamento/`, NÃO em `config.py`.
- `data/analysis/relatorio_formato_densidade.md` gitignored (`git check-ignore` confirma) — não commitado.
- `dimensionamento/config.py` aparece no diff só por CRLF→LF; conteúdo idêntico (diff `-w` vazio); não é o M1.

## Decisão recomendada
Fechar ciclo. BLK-VIAB-07 APROVADO — GO honesto reproduzido, guardrails intactos, zero novas falhas atribuíveis ao bloco.
