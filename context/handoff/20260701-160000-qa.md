# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (Documentation/orquestrador): mover BLK-RELPON-03 para `completed.md`
via `scripts/housekeeping_move_block.py` (Passo 6.0) e registrar o gate visual humano do PDF.

## VEREDITO
APROVADO COM RESSALVAS (ressalva NÃO bloqueante de testes; requisito de fechamento humano)

## Justificativa
A entrega adere byte-a-byte ao plano aprovado (D1 PNG 1000×704, D2 painéis 540): a suíte full
serial fecha 1135 passed / 1 skipped / 0 failed, o fix do letterbox está provado numericamente
(sobra ≤ 0,23 pt nos dois eixos) e o escopo é estritamente RENDER (READ-ONLY M1). A única
ressalva é o gate VISUAL humano do PDF, que é requisito de fechamento (não de testes).

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- nenhuma

## Testes faltantes
- nenhum. O defeito (letterbox) nasceu sem cobertura e foi coberto por 3 testes novos:
  `_fit_contain` trava sobra ≤ 1 pt nos dois eixos (540×380), documenta o fallback 560×380 e o
  smoke parametrizado confirma PNG 1000×704 nas 5 camadas.

## Riscos remanescentes
- **Gate visual humano do PDF (requisito de fechamento, NÃO de testes):** a eliminação da barra
  cinza é mudança de RENDER; o humano deve abrir o PDF (5 mapas) e confirmar (i) barra cinza sumiu
  top/base; (ii) sem distorção da geografia/hexes; (iii) sem sobreposição de moldura/título/legenda
  lateral/rodapé/marca d'água; (iv) enquadramento vertical mais alto é aceitável. O Streamlit da
  porta 8501 (background) serve essa revisão.
- Enquadramento vertical percebido: com `map_box` mais alto a geografia (contain) ganha mais
  respiro vertical; NÃO distorce (forma dos hexes intacta, `focus_bounds` compartilhado). Aceitável
  e esperado — item do gate visual, não regressão.

## Saída literal das validações (re-executadas pelo QA)

### Prova numérica do fix (import direto, sem rede — `basemap=False`)
```
draw_w=539.7727 draw_h=380.0000 x=34.1136 y=100.0000
sobra_w=0.2273 sobra_h=0.0000
fallback 560x380: draw_w=539.7727 sobra_w=20.2273
```
Interpretação: para img=(1000,704) no painel (540,380), `_fit_contain` cobre os DOIS eixos com
sobra ≤ 0,23 pt (letterbox eliminado). O fallback 560×380 sobra 20,23 pt de largura (documentado
como não usado em produção → por isso a padronização em 540).

### `python -m pytest -n auto -q` (gate único) — ABORTOU por instabilidade de ambiente
```
INTERNALERROR> ...  xdist/workermanage.py ... execnet/gateway_io.py ...
INTERNALERROR>   File "...subprocess.py", line 1430, in _make_inheritable
INTERNALERROR>     h = _winapi.DuplicateHandle(...)
INTERNALERROR> OSError: [WinError 6] Identificador inv�lido
```
`-n auto` (pytest-xdist/execnet) aborta com `OSError WinError 6` no Python 3.14 local ao criar os
workers — instabilidade de AMBIENTE (não de código), exatamente como o Builder antecipou/reportou.
Fallback para a full SERIAL abaixo (gate único cumprido em serial).

### `python -m pytest -q` (suíte COMPLETA, serial — gate único)
```
1135 passed, 1 skipped, 140679 warnings in 168.91s (0:02:48)
```
ZERO falhas. (Contagem local diverge do baseline histórico 532/2026-05-28 por diferença de
ambiente Python 3.14; o gate é "0 failed", não a contagem absoluta — vide handoff do Planner.)

### `python -m pytest -q tests/unit/test_relatorio_municipal.py tests/integration/test_streamlit_app.py`
```
246 passed, 12738 warnings in 15.11s
```
Cobre os 3 testes novos (letterbox/fallback/altura 704) + `/Count 9`, `PDF_SECTION_HEADERS`,
`BLK-RELMUN-01`, `OpenStreetMap`/`CARTO`, marca d'água ≥8, anti-PII,
`test_coexistencia_relatorio_pontual_intocado` (byte-identity do Pontual) + smoke do Streamlit.

### `python -m ruff check .`
```
All checks passed!
```

### `python -m ruff check src/motor_expansao/dashboard/relatorio_municipal.py tests/unit/test_relatorio_municipal.py`
```
All checks passed!
```

### `python -m mypy src/motor_expansao/dashboard/relatorio_municipal.py`
```
Success: no issues found in 1 source file
```

### `python -c "import streamlit_app; print('import ok')"`
```
(warnings benignos de bare-mode do Streamlit)
import ok
```

### Housekeeping (Passo 6.0) — estado PRÉ-fechamento (NÃO executei o move)
```
=== HOUSEKEEPING --check (deve FALHAR agora) ===
FALHA --check: stub ausente no backlog para BLK-RELPON-03
exit=1
=== HELPER TEST ===
10 passed, 501 warnings in 0.31s
```
```
=== backlog heading ===
119:### BLK-RELPON-03 — Eliminar a barra cinza (letterbox) dos mapas do Relatório Municipal
=== completed heading ===
(vazio = ausente de completed, correto)
```
Estado correto para o pré-fechamento: heading `### BLK-RELPON-03` íntegro em `backlog.md`
(linha 119), ausente de `completed.md`, stub ainda ausente → `--check` FALHA como esperado.
`test_housekeeping_helper.py` verde. O move é passo de fechamento (não do QA).

## Conferência de no-bypass
Confirmo que NENHUMA validação usou contorno de config/artefatos reais (`--config /dev/null`,
fixture que evita o caminho real, ou mock do caminho crítico). Este bloco é RENDER de PDF: não há
tooling que dependa de config/artefatos de produção. As fixtures sintéticas de `df_muni`/
`municipio_result` em `test_relatorio_municipal.py` isolam a geometria de render e a geração do PDF
— prática LEGÍTIMA de teste unitário (a suíte NUNCA bate na rede; `basemap=False`), explicitamente
FORA do escopo da regra de bypass (que mira tooling de produção). A prova numérica do fix foi feita
por import direto das funções reais de produção (`_fit_contain`), sem mock.

## Conferência cruzada com log do Builder
BATE. O Builder reportou o INTERNALERROR do `-n auto` (reproduzido por mim), a validação serial
verde, ruff/mypy limpos e import ok. Diferença de contagem: o Builder rodou apenas o subconjunto
(246 passed); o QA rodou a full serial (1135 passed / 1 skipped) — gate único cumprido aqui, sem
regressão. O diff de código coincide exatamente com os passos 1–7 do handoff do Builder/Planner.

## Guardrails verificados
- **score_priorizacao não alterado:** sim. `git diff --stat HEAD` restrito a
  `relatorio_municipal.py` + `test_relatorio_municipal.py` + governança (`handoff.md`,
  `backlog.md`, `current_task.md`). ZERO toque em `config.py`, `data/staging`, `data/outputs`,
  pipelines/m1, scoring, carteira, plano ou artefatos oficiais.
- **Artefatos M1 preservados:** sim (nenhum pipeline/artefato lido/escrito; RENDER-only).
- **Aderência D1/D2 e escopo (auditoria específica):**
  - `render_mapas_municipio`: `height` 620→704 (confirmado no diff). ✔
  - `_render_mapa_municipio`: `height` 620→704. ✔
  - `_score_page`: `_draw_framed_map(..., max_w=560.0)`→`540.0` (linha 1661). ✔ Painel de legenda
    lateral (`px=626.0`) INTOCADO (folga garantida).
  - `_draw_map`: defaults `600.0/430.0`→`540.0/380.0`; passa a delegar a `_fit_contain`. ✔
  - `_fit_contain`: extração BYTE-IDÊNTICA — li o cálculo antigo (`scale=min`, `draw_w/draw_h`,
    centralização a partir das âncoras) vs. o helper; fórmula idêntica, sem mudança de
    comportamento. `_draw_map` delega. ✔
  - Sem novo parâmetro de contrato público quebrando callers (só mudou VALOR default de `height`;
    assinatura pública preservada). ✔
  - As 4 chamadas de `_draw_framed_map` que já usavam `max_w=540.0` (linhas 1545/1603/1711/1773):
    inalteradas. ✔
  - `censo_map.py`/`censo_point.py`/`censo_report.py` (Relatório Pontual): INTOCADOS —
    `test_coexistencia_relatorio_pontual_intocado` (byte-identity) verde. ✔
- **Prova real do fix (não só o teste do Builder):** import direto de `_fit_contain` →
  draw_h=380,0 e draw_w=539,77 (sobra ≤ 0,23 pt nos dois eixos → letterbox eliminado); teste
  parametrizado confirma PNG 1000×704 nas 5 camadas. ✔
- **Integridade do PDF:** `/Count 9` (8 páginas), `PDF_SECTION_HEADERS`, marca d'água anti-PII
  (≥8), `set_compression(False)`, `pdf_version="1.4"`, atribuição OpenStreetMap/CARTO — todos
  verdes na suíte (`test_pdf_municipal_8_paginas_e_secoes`, `..._marca_dagua_solicitante`,
  `..._sem_pii`) + `_UltraPDF` mantém `pdf_version="1.4"` e `set_compression(False)`. ✔
- **Parâmetros canônicos §3 preservados:** H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0,
  pesos renda=0.40/pop=0.60 — INTOCADOS (nenhum parâmetro de score/pipeline tocado).
- **Validações re-executadas pelo QA (sem bypass):**
  - `pytest -n auto` → INTERNALERROR de ambiente (WinError 6) → fallback serial.
  - `pytest -q` (full serial) → 1135 passed, 1 skipped, 0 failed.
  - subset relatorio_municipal + streamlit_app → 246 passed.
  - `ruff check .` / escopo → All checks passed!
  - `mypy relatorio_municipal.py` → Success.
  - `import streamlit_app` → import ok.
  - housekeeping `--check` → FALHA esperada (pré-fechamento); `test_housekeeping_helper` → 10 passed.
- **Escopo respeitado:** sim (só `relatorio_municipal.py` + teste + governança).

## Decisão recomendada
FECHAR o ciclo após: (1) o passo de fechamento mover BLK-RELPON-03 para `completed.md` via
`scripts/housekeeping_move_block.py` (helper versionado; NÃO move manual); (2) o gate VISUAL humano
do PDF confirmar a ausência da barra cinza e a ausência de sobreposição/distorção nas 5 páginas.
Os testes/guardrails estão APROVADOS; o gate visual é a única pendência de fechamento (não bloqueia
os testes).
