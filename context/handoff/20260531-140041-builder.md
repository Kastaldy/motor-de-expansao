# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA

## Bloco executado
BLK-SCORE-04 — Backtest read-only multivariado das features mercado/censitárias vs. desfecho
(`alunos_recorrentes`). Criticidade Alta; REVISÃO HUMANA já APROVADA por Felipe Silva em
2026-05-31 (poda 19→12, sentinela mantida, âncora do dataset, OLS diagnóstico restrito).
READ-ONLY estrito sobre o M1.

## O que foi feito
Implementado EXATAMENTE o plano técnico do Planner (12 passos), reusando as funções puras de
`analysis/score_backtest.py` por import (sem alterá-lo):

- `analysis/feature_backtest_mercado.py` (NOVO):
  - `load_inputs`: único ponto de I/O. Lê o dataset inteiro (441 linhas) e o mercado SÓ com
    `columns=["hex_id", *features_de_mercado]` (11 colunas de mercado; a âncora vem do dataset).
    Nunca carrega as 131 cols / 1,5M linhas inteiras.
  - `join_features`: dedup do mercado por `hex_id`, left-join com `suffixes=("","_merc")`,
    `match_stats` com taxa de match (pivô `n_concorrentes_mapeados_2km`) + âncora preservada do
    dataset (`ancora_origem="dataset_validacao"`). A âncora homônima do mercado é excluída da
    seleção por design (não vira feature testada).
  - `prepare_features`: política de sentinela explícita e idempotente (no-op de valores) — mantém
    `dist_concorrente_mais_proximo_m` real (cauda ~652 km), zeros como ausência real. Documenta
    que a leitura primária é Spearman.
  - loop `correlate_by_cell` por feature (AGG + por rede, N_MIN=10) + `bootstrap_ci_spearman` no
    AGG (N≥30, seed=42, n_boot=2000).
  - `ranking_agg_abs`: ordena por |rho| DESC (mantém sinal cru, ordena por magnitude).
  - `ols_diagnostico`: `numpy.linalg.lstsq`, 4 regressores z-scored + intercepto, listwise, R²,
    betas padronizados, `cond(X)`, nota de instabilidade. NÃO persiste nada.
  - `build_feature_report`: string pura, §1 método / §2 match / §3 tabela feature×célula (+IC
    bootstrap) / §4 ranking |rho| / §5 OLS diagnóstico com os 3 avisos / §6 Limitações (5
    herdadas do BLK-SCORE-02 §5 + 2 específicas: colinearidade, sentinela) / §7 apêndice N / §8
    reprodutibilidade.
  - `_try_figures`: matplotlib Agg sob try/except (barras |rho| AGG, gitignored).
  - `main()`: orquestra, escreve `data/analysis/relatorio_backtest_mercado.md`, imprime resumo
    agregado SEM PII, `return 0`. Bootstrap de `sys.path` para rodar standalone (`analysis` é
    pacote; sob pytest o `pythonpath` já resolve).
- `tests/unit/test_feature_backtest_mercado.py` (NOVO): 7 casos sintéticos (só fixtures em
  memória, JAMAIS o parquet real): match-rate; âncora não-sobrescrita; variância-zero→indefinido;
  ranking por |rho|; OLS estrutura+pureza (não escreve arquivo); relatório seções+anti-PII;
  relatório marca indefinido.

Ajuste vs. plano: o teste anti-PII checa que o VALOR do nome de unidade não vaza (não o literal
"nome_unidade", que aparece legitimamente na nota de guardrail do cabeçalho) — mesma convenção do
teste do BLK-SCORE-02. Adicionado bootstrap de `sys.path` no script (o plano usa
`from analysis.score_backtest import ...`, que não resolve em execução direta sem isso).

## Arquivos alterados
- `analysis/feature_backtest_mercado.py` — NOVO (versionável).
- `tests/unit/test_feature_backtest_mercado.py` — NOVO (versionável).
- `data/analysis/relatorio_backtest_mercado.md` — NOVO, gitignored (gerado pela execução).
- `data/analysis/fig_feature_rho_agg.png` — opcional, gitignored (gerado sob try/except).
- `context/handoff.md` (este) + snapshot append-only + `tasks/current_task.md` (status QA).
- NÃO tocados: `analysis/score_backtest.py`, `data/analysis/relatorio_backtest.md` (BLK-SCORE-02),
  `src/`, `config.py`, `constants.py`, `data/outputs/`, `data/staging/`, `brasil_*.parquet`,
  `hexagonos_brasil_*.parquet`.

## Validações executadas (com saída)
1. `python -m pytest tests/unit/test_feature_backtest_mercado.py -q`
   → **7 passed** (1 ajuste anti-PII aplicado após 1ª rodada; verde no final).
2. `python -m pytest tests/unit/test_score_backtest.py -q`
   → **15 passed** (reuso/import não quebrou o BLK-SCORE-02). [combinado com (1): 22 passed]
3. `python analysis/feature_backtest_mercado.py` → **EXIT 0**; relatório gerado. Saída agregada:
   - `match: 390/391 (99.7%)` (taxa REAL medida pelo script; bate com a estimativa do Planner)
   - top-5 |rho| (AGG): #1 `n_unidades_ultra_2km` +0.346; #2 `gap_rede_propria_1km` -0.312;
     #3 `score_setor_2022_calibrado` +0.148; #4 `coverage_pct_setor_2022` +0.087;
     #5 `dist_concorrente_mais_proximo_m` +0.055.
   - `OLS diagnostico: N=370, R2=+0.034`.
4. `python -m pytest -q` → **624 passed, 1 skipped, 9 warnings** (baseline da branch 617 + 7
   novos). Verde.
5. `python -c "import streamlit_app"` → `import ok`.
6. `git status --porcelain` → só novos versionáveis (`analysis/feature_backtest_mercado.py`,
   `tests/unit/test_feature_backtest_mercado.py`) + handoff/task/snapshots; relatório e figura
   gitignored (não aparecem).
7. `git diff --stat -- src/ data/outputs/ data/staging/ config.py analysis/score_backtest.py`
   → **VAZIO** (nenhum artefato M1/oficial tocado). EXIT 0.

## ACHADOS principais (agregados, sem PII — para QA e usuário)
- **Match do join**: 390/391 = **99.7%** (mercado nacional cobre quase todos os hexes).
- **Ranking por |rho| (Spearman, AGG, N≈370–377)** — top:
  1. `n_unidades_ultra_2km` rho=**+0.346** — IC95% [+0.246,+0.435] (positivo, exclui 0).
  2. `gap_rede_propria_1km` rho=**-0.312** — IC95% [-0.392,-0.226] (negativo, exclui 0).
  3. `score_setor_2022_calibrado` (âncora censitária) rho=**+0.148** — IC95% [+0.052,+0.251]
     (REPRODUZ exatamente o +0.148 do BLK-SCORE-02; positivo, exclui 0).
  4. `coverage_pct_setor_2022` rho=+0.087 — IC95% [-0.013,+0.189] (cruza 0).
  5–12: `dist_concorrente_mais_proximo_m` +0.055, `densidade_pop_setor_hab_km2` -0.047,
     `pop_total_setor_2022` -0.047, `oferta_efetiva_mapeada_2km` -0.034,
     `pressao_concorrencial_score_2km` -0.034, `n_concorrentes_mapeados_2km` -0.033,
     `residual_indice_mapeado` +0.030, `share_smart_fit_2km` +0.027 — todas com IC que cruza 0
     (sinal individual nulo/fraco).
- **Features de rede própria** (`n_unidades_ultra_2km`, `gap_rede_propria_1km`) lideram o |rho|
  apesar da baixa variância (nunique 3–4): RODARAM (não caíram em "indefinido" no AGG), com IC que
  exclui 0. CAUTELA: sinal de canibalização/saturação da rede própria provavelmente reflete onde a
  Ultra JÁ se instalou (endogeneidade/seleção) — leitura para o QA/usuário, não causalidade.
- **Nenhuma feature caiu em "indefinido" no AGG** com o dado real (todas as 12 rodaram); o
  tratamento de variância-zero existe e está coberto por teste sintético.
- **OLS diagnóstico** (4 regressores z-scored, N=370 listwise): **R²=+0.034** (baixíssimo);
  betas padronizados — `score_setor_2022_calibrado` +0.169 (maior), `dist_…` +0.054,
  `pressao_…` +0.047, `densidade_…` -0.034; cond=1.9 (sem colinearidade severa nesse subconjunto);
  nota "ok" (N≥50). Leitura: conjuntamente, esses 4 explicam ~3% da variância do desfecho — sinal
  fraco; coerente com a leitura individual. **Diagnóstico apenas; nenhum coeficiente persistido.**
- **Síntese para o gate G4 (DEC-001)**: além de pop/renda do M1, a única feature censitária com
  sinal individual positivo robusto e replicado é a âncora `score_setor_2022_calibrado` (+0.148,
  já conhecida). As features de mercado/competição têm sinal individual ~nulo (IC cruza 0). Os dois
  maiores |rho| são features de rede própria, suspeitas de endogeneidade. Evidência NÃO sustenta,
  por si só, enriquecer o score com features de mercado; ver Limitações §6 (maturação, N,
  heterogeneidade, colinearidade, sentinela).

## Problemas
- 1ª rodada do teste anti-PII falhou (checava o literal "nome_unidade", presente na nota de
  guardrail do cabeçalho). Corrigido para checar o VALOR do nome fictício (convenção do
  BLK-SCORE-02). Demais validações verdes de primeira após o bootstrap de `sys.path`.

## Pendências
- QA: revisar relatório (§3 tabelas, §5 avisos, §6 limitações), conferir reprodutibilidade
  (re-rodar e comparar rho/R²), confirmar ausência de PII e de escrita em M1.
- Orquestrador: fechar o ciclo (commit por path; relatório/figura gitignored NÃO versionar).

## Riscos
- Endogeneidade das features de rede própria (top do |rho|) — sinalizado no relatório/achados; não
  interpretar como causal.
- Multiplicidade de testes (12 features) — p não corrigido; relatório enfatiza magnitude/IC.
- Confounds herdados do BLK-SCORE-02 §5 (maturação, hex por centroide, EngCorpo estimado).

## Guardrails verificados
- score_priorizacao não alterado: **sim** (não tocado; pesos 0.40/0.60 e H3_RESOLUTION=7 intactos).
- artefatos M1 preservados: **sim** (`git diff --stat` em `src/ data/outputs/ data/staging/
  config.py` VAZIO).
- score_backtest.py inalterado: **sim** (só importado; diff vazio).
- relatorio_backtest.md do BLK-SCORE-02 inalterado: **sim**.
- Saída só em `data/analysis/` (gitignored): **sim**. Sem PII no relatório/prints: **sim**.
- Nenhuma dependência nova; sem `git add`/commit; nenhum comando no VPS: **sim**.
- Reprodutibilidade: seed=42, n_boot=2000, N_MIN=10.
