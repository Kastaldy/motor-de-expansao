# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
Builder — com **gate humano D1 (pré-aprovado)**: ver "Alerta de aprovação" abaixo.

## Entendimento da tarefa
Trocar a paleta de cor dos hexágonos APROVADOS do Relatório Municipal (camadas `cobertura`/`resumo`)
de amarelo/laranja para 2 tons de verde (D1 pré-aprovado) e atualizar somente o TEXTO VISÍVEL "amarelo(s)"
do PDF para "hexágonos destacados", sem tocar identificadores, critério de destaque, score ou artefatos M1.

## Plano técnico

### 1. Troca de cor (D1 pré-aprovado)
1.1. Em `src/motor_expansao/dashboard/relatorio_municipal.py:89-90`, trocar:
   - `_COR_APROVADO_PROPRIO = (255, 210, 28)` → `_COR_APROVADO_PROPRIO = (20, 170, 80)`
   - `_COR_APROVADO_MUNICIPAL = (245, 140, 30)` → `_COR_APROVADO_MUNICIPAL = (90, 190, 120)`
   - `_COR_REPROVADO = (150, 156, 170)` (linha 91): **NÃO tocar**.
1.2. NÃO editar nenhum outro ponto de cor — os seguintes derivam automaticamente das 2 constantes-fonte
   e só precisam ser CONFERIDOS (via teste/print, não edição):
   - `_HEX_DESTAQUE_RGBA = (*_COR_APROVADO_PROPRIO, 200)` (:94)
   - `_HEX_DESTAQUE_MUNICIPAL_RGBA = (*_COR_APROVADO_MUNICIPAL, 200)` (:95)
   - `_HEX_APROVADO_RGBA = (*_COR_APROVADO_PROPRIO, 210)` (:102)
   - `_HEX_APROVADO_MUNICIPAL_RGBA = (*_COR_APROVADO_MUNICIPAL, 210)` (:103)
   - `_HEX_REPROVADO_RGBA = (*_COR_REPROVADO, 170)` (:104) — inalterado (fonte não muda)
   - `_COBERTURA_LEGENDA` (:105-109) e `_RESUMO_LEGENDA` (:111-114) — referenciam as constantes por
     nome, herdam o novo RGB automaticamente
   - Uso em desenho/legenda: `:1038` (`_HEX_APROVADO_RGBA`/`_HEX_APROVADO_MUNICIPAL_RGBA`, camada
     cobertura), `:1044` (`_HEX_DESTAQUE_RGBA`/`_HEX_DESTAQUE_MUNICIPAL_RGBA`, camada resumo),
     `:1100-1101` (seleção de legenda por `camada`)
   - Conclusão: bastam as 2 edições de 1.1; nenhum outro ponto de cor precisa de edição direta.
1.3. Comentários-fonte (NÃO user-facing, mas descrevem cor errada após a troca — atualizar por
   coerência, edição mínima, sem inflar escopo):
   - `:85-88` (comentário acima de `_COR_APROVADO_PROPRIO`): hoje diz "= dourado forte" e
     "= laranja"; trocar para "= verde forte" e "= verde médio (fallback municipal)".
   - `:99-101` (comentário acima de `_HEX_APROVADO_RGBA`): hoje diz "Aprovado dado proprio (dourado) /
     Aprovado fallback municipal (laranja)"; trocar para "Aprovado dado proprio (verde forte) /
     Aprovado fallback municipal (verde medio)".
   - Estes 2 pontos são os ÚNICOS comentários de cor a atualizar nesta seção; não expandir para outros
     comentários fora do escopo de cor (ver item 2 para os de "amarelo").

### 2. Texto visível "amarelo" → wording neutro
2.1. Strings EXIBIDAS no PDF (mandatórias, correspondem ao critério de aceite do backlog):
   - `:1656` — `pdf.multi_cell(pw - 24, 13, _ascii("Soma dos hexágonos amarelos / 2.500"))` →
     `_ascii("Soma dos hexágonos destacados / 2.500")`
   - `:2070` — `"Espaço = soma dos hexágonos amarelos / 2.500. "` →
     `"Espaço = soma dos hexágonos destacados / 2.500. "`
   Confirmado por grep (`amarel` case-insensitive) que estas são as ÚNICAS 2 ocorrências de texto
   literal exibido no PDF; nenhuma outra string visível contém "amarelo".
2.2. IDENTIFICADORES — **NÃO TOCAR** (mesmo nome, mesma lógica; são a "armadilha" do bloco):
   - `soma_oferta_amarelos` (variável local + chave de `result`): `:616`, `:673`, `:1657`, `:1658`
   - `n_hex_amarelos` (variável local + chave de `result`): `:617`, `:670`, `:671`, `:672`, `:1661`
   - `parcelas_amarelos` (chave de `result`): `:675`, `:1657`
   Todas vivem dentro da função que monta o dict de retorno (bloco `:611-684`, em torno do `return {...}`
   de `:665-684`) e são consumidas por `:1656-1666` (render do PDF) e pelos testes (ver seção 4).
2.3. Comentários/docstrings internos com "amarelo" (NÃO são texto exibido; opcional, NÃO bloqueante
   para QA, mas recomendado por coerência — decisão do Builder tocar ou não):
   - `:10` (docstring do módulo, cita "amarelo" entre aspas ao descrever D1) — pode manter como está
     (documenta histórico da nomenclatura) ou trocar para "destacado".
   - `:879`, `:882`, `:883` (docstring de função descrevendo as camadas do mapa — "destacados em
     amarelo", "fallback amarelo", "aprovados... em amarelo") — se tocado, trocar "amarelo" por "verde"
     (aqui o termo descreve COR, não é o wording de texto do PDF).
   - `:1070`, `:1072` (comentário sobre rótulos de oferta "sobre hexes amarelos") — se tocado, trocar
     por "hexes destacados" (aqui o termo é usado como sinônimo de "destacado", não de cor).
   - `:1202` (docstring: "(destacados, amarelo) e reprovados (cinza)") — se tocado, trocar "amarelo"
     por "verde".
   Se o Builder optar por não tocar nestes 6 pontos, registrar a omissão no relatório de fechamento
   (não é falha de critério de aceite).

### 3. Emenda de terminologia na DEC-011 (CLAUDE.md)
3.1. Localizar a DEC-011 em `CLAUDE.md` (bloco começa em `:301`; a emenda BLK-RELMUN-03 já existe em
   `:311`, logo antes do heading `### DEC-012` em `:313`).
3.2. Inserir um NOVO parágrafo de emenda logo após a linha `:311` (emenda BLK-RELMUN-03) e antes da
   linha em branco que precede `### DEC-012`, seguindo o padrão "Emenda AAAA-MM-DD (...)" já usado.
   Texto sugerido (ajustar pontuação/acentuação ao colar):

   ```
   - **Emenda 2026-07-08 (BLK-RELMUN-05, APROVADA por Vinicius): cor de exibição dos hexágonos
     aprovados no Relatório Municipal passa de amarelo/laranja para VERDE (2 tons); wording textual
     "amarelo(s)" nas páginas 2/8 ("Espaço para academias") passa a "hexágonos destacados".**
     Mudam SOMENTE `_COR_APROVADO_PROPRIO` (255,210,28)→(20,170,80) e `_COR_APROVADO_MUNICIPAL`
     (245,140,30)→(90,190,120) em `relatorio_municipal.py`, e o texto das linhas correspondentes a
     `:1656`/`:2070` do arquivo original ("Soma dos hexágonos amarelos.../Espaço = soma dos hexágonos
     amarelos..." → "...hexágonos destacados..."). Cor ≠ critério: o critério de "hexágono destacado"
     (`oferta_efetiva_disponivel >= OFERTA_DESTAQUE_MIN=2000`, emenda BLK-RELMUN-03 acima) e o
     `flag_sam`/score/pesos/artefatos oficiais do M1 permanecem INTOCADOS. Identificadores
     (`n_hex_amarelos`, `soma_oferta_amarelos`, `parcelas_amarelos`, chaves de `result`) e
     `_COR_REPROVADO` (cinza) permanecem INALTERADOS. É mudança de DISPLAY LOCAL a
     `relatorio_municipal.py` (§5 guardrail permanente). Referências: `tasks/backlog.md`
     (BLK-RELMUN-05); `src/motor_expansao/dashboard/relatorio_municipal.py`;
     `tests/unit/test_relatorio_municipal.py`.
   ```
3.3. Não alterar o restante da DEC-011 (partes 1/2, emenda BLK-RELMUN-03) nem nenhuma outra DEC.

### 4. Testes — `tests/unit/test_relatorio_municipal.py`
4.1. Levantamento feito (grep completo por cor/tuplas/"amarelo"): **nenhum teste hoje trava as tuplas
   RGB antigas nem a string "amarelo" no PDF** — logo nenhum teste existente QUEBRA com a troca. Os
   testes que usam os identificadores (`n_hex_amarelos`/`soma_oferta_amarelos`) em `:123-126`,
   `:162-164`, `:376-377` continuam passando sem alteração (identificadores intocados).
4.2. Adicionar import das 3 constantes de cor ao bloco de import existente (`:18-31`), ao lado de
   `_hex_destacado_mask`/`_prettify_rede`:
   `_COR_APROVADO_PROPRIO`, `_COR_APROVADO_MUNICIPAL`, `_COR_REPROVADO`.
4.3. Adicionar um teste NOVO (rede de segurança, trava os critérios de aceite do backlog) — sugestão de
   nome e corpo, próximo ao bloco "Agregacao (D1, D4, D5, D6, zonas)" (`:113-115`):
   ```python
   def test_cores_aprovados_verdes_blk_relmun_05():
       """BLK-RELMUN-05: aprovados em verde (2 tons distinguíveis); reprovado cinza inalterado."""
       assert _COR_APROVADO_PROPRIO == (20, 170, 80)
       assert _COR_APROVADO_MUNICIPAL == (90, 190, 120)
       assert _COR_REPROVADO == (150, 156, 170)
       assert _COR_APROVADO_PROPRIO != _COR_APROVADO_MUNICIPAL != _COR_REPROVADO
   ```
4.4. Em `test_pdf_municipal_8_paginas_e_secoes` (`:344-368`), adicionar 2 asserts logo após o bloco
   existente de checagem "SAM Fitness" (`:361-364`, que já segue o mesmo padrão de checar wording no
   PDF gerado):
   ```python
   # BLK-RELMUN-05: wording neutro - PDF nao deve mais mencionar "amarelo(s)" como texto exibido.
   assert b"amarelo" not in pdf_bytes.lower()
   assert "hexágonos destacados".encode("latin-1") in pdf_bytes
   ```
4.5. NÃO alterar `:123-126`, `:162-164`, `:376-377` (identificadores) — são a prova de que o refactor
   não vazou para os nomes de campo.

### 5. Validações obrigatórias
- `ruff check .`
- `mypy src`
- `python -c "import streamlit_app"` (smoke de import do app)
- `pytest -q tests/unit/test_relatorio_municipal.py`
- `pytest -q tests/integration/test_relatorio_acentuacao_regressao.py` (se existir — cobre o PDF
  municipal quanto a caracteres fora de latin-1 virando "?"; a troca de wording usa só acentos
  portugueses padrão, sem travessão/bullet/seta, então não deve introduzir regressão)
- Suíte completa (`pytest -q`) como gate único do QA — nota ambiental: `pytest-xdist` instável em
  Python 3.14 local, preferir execução SERIAL; Smart App Control pode interferir no import de `h3` —
  **NO-BYPASS**, investigar a causa raiz se reincidir, nunca pular o gate.
- Revisão VISUAL do PDF gerado (`gerar_pdf_relatorio_municipal`) confirmando os 2 tons de verde
  distinguíveis entre si e do cinza de reprovado sobre o basemap claro (critério de aceite do backlog,
  não automatizável por assert de texto).

### 6. Guardrail READ-ONLY M1 / escopo
- `git diff` restrito a: `src/motor_expansao/dashboard/relatorio_municipal.py`,
  `tests/unit/test_relatorio_municipal.py`, `CLAUDE.md` (só a emenda da seção 3), e bookkeeping
  (`tasks/current_task.md`, `tasks/completed.md`, `tasks/backlog.md`, `context/handoff.md`,
  `context/handoff/`).
- Zero alteração em `pipelines/m1/`, `config.py`, `censo_point.py`/`censo_map.py`/`censo_report.py`,
  qualquer artefato de `data/staging`/`data/outputs` oficiais.
- NÃO tocar `_ZONA_CORES_PDF`/`_ZONA_CORES_RGBA` (`:155-161`, cores de ZONA da página Domínio —
  turquesa/magenta/laranja, semântica distinta).
- NÃO tocar `_hex_destacado_mask` / critério `oferta_efetiva_disponivel >= 2000` / `flag_sam` /
  `score_priorizacao` / `hex_score_estrutural` / carteira / plano / artefatos oficiais.
- Núcleo `censo_*`, estrutura de páginas do PDF (ordem/`/Count 9`), marca d'água, `set_compression`,
  `pdf_version`: intocados.

## Arquivos afetados
- `src/motor_expansao/dashboard/relatorio_municipal.py` (:85-91, :99-101 comentários; :1656, :2070
  texto; opcionalmente :10, :879, :882, :883, :1070, :1072, :1202 comentários/docstrings)
- `tests/unit/test_relatorio_municipal.py` (import :18-31; novo teste de cor; 2 asserts em
  `test_pdf_municipal_8_paginas_e_secoes`)
- `CLAUDE.md` (emenda na DEC-011, após `:311`)
- `tasks/current_task.md`, `tasks/completed.md`, `tasks/backlog.md`, `context/handoff.md`,
  `context/handoff/` (fechamento de ciclo)

## Dependências
- Nenhuma dependência técnica externa; bloco autocontido em `relatorio_municipal.py` + testes + doc.

## Riscos técnicos
- Tocar por engano um IDENTIFICADOR com "amarelo" (`n_hex_amarelos`, `soma_oferta_amarelos`,
  `parcelas_amarelos` ou chave de `result`) em vez de só cor/texto — mitigação: grep `amarel`
  antes/depois da edição, diff revisado linha a linha (ver seção 2.2).
- Esquecer alguma ocorrência de texto visível "amarelo" além de `:1656`/`:2070` — mitigação: grep
  completo já feito nesta etapa (seção 2.1) confirma que são as únicas 2; reconferir pós-edição.
- Alterar por engano as cores de ZONA (`:155-161`) ou o critério de destaque — fora de escopo,
  reforçado na seção 6.
- 2 tons de verde pouco distinguíveis visualmente entre si ou do cinza — mitigação: revisão visual
  obrigatória do PDF antes de fechar QA (seção 5).

## Critérios de aceite finais
- `_COR_APROVADO_PROPRIO == (20, 170, 80)`; `_COR_APROVADO_MUNICIPAL == (90, 190, 120)`;
  `_COR_REPROVADO == (150, 156, 170)` (inalterado).
- PDF municipal renderiza aprovados (`cobertura`/`resumo`) em 2 tons de verde distinguíveis entre si e
  do cinza de reprovado; revisão visual aprovada.
- Nenhuma menção textual "amarelo(s)" remanescente no PDF; `:1656`/`:2070` (arquivo original) agora
  dizem "hexágonos destacados".
- `n_hex_amarelos`, `soma_oferta_amarelos`, `parcelas_amarelos` e chaves de `result`: mesmos nomes,
  mesma lógica de cálculo (testes de `:123-126`/`:162-164`/`:376-377` continuam verdes sem edição).
- Critério de destaque (`oferta_efetiva_disponivel >= 2000`), `flag_sam`, score e artefatos oficiais
  do M1 inalterados; nenhum arquivo de `data/staging`/`data/outputs` oficiais tocado.
- `tests/unit/test_relatorio_municipal.py` passa (incl. novo teste de cor + novos asserts de PDF).
- `ruff` e `mypy` limpos.
- `CLAUDE.md` recebe a emenda de terminologia na DEC-011 (seção 3).
- Suíte completa verde, sem regressão fora do escopo.

## Validações obrigatórias
- Comando: `ruff check .` — Critério mínimo: 0 erros.
- Comando: `mypy src` — Critério mínimo: 0 erros.
- Comando: `python -c "import streamlit_app"` — Critério mínimo: sem exceção.
- Comando: `pytest -q tests/unit/test_relatorio_municipal.py` — Critério mínimo: 0 falhas.
- Comando: `pytest -q tests/integration/test_relatorio_acentuacao_regressao.py` (se existir) —
  Critério mínimo: 0 falhas (nenhum "?" de caractere fora de latin-1).
- Comando: `pytest -q` (suíte completa, serial) — Critério mínimo: 0 falhas, sem regressão fora do
  escopo do bloco.

## Fora de escopo
- Qualquer identificador com "amarelo" (nomes de variável/chave de `result`).
- Cores de ZONA da página Domínio (`_ZONA_CORES_PDF`/`_ZONA_CORES_RGBA`, `:155-161`).
- Critério de "hexágono destacado" (DEC-011 + emenda BLK-RELMUN-03), `flag_sam`, `score_priorizacao`,
  `hex_score_estrutural`, carteira, plano, artefatos oficiais do M1.
- Núcleo `censo_*`, estrutura de páginas do PDF, marca d'água, `set_compression`/`pdf_version`.
- Qualquer outro bloco do backlog (ex.: BLK-RELMUN-06).

## Alerta de aprovação
D1 (tons de verde) já foi **PRÉ-APROVADO por Vinicius em 2026-07-08** (registrado em
`tasks/current_task.md`). Este handoff não abre uma nova decisão de produto — o orquestrador deve
apenas RECONFIRMAR com o humano antes de acionar o Builder (confirmação rápida, não um novo gate de
decisão), conforme a esteira definida: Block Orchestrator → Planner → [confirmação humana rápida] →
Builder (model override +1, opus) → QA (opus 4.8). Critérios de destaque/score/M1: READ-ONLY, sem
necessidade de aprovação adicional (bloco classificado como Média, não Crítica).

## Guardrails ativos
- §5 (CLAUDE.md): visualizações/relatórios não podem recalcular ou alterar `score_priorizacao`,
  `hex_score_estrutural`, carteira, plano curto prazo, plano de domínio ou artefatos oficiais do M1
  sem aprovação explícita. Este bloco é 100% READ-ONLY sobre o M1.
- §2 (CLAUDE.md): NUNCA acentuar identificadores; texto voltado ao usuário deve ter acentuação
  correta do português — aplica-se ao novo wording "hexágonos destacados" que substitui "amarelo(s)".
  Pontuação ASCII no PDF (fpdf2/`_ascii()`/latin-1): o novo texto usa só acentos padrão, sem
  travessão/bullet/seta/aspas curvas — sem risco de "?" silencioso.
- DEC-011 (+ emenda BLK-RELMUN-03): critério de "hexágono destacado" =
  `oferta_efetiva_disponivel >= OFERTA_DESTAQUE_MIN (2000)` — cor de exibição é camada de VISUALIZAÇÃO
  separada do critério; a emenda deste bloco (seção 3) deixa essa separação explícita no CLAUDE.md.
- Parâmetros canônicos imutáveis sem DEC (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0,
  pesos renda=0.40/pop=0.60): não tocados por este bloco (camada de display, não de score).
