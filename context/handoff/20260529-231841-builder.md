# Handoff — Builder — BLK-ARCH-01 (FATIA-1)

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-ARCH-01 FATIA-1 — Consolidação núcleo M1 + dashboard

## O que foi feito
Migração mecânica (import-rewiring) do legado de raiz/flat para `src/motor_expansao/`,
na ordem segura recomendada pelo Planner `0→1→2→3→4→6→5→7→8`. Nenhuma assinatura,
função, valor ou lógica alterada. `git mv`/`git rm` preservaram histórico.

- **Passo 0 (baseline + hashes):** `pytest -q` verde e 4 hashes sha256 capturados.
  DIVERGÊNCIA DE CONTAGEM ESPERADA: baseline real = `542 passed, 1 skipped` (não 532).
  Causa: os ciclos BLK-OPS-09 e BLK-OPS-10 foram mergeados após a baseline de 2026-05-28
  documentada no CLAUDE.md/handoff, adicionando ~10 testes. 0 falhas. Adotei
  `542 passed, 1 skipped` como MINHA baseline a preservar; os hashes são a prova formal
  de não-mutação. Reportado ao orquestrador.
- **Passo 1:** `git mv dashboard/constants.py → src/motor_expansao/dashboard/constants.py`;
  reapontados 9 call sites `dashboard.constants → motor_expansao.dashboard.constants`
  (não-teste + teste). Ajustado também `dashboard/utils.py:5` (ainda flat) para o novo
  caminho — necessário porque o flat `dashboard.utils` ainda era importado e quebraria
  a coleta (o Planner antecipou isso na nota do Passo 1). `pytest -q` → 542/1.
- **Passo 2:** `git mv dashboard/utils.py → src/motor_expansao/dashboard/utils.py`
  (já com o import de constants corrigido); reapontados 4 call sites
  `dashboard.utils → motor_expansao.dashboard.utils`. `pytest -q` → 542/1.
- **Passo 3:** `git rm dashboard/__init__.py` (vazio); diretório `dashboard/` flat removido.
  Grep `from dashboard(...)|import dashboard` (excl. context) → vazio. `pytest -q` → 542/1.
- **Passo 4:** `git mv ibge_censo.py → src/motor_expansao/pipelines/m1/ibge_censo.py`;
  import direto em `hex_enrichment.py` e `fase1_bi_exports.py` (removidos branches mortos
  `jobs.pipelines.ibge_censo`/`from ibge_censo`); reapontadas 10 ocorrências de teste.
  `pytest -q` → 542/1.
- **Passo 6 (antes do 5, conforme ordem recomendada):**
  `git mv config.py → src/motor_expansao/config.py`; substituídos os try/except
  `api.config`/`config` por `from motor_expansao.config import settings` em
  `core/constants.py`, `base_h3_brasil.py`, `hex_enrichment.py` e `poi_enrichment.py`
  (este ainda em raiz no momento, ajustado aqui porque o root `config` deixou de existir).
  `fora_primeira_fase/*` NÃO tocado (órfãos por design). `pytest -q` → 542/1.
- **Passo 5:** `git mv poi_enrichment.py → src/motor_expansao/pipelines/m1/poi_enrichment.py`
  (já config-fixed); import direto `POIEnricher` em `hex_enrichment.py` (removido último
  branch morto); reapontadas ~11 ocorrências `from poi_enrichment import` + 1 string de
  `patch("poi_enrichment.h3...")` em testes. `pytest -q` → 542/1.
- **Passo 7:** `git rm base_h3_brasil.py hex_enrichment.py fase1_bi_exports.py`;
  reapontados imports de teste (`test_scoring`, `test_hex_enrichment_brasil`,
  `test_fase1_bi_exports`, `test_base_h3_brasil`, `test_fontes_gratuitas`) e TODAS as
  strings `patch(...)`/`monkeypatch.setattr(...)` que referenciavam os módulos antigos.
  Limpo o fallback morto `from fase1_bi_exports import` interno em
  `hex_enrichment.py` (o primário `motor_expansao...fase1_bi_exports` já resolve).
  Removido o teste obsoleto `test_legacy_m1_wrappers_export_modules` (validava os wrappers
  que esta fatia remove por design → contagem cai 542→541). DESVIO JUSTIFICADO: dois
  módulos de `jobs/pipelines/*` (`fase_a_censo2022_setores.py`,
  `teste_setor_censitario_2010.py`) tinham `from hex_enrichment import ...` que dependia
  do wrapper removido e são exercidos por testes EM ESCOPO; reapontei só esses imports para
  `motor_expansao.pipelines.m1.hex_enrichment` (rewiring aditivo, sem mover/reestruturar
  jobs/pipelines — a migração estrutural permanece em BLK-ARCH-01a). Grep de módulos
  removidos → vazio. `pytest -q` → 541/1.
- **Passo 8:** `ruff check .` acusou 8 erros I001 (ordenação de imports, efeito mecânico
  das movimentações) → `ruff check --fix` (só reordenou imports, nenhuma lógica). mypy
  `src/` Success (só notas annotation-unchecked). `import streamlit_app` → ok. Hashes
  recapturados = idênticos ao Passo 0. `pytest -q` final → 541/1.

## Arquivos alterados
- `git mv config.py → src/motor_expansao/config.py`
- `git mv dashboard/constants.py → src/motor_expansao/dashboard/constants.py`
- `git mv dashboard/utils.py → src/motor_expansao/dashboard/utils.py` (+ import constants corrigido)
- `git mv ibge_censo.py → src/motor_expansao/pipelines/m1/ibge_censo.py`
- `git mv poi_enrichment.py → src/motor_expansao/pipelines/m1/poi_enrichment.py` (+ import config corrigido)
- `git rm base_h3_brasil.py hex_enrichment.py fase1_bi_exports.py` (wrappers de raiz)
- `git rm dashboard/__init__.py` (+ diretório `dashboard/` flat removido)
- Edição de import (src): `streamlit_app.py`, `core/constants.py`,
  `dashboard/{components,data,pages}.py`, `pipelines/m1/{base_h3_brasil,hex_enrichment,fase1_bi_exports}.py`
- Edição de import (testes): `tests/integration/{test_streamlit_app,test_fase1_bi_exports,test_expansao_dominio,test_base_h3_brasil,test_hex_enrichment_brasil}.py`,
  `tests/unit/{test_scoring,test_expansao_dominio,test_motor_expansao_import}.py`,
  `tests/contracts/test_fontes_gratuitas.py`
- Edição de import (DESVIO, jobs/pipelines — só rewiring de `from hex_enrichment import`):
  `jobs/pipelines/fase_a_censo2022_setores.py`, `jobs/pipelines/teste_setor_censitario_2010.py`
- Reordenação de imports por `ruff --fix` (8 arquivos; cosmético): `streamlit_app.py`,
  `dashboard/{components,pages}.py`, `pipelines/m1/{fase1_bi_exports,hex_enrichment}.py`,
  `tests/integration/{test_streamlit_app,test_fase1_bi_exports}.py`, `tests/unit/test_scoring.py`

## Prova de não-mutação M1 (hashes)
- ANTES (Passo 0):
  - c226954945ad0757a0429c84c43f410492c0ea7d15ca1d9b6a15f68727806567 data/staging/brasil_priorizados.parquet
  - 7baa07a2cbc0b7d8f2a8878932cae0ebf9400fce00e9c48c366c9903215f131b data/staging/brasil_estrutural.parquet
  - 805c65e28adfd800c7d5524e73fa9b0a044b992f17d617d0f2c6e40f9bbf61ca data/staging/hexagonos_brasil_oportunidades.parquet
  - 0cfb1015fc9df8b63776eb07ae3e666766905f768a4a78a406ae4d6b7cb6f618 data/outputs/hexagonos_brasil_dashboard.parquet
- DEPOIS (Passo 8):
  - c226954945ad0757a0429c84c43f410492c0ea7d15ca1d9b6a15f68727806567 data/staging/brasil_priorizados.parquet
  - 7baa07a2cbc0b7d8f2a8878932cae0ebf9400fce00e9c48c366c9903215f131b data/staging/brasil_estrutural.parquet
  - 805c65e28adfd800c7d5524e73fa9b0a044b992f17d617d0f2c6e40f9bbf61ca data/staging/hexagonos_brasil_oportunidades.parquet
  - 0cfb1015fc9df8b63776eb07ae3e666766905f768a4a78a406ae4d6b7cb6f618 data/outputs/hexagonos_brasil_dashboard.parquet
- Resultado: IDÊNTICOS

## Validações executadas
- pytest -q: 541 passed, 0 failed, 1 skipped (baseline real era 542/1; cai p/ 541 por
  remoção do teste obsoleto `test_legacy_m1_wrappers_export_modules`)
- import streamlit_app: ok
- ruff check .: limpo (após --fix de 8 I001 de ordenação)
- mypy src/: sem erros (Success em 23 arquivos; só notas annotation-unchecked, toleradas)
- grep imports legados (`from (base_h3_brasil|hex_enrichment|fase1_bi_exports|ibge_censo|poi_enrichment|config) import`, excl. fora_primeira_fase): vazio
- grep dashboard (`from dashboard...|import dashboard`, excl. context): vazio

## Problemas encontrados
1. Baseline divergente (542 vs 532 documentado) — explicado por merges BLK-OPS-09/10
   posteriores à baseline; 0 falhas. Não é regressão.
2. O plano não previa `tests/unit/test_motor_expansao_import.py::test_legacy_m1_wrappers_export_modules`,
   cujo único propósito era afirmar que os wrappers de raiz existem. Como a fatia os remove
   por design, removi esse teste obsoleto (in-scope do Passo 7). Contagem 542→541.
3. O plano não listou as strings de `patch(...)`/`monkeypatch.setattr(...)` que referenciam
   os módulos antigos por nome (em `test_fontes_gratuitas.py`, `test_base_h3_brasil.py`,
   `test_hex_enrichment_brasil.py`). Reapontadas todas (rewiring de teste, in-scope).
4. DESVIO controlado em `jobs/pipelines/*`: dois módulos out-of-scope dependiam do wrapper
   `hex_enrichment` removido e são importados por testes em escopo. Editei APENAS os imports
   (`from hex_enrichment import` → `from motor_expansao.pipelines.m1.hex_enrichment import`),
   sem mover nem reestruturar nada de jobs/pipelines. A migração estrutural de jobs/pipelines
   continua integralmente em BLK-ARCH-01a.

## Pendências
- BLK-ARCH-01a — Migrar `jobs/pipelines/*` (21 módulos) para `src/` e limpar `pythonpath`
  (`"."`). Os dois imports de hex_enrichment já apontam para o caminho canônico, o que
  facilita o sub-bloco.
- Atualizar CLAUDE.md/handoff baseline pytest para o número corrente (541 após esta fatia)
  quando o ciclo fechar — fora do escopo do Builder.

## Riscos remanescentes
- `fora_primeira_fase/*` mantém `from config import settings` órfãos por design (só
  importáveis via `"."` no pythonpath; não coletados). Esperado, não consertar.
- `pythonpath = ["."]` ainda mascara eventuais imports de raiz; mantido nesta fatia
  porque jobs/ depende. Removê-lo é tarefa de BLK-ARCH-01a.

## Guardrails verificados
- score_priorizacao não alterado: sim (nenhuma lógica/peso/parâmetro tocado)
- Artefatos M1 preservados (hash idêntico): sim (4/4 byte-idênticos)
- Dashboard offline mantido: sim (`import streamlit_app` ok; só import-rewiring)
- jobs/pipelines/* intocado: parcial — estrutura/lógica intocada; só 2 linhas de import
  reapontadas (DESVIO justificado acima) para manter testes verdes; migração real fica em BLK-ARCH-01a
- pythonpath inalterado: sim
