# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-ACENTO-01 — Acentuação da UI do dashboard (Streamlit)

## O que foi feito
Acentuação correta de todo o texto de EXIBIÇÃO da UI do dashboard, banimento de tipografia
"esperta" (travessão) nas strings voltadas ao usuário e criação da camada de LABEL de exibição
para faixas e elegibilidade híbrida — tudo READ-ONLY sobre o M1 (nenhum identificador, score,
enum bruto, nome de coluna ou artefato tocado). Decisões do gate humano (D1, D1-bis Opção A,
fallback "Não informado") implementadas exatamente como confirmadas em 2026-07-07.

Detalhe por eixo:
- **D1 (FAIXA_LABELS):** criado `FAIXA_LABELS` em `dashboard/constants.py` (não `core/constants.py`
  — correção do Planner respeitada). Mapeamento aprovado: prioridade_maxima→"Prioridade máxima",
  alta→"Alta", media→"Média", baixa→"Baixa", descartado→"Descartado", inviavel→"Inviável".
  Usado via `format_func` no multiselect de Faixa (`pages.py`), em `render_faixa_legend`
  (`components.py`). O valor bruto de `FAIXA_ORDEM` e as chaves de `FAIXA_COLORS` ficaram INTOCADOS.
  `build_faixa_comparison_figure` NÃO regrediu cor: já usava `color="faixa_label"` derivado do
  valor bruto categórico + `color_discrete_map=FAIXA_COLORS` — nada mudou lá (apenas o eixo x já
  mostrava o bruto; não foi alterado, evitando qualquer regressão de cor).
- **D1-bis (Opção A):** criado `HYBRID_ELIGIBILITY_LABELS = {"Elegivel":"Elegível",
  "Nao elegivel":"Não elegível","Sem camada":"Sem camada"}` em `constants.py`; aplicado via
  `format_func` no multiselect de Elegibilidade. O valor bruto gravado em `elegibilidade_hibrida`
  (`data.py`) e `HYBRID_ELIGIBILITY_ORDER` ficaram INTOCADOS; ZERO fixture de teste de
  elegibilidade alterada.
- **Fallback "Nao informado" → "Não informado":** trocado nas 5 ocorrências juntas
  (`data.py` x3 incluindo `categories=FAIXA_ORDEM + ["Não informado"]`, `components.py` x2). A
  categoria `pd.Categorical` continua casando 1:1 (testes verdes).
- **Labels de configuração:** `COLOR_MODES` ("Híbrido","Censitário","Expansão de Domínio") e
  `OVERLAYS` ("Âncoras Domínio") acentuados (só o valor "label"; as chaves/ids intocados).
- **Acentuação ampla de strings de exibição:** `pages.py` (~334 substituições), `components.py`
  (tooltips do mapa, legendas, KPIs, títulos/labels de gráficos Plotly, cabeçalhos de tabela
  renomeados, valores "Sim/Não", alertas híbridos, expander de proveniência), `streamlit_app.py`
  (2 strings), `data.py` (mensagem de ValueError exibida via st.error).
- **Cabeçalhos de tabela renomeados** (display) acentuados com os seus selectores/downstream
  atualizados em lockstep: `build_ranking_table`+`style_ranking_table` ("Score Priorização",
  "Viável"), `build_hybrid_top_hexes_table`, `build_hybrid_municipios_table`,
  `build_hybrid_portfolio_table` (listas de `.map` "Sim/Não" e primary_labels ajustadas junto).
- **Tipografia:** todos os travessões `—` em strings de EXIBIÇÃO trocados por `-` (com espaços)
  em `pages.py`/`components.py` (incl. `<h1>` do hero, badges HTML, títulos de seção, botões
  "Baixar PDF - {nome}"). Travessões em comentários/docstrings foram DEIXADOS como estão (fora do
  escopo "texto voltado ao usuário", para não inflar o diff). Nenhum bullet/seta/reticências/aspas
  curvas/© introduzido.
- **Testes:** asserts acoplados atualizados em lockstep (caption do mapa, "não afeta score",
  tooltips "Score Censitário 2022"/"Score Híbrido", warnings híbrido/domínio, labels de botões de
  lote "Gerar Relatórios (N)"/"Baixar PDF - X", "3/3 concluído", `match="colunas obrigatórias"`,
  expander "Análise", label "Tese de domínio"). Adicionado teste novo
  `test_faixa_e_elegibilidade_labels_via_format_func` (D1/D1-bis): confirma os mapeamentos de
  label e que os `format_func` dos multiselects devolvem o texto acentuado enquanto `options`
  seguem os valores BRUTOS.

## Arquivos alterados
- `src/motor_expansao/dashboard/constants.py` — +FAIXA_LABELS, +HYBRID_ELIGIBILITY_LABELS;
  acentuação dos "label" de COLOR_MODES ("Híbrido"/"Censitário"/"Expansão de Domínio") e OVERLAYS
  ("Âncoras Domínio"). Enums brutos / ids / cores intocados.
- `src/motor_expansao/dashboard/components.py` — import de FAIXA_LABELS; `render_faixa_legend`
  usa FAIXA_LABELS; acentuação de tooltips do mapa (m1/hibrido), legendas, KPIs de
  `build_business_answers`/`build_indicator_snapshot`, títulos/labels de gráficos, cabeçalhos das
  tabelas de ranking/híbrido + selectores downstream (`style_ranking_table`), valores "Sim/Não",
  alertas híbridos, expander de proveniência; travessões de exibição → "-"; "Nao informado" →
  "Não informado" (x2).
- `src/motor_expansao/dashboard/data.py` — "Nao informado" → "Não informado" (x3, incl. categoria
  do `pd.Categorical`); mensagem de ValueError acentuada (exibida via st.error).
- `src/motor_expansao/dashboard/pages.py` — import de FAIXA_LABELS/HYBRID_ELIGIBILITY_LABELS;
  format_func nos multiselects de Faixa e Elegibilidade; ~340 strings de exibição acentuadas
  (labels, help=, placeholder=, st.metric/caption/markdown/info/warning, HTML de markdown,
  títulos/labels/anotações de Plotly, cabeçalhos de rename + selectores em lockstep, botões,
  progress text); travessões de exibição → "-"; `<h1>` do hero corrigido.
- `streamlit_app.py` — `st.info` "análise", `st.caption` "hexágonos".
- `tests/integration/test_streamlit_app.py` — asserts atualizados em lockstep + novo teste
  `test_faixa_e_elegibilidade_labels_via_format_func`.
- `tasks/current_task.md` — status: aguardando QA.

## Validações executadas
- ruff (`python -m ruff check .`): **All checks passed!** (0 erros).
- mypy (`python -m mypy src`): **7 erros PRÉ-EXISTENTES e ambientais** — todos
  "Library stubs not installed for requests" em arquivos NÃO tocados por este bloco
  (`dimensionamento/growth_api_client.py`, `pipelines/m1/base_h3_brasil.py`,
  `pipelines/m1/ibge_censo.py`, `pipelines/m1/poi_enrichment.py`, `api/geo.py`,
  `api/telegram_bot.py`, `api/service.py`). **ZERO erro novo** nos arquivos do bloco
  (dashboard/*, streamlit_app.py). Causa: `types-requests` não instalado no ambiente.
- import smoke (`python -c "import streamlit_app"`): **import ok**.
- pytest FOCADO (`pytest -q tests/integration/test_streamlit_app.py
  tests/unit/test_dashboard_format_utils.py`): **234 passed, 0 failed** (11941 warnings de
  DeprecationWarning do pytest-asyncio no Python 3.14 — pré-existentes, não relacionadas).
- py_compile de todos os arquivos editados: OK.
- Sweep de tipografia nos arquivos editados: 0 travessão/bullet/seta/reticências/aspas curvas/©
  em strings de EXIBIÇÃO (remanescentes só em comentários/docstrings, fora de escopo).
- Sweep de tokens de exibição capitalizados (Relatorio/Analise/Populacao/Municipio/Expansao/
  Dominio/Censitario/Hibrido/...): 0 em strings de exibição (remanescentes só em comentários/
  docstrings e em VALORES BRUTOS de comparação `"Estrategico"/"Tatica"` intocados de propósito).

## Problemas encontrados
- `pytest -n auto` (xdist) abortou com `INTERNALERROR EOFError` do execnet no Python 3.14 —
  instabilidade AMBIENTAL do worker paralelo, NÃO falha de teste. A execução SERIAL do mesmo
  subconjunto passou 234/234. (Registrado honestamente; sem bypass.)
- Bloqueio do Smart App Control (WDAC) sobre a DLL do h3 NÃO ocorreu neste ambiente:
  `import h3` = 4.5.0 e a coleta/execução do pytest rodaram normalmente.

## Pendências
- QA deve rodar a SUÍTE COMPLETA (gate único). Recomendado SERIAL (`pytest -q`) para evitar a
  instabilidade do xdist no Python 3.14 deste ambiente.
- Revisão visual humana da UI (critério de aceite do Planner): filtro/legenda de faixa, gráfico de
  comparação por faixa (cores por faixa preservadas), Carteira e Plano, Expansão de Domínio.

## Riscos remanescentes
- Cabeçalhos de tabela renomeados são acoplados por string a selectores downstream
  (`style_ranking_table`, listas `primary_labels`/`for col in [...]`, `.map`); todos foram
  atualizados em lockstep e a suíte focada está verde, mas a revisão visual das tabelas confirma
  que nenhuma coluna "sumiu".
- A coluna "Faixa" de `build_ranking_table` segue exibindo o VALOR BRUTO (comportamento
  pré-existente; `style_ranking_table.faixa_style` depende do bruto para cor e do check
  `!= "descartado"`) — não foi alterada para não introduzir regressão de cor (fora do escopo dos
  3 call sites do gate D1).

## Guardrails verificados
- score_priorizacao não alterado: sim (nenhuma fórmula/peso/artefato tocado; só camada de exibição).
- Artefatos M1 preservados: sim (git diff restrito a dashboard/*, streamlit_app.py, teste;
  nenhum parquet, `config.py`, `core/constants.py`, `censo_*`, `relatorio_municipal.py`,
  `pipelines/m1`, `calcular_colunas_mercado.py` tocado).
- Dashboard offline mantido: sim (nenhuma dependência de rede nova).
- Identificadores intocados: sim (key=/.st-key-*/enums brutos FAIXA_ORDEM/HYBRID_ELIGIBILITY_ORDER/
  nomes de coluna/slugs/`"classico"`/METODO_* preservados; camada de LABEL de exibição usada).
