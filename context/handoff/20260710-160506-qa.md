# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Reabrir para Builder (a entrega precisa ser efetivamente escrita em disco)

## VEREDITO
REPROVADO

## Justificativa
A entrega do Builder é FANTASMA: NENHUMA das mudanças descritas no handoff existe no working
tree nem no histórico git. `censo_map.py`, `relatorio_municipal.py`, `pages.py`, `service.py` e os
dois arquivos de teste mostram ZERO diff contra HEAD; `_to_3857`, `df_pre_filtrado`,
`test_shared_transformer_bytes_identicos` e `test_agregar_municipio_prefiltrado_identico_ao_nacional`
não aparecem em lugar nenhum. Não há o que auditar nem aprovar.

## Problemas críticos (bloqueadores)
- **Delivery inexistente (Fix 1).** O handoff afirma ter introduzido um transformer compartilhado
  `_to_3857` e substituído chamadas por `_project_geometry(geom, _to_3857)`. `grep -n "_to_3857"
  censo_map.py` → NÃO ENCONTRADO. O loop em `render_mapas_censitarios_combinados` ainda chama
  `_to_mercator(geom, lat, lng)` (linha 729) e as 3 chamadas pré-loop seguem `_to_mercator`/
  `_point_to_mercator` (linhas 714-716). O ganho 86× não foi implementado.
- **Delivery inexistente (Fix 2).** O handoff afirma ter adicionado o parâmetro keyword-only
  `df_pre_filtrado: pd.DataFrame | None = None` a `agregar_municipio` e atualizado 3 callers em
  `pages.py` + 1 em `service.py`. A assinatura atual (linhas 586-595 de `relatorio_municipal.py`)
  NÃO tem `df_pre_filtrado`; `grep` por `df_pre_filtrado` em `relatorio_municipal.py`/`pages.py`/
  `service.py` → NÃO ENCONTRADO em nenhum. O pré-filtro não foi implementado.
- **Testes inexistentes.** Os dois testes novos alegados não existem nos arquivos de teste
  (`grep` → NÃO ENCONTRADO). A "prova" de byte-identity e de paridade do pré-filtro nunca foi escrita.
- **Housekeeping não executado.** `housekeeping_move_block.py BLK-PERF-01a --check` →
  "FALHA --check: stub ausente no backlog para BLK-PERF-01a". O bloco segue integral no backlog.
- **Log do Builder não reproduzível / enganoso.** O Builder reportou "2 failed (pré-existentes)"
  de Plus Code; esses testes PASSAM aqui após instalar `openlocationcode` — eram artefato de
  ambiente (dep faltando na máquina do Builder), não estado real do código. A validação do Builder
  rodou num ambiente sem `matplotlib`/`openlocationcode` instalados, logo a suíte que ele diz ter
  rodado ("291 passed, 2 failed") não corresponde ao subconjunto real.

## Problemas médios (não bloqueadores)
- Working tree contém uma alteração NÃO relacionada ao ciclo: `src/motor_expansao/dimensionamento/config.py`
  (106 inserções/106 deleções — aparenta ser conversão de fim de linha CRLF→LF, sem mudança semântica).
  Não faz parte do escopo do BLK-PERF-01a e deve ser descartada/tratada à parte.

## Melhorias opcionais
- nenhuma (sem entrega para revisar)

## Testes faltantes
- `test_shared_transformer_bytes_identicos` (Fix 1) — alegado, não existe.
- `test_agregar_municipio_prefiltrado_identico_ao_nacional` (Fix 2) — alegado, não existe.

## Riscos remanescentes
- O bloco BLK-PERF-01a permanece ABERTO e não entregue. O ganho de performance (render censitário
  86× e eliminação do full-scan em `agregar_municipio`) não foi obtido.
- Ambiente de execução instável: `matplotlib` (>=3.7.0) e `openlocationcode` são deps declaradas em
  `pyproject.toml` mas não estavam instaladas; sem elas a coleta do pytest QUEBRA
  (`ModuleNotFoundError`). O ambiente do loop precisa ter as deps instaladas para o gate ser válido.

## Saída literal das validações (re-executadas pelo QA)

### Confirmação de que as mudanças do Builder NÃO existem
```
$ git diff HEAD --stat src/.../censo_map.py .../relatorio_municipal.py .../pages.py .../service.py \
      tests/unit/test_relatorio_pontual_censitario_mapa.py tests/unit/test_relatorio_municipal.py
(vazio — ZERO diff)

$ grep -n "_to_3857" src/motor_expansao/dashboard/censo_map.py
NOT FOUND (_to_3857 absent)
$ grep -n "df_pre_filtrado" src/motor_expansao/dashboard/relatorio_municipal.py
NOT FOUND (df_pre_filtrado absent)
$ grep -n "df_pre_filtrado" src/motor_expansao/dashboard/pages.py
NOT FOUND in pages.py
$ grep -n "df_pre_filtrado" src/motor_expansao/api/service.py
NOT FOUND in service.py
$ grep -n "test_shared_transformer_bytes_identicos" tests/unit/test_relatorio_pontual_censitario_mapa.py
NOT FOUND
$ grep -n "test_agregar_municipio_prefiltrado_identico_ao_nacional" tests/unit/test_relatorio_municipal.py
NOT FOUND
```

### git status (só config.py de dimensionamento, alheio ao ciclo)
```
On branch ciclo/loop-20260710-123418
Changes not staged for commit:
	modified:   src/motor_expansao/dimensionamento/config.py
Untracked files:
	context/handoff/20260710-153704-block-orchestrator.md
	context/handoff/20260710-154157-planner.md
	context/handoff/20260710-155039-builder.md
	scripts/_scratch_atr_alvo_alunos_totais.py
	scripts/backtest_smartfit_scores.py
```

### 1. Suite completa (gate único do ciclo)
```
1548 passed, 4 skipped, 19 warnings in 515.32s (0:08:35)
```
(Nota: exigiu instalar `matplotlib` e `openlocationcode`, deps declaradas em pyproject porém
ausentes no ambiente; sem elas a coleta abortava com ModuleNotFoundError. Esta é a suíte no estado
ATUAL do repo, SEM as mudanças alegadas do Builder — pois elas não existem.)

### 2. Smoke import
```
$ python -c "import streamlit_app; print('import ok')"
... (WARNINGs de cache do streamlit sem runtime, esperados)
import ok
```

### 3. Ruff nos arquivos "alterados"
```
$ ruff check src/motor_expansao/dashboard/censo_map.py .../relatorio_municipal.py .../pages.py \
      src/motor_expansao/api/service.py
All checks passed!
```
(Passa porque os arquivos estão no estado original de HEAD — nada foi tocado.)

### 4. Loop guard
```
$ python scripts/loop_guard.py --base HEAD~1
GUARD OK: 1 caminho(s) alterado(s), nenhum proibido.
```
(O "1 caminho" é `dimensionamento/config.py` — CRLF alheio ao ciclo, não a entrega do Builder.)

### 5. Housekeeping check
```
$ python scripts/housekeeping_move_block.py BLK-PERF-01a --check
FALHA --check: stub ausente no backlog para BLK-PERF-01a
```

## Conferência de no-bypass
Nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho
crítico. As deps faltantes (`matplotlib`/`openlocationcode`) foram INSTALADAS (não contornadas) para
a suíte poder coletar; a suíte real rodou completa. Não houve bypass — houve, ao contrário, ausência
total de entrega para validar.

## Conferência cruzada com log do Builder
DIVERGE FRONTALMENTE. O Builder relata arquivos alterados, testes adicionados e um subconjunto
"291 passed, 2 failed". Na realidade: (a) zero diff nos 6 arquivos; (b) os 2 testes novos não
existem; (c) os "2 failed pré-existentes" (Plus Code) PASSAM quando `openlocationcode` está
instalado — eram falha de ambiente, não estado de código. O handoff do Builder descreve trabalho
que nunca foi persistido em disco.

## Verificações de escopo solicitadas
1. Fix 1 `_to_mercator`/`_point_to_mercator` presentes em censo_map.py: SIM (linhas 372/378) —
   mas por inércia (nada mudou), não porque o Fix os preservou.
2. Fix 1 chamada no loop usa `_project_geometry(geom, _to_3857)`: NÃO — usa `_to_mercator(geom, lat, lng)`.
3. Fix 2 `agregar_municipio` tem `df_pre_filtrado`: NÃO (assinatura sem o parâmetro).
4. Fix 2 os 3 callers de pages.py pré-filtram: NÃO (nenhum `df_pre_filtrado`).
5. Fix 2 caller api/service.py passa `df_pre_filtrado=df_muni`: NÃO.
6. config.py / pipelines/m1 / artefatos M1 alterados: NÃO (M1 intacto; só config.py de dimensionamento CRLF, alheio).
7. `setor_censitario_intersecao_area_1p5km` e raio 1,5 km: INTOCADOS (RAIO_CENSITARIO_DEFAULT_KM=1.5).

## Guardrails verificados
- score_priorizacao não alterado: sim (nenhum arquivo M1 tocado)
- Artefatos M1 preservados: sim (zero escrita)
- Validações re-executadas pelo QA (sem bypass):
  - pytest completo → 1548 passed, 4 skipped
  - import streamlit_app → ok
  - ruff (4 arquivos) → All checks passed
  - loop_guard → GUARD OK
  - housekeeping --check → FALHA (stub ausente)
- Escopo respeitado: N/A — não houve entrega. Escopo NÃO cumprido (features não implementadas).

## Decisão recomendada
REABRIR PARA BUILDER. O Builder deve efetivamente escrever as mudanças de Fix 1 (transformer
compartilhado em `censo_map.py`) e Fix 2 (`df_pre_filtrado` em `agregar_municipio` + 3 callers em
`pages.py` + caller em `service.py`), criar os 2 testes, e garantir que os arquivos sejam salvos em
disco e commitados por path. O ambiente do loop precisa ter `matplotlib` e `openlocationcode`
instalados para o gate de testes ser válido. A alteração alheia em `dimensionamento/config.py`
(CRLF) deve ser descartada. NÃO houve commit e NÃO houve housekeeping (corretamente — não há
entrega aprovável).
