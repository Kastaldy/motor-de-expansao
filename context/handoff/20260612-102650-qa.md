# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Fechamento manual (registrar RECORTE em `completed.md` SEM mover/fechar o bloco amplo BLK-UI-01).

## VEREDITO
APROVADO

## Justificativa
Recorte do BLK-UI-01 (sidebar/loading/limpeza visual) entregue como mudanças puramente CSS/markdown/spinner/strings, READ-ONLY sobre o M1; D1..D9 aplicados exatamente conforme o gate humano; ruff/mypy/import/suíte-alvo verdes. A suíte full apresenta 3 failures PRÉ-EXISTENTES, provados idênticos no tree limpo (com as mudanças deste ciclo stashed) e não relacionados ao escopo de UI — não são regressão deste ciclo.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- 3 failures PRÉ-EXISTENTES na suíte full (independentes deste ciclo; comprovados no tree limpo):
  1. `test_csvs_concorrentes_legiveis[csv_path1-223]` → 226 != 223 (drift de snapshot do CSV real local `concorrentes/unidades_*.csv`, gitignored).
  2. `test_csvs_concorrentes_legiveis[csv_path2-472]` → 455 != 472 (idem, `unidades_panobianco.csv`).
  3. `test_parquet_final_respeita_guardrails_do_piloto` → 86 hexes com `flag_sam & flag_canibalizacao_ultra_1km` (esperado 0). É o gate DEC-006: a regeneração dos parquets paralelos de mercado é "passo OPERACIONAL pós-merge separado, NÃO requisito de fechamento de testes". O parquet servido ainda reflete o gate antigo.
  - Recomendação: tratar como dívida operacional fora deste ciclo (regenerar `concorrentes/*` snapshot + parquets paralelos de mercado). Não bloqueia o recorte de UI.

## Melhorias opcionais
- nenhuma

## Testes faltantes
- nenhum (mudanças são CSS/markdown/spinner/strings; o teste de integração já foi ajustado com `mock.patch("streamlit.spinner")`; nenhum teste asseria os captions/pills removidos).

## Riscos remanescentes
- Sidebar `expanded` por padrão (D1=A) consome espaço horizontal em telas estreitas — trade-off de produto aceito no gate.
- Os 3 failures pré-existentes seguem vermelhos até regeneração de dados/parquets paralelos (fora deste ciclo).

## Saída literal das validações (re-executadas pelo QA)

### 1a. Suíte FULL com `-n auto` (tentativa — bug de AMBIENTE execnet × Python 3.14)
```
$ python -m pytest -n auto -q
INTERNALERROR>     node.setup()
INTERNALERROR>     self.gateway._rinfo()
INTERNALERROR>   File ".../xdist/workermanage.py", line 331, in setup
INTERNALERROR>   File ".../execnet/gateway.py", line 89, in _rinfo
INTERNALERROR>     ch.waitclose()
INTERNALERROR>   File ".../execnet/gateway_base.py", line 571, in from_io
INTERNALERROR>     raise EOFError("couldn't load message header, " + e.args[0]) from None
INTERNALERROR> EOFError: couldn't load message header, expected 9 bytes, got 0
```
REPRODUZIDO o INTERNALERROR de gateway do xdist/execnet no Python 3.14 LOCAL — problema de AMBIENTE conhecido, NÃO regressão. Conforme instruído, NÃO mascarei com `-p no:xdist`; rodei a suíte SERIAL (1b) como gate único.

### 1b. Suíte FULL SERIAL (gate único do ciclo)
```
$ python -m pytest -q
=========================== short test summary info ===========================
FAILED tests/integration/test_modelo_mercado_hexagonos.py::test_csvs_concorrentes_legiveis[csv_path1-223]
FAILED tests/integration/test_modelo_mercado_hexagonos.py::test_csvs_concorrentes_legiveis[csv_path2-472]
FAILED tests/integration/test_modelo_mercado_hexagonos.py::test_parquet_final_respeita_guardrails_do_piloto
3 failed, 695 passed, 1 skipped, 71388 warnings in 152.84s (0:02:32)
```
Rodado SERIAL por causa do bug execnet×3.14 (ver 1a). Os 3 failed são PRÉ-EXISTENTES e fora do escopo de UI — prova de pré-existência em 1c.

### 1c. Prova de PRÉ-EXISTÊNCIA dos 3 failures (mudanças do ciclo stashed → tree limpo)
```
$ git stash push -- streamlit_app.py src/motor_expansao/dashboard/pages.py tests/integration/test_streamlit_app.py
Saved working directory and index state WIP on ciclo/BLK-UI-01: 5af6e2c ...
$ python -m pytest -q tests/integration/test_modelo_mercado_hexagonos.py::test_csvs_concorrentes_legiveis tests/integration/test_modelo_mercado_hexagonos.py::test_parquet_final_respeita_guardrails_do_piloto
=========================== short test summary info ===========================
FAILED tests/integration/test_modelo_mercado_hexagonos.py::test_csvs_concorrentes_legiveis[csv_path1-223]
FAILED tests/integration/test_modelo_mercado_hexagonos.py::test_csvs_concorrentes_legiveis[csv_path2-472]
FAILED tests/integration/test_modelo_mercado_hexagonos.py::test_parquet_final_respeita_guardrails_do_piloto
3 failed, 1 passed, 739 warnings in 2.24s
$ git stash pop   # mudanças do ciclo restauradas (diff confirmado intacto)
```
CONCLUSÃO: os 3 failures falham IDÊNTICOS no tree limpo (sem as mudanças deste ciclo) → 100% pré-existentes, ZERO regressão introduzida pelo recorte de UI. Detalhe das causas:
- csv_path1/csv_path2: `assert len(df) == expected_rows` contra CSVs reais locais em `concorrentes/` (gitignored); contagem real divergiu do hardcode (226 vs 223; 455 vs 472) — drift de snapshot de dados, não de código.
- parquet guardrail: `flag_sam & flag_canibalizacao_ultra_1km` = 86 sobre 1.542.531 linhas do `hexagonos_mercado_mapeado.parquet` → gate DEC-006 (regeneração de parquets paralelos é passo pós-merge operacional, não requisito de fechamento de testes).

### 2. Suíte alvo
```
$ python -m pytest -q tests/integration/test_streamlit_app.py
182 passed, 9101 warnings in 14.25s
```

### 3. Smoke import
```
$ python -c "import streamlit_app; print('import ok')"
... (WARNING streamlit.runtime.caching: No runtime found — esperado fora de runtime)
import ok
```

### 4a. Lint (ruff) no escopo tocado
```
$ python -m ruff check streamlit_app.py src/motor_expansao/dashboard/pages.py tests/integration/test_streamlit_app.py
All checks passed!
```

### 4b. Type (mypy) no escopo tocado
```
$ python -m mypy streamlit_app.py src/motor_expansao/dashboard/pages.py
Success: no issues found in 2 source files
```

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real, ou mock do caminho crítico. A suíte full rodou SERIAL apenas por causa do bug de ambiente execnet×Python 3.14 (INTERNALERROR de gateway, reproduzido e colado em 1a) — NÃO por `-p no:xdist` nem por qualquer contorno de config/artefato. A prova de pré-existência (1c) usou `git stash` legítimo das mudanças do ciclo, sem alterar config ou dados. Este ciclo não envolve tooling de produção (SOPS/segredos), então a regra de no-bypass de tooling não tem caminho aplicável aqui; os testes pytest usam fixtures/dados reais normalmente.

## Conferência cruzada com log do Builder
Bate.
- Suíte alvo: Builder reportou 182 passed → QA reproduziu 182 passed. ✔
- import ok: ✔. ruff "All checks passed!": ✔. mypy "Success: no issues found in 2 source files": ✔.
- Bug execnet×3.14 com `-n auto`: o Builder reportou; o QA REPRODUZIU literalmente (1a). ✔
- Diff confere com o handoff do Builder linha a linha (D1..D9): ✔ (auditado via `git diff HEAD`).
- O Builder NÃO commitou (working tree com mudanças não-commitadas) e NÃO tocou nos paths pré-sujos: ✔ confirmado (`git status` mostra `streamlit_app.py`/`pages.py`/`test_streamlit_app.py` modificados; os paths pré-sujos `_metadata.json` e `relatorio_pontual_censitario_base_geo.md` seguem modificados de antes, NÃO foram tocados por este ciclo).
- Builder não mencionou os 3 failures da suíte full porque rodou só a alvo (a full é gate do QA) — QA assumiu, rodou e provou pré-existência. Sem divergência material.

## Auditoria das decisões do gate (D1..D9 — aderência EXATA)
- D1=A (sidebar `expanded`): ✔ `initial_sidebar_state="collapsed"` → `"expanded"` em `st.set_page_config`.
- D2=B (borda ciano 2px + box-shadow): ✔ `border-right: 2px solid rgba(25, 183, 255, 0.45)` + `box-shadow: 4px 0 24px rgba(0, 0, 0, 0.35)` no bloco `[data-testid="stSidebar"]`; sem `!important`.
- D3=B (cabeçalho estilizado na sidebar; selectbox de UF permanece 1º widget/gate): ✔ `### Filtros globais` → bloco HTML decorativo (`unsafe_allow_html=True`), caption preservado, `st.sidebar.selectbox("UF", …)` segue o PRIMEIRO widget de INPUT (header e caption são markdown decorativo ANTES); `render_uf_selectbox` chamado antes de `load_uf_slice` em `main()` (gate da carga lazy preservado).
- D4 (3 spinners): ✔ "Carregando dados da UF..." (`load_uf_slice`, dentro do `try`/`except` preservado), "Construindo mapa..." (envolve só `deck, n_points = build_unified_map_figure(...)`; `if deck is None:` e render FORA do `with`), "Gerando mapas censitarios..." (`render_mapas_censitarios_combinados`). Strings idênticas ao gate.
- D5=A (remover caption proveniência, manter "Recorte atual"): ✔ 2º caption "Base oficial preservada: …" removido; 1º caption "Recorte atual: …" intacto.
- D6=A (3 pills "Onde expandir (M1)"/"Qual bairro (Censitario)"/"Fila operacional (Hibrido)", sem nome de coluna): ✔ 5 pills → 3 pills sem `score_priorizacao`/`score_setor_2022_calibrado`/`score_expansao_hibrido` nem lista de UFs.
- D7=A (remover caption pydeck estado vazio; PRESERVAR caption centroide H3): ✔ caption de limitações do pydeck (estado vazio, ~linha 2040) removido; caption de centroide H3 (estado COM ponto, linhas 2061-2064) PRESERVADO — verificado por leitura e por ausência no diff.
- D8=A (consolidar captions do híbrido em 1): ✔ dois `st.caption()` de `render_modelo_hibrido_v2` → um único.
- D9=A (não mexer em legendas): ✔ nada feito em legendas/expander (sem ocorrência no diff).

## Guardrails verificados
- score_priorizacao não alterado: **sim**. `git diff` dos bodies não toca nenhum identificador de score; o único match de `load_uf_slice` é re-indentação sob `with st.spinner(...)`, a CHAMADA inalterada.
- Artefatos M1 preservados: **sim**. Nenhuma escrita em `score_priorizacao`/`hex_score_estrutural`/`score_setor_2022_calibrado`/`score_expansao_hibrido`/`score_oportunidade_residual`/SAM/carteira/plano/parquets oficiais. Parâmetros canônicos do §3 (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0, renda=0.40, pop=0.60) INALTERADOS.
- Contratos de performance (Blocos 4/5/6) intactos: **sim**. `load_uf_slice`/`read_enriched_uf_partition`/`load_uf_catalog` (Bloco 4), `render_tab_selector`/`st.segmented_control` (Bloco 5), `_downsample_map_index`/`MAP_SOURCE_COLUMNS_*` (Bloco 6) com CORPO INTOCADO — apenas wrappers `with st.spinner(...)` por fora de `load_uf_slice`/`build_unified_map_figure`/`render_mapas_censitarios_combinados`.
- Sem dependência de API ao vivo introduzida: **sim**. Spinners são context managers decorativos; DEC-004 (basemap) inalterada; carga/interatividade do dashboard segue offline.
- `render_uf_selectbox` é o PRIMEIRO widget de input da sidebar (gate da carga lazy): **sim**.
- Validações re-executadas pelo QA (sem bypass): pytest full serial → 3 failed pré-existentes / 695 passed / 1 skipped; pytest alvo → 182 passed; import → ok; ruff → All checks passed!; mypy → Success.
- Escopo respeitado: **sim** — só `streamlit_app.py`, `pages.py`, `test_streamlit_app.py`; CSS/markdown/spinner/strings; nada fora do escopo permitido do BLK-UI-01.

## Housekeeping
N/A neste ciclo. A entrega é um RECORTE do BLK-UI-01 amplo (3 melhorias: sidebar/loading/limpeza visual), NÃO a refatoração ampla das 4 abas. NÃO rodei o helper de move e NÃO fechei o bloco BLK-UI-01 — fechá-lo seria prematuro (as demais frentes de UX das 4 abas seguem pendentes). O bloco amplo BLK-UI-01 PERMANECE ABERTO no backlog. Recomendação ao orquestrador: registrar este recorte em `completed.md` como ENTREGA DE RECORTE, sem mover/fechar o bloco amplo via `scripts/housekeeping_move_block.py`.

## Decisão recomendada
Fechar o ciclo do RECORTE: registrar a entrega de recorte em `completed.md` SEM fechar o bloco amplo BLK-UI-01 (permanece aberto para as demais frentes). Abrir/registrar como dívida operacional separada (fora deste ciclo) a regeneração dos snapshots de `concorrentes/*` e dos parquets paralelos de mercado (DEC-006) para zerar os 3 failures pré-existentes da suíte full.

## Paths pré-sujos (NÃO tocados pelo QA — alheios ao ciclo)
- data/outputs/setores_censitarios_2022_geo/_metadata.json
- data/reports/relatorio_pontual_censitario_base_geo.md
