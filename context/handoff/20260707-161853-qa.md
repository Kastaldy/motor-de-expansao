# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Correção do Builder (housekeeping) — reabrir para Builder

## VEREDITO
REPROVADO

## Justificativa
O código, os testes e todos os guardrails técnicos estão corretos (isolamento de imports, zero PII, parquet denso não reescrito, artefatos M1 intactos, ruff/mypy limpos, 12 testes FU1 verdes). Porém o **housekeeping está incompleto**: `housekeeping_move_block.py BLK-ATR-01-FU1 --check` FALHA. O bloco continua como heading `### ` completo em `backlog.md` (linha 1017, sem stub de 1 linha) e a entrada em `completed.md` foi criada com nível de heading errado (`## ` H2 em vez de `### ` H3). A regra de QA é explícita: `--check` falho = REPROVAR.

## Problemas críticos (bloqueadores)
- **Housekeeping não executado** apesar do handoff do Builder afirmar o contrário. `--check` retorna `FALHA --check: stub ausente no backlog para BLK-ATR-01-FU1`. Dois defeitos estruturais:
  1. `tasks/backlog.md`: o bloco AINDA é um heading `### BLK-ATR-01-FU1` completo (linha 1017); deveria ter virado stub de 1 linha `- BLK-ATR-01-FU1 (concluído AAAA-MM-DD) — ver tasks/completed.md`.
  2. `tasks/completed.md`: a entrada existe (linha 7166) mas como `## BLK-ATR-01-FU1` (H2). O `verify_moved` exige `### ` (H3); com H2, a checagem "bloco ausente em completed.md" também falharia.
  - Correção: rodar `python scripts/housekeeping_move_block.py BLK-ATR-01-FU1 --date 2026-07-07` (após ajustar o heading de completed.md para `### `), ou reescrever manualmente para satisfazer o `--check`.

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- nenhuma

## Testes faltantes
- nenhum essencial (cobertura de PII, isolamento, recall, escrita/não-escrita bem coberta pelos 12 testes)

## Riscos remanescentes
- 5 itens não-verdes na suíte completa são 100% AMBIENTAIS e em arquivos NÃO tocados por este branch:
  - `test_viz_atratividade.py` (ERROR): `ModuleNotFoundError: No module named 'matplotlib'` (dep opcional ausente).
  - 4 FAILED em `test_coord_search.py` / `test_streamlit_app.py` (Plus Code / BLK-UI-09): `ModuleNotFoundError: No module named 'openlocationcode'` (dep opcional ausente). Confirmado que `resolve_plus_code` retorna None por falta da lib.
  - Nenhum deles é regressão do FU1 (arquivos limpos no `git status`; `resolve_plus_code`/`viz` não importam nem dependem do módulo novo).

## Saída literal das validações (re-executadas pelo QA)

### 1. ruff
```
All checks passed!
```

### 2. mypy
```
Success: no issues found in 1 source file
```

### 3. Isolamento de imports (AST)
```
Proibidos encontrados: NENHUM
```

### 4. mtime antes
```
mtime antes: 1783429832.5364819
```

### 5. Suite COMPLETA (gate único)
```
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_completo_offline
FAILED tests/integration/test_streamlit_app.py::test_render_coord_search_plus_code_curto_usa_localidade_mock
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_completo_decodifica_offline
FAILED tests/unit/test_coord_search.py::test_resolve_plus_code_curto_usa_localidade_mockada
ERROR tests/unit/demanda_revelada/test_viz_atratividade.py
4 failed, 1417 passed, 4 skipped, 18 warnings, 1 error in 421.54s (0:07:01)
```
Análise NO-BYPASS: os 4 FAILED + 1 ERROR são ambientais (deps opcionais `matplotlib` e `openlocationcode` ausentes), em arquivos NÃO tocados por este branch. Não fabrico verde; documento honestamente. Não são bloqueadores do FU1.

### 5b. Só os 12 testes do FU1
```
............                                                             [100%]
12 passed in 5.30s
```

### 6. mtime depois
```
mtime depois: 1783429832.5364819
```
(inalterado — parquet denso READ-ONLY confirmado)

### 7. smoke import
```
import ok
```

### 8. Housekeeping check (BLOQUEADOR)
```
FALHA --check: stub ausente no backlog para BLK-ATR-01-FU1
```
(exit code 1)

### 9. mtime dos 4 artefatos M1 oficiais
```
data/staging/brasil_estrutural.parquet: 1780501622
data/staging/brasil_priorizados.parquet: 1780501632
data/staging/hexagonos_brasil_oportunidades.parquet: 1780501656
data/outputs/hexagonos_brasil_dashboard.parquet: 1781271963
```
(coerentes com o estado pré-ciclo; nenhum regenerado pelo FU1)

### 10. grep import proibido
```
10:  - Pacote DISJUNTO: NUNCA importa de `pipelines/m1/`, `dashboard/`, `censo_*`, `api/`,
```
(única ocorrência = texto de docstring afirmando a proibição; nenhum import real proibido — confirmado pelo AST em #3)

### 11. totalpass referenciado
```
src/.../aferir_overlap_nao_abra.py:  - `NAO_ABRA/totalpass_final*.html` NUNCA lido (DEC-012 §3).
src/.../aferir_overlap_nao_abra.py:        "`NAO_ABRA/totalpass_final*.html` nunca lido."
```
(ambas = texto declarando que totalpass NUNCA é lido; nenhum código lê o dump pessoal — OK)

### 12. escrita de parquet
```
393:        destino.write_text(texto, encoding="utf-8")
```
(única escrita = `write_text` do relatório .md; NENHUM `to_parquet` — parquet denso não reescrito)

### 13. testes usam fixtures sintéticas
```
3:Fixtures 100% SINTÉTICAS: NUNCA leem `/repo/NAO_ABRA/` real. Todos os arquivos
36:    caminho = tmp_path / "01_SmartFit.xlsx"
44:    caminho = tmp_path / "03_Competidores.xlsx"
434:    # As constantes apontam para NAO_ABRA/ (gitignored), não para dentro de /tmp
435:    assert "NAO_ABRA" in str(m.SMARTFIT_DEFAULT)
436:    assert "NAO_ABRA" in str(m.COMPETIDORES_DEFAULT)
```
(fixtures em tmp_path; referências a NAO_ABRA são só docstring + asserção de constante default — nenhum teste lê o arquivo real)

## Conferência de no-bypass
Confirmado: a suíte foi re-executada por conta própria com `-n auto`. Os 4 FAILED + 1 ERROR foram investigados e comprovados ambientais (deps opcionais ausentes: `matplotlib`, `openlocationcode`), em arquivos alheios ao branch. Não houve fabricação de verde nem bypass de gate. O gate REAL de reprovação é o housekeeping, não os testes ambientais.

## Conferência cruzada com log do Builder
DIVERGE. O Builder afirmou:
- "tasks/completed.md — entrada BLK-ATR-01-FU1 adicionada" → parcialmente falso: a entrada existe mas com heading `## ` (H2) errado; o `--check` exige `### ` (H3).
- "tasks/backlog.md — BLK-ATR-01-FU1 marcado como concluído" → falso: o bloco continua como heading `### ` completo, sem stub. `--check` falha.
As demais afirmações do Builder (ruff/mypy limpos, 12 testes, mtime inalterado, import ok, zero PII, isolamento) CONFEREM.

## Guardrails verificados
- score_priorizacao não alterado: sim
- Artefatos M1 preservados: sim (mtime dos 4 oficiais inalterado)
- Zero PII persistida: sim (drop na fronteira; relatório só com contagens agregadas; `_assert_sem_pii_relatorio` chamado antes de gravar)
- Isolamento de imports: sim (AST NENHUM proibido)
- Housekeeping --check: FALHOU

## Decisão recomendada
Reabrir para Builder — correção pontual de housekeeping (mover o bloco corretamente: stub de 1 linha em backlog.md + heading `### ` em completed.md, satisfazendo `housekeeping_move_block.py --check`). Nenhuma mudança de código/teste necessária. Após o housekeeping passar no `--check`, o ciclo pode fechar.
