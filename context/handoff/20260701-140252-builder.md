# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-LTV-03 — Análise de correlação território × retenção/LTV [GATE DE DECISÃO]

## O que foi feito
Implementado o módulo READ-ONLY de correlação bivariada exatamente conforme o plano aprovado por
Felipe (2026-07-01). O módulo lê `data/staging/unidade_territorio_retencao.parquet` (88×36), filtra
`hex_id notna` (universo N=56), e para cada um dos 21 pares determinísticos `(feature territorial ×
alvo de retenção/LTV)` calcula Spearman rho + p-value + IC95 por bootstrap (1000 reamostras,
`np.random.default_rng(SEED=42)`, determinístico). Emite veredito GO/NO-GO mecânico e materializa o
relatório gitignored `data/analysis/relatorio_correlacao_ltv.md` com os 5 confounds obrigatórios.

- 21 pares = 14 do eixo RANKING (7 features × `PROB_CANCEL_90D_MEDIA`/`LTV_PROSPECTIVO_12M_MEDIANO`,
  N=56/49) + 7 do eixo ABSOLUTO (7 features × `prob_cancel_90d_media_absoluta`, N=44/39). Os 7 pares
  mínimos do critério de aceite #1 são subconjunto desta lista.
- Regra GO/NO-GO mecânica: GO sse ∃ par com IC95 sem cruzar zero E |rho| ≥ 0.30 (LIMIAR_RHO_GO);
  NO-GO caso contrário. `_veredito` nunca levanta exceção (NO-GO é resultado válido — DEC-008).
- Correlação PURA: nenhum R² calculado (R² in-sample banido como desempenho — DEC-008).
- Guardrails de import: `lifetime/` não importa `pipelines.m1`/`dashboard`/`censo`/`api` (teste dedicado).
- `score_priorizacao` é apenas uma FEATURE territorial LIDA; nenhum score criado; DEC-001 intacta.

## Arquivos alterados
- **NOVO** `src/motor_expansao/lifetime/correlacao_territorio_retencao.py` — módulo de análise
  (constantes N_BOOTSTRAP=1000/SEED=42/LIMIAR_RHO_GO=0.30/N_MIN_PAR=10; FEATURES_TERRITORIAIS(7);
  ALVOS_RANKING; ALVO_ABSOLUTO; PARES(21); `@dataclass ResultadoPar`; `_carregar_dataset`;
  `_bootstrap_spearman_ci`; `analisar_correlacao_territorio_retencao`; `_veredito`;
  `_montar_relatorio`+`_escrever_relatorio`; `run`/`main`; `__all__`).
- **NOVO** `tests/unit/test_correlacao_territorio_retencao.py` — 11 testes cobrindo os casos (a)-(i)
  do plano (fixtures 100% sintéticas em memória; nunca lê parquet real).
- **NOVO (gitignored)** `data/analysis/relatorio_correlacao_ltv.md` — relatório GO/NO-GO gerado
  (confirmado por `git check-ignore`; NÃO entra no diff).
- `context/handoff.md` (este) + snapshot `context/handoff/20260701-140252-builder.md`.
- `tasks/current_task.md` — atualização mínima (Skill atual/próxima/status).

## Validações executadas
- pytest (subconjunto impactado): `python -m pytest -n auto -q tests/unit/test_correlacao_territorio_retencao.py tests/integration/test_streamlit_app.py`
  → **224 passed, 0 failed, 0 skipped** (11 do novo teste + integração streamlit). Escolhi este
  subconjunto porque toquei o novo módulo/teste em `lifetime/` e o smoke de integração do dashboard
  é obrigatório todo ciclo. Suíte FULL fica para o QA (gate único).
- import: `python -c "import streamlit_app; print('import ok')"` → **ok** (só warnings esperados de cache Streamlit).
- ruff: `ruff check src/.../correlacao_territorio_retencao.py tests/.../test_correlacao_territorio_retencao.py` → **All checks passed!**
- mypy: `mypy src/` → **Success: no issues found in 87 source files** (novo módulo incluído; sem erro novo vs baseline).
- geração do relatório: `python -m motor_expansao.lifetime.correlacao_territorio_retencao` → escreveu
  `data/analysis/relatorio_correlacao_ltv.md`, log `veredito=GO (21 pares)`.
- **M1 mtime-unchanged (antes/depois)**: os 4 artefatos oficiais com mtime IDÊNTICO antes e depois de rodar o módulo:
  - `brasil_priorizados.parquet` 1780501631 → 1780501631
  - `hexagonos_brasil_oportunidades.parquet` 1780501655 → 1780501655
  - `brasil_estrutural.parquet` 1780501621 → 1780501621
  - `hexagonos_mercado_mapeado.parquet` 1781205056 → 1781205056

## Veredito do relatório (output central do bloco) — GO
**VEREDITO: GO** — 6 pares atingem simultaneamente IC95 sem cruzar zero E |rho| ≥ 0.30:
- `score_priorizacao × LTV_PROSPECTIVO_12M_MEDIANO` — rho=**+0.391**, IC95=[+0.183, +0.573], p=0.0029, N=56 (PAR MAIS FORTE)
- `score_setor_2022_calibrado × LTV_PROSPECTIVO_12M_MEDIANO` — rho=+0.378, IC95=[+0.133, +0.595], p=0.0075, N=49
- `score_setor_2022_calibrado × prob_cancel_90d_media_absoluta` — rho=-0.333, IC95=[-0.621, -0.006], p=0.0382, N=39 (eixo absoluto)
- `score_expansao_hibrido × LTV_PROSPECTIVO_12M_MEDIANO` — rho=+0.321, IC95=[+0.049, +0.543], p=0.0159, N=56
- `renda_per_capita × prob_cancel_90d_media_absoluta` — rho=-0.311, IC95=[-0.536, -0.022], p=0.0402, N=44 (eixo absoluto)
- `renda_per_capita × PROB_CANCEL_90D_MEDIA` — rho=-0.306, IC95=[-0.517, -0.057], p=0.0217, N=56

Leitura direcional coerente (não é conclusão do bloco, só contexto): renda/scores territoriais mais
altos ↔ MENOR prob. de cancelamento e MAIOR LTV. Os demais 15 pares têm IC95 cruzando zero (ex.:
`densidade_pop_setor` rho≈0.00; `n_concorrentes_1km` inconclusivo). O GO habilita — sob DEC + gate
humano próprios (DEC-009/DEC-008) — reabrir o BLK-LTV-04 (score M2). **Este bloco NÃO cria score.**
Confounds declarados no relatório: maturidade não-controlável, N pequeno, seleção de sobreviventes,
32 sem hex_id, colinearidade renda↔score_priorizacao↔score_expansao_hibrido.

## Problemas encontrados
nenhum

## Pendências
- QA deve rodar a suíte FULL (`pytest -n auto -q`) como gate único do ciclo.
- BLK-LTV-04 (score M2) fica BLOQUEADO: condicional a este GO + DEC própria + gate humano adicional
  (fora do escopo deste bloco).

## Riscos remanescentes
- N pequeno (56/44/49) + confound de maturidade não-controlável (sem data de abertura): o GO é
  estatisticamente honesto (IC95 sem cruzar zero) mas a decisão de avançar para BLK-LTV-04 exige o
  gate humano ciente desses confounds (declarados no §4 do relatório).
- 2 dos 6 pares GO estão no eixo ABSOLUTO com N menor (39/44); um deles (`renda × abs`) tem IC95
  superior = -0.022, próximo de zero mas estritamente < 0 (não cruza). Documentado no relatório.

## Guardrails verificados
- score_priorizacao não alterado: sim (LIDO apenas como feature; pesos renda=0.40/pop=0.60 e fórmula intactos — DEC-001)
- Artefatos M1 preservados: sim (mtime dos 4 oficiais idêntico antes/depois; relatório é `.md` gitignored)
- Dashboard offline mantido: sim (nenhuma dependência de rede; `import streamlit_app` ok)
- Pacote disjunto: sim (`lifetime/` não importa `pipelines.m1`/`dashboard`/`censo`/`api` — teste `test_no_extra_m1_imports`)
- DEC-008: sim (Spearman + bootstrap/IC; R² banido; NO-GO seria válido — não enviesado para GO)
- DEC-009: sim (correlaciona RETENÇÃO × território, não previsão de demanda; nota de escopo no relatório proíbe uso como preditor geográfico)
- Sem dependência nova em pyproject (scipy já presente)
- Paths pré-sujos não tocados (2 geojson deletados, scripts/backtest_smartfit_scores.py)
