# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-UI-07 — Refinos de UX/UI do dashboard (2x2 do Relatório Censitário + filtros/busca na tela principal)

## Gate humano aplicado
Vinicius aprovou em 2026-06-16 a execução conforme o plano do Planner, com D1–D4 resolvidas:
- D1 = expander "Filtros avancados" no CORPO (`st.expander`, `expanded=False`).
- D2 = `initial_sidebar_state` `"expanded"` → `"collapsed"`.
- D3 = UF (selectbox) sozinho no corpo antes de `load_uf_slice`; Município+Faixa numa `st.columns(2)` abaixo; busca por coordenada imediatamente acima de `render_tab_selector`.
- D4 = `use_container_width=True` nas 4 imagens do grid 2x2.

## O que foi feito
### F1 — Grade 2x2 das 4 imagens do Relatório Pontual Censitário (`pages.py`, `render_relatorio_pontual_censitario`)
- As 4 chamadas sequenciais `st.image(..., width=_CENSUS_PREVIEW_WIDTH_PX)` foram substituídas por uma grade 2x2: `row1_col1/row1_col2 = st.columns(2)` (densidade/renda) e `row2_col1/row2_col2 = st.columns(2)` (score/concorrentes), cada imagem com `use_container_width=True` (D4).
- Ordem das camadas preservada (linha1: densidade, renda; linha2: score, concorrentes); captions byte-a-byte iguais.
- Grep confirmou que `_CENSUS_PREVIEW_WIDTH_PX` tinha uso único (as 4 imagens) → constante + comentário removidos (sem código morto).

### F2 — UF/Município/Faixa para o corpo (`pages.py` + `streamlit_app.py`)
- `render_uf_selectbox`: `st.sidebar.markdown/caption/selectbox` → `st.*` (corpo). Assinatura e retorno (`str | None`) inalterados; sem `key=`.
- `render_sidebar_filters`: Município e Faixa agora via `mcol1, mcol2 = st.columns(2)` (corpo, lado a lado), `mcol1.multiselect`/`mcol2.multiselect`. Expander "Filtros avancados" `st.sidebar.expander` → `st.expander` (D1); conteúdo interno inalterado (já usava `st.*`). Nome da função e contrato de 8 retornos preservados.
- `streamlit_app.py main()`: `selected_uf = render_uf_selectbox(uf_catalog)` segue como 1ª chamada significativa, antes de `load_uf_slice`, com `st.info`+`st.stop()` intactos (carga lazy por UF preservada). Texto do `st.info` ajustado: removido "na barra lateral".
- `initial_sidebar_state="collapsed"` (D2).

### F3 — Busca por coordenada para o corpo (`pages.py` + `streamlit_app.py`)
- `render_coord_search_sidebar`: 4 chamadas `st.sidebar.*` → `st.*`; `key="coord_search_input"` preservada byte-a-byte; nome da função mantido (consumido por testes via `mock.patch.object`).
- `main()`: o trio `search_pin = render_coord_search_sidebar()` + `_search_result` + `search_hex_id` foi movido de logo após `render_sidebar_filters` para imediatamente ANTES de `render_tab_selector` (após o guard `if filtered_df.empty: return`). `search_pin`/`search_hex_id` ficam definidos antes de todos os consumidores (`render_pdf_download_topo`, `render_hex_search_result`, dispatches de aba).

### Testes (`tests/integration/test_streamlit_app.py`)
- `test_render_relatorio_pontual_censitario_com_coordenada_gera_mapa_e_downloads` (L4370): assert reescrito. `streamlit.columns` agora capturado por `_capture_columns` (registra os MagicMocks de coluna e os argumentos); `assert image_mock.call_count == 0` (st.image direto não é mais usado), `total_images = sum(c.image.call_count) == 4` (col.image agregado), e `col_args.count(2) >= 2` (arranjo 2x2).
- 3 testes novos de namespace-corpo: `test_render_uf_selectbox_no_corpo_nao_usa_sidebar`, `test_render_sidebar_filters_municipio_e_faixa_no_corpo` (valida 8 retornos + Município/Faixa via col.multiselect, sem sidebar), `test_render_coord_search_no_corpo_preserva_key` (valida `key="coord_search_input"`, sem sidebar).

## Arquivos alterados
- `src/motor_expansao/dashboard/pages.py` — grade 2x2 das 4 imagens + remoção de `_CENSUS_PREVIEW_WIDTH_PX`; `render_uf_selectbox`/`render_sidebar_filters`/`render_coord_search_sidebar` migrados de `st.sidebar.*` para `st.*` (corpo); Município+Faixa em `st.columns(2)`; expander avançado no corpo. Docstrings atualizadas.
- `streamlit_app.py` — `initial_sidebar_state="collapsed"`; texto do `st.info` sem "na barra lateral"; trio `search_pin` reposicionado para antes de `render_tab_selector`.
- `tests/integration/test_streamlit_app.py` — assert do teste do relatório reescrito (col.image agregado + 2x2); 3 testes novos de namespace-corpo.
- `tasks/current_task.md` — status → aguardando QA; D1–D4 registradas.

## Validações executadas
- `python -m pytest -n auto -q tests/integration/test_streamlit_app.py`: **199 passed, 0 failed, 0 skipped** (99510 warnings de deprecation alheios — pytest_asyncio/Py3.14). Subconjunto escolhido por ser o arquivo de teste do dashboard que cobre todas as funções tocadas.
- `python -m pytest -q tests/integration/test_streamlit_app.py -k "no_corpo or municipio_e_faixa or preserva_key or gera_mapa_e_downloads or renderiza_apenas_a_aba_ativa"`: **5 passed, 194 deselected** (confirma os testes novos + o reescrito + o de render lazy de abas).
- `python -c "import streamlit_app; print('import ok')"`: **import ok**.
- `python -m ruff check src/motor_expansao/dashboard/pages.py streamlit_app.py tests/integration/test_streamlit_app.py`: **All checks passed!** (após remover binding `as multiselect_mock` não usado — F841).
- `python -m mypy src/motor_expansao/dashboard/pages.py streamlit_app.py`: **Success: no issues found in 2 source files**.
- `git diff --name-only`: apenas `pages.py`, `streamlit_app.py`, `test_streamlit_app.py`, `current_task.md`, `context/handoff.md` (+ os 2 paths pré-sujos alheios NÃO commitados).
- `git diff --stat` sobre `censo_map.py`/`censo_report.py`/`censo_point.py`/`config.py`/`components.py`/`constants.py`/`utils.py`: **vazio** (zero edição).

## Problemas encontrados
- Ruff acusou F841 (`multiselect_mock` não usado) num teste novo; corrigido removendo o binding `as`. Reexecução: limpo.
- `ruff`/`mypy` não estão no PATH como executáveis nesta máquina; usados via `python -m ruff`/`python -m mypy`.

## Pendências
- Suíte FULL (`pytest -n auto`) é gate do QA (não rodada aqui por contrato do Builder).
- Os 2 paths pré-sujos (`data/outputs/setores_censitarios_2022_geo/_metadata.json`, `data/reports/relatorio_pontual_censitario_base_geo.md`) seguem como estão — NÃO commitar (alheios ao ciclo).

## Riscos remanescentes
- Dívida operacional herdada (não regressão deste ciclo, ver `current_task.md`): 3 failures pré-existentes na suíte full (`test_csvs_concorrentes_legiveis[csv_path1-223]`, `[csv_path2-472]`, `test_parquet_final_respeita_guardrails_do_piloto`). O QA deve confirmar que continuam sendo só essas 3 e que não há failure novo.

## Guardrails verificados
- score_priorizacao não alterado: sim (READ-ONLY; só reposicionamento/namespace de widgets).
- Artefatos M1 preservados: sim (nenhum artefato/pipeline tocado).
- Dashboard offline mantido: sim (nenhuma dependência/API ao vivo adicionada).
- Carga lazy por UF preservada: sim (`render_uf_selectbox` segue 1ª chamada significativa, antes de `load_uf_slice`, com `st.stop()` intacto).
- Render lazy de abas preservado: sim (`render_tab_selector` e dispatch por aba intactos; `test_main_renderiza_apenas_a_aba_ativa` verde).
- Arquivos protegidos (`censo_*`, `config.py`, `components.py`, `constants.py`, `utils.py`, `pipelines/m1`) intocados: sim (diff vazio).
