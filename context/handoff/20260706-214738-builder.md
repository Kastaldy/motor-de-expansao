# Handoff — Builder (correção BLK-UI-10, segunda tentativa)

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA (segunda tentativa — gate único: suite FULL)

## Bloco
**BLK-UI-10 — PoC de repaginação do dashboard: tema denso + mapa Leaflet client-side**

## Status
CORREÇÃO APLICADA — aguarda QA.

## O que foi feito (correção mínima)

O QA identificou que `streamlit_app.py` NUNCA foi modificado pela tentativa anterior.
Esta correção aplica os entregáveis pendentes e commita por path.

### 1. streamlit_app.py — opt-in adicionado ao início de `main()`

Adicionado ao início de `main()` (import lazy dentro da função):
```python
def main() -> None:
    from motor_expansao.dashboard.ui_proto import is_proto_enabled, render_proto_page

    if is_proto_enabled():
        render_proto_page()
        return

    inject_styles()
    render_header()
    ...
```
Com `ULTRA_PROTO=1` → `render_proto_page()` é chamado e `main()` retorna imediatamente.
Sem a flag → caminho de produção intacto (inject_styles/render_header seguem byte-a-byte).

### 2. .gitignore — entrada de data/outputs/ui_proto/ adicionada

Nota: `data/cache/` já estava gitignored (linha 67), cobrindo `data/cache/ui_proto/`.
A entrada `data/outputs/ui_proto/` foi adicionada por completude (caso o módulo venha
a escrever outputs).

### 3. tests/unit/test_ui_proto.py — 2 novos testes de wiring (total: 37)

`TestStreamlitAppWiring` (nova classe, 2 testes):
- `test_proto_enabled_chama_render_proto_page`: ULTRA_PROTO=1 → `render_proto_page()` chamado 1x.
- `test_proto_desabilitado_nao_chama_render_proto_page`: flag ausente → `render_proto_page()` NÃO
  chamado; `inject_styles()` (caminho de produção) chamado 1x.

### 4. Arquivos untracked da tentativa anterior commitados por path

- `src/motor_expansao/dashboard/ui_proto.py` (PoC opt-in — Fases A+B)
- `src/motor_expansao/dashboard/ui_theme.py` (CSS/tema Ultra)
- `tests/unit/test_ui_proto.py` (37 testes; 35 originais + 2 novos de wiring)
- `data/reports/ui_poc_leaflet.md` (relatório comparação Leaflet vs pydeck)
- `streamlit_app.py` (opt-in 5 linhas em main())
- `.gitignore` (data/outputs/ui_proto/ adicionado)

### O que NÃO foi tocado (guardrails)
- `config.py`, `pipelines/m1/`, `components.py`, `pages.py`: INTOCADOS.
- `pyproject.toml`: INALTERADO (zero dep nova; Leaflet/h3-js por CDN).
- Mtime dos 4 oficiais M1: INALTERADO.
- `src/motor_expansao/dimensionamento/config.py` (CRLF noise): FORA do commit.
- `scripts/backtest_smartfit_scores.py` (script solto fora de escopo): FORA do commit.

## Validações executadas

### ruff
```
ruff check src/motor_expansao/dashboard/ui_theme.py src/motor_expansao/dashboard/ui_proto.py streamlit_app.py tests/unit/test_ui_proto.py
All checks passed!
```

### pytest (subconjunto do ciclo)
```
python -m pytest -n auto -q tests/unit/test_ui_proto.py
37 passed in 5.01s
```

### import streamlit_app
```
python -c "import streamlit_app; print('import ok')"
import ok
```

### loop_guard
```
python scripts/loop_guard.py --base ciclo/loop-20260706-152137
GUARD OK: 31 caminho(s) alterado(s), nenhum proibido.
```

## Conferência de guardrails
- score_priorizacao / pesos não alterados: SIM (READ-ONLY; módulos só LÊEM scores; nenhuma escrita).
- Artefatos M1 preservados: SIM (mtime dos 4 oficiais inalterado; loop_guard OK).
- Parâmetros canônicos (H3_RESOLUTION=7, RENDA_MIN=4500, pesos 0.40/0.60): INALTERADOS.
- Dep nova ao pyproject.toml: NENHUMA (Leaflet/h3-js por CDN no HTML embutido).
- NÃO tocar build_map_figure, components.py, constants.py, pipelines/m1/: RESPEITADO.
- Commit por path (NÃO git add -A): SIM — 6 paths explícitos.

## Notas para o QA

1. Os 4 testes pre-existentes de `plus_code` (openlocationcode ausente no ambiente) continuam
   a falhar — pré-existentes, não relacionados ao BLK-UI-10.
2. O grep de opt-in deve agora retornar resultados:
   ```
   grep -n "ULTRA_PROTO\|render_proto\|is_proto_enabled\|ui_proto" streamlit_app.py
   ```
   Deve mostrar as 2 linhas do import e as 2 do bloco if.
3. git diff ciclo/loop-20260706-152137...HEAD -- streamlit_app.py deve mostrar o bloco de 5 linhas.
4. git status deve mostrar todos os 6 arquivos do ciclo como tracked (A ou M), não untracked.
