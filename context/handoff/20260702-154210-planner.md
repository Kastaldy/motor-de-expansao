# Handoff — Planner

## Skill que gerou este handoff
Planner

## Próxima Skill recomendada
REVISÃO HUMANA (gate) — esteira Alta com gate humano de MODELAGEM antes do Builder.

## Entendimento da tarefa
Construir um módulo novo, isolado e READ-ONLY que reproduz o baseline do TP-06 e valida OUT-OF-FOLD (mesmo harness/seed) residuais CANDIDATOS contra `log1p(membros)`, comparando por IC95 da DIFERENÇA pareada e por recorte metropolitano — para decidir SE e QUAL candidato alimenta o BLK-TP-09. **Correção de semântica do gate:** as academias menores de bairro CONSOMEM demanda e devem SOMAR à oferta consumida (o Motor as ignora hoje); o DEDUP existe para NÃO contar duas vezes as REDES já mapeadas (Smart/Sky/Blue/Pano...). Além disso, testar capacidade REAL por rede em vez do 2.500 fixo.

## Achados dos 2 PRÉ-REQUISITOS DE DADO (verificados pelo Planner, agregados, sem PII)

### Pré-requisito 1 — rótulo de rede por academia menor: **NÃO EXISTE HOJE.**
- Schema real de `data/staging/oferta_academias_menores_h3.parquet`: `hex_id, n_academias_menores, alunos_academias_menores, n_plano_tp0..tp7, versao_contrato`. **Nenhuma identidade de rede** — `Nome_Academia` foi dropado na fronteira anti-PII do TP-08. É só agregado por hex.
- **Consequência:** NÃO dá para deduplicar/classificar rede a partir do parquet atual. Dedup por rede exige RE-INGESTÃO do `NAO_ABRA/03_Competidores.xlsx` com um rótulo de rede derivado do `Nome_Academia` NA FRONTEIRA (classifica em categoria de rede via mapa de padrões, DESCARTA o nome cru — anti-PII preservado). Isso é sub-escopo próprio → recomendo vira **BLK-TP-08-FU (re-ingestão com `rede_menor`)** do qual este FU1 passa a depender para o dedup FINO por rede. Ver Decisão (E).
- **Aproximação viável HOJE (sem re-ingestão):** dedup por **hex-overlap** — em hex onde já existe rede mapeada (`concorrentes_mapeados.hex_id_res7`), tratar a oferta menor como possivelmente já contida; fora, somar integral. É grosseiro (não distingue filial-de-rede de independente DENTRO do hex) e a limitação é declarada.

### Pré-requisito 2 — médias reais de alunos/unidade por rede: **PARCIAL.**
- `concorrentes_mapeados.parquet` tem `rede` (28 redes: smart_fit 1000, panobianco 458, bluefit 226, selfit 221, ...) mas **NÃO tem coluna de alunos/unidade**. Não serve de fonte de capacidade.
- Fonte estruturada real = `data/staging/base_calibracao_multirede.parquet` (`marca`, `alunos_reais`, `metragem`): cobre **apenas 3 marcas** — `skyfit` (n=301, média **2.295**, mediana 2.228), `engenharia_do_corpo` (n=58, média **3.283**), `ultra` (n=54, média 2.429). **NÃO contém Smart Fit nem Blue Fit nem Panobianco.**
- **Consequência (honesta):** das redes do exemplo do gate, só **Sky Fit ≈ 2,3k é dado REAL**; **Smart ~2,2k e Panobianco ~1,9k NÃO estão no repo** (são estimativas externas do gate, sem fonte no parquet). Das 28 redes de `concorrentes_mapeados`, temos capacidade real para 2 (`skyfit`, `engenharia_do_corpo`); as demais 26 (incl. Smart e Pano, os mais numerosos) precisam de FALLBACK. `data/validacao/Sky Fit dados.xlsx` e `KPIs_Smart_2025_02.xlsx` existem mas são planilhas cruas/PII (gitignored) — não estruturadas p/ média limpa por unidade sem parsing pesado, fora do escopo deste bloco.
- **Fallback recomendado:** rede com média real → usa a média (skyfit 2.295, engenharia 3.283); rede sem média → capacidade default atual **2.500** (mantém o comportamento de produção como neutro). Marcar como caveat central: o Candidato C só re-pondera 2 das 28 redes com dado real → efeito estrutural pequeno e enviesado às redes conhecidas.

## Plano técnico

### 1. Módulo novo `src/motor_expansao/demanda_revelada/revalidacao_residual_candidatos.py`
Funções PURAS, pacote DISJUNTO (importa só `numpy`/`pandas`/`scipy`/`sklearn` e os irmãos `demanda_revelada.calibracao_residual`, `demanda_revelada.contrato`, `dimensionamento.aderencia`, `dimensionamento.backtest_dim`). NUNCA importa `pipelines/m1`, `dashboard`, `censo_*`, `api`, `config.py` raiz.

1.1. **Reuso do harness** (não duplicar k-fold): de `calibracao_residual` importar `_selecionar_alpha_e_oof`, `_metodo_validacao`, `_ic_bootstrap_r2`, `_ic_bootstrap_rho`, `_rho_oof`, `SEED`, `N_BOOTSTRAP`, `LIMIAR_R2_GO`, `LIMIAR_RHO_SUPORTE`; de `dimensionamento.backtest_dim` importar `_r2`, `_rmse`. Alvo `y = log1p(membros)`; feature = `score.reshape(-1,1)`.

1.2. **`construir_residuais_candidatos(df_join, of_menores, hexes_concorrentes, n_unid_rede_por_hex, *, capacidade_por_rede, capacidade_default=2500.0) -> pd.DataFrame`** — devolve `df_join` + colunas EM MEMÓRIA: `residual_baseline`, `residual_cand_A`, `residual_cand_C`, `flag_metropolitano` (uf∈{SP,MG,RJ}). Nunca escreve parquet.

1.3. **Candidato A (oferta ENRIQUECIDA com dedup) — semântica corrigida (SOMAR à oferta consumida):**
   - Oferta menor NÃO-duplicada com a aproximação de hex-overlap disponível hoje: `alunos_menores_add = where(hex_id ∈ hexes_concorrentes, 0.0, alunos_academias_menores.fillna(0))` — em hex já com rede mapeada, assume-se coberto (não soma, evita dupla-contagem das redes já mapeadas); em hex SEM rede mapeada, soma integral (academias independentes genuínas que o Motor ignora).
   - `oferta_consumida_ajustada = oferta_consumida_total_estimada + alunos_menores_add` → o residual DISPONÍVEL CAI onde há academias menores não mapeadas.
   - `oferta_efetiva_ajustada = clip(sam_fitness_potencial − oferta_consumida_ajustada, 0, None)`; `residual_cand_A = clip(100·oferta_efetiva_ajustada/2.500, 0, 100)`.
   - **Necessita 2 colunas extra no join** (confirmado presentes no parquet): `sam_fitness_potencial` e `oferta_consumida_total_estimada`. Estender o join local para trazê-las READ-ONLY.
   - **Limitação declarada:** sem `rede_menor` (pré-req 1), o dedup é por hex-overlap, não por filial-de-rede; se o TP-08-FU entregar `rede_menor`, o Candidato A é refeito removendo só as filiais de redes já em `concorrentes_mapeados` e somando o resto (matemática idêntica trocando a máscara de overlap pela de "não é filial de rede mapeada").

1.4. **Candidato C (capacidade POR REDE) — estrutural:**
   - Recalcular `oferta_consumida_mercado_estimada` por hex ponderando cada concorrente pela capacidade real da sua rede em vez do 2.500 fixo. **Problema de granularidade (declarar):** `hexagonos_mercado_mapeado` traz `oferta_consumida_mercado_estimada` já agregada (não a lista de redes por hex). Para re-pesar por rede é preciso reconstruir a contagem por rede por hex a partir de `concorrentes_mapeados` (`rede`, `hex_id_res7`) — READ-ONLY, agregação nova em memória `n_unid_rede_por_hex` (passada como argumento; construída no `__main__`).
   - `oferta_consumida_mercado_C = Σ_rede n_unid_rede_por_hex · capacidade_por_rede.get(rede, capacidade_default)`.
   - `capacidade_por_rede` = `{"skyfit": 2295.0, "engenharia_do_corpo": 3283.0}` (médias reais medidas); todas as outras 26 redes → `capacidade_default=2.500`. Ultra própria mantém o tratamento atual (`oferta_consumida_ultra_estimada` intocada).
   - `oferta_efetiva_C = clip(sam_fitness_potencial − (oferta_consumida_mercado_C + oferta_consumida_ultra_estimada), 0, None)`; `residual_cand_C = clip(100·oferta_efetiva_C/2.500, 0, 100)`.
   - **Caveat central:** só 2 de 28 redes têm média real → o C move o residual só onde skyfit/engenharia aparecem; hexes dominados por Smart/Pano (as redes mais numerosas, SEM dado) → C ≈ baseline. Efeito estrutural pequeno e enviesado. Reportar quantos hexes do join efetivamente mudam.

1.5. **Candidato B (capacidade GLOBAL) = NÃO é cenário separado.** Registrar como NOTA no relatório: re-escalar só a capacidade global `C` no `clip(100·oferta/C,0,100)` é rescale MONOTÔNICO de `oferta_efetiva_disponivel`; como o Ridge de 1 feature re-absorve a escala no coeficiente, `R2_oof`/`rho_oof` seriam ~idênticos ao baseline (só a fração saturada — 17,4% a C=2.500 — mudaria). Não vale rodar como candidato próprio.

1.6. **`validar_cenario(df_join, coluna_score) -> ResultadoCenario`** — dataclass leve: `nome, r2_oof_log, ic95_r2_oof, rho_oof, ic95_rho_oof, alpha, n, metodo, y_pred_oof` (guardado p/ bootstrap pareado). Roda `_selecionar_alpha_e_oof` + IC95 bootstrap (seed=42). **R² in-sample NUNCA no veredito** (DEC-008).

1.7. **`comparar_pareado(y, pred_baseline_oof, pred_cand_oof, *, seed=42, n=N_BOOTSTRAP) -> (delta_medio, ic95)`** — bootstrap PAREADO: mesmos índices reamostrados aplicados aos DOIS vetores oof; `Δ = _r2(y_b, pred_cand_b) − _r2(y_b, pred_base_b)`. **Candidato "vence"** ⇔ IC95 de Δ não cruza zero (inferior > 0).

1.8. **`comparar_por_recorte(...)`** — repetir 1.6/1.7 em 3 recortes: completo (n=16.411), metropolitano SP/MG/RJ (n≈8.367), não-metropolitano (n≈8.044). Ganho só é robusto se sobrevive FORA de SP/MG/RJ.

1.9. **Orquestrador `revalidar_candidatos(...) -> RevalidacaoResult`** monta os 3 vetores (baseline, A, C), valida cada um, roda comparação pareada A−baseline e C−baseline nos 3 recortes, decide veredito honesto por candidato. `__main__` (`# pragma: no cover`) lê parquets reais, constrói `n_unid_rede_por_hex` e escreve relatório; não exercitado em teste.

1.10. **Anti-PII:** reusar a checagem word-boundary de `COLUNAS_PII_PROIBIDAS` antes de escrever o `.md`.

### 2. Relatório `data/analysis/revalidacao_residual_candidatos.md` (gitignored, sem PII)
Seções: §1 Baseline reproduzido (n=16.411, R2_oof_log≈+0,3119, rho_oof≈+0,4615, top-3 UF); §2 Achados dos 2 pré-requisitos (SEM rótulo de rede; capacidade real só p/ skyfit/engenharia); §3 Candidato A (fórmula do enriquecimento + dedup hex-overlap + limitação); §4 Candidato C (capacidade por rede + fallback + quantos hexes mudam); §5 NOTA capacidade global (rescale monotônico); §6 Tabela comparativa (R2_oof/IC95/rho_oof por cenário); §7 Δ pareado nos 3 recortes; §8 VEREDITO honesto por candidato + recomendação ao TP-09; §9 confounds (7 do TP-06 + dedup + capacidade-parcial).

### 3. Testes `tests/unit/demanda_revelada/test_revalidacao_residual_candidatos.py` (fixtures SINTÉTICAS)
3.1 Candidato A soma corretamente (hex SEM rede mapeada soma; hex COM overlap não soma; residual cai onde soma); `oferta_efetiva_ajustada` nunca negativa; residual em [0,100]. 3.2 Candidato C re-pesa por rede (rede conhecida usa média; desconhecida usa 2.500). 3.3 R² in-sample banido do veredito. 3.4 IC determinístico (seed=42 reprodutível). 3.5 Bootstrap pareado usa MESMOS índices nos 2 vetores (cand=base ⇒ Δ≈0, IC cobre 0). 3.6 Isolamento de import (AST-scan, sem M1/dashboard/censo/api/config). 3.7 Anti-PII no relatório/frames. 3.8 NO-GO é resultado válido sem exceção.

### 4. Fechamento (pós-merge, ad-hoc)
Atualizar "Depende de" do BLK-TP-09 (e, se o gate aprovar a Decisão E, criar o BLK-TP-08-FU de re-ingestão) em `tasks/backlog.md` — passo operacional, o Builder não edita backlog no core.

## Arquivos afetados
- `src/motor_expansao/demanda_revelada/revalidacao_residual_candidatos.py` (NOVO)
- `tests/unit/demanda_revelada/test_revalidacao_residual_candidatos.py` (NOVO)
- `data/analysis/revalidacao_residual_candidatos.md` (NOVO, gitignored, via `__main__`, sem PII)
- `context/handoff.md`, `context/handoff/20260702-HHMMSS-planner.md`, `tasks/current_task.md` (bookkeeping)
- (fechamento pós-merge) `tasks/backlog.md` — "Depende de" do BLK-TP-09 (+ eventual BLK-TP-08-FU)

## Dependências
- Harness `calibracao_residual.py` (API estável, não alterar assinaturas).
- Parquets READ-ONLY (só no `__main__`, nunca em teste): `demanda_revelada_h3`; `hexagonos_mercado_mapeado` (agora com `sam_fitness_potencial`, `oferta_consumida_total_estimada`, `oferta_consumida_ultra_estimada`, `oferta_consumida_mercado_estimada` além de `score_oportunidade_residual`/`oferta_efetiva_disponivel`/`uf`); `oferta_academias_menores_h3`; `concorrentes_mapeados` (`rede`,`hex_id_res7`); `base_calibracao_multirede` (`marca`,`alunos_reais` p/ capacidade por rede).
- **Dependência de dado do Candidato C:** capacidade real só p/ 2 redes → fallback 2.500 nas demais (caveat).
- **Dependência de sub-escopo do dedup FINO por rede (Candidato A):** re-ingestão `03_Competidores` com `rede_menor` = BLK-TP-08-FU (ver Decisão E). Sem ele, dedup por hex-overlap (aproximação).
- Sem dependência nova no `pyproject.toml`.

## Riscos técnicos
- **Sem rótulo de rede nas menores:** dedup fino impossível hoje → aproximação por hex-overlap (limitação declarada) ou depender do TP-08-FU.
- **Capacidade real só p/ 2 de 28 redes:** o Candidato C re-pondera pouco e enviesado (Smart/Pano, os mais numerosos, ficam no default) → efeito estrutural pequeno; risco de ser inconclusivo. Reportar honestamente.
- **Ganho dependente do metropolitano:** qualquer vitória precisa sobreviver fora de SP/MG/RJ (n≈8.044).
- **Sobreajuste a ruído (DEC-008):** declarar "vence" só com IC de Δ pareado sem cruzar zero; coords ~1 km atenuam o sinal no join res-7.
- **Divergência do baseline:** se não bater +0,3119 / n=16.411 é defeito de join — parar e diagnosticar antes de comparar.

## Critérios de aceite finais
- Baseline reproduz o TP-06 (seed=42): `R2_oof_log ≈ +0,3119`, `rho_oof ≈ +0,4615`, `n_join = 16.411`.
- Candidatos A e C validados pelo MESMO harness/seed, com R2_oof_log + rho_oof + IC95, e comparação vs baseline pelo IC95 da DIFERENÇA PAREADA (não cruza zero para "vence").
- Δ pareado reportado em 3 recortes; veredito registra se o ganho generaliza fora de SP/MG/RJ.
- Candidato A SOMA a oferta menor não-duplicada (semântica corrigida) com dedup declarado; Candidato C usa capacidade real onde há (skyfit/engenharia) e fallback 2.500 documentado.
- Achados dos 2 pré-requisitos (sem rótulo de rede; capacidade parcial) documentados no relatório.
- Veredito honesto (DEC-008): "não supera baseline" é VÁLIDO; R² in-sample nunca no veredito; caveat ~1% metropolitano reportado.
- ZERO escrita em artefato M1/mercado: `mtime` de `hexagonos_mercado_mapeado.parquet` e dos 4 oficiais M1 inalterado; `calcular_colunas_mercado.py` intocado; nenhum parquet de mercado novo.
- Anti-PII: nenhuma coluna de `COLUNAS_PII_PROIBIDAS` em frame/relatório; testes só fixture sintética; `NAO_ABRA/` não tocado.

## Validações obrigatórias
- Comando: `python -m pytest -n auto -q tests/unit/demanda_revelada/ tests/integration/test_streamlit_app.py` — Critério: 0 falhas.
- Comando: `python -c "import streamlit_app"` — Critério: 0 erro de import.
- Comando (isolamento): `python -c "import ast,sys; src=open('src/motor_expansao/demanda_revelada/revalidacao_residual_candidatos.py').read(); mods=[n.module or '' for n in ast.walk(ast.parse(src)) if isinstance(n,ast.ImportFrom)]; bad=[m for m in mods if any(m.startswith(p) for p in ('motor_expansao.pipelines.m1','motor_expansao.dashboard','motor_expansao.censo','motor_expansao.api'))]; sys.exit(1 if bad else 0)"` — Critério: exit 0.
- Comando (mtime): comparar `stat -c %Y` de `hexagonos_mercado_mapeado.parquet` + 4 oficiais M1 (`brasil_estrutural`, `brasil_priorizados`, `hexagonos_brasil_oportunidades`, `hexagonos_brasil_dashboard`) antes/depois do Builder — Critério: mtime inalterado.
- Comando: `ruff check src/motor_expansao/demanda_revelada/revalidacao_residual_candidatos.py tests/unit/demanda_revelada/test_revalidacao_residual_candidatos.py` — Critério: 0 erro.

## Fora de escopo
- APLICAR qualquer candidato: editar `calcular_colunas_mercado.py`, alterar a fórmula de `score_oportunidade_residual`/`oferta_efetiva_disponivel` em produção, regenerar `hexagonos_mercado_mapeado.parquet`/derivados — é o BLK-TP-09 (DEC + gate).
- Tocar QUALQUER artefato oficial do M1.
- Gerar parquet de mercado novo/oficial (a saída é relatório + módulo/teste).
- Usar `membros`/qualquer coluna da demanda como PREDITOR geográfico de magnitude (DEC-009): é ALVO OBSERVADO.
- Re-ingerir `03_Competidores.xlsx` / criar `rede_menor` NESTE bloco, SE o gate decidir que a re-ingestão vira BLK-TP-08-FU (ver Decisão E). Tocar `NAO_ABRA/`/PII; expandir res-8.
- Parsear as planilhas cruas de `data/validacao/` para extrair capacidade de Smart/Pano (fora de escopo; fica no fallback).
- Resolver o BLK-TP-09 ou outro bloco.

## Alerta de aprovação
ATENÇÃO: ESTA TAREFA É MODELAGEM READ-ONLY QUE ALIMENTA A DECISÃO DE UM SCORE DE PRODUÇÃO (BLK-TP-09). NÃO ALTERA `score_priorizacao`/`hex_score_estrutural`/PESOS/ARTEFATOS OFICIAIS DO M1 NEM A FÓRMULA DO RESIDUAL EM PRODUÇÃO — LOGO NÃO É CRÍTICA PELO §5. MAS ENVOLVE ESCOPO NÃO-TRIVIAL (ENRIQUECIMENTO COM DEDUP + CAPACIDADE POR REDE + 2 PRÉ-REQUISITOS DE DADO PARCIALMENTE INDISPONÍVEIS). **REVISÃO HUMANA DE MODELAGEM OBRIGATÓRIA ANTES DO BUILDER.** O USUÁRIO DEVE LER ESTE HANDOFF, RESOLVER AS DECISÕES ABAIXO (ESPECIALMENTE A/E, QUE DEPENDEM DE DADO QUE NÃO EXISTE HOJE) E CONFIRMAR.

## DECISÕES PARA O GATE HUMANO

### (A) Fórmula exata do Candidato A dado o que o dado permite HOJE
- **Recomendação:** rodar o Candidato A com a semântica corrigida (SOMAR a oferta menor à `oferta_consumida_total_estimada`) usando o **dedup por hex-overlap** como aproximação — porque `oferta_academias_menores_h3` NÃO tem rótulo de rede (pré-req 1). Fórmula em 1.3: soma `alunos_academias_menores` só em hex SEM rede mapeada; hex com rede mapeada assume-se já contado. Limitação declarada no relatório.
- **Alternativa (melhor, mas bloqueada):** dedup FINO por rede exige `rede_menor` → depende da Decisão (E). Recomendo rodar a aproximação já neste bloco e, se A parecer promissor, refazer no TP-09 com o dado fino.

### (C) Fonte das médias por rede (Candidato C) + fallback
- **Fonte real disponível:** `base_calibracao_multirede.parquet` → **skyfit 2.295** e **engenharia_do_corpo 3.283** (médias reais medidas). **Smart Fit, Blue Fit, Panobianco NÃO estão no repo** (o "2,2k/1,9k" do gate é estimativa externa sem fonte local).
- **Recomendação:** `capacidade_por_rede = {"skyfit":2295, "engenharia_do_corpo":3283}`; TODAS as outras 26 redes (incl. Smart/Pano, as mais numerosas) → fallback **2.500** (neutro vs produção). Caveat central no relatório: o C só re-pondera 2 de 28 redes → efeito estrutural pequeno e enviesado; provável inconclusivo. O gate pode INJETAR médias externas de Smart/Pano se aceitar a proveniência (o módulo recebe `capacidade_por_rede` como parâmetro).

### (E) A re-ingestão com rótulo de rede entra NESTE bloco ou vira BLK-TP-08-FU?
- **Recomendação: vira BLK-TP-08-FU (dependência), NÃO entra neste FU1.** Motivo: re-ingerir o `03_Competidores.xlsx` derivando `rede_menor` na fronteira (classificar por padrão de nome, descartar o nome cru) é um pipeline anti-PII próprio, com contrato/teste/validação — misturá-lo aqui incha o bloco e mistura ingestão com modelagem. Este FU1 roda A na aproximação hex-overlap HOJE; se o gate quiser o dedup fino, aprova o TP-08-FU e o TP-09 (aplicação) o consome. Alternativa (se o gate priorizar o dedup fino já): expandir este bloco para incluir a re-ingestão — porém sai da esteira "um bloco por vez" e vira Crítico de dado.

### (F) Critério de "vence" + recorte metropolitano (confirmar)
- **Recomendação (confirmar):** candidato VENCE ⇔ IC95 do `Δ = R2_oof(cand) − R2_oof(baseline)` por bootstrap PAREADO (mesmos folds/seed=42) não cruza zero (inferior > 0). Reportar Δ em 3 recortes (completo n=16.411; SP/MG/RJ; fora n≈8.044); só recomenda ao TP-09 se o ganho SOBREVIVE fora de SP/MG/RJ; se só no metropolitano, veredito = "não generaliza, limitado ao ~1% metropolitano" e recomendação = NÃO aplicar (ou aplicar só com aceite explícito da limitação). Caveat de cobertura ~1,06% do universo sempre reportado (DEC-012).

## Guardrails ativos
- **§5 (READ-ONLY sobre o M1):** zero recálculo/alteração de `score_priorizacao`, `hex_score_estrutural`, pesos (renda=0,40/pop=0,60), carteira, plano, artefatos oficiais; NÃO alterar a fórmula de `score_oportunidade_residual` em produção nem regenerar `hexagonos_mercado_mapeado.parquet`/derivados. Candidatos só EM MEMÓRIA / relatório.
- **DEC-008:** validação out-of-fold vs baseline da média; R² in-sample BANIDO do veredito; IC95 bootstrap seed-fixa; comparação de candidatos out-of-fold; NO-GO é resultado VÁLIDO.
- **DEC-009:** `membros` é ALVO OBSERVADO; PROIBIDO usar `membros`/qualquer coluna da demanda como preditor geográfico de magnitude ou ajuste do score.
- **DEC-012 (anti-PII / isolamento):** pacote `demanda_revelada/` DISJUNTO — nunca importa de `pipelines/m1/`, `censo_*`, `dashboard/`, `api`, `config.py` raiz; zero coluna de `COLUNAS_PII_PROIBIDAS`; fonte real (`NAO_ABRA/`) nunca tocada; testes só com fixture sintética.
- **DEC-013:** oferta das academias menores entra só na camada de mercado/residual (candidata), COM DEDUP (das redes já mapeadas); READ-ONLY sobre o M1 e censitário.
- **Não expandir escopo; um bloco por vez; não implementar (Planner).**
