# Handoff — Builder

## Skill que gerou este handoff
Builder

## Próxima Skill recomendada
QA/Quality Analyzer

## Bloco executado
BLK-OPS-03 — Manifesto de proveniência nos outputs

## O que foi feito
Implementação ADITIVA e NÃO-MUTANTE do manifesto de proveniência, exatamente conforme
o plano (seções A/B/C/D). Nenhum byte de artefato M1 foi tocado; o manifesto fica AO LADO,
em `data/outputs/_manifest.json`.

### A) Novo módulo `src/motor_expansao/pipelines/m1/provenance.py` (NOVO)
- `from __future__ import annotations`; imports stdlib `hashlib`, `json`, `re`, `subprocess`,
  `from datetime import UTC, datetime`, `from pathlib import Path`, `from typing import Any`.
- Fontes canônicas confirmadas lendo o repo real: `from motor_expansao.config import settings`
  e `from motor_expansao.core.constants import PESOS_HEX_SCORE_ESTRUTURAL` (nomes confirmados;
  `settings.H3_RESOLUTION`/`DIST_MIN_ULTRA_KM`/`RENDA_MIN` e as chaves
  `PESOS_HEX_SCORE_ESTRUTURAL["renda_per_capita"]`=0.40 / `["populacao_proxy"]`=0.60).
- Constantes: `MANIFEST_PATH = Path("data/outputs/_manifest.json")`,
  `ULTRA_CSV_PATH = Path("data/ultra/Ultra.csv")`, `IBGE_VINTAGE = "censo_2022"`,
  `SCHEMA_VERSION = 1`.
- `_sha256_file(path)`: lê BYTES BRUTOS em modo binário (chunks de 1 MiB); `None` se não existe.
- `_git_commit()`: `git rev-parse HEAD` via `subprocess.run(..., cwd=str(_REPO_ROOT))`, dentro de
  `try/except (OSError, subprocess.SubprocessError)`; retorna `None` se `returncode != 0` ou se a
  saída não casa `^[0-9a-f]{7,40}$`. Nunca derruba o pipeline.
- `build_manifest(*, ultra_csv_path=..., ibge_vintage=...) -> dict[str, Any]`: função pura com as
  9 chaves do plano. Pesos lidos da constante (NUNCA hardcoded).
- `write_manifest(path=MANIFEST_PATH, *, ...) -> Path`: `mkdir(parents=True, exist_ok=True)` +
  `write_text(json.dumps(..., ensure_ascii=False, indent=2) + "\n", encoding="utf-8")` (UTF-8 puro, sem BOM).
- DECISÃO cwd do git: confirmada a profundidade `parents[4]`
  (`m1`/`pipelines`/`motor_expansao`/`src`/raiz). Constante de módulo `_REPO_ROOT`.
- DESVIO MENOR vs. texto literal do plano: o plano grafava `datetime.now(timezone.utc)`; ruff
  (regra UP017) exige `datetime.UTC`. Troquei para `from datetime import UTC, datetime` +
  `datetime.now(UTC)`. Funcionalmente idêntico (offset UTC == 0); Python do ambiente é 3.14
  (UTC alias disponível desde 3.11). mypy/ruff limpos após a troca.

### B) `fase1_bi_exports.py`
- Import no topo (após `ibge_censo`): `from motor_expansao.pipelines.m1.provenance import write_manifest`.
- Último passo de `main()`, após o bloco HYBRID/enriquecido: `manifest_path = write_manifest()` +
  `print(...)`. Fora de `generate_fase1_bi_artifacts`/`write_outputs`/funções de score.

### C) Render read-only no dashboard
- `components.py`: adicionados `import json` e `from pathlib import Path` (não existiam); novo
  `render_manifest_footer(manifest_path: Path) -> None` (expander read-only; `return` se o JSON não
  existe; `try/except (OSError, ValueError)`).
- `streamlit_app.py`: constante `MANIFEST_PATH = Path(__file__).resolve().parent / "data" / "outputs" / "_manifest.json"`
  junto às demais; `render_manifest_footer` adicionado ao bloco de import de `dashboard.components`;
  chamada `render_manifest_footer(MANIFEST_PATH)` como último statement de `main()`, FORA dos branches de aba.

### D) Teste `tests/unit/test_manifest.py` (NOVO)
- 6 casos do plano: `test_build_manifest_has_all_required_keys`, `test_canonical_values`,
  `test_sha256_present_with_file` (bytes não-UTF8 0xff, compara com `hashlib`),
  `test_sha256_none_when_file_absent`, `test_volatile_fields_present_and_typed`
  (`datetime.fromisoformat` + offset UTC; commit None ou `^[0-9a-f]{7,40}$`),
  `test_write_manifest_creates_utf8_json` (relê via `json.loads`, confere ausência de BOM).
- Idiom casado a `tests/unit/`: `from __future__ import annotations`, pytest, `tmp_path`, `re`.

## Arquivos alterados
- `src/motor_expansao/pipelines/m1/provenance.py` — NOVO. Módulo isolado do manifesto.
- `src/motor_expansao/pipelines/m1/fase1_bi_exports.py` — MODIFICADO. 1 import + passo final em `main()`.
- `src/motor_expansao/dashboard/components.py` — MODIFICADO. `import json`/`Path` + `render_manifest_footer`.
- `streamlit_app.py` — MODIFICADO. Constante `MANIFEST_PATH`, import do helper, chamada no fim de `main()`.
- `tests/unit/test_manifest.py` — NOVO. 6 testes de presença/schema/valores/sha/voláteis/UTF-8.
- `tasks/current_task.md` — MODIFICADO (processo).
- `context/handoff.md` + `context/handoff/20260530-132436-builder.md` — handoff.

## Validações executadas
- pytest tests/unit/test_manifest.py: 6 passed
- pytest tests/integration/test_streamlit_app.py: 147 passed
- import streamlit_app: ok
- pytest -q (suíte completa): 547 passed, 1 skipped, 9 warnings (subiu vs. 532; +1 skipped preservado; nada quebrou)
- mypy src/motor_expansao/pipelines/m1/provenance.py: Success, no issues
- ruff (provenance.py, test_manifest.py, streamlit_app.py, components.py): All checks passed

## Prova de não-mutação (nota para o QA)
O código apenas LÊ parâmetros canônicos + bytes do `Ultra.csv` e ESCREVE `data/outputs/_manifest.json`.
Nenhuma chamada `to_parquet`/`to_csv` de artefato M1 foi adicionada. QA deve hashear pré/pós (sha256):
- data/outputs/hexagonos_brasil_dashboard.parquet
- data/outputs/hexagonos_mapa_sample.parquet
- data/staging/brasil_estrutural.parquet
- data/staging/brasil_priorizados.parquet
- data/staging/hexagonos_brasil_oportunidades.parquet
Esperado: hashes IDÊNTICOS pré/pós; qualquer divergência → REPROVAR. (Builder NÃO rodou
`fase1_bi_exports.main()`; smoke test de `write_manifest` foi feito apenas em pasta temporária.)

## Problemas encontrados
- Ruff UP017 em `timezone.utc`: resolvido trocando para `datetime.UTC` (desvio menor, documentado acima).

## Pendências
- nenhuma

## Riscos remanescentes
- nenhum (módulo stdlib-only, isolado; manifesto idempotente e read-only no dashboard).

## Guardrails verificados
- score_priorizacao não alterado: sim
- Artefatos M1 preservados (código só adiciona _manifest.json): sim
- Dashboard offline mantido: sim (sem dependência de API ao vivo)
- JSON UTF-8 sem BOM: sim (testado)
- Pesos lidos de PESOS_HEX_SCORE_ESTRUTURAL (não hardcoded): sim
