# Handoff — QA/Quality Analyzer

## Skill que gerou este handoff
QA/Quality Analyzer

## Próxima Skill recomendada
Block Orchestrator (fechamento) — merge de `ciclo/BLK-MAP-02` -> `integracao/map02-relmun05-06`

## VEREDITO
APROVADO

## Justificativa
Mudança trivial de UI (envolver o `st.multiselect("Redes de concorrentes", ...)` em
`st.expander(..., expanded=False)`), 100% em `dashboard/pages.py`, camada de visualização
READ-ONLY sobre o M1; todos os identificadores, `key`, `options`/`default`, `format_func` e a
lógica BLK-MAP-01 preservados byte-a-byte; todas as validações re-executadas pelo QA passam
(ruff 0 erros, mypy só os 7 pré-existentes de types-requests, import ok) e a suíte FULL
(resultado autoritativo do orquestrador, serial) fecha com 1535 passed e a ÚNICA falha sendo a
pré-existente/ambiental do M2 (`test_score_retencao_territorial::test_run_readonly_m1_por_mtime`,
parquet gitignored ausente), alheia a este bloco.

## Problemas críticos (bloqueadores)
- nenhum

## Problemas médios (não bloqueadores)
- nenhum

## Melhorias opcionais
- nenhuma. (O label do expander e do multiselect são ambos "Redes de concorrentes"; duplicação
  cosmética inofensiva — o multiselect aninhado herda o título do expander. Sem impacto funcional.)

## Testes faltantes
- nenhum obrigatório. Nenhum assert existente travava label/posição do widget
  (`mapa_territorial_redes_concorrentes`/"Redes de concorrentes" não aparecem em asserts do
  `test_streamlit_app.py`), então o teste não precisou ser alterado; `st.expander` é widget
  nativo do Streamlit, sem lógica nova a cobrir.

## Riscos remanescentes
- nenhum. `selected_redes` é inicializado como `[]` antes do `if _show_rede_filter:`; quando o
  filtro aparece, o corpo do `with st.expander` sempre roda e reatribui `selected_redes`, e a
  lógica BLK-MAP-01 (seleção vazia => `competitors_df_filtered = None` => esconde concorrentes)
  segue fora do `with`, no nível de indentação de antes.

## Saída literal das validações (re-executadas pelo QA)

### 1. Suíte FULL (`python -m pytest -q`, serial) — resultado AUTORITATIVO do orquestrador
Nota: a sessão do QA deixou a suíte rodando em background sem gravar snapshot; o orquestrador
limpou o processo órfão e rodou a suíte completa serial ELE MESMO até o fim. Este é o resultado
autoritativo (o QA NÃO relançou a suíte completa para evitar novo órfão).
```
python -m pytest -q  (serial)
...
FAILED tests/unit/test_score_retencao_territorial.py::test_run_readonly_m1_por_mtime
1 failed, 1535 passed, 2 skipped, 241313 warnings in 1311.10s
```
A ÚNICA falha (`test_run_readonly_m1_por_mtime`) é PRÉ-EXISTENTE/ambiental: FileNotFoundError do
parquet M2 gitignored (`unidade_territorio_retencao.parquet`), sem relação com este bloco de UI.
Zero falhas novas introduzidas por BLK-MAP-02.

### 2. ruff (`python -m ruff check .`)
```
All checks passed!
```

### 3. mypy (`python -m mypy src`)
```
src\motor_expansao\dimensionamento\growth_api_client.py:28: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\ibge_censo.py:24: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\poi_enrichment.py:15: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\pipelines\m1\base_h3_brasil.py:31: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\geo.py:21: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\telegram_bot.py:23: error: Library stubs not installed for "requests"  [import-untyped]
src\motor_expansao\api\service.py:223: error: Library stubs not installed for "requests"  [import-untyped]
Found 7 errors in 7 files (checked 108 source files)
```
Os 7 erros são PRÉ-EXISTENTES, todos da mesma causa raiz (`types-requests` não instalado) em
arquivos NÃO tocados por este bloco. ZERO erro em `dashboard/pages.py`. Não é regressão.

### 4. import (`python -c "import streamlit_app; print('import ok')"`)
```
import ok
```

### 5. git diff --stat
```
 context/handoff.md                    | 143 ++++++----------------------------
 src/motor_expansao/dashboard/pages.py |  15 ++--
 tasks/current_task.md                 |  53 +++++++------
 3 files changed, 63 insertions(+), 148 deletions(-)
```
Único arquivo de CÓDIGO alterado: `src/motor_expansao/dashboard/pages.py` (+8/-7). Os outros dois
são bookkeeping (handoff + current_task). ZERO mudança em pipelines/m1, config.py, censo_*,
artefatos oficiais ou qualquer parquet. `git diff --numstat -- src/ config.py` retorna só
`pages.py`.

### 6. housekeeping (`python scripts/housekeeping_move_block.py BLK-MAP-02 --check`)
```
FALHA --check: stub ausente no backlog para BLK-MAP-02
EXIT=1
```
Estado PRÉ-MOVE correto: a falha "stub ausente" RECONHECE o bloco (NÃO é BlockNotFound). O move é
passo do orquestrador no fechamento, não do QA.

### 7. Reconferência do diff de código (`git diff src/motor_expansao/dashboard/pages.py`)
```
-        selected_redes = st.multiselect(
-            "Redes de concorrentes",
-            options=_all_redes,
-            default=_all_redes,
-            format_func=lambda r: COMPETITOR_BRANDS.get(r, {}).get("label", r),
-            key="mapa_territorial_redes_concorrentes",
-        )
+        with st.expander("Redes de concorrentes", expanded=False):
+            selected_redes = st.multiselect(
+                "Redes de concorrentes",
+                options=_all_redes,
+                default=_all_redes,
+                format_func=lambda r: COMPETITOR_BRANDS.get(r, {}).get("label", r),
+                key="mapa_territorial_redes_concorrentes",
+            )
```
Confirmado por leitura direta de `pages.py` (linhas 4373–4397): o `with st.expander(...)` está
DENTRO do `if _show_rede_filter:` (indent 8); o multiselect foi reindentado um nível (12) sem
alterar mais nada. `key="mapa_territorial_redes_concorrentes"`, `options=_all_redes`,
`default=_all_redes` e `format_func` idênticos. A lógica BLK-MAP-01 (linhas 4391–4397:
`if _show_rede_filter and not selected_redes: competitors_df_filtered = None` / `elif` / `else`)
permanece FORA do `with`, no nível de função (indent 4), consumindo `selected_redes` e
`competitors_df_filtered` a jusante. Nenhum identificador acentuado/renomeado; `COMPETITOR_BRANDS`,
legenda (`_render_unified_legend`) e cluster intocados.

## Conferência de no-bypass
Confirmo que nenhuma validação usou `--config /dev/null`, fixture fora do caminho real ou mock do
caminho crítico. A suíte roda com fixtures legítimas de teste (isolamento de lógica, prática
normal). N/A a regra de tooling-vs-produção deste bloco (mudança pura de UI, sem tooling de
segredos/artefatos de produção).

## Conferência cruzada com log do Builder
Bate. Builder reportou ruff "All checks passed!", mypy 7 pré-existentes (0 em pages.py), import ok,
e subconjunto `test_streamlit_app.py` 232 passed. QA reproduziu ruff/mypy/import idênticos; a suíte
FULL (autoritativa do orquestrador) fecha 1535 passed com a única falha pré-existente do M2 —
consistente com a natureza trivial da mudança.

## Guardrails verificados
- score_priorizacao não alterado: sim (nenhum arquivo de score/pipeline/config/artefato tocado;
  só `dashboard/pages.py`).
- Artefatos M1 preservados: sim (mudança 100% na camada de visualização; nenhum parquet escrito;
  §5 READ-ONLY M1 respeitado).
- Parâmetros canônicos preservados: sim (H3_RESOLUTION=7, DIST_MIN_ULTRA_KM=1.0, RENDA_MIN=4500.0,
  pesos renda=0.40/pop=0.60 — nenhum tocado; config.py não alterado).
- Identificadores não acentuados/renomeados: sim (`key="mapa_territorial_redes_concorrentes"`,
  `_all_redes`, `selected_redes`, `_show_rede_filter`, `competitors_df_filtered` inalterados).
- Validações re-executadas pelo QA (sem bypass): pytest FULL (orquestrador) → 1535 passed / 1
  pré-existente M2; ruff → All checks passed; mypy → só 7 pré-existentes; import → ok;
  housekeeping --check → FALHA "stub ausente" (pré-move correto).
- Escopo respeitado: sim (só `pages.py` de código + bookkeeping handoff/current_task).

## Decisão recomendada
Fechar ciclo (housekeeping move + commit por path + merge para `integracao/map02-relmun05-06` são
do orquestrador).
